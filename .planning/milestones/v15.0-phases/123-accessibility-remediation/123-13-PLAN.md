---
phase: 123-accessibility-remediation
plan: 13
type: execute
wave: 5
depends_on: ["123-01", "123-02", "123-03", "123-04", "123-05", "123-06", "123-07", "123-08", "123-09", "123-10", "123-11", "123-12"]
files_modified:
  - .planning/phases/123-accessibility-remediation/123-CLOSEOUT.md
  - .planning/REQUIREMENTS.md
autonomous: false
requirements: [A11Y-02, A11Y-03]

must_haves:
  truths:
    - "D-10: all four themes clear the same AA bar in the final run, with `readable` held to AA and no second threshold, second ledger or second pass/fail anywhere"
    - "D-12: the guard is proven twice — the durable in-suite self-test passes on every run, and the operator supplies the live gated-server evidence an agent is structurally blocked from producing"
    - "D-18: a blocking operator visual checkpoint confirms the nav's visual hierarchy survived the /NN deletions and that /forge's header spacing did not regress from PageHeader's baked-in mb-4"
    - "C6: the final before/after contrast claim is paired with the pre-change class strings measured from git in the same page, rasterised the same way"
    - "C7: the final JSON report contains no test that is `\"status\": \"failed\"` while its annotations include `type: \"skip\"`"
  artifacts:
    - path: ".planning/phases/123-accessibility-remediation/123-CLOSEOUT.md"
      provides: "the final gate run, the four discriminating-control results, the operator's two verdicts, and the requirement-closure evidence"
  key_links:
    - from: ".planning/REQUIREMENTS.md"
      to: ".planning/phases/123-accessibility-remediation/123-CLOSEOUT.md"
      via: "A11Y-02 and A11Y-03 marked from measured evidence, by hand"
      pattern: "A11Y-0"
---

<objective>
Close the phase on measured evidence, and only on measured evidence.

A11Y-02 is `expect(results.violations).toEqual([])` across every measured cell — an empty array, not
an empty contrast bucket. A11Y-03 is that the suite cannot report green against a page it never
rendered, which four separate mechanisms now enforce. This plan runs the real gate (assertions ON,
not `A11Y_MEASURE_ONLY`), records every discriminating control, obtains the two things an agent
structurally cannot produce, and marks the requirements by hand.

Purpose: D-10, D-12's evidence half, D-18, and controls C6 and C7.
Output: `123-CLOSEOUT.md` plus hand-edited requirement marks.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/123-accessibility-remediation/123-CONTEXT.md
@.planning/phases/123-accessibility-remediation/123-VALIDATION.md
@.planning/phases/123-accessibility-remediation/123-CONTRAST-RESULT.md
@.planning/phases/123-accessibility-remediation/123-CRITERION-DECISION.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md

<interfaces>
<!-- The real gate command. Note: assertions ON — A11Y_MEASURE_ONLY must be UNSET, because a
     measure-only run is now deliberately non-green (D-14) and proves nothing about violations. -->
  VITE_CLERK_PUBLISHABLE_KEY= npm run dev:noauth          # separate terminal, Git Bash
  PW_BASE_URL=http://localhost:5181 \
    PLAYWRIGHT_JSON_OUTPUT_NAME=.planning/phases/123-accessibility-remediation/123-final-report.json \
    node_modules/.bin/playwright test e2e/theme-contrast.spec.ts --reporter=json

<!-- PowerShell's `VAR=` assignment DELETES the variable rather than setting it empty, so the
     dev:noauth server must be started from Git Bash or the Clerk gate stays up and every cell
     hits the skip branch. That would now correctly fail the run (D-11) — for the wrong reason. -->

<!-- `node_modules/.bin/playwright`, never bare `npx playwright`: a global Playwright of a different
     version is installed on this machine and cannot resolve repo-local modules, so its result is
     not a result about the version that ships. -->

<!-- Pre-123 control, re-derived by parsing violations[].nodes.length across the 20 files in
     .planning/phases/122-.../a11y-after/ :  24 objects / 209 nodes
       20 objects color-contrast, 4 objects aria-prohibited-attr.
     Never re-derive this by grepping `"id"` — `"id"` also appears on every per-node check object
     and returns 225 against a 209-node corpus. -->

<!-- The 4 discriminating controls this plan owns or closes, from 123-VALIDATION.md:
     C1/C2 — implemented and proven in plan 123-01's self-test; re-confirm it still passes here.
     C6    — before/after measured from git-extracted pre-change class strings, plan 123-02.
     C7    — no test `"status": "failed"` while annotated `type: "skip"`, asserted on THIS run's
             JSON report. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the real gate and record every discriminating control</name>
  <files>.planning/phases/123-accessibility-remediation/123-CLOSEOUT.md</files>
  <read_first>
    - .planning/phases/123-accessibility-remediation/123-VALIDATION.md § "Discriminating Controls" (all seven rows — every one must be recorded as measured, with its result)
    - .planning/phases/123-accessibility-remediation/123-CRITERION-DECISION.md (whether the criterion is 20 cells or 188 — the gate run must cover whichever was chosen)
    - .planning/phases/123-accessibility-remediation/123-CONTRAST-RESULT.md (the pre-sweep figures the final delta is stated against)
    - e2e/theme-contrast.spec.ts and e2e/a11y-gate-guard.spec.ts (what each run actually asserts)
  </read_first>
  <action>
    Run the full gate with assertions ON (`A11Y_MEASURE_ONLY` UNSET) against `dev:noauth` on 5181,
    writing the JSON report to
    `.planning/phases/123-accessibility-remediation/123-final-report.json`. If the operator chose
    `widen` at plan 123-09, run with `A11Y_SCAN_ALL=1`; otherwise run the default 20-cell matrix.

    Also run, and record the result of, every other gate this phase built:
      `node_modules/.bin/playwright test e2e/a11y-gate-guard.spec.ts`   (D-11/D-12 durable, C1, C2)
      `node_modules/.bin/playwright test e2e/contrast-isolation.spec.ts` (D-02 pass 2, C3, C6)
      `node_modules/.bin/playwright test e2e/theme-rendered-result.spec.ts` (122's spec — must still be 47/47)
      `npm test`                                                        (unit suite)
      `npx tsc --noEmit`
      `npm run build`

    Write `123-CLOSEOUT.md` with:

    **1. The gate result.** Exit code, `stats` block verbatim, and per-rule violation object and
    node counts with units. Zero violations is the criterion; state it as `violations.toEqual([])`
    satisfied, not as "contrast is fixed".

    **2. All seven discriminating controls (C1-C7), each with its measured result** and the run that
    produced it. C1 and C2 come from the self-test; C3 from the isolation spec; C4 from the sweep
    ledgers; C5 from the scan-count assertion; C6 from the git-anchored before/after; C7 asserted on
    THIS run's report. A control listed as "pending" at close is a gate with nothing behind it, and
    that is the defect this phase exists to remove — none may remain pending.

    **3. C7 explicitly.** Parse `123-final-report.json` and count tests where
    `results[0].status === "failed"` AND `annotations.some(a => a.type === "skip")`. The count must
    be **0**. Pair it with the known-positive from plan 123-01's C2 fixture (where the same
    predicate is non-zero), so the predicate is shown to fire in both directions rather than being
    trivially true against a report with no skips in it at all.

    **4. D-10.** A per-theme table showing all four themes at the same AA bar, `readable` included,
    with no separate threshold and no separate pass/fail. State the pre-123 `readable` node count
    (78 of 209) and its final figure in the same unit.

    **5. Delta.** Final versus the pre-123 control (24 objects / 209 nodes), by rule, with units,
    and versus `123-CONTRAST-RESULT.md`'s post-scan figures.

    **6. Residual gaps, named.** Occurrences classified `not-reached` in the ledgers and guarded only
    by D-02's isolation table; routes carrying `marker: null`; `/chat`, excluded as out of milestone
    scope; and D-08's shadow boundary. Quantify each rather than gesturing at it.
  </action>
  <verify>
    <automated>test -f .planning/phases/123-accessibility-remediation/123-final-report.json &amp;&amp; node -e "const r=require('./.planning/phases/123-accessibility-remediation/123-final-report.json');console.log(JSON.stringify(r.stats))"</automated>
  </verify>
  <acceptance_criteria>
    - The gate run's aggregated `wcag2a`/`wcag2aa` violation count across the criterion cells is
      **0**, for **all four themes**.
    - Capture/cell-count sanity BEFORE any zero is recorded: `stats.expected` matches the expected
      cell count for the chosen criterion (21 for the 20-cell matrix plus the C5 count test, 189 for
      the widened one). A run that enumerated nothing also reports 0 violations.
    - `stats.skipped` is **0** on the gate run. A non-zero value means cells were gated and, per
      D-11, the run also exited non-zero — record both rather than reporting a partial pass.
    - C7's count is **0** on this report, and the paired known-positive from the C2 fixture is
      non-zero. Both figures recorded.
    - `node_modules/.bin/playwright test e2e/a11y-gate-guard.spec.ts` passes 5/5.
    - `node_modules/.bin/playwright test e2e/theme-rendered-result.spec.ts` still passes **47/47** —
      Phase 122's frozen figure, unchanged by this phase's harness refactor.
    - `npx tsc --noEmit` exits 0; `npm run build` exits 0; `npm test` has no new failures against a
      stashed-source control run.
    - Every one of C1-C7 is recorded with a measured result. **None may read "pending".**
  </acceptance_criteria>
  <done>The real gate is green on measured evidence, every discriminating control has a result, and the residual gaps are quantified rather than implied.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Operator visual checkpoint and live gated-server evidence</name>
  <files>.planning/phases/123-accessibility-remediation/123-CLOSEOUT.md</files>
  <read_first>
    - .planning/phases/123-accessibility-remediation/123-CLOSEOUT.md (Task 1's results, so the operator sees what the automation already established)
    - .planning/phases/123-accessibility-remediation/123-CONTEXT.md § D-18 and § D-12
  </read_first>
  <what-built>
    Nine files of app-shell and component styling changed: every `text-*/NN` opacity modifier that
    measured below WCAG-AA has been deleted, `/forge` now renders its header through the shared
    `PageHeader` component, and the four status-fill badge pairings were re-tokenised. Separately,
    the contrast suite gained four guards that make a vacuous green impossible: it fails on any
    skipped cell, waits on per-page content rather than the app shell, ends non-zero in
    measurement-only mode, and carries a self-test that proves the Clerk-gate guard still fires.

    Two things automation cannot settle. Whether de-emphasis still reads as de-emphasis after the
    opacity modifiers were removed is a perceptual call no ratio threshold captures — a pairing can
    clear 4.5:1 and still flatten a visual hierarchy. And the live gated-server run is structurally
    blocked for an agent in this session, exactly as Phase 122 recorded for the same probe.
  </what-built>
  <how-to-verify>
    **Part A — visual (D-18).**
    1. Run `npm run dev` and open `http://localhost:5173`.
    2. Using the theme switcher, visit each of the four themes in turn: **cyan**, **emerald**,
       **readable**, **aubergine**. For each theme, answer these two questions:
       a. On the left navigation: can you still tell, at a glance and without hovering, which item
          is the ACTIVE one, which items are ordinary inactive items, and which are disabled
          ("soon") — and do the small uppercase GROUP HEADERS still read as headers rather than as
          another row of nav items? Say what you see, not whether it looks acceptable.
       b. Navigate to `/forge`. Is the vertical gap between the "Forge" title and the panel below it
          the same as it was, or larger? This one is specifically asked because `PageHeader` bakes in
          a `mb-4` that the old hand-rolled header did not have; the conversion passes `mb-0` to
          cancel it, and this is the check that the cancellation worked. Compare against
          `/live-run` and `/analytics`, which use the same component.
    3. While you are on `/forge` in each theme, glance at the job-list column and the severity
       badges: is any label hard to read against its fill?

    **Part B — live gated-server evidence (D-12).**
    4. Ensure `VITE_CLERK_PUBLISHABLE_KEY` IS set in `.env.local` so the Clerk gate is up, and start
       the ordinary gated dev server on 5173 (`npm run dev`). Do not sign in.
    5. From a second terminal, run:
       `PW_BASE_URL=http://localhost:5173 node_modules/.bin/playwright test e2e/theme-contrast.spec.ts --reporter=list`
    6. Report three things: the **exit code**, the number of cells reported **skipped**, and whether
       any cell is reported **failed**. Expected per D-11: exit code non-zero, cells still reading
       `skipped`, none reading `failed`. If what you see differs from that, say what you actually
       saw — the difference is the finding.
  </how-to-verify>
  <action>
    Present the steps above and wait. Record the operator's answers verbatim in `123-CLOSEOUT.md`
    under `## Operator verification`, with the date and their own words for each of Part A's three
    questions and Part B's three figures. Do not paraphrase into a verdict.

    If Part A reports a flattened hierarchy or a doubled `/forge` gap, that is a real defect: file it
    and fix it before close, do not record it as an accepted cosmetic note. Phase 122's equivalent
    checkpoint caught "one flat tone for the most part", which four automated gates had missed, and
    it took three gap-closure plans to resolve.

    If Part B's figures do not match D-11's expected shape, the durable self-test models something
    the real gate does not — record the discrepancy and treat the self-test, not the live run, as the
    thing that needs correcting.
  </action>
  <verify>
    <human-check>Operator has answered all six questions and each answer is recorded verbatim with a date.</human-check>
  </verify>
  <acceptance_criteria>
    - `123-CLOSEOUT.md` contains an `## Operator verification` section with the operator's verbatim
      words for Part A questions (a), (b) and (3), across all four themes.
    - It records Part B's three figures: exit code, skipped-cell count, failed-cell count.
    - If any answer reports a defect, the section names the follow-up plan or commit that closed it —
      the phase does not close on an unresolved checkpoint finding.
    - The operator was NOT told the expected answer to Part A in advance; only Part B carries an
      expectation, and that is deliberate because it is a mechanical check with a defined shape.
  </acceptance_criteria>
  <resume-signal>Answer each numbered question in your own words, then type `approved` or describe what you saw that looked wrong.</resume-signal>
  <done>Both things automation cannot produce are on record: a human's account of what the four themes actually look like, and a real gated-server run's exit code and skip counts.</done>
</task>

<task type="auto">
  <name>Task 3: Mark A11Y-02 and A11Y-03 by hand from the recorded evidence</name>
  <files>.planning/REQUIREMENTS.md, .planning/phases/123-accessibility-remediation/123-CLOSEOUT.md</files>
  <read_first>
    - .planning/phases/123-accessibility-remediation/123-CLOSEOUT.md (Task 1's gate results and Task 2's operator answers — the evidence the marks rest on)
    - .planning/REQUIREMENTS.md (:67 A11Y-02, :68 A11Y-03, and :66 A11Y-01's closure note for the established evidence-trail style)
    - .planning/ROADMAP.md § "Phase 123" (the two success criteria, both corrected at plan time)
  </read_first>
  <action>
    Mark A11Y-02 and A11Y-03 in `.planning/REQUIREMENTS.md` BY HAND with asserted single-occurrence
    string replaces. Do NOT run `gsd-sdk phase.complete`: it flips every requirement mapped to the
    phase to `Complete` including ones deliberately closed as PARTIAL, wipes the `stopped_at`
    narrative, and ticks the ROADMAP checkbox while leaving the Progress row contradicting it. This
    project has already been bitten by it on Phase 120's POLISH-04.

    Each mark carries its own evidence trail in the style of A11Y-01's closure note at `:66`: the
    commands run, the measured figures with their units, the discriminating controls and their
    results, and the operator's verification date. If any part of either requirement is not fully
    satisfied — a residual `not-reached` population, a route with `marker: null`, `/chat` excluded —
    mark it **Partial** with `[~]` and name what is open. A partial mark that says what is open is
    worth more than a green one that is wrong, and a false green in the requirements table is the
    exact defect class this phase exists to remove.

    Then update `.planning/ROADMAP.md`'s Phase 123 Plans line and plan list to reflect the executed
    set, and its Progress row consistently with the requirement marks — the file must not contradict
    itself. Do not run `roadmap.annotate-dependencies`.

    Finally, run `powershell -File C:\Users\mandr\scripts\gsd-state-coherence.ps1 -Path .` and record
    its verdict. If STATE.md is hand-edited in this task, edit BOTH the frontmatter and the body
    copies of every duplicated field (`stopped_at` / `Stopped at:`, `last_activity` /
    `Last activity:`, `last_updated` / `Last session:`) — the frontmatter is re-derived from the body
    by the next state verb, so a half-edit silently reverts.
  </action>
  <verify>
    <automated>git diff --stat .planning/REQUIREMENTS.md .planning/ROADMAP.md</automated>
  </verify>
  <acceptance_criteria>
    - `git diff .planning/REQUIREMENTS.md` shows changes to exactly the A11Y-02 and A11Y-03 lines
      and nothing else. Read every deletion in the diff and account for it — a concurrent session
      shares this checkout.
    - Each mark is `[x]` with a full evidence trail, or `[~]` Partial with the open item named.
      No mark is `[x]` without a figure and a command behind it.
    - `grep -c "A11Y-02" .planning/REQUIREMENTS.md` is unchanged from before the edit (the line was
      edited, not duplicated).
    - `.planning/ROADMAP.md`'s Phase 123 Progress row and its requirement checkboxes agree with each
      other; the file does not contradict itself.
    - `gsd-state-coherence.ps1` reports no refusal. If it refuses, fix the disagreement it names
      before committing — do not commit over it.
    - `git log -1 --format=%H` is checked before any `--amend`, and no `--amend` is used in this
      shared checkout; prefer a fresh commit.
  </acceptance_criteria>
  <done>Both requirements carry marks backed by measured evidence and an operator date, the roadmap agrees with itself, and the coherence guard passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Clerk gate → contrast suite | Part B of the checkpoint deliberately raises the real auth gate and observes the suite's behaviour against it. This is the only point in the phase where a genuinely gated server is exercised. |
| operator → requirements table | A human verdict is converted into a durable `Complete` / `Partial` mark that later phases treat as ground truth. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-123-36 | Spoofing | "zero violations" | mitigate | `stats.expected` is asserted to match the expected cell count BEFORE any zero is accepted, and `stats.skipped` must be 0 — a run that enumerated nothing, and a run whose cells were all gated, both report zero violations otherwise. |
| T-123-37 | Spoofing | C7's zero | mitigate | The predicate is paired with a known-positive from plan 123-01's C2 fixture, so a zero proves the signature is absent rather than proving the predicate never fires. |
| T-123-38 | Repudiation | requirement marks | mitigate | Marks are hand-written with commands, figures, units and the operator's date. `gsd-sdk phase.complete` is explicitly forbidden here because it writes a false green over PARTIAL requirements without saying so in its payload. |
| T-123-39 | Tampering | shared-checkout commits | mitigate | `git diff` is read line by line before committing and every deletion accounted for; no `--amend`; the coherence guard must pass rather than be committed over. A concurrent session shares this branch. |
| T-123-40 | Information disclosure | committed capture and report JSON | mitigate | Before committing `123-final-report.json` and the capture directories, scan them with a FIXED-STRING matcher for the home-directory path prefix and for any `sk_`/`pk_live` token, and redact any hit. Never hand-escape backslashes into the matcher — use `grep -F` with the string exactly as it appears on disk. |
| T-123-SC | Tampering | npm/pip/cargo installs | mitigate | Not applicable — zero packages added; no install task exists in this plan. `123-RESEARCH.md` § Package Legitimacy Audit records the same. |

**Concurrence with `123-RESEARCH.md` § Security Domain:** confirmed. This plan runs existing tests,
writes planning documents and hand-edits two planning files. It changes no application code and adds
no dependency. Part B of the checkpoint exercises the real Clerk gate as an OBSERVER — it signs
nothing in and changes no auth configuration — and the two genuine handling concerns (committed
report output, shared-checkout commits) are addressed as T-123-40 and T-123-39.
</threat_model>

<verification>
- Gate run: 0 `wcag2a`/`wcag2aa` violations, `stats.expected` matching the criterion cell count, `stats.skipped` = 0.
- `e2e/a11y-gate-guard.spec.ts` 5/5; `e2e/theme-rendered-result.spec.ts` 47/47.
- C1-C7 all recorded with measured results; none pending.
- C7 = 0 on this report, non-zero on the paired C2 fixture.
- Operator's Part A answers and Part B figures recorded verbatim.
- `npx tsc --noEmit` and `npm run build` both exit 0; `npm test` has no new failures.
- `gsd-state-coherence.ps1` does not refuse.
</verification>

<success_criteria>
- `e2e/theme-contrast.spec.ts` passes with zero `wcag2a`/`wcag2aa` violations across every measured
  theme x page cell, all four themes held to the same AA bar (A11Y-02).
- The suite is structurally unable to report green against a page it never rendered: it fails on any
  skip while preserving per-cell `skipped` status, waits on per-page content, is non-green in
  measurement mode, and carries a self-test proving the gate guard fires — with the live gated-server
  behaviour confirmed by the operator (A11Y-03).
- Every discriminating control has a measured result; every residual gap is named and quantified.
</success_criteria>

<output>
Create `.planning/phases/123-accessibility-remediation/123-13-SUMMARY.md` when done. Record the gate
`stats` block verbatim, all seven control results, the operator's verbatim answers with their date,
the final marks applied to A11Y-02 and A11Y-03 with their reasoning, and the coherence guard's
verdict.
</output>
