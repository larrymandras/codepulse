---
phase: 123-accessibility-remediation
plan: 08
type: execute
wave: 2
depends_on: ["123-02", "123-03", "123-04", "123-05", "123-06", "123-07"]
files_modified:
  - e2e/contrast-isolation.spec.ts
  - .planning/phases/123-accessibility-remediation/123-CONTRAST-RESULT.md
autonomous: true
requirements: [A11Y-02]

must_haves:
  truths:
    - "D-02: the ledger is produced by two passes — axe against real pixels and real ancestors for reachable sites, canvas rasterisation for sites axe cannot reach — and every row is labelled by which pass produced it"
    - "D-16: the scan covers all 47 route files x 4 themes = 188 cells, while A11Y-02's pass/fail criterion remains the 20 cells A11Y-01 measured"
    - "C5: the run asserts it actually enumerated 47 routes, distinguished from the 62-file top-level glob that includes test files"
  artifacts:
    - path: ".planning/phases/123-accessibility-remediation/123-CONTRAST-RESULT.md"
      provides: "the pass-labelled ledger: 188-cell axe result, class-level isolation table, and the ratio-gated remediation list plan 123-11 executes"
    - path: ".planning/phases/123-accessibility-remediation/a11y-widened/"
      provides: "188 raw axe capture JSONs from the widened scan"
  key_links:
    - from: ".planning/phases/123-accessibility-remediation/123-CONTRAST-RESULT.md"
      to: "the 176 `text-*/NN` occurrences across 65 files"
      via: "every occurrence classified measured-passing / measured-failing / not-reached"
      pattern: "not-reached"
---

<objective>
Produce the ratio gate D-01 depends on. "Fix only those below threshold" is the locked decision;
this plan is where the threshold is measured. Two passes, one ledger, every row labelled by which
pass produced it — because "measured" and "calculated" are not interchangeable words in this phase's
output, and an analytic composite would have missed the 4.48:1 node whose text sits behind a
semi-transparent intermediate layer.

Purpose: D-02's hybrid measurement, D-16's widened scan, and control C5.
Output: `123-CONTRAST-RESULT.md` — the ledger and the explicit remediation list plan 123-11 executes
— plus 188 raw capture JSONs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/123-accessibility-remediation/123-CONTEXT.md
@.planning/phases/123-accessibility-remediation/123-RESEARCH.md
@.planning/phases/123-accessibility-remediation/123-VALIDATION.md
@.planning/phases/122-tokens-primitives-contrast-measurement/122-CONTRAST-BASELINE.md
@e2e/contrast-isolation.spec.ts
@e2e/a11y-routes.ts

<interfaces>
<!-- The corpus, re-derived live 2026-08-20 (occurrences via `grep -rhoE ... | wc -l`,
     files via `grep -rlE ... | sort -u | wc -l` — NOT `grep -c`, which counts matching lines
     and emits a `path:0` row per file scanned):
       176 occurrences   65 files   1 of which is a test file (src/components/JobsPanel.test.tsx)
     Do NOT use CONTEXT.md's original "75 files"; it is wrong and CONTEXT.md says so in place. -->

<!-- The 15 unique class strings — this is the pass-2 measurement UNIT. Measuring 15 classes x 4
     themes x N surfaces is tractable; measuring 176 occurrences individually is not, and is not
     what D-02 asks for. Occurrence counts in parentheses:
       text-primary/70 (45)          text-muted-foreground/50 (32)
       text-muted-foreground/70 (23) text-muted-foreground/60 (16)
       text-primary/80 (14)          text-muted-foreground/80 (14)
       text-primary/60 (10)          text-primary/40 (7)
       text-primary/50 (5)           text-primary/90 (3)
       text-primary/30 (2)           text-muted-foreground/40 (2)
       text-muted-foreground/30 (1)  text-(--status-warn)/80 (1)
       text-(--status-error)/60 (1) -->

<!-- Route denominator: 47 = 42 top-level non-test + 5 under src/pages/hr/.
     Control, never to be propagated as a route count: the same top-level glob INCLUDING tests
     returns 62. -->

<!-- Pre-123 control, re-derived this session by parsing violations[].nodes.length across the 20
     files in .planning/phases/122-.../a11y-after/ :
       24 violation objects / 209 nodes; 20 objects color-contrast, 4 aria-prohibited-attr.
     Do NOT re-derive this by grepping `"id"` — `"id"` also appears on every per-node check object,
     which is why a naive grep returns 225 against a 209-node corpus. -->

<!-- D-03 threshold rule, implemented as `wcagThresholdFor` in e2e/lib/contrast.ts by plan 123-02:
     3:1 when fontSizePx >= 24, or fontSizePx >= 18.66 && fontWeight >= 700; otherwise 4.5:1.
     Font size is a property of an OCCURRENCE, not of a class, so the class-level pass-2 table uses
     a flat 4.5:1 and any occurrence claimed under the 3:1 allowance must carry a `readTextMetrics`
     reading attached to its ledger row. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the widened 47 x 4 axe scan (pass 1)</name>
  <files>.planning/phases/123-accessibility-remediation/123-CONTRAST-RESULT.md</files>
  <read_first>
    - e2e/a11y-routes.ts (the 47-route table, its markers, and which 5 rows carry `criterion: true`)
    - e2e/theme-contrast.spec.ts (the `A11Y_SCAN_ALL` and `A11Y_MEASURE_ONLY` switches added by plan 123-03, and the capture write at :81-98)
    - .planning/phases/123-accessibility-remediation/123-CONTEXT.md § D-16
    - .planning/phases/123-accessibility-remediation/123-VALIDATION.md § "Discriminating Controls" (C5 is owned by this task)
  </read_first>
  <action>
    Start `dev:noauth` on 5181 from Git Bash: `VITE_CLERK_PUBLISHABLE_KEY= npm run dev:noauth`.
    PowerShell's empty-string assignment DELETES the variable rather than setting it empty, which
    would leave the Clerk gate up — every cell would then hit the skip branch and (after plan
    123-01) correctly fail the run for the wrong reason.

    Run the widened scan:
    `A11Y_SCAN_ALL=1 A11Y_MEASURE_ONLY=1
     A11Y_CAPTURE_DIR=.planning/phases/123-accessibility-remediation/a11y-widened/
     PW_BASE_URL=http://localhost:5181 node_modules/.bin/playwright test e2e/theme-contrast.spec.ts`
    Invoke `node_modules/.bin/playwright` explicitly — a different-version global Playwright is
    installed on this machine and cannot resolve repo-local modules, so a bare `npx playwright`
    result is not a result about the shipped version.

    `A11Y_MEASURE_ONLY=1` is required so all 188 cells complete and all 188 captures are written.
    The run WILL exit non-zero — that is D-14 working as designed, not a failure. Record the exit
    code and the reason in the same breath so a later reader cannot mistake it for a red scan.
    Measure the wall-clock runtime and record it: `123-RESEARCH.md` Assumption A1 extrapolated
    "minutes" from 122's 12.2s/20-cell figure and never re-measured. This is the measurement.

    Parse the 188 captures into the first section of `123-CONTRAST-RESULT.md`:
    - Per-rule object and node totals across all 188 cells, and separately across the 20 criterion
      cells. State the UNIT on every column.
    - A per-route table of remaining violations, sorted by node count.
    - Explicit callouts for the routes with `marker: null` in `a11y-routes.ts` (scanned but not
      marker-gated) and for `/chat`, which is out of scope for the whole v15.0 milestone and whose
      violations are recorded, never remediated by this phase.
    - Every row labelled `pass: axe`.

    **C5.** In the same document, record the assertion results proving the scan enumerated what it
    claims: the number of distinct routes present in the capture filenames must be **47**, the
    number of capture files **188**, and neither figure may be **62**. Re-derive the 47 from the
    captures themselves (`ls .../a11y-widened/*.json` split on the `__` separator, distinct route
    names), not from `a11y-routes.ts` — a table that says 47 and a run that visited 40 would agree
    with each other and disagree with reality.
  </action>
  <verify>
    <automated>ls .planning/phases/123-accessibility-remediation/a11y-widened/*.json | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `ls .planning/phases/123-accessibility-remediation/a11y-widened/*.json | wc -l` returns **188**.
    - Distinct route names parsed out of those filenames number **47**, and the document states
      explicitly that 47 is not 62 and why 62 is wrong (it is the top-level page glob including
      test files).
    - Across the 20 criterion cells only, the aggregated `color-contrast` node count and the
      aggregated `aria-prohibited-attr` object count are both **0** — plans 123-04, 123-05 and
      123-06 have landed by this wave. If either is non-zero, that is a finding to report against
      the responsible plan, not to absorb into this ledger.
    - Must-differ control: the aggregated violation count across all 188 cells is reported
      separately and is expected to be non-zero (42 routes have never been scanned). If it is also
      0, treat that as a suspected broken scan and prove otherwise before recording it — a probe
      that returns 0 everywhere carries no information.
    - The recorded wall-clock runtime is a measured figure with its command, not an extrapolation.
    - Every table row in section 1 carries `pass: axe`.
  </acceptance_criteria>
  <done>188 real captures on disk, a per-route violation table, a proven route count of 47, and the criterion cells confirmed clean.</done>
</task>

<task type="auto">
  <name>Task 2: Run the class-level isolation table (pass 2) and classify all 176 occurrences</name>
  <files>e2e/contrast-isolation.spec.ts, .planning/phases/123-accessibility-remediation/123-CONTRAST-RESULT.md</files>
  <read_first>
    - e2e/contrast-isolation.spec.ts (the `CLASS_MATRIX` and the artifact writer built by plan 123-02)
    - e2e/lib/contrast.ts (`paintedColorOfClass`, `contrastRatio`, `readTextMetrics`, `wcagThresholdFor`)
    - .planning/phases/123-accessibility-remediation/123-CONTEXT.md § D-01, § D-02, § D-03
    - .planning/phases/122-tokens-primitives-contrast-measurement/122-21-REMATRIX.md § "Root Cause 2"
    - .planning/phases/123-accessibility-remediation/123-LOADING-STATE-FINDING.md (plan 123-05's verdict on why a no-modifier token measured 2.68:1 — if it identified a mechanism that composites opacity, the same mechanism can apply to any occurrence and the classification must account for it)
  </read_first>
  <action>
    Run `node_modules/.bin/playwright test e2e/contrast-isolation.spec.ts` to produce the class x
    theme x surface ratio table in `e2e/.artifacts/123-isolation-pass2.json`. Extend the spec's
    `CLASS_MATRIX` if the widened scan revealed a surface a class sits on that the matrix does not
    cover; keep every row's `pass` label.

    Then classify **all 176 occurrences** across the 65 files into exactly one of three buckets, and
    record the bucket counts with their units:
    - **measured-passing (pass: axe)** — the occurrence rendered during the 188-cell scan and axe
      raised no `color-contrast` violation for it.
    - **measured-failing (pass: axe)** — the occurrence rendered and axe flagged it. These are
      plan 123-11's remediation list.
    - **not-reached** — the occurrence did not render during the scan (state-gated, error-only,
      behind a live bridge, or on a route whose marker is `null`). Adjudicate these from the
      class-level pass-2 table: a class that measures below 4.5:1 on the surface its occurrences sit
      on is remediated; a class that measures above on every surface it is used on is left alone,
      because D-01 explicitly rejected blanket-banning the idiom and some `/NN` uses are deliberate
      de-emphasis. Label every such row `pass: isolation`.

    Two rules that must be visible in the document, not just applied:
    - Any occurrence claimed as passing under D-03's 3:1 large-text allowance must carry a
      `readTextMetrics` reading (font-size px and font-weight) on its row. Without that reading the
      row owes 4.5:1. `DashboardLayout.tsx:148` is 14px and `:91` is 12px bold — both owed 4.5:1,
      which is the calibration case.
    - Never state a ratio derived by hex+alpha arithmetic. Tailwind v4's `/NN` compiles to
      `color-mix(in oklab, ...)`, a perceptual mix, so arithmetic cannot reproduce what the browser
      paints even with a correctly-identified ancestor.

    Write section 2 (the class-level table) and section 3 (the remediation list) of
    `123-CONTRAST-RESULT.md`. Section 3 must be file-and-line explicit: every occurrence plan 123-11
    is to edit, with its class string, its measured ratio, its pass label and its remedy (delete the
    `/NN`, or step to a named quieter token). Anything NOT on that list is explicitly recorded as
    left alone, with its measured ratio — a sweep that silently skips sites is the shape D-01
    exists to reject, and so is a sweep that edits sites nobody measured.

    The bucket counts must reconcile: measured-passing + measured-failing + not-reached = **176**,
    minus whatever plans 123-04 and 123-05 already removed. State the arithmetic explicitly and show
    it balancing; an unreconciled total means an occurrence was lost.
  </action>
  <verify>
    <automated>node_modules/.bin/playwright test e2e/contrast-isolation.spec.ts --reporter=line &amp;&amp; test -f e2e/.artifacts/123-isolation-pass2.json</automated>
  </verify>
  <acceptance_criteria>
    - `e2e/.artifacts/123-isolation-pass2.json` contains at least 60 rows (15 classes x 4 themes),
      every one carrying a `pass` field.
    - The three bucket counts in `123-CONTRAST-RESULT.md` sum to the live occurrence total, and the
      document shows the arithmetic. Re-derive the live total at write time with
      `grep -rhoE 'text-primary/[0-9]+|text-muted-foreground/[0-9]+|text-\(--[a-zA-Z-]+\)/[0-9]+' src | wc -l`
      and state it — it will be lower than 176 because plans 123-04 (9) and 123-05 (1) already
      removed some. Do not carry 176 forward as if untouched.
    - C4 control, in the document: the same command that produced the population figure must also
      return a known-present string. Show that `grep -rlE ... src | grep -c 'DashboardLayout'`
      returns 0 after plan 123-04 (that file is now clean) while
      `grep -rlE ... src | grep -c 'ToolExecutionPanel'` returns 1 — one known-absent and one
      known-present, so the probe is shown to discriminate in both directions.
    - Section 3 lists every occurrence to be edited with file, line, class string, measured ratio
      and pass label. No row says "measured" without a pass label.
    - No ratio anywhere in the document is described as calculated, computed, derived or estimated
      from token hex values.
    - The document names how many occurrences fell in `not-reached` and how many routes carried
      `marker: null`, so the residual gap D-17 states is quantified rather than gestured at.
  </acceptance_criteria>
  <done>A pass-labelled ledger classifying every surviving `text-*/NN` occurrence, a reconciled arithmetic, and an explicit file-and-line remediation list for plan 123-11 that no executor has to interpret.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev server → Playwright page | 188 cells navigate a locally-served development build; page content is developer-authored. |
| capture JSON → planning artifact | Raw axe output including rendered HTML fragments is written under `.planning/` and committed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-123-21 | Spoofing | "the scan covered 47 routes" | mitigate | The route count is re-derived from the capture FILENAMES, not from the table that configured the run, and is asserted to be neither fewer than 47 nor equal to 62. A table and a run that agree with each other but not with reality cannot pass this. |
| T-123-22 | Spoofing | "measured" ledger rows | mitigate | Every row carries a `pass` field, and the acceptance criteria forbid any ratio described as calculated. D-02's rule is enforced by the artifact schema, not only by prose. |
| T-123-23 | Repudiation | occurrence accounting | mitigate | The three bucket counts must sum to a live re-derived population with the arithmetic shown, so a lost occurrence surfaces as an imbalance instead of vanishing. |
| T-123-24 | Information disclosure | committed capture JSON | mitigate | Captures contain rendered markup from a local `dev:noauth` server with no real credentials, matching Phase 122's already-committed `a11y-before`/`a11y-after` sets. Before committing, grep the captures with a FIXED-STRING matcher for the home-directory path prefix and for any `sk_`/`pk_live` token, and redact any hit. |
| T-123-SC | Tampering | npm/pip/cargo installs | mitigate | Not applicable — zero packages added; no install task exists in this plan. `123-RESEARCH.md` § Package Legitimacy Audit records the same. |

**Concurrence with `123-RESEARCH.md` § Security Domain:** confirmed. This plan runs existing test
specs and writes planning documents; it changes no application code, no route, no dependency and no
authentication, authorization, input-validation or cryptography surface. The one genuine handling
concern is the committed capture output, addressed as T-123-24.
</threat_model>

<verification>
- 188 capture JSONs exist; 47 distinct routes derived from their filenames; neither figure is 62.
- The 20 criterion cells aggregate to 0 `color-contrast` nodes and 0 `aria-prohibited-attr` objects.
- The 188-cell aggregate is reported separately and is non-zero (the must-differ control).
- `e2e/.artifacts/123-isolation-pass2.json` holds >= 60 pass-labelled rows.
- The three occurrence buckets reconcile against a live re-derived total, arithmetic shown.
</verification>

<success_criteria>
- One ledger, two passes, every row labelled by which pass produced it.
- A file-and-line remediation list plan 123-11 can execute without interpretation, and an explicit
  record of every site left alone with its measured ratio.
- The 42 previously-unscanned routes have real numbers, so the D-16 checkpoint in plan 123-09 is a
  decision about data rather than a guess.
</success_criteria>

<output>
Create `.planning/phases/123-accessibility-remediation/123-08-SUMMARY.md` when done. Record: the
measured wall-clock runtime of the 188-cell scan with its command; the exit code and why it was
non-zero; the criterion-cell and full-scan aggregates with units; the three bucket counts and their
reconciled arithmetic; and the number of `not-reached` occurrences and `marker: null` routes.
</output>
