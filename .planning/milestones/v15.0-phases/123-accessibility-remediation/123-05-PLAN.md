---
phase: 123-accessibility-remediation
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/graph/CodeVaultGraph.tsx
  - src/components/RunTimeline.tsx
  - .planning/phases/123-accessibility-remediation/123-LOADING-STATE-FINDING.md
autonomous: true
requirements: [A11Y-02]

must_haves:
  truths:
    - "D-01: the two measured non-shell contrast violators are fixed as a class — the mechanism that produced them is identified and swept, not just the two flagged instances"
    - "D-04: the remedy deletes the /NN modifier or steps to a quieter token; alpha is never reintroduced, and the composited opacity that actually caused the failure is addressed rather than masked"
  artifacts:
    - path: "src/components/graph/CodeVaultGraph.tsx"
      provides: "AA-compliant graph loading state"
    - path: "src/components/RunTimeline.tsx"
      provides: "AA-compliant thinking indicator"
    - path: ".planning/phases/123-accessibility-remediation/123-LOADING-STATE-FINDING.md"
      provides: "the measured cause of the two non-shell violators and the class-level sweep derived from it"
  key_links:
    - from: "src/components/RunTimeline.tsx"
      to: "prefers-reduced-motion"
      via: "reducedMotion ? \"\" : \"animate-pulse\" — the built-in control that isolates the cause"
      pattern: "reducedMotion"
---

<objective>
Fix the two `color-contrast` violators that do **not** live in the app shell and that plan 123-04
therefore cannot clear:

  `/graphs`  `.text-primary\/70.text-base`   cyan **1.93**, emerald **1.90**, aubergine **2.21**
             `<p class="text-primary/70 font-mono text-base animate-pulse">Loading graph snapshot…</p>`
             `src/components/graph/CodeVaultGraph.tsx:892`
  `/live-run` `.text-\(--muted-foreground\)`  cyan **2.68**, emerald **2.69**, aubergine **4.48**
             `<div class="text-(--muted-foreground) text-base animate-pulse">Thinking...</div>`
             `src/components/RunTimeline.tsx:81`

Six nodes, three themes each, all six carrying `animate-pulse`. The second one has **no opacity
modifier at all** — it is the 4.48:1 near-miss D-17 names as the reason a grep bucket cannot be the
ratchet, and its cyan/emerald readings of 2.68/2.69 are far worse than the 4.48 the upstream
artifacts quote, because 4.48 is only aubergine's.

Purpose: D-01 (fix the class, not the instance) and D-04. This plan also has to answer *why* a
token with no alpha on it measured 2.68:1, because the answer decides the remedy.
Output: two component fixes plus `123-LOADING-STATE-FINDING.md` recording the measured cause and
the class sweep it implies.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/123-accessibility-remediation/123-CONTEXT.md
@.planning/phases/123-accessibility-remediation/123-RESEARCH.md
@.planning/phases/122-tokens-primitives-contrast-measurement/122-21-REMATRIX.md
@.planning/phases/120-polish-verified-defects/120-SANCTIONED-PATTERNS.md
@src/components/graph/CodeVaultGraph.tsx
@src/components/RunTimeline.tsx
@CLAUDE.md

<interfaces>
<!-- The two sites, verbatim -->
src/components/graph/CodeVaultGraph.tsx:890-893
  <div className="h-[600px] flex items-center justify-center rounded-[var(--radius)] border border-primary/20 bg-card/50">
    <p className="text-primary/70 font-mono text-base animate-pulse">
      Loading graph snapshot…
    </p>

src/components/RunTimeline.tsx:78-86
  if (showThinking) {
    return (
      <div className={`text-(--muted-foreground) text-base ${reducedMotion ? "" : "animate-pulse"}`}>
        Thinking...
      </div>
    );
  }

<!-- LEADING HYPOTHESIS, stated as a hypothesis and NOT as a finding: Tailwind's `animate-pulse`
     keyframes drive `opacity` between 1 and 0.5, and axe samples the element mid-animation. A
     halved composited opacity would explain a no-modifier token reading 2.68:1 when
     122-FOLLOW-UPS.md A1 measured the same token at 5.04-6.93:1 at full opacity. It would also
     explain CodeVaultGraph's 1.93, which is far lower than 0.70 alpha alone predicts.
     `bg-card/50` on the CodeVaultGraph wrapper is a second, independent semi-transparent layer
     (122-21-REMATRIX.md Root Cause 2 is exactly this shape).
     This MUST be tested, not assumed — see Task 1's control. -->

<!-- The control the code hands you for free: RunTimeline already omits `animate-pulse` when
     `reducedMotion` is true. Playwright's `page.emulateMedia({ reducedMotion: "reduce" })` flips
     it. If the violation disappears under reduced motion and persists without it, animate-pulse
     is the cause. If it persists under BOTH, the cause is the surface, not the animation. -->

<!-- Tailwind v4 mechanics, verified against this repo's own production build:
     `/NN` on a var()-backed token compiles to
       .text-primary\/70 { color: var(--primary) }
       @supports (color: color-mix(...)) { .text-primary\/70 { color: color-mix(in oklab, var(--primary) 70%, transparent) } }
     so deleting `/70` always falls back to a rule that already exists. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Measure why a no-modifier token read 2.68:1, with a must-differ control</name>
  <files>.planning/phases/123-accessibility-remediation/123-LOADING-STATE-FINDING.md</files>
  <read_first>
    - src/components/RunTimeline.tsx (:70-90 — the `showThinking` branch and the `reducedMotion` flag that gates `animate-pulse`)
    - src/components/graph/CodeVaultGraph.tsx (:885-900 — the loading branch, its `bg-card/50` wrapper and `border-primary/20`)
    - .planning/phases/122-tokens-primitives-contrast-measurement/122-21-REMATRIX.md § "Root Cause 2" (the semi-transparent intermediate layer that defeats analytic compositing)
    - .planning/phases/122-tokens-primitives-contrast-measurement/122-FOLLOW-UPS.md § A1 (full-opacity `--muted-foreground` measured at 6.93 cyan / 5.20 readable / 5.04 aubergine)
    - .planning/phases/120-polish-verified-defects/120-SANCTIONED-PATTERNS.md (which visual idioms are blessed and which were retired — a `/NN` deletion must not resurrect a retired pattern)
  </read_first>
  <action>
    Run the `/live-run` cell in cyan under two conditions that differ in exactly ONE variable, and
    record both axe readings for the `.text-\(--muted-foreground\)` node:
      (a) default — `animate-pulse` present;
      (b) `page.emulateMedia({ reducedMotion: "reduce" })` — the component's own `reducedMotion`
          branch omits `animate-pulse`, so the class is genuinely absent from the DOM.
    Use a scratch Playwright spec under `e2e/.scratch/` (gitignored, deleted afterwards) or a
    `--grep`'d temporary edit; either way the two runs must differ only in the media emulation.
    Invoke `node_modules/.bin/playwright` explicitly, never bare `npx playwright` — a
    different-version global Playwright on this machine cannot resolve repo-local modules.

    Interpret strictly:
    - Violation present in (a), absent in (b) => `animate-pulse`'s opacity keyframe is the cause.
    - Violation present in BOTH => the cause is the surface/ancestor stack, not the animation, and
      the hypothesis in `<interfaces>` is disproven. Say so plainly and find the real cause before
      choosing a remedy; do not carry a disproven mechanism into the fix.
    - Violation absent in BOTH => the probe did not reach the element. That is a broken measurement,
      not a clean result — the `showThinking` branch is state-gated and may not have rendered.
      Confirm the element is present in the DOM before reading any verdict from its absence.

    Whatever the answer, sweep the CLASS before writing the remedy: derive the population of sites
    that share the identified mechanism (for the animation hypothesis, that is text elements
    carrying `animate-pulse`; state the exact command and its UNIT — `grep -rlF` for files,
    `grep -rhoF ... | wc -l` for occurrences, never `grep -c`, which counts matching lines and
    emits a `path:0` row for every file scanned). Cross-check the resulting population against
    `120-SANCTIONED-PATTERNS.md`, which governs which `animate-pulse` uses were blessed and which
    were retired in Phase 120.

    Write `123-LOADING-STATE-FINDING.md` with: the two controlled readings and their exact ratios;
    the verdict on the hypothesis (confirmed / disproven, in those words); the derived population
    with its command and unit; the subset of that population that is in scope for THIS phase
    (the 6 measured nodes are, by force — A11Y-02's criterion is `violations.toEqual([])` on the
    20 measured cells); and any remainder explicitly deferred with a named reason, never silently.
  </action>
  <verify>
    <automated>test -f .planning/phases/123-accessibility-remediation/123-LOADING-STATE-FINDING.md &amp;&amp; grep -cE "confirmed|disproven" .planning/phases/123-accessibility-remediation/123-LOADING-STATE-FINDING.md</automated>
  </verify>
  <acceptance_criteria>
    - The document records TWO measured ratios for the same `/live-run` cyan node, differing only in
      reduced-motion emulation, both quoted to two decimal places.
    - The document states the verdict using the literal word `confirmed` or `disproven`.
    - The document states the class population with its derivation command AND its unit
      (files / occurrences), and does not use `grep -c` for a population figure.
    - `git status --porcelain e2e/` shows no `.scratch` leftovers.
    - The document names at least one entry from `120-SANCTIONED-PATTERNS.md` it checked against,
      or states explicitly that the file contains no rule bearing on this population.
  </acceptance_criteria>
  <done>The cause of a no-alpha token measuring 2.68:1 is established by a two-condition control, not inferred, and the class it belongs to is enumerated with a stated unit.</done>
</task>

<task type="auto">
  <name>Task 2: Remedy both loading states and prove all six nodes clear AA</name>
  <files>src/components/graph/CodeVaultGraph.tsx, src/components/RunTimeline.tsx</files>
  <read_first>
    - .planning/phases/123-accessibility-remediation/123-LOADING-STATE-FINDING.md (Task 1's verdict — the remedy follows from it)
    - src/components/graph/CodeVaultGraph.tsx (:885-900)
    - src/components/RunTimeline.tsx (:70-90)
    - src/components/graph/CodeVaultGraph.test.tsx (:199 asserts `getByText("Loading graph snapshot…")` — the text must not change)
    - .planning/phases/123-accessibility-remediation/123-CONTEXT.md § D-04
    - CLAUDE.md § Styling (token-driven, never hardcode hex; Phase 122 froze the palette)
  </read_first>
  <action>
    `src/components/graph/CodeVaultGraph.tsx:892` — delete the `/70` from `text-primary/70`, per
    D-04's default remedy. Leave `bg-card/50` and `border-primary/20` on the wrapper untouched:
    they are `bg-`/`border-` family, outside this sweep's three text families, and belong to a
    different unmeasured population. Do NOT change the string `Loading graph snapshot…` — its own
    test asserts that exact text at `CodeVaultGraph.test.tsx:199`.

    `src/components/RunTimeline.tsx:81` — there is no `/NN` to delete here, so D-04's default does
    not apply and the remedy must follow Task 1's verdict:
      - If `animate-pulse` was CONFIRMED as the cause: keep the token, and change how the pulse is
        expressed so it no longer drives text opacity below the AA floor. Prefer a mechanism already
        blessed in `120-SANCTIONED-PATTERNS.md`; if none applies, step the text to
        `text-foreground` so it clears 4.5:1 even at the animation's opacity trough. Whichever is
        chosen, keep the existing `reducedMotion` branch intact — it is a shipped accessibility
        affordance and Phase 122's `theme-reduced-motion` spec asserts against this family.
      - If the animation hypothesis was DISPROVEN: step to the next quieter passing TOKEN per D-04.
    Never reintroduce an alpha modifier, and never introduce a hex or a palette class.

    After both edits, re-measure. Start `dev:noauth` on 5181 from Git Bash
    (`VITE_CLERK_PUBLISHABLE_KEY= npm run dev:noauth`; PowerShell's empty-string assignment DELETES
    the variable and leaves the Clerk gate up), then run only the affected cells:
    `PW_BASE_URL=http://localhost:5181 A11Y_MEASURE_ONLY=1 A11Y_CAPTURE_DIR=.planning/phases/123-accessibility-remediation/a11y-loading-state/
     node_modules/.bin/playwright test e2e/theme-contrast.spec.ts --grep "Graphs|LiveRun"`.
  </action>
  <verify>
    <automated>npx tsc --noEmit &amp;&amp; npm test -- src/components/graph/CodeVaultGraph.test.tsx src/components/__tests__/blocks.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Across the 8 re-measured cells (Graphs and LiveRun x 4 themes), the count of `color-contrast`
      nodes whose target is `.text-primary\/70.text-base` or `.text-\(--muted-foreground\)` is
      **0** — down from 6. Derive by parsing `violations[].nodes` and matching on `node.target`,
      never by grepping `"id"`.
    - Must-differ control: in the SAME parsed output, the app-shell nav nodes are still counted and
      reported (they are 0 only if plan 123-04 has already landed). State which state was observed.
      A probe that returns 0 for every selector regardless is not evidence.
    - `grep -ohE 'text-primary/[0-9]+' src/components/graph/CodeVaultGraph.tsx | wc -l` returns
      **5** (was 6 — this file holds 6 sweep occurrences and only the one at `:892` is this plan's;
      the other 5 belong to plan 123-11's leaf sweep). Record both numbers.
    - `grep -oE "#[0-9a-fA-F]{3,8}" src/components/RunTimeline.tsx | wc -l` is unchanged — no hex
      introduced.
    - `grep -Fc "reducedMotion" src/components/RunTimeline.tsx` returns at least 1 — the
      reduced-motion affordance survives the fix.
    - `npx tsc --noEmit` exits 0; `npm test -- src/components/graph/CodeVaultGraph.test.tsx` passes.
  </acceptance_criteria>
  <done>All six non-shell measured contrast nodes clear AA, the reduced-motion affordance is intact, the graph loading text is unchanged, and no alpha or hex was introduced.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none introduced | Two presentational loading states change CSS class strings. No data, auth or route behaviour changes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-123-13 | Spoofing | "the violation is gone" claim | mitigate | Both readings come from a two-condition control differing in one variable, and the post-fix count is paired with a must-differ selector (the shell nodes) so a probe returning 0 for everything fails rather than passing. |
| T-123-14 | Denial of service | reduced-motion affordance | mitigate | `RunTimeline`'s `reducedMotion` branch is an accessibility affordance for vestibular disorders. The acceptance criteria assert it survives the fix; removing the animation entirely is acceptable, removing the *branch* is not. |
| T-123-SC | Tampering | npm/pip/cargo installs | mitigate | Not applicable — zero packages added; no install task exists in this plan. `123-RESEARCH.md` § Package Legitimacy Audit records the same. |

**Concurrence with `123-RESEARCH.md` § Security Domain:** confirmed after reading both files under
change. `CodeVaultGraph.tsx`'s loading branch and `RunTimeline.tsx`'s thinking indicator are
presentational; this plan changes `className` strings and nothing else. No authentication,
authorization, input-validation or cryptography surface is involved, no route changes, no dependency
is added.
</threat_model>

<verification>
- The controlled two-condition measurement is recorded with both ratios and an explicit verdict.
- 0 of the 6 previously-failing non-shell nodes remain across the 8 re-measured cells.
- `npx tsc --noEmit` exits 0; `CodeVaultGraph.test.tsx` still passes.
- `reducedMotion` still gates the animation in `RunTimeline.tsx`.
</verification>

<success_criteria>
- The two non-shell `color-contrast` violators clear 4.5:1 in cyan, emerald and aubergine.
- The mechanism that caused a no-modifier token to read 2.68:1 is stated as a measured verdict, and
  the class it belongs to is enumerated with its unit.
- No alpha reintroduced, no hex introduced, no test text changed, reduced-motion branch intact.
</success_criteria>

<output>
Create `.planning/phases/123-accessibility-remediation/123-05-SUMMARY.md` when done. Record the two
controlled ratios, the verdict word used, the remedy chosen for `RunTimeline.tsx` and why, and the
before/after `text-primary/NN` occurrence counts in `CodeVaultGraph.tsx`.
</output>
