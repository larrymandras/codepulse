---
phase: 127-ack-aware-retention-janitors
plan: 05
type: execute
wave: 3
depends_on: [127-03]
files_modified:
  - convex/ideation.test.ts
autonomous: true
requirements: [JANITOR-02]
user_setup: []

must_haves:
  truths:
    - "A row whose `dismissedAt` is absent is never returned by the delete step's index range — asserted on the RAW query result with a discriminating control (Verification A)"
    - "A `severity: \"critical\"` and a `severity: \"high\"` row 400 days old survive auto-dismiss in the same run in which a same-age `medium` row is dismissed and deleted (Verification B, D-04)"
    - "A batch of entirely carved-out rows still advances the cursor (Verification C, D-08)"
    - "Full batch reschedules, short batch stops, ceiling does zero work, and `batchesDone` is carried across the dismissing→deleting transition (Verification D)"
    - "The janitor logs on a zero-row run — the assertion R-01 makes mandatory, because for ~83 days that log line is the only evidence the mechanism is dormant rather than dead"
    - "A finding created moments ago is NOT auto-dismissed — the seconds/milliseconds unit is asserted, not assumed"
    - "This plan does NOT assert anything about `status: \"converted\"` — D-10 fences that gap out of the phase"
  artifacts:
    - path: "convex/ideation.test.ts"
      provides: "new test file — mock ctx fixture plus Verifications A, B, C, D and R-01's log assertion"
      contains: "autoCloseAndPruneHandler"
      min_lines: 150
  key_links:
    - from: "convex/ideation.test.ts"
      to: "convex/ideation.ts autoCloseAndPruneHandler"
      via: "direct handler import, hand-built fake ctx"
      pattern: "autoCloseAndPruneHandler"
---

<objective>
Prove the `ideationFindings` janitor's guarantees, including the one guarantee that cannot be
proven by observing deletions at all: that it runs and reports itself when it matches nothing.

Purpose: this janitor is inert by design until roughly 2026-11-16. Its correctness cannot be
inferred from its effects during that window, because it has none. The tests are the only
evidence it works, and the log assertion is the only evidence that will hold in production
before November.

Output: `convex/ideation.test.ts` — a new file. `convex/ideation.ts` had zero tests before this.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/127-ack-aware-retention-janitors/127-CONTEXT.md
@.planning/phases/127-ack-aware-retention-janitors/127-VALIDATION.md
@.planning/phases/127-ack-aware-retention-janitors/127-03-SUMMARY.md
@./CLAUDE.md

Read `127-CONTEXT.md`'s "Verification Criteria" section and `127-VALIDATION.md`'s
"Known limitation" section first. The limitation applies here identically, with `dismissedAt`
in place of `closedAt`, and must be stated in the test file.
</context>

<interfaces>
<!-- Extracted from the live tree 2026-08-25. -->

`convex/ideation.test.ts` DOES NOT EXIST. You are creating it. `vitest.config.ts`'s `include`
array already covers `convex/**/*.test.ts` (line 59), so no config change is needed.

This repo's convention is a per-file local mock builder, not a shared conftest-equivalent — see
`convex/media.test.ts:513` and `convex/inbox.test.ts`'s `makeRecordingDb`. Define your own.

`convex/media.test.ts:513-560` — `makeJanitorMockCtx`, the fixture pattern: it threads the REAL
`q.gte(...).lt(...)` bounds through a real filter, and at `:548` filters
`r.deletedAt !== undefined` as an explicit line, precisely so a mutation-testing control can
turn it red.

`convex/media.test.ts:636-713` — batch/reschedule/ceiling tests to adapt.

`convex/inbox.test.ts` after plan 127-04 — the sibling suite. Match its structure and naming so
the two read as a pair. Do NOT import its fixture; write a local one, per this repo's convention.

`convex/ideation.ts` after plan 127-03 exports: `autoCloseAndPruneHandler`, `autoCloseAndPrune`,
`IDEATION_AUTODISMISS_AGE_SEC`, `IDEATION_DISMISSED_GRACE_SEC`, `IDEATION_JANITOR_BATCH_SIZE`,
`IDEATION_JANITOR_MAX_BATCHES`, `shouldAutoDismiss`, `shouldDeleteDismissed`. Confirm the exact
names against `127-03-SUMMARY.md` and the file itself.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: New test file — fixture, Verification A, R-01's log assertion, unit-scale control</name>
  <files>convex/ideation.test.ts</files>
  <read_first>
    - `convex/media.test.ts:498-600` — `makeJanitorMockCtx` in full, including its docstring.
    - `convex/ideation.ts` — the janitor you are testing, plus `recordFinding`/`dismissFinding`
      for the field shapes real rows carry.
    - `convex/inbox.test.ts` after plan 127-04 — the sibling suite's structure.
    - `127-VALIDATION.md` § "Known limitation".
    - `127-CONTEXT.md` § R-01.
  </read_first>
  <action>
    Create `convex/ideation.test.ts` with a file-header docstring stating that `convex/ideation.ts`
    had zero tests before this file — the Wave 0 gap `127-VALIDATION.md` names — and that this
    repo has no `convex-test` runtime harness, so the fixture below is a hand-rolled in-memory
    fake `ctx.db` following `media.test.ts:513`'s convention.

    Build `makeIdeationJanitorMockCtx(rows)` that threads the REAL bounds the handler passes
    into `withIndex` — `eq` on `dismissed`, `gte`/`lt` on `createdAt` and on `dismissedAt` —
    through a real filter over the supplied rows. Reimplement as EXPLICIT readable lines the two
    index behaviours the handler depends on: a `.gte`/`.lt` range on `dismissedAt` matches only
    rows whose `dismissedAt` is a number, and the `by_dismissed` equality plus `createdAt` range
    composes as the real widened index would. Record `db.patch` and `db.delete` calls with full
    argument objects, and record what `.take(n)` was called with.

    **Verification A — assert at the QUERY layer.** Seed an undismissed row positioned so it
    WOULD fall inside the delete step's cursor/cutoff range if its `dismissedAt` were `0`. Drive
    the delete step and assert the raw batch length is 0. **Control:** a second row with
    `dismissedAt` explicitly `0` under the same cutoff IS returned. Write the control first.

    Add a comment on the Verification A describe stating what it does NOT prove: with no
    `convex-test` harness, "the index excludes absent fields" is reimplemented in this mock in
    JavaScript. A green A shows the HANDLER asks for the right range; it does not show that
    Convex's real index excludes `undefined`. That property rests on the citation at
    `convex/controlVerbSwaps.ts:97-117` and two production call sites, not on this test.

    **R-01's log assertion — the reason this janitor is falsifiable at all before November.**
    Spy on `console.log` (`vi.spyOn(console, "log")`). Run the handler against an EMPTY fixture —
    zero rows, zero matches, zero work — and assert a log line was emitted and that it contains a
    stable, greppable marker naming the ideation janitor and the step. Then assert it also
    contains a rendered cutoff DATE, and that the date's year is the current year, not 1970 — a
    1970 date is the direct signature of a seconds/milliseconds error, and this repo has shipped
    that defect before. Comment the test with why it is not optional: at M=180d the auto-dismiss
    step matches zero rows until roughly 2026-11-16, so for ~83 days this line is the only signal
    distinguishing correct-and-dormant from dead-on-arrival.

    **Unit-scale control.** Seed a finding with `createdAt === nowSec` and assert it is NOT
    auto-dismissed; seed one at `nowSec - 181 * 24 * 60 * 60` and assert it IS. Write the age
    threshold independently in the test rather than importing the constant, following
    `media.test.ts`'s `THIRTY_DAYS_MS` discipline, so the test checks the constant rather than
    restating it.
  </action>
  <verify>
    <automated>npx vitest run convex/ideation.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run convex/ideation.test.ts` passes with a non-zero test count (a selector or
      file that matches zero tests exits 0 — read the count, not the exit code).
    - `npx vitest run convex/ideation.test.ts -t "structural"` passes and reports at least 2
      tests.
    - The zero-row log test passes, and asserts BOTH the marker and a non-1970 cutoff year.
    - Mutation proof, performed and recorded in the SUMMARY: temporarily move the janitor's
      `console.log` inside an `if (actedCount > 0)` branch, re-run, confirm the zero-row log test
      turns RED. Restore. If it stays green the test is not exercising R-01.
    - Second mutation proof: change the auto-dismiss cutoff arithmetic from seconds to
      milliseconds, re-run, confirm the unit-scale control turns RED. Restore.
  </acceptance_criteria>
  <done>`convex/ideation.test.ts` exists with a bounds-applying fixture; Verification A passes with its discriminating control and stated limitation; R-01's log assertion and the unit-scale control are each proven to fail under a deliberate mutation.</done>
</task>

<task type="auto">
  <name>Task 2: Verifications B, C and D for the ideation janitor</name>
  <files>convex/ideation.test.ts</files>
  <read_first>
    - The fixture from Task 1.
    - `127-CONTEXT.md` § D-04 and § "Verification Criteria" B, C, D.
    - `convex/media.test.ts:636-713` — the batch/reschedule/ceiling tests to adapt.
    - `convex/ideation.ts`'s `shouldAutoDismiss` and `shouldDeleteDismissed` as shipped.
  </read_first>
  <action>
    **Verification B.** Seed three undismissed findings all 400 days old, in one batch:
    `severity: "critical"`, `severity: "high"`, and `severity: "medium"`. Drive the chain to
    convergence. Assert the critical and high rows are STILL `dismissed: false` with
    `dismissedAt` undefined and still present; the medium row was dismissed and then deleted.
    The medium row is the control that proves the run did any work at all — a
    critical-and-high-only fixture would pass against a handler that does nothing.

    Add the delete-step asymmetry case, which is where a reused predicate would silently break:
    a `severity: "critical"` finding that a HUMAN dismissed 400 days ago (`dismissed: true`,
    `dismissedAt` set) MUST be deleted. `critical` blocks auto-dismissal only; once a human
    dismisses it, it is ordinary closed data. Pair it with an undismissed critical row in the
    same fixture that must survive — the two together are what discriminate.

    Name blocks so `-t "carve-out"` selects them, matching `127-VALIDATION.md`.

    **Verification C.** A FULL batch (`IDEATION_JANITOR_BATCH_SIZE` rows) of entirely
    carved-out rows — all `severity: "critical"`, all past the cutoff, strictly increasing
    `createdAt`. Assert zero patches AND a strictly advanced rescheduled cursor, and explicitly
    assert it does NOT reschedule with an unchanged cursor.

    **Verification D.** Adapt `media.test.ts:636-713`: full batch reads
    `.take(IDEATION_JANITOR_BATCH_SIZE)` and reschedules with an advanced cursor and
    `batchesDone: 1`; a short DELETE batch does not reschedule; `batchesDone` already at
    `IDEATION_JANITOR_MAX_BATCHES` does zero work; a full batch reaching the ceiling on this
    invocation still does its own work but does not reschedule.

    Add the two-step transition test: a SHORT batch in the `"dismissing"` step transitions to
    `"deleting"` with `cursor: 0` and `batchesDone` CARRIED, not reset. Assert the scheduled args
    literally.

    Do NOT assert anything about `status: "converted"` or `convertedAt` — D-10 fences that gap
    out of this phase, and a test asserting current behaviour there would harden a gap the
    roadmap deliberately left open.
  </action>
  <verify>
    <automated>npx vitest run convex/ideation.test.ts && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run convex/ideation.test.ts -t "carve-out"` passes with at least 3 tests.
    - `npx vitest run convex/ideation.test.ts -t "cursor advances on skip"` passes.
    - `npx vitest run convex/ideation.test.ts -t "batch"` passes with at least 5 tests.
    - The transition test asserts the scheduled args contain `batchesDone: 1` and `cursor: 0`.
    - `grep -n "converted" convex/ideation.test.ts` returns NOTHING. Control:
      `grep -c "dismissed" convex/ideation.test.ts` is non-zero.
    - `npm test` — full suite green, no pre-existing test regressed.
  </acceptance_criteria>
  <done>Verifications B, C and D pass, including the human-dismissed-critical delete case and the carried-`batchesDone` transition; the full suite is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test fixture → handler under test | The mock ctx is the only stand-in for Convex's index semantics; its fidelity bounds every claim here. |
| log line → the operator, for ~83 days | During the dormant window the log is the ONLY observable output of this mechanism. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-127-17 | Repudiation | A dormant janitor indistinguishable from a dead one, with no test forcing the distinction | mitigate | The zero-row log assertion, proven to turn red when the log is moved behind an `actedCount > 0` guard. Includes a non-1970 cutoff-year assertion so a units defect cannot hide inside a green log test. |
| T-127-18 | Tampering | The `critical`/`high` carve-out silently regressing | mitigate | Same-batch control pairs in Verification B, plus the human-dismissed-critical delete case that a single reused predicate would get wrong. No database-level backstop exists; plan 127-07's manual mutation-testing control is the second half. |
| T-127-19 | Tampering | A mock faithful enough to pass but not to fail | mitigate | Real bounds threaded through a real filter, per `media.test.ts:513-560`. Two live mutation proofs required in Task 1's acceptance criteria. |
| T-127-SC | Tampering | npm/pip/cargo installs | accept | No packages installed; Vitest already configured. |

No `high`-severity threat. The residual — that no test proves Convex's REAL index excludes
`undefined` — is disclosed, and is part of why plan 127-08's deploy is blocking.
</threat_model>

<verification>
1. `npx vitest run convex/ideation.test.ts` — green with a non-zero test count.
2. `npm test` — full suite green.
3. Both mutation proofs from Task 1, performed and recorded.
4. Every `-t` selector in `127-VALIDATION.md`'s per-task map resolves to a non-zero count.
</verification>

<success_criteria>
- Verifications A, B, C, D and R-01's log assertion all pass, each with a control that could
  have come out the other way.
- The log assertion and the unit-scale control are each proven to fail under a deliberate
  mutation.
- Verification A's limitation is stated in the test file.
- `npm test` is green.
</success_criteria>

<output>
Create `.planning/phases/127-ack-aware-retention-janitors/127-05-SUMMARY.md` when done.
Record: both mutation proofs (what you changed, what turned red, that you restored it), the
per-selector test counts, and the exact greppable marker the janitor logs — plan 127-08's
Verification F greps `docker logs convex-backend` for it.
</output>
