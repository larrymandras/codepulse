---
phase: 120-polish-verified-defects
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/HeroStatsBar.tsx
  - .planning/phases/120-polish-verified-defects/120-FABRICATION-INVENTORY.md
autonomous: true
requirements: [POLISH-01, POLISH-04]

must_haves:
  truths:
    - "D-07: the fabricated Integrations row is DELETED outright, not rewired — the honest emitter-backed version already ships on /infrastructure via IntegrationHealth.tsx backed by convex/integrations.ts healthStatus, so rewiring the hero would duplicate an existing page rather than add signal, and LINEAR and VERCEL have no emitter at all"
    - "D-08: the fabrication CLASS is swept, every hit is reported with file:line, and only the unambiguous ones are fixed — anything needing a judgment call is RECORDED in the phase artifacts as input for Phase 122's TOKEN-04 six-state tile work, never silently fixed and never silently dropped"
    - "The sweep reports what it DROPPED and why, so its precision is visible: the repo is dense with comments of the form no hardcoded hex and never a fabricated total, which are prior phases' honesty guarantees and are the opposite of a defect — a sweep that reports them as hits is crying wolf"
    - "No surface on the Dashboard hero asserts an integration's health without a live source behind it"
    - "HeroStatsBar's own hover:scale-[1.01] and its unconditional decorative pulse dot are removed here, because this plan owns the file and no other wave-1 plan may edit it"
  artifacts:
    - path: "src/components/HeroStatsBar.tsx"
      provides: "Hero with the fabricated Integrations strip removed"
      contains: "KPI grid"
    - path: ".planning/phases/120-polish-verified-defects/120-FABRICATION-INVENTORY.md"
      provides: "D-08 inventory: every hit, its disposition, the owning phase for deferrals, and the dropped-false-positive account"
      min_lines: 40
  key_links:
    - from: "src/components/IntegrationHealth.tsx"
      to: "convex/integrations.ts healthStatus"
      via: "useIntegrationHealth — the genuinely derived integration health that made delete-don't-rewire the right call"
      pattern: "healthStatus"
---

<objective>
Delete the fabricated Integrations row from the Dashboard hero (D-07), then sweep the render paths for
the same defect CLASS, report every hit with `file:line`, and fix only the unambiguous ones (D-08).

Purpose: POLISH-04's rule is that no surface asserts a figure with no emitter behind it. One instance is
verified — a hardcoded five-name strip with unconditional green dots and a literal
"(Integrations row simulation)" comment. The requirement's text is general, so the sweep is required;
but the sweep's value is its precision, not its volume, and this repo is full of comments that *look*
like hits and are actually prior phases' honesty guarantees.

Output: a hero with the strip gone, and `120-FABRICATION-INVENTORY.md` recording every hit, its
disposition, and what was dropped.

**Do not rewire the row.** That option was considered and rejected: the honest version already renders
on `/infrastructure`, two of the five names have no emitter in the system at all, and the hero space is
where Phase 125's Pulse ECG lands.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/120-polish-verified-defects/120-CONTEXT.md
@CLAUDE.md

@src/components/HeroStatsBar.tsx
@src/components/IntegrationHealth.tsx

<interfaces>
<!-- MEASURED 2026-08-17. Locate by content; lines move in a shared checkout. -->

### The verified defect — src/components/HeroStatsBar.tsx
  ~:161-170 the whole block, comment included. CONTEXT.md cites 161-168; the block actually runs to the
  closing `</div>` at ~:170. Delete all of it:
    {/* Quick Action Badges (Integrations row simulation) */}
    <div className="flex items-center gap-2 overflow-x-auto pb-2 scrollbar-none border-y border-border/50 py-3">
      <span ...>Integrations</span>
      {['GITHUB', 'LINEAR', 'SLACK', 'CONVEX', 'VERCEL'].map((integration) => (
        <div ...><span className="w-1.5 h-1.5 rounded-full bg-emerald-500/80"></span><span ...>{integration}</span></div>
      ))}
    </div>
  The dots are unconditional `bg-emerald-500/80` — green regardless of any state. `LINEAR` and `VERCEL`
  have no emitter anywhere in the system.

### Why delete rather than rewire (D-07) — verified, so the rejected option was real
  `convex/integrations.ts:40` `healthStatus` derives from real tables (`gitCommits`, `events`,
  `supabaseHealth`) with per-type staleness thresholds; `src/hooks/useIntegrationHealth.ts` and
  `src/components/IntegrationHealth.tsx` consume it; it already renders on `Infrastructure.tsx:86`.

### This plan's other two edits in the same file (exclusive ownership — 120-01 excludes this file)
  ~:124  the card wrapper carries `hover:scale-[1.01] transition-transform duration-300`. Remove both
         tokens; read the line first and confirm no other transform utility is present (`translate-`,
         `rotate-`, `group-hover:scale-`). Leave `glow-card`, `shadow-[var(--glow-xs)]`,
         `hover:shadow-[var(--glow-sm)]`, `hover:border-primary/50` and `transition-colors` alone (D-01).
  ~:133  `<span className="w-1.5 h-1.5 rounded-full bg-primary animate-pulse" />` beside the
         "System Load" label. Unconditional, therefore decorative under D-09. Remove ONLY
         `animate-pulse`; keep the static dot (D-09's decision is de-animate, not remove). No
         reduced-motion gating is needed on a removed animation.

### KNOWN FABRICATION IN THE SAME FILE THAT IS **NOT** THIS PHASE'S TO FIX — record it
  ~:141  `<AnimatedNumber value={stats.activeSessions > 0 ? 100 - (stats.errorRate * 2) : 100} ... />`
         rendered under the label "System Load" with the eyebrow "LIVE / 5H WINDOW". This is a synthetic
         figure: it is an error-rate arithmetic dressed as a load percentage, and it reads 100% whenever
         there are no active sessions. It IS a POLISH-04-class defect — and `REQUIREMENTS.md:59`
         (SIGNAL-02) already assigns it: "A Pulse ECG canvas hero replaces the **synthetic**
         'SYSTEM LOAD' bar", Phase 125. So its disposition is already decided. **Record it in the
         inventory with SIGNAL-02 / Phase 125 named as the owner; do not fix it and do not delete the
         bar.** Removing the hero's only large number now would leave a hole that Phase 125 fills, and
         re-deciding an assigned requirement is out of scope.
  ~:127  `className={\`w-2 h-2 rounded-full shadow-[0_0_8px_currentColor] ${hc.bg} text-${hc.bg.replace('bg-','')}\`}`
         — `text-${...}` is a runtime-interpolated Tailwind class, which Tailwind's static extractor
         cannot see, so that colour never ships. A real defect, but not a fabrication and not
         enumerated. Record it for Phase 122 and change nothing.

### The D-08 sweep — a PRE-MEASURED starting point, to be re-run and extended, not trusted
Run these yourself; the figures below are a 2026-08-17 reading given so you can tell a real find from a
re-discovery.

  `Math.random` in non-test `src/` — 9 hits, ALL legitimate, none asserting a figure:
    `voice/AvatarAura.tsx:189-192` particle physics; `lib/audioEngine.ts:97,315,366,493` note/jitter
    selection; `pages/ConfigPage.tsx:347` randomised SKELETON widths (a loading placeholder, not a
    value). Expect to drop all nine — and say so.

  Hardcoded display ARRAYS in a render path — the `['GITHUB',...]` strip is the only one found.

  Comment keywords (`simulat`, `mock`, `demo`, `fabricat`, `placeholder`, `hardcoded`, `for now`) —
    ~40 hits, of which **one** is a defect (`HeroStatsBar.tsx:161`'s "(Integrations row simulation)").
    The overwhelming majority are NEGATIVE assertions from prior phases: "never a fabricated $0.00",
    "no hardcoded hex", "never a hardcoded claim about another repo's state", "renders only the fields
    present, nothing fabricated". Reporting those as hits would be exactly the crying-wolf failure
    `CLAUDE.md`'s review discipline forbids. Several `simulation` hits are `d3ReheatSimulation` — a
    force-graph API name.

  Extend the sweep beyond the above with at least: a search for a status/health indicator whose colour
  or state is a literal rather than derived (the `bg-emerald-500/80` shape), and a search for a numeric
  literal rendered inside a metric/KPI/tile component. Report what you searched, including the searches
  that returned nothing — a sweep with no negative results is not a sweep.

### Fixed-string discipline
`hover:scale-[1.01]` contains regex metacharacters. Search for it with `git grep -F` / `grep -F` /
`rg -F` only. Any Windows path or bracketed literal in this sweep needs the same treatment.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete the fabricated Integrations row, plus this file's scale and pulse (D-07)</name>

  <files>src/components/HeroStatsBar.tsx</files>

  <read_first>
    - `src/components/HeroStatsBar.tsx` — the whole render body, ~:118 to the end. You need to see what
      sits above and below the Integrations block (the top progress card at ~:122-159 and the KPI grid
      at ~:172+) so you can tell whether removing the block leaves a spacing artefact.
    - `src/components/IntegrationHealth.tsx` and `src/pages/Infrastructure.tsx` around :86 — confirm for
      yourself that the honest version really does already render, since that fact is D-07's entire
      justification. If it does not, stop and report it rather than deleting the hero row on a stale
      premise.
    - `.planning/phases/120-polish-verified-defects/120-CONTEXT.md` § D-07.
  </read_first>

  <action>
**A. Delete the Integrations block.** Remove the block from its `{/* Quick Action Badges (Integrations
row simulation) */}` comment through its closing `</div>` inclusive. The comment goes with it — a
comment naming a simulation that no longer exists is a stale claim.

The parent is `<div className="flex flex-col gap-6">`, so the gap collapses cleanly and no spacer is
needed. Do NOT add a placeholder, an empty state, a "coming soon", or a link to `/infrastructure` in its
place. The requirement is that nothing asserts an unsourced figure; the correct residue is nothing at
all, and Phase 125 occupies this space.

**B. Remove this file's `hover:scale-[1.01]`** at ~:124, together with its co-located
`transition-transform duration-300` — read the line first and keep the transition only if another
transform utility is present on it. Leave every `glow-card` / `shadow-[var(--glow-*)]` /
`hover:border-primary/50` / `transition-colors` token untouched (D-01).

**C. De-animate the unconditional pulse dot** at ~:133: remove the `animate-pulse` token only, keeping
the `<span>` and its `w-1.5 h-1.5 rounded-full bg-primary` classes as a static dot (D-09).

**D. Change nothing else in this file.** Specifically: do not touch the `AnimatedNumber` at ~:141, the
`hc.bg` status dot at ~:127, the progress bar, the Memory readout, or the KPI grid. Two of those are
recorded in Task 2 as owned by later phases and one is out of scope entirely.
  </action>

  <verify>
    <automated>npx tsc --noEmit && npx vitest run</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c 'LINEAR' src/components/HeroStatsBar.tsx` is 0; same for `VERCEL`, `Integrations row simulation`,
      and `bg-emerald-500/80`.
    - `git grep -cF 'hover:scale-[1.01]' -- src/components/HeroStatsBar.tsx` is 0 and
      `git grep -cF 'animate-pulse' -- src/components/HeroStatsBar.tsx` is 0.
    - CONTROLS proving the deletion was scoped rather than a range cut:
      `grep -c 'AnimatedNumber' src/components/HeroStatsBar.tsx` is unchanged,
      `grep -c 'MetricCard' src/components/HeroStatsBar.tsx` is unchanged,
      `grep -c 'glow-card' src/components/HeroStatsBar.tsx` is unchanged, and
      `grep -c 'rounded-full bg-primary' src/components/HeroStatsBar.tsx` is >= 1 (the static dot
      survived — D-09 is de-animate, not delete).
    - `git grep -c 'healthStatus' -- convex/integrations.ts` is >= 1 and
      `git grep -c 'IntegrationHealth' -- src/pages/Infrastructure.tsx` is >= 1 — D-07's premise is
      re-verified rather than assumed, and both must be recorded in the SUMMARY.
    - `npx tsc --noEmit` exits 0; `npx vitest run` shows no new failures against the pre-plan count.
    - `git diff --stat` touches only `src/components/HeroStatsBar.tsx`.
  </acceptance_criteria>

  <done>The Dashboard hero no longer claims five integrations are green; nothing was put in the row's place; the honest version on `/infrastructure` is confirmed still live.</done>
</task>

<task type="auto">
  <name>Task 2: Sweep the fabrication CLASS, report every hit, fix only the unambiguous (D-08)</name>

  <files>.planning/phases/120-polish-verified-defects/120-FABRICATION-INVENTORY.md</files>

  <read_first>
    - `<interfaces>` § "The D-08 sweep" above — then RE-RUN every search yourself. The figures there are
      a prior reading, given so you can distinguish a genuine new find from a re-discovery. A count
      copied from a plan is not evidence.
    - `CLAUDE.md` § "Code Review & Findings Precision" — zero false positives, `file:line` evidence for
      every finding, a confidence level, default to "not real" when uncertain, and a closing "what I
      dropped and why". That section governs this task's output format.
  </read_first>

  <action>
Create `.planning/phases/120-polish-verified-defects/120-FABRICATION-INVENTORY.md`.

Run the sweep. At minimum: hardcoded display arrays in a render path; `Math.random` in non-test `src/`;
the comment keywords; status/health indicators whose colour or state is a literal rather than derived
(the `bg-emerald-500/80` shape); numeric literals rendered inside a metric/KPI/tile component. Add any
further search the first pass suggests. Paste the literal command and its literal output for each,
including the ones that return nothing.

Then write the inventory with these sections:

**§1 — Searches run.** Each command verbatim, its raw hit count, including the empty ones. State that an
empty result is a result.

**§2 — FIXED (unambiguous).** The Integrations row, `file:line`, what it asserted, why it was
unambiguous (a literal name array, unconditional green dots, and a self-describing "simulation"
comment — three independent tells).

**§3 — RECORDED, deferred to a named phase.** One row per hit, each with `file:line`, the exact
expression, a confidence level, what false claim it makes, and **the owning phase**. It must include at
minimum:
  - `HeroStatsBar.tsx` ~:141 — the synthetic "System Load" percentage. Owner: **SIGNAL-02, Phase 125**
    (`REQUIREMENTS.md:59` names it as "the synthetic 'SYSTEM LOAD' bar"). Confidence: high — it is
    error-rate arithmetic labelled as load, and it reads 100% when there is no traffic. State plainly
    that this phase left a known fabrication in place because a later requirement already owns it, and
    that POLISH-04 is therefore not fully closed by this plan on this one figure.
  - `HeroStatsBar.tsx` ~:127 — the runtime-interpolated `text-${...}` Tailwind class that never ships.
    Owner: **Phase 122**. Confidence: high. Not a fabrication; a dead style.
  - Anything else your sweep finds that needs a judgment call. Do not fix these — D-08 is explicit that
    a judgment call is recorded, and the record is what Phase 122's six-state tile work consumes, since
    every metric there has to declare its state anyway.

**§4 — DROPPED, with the reason (the precision account).** Enumerate the false-positive families you
searched and rejected, with a representative `file:line` each and one clause of reasoning. At minimum:
the ~40 comment-keyword hits that are prior phases' NEGATIVE honesty assertions ("never a fabricated
$0.00", "no hardcoded hex", "nothing fabricated") — these are the opposite of the defect; the nine
`Math.random` hits (audio synthesis, particle physics, skeleton widths); and `d3ReheatSimulation`, a
force-graph API name that matches `simulat`. State the total dropped versus the total reported. A sweep
that reports 40 hits and calls it thoroughness is worse than one that reports 3 and shows its
rejections.

**§5 — Handoff line.** One sentence naming which POLISH-04 residue remains open and which phase owns
each piece, so the phase VERIFICATION does not have to re-derive it.

**§6 — residue closeout.** Record your post-edit `hover:scale-[1.01]` and `animate-pulse` counts for
`HeroStatsBar.tsx` (both should be 0) in THIS file, so the phase VERIFICATION can close plan 120-01's
residue accounting without opening the source. **Do not write to `120-SWEEP-EVIDENCE.md`** — four wave-1
plans run in parallel against a shared checkout and each owns exactly one artifact: `120-01` owns
`120-SWEEP-EVIDENCE.md`, `120-02` owns `120-SHELL-EVIDENCE.md`, `120-03` owns
`120-SANCTIONED-PATTERNS.md`, and this plan owns `120-FABRICATION-INVENTORY.md`.
  </action>

  <verify>
    <automated>test -s .planning/phases/120-polish-verified-defects/120-FABRICATION-INVENTORY.md && grep -c 'SIGNAL-02' .planning/phases/120-polish-verified-defects/120-FABRICATION-INVENTORY.md</automated>
  </verify>

  <acceptance_criteria>
    - All six sections exist.
    - §1 contains literal commands and literal output, including at least one search that returned zero
      hits.
    - §3 names `SIGNAL-02` / Phase 125 as the owner of the synthetic System Load figure and states in
      plain words that POLISH-04 is not fully closed on that figure by this phase.
    - §4 exists, gives a representative `file:line` for each dropped family, and states the
      dropped-versus-reported totals. An inventory with no §4 does not satisfy this task.
    - Every entry in §2 and §3 carries a `file:line` and a confidence level. Any finding you cannot
      substantiate from the live code must be dropped into §4 rather than reported.
  </acceptance_criteria>

  <done>The fabrication class is swept, the one unambiguous instance is fixed, the two known-but-owned instances are on the record with their phase, and the false-positive families are visibly rejected rather than quietly omitted.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator's belief ← rendered figure | The defect class IS the boundary: a surface asserting a state with no source behind it causes an operator to make decisions on a fiction. This is the same honesty rule that governed v14.0's MISSION-03. |
| none technical | This plan deletes JSX and writes a markdown artifact. No input parsing, no network, no persistence, no new dependency. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-120-15 | Spoofing | A dashboard surface asserting five integrations are healthy with no emitter behind any of them, two having no emitter in the system at all | mitigate | The block is deleted outright rather than rewired; nothing is put in its place; the honest emitter-backed surface on `/infrastructure` is re-verified as still live before the deletion |
| T-120-16 | Repudiation | A "sweep" reporting 40 keyword hits, burying the one real defect in prior phases' honesty comments and consuming triage turns | mitigate | §4's mandatory dropped-with-reason account plus per-finding confidence levels; `CLAUDE.md`'s zero-false-positive rule is named as the governing format |
| T-120-17 | Repudiation | Leaving a KNOWN fabrication in place silently, so a later phase reads POLISH-04 as fully closed | mitigate | §3 records the synthetic System Load figure with SIGNAL-02 / Phase 125 as owner and states explicitly that POLISH-04 is not fully closed on it; §5 repeats it as a handoff line |
| T-120-18 | Tampering | A range-based deletion in `HeroStatsBar.tsx` catching the KPI grid or the progress card | mitigate | Delete by content from the comment to its closing tag; unchanged `AnimatedNumber` / `MetricCard` / `glow-card` counts are asserted as controls |
| T-120-SC | Tampering | npm/pip/cargo installs | mitigate | No packages are added. If an install becomes necessary, stop and run the Package Legitimacy Gate first. |
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0; `npx vitest run` shows no new failures against the pre-plan baseline.
- `LINEAR`, `VERCEL`, `Integrations row simulation` and `bg-emerald-500/80` are all 0 in
  `HeroStatsBar.tsx`; `AnimatedNumber`, `MetricCard` and `glow-card` counts unchanged.
- `hover:scale-[1.01]` and `animate-pulse` are 0 in `HeroStatsBar.tsx`; a static
  `rounded-full bg-primary` dot survives.
- D-07's premise re-verified: `convex/integrations.ts healthStatus` exists and `Infrastructure.tsx`
  still renders `IntegrationHealth`.
- `120-FABRICATION-INVENTORY.md` has all five sections, literal command output including a zero-hit
  search, SIGNAL-02 named as owner of the System Load figure, and the §4 precision account.
- **Attended check, not automatable here:** with `dev:noauth` running, load the Dashboard and confirm
  the Integrations strip is gone and the layout above and below it closed cleanly with no orphaned
  divider or double gap (the deleted block carried `border-y`). Record the observation in the SUMMARY.
</verification>

<success_criteria>
- No Dashboard surface asserts an integration's health.
- Nothing replaced the deleted row (D-07).
- The fabrication class was swept, and the sweep's precision is visible in a dropped-with-reason
  account.
- Every deferred hit names its owning phase; the one POLISH-04 residue is stated openly rather than
  implied closed.
</success_criteria>

<output>
Create `.planning/phases/120-polish-verified-defects/120-05-SUMMARY.md` when done.

**Do not commit.** Shared checkout on `master` with concurrent sessions; the orchestrator owns the
commit. Never `git add -A`, never `git commit --amend`.

**Every line number, count and code fragment in this plan is a DRAFT from a 2026-08-17 reading, not a
spec.** Re-run every search yourself and correct anything that does not match the live code, saying so
in the SUMMARY. In particular, if the `/infrastructure` integration health is NOT actually live, stop
and report — D-07's whole justification rests on it and deleting the hero row on a stale premise would
remove the app's only integration surface.
</output>
