---
phase: 120-polish-verified-defects
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/Analytics.tsx
  - src/pages/Dashboard.tsx
  - src/pages/Dreaming.tsx
  - src/pages/MeetingBot.tsx
  - src/pages/Memory.tsx
  - src/pages/Infrastructure.tsx
  - src/pages/HivePage.tsx
  - src/pages/Quality.tsx
  - src/pages/GraphsHub.tsx
  - src/pages/ForgePage.tsx
  - src/pages/WhatsApp.tsx
  - src/pages/hr/AgentAnalytics.tsx
  - src/pages/hr/Teams.tsx
  - src/pages/hr/Roster.tsx
  - src/pages/hr/Catalog.tsx
  - src/components/CallStatsBar.tsx
  - src/components/CallGraphPanel.tsx
  - src/components/OperatorScoreCard.tsx
  - src/components/ToolExecutionPanel.tsx
  - src/components/AgentTopology.tsx
  - src/components/ToolBreakdown.tsx
  - src/components/MetricCard.tsx
  - src/components/GitActivityWidget.tsx
  - src/components/FactsTable.tsx
  - src/components/EventFeed.tsx
  - src/components/DriftTimeline.tsx
  - src/components/DockerPanel.tsx
  - src/components/CommandTryItForm.tsx
  - src/components/AgentVoiceCard.tsx
  - src/components/ActiveSessions.tsx
  - src/components/hr/TeamEditor.tsx
  - src/components/hr/WizardShell.tsx
  - src/components/hr/TeamCard.tsx
  - src/components/hr/CatalogCard.tsx
  - src/components/hr/CatalogBrowser.tsx
  - src/components/forge/ForgeMetadataPanel.tsx
  - .planning/phases/120-polish-verified-defects/120-SWEEP-EVIDENCE.md
autonomous: true
requirements: [POLISH-01]

must_haves:
  truths:
    - "D-01: exactly the enumerated kill-list token is removed and nothing else — glow-card, shadow-[var(--glow-xs)], hover:shadow-[var(--glow-sm)] and hover:border-primary/50 survive untouched in the same class strings, because replacing them is TOKEN-01's job in Phase 122 and the app is accepted as still reading glowy after this phase"
    - "D-02: no refactor is performed — the repeated card class-string is NOT collapsed into GlassPanel or any other primitive, and no file gains or loses a component boundary"
    - "An operator hovering any card in the app sees no scale transform: a fixed-string search for hover:scale-[1.01] across src/ returns zero hits"
    - "No orphaned transition remains: transition-transform duration-300 survives ONLY on the lines that still carry another transform utility, and transition-all duration-300 is never removed because it governs surviving border/shadow hover states"
    - "The mobile-drawer and card-lift transforms that legitimately need transition-transform still animate — the sweep is proven not to have stripped a load-bearing transition"
  artifacts:
    - path: "src/components/hr/CatalogCard.tsx"
      provides: "The special-case proof: hover:scale removed while transition-transform survives for hover:-translate-y-1 and the sibling group-hover:scale-110 emoji"
      contains: "transition-transform duration-300"
    - path: "src/pages/Quality.tsx"
      provides: "The transition-all special case: scale removed, transition-all retained for the surviving hover:border-primary/50"
      contains: "transition-all duration-300"
  key_links:
    - from: "src/components/hr/TeamCard.tsx"
      to: "hover:-translate-y-1"
      via: "transition-transform duration-300 retained on the same element"
      pattern: "hover:-translate-y-1"
---

<objective>
Remove the unanimous kill-list's highest-volume item — `hover:scale-[1.01]` — and its co-located
duplicate `transition-transform duration-300` fragment, across 36 files.

Purpose: POLISH-01's success criterion 1 is a repo-wide grep returning zero live hits. This is the
single largest population (110 measured occurrences across 38 files) and it is mechanical. Removing
the scale without removing the now-purposeless transition would leave 110 sites of orphaned noise;
removing the transition where another transform still needs it would silently break a mobile drawer
and two card lifts.

Output: 36 files with the scale transform gone, the paired transition gone where it has no other
consumer, and both special-case families provably intact.

**Two of the 38 files carrying `hover:scale-[1.01]` are deliberately NOT in this plan** —
`src/pages/WarRoom.tsx` is owned by plan `120-03` and `src/components/HeroStatsBar.tsx` by plan
`120-05`, each of which removes its own occurrences alongside the other work it does in that file.
This is exclusive file ownership so wave-1 plans never edit the same file. Do not "helpfully" fix
them here; doing so creates a merge conflict with a concurrent plan.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/120-polish-verified-defects/120-CONTEXT.md
@CLAUDE.md

<interfaces>
<!-- MEASURED IN THIS REPO on 2026-08-17 with fixed-string git grep. Do not re-derive from prose. -->

`hover:scale-[1.01]` CONTAINS REGEX METACHARACTERS (`[`, `]`, `.`). Every search for it MUST use a
fixed-string matcher — `git grep -F`, `grep -F`, `rg -F`, or PowerShell `-SimpleMatch`. A
non-fixed-string pattern silently under-matches and will make a zero look like success.

MEASURED POPULATION (whole repo, src/ only, includes the two files this plan excludes):
  hover:scale-[1.01]                   110 occurrences / 38 files
  transition-transform duration-300    110 occurrences / 37 files
  lines carrying BOTH                  109
  lines carrying hover:scale + `transition-all duration-300`   8

THE THREE LINES WHERE `transition-transform duration-300` MUST SURVIVE (another transform utility
is present on the same element, so the transition still has a live consumer):
  src/components/hr/CatalogCard.tsx:28   — also has `hover:-translate-y-1`
  src/components/hr/TeamCard.tsx:26      — also has `hover:-translate-y-1`
  src/pages/WarRoom.tsx:296              — NOT THIS PLAN (120-03). Load-bearing for the F8 mobile
                                           room-list drawer: the template literal toggles
                                           `translate-x-0` / `-translate-x-full`, so removing the
                                           transition makes the drawer snap instead of slide.

THE EIGHT LINES CARRYING `transition-all duration-300` — never remove that class, it governs the
surviving `hover:border-primary/50` / `hover:shadow-[var(--glow-sm)]`:
  src/components/MetricCard.tsx:105
  src/components/hr/CatalogCard.tsx:28
  src/components/hr/TeamCard.tsx:26
  src/pages/Quality.tsx:44           — has NO `transition-transform` at all; only the scale goes
  src/pages/hr/AgentAnalytics.tsx:128, :172, :180, :204

A NEAR-MISS THAT MUST NOT BE TOUCHED:
  src/components/hr/CatalogCard.tsx:30 — `group-hover:scale-110 transition-transform duration-300`
  on the category emoji. This is NOT `hover:scale-[1.01]` and is not on the kill list. It is why
  CatalogCard is the one file whose two counts differ (1 scale / 2 transitions).

CLASSES THAT SURVIVE THIS PHASE (D-01) — leaving them is correct, not an oversight:
  glow-card, shadow-[var(--glow-xs)], hover:shadow-[var(--glow-sm)], hover:border-primary/50,
  hover:-translate-y-1, transition-colors, transition-all, backdrop-blur-md
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove the scale + its orphaned transition across the 34 straightforward files</name>

  <files>All files in this plan's `files_modified` EXCEPT src/components/hr/CatalogCard.tsx and src/components/hr/TeamCard.tsx</files>

  <read_first>
    - `.planning/phases/120-polish-verified-defects/120-CONTEXT.md` § D-01 and § D-02 — the restraint
      rule. Read it before your first edit; the temptation to also strip `glow-card` while you are in
      the class string is exactly what D-01 forbids.
    - `src/pages/Quality.tsx:44` — read this line before editing anything. It is the one line in this
      task carrying `transition-all duration-300` and no `transition-transform`, and it proves the
      transformation is per-line, not a blind two-token delete.
    - `src/components/MetricCard.tsx:105` and `src/pages/hr/AgentAnalytics.tsx:128` — the other shape
      you will meet: `transition-all duration-300` AND `transition-transform duration-300` on the same
      element. Only the latter goes.
  </read_first>

  <action>
Re-measure before editing, so the SUMMARY records a before figure you took yourself rather than one
copied from this plan: run a fixed-string count of `hover:scale-[1.01]` and of
`transition-transform duration-300` across `src/`, and record both numbers.

Then, for every line in this task's files that contains the fixed string `hover:scale-[1.01]`, apply
this per-line rule. Judge each line by reading it, not by pattern position:

1. Delete the token `hover:scale-[1.01]` and exactly one adjoining space, leaving no double space and
   no leading/trailing space inside the class string.
2. Then decide the transition separately. Delete `transition-transform duration-300` from that line
   ONLY IF the line contains no other transform utility. Treat any of `translate-`, `rotate-`,
   `skew-`, `group-hover:scale-`, or a second `scale-` as "another transform utility present" — in
   that case the transition stays.
3. NEVER delete `transition-all duration-300`, `transition-colors`, or `transition-opacity`. On the
   eight `transition-all` lines listed in `<interfaces>`, `transition-all` is what animates the
   `hover:border-primary/50` and `hover:shadow-[var(--glow-sm)]` that D-01 keeps; deleting it would
   turn a surviving hover state into an instant snap.
4. Change nothing else on the line. Do not reorder classes, do not reformat, do not remove
   `glow-card` or any `shadow-[var(--glow-*)]`.

A scripted transformation is acceptable and probably better than 108 hand-edits, but it must be
written against the rule above rather than a two-token blanket delete, and you must diff the result
before staging. If you script it, the script goes in the scratchpad, not the repo.

`src/pages/Analytics.tsx` carries 27 occurrences — the heaviest single file. Verify its post-edit
count is exactly 0 on its own, not just as part of the repo total, so a partial pass on the biggest
file cannot hide inside an aggregate.
  </action>

  <verify>
    <automated>npx tsc --noEmit && npx vitest run</automated>
  </verify>

  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0.
    - `npx vitest run` shows no new failures relative to the count you recorded before starting.
      Record both the before and after totals in the SUMMARY.
    - A fixed-string count of `hover:scale-[1.01]` across this task's files is 0. Use
      `git grep -cF 'hover:scale-[1.01]' -- <files>` or `grep -rF`; a pattern without the
      fixed-string flag does not count as evidence.
    - CONTROL, run in the same command: a fixed-string count of `glow-card` across the same files is
      unchanged from before the edit. This is what proves the sweep was surgical rather than greedy —
      a zero on the scale with a changed `glow-card` count means D-01 was violated.
    - `git grep -cF 'transition-all duration-300' -- src/components/MetricCard.tsx src/pages/Quality.tsx src/pages/hr/AgentAnalytics.tsx` is unchanged (1, 1, 4 respectively).
    - `git diff --stat` touches only files in this plan's `files_modified`, and neither
      `src/pages/WarRoom.tsx` nor `src/components/HeroStatsBar.tsx` appears.
  </acceptance_criteria>

  <done>No card in these 34 files scales on hover, no transition is left with nothing to animate, and every surviving glow/border class is byte-identical to before.</done>
</task>

<task type="auto">
  <name>Task 2: The two lift cards — scale removed, transition-transform retained</name>

  <files>src/components/hr/CatalogCard.tsx, src/components/hr/TeamCard.tsx</files>

  <read_first>
    - `src/components/hr/CatalogCard.tsx` lines 28 and 30 together. Line 28 is the card wrapper
      (`hover:-translate-y-1 hover:scale-[1.01] transition-transform duration-300`); line 30 is the
      category emoji (`group-hover:scale-110 transition-transform duration-300`). Neither
      `transition-transform` may be removed and line 30 must not be edited at all.
    - `src/components/hr/TeamCard.tsx:26` — same wrapper shape, same `hover:-translate-y-1`.
  </read_first>

  <action>
On `src/components/hr/CatalogCard.tsx:28` and `src/components/hr/TeamCard.tsx:26`, remove ONLY the
`hover:scale-[1.01]` token. Leave `hover:-translate-y-1`, `transition-transform duration-300` and
`transition-all duration-300` exactly as they are.

`hover:-translate-y-1` is a card lift, not a scale. It is not on the enumerated kill list, so D-01
keeps it — and because it keeps it, `transition-transform duration-300` on these two lines still has
a live consumer and is not orphaned noise. Say so in a short inline comment on each line's component
(one comment per file, above the returned element, naming D-01) so a Phase 122 reviewer does not read
the surviving transition as a missed sweep.

Do not touch `src/components/hr/CatalogCard.tsx:30`.
  </action>

  <verify>
    <automated>npx tsc --noEmit && npx vitest run src/components/hr</automated>
  </verify>

  <acceptance_criteria>
    - `git grep -cF 'hover:scale-[1.01]' -- src/components/hr/CatalogCard.tsx src/components/hr/TeamCard.tsx` returns 0 for both.
    - `git grep -cF 'transition-transform duration-300' -- src/components/hr/CatalogCard.tsx` is
      exactly 2 (unchanged), and the same count for `TeamCard.tsx` is exactly 1 (unchanged).
    - `git grep -cF 'hover:-translate-y-1' -- src/components/hr/CatalogCard.tsx src/components/hr/TeamCard.tsx` is 1 for each — the lift survived.
    - `git diff src/components/hr/CatalogCard.tsx` shows line 30 unchanged.
  </acceptance_criteria>

  <done>Both lift cards no longer scale, still lift smoothly, and the emoji hover on CatalogCard is untouched.</done>
</task>

<task type="auto">
  <name>Task 3: Prove the sweep repo-wide and hand the residue to the next plans</name>

  <files>.planning/phases/120-polish-verified-defects/120-SWEEP-EVIDENCE.md</files>

  <read_first>
    - The before/after counts you recorded in Tasks 1 and 2.
  </read_first>

  <action>
Create `.planning/phases/120-polish-verified-defects/120-SWEEP-EVIDENCE.md` recording, as raw command
output pasted verbatim rather than prose summary:

1. The fixed-string `hover:scale-[1.01]` count across all of `src/` AFTER this plan. It will NOT be
   zero — it will be exactly the residue in `src/pages/WarRoom.tsx` (2) and
   `src/components/HeroStatsBar.tsx` (1), i.e. 3. State that number and name the two plans that own
   it (`120-03`, `120-05`). POLISH-01's "zero live hits" criterion is closed by those plans, not this
   one; recording 3 here honestly is the point, and writing "zero" would be a false green.
2. The `transition-transform duration-300` count after this plan, with the surviving lines named and
   each one's justification in a single clause (CatalogCard:28 / TeamCard:26 — `hover:-translate-y-1`;
   CatalogCard:30 — `group-hover:scale-110`; WarRoom:296 — the F8 drawer slide, owned by 120-03).
3. The unchanged `glow-card` control count, labelled as the control and stating what it rules out.
4. Any line where you judged the rule ambiguous and what you decided.

This file is OWNED BY THIS PLAN ALONE. Four wave-1 plans run in parallel against a shared git
checkout, so no two of them may write the same artifact — 120-02 writes `120-SHELL-EVIDENCE.md`,
120-03 writes `120-SANCTIONED-PATTERNS.md`, and 120-05 writes `120-FABRICATION-INVENTORY.md`. Do not
append to any of those and do not expect them to exist when you run.
  </action>

  <verify>
    <automated>test -s .planning/phases/120-polish-verified-defects/120-SWEEP-EVIDENCE.md && grep -cF 'hover:scale-[1.01]' .planning/phases/120-polish-verified-defects/120-SWEEP-EVIDENCE.md</automated>
  </verify>

  <acceptance_criteria>
    - The file exists and contains a literal command line and its literal output for each of the four
      items, not a re-typed number.
    - The residual count is stated as a number with the owning plan named next to it. A claim of
      "zero" repo-wide at this point is wrong and is a blocking defect.
  </acceptance_criteria>

  <done>The sweep's result is recorded as measured output with its residue attributed, so the phase-level "zero hits" claim can be assembled from evidence rather than assumed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none crossed | This plan changes only Tailwind class strings in already-rendered components. No input parsing, no network, no persistence, no new dependency. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-120-01 | Tampering | A scripted class-string rewrite across 34 files silently altering an unrelated class | mitigate | Per-line rule read against each line, plus the unchanged-`glow-card` control count and a `git diff --stat` restricted to `files_modified` |
| T-120-02 | Denial of Service | Removing a transition that a layout animation depends on, leaving a drawer or overlay visually broken | mitigate | The three transform-carrying lines are enumerated in `<interfaces>`; two are handled by name in Task 2 and the third is excluded from this plan entirely |
| T-120-SC | Tampering | npm/pip/cargo installs | mitigate | No packages are added by this plan. If any install becomes necessary, stop and run the Package Legitimacy Gate before proceeding. |
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0.
- `npx vitest run` shows no new failures against the pre-plan baseline (record both counts).
- Fixed-string `hover:scale-[1.01]` count across this plan's 36 files is 0, with the `glow-card`
  control count unchanged in the same run.
- `transition-transform duration-300` survives on exactly CatalogCard:28, CatalogCard:30 and
  TeamCard:26 within this plan's files, each with a named live consumer.
- `120-SWEEP-EVIDENCE.md` records the residual 3 hits and attributes them to 120-03 and 120-05.
</verification>

<success_criteria>
- No card in the 36 files scales on hover.
- No orphaned `transition-transform duration-300` remains; no load-bearing one was removed.
- `glow-card` / `shadow-[var(--glow-*)]` / `hover:border-primary/50` counts are unchanged (D-01).
- No component boundary was introduced or removed (D-02).
</success_criteria>

<output>
Create `.planning/phases/120-polish-verified-defects/120-01-SUMMARY.md` when done.

**Do not commit.** This is a shared checkout on `master` with concurrent sessions; the orchestrator
owns the commit for this phase. Never `git add -A` and never `git commit --amend` here.

**Treat every class string quoted in this plan as a DRAFT taken from a 2026-08-17 reading of the
repo, not as a spec.** Lines move in a shared checkout. If a cited line does not say what this plan
reports, the live code is right and this plan is stale — correct it, apply the rule to what is
actually there, and record the discrepancy in the SUMMARY.
</output>
