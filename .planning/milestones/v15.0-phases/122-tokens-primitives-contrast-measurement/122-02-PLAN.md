---
phase: 122-tokens-primitives-contrast-measurement
plan: 02
type: execute
wave: 1
depends_on: ["122-01"]
files_modified:
  - src/index.css
  - .planning/phases/122-tokens-primitives-contrast-measurement/122-RAMP-DERIVATION.md
autonomous: true
requirements: [TOKEN-01]

must_haves:
  truths:
    - "D-01: the surface ramp is the source and --background/--card/--popover/--border become var() aliases of it, so all 547 existing call sites and all 30 shadcn primitives inherit for free"
    - "D-03: cyan takes the winning ramp verbatim from sources/themes/default.css; readable and aubergine are hand-tuned against MEASURED contrast; emerald and amber are derived mechanically from their existing surfaces"
    - "D-04: amber receives the full surface ramp so TOKEN-01's 'all 5 themes' is literally true, but is NOT added to ThemeSwitcher and NOT added to the contrast matrix"
    - "Every one of the five theme blocks defines its own complete --surface-0/1/2/3 + --hairline + --hairline-strong; none silently inherits another theme's ramp through the shared .dark selector list"
    - "The four surface steps are DISTINCT values within each theme, not four aliases of one colour"
  artifacts:
    - path: "src/index.css"
      provides: "Per-theme surface ramps and the four semantic aliases pointing at them"
      contains: "--surface-0"
    - path: ".planning/phases/122-tokens-primitives-contrast-measurement/122-RAMP-DERIVATION.md"
      provides: "Per-theme derivation record: source of each value, and the measured contrast for readable/aubergine"
  key_links:
    - from: "src/index.css [data-theme] blocks"
      to: "--surface-0/1/2/3"
      via: "var() alias on --background/--card/--popover/--border"
      pattern: "--background: var\\(--surface-0\\)"
---

<objective>
Give all five themes a layered surface ramp (`--surface-0/1/2/3`, `--hairline`, `--hairline-strong`)
and re-point the existing shadcn semantic tokens at it, so every `bg-background` / `bg-card` /
`bg-popover` / `border-border` call site in the app -- and all 30 shadcn primitives in
`src/components/ui/` that hardcode those classes internally -- starts reading the Borealis ramp with
zero call-site churn.

Purpose: this is the highest-leverage edit in the milestone. D-01 rejected migrating 547 call sites
to `bg-[var(--surface-N)]` precisely because the alias buys the same result for one file's work.
Output: `src/index.css` carries five complete ramps; `122-RAMP-DERIVATION.md` records where each
number came from and what was measured.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/122-tokens-primitives-contrast-measurement/122-CONTEXT.md
@.planning/phases/122-tokens-primitives-contrast-measurement/122-RESEARCH.md
@.claude/skills/sketch-findings-codepulse/SKILL.md
@.claude/skills/sketch-findings-codepulse/sources/themes/default.css
@CLAUDE.md

<interfaces>
STRUCTURAL FACTS about `src/index.css` (733 lines) that this edit must not break. Verify each one
yourself by reading the file before editing -- these are stated to save you the search, not to be
trusted blind:

- `@theme` at `:12` (font namespaces only). `@theme inline` at `:17` -- already contains
  `--color-background: var(--background)`, `--color-card: var(--card)`,
  `--color-popover: var(--popover)`, `--color-border: var(--border)`. Because these are `inline`,
  the compiled utility embeds the LIVE `var()` reference, so a `[data-theme]` override resolves at
  runtime. **This block needs NO change** -- `--surface-*` are pure intermediate custom properties
  and never need Tailwind utility registration (RESEARCH.md Pattern 1).
- `:root` at `:62` -- the LIGHT monochrome palette (`--card: oklch(1 0 0)` etc.). Do not repoint
  `:root`; the light palette is not part of TOKEN-01's five themes.
- `:134` is `.dark,` and `:135` is `[data-theme="cyan"] {` -- **one shared selector list, not two
  blocks.** `index.html`'s pre-paint script ALWAYS runs `documentElement.classList.add('dark')`, so
  this block is the dark baseline for EVERY theme, and the four other theme blocks (`:196` emerald,
  `:218` amber, `:237` readable, `:303` aubergine) override it at equal specificity by source order.
  Consequence you must handle: `emerald` and `amber` today declare NO `--card`, `--popover` or
  `--border` of their own -- they silently inherit cyan's `#0a0a0c` / `#0a0a0c` / `#1e1e24` through
  this shared list. If you add a ramp only to the shared block, emerald and amber will inherit CYAN's
  surfaces. Every theme must therefore declare its OWN complete ramp.

Existing per-theme surface values, read from the file this session:
- cyan (`.dark, [data-theme="cyan"]`): `--background:#040405`, `--card:#0a0a0c`, `--popover:#0a0a0c`, `--border:#1e1e24`
- emerald: `--background:#020617`; no `--card`/`--popover`/`--border` of its own
- amber:   `--background:#0a0a0a`; no `--card`/`--popover`/`--border` of its own
- readable: `--background:#111318`, `--card:#181c24`, `--popover:#1d2130`, `--border:#2a3144`
- aubergine: `--background:#120d18`, `--card:#1a1324`, `--popover:#1e172a`, `--border:#2e2040`

The winning ramp, verbatim from `.claude/skills/sketch-findings-codepulse/sources/themes/default.css`:
`--surface-0:#05060a` (page), `--surface-1:#0b0d12` (card), `--surface-2:#12151c` (raised/popover),
`--surface-3:#191d26` (hover on raised), `--hairline:#1c2029`, `--hairline-strong:#2a2f3b`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define the surface ramp in all five theme blocks</name>
  <files>src/index.css</files>
  <read_first>
    - src/index.css (the file being modified -- at minimum lines 1-30, 62-135, and each of the five theme blocks in full)
    - .claude/skills/sketch-findings-codepulse/sources/themes/default.css (the source of cyan's verbatim ramp)
    - .planning/phases/122-tokens-primitives-contrast-measurement/122-CONTEXT.md (D-01, D-03, D-04)
  </read_first>
  <action>
    Add six declarations -- `--surface-0`, `--surface-1`, `--surface-2`, `--surface-3`, `--hairline`,
    `--hairline-strong` -- to EACH of the five theme blocks, at the top of each block with a short
    comment naming the derivation source. Do not add them to `:root` (the light palette is not in
    TOKEN-01's scope) and do not add them to `@theme` or `@theme inline` (they are consumed only
    through the existing `--background`/`--card`/`--popover`/`--border` aliases, per RESEARCH.md
    Pattern 1).

    Per-theme derivation, per D-03 -- effort goes where the constraint is:

    - **cyan** (`.dark, [data-theme="cyan"]`): the winning ramp VERBATIM, no adjustment --
      `#05060a` / `#0b0d12` / `#12151c` / `#191d26`, hairline `#1c2029`, hairline-strong `#2a2f3b`.
      These are darker than today's `#040405`/`#0a0a0c`/`#1e1e24` on some steps and lighter on others;
      that is the intended visual change, not drift.
    - **emerald**: derive MECHANICALLY from its existing `--background: #020617` -- keep that exact
      hue/chroma direction and step lightness upward in even increments to produce four distinct
      steps, then a hairline one step above `--surface-3`'s neighbourhood. Anchor `--surface-0` to
      `#020617` itself so the page background does not shift. Show the arithmetic in the derivation
      artifact.
    - **amber**: same mechanical derivation anchored on its existing `--background: #0a0a0a`
      (a true neutral, so the ramp stays neutral). D-04: amber gets the full token set so the block
      never rots, but is NOT added to `ThemeSwitcher` and NOT added to the contrast matrix. Do not
      touch `src/components/ThemeSwitcher.tsx` in this plan.
    - **readable**: HAND-TUNED. It is the WCAG-AA-constrained theme and a mechanical lightness step
      would flatten that tuning. Start from its existing `#111318` / `#181c24` / `#1d2130` / `#2a3144`
      as surface-0/1/2/border, invent a fourth step (`--surface-3`) above `--surface-2`, and set
      `--hairline` from the existing `#2a3144`. Every value must be MEASURED, not asserted -- see the
      measurement rule in Task 2.
    - **aubergine**: HAND-TUNED. Same approach anchored on `#120d18` / `#1a1324` / `#1e172a` /
      `#2e2040`, preserving the deliberate violet cast of the editorial palette. Do not neutralise it
      toward the cyan ramp.

    Constraint that binds every theme: the four surface steps must be DISTINCT values within that
    theme. Four aliases of one colour satisfies the grep and destroys the layering, and D-27's
    rendered-result probe exists specifically to catch it.
  </action>
  <acceptance_criteria>
    - `grep -c -- '--surface-0:' src/index.css` returns 5 (one per theme block; unit = matching LINES, and each theme declares it once, so lines == themes here -- confirm by also printing `grep -n -- '--surface-0:' src/index.css` and checking the five line numbers each fall inside a different theme block).
    - Same for `--surface-1:`, `--surface-2:`, `--surface-3:`, `--hairline:`, `--hairline-strong:` -- each returns 5.
    - `grep -n -- '--surface-0:' src/index.css` shows one hit at or just after each of: the `.dark, [data-theme="cyan"]` block, `[data-theme="emerald"]`, `[data-theme="amber"]`, `[data-theme="readable"]`, `[data-theme="aubergine"]`. Print the block each hit lands in.
    - Within each theme, the four `--surface-N` values are four DIFFERENT strings. Extract all 20 values and assert 5 sets of 4 distinct values -- a set of size < 4 in any theme fails this task.
    - cyan's four values are byte-identical to `default.css`'s: verify with `grep -F '#05060a' src/index.css`, `grep -F '#0b0d12'`, `grep -F '#12151c'`, `grep -F '#191d26'`, `grep -F '#1c2029'`, `grep -F '#2a2f3b'` -- each returning at least 1. Use `-F`; these literals contain `#`.
    - `src/components/ThemeSwitcher.tsx` is unchanged: `git diff --name-only` for this task does NOT list it.
    - `npm run build` exits 0 (Tailwind compiles the stylesheet; a malformed block fails the build).
  </acceptance_criteria>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>All five theme blocks declare a complete, internally distinct six-token surface ramp, with cyan's values verbatim from the winning theme file.</done>
</task>

<task type="auto">
  <name>Task 2: Re-point the semantic tokens at the ramp and record the derivation</name>
  <files>src/index.css, .planning/phases/122-tokens-primitives-contrast-measurement/122-RAMP-DERIVATION.md</files>
  <read_first>
    - src/index.css (as edited by Task 1)
    - .planning/phases/122-tokens-primitives-contrast-measurement/122-CONTEXT.md (D-01's exact alias mapping)
    - .planning/phases/120-polish-verified-defects/120-DESIGN-REVIEW-HANDOFF.md (the rasterise-don't-scrape rule and why)
  </read_first>
  <action>
    In EACH of the five theme blocks, replace the literal `--background`, `--card`, `--popover` and
    `--border` declarations with the four aliases D-01 specifies: `--background: var(--surface-0)`,
    `--card: var(--surface-1)`, `--popover: var(--surface-2)`, `--border: var(--hairline)`.

    For `emerald` and `amber`, which today declare no `--card`/`--popover`/`--border` of their own,
    ADD all four aliases. Without them those themes keep inheriting cyan's literals through the shared
    `.dark` selector list and the ramp would be decorative.

    Also re-point `--input` to `var(--hairline)` in every theme that declares it -- it currently
    carries the same literal as `--border` in cyan/readable/aubergine, and leaving it behind would
    reintroduce the exact drift TOKEN-01 exists to end. Leave `--sidebar-border` alone in this plan:
    Phase 124 owns the sidebar and D-17 draws that boundary.

    Then measure, do not assert. For `readable` and `aubergine` -- the two hand-tuned themes -- prove
    the ramp meets its constraint by RASTERISING, never by parsing a computed colour string: assign
    the colour to `canvas.fillStyle`, `fillRect`, read the pixel back with `getImageData`. Tailwind v4
    emits `oklch()`, and a regex scrape reads the hue angle as a colour channel (the tell in Phase 120
    was an impossible `rgb(0,0,262)`). `canvas.fillStyle` silently keeps its PRIOR value on
    unparseable input, so set a magenta sentinel first and return null rather than a guess if the
    sentinel survives. A one-off Node/Playwright script is fine here; the durable version is D-27's
    spec in plan 122-18.

    Write `122-RAMP-DERIVATION.md` recording, per theme: each of the six values, its DERIVATION
    SOURCE (verbatim / mechanical arithmetic shown / hand-tuned), and for readable + aubergine the
    measured contrast of `--foreground` against `--surface-1` with the ratio and the method named.
    Record explicitly that amber's ramp exists but is unmeasurable against a rendered page because it
    is unreachable from `ThemeSwitcher` (D-04) -- that is a stated limitation, not an omission.
  </action>
  <acceptance_criteria>
    - `grep -cF -- '--background: var(--surface-0)' src/index.css` returns 5.
    - `grep -cF -- '--card: var(--surface-1)' src/index.css` returns 5.
    - `grep -cF -- '--popover: var(--surface-2)' src/index.css` returns 5.
    - `grep -cF -- '--border: var(--hairline)' src/index.css` returns 5.
    - CONTROL that no literal survived: inside the five theme blocks, `grep -nE -- '--(background|card|popover|border):\s*#' src/index.css` returns ZERO hits at line numbers >= the `.dark,` line. Hits inside `:root` (line 62-133) are expected and correct -- print the line numbers and account for each, do not accept a bare count.
    - The measured contrast figures for readable and aubergine in `122-RAMP-DERIVATION.md` were produced by a rasterised probe. The script must print, for one control colour whose sRGB value is known independently (use `#ffffff` -> expect exactly 255,255,255), the round-tripped pixel -- if that control does not come back 255,255,255 the probe is broken and no other number from it may be quoted.
    - `npx tsc --noEmit` exits 0 and `npm run build` exits 0.
    - `npx vitest run` shows no NEW failures against the pre-plan baseline. Record both the baseline and the post-edit figures (files passed / tests passed / failed) as numbers.
  </acceptance_criteria>
  <verify>
    <automated>npm run build && npx vitest run</automated>
  </verify>
  <done>All four semantic surface tokens alias the ramp in every theme, `--input` follows `--hairline`, and the derivation of every value is recorded with the hand-tuned themes' contrast measured by rasterisation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none introduced | This plan edits one stylesheet. No input crosses any boundary; no data is read, written or transmitted. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-122-02-A | Information Disclosure | contrast/visibility of security-relevant UI | accept | A surface-token change could in principle reduce the legibility of a warning or error state. Accepted here rather than mitigated because it is measured, not assumed: plan 122-01 captured the before-matrix and plan 122-19 captures the after-matrix, so a regression is detectable by delta rather than by review. |
| T-122-02-SC | Tampering | npm/pip/cargo installs | mitigate | Zero packages installed. RESEARCH.md's Package Legitimacy Audit records zero new packages for this phase; nothing here changes that. |

RESEARCH.md's assessment stands: no ASVS category applies. This is frontend CSS with zero new input
surfaces, zero auth changes, zero external data ingestion and zero new dependencies. No threat was
found that the research missed.
</threat_model>

<verification>
- `npm run build` exits 0
- `npx tsc --noEmit` exits 0
- `npx vitest run` at or above the recorded pre-plan baseline, zero new failures
- Six ramp tokens x five theme blocks present, each theme's four surface steps distinct
- Four aliases present five times each; zero surviving surface hex literals below the `.dark,` line
- `ThemeSwitcher.tsx` untouched
- `122-RAMP-DERIVATION.md` records source and, for readable/aubergine, a rasterised measurement with its own known-value control
</verification>

<success_criteria>
Every surface in the app reads a per-theme layered ramp instead of a scattered literal, achieved by
editing one file, with the derivation of all 30 values (5 themes x 6 tokens) recorded and the two
constrained themes' contrast measured rather than asserted.
</success_criteria>

<output>
Create `.planning/phases/122-tokens-primitives-contrast-measurement/122-02-SUMMARY.md` when done
</output>
