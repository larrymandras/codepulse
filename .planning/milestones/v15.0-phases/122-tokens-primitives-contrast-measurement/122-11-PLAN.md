---
phase: 122-tokens-primitives-contrast-measurement
plan: 11
type: execute
wave: 4
depends_on: ["122-03"]
files_modified:
  - src/components/PageHeader.tsx
  - src/components/PageHeader.test.tsx
  - src/pages/Analytics.tsx
  - src/pages/BuildProgress.tsx
  - .planning/phases/122-tokens-primitives-contrast-measurement/122-PAGEHEADER-ADOPTION.md
autonomous: true
requirements: [TOKEN-05]

must_haves:
  truths:
    - "D-17: PageHeader is the PAGE layer only -- title, optional 11px mono uppercase eyebrow, optional subtitle, actions slot -- and Phase 124's 48px 3-zone app chrome sits ABOVE it, so neither phase redoes the other's work"
    - "D-18: adoption is complete with NAMED exemptions -- every surface that deliberately has no header is recorded with its reason, never left silently unconverted, so 'every route' means something checkable"
    - "The corrected adoption figure is 38 of 42 source pages plus all 5 subdirectory pages; the earlier 44-of-62 figure mixed 20 test files into both numerator and denominator and must not be propagated"
    - "PageHeader reads the new surface, hairline and type tokens and carries no hardcoded colour"
  artifacts:
    - path: "src/components/PageHeader.tsx"
      provides: "Page-layer header contract: title, eyebrow, subtitle, icon, actions"
      exports: ["PageHeader"]
    - path: ".planning/phases/122-tokens-primitives-contrast-measurement/122-PAGEHEADER-ADOPTION.md"
      provides: "The re-derived adoption count with its unit, and the named exemption register with a reason per exemption"
  key_links:
    - from: "src/pages/Analytics.tsx"
      to: "src/components/PageHeader.tsx"
      via: "import and render, replacing the hand-rolled h1"
      pattern: "PageHeader"
---

<objective>
Grow `PageHeader` into the page-layer contract TOKEN-05 requires -- eyebrow, subtitle, token-driven
type and surface -- convert the two pages that hand-rolled what it already produces, and record the
remaining exemptions by name with their reasons.

Purpose: TOKEN-05 is mostly a CONTRACT problem, not a rollout problem -- 38 of 42 source pages and
all 5 subdirectory pages already use it. What is missing is the contract itself and an honest account
of the four that do not.
Output: an extended `PageHeader` with tests, two pages converted, and a named exemption register.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/122-tokens-primitives-contrast-measurement/122-CONTEXT.md
@.planning/phases/122-tokens-primitives-contrast-measurement/122-PATTERNS.md
@.claude/skills/sketch-findings-codepulse/SKILL.md
@src/index.css
@CLAUDE.md

<interfaces>
### `src/components/PageHeader.tsx` today -- the ENTIRE file is 23 lines

```
interface PageHeaderProps {
  title: React.ReactNode;
  icon?: LucideIcon;
  actions?: React.ReactNode;
  className?: string;
}
```
It renders `flex items-center justify-between mb-4`, an optional `h-6 w-6` icon, an
`h1.text-2xl.font-bold.text-foreground`, and the `actions` slot. D-17 grows this ADDITIVELY -- every
existing prop must survive, because 43 files render it.

### The four non-adopting pages, each confirmed by direct read (`122-PATTERNS.md` §7)

- `src/pages/Analytics.tsx:54` -- `<h1 className="text-2xl font-bold text-foreground">Analytics</h1>`,
  no `PageHeader` import anywhere in the file. Uses `SectionHeader` (a different, lower-tier
  component) for its two subsection dividers at `:177` and `:231` -- those are NOT page headers and
  stay.
- `src/pages/BuildProgress.tsx:24` -- `<h1 className="text-2xl font-bold text-foreground mb-4">Build
  Progress</h1>`. That class string is byte-identical to what `PageHeader` already generates plus a
  separate `mb-4`, i.e. this page hand-rolled the component. Simplest of the four.
- `src/pages/Chat.tsx:928` -- `<h1 className="font-mono font-bold tracking-[0.15em]
  text-base">ASTRIDR</h1>`, a small mono brand wordmark deep inside a voice/avatar section. This is
  NOT a page title competing with `PageHeader`; it is unrelated brand chrome on a full-bleed presence
  view. CONTEXT.md names Chat as the likely genuine exemption.
- `src/pages/ForgePage.tsx:151` -- `<h1 className="text-2xl font-bold text-foreground">Forge</h1>`,
  no `PageHeader` import.

`ForgePage.tsx` is deliberately NOT in this plan's `files_modified`: plan 122-08 (an earlier wave)
owns its colour sweep, and its header conversion is folded into this plan's exemption register as an
explicit decision rather than a second edit to the same file in the same wave. Decide in Task 3
whether it converts, and if it does, say which plan carries the edit.

### Counting discipline (D-18's own correction)
`38 of 42` is the corrected figure, re-derived excluding `.test.tsx`. An earlier figure of `44 of 62`
mixed the 20 test files into BOTH numerator and denominator. Do not propagate 44/62. Re-derive both
numbers yourself and name the unit (FILES).

### The boundary with Phase 124 (D-17, load-bearing)
Phase 124 (SHELL-01) builds a 48px 3-zone bar -- breadcrumb / command bar / system chip + E-Stop +
overflow -- that sits ABOVE this header as APP chrome. Do NOT build any of that here. Breadcrumb,
command bar and E-Stop are app-level concerns and 124 would inherit a header shaped before its own
discussion. Two distinct layers, no overlap.

Likewise SIGNAL-03 (Astridr's italic serif voice) is explicitly a later single-surface trial. D-17's
eyebrow and type work must not pre-empt it -- the eyebrow is 11px mono uppercase, not a serif.

### Design law for the type this header carries
One 11px mono uppercase eyebrow style. Geist sentence case for UI; JetBrains Mono for data only.
Hero numerals 40px/300/tabular (not this component's job). Depth via shadow, never glow.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend PageHeader with the eyebrow, subtitle and token-driven surface</name>
  <files>src/components/PageHeader.tsx, src/components/PageHeader.test.tsx</files>
  <read_first>
    - src/components/PageHeader.tsx (read the whole 23-line file -- it is the entire current contract)
    - src/index.css (the surface, hairline and font tokens now available)
    - .planning/phases/122-tokens-primitives-contrast-measurement/122-CONTEXT.md (D-17 and its Phase 124 boundary)
    - .claude/skills/sketch-findings-codepulse/SKILL.md (the type law: one 11px mono uppercase eyebrow)
  </read_first>
  <behavior>
    - Every existing prop still works unchanged: `title`, `icon`, `actions`, `className` -- 43 files render this component and none may need editing
    - A new optional `eyebrow` prop renders an 11px mono uppercase line above the title
    - A new optional `subtitle` prop renders below the title in muted ink
    - With neither `eyebrow` nor `subtitle` supplied, the rendered output is structurally equivalent to today's -- no stray empty elements, no layout shift
    - The component contains no hex literal and no raw palette class
    - It renders no breadcrumb, no command bar and no E-Stop -- those belong to Phase 124
  </behavior>
  <action>
    Extend `PageHeader.tsx` additively. Add optional `eyebrow` and `subtitle` props alongside the four
    existing ones. The eyebrow is the single 11px mono uppercase style the design law fixes -- use the
    mono font token and a muted ink token, with letter-spacing consistent with the mono eyebrow
    treatment. The subtitle sits below the title in `text-muted-foreground`.

    Everything colour reads a token: `text-foreground`, `text-muted-foreground`, and where the header
    carries a bottom rule, `border-border` (which now aliases `--hairline`). No hex, no palette class,
    no `glow-*`.

    Do not add a breadcrumb, a command bar, an E-Stop or a system chip. D-17 draws that line and
    Phase 124 owns everything above it.

    Do not introduce a serif anywhere. SIGNAL-03 is a later single-surface trial and this component
    must not pre-empt it.

    Create `src/components/PageHeader.test.tsx` (none exists today) covering every behavior line, RED
    before GREEN. Follow the repo's per-file mocking convention.
  </action>
  <acceptance_criteria>
    - `npx vitest run src/components/PageHeader.test.tsx` passes with a named test per behavior line.
    - BACKWARD-COMPATIBILITY CONTROL: a test renders `PageHeader` with ONLY the four pre-existing props and asserts the output contains the title text, the icon and the actions -- and additionally asserts no eyebrow or subtitle element is present. Without the second half, a component that always renders an empty eyebrow would pass.
    - A source-level test (comment lines stripped first, so the file's own prose cannot self-invalidate the gate) asserts `PageHeader.tsx` contains no `#` colour literal and no `/-(slate|zinc|gray|neutral|stone)-[0-9]/` class.
    - A source-level test asserts the file contains no breadcrumb, command-bar or E-Stop markup -- the Phase 124 boundary made checkable rather than only stated.
    - Full `npx vitest run` shows zero NEW failures against the recorded baseline. 43 files render this component; record baseline and post-edit figures as numbers.
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/components/PageHeader.test.tsx</automated>
  </verify>
  <done>`PageHeader` carries the page-layer contract with an eyebrow and subtitle, breaks no existing caller, holds no hardcoded colour, and provably contains none of Phase 124's app chrome.</done>
</task>

<task type="auto">
  <name>Task 2: Convert the two pages that hand-rolled the header</name>
  <files>src/pages/Analytics.tsx, src/pages/BuildProgress.tsx</files>
  <read_first>
    - src/pages/BuildProgress.tsx (the `h1` at `:24` and its surrounding layout)
    - src/pages/Analytics.tsx (the `h1` at `:54`, and `SectionHeader`'s two uses at `:177`/`:231` which are NOT page headers)
    - src/components/PageHeader.tsx (as extended by Task 1)
    - src/components/SectionHeader.tsx (so the distinction between the two components is clear before editing)
    - .planning/phases/121-analytics-query-resilience/121-VERIFICATION.md (what Phase 121 established about Analytics.tsx's structure -- it was stripped of all fetching and split across boundaries; do not undo that)
  </read_first>
  <action>
    Replace `BuildProgress.tsx:24`'s hand-rolled `h1` with `PageHeader`. Its class string is
    byte-identical to what the component already generates plus a separate `mb-4`, so this is a
    straight substitution -- preserve the `mb-4` spacing by whatever means the component offers
    (`className` passthrough) rather than dropping it.

    Replace `Analytics.tsx:54`'s hand-rolled `h1` with `PageHeader`. Leave `SectionHeader` at `:177`
    and `:231` exactly as they are -- they are subsection dividers, a different tier, and converting
    them would be a scope overrun.

    `Analytics.tsx` is load-bearing: Phase 121 stripped it of all fetching, split its Summary Row
    across four `SectionErrorBoundary` boundaries, and shipped an AST-derived structural ratchet
    (`src/pages/Analytics.structuralGuard.test.ts`) that FAILS if a query-shaped hook reappears in the
    page's own function body or if an element is left unwrapped. Do not hoist anything, do not add a
    hook, do not move an element out of a boundary. Run that ratchet specifically after editing.
  </action>
  <acceptance_criteria>
    - `git grep -cF 'PageHeader' -- src/pages/Analytics.tsx` returns at least 2 (import plus render), same for `src/pages/BuildProgress.tsx`.
    - `git grep -cF '<h1' -- src/pages/Analytics.tsx src/pages/BuildProgress.tsx` returns 0.
    - `git grep -cF 'SectionHeader' -- src/pages/Analytics.tsx` is UNCHANGED from its pre-edit value. State both numbers -- the subsection dividers must survive.
    - `npx vitest run src/pages/Analytics.structuralGuard.test.ts` passes. This is Phase 121's ratchet and it is the specific guard this edit could trip.
    - CONTROL that the ratchet is still capable of failing: it ships its own synthetic-mutation tests. Confirm those mutation cases still report RED in the same run -- a ratchet that passes because it stopped analysing is worse than none.
    - `npx tsc --noEmit` exits 0; full `npx vitest run` shows zero new failures against the recorded baseline.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/pages/Analytics.structuralGuard.test.ts && npx tsc --noEmit</automated>
  </verify>
  <done>Analytics and BuildProgress render the shared header, Analytics' subsection dividers and its Phase 121 boundary structure are intact, and the structural ratchet still both passes and can still fail.</done>
</task>

<task type="auto">
  <name>Task 3: Re-derive adoption and write the named exemption register</name>
  <files>.planning/phases/122-tokens-primitives-contrast-measurement/122-PAGEHEADER-ADOPTION.md</files>
  <read_first>
    - src/pages/Chat.tsx around `:928` (the mono brand wordmark -- read enough surrounding JSX to state what it actually is)
    - src/pages/ForgePage.tsx around `:151` (the hand-rolled `h1`)
    - .planning/phases/122-tokens-primitives-contrast-measurement/122-CONTEXT.md (D-18 and its 44/62 correction)
  </read_first>
  <action>
    Re-derive the adoption figure yourself and name its unit. Denominator: `ls src/pages/*.tsx |
    grep -v '\.test\.' | wc -l` -- that is a FILES count. Numerator: the subset importing
    `PageHeader`. Do the same separately for `src/pages/*/*.tsx`. Report four numbers, each labelled.
    Do not propagate `44 of 62`; that figure mixed the 20 test files into both halves.

    Write `122-PAGEHEADER-ADOPTION.md` with:
    1. The re-derived adoption figures, before and after Task 2, with units named.
    2. The NAMED EXEMPTION REGISTER -- one entry per page that deliberately has no `PageHeader`, each
       with its reason. `Chat.tsx` is the expected entry: it is a full-bleed presence view and its
       `h1` at `:928` is a mono brand wordmark inside a voice/avatar section, not a page title. State
       that as the reason rather than "by design".
    3. A DECISION on `ForgePage.tsx:151`. It hand-rolls a plain page title, which is the same shape as
       BuildProgress and therefore convertible. Either record it as converted (naming which plan
       carries the edit, since plan 122-08 owns that file's colour sweep in an earlier wave) or record
       it as an exemption with a reason. What is not acceptable is leaving it unmentioned -- D-18's
       whole point is that "every route" has to be checkable.
    4. A statement of what "every route" now means, checkably: the exact command that reproduces the
       adoption figure, so a later phase can re-run it rather than re-derive it.

    The exemption register is what plan 122-17's ratchet will consume as its `KNOWN_EXEMPT` source for
    the `PageHeader` bucket, so every entry needs a reason a reviewer can disagree with -- a record,
    not a blessing.
  </action>
  <acceptance_criteria>
    - Four adoption figures are reported (top-level before/after, subdirectory before/after), each labelled with its unit (FILES).
    - The register has one entry per non-adopting page, each with a reason; the total of adopting plus exempt pages equals the re-derived denominator. Show that arithmetic.
    - `ForgePage.tsx` appears with an explicit decision -- converted (naming the carrying plan) or exempt (with a reason).
    - The document contains the exact reproduction command, and running it yields the figure the document quotes. Run it and show that it does.
    - The string `44 of 62` appears nowhere in the document except, if at all, in an explicit note that the figure is wrong and why.
  </acceptance_criteria>
  <verify>
    <automated>test -f .planning/phases/122-tokens-primitives-contrast-measurement/122-PAGEHEADER-ADOPTION.md</automated>
  </verify>
  <done>Adoption is a re-derived, unit-labelled, reproducible figure, and every page without a shared header is named with its reason rather than silently absent.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none introduced | A presentational header component and two page edits. No input crosses any boundary; no route, data flow or auth change. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-122-11-A | Denial of Service (to the page) | `src/pages/Analytics.tsx` | mitigate | Phase 121 exists because a single `useQuery` throw could blank this route, and it shipped an AST ratchet to keep it that way. A header edit that hoisted a hook or moved an element out of a `SectionErrorBoundary` would silently re-open that. Mitigated by running the ratchet specifically, AND by confirming the ratchet's own synthetic mutations still report RED so a pass is not a pass-by-not-analysing. |
| T-122-11-B | Tampering | `PageHeader.tsx`'s prop surface | mitigate | 43 files render this component. A non-additive change would break callers this plan does not touch. Mitigated by the backward-compatibility control test and by the full-suite baseline comparison. |
| T-122-11-SC | Tampering | npm/pip/cargo installs | mitigate | Zero packages installed. RESEARCH.md's Package Legitimacy Audit records zero new packages for this phase. |

RESEARCH.md's assessment stands: no ASVS category applies.
</threat_model>

<verification>
- `npx vitest run src/components/PageHeader.test.tsx` passes; `npx vitest run src/pages/Analytics.structuralGuard.test.ts` passes with its own mutation cases still RED
- Full `npx vitest run` shows zero new failures against the recorded baseline; `npx tsc --noEmit` exits 0; `npm run build` exits 0
- `PageHeader` accepts eyebrow and subtitle, breaks no existing caller, and provably contains no Phase 124 app chrome and no hardcoded colour
- Analytics and BuildProgress render `PageHeader`; Analytics' `SectionHeader` count is unchanged
- `122-PAGEHEADER-ADOPTION.md` carries re-derived unit-labelled figures, a reproduction command that actually reproduces them, and one named exemption per non-adopting page including an explicit ForgePage decision
</verification>

<success_criteria>
The page-layer header contract exists, is adopted everywhere it should be, and every place it is
deliberately absent is named with a reason -- so "every route uses the shared PageHeader contract" is
a claim a reviewer can check in one command rather than take on trust.
</success_criteria>

<output>
Create `.planning/phases/122-tokens-primitives-contrast-measurement/122-11-SUMMARY.md` when done
</output>
