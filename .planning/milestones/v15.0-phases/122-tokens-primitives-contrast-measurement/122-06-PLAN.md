---
phase: 122-tokens-primitives-contrast-measurement
plan: 06
type: execute
wave: 3
depends_on: ["122-03"]
files_modified:
  - src/components/NotificationBell.tsx
  - src/components/ObsidianGraph.tsx
  - src/components/OperatorScoreCard.tsx
  - src/components/OrbitalStatusRings.tsx
  - src/components/OriginBadge.tsx
  - src/components/PermissionDecisionsChart.tsx
  - src/components/PhaseProgressBars.tsx
  - src/components/PluginPanel.tsx
  - src/components/PrivacyShield.tsx
  - src/components/ProfileCard.tsx
  - src/components/PromptActivityChart.tsx
  - src/components/ProviderControls.tsx
  - src/components/ProviderHealthPanel.tsx
  - src/components/QueenNode.tsx
  - src/components/RateLimitGauges.tsx
  - src/components/RecentGitActivity.tsx
  - src/components/RecoveryCommits.tsx
  - src/components/RecoveryTimeline.tsx
  - src/components/ReplayButton.tsx
  - src/components/RoutingDecisionsTable.tsx
  - src/components/SankeyFlow.tsx
  - src/components/ScanResultsPanel.tsx
  - src/components/SectionErrorBoundary.tsx
  - src/components/SecurityEventFeed.tsx
  - src/components/SecurityStats.tsx
  - src/components/SessionCapabilities.tsx
  - src/components/SessionComparison.tsx
  - src/components/SessionDurationHistogram.tsx
  - src/components/SessionHeader.tsx
  - src/components/SessionTimeline.tsx
  - src/components/Skeleton.tsx
  - src/components/SupabasePanel.tsx
  - src/components/SwarmTaskNode.tsx
  - src/components/SystemResources.tsx
  - .planning/phases/122-tokens-primitives-contrast-measurement/sweep-ledgers/122-06-LEDGER.md
autonomous: true
requirements: [TOKEN-01, TOKEN-02, TOKEN-03]

must_haves:
  truths:
    - "Slice C (src/components/ N-S, 34 files) holds zero raw palette classes, zero surface hex literals, zero duration-NNN classes and zero raw violet utilities after this plan, outside entries recorded in its ledger"
    - "Skeleton.tsx's placeholder surface reads the token layer, so the skeletons TOKEN-04 requires to be shaped like their content inherit the ramp rather than a palette literal"
    - "Every zero-result gate in this slice is paired with a control proving the matcher can still return non-zero"
  artifacts:
    - path: ".planning/phases/122-tokens-primitives-contrast-measurement/sweep-ledgers/122-06-LEDGER.md"
      provides: "Per-file, per-bucket before/after counts and the per-site violet adjudication for slice C"
  key_links:
    - from: "slice C component class strings"
      to: "src/index.css theme tokens"
      via: "semantic Tailwind utilities and bracket-paren var references"
      pattern: "bg-card|bg-background|bg-popover|border-border"
---

<objective>
Sweep slice C -- the 34 files in `src/components/` beginning N through S -- clean of every hardcoded
surface, motion literal and raw violet.

Purpose: one of five disjoint, fully parallel slices covering all 143 sweep files. This slice carries
the largest violet concentration outside the `hr/` tree (`OriginBadge`, `PluginPanel`,
`RoutingDecisionsTable`, `SessionCapabilities`, `SessionTimeline`, `SwarmTaskNode`), so its
adjudication is the substantive half of the work.
Output: 34 token-clean files plus a ledger recording what changed and why.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/122-tokens-primitives-contrast-measurement/122-CONTEXT.md
@.claude/skills/sketch-findings-codepulse/SKILL.md
@src/index.css
@src/components/StatusBadge.tsx
@CLAUDE.md

<interfaces>
### The conversion contract (identical across all five sweep slices)

Tokens now available from `src/index.css` (all five themes): `--surface-0/1/2/3`, `--hairline`,
`--hairline-strong`, `--astridr`, `--status-ok/warn/error/info`, `--status-error-fill`,
`--status-error-on-fill`, `--duration-fast/normal/slow`, `--ease-house`.

PREFER the existing semantic utilities -- they already alias the ramp:
`bg-background` = `--surface-0` (page) | `bg-card` = `--surface-1` | `bg-popover` = `--surface-2` |
`border-border` = `--hairline` | `bg-muted` / `text-muted-foreground` for de-emphasised fills and ink.
Bracket references only when no alias fits: `bg-[var(--surface-3)]` (hover on raised),
`border-[var(--hairline-strong)]` (emphasised divider).

**Palette classes** -- `(bg|text|border|from|to|via)-(slate|zinc|gray|neutral|stone)-NNN`:
page -> `bg-background`; card/panel -> `bg-card`; raised/popover/dropdown/tooltip -> `bg-popover`;
hover on raised -> `bg-[var(--surface-3)]`; chip/track/skeleton -> `bg-muted`; border ->
`border-border` (emphasised -> `border-[var(--hairline-strong)]`); dim ink (`-400`/`-500`) ->
`text-muted-foreground`; primary ink (`-50`/`-100`/`-200`) -> `text-foreground`. Opacity suffixes
survive the swap.

**Hex literals** -- `(bg|border|text)-[#rrggbb]` in a className: same table. A genuinely data-driven
hex (chart series, graph node) is NOT a surface -- record it in the ledger as out of scope for this
bucket, naming what it is, rather than forcing it to a token to turn a grep green.

**Motion** -- `duration-NNN`: 75/100/150 -> `duration-fast`; 200/250 -> `duration-normal`;
300/350 -> `duration-slow`; >= 400 -> `duration-slow` AND record the old value in the ledger. Add
`ease-house` where a transition carried no explicit easing or a non-house one.

**Violet** -- `(bg|text|border)-(violet|purple|fuchsia)-NNN`: adjudicate EACH site by what the element
REPRESENTS. Astridr's identity/voice/agent/memory/routing -> `bg-[var(--astridr)]` /
`text-(--astridr)` / `border-(--astridr)/40`, using the bracket-paren convention already live in
`src/components/StatusBadge.tsx:22-27`. Everything else -> re-hue to `--primary`, a `--status-*`, or a
neutral. One ledger row per site with the verdict and its reason.

### Counting discipline
`grep -c` = matching LINES per file including `path:0` rows; `grep -o | wc -l` = OCCURRENCES;
`grep -l | wc -l` = FILES. Name the unit before quoting a number. For any literal containing `#` or a
backslash use the fixed-string flag typed exactly as it appears on disk -- never hand-escape.

### Files explicitly NOT in this slice
`MetricCard.tsx`, `HeroStatsBar.tsx`, `chat/VitalsRail.tsx`, `forge/ForgeStatusBadge.tsx` and
`skills/IntakeStatusBadge.tsx` are excluded from every sweep slice (owners: 122-10, 122-12, 122-13).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-derive slice C's population and open the ledger</name>
  <files>.planning/phases/122-tokens-primitives-contrast-measurement/sweep-ledgers/122-06-LEDGER.md</files>
  <read_first>
    - .planning/phases/122-tokens-primitives-contrast-measurement/122-CONTEXT.md (D-02, D-08, D-09 and the matcher-discipline note under D-25)
    - src/index.css (the five theme blocks and the `@theme`/`@utility` region)
    - src/components/StatusBadge.tsx lines 22-28
  </read_first>
  <action>
    Re-derive this slice's population from the corpus, scoped to exactly this plan's `files_modified`
    list. Do not quote CONTEXT.md's figures -- D-02 instructs re-derivation.

    Record per file, per bucket: the count and its UNIT. Buckets and regexes:
    palette `(bg|text|border|from|to|via)-(slate|zinc|gray|neutral|stone)-[0-9]{2,3}`;
    hex `(bg|border|text)-\[#` (fixed-string the `#`); motion `duration-[0-9]+`;
    violet `(bg|text|border)-(violet|purple|fuchsia)-[0-9]{2,3}`.

    Create `sweep-ledgers/122-06-LEDGER.md` with a BEFORE table (one row per file, including all-zero
    files) plus empty AFTER and ADJUDICATION sections, and the four regexes recorded verbatim.
  </action>
  <acceptance_criteria>
    - The BEFORE table has 34 rows, one per file in `files_modified`, including all-zero files.
    - Every count is labelled with its unit.
    - The four regexes appear verbatim.
    - CONTROL: `src/components/ObsidianGraph.tsx` is known (measured during planning) to hold a hex literal and `src/components/OriginBadge.tsx` a violet utility. Run those two matchers against those two files and confirm non-zero BEFORE trusting any zero elsewhere. Say so in the ledger.
  </acceptance_criteria>
  <verify>
    <automated>test -f .planning/phases/122-tokens-primitives-contrast-measurement/sweep-ledgers/122-06-LEDGER.md</automated>
  </verify>
  <done>Slice C's four-bucket population is measured, unit-labelled, and recorded with its matchers proven against known-positive controls.</done>
</task>

<task type="auto">
  <name>Task 2: Convert every palette class and hex literal in slice C</name>
  <files>src/components/NotificationBell.tsx, src/components/ObsidianGraph.tsx, src/components/OperatorScoreCard.tsx, src/components/OrbitalStatusRings.tsx, src/components/OriginBadge.tsx, src/components/PermissionDecisionsChart.tsx, src/components/PhaseProgressBars.tsx, src/components/PluginPanel.tsx, src/components/PrivacyShield.tsx, src/components/ProfileCard.tsx, src/components/PromptActivityChart.tsx, src/components/ProviderControls.tsx, src/components/ProviderHealthPanel.tsx, src/components/QueenNode.tsx, src/components/RateLimitGauges.tsx, src/components/RecentGitActivity.tsx, src/components/RecoveryCommits.tsx, src/components/RecoveryTimeline.tsx, src/components/ReplayButton.tsx, src/components/RoutingDecisionsTable.tsx, src/components/SankeyFlow.tsx, src/components/ScanResultsPanel.tsx, src/components/SectionErrorBoundary.tsx, src/components/SecurityEventFeed.tsx, src/components/SecurityStats.tsx, src/components/SessionCapabilities.tsx, src/components/SessionComparison.tsx, src/components/SessionDurationHistogram.tsx, src/components/SessionHeader.tsx, src/components/SessionTimeline.tsx, src/components/Skeleton.tsx, src/components/SupabasePanel.tsx, src/components/SwarmTaskNode.tsx, src/components/SystemResources.tsx</files>
  <read_first>
    - Each file before editing it -- read the surrounding JSX so each class is mapped by the ROLE the element plays
    - src/index.css (what each token resolves to per theme)
    - .planning/phases/122-tokens-primitives-contrast-measurement/122-CONTEXT.md (D-01, D-02)
  </read_first>
  <action>
    Apply the palette and hex halves of the conversion contract to all 34 files, file by file, never
    batch-sed.

    Specific care in this slice:
    - `Skeleton.tsx` is the shared placeholder surface. Its fill maps to `bg-muted`, matching the
      shadcn `ui/skeleton.tsx` idiom, so that TOKEN-04's content-shaped skeletons inherit the ramp.
    - `SectionErrorBoundary.tsx` renders the failure surface that every panel falls back to. Its
      error affordance must stay visibly a failure -- map its fill to the error tokens the token layer
      now provides, not to a neutral card.
    - `ObsidianGraph.tsx`, `SankeyFlow.tsx`, `PermissionDecisionsChart.tsx`,
      `SessionDurationHistogram.tsx` and `PromptActivityChart.tsx` are data-visualisation surfaces:
      their CHROME (panel, axis, gridline, legend background) converts; their SERIES colours are data,
      not surfaces, and are recorded in the ledger as out of scope for this bucket.
    - `QueenNode.tsx` and `SwarmTaskNode.tsx` are React Flow nodes -- their fills are still surfaces
      and convert normally.

    Preserve everything that is not a colour: sizing, spacing, positioning, pointer, visibility,
    opacity suffixes, `data-*`/`aria-*` attributes and conditional class logic stay byte-identical.

    Update the ledger's AFTER table as you go.
  </action>
  <acceptance_criteria>
    - `git grep -lE '(bg|text|border|from|to|via)-(slate|zinc|gray|neutral|stone)-[0-9]{2,3}' -- <the 34 files>` returns ZERO files, or every survivor has a dated justification row in the ledger.
    - `git grep -lE '(bg|border|text)-\[#' -- <the 34 files>` likewise returns zero, or every survivor is a ledger-recorded data-driven colour.
    - CONTROL: run the identical command against a file OUTSIDE this slice known to still hold a palette class at this point, and confirm non-zero.
    - `npx tsc --noEmit` exits 0.
    - `npx vitest run` shows zero NEW failures against the recorded pre-plan baseline; record both figures as numbers.
    - For three files chosen at random from the slice, read `git diff` in full and confirm every removed token on every changed line is a colour class. Name the three.
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run</automated>
  </verify>
  <done>Slice C holds no raw palette class and no surface hex literal outside ledger-recorded exceptions, and its error and skeleton surfaces still read as themselves.</done>
</task>

<task type="auto">
  <name>Task 3: Convert slice C's motion literals and adjudicate its six violet files</name>
  <files>src/components/OriginBadge.tsx, src/components/PluginPanel.tsx, src/components/RoutingDecisionsTable.tsx, src/components/SessionCapabilities.tsx, src/components/SessionTimeline.tsx, src/components/SwarmTaskNode.tsx, src/components/NotificationBell.tsx, src/components/ReplayButton.tsx</files>
  <read_first>
    - The ledger's BEFORE table from Task 1 -- authoritative for which files hold motion or violet hits
    - src/components/StatusBadge.tsx lines 22-28 (the convention to imitate for `--astridr`)
    - .planning/phases/122-tokens-primitives-contrast-measurement/122-CONTEXT.md (D-08, D-09, D-10)
  </read_first>
  <action>
    Apply the motion and violet halves of the conversion contract to every slice-C file the ledger
    shows holding a `duration-NNN` or a raw violet utility. The `files` list above is indicative;
    the re-derived ledger is authoritative.

    Motion: map each `duration-NNN` per the contract's table and record any old value >= 400ms.

    Violet, adjudicated per SITE: this slice holds the densest non-`hr/` concentration and each file
    means something different. `OriginBadge` denotes where a thing came from -- violet converts to
    `var(--astridr)` only where the origin IS Astridr, and re-hues where it is a generic origin.
    `RoutingDecisionsTable` shows which brain/engine handled a turn -- Astridr-owned rows convert, a
    third-party provider's row does not. `PluginPanel`, `SessionCapabilities`, `SessionTimeline` and
    `SwarmTaskNode` are the likeliest re-hue cases (category/severity accents rather than identity),
    but decide from the code, not from this sentence. Write one ledger row per site: file, line, what
    it represents, verdict, replacement.

    Where a violet was carrying a distinction no token expresses (e.g. one arbitrary colour in a
    multi-category legend), say so in the ledger and pick the closest correct token -- inventing a new
    token is out of this plan's scope.
  </action>
  <acceptance_criteria>
    - `git grep -lE 'duration-[0-9]+' -- <the 34 slice-C files>` returns ZERO files.
    - POSITIVE control on the replacement: after `npm run build`, a fixed-string search of the built stylesheet finds a rule for whichever duration utility you used. Absence of the old classes is NOT sufficient proof.
    - `git grep -lE '(bg|text|border)-(violet|purple|fuchsia)-[0-9]{2,3}' -- <the 34 slice-C files>` returns ZERO files.
    - The ADJUDICATION section has one row per violet site found in Task 1; the row count equals the Task 1 violet occurrence count for this slice. State both numbers.
    - At least one site in this slice is adjudicated CONVERT and at least one is adjudicated RE-HUE, or the ledger explains why the split is one-sided -- an adjudication that reaches the same verdict everywhere is a rule being applied, not a judgement being made, and D-08 asks for the audit.
    - `npx tsc --noEmit` exits 0; `npx vitest run` shows zero new failures.
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run</automated>
  </verify>
  <done>Slice C's motion reads the shared tokens (proven by a rule present in the built CSS) and each of its violet sites carries an individual, reasoned verdict.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none introduced | Class-string edits in presentational components. No input crosses any boundary; no data flow changes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-122-06-A | Tampering | `SectionErrorBoundary.tsx`, `SecurityEventFeed.tsx`, `SecurityStats.tsx`, `PrivacyShield.tsx` | mitigate | These four render failure, security-event and PII-masking state. Flattening any of them into a neutral card would hide a condition an operator must see. Mitigated by the named per-file instruction to keep the error affordance reading as a failure, plus the diff review requiring every removed token to be a colour class. |
| T-122-06-B | Information Disclosure | `PrivacyShield.tsx` | mitigate | `PrivacyShield` is the visual affordance for PII masking. This plan changes only its colour classes; its masking logic and its conditional class predicates must be byte-identical after the edit. Verify by reading the full diff of that file specifically, not by sampling. |
| T-122-06-SC | Tampering | npm/pip/cargo installs | mitigate | Zero packages installed. RESEARCH.md's Package Legitimacy Audit records zero new packages for this phase. |

RESEARCH.md's assessment stands: no ASVS category applies.
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0; `npm run build` exits 0
- `npx vitest run` at or above the recorded baseline, zero new failures
- All four bucket matchers return zero files across slice C, or every survivor has a ledger row
- The duration replacement is proven by a POSITIVE rule in the built CSS
- `PrivacyShield.tsx`'s full diff shows only colour-class changes
- `sweep-ledgers/122-06-LEDGER.md` carries BEFORE, AFTER and per-site ADJUDICATION sections with a mixed set of violet verdicts
</verification>

<success_criteria>
Thirty-four components in slice C read the Borealis token layer for every surface, border, ink,
timing and violet; the error, skeleton and privacy surfaces still read as themselves; and every
violet decision is individually recorded.
</success_criteria>

<output>
Create `.planning/phases/122-tokens-primitives-contrast-measurement/122-06-SUMMARY.md` when done
</output>
