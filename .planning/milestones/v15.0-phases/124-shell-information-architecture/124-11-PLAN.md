---
phase: 124-shell-information-architecture
plan: 11
type: execute
wave: 9
depends_on: ["124-10"]
files_modified:
  - .planning/phases/124-shell-information-architecture/124-CHECKPOINT.md
autonomous: false
requirements: [SHELL-01, SHELL-02]

must_haves:
  truths:
    - "The assembled shell is judged by the operator on a real browser before the phase closes — Phases 122 and 123 both used this, and 123's D-18 checkpoint surfaced two real defects beyond the question it asked"
    - "D-06's outcome is reported as what SHIPPED, not as what the spec asked for — if the wrap stayed, the checkpoint says so"
    - "Every route still resolves at the same URL it did before the regroup — criterion 3 is confirmed by a human walking the app, not only by a unit test"
  artifacts:
    - path: ".planning/phases/124-shell-information-architecture/124-CHECKPOINT.md"
      provides: "The operator's recorded verdict and any defects found"
  key_links:
    - from: ".planning/phases/124-shell-information-architecture/124-CHECKPOINT.md"
      to: "src/layouts/DashboardLayout.tsx"
      via: "operator observation of the assembled shell"
      pattern: "DashboardLayout"
---

<objective>
Put the assembled shell in front of the operator before the phase closes.

Purpose: 124 is the most visual phase of this milestone and it changes the chrome on every route.
`124-CONTEXT.md`'s Claude's-Discretion section recommends this checkpoint by default, and Phase
123's equivalent surfaced two real defects beyond the question it was asked — so this is not
ceremony.
Output: a recorded verdict, and any defect list that comes with it.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/124-shell-information-architecture/124-CONTEXT.md
@.planning/phases/124-shell-information-architecture/124-UI-SPEC.md
@.planning/phases/123-accessibility-remediation/123-CLOSEOUT.md

**Do not open this checkpoint until every prior plan's automated gates are green.** A checkpoint
run against a half-finished shell wastes the operator's time and returns findings that are already
known.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Assemble the evidence pack and confirm the suite is green</name>
  <read_first>
    - Every `124-*-SUMMARY.md` in this phase directory
    - `.planning/phases/124-shell-information-architecture/124-VALIDATION.md`
      (the per-requirement verification map and the sign-off list)
  </read_first>
  <files>.planning/phases/124-shell-information-architecture/124-CHECKPOINT.md</files>
  <action>
    Run the full gate set and record each result verbatim: `npx tsc --noEmit`, `npm test`,
    `npx playwright test e2e/polish-geometry.spec.ts`, and the accessibility matrix
    (`e2e/theme-contrast.spec.ts`), all Clerk-free per `package.json`'s `test:e2e:noauth:help`
    recipe. A skipped Playwright cell is NOT a pass — report skips as NOT VERIFIED.

    Create `124-CHECKPOINT.md` holding, before the operator is asked anything:
    - **What shipped for D-06**, stated as a result rather than a target: the measured zone
      min-content sums at 375px and 900px, and whether `h-12` was adopted or `min-h-14 flex-wrap`
      stayed. If the wrap stayed, say so in the first sentence.
    - The `⋯` menu's exact contents (four items, no Help) and whether the nested `ThemeSwitcher`
      Select worked inside a `DropdownMenuItem` or needed the plain-`<div>` fallback.
    - The D-05 cmdk verdict — defect reproduced and fixed, or not reproduced and dropped — with the
      raw `data-value` pair.
    - SYS/LAT's shipped location, and confirmation their real-or-hidden gating survived.
    - The rendered rail measurement and the 232px `asideRect` figure.
    - Any item this phase deliberately did NOT do: Help (deferred), the Signal Horizon (Phase 125),
      the `a11y-02` 42-route backlog (out of scope), and the SYS/LAT-on-Dashboard option.
    - The gate results above.

    Then start the dev server the operator will actually look at and confirm it is serving. Note
    that a Node server on this machine may bind `::1` only — probe BOTH `localhost` and `127.0.0.1`
    before reporting the server up, and quote the responses.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `124-CHECKPOINT.md` exists and its first sentence states which header-height branch shipped.
    - All four gate results are quoted verbatim, including any skip counts.
    - The dev server probe quotes responses from BOTH `localhost` and `127.0.0.1` with the port.
    - No section of the checkpoint document asserts a design target as an outcome — every number
      present is one measured in plans 124-01 through 124-10 and is cited to its SUMMARY.
  </acceptance_criteria>
  <done>The operator has one page holding what shipped, what was measured, and what was deliberately left out.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Operator visual checkpoint on the assembled shell</name>
  <files>.planning/phases/124-shell-information-architecture/124-CHECKPOINT.md</files>
  <action>
    Present the checklist below to the operator, wait for their response, and append their
    verbatim answer plus a disposition for every defect they raise to `124-CHECKPOINT.md`.
    Do not paraphrase their wording and do not summarise a defect away. Silence is not
    approval: wait for an explicit response before proceeding.
  </action>
  <what-built>
    The complete Borealis shell: a three-zone header (breadcrumb / command bar / system chip +
    E-Stop + `⋯` + user menu) and a 232px sidebar with four collapsible domains, count badges on
    Inbox and Alerts, and a 2px accent rail on the active item. Every route keeps the URL it had
    before.
  </what-built>
  <how-to-verify>
    1. Open the running dev server (URL and port are recorded at the top of `124-CHECKPOINT.md`).
    2. **Sidebar.** Confirm four domain headings read Command, Observe, Agents, System. Collapse
       one, reload the page, and confirm it is still collapsed. Collapse the whole sidebar to the
       48px rail, then expand it, and confirm the domain you closed is still closed.
    3. **Active item.** Click three items in different domains. Each time, confirm the active row
       shows a thin accent bar down its left edge and a faint tint — and that this is the ONLY
       thing distinguishing it, since the label weight is deliberately identical to the others. Say
       whether that reads clearly enough, or whether it is too subtle to find at a glance.
    4. **Badges.** Look at Inbox and Alerts. If you see a number, it means real unfinished work is
       waiting. If you see nothing, that means either nothing is waiting or the count has not
       loaded — both correctly show nothing. Confirm no other item has a badge.
    5. **Header.** Confirm the breadcrumb reads "Domain / Page" and changes as you navigate. Open
       the `⋯` menu and confirm it holds exactly four controls — theme, privacy, CRT, ambient audio
       — plus the SYS/LAT figures. Change the theme from inside the menu and confirm it applies.
       There is deliberately no Help entry.
    6. **System chip.** Note which word it shows (Nominal / Attention / Critical / Offline) and say
       whether it agrees with what the Alerts badge is telling you.
    7. **Narrow.** Resize the window to roughly 900px wide, then narrower still, to about 375px.
       At 900px, confirm every header control on the right is fully visible and clickable — this is
       the exact width where the previous header silently cut them off. At 375px, confirm the
       hamburger replaces the breadcrumb, the search box is gone, E-Stop and `⋯` are still there,
       and nothing is clipped off the right edge. `Ctrl+K` should still open the palette at both
       widths.
    8. **Routes.** Visit five pages that MOVED domain in this regroup — Automation, Tool Galaxy,
       Briefings, Config, and Workspace Map — and confirm each loads the page you expect at the
       same address it always had.
    9. **Overall.** Does the shell read as calmer than before? Anything that looks wrong,
       unfinished, or louder than it should be — say it, including things this checklist did not
       ask about.
  </how-to-verify>
  <resume-signal>Type "approved" to close the checkpoint, or list what you found.</resume-signal>
  <verify>
    <human-check>Operator confirms the assembled shell, or returns a defect list.</human-check>
  </verify>
  <acceptance_criteria>
    - The operator's verbatim response is recorded in `124-CHECKPOINT.md`, including any answer
      given to step 3's subtlety question and step 9's open question.
    - Every defect reported is written up with the step that surfaced it and a disposition — fixed
      in this phase, filed as a todo, or explicitly accepted — with no defect left only in prose.
    - If the operator does not approve, the phase does NOT close; the defects are planned as gap
      closure before `/gsd:verify-work`.
  </acceptance_criteria>
  <done>The operator has judged the assembled shell and their verdict plus any findings are recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| auth-disabled dev server ← operator's browser | The checkpoint may be run against `dev:noauth`, which disables Clerk. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-124-11-01 | Information disclosure | An auth-disabled dev server left running and reachable | mitigate | If `dev:noauth` is used it must keep its `--host 127.0.0.1` pin, which exists specifically to stop an auth-disabled server being published to the tailnet or LAN. Stop the server when the checkpoint closes and record that you did. Prefer the normal `npm run dev` for the operator's pass if the Clerk key is available. |
| T-124-11-02 | Repudiation | Reporting the phase complete on a green suite while the checkpoint is still open | mitigate | This plan is `autonomous: false` and its checkpoint is `gate="blocking"`. Silence from the operator is not approval, and a green gate set is not the review — the whole point of the second look is that it sees what the automated gates structurally cannot. Do not write a "ready" or "complete" status into `STATE.md` or `ROADMAP.md` while this task is open. |
| T-124-11-03 | Repudiation | A defect mentioned in conversation and never written down | mitigate | An acceptance criterion requires every reported defect to carry a disposition in `124-CHECKPOINT.md`. A recurring caveat is deferred work wearing the costume of transparency. |
| T-124-11-SC | Tampering | npm/pip/cargo installs | accept | Zero installs. |
</threat_model>

<verification>
- `npx tsc --noEmit`, `npm test`, `npx playwright test e2e/polish-geometry.spec.ts` and the
  accessibility matrix all green, with any skips recorded as NOT VERIFIED.
- `124-CHECKPOINT.md` exists and holds the operator's verbatim verdict.
- Every defect the operator raised carries an explicit disposition.
</verification>

<success_criteria>
The operator has looked at the assembled shell on a real browser at three widths and across a
domain-moved route sample, their verdict is recorded verbatim, and nothing they found is left
undisposed.
</success_criteria>

<output>
Create `.planning/phases/124-shell-information-architecture/124-11-SUMMARY.md` when done.
</output>
