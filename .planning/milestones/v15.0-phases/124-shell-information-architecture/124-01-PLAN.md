---
phase: 124-shell-information-architecture
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/__tests__/navRegistry.routes.test.ts
autonomous: true
requirements: [SHELL-02]

must_haves:
  truths:
    - "D-01: the locked 44-row nav map has an exact expected value asserted in code, not a judgement call"
    - "Success criterion 3 has an automated, falsifiable guard: the sorted set of `to` values in navItems is asserted against a committed literal golden array captured BEFORE the regroup"
    - "The guard is shown to FAIL on an added route, a removed route, and a renamed `to` with no count change — all three mutations syntactically valid"
    - "A label-only rename (the thing D-05 deliberately does in plan 124-04) does NOT fail the guard"
  artifacts:
    - path: "src/lib/__tests__/navRegistry.routes.test.ts"
      provides: "Criterion-3 route-set assertion + golden fixture"
      contains: "GOLDEN_ROUTE_SET"
      min_lines: 60
  key_links:
    - from: "src/lib/__tests__/navRegistry.routes.test.ts"
      to: "src/lib/navRegistry.ts"
      via: "import { navGroups, navItems }"
      pattern: "from \"\\.\\./navRegistry\""
---

<objective>
Land the automated guard for Phase 124's hardest success criterion — "a route-list diff taken
before and after the regroup is identical" — and prove it actually discriminates before any
regroup happens.

Purpose: a golden fixture captured AFTER the change is vacuous. This plan must land in wave 1,
before plan 124-04 rewrites `navGroups`.
Output: `src/lib/__tests__/navRegistry.routes.test.ts`, green against the pre-regroup registry,
with a recorded three-case mutation proof.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/124-shell-information-architecture/124-CONTEXT.md
@.planning/phases/124-shell-information-architecture/124-VALIDATION.md
@src/lib/navRegistry.ts

**The planning documents are CLAIMS. The live code is EVIDENCE.** If anything below contradicts
what you read in the repository, the repository wins — correct the plan in your SUMMARY rather
than transcribing a wrong instruction.

<interfaces>
Exported by `src/lib/navRegistry.ts` (read live 2026-08-21, `navRegistry.ts:116-129`, `:218-229`):

```typescript
export interface NavItem { to: string; label: string; icon: string; group: string; }
export interface NavGroupConfig { group: string; items: NavItem[]; }
export const navGroups: NavGroupConfig[];
export const navItems: NavItem[];   // flattened from navGroups, deduped by `to`
```

The dedup IIFE at `navRegistry.ts:218-229` is the load-bearing object: regrouping changes which
`NavGroupConfig` an item sits in, and cannot change the flattened `to` set. Do not modify it.

**Cross-check value only — you must still re-derive this live (Task 1).** Derived from
`src/lib/navRegistry.ts` on 2026-08-21: 44 items, 44 unique `to` values, sorted:

```
/  /alerts  /analytics  /automation  /bifrost  /briefings  /build  /capabilities
/channels/whatsapp  /chat  /config  /doc-comments  /dreaming  /executions  /forge  /galdr
/graphs  /hive  /hr/analytics  /hr/catalog  /hr/onboarding  /hr/roster  /hr/teams  /ideation
/inbox  /infrastructure  /insights  /knowledge-graph  /live-run  /loom  /mcp-inventory
/meeting-bot  /memory  /quality  /reminders  /security  /self-healing  /skills  /studio
/tasks  /tool-galaxy  /tools  /war-room  /workspace-map
```

If your live derivation disagrees with these 44 values, STOP and report the drift — the golden
fixture must be captured from the live pre-regroup file, not transcribed from this document.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-derive the live route set and write the golden-fixture test</name>
  <read_first>
    - `src/lib/navRegistry.ts` (full — the object under test, especially `:218-229`)
    - `src/lib/__tests__/formatters.test.ts` (any existing file in this directory, for the
      repo's plain-Vitest unit-test file conventions: import style, `describe`/`it` shape)
    - `.planning/phases/124-shell-information-architecture/124-VALIDATION.md`
      (§ "Mutation-Proof Requirement")
  </read_first>
  <files>src/lib/__tests__/navRegistry.routes.test.ts</files>
  <action>
    First RE-DERIVE the route set from the live file rather than trusting this plan: in a throwaway
    node/vitest run, read every `to` value out of `navGroups`, sort them, and print the count plus
    the sorted list. Record the raw output in your SUMMARY. If the count is not 44, or the sorted
    list differs from the cross-check array in this plan's `<interfaces>` block, STOP and report —
    the registry has drifted since 2026-08-21 and the discrepancy must be adjudicated before the
    regroup.

    Then create `src/lib/__tests__/navRegistry.routes.test.ts` importing `{ navGroups, navItems }`
    from `../navRegistry`. Declare a module-level `const GOLDEN_ROUTE_SET: readonly string[]`
    holding the sorted `to` values EXACTLY as your live derivation produced them — a committed
    literal array, NOT a `git show <sha>:...` read and NOT a relative git ref. Add a file-header
    docstring stating: what this fixture is (the pre-regroup route set, captured on the date you
    capture it), why it is a literal (relative refs rot in a shared checkout; an amended or
    squashed anchor commit would silently void a SHA-anchored fixture), and that success criterion
    3 compares the ROUTE SET, never the group structure — because the group structure is precisely
    what SHELL-02 changes.

    Write these assertions:
    (a) `navItems` sorted `to` values deep-equal `GOLDEN_ROUTE_SET`.
    (b) The sorted `to` values flattened directly from `navGroups` (before dedup) deep-equal
        `GOLDEN_ROUTE_SET` too — this catches an item duplicated across two domains during the
        regroup, which `navItems`' dedup would otherwise absorb silently.
    (c) `new Set(GOLDEN_ROUTE_SET).size === GOLDEN_ROUTE_SET.length` — the fixture itself holds no
        duplicates (a self-check on the fixture, so a bad capture cannot make the guard vacuous).
    (d) A NEGATIVE-CONTROL test asserting that a LABEL change does not move the route set: build a
        local deep copy of `navGroups` with one item's `label` altered and one item's `group`
        altered, flatten it, and assert its sorted `to` set STILL equals `GOLDEN_ROUTE_SET`. This
        is what proves the guard permits the exact change plan 124-04 makes deliberately (D-05's
        rename and the D-01 regroup) while still catching a path change.

    Do NOT assert on `navItems.length === 44` alone anywhere — a bare cardinality assertion is the
    defect this guard exists to avoid.
  </action>
  <verify>
    <automated>npx vitest run src/lib/__tests__/navRegistry.routes.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/lib/__tests__/navRegistry.routes.test.ts` exits 0 with 4 passing tests.
    - `grep -c "GOLDEN_ROUTE_SET" src/lib/__tests__/navRegistry.routes.test.ts` returns ≥ 4.
    - `grep -cF "git show" src/lib/__tests__/navRegistry.routes.test.ts` returns 0 (fixture is a
      literal, not a git read).
    - The SUMMARY contains the verbatim output of the live re-derivation (count + sorted list).
    - `grep -cE "navItems\.length ===" src/lib/__tests__/navRegistry.routes.test.ts` returns 0.
  </acceptance_criteria>
  <done>The route-set guard exists, passes against the pre-regroup registry, and its golden fixture was captured live rather than transcribed.</done>
</task>

<task type="auto">
  <name>Task 2: BLOCKING mutation-proof — three cases, one at a time</name>
  <read_first>
    - `src/lib/__tests__/navRegistry.routes.test.ts` (the file you just wrote)
    - `src/lib/navRegistry.ts` (the file being mutated)
    - `.planning/phases/124-shell-information-architecture/124-VALIDATION.md`
      (§ "Mutation-Proof Requirement" — the three cases and why case 3 is the one that matters)
  </read_first>
  <files>src/lib/navRegistry.ts</files>
  <action>
    Apply three mutations to `src/lib/navRegistry.ts`, ONE AT A TIME, running
    `npx vitest run src/lib/__tests__/navRegistry.routes.test.ts` after each and reverting before
    the next. Every mutation must leave the module syntactically valid and importable — a mutation
    that breaks the import produces a Vitest COLLECTION ERROR, which is red for the wrong reason
    and proves nothing. Confirm each run is a genuine assertion failure naming the route set, not a
    collection error, and paste the failing assertion line into your SUMMARY.

    Mutation 1 (ADD): duplicate an existing item inside its group with a new distinct `to` value,
    e.g. add `{ to: "/mutation-probe", label: "Probe", icon: "grid", group: "OBSERVE" }` to the
    OBSERVE items array. Expect FAIL.

    Mutation 2 (REMOVE): delete one whole item object from `navGroups` — remove the `/alerts` entry
    from OBSERVE. Expect FAIL.

    Mutation 3 (RENAME `to`, no count change): change the `/alerts` item's `to` from `"/alerts"` to
    `"/alerts2"`, changing nothing else. Expect FAIL. **This is the case that matters**: plan
    124-04 renames two nav LABELS without touching their `to` values, so a guard that catches only
    (1) and (2) is asserting cardinality and would pass the exact defect class it exists to catch.

    After the third revert, confirm `git diff --stat -- src/lib/navRegistry.ts` is EMPTY and
    `npx vitest run src/lib/__tests__/navRegistry.routes.test.ts` is green again. Do not leave any
    mutation in the tree.
  </action>
  <verify>
    <automated>git diff --exit-code -- src/lib/navRegistry.ts && npx vitest run src/lib/__tests__/navRegistry.routes.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - SUMMARY records three separate runs, each showing a FAILED assertion (not a collection
      error / not `Cannot find module` / not a TS parse error), with the failing test name quoted.
    - Mutation 3's failure message shows the route-set mismatch (`/alerts2` present, `/alerts`
      absent), proving the guard compares paths and not counts.
    - `git diff --exit-code -- src/lib/navRegistry.ts` exits 0 after the final revert.
    - `npx vitest run src/lib/__tests__/navRegistry.routes.test.ts` exits 0 after the final revert.
  </acceptance_criteria>
  <done>All three mutations were shown to fail the guard for the right reason, and the registry is byte-identical to its pre-mutation state.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none crossed | This plan adds a test file only. No runtime code path, no network, no user input, no data at rest. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-124-01-01 | Tampering | `src/lib/navRegistry.ts` mutated during the proof | mitigate | Each mutation is applied one at a time and reverted; Task 2's automated verify is `git diff --exit-code -- src/lib/navRegistry.ts`, so a left-behind mutation fails the task rather than shipping. |
| T-124-01-02 | Repudiation | A guard that silently stops discriminating | mitigate | The three-case mutation proof, plus assertion (c) (fixture self-duplication check) and (d) (label-change negative control), make a vacuous green detectable. |
| T-124-01-SC | Tampering | npm/pip/cargo installs | accept | No package install occurs in this plan. `124-RESEARCH.md` § Package Legitimacy Audit records zero installs for the whole phase; `components.json` carries `"registries": {}`. |
| T-124-01-03 | Information disclosure | Test fixture contents | accept | The fixture holds only in-app route paths already visible in the client bundle to anyone who can load the SPA. Per `CLAUDE.md`'s SEED-008 note the tailnet is this deployment's auth boundary; this plan neither depends on nor changes that. |
</threat_model>

<verification>
- `npx vitest run src/lib/__tests__/navRegistry.routes.test.ts` — green.
- `git diff --exit-code -- src/lib/navRegistry.ts` — clean.
- SUMMARY contains: the live re-derivation output, and three mutation runs each showing a genuine
  assertion failure.
</verification>

<success_criteria>
The route-set guard exists and is committed BEFORE any regroup; its golden fixture was captured
from the live pre-regroup registry; and it has been shown to fail on an added route, a removed
route, and a renamed `to`, while passing a label-only change.
</success_criteria>

<output>
Create `.planning/phases/124-shell-information-architecture/124-01-SUMMARY.md` when done.
</output>
