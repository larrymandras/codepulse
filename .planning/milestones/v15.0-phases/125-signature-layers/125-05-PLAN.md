---
phase: 125-signature-layers
plan: 05
type: execute
wave: 2
depends_on: ["125-01"]
files_modified:
  - package.json
  - package-lock.json
  - src/pages/Briefings.tsx
  - src/components/BriefingFeedItem.tsx
  - src/components/BriefingFeedItem.test.tsx
autonomous: true
requirements: [SIGNAL-03]

must_haves:
  truths:
    - "D-13: the serif is live on Briefings and nowhere else — InsightsChat and `/chat` are untouched, because `ChatBubble` is shared and regressing `/chat` is explicitly out of scope"
    - "D-14: Instrument Serif is self-hosted via `@fontsource/instrument-serif`, imported INSIDE the lazy Briefings module — never `index.html`'s global font link and never `main.tsx`, so anyone who never opens `/briefings` downloads nothing"
    - "D-14: the import is the `/400-italic.css` SUBPATH — the bare package root ships only the normal face (RESEARCH R-6, read from the tarball), so a bare import would silently render faux-italic or Georgia and the trial would evaluate a face that would never ship"
    - "D-15: the serif applies in cyan, emerald, amber and aubergine and NOT in `readable`, enforced by the `[data-theme=\"readable\"]` CSS override landed in 125-01, not by a JS theme check"
    - "D-15: chunk isolation is proven on the BUILD OUTPUT — the entry stylesheet resolved from `dist/index.html` contains no Instrument Serif face, while a non-entry stylesheet does"
  artifacts:
    - path: "src/pages/Briefings.tsx"
      provides: "The module-scoped italic-subpath font import"
      contains: "@fontsource/instrument-serif/400-italic.css"
    - path: "src/components/BriefingFeedItem.tsx"
      provides: "Ástríðr's voice applied to her authored prose only"
      contains: "briefing-voice"
    - path: "src/components/BriefingFeedItem.test.tsx"
      provides: "Italic-subpath source-shape guard, class-application and scope tests"
      min_lines: 80
  key_links:
    - from: "src/pages/Briefings.tsx"
      to: "@fontsource/instrument-serif/400-italic.css"
      via: "side-effect CSS import inside the lazy route module"
      pattern: "@fontsource/instrument-serif/400-italic\\.css"
    - from: "src/components/BriefingFeedItem.tsx"
      to: "src/index.css .briefing-voice"
      via: "className on the narrative and summary prose"
      pattern: "briefing-voice"
---

<objective>
Put Ástríðr's italic serif voice on Briefings — exactly one surface — with the real face the
sketch validated, loaded only by the route that uses it.

Purpose: SIGNAL-03 is a TRIAL whose whole point is deciding what an app-wide commit would ship, so
it must exercise the actual Instrument Serif italic face rather than a synthesised oblique or a
Georgia fallback. RESEARCH R-6 read the package tarball and found the bare package root carries
only `font-style: normal` — a naive import fails silently and would make the verdict meaningless.
Output: the dependency, the subpath import, the applied class, and its guards.
This plan does NOT record the verdict — that is plan 125-10's blocking operator checkpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/125-signature-layers/125-CONTEXT.md
@.planning/phases/125-signature-layers/125-RESEARCH.md
@.planning/phases/125-signature-layers/125-UI-SPEC.md
@CLAUDE.md

**The planning documents are CLAIMS. The live code is EVIDENCE.** If anything below contradicts
what you read in the repository, the repository wins — correct the plan in your SUMMARY rather
than transcribing a wrong instruction.

<interfaces>
Landed by plan 125-01 in `src/index.css`, consumed here — do not redeclare either:
- `--font-voice: 'Instrument Serif', Georgia, serif;`
- `.briefing-voice { font-family: var(--font-voice); font-style: italic; font-size: 17px;
   line-height: 1.5; }`
- `[data-theme="readable"] .briefing-voice { font-family: var(--font-geist); font-style: normal; }`

Package legitimacy, already adjudicated in `125-RESEARCH.md` §"Package Legitimacy Audit":
`@fontsource/instrument-serif` — npm, `5.3.0`, repo `github.com/fontsource/font-files` (the same
publisher org as the already-installed `@fontsource-variable/geist`), `slopcheck scan` returned
`"status": "OK"` with zero flags. Disposition **Approved / [VERIFIED]**, not `[ASSUMED]` and not
`[SUS]` — so no blocking human legitimacy checkpoint applies. (Plans 125-01 and 125-04 describe
this as sitting "behind a blocking legitimacy checkpoint"; that overstates it. The correct gate,
implemented in Task 1, is a re-verification before install with a STOP branch if the verdict has
changed since 2026-08-21.)

The prose that gets the voice (`src/components/BriefingFeedItem.tsx`, read live):
- `:49-50` — the collapsed row's `<span className="text-base flex-1 truncate">{summary ||
  narrative.slice(0, 100)}</span>`
- `:62` — the expanded `<p className="text-base whitespace-pre-wrap">{narrative}</p>`
Everything else in that component — the DIGEST/SESSION pill, the formatted date, the session id,
the cost line — stays in the house sans/mono. The machine speaks sans/mono; she speaks serif.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-verify and install the font, and import the italic subpath inside the lazy route</name>
  <read_first>
    - `.planning/phases/125-signature-layers/125-RESEARCH.md` §R-6 and §"Package Legitimacy Audit"
      — the tarball finding that `index.css` carries only `font-style: normal`, the two style
      subpaths, and the 22,128-byte italic latin woff2
    - `package.json:28` — how `@fontsource-variable/geist` is declared, and `src/index.css:39`
      where it is imported, so the difference between the variable and static conventions is read
      rather than assumed
    - `src/pages/Briefings.tsx` (whole file, 76 lines) and `src/App.tsx:28`
      (`const Briefings = lazy(() => import("./pages/Briefings"));`)
    - `index.html:16` — the global font link this import must NOT go near
  </read_first>
  <files>package.json, package-lock.json, src/pages/Briefings.tsx</files>
  <action>
    BEFORE installing, re-run the legitimacy checks and record their verbatim output in the
    SUMMARY: `npm view @fontsource/instrument-serif version repository.url`, and
    `slopcheck scan --pkg npm @fontsource/instrument-serif`. If the version has moved past 5.3.0,
    install `5.3.0` explicitly rather than the new latest, and say so. If slopcheck returns
    anything other than OK with zero flags, STOP and report — do not install.

    Then `npm install @fontsource/instrument-serif`. Confirm the installed tree actually contains
    `node_modules/@fontsource/instrument-serif/400-italic.css` and that its `@font-face` block
    declares `font-style: italic` — read the file, do not infer it from the filename. Record the
    `@font-face` block's `font-style` and `src` lines verbatim in the SUMMARY. This is the check
    that makes the whole trial meaningful.

    Add, at the top of `src/pages/Briefings.tsx`, the side-effect import
    `import "@fontsource/instrument-serif/400-italic.css";` with a comment naming D-14 and stating
    the two load-bearing facts: it is the SUBPATH because the bare root ships normal only, and it
    lives in this lazy route module so the face is not downloaded by anyone who never opens
    `/briefings`.

    Do NOT add the family to `index.html`. Do NOT import it in `main.tsx`, `src/index.css`, or
    `BriefingFeedItem.tsx`. Do NOT add a normal-weight import — the trial is the italic face.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - The SUMMARY contains the verbatim `npm view` output, the verbatim `slopcheck scan` verdict,
      and the verbatim `font-style` / `src` lines from the installed `400-italic.css`.
    - `grep -c "@fontsource/instrument-serif" package.json` returns 1.
    - `grep -c '@fontsource/instrument-serif/400-italic.css' src/pages/Briefings.tsx` returns 1.
    - `grep -rc "instrument-serif" index.html src/main.tsx src/index.css` returns 0 for all three.
    - `npm run build` exits 0.
    - `grep -l "Instrument Serif" dist/assets/*.css` lists at least one file, and that file is NOT
      the stylesheet referenced by `dist/index.html`'s `rel="stylesheet"` link. Record both the
      entry stylesheet name and the matching file name(s) in the SUMMARY — the contrast is the
      evidence, a single grep hit is not.
  </acceptance_criteria>
  <done>The real Instrument Serif italic face is installed and loaded from the lazy Briefings chunk only, verified against the package's own `@font-face` declaration and against the build's chunk split.</done>
</task>

<task type="auto">
  <name>Task 2: Apply the voice to her authored prose, and nothing else</name>
  <read_first>
    - `src/components/BriefingFeedItem.tsx` (whole file, 76 lines) — in particular the collapsed
      summary span at `:49-50` and the expanded narrative `<p>` at `:62`
    - `src/index.css` — the `.briefing-voice` rule and its `[data-theme="readable"]` override,
      landed by plan 125-01
    - `.planning/phases/125-signature-layers/125-UI-SPEC.md` §"Serif Voice Trial Contract" — the
      exact scope line ("her authored long-form prose, not the surrounding chrome")
    - `.claude/skills/sketch-findings-codepulse/references/shell-and-dashboard.md` §3 — the
      typography law that assigns serif to her and sans/mono to the machine
  </read_first>
  <files>src/components/BriefingFeedItem.tsx</files>
  <action>
    Add the `briefing-voice` class to exactly two elements in `BriefingFeedItem.tsx`: the collapsed
    row's summary/narrative span (`:49-50`) and the expanded narrative paragraph (`:62`).

    On both, DROP the existing `text-base` class — `.briefing-voice` sets `font-size: 17px`
    explicitly per UI-SPEC's typography row, and leaving `text-base` (16px) in place makes the
    rendered size depend on stylesheet ordering rather than on the contract. Keep every other class
    on those elements unchanged, specifically `flex-1 truncate` on the span and
    `whitespace-pre-wrap` on the paragraph.

    Change nothing else in the file. The DIGEST/SESSION pill, `formatDate` output, the session id
    line and the cost line stay exactly as they are — they are the machine speaking, and the whole
    point of the trial is the contrast between the two voices on one screen. Do not touch
    `ChatBubble`, `InsightsChat`, or anything under `/chat`.
  </action>
  <verify>
    <automated>npx vitest run src/components/BriefingFeedItem.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "briefing-voice" src/components/BriefingFeedItem.tsx` returns 2.
    - `grep -c "text-base" src/components/BriefingFeedItem.tsx` returns 0.
    - `git diff --stat src/components/BriefingFeedItem.tsx` shows ≤ 4 changed lines.
    - `git diff --name-only` lists no file under `src/components/ChatBubble*`,
      `src/pages/InsightsChat.tsx`, or `src/pages/Chat*`.
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>Her narrative and summary prose render in the voice; every machine-authored datum around them is untouched, and the diff proves `/chat` was never in scope.</done>
</task>

<task type="auto">
  <name>Task 3: Guard the subpath, the class application and the theme scope</name>
  <read_first>
    - `src/App.test.tsx:229-283` — the `describe('App source shape (DEBT-03 regression guard)')`
      block: `readFileSync` + `resolve(process.cwd(), …)`, asserting the SOURCE shape because a
      module import would erase it
    - `src/components/BriefingFeedItem.tsx` and `src/pages/Briefings.tsx` as just edited
    - `.planning/phases/125-signature-layers/125-VALIDATION.md` — the two SIGNAL-03 rows
    - any existing `src/components/*.test.tsx` for this repo's render-test conventions
  </read_first>
  <files>src/components/BriefingFeedItem.test.tsx</files>
  <action>
    Create `src/components/BriefingFeedItem.test.tsx` with three groups.

    **Source-shape guard (the R-6 defect class).** Read `src/pages/Briefings.tsx` from disk with
    `readFileSync`, not via import. Assert it CONTAINS
    `@fontsource/instrument-serif/400-italic.css`, and assert it does NOT match a bare-root import
    of the package (a regex for an import of `@fontsource/instrument-serif` with no subpath). Also
    read `index.html`, `src/main.tsx` and `src/index.css` from disk and assert none contains
    `instrument-serif` — this is the chunk-scope half, and it is what stops a future "just add it
    to the global font link" edit.

    **Class application.** Render `BriefingFeedItem` with a fixture briefing that has both a
    `summary` and a multi-line `narrative`. Assert the collapsed summary element carries
    `briefing-voice`. Then click the row to expand and assert the narrative paragraph carries
    `briefing-voice` — exercise the expansion for real, do not assert on the collapsed DOM and call
    it covered. Assert the date element and the type pill do NOT carry the class, so a blanket
    application to the whole card would fail here.

    **Theme scope (D-15).** Read `src/index.css` from disk and assert it contains a
    `[data-theme="readable"] .briefing-voice` rule whose body sets `font-family: var(--font-geist)`
    and `font-style: normal`, and that this rule appears AFTER the base `.briefing-voice` rule in
    the file (source order decides the cascade here — assert on the two indices, not just on
    presence). State plainly in a comment that this is a SOURCE assertion about the override's
    existence and ordering, not a rendered-pixel measurement: jsdom does not apply real cascade or
    load webfonts, so the rendered claim belongs to plan 125-10's operator checkpoint, which is
    where a human looks at all five themes.

    Then run the mutation proofs and record each verbatim: (1) change the import in `Briefings.tsx`
    to the bare package root — the source-shape guard must go RED; (2) remove `briefing-voice` from
    the expanded paragraph only — the expansion test must go RED while the collapsed test stays
    green, proving the two are independently covered; (3) delete the `[data-theme="readable"]`
    override from `src/index.css` — the theme-scope test must go RED. Revert all three.
  </action>
  <verify>
    <automated>npx vitest run src/components/BriefingFeedItem.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/components/BriefingFeedItem.test.tsx` exits 0 with ≥ 7 passing tests.
    - `grep -c "readFileSync" src/components/BriefingFeedItem.test.tsx` returns ≥ 1.
    - `grep -c "400-italic" src/components/BriefingFeedItem.test.tsx` returns ≥ 1.
    - The SUMMARY contains all three mutation runs' verbatim RED output plus the GREEN output after
      each revert, and mutation (2)'s output must show the collapsed-row test still PASSING while
      the expanded one fails.
    - `npm test` exits 0.
  </acceptance_criteria>
  <done>A bare-root import, a lost class on either surface, a leak into the global font link, and a deleted readable override each fail a test that has been shown to fire.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → this repo's dependency tree | A new third-party package enters the build |
| Convex-stored briefing text → rendered DOM | Ástríðr-authored prose is rendered as HTML text content |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-125-05-SC | Tampering | `npm install @fontsource/instrument-serif` | mitigate | Disposition in the audit table is Approved / `[VERIFIED]`, so no blocking human checkpoint applies — but the verdict is re-derived at install time rather than trusted from a 2026-08-21 document: `npm view` for version and repository, `slopcheck scan` for flags, and a read of the installed `400-italic.css` `@font-face` block. Any non-OK verdict is an explicit STOP. `package-lock.json` is committed so the resolved tree is pinned. |
| T-125-05-01 | Information disclosure | third-party webfont request | mitigate | The face is SELF-HOSTED via `@fontsource` and bundled — no request to a Google Fonts or foundry CDN is added, so no visitor IP or Referer leaks to a third party. This is a stated benefit of D-14's rejection of the global font-link alternative, and is enforced by the guard asserting `index.html` contains no `instrument-serif`. |
| T-125-05-02 | Tampering | briefing prose rendered as content | accept | `narrative` and `summary` are rendered as React text children, which escape by construction; no `dangerouslySetInnerHTML` is introduced. The change is a font-family and font-size, and does not alter how the text is inserted. |
| T-125-05-03 | Denial of Service | 22 KB webfont in the entry bundle | mitigate | The import lives inside `src/pages/Briefings.tsx`, which `src/App.tsx:28` loads lazily, and `BriefingFeedItem` has exactly one importer. Proven on build output, not by reasoning: the entry stylesheet resolved from `dist/index.html` must contain no Instrument Serif face while a non-entry stylesheet does. The D-10/D-18 entry-chunk ratchet is the second, independent control. |
| T-125-05-04 | Repudiation | shipping the wrong face and calling the trial done | mitigate | This is the failure R-6 exists to prevent — a bare import renders faux-italic and the verdict would be about a face that would never ship. Guarded twice: a read of the installed `@font-face` `font-style` at install time, and a source-shape test that a bare-root import makes RED. |
</threat_model>

<verification>
- `npm test` green; `npx tsc --noEmit` exits 0; `npm run build` exits 0.
- `npx vitest run src/entryChunk.ratchet.test.ts` still passes after the build — if the font CSS
  has leaked into the entry stylesheet this is where it shows up as bytes.
- `git diff --stat` confined to the five files in `files_modified`. `package-lock.json` moving is
  expected; anything under `src/components/ChatBubble*` or `src/pages/InsightsChat.tsx` moving is
  a scope breach and must be reverted, not explained.
</verification>

<success_criteria>
- The real Instrument Serif ITALIC face renders her narrative and summary prose on `/briefings`.
- Nothing outside Briefings downloads or uses it, proven on the build output with a control.
- `readable` keeps Geist for that prose via a CSS override whose existence and source order are
  asserted.
- Every one of the four regressions that would quietly void the trial has a test shown to fire.
</success_criteria>

<output>
Create `.planning/phases/125-signature-layers/125-05-SUMMARY.md` when done.
</output>
