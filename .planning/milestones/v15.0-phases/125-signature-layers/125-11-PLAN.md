---
phase: 125-signature-layers
plan: 11
type: execute
wave: 4
depends_on: ["125-07", "125-08", "125-09"]
files_modified:
  - src/components/HeroStatsBar.tsx
  - src/components/HeroStatsBar.test.tsx
  - src/pages/Dashboard.tsx
autonomous: true
requirements: [SIGNAL-02]

must_haves:
  truths:
    - "D-09: the whole top card goes — the synthetic `100 - errorRate*2` SYSTEM LOAD numeral and its fabricated gradient bar are deleted, not restyled, and the Pulse ECG becomes the Dashboard's hero in its place"
    - "D-09: the health status dot dies as a genuine duplicate, because the Signal Horizon and the header's system chip carry that exact state on every page — the datum still renders, just not three times"
    - "D-09: no real number is lost — the memory hit-rate is re-derived live and confirmed to still render as a KPI tile, honouring 124's D-08 rule that real numbers relocate rather than die"
    - "D-09: the KPI grid is otherwise untouched — no tile added, removed, reordered or restyled beyond what the above requires"
    - "SIGNAL-02 criterion 2: with every Phase 125 frontend change landed, `npm run build` produces an entry chunk within D-18's +2% ceilings, measured and reported in bytes"
  artifacts:
    - path: "src/components/HeroStatsBar.tsx"
      provides: "KPI grid only — the fabricated top card removed"
      min_lines: 150
    - path: "src/pages/Dashboard.tsx"
      provides: "The Pulse ECG mounted as the Dashboard hero"
      contains: "PulseEcgHero"
  key_links:
    - from: "src/pages/Dashboard.tsx"
      to: "src/components/PulseEcgHero.tsx"
      via: "import + SectionErrorBoundary-wrapped mount above HeroStatsBar"
      pattern: "PulseEcgHero"
---

<objective>
Retire the Dashboard's fabricated SYSTEM LOAD hero and put the measured Pulse ECG in its place —
then prove, on a real build with every Phase 125 change present, that the entry-chunk budget holds.

Purpose: `ROADMAP.md:713` assigns SIGNAL-02 the POLISH-04 residue — the `100 - errorRate*2`
composite is the named fabrication this phase exists to remove. This is the plan that removes it.
It is also the last frontend plan, so it is where success criterion 2's byte clause gets its only
honest measurement.
Output: `HeroStatsBar.tsx` reduced to its KPI grid, `Dashboard.tsx` mounting the hero, and a
recorded post-build ratchet result.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/125-signature-layers/125-CONTEXT.md
@.planning/phases/125-signature-layers/125-UI-SPEC.md
@.planning/phases/124-shell-information-architecture/124-CONTEXT.md
@.planning/phases/125-signature-layers/125-09-SUMMARY.md

**The planning documents are CLAIMS. The live code is EVIDENCE.** If anything below contradicts
what you read in the repository, the repository wins — correct the plan in your SUMMARY rather
than transcribing a wrong instruction.

<interfaces>
Landed by plan 125-09, mounted here: `export default function PulseEcgHero(): JSX.Element` — no
props. It calls `usePulseWindow()` itself.

Live structure of `src/components/HeroStatsBar.tsx`, read 2026-08-21 — RE-DERIVE IT, do not trust
these line numbers after other plans have landed:
- `:163-199` — the top card: a status eyebrow + health dot at `:166-171`, the `System Load` label,
  the `AnimatedNumber` rendering `stats.activeSessions > 0 ? 100 - (stats.errorRate * 2) : 100` at
  `:181-183`, the gradient bar whose width is that same expression at `:186-192`, and the memory
  readout at `:194-197`.
- `:202-230` — the KPI grid: `kpis.slice(0, 4)` and `kpis.slice(4, 8)` over two `MetricCard` rows.

**A measured correction to D-09's premise, which the executor must re-derive before acting.**
D-09 says the memory hit-rate "relocates to a new tile in the existing KPI grid". Read live on
2026-08-21, the `kpis` array already holds EIGHT entries — Sessions, Error Rate, Alerts, Security,
Memory Hit Rate, Durable Facts, Advisor Savings, Startup Time — at `:80/89/99/114/123/132/141/152`,
and `slice(0,4)` + `slice(4,8)` renders all eight. Memory Hit Rate is index 4 and is therefore
ALREADY a rendered tile; the top card's `:194-197` readout is a pure duplicate of it. If that still
holds when you read the file, D-09's relocation requirement is satisfied by the existing tile and
NO tile is added — deleting the card loses nothing. Re-derive and report the array before you
delete anything; if the count or ordering has changed, adjust and say so.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete the fabricated top card, proving no real datum dies with it</name>
  <read_first>
    - `src/components/HeroStatsBar.tsx` (whole file) — the `kpis` array, the top card, the grid
    - `.planning/phases/124-shell-information-architecture/124-CONTEXT.md:97-103` — D-08's rule
      that real numbers relocate rather than being deleted, which D-09 applies here
    - `.planning/phases/125-signature-layers/125-UI-SPEC.md:184-190` — the what-dies-vs-relocates
      table
    - `src/layouts/DashboardLayout.tsx` — `SystemChip` and the mounted `SignalHorizon`, i.e. the two
      surfaces that already carry the health state the dot duplicates
    - `src/components/HeroStatsBar.test.tsx` — the existing suite, so you know what breaks
  </read_first>
  <files>src/components/HeroStatsBar.tsx, src/components/HeroStatsBar.test.tsx</files>
  <action>
    FIRST re-derive and report, before deleting anything: print the `kpis` array's labels with
    their indices, and state which indices the two `.slice()` calls actually render. Record this in
    your SUMMARY. Then state explicitly, for each datum in the top card, where it goes:
    - health status dot → DELETED as a duplicate (`SystemChip` in the header and the Signal Horizon
      on every route both carry this state);
    - `System Load` numeral and gradient bar → DELETED as fabricated (`100 - errorRate*2` is
      POLISH-04's named composite);
    - memory hit-rate readout → DELETED as a duplicate IF and only if your re-derivation confirms
      Memory Hit Rate is a rendered KPI tile. If it is NOT rendered, add it to the rendered range
      instead and say so — a real number may not silently die.

    Then delete the entire top-card `<div>` (the `bg-card/60 backdrop-blur-md …` block), leaving the
    KPI grid as `HeroStatsBar`'s only output. Remove any import that becomes unused as a result
    (`AnimatedNumber` is the likely one — check, do not assume) and any local variable that only
    the deleted card used. Do not restyle, reorder or re-label any `MetricCard`, and do not change
    the `.slice()` ranges unless the memory-tile branch above requires it — D-09 says the grid is
    otherwise untouched.

    If your re-derivation shows a defined `KpiDef` that neither slice renders, REPORT it as a
    finding with its label and index. Do not fix it here: silently rendering a tile the grid has
    never shown is outside D-09's scope, and the correct outcome is that the next reader knows.

    Update `src/components/HeroStatsBar.test.tsx`: delete or rewrite any assertion about the top
    card, and ADD an assertion that the fabricated expression is gone — assert the rendered output
    contains no element derived from `100 - errorRate*2` by driving the mocked stats with an error
    rate that would have produced a distinctive percentage and asserting that percentage is absent.
    Also assert the memory hit-rate value is still present exactly ONCE in the rendered output;
    before this change it appeared twice, so a count of 1 is the real evidence that a duplicate died
    and a datum did not.
  </action>
  <verify>
    <automated>npx vitest run src/components/HeroStatsBar.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "errorRate \* 2" src/components/HeroStatsBar.tsx` returns 0.
    - `grep -c "System Load" src/components/HeroStatsBar.tsx` returns 0.
    - `grep -c "AnimatedNumber" src/components/HeroStatsBar.tsx` returns 0 (or, if it is still used
      elsewhere in the file, the SUMMARY names the surviving call site).
    - The SUMMARY contains the re-derived `kpis` label/index listing and the rendered-index ranges.
    - The SUMMARY states, per datum, whether it was deleted-as-duplicate, deleted-as-fabricated, or
      preserved — with the evidence for the duplicate claims.
    - `npx vitest run src/components/HeroStatsBar.test.tsx` exits 0, including the new
      exactly-once memory-hit-rate assertion.
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>The Dashboard's fabricated headline is gone, the two genuine duplicates are gone with it, and the one real number in that card is proven to still render — exactly once.</done>
</task>

<task type="auto">
  <name>Task 2: Mount the Pulse ECG as the Dashboard hero</name>
  <read_first>
    - `src/pages/Dashboard.tsx:1-30` (imports) and `:55-75` (the `OperatorScoreCard` and
      `Hero Stats Bar` sections, including how each is wrapped in `SectionErrorBoundary` and the
      `md:col-span-12` grid idiom)
    - `src/components/PulseEcgHero.tsx` — the component being mounted
    - `src/components/PageHeader.tsx` — the hero renders UNDER the page header, not instead of it
    - `.planning/phases/125-signature-layers/125-UI-SPEC.md:184` — "Renders in its place, still
      under `Dashboard.tsx`'s `PageHeader`, not replacing it"
  </read_first>
  <files>src/pages/Dashboard.tsx</files>
  <action>
    Import `PulseEcgHero` and mount it in its own `md:col-span-12` section immediately BEFORE the
    existing Hero Stats Bar section, so it occupies the structural position the deleted card held —
    under the `PageHeader`, above the KPI grid. Wrap it in
    `<SectionErrorBoundary name="Pulse">` exactly as the neighbouring sections are wrapped, so a
    canvas or query failure cannot take the Dashboard down.

    Import it statically. `Dashboard` is already lazy (`src/App.tsx:18`), so the ECG lands in the
    lazy Dashboard chunk either way — a second lazy boundary inside a lazy route would add a
    Suspense flash for no byte benefit. State that reasoning in a comment, because F-3's inversion
    (the horizon is the entry-chunk risk, not the ECG) is the non-obvious part and the next reader
    will otherwise assume this should have been lazy.

    Do not wrap it in `heroFlashRef` or any live-flash treatment — the trace is already the live
    signal, and a flashing container around a flashing trace is two animations in one region, which
    the milestone's ≤1-animation-per-region-per-second contract forbids. Change nothing else on this
    page: no other panel, no tab, no grid span.
  </action>
  <verify>
    <automated>npx vitest run src/components/PulseEcgHero.test.tsx && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "PulseEcgHero" src/pages/Dashboard.tsx` returns 2 (import + mount).
    - `grep -c 'SectionErrorBoundary name="Pulse"' src/pages/Dashboard.tsx` returns 1.
    - `grep -c "heroFlashRef" src/pages/Dashboard.tsx` is unchanged from before this task — quote
      the before and after counts in the SUMMARY.
    - `git diff --stat src/pages/Dashboard.tsx` shows fewer than 15 changed lines.
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>The measured 60s trace occupies the Dashboard's hero slot, contained by an error boundary, in the lazy chunk where it belongs.</done>
</task>

<task type="auto">
  <name>Task 3: Measure the entry chunk with every Phase 125 change present</name>
  <read_first>
    - `src/entryChunk.ratchet.test.ts` — the ratchet from plan 125-01, its baselines and its
      skip-with-reason shape
    - `.planning/phases/125-signature-layers/125-01-SUMMARY.md` and
      `.planning/phases/125-signature-layers/125-08-SUMMARY.md` — the byte figures those plans
      recorded, so this measurement is a trend rather than a single point
    - `.planning/phases/125-signature-layers/125-CONTEXT.md` — D-10 and D-18, including the stated
      honesty that 2% was chosen drift-tolerant rather than fitted
  </read_first>
  <files></files>
  <action>
    Run `npm run build` on the complete Phase 125 frontend, then
    `npx vitest run src/entryChunk.ratchet.test.ts`, then the full `npm test` and
    `npx tsc --noEmit`.

    Report the measured entry JS and entry CSS byte counts, the D-18 ceilings
    (`Math.floor(583049 * 1.02)` and `Math.floor(237359 * 1.02)`), the deltas from the pre-125
    baseline in bytes and percent, and the same figures recorded by plans 125-01 and 125-08 so the
    growth is visible as a sequence rather than as one number. State plainly which Phase 125 change
    is responsible for the entry-chunk growth: the Signal Horizon, because `DashboardLayout` is a
    static import — the ECG and the serif are both in lazy chunks.

    If the ratchet FAILS: report the measured bytes, the ceiling and the delta, and STOP. Do not
    raise the allowance, do not add an exemption, do not re-baseline. A ratchet that gets relaxed
    the first time it fires is a ratchet nobody trusts, which is D-18's own stated reason for
    rejecting the tighter alternatives. The correct response is to report and let a person decide.

    Also confirm two build-output facts and record both commands with their output:
    (i) `grep -l "__signalHorizonStub" dist/assets/*.js` finds NOTHING — the DEV simulation hook is
        absent from production;
    (ii) the stylesheet referenced by `dist/index.html`'s `rel="stylesheet"` link contains no
        Instrument Serif face, while some other emitted stylesheet does — the chunk-scope contrast
        from plan 125-05, re-checked now that everything has landed.
  </action>
  <verify>
    <automated>npm run build && npx vitest run src/entryChunk.ratchet.test.ts && npm test && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits 0; `npm test` exits 0; `npx tsc --noEmit` exits 0.
    - `npx vitest run src/entryChunk.ratchet.test.ts` exits 0 with the assertions RUN, not skipped —
      quote the vitest output line showing passed rather than skipped counts.
    - The SUMMARY contains the measured entry JS and CSS byte counts, both ceilings, both deltas in
      bytes and percent, and the 125-01 / 125-08 figures alongside them.
    - The SUMMARY contains the verbatim `grep -l "__signalHorizonStub" dist/assets/*.js` command and
      its empty result.
    - The SUMMARY names the entry stylesheet from `dist/index.html` and states the Instrument Serif
      grep result for it (expected 0) AND for the other emitted stylesheets (expected ≥1) — the
      pair, not just the zero.
    - If the ratchet failed, the plan is reported BLOCKED with the numbers and no allowance was
      changed.
  </acceptance_criteria>
  <done>Success criterion 2's byte clause is answered with measured numbers on a complete build, and the two things that could quietly break the chunk model — a DEV hook in production and a webfont in the entry stylesheet — are re-checked with controls.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| removed hero → operator's mental model | Deleting a displayed figure removes information from the person who relies on it |
| build output → production | The final bundle is what ships; DEV-only code must not survive into it |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-125-11-01 | Repudiation | deleting a real datum while claiming it relocated | mitigate | The `kpis` array is re-derived live before anything is deleted, each datum in the card is classified with evidence, and the memory hit-rate is asserted to render EXACTLY ONCE afterwards — a count that was two before and is one after, which is real evidence rather than a claim. |
| T-125-11-02 | Repudiation | continuing to display a fabricated figure | mitigate | The `100 - errorRate*2` composite is deleted, asserted by both a source grep returning 0 and a render assertion that the distinctive percentage it would produce is absent from the DOM. |
| T-125-11-03 | Elevation of privilege | `__signalHorizonStub` reaching production | mitigate | Re-verified on the FINAL build output with a grep that must return nothing, not on the source. Vite statically replaces `import.meta.env.DEV`, but "should be eliminated" is not "was eliminated". |
| T-125-11-04 | Denial of Service | a canvas or query failure taking the Dashboard down | mitigate | `SectionErrorBoundary name="Pulse"` wraps the hero, matching the neighbouring sections. Plan 125-09's read is already an imperative catchable query rather than a `useQuery`, so the throw-unmounts-the-tree path is closed twice. |
| T-125-11-05 | Tampering | relaxing the ratchet to make it pass | mitigate | Explicitly prohibited: a failure is reported with numbers and the plan STOPS. No allowance change, no exemption, no re-baseline. This is D-18's own stated reason for rejecting a tighter allowance — a ratchet that gets relaxed on first fire is worthless. |
| T-125-11-06 | Information disclosure | third-party font request in the entry path | mitigate | Re-checked on final output: the entry stylesheet must contain no Instrument Serif face while another emitted stylesheet does. The pair is the evidence; a single zero would not discriminate a missing font from a correctly-scoped one. |
| T-125-11-SC | Tampering | npm/pip/cargo installs | mitigate | This plan installs nothing. |
</threat_model>

<verification>
- `npm run build`, `npm test`, `npx tsc --noEmit` all exit 0.
- `npx vitest run src/entryChunk.ratchet.test.ts` PASSES with assertions run, not skipped.
- `git diff --stat` confined to the three files in `files_modified`.
- Manual smoke on `npm run dev`: `/` shows the Pulse ECG where the SYSTEM LOAD bar was, the KPI grid
  is unchanged below it, the horizon is under the header, and no health dot appears in the hero.
  Record what you saw, including the ECG's `data-ecg-state` value at the time.
</verification>

<success_criteria>
- The synthetic SYSTEM LOAD hero is gone and the measured Pulse ECG is in its place.
- No real number was lost; both deleted duplicates are evidenced as duplicates.
- The entry chunk is within D-18's ceilings on a complete build, with the numbers recorded.
</success_criteria>

<output>
Create `.planning/phases/125-signature-layers/125-11-SUMMARY.md` when done.
</output>
