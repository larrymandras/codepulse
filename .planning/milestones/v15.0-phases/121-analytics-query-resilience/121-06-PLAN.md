---
phase: 121-analytics-query-resilience
plan: 06
type: execute
wave: 3
depends_on: ["121-02"]
files_modified:
  - src/components/LlmAnalyticsPanel.tsx
  - src/components/LlmAnalyticsPanel.test.tsx
autonomous: true
requirements: [DEBT-08]

must_haves:
  truths:
    - "`LlmAnalyticsPanel` consumes the rollup-backed `{ rows, asOf, expectedBuckets, presentBuckets, rowsRead, truncated }` shape from both migrated queries and renders the same Provider Comparison chart and Model Breakdown table it renders today. (D-07)"
    - "The panel renders one `as of HH:MM` freshness label sourced from the payload's `asOf`, so no rollup-derived number renders uncaveated between this phase and Phase 122. (D-10, D-11)"
    - "The label never overstates freshness: when the two queries disagree on `asOf`, the OLDER value is shown, and when `asOf` is null nothing is claimed. (D-10, D-11)"
    - "The money column still comes from `costDerived.costBreakdown`, never from a rollup or a raw `llmMetrics.cost` sum."
  artifacts:
    - path: "src/components/LlmAnalyticsPanel.tsx"
      provides: "Rollup-shape consumer plus the D-11 freshness label"
      contains: "as of"
    - path: "src/components/LlmAnalyticsPanel.test.tsx"
      provides: "Fixtures matching the migrated payload shape, plus freshness-label cases"
  key_links:
    - from: "src/components/LlmAnalyticsPanel.tsx"
      to: "api.llm.providerBreakdown / api.llm.costByModel"
      via: "useQuery returning the new object shape"
      pattern: "\\.rows"
---

<objective>
Rewire the only `src/` consumer of the two migrated queries to the new payload shape, and ship D-11's
single freshness label.

Purpose: plan 121-02 changes both queries from a bare array / bare record to an object carrying
`rows` plus freshness metadata. This panel is the one consumer. D-11 caps this phase's rendering at
one minimal `as of HH:MM` label — enough that no stale number renders uncaveated before Phase 122,
without pre-empting TOKEN-04's six-state contract.
Output: a working panel on the new shape, with a freshness label that cannot overstate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/121-analytics-query-resilience/121-CONTEXT.md
@.planning/phases/121-analytics-query-resilience/121-PATTERNS.md
@.planning/phases/121-analytics-query-resilience/121-02-SUMMARY.md

<interfaces>
Payload shapes produced by plan 121-02 (read `convex/llm.ts` for the authoritative version):

  api.llm.providerBreakdown -> {
    rows: Array<{ provider: string; calls: number }>,
    asOf: number | null,          // newest bucket_start actually summed, EPOCH SECONDS
    expectedBuckets: number,      // 720 for a 30-day hourly window
    presentBuckets: number,       // distinct bucket_start values returned
    rowsRead: number,
    truncated: boolean
  }

  api.llm.costByModel -> {
    rows: Array<{ model: string; calls: number; tokens: number }>,
    asOf: number | null, expectedBuckets: number, presentBuckets: number,
    rowsRead: number, truncated: boolean
  }

Unchanged and still the source of the money column (Phase 104 D-01 — the raw `llmMetrics.cost` sum
is stored but is NOT the displayed truth; it measured ~13% low):

  api.costDerived.costBreakdown({ period: "hourly", lookbackHours: 30 * 24 })
    -> { rows: Array<{ model, provider, billingType, billedUsd, priced, ... }> }

Existing honest-loading convention in this file, to preserve exactly:
  `breakdown === undefined` renders `--`; an unpriced model renders `Unpriced`; never `$0.00`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Consume the rollup payload shape and render the D-11 freshness label</name>
  <files>src/components/LlmAnalyticsPanel.tsx</files>
  <read_first>
    - `src/components/LlmAnalyticsPanel.tsx` in full (116 lines) — the three `useQuery` calls, the `derivedByModel` join, the `modelRows` sort, the `barData` mapping, and the `--` / `Unpriced` money-column convention
    - `convex/llm.ts` as rewritten by plan 121-02 — the exact returned field names and the `asOf` units
    - `src/lib/formatters.ts` — check whether an existing formatter produces an `HH:MM` string before writing a new one
    - `.planning/phases/121-analytics-query-resilience/121-CONTEXT.md` § D-10 and D-11
  </read_first>
  <action>
    Rewire the two migrated reads and add one label.

    1. `const providerResult = useQuery(api.llm.providerBreakdown, {});` then
       `const providerData = providerResult?.rows ?? [];`. `barData` maps
       `{ label: p.provider, value: p.calls }` exactly as today.
    2. `const modelResult = useQuery(api.llm.costByModel);` then build `modelRows` from
       `modelResult?.rows ?? []`, which is now an ARRAY of `{ model, calls, tokens }` rather than the
       old `Record<model, {...}>` — replace the `Object.entries(...)` mapping accordingly. Keep the
       join against `derivedByModel` and keep the existing sort (priced rows first, most expensive
       down, unpriced last) byte-for-byte in behaviour.
    3. Leave the `costDerived.costBreakdown` read and the entire money column untouched. The rollups
       carry no cost figure by design (D-07 removed it from both endpoints); if a future edit is
       tempted to read `calls`/`tokens` into a dollar value, the `--` / `Unpriced` convention and
       Phase 104 D-01 both forbid it. Say so in a comment beside the money column.
    4. D-11 label. Compute one `asOf` for the whole panel as the OLDER of `providerResult?.asOf` and
       `modelResult?.asOf`, treating `null`/`undefined` as "unknown". Render, in the panel's existing
       header area (beside the `Provider Comparison` heading, in the muted-foreground text style
       already used for secondary notes in this file):
         - when `asOf` is a number: `as of HH:MM`, formatted from `new Date(asOf * 1000)` — `asOf` is
           EPOCH SECONDS, and a millisecond interpretation renders a 1970 date, which is the tell to
           check for. Use 24-hour local time.
         - when either query is still loading (`undefined`): render nothing rather than a placeholder
           time.
         - when `asOf` is `null` because a query returned no rows: render `no data yet` rather than a
           time, so an empty rollup never reads as fresh.
       Add a short comment stating why the OLDER value is used: the label describes the panel, and the
       panel is only as fresh as its stalest input.
    5. Do NOT build a shared stale/partial/unavailable component, a status-returning query hook, or a
       six-state tile. Those are Phase 122's TOKEN-04 charter and D-11 is explicit that this phase
       ships a label and nothing more. `presentBuckets` / `expectedBuckets` / `truncated` are carried
       in the payload for 122 and are deliberately NOT rendered here — say so in a comment so a
       reviewer does not read them as forgotten.
       Do not restyle this panel's existing hardcoded `bg-gray-800/50` / `text-gray-*` chrome; that is
       explicitly Phase 122's work.
  </action>
  <acceptance_criteria>
    - `grep -c "Object.entries(callsAndTokensByModel)" src/components/LlmAnalyticsPanel.tsx` returns 0 — the record-shaped consumption is gone.
    - `grep -cE "providerResult\?\.rows|modelResult\?\.rows" src/components/LlmAnalyticsPanel.tsx` returns at least 2.
    - `grep -c "as of" src/components/LlmAnalyticsPanel.tsx` returns at least 1, and `grep -c "asOf \* 1000" src/components/LlmAnalyticsPanel.tsx` returns at least 1 — the seconds-to-millis conversion is present.
    - `grep -c "costDerived.costBreakdown" src/components/LlmAnalyticsPanel.tsx` still returns 1 — the money column source is unchanged.
    - `grep -cE "formatCost\(\s*row\.cost\s*\)|\.cost\b" src/components/LlmAnalyticsPanel.tsx` shows no read of a `cost` field from either migrated query (they no longer return one).
    - Comments exist stating (a) why the OLDER `asOf` is used, (b) that `presentBuckets`/`expectedBuckets`/`truncated` are intentionally unrendered until Phase 122.
    - `npx tsc --noEmit` reports zero errors.
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>The panel renders from the rollup payload and carries one honest `as of HH:MM` label that cannot overstate freshness.</done>
</task>

<task type="auto">
  <name>Task 2: Update the panel's test fixtures to the migrated shape and cover the freshness label</name>
  <files>src/components/LlmAnalyticsPanel.test.tsx</files>
  <read_first>
    - `src/components/LlmAnalyticsPanel.test.tsx` in full (129 lines) — the `mockUseQuery` switch on `api.*` string refs, every existing fixture, and the source-level assertion at lines 120-127 that this panel does not format `row.cost`
    - `src/components/LlmAnalyticsPanel.tsx` as rewritten in Task 1
    - `.planning/phases/121-analytics-query-resilience/121-VALIDATION.md` § "Assertion Discipline", rule 1
  </read_first>
  <action>
    Update every `api.llm.providerBreakdown` and `api.llm.costByModel` fixture from the old array /
    record shapes to the new object shape, and keep every existing assertion about the rendered chart,
    the model table and the money column passing on the new fixtures. Do not weaken an existing
    assertion to make it pass — if one genuinely no longer applies because the shape changed, delete it
    and say which and why in the summary rather than loosening it.

    Keep the existing source-level regression assertion (`expect(src).not.toMatch(/formatCost\(\s*row\.cost\s*\)/)`
    and `expect(src).toContain("costDerived.costBreakdown")`) — it guards the Phase 104 D-01 rule and is
    still exactly right.

    Add a `describe("freshness label (Phase 121 D-11)")` block:
      - Both queries return an `asOf` in epoch seconds, differing by an hour: assert the rendered label
        shows the OLDER of the two times, formatted `HH:MM`. Assert on the rendered TEXT via
        `screen.getByText`, not on a computed variable — the observable outcome, per assertion rule 1.
      - A control that would catch the seconds/millis error: seed an `asOf` whose correct rendering is
        a known recent time and assert the rendered year is NOT 1970. State in a comment that a
        millisecond interpretation is what produces 1970, so the control's discriminating power is
        visible rather than implied.
      - `asOf: null` on either query: assert `no data yet` renders and no `HH:MM` time does.
      - Both queries `undefined` (loading): assert neither a time nor `no data yet` renders.
      - A `truncated: true` fixture still renders the chart and table normally — this phase does not
        surface truncation in the UI, and the test states that as an intentional boundary rather than
        leaving it untested.
  </action>
  <acceptance_criteria>
    - `npx vitest run src/components/LlmAnalyticsPanel.test.tsx` passes.
    - `grep -c "asOf" src/components/LlmAnalyticsPanel.test.tsx` returns at least 4.
    - A test asserts the rendered label is the OLDER of two differing `asOf` values.
    - A test asserts the rendered text does not contain `1970`, with a comment naming the millisecond misinterpretation as the failure it discriminates.
    - The pre-existing `formatCost(row.cost)` source assertion is still present and passing.
    - `npx vitest run` (full suite) passes with no remaining failures anywhere.
    - Any assertion deleted rather than migrated is named in the summary with its reason.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/components/LlmAnalyticsPanel.test.tsx</automated>
  </verify>
  <done>The panel's tests exercise the migrated payload shape and prove the freshness label is honest in all four states, including a control against the epoch-units error.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Convex query payload → rendered figure | A rollup payload can be stale by up to 60 minutes by construction, and can be truncated. Rendering it without a caveat asserts a freshness the data does not have. |
| render-phase throw → `SectionErrorBoundary name="LLM Analytics"` | Already in place at `src/pages/Analytics.tsx:336`; unchanged by this plan. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-121-23 | Repudiation | rollup figures rendered as current | mitigate | One `as of HH:MM` label sourced from the payload's `asOf`, using the OLDER of the two queries' values, rendering `no data yet` on `null` and nothing while loading. Tested in all four states. |
| T-121-24 | Repudiation | epoch-unit misinterpretation | mitigate | `asOf` is epoch SECONDS; the render converts with `asOf * 1000` and a test asserts the rendered text contains no `1970`, which is what a millisecond reading produces. A threshold or label that passes by accident reads identically to one that passes correctly, so the control is required, not optional. |
| T-121-25 | Information Disclosure | raw cost figure reaching the money column | mitigate | The migrated queries no longer return a `cost` field at all, and the pre-existing source-level assertion forbidding `formatCost(row.cost)` is retained. Money continues to come from `costDerived.costBreakdown` per Phase 104 D-01. |
| T-121-26 | Repudiation | truncation not surfaced in the UI | accept, stated | `truncated` ships in the payload but is deliberately unrendered in this phase — D-11 caps rendering at the freshness label, and the six-state tile that would surface it is TOKEN-04 in Phase 122. A test asserts the current behaviour so the boundary is recorded rather than assumed. |
| T-121-SC | Tampering | npm/pip/cargo installs | accept — no installs | Zero new dependencies. No package-manager install task exists, so the Package Legitimacy Gate does not apply. |
</threat_model>

<verification>
- `npx vitest run` full suite passes with zero failures — this is the plan where the deliberate breakage introduced by 121-02 is closed, so a remaining failure is a real defect, not expected fallout.
- `npx tsc --noEmit` clean.
- The `1970` control assertion's presence is shown by its grep output in the SUMMARY.
</verification>

<success_criteria>
- The panel renders the same chart and table from the rollup payload.
- One freshness label renders, correct in all four states, never overstating.
- The money column's source and its guarding assertion are unchanged.
</success_criteria>

<output>
Create `.planning/phases/121-analytics-query-resilience/121-06-SUMMARY.md` when done.
</output>
