---
phase: 121-analytics-query-resilience
plan: 05
type: execute
wave: 3
depends_on: ["121-04"]
files_modified:
  - src/pages/Analytics.structuralGuard.test.ts
autonomous: true
requirements: [DEBT-08]

must_haves:
  truths:
    - "A test derives the set of query-shaped hook calls in `Analytics()`'s own function body from the file's AST, not from a list of names, and fails if that set is non-empty. (D-04)"
    - "A test derives every custom JSX element `Analytics()` renders from the AST and fails if any of them lacks a `SectionErrorBoundary` ancestor, with only a small closed allowlist of presentational primitives exempt."
    - "The ratchet is proven to constrain: injecting a SYNTHETIC hook name that appears nowhere in this repo's history makes it fail, which no by-name test could do. (D-04)"
    - "The injected mutation is proven syntactically valid before its failure is believed, so a parse error cannot masquerade as a passing guard."
    - "Every tag on the presentational allowlist is verified to contain no `useQuery`/`usePaginatedQuery` of its own, so exempting it cannot hide a query."
  artifacts:
    - path: "src/pages/Analytics.structuralGuard.test.ts"
      provides: "AST-derived structural ratchet for /analytics plus its own mutation tests"
      contains: "createSourceFile"
  key_links:
    - from: "src/pages/Analytics.structuralGuard.test.ts"
      to: "typescript compiler API"
      via: "ts.createSourceFile over the page's own source text"
      pattern: "ts\\.createSourceFile"
    - from: "src/pages/Analytics.structuralGuard.test.ts"
      to: "src/pages/Analytics.tsx"
      via: "readFileSync(resolve(process.cwd(), ...))"
      pattern: "src/pages/Analytics.tsx"
---

<objective>
Build the D-04 structural ratchet: a test that derives, from the AST of `src/pages/Analytics.tsx`
itself, whether any query-shaped hook sits unprotected — and prove that the ratchet actually
constrains by mutating the source with a query name that has never existed in this repo.

Purpose: an enumerated by-name test only ratifies today's fixes. The 2026-08-17 precedent that
produced this requirement is a guard listing required items by name that stayed green through five
further instances of the identical defect. This ratchet must fail on the NEXT query someone hoists
back into the page body, whose name nobody can know today.
Output: one test file, with no new dependency and no production-code change.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/121-analytics-query-resilience/121-CONTEXT.md
@.planning/phases/121-analytics-query-resilience/121-RESEARCH.md
@.planning/phases/121-analytics-query-resilience/121-PATTERNS.md
@.planning/phases/121-analytics-query-resilience/121-VALIDATION.md
@.planning/phases/121-analytics-query-resilience/121-04-SUMMARY.md

<interfaces>
`typescript` is already a devDependency (`package.json:86`, version `^6.0.3`) and its compiler API is
importable from a Vitest test with zero install. Nothing in `src/` currently imports it as a value —
control-verified: a `readFileSync` search over the same tree returns 14 files, so the zero is a real
finding about AST tooling and not a broken search. There is therefore NO in-repo analog for the AST
walk; only the file-location idiom transfers, from
`src/components/LlmAnalyticsPanel.test.tsx:120-127`:

  const src = readFileSync(resolve(process.cwd(), "src/components/LlmAnalyticsPanel.tsx"), "utf-8");

That precedent's MATCHING strategy (a regex for one known-bad literal) is exactly what D-04 rejects.
Take the file-reading opening move, nothing else.

Public TypeScript API surface this test uses:
  ts.createSourceFile(fileName, text, ts.ScriptTarget.Latest, /*setParentNodes*/ true, ts.ScriptKind.TSX)
  ts.forEachChild / ts.isCallExpression / ts.isIdentifier / ts.isJsxElement /
  ts.isJsxSelfClosingElement / ts.isJsxOpeningElement / ts.isFunctionDeclaration
  ts.transpileModule(text, { reportDiagnostics: true, compilerOptions: { jsx: ts.JsxEmit.Preserve } })
    -> { diagnostics } — used to prove a mutated source still parses, via public API rather than the
    internal `parseDiagnostics` property.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the AST-derived ratchet as a pure function over source text, with two checks</name>
  <files>src/pages/Analytics.structuralGuard.test.ts</files>
  <read_first>
    - `src/pages/Analytics.tsx` as rewired by plan 121-04 — the current import block, the `Analytics()` function body, and the full returned JSX tree
    - `src/components/LlmAnalyticsPanel.test.tsx` lines 108-129 — the `readFileSync(resolve(process.cwd(), ...))` idiom only
    - `.planning/phases/121-analytics-query-resilience/121-RESEARCH.md` § Q1 — the full ratchet design including the `REACT_SAFE_HOOKS` set and the closed presentational allowlist, plus its stated blind spots
    - `src/components/MetricCard.tsx`, `src/components/AnomalyBadge.tsx`, `src/components/FlexBarChart.tsx`, `src/components/GlassPanel.tsx`, `src/components/SectionHeader.tsx`, `src/components/InfoTooltip.tsx`, `src/components/ui/badge.tsx`, `src/components/ui/table.tsx` — read enough of each to confirm none calls a Convex hook
  </read_first>
  <action>
    Create `src/pages/Analytics.structuralGuard.test.ts`. Structure it so the analysis is a PURE
    FUNCTION of source text — `function analyzeAnalyticsSource(src: string): { hoistedHooks: string[]; unwrappedElements: string[] }`
    — exported or local to the file, invoked by the tests. Purity is what makes Task 2's mutation test
    possible without writing to disk.

    Parse with `ts.createSourceFile("Analytics.tsx", src, ts.ScriptTarget.Latest, true, ts.ScriptKind.TSX)`.
    Locate the default-exported function named `Analytics`. Then run two independent checks.

    CHECK 1 — no query-shaped hook call in the function's own body.
      Walk the function body's statements, deliberately NOT descending into the JSX subtree it returns.
      Collect every `CallExpression` whose callee is an identifier matching `/^use[A-Z]/` and is not in
      a `REACT_SAFE_HOOKS` set containing exactly the React and react-router builtins:
      `useState`, `useEffect`, `useMemo`, `useCallback`, `useRef`, `useReducer`, `useContext`, `useId`,
      `useLayoutEffect`, `useImperativeHandle`, `useDeferredValue`, `useTransition`, `useSyncExternalStore`,
      `useNavigate`, `useParams`, `useLocation`, `useSearchParams`.
      This is DERIVED, not enumerated: the set lists hooks that are known-safe by their framework
      origin, and anything else — including a hook name invented tomorrow — is a violation. Do not add
      the current query hook names to any list anywhere in this file; there must be nothing to keep
      up to date when a new query appears.
      Assert `hoistedHooks` is EMPTY. After plan 121-04 the true value is zero; state that expected
      value in a comment together with the pre-121-04 value of 10, so a future reader can see the
      ratchet was set at a measured floor rather than at whatever the file happened to contain.

    CHECK 2 — every custom element rendered has a boundary ancestor.
      Walk the JSX returned by `Analytics()`. For every `JsxElement` / `JsxSelfClosingElement` whose
      tag name starts with an uppercase letter, is not `SectionErrorBoundary` itself, and is not in a
      small closed `PRESENTATIONAL_ALLOWLIST`, assert it has a `SectionErrorBoundary` ancestor within
      the same JSX tree. Report violations as tag names in `unwrappedElements` and assert that array
      is empty.
      Seed `PRESENTATIONAL_ALLOWLIST` with ONLY the tags that actually remain in the rewired file, and
      add an inline comment beside each recording that it was grepped for `useQuery`/`usePaginatedQuery`
      and found clean, with the date. Do not carry forward a tag that no longer appears — an allowlist
      entry for an absent tag is an unexamined exemption waiting for a future reintroduction.
      Resolve the tag identity through the file's import specifiers rather than trusting the literal
      JSX tag string, so an aliased import (`import Foo as SectionErrorBoundary`) cannot defeat the
      check. The current import block is unaliased; the resolution is there so it stays true.

    Record the ratchet's own stated limitation in a header comment, as a limitation and not a hedge:
    this walk covers ONE file, which is sound only while `Analytics.tsx` composes its own sections
    inline. If a later phase extracts a layout component out of this page, this test silently stops
    covering whatever moved, and its file target must move with it.
  </action>
  <acceptance_criteria>
    - `src/pages/Analytics.structuralGuard.test.ts` exists; `grep -c "ts.createSourceFile" src/pages/Analytics.structuralGuard.test.ts` returns at least 1.
    - `grep -cE "cacheStats|subscriptionUsage|billedOverTime|getActiveAnomalies|eventCountsByPeriod|advisorEvents" src/pages/Analytics.structuralGuard.test.ts` returns 0 — the ratchet must contain no list of today's query names.
    - `REACT_SAFE_HOOKS` contains only React/react-router builtins; no Convex hook name appears in it.
    - Every entry in `PRESENTATIONAL_ALLOWLIST` has an inline comment recording the `useQuery` grep result and its date, and every entry actually appears in the current `src/pages/Analytics.tsx`. Paste the verifying grep output into the summary.
    - `npx vitest run src/pages/Analytics.structuralGuard.test.ts` passes with `hoistedHooks` empty and `unwrappedElements` empty.
    - `npx tsc --noEmit` reports no errors — the file imports `typescript` types, so a type error here must fail the type gate as well as the test.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/pages/Analytics.structuralGuard.test.ts</automated>
  </verify>
  <done>Two AST-derived checks pass against the live `Analytics.tsx`, with no query name enumerated anywhere in the ratchet.</done>
</task>

<task type="auto">
  <name>Task 2: Mutation-test the ratchet with a synthetic hook and a synthetic unwrapped element</name>
  <files>src/pages/Analytics.structuralGuard.test.ts</files>
  <read_first>
    - `src/pages/Analytics.structuralGuard.test.ts` as written in Task 1
    - `src/pages/Analytics.tsx` — the exact text of the `export default function Analytics() {` line and the opening of the returned JSX, so the injections land at a valid position
    - `convex/aggregates.test.ts` around the `paginate()` single-call guard comment — the nearest in-repo precedent for "a guard must be shown to actually constrain", quoted for its reasoning, not its mechanism
    - `.planning/phases/121-analytics-query-resilience/121-VALIDATION.md` § "Assertion Discipline", rule 3
  </read_first>
  <action>
    Add a `describe("the ratchet actually constrains")` block with three cases, all operating on the
    real file's text held in memory — never write a mutated file to disk.

    Case A — synthetic hoisted hook. Take the real source, insert
    `  const __syntheticProbe = useTotallyNewThingNobodyHasWrittenYet();`
    immediately after the `export default function Analytics() {` line. The hook name must be one that
    appears NOWHERE in this repository's history — grep for it first and report the zero — because
    that is precisely what a by-name guard could never catch and is therefore the whole point of the
    case. Assert `analyzeAnalyticsSource(mutated).hoistedHooks` contains that name.

    Case B — synthetic unwrapped element. Take the real source and insert a
    `<SomeBrandNewPanelNobodyHasWrittenYet />` element into the returned JSX at a position with no
    `SectionErrorBoundary` ancestor — for example as a direct child of the outer grid `div`. Assert
    `analyzeAnalyticsSource(mutated).unwrappedElements` contains that tag. This is the case that would
    have caught the unwrapped Summary Row.

    Case C — validity precondition, asserted BEFORE the failure in each of A and B. Run
    `ts.transpileModule(mutated, { reportDiagnostics: true, compilerOptions: { jsx: ts.JsxEmit.Preserve } })`
    and assert `diagnostics` is empty. A syntactically invalid mutation produces a parse or collection
    error that reads exactly like a guard firing, which is the failure mode D-04 names explicitly. Make
    this assertion run first in each case so an invalid mutation fails the test for the right reason
    rather than being read as evidence the ratchet works.

    Also assert the negative control in the same block: `analyzeAnalyticsSource(<the UNMUTATED real source>)`
    returns empty arrays for both checks. Without it, an `analyzeAnalyticsSource` that reports a
    violation for every input would pass cases A and B while proving nothing.
  </action>
  <acceptance_criteria>
    - Three cases exist; `grep -c "transpileModule" src/pages/Analytics.structuralGuard.test.ts` returns at least 2 (one validity check per mutation).
    - `git grep -cF "useTotallyNewThingNobodyHasWrittenYet"` outside this test file returns 0; paste the command and its output into the summary as evidence the name is genuinely novel.
    - Case A asserts on `hoistedHooks`; Case B asserts on `unwrappedElements`; both assert empty `diagnostics` FIRST.
    - The unmutated-source control asserts both arrays empty in the same describe block.
    - No mutated source is ever written to disk: `grep -cE "writeFileSync|appendFileSync|fs\.write" src/pages/Analytics.structuralGuard.test.ts` returns 0.
    - `npx vitest run src/pages/Analytics.structuralGuard.test.ts` passes.
    - Second-order mutation recorded in the summary: temporarily replace `analyzeAnalyticsSource`'s CHECK 1 body with `return { hoistedHooks: [], unwrappedElements: [] }`, confirm cases A and B FAIL while the unmutated control still passes, then revert. This proves the mutation cases are load-bearing and not passing for an incidental reason.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/pages/Analytics.structuralGuard.test.ts</automated>
  </verify>
  <done>The ratchet is shown to fail on a query name and a component name that have never existed in this repo, with each mutation proven to parse before its failure is believed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| future contributor edit → `/analytics` route availability | The regression this ratchet guards. The threat is a well-intentioned future edit that hoists a query back into the page body. |
| test-time file read → source text | Read-only. The ratchet never writes a file and never executes the page's code. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-121-19 | Denial of Service | `/analytics` route, regression over time | mitigate | An AST-derived ratchet that fails on ANY non-builtin hook in `Analytics()`'s body and on any custom element lacking a boundary ancestor, containing no list of current query names, mutation-tested with an identifier that has never existed in the repo. |
| T-121-20 | Repudiation | a guard that passes for the wrong reason | mitigate | Every mutation is proven syntactically valid via `ts.transpileModule` diagnostics BEFORE its failure is accepted; an unmutated-source control asserts the analyzer does not report violations unconditionally; a second-order mutation stubs the analyzer to prove the cases are load-bearing. |
| T-121-21 | Tampering | allowlisted presentational tag hiding a query | mitigate | Every allowlist entry is grepped for `useQuery`/`usePaginatedQuery` with the result and date recorded inline, and entries for tags no longer present in the file are removed rather than carried forward. |
| T-121-22 | Denial of Service | ratchet coverage silently lapsing | accept, stated | The walk covers one file and is sound only while `Analytics.tsx` composes its sections inline. If a later phase extracts a layout component, coverage lapses silently. Recorded as a header-comment limitation rather than mitigated, because cross-file resolution is materially more machinery than this phase's scope supports. |
| T-121-SC | Tampering | npm/pip/cargo installs | accept — no installs | `typescript ^6.0.3` is already a devDependency; the compiler API needs no install. No package-manager install task exists, so the Package Legitimacy Gate does not apply. |
</threat_model>

<verification>
- `npx vitest run src/pages/Analytics.structuralGuard.test.ts` passes.
- `npx tsc --noEmit` clean.
- The novel-identifier grep, the allowlist `useQuery` greps, and the second-order analyzer-stub mutation are each pasted into the SUMMARY with their commands and observed output.
</verification>

<success_criteria>
- The ratchet passes against the current page with both derived sets empty.
- It fails on a synthetic hook name and a synthetic unwrapped element, each proven to parse first.
- It contains no enumeration of the queries `/analytics` reads today.
</success_criteria>

<output>
Create `.planning/phases/121-analytics-query-resilience/121-05-SUMMARY.md` when done.
</output>
