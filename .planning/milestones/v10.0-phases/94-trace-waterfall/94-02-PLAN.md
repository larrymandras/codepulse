---
phase: 94-trace-waterfall
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - C:/Users/mandr/astridr-repo/astridr/engine/telemetry.py
  - C:/Users/mandr/astridr-repo/astridr/agent/loop.py
  - C:/Users/mandr/astridr-repo/astridr/providers/anthropic_provider.py
  - C:/Users/mandr/astridr-repo/astridr/providers/openrouter.py
  - C:/Users/mandr/astridr-repo/astridr/providers/ollama.py
  - C:/Users/mandr/astridr-repo/tests/unit/test_trace_context.py
autonomous: true
requirements: [TRACE-01]
must_haves:
  truths:
    - "A fresh traceId is generated once per agent-loop turn and readable via a contextvar"
    - "Every LLM call made during a turn — from chat AND from queen/automation — attaches that turn's traceId to its llm_call payload"
    - "traceId and goalId are independent contextvars; a single llm_call may carry both, neither, or either"
    - "Ollama's llm_call emitter (which had no goalId precedent) now attaches traceId"
  artifacts:
    - path: "C:/Users/mandr/astridr-repo/astridr/engine/telemetry.py"
      provides: "_current_trace_id ContextVar + set/reset/get_trace_context trio"
      contains: "_current_trace_id"
    - path: "C:/Users/mandr/astridr-repo/astridr/agent/loop.py"
      provides: "set_trace_context/reset_trace_context wrap around _process_inner"
      contains: "set_trace_context"
    - path: "C:/Users/mandr/astridr-repo/astridr/providers/ollama.py"
      provides: "net-new traceId attach on ollama llm_call payload"
      contains: "get_trace_context"
  key_links:
    - from: "astridr/agent/loop.py _process_inner"
      to: "astridr/engine/telemetry.py set_trace_context"
      via: "set-before-work / reset-in-finally"
      pattern: "set_trace_context"
    - from: "astridr providers (anthropic/openrouter/ollama)"
      to: "astridr/engine/telemetry.py get_trace_context"
      via: "read-and-attach at each llm_call emit site"
      pattern: "get_trace_context"
---

<objective>
Make `traceId` real on the producer side (Ástríðr, cross-repo). Nothing emits a `traceId` today — "ingest pass-through" is dead weight without this (D-01). Add a per-turn `_current_trace_id` contextvar (mirroring the existing `goalId` trio), set it once at the universal loop choke point `_process_inner`, and read-and-attach it at all three provider `llm_call` emit sites so chat AND automation turns are traced uniformly (D-02/D-03).

Purpose: One traceId per agent-loop turn, propagated with one mechanism to every emitting provider, so CodePulse can group a session's calls into turns.
Output: New contextvar trio in `telemetry.py`, insertion in `loop.py`, attach blocks in the three provider files, and a `test_trace_context.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/94-trace-waterfall/94-CONTEXT.md
@.planning/phases/94-trace-waterfall/94-RESEARCH.md
@.planning/phases/94-trace-waterfall/94-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the _current_trace_id contextvar trio and set it per-turn in _process_inner</name>
  <read_first>
    - C:/Users/mandr/astridr-repo/astridr/engine/telemetry.py (goalId precedent: _current_goal_id at lines 81-83; set_/reset_/get_goal_context trio at lines 546-561; note the `# contextvar-ok:` marker convention)
    - C:/Users/mandr/astridr-repo/astridr/agent/loop.py (process() -> _process_inner try/finally shape, lines 797-838; _process_inner entry at 819+; run_state.turn_count increment)
    - C:/Users/mandr/astridr-repo/astridr/channels/router.py (lines 489-516 — the set_goal_context(uuid4())/reset-in-finally SHAPE to mirror; NOT the insertion site for traceId)
    - C:/Users/mandr/astridr-repo/astridr/automation/queen.py (goalId set/reset at lines 744-797 — evidence automation bypasses router.py, so router.py is the WRONG insertion point for traceId)
    - C:/Users/mandr/astridr-repo/tests/unit/tools/test_delegate_task_goal_grouping.py (existing goalId-contextvar test to mirror for the new trace test)
  </read_first>
  <behavior>
    - After set_trace_context(x), get_trace_context() returns x
    - After reset_trace_context(token), get_trace_context() returns the prior value (None at top level)
    - traceId and goalId are stored in separate ContextVars — setting one does not change the other
    - Entering _process_inner sets a fresh non-None traceId; leaving it (finally) resets to the prior value even on exception
  </behavior>
  <action>
    In telemetry.py, add a `_current_trace_id: contextvars.ContextVar[str | None]` declared exactly like `_current_goal_id` (default=None, with a `# contextvar-ok: trace ID, not a secret` marker), plus a `set_trace_context(trace_id)` / `reset_trace_context(token)` / `get_trace_context()` trio mirroring the goalId functions in shape. This MUST be a DISTINCT variable from `_current_goal_id` — do not reuse or alias goalId (RESEARCH.md Pitfall 3: a goal spans many turns; a plain-chat turn has a traceId but no goal; both may coexist on one call).

    In loop.py, wrap the body of `_process_inner` so a fresh `set_trace_context(str(uuid.uuid4()))` runs at entry and `reset_trace_context(token)` runs in a `finally` — mirroring router.py's set/try/finally shape but applied HERE, at `_process_inner`, because it is the single per-`process()`-call choke point reached by both `router.py` chat turns and `queen.py`/automation turns (RESEARCH.md Pitfall 2 / A1). Use a bare `uuid.uuid4()` with no prefix, matching the goalId format precedent (router.py:498). Ensure `uuid` is imported in loop.py (add the import if absent). Do not touch the existing goal-context plumbing.

    Add C:/Users/mandr/astridr-repo/tests/unit/test_trace_context.py covering the four behaviors above (set/get roundtrip, reset restores prior, independence from goalId, and — via a thin wrapper or direct call if feasible — that a turn sets a non-None traceId and resets on exit). Mirror the structure of the existing goalId-contextvar test.
  </action>
  <verify>
    <automated>cd /c/Users/mandr/astridr-repo && python -m pytest tests/unit/test_trace_context.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `rg "_current_trace_id" C:/Users/mandr/astridr-repo/astridr/engine/telemetry.py` matches the ContextVar declaration
    - `rg "def (set|reset|get)_trace_context" C:/Users/mandr/astridr-repo/astridr/engine/telemetry.py` matches all three functions
    - telemetry.py still contains `_current_goal_id` unchanged (both vars present and distinct)
    - `rg "set_trace_context" C:/Users/mandr/astridr-repo/astridr/agent/loop.py` matches inside/around _process_inner, and `rg "reset_trace_context" C:/Users/mandr/astridr-repo/astridr/agent/loop.py` matches in a finally block
    - `rg "set_trace_context" C:/Users/mandr/astridr-repo/astridr/channels/router.py` returns NOTHING (insertion is at the loop, not the router)
    - `python -m pytest tests/unit/test_trace_context.py -q` passes (run from astridr-repo)
  </acceptance_criteria>
  <done>A distinct per-turn traceId contextvar exists and is set/reset at _process_inner for both chat and automation turns; tests green.</done>
</task>

<task type="auto">
  <name>Task 2: Attach traceId at all three provider llm_call emit sites</name>
  <read_first>
    - C:/Users/mandr/astridr-repo/astridr/providers/anthropic_provider.py (goalId attach block, lines 560-579 — the _gid read-and-attach pattern to mirror for _tid)
    - C:/Users/mandr/astridr-repo/astridr/providers/openrouter.py (goalId attach block, lines 308-325 — identical shape)
    - C:/Users/mandr/astridr-repo/astridr/providers/ollama.py (llm_call emit block, lines 213-228 — CONFIRMED to have NO get_goal_context() call; traceId attach here is net-new, shaped after the sibling providers)
    - C:/Users/mandr/astridr-repo/astridr/engine/telemetry.py (get_trace_context added in Task 1; sessionId is auto-injected in send() but goalId is read per-site — follow the goalId per-site pattern, NOT the sessionId auto-inject pattern)
  </read_first>
  <action>
    In anthropic_provider.py and openrouter.py: alongside the existing `_gid = _get_goal_ctx()` block, import `get_trace_context as _get_trace_ctx` from astridr.engine.telemetry and add `_tid = _get_trace_ctx()` then `if _tid: _llm_payload["traceId"] = _tid`, immediately after the goalId attach. Do not change any existing goalId behavior.

    In ollama.py: this emit site passes an inline dict literal directly to `self._telemetry.send("llm_call", {...})` and has no goal-context read today. Refactor it to build a named `_llm_payload` dict from the same existing keys (provider, model, inputTokens, outputTokens, cachedTokens, costUsd, latencyMs, finishReason, hasToolCalls — preserve every current field and value verbatim), then read `_tid = _get_trace_ctx()` and conditionally attach `_llm_payload["traceId"] = _tid`, then send `_llm_payload`. Attach ONLY traceId — do NOT add goalId to ollama (out of scope; D-03 is about uniform traceId, not backfilling goalId).

    Use the per-site read-and-attach convention (like goalId), NOT the central auto-inject convention (like sessionId in send()) — mixing auto-inject into send() adds branching for only 3 call sites and is an explicit anti-pattern (RESEARCH.md).
  </action>
  <verify>
    <automated>cd /c/Users/mandr/astridr-repo && python -m pytest tests/unit/test_trace_context.py -q</automated>
    <automated>cd /c/Users/mandr/astridr-repo && python -m py_compile astridr/providers/anthropic_provider.py astridr/providers/openrouter.py astridr/providers/ollama.py</automated>
  </verify>
  <acceptance_criteria>
    - `rg -c "get_trace_context" C:/Users/mandr/astridr-repo/astridr/providers/anthropic_provider.py C:/Users/mandr/astridr-repo/astridr/providers/openrouter.py C:/Users/mandr/astridr-repo/astridr/providers/ollama.py` shows a match in all three files
    - `rg "_llm_payload\[.traceId.\] = _tid" C:/Users/mandr/astridr-repo/astridr/providers/ollama.py` matches (net-new attach present)
    - ollama.py preserves all nine original payload keys (provider, model, inputTokens, outputTokens, cachedTokens, costUsd, latencyMs, finishReason, hasToolCalls) — grep each is still present
    - `rg "goalId" C:/Users/mandr/astridr-repo/astridr/providers/ollama.py` returns NOTHING (goalId was NOT added to ollama)
    - `python -m py_compile` on all three providers exits 0 (no syntax error)
  </acceptance_criteria>
  <done>All three providers attach traceId when present; ollama's payload is refactored to a named dict preserving every field; py_compile clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Ástríðr process internals → telemetry buffer | traceId is a self-generated uuid4, never operator-supplied input |
| Ástríðr telemetry → CodePulse `/runtime-ingest` | outbound HTTP; auth is the existing Bearer key on the ingest side |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-94-03 | Information Disclosure | traceId value in logs/telemetry | accept | traceId is an opaque uuid4 grouping key, not a secret — same classification as the existing goalId (marked `# contextvar-ok: not a secret`). No PII, no credential. |
| T-94-04 | Tampering | Reusing/aliasing goalId as traceId collapsing two distinct groupings | mitigate | Distinct ContextVar enforced (Pitfall 3); acceptance criteria assert both vars coexist and router.py is untouched. |
| T-94-SC | Tampering | pip installs | accept | This plan installs ZERO new packages — uses only stdlib `uuid`/`contextvars` already imported. No supply-chain surface. |
</threat_model>

<verification>
- `python -m pytest tests/unit/test_trace_context.py -q` green (run from astridr-repo)
- `python -m py_compile` clean on all three providers
- router.py contains no trace-context code; goalId plumbing unchanged
- Live end-to-end confirmation is deferred to Plan 05 (D-05 operator gate) — this plan's automated bar is unit + syntax only, since a live trace requires the deployed CodePulse ingest from Plan 01 and a running astridr stack
</verification>

<success_criteria>
- A distinct per-turn traceId contextvar exists and is set at _process_inner (covers chat + automation)
- All three providers attach traceId to their llm_call payload when set
- goalId behavior and router.py are unchanged
- Unit tests + py_compile green
</success_criteria>

<output>
Create `.planning/phases/94-trace-waterfall/94-02-SUMMARY.md` when done
</output>
