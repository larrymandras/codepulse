---
phase: 95-hardening-security-audit-key-rotation-dependency-majors
plan: 03
type: execute
wave: 3
depends_on: [95-01, 95-02, 95-04]
files_modified:
  - .gitignore
  - .planning/phases/95-hardening-security-audit-key-rotation-dependency-majors/95-SECURITY-AUDIT.md
  - .planning/REQUIREMENTS.md
  - convex/ingestAuth.ts
autonomous: false
requirements: [HARD-01]

must_haves:
  truths:
    - "The /cso audit ran against the settled shipped tree (src/ + convex/ + build/config)"
    - "npm audit result (0 vulnerabilities) is recorded"
    - "GitHub secret-scan result is recorded"
    - "Every confirmed finding has file:line evidence and a remediation status; no open confirmed findings remain (fixed or operator-approved-deferred)"
    - "95-SECURITY-AUDIT.md is committed with confirmed findings, remediation status, and a 'what I dropped and why' precision note"
  artifacts:
    - path: ".planning/phases/95-hardening-security-audit-key-rotation-dependency-majors/95-SECURITY-AUDIT.md"
      provides: "Durable audit record (D-07)"
      contains: "Confirmed Findings"
    - path: ".gitignore"
      provides: ".audits/ ignored so raw skill output is not committed"
      contains: ".audits"
  key_links:
    - from: "95-SECURITY-AUDIT.md"
      to: "convex/ingestAuth.ts"
      via: "fail-open/fail-closed asymmetry finding with file:line evidence"
      pattern: "ingestAuth"
---

<objective>
Run HARD-01 LAST (D-11) so it certifies the shipped tree Plans 01/02 settled (Pitfall 5). Invoke the `/cso` skill (Skill tool — do NOT reimplement) over CodePulse repo scope only (src/ + convex/ + build/config; D-04), supplement with `npm audit` + GitHub secret scanning (D-06), and honor the inventory → confirm → fix gate (D-05): produce a confirmed-findings inventory, get operator approval on the fix list, then remediate in one pass. Deliver the durable record `95-SECURITY-AUDIT.md` (D-07).

Purpose: A short list of confirmed, file:line-evidenced findings (zero-false-positive bar) that certifies the actually-shipped code, with the highest-value target being `convex/ingestAuth.ts`'s fail-open/fail-closed asymmetry reported honestly.
Output: Committed `95-SECURITY-AUDIT.md` + any operator-approved remediations.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@convex/ingestAuth.ts
@convex/http.ts
@.planning/phases/95-hardening-security-audit-key-rotation-dependency-majors/95-RESEARCH.md
@.planning/phases/95-hardening-security-audit-key-rotation-dependency-majors/95-CONTEXT.md

<interfaces>
<!-- /cso skill mechanics + output shape (VERIFIED: SKILL.md read). -->
Skill: invoke via the Skill tool (single-pass, confidence-gated 8/10, zero-false-positive doctrine). Writes .audits/security/<YYYY-MM-DD>-<HHMMSS>.{md,json}. Create .audits/ and gitignore it.
JSON schema: {findings:[{id,title,severity,confidence,status,file,line,quote,exploit,fix}], surface_summary, supply_chain, filter_stats, dropped, scope, threshold}.
Report sections: Verdict (ship/fix-first/block) · Findings (file:line + quote + exploit + fix) · Surface map · Supply chain · "What I dropped and why".

<!-- Highest-value audit target (VERIFIED: convex/ingestAuth.ts read). -->
validateIngestAuth (ingestAuth.ts:72-77): "if (!expectedKey) return true" — FAILS OPEN when ASTRIDR_INGEST_API_KEY unset (whole /ingest + /runtime-ingest family accepts anonymous writes).
validateForgeIngestAuth (ingestAuth.ts:88-97): "if (!expectedKey) return _env.FORGE_INGEST_ALLOW_ANON === 'true'" — FAILS CLOSED.
getCorsHeadersWithAllowlist (ingestAuth.ts:35-57): ACAO="*" fallback when allowlist null (env unset) — fail-open-by-design for dev.
The asymmetry is prod-mitigated (ASTRIDR key set + verified 07-05), so report it CONFIRMED, LOW-severity, MITIGATED — with exploit path + fix option — for operator triage per D-05, NOT an automatic fix.

<!-- Corroborating facts. -->
npm audit: currently 0 vulnerabilities (RESEARCH-verified). CI already runs a "Secret Scan" GitHub Action (passed on PR #50). Pattern to mirror: Phase 94's 94-SECURITY.md (Trust Boundaries · Threat Register · Accepted Risks Log · Security Audit Trail · Sign-Off).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run /cso + npm audit + secret scan; write the confirmed-findings inventory into 95-SECURITY-AUDIT.md</name>
  <files>.gitignore, .planning/phases/95-hardening-security-audit-key-rotation-dependency-majors/95-SECURITY-AUDIT.md</files>
  <read_first>
    - C:/Users/mandr/.claude/skills/cso/SKILL.md (the audit skill mechanics + output schema — invoke, do not reimplement)
    - convex/ingestAuth.ts (the highest-value target — fail-open/fail-closed asymmetry at lines 72-77 vs 88-97)
    - convex/http.ts (the 43-route httpAction ingest surface + CORS)
    - .planning/phases/95-hardening-security-audit-key-rotation-dependency-majors/95-RESEARCH.md (Security Domain: ASVS map, threat patterns, /cso output shape for D-07)
  </read_first>
  <action>
    Invoke the `/cso` skill via the Skill tool over CodePulse repo scope ONLY (src/ + convex/ + build/config surfaces; D-04 — Ástríðr-side seams are out of scope, any cross-repo observation becomes an astridr follow-up NOTE, not phase work). First create the `.audits/` directory and add `.audits/` to `.gitignore` (the skill writes raw output to `.audits/security/<ts>.{md,json}`; that raw output is NOT committed — only the curated 95-SECURITY-AUDIT.md is). Run `npm audit` (expect 0 vulnerabilities) and GitHub secret scanning via the MCP `run_secret_scanning` tool (D-06; this also corroborates HARD-02 evidence). Then author `95-SECURITY-AUDIT.md` in the phase directory following the Phase 94 94-SECURITY.md structure (Trust Boundaries · Threat Register · Accepted Risks Log · Security Audit Trail · Sign-Off) plus the /cso report sections (Verdict · Confirmed Findings with file:line+quote+exploit+fix · Surface map · Supply chain · "What I dropped and why"). Every finding MUST carry file:line evidence and a quote; DROP any finding you cannot substantiate from live code (zero-false-positive bar). Explicitly include the confirmed `convex/ingestAuth.ts` fail-open/fail-closed asymmetry as CONFIRMED / LOW-severity / prod-MITIGATED, with the exploit path ("if ASTRIDR_INGEST_API_KEY is ever unset in prod, the entire /ingest + /runtime-ingest family accepts anonymous writes") and a fix option (make it fail-closed like the Forge path, or add a prod boot assertion) — leave its status as "pending operator decision" for Task 2, do NOT auto-fix. Append the npm-audit + secret-scan results and the "what I dropped and why" one-liner. This is the INVENTORY step of D-05 — no fixes yet.
  </action>
  <verify>
    <automated>test -f .planning/phases/95-hardening-security-audit-key-rotation-dependency-majors/95-SECURITY-AUDIT.md && grep -q "Confirmed Findings" .planning/phases/95-hardening-security-audit-key-rotation-dependency-majors/95-SECURITY-AUDIT.md && grep -q "ingestAuth" .planning/phases/95-hardening-security-audit-key-rotation-dependency-majors/95-SECURITY-AUDIT.md && grep -qi "npm audit" .planning/phases/95-hardening-security-audit-key-rotation-dependency-majors/95-SECURITY-AUDIT.md</automated>
  </verify>
  <acceptance_criteria>
    - `.audits/` exists and is listed in `.gitignore`
    - `95-SECURITY-AUDIT.md` exists in the phase directory with sections: Verdict, Confirmed Findings, Surface map, Supply chain, "What I dropped and why"
    - The convex/ingestAuth.ts fail-open/fail-closed asymmetry appears as a confirmed finding with file:line (ingestAuth.ts:72-77 vs 88-97), exploit path, and fix option
    - npm audit result (0 vulnerabilities) and secret-scan result are recorded in the doc
    - Every listed finding has file:line evidence and a quote (no speculative findings)
  </acceptance_criteria>
  <done>Audit inventory complete: /cso + npm audit + secret scan run, 95-SECURITY-AUDIT.md drafted with evidenced confirmed findings, no fixes applied yet.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 2: Operator reviews confirmed findings and approves the fix list (D-05 gate)</name>
  <files>.planning/phases/95-hardening-security-audit-key-rotation-dependency-majors/95-SECURITY-AUDIT.md</files>
  <read_first>
    - .planning/phases/95-hardening-security-audit-key-rotation-dependency-majors/95-SECURITY-AUDIT.md (the inventory produced in Task 1)
    - .planning/phases/95-hardening-security-audit-key-rotation-dependency-majors/95-CONTEXT.md (D-05: inventory → confirm → fix; deferral must be explicit)
  </read_first>
  <action>Pause and present the Task 1 confirmed-findings inventory for operator triage (D-05 inventory → confirm → fix gate). Do not auto-approve. Capture per-finding dispositions (fix now vs accept-with-rationale), especially the headline ingestAuth fail-open decision, then hand the approved fix list to Task 3.</action>
  <acceptance_criteria>
    - Operator has selected a disposition for the ingestAuth asymmetry (fix-failclosed | fix-bootassert | accept-mitigated)
    - Every other confirmed finding is explicitly approved-for-fix or deferred-with-one-line-rationale
    - No finding is left without a disposition
  </acceptance_criteria>
  <decision>Which confirmed findings get fixed in this phase, and which (if any) are explicitly deferred with recorded rationale? The headline decision is the ingestAuth fail-open asymmetry: fix now (make /ingest family fail-closed like /forge-ingest, or add a prod boot assertion) or accept as prod-mitigated with a recorded rationale.</decision>
  <context>Per D-05, the audit produces a confirmed-findings inventory; the operator approves the fix list; then all approved findings are remediated in one pass. "No open confirmed findings" means fixed OR operator-approved-deferred with rationale. The ingestAuth asymmetry is real but prod-mitigated (ASTRIDR key set + verified 07-05), so it is a legitimate accept-with-rationale candidate, not a forced fix.</context>
  <options>
    <option id="fix-failclosed">
      <name>Fix ingestAuth to fail closed</name>
      <pros>Removes the latent anonymous-write path entirely; symmetric with /forge-ingest; defense-in-depth if the env var is ever dropped</pros>
      <cons>Changes dev behavior (unauthenticated local ingest now requires an explicit anon opt-in); small code change + test update</cons>
    </option>
    <option id="fix-bootassert">
      <name>Add a prod boot assertion that ASTRIDR_INGEST_API_KEY is set</name>
      <pros>Keeps dev fail-open convenience; makes the prod-unset case loud instead of silent</pros>
      <cons>Assertion placement/runtime nuance in Convex; still fail-open by default in code</cons>
    </option>
    <option id="accept-mitigated">
      <name>Accept as prod-mitigated with recorded rationale</name>
      <pros>Honest to the verified-07-05 state; zero code churn; documented in the Accepted Risks Log</pros>
      <cons>Latent asymmetry remains in code; relies on the env var staying set</cons>
    </option>
  </options>
  <resume-signal>Select per-finding dispositions: for the ingestAuth asymmetry choose fix-failclosed | fix-bootassert | accept-mitigated, and approve/defer each other confirmed finding (deferrals need a one-line rationale).</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Remediate approved findings in one pass and finalize 95-SECURITY-AUDIT.md</name>
  <files>convex/ingestAuth.ts, .planning/phases/95-hardening-security-audit-key-rotation-dependency-majors/95-SECURITY-AUDIT.md, .planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/phases/95-hardening-security-audit-key-rotation-dependency-majors/95-SECURITY-AUDIT.md (findings + operator dispositions from Task 2)
    - convex/ingestAuth.ts (the file most likely to be remediated, depending on the Task 2 decision)
    - .planning/REQUIREMENTS.md (HARD-01 checkbox at line 20)
  </read_first>
  <action>
    Apply every operator-APPROVED fix from Task 2 in one pass (matches the standing inventory → confirm → fix-in-one-pass bug pattern, D-05). If the operator chose fix-failclosed or fix-bootassert for the ingestAuth asymmetry, implement it in `convex/ingestAuth.ts` and add/adjust the corresponding unit test (tests live alongside source, Vitest+jsdom); if accept-mitigated, make NO code change and instead record it in the Accepted Risks Log. After any code change, run the green bar `npx tsc --noEmit && npx vitest run && npm run build` (all exit 0). Update each finding's status in `95-SECURITY-AUDIT.md` to fixed / accepted-deferred (with rationale) so NO finding is left "open" — the Verdict line must read ship. Confirm the "what I dropped and why" precision note is present. Then check the HARD-01 box in `.planning/REQUIREMENTS.md` (mark it resolved: /cso run, findings remediated or explicitly accepted, npm audit 0 vulns, secret scan clean). Record any Ástríðr-side observations as astridr follow-up notes in the doc (D-04) — do NOT fix cross-repo here.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run && npm run build && ! grep -qiE "status:\s*open|\bopen finding" .planning/phases/95-hardening-security-audit-key-rotation-dependency-majors/95-SECURITY-AUDIT.md</automated>
  </verify>
  <acceptance_criteria>
    - Every approved fix is applied; every deferred finding has a recorded rationale in the Accepted Risks Log
    - No finding in 95-SECURITY-AUDIT.md has status "open"; the Verdict line reads "ship"
    - If code changed: `npx tsc --noEmit && npx vitest run && npm run build` all exit 0, and the changed auth path has a matching test
    - REQUIREMENTS.md HARD-01 box is checked with a resolution note
    - "What I dropped and why" precision note present in the doc
  </acceptance_criteria>
  <done>All approved findings remediated (or explicitly accepted), audit doc finalized to ship verdict, HARD-01 marked resolved, green bar passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Ástríðr / external client → CodePulse ingest httpActions | Untrusted server-to-server writes cross into Convex via bearer-authed /ingest, /runtime-ingest, /forge-ingest family (43 routes, convex/http.ts) |
| Browser → CodePulse httpActions (CORS) | Cross-origin reads gated by CODEPULSE_ALLOWED_ORIGIN allowlist |
| npm registry → repo dependency tree | Supply-chain surface (npm audit) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-95-01 | Spoofing / Tampering | validateIngestAuth (convex/ingestAuth.ts:72-77) | mitigate (prod) / operator-decision | Fail-OPEN when ASTRIDR_INGEST_API_KEY unset — whole /ingest+/runtime-ingest family accepts anonymous writes. Prod-mitigated (key set, verified 07-05). Reported CONFIRMED/LOW/MITIGATED; Task 2 operator decides fail-closed vs boot-assert vs accept. |
| T-95-02 | Info disclosure | getCorsHeadersWithAllowlist (ingestAuth.ts:44-46) | accept | ACAO="*" only when CODEPULSE_ALLOWED_ORIGIN unset (dev fallback); prod checklist requires the env var. Confirm via `convex env list`. |
| T-95-03 | Elevation | FORGE_INGEST_ALLOW_ANON | mitigate | Correct fail-closed default; audit confirms it is not "true" in prod. |
| T-95-04 | Info disclosure | Secrets in git history | mitigate | GitHub `run_secret_scanning` (D-06) + existing CI Secret Scan step; results recorded in the audit doc. |
| T-95-05 | Vulnerable components | dependency tree | mitigate | `npm audit` (currently 0 vulns) recorded in the audit doc. |
| T-95-SC | Tampering | npm/build supply chain | accept | No new installs in this plan; audit certifies the settled tree from Plans 01/02. |

This is the real audit surface for the phase; the register above is the honest, evidenced threat model (not invented).
</threat_model>

<verification>
- `/cso` run over src/ + convex/ + build/config (D-04 scope)
- `npm audit` result (0 vulns) + secret-scan result recorded
- 95-SECURITY-AUDIT.md has Verdict=ship, all findings fixed or accepted-with-rationale, "what I dropped and why" note
- `npx tsc --noEmit && npx vitest run && npm run build` green after any remediation
- REQUIREMENTS.md HARD-01 checked
</verification>

<success_criteria>
No open confirmed findings remain — each is fixed or operator-approved-deferred with rationale. The committed 95-SECURITY-AUDIT.md certifies the shipped tree with file:line-evidenced findings, npm-audit + secret-scan results, and the precision note. HARD-01 resolved.
</success_criteria>

<output>
Create `.planning/phases/95-hardening-security-audit-key-rotation-dependency-majors/95-03-SUMMARY.md` when done.
</output>
