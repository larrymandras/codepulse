---
phase: 95-hardening-security-audit-key-rotation-dependency-majors
plan: 04
type: execute
wave: 2
depends_on: [95-01]
files_modified:
  - .planning/REQUIREMENTS.md
autonomous: false
requirements: [HARD-02]

must_haves:
  truths:
    - "The Forge daemon's local env (FORGE_INGEST_API_KEY + 3 ingest URLs) matches the Convex value and targets the .convex.site host"
    - "Fresh rows from real emitters (Forge daemon + Ástríðr) land in prod Convex tidy-whale-981 during a live round trip"
    - "REQUIREMENTS.md HARD-02 wording and the forge-deployment-tidy-whale-981 memory reflect the verified close-out"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "HARD-02 verified-close-out wording (placeholder retired)"
      contains: "HARD-02"
  key_links:
    - from: "Forge daemon + Ástríðr emitters"
      to: "prod Convex tidy-whale-981 (.convex.site httpActions)"
      via: "bearer-authed POST to /forge-ingest and /runtime-ingest family"
      pattern: "forge-ingest|runtime-ingest"
---

<objective>
Close out HARD-02 as a verification + documentation task (D-01: NO new rotation — the 2026-07-05 secret verification stands). Two things remain: confirm the ONE un-verified side (the Forge daemon's local env, watching the `.convex.site` vs `.cloud` host gotcha) and prove the live real-emitter round trip (D-02: Forge daemon + Ástríðr each POST organically; fresh rows land in prod Convex). Then update the records (D-03).

Purpose: The 07-05 check covered only the Ástríðr side. HARD-02's honest close requires the Forge-daemon side + a live E2E round trip, matching the Phase 93 D-04 / Phase 94 D-05 bar (convex-test/CI green alone does not close it).
Output: Verified live ingest, updated REQUIREMENTS.md + memory file.

Independence note: This plan needs the live stack and is independent of the code/dependency work (D-11). It depends on Plan 01 only to serialize the shared REQUIREMENTS.md edit — not for any code artifact.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@docs/forge-deploy-checklist.md
@.planning/phases/95-hardening-security-audit-key-rotation-dependency-majors/95-RESEARCH.md
@.planning/phases/95-hardening-security-audit-key-rotation-dependency-majors/95-CONTEXT.md

<interfaces>
<!-- HARD-02 env + host rules (VERIFIED from memory forge-deployment-tidy-whale-981 + docs/forge-deploy-checklist.md). -->
Prod Convex deployment: tidy-whale-981. httpActions host = https://tidy-whale-981.convex.site (NOT .cloud — Pitfall 3).
Already verified 07-05 (Ástríðr side): FORGE_INGEST_API_KEY (48 chars) + ASTRIDR_INGEST_API_KEY (43 chars) real on prod; astridr-repo .env ASTRIDR_INGEST_API_KEY matches; astridr CONVEX_URL = https://tidy-whale-981.convex.site.

Un-verified side (Forge daemon local env, from docs/forge-deploy-checklist.md):
- FORGE_INGEST_API_KEY (must match the Convex value)
- CONVEX_FORGE_INGEST_URL (base; daemon appends /forge-ingest)
- FORGE_LOG_INGEST_URL (full path incl. /forge-log-ingest)
- FORGE_FILE_INGEST_URL (full path incl. /forge-file-ingest)
Daemon: separate forge dir, ephemeral loopback ports (printed in "[forge] listening on …" banner), env set in the SAME shell before `npm run dev` in the forge dir.

Ingest routes (convex/http.ts): Forge → /forge-ingest, /forge-log-ingest, /forge-file-ingest (auth validateForgeIngestAuth, fail-CLOSED). Ástríðr → /runtime-ingest + /ingest family (auth validateIngestAuth). Target tables: jobs/workspaces (forge), llmMetrics/dockerContainers/sessions/events (Ástríðr).

Pitfall 4: war-room containers run the pre-94 image and emit stale/untraced rows — do NOT count them as HARD-02 evidence. Filter by post-test timestamp + specific emitter identity.

.env files are hook-blocked — NEVER read them. Verify by round-trip behavior + `npx convex env list` char-count, not by printing secret values.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Confirm Forge-daemon env + live real-emitter round trip into prod Convex</name>
  <files>docs/forge-deploy-checklist.md</files>
  <read_first>
    - docs/forge-deploy-checklist.md (OPS-01 rotation/verification checklist — the 4 daemon env vars + host rules)
    - C:/Users/mandr/.claude/projects/C--Users-mandr-codepulse/memory/forge-deployment-tidy-whale-981.md (07-05 verification record, .site vs .cloud rules, rotation procedure)
    - .planning/phases/95-hardening-security-audit-key-rotation-dependency-majors/95-RESEARCH.md (HARD-02 deep dive, Pitfalls 3 & 4)
  </read_first>
  <action>Pause for the operator-run live verification (inherently manual — requires the Forge daemon + Ástríðr running as real emitters, D-02). Automated support WITHOUT reading .env: `npx convex env list` (char-count comparison) and Convex table inspection for fresh rows. Present the how-to-verify steps below, coordinate with the pending Phase-93 redeploy (piggyback, do not duplicate `npx convex deploy`), and wait for the resume signal. Do not auto-approve.</action>
  <acceptance_criteria>
    - `npx convex env list` confirms FORGE_INGEST_API_KEY (48 chars) + ASTRIDR_INGEST_API_KEY (43 chars) present and FORGE_INGEST_ALLOW_ANON is not "true"
    - Forge daemon env vars target `https://tidy-whale-981.convex.site` (not .cloud) and the key matches the Convex value (verified by successful round trip, not by printing the secret)
    - Fresh rows from BOTH real emitters land in prod Convex tables, filtered by post-test timestamp + emitter identity (not stale war-room pre-94 rows)
    - Synthetic-curl-only evidence is NOT accepted (D-02 requires real emitters)
  </acceptance_criteria>
  <what-built>
    HARD-02 was substantially closed on 07-05 (Ástríðr side verified). The remaining work is inherently manual/live: confirm the Forge DAEMON's local env matches, and prove a real-emitter round trip. Automated tooling that can help WITHOUT reading .env: `npx convex env list` (char-count comparison for FORGE_INGEST_API_KEY / ASTRIDR_INGEST_API_KEY / CODEPULSE_ALLOWED_ORIGIN / FORGE_INGEST_ALLOW_ANON), and Convex table inspection to confirm fresh rows.
  </what-built>
  <how-to-verify>
    1. Confirm prod Convex env without printing secrets: `npx convex env list` — verify FORGE_INGEST_API_KEY (48 chars) and ASTRIDR_INGEST_API_KEY (43 chars) are present, and FORGE_INGEST_ALLOW_ANON is NOT "true".
    2. Confirm the Forge daemon's local env (the un-verified side): in the forge dir shell, ensure FORGE_INGEST_API_KEY matches the Convex value and that CONVEX_FORGE_INGEST_URL / FORGE_LOG_INGEST_URL / FORGE_FILE_INGEST_URL all target `https://tidy-whale-981.convex.site` (NOT .cloud — Pitfall 3). Do NOT read the .env file; confirm via daemon behavior + the char-count match.
    3. Start the real emitters: the Forge daemon (`npm run dev` in the forge dir, env set in the same shell) and Ástríðr (Docker). Coordinate with the pending Phase-93 Convex redeploy in STATE.md "Operator Next Steps" — piggyback on it rather than issuing a duplicate `npx convex deploy` (Claude's discretion, D-11 open question 3).
    4. Trigger organic POSTs: let the Forge daemon POST a job-state/log/file event and Ástríðr POST a runtime event, each with its configured key.
    5. Confirm fresh rows in prod Convex tidy-whale-981: inspect the forge jobs/workspaces + Ástríðr llmMetrics/sessions/events tables. Filter by post-test timestamp and emitter identity — do NOT count stale war-room pre-94 rows (Pitfall 4). Synthetic curl alone does NOT satisfy D-02.
    6. Record the round-trip evidence (timestamps, table names, emitter identity) for Task 2 and optionally into 95-SECURITY-AUDIT.md (Plan 03).
  </how-to-verify>
  <resume-signal>Type "verified" with the observed fresh-row evidence (table + timestamp + emitter), or describe where the round trip failed (e.g. daemon logged a POST but no row appeared → likely .cloud/.site misroute, Pitfall 3).</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Update records — REQUIREMENTS.md HARD-02 + forge-deployment memory (D-03)</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (HARD-02 line 21 — retire the `<new-strong-secret>` placeholder wording)
    - C:/Users/mandr/.claude/projects/C--Users-mandr-codepulse/memory/forge-deployment-tidy-whale-981.md (the memory to update with the daemon-side + round-trip close-out)
    - .planning/phases/95-hardening-security-audit-key-rotation-dependency-majors/95-CONTEXT.md (D-01/D-03 close-out framing)
  </read_first>
  <action>
    Per D-03: rewrite REQUIREMENTS.md HARD-02 to reflect the verified close-out — the `<new-strong-secret>` placeholder is retired; both keys are confirmed real on prod (Ástríðr side 07-05, Forge-daemon side verified this phase), and a live real-emitter round trip confirmed fresh rows in tidy-whale-981. State explicitly that NO new rotation was performed (D-01) — this was verification + documentation, not a rotation. Then update the `forge-deployment-tidy-whale-981` memory file to append the Forge-daemon-side env confirmation and the round-trip evidence (date, tables, emitter identity) from Task 1. Do NOT print or store any secret values in either file — reference char-counts and behavior only. If Task 1 surfaced secret-exposure evidence (keys in git history/logs/committed files), escalate that as new information (D-01 escalation clause) — but the default close is verified-no-rotation.
  </action>
  <verify>
    <automated>grep -niE "no new rotation|verified close-out|round.?trip" .planning/REQUIREMENTS.md | head -1</automated>
  </verify>
  <acceptance_criteria>
    - REQUIREMENTS.md HARD-02 no longer references `<new-strong-secret>` as an open placeholder
    - REQUIREMENTS.md HARD-02 states the verified close-out (both keys real, live round trip confirmed) and that no new rotation was performed
    - forge-deployment-tidy-whale-981 memory file contains the Forge-daemon-side env confirmation + round-trip evidence
    - No secret values are printed in either file (char-counts/behavior only)
  </acceptance_criteria>
  <done>REQUIREMENTS.md HARD-02 and the forge-deployment memory reflect the verified close-out; placeholder retired.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Forge daemon → prod Convex httpActions | Server-to-server bearer-authed ingest across the public internet (.convex.site) |
| Ástríðr → prod Convex httpActions | Server-to-server bearer-authed runtime ingest |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-95-02H-01 | Spoofing | /forge-ingest bearer auth | mitigate | validateForgeIngestAuth fails CLOSED; round trip confirms the daemon presents the matching FORGE_INGEST_API_KEY. Verify FORGE_INGEST_ALLOW_ANON is not "true" in prod. |
| T-95-02H-02 | Info disclosure | Secret handling during verification | mitigate | Never read .env (hook-blocked); verify via `convex env list` char-count + round-trip behavior only. No secret values written to REQUIREMENTS.md or memory. |
| T-95-02H-03 | Repudiation | Stale war-room emissions mistaken for evidence | accept | Pitfall 4 — filter evidence by post-test timestamp + emitter identity; documented explicitly. |

Honest note: this is a live-verification plan, not a code change. Its security relevance is confirming the already-set prod secrets work end-to-end and are not exposed. The fail-open/fail-closed asymmetry on the /ingest family is analyzed in Plan 03's audit, not here.
</threat_model>

<verification>
- Forge daemon env confirmed matching + targeting .convex.site
- Live real-emitter round trip: fresh rows in tidy-whale-981 (filtered by timestamp/emitter, not stale war-room rows)
- `npx convex env list` confirms both keys present + FORGE_INGEST_ALLOW_ANON not "true"
- REQUIREMENTS.md HARD-02 + forge-deployment memory updated
</verification>

<success_criteria>
HARD-02 closed honestly: both sides of the ingest key verified (Ástríðr 07-05 + Forge daemon this phase), live round trip proven with real emitters, records updated to reflect verified-no-rotation. No secret values exposed.
</success_criteria>

<output>
Create `.planning/phases/95-hardening-security-audit-key-rotation-dependency-majors/95-04-SUMMARY.md` when done.
</output>
