---
phase: 95-hardening-security-audit-key-rotation-dependency-majors
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/ui/calendar.tsx
  - tsconfig.json
  - package.json
  - package-lock.json
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [HARD-03, HARD-04]

must_haves:
  truths:
    - "react-day-picker is fully removed from the repo (no imports, not in package.json)"
    - "typescript 6.0.3 is installed and `npx tsc --noEmit` exits 0"
    - "Full Vitest suite (184 files) and `npm run build` pass on TS 6.0.3"
    - "Redundant @types/diff and @types/js-yaml devDeps are removed"
    - "REQUIREMENTS.md HARD-03/HARD-04 wording reflects the actual resolution (deletion + folded majors)"
  artifacts:
    - path: "tsconfig.json"
      provides: "TS 6.0 node-globals resolution via a types array"
      contains: "\"types\""
    - path: "package.json"
      provides: "TS6 devDep, react-day-picker removed, redundant @types removed"
      contains: "\"typescript\": \"^6.0.3\""
    - path: ".planning/REQUIREMENTS.md"
      provides: "Updated HARD-03/HARD-04 resolution wording"
      contains: "HARD-04"
  key_links:
    - from: "tsconfig.json"
      to: "@types/node ambient globals"
      via: "compilerOptions.types array"
      pattern: "\"types\"\\s*:\\s*\\[\\s*\"node\""
---

<objective>
Land the two code/dependency changes that must ship BEFORE the security audit runs (D-11): delete the dead `react-day-picker` primitive (HARD-04) and bump TypeScript 5.9.3 → 6.0.3 green (HARD-03), including removal of the two now-redundant `@types/*` stubs. Update REQUIREMENTS.md wording to reflect that HARD-04 resolved by deletion and that four majors (D-10) already landed.

Purpose: This settles the shipped code tree so the Plan 03 `/cso` audit certifies the actual state (Pitfall 5). Per RESEARCH.md, the entire TS 6.0 migration is a single-root-cause tsconfig fix (all 22 PR #50 CI errors are Node-globals-not-found), NOT a code migration.
Output: A green tree on TS 6.0.3 with react-day-picker gone and REQUIREMENTS.md wording current.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/95-hardening-security-audit-key-rotation-dependency-majors/95-RESEARCH.md
@.planning/phases/95-hardening-security-audit-key-rotation-dependency-majors/95-CONTEXT.md

<interfaces>
<!-- The existing in-repo TS-6-safe node-global workaround (Option B reference only — do NOT default to it). -->
From convex/ingestAuth.ts:9-10 — `const _env = (globalThis as any).process?.env ?? {}` with comment "The Convex tsconfig does not include @types/node." This is the per-file dodge; Option A (tsconfig `types` array) is the default fix and touches one file instead of 20+.

From tsconfig.json (current, single flat config): no `types` field, no `lib` field, `moduleResolution: "Bundler"`, `strict: true`, `skipLibCheck: true`, `include: ["src/**/*.ts","src/**/*.tsx","convex/**/*.ts"]`. There are NO tsconfig.app.json / tsconfig.node.json split configs.

PR #50 CI breakage histogram (run 28493109341): 22 errors, ONE root cause — 14× TS2591 (process/fs/path/Buffer not found), 8× TS2304 (__dirname/global not found). Zero API-removal or strictness errors. All 184 test files import describe/it/expect/vi explicitly from 'vitest'.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete the dead react-day-picker primitive (HARD-04)</name>
  <files>src/components/ui/calendar.tsx, package.json, package-lock.json</files>
  <read_first>
    - src/components/ui/calendar.tsx (the file to delete — confirm it is the sole react-day-picker importer)
    - package.json (react-day-picker at ^9.14.0, line ~47)
    - .planning/phases/95-hardening-security-audit-key-rotation-dependency-majors/95-RESEARCH.md (HARD-04 deep dive — verified dead-code chain)
  </read_first>
  <action>
    Per D-08: delete `src/components/ui/calendar.tsx` (verified sole importer of `react-day-picker`; zero consumers of the exported `Calendar`/`CalendarDayButton` — the only `Calendar`/`calendar` hits in src/ are unrelated `calendar_read`/`calendar_write` tool-name strings in ToolsStep.tsx and a prose comment in QualityTrendChart.tsx). Then run `npm uninstall react-day-picker` to remove it from package.json AND package-lock.json. There is no `@types/react-day-picker` to remove (the lib shipped its own types). Do NOT migrate — re-add via `npx shadcn add calendar` is the documented future path if a date-picker is ever needed. Do NOT touch the dependabot branch here — remote branch cleanup is Plan 02.
  </action>
  <verify>
    <automated>grep -rn "react-day-picker" src convex ; test $? -ne 0 || echo "FAIL: refs remain"</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/ui/calendar.tsx` no longer exists
    - `grep -rn "react-day-picker" src convex` returns no matches (exit 1)
    - package.json does not contain `"react-day-picker"`
    - package-lock.json no longer resolves `react-day-picker`
    - `npx tsc --noEmit` still exits 0 (on TS 5.9.3 at this point — TS6 bump is Task 2)
  </acceptance_criteria>
  <done>calendar.tsx deleted, react-day-picker gone from package.json + lockfile, no remaining references, tsc still green.</done>
</task>

<task type="auto">
  <name>Task 2: Bump TypeScript to 6.0.3 and resolve node globals at the tsconfig level (HARD-03)</name>
  <files>tsconfig.json, package.json, package-lock.json</files>
  <read_first>
    - tsconfig.json (single flat config — target state is adding a `types` array)
    - convex/ingestAuth.ts (lines 1-10 — the existing globalThis workaround comment, Option B reference)
    - .planning/phases/95-hardening-security-audit-key-rotation-dependency-majors/95-RESEARCH.md (HARD-03 deep dive: full 22-error histogram, single root cause, Option A remediation, Pitfall 2)
  </read_first>
  <action>
    Per D-09: run `npm install --save-dev typescript@6.0.3` (first-party Microsoft package; RESEARCH Package Legitimacy Audit dispositions it Approved — no legitimacy checkpoint needed). Then run `npx tsc --noEmit` to reproduce PR #50's 22 errors (14× TS2591 for process/fs/path/Buffer, 8× TS2304 for __dirname/global — all "Node global not found", zero API/strictness errors). Apply Option A (recommended): add `"types": ["node"]` to `compilerOptions` in tsconfig.json. This is low-risk because all 184 test files import vitest primitives explicitly, so restricting the auto-included `@types/*` set does not strip any relied-upon ambient global. Then run the VERIFY LOOP (Pitfall 2): re-run `npx tsc --noEmit`; if new TS2304/TS2582 errors appear (e.g. "Cannot find name 'describe'" or an `import.meta.env` break), expand the array only as errors dictate — add `"vite/client"` for `import.meta.env` types, or the specific missing `@types` package — and re-run until `tsc --noEmit` exits 0. Do NOT default to Option B (per-file `globalThis` casts across 20+ files); use it only if the tsconfig approach proves insufficient after iterating. Do NOT edit the 5 prod break-site files (convex/emailDigest.ts, convex/gatewayQuota.ts, convex/auth.config.ts, convex/forgeFileIngest.ts, src/hooks/useThemeColors.ts) — the tsconfig fix resolves them all.
  </action>
  <verify>
    <automated>npx tsc --noEmit && node -e "console.log(require('./node_modules/typescript/package.json').version)"</automated>
  </verify>
  <acceptance_criteria>
    - `node -e "require('./node_modules/typescript/package.json').version"` prints `6.0.3`
    - package.json devDependencies contains `"typescript": "^6.0.3"`
    - tsconfig.json `compilerOptions.types` includes `"node"` (plus any companions the verify loop required)
    - `npx tsc --noEmit` exits 0 with zero errors
    - No new `globalThis` casts were added to the 5 prod break-site files
  </acceptance_criteria>
  <done>TypeScript 6.0.3 installed, tsconfig types array resolves all node globals, tsc --noEmit exits 0.</done>
</task>

<task type="auto">
  <name>Task 3: Remove redundant @types stubs, run the full green bar, update REQUIREMENTS.md wording</name>
  <files>package.json, package-lock.json, .planning/REQUIREMENTS.md</files>
  <read_first>
    - package.json (@types/diff ^7.0.2 line ~72, @types/js-yaml ^4.0.9 line ~73; diff ^8.0.3, js-yaml ^5.2.1 runtime deps)
    - .planning/REQUIREMENTS.md (HARD-03 line 22, HARD-04 line 23 — the wording to update)
    - .planning/phases/95-hardening-security-audit-key-rotation-dependency-majors/95-CONTEXT.md (D-03/D-08/D-10 wording-update requirements)
  </read_first>
  <action>
    Run `npm uninstall @types/diff @types/js-yaml` — both are redundant (diff@8 ships `libcjs/index.d.ts`, js-yaml@5 ships `./dist/js-yaml.d.ts`) AND major-mismatched with the installed runtime packages (diff@8.0.3, js-yaml@5.2.1). Then run the full green bar per D-09/D-10: `npx tsc --noEmit && npx vitest run && npm run build` — all three must exit 0. If any test relied on the DefinitelyTyped stub types, fix by importing from the runtime package's bundled types (do NOT re-add the stubs). Then update `.planning/REQUIREMENTS.md`: rewrite HARD-04 to state it was resolved by deleting the dead `calendar.tsx` primitive and dropping `react-day-picker` (not a 9→10 migration); rewrite HARD-03 to state TS 6.0.3 landed green via a tsconfig node-globals fix, and add a folded-scope note recording that diff@8, js-yaml@5, jsdom@29, react-easy-crop@6 (D-10) already merged to master 2026-07-04 and are verified green, and that the redundant @types/diff + @types/js-yaml were removed. Do NOT check the HARD-01 or HARD-02 boxes (those close in Plans 03/04).
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - package.json does not contain `"@types/diff"` or `"@types/js-yaml"`
    - `npx tsc --noEmit` exits 0
    - `npx vitest run` reports all 184 test files passing (exit 0)
    - `npm run build` exits 0
    - REQUIREMENTS.md HARD-04 wording says "resolved by deletion" (no "9→10 migration")
    - REQUIREMENTS.md HARD-03 contains a folded-scope note naming diff@8, js-yaml@5, jsdom@29, react-easy-crop@6
  </acceptance_criteria>
  <done>Redundant @types removed, full green bar passes, REQUIREMENTS.md HARD-03/HARD-04 wording reflects reality.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → local dev tree | New devDep install (`typescript@6.0.3`) crosses a supply-chain boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-95-01-SC | Tampering | npm install typescript@6.0.3 | accept | First-party Microsoft package, mainstream, in committed lockfile family; `npm audit` reports 0 vulnerabilities (RESEARCH Package Legitimacy Audit — Approved, no [ASSUMED]/[SUS]). No blocking-human gate warranted. |
| T-95-01-DC | Tampering | Deleting calendar.tsx / removing deps | accept | Deletion of a verified-dead primitive reduces attack surface; no runtime behavior change (zero consumers). |

Honest note: this plan's threat surface is minimal — it removes dead code and bumps a build-tool version. No new data paths, endpoints, or auth logic. The real audit surface lives in Plan 03.
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0 on TS 6.0.3
- `npx vitest run` — full 184-file suite green
- `npm run build` (vite/Rolldown) exits 0
- `grep -rn "react-day-picker" src convex` returns no matches
- REQUIREMENTS.md HARD-03/HARD-04 wording updated
</verification>

<success_criteria>
react-day-picker fully removed; TypeScript 6.0.3 green across tsc + vitest + build; redundant @types stubs gone; REQUIREMENTS.md reflects the deletion + folded-majors reality. Shipped code tree is settled for the Plan 03 audit.
</success_criteria>

<output>
Create `.planning/phases/95-hardening-security-audit-key-rotation-dependency-majors/95-01-SUMMARY.md` when done.
</output>
