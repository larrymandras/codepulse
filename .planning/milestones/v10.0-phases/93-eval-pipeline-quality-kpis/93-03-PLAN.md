---
phase: 93-eval-pipeline-quality-kpis
plan: 03
type: execute
wave: 2
depends_on: ["93-01"]
files_modified:
  - C:/Users/mandr/astridr-repo/astridr/integrations/langfuse_eval.py
  - C:/Users/mandr/astridr-repo/tests/test_langfuse_eval.py
autonomous: true
requirements: [EVAL-01]
user_setup:
  - service: astridr-env
    why: "The mirror POST only fires when the CodePulse ingest target is configured"
    env_vars:
      - name: CONVEX_URL
        source: "CodePulse Convex .convex.site HTTP base (tidy-whale-981) — the Phase 90 compose env anchor already carries this"
      - name: ASTRIDR_INGEST_API_KEY
        source: "Same Bearer key already used by the Phase 90 war-room/transcript mirrors"
must_haves:
  truths:
    - "spawn_score fires a fire-and-forget POST to CodePulse /runtime-ingest with a task_quality event, in addition to the unchanged Langfuse write"
    - "The mirror POST skips silently when CONVEX_URL or ASTRIDR_INGEST_API_KEY is unset, and never raises into the score path"
    - "The payload carries profileId, session_id, agent_id, the numeric score, and a producer-generated idempotencyKey"
  artifacts:
    - path: "C:/Users/mandr/astridr-repo/astridr/integrations/langfuse_eval.py"
      provides: "_post_task_quality fire-and-forget mirror + env anchor + call site in spawn_score"
      contains: "runtime-ingest"
  key_links:
    - from: "langfuse_eval.py spawn_score"
      to: "CodePulse /runtime-ingest"
      via: "asyncio.create_task fire-and-forget POST"
      pattern: "task_quality"
---

<objective>
Build EVAL-01's producer half (D-01, cross-repo): make Ástríðr's `langfuse_eval.py` mirror each `task_quality` score to CodePulse's `/runtime-ingest`, alongside its unchanged Langfuse write, following the Phase 90 fire-and-forget mirror precedent exactly.

Purpose: Nothing currently POSTs these scores to CodePulse — this is the change that makes EVAL-01 real end-to-end. Without it, the ingest case from Plan 01 never receives data.

Output: a `_post_task_quality` fire-and-forget function + env anchor in `langfuse_eval.py`, its call site inside `spawn_score`, and extended producer tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/93-eval-pipeline-quality-kpis/93-CONTEXT.md
@.planning/phases/93-eval-pipeline-quality-kpis/93-PATTERNS.md

<interfaces>
<!-- Cross-repo. The mirror MUST match CodePulse's ingest contract from Plan 01. -->

CodePulse ingest contract (Plan 01, convex/runtimeIngest.ts): single-event body
`{ "eventType": "task_quality", "data": { ... } }` (NOT the legacy `{"events":[...]}` batch),
Bearer-authed. The data fields the case reads (coalesced): profileId/profile_id, sessionId/session_id,
score/overall, idempotencyKey/event_id.

Phase 90 mirror precedent (astridr-repo/astridr/channels/war_room/dispatcher.py:38-45, 212-238):
module-level `_CONVEX_URL = os.environ.get("CONVEX_URL","")` + `_CODEPULSE_INGEST_KEY = os.environ.get("ASTRIDR_INGEST_API_KEY","")`;
an `async def _post_...(...)` that returns early if either is unset, gets a pooled client, POSTs JSON with
`Authorization: Bearer {key}`, logs on `status_code >= 300`, and swallows all exceptions (never raises);
call site spawns it via `asyncio.create_task(...)` tracked in a background-task set / done-callback.

Producer to modify (astridr-repo/astridr/integrations/langfuse_eval.py:54-117): `spawn_score`/`_write_score`,
existing D-03 dual-write target with a `self._tasks` set + `task.add_done_callback(self._tasks.discard)` already present.
T-73-16: scores carry only numeric values + agent_id/session_id — no PII, no transcript.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the CodePulse mirror POST to spawn_score</name>
  <files>C:/Users/mandr/astridr-repo/astridr/integrations/langfuse_eval.py</files>
  <read_first>
    - C:/Users/mandr/astridr-repo/astridr/integrations/langfuse_eval.py (the full file — spawn_score/_write_score at L54-117 and the class's _tasks bookkeeping)
    - C:/Users/mandr/astridr-repo/astridr/channels/war_room/dispatcher.py (L38-45 env anchor, L212-238 fire-and-forget POST, L34-36/L248 task-tracking call site)
  </read_first>
  <action>
    In `langfuse_eval.py`, add a module-level env-anchor pair identical to the dispatcher precedent: `_CONVEX_URL = os.environ.get("CONVEX_URL", "")` and `_CODEPULSE_INGEST_KEY = os.environ.get("ASTRIDR_INGEST_API_KEY", "")` (carry the same `# cg-ok`/`# secretref-ok` comments as the dispatcher so the credential-guard hooks stay satisfied). Add an `async def _post_task_quality(payload: dict) -> None` mirroring `_post_codepulse_warroom`'s exact shape but POSTing to `f"{_CONVEX_URL.rstrip('/')}/runtime-ingest"` with body `{"eventType": "task_quality", "data": {...}}` (single-event contract, not the legacy batch). It must return early when either env var is unset, log on `status_code >= 300`, and swallow all exceptions (never raise — D-03 independent gates).

    Include in `data`: `profileId` (the active operational profile Ástríðr knows for this score — resolves RESEARCH Pitfall 1 by having the producer carry it directly), `session_id`, `agent_id`, `score` (the numeric value), and a producer-generated `idempotencyKey` (D-05 — derive a stable id from the score's event/session identity, e.g. the existing Langfuse score id or `f"{session_id}:{agent_id}:{score_name}"`). Do NOT include transcript or PII (T-73-16).

    At the score path inside `spawn_score` (alongside the existing `self._write_score(...)` task spawn), add a second `asyncio.create_task(self._post_task_quality({...}))` tracked with the same `self._tasks` add + `task.add_done_callback(self._tasks.discard)` bookkeeping. Do NOT touch the existing Langfuse write path or the local `_score_cache`/`get_score_trend()` (D-03).
  </action>
  <verify>
    <automated>cd C:/Users/mandr/astridr-repo && python -c "import ast,sys; ast.parse(open('astridr/integrations/langfuse_eval.py').read()); print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `langfuse_eval.py` contains `/runtime-ingest` and `"eventType": "task_quality"` (or equivalent dict literal) and the `_CONVEX_URL`/`_CODEPULSE_INGEST_KEY` env anchors.
    - The mirror function returns early when either env var is empty and contains a try/except that logs and does not re-raise.
    - The existing Langfuse write path is unchanged (still called from spawn_score).
    - The file parses (`python -c ast.parse` prints `ok`).
  </acceptance_criteria>
  <done>spawn_score dual-writes: Langfuse (unchanged) + a fire-and-forget task_quality mirror to CodePulse gated on the two env vars.</done>
</task>

<task type="auto">
  <name>Task 2: Extend the producer tests</name>
  <files>C:/Users/mandr/astridr-repo/tests/test_langfuse_eval.py</files>
  <read_first>
    - C:/Users/mandr/astridr-repo/tests/test_langfuse_eval.py (existing spawn_score/_write_score tests + mocking style)
  </read_first>
  <action>
    Add tests asserting: (1) when `CONVEX_URL` + `ASTRIDR_INGEST_API_KEY` are set, `spawn_score` issues a POST to a `/runtime-ingest` URL with an `Authorization: Bearer` header and a body whose `eventType == "task_quality"` and whose `data` carries `profileId`, `session_id`, `agent_id`, `score`, and `idempotencyKey`; (2) when either env var is unset, no CodePulse POST is attempted; (3) a raised/failed mirror POST does not propagate out of `spawn_score` (the Langfuse write and the caller are unaffected). Mock the HTTP client per the file's existing convention — do not make a real network call.
  </action>
  <verify>
    <automated>cd C:/Users/mandr/astridr-repo && python -m pytest tests/test_langfuse_eval.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_langfuse_eval.py` contains a test asserting the `task_quality` mirror body + Bearer header.
    - A test asserts no CodePulse POST when env vars are unset.
    - A test asserts a mirror failure does not raise out of spawn_score.
    - `python -m pytest tests/test_langfuse_eval.py -q` exits 0.
  </acceptance_criteria>
  <done>Producer behavior is pinned by tests: gated mirror, correct payload/header, never-raises isolation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Ástríðr process → CodePulse ingest | Outbound POST carries the shared ingest Bearer key |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-93-08 | Information Disclosure | ingest Bearer key leaked in logs/errors | mitigate | key read from env only; never logged (log body truncated to 200 chars per precedent, key never in body) |
| T-93-09 | Denial of Service | mirror failure blocks/raises into the score path | mitigate | fire-and-forget task + swallow-all-exceptions; skip-if-unset guard (D-03 independent gates) |
| T-93-SC | Tampering | python installs | accept | no new packages — reuses the existing pooled HTTP client + stdlib os/asyncio |
</threat_model>

<verification>
- `python -m pytest tests/test_langfuse_eval.py -q` green.
- File parses; existing Langfuse path untouched.
- Live round-trip against prod Convex deferred to Plan 06 (D-04).
</verification>

<success_criteria>
- Producer mirrors task_quality scores to `/runtime-ingest` when configured, silently skips when not, never raises.
- Payload matches the Plan 01 ingest contract (eventType + data fields + idempotencyKey).
</success_criteria>

<output>
Create `.planning/phases/93-eval-pipeline-quality-kpis/93-03-SUMMARY.md` when done.
</output>
