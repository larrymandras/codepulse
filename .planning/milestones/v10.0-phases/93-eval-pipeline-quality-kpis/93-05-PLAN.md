---
phase: 93-eval-pipeline-quality-kpis
plan: 05
type: execute
wave: 4
depends_on: ["93-04"]
files_modified:
  - src/hooks/useEvalScores.ts
  - src/components/QualityTrendChart.tsx
  - src/components/StatusBadge.tsx
  - src/pages/Quality.tsx
  - src/pages/QualityDetail.tsx
  - src/App.tsx
  - src/layouts/DashboardLayout.tsx
  - src/pages/Quality.test.tsx
autonomous: true
requirements: [EVAL-03]
must_haves:
  truths:
    - "A Quality nav entry (Gauge icon, OBSERVE group) routes to /quality showing a per-persona KPI card grid"
    - "Each persona card shows score /100, sparkline, delta badge, and a REGRESSION badge when flagged; clicking it navigates to /quality/:profileId"
    - "The persona detail page renders a multi-dimension Recharts LineChart with ReferenceLine change-event markers, a per-dimension breakdown, and a judged-sessions list with rationale + View session link"
    - "Empty states render when no evalScores exist yet (no crash)"
  artifacts:
    - path: "src/pages/Quality.tsx"
      provides: "Quality KPI page — metric row + per-persona card grid + range Select"
      min_lines: 40
    - path: "src/pages/QualityDetail.tsx"
      provides: "Persona drill-in — trend chart + dimension bars + judged sessions"
      min_lines: 40
    - path: "src/components/QualityTrendChart.tsx"
      provides: "Recharts LineChart with per-dimension lines + ReferenceLine markers"
      contains: "ReferenceLine"
    - path: "src/hooks/useEvalScores.ts"
      provides: "useQuery wrappers for the KPI/detail/judged-session queries"
      exports: ["useQualityKpis", "usePersonaDetail", "useJudgedSessions"]
  key_links:
    - from: "src/pages/Quality.tsx"
      to: "api.evalScores.listPersonaKpis"
      via: "useQualityKpis hook"
      pattern: "listPersonaKpis"
    - from: "src/App.tsx"
      to: "src/pages/Quality.tsx"
      via: "lazy route /quality"
      pattern: "quality"
---

<objective>
Build EVAL-03's operator surface (D-15/D-16/D-17): a dedicated Quality page with a per-persona KPI grid, a persona drill-in with a multi-dimension trend chart marked with change events, and the judged-sessions list.

Purpose: The operator must be able to see per-persona quality trends and spot a regression tied to a persona change.

Output: the two pages, the trend chart component, the `useEvalScores` hooks, the `regression` StatusBadge variant, and the nav+route wiring — all reusing existing card/chart/badge patterns.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/93-eval-pipeline-quality-kpis/93-UI-SPEC.md
@.planning/phases/93-eval-pipeline-quality-kpis/93-PATTERNS.md

<interfaces>
<!-- Extracted from the live repo + Plan 04 queries. -->

Backend queries (Plan 04, convex/evalScores.ts): `api.evalScores.listPersonaKpis` (per-persona current score,
sparkline, delta, activeRegression), `api.evalScores.getPersonaDetail({profileId, rangeDays})` (per-dimension
series + change-event markers), `api.evalScores.listJudgedSessions({profileId, rangeDays})`.

Hook convention (src/hooks/useAlerts.ts:1-28): thin `useQuery(api.x.y) ?? []` / `?? null` wrappers.

Card/threshold pattern (src/components/OperatorScoreCard.tsx): loading = undefined ("—"/Loading), empty = null
(no-data copy), score digit `text-3xl font-bold tabular-nums`, `Sparkline`, `SubScoreBar` for per-dimension.
`src/components/MetricCard.tsx:31-49` exports `thresholdColor(value, {ok,warn,invertDirection})` — reuse verbatim.
Scores stored 0-1, DISPLAY as 0-100 via `Math.round(score*100)` + "/100".
Score color: `thresholdColor(score, { ok: 0.8, warn: 0.5, invertDirection: true })`.

Chart precedent (src/components/hr/detail/ResponseTimeChart.tsx): Recharts + shadcn ChartContainer/ChartConfig +
ReferenceLine. For the trend, use LineChart/Line (per-dimension + overall, strokeWidth={2}, dot={false}),
Y domain [0,100], ReferenceLine verticals for change-event markers. NOTE: UI-SPEC cites CompletionRateChart.tsx
which DOES NOT EXIST — ResponseTimeChart.tsx is the real analog.

StatusBadge (src/components/StatusBadge.tsx:17): `legacyMap` — add `regression: { semantic: "error", label: "REGRESSION" }`.

Nav (src/layouts/DashboardLayout.tsx): `iconComponents` map L78, `navGroups` OBSERVE cluster L182-193 (after Alerts).
Add `gauge: Gauge` (Lucide) to the map and `{ to:"/quality", label:"Quality", icon:"gauge", group:"OBSERVE" }`.
Routes (src/App.tsx): lazy-load pattern like Analytics; add `/quality` and `/quality/:profileId`.
Wrap widget groups in `SectionErrorBoundary`. Range control = shadcn Select (7/30/90-day presets, default 30) — NOT react-day-picker.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Hooks + trend chart + regression badge variant</name>
  <files>src/hooks/useEvalScores.ts, src/components/QualityTrendChart.tsx, src/components/StatusBadge.tsx, src/pages/Quality.test.tsx</files>
  <read_first>
    - src/hooks/useAlerts.ts (thin useQuery wrapper convention)
    - src/components/hr/detail/ResponseTimeChart.tsx (Recharts + ChartContainer + ReferenceLine — the real analog)
    - src/components/PulseChart.tsx (L64 strokeWidth={2} line precedent)
    - src/components/StatusBadge.tsx (legacyMap at L17)
    - src/components/ui/chart.tsx (ChartContainer/ChartConfig API)
  </read_first>
  <action>
    Create `src/hooks/useEvalScores.ts` exporting `useQualityKpis()` → `useQuery(api.evalScores.listPersonaKpis) ?? []`, `usePersonaDetail(profileId, rangeDays)` → `useQuery(api.evalScores.getPersonaDetail, { profileId, rangeDays }) ?? null`, and `useJudgedSessions(profileId, rangeDays)` → `useQuery(api.evalScores.listJudgedSessions, { profileId, rangeDays }) ?? []`, following the `useAlerts.ts` convention.

    Create `src/components/QualityTrendChart.tsx` adapting `ResponseTimeChart.tsx`: a Recharts `LineChart` inside `ChartContainer`, one `Line` per rubric dimension + one for overall (`strokeWidth={2}`, `dot={false}`, colors `var(--chart-1..5)`), X-axis = date, Y-axis domain `[0, 100]`, and `ReferenceLine` verticals for each change-event marker (label = change type). Include the empty-state copy ("No judged sessions...") when there is no data. Values arrive 0-1 → render at 0-100.

    In `src/components/StatusBadge.tsx`, add `regression: { semantic: "error", label: "REGRESSION" }` to `legacyMap` (do not invent a new badge component).

    Create `src/pages/Quality.test.tsx` (jsdom, per src/test/setup.ts mocks) asserting the chart renders a line per dimension and a ReferenceLine per marker on a fixture, and that the empty-state copy renders on empty data.
  </action>
  <verify>
    <automated>npx vitest run src/pages/Quality.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `src/hooks/useEvalScores.ts` exports `useQualityKpis`, `usePersonaDetail`, `useJudgedSessions`.
    - `src/components/QualityTrendChart.tsx` contains `LineChart`, `ReferenceLine`, and Y domain `[0, 100]`.
    - `src/components/StatusBadge.tsx` `legacyMap` contains `regression`.
    - `npx vitest run src/pages/Quality.test.tsx` exits 0; `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>The data hooks, the change-marked trend chart, and the regression badge exist and are covered by a rendering test.</done>
</task>

<task type="auto">
  <name>Task 2: Quality page + persona detail page</name>
  <files>src/pages/Quality.tsx, src/pages/QualityDetail.tsx</files>
  <read_first>
    - src/components/OperatorScoreCard.tsx (card loading/empty/data states, Sparkline, SubScoreBar)
    - src/components/MetricCard.tsx (thresholdColor + MetricCard for the metric row)
    - src/pages/Profiles.tsx (per-entity card grid layout `grid grid-cols-1 md:grid-cols-3 gap-4`)
    - src/pages/SessionDetail.tsx (back-nav breadcrumb convention + the View-session route target)
    - src/components/SectionErrorBoundary.tsx, src/components/InfoTooltip.tsx
  </read_first>
  <action>
    Create `src/pages/Quality.tsx` (route `/quality`): H1 "Quality" + subtitle per UI-SPEC; a metric row of 4 `MetricCard`s (Personas Judged, Sessions Judged (range), Active Regressions (severity critical if >0), Avg Overall Score /100); a `SectionHeader` "Per-Persona Quality" whose `action` slot holds a shadcn `Select` range preset (7/30/90-day, default 30 — NOT react-day-picker); and a KPI card grid (`grid grid-cols-1 md:grid-cols-3 gap-4`) driven by `useQualityKpis()`. Each card follows `OperatorScoreCard`: score digit colored via `thresholdColor(score,{ok:0.8,warn:0.5,invertDirection:true})` shown as `Math.round(score*100)` + "/100", a `Sparkline`, a delta badge (▲/▼/→ + signed points vs previous range), a `StatusBadge status="regression"` when `activeRegression`, `onClick` → `/quality/${profileId}`, and the per-card empty-state copy when zero judged sessions in range. Wrap the grid in `SectionErrorBoundary name="Quality KPIs"`. Handle the page-level empty state (no evalScores anywhere) with the UI-SPEC "No quality data yet" copy.

    Create `src/pages/QualityDetail.tsx` (route `/quality/:profileId`): persona name H1 + back-to-Quality breadcrumb (reuse SessionDetail's convention); `QualityTrendChart` fed by `usePersonaDetail(profileId, range)`; a per-dimension breakdown reusing `SubScoreBar` styling (one row per rubric dimension); and a judged-sessions list (`useJudgedSessions`) rendered as `EntityRow`s (primary = session summary/date, secondary = truncated rationale, trailing = "View session →" link to the existing SessionDetail route). Wrap each group in its own `SectionErrorBoundary`. Empty states per UI-SPEC.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run src/pages/Quality.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `src/pages/Quality.tsx` renders a `MetricCard` row and a KPI grid via `useQualityKpis`, with the page-level "No quality data yet" empty state.
    - `src/pages/QualityDetail.tsx` renders `QualityTrendChart`, a per-dimension breakdown, and a judged-sessions `EntityRow` list with a "View session" link.
    - Score display uses `Math.round(score*100)` + "/100" (no raw 0-1 decimals in the KPI surface).
    - `npx tsc --noEmit` exits 0; `npx vitest run src/pages/Quality.test.tsx` exits 0.
  </acceptance_criteria>
  <done>Both Quality pages render from real queries with the specified layout, thresholds, and empty states.</done>
</task>

<task type="auto">
  <name>Task 3: Nav entry + lazy routes</name>
  <files>src/App.tsx, src/layouts/DashboardLayout.tsx</files>
  <read_first>
    - src/layouts/DashboardLayout.tsx (iconComponents map L78, navGroups OBSERVE cluster L182-193)
    - src/App.tsx (lazy import + Suspense-wrapped Route pattern for Analytics)
  </read_first>
  <action>
    In `src/layouts/DashboardLayout.tsx`, import `Gauge` from `lucide-react`, add `gauge: Gauge` to `iconComponents`, and add `{ to: "/quality", label: "Quality", icon: "gauge", group: "OBSERVE" }` to the OBSERVE cluster after the Alerts entry. In `src/App.tsx`, add `const Quality = lazy(() => import("./pages/Quality"))` and `const QualityDetail = lazy(() => import("./pages/QualityDetail"))`, and register `<Route path="/quality" ...>` and `<Route path="/quality/:profileId" ...>` inside `DashboardLayout`, each wrapped in the existing `Suspense` fallback pattern.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `src/layouts/DashboardLayout.tsx` contains `gauge: Gauge` and a `/quality` navGroups entry in OBSERVE.
    - `src/App.tsx` lazy-imports `Quality` + `QualityDetail` and registers both routes.
    - `npm run build` exits 0 (both pages lazy-chunked).
  </acceptance_criteria>
  <done>Quality is reachable from the OBSERVE nav and both routes build clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Convex queries → React client | Judge rationale text rendered in the browser |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-93-13 | Tampering (XSS) | judge rationale / session summary rendered in the UI | mitigate | render via React text nodes only — no `dangerouslySetInnerHTML` in the Quality pages/chart |
| T-93-12 | Information Disclosure | apiKey reaching the client | mitigate | Quality hooks read only KPI/detail/judged-session queries; none return apiKey (Plan 04) |
| T-93-SC | Tampering | npm installs | accept | no new packages — recharts/lucide/shadcn primitives all already installed (UI-SPEC Registry Safety) |
</threat_model>

<verification>
- `npm run build` succeeds; `npx tsc --noEmit` clean; `npm test` green.
- Live render against real data deferred to Plan 06 (D-04).
</verification>

<success_criteria>
- Quality page + persona detail render per UI-SPEC with reused card/chart/badge patterns.
- Regression badge + change-event markers visible; empty states safe.
- Nav + routes wired, lazy-loaded, build clean.
</success_criteria>

<output>
Create `.planning/phases/93-eval-pipeline-quality-kpis/93-05-SUMMARY.md` when done.
</output>
