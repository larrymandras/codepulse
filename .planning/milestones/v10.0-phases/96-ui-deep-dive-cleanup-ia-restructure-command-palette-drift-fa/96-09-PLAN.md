---
phase: 96-ui-deep-dive-cleanup-ia-restructure-command-palette-drift-fa
plan: 09
type: execute
wave: 2
depends_on: ["96-01"]
files_modified:
  - src/pages/ForgePage.tsx
  - src/pages/WarRoom.tsx
autonomous: true
requirements: [F8, F7]
must_haves:
  truths:
    - "ForgePage's fixed-width (w-[280px]) master list pane collapses/stacks responsively on small viewports instead of squeezing the detail pane to ~40px (F8)"
    - "WarRoom's fixed-width (w-64) sidebar collapses/stacks responsively on small viewports (F8)"
    - "Any icon-only collapse toggle has a 44x44px min hit target and an explicit aria-label (UI-SPEC spacing exception)"
    - "WarRoom header uses <PageHeader> (F7); ForgePage header already compliant — left unchanged unless it needs the toggle in its actions slot"
  artifacts:
    - path: "src/pages/ForgePage.tsx"
      provides: "Responsive master-detail on mobile"
      contains: "md:"
    - path: "src/pages/WarRoom.tsx"
      provides: "Responsive sidebar on mobile"
      contains: "md:"
  key_links:
    - from: "src/pages/ForgePage.tsx list pane"
      to: "responsive breakpoint classes"
      via: "Tailwind sm:/md: + toggle state"
      pattern: "md:"
---

<objective>
Fix the two master-detail pages whose fixed-width side panes break on narrow viewports (F8): ForgePage's `w-[280px] shrink-0` job list and WarRoom's `w-64 flex-shrink-0` sidebar. On a 320px viewport the detail pane currently gets ~40px. Add a breakpoint collapse (stacked or toggleable-master, per-page discretion). Migrate WarRoom's header to `<PageHeader>` (F7); ForgePage's header is already compliant.

Purpose: Makes both surfaces usable on mobile. Isolated to two files.
Output: Responsive panes on both pages; standard WarRoom header.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/96-ui-deep-dive-cleanup-ia-restructure-command-palette-drift-fa/96-PATTERNS.md
@.planning/phases/96-ui-deep-dive-cleanup-ia-restructure-command-palette-drift-fa/96-UI-SPEC.md

<interfaces>
Toggleable-master reference (src/layouts/DashboardLayout.tsx:648-685): desktop fixed pane (`hidden md:flex w-...`) + mobile overlay (`fixed inset-0 z-40 bg-black/50 md:hidden`) + slide-in aside (`fixed ... transform transition-transform md:hidden translate-x-0/-translate-x-full`) + close button with aria-label. Use this pattern for toggleable-master, or a simpler `flex-col md:flex-row` stack if the list should stay visible by default on mobile.

Panes to fix:
- src/pages/ForgePage.tsx:150 — `<div className="w-[280px] shrink-0 border-r border-border overflow-hidden">` (no responsive collapse). Add breakpoint behavior; if using a toggle, the toggle button is icon-only → 44x44 min hit target + aria-label (e.g. "Show job list").
- src/pages/WarRoom.tsx:270 — `<GlassPanel className="w-64 flex-shrink-0 ...">` (same issue).

UI-SPEC spacing exception: mobile collapse toggle hit target ≥ 44x44px (`size-11`/`min-h-11 min-w-11` wrapping a smaller Lucide icon); icon-only buttons MUST carry aria-label.

Header (F7): WarRoom h1 `text-xl font-semibold` → PageHeader title="War Room". ForgePage h1 (`text-2xl font-bold text-foreground shrink-0`) is already compliant — leave it, unless seating the collapse toggle in a PageHeader actions slot is cleaner (optional).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Responsive collapse for ForgePage + WarRoom panes; WarRoom header</name>
  <files>src/pages/ForgePage.tsx, src/pages/WarRoom.tsx</files>
  <read_first>
    - src/pages/ForgePage.tsx (list pane ~150; overall layout; header ~145)
    - src/pages/WarRoom.tsx (sidebar ~270; header ~252)
    - src/layouts/DashboardLayout.tsx (toggleable-master pattern ~648-685)
    - src/components/PageHeader.tsx (Plan 01)
  </read_first>
  <action>ForgePage.tsx: make the `w-[280px] shrink-0` list pane responsive — either stack it above the detail on small viewports (`flex-col md:flex-row` with the list `w-full md:w-[280px]`) or make it a toggleable master overlay following the DashboardLayout pattern; whichever is chosen, the detail pane must get full width on mobile. If a toggle button is added, give it a ≥44x44 hit target and an aria-label (e.g. "Show job list"). WarRoom.tsx: apply the same treatment to the `w-64 flex-shrink-0` sidebar, and replace the h1 with PageHeader title="War Room" (preserve existing header controls via actions). Do not introduce hardcoded hex; use tokens (CLAUDE.md).</action>
  <verify>
    <automated>grep -qE 'md:(flex-row|w-\[280px\]|flex|hidden)' src/pages/ForgePage.tsx && grep -qE 'md:(flex-row|w-64|flex|hidden)' src/pages/WarRoom.tsx && grep -q 'PageHeader' src/pages/WarRoom.tsx && npx tsc --noEmit && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - ForgePage.tsx list pane no longer uses an unconditional fixed width — a responsive class (`md:`/`sm:` prefix or `w-full md:w-[280px]`) governs it
    - WarRoom.tsx sidebar likewise has responsive breakpoint handling
    - Any icon-only toggle added has an `aria-label` (`grep -q 'aria-label' <file>` where a toggle was introduced)
    - WarRoom.tsx imports and renders `<PageHeader`
    - `npx tsc --noEmit` clean; `npx vitest run src/pages/__tests__/WarRoom.test.tsx src/pages/__tests__/ForgePage.test.tsx` still green
  </acceptance_criteria>
  <done>Both master-detail pages are responsive; WarRoom header standardized.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| (none new) | Layout/responsive-styling change only; no data or network path touched |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-96-09-01 | Denial of Service (usability) | fixed-width panes on mobile | mitigate | Responsive collapse restores usable detail width on small viewports |
| T-96-09-SC | Tampering | package installs | accept | No packages installed this phase |
</threat_model>

<verification>
- `npx tsc --noEmit` clean
- existing ForgePage/WarRoom tests green
- responsive breakpoint classes present on both panes
</verification>

<success_criteria>
ForgePage and WarRoom master-detail layouts collapse responsively; WarRoom header standardized; a11y hit targets honored.
</success_criteria>

<output>
Create `.planning/phases/96-ui-deep-dive-cleanup-ia-restructure-command-palette-drift-fa/96-09-SUMMARY.md` when done.
</output>
