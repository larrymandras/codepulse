---
phase: 96-ui-deep-dive-cleanup-ia-restructure-command-palette-drift-fa
plan: 11
type: execute
wave: 2
depends_on: ["96-01"]
files_modified:
  - src/pages/hr/Roster.tsx
  - src/pages/hr/Catalog.tsx
  - src/pages/hr/Onboarding.tsx
  - src/pages/hr/Teams.tsx
  - src/pages/hr/AgentAnalytics.tsx
  - src/pages/Dashboard.tsx
  - src/pages/Alerts.tsx
  - src/pages/Briefings.tsx
  - src/pages/Capabilities.tsx
  - src/pages/Settings.tsx
  - src/pages/SelfHealing.tsx
autonomous: true
requirements: [F7]
must_haves:
  truths:
    - "All 5 hr/* pages (Roster, Catalog, Onboarding, Teams, AgentAnalytics) render their title via <PageHeader> — the uppercase-mono variants and the missing h1 on Onboarding are replaced with the standard (F7)"
    - "Dashboard, Alerts, Briefings, Capabilities, Settings, SelfHealing render their title via <PageHeader> — the text-2xl-without-text-foreground variants are standardized (F7)"
    - "None of these pages keeps a bespoke h1 after migration"
  artifacts:
    - path: "src/pages/hr/Roster.tsx"
      provides: "Standardized header via PageHeader"
      contains: "PageHeader"
  key_links:
    - from: "these 11 pages"
      to: "src/components/PageHeader.tsx"
      via: "import + render"
      pattern: "PageHeader"
---

<objective>
F7 header sweep, batch A: migrate 11 leftover pages (the 5 `hr/*` pages plus Dashboard, Alerts, Briefings, Capabilities, Settings, SelfHealing) to the shared `<PageHeader>`. These pages are owned by no other plan, so this plan owns them exclusively. Each migration is the same mechanical transform: replace the bespoke `<h1>` with `<PageHeader title="…" icon={…?} actions={…?} />`.

Purpose: Half of the 35-page header standardization. Depends only on Plan 01 (PageHeader).
Output: 11 pages on the standard header.

Scope note: this plan touches 11 files, one above the 10-file guidance. Deliberate and low-risk — every edit is the SAME mechanical transform (swap the bespoke `<h1>` for `<PageHeader>`) on pages owned by no other plan, with no shared state or cross-file logic. Splitting would add plan overhead without reducing per-file risk.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/96-ui-deep-dive-cleanup-ia-restructure-command-palette-drift-fa/96-PATTERNS.md
@.planning/phases/96-ui-deep-dive-cleanup-ia-restructure-command-palette-drift-fa/96-UI-SPEC.md

<interfaces>
PageHeader (Plan 01): `<PageHeader title icon? actions? className? />` emits `<h1 className="text-2xl font-bold text-foreground">`; icon is an optional Lucide component; actions is an optional right-aligned node.

Current headers to replace (from the audited tree):
- src/pages/hr/Roster.tsx, hr/Catalog.tsx, hr/Teams.tsx, hr/AgentAnalytics.tsx: `<h1 className="text-xl font-bold font-mono tracking-wide text-foreground uppercase flex items-center gap-2">` with a leading Lucide icon — pass the icon to PageHeader's `icon` prop; keep the same title text.
- src/pages/hr/Onboarding.tsx: NO h1 today — add `<PageHeader title="Onboarding" />`.
- src/pages/Dashboard.tsx, Alerts.tsx, Briefings.tsx, Capabilities.tsx, Settings.tsx, SelfHealing.tsx: `<h1 className="text-2xl font-bold">` (missing text-foreground) — replace with `<PageHeader title="…" />` preserving any icon/right-controls.

Preserve each page's existing title string and any header-right controls (pass through `actions`). Do NOT reintroduce `max-h-[...]` caps.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate hr/* headers (5 pages)</name>
  <files>src/pages/hr/Roster.tsx, src/pages/hr/Catalog.tsx, src/pages/hr/Onboarding.tsx, src/pages/hr/Teams.tsx, src/pages/hr/AgentAnalytics.tsx</files>
  <read_first>
    - src/pages/hr/Roster.tsx (header block + leading icon)
    - src/pages/hr/Catalog.tsx, src/pages/hr/Teams.tsx, src/pages/hr/AgentAnalytics.tsx (same uppercase-mono header pattern)
    - src/pages/hr/Onboarding.tsx (no h1 — add one)
    - src/components/PageHeader.tsx (Plan 01 API)
  </read_first>
  <action>In each of Roster/Catalog/Teams/AgentAnalytics, replace the `text-xl font-bold font-mono ... uppercase` h1 with `<PageHeader title="<existing title>" icon={<existing Lucide icon component>} actions={<existing right controls, if any>} />` — keep the same title text and icon, drop the mono/uppercase styling. In Onboarding, add `<PageHeader title="Onboarding" />` at the top of the page content. Import PageHeader from "@/components/PageHeader" in each.</action>
  <verify>
    <automated>for f in Roster Catalog Onboarding Teams AgentAnalytics; do grep -q 'PageHeader' "src/pages/hr/$f.tsx" || { echo "MISSING $f"; exit 1; }; done && grep -rL 'font-mono tracking-wide.*uppercase' src/pages/hr/Roster.tsx src/pages/hr/Catalog.tsx src/pages/hr/Teams.tsx src/pages/hr/AgentAnalytics.tsx | wc -l | grep -qx 4 && npx tsc --noEmit && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - All 5 hr/* files import and render `<PageHeader`
    - The 4 previously uppercase-mono h1s no longer carry `font-mono tracking-wide ... uppercase` on the title element
    - Onboarding.tsx now renders a header
    - `npx tsc --noEmit` clean; `npx vitest run` for any existing hr tests stays green
  </acceptance_criteria>
  <done>All hr/* pages use the standard header.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate 6 top-level page headers</name>
  <files>src/pages/Dashboard.tsx, src/pages/Alerts.tsx, src/pages/Briefings.tsx, src/pages/Capabilities.tsx, src/pages/Settings.tsx, src/pages/SelfHealing.tsx</files>
  <read_first>
    - src/pages/Dashboard.tsx, src/pages/Alerts.tsx, src/pages/Briefings.tsx, src/pages/Capabilities.tsx, src/pages/Settings.tsx, src/pages/SelfHealing.tsx (each `text-2xl font-bold` h1)
    - src/components/PageHeader.tsx (Plan 01 API)
  </read_first>
  <action>In each page, replace the bespoke `<h1 className="text-2xl font-bold">` with `<PageHeader title="<existing title>" />` (pass any leading icon via `icon` and any right-side controls via `actions`). Import PageHeader from "@/components/PageHeader".</action>
  <verify>
    <automated>for f in Dashboard Alerts Briefings Capabilities Settings SelfHealing; do grep -q 'PageHeader' "src/pages/$f.tsx" || { echo "MISSING $f"; exit 1; }; done && npx tsc --noEmit && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - All 6 files import and render `<PageHeader`
    - No `<h1 className="text-2xl font-bold"` without `text-foreground` remains in these 6 files (`grep -rc 'text-2xl font-bold"' src/pages/Dashboard.tsx ...` == 0 for the bespoke form)
    - `npx tsc --noEmit` clean; existing tests for these pages stay green
  </acceptance_criteria>
  <done>The 6 top-level pages use the standard header.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| (none new) | Header-only presentational migration; no data/network change |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-96-11-01 | Tampering | divergent header styling | mitigate | Single shared component removes the per-page header drift surface |
| T-96-11-SC | Tampering | package installs | accept | No packages installed this phase |
</threat_model>

<verification>
- Each of the 11 pages imports and renders `<PageHeader`
- `npx tsc --noEmit` clean; full `npm test` green after the wave
</verification>

<success_criteria>
11 leftover pages (5 hr/* + 6 top-level) migrated to the standard PageHeader.
</success_criteria>

<output>
Create `.planning/phases/96-ui-deep-dive-cleanup-ia-restructure-command-palette-drift-fa/96-11-SUMMARY.md` when done.
</output>
