---
phase: 96-ui-deep-dive-cleanup-ia-restructure-command-palette-drift-fa
plan: 10
type: execute
wave: 2
depends_on: ["96-01"]
files_modified:
  - src/pages/DocComments.tsx
  - src/components/ThemeSwitcher.tsx
  - src/pages/KnowledgeGraph.tsx
  - src/pages/ToolGalaxy.tsx
  - src/pages/BuildProgress.tsx
  - src/pages/Analytics.tsx
autonomous: true
requirements: [F10, F9, D-10, F7]
must_haves:
  truths:
    - "DocComments uses token classes (border-border, text-muted-foreground, bg-primary/15) instead of raw zinc-*/emerald-* colors, and gains a page header (F10)"
    - "ThemeSwitcher's SelectTrigger has an aria-label (F10 — the only header control lacking an accessible name)"
    - "The DOM bg-[#09090b] backgrounds on KnowledgeGraph and ToolGalaxy are replaced with a token (bg-background/bg-card) (F10, ride-along)"
    - "KnowledgeGraph and ToolGalaxy render their title via <PageHeader> (icon + InfoTooltip preserved) — no bespoke <h1> remains, satisfying UI-SPEC's 'all 35 pages migrate; no page keeps a bespoke header' (F7)"
    - "BuildProgress.tsx uses typed api.* Convex results — the three `as any` prop casts (components/pipelines/activity at :41,42,46) and the `c: any` at :17 are removed (F10 untyped-access)"
    - "Analytics dead UI removed: the zeroed TokenSavingsIndicator, the fetched-then-discarded errorTrend query, and the duplicate LlmProviderPanel (F9/D-10)"
  artifacts:
    - path: "src/pages/DocComments.tsx"
      provides: "Token-compliant colors + page header"
      contains: "PageHeader"
    - path: "src/pages/BuildProgress.tsx"
      provides: "Typed Convex access (no `as any`)"
      contains: "TeamStatusCards"
  key_links:
    - from: "src/components/ThemeSwitcher.tsx"
      to: "SelectTrigger aria-label"
      via: "accessible name"
      pattern: "aria-label"
    - from: "src/pages/KnowledgeGraph.tsx + src/pages/ToolGalaxy.tsx"
      to: "src/components/PageHeader.tsx"
      via: "import + render (icon prop)"
      pattern: "PageHeader"
---

<objective>
Clean up the F10 token/a11y minors, the F7 header migration for KnowledgeGraph/ToolGalaxy, the remaining F10 untyped-Convex-access on BuildProgress, and the F9/D-10 Analytics dead UI. DocComments moves off raw `zinc-*`/`emerald-*` palette classes to tokens and gains a header; ThemeSwitcher's trigger gets an `aria-label`; the DOM `bg-[#09090b]` backgrounds on KnowledgeGraph/ToolGalaxy (out-of-scope canvas hex excepted) get a token AND those two pages migrate their bespoke `<h1>` to `<PageHeader>` (icon preserved); BuildProgress drops its three `as any` prop casts and `c: any` for typed `api.*` access; Analytics sheds its dead widgets.

Purpose: Closes the low-severity consistency items and the header/typing coverage on files owned by no other plan (BuildProgress, KnowledgeGraph, ToolGalaxy are unowned elsewhere).
Output: Token-compliant DocComments/KG/ToolGalaxy, standard PageHeader on KG/ToolGalaxy, accessible ThemeSwitcher, typed BuildProgress, dead-code-free Analytics.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/96-ui-deep-dive-cleanup-ia-restructure-command-palette-drift-fa/96-PATTERNS.md
@.planning/phases/96-ui-deep-dive-cleanup-ia-restructure-command-palette-drift-fa/FINDINGS.md
@CLAUDE.md

<interfaces>
DocComments raw colors (src/pages/DocComments.tsx:67,73-74,90): `border-zinc-800`, `bg-emerald-500/15 text-emerald-300`, `text-zinc-300 hover:bg-zinc-900`, `text-zinc-500` → token classes: `border-border`, `bg-primary/15 text-primary`, `text-muted-foreground hover:bg-card`, `text-muted-foreground` (CLAUDE.md: never hardcode hex or raw palette). No h1 today → add `<PageHeader title="Doc Review" />` (BuildProgress:24 is the compliant analog). Also present: hardcoded `author: "larry"` and `doc_type: "gsd_spec"` (:48-49) — replace `author:"larry"` with the in-scope `profileId` identity if available; leave `doc_type` as-is only if no loaded-doc source exists (note the choice).

ThemeSwitcher (src/components/ThemeSwitcher.tsx:43): `<SelectTrigger className="w-[160px] h-8 ...">` missing an accessible name → add `aria-label="Select theme"` (Radix SelectTrigger accepts ARIA props directly).

KnowledgeGraph (src/pages/KnowledgeGraph.tsx:915) and ToolGalaxy (src/pages/ToolGalaxy.tsx:321): DOM element with `bg-[#09090b]` → replace with `bg-background` (the zinc `#09090b` neutral maps to --background in dark themes; confirm against src/index.css). These are DOM (not canvas ctx.fillStyle) so they CAN read tokens.

KnowledgeGraph header (src/pages/KnowledgeGraph.tsx:601) and ToolGalaxy header (src/pages/ToolGalaxy.tsx:597) — BOTH still render a bespoke `<h1 className="text-2xl font-bold flex items-center gap-2">` (missing `text-foreground`) with a leading Lucide icon (`<Share2 .../>` / `<Boxes .../>`) followed by the title text and an `<InfoTooltip .../>`. UI-SPEC binds "all 35 pages migrate; no page keeps a bespoke header." Migrate both to `<PageHeader>` (Plan 01 API: `<PageHeader title icon? actions? className? />`, emits `<h1 className="text-2xl font-bold text-foreground">`): pass the existing Lucide icon component via the `icon` prop, keep the title text, and preserve the `<InfoTooltip>` (fold it into the `title` node, e.g. `title={<>KG Explorer <InfoTooltip .../></>}`, so the tooltip is not lost). The trailing `<p className="text-sm text-muted-foreground font-mono mt-1">…` subheading line stays as-is beneath the header. Import PageHeader from "@/components/PageHeader".

BuildProgress typed access (src/pages/BuildProgress.tsx) — the queries resolve to typed arrays via `useQuery(api.build.phaseProgress) ?? []` (:10), `api.pipelines.listAll` (:13), `api.build.recentActivity` (:12). Three prop casts short-circuit typing: `<TeamStatusCards components={components as any} pipelines={pipelines as any} />` (:41), `<BuildActivityFeed entries={activity as any} />` (:42), `<ComponentTable components={components as any} />` (:46); plus `components.filter((c: any) => …)` (:17). Child prop contracts (from the component files):
```typescript
// TeamStatusCards
interface TeamStatusCardsProps {
  components: Array<{ component: string; phase: string; status: string }>;
  pipelines: Array<{ name: string; status: string }>;
}
// BuildActivityFeed
{ entries: ActivityEntry[] }        // ActivityEntry defined in BuildActivityFeed.tsx:3
// ComponentTable
{ components: ComponentRow[] }       // ComponentRow defined in ComponentTable.tsx:4
```
Remove the three `as any` casts and the `c: any`. The Convex `_generated` return types are the source of truth — where they structurally satisfy the child prop interface, drop the cast directly; where a field is named/typed differently, map to the child's shape or align the child interface (do NOT reintroduce `any`). The header on BuildProgress:24 is already the compliant gold standard — do NOT touch it.

Analytics dead UI (src/pages/Analytics.tsx): `TokenSavingsIndicator savedTokens={0} totalTokens={0}` at :31,88 (remove import + render); `const errorTrend = useQuery(...)` fetched then `void errorTrend` at :64,80-81 (either wire into ErrorRateTrend's prop if useful, or remove the query — do not leave a fetched-but-unused query); duplicate `<LlmProviderPanel />` at :384 (remove the second instance). Analytics header is already compliant — do not touch it.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: DocComments tokens + header; ThemeSwitcher aria-label; KG/ToolGalaxy bg token + PageHeader migration</name>
  <files>src/pages/DocComments.tsx, src/components/ThemeSwitcher.tsx, src/pages/KnowledgeGraph.tsx, src/pages/ToolGalaxy.tsx</files>
  <read_first>
    - src/pages/DocComments.tsx (raw colors ~67,73-74,90; hardcoded author/doc_type ~48-49; no h1)
    - src/components/ThemeSwitcher.tsx (SelectTrigger ~43)
    - src/pages/KnowledgeGraph.tsx (bg-[#09090b] ~915; bespoke h1 + Share2 icon + InfoTooltip ~601)
    - src/pages/ToolGalaxy.tsx (bg-[#09090b] ~321; bespoke h1 + Boxes icon + InfoTooltip ~597)
    - src/index.css (confirm which token maps to #09090b under default cyan theme)
    - src/components/PageHeader.tsx (Plan 01 — icon + title + actions API)
  </read_first>
  <action>DocComments.tsx: replace `border-zinc-800`→`border-border`, `bg-emerald-500/15 text-emerald-300`→`bg-primary/15 text-primary`, `text-zinc-300 hover:bg-zinc-900`→`text-muted-foreground hover:bg-card`, `text-zinc-500`→`text-muted-foreground`; add `<PageHeader title="Doc Review" />` at the top of the page content. Replace `author: "larry"` with the in-scope `profileId` (if available); if `doc_type` has no loaded-doc source, leave it and note it in the SUMMARY. ThemeSwitcher.tsx: add `aria-label="Select theme"` to the SelectTrigger. KnowledgeGraph.tsx and ToolGalaxy.tsx: (a) replace `bg-[#09090b]` with `bg-background` (or the correct token per src/index.css); (b) migrate the bespoke `<h1 className="text-2xl font-bold flex items-center gap-2">…</h1>` to `<PageHeader icon={Share2 /* KG */ or Boxes /* ToolGalaxy */} title={<>KG Explorer <InfoTooltip … /></> /* or "Tool Galaxy" with its InfoTooltip */} />` — pass the existing Lucide icon via the `icon` prop, keep the title text, preserve the `<InfoTooltip>` inside the `title` node, and leave the trailing `<p …font-mono mt-1>` subheading in place beneath the header. Import PageHeader from "@/components/PageHeader". Do NOT touch canvas `ctx.fillStyle` colors (out of scope).</action>
  <verify>
    <automated>! grep -qE 'zinc-|emerald-' src/pages/DocComments.tsx && grep -q 'PageHeader' src/pages/DocComments.tsx && grep -q 'aria-label' src/components/ThemeSwitcher.tsx && ! grep -q 'bg-\[#09090b\]' src/pages/KnowledgeGraph.tsx && ! grep -q 'bg-\[#09090b\]' src/pages/ToolGalaxy.tsx && grep -q 'PageHeader' src/pages/KnowledgeGraph.tsx && grep -q 'PageHeader' src/pages/ToolGalaxy.tsx && ! grep -q 'text-2xl font-bold flex items-center gap-2' src/pages/KnowledgeGraph.tsx && ! grep -q 'text-2xl font-bold flex items-center gap-2' src/pages/ToolGalaxy.tsx && npx tsc --noEmit && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE 'zinc-|emerald-' src/pages/DocComments.tsx` == 0 and DocComments renders `<PageHeader`
    - `grep -q 'aria-label' src/components/ThemeSwitcher.tsx` matches on the SelectTrigger
    - `grep -c 'bg-\[#09090b\]' src/pages/KnowledgeGraph.tsx` == 0 and same for ToolGalaxy.tsx
    - Both KnowledgeGraph.tsx and ToolGalaxy.tsx render `<PageHeader` and no longer carry the bespoke `text-2xl font-bold flex items-center gap-2` h1 (their icon and InfoTooltip are preserved)
    - `npx tsc --noEmit` clean
  </acceptance_criteria>
  <done>DocComments token-compliant with header; ThemeSwitcher accessible; DOM bg hex tokenized; KG/ToolGalaxy on the standard PageHeader.</done>
</task>

<task type="auto">
  <name>Task 2: Remove Analytics dead UI</name>
  <files>src/pages/Analytics.tsx</files>
  <read_first>
    - src/pages/Analytics.tsx (TokenSavingsIndicator ~31,88; errorTrend query ~64,80-81; duplicate LlmProviderPanel ~384)
  </read_first>
  <action>Remove the `<TokenSavingsIndicator savedTokens={0} totalTokens={0} />` render and its import. Resolve the discarded `errorTrend`: if `ErrorRateTrend` can consume it meaningfully, pass it as a prop; otherwise delete the `useQuery` and the `void errorTrend` line entirely (no fetched-but-unused query). Remove the duplicate second `<LlmProviderPanel />` instance (keep the first). Do not touch the (already compliant) Analytics header.</action>
  <verify>
    <automated>! grep -q 'TokenSavingsIndicator' src/pages/Analytics.tsx && ! grep -q 'void errorTrend' src/pages/Analytics.tsx && npx tsc --noEmit && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'TokenSavingsIndicator' src/pages/Analytics.tsx` == 0 (import + render gone)
    - `grep -c 'void errorTrend' src/pages/Analytics.tsx` == 0
    - Exactly one `<LlmProviderPanel` render remains (`grep -c '<LlmProviderPanel' src/pages/Analytics.tsx` == 1)
    - `npx tsc --noEmit` clean (no unused-import errors)
  </acceptance_criteria>
  <done>Analytics carries no zeroed indicator, no discarded query, no duplicate panel.</done>
</task>

<task type="auto">
  <name>Task 3: Type BuildProgress Convex access (drop the `as any` casts)</name>
  <files>src/pages/BuildProgress.tsx</files>
  <read_first>
    - src/pages/BuildProgress.tsx (queries :10-14; `c: any` :17; three `as any` prop casts :41,42,46)
    - src/components/TeamStatusCards.tsx (TeamStatusCardsProps :46-49)
    - src/components/BuildActivityFeed.tsx (ActivityEntry :3; entries prop :41)
    - src/components/ComponentTable.tsx (ComponentRow :4; components prop :22)
    - convex/build.ts and convex/pipelines.ts (return shapes of phaseProgress / recentActivity / listAll)
  </read_first>
  <action>Remove the three `as any` prop casts (`components as any`, `pipelines as any` at :41; `activity as any` at :42; `components as any` at :46) and the `c: any` annotation at :17. Rely on the typed `api.*` return types from `convex/_generated`. Where a Convex return type structurally satisfies the child prop interface (TeamStatusCardsProps, `ActivityEntry[]`, `ComponentRow[]`), drop the cast directly. Where a field differs in name or type, either map the array to the child's shape or align the child component's interface to the Convex return type — but do NOT reintroduce `any` or `as any`, and do NOT touch the already-compliant `<h1>` header at :24 or the MetricCard grid.</action>
  <verify>
    <automated>! grep -qE 'as any|: any' src/pages/BuildProgress.tsx && npx tsc --noEmit && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'as any' src/pages/BuildProgress.tsx` == 0
    - `grep -c ': any' src/pages/BuildProgress.tsx` == 0 (the `c: any` filter callback is typed)
    - `npx tsc --noEmit` exits 0 (typed props compile against child interfaces)
  </acceptance_criteria>
  <done>BuildProgress passes typed Convex results to its three child components with no `any` escape hatch.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| client → Convex (build/pipelines) | BuildProgress reads phaseProgress/recentActivity/listAll; typing these removes the `as any` tampering gap |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-96-10-01 | Repudiation (misleading UI) | zeroed TokenSavingsIndicator | mitigate | Remove the always-zero widget so it can't imply a real (zero) savings figure |
| T-96-10-02 | Information Disclosure | hardcoded author "larry" | mitigate | Replace with the in-scope profile identity where available (removes a hardcoded personal literal) |
| T-96-10-03 | Tampering | BuildProgress `as any` Convex prop casts | mitigate | Swap to typed `api.*` results so payload/shape mismatches fail at compile time (F10 untyped-access) |
| T-96-10-SC | Tampering | package installs | accept | No packages installed this phase |
</threat_model>

<verification>
- `grep -cE 'zinc-|emerald-' src/pages/DocComments.tsx` == 0
- KG/ToolGalaxy render `<PageHeader` and drop their bespoke h1
- `grep -c 'as any' src/pages/BuildProgress.tsx` == 0
- `npx tsc --noEmit` clean
</verification>

<success_criteria>
F10 token/a11y minors resolved on the owned files; KG/ToolGalaxy on the standard PageHeader (F7); BuildProgress typed (F10 untyped-access); Analytics dead UI removed.
</success_criteria>

<output>
Create `.planning/phases/96-ui-deep-dive-cleanup-ia-restructure-command-palette-drift-fa/96-10-SUMMARY.md` when done.
</output>
</content>
</invoke>
