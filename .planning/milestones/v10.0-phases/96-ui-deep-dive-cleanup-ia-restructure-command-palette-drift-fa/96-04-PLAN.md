---
phase: 96-ui-deep-dive-cleanup-ia-restructure-command-palette-drift-fa
plan: 04
type: execute
wave: 2
depends_on: ["96-01"]
files_modified:
  - src/pages/Tasks.tsx
  - src/pages/MissionControl.tsx
  - src/pages/Profiles.tsx
  - src/pages/Agents.tsx
  - src/App.tsx
  - src/pages/__tests__/Tasks.test.tsx
autonomous: true
requirements: [F1, D-01, D-02, D-08, F5, F7, F10]
must_haves:
  truths:
    - "The Tasks board offers a 'By Status' (default Kanban) view and a 'By Agent' view via a segmented control (D-01)"
    - "The 'By Agent' view is deep-linkable via ?view=agent and the control syncs to that query param (D-01)"
    - "/mission-control redirects to /tasks?view=agent and the MissionControl page component is deleted (D-02)"
    - "Profiles.tsx and Agents.tsx are deleted along with their App.tsx imports; /profiles and /agents still redirect to /hr/roster (D-08, F5)"
    - "Tasks.tsx uses typed api.tasks.* (not anyApi), migrates its header to <PageHeader>, and drops its max-h-[500px] cap (F10/F7)"
  artifacts:
    - path: "src/pages/Tasks.tsx"
      provides: "Merged board with By Status / By Agent toggle"
      contains: "view=agent"
    - path: "src/App.tsx"
      provides: "mission-control redirect; orphan imports removed"
      contains: "/mission-control"
  key_links:
    - from: "src/App.tsx"
      to: "/tasks?view=agent"
      via: "<Navigate replace> on /mission-control"
      pattern: "mission-control"
    - from: "src/pages/Tasks.tsx"
      to: "api.tasks + api.missionControl.listTasksByAgent"
      via: "typed useQuery"
      pattern: "api\\.tasks\\."
---

<objective>
Merge Mission Control into Tasks as one board with a view toggle (D-01), redirect `/mission-control` → `/tasks?view=agent` and delete the MissionControl page (D-02), delete the orphaned Profiles/Agents pages (D-08), and clean up the surface while it's open: type the `anyApi.tasks.*` calls (F10), migrate the header to `<PageHeader>`, remove the `max-h-[500px]` cap (F7). This plan solely owns `App.tsx` and does all App.tsx edits including the F10 Suspense-fallback token fix.

Purpose: The merge is the highest-complexity UI change this phase. Both views already query the same `tasks` Convex table — no schema change, no data migration.
Output: Merged board, redirects, three deleted page files, typed Convex access, standard header.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/96-ui-deep-dive-cleanup-ia-restructure-command-palette-drift-fa/96-PATTERNS.md
@.planning/phases/96-ui-deep-dive-cleanup-ia-restructure-command-palette-drift-fa/96-RESEARCH.md
@.planning/phases/96-ui-deep-dive-cleanup-ia-restructure-command-palette-drift-fa/96-UI-SPEC.md

<interfaces>
Tasks.tsx current untyped access (src/pages/Tasks.tsx:8-9,24-26) — swap anyApi → typed api in the same pass:
`import { anyApi } from "convex/server"; const rawTasks = useQuery(anyApi.tasks.listByColumn) ?? []; const moveColumn = useMutation(anyApi.tasks.moveColumn); const createTask = useMutation(anyApi.tasks.create);`

MissionControl per-agent read to fold in (src/pages/MissionControl.tsx:38-43), all typed api.* already:
`useQuery(api.missionControl.listTasksByAgent); useQuery(api.agentProfiles.list); useAvatars(); useRosterAgents(); useMutation(api.missionControl.reassignTask);`
`convex/missionControl.ts` queries the SAME `tasks` table — REUSE it as-is (do NOT rename/delete the Convex module; only the React page is deleted). This avoids Convex codegen churn.

View-toggle analog (src/pages/Security.tsx:155-159): shadcn `Tabs` value-controlled. UI-SPEC calls for a quieter segmented control seated in the PageHeader `actions` slot — accent only on active segment; it must not compete with the board. Sync the active value to the `?view=` query param (react-router `useSearchParams`); labels: "By Status" (default) / "By Agent".

App.tsx facts (verified): line 11 `import Profiles from "./pages/Profiles";`, line 24 `const Agents = lazy(...)`, line 50 `const MissionControl = lazy(...)`, line 93 Analytics Suspense fallback uses `text-gray-500` (F10 — change to `text-muted-foreground`), lines 99-100 existing `/profiles` and `/agents` → `<Navigate to="/hr/roster" replace />` (KEEP), line 132 `/mission-control` route rendering MissionControl (replace with a redirect).

Redirect pattern (Navigate `to` supports a query string): `<Route path="/mission-control" element={<Navigate to="/tasks?view=agent" replace />} />`.

Tasks.tsx header (src/pages/Tasks.tsx:101) `text-lg font-semibold`; anomalous cap at :98 `max-h-[500px]`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED test for merged Tasks board</name>
  <files>src/pages/__tests__/Tasks.test.tsx</files>
  <read_first>
    - src/pages/Tasks.tsx (current Kanban structure, anyApi usage, header)
    - src/pages/MissionControl.tsx (per-agent view logic to fold)
    - src/pages/__tests__/Inbox.test.tsx (mocking conventions for Convex useQuery/useMutation in this repo)
  </read_first>
  <behavior>
    - Test 1: with no ?view param, the board renders in "By Status" mode (Kanban columns present).
    - Test 2: with ?view=agent (MemoryRouter initialEntries /tasks?view=agent), the board renders the per-agent grouping.
    - Test 3: clicking the "By Agent" segment switches the active view.
    - Test 4: the page header renders via PageHeader with title "Tasks".
  </behavior>
  <action>Create Tasks.test.tsx. Mock useQuery/useMutation (return sample tasks with/without agentId), mock useRosterAgents/useAvatars. Render under MemoryRouter with initialEntries to drive the ?view= param. Assert view mode by presence of status-column vs agent-group markers, and assert the toggle click switches views. This MUST fail against current Tasks.tsx (no view toggle, no PageHeader).</action>
  <verify>
    <automated>npx vitest run src/pages/__tests__/Tasks.test.tsx 2>&1 | grep -qE "[0-9]+ failed" && echo RED-OK</automated>
  </verify>
  <acceptance_criteria>
    - src/pages/__tests__/Tasks.test.tsx exists and drives ?view=agent via MemoryRouter
    - Fails against current Tasks.tsx (RED confirmed)
  </acceptance_criteria>
  <done>Failing test encodes the merged-board contract.</done>
</task>

<task type="auto">
  <name>Task 2: Merge Mission Control into Tasks (view toggle, typed api, header, uncap)</name>
  <files>src/pages/Tasks.tsx</files>
  <read_first>
    - src/pages/Tasks.tsx (full file)
    - src/pages/MissionControl.tsx (per-agent view render + reassign logic)
    - src/pages/Security.tsx (Tabs view-toggle analog ~155-159)
    - src/components/PageHeader.tsx (from Plan 01)
    - src/pages/__tests__/Tasks.test.tsx (contract to satisfy)
  </read_first>
  <action>In Tasks.tsx: (1) add a ?view=-synced segmented control (via useSearchParams) with "By Status" (default) and "By Agent" segments, seated in the PageHeader actions slot, styled as a quiet control with accent only on the active segment (UI-SPEC). (2) Render the existing Kanban for "By Status"; for "By Agent", fold in MissionControl's per-agent grouping using useQuery(api.missionControl.listTasksByAgent) + useMutation(api.missionControl.reassignTask) + useRosterAgents()/useAvatars() (reuse the Convex module unchanged). (3) Swap anyApi.tasks.* to typed api.tasks.* for listByColumn/moveColumn/create (F10). (4) Replace the h1 (text-lg font-semibold) with PageHeader title="Tasks" actions={viewToggle}. (5) Remove the max-h-[500px] class (F7). Keep existing empty-state copy unchanged (UI-SPEC).</action>
  <verify>
    <automated>npx vitest run src/pages/__tests__/Tasks.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'anyApi' src/pages/Tasks.tsx` returns 0 (typed api now)
    - `grep -qE 'view=agent|"agent"' src/pages/Tasks.tsx` matches (view param handling)
    - `grep -q 'PageHeader' src/pages/Tasks.tsx` matches and `grep -c 'max-h-\[500px\]' src/pages/Tasks.tsx` == 0
    - `npx vitest run src/pages/__tests__/Tasks.test.tsx` exits 0
    - `npx tsc --noEmit` clean for Tasks.tsx (anyApi removal must not introduce type errors)
  </acceptance_criteria>
  <done>One board, two views, typed, standard header, uncapped.</done>
</task>

<task type="auto">
  <name>Task 3: Delete orphan pages + wire App.tsx redirects and token fix</name>
  <files>src/App.tsx, src/pages/MissionControl.tsx, src/pages/Profiles.tsx, src/pages/Agents.tsx</files>
  <read_first>
    - src/App.tsx (imports lines 11/24/50; routes lines 93/99-100/132)
    - src/pages/MissionControl.tsx, src/pages/Profiles.tsx, src/pages/Agents.tsx (confirm delete targets)
  </read_first>
  <action>Re-verify no stray importers first (grep for pages/Profiles, pages/Agents, pages/MissionControl across src/ — expect only App.tsx and the files themselves). Then in App.tsx: remove the Profiles import (line 11), the Agents lazy import (line 24), and the MissionControl lazy import (line 50); KEEP the existing /profiles and /agents redirects to /hr/roster; replace the /mission-control route (line 132) with a Navigate redirect to "/tasks?view=agent" replace; change the Analytics Suspense fallback text-gray-500 (line 93) to text-muted-foreground (F10). Delete src/pages/MissionControl.tsx, src/pages/Profiles.tsx, src/pages/Agents.tsx (git history preserves them, D-08). Do NOT delete convex/missionControl.ts.</action>
  <verify>
    <automated>test ! -f src/pages/MissionControl.tsx && test ! -f src/pages/Profiles.tsx && test ! -f src/pages/Agents.tsx && grep -q 'to="/tasks?view=agent"' src/App.tsx && ! grep -q 'text-gray-500' src/App.tsx && npx tsc --noEmit && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - The three page files no longer exist
    - `grep -q 'to="/tasks?view=agent"' src/App.tsx` matches
    - `grep -c 'text-gray-500' src/App.tsx` == 0
    - `grep -c 'pages/Profiles\|pages/Agents\|pages/MissionControl' src/App.tsx` == 0
    - `npx tsc --noEmit` exits 0 (no dangling imports)
  </acceptance_criteria>
  <done>Orphans deleted, redirects wired, App.tsx type-clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| client → Convex (tasks/missionControl) | Task read + reassign mutation; typing these removes the anyApi tampering gap |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-96-04-01 | Tampering | anyApi.tasks.* untyped calls | mitigate | Swap to typed api.tasks.* so payload/shape mismatches fail at compile time (D-10 "type it while merging") |
| T-96-04-02 | Denial of Service | listTasksByAgent .collect() | accept | Pre-existing bounded task volume; no new unbounded scan introduced by the merge (reuses existing query) |
| T-96-04-SC | Tampering | package installs | accept | No packages installed this phase |
</threat_model>

<verification>
- `npx vitest run src/pages/__tests__/Tasks.test.tsx` green
- three orphan page files deleted; `npx tsc --noEmit` clean
- `/mission-control` redirects to `/tasks?view=agent`
</verification>

<success_criteria>
Tasks and Mission Control are one board with a deep-linkable toggle; orphans gone; App.tsx clean and typed.
</success_criteria>

<output>
Create `.planning/phases/96-ui-deep-dive-cleanup-ia-restructure-command-palette-drift-fa/96-04-SUMMARY.md` when done.
</output>
