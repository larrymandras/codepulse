---
phase: 96-ui-deep-dive-cleanup-ia-restructure-command-palette-drift-fa
plan: 08
type: execute
wave: 2
depends_on: ["96-01"]
files_modified:
  - src/pages/MeetingBot.tsx
  - src/pages/Skills.tsx
  - src/components/skills/CategoryEditPopover.tsx
  - src/pages/__tests__/MeetingBot.test.tsx
autonomous: true
requirements: [D-10, F9, F7]
must_haves:
  truths:
    - "MeetingBot's agent dropdown is populated from the live roster via useRosterAgents(), not the 6 hardcoded agent names (D-10/F9)"
    - "Skills create-category modal no longer renders a permanently-disabled no-op delete button (D-10) — the delete affordance is not shown when isNew/!canDelete"
    - "MeetingBot and Skills headers use <PageHeader> (F7)"
  artifacts:
    - path: "src/pages/MeetingBot.tsx"
      provides: "Live-roster agent select"
      contains: "useRosterAgents"
    - path: "src/pages/__tests__/MeetingBot.test.tsx"
      provides: "Roster-driven dropdown test"
  key_links:
    - from: "src/pages/MeetingBot.tsx"
      to: "useRosterAgents"
      via: "hook → SelectItem map"
      pattern: "useRosterAgents"
---

<objective>
Replace MeetingBot's 6 hardcoded agent names with the live roster (D-10/F9), and drop the Skills create-modal's permanently-disabled no-op delete button (D-10) rather than leaving a dead control. Migrate both headers to `<PageHeader>` (F7).

Purpose: Two cheap live/dead-UI fixes on the same wave; both are isolated per-page changes.
Output: Roster-driven MeetingBot dropdown + test, guarded delete affordance in the Skills popover, standard headers.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/96-ui-deep-dive-cleanup-ia-restructure-command-palette-drift-fa/96-PATTERNS.md
@.planning/phases/96-ui-deep-dive-cleanup-ia-restructure-command-palette-drift-fa/96-RESEARCH.md
@.planning/phases/96-ui-deep-dive-cleanup-ia-restructure-command-palette-drift-fa/96-UI-SPEC.md

<interfaces>
Roster hook usage analog (src/pages/WarRoom.tsx:34,53): `import { useRosterAgents } from "@/hooks/useRosterAgents"; const { agents } = useRosterAgents();`. `RosterAgent` (src/hooks/useRosterAgents.ts:18-24) extends `AgentListItem` — use its `name`/id field for the SelectItem value/label.

MeetingBot hardcoded list to replace (src/pages/MeetingBot.tsx:156-163): a `<Select>` with 6 static `<SelectItem>`s (freya/astrid/hervor/hildr/gondul/ragnhildr). Replace the static items with `agents.map(...)` from useRosterAgents().

Skills no-op (src/pages/Skills.tsx:376-389): create-category modal passes `onDelete={() => {}}` and `canDelete={false}` with `isNew`. CategoryEditPopover (src/components/skills/CategoryEditPopover.tsx:136-144) renders a delete `<button disabled={!canDelete}>`. Per RESEARCH OQ3 (resolved): gate the whole delete `<button>` render on `canDelete` (or an `isNew` prop) so it isn't shown at all when nothing exists to delete. No new handler needed.

Headers (F7): MeetingBot h1 `text-xl font-semibold` → PageHeader; Skills h1 `text-2xl font-mono … text-primary` (terminal micro-header) → PageHeader title="Skills".
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED test — MeetingBot dropdown reflects live roster</name>
  <files>src/pages/__tests__/MeetingBot.test.tsx</files>
  <read_first>
    - src/pages/MeetingBot.tsx (hardcoded Select ~156-163)
    - src/pages/WarRoom.tsx (useRosterAgents consumption ~34,53)
    - src/hooks/useRosterAgents.ts (RosterAgent shape ~18-24)
    - src/pages/__tests__/Inbox.test.tsx (mocking conventions)
  </read_first>
  <behavior>
    - Test 1: with useRosterAgents mocked to return a known roster (e.g. ["Odin","Thor"]), the agent select renders those options and NOT the hardcoded 6 names.
    - Test 2: with an empty roster, no stale hardcoded names appear (e.g. "hervor" is absent).
  </behavior>
  <action>Create MeetingBot.test.tsx. Mock `useRosterAgents` to return a controllable agents array; render MeetingBot; assert the select options correspond to the mocked roster and that a previously-hardcoded name (e.g. "hervor"/"gondul") is not present. This MUST fail against the current hardcoded list.</action>
  <verify>
    <automated>npx vitest run src/pages/__tests__/MeetingBot.test.tsx 2>&1 | grep -qi fail && echo RED-OK</automated>
  </verify>
  <acceptance_criteria>
    - src/pages/__tests__/MeetingBot.test.tsx exists and mocks useRosterAgents
    - Asserts roster-driven options and absence of hardcoded names
    - Fails against current MeetingBot.tsx (RED confirmed)
  </acceptance_criteria>
  <done>Failing test encodes the live-roster dropdown contract.</done>
</task>

<task type="auto">
  <name>Task 2: Wire live roster + guard Skills delete affordance + headers</name>
  <files>src/pages/MeetingBot.tsx, src/pages/Skills.tsx, src/components/skills/CategoryEditPopover.tsx</files>
  <read_first>
    - src/pages/MeetingBot.tsx (Select ~156-163; header)
    - src/pages/WarRoom.tsx (useRosterAgents pattern)
    - src/pages/Skills.tsx (create-modal call site ~376-389; header ~167)
    - src/components/skills/CategoryEditPopover.tsx (delete button ~136-144, props)
    - src/components/PageHeader.tsx (Plan 01)
    - src/pages/__tests__/MeetingBot.test.tsx (contract to satisfy)
  </read_first>
  <action>MeetingBot.tsx: import useRosterAgents; replace the 6 static SelectItems with `agents.map((a) => <SelectItem key={a.id/name} value={a.id/name}>{a.name}</SelectItem>)`, matching WarRoom's field usage. Migrate the header to PageHeader title="Meeting Bot". CategoryEditPopover.tsx: gate the delete `<button>` render on `canDelete` (only render the button when canDelete is true, or add an `isNew` prop and return null for the button when isNew) so the create modal shows no disabled delete control. Skills.tsx: the `onDelete={() => {}}` becomes harmless (button no longer renders); migrate the header to PageHeader title="Skills".</action>
  <verify>
    <automated>npx vitest run src/pages/__tests__/MeetingBot.test.tsx && npx tsc --noEmit && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'useRosterAgents' src/pages/MeetingBot.tsx` and no hardcoded roster names remain (`grep -ci 'value="hervor"' src/pages/MeetingBot.tsx` == 0)
    - CategoryEditPopover only renders the delete button when canDelete (verify the button JSX is inside a `canDelete &&` guard or equivalent)
    - MeetingBot and Skills both import and render `<PageHeader`
    - `npx vitest run src/pages/__tests__/MeetingBot.test.tsx` exits 0
    - `npx tsc --noEmit` clean
  </acceptance_criteria>
  <done>Roster is live; the dead delete control is gone; headers standardized.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| client → Convex roster query | useRosterAgents read; rendered as select options |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-96-08-01 | Repudiation (stale UI) | hardcoded agent names | mitigate | Drive the dropdown from live roster so it can't drift from the real agent set |
| T-96-08-02 | Spoofing | roster values | accept | Roster comes from the same trusted Convex source used by WarRoom; no new input path |
| T-96-08-SC | Tampering | package installs | accept | No packages installed this phase |
</threat_model>

<verification>
- `npx vitest run src/pages/__tests__/MeetingBot.test.tsx` green
- `npx tsc --noEmit` clean
</verification>

<success_criteria>
MeetingBot dropdown is roster-driven; Skills create modal shows no dead delete button; both headers standardized.
</success_criteria>

<output>
Create `.planning/phases/96-ui-deep-dive-cleanup-ia-restructure-command-palette-drift-fa/96-08-SUMMARY.md` when done.
</output>
