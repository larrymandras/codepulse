---
phase: 101-reminders-calendar-command-center
plan: 06
type: execute
wave: 3
depends_on: [01, 02]
repo: codepulse
files_modified:
  - C:\Users\mandr\codepulse\src\pages\Reminders.tsx
  - C:\Users\mandr\codepulse\src\components\reminders\ReminderList.tsx
  - C:\Users\mandr\codepulse\src\components\reminders\CalendarOverlay.tsx
  - C:\Users\mandr\codepulse\src\components\reminders\QuickAdd.tsx
  - C:\Users\mandr\codepulse\src\pages\Reminders.test.tsx
  - C:\Users\mandr\codepulse\src\App.tsx
  - C:\Users\mandr\codepulse\src\lib\navRegistry.ts
autonomous: true
requirements: [UI-01, UI-02, CAL-02]
must_haves:
  truths:
    - "the Reminders page is a lazy route /reminders registered in navRegistry COMMAND cluster, following App.tsx's lazy+Suspense pattern exactly (D-08, UI-01)"
    - "the page is profile-segmented (personal/business/consulting) each with its own accent; switching profiles swaps both the reminder list and the calendar overlay (UI-01)"
    - "reminders render via useQuery(api.reminders.listByProfile) and calendar via useQuery(api.calendarEvents.listByProfile) so an astridr-sourced reminder or a fresh calendar push appears WITHOUT a manual refresh (realtime, REM-01/CAL-02)"
    - "due-dated reminders render as chips on the calendar grid alongside read-only Google events, visually distinct; the page NEVER writes Google (D-02)"
    - "reminder title/notes/event text render as React text nodes (no dangerouslySetInnerHTML) so cached Google/agent text cannot inject markup (T-101-03)"
    - "complete/snooze/quick-add are optimistic against api.reminders mutations and reconcile on server confirm; all motion respects prefers-reduced-motion (UI-02)"
  artifacts:
    - path: "C:\\Users\\mandr\\codepulse\\src\\pages\\Reminders.tsx"
      provides: "profile-segmented reminders + calendar overlay page"
      contains: "listByProfile"
    - path: "C:\\Users\\mandr\\codepulse\\src\\lib\\navRegistry.ts"
      provides: "Reminders nav entry in COMMAND cluster"
      contains: "/reminders"
  key_links:
    - from: "Reminders.tsx"
      to: "api.reminders.listByProfile"
      via: "useQuery (realtime)"
      pattern: "listByProfile"
---

<objective>
Build the Reminders command-center page: profile-segmented (personal/business/consulting), a grouped reminder list beside a read-only Google Calendar overlay, quick actions and quick-add, and the effects that make it feel "kick ass" without becoming a generic todo app. Satisfies UI-01, UI-02, CAL-02 and the full 101-UI-SPEC contract.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/101-reminders-calendar-command-center/101-CONTEXT.md
@.planning/phases/101-reminders-calendar-command-center/101-UI-SPEC.md
@.planning/phases/101-reminders-calendar-command-center/101-01-PLAN.md

<interfaces>
Convex (plans 01/02): api.reminders.{listByProfile,create,update,complete,snooze,remove}; api.calendarEvents.listByProfile.
Profile IDs: "personal"|"business"|"consulting".
App.tsx pattern: `const X = lazy(() => import("./pages/X"))` + `<Route path="/x" element={<Suspense fallback={...}><X/></Suspense>}/>`.
navRegistry.ts: add to the COMMAND group's items: `{ to: "/reminders", label: "Reminders", icon: "<icon>", group: "COMMAND" }`; ensure the icon key exists in iconComponents (reuse "clock"/"bell" or add one to the map).
House style: mono type, oklch primary glow, per-profile accent, PageHeader, shadcn ui/* primitives, dark/light theme aware. See 101-UI-SPEC.md for the full contract.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Route + nav registration</name>
  <files>C:\Users\mandr\codepulse\src\App.tsx, C:\Users\mandr\codepulse\src\lib\navRegistry.ts</files>
  <read_first>
    - src/App.tsx (lazy + Suspense route block)
    - src/lib/navRegistry.ts (COMMAND cluster, iconComponents map, NavItem shape)
  </read_first>
  <behavior>
    - /reminders is a lazy route; a "Reminders" item appears in the COMMAND cluster with a valid icon.
  </behavior>
  <action>
    Add the lazy import + Suspense route to App.tsx (mirror an existing COMMAND page, e.g. Tasks). Add the nav item to navRegistry COMMAND group; ensure its icon key is in iconComponents (add a lucide import + map entry if not).
  </action>
  <verify>
    <automated>cd /c/Users/mandr/codepulse && npx tsc --noEmit && npx vitest run src/lib/__tests__ 2>/dev/null || true</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "/reminders" src/lib/navRegistry.ts src/App.tsx` present; icon key resolves in iconComponents.
    - typecheck clean.
  </acceptance_criteria>
  <done>Reminders is reachable from the sidebar and routes to the page.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Profile-segmented page shell + reminder list</name>
  <files>C:\Users\mandr\codepulse\src\pages\Reminders.tsx, C:\Users\mandr\codepulse\src\components\reminders\ReminderList.tsx, C:\Users\mandr\codepulse\src\components\reminders\QuickAdd.tsx, C:\Users\mandr\codepulse\src\pages\Reminders.test.tsx</files>
  <read_first>
    - a recent lazy page (src/pages/Tasks.tsx / Alerts.tsx) for useQuery/useMutation + PageHeader + profile handling
    - 101-UI-SPEC.md (layout, groups, quick actions, effects)
  </read_first>
  <behavior>
    - Segmented control [Personal · Business · Consulting] (+ optional All), persisted to localStorage, each with an accent that tints the page glow.
    - ReminderList grouped Overdue / Today / Upcoming / Done (Done collapsed); rows show title, relative due, priority pip, recurrence icon, tags, subtle origin marker.
    - Quick actions: complete (optimistic + settle animation), snooze menu, edit. QuickAdd bar with due/priority/recurrence controls, ⌘K/N focus; defaults profile to the selected one.
    - Data via useQuery(api.reminders.listByProfile,{profileId}); mutations optimistic.
    - Empty states per group; prefers-reduced-motion disables pulses/count-ups; text rendered as React nodes only.
  </behavior>
  <action>
    Build Reminders.tsx (shell + segmented control + accent), ReminderList.tsx (grouping + rows + quick actions), QuickAdd.tsx. Wire realtime queries + optimistic mutations. Follow the UI-SPEC exactly. Tests (component, mocked Convex): switching profile re-queries with the new profileId; an added reminder appears; complete calls api.reminders.complete optimistically; overdue rows get overdue styling; no dangerouslySetInnerHTML in the tree.
  </action>
  <verify>
    <automated>cd /c/Users/mandr/codepulse && npx vitest run src/pages/Reminders.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Test: selecting Business queries listByProfile with profileId "business".
    - Test: complete triggers api.reminders.complete; the row moves to Done optimistically.
    - Test: an overdue reminder renders overdue styling and is in the Overdue group.
    - `grep -rc "dangerouslySetInnerHTML" src/components/reminders src/pages/Reminders.tsx` == 0.
  </acceptance_criteria>
  <done>The profile-segmented list with quick actions + quick-add works against the live store.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Calendar overlay (read-only) with reminder chips</name>
  <files>C:\Users\mandr\codepulse\src\components\reminders\CalendarOverlay.tsx, C:\Users\mandr\codepulse\src\pages\Reminders.test.tsx</files>
  <read_first>
    - 101-UI-SPEC.md (calendar pane, chip distinction)
    - 101-02 calendarEvents.listByProfile shape
  </read_first>
  <behavior>
    - Month view (week toggle) for the selected profile: renders read-only Google events (from api.calendarEvents.listByProfile) AND due-dated reminders as chips on their due day, visually distinct (accent-filled reminder chips vs. outline Google events), priority-colored.
    - Clicking a day filters/scrolls the list to that day. Responsive: collapses under the list on narrow widths; page body never scrolls horizontally.
    - Read-only: no create/edit of Google events; no write path.
  </behavior>
  <action>
    Build CalendarOverlay.tsx from the two realtime queries. Keep it dependency-light (prefer a small hand-rolled month grid or an existing lightweight date util already in deps — do NOT add a heavy calendar lib without checking package.json). Reminder chips are distinct from Google events. Tests: a due-dated reminder shows a chip on its day; a Google event renders as a (distinct) event; there is no Google-write handler; narrow viewport collapses to single column.
  </action>
  <verify>
    <automated>cd /c/Users/mandr/codepulse && npx vitest run src/pages/Reminders.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Test: a reminder with dueAt on day D renders a reminder-chip on day D; a calendarEvents row renders as a visually distinct event.
    - Test: switching profile swaps the overlay's events.
    - No Google mutation import/handler in the component.
  </acceptance_criteria>
  <done>The overlay shows Google events + reminder chips per profile, read-only, responsive.</done>
</task>

</tasks>

<verification>
- `cd /c/Users/mandr/codepulse && npx vitest run src/pages/Reminders.test.tsx` green; `npx tsc --noEmit` clean.
- Live (webapp-testing / playwright): dev server up, /reminders renders; switch all three profiles (accents change, panes swap); create a reminder in the UI and confirm it persists on reload; a reminder created via the Ástríðr tool appears without refresh (realtime); an overdue item shows overdue styling + a calendar chip; prefers-reduced-motion disables motion.
</verification>

<success_criteria>
- Lazy /reminders route in the COMMAND cluster; profile-segmented with per-profile accents.
- Realtime list + read-only calendar overlay with reminder chips; cross-surface sync visible without refresh.
- Optimistic quick actions + quick-add; effects tasteful and reduced-motion-safe; no Google write; no HTML injection.
- Meets the 101-UI-SPEC contract.
</success_criteria>

<output>
Create `.planning/phases/101-reminders-calendar-command-center/101-06-SUMMARY.md` when done.
</output>
