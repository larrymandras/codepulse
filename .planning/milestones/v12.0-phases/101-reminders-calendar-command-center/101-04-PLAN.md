---
phase: 101-reminders-calendar-command-center
plan: 04
type: execute
wave: 3
depends_on: [02]
repo: astridr-repo
files_modified:
  - C:\Users\mandr\astridr-repo\astridr\automation\calendar_cache.py
  - C:\Users\mandr\astridr-repo\tests\automation\test_calendar_cache.py
  - C:\Users\mandr\astridr-repo\astridr\automation\jobs.py
autonomous: true
requirements: [CAL-01]
must_haves:
  truths:
    - "a cron fetches each profile's Google Calendar via google_workspace list_events on its OWN account alias (personal->mandrasle, business->lmandras@myprotectall, consulting->lemandras@forgedinai) and pushes normalized events to /calendar-ingest (D-03, D-06, CAL-01)"
    - "the fetch is a bounded forward window (default ~60 days) so the cache stays small, and each profile pushes independently so one account's auth failure never blanks another profile's cache (D-10)"
    - "the cron only READS Google (list_events) and only WRITES the Convex cache — it never creates/updates a Google event (D-02)"
    - "a per-account auth/permission failure is logged (structlog) and skipped, not fatal — the other profiles still refresh (RELI)"
  artifacts:
    - path: "C:\\Users\\mandr\\astridr-repo\\astridr\\automation\\calendar_cache.py"
      provides: "per-profile list_events -> normalize -> POST /calendar-ingest, bounded window, per-profile isolation"
      contains: "list_events"
  key_links:
    - from: "calendar_cache cron"
      to: "/calendar-ingest"
      via: "ConvexHandler.send_to per profile"
      pattern: "calendar-ingest"
---

<objective>
Keep the CodePulse calendar overlay fresh: an Ástríðr cron fetches each profile's real Google Calendar (read-only) on its own account and pushes a normalized event window to `/calendar-ingest`. This is the CAL-01 source half — the browser never touches Google; Ástríðr is the only fetcher (D-03).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/101-reminders-calendar-command-center/101-CONTEXT.md
@.planning/phases/101-reminders-calendar-command-center/101-02-PLAN.md

<interfaces>
Profile -> Google account alias (config/google-workspace.yaml):
  personal -> mandrasle@gmail.com | business -> lmandras@myprotectall.com | consulting -> lemandras@forgedinai.ai
google_workspace calendar (astridr/tools/google_workspace.py):
  _handle_calendar / _calendar_list -> list_events(account_alias, time_min, time_max)  // per-account, permission-gated
  ⚠ confirm the account_alias enum accepts "consulting" (its docstring lists only personal/business/astridr) — extend if needed or the consulting fetch is rejected.
/calendar-ingest body (plan 02): { profileId, calendarAccount, events:[{googleEventId,title,start,end,allDay,location?}], fetchedAt }
  (epoch seconds; upsert by googleEventId + prune stale for that profile+account)
Scheduler: astridr/automation/cron.py / jobs.py — register the job like existing periodic jobs.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: per-profile fetch + normalize + push</name>
  <files>C:\Users\mandr\astridr-repo\astridr\automation\calendar_cache.py, C:\Users\mandr\astridr-repo\tests\automation\test_calendar_cache.py</files>
  <read_first>
    - astridr/tools/google_workspace.py (_calendar_list / list_events signature + return shape)
    - astridr/engine/telemetry.py (ConvexHandler.send_to)
    - config/google-workspace.yaml (aliases + allowed_actions incl list_events)
  </read_first>
  <behavior>
    - For each profile in [personal, business, consulting]: call list_events on its alias for [now, now+window] (window configurable, default 60 days); map Google event -> {googleEventId=event.id, title=summary, start/end epoch seconds (handle all-day date vs dateTime), allDay bool, location?}; POST to /calendar-ingest with profileId + calendarAccount(alias/email) + fetchedAt.
    - Per-profile try/except: an auth/permission/HTTP error is logged via structlog and skipped; the loop continues to the next profile.
    - Read-only: no create_event call anywhere.
  </behavior>
  <action>
    Implement calendar_cache.refresh() iterating the three profiles. Normalize all-day (date) vs timed (dateTime) events to epoch seconds. Push per profile. Isolate failures per profile. Tests mock list_events + the POST: three profiles each push their own account's events to /calendar-ingest; an all-day event normalizes with allDay:true; a raised error on the business account still lets personal+consulting push (isolation); assert NO create_event is ever called.
  </action>
  <verify>
    <automated>cd /c/Users/mandr/astridr-repo && python -m pytest tests/automation/test_calendar_cache.py -q</automated>
  </verify>
  <acceptance_criteria>
    - Test: each profile pushes to /calendar-ingest with its own calendarAccount and only list_events (never create_event) is called.
    - Test: an exception fetching one profile does not prevent the others pushing (per-profile isolation).
    - Test: an all-day Google event maps to allDay:true with sane epoch start/end.
  </acceptance_criteria>
  <done>refresh() fetches all three calendars read-only and pushes normalized windows, failure-isolated.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: register the periodic job</name>
  <files>C:\Users\mandr\astridr-repo\astridr\automation\jobs.py</files>
  <read_first>
    - astridr/automation/jobs.py + cron.py (existing periodic job registration)
  </read_first>
  <behavior>
    - calendar_cache.refresh runs on a periodic schedule (e.g. every 15-30 min — pick per existing cadence conventions; document the interval).
  </behavior>
  <action>
    Register the job following the existing periodic-job pattern. Keep the interval a named constant so it is tunable. Do not block the loop — follow the existing async job convention.
  </action>
  <verify>
    <automated>cd /c/Users/mandr/astridr-repo && python -m pytest tests/automation/test_calendar_cache.py -q</automated>
  </verify>
  <acceptance_criteria>
    - The job is registered in the scheduler with a named interval constant.
  </acceptance_criteria>
  <done>The calendar cache refreshes on a schedule.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Google Calendar -> cron -> /calendar-ingest | event text (attacker-influenceable via a shared invite) crosses into the cache and later renders in CodePulse |
| cron -> Convex | authed push (ASTRIDR_INGEST_API_KEY) |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-101-08 | EoP | Google write | eliminate | Only list_events is called; no create_event path in this module (D-02). Unit-asserted. |
| T-101-09 | Tampering (XSS) | event title/location | mitigate downstream | Carried as data to the cache; page renders as React text (plan 02 T-101-03). |
| T-101-10 | DoS/leak | per-account failure | mitigate | Per-profile isolation + structlog; one account's OAuth failure neither blanks other caches nor crashes the loop. |
</threat_model>

<verification>
- `cd /c/Users/mandr/astridr-repo && python -m pytest tests/automation/test_calendar_cache.py -q` green.
- Manual (needs live creds): run refresh once, confirm calendarEvents populated for all three profiles in Convex; a stale event dropped next cycle (plan 02 prune).
</verification>

<success_criteria>
- Three profiles' Google calendars fetched read-only on a schedule and pushed to /calendar-ingest, failure-isolated, bounded window.
- No Google write anywhere; consulting alias confirmed accepted.
</success_criteria>

<output>
Create `.planning/phases/101-reminders-calendar-command-center/101-04-SUMMARY.md` when done.
</output>
