---
phase: 88-analytics-rollup
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - convex/lib/sankeyClassify.ts
  - convex/analyticsRollup.test.ts
  - convex/analytics.test.ts
  - convex/aggregates.test.ts
autonomous: true
requirements: [AR-01, AR-02, AR-03]

must_haves:
  truths:
    - "The sankey classifier (categoryOf/outcomeOf) exists in one shared module imported by both the read and write paths so classification can never drift"
    - "Three Nyquist test files exist and run, asserting the rollup invariants (idempotency, no-key-counted, cron-removal-no-double-count, retention-leaves-aggregates, heatmap mapping, missing-hour=0)"
    - "Tests for already-implemented logic (sankeyClassify) pass; tests for Wave 1 logic fail RED until implemented"
  artifacts:
    - path: "convex/lib/sankeyClassify.ts"
      provides: "Shared categoryOf(eventType) and outcomeOf(eventType) exports"
      exports: ["categoryOf", "outcomeOf"]
    - path: "convex/analyticsRollup.test.ts"
      provides: "Idempotency, no-key-counted, backfill-count-equality, increment patch-or-insert invariants"
      min_lines: 40
    - path: "convex/analytics.test.ts"
      provides: "Heatmap day-of-week×hour mapping, errorRateTrend missing-hour=0 invariants"
      min_lines: 30
    - path: "convex/aggregates.test.ts"
      provides: "Cron-removal non-double-count + dataRetention-leaves-aggregates invariants (extends existing file)"
      contains: "Phase 88"
  key_links:
    - from: "convex/lib/sankeyClassify.ts"
      to: "convex/analytics.ts AND convex/analyticsRollup.ts"
      via: "named import { categoryOf, outcomeOf }"
      pattern: "import.*sankeyClassify"
---

<objective>
Create the shared sankey classifier module and the three Nyquist test scaffolds before any write-path or read-path implementation lands.

Purpose: Extracting categoryOf/outcomeOf to one module BEFORE the ingest-time sankey increment is written prevents Pitfall 2 (classification drift between write and read paths). Standing up the test files first establishes the Nyquist invariant gates so Wave 1/3 implementation has a RED→GREEN target.
Output: convex/lib/sankeyClassify.ts (new) + 3 test files (1 new logic test, 1 new query test, 1 extension of the existing aggregates.test.ts).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/88-analytics-rollup-table-durable-fix-for-convex-16-mib-read-li/88-CONTEXT.md
@.planning/phases/88-analytics-rollup-table-durable-fix-for-convex-16-mib-read-li/88-RESEARCH.md
@.planning/phases/88-analytics-rollup-table-durable-fix-for-convex-16-mib-read-li/88-PATTERNS.md
@.planning/phases/88-analytics-rollup-table-durable-fix-for-convex-16-mib-read-li/88-VALIDATION.md

<interfaces>
<!-- The classifier contract Wave 1 (analyticsRollup.ts) and Wave 3 (analytics.ts) both consume. -->
From convex/lib/sankeyClassify.ts (to be created):
  export function categoryOf(eventType: string): string
  export function outcomeOf(eventType: string): string

Existing source to extract (convex/analytics.ts:53-65) — categoryOf is a `const` arrow fn; outcomeOf currently takes `{eventType, payload}` but only reads eventType (confirmed RESEARCH OQ-2). Extract outcomeOf to take a plain `eventType: string`.

Test ctx.db mock pattern (convex/llm.test.ts:7-14) and pure-logic vitest pattern (convex/aggregates.test.ts) are the canonical analogs — no convex-test harness.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extract categoryOf/outcomeOf into convex/lib/sankeyClassify.ts</name>
  <files>convex/lib/sankeyClassify.ts, convex/analytics.ts</files>
  <read_first>
    - convex/analytics.ts:53-65 (the current `categoryOf` / `outcomeOf` arrow-fn declarations — source of truth for the exact branch logic)
    - convex/analytics.ts:39-96 (toolFlowSankey — the only current consumer, to confirm call shape)
    - 88-PATTERNS.md "NEW convex/lib/sankeyClassify.ts" section (extract-verbatim instructions)
  </read_first>
  <behavior>
    - categoryOf("tool_use") === "Tool Use"; categoryOf("llm_call") === "LLM"; categoryOf("model_x") === "LLM"; categoryOf("file_write") === "File Ops"; categoryOf("agent_spawn") === "Agents"; categoryOf("anything_else") === "Other"
    - outcomeOf("ToolError") === "Error"; outcomeOf("PostToolUseFailure") === "Error"; outcomeOf("hitl_review") === "HITL"; outcomeOf("Info") === "Success"
    - Logic is byte-identical to the current analytics.ts implementation (semantically equivalent rewrite, no behavior change)
  </behavior>
  <action>
    Create convex/lib/sankeyClassify.ts exporting two named functions: `categoryOf(eventType: string): string` and `outcomeOf(eventType: string): string`. Copy the branch logic VERBATIM from convex/analytics.ts:53-65 — do NOT alter any string literal ("Tool Use", "LLM", "File Ops", "Agents", "Other", "Error", "HITL", "Success") or any prefix/substring test (startsWith "tool_"/"llm_"/"model_"/"file_"/"agent_"; includes "error"/"fail"/"hitl"/"review"). Drop the unused `payload` param: outcomeOf takes a plain `eventType: string` (RESEARCH OQ-2 confirms payload is never read). Then in convex/analytics.ts, DELETE the two local `const categoryOf` / `const outcomeOf` declarations and add `import { categoryOf, outcomeOf } from "./lib/sankeyClassify";` at the top. Update the toolFlowSankey call site so `outcomeOf` is invoked with the eventType string directly (not the `{eventType, payload}` object). No fenced code — follow the read_first analog.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20; npx vitest run convex/analyticsRollup.test.ts -t "categoryOf" 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - convex/lib/sankeyClassify.ts exports `categoryOf` and `outcomeOf` (grep `export function categoryOf` and `export function outcomeOf` each return 1)
    - convex/analytics.ts contains `import { categoryOf, outcomeOf } from "./lib/sankeyClassify"` and no longer declares `const categoryOf` / `const outcomeOf`
    - `npx tsc --noEmit` exits 0 (no type errors from the extraction)
    - Classifier unit tests in convex/analyticsRollup.test.ts pass
  </acceptance_criteria>
  <done>Shared classifier module exists, analytics.ts imports it, type check clean, classifier tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Scaffold convex/analyticsRollup.test.ts (rollup write-path invariants)</name>
  <files>convex/analyticsRollup.test.ts</files>
  <read_first>
    - convex/aggregates.test.ts (canonical pure-logic vitest pattern — describe/test/expect, no convex-test harness)
    - convex/llm.test.ts:7-14 (inline mock ctx.db pattern: in-memory array store with query/withIndex/collect/first/insert/patch)
    - 88-PATTERNS.md "NEW convex/analyticsRollup.test.ts" section + "Mock ctx.db pattern"
    - 88-VALIDATION.md "Per-Task Verification Map" (the exact invariants this file owns)
  </read_first>
  <behavior>
    - Idempotency: invoking the dedup+increment path twice with the SAME idempotencyKey yields exactly 1 event row and 1 rollup increment (bucket value === 1)
    - No-key-counted (D-05): invoking twice with NO idempotencyKey yields 2 event rows and 2 increments (bucket value === 2) — never a lossy hash drop
    - Backfill count-equality: for N seeded events across multiple eventTypes/hours, the sum of "events" bucket values === N
    - Increment patch-or-insert: first call to incrementEventBucket inserts a row (value 1); second call for the same {eventType, hour} patches to value 2 (does NOT create a second row)
    - categoryOf/outcomeOf return the correct strings for known eventTypes (imported from ./lib/sankeyClassify)
  </behavior>
  <action>
    Create convex/analyticsRollup.test.ts using the pure-logic vitest pattern (import { describe, test, expect } from "vitest"). Build an in-memory aggregates+events store mock (makeStore helper) mirroring convex/llm.test.ts:7-14 — supporting query("aggregates"/"events").withIndex(...).collect()/.first(), insert, and patch over a plain array. Import `categoryOf, outcomeOf` from "./lib/sankeyClassify". Write describe blocks for: "idempotency invariants" (same-key → 1 row/value 1; no-key twice → 2 rows/value 2), "increment patch-or-insert" (insert then patch), "backfill count-equality" (N seeded events → bucket sum === N), and "classifier" (categoryOf/outcomeOf string assertions). The increment/dedup behavioral tests reference `incrementEventBucket`, `incrementSankeyBuckets`, and the dedup path from convex/analyticsRollup.ts + convex/events.ts which DO NOT EXIST YET (Wave 1) — these tests are EXPECTED to fail RED now and go GREEN after Plan 02. Import them with try/skip or write them so they RED cleanly (compile must still pass — if the module is missing, gate those describe blocks behind a dynamic import guard that marks them failing, not erroring the whole file). The classifier describe block must pass NOW.
  </action>
  <verify>
    <automated>npx vitest run convex/analyticsRollup.test.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - convex/analyticsRollup.test.ts exists and the file loads without a compile/parse error
    - The "classifier" describe block passes (green)
    - The idempotency / increment / backfill describe blocks are present and reference incrementEventBucket + the idempotencyKey dedup path (grep `idempotencyKey` and `incrementEventBucket` each ≥1)
    - No watch-mode flags in the verify command
  </acceptance_criteria>
  <done>Write-path invariant test file exists; classifier tests green; rollup invariant tests present and RED-pending Wave 1.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Scaffold convex/analytics.test.ts + extend convex/aggregates.test.ts (read-path + retention invariants)</name>
  <files>convex/analytics.test.ts, convex/aggregates.test.ts</files>
  <read_first>
    - convex/aggregates.test.ts (existing — extend in place; follow its describe nesting; do not rewrite existing tests)
    - convex/analytics.ts:3-37 (activityHeatmap — the `new Date(bucket_start*1000).getDay()/.getHours()` mapping to preserve)
    - convex/analytics.ts:146-193 (errorRateTrend — the 24-slot-init-to-0 pattern, Pitfall 7)
    - convex/dataRetention.ts:6-21 (purgeOldTelemetryEvents — queries "events" only, never "aggregates")
    - 88-PATTERNS.md "NEW convex/analytics.test.ts" + "convex/aggregates.test.ts (extend)" sections
  </read_first>
  <behavior>
    - heatmap mapping: a bucket_start at a known UTC epoch maps to the expected {day: getDay(), hour: getHours()} cell (UTC-based, NOT timezone-shifted — Pitfall 4)
    - errorRateTrend missing-hour=0: the query returns all 24 hour slots even when some hours have NO error buckets; absent buckets render `errors: 0`, never absent/null (Pitfall 7)
    - cron-removal non-double-count: after the event-count branch is removed from computeHourly, running computeHourly over an hour that already has ingest-time "events" buckets inserts no new "events" rows and leaves existing values unchanged (D-02)
    - retention safety: purgeOldTelemetryEvents only ever deletes from "events"; the set of tables it deletes from contains no "aggregates" entry (D-12)
  </behavior>
  <action>
    Create convex/analytics.test.ts (pure-logic vitest, mirroring aggregates.test.ts). Add a describe "heatmap derivation" asserting a fixed UTC bucket_start epoch maps to the expected {day, hour} (use `new Date(bucket_start*1000).getDay()` / `.getHours()` to compute the expectation so the test is timezone-stable). Add a describe "errorRateTrend missing-hour" asserting 24 slots returned with `errors: 0` for empty hours. These reference the rewritten analytics.ts query logic landing in Plan 04 — write them so they RED cleanly until then (guard the query-import behind dynamic import; the pure mapping-math assertions can pass now). Then EXTEND convex/aggregates.test.ts (append a new `describe("Phase 88 — cron removal non-double-count invariant", ...)`) with: (a) a test that an existing-key set blocks a re-insert (cron-removal idempotency, per 88-PATTERNS.md), and (b) a test that the dataRetention purge target-table set contains no "aggregates". Do NOT modify existing aggregates.test.ts tests.
  </action>
  <verify>
    <automated>npx vitest run convex/analytics.test.ts convex/aggregates.test.ts 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - convex/analytics.test.ts exists; the heatmap-mapping and missing-hour pure-math assertions pass green
    - convex/aggregates.test.ts contains a new describe block matching `Phase 88` (grep `Phase 88` in convex/aggregates.test.ts returns ≥1)
    - The dataRetention test asserts the deleted-table set has zero "aggregates" entries (grep `aggregates` within the new block + a `toHaveLength(0)` style assertion)
    - All previously-green aggregates.test.ts tests still pass (no regression)
  </acceptance_criteria>
  <done>Read-path + retention invariant tests exist; pure-math + retention assertions green; query-derivation tests RED-pending Plan 04.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new in this plan) | This plan is test scaffolding + a pure-function extraction. No new ingress, no new write path, no external input crosses a boundary here. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-88-01 | Tampering | sankeyClassify extraction | mitigate | Extract logic VERBATIM (no string/branch change) so the read/write classification can never diverge — divergence would let a producer's events be silently misclassified into wrong sankey edges. Asserted by the classifier unit tests in this plan. |
| T-88-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase (RESEARCH Package Legitimacy Audit: zero new packages; only `convex`, already in manifest). No legitimacy checkpoint required. |
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0
- `npx vitest run convex/` runs; the classifier + pure-math + retention assertions are green; the write/read implementation-dependent assertions are present and RED (expected until Plans 02/04)
- No 3 consecutive tasks lack automated verify (all 3 tasks have `<automated>`)
</verification>

<success_criteria>
- Shared classifier module exists and is the SOLE source of categoryOf/outcomeOf (analytics.ts imports it; no local copy remains)
- 3 Nyquist test files exist and cover all 8 invariants from 88-VALIDATION.md
- Type check clean; classifier + pure-logic assertions green
</success_criteria>

<output>
Create `.planning/phases/88-analytics-rollup-table-durable-fix-for-convex-16-mib-read-li/88-01-SUMMARY.md` when done
</output>
