---
phase: 89-readable-themes-editorial-skin-toggle
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/index.css
autonomous: true
requirements: [TH-01, TH-02, TH-03, TH-04]
must_haves:
  truths:
    - "Readable Dark and Midnight Aubergine themes apply their full token sets via [data-theme] blocks"
    - "All four theme blocks declare --vault-node-color (default violet #8b5cf6)"
    - "Matrix Emerald and Electric Cyan are unchanged and still coexist"
    - "Aubergine renders paper-grain + ambient radial gradients; readable/aubergine suppress matrix-bg and the CRT scanline bar; reduced-motion suppresses the aubergine pseudo-elements"
    - "src/index.css chrome (scrollbar, glow-card::before, glitch/matrix gradients) reads var(--primary)/var(--glow-*) instead of hardcoded cyan/emerald"
    - "src/index.css defines .nav-active-shadow / .nav-hover-shadow / .avatar-glow utility classes driven by var(--primary)/var(--glow-*) so the DashboardLayout nav-active + avatar glows (Category E) resolve per-theme"
  artifacts:
    - path: "src/index.css"
      provides: "Readable + Aubergine token blocks, --vault-node-color on all 4 blocks, aubergine surface effects, effect suppression, reduced-motion, tokenized chrome, Category E nav/avatar glow utility classes"
      contains: "[data-theme=\"readable\"]"
  key_links:
    - from: "[data-theme=\"readable\"] / [data-theme=\"aubergine\"]"
      to: ".matrix-bg / .crt-scanline-bar"
      via: "display:none suppression rules"
      pattern: "crt-scanline-bar"
    - from: "[data-theme=\"aubergine\"] body::before / ::after"
      to: "prefers-reduced-motion: reduce"
      via: "opacity:0 under reduced-motion"
      pattern: "prefers-reduced-motion"
    - from: ".nav-active-shadow / .avatar-glow (src/index.css)"
      to: "src/layouts/DashboardLayout.tsx nav-active + avatar (Plan 05)"
      via: "var(--primary)/var(--glow-*) tokens consumed via class swap"
      pattern: "nav-active-shadow"
---

<objective>
Add the two new theme token blocks (`[data-theme="readable"]`, `[data-theme="aubergine"]`) to `src/index.css`, add the `--vault-node-color` token to all FOUR theme blocks (per locked decision), add the aubergine editorial surface effects, suppress matrix-grid + CRT scanline bar under the two new themes, tokenize the in-CSS chrome (Category F) that currently hardcodes cyan/emerald, AND define the Category E nav-active / avatar glow utility classes that Plan 05 swaps onto DashboardLayout. This is the single source of `src/index.css` changes for the phase.

Purpose: A theme = one token set (TH-01). Readable Dark delivers WCAG-AA contrast with glow disabled (TH-02); Midnight Aubergine delivers the editorial skin with paper-grain + ambient gradients (TH-03); Matrix Emerald + Electric Cyan are untouched and coexist (TH-04). The Category E nav-active indicator and avatar glow currently hardcode emerald `rgba(16,185,129,...)` literals in DashboardLayout — extracting them into `var(--primary)`-driven utility classes here lets the most-visible chrome resolve to the correct per-theme color (and disappear in readable, which sets `--glow-*: none`).
Output: `src/index.css` with two new full token blocks, `--vault-node-color` on all four blocks, aubergine `body::before`/`body::after`, effect-suppression + reduced-motion rules, tokenized scrollbar/glow-card/glitch/matrix-bg chrome, and the `.nav-active-shadow`/`.nav-hover-shadow`/`.avatar-glow` utility classes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/89-readable-themes-editorial-skin-toggle/89-UI-SPEC.md
@.planning/phases/89-readable-themes-editorial-skin-toggle/89-RESEARCH.md
@.planning/phases/89-readable-themes-editorial-skin-toggle/89-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Readable Dark + Aubergine token blocks; add --vault-node-color to all four themes</name>
  <files>src/index.css</files>
  <read_first>
    src/index.css (read the full existing token region: .dark/[data-theme="cyan"] block ~127-182, [data-theme="emerald"] ~184-200, [data-theme="amber"] ~202-218 — append new blocks AFTER amber to preserve source order)
    .planning/phases/89-readable-themes-editorial-skin-toggle/89-UI-SPEC.md (§Per-Theme Token Values — Readable Dark and Midnight Aubergine tables; copy values EXACTLY)
    .planning/phases/89-readable-themes-editorial-skin-toggle/89-PATTERNS.md (§src/index.css self-analog emerald block; §Correct append order; source-order rule)
    .planning/phases/89-readable-themes-editorial-skin-toggle/89-RESEARCH.md (§Cascade Mechanics; Pitfall 5 source-order)
  </read_first>
  <action>
    Append `[data-theme="readable"]` then `[data-theme="aubergine"]` blocks AFTER the existing `[data-theme="amber"]` block — never reorder or move the cyan/emerald/amber blocks (Pitfall 5 / PATTERNS source-order rule). Both new blocks define the FULL token set (not partial overrides) using the EXACT hex values from UI-SPEC §Per-Theme Token Values. Readable spot-checks: `--background:#111318`, `--foreground:#e8eaf0`, `--card:#181c24`, `--primary:#5eead4`, `--accent:#3b82f6`, `--muted-foreground:#8892a4`, and `--glow-xs/sm/md/lg: none`. Aubergine spot-checks: `--background:#120d18`, `--foreground:#f0e8dc`, `--primary:#c084fc`, `--accent:#10b981`, `--muted-foreground:#9b8a74`, `--glow-sm:0 0 15px rgba(192,132,252,0.20)`. Include sidebar tokens, chart-1..5, chart-bar/-accent, chart-p50/p95/p99, status/metric tokens, glass tokens, speaking-ring tokens per the UI-SPEC tables. Add `--sidebar-active-bar: var(--primary)` in both. SEPARATELY, add a `--vault-node-color` token to ALL FOUR exposed theme blocks (cyan/.dark, emerald, readable, aubergine) AND the amber block, defaulting to `#8b5cf6` (violet) in every block per locked decision: vault nodes keep a consistent violet identity but become theme-declarable. Do NOT make vault nodes track `--accent`. Do NOT touch the `@theme inline` block (lines 13-56) — new themes only override raw custom properties.
  </action>
  <verify>
    <automated>node -e "const c=require('fs').readFileSync('src/index.css','utf8'); const blocks={cyan:/(?:\.dark|\[data-theme=\"cyan\"\])\s*,?\s*(?:\.dark|\[data-theme=\"cyan\"\])?\s*\{[\s\S]*?\n\}/, emerald:/\[data-theme=\"emerald\"\]\s*\{[\s\S]*?\n\}/, amber:/\[data-theme=\"amber\"\]\s*\{[\s\S]*?\n\}/, readable:/\[data-theme=\"readable\"\]\s*\{[\s\S]*?\n\}/, aubergine:/\[data-theme=\"aubergine\"\]\s*\{[\s\S]*?\n\}/}; let fail=0; for(const [name,re] of Object.entries(blocks)){const m=c.match(re); if(!m){console.error('MISSING block:',name); fail=1; continue;} if(!/--vault-node-color/.test(m[0])){console.error('block',name,'missing --vault-node-color'); fail=1;}} process.exit(fail);"</automated>
  </verify>
  <acceptance_criteria>
    - src/index.css contains `[data-theme="readable"]` AND `[data-theme="aubergine"]`, both appearing AFTER `[data-theme="amber"]` in source order
    - readable block contains `--background: #111318`, `--foreground: #e8eaf0`, `--primary: #5eead4`, and `--glow-xs: none` (and sm/md/lg: none)
    - aubergine block contains `--background: #120d18`, `--foreground: #f0e8dc`, `--primary: #c084fc`, `--accent: #10b981`
    - `--vault-node-color` appears INSIDE EACH of the five theme blocks individually — cyan/.dark, emerald, amber, readable, AND aubergine — each defaulting to `#8b5cf6`. The verify node script matches each `[data-theme="..."]`/`.dark` block region and asserts `--vault-node-color` is present within it (a bare >=5 total count is insufficient — distribution across all 5 blocks is required)
    - The `@theme inline` block is unchanged
    - emerald and cyan token values are byte-unchanged from before
    - `npm run build` exits 0 (CSS compiles)
  </acceptance_criteria>
  <done>Both new full token blocks present after amber; --vault-node-color present inside each of the five theme blocks (per-block verified, not just a total count); existing themes untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Aubergine surface effects + effect suppression + reduced-motion</name>
  <files>src/index.css</files>
  <read_first>
    src/index.css (read the existing reduced-motion block ~441-447 — append a NEW @media block after it, do not edit the existing one; read .matrix-bg rule ~417)
    .planning/phases/89-readable-themes-editorial-skin-toggle/89-RESEARCH.md (§Aubergine Surface Effects — body::before/::after pattern + SVG turbulence data URI; §CRT/Scanline Suppression)
    .planning/phases/89-readable-themes-editorial-skin-toggle/89-UI-SPEC.md (§Midnight Aubergine editorial surface effects; §Animation Contract)
    .planning/phases/89-readable-themes-editorial-skin-toggle/89-PATTERNS.md (§Effect suppression pattern; §Reduced-motion block)
  </read_first>
  <action>
    Add `[data-theme="aubergine"] body::before` (paper-grain): `content:''; position:fixed; inset:0; pointer-events:none; z-index:-1; opacity:0.025;` with a `background-image` inline SVG `feTurbulence` data URI (the ~230-byte fractalNoise pattern from RESEARCH §Aubergine Surface Effects). Add `[data-theme="aubergine"] body::after` (ambient gradient): two soft radials — `rgba(192,132,252,0.06)` at top-left, `rgba(16,185,129,0.04)` at bottom-right — `position:fixed; inset:0; pointer-events:none; z-index:-1`. Use `z-index:-1` to match the existing `.matrix-bg` stacking pattern. Add suppression rules: `[data-theme="readable"] .matrix-bg, [data-theme="aubergine"] .matrix-bg { display:none; }` and `[data-theme="readable"] .crt-scanline-bar, [data-theme="aubergine"] .crt-scanline-bar { display:none; }` (the `.crt-scanline-bar` class is added to the bar div in Plan 05). Append a NEW `@media (prefers-reduced-motion: reduce)` block (do not modify the existing one at ~441) that sets `[data-theme="aubergine"] body::before, [data-theme="aubergine"] body::after { opacity:0; }`. Per locked decision: do NOT implement a new scanline animation; do NOT touch `animate-scanline`/`.crt-overlay` here (those dead references are removed in Plan 05).
  </action>
  <verify>
    <automated>grep -q 'data-theme="aubergine"\] body::before' src/index.css && grep -q 'data-theme="aubergine"\] body::after' src/index.css && grep -q 'crt-scanline-bar' src/index.css && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - src/index.css contains `[data-theme="aubergine"] body::before` with `feTurbulence` data URI and `opacity:0.025`
    - src/index.css contains `[data-theme="aubergine"] body::after` with two radial-gradient layers (plum + emerald rgba)
    - both pseudo-elements use `z-index:-1` and `pointer-events:none`
    - src/index.css contains `display:none` suppression for `.matrix-bg` AND `.crt-scanline-bar` under BOTH `[data-theme="readable"]` and `[data-theme="aubergine"]`
    - a NEW `@media (prefers-reduced-motion: reduce)` block sets aubergine `body::before`/`body::after` opacity:0; the original reduced-motion block is unchanged
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>Aubergine paper-grain + gradients render scoped to aubergine; matrix-bg + scanline bar hidden under both new themes; reduced-motion suppresses the aubergine pseudo-elements.</done>
</task>

<task type="auto">
  <name>Task 3: Tokenize in-CSS chrome (Category F)</name>
  <files>src/index.css</files>
  <read_first>
    src/index.css (read the Category F sites: glow-card::before ~258; scrollbar-track/thumb/thumb:hover ~351-359; glitch-text text-shadow ~382; .matrix-bg radial-gradient ~417)
    .planning/phases/89-readable-themes-editorial-skin-toggle/89-RESEARCH.md (§Hardcoded Color Inventory Category F — exact line list and replacement guidance)
    .planning/phases/89-readable-themes-editorial-skin-toggle/89-UI-SPEC.md (§Hardcoded Color Migration)
  </read_first>
  <action>
    Migrate the Category F in-CSS hardcoded `#06b6d4`/`rgba(6,182,212,...)` chrome to token-driven values so they follow the active theme. Targets (per RESEARCH §Category F): `glow-card::before` radial-gradient → use `var(--primary)` (via CSS relative-color `oklch(from var(--primary) l c h / 0.3)` or restructure to `var(--glow-sm)`); scrollbar-track border, scrollbar-thumb, scrollbar-thumb:hover (incl. its box-shadow) → `var(--primary)` with appropriate alpha via `oklch(from var(--primary) l c h / <a>)`; `glitch-text` text-shadow → `var(--primary)`; `.matrix-bg` radial-gradient → `var(--primary)` alpha. These are CSS-to-CSS replacements only — do NOT change the layout/size/structure of these rules. The readable theme sets `--glow-*: none`, so any glow-card shadow that routes through a glow token correctly disappears in readable. Do NOT migrate provider/skills identity color maps (those live in component/lib files and are EXEMPT — handled outside this plan). After migration, confirm no `#06b6d4` or `rgba(6,182,212` literal remains in the scrollbar/glow-card/glitch/matrix-bg rules (token-definition blocks themselves legitimately keep hex and are out of scope here).
  </action>
  <verify>
    <automated>npm run build && grep -n 'rgba(6, *182, *212' src/index.css | grep -viE 'data-theme|--[a-z-]+:' || true</automated>
  </verify>
  <acceptance_criteria>
    - glow-card::before, scrollbar (track/thumb/hover), glitch-text, and .matrix-bg rules reference `var(--primary)` / `var(--glow-*)` / `oklch(from var(--primary)...)` instead of literal `#06b6d4`/`rgba(6,182,212,...)`
    - the token DEFINITION blocks (`--primary: #06b6d4` etc.) still keep their hex (unchanged)
    - no hardcoded cyan literal remains inside the chrome RULE bodies (scrollbar/glow-card/glitch/matrix-bg)
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>Scrollbar, glow-card, glitch-text, and matrix-bg chrome resolve from theme tokens; readable theme yields no glow on these.</done>
</task>

<task type="auto">
  <name>Task 4: Add Category E nav-active / avatar glow utility classes (consumed by Plan 05)</name>
  <files>src/index.css</files>
  <read_first>
    src/index.css (read the existing glow tokens ~117-120: --glow-xs/sm/md/lg; read a representative utility rule region to match formatting/source-order conventions; the nav-active inset shadows must be expressed against var(--primary))
    src/layouts/DashboardLayout.tsx (the three Category E sites that Plan 05 will swap to these classes — :313 nav active inset shadow, :314 nav hover inset shadow, :389 avatar glow shadow — read them to mirror the exact offsets/blur/spread so the visual is byte-faithful, only the COLOR moves to a token)
    .planning/phases/89-readable-themes-editorial-skin-toggle/89-RESEARCH.md (§Category E — the four hardcoded inline shadows; recommendation to extract into a .nav-active-shadow CSS utility driven by var(--primary)/var(--glow-*))
    .planning/phases/89-readable-themes-editorial-skin-toggle/89-UI-SPEC.md (§Hardcoded Color Migration — Category E nav/avatar)
  </read_first>
  <action>
    Add three utility classes to src/index.css (place them near the other glow/effect utilities, after the Category F chrome rules so source order stays logical). `.nav-active-shadow` reproduces DashboardLayout.tsx:313 — the inset emerald active-state indicator bar — keeping the EXACT offsets/blur/spread (`inset 2px 0 15px <c1>, inset 3px 0 0 <c2>`) but expressing the color via `var(--primary)`: use CSS relative-color `oklch(from var(--primary) l c h / 0.15)` for the soft inner glow and `var(--primary)` (full alpha) for the 3px indicator edge. `.nav-hover-shadow` reproduces DashboardLayout.tsx:314 — the hover inset shadow (`inset 2px 0 10px oklch(from var(--primary) l c h / 0.1), inset 3px 0 0 oklch(from var(--primary) l c h / 0.5)`). `.avatar-glow` reproduces DashboardLayout.tsx:389 — the avatar outer glow (`0 0 10px oklch(from var(--primary) l c h / 0.3)`); a hover variant may route through `var(--glow-md)` or `:hover` raising alpha to ~0.6 to mirror the existing `hover:shadow-[0_0_20px_rgba(16,185,129,0.6)]`. Drive every color exclusively from `var(--primary)` / `var(--glow-*)` — NO `rgba(16,185,129,...)` literals. Because readable sets `--glow-*: none` and `--primary` to teal #5eead4, these classes will resolve to the correct per-theme color (and to none where a glow token is used). These class definitions are CONSUMED by Plan 05 (Wave 2, depends_on 89-02), which swaps the three arbitrary `shadow-[...]` usages at DashboardLayout.tsx:313/314/389 onto these classes — do NOT edit DashboardLayout.tsx in this plan (89-02 owns only src/index.css; the swap is Plan 05's exclusive write to that file, no same-wave conflict). Do NOT alter the icon/label `drop-shadow-[...]` literals at :319/:322 — those are out of Category E scope per RESEARCH and left for a later sweep.
  </action>
  <verify>
    <automated>node -e "const c=require('fs').readFileSync('src/index.css','utf8'); let f=0; for(const cls of ['.nav-active-shadow','.nav-hover-shadow','.avatar-glow']){const re=new RegExp('\\\\'+cls.replace('.','\\\\.')+'\\\\s*\\\\{[\\\\s\\\\S]*?\\\\}'); const m=c.match(re); if(!m){console.error('MISSING class',cls); f=1; continue;} if(!/var\\(--(primary|glow-[a-z]+)\\)/.test(m[0])){console.error(cls,'does not reference var(--primary)/var(--glow-*)'); f=1;} if(/rgba\\(\\s*16,\\s*185,\\s*129/.test(m[0])){console.error(cls,'still has emerald literal'); f=1;}} process.exit(f);" && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - src/index.css defines `.nav-active-shadow`, `.nav-hover-shadow`, and `.avatar-glow`
    - each class drives its color from `var(--primary)` (via relative-color syntax) and/or `var(--glow-*)`; NONE contains an `rgba(16,185,129,...)` emerald literal
    - `.nav-active-shadow` preserves the original inset offsets/blur of DashboardLayout.tsx:313 (`inset 2px 0 15px` + `inset 3px 0 0`); only the color moved to a token
    - the classes are placed after the Category F chrome rules in source order; no other rules reordered
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>Theme-driven `.nav-active-shadow`/`.nav-hover-shadow`/`.avatar-glow` utilities exist in src/index.css, color sourced from var(--primary)/var(--glow-*), ready for Plan 05 to swap onto DashboardLayout nav-active + avatar.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CSS custom properties → rendered surface | Theme token values flow into the browser paint; no executable code, no user input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-89-03 | Tampering | inline SVG data URI in body::before | accept | Static author-controlled SVG turbulence filter; no external fetch, no script, no user input — pure decorative texture |
| T-89-04 | Denial of Service | full-viewport fixed pseudo-elements | mitigate | `pointer-events:none` + `z-index:-1` so they never intercept input or cover content; suppressed under reduced-motion |
</threat_model>

<verification>
- `npm run build` compiles CSS with both new theme blocks
- `grep`/node confirms `[data-theme="readable"]`, `[data-theme="aubergine"]`, `--vault-node-color` present INSIDE each of the 5 theme blocks (per-block), aubergine `body::before`/`::after`, `.crt-scanline-bar` suppression, reduced-motion aubergine block, and the three Category E utility classes (`.nav-active-shadow`/`.nav-hover-shadow`/`.avatar-glow`)
- Manual (documented in 89-VALIDATION §Manual-Only): aubergine paper-grain subtlety + warm gradients read correctly on hard refresh
</verification>

<success_criteria>
- Readable Dark + Midnight Aubergine token blocks present with exact UI-SPEC values, after amber in source order
- `--vault-node-color` inside every theme block (violet default), per-block verified
- Aubergine surface effects + effect suppression + reduced-motion rules in place
- Category F in-CSS chrome tokenized; cyan/emerald token blocks unchanged
- Category E nav-active / avatar glow utility classes defined and token-driven, ready for Plan 05 consumption
</success_criteria>

<output>
Create `.planning/phases/89-readable-themes-editorial-skin-toggle/89-02-SUMMARY.md` when done
</output>
