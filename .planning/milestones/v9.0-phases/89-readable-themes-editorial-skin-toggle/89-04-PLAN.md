---
phase: 89-readable-themes-editorial-skin-toggle
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/hr/CatalogCard.tsx
  - src/components/hr/TeamEditor.tsx
  - src/components/hr/TeamCard.tsx
  - src/components/hr/WizardShell.tsx
  - src/components/hr/detail/DetailConfigTab.tsx
  - src/components/hr/AgentDetailSheet.tsx
  - src/components/hr/AgentCard.tsx
  - src/components/skills/NewSkillsBanner.tsx
  - src/components/skills/CategoryGrid.tsx
  - src/pages/Alerts.tsx
  - src/pages/Dashboard.tsx
  - src/pages/Skills.tsx
  - src/pages/hr/AgentAnalytics.tsx
  - src/pages/hr/Roster.tsx
  - src/pages/hr/Teams.tsx
  - src/pages/hr/Catalog.tsx
autonomous: true
requirements: [TH-01]
must_haves:
  truths:
    - "HR + skills components and the 7 listed pages use var(--glow-*)/var(--primary) for glow/shadow chrome"
    - "No hardcoded cyan/emerald glow/shadow literal remains in these files"
    - "Skills category swatch maps are left untouched (EXEMPT identity colors)"
  artifacts:
    - path: "src/components/hr/AgentDetailSheet.tsx"
      provides: "Glow/shadow migrated to glow tokens (9 sites)"
      contains: "var(--glow"
  key_links:
    - from: "hr/skills/page component shadow utilities"
      to: "--glow-* tokens"
      via: "shadow-[var(--glow-*)] Tailwind arbitrary values"
      pattern: "var\\(--glow"
---

<objective>
Continue the Category A glow/shadow migration (TH-01) across the HR components, skills components, and the 7 pages that hold hardcoded glow/shadow. Same mechanical swap as Plan 03 — distinct file set, no overlap.

Purpose: Finish TH-01 component-side migration so no surface keeps hardcoded chrome glow. Skills category swatch maps stay (EXEMPT identity colors).
Output: 16 files (9 components + 7 pages) with glow/shadow reading from theme tokens.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/89-readable-themes-editorial-skin-toggle/89-UI-SPEC.md
@.planning/phases/89-readable-themes-editorial-skin-toggle/89-RESEARCH.md
@.planning/phases/89-readable-themes-editorial-skin-toggle/89-PATTERNS.md

<migration_rules>
<!-- Identical swap contract to Plan 03. Source: RESEARCH §Category A. -->

  shadow-[0_0_8px_rgba(...0.05..0.1)]   becomes shadow-[var(--glow-xs)]
  shadow-[0_0_15px_rgba(...)]           becomes shadow-[var(--glow-sm)]
  shadow-[0_0_20-25px_rgba(...)]        becomes shadow-[var(--glow-md)]
  shadow-[0_0_40px_rgba(...)]           becomes shadow-[var(--glow-lg)]
  hover:shadow-[...]                     becomes hover:shadow-[var(--glow-sm|md)]
  drop-shadow-[0_0_8px_rgba(...0.8)]    becomes drop-shadow-[var(--glow-xs)]

Brand-accent cyan/emerald text/border/bg literals become text-primary / border-primary /
bg-primary (or bg-[var(--primary)]).

EXEMPT — DO NOT MIGRATE (locked decision):
  - Skills category swatch maps in skills/* files (CategoryCard, CategoryEditPopover,
    FavoriteSkills, SkillsInCategory and any COLOR_HEX/category-color Record) — data-driven
    identity, not chrome. NewSkillsBanner / CategoryGrid only migrate their glow/shadow CHROME,
    NOT any category swatch hex they render.
  - Any data-driven series/identity color.
</migration_rules>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate glow/shadow in HR components (7 files)</name>
  <files>src/components/hr/CatalogCard.tsx, src/components/hr/TeamEditor.tsx, src/components/hr/TeamCard.tsx, src/components/hr/WizardShell.tsx, src/components/hr/detail/DetailConfigTab.tsx, src/components/hr/AgentDetailSheet.tsx, src/components/hr/AgentCard.tsx</files>
  <read_first>
    Each of the 7 HR component files (read once; extract all shadow/drop-shadow arbitrary values and cyan/emerald chrome literals in a single pass — AgentDetailSheet has ~9 sites)
    .planning/phases/89-readable-themes-editorial-skin-toggle/89-RESEARCH.md (§Category A)
    The `<migration_rules>` block above
  </read_first>
  <action>
    Replace every hardcoded Tailwind arbitrary glow/shadow (`shadow-[0_0_*px_rgba(6,182,212|16,185,129,...)]`, `hover:shadow-[...]`, `drop-shadow-[...]`) with the nearest `var(--glow-*)` token per the mapping. Replace brand-accent cyan/emerald text/border/bg with `text-primary`/`border-primary`/`bg-primary`. Leave data-driven/identity hex untouched. Structure/props/layout unchanged.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - None of the 7 HR files contains a `shadow-[...rgba(6,182,212...` or `shadow-[...rgba(16,185,129...` glow literal
    - Migrated glow utilities reference `var(--glow-xs|sm|md|lg)`
    - `npx tsc --noEmit` reports no new errors
  </acceptance_criteria>
  <done>HR component glow/shadow migrated to tokens.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate glow/shadow in skills components + 7 pages (9 files)</name>
  <files>src/components/skills/NewSkillsBanner.tsx, src/components/skills/CategoryGrid.tsx, src/pages/Alerts.tsx, src/pages/Dashboard.tsx, src/pages/Skills.tsx, src/pages/hr/AgentAnalytics.tsx, src/pages/hr/Roster.tsx, src/pages/hr/Teams.tsx, src/pages/hr/Catalog.tsx</files>
  <read_first>
    Each of the 9 files (read once; extract shadow/drop-shadow arbitrary values and cyan/emerald chrome literals in a single pass)
    .planning/phases/89-readable-themes-editorial-skin-toggle/89-RESEARCH.md (§Category A; §Category C skills swatch EXEMPT)
    The `<migration_rules>` block above
  </read_first>
  <action>
    Same migration across these 9 files. CRITICAL for skills files: migrate ONLY the glow/shadow CHROME utilities — do NOT touch any category swatch color map / COLOR_HEX record (EXEMPT identity colors). For the 7 pages, swap glow/shadow arbitrary values to `var(--glow-*)` and brand-accent hex to `*-primary`. Leave data-driven/identity hex untouched.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - None of the 9 files contains a `shadow-[...rgba(6,182,212...` or `shadow-[...rgba(16,185,129...` glow literal
    - Skills category swatch / COLOR_HEX maps are byte-unchanged (EXEMPT)
    - Migrated glow utilities reference `var(--glow-xs|sm|md|lg)`
    - `npx tsc --noEmit` reports no new errors
  </acceptance_criteria>
  <done>Skills chrome + 7 pages migrated; skills swatch maps preserved.</done>
</task>

<task type="auto">
  <name>Task 3: Build + leftover-scan gate for this plan's files</name>
  <files>src/components/hr/AgentDetailSheet.tsx, src/pages/Dashboard.tsx</files>
  <read_first>
    The `<migration_rules>` block above (legitimate remaining hex vs chrome leftover)
  </read_first>
  <action>
    Run a production build to confirm Tailwind JIT generates the migrated glow-token utilities with no CSS compile error (Pitfall 4). Scan all 16 files in this plan for any remaining cyan/emerald glow/shadow literal; any hit that is NOT a documented data-driven identity / skills swatch color is a leftover to fix. No new files.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits 0
    - A grep across the 16 files finds zero `shadow-[...rgba((6,182,212|16,185,129)...]` glow leftovers
    - Any remaining cyan/emerald hex is a documented skills swatch / data-driven identity color, noted in the SUMMARY
  </acceptance_criteria>
  <done>Build green; no chrome-glow leftovers across the 16 files.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Component/page className strings → rendered DOM | Only Tailwind utility strings change; no data, props, or executable paths altered |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-89-07 | Tampering | className token swaps | accept | Purely presentational; no input handling or data flow touched |
| T-89-08 | Denial of Service | Tailwind JIT on `var(--glow-*): none` | mitigate | Task 3 build gate confirms `box-shadow: none` resolves (Pitfall 4) before merge |
</threat_model>

<verification>
- `npx tsc --noEmit` clean
- `npm run build` exits 0
- grep finds no cyan/emerald glow/shadow literals across the 16 files (except documented identity/swatch colors)
</verification>

<success_criteria>
- All 16 HR/skills/page files use `var(--glow-*)`/`var(--primary)` for glow/shadow chrome
- Skills category swatch maps untouched
- Build green, no leftovers
</success_criteria>

<output>
Create `.planning/phases/89-readable-themes-editorial-skin-toggle/89-04-SUMMARY.md` when done
</output>
