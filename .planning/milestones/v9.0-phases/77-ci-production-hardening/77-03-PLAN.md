---
phase: 77-ci-production-hardening
plan: 03
type: execute
wave: 2
depends_on: ["77-01", "77-02"]
files_modified:
  - docs/DEPLOY.md
  - .planning/REQUIREMENTS.md
autonomous: false
requirements: [OPS-01, OPS-02, OPS-03]
tags: [ops, deploy, cors, docs]

user_setup:
  - service: convex
    why: "Set the production CORS allowlist env var on the Convex cloud deployment"
    env_vars:
      - name: CODEPULSE_ALLOWED_ORIGIN
        source: "Set via `npx convex env set CODEPULSE_ALLOWED_ORIGIN '<frontend-origin>,http://localhost:5173'` (or Convex Dashboard -> Settings -> Environment Variables)"
    dashboard_config:
      - task: "Set CODEPULSE_ALLOWED_ORIGIN to the deployed frontend origin (Vercel/Netlify URL) plus localhost dev origin, comma-separated, no spaces"
        location: "Convex Dashboard -> prod deployment -> Settings -> Environment Variables (or `npx convex env set --prod`)"

must_haves:
  truths:
    - "CODEPULSE_ALLOWED_ORIGIN is set on the Convex prod deployment (verifiable via npx convex env list)"
    - "A deploy checklist documents the env var value, where to set it, and the procedure so a non-local origin can be configured without guessing"
    - "OPS-03 is marked N/A in REQUIREMENTS.md with a pointer to the Astridr supabase-migration-check.yml workflow"
    - "The Gitleaks workflow has been manually verified to exit 1 (block) on an introduced test secret, then reverted"
  artifacts:
    - path: "docs/DEPLOY.md"
      provides: "Production deploy checklist: CORS env var value/procedure + which control does what"
      contains: "CODEPULSE_ALLOWED_ORIGIN"
    - path: ".planning/REQUIREMENTS.md"
      provides: "OPS-03 traceability marked N/A with upstream pointer"
      contains: "N/A"
  key_links:
    - from: "docs/DEPLOY.md"
      to: "convex/ingestAuth.ts getCorsHeaders allowlist"
      via: "documents the env var the allowlist parses"
      pattern: "CODEPULSE_ALLOWED_ORIGIN"
    - from: ".planning/REQUIREMENTS.md OPS-03 row"
      to: "astridr-repo supabase-migration-check.yml"
      via: "N/A annotation with upstream pointer"
      pattern: "supabase-migration-check"
---

<objective>
Land the operational/documentation deliverables that finish Phase 77 (wave 2, after the CORS code and Gitleaks workflow land): set `CODEPULSE_ALLOWED_ORIGIN` on the Convex prod deployment and write a deploy checklist (OPS-01, D-08); manually validate that the Gitleaks workflow blocks on a real secret (OPS-02, D-02 final proof); and mark OPS-03 N/A in REQUIREMENTS.md with a pointer to the Astridr workflow that already satisfies it upstream (OPS-03, D-01).

Purpose: Plan 01 makes CORS fail-closed in code, but production is only correct when the env var is actually set — and a developer can only configure a non-local origin without guessing if the procedure is documented (OPS-01 success criterion #4). The manual secret-test is the only way to prove the Gitleaks enforce path actually blocks (exit 1), not merely classifies. OPS-03 needs a one-line traceability edit so coverage is honest about where the control lives (Astridr, not CodePulse).

Output: `docs/DEPLOY.md`, `CODEPULSE_ALLOWED_ORIGIN` set in Convex prod, the Gitleaks block verified, and OPS-03 marked N/A in REQUIREMENTS.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/77-ci-production-hardening/77-CONTEXT.md
@.planning/phases/77-ci-production-hardening/77-RESEARCH.md
@.planning/phases/77-ci-production-hardening/77-PATTERNS.md
@.planning/phases/77-ci-production-hardening/77-VALIDATION.md

<interfaces>
<!-- Inputs this plan documents/validates. Plans 01 and 02 produce these. -->
From Plan 01 (convex/ingestAuth.ts): getCorsHeaders(request) parses CODEPULSE_ALLOWED_ORIGIN via parseAllowlist into a module-init Set; comma-separated, no spaces around commas. Dev fallback "*" applies only when the var is UNSET — so prod MUST set it.
From Plan 02 (.github/workflows/gitleaks-scan.yml): secret_found -> exit 1; scan_error -> ::warning + exit 0; clean -> exit 0. The manual test must produce secret_found (red, exit 1), NOT scan_error (neutral).

Convex env CLI (RESEARCH, VERIFIED):
  npx convex env set CODEPULSE_ALLOWED_ORIGIN 'https://<frontend>.vercel.app,http://localhost:5173'
  npx convex env list   # verify
  npx convex env set --prod CODEPULSE_ALLOWED_ORIGIN '<value>'   # explicit prod (A2: assumed flag; dashboard UI is the fallback)

Deploy = npm run deploy = `npx convex deploy && npx vite build` (CONTEXT code_context).

REQUIREMENTS.md OPS-03 current text (line 72): "- [ ] **OPS-03**: The Supabase migration-drift CI check passes (green) on `master`"
Traceability row (line 133): "| OPS-03 | Phase 77 | Pending |"
Upstream pointer: C:\Users\mandr\astridr-repo\.github\workflows\supabase-migration-check.yml (CodePulse has no supabase/ directory — nothing to drift-check).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write docs/DEPLOY.md and mark OPS-03 N/A in REQUIREMENTS.md</name>
  <files>docs/DEPLOY.md, .planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (the OPS-03 line ~72 + traceability row ~133 to edit)
    - 77-CONTEXT.md D-01 (OPS-03 N/A rationale + upstream pointer) and D-08 (deploy checklist scope)
    - 77-RESEARCH.md "Code Examples — Convex env var CLI commands" (lines 435-452) and "Security Domain" (lines 536-557, which control does what)
    - 77-PATTERNS.md "No Analog Found" (lines 474-484) — DEPLOY.md authored from scratch; REQUIREMENTS.md single-line edit
  </read_first>
  <action>
    Create `docs/DEPLOY.md` (Claude's discretion on exact format per D-08) as a CodePulse production deploy checklist. It MUST document, for `CODEPULSE_ALLOWED_ORIGIN` (OPS-01): (a) WHAT to set it to — a fill-in instruction "the deployed frontend origin (your Vercel/Netlify URL) plus the localhost dev origin, comma-separated with NO spaces around commas" (do NOT hardcode a specific origin — the frontend host is an open question per RESEARCH); (b) WHERE — Convex prod deployment env vars; (c) HOW — the `npx convex env set CODEPULSE_ALLOWED_ORIGIN '<value>'` command (and `npx convex env list` to verify), noting the Convex Dashboard env-vars UI as the fallback; (d) WHY fail-closed — when the var is UNSET the dev fallback is permissive `"*"`, so prod MUST set it. Also include a short "Which control does what" note (from RESEARCH Security Domain): CORS protects browser-initiated cross-origin requests; non-browser ingest (Astridr agents) is gated by `validateIngestAuth` (a separate, deferred control) — so a maintainer does not assume CORS gates server-to-server. Include the standard deploy command `npm run deploy` (= `npx convex deploy && npx vite build`). Then edit `.planning/REQUIREMENTS.md` per D-01: change the OPS-03 bullet to mark it N/A (satisfied upstream) with a one-line rationale (CodePulse has no `supabase/` schema to drift-check) and a pointer to `astridr-repo .github/workflows/supabase-migration-check.yml`; update the traceability table OPS-03 row Status from `Pending` to `N/A (upstream — astridr supabase-migration-check.yml)`.
  </action>
  <verify>
    <automated>if ((Select-String -Path docs/DEPLOY.md -Pattern 'CODEPULSE_ALLOWED_ORIGIN' -Quiet) -and (Select-String -Path .planning/REQUIREMENTS.md -Pattern 'N/A' -Quiet) -and (Select-String -Path .planning/REQUIREMENTS.md -Pattern 'supabase-migration-check' -Quiet)) { exit 0 } else { exit 1 }</automated>
  </verify>
  <acceptance_criteria>
    - `docs/DEPLOY.md` exists and contains `CODEPULSE_ALLOWED_ORIGIN`, the `npx convex env set` command, the `npx convex env list` verify step, the fail-closed/dev-fallback rationale, and the "which control does what" CORS-vs-validateIngestAuth note
    - `docs/DEPLOY.md` does NOT hardcode a specific production origin (uses a fill-in instruction)
    - `.planning/REQUIREMENTS.md` OPS-03 bullet is marked N/A with the Astridr `supabase-migration-check.yml` pointer
    - `.planning/REQUIREMENTS.md` traceability table OPS-03 row Status reads N/A (upstream), not Pending
  </acceptance_criteria>
  <done>Deploy checklist documents the CORS env var (value/where/how/why) + control responsibilities; OPS-03 marked N/A with upstream pointer in both the bullet and the traceability table.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Set CODEPULSE_ALLOWED_ORIGIN in Convex prod (human-run CLI)</name>
  <files>convex/ingestAuth.ts</files>
  <read_first>
    - docs/DEPLOY.md (from Task 1 — the procedure being executed)
    - 77-RESEARCH.md "Code Examples — Convex env var CLI commands" (lines 435-452)
    - 77-VALIDATION.md "Manual-Only Verifications" row "CODEPULSE_ALLOWED_ORIGIN set in Convex cloud"
  </read_first>
  <what-built>Plan 01 made CORS fail-closed in code; this gate sets the env var that drives the allowlist on the Convex prod deployment so production CORS is correct (OPS-01 success criterion #3). This requires the actual deployed frontend origin, which only Larry knows, and Convex prod credentials — Claude cannot supply either.</what-built>
  <action>Pause for Larry to set `CODEPULSE_ALLOWED_ORIGIN` on the Convex prod deployment via `npx convex env set`. Claude cannot supply the real frontend origin or prod credentials, so this is a human-action gate — do NOT attempt to set it autonomously; surface the procedure from docs/DEPLOY.md and wait.</action>
  <how-to-verify>
    1. Decide the production allowlist value: the deployed frontend origin (Vercel/Netlify URL) + the localhost dev origin, comma-separated, NO spaces — e.g. `https://codepulse.vercel.app,http://localhost:5173`.
    2. Run: `npx convex env set CODEPULSE_ALLOWED_ORIGIN '<that value>'` (use `--prod` or set `CONVEX_DEPLOY_KEY` to target the prod deployment; the Convex Dashboard env-vars UI is an equivalent fallback).
    3. Run `npx convex env list` and confirm `CODEPULSE_ALLOWED_ORIGIN` appears with the expected value.
    4. (Optional sanity) After the next `npm run deploy`, confirm a browser request from the allowed origin succeeds and an un-listed origin is blocked.
  </how-to-verify>
  <verify>Human runs `npx convex env list`; output shows `CODEPULSE_ALLOWED_ORIGIN` set with a comma-separated value (no spaces around commas) on the prod deployment.</verify>
  <acceptance_criteria>
    - `npx convex env list` shows `CODEPULSE_ALLOWED_ORIGIN` set on the prod deployment with a non-empty, comma-separated value (no spaces around commas)
    - The value includes the real deployed frontend origin (not a placeholder)
  </acceptance_criteria>
  <done>CODEPULSE_ALLOWED_ORIGIN is set on Convex prod with the real frontend origin + localhost, confirmed via `npx convex env list`.</done>
  <resume-signal>Type "set" and paste the `npx convex env list` line confirming CODEPULSE_ALLOWED_ORIGIN, or describe any blocker.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Manually verify Gitleaks blocks (exit 1) on a real secret</name>
  <files>.github/workflows/gitleaks-scan.yml</files>
  <read_first>
    - .github/workflows/gitleaks-scan.yml (from Plan 02 — the enforce step under test)
    - 77-VALIDATION.md "Manual-Only Verifications" row "secret_found -> exit 1 blocks the build"
    - 77-CONTEXT.md "Specific Ideas" (the secret_found vs scan_error distinction)
  </read_first>
  <what-built>Plan 02 authored the Gitleaks workflow with a 3-state classify+enforce. This gate proves the enforce path actually BLOCKS on a real finding (secret_found -> exit 1), and that an introduced secret is classified as a leak — NOT as a neutral scan_error. This is the only validation that exercises the fail-loud path end-to-end; it requires pushing a throwaway branch with a fake secret, which must not live on master.</what-built>
  <action>Pause for Larry to run the manual secret-block test: on a throwaway branch, introduce a fake realistic secret, push, confirm the Gitleaks check goes red with secret_found/exit 1 (not scan_error), then delete the branch. Claude must not commit a fake secret to any retained branch — this is a human-driven, revert-after validation.</action>
  <how-to-verify>
    1. On a throwaway branch (e.g. `test/gitleaks-block`), add a fake but realistic-looking secret that default gitleaks rules detect — e.g. a fake AWS key line `AKIA` + 16 uppercase/digits in a scratch file NOT covered by any `.gitleaks.toml` allowlist path.
    2. Push the branch / open a PR so `gitleaks-scan.yml` runs.
    3. Confirm the Gitleaks check goes RED and the run log shows `result=secret_found` and the enforce step `exit 1` (an `::error`), NOT a neutral `scan_error` `::warning`.
    4. Confirm the CodePulse dashboard notify shows conclusion `failure` (not `neutral`) for this run.
    5. Delete the throwaway branch and the scratch file — leave no fake secret in history.
  </how-to-verify>
  <verify>On a throwaway branch, an introduced fake secret causes the Gitleaks check to go red with `result=secret_found` and enforce `exit 1`; the branch is then deleted leaving no fake secret in any retained history.</verify>
  <acceptance_criteria>
    - The introduced fake secret produced a `secret_found` classification and the enforce step exited 1 (red check), not `scan_error`
    - The throwaway branch + scratch file are deleted; no fake secret remains in any retained branch
    - The clean `master` run of the workflow is green (post-baseline, from Plan 02)
  </acceptance_criteria>
  <done>Gitleaks enforce proven to block on a real secret (secret_found/exit 1, red), distinct from scan_error; throwaway branch reverted; clean master run green.</done>
  <resume-signal>Type "blocked-confirmed" (Gitleaks went red with secret_found/exit 1 and was reverted), or describe what happened.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → Convex prod env config | Setting CODEPULSE_ALLOWED_ORIGIN is the deployment-time control that makes the Plan 01 fail-closed CORS actually closed in prod |
| developer → deploy procedure | An undocumented CORS env var leads to either a permissive `"*"` (unset) or a guessed/wrong origin (broken CORS); the checklist removes the guess |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-77-08 | Elevation of Privilege | prod CORS left permissive because env var unset | mitigate | Deploy checklist (docs/DEPLOY.md) + Task 2 gate set CODEPULSE_ALLOWED_ORIGIN in Convex prod; dev fallback `"*"` documented as dev-only so the unset state is never shipped silently |
| T-77-09 | Repudiation / Tampering | Gitleaks enforce assumed-working but never proven to block | mitigate | Task 3 introduces a real-looking secret on a throwaway branch and confirms `secret_found` -> exit 1 (red), then reverts — proving the fail-loud path, distinct from scan_error |
| T-77-10 | Information Disclosure | OPS-03 silently dropped, hiding that drift-check lives upstream | accept | OPS-03 marked N/A in REQUIREMENTS.md with explicit pointer to Astridr `supabase-migration-check.yml`; traceability stays honest (no CodePulse supabase schema exists to drift-check) |
</threat_model>

<verification>
- `docs/DEPLOY.md` documents CODEPULSE_ALLOWED_ORIGIN value/where/how/why + the CORS-vs-validateIngestAuth control note; no hardcoded origin
- `.planning/REQUIREMENTS.md` marks OPS-03 N/A with the Astridr `supabase-migration-check.yml` pointer in both the bullet and the traceability table
- (gate) `npx convex env list` shows CODEPULSE_ALLOWED_ORIGIN set in prod
- (gate) Gitleaks workflow confirmed to exit 1 / red on an introduced test secret, then reverted
</verification>

<success_criteria>
- CODEPULSE_ALLOWED_ORIGIN is set on Convex prod (OPS-01 #3) and a developer can configure a non-local origin from the checklist without guessing (OPS-01 #4)
- The Gitleaks enforce path is proven to block on a real secret (OPS-02), classified as secret_found not scan_error
- OPS-03 is marked N/A with an upstream pointer (OPS-03 traceability honest)
</success_criteria>

<output>
Create `.planning/phases/77-ci-production-hardening/77-03-SUMMARY.md` when done
</output>
