---
phase: 82-files-preview-hardening
plan: 02
type: execute
wave: 2
depends_on: ["82-01"]
files_modified:
  - convex/forge.ts
  - convex/crons.ts
  - convex/forgeFileIngest.test.ts
  - docs/forge-deploy-checklist.md
autonomous: true
requirements: [FI-14]
must_haves:
  truths:
    - "File/artifact records past the 7-day TTL are deleted while in-window records survive"
    - "When the per-job artifact-byte cap is exceeded, oldest artifacts are deleted first and newest survive"
    - "Image artifact blobs are removed from Convex File Storage when their doc rows are pruned (no blob leak)"
    - "A deploy checklist documents CODEPULSE_ALLOWED_ORIGIN and every Forge ingest env var"
  artifacts:
    - path: "convex/forge.ts"
      provides: "sweepForgeFileRecords internalMutation deleting forgeFiles + forgeArtifacts (blob-before-row for images)"
      contains: "export const sweepForgeFileRecords"
    - path: "convex/crons.ts"
      provides: "sweep-forge-file-records daily cron at 04:00 UTC"
      contains: "sweep-forge-file-records"
    - path: "docs/forge-deploy-checklist.md"
      provides: "OPS-01 env-var deploy checklist"
      contains: "CODEPULSE_ALLOWED_ORIGIN"
  key_links:
    - from: "convex/crons.ts"
      to: "convex/forge.ts sweepForgeFileRecords"
      via: "crons.daily internal.forge.sweepForgeFileRecords"
      pattern: "sweepForgeFileRecords"
    - from: "sweepForgeFileRecords"
      to: "Convex File Storage"
      via: "ctx.storage.delete(storageId) before ctx.db.delete(_id)"
      pattern: "storage\\.delete"
---

<objective>
Bound the new file/artifact tables with a retention sweep mirroring Phase 81's `sweepForgeLogChunks` (7-day TTL + per-job byte cap, drop-oldest), extended so image blobs in Convex File Storage are deleted alongside their doc rows (D-05, CRITICAL — blob leak is permanent without GC). Add the daily cron offset from the Phase 81 sweep hour, and ship the OPS-01 deploy checklist documenting CODEPULSE_ALLOWED_ORIGIN + every Forge ingest env var.

Purpose: Closes the retention + OPS-01 portions of FI-14. The sweep reuses the pure helpers exported in 82-01; this plan wires them into the internalMutation + cron and adds the blob-delete ordering and the deploy doc.
Output: `sweepForgeFileRecords` in forge.ts, `sweep-forge-file-records` cron, additional sweep tests in forgeFileIngest.test.ts, and `docs/forge-deploy-checklist.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/82-files-preview-hardening/82-SPEC.md
@.planning/phases/82-files-preview-hardening/82-RESEARCH.md
@.planning/phases/82-files-preview-hardening/82-PATTERNS.md

<interfaces>
<!-- 82-01 exports the executor builds on. -->
From convex/forge.ts (added in 82-01, Phase 82 section):
- export const SEVEN_DAYS_MS  (defined Phase 81, reused)
- export const ARTIFACT_BYTE_CAP_PER_JOB = 10_000_000;
- export function artifactByteSize(a: { textContent?: string; sizeBytes: number }): number
- export function selectFileTtlDeletes<T extends { _id; createdAt: string }>(records, now): T[]
- export function selectFileCapDeletes<T extends { _id; createdAt: string; sizeBytes: number; textContent?: string }>(records, capBytes): T[]
- forgeFiles + forgeArtifacts tables (forgeArtifacts rows carry optional storageId: Id<"_storage">)

Phase 81 analog (convex/forge.ts L579-617 — verified live):
- export const sweepForgeLogChunks = internalMutation({ args:{}, handler: two-pass — TTL via selectTtlDeletes(allChunks, now), then per-job via selectCapDeletes(chunks, LOG_BYTE_CAP_PER_JOB) })

convex/crons.ts L111-116 (verified live):
  crons.daily("sweep-forge-log-chunks", { hourUTC: 3, minuteUTC: 30 }, internal.forge.sweepForgeLogChunks);
  (existing cron hours in use: 1:00, 2:00, 3:00, 3:30, 6:00, 6:05 — pick 4:00 to avoid contention)

docs/forge-deploy-checklist.md — NEW, no analog. Env vars to document (from 82-PATTERNS.md "No Analog Found", L889):
  CODEPULSE_ALLOWED_ORIGIN, FORGE_INGEST_API_KEY, FORGE_INGEST_ALLOW_ANON, CONVEX_FORGE_INGEST_URL, FORGE_LOG_INGEST_URL, FORGE_FILE_INGEST_URL.
</interfaces>

Retention rules to honor (from STATE.md Phase 81 notes):
- sweep is internalMutation (cron scheduler, no Clerk identity).
- Two-pass: TTL first, then per-job cap.
- selectFileCapDeletes iterates oldest-first; newest survive by construction.
- D-05 CRITICAL ordering: ctx.storage.delete(storageId) BEFORE ctx.db.delete(_id) for image artifacts.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: sweepForgeFileRecords internalMutation + sweep tests</name>
  <files>convex/forge.ts, convex/forgeFileIngest.test.ts</files>
  <read_first>
    - convex/forge.ts L579-617 (sweepForgeLogChunks — the two-pass TTL+cap internalMutation shape to clone)
    - convex/forge.ts L527-577 (SEVEN_DAYS_MS, helper definitions — the 82-01 Phase 82 additions live just below this block)
    - .planning/phases/82-files-preview-hardening/82-PATTERNS.md §"sweepForgeLogChunks retention sweep pattern" (L173-184) and §"Convex File Storage blob delete ordering (D-05 CRITICAL)" (L870-881)
    - .planning/phases/82-files-preview-hardening/82-RESEARCH.md §"Retention sweep (sweepForgeFileRecords)" (L390-415) and Pitfall 2 (L625-629)
    - convex/forgeFileIngest.test.ts (the sweep / blob-ordering test groups stubbed in 82-01 Task 1 — extend with concrete assertions if 82-01 left them ordering-only)
  </read_first>
  <action>
    Add `export const sweepForgeFileRecords = internalMutation({ args: {}, handler: async (ctx) => {...} })` to convex/forge.ts's Phase 82 section, cloning sweepForgeLogChunks's two-pass shape.

    Pass 1 (TTL): set now = Date.now(); collect forgeArtifacts via ctx.db.query("forgeArtifacts").collect(); compute artifactTtlDeletes = selectFileTtlDeletes(allArtifacts, now); for each, if artifact.storageId then await ctx.storage.delete(artifact.storageId) FIRST, then await ctx.db.delete(artifact._id) (blob-before-row, D-05). Then collect + selectFileTtlDeletes on forgeFiles and ctx.db.delete each (forgeFiles rows have no storageId).

    Pass 2 (per-job cap): group surviving forgeArtifacts by (hostId, forgeJobId); for each group, capDeletes = selectFileCapDeletes(groupArtifacts, ARTIFACT_BYTE_CAP_PER_JOB); delete with the same storage.delete-before-db.delete ordering. (Per-job file-COUNT bounding is enforced daemon-side via the file-count cap in 82-04; the byte cap is the artifact-storage bound here.)

    In convex/forgeFileIngest.test.ts, ensure the retention test group covers: (a) selectFileTtlDeletes deletes a record with createdAt 8 days old, keeps one 6 days old and one exactly at the boundary; (b) selectFileCapDeletes over the cap deletes oldest-first and the newest record survives; (c) the D-05 ordering group asserts storage.delete is invoked before db.delete for an artifact with storageId, and storage.delete is NOT invoked for a text artifact (no storageId) — use a vi.fn() call-order capture.
  </action>
  <verify>
    <automated>npx vitest run convex/forgeFileIngest.test.ts 2>&1 | tail -6</automated>
  </verify>
  <acceptance_criteria>
    - `convex/forge.ts` contains `export const sweepForgeFileRecords` as an `internalMutation`.
    - Inside sweepForgeFileRecords, every `ctx.db.delete` of a forgeArtifacts row is preceded (in source order, within the loop) by an `if (...storageId) await ctx.storage.delete(...)` guard.
    - `convex/forgeFileIngest.test.ts` asserts blob-delete-before-row-delete ordering for image artifacts and no-storage-delete for text artifacts.
    - `npx vitest run convex/forgeFileIngest.test.ts` exits 0.
  </acceptance_criteria>
  <done>sweepForgeFileRecords implemented with TTL + per-job cap passes and D-05 blob ordering; retention tests green.</done>
</task>

<task type="auto">
  <name>Task 2: sweep-forge-file-records cron + OPS-01 deploy checklist</name>
  <files>convex/crons.ts, docs/forge-deploy-checklist.md</files>
  <read_first>
    - convex/crons.ts L111-116 (sweep-forge-log-chunks daily cron — clone, change name + hour + handler) plus L1-80 to confirm no existing cron is at hourUTC:4
    - .planning/phases/82-files-preview-hardening/82-PATTERNS.md §"convex/crons.ts" (L264-286) and §"No Analog Found — docs/forge-deploy-checklist.md" (L887-889)
    - .planning/phases/82-files-preview-hardening/82-RESEARCH.md §"OPS-01: CODEPULSE_ALLOWED_ORIGIN" (L288-294) and Pitfall 5 (L642-645)
    - docs/DEPLOY.md (existing deploy doc — match its markdown style/tone for the new checklist)
  </read_first>
  <action>
    In convex/crons.ts, after the sweep-forge-log-chunks block (L116), add crons.daily("sweep-forge-file-records", { hourUTC: 4, minuteUTC: 0 }, internal.forge.sweepForgeFileRecords) with a "// Phase 82: Forge file/artifact retention (D-05)" comment. Hour 4:00 UTC is offset from the 3:30 log sweep to avoid scheduler contention (the Phase 81 offset precedent).

    Create docs/forge-deploy-checklist.md (OPS-01). A markdown table documenting every Forge ingest env var with columns Variable | Required | Where set | Purpose:
      - CODEPULSE_ALLOWED_ORIGIN — required in prod — Convex cloud deployment env — production CORS allowlist; when set, getCorsHeaders echoes the specific origin (no wildcard fallback for Forge endpoints). Note it must be SET in prod (currently commented in .env.example).
      - FORGE_INGEST_API_KEY — required — Convex deployment env AND forge daemon process env — shared bearer for /forge-ingest, /forge-log-ingest, /forge-file-ingest; server-to-server only, NEVER in browser code.
      - FORGE_INGEST_ALLOW_ANON — optional (default unset → fail-closed) — Convex deployment env — explicit opt-in to allow anonymous ingest; leave unset in prod.
      - CONVEX_FORGE_INGEST_URL — required (forge daemon) — forge daemon env — base URL the emitter appends /forge-ingest to.
      - FORGE_LOG_INGEST_URL — required for log streaming (forge daemon) — forge daemon env — FULL endpoint URL for /forge-log-ingest.
      - FORGE_FILE_INGEST_URL — required for file/artifact streaming (forge daemon) — forge daemon env — FULL endpoint URL for /forge-file-ingest (store the complete path incl. /forge-file-ingest to avoid the double-path 404, Pitfall 5).
    Add a short section noting the daemon gates are independent: an unset FORGE_FILE_INGEST_URL makes file ingest a no-op without affecting logs. Reference that 82-04 wires the daemon side.
  </action>
  <verify>
    <automated>grep -c "sweep-forge-file-records" convex/crons.ts; grep -v '^#' docs/forge-deploy-checklist.md | grep -c "FORGE_FILE_INGEST_URL"; npx tsc --noEmit 2>&1 | grep "crons\.ts" || echo "tsc crons clean"</automated>
  </verify>
  <acceptance_criteria>
    - `convex/crons.ts` registers a daily cron named `sweep-forge-file-records` at `hourUTC: 4` dispatching `internal.forge.sweepForgeFileRecords`.
    - No other cron in convex/crons.ts shares `{ hourUTC: 4, minuteUTC: 0 }`.
    - `docs/forge-deploy-checklist.md` exists and (excluding markdown headers) references `CODEPULSE_ALLOWED_ORIGIN`, `FORGE_INGEST_API_KEY`, `FORGE_LOG_INGEST_URL`, and `FORGE_FILE_INGEST_URL`.
    - `npx tsc --noEmit` reports no errors in crons.ts.
  </acceptance_criteria>
  <done>Daily file-retention cron registered at 04:00 UTC; OPS-01 deploy checklist documents every Forge ingest env var.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cron scheduler → sweepForgeFileRecords | Internal mutation, no Clerk identity; deletes doc rows + File Storage blobs |
| Convex doc row → File Storage blob | storageId references a blob that must be explicitly deleted (no GC) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-82-05 | Info Disclosure / Storage DoS | image blob leak on prune | mitigate | sweepForgeFileRecords calls `ctx.storage.delete(storageId)` BEFORE `ctx.db.delete(_id)` (D-05); test asserts the ordering |
| T-82-07 | DoS | unbounded file/artifact growth | mitigate | 7-day TTL pass + per-job ARTIFACT_BYTE_CAP_PER_JOB (10 MB) cap pass, drop-oldest; daily cron |
| T-82-08 | Tampering / Info Disclosure | production CORS wildcard | mitigate | OPS-01 checklist mandates CODEPULSE_ALLOWED_ORIGIN set in prod so getCorsHeaders echoes a specific origin (no wildcard for Forge endpoints) |
| T-82-09 | Info Disclosure | bearer key in deploy doc | accept | docs/forge-deploy-checklist.md documents the NAME and purpose of FORGE_INGEST_API_KEY only — never a value; doc is committed, no secret material |
| T-82-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages this plan (82-RESEARCH Package Legitimacy Audit: zero installs) |
</threat_model>

<verification>
- `npx vitest run convex/forgeFileIngest.test.ts` — TTL, per-job cap, and D-05 blob-ordering tests green.
- `npx tsc --noEmit` — clean for forge.ts and crons.ts.
- `npm test` — full suite green (per-wave merge gate).
- Manual (deferred to verify-work): confirm CODEPULSE_ALLOWED_ORIGIN is actually set in the Convex cloud deployment per the new checklist.
</verification>

<success_criteria>
- sweepForgeFileRecords prunes forgeFiles + forgeArtifacts by 7-day TTL and per-job byte cap, deleting image blobs before doc rows.
- Daily cron sweep-forge-file-records registered at 04:00 UTC, offset from the 03:30 log sweep.
- Retention/cleanup test demonstrates past-TTL and over-cap deletion (doc + blob) while in-window records survive.
- docs/forge-deploy-checklist.md documents CODEPULSE_ALLOWED_ORIGIN + every Forge ingest env var.
</success_criteria>

<output>
Create `.planning/phases/82-files-preview-hardening/82-02-SUMMARY.md` when done
</output>
