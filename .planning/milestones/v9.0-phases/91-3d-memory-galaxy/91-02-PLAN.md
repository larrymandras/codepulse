---
phase: 91-3d-memory-galaxy
plan: 02
type: execute
wave: 1
depends_on: ["91-01"]
files_modified:
  - src/components/graph/ForceGraph3D.tsx
  - src/lib/graph-center.ts
  - src/lib/graph-center.test.ts
autonomous: true
requirements: [G3D-01, G3D-02]

must_haves:
  truths:
    - "A ForceGraph3D component renders react-force-graph-3d behind a forwardRef handle, with three.js imports confined to this one file"
    - "The 3D simulation terminates (finite cooldownTicks) so onEngineStop fires and zoomToFit runs"
    - "centerNode3DWhenReady polls for node coords then drives cameraPosition with a correct lookAt"
  artifacts:
    - path: "src/components/graph/ForceGraph3D.tsx"
      provides: "Lazy-boundary 3D render surface + ForceGraph3DHandle imperative API"
      contains: "forwardRef<ForceGraph3DHandle"
      min_lines: 60
    - path: "src/lib/graph-center.ts"
      provides: "centerNode3DWhenReady + Centerable3DHandle for the ?focus= 3D branch"
      contains: "export function centerNode3DWhenReady"
  key_links:
    - from: "src/components/graph/ForceGraph3D.tsx"
      to: "react-force-graph-3d"
      via: "default import (the dynamic-import boundary isolating three.js)"
      pattern: "from \"react-force-graph-3d\""
    - from: "src/lib/graph-center.ts"
      to: "ForceGraph3DHandle.cameraPosition"
      via: "centerNode3DWhenReady tick"
      pattern: "cameraPosition\\("
---

<objective>
Build the 3D render surface (`ForceGraph3D.tsx`) as a thin lazy-loadable wrapper over `react-force-graph-3d`, and extend `graph-center.ts` with the 3D camera-centering helper for the `?focus=` deep-link branch.

Purpose: G3D-01 requires a lazy-isolated 3D render component (all three.js imports confined here so the 2D bundle never pulls three — SC#2); G3D-02 requires correct sphere color/size props and clean simulation termination. D-01b requires a 3D centering helper with `cameraPosition` semantics distinct from the 2D `centerAt`/`zoom`.
Output: `src/components/graph/ForceGraph3D.tsx` (new) + `centerNode3DWhenReady` in `src/lib/graph-center.ts` (+ unit test).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/91-3d-memory-galaxy/91-RESEARCH.md
@.planning/phases/91-3d-memory-galaxy/91-PATTERNS.md
@.planning/phases/91-3d-memory-galaxy/91-UI-SPEC.md

<interfaces>
<!-- 2D analog contract — ForceGraphCanvas.tsx L30-86 -->
<!-- ForceGraphHandle: centerAt, zoom, zoomToFit, d3Force, d3ReheatSimulation -->
<!-- ForceGraphCanvasProps: data {nodes,links}, colorFn, labelFn, linkColorFn, linkWidthFn,
     onNodeClick, onNodeHover, onBackgroundClick, onEngineStop, nodeRelSize, className -->

<!-- 3D handle to expose — RESEARCH §Pattern 8 / PATTERNS.md L57-71 -->
<!-- ForceGraph3DHandle: cameraPosition(pos,lookAt?,ms?), zoomToFit(ms?,px?,filterFn?),
     refresh(), scene(), renderer(), d3Force(name,force?), d3ReheatSimulation(),
     pauseAnimation(), resumeAnimation() -->

<!-- Existing 2D centering — graph-center.ts L16-61 -->
<!-- CenterableHandle: centerAt(x,y,ms?), zoom(k,ms?); centerNodeWhenReady polls x/y via RAF, maxFrames=90 -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ForceGraph3D.tsx lazy wrapper + handle</name>
  <files>src/components/graph/ForceGraph3D.tsx</files>
  <read_first>
    - src/components/graph/ForceGraphCanvas.tsx L1-151 (import block L1-9, `ForceGraphHandle` L30-38, `ForceGraphCanvasProps` L40-86, `forwardRef`/`useImperativeHandle` shape L88-151) AND L287-329 (outer container `<div className={className ?? ...}>` with `bg-[#09090b]`)
    - .planning/phases/91-3d-memory-galaxy/91-PATTERNS.md §"src/components/graph/ForceGraph3D.tsx" L23-165 (imports swap L40-42, ForceGraph3DHandle L57-71, forwardRef shape L74-92, props interface + nodeValFn addition L94-110, container L112-137, core 3D prop baseline L139-162, anti-pattern L164)
    - .planning/phases/91-3d-memory-galaxy/91-RESEARCH.md §Pattern 2 (prop baseline L271-299), §Pattern 8 (handle L444-465)
    - .planning/phases/91-3d-memory-galaxy/91-UI-SPEC.md §"3D Sphere Geometry Props" L179-196
  </read_first>
  <action>
    Create `src/components/graph/ForceGraph3D.tsx` as a `forwardRef<ForceGraph3DHandle, ForceGraph3DProps>`. Default-import the library as `ForceGraph3DLib` from `react-force-graph-3d` — this file is the ONLY place `react-force-graph-3d`/`three`/`3d-force-graph` may be referenced (the dynamic-import boundary that keeps three.js out of the main chunk — SC#2). Export the `ForceGraph3DHandle` interface exactly per the §interfaces block (cameraPosition, zoomToFit, refresh, scene, renderer, d3Force, d3ReheatSimulation, pauseAnimation, resumeAnimation), wired via `useImperativeHandle` to an internal `fgRef3dInner` ref. Define `ForceGraph3DProps` mirroring `ForceGraphCanvasProps` prop names where they map (`data`, `colorFn`, `labelFn`, `linkColorFn`, `onNodeClick`, `onNodeHover`, `onBackgroundClick`, `onEngineStop`, `className`) PLUS `nodeValFn?: (node:any)=>number` for the selection size bump. Wrap `<ForceGraph3DLib>` in the same outer `<div className={className ?? canvasClass-equivalent}>` shape (do NOT add an inner backdrop div — `backgroundColor="#09090b"` handles the dark fill per D-02a). Pass the prop baseline from UI-SPEC §3D Sphere Geometry Props: `graphData={data}`, `nodeId="id"`, `nodeLabel={labelFn}`, `nodeColor={colorFn}`, `nodeVal={nodeValFn}`, `nodeRelSize={4}`, `nodeResolution={6}`, `linkColor={linkColorFn}`, `linkOpacity={0.2}`, `linkWidth={0.6}`, `backgroundColor="#09090b"`, `cooldownTicks={150}` (NOT the Infinity default — Pitfall 3: onEngineStop never fires otherwise), `warmupTicks={0}`, `d3VelocityDecay={0.3}`. Forward `onNodeClick`/`onNodeHover`/`onBackgroundClick`/`onEngineStop` to the props. The `#09090b` backdrop and library defaults are the only hardcoded colors here — all node/link colors arrive via props (caller passes theme-resolved hex).
  </action>
  <verify>
    <automated>npx tsc --noEmit && grep -Eq "forwardRef<ForceGraph3DHandle" src/components/graph/ForceGraph3D.tsx && grep -Eq "cooldownTicks=\{150\}" src/components/graph/ForceGraph3D.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `ForceGraph3D.tsx` source contains `forwardRef<ForceGraph3DHandle`
    - `ForceGraph3D.tsx` source contains `cooldownTicks={150}` and `nodeResolution={6}` and `backgroundColor="#09090b"`
    - The only `react-force-graph-3d` import in the repo is in this file: `grep -rl "react-force-graph-3d" src --include=*.tsx --include=*.ts | grep -v test` returns only `src/components/graph/ForceGraph3D.tsx`
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>ForceGraph3D.tsx exposes the 3D handle, sets finite cooldownTicks, confines three.js imports to itself, and typechecks.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend graph-center.ts with centerNode3DWhenReady</name>
  <files>src/lib/graph-center.ts, src/lib/graph-center.test.ts</files>
  <read_first>
    - src/lib/graph-center.ts L1-61 (full file — `CenterableHandle` L16-19, `centerNodeWhenReady` RAF/cancel/maxFrames structure L30-61)
    - src/lib/graph-center.test.ts (existing test patterns for centerNodeWhenReady — mirror its fake-handle + RAF-flush approach)
    - .planning/phases/91-3d-memory-galaxy/91-PATTERNS.md §"src/lib/graph-center.ts (extend)" L536-611 (Centerable3DHandle + centerNode3DWhenReady)
    - .planning/phases/91-3d-memory-galaxy/91-RESEARCH.md §Pattern 5 (L358-393), Pitfall 6 (lookAt must be node coords, L551-557)
  </read_first>
  <behavior>
    - Given a fake handle and a node with x=10,y=20,z=5: centerNode3DWhenReady calls cameraPosition once with position {x:10,y:20,z:155} (z + 150 pull-back) and lookAt {x:10,y:20,z:5}
    - Given a node with z undefined: position z = 150, lookAt z = 0
    - Given a node whose x/y are initially undefined then assigned: it retries via the scheduler and fires once coords appear (mirror centerNodeWhenReady polling)
    - Returns a cancel fn; after cancel, no cameraPosition call fires
  </behavior>
  <action>
    Append (do NOT modify `centerNodeWhenReady`) a `Centerable3DHandle` interface (`cameraPosition(position, lookAt?, ms?)`, `zoomToFit(ms?, px?)`) and `centerNode3DWhenReady(fgRef, node, ms=800, maxFrames=90)` to `src/lib/graph-center.ts`, structurally mirroring `centerNodeWhenReady` (RAF `schedule` with setTimeout fallback, `cancelled` flag, `frames`/`maxFrames` bound, returns a cancel fn). In the `tick`, when `node.x`/`node.y` are non-null, call `fgRef.current?.cameraPosition({ x: node.x, y: node.y, z: (node.z ?? 0) + 150 }, { x: node.x, y: node.y, z: node.z ?? 0 }, ms)` — pass the node coords as the explicit `lookAt` (Pitfall 6: a null lookAt aims at scene origin). Add the matching test cases per the behavior block to `src/lib/graph-center.test.ts`, reusing the existing fake-handle + RAF-flush helpers in that file.
  </action>
  <verify>
    <automated>npx vitest run src/lib/graph-center.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `graph-center.ts` contains `export function centerNode3DWhenReady` and `export interface Centerable3DHandle`
    - `centerNodeWhenReady` (L30-61) is byte-unchanged (no edit to the 2D path)
    - `npx vitest run src/lib/graph-center.test.ts` exits 0 with the new cameraPosition cases passing
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>centerNode3DWhenReady drives cameraPosition with a correct lookAt and pull-back; tests green; 2D centerer untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| graph data → WebGL renderer | Client-side node/link arrays drive Three.js geometry (browser only) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-91-DOS | Denial of Service | ForceGraph3D rendering large `graphData` | accept | Opt-in mode with no runtime fallback (D-03). Node/link counts are already bounded by the existing `graphSnapshots` truncation upstream; FPS is validated as a manual gate (plan 05). No new unbounded input introduced here. |
| T-91-SC | Tampering | three.js reachable only via this lazy file | mitigate | All three.js imports confined to `ForceGraph3D.tsx`; build-manifest isolation is verified in plan 04 (SC#2). |

No HIGH-severity threats. This plan adds pure client-side render code and a pure-function centerer; no network, auth, storage, or Convex surface.
</threat_model>

<verification>
- `npx tsc --noEmit` clean
- `react-force-graph-3d` referenced only in `ForceGraph3D.tsx`
- `npx vitest run src/lib/graph-center.test.ts` green
</verification>

<success_criteria>
3D render surface and 3D centering helper exist and typecheck; three.js imports isolated; centerer unit-tested.
</success_criteria>

<output>
Create `.planning/phases/91-3d-memory-galaxy/91-02-SUMMARY.md` when done
</output>
