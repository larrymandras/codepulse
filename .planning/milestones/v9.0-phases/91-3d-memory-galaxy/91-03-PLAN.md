---
phase: 91-3d-memory-galaxy
plan: 03
type: execute
wave: 2
depends_on: ["91-01", "91-02"]
files_modified:
  - src/components/graph/CodeVaultGraph.tsx
autonomous: true
requirements: [G3D-01, G3D-02]

must_haves:
  truths:
    - "A 2D|3D segmented toggle is visible in the CodeVaultGraph header right cluster and reflects the active mode"
    - "Switching to 3D mounts the lazy ForceGraph3D; switching to 2D restores the unchanged ForceGraphCanvas"
    - "renderMode persists to idb-keyval ('codepulse:render-mode') and rehydrates on mount, coerced to 2d|3d"
    - "3D node/link colors come from useThemeColors() hex fields; selection/hover/dim encoded per UI-SPEC; refresh() redraws after the sim stops"
    - "?focus= deep-link centers via centerNode3DWhenReady when renderMode is 3d"
    - "The 2D ForceGraphCanvas mount and its props are unchanged (SC#1 no regression)"
  artifacts:
    - path: "src/components/graph/CodeVaultGraph.tsx"
      provides: "renderMode state, toggle UI, lazy 3D swap, 3D color callbacks, idb persistence, focus 3D branch"
      contains: "codepulse:render-mode"
  key_links:
    - from: "src/components/graph/CodeVaultGraph.tsx"
      to: "./ForceGraph3D (LazyForceGraph3D)"
      via: "React.lazy module-level + Suspense render slot"
      pattern: "lazy\\(\\(\\) => import\\(\"\\./ForceGraph3D\"\\)\\)"
    - from: "src/components/graph/CodeVaultGraph.tsx"
      to: "idb-keyval"
      via: "idbGet/idbSet of codepulse:render-mode"
      pattern: "codepulse:render-mode"
    - from: "src/components/graph/CodeVaultGraph.tsx"
      to: "centerNode3DWhenReady"
      via: "useFocusParam onFocus 3D branch"
      pattern: "centerNode3DWhenReady\\("
---

<objective>
Integrate the opt-in 3D render mode into the existing `GraphContent` shell: a `2D | 3D` toggle, idb-keyval persistence, a lazy render-surface swap, theme-aware 3D color/size callbacks, and the `?focus=` 3D centering branch — all while leaving the 2D path byte-unchanged.

Purpose: This is the G3D-01 + G3D-02 payload. Full parity (D-01) means only the render surface swaps inside `GraphContent`; the detail panel, source filter, fullscreen, KG links, and neighbor nav stay mounted. Making the Wave 0 `ForceGraph3D.test.tsx` (SC#1/SC#4-mock/SC#5) go GREEN is the acceptance signal for this plan.
Output: modified `src/components/graph/CodeVaultGraph.tsx`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/91-3d-memory-galaxy/91-PATTERNS.md
@.planning/phases/91-3d-memory-galaxy/91-UI-SPEC.md
@.planning/phases/91-3d-memory-galaxy/91-RESEARCH.md

<interfaces>
<!-- ForceGraph3D (from plan 02) — src/components/graph/ForceGraph3D.tsx -->
<!-- props: data, colorFn, labelFn, linkColorFn, nodeValFn, onNodeClick, onNodeHover,
     onBackgroundClick, onEngineStop, className; ref: ForceGraph3DHandle (cameraPosition, zoomToFit, refresh, ...) -->

<!-- centerNode3DWhenReady (from plan 02) — src/lib/graph-center.ts -->
<!-- centerNode3DWhenReady(fgRef, node {x?,y?,z?}, ms?, maxFrames?) : () => void -->

<!-- ThemeColors (existing) — colors.primary (hex), colors.vaultNode (hex) safe for 3D;
     colors.primaryAlpha18 / vaultNodeAlpha18 are rgba — NOT safe for nodeColor/linkColor -->

<!-- Existing host anchors in CodeVaultGraph.tsx:
     L110-115 state block; L120 useThemeColors; L125-147 colorFn/linkColorFn;
     L152-160 useFocusParam onFocus (2D centerNodeWhenReady);
     L255-270 selectedNode + neighborNodes memo;
     L319-321 canvasClass; L329-332 chipClass; L380-404 right cluster + source-filter group;
     L478-493 ForceGraphCanvas mount (the swap point) -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: renderMode state, idb persistence, and the 2D|3D toggle UI (D-04)</name>
  <files>src/components/graph/CodeVaultGraph.tsx</files>
  <read_first>
    - src/components/graph/CodeVaultGraph.tsx L110-115 (state block), L319-332 (canvasClass + chipClass), L380-430 (right cluster: source-filter group L382-404 + fullscreen Button L409-429)
    - .planning/phases/91-3d-memory-galaxy/91-PATTERNS.md §"idb-keyval render-mode persistence" L399-437 (import L405, state L411, hydrate-on-mount with cancel guard L416-422, handleModeToggle L427-430, coercion L434-436) AND §"Analog C/D" L209-269 (renderModeChipClass + the cloned chip group markup)
    - .planning/phases/91-3d-memory-galaxy/91-UI-SPEC.md §"2D | 3D Toggle Anatomy" L199-266 (placement between source-filter group and fullscreen button, chip classes, ARIA `role="group" aria-label="Render mode"`, disabled-while-loading variant, state machine)
    - src/hooks/useKnowledgeGraph.ts L120-151 (the idb-keyval hydrate/persist analog with cancel guard)
  </read_first>
  <behavior>
    - On mount with no idb value: renderMode is "2d"; ForceGraphCanvas renders (force-graph-canvas testid present)
    - Clicking the "3D" button sets renderMode "3d" and calls idbSet("codepulse:render-mode","3d")
    - Clicking "2D" sets renderMode "2d" and calls idbSet("codepulse:render-mode","2d")
    - idbGet returning "3d" on mount hydrates to 3d; any other value stays "2d" (coercion — never setRenderMode(saved) blindly)
    - Toggle group renders role="group" aria-label="Render mode" with two buttons whose aria-pressed reflects the active mode
  </behavior>
  <action>
    Add `import { get as idbGet, set as idbSet } from "idb-keyval";` and ensure `React`/`lazy`/`Suspense`/`useMemo` are imported. In `GraphContent`, add `const [renderMode, setRenderMode] = useState<"2d" | "3d">("2d");` and `const [hoveredNodeId, setHoveredNodeId] = useState<string | null>(null);` to the L110-115 state block. Add a hydrate-on-mount effect (mirror useKnowledgeGraph.ts L121-142): `idbGet("codepulse:render-mode")` and, with a `cancelled` cancel guard, `if (!cancelled && saved === "3d") setRenderMode("3d")` (coerce — V5 input validation; never trust the raw value), `.catch` swallowed for private-browsing. Add `const handleModeToggle = (mode: "2d"|"3d") => { setRenderMode(mode); idbSet("codepulse:render-mode", mode).catch(()=>{}); }`. Add `renderModeChipClass(mode)` cloned from `chipClass` (active = `bg-primary/10 text-primary border-primary/40`, inactive = `bg-transparent text-muted-foreground border-border`). Insert a `<div role="group" aria-label="Render mode" className="flex items-center gap-1">` with `2D` and `3D` `<button>`s (each `aria-pressed`, `onClick={() => handleModeToggle(...)}`) into the right-cluster div at L380, immediately before the fullscreen `<TooltipProvider>` block (L409) — per UI-SPEC placement `[ Code | Vault | Both ] [ 2D | 3D ] [ fullscreen ]`. Do not introduce a new component; clone the existing chip pattern.
  </action>
  <verify>
    <automated>npx vitest run src/components/graph/ForceGraph3D.test.tsx src/components/graph/CodeVaultGraph.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Source contains `useState<"2d" | "3d">("2d")` and `codepulse:render-mode` (both idbGet and idbSet sites)
    - Source contains `role="group" aria-label="Render mode"` with `2D` and `3D` buttons carrying `aria-pressed`
    - idb hydrate uses a `cancelled` guard and the `saved === "3d"` coercion (no `setRenderMode(saved)`)
    - The SC#1 toggle-restore test in `ForceGraph3D.test.tsx` advances past the toggle-button query (button now exists)
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Toggle visible and wired; renderMode persists/rehydrates via idb-keyval with coercion.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Lazy render-surface swap + theme-aware 3D color/size callbacks + refresh effect</name>
  <files>src/components/graph/CodeVaultGraph.tsx</files>
  <read_first>
    - src/components/graph/CodeVaultGraph.tsx L125-147 (2D colorFn/linkColorFn useCallback([colors]) pattern), L255-270 (selectedNode + neighborNodes memo), L319-321 (canvasClass), L474-494 (ForceGraphCanvas mount — the swap point)
    - .planning/phases/91-3d-memory-galaxy/91-PATTERNS.md §"Analog A/B" L170-207 (module-level `React.lazy(() => import("./ForceGraph3D"))` + Suspense fallback), §"Analog E" L271-311 (conditional swap slot, rename fgRef→fgRef2d, add fgRef3d), §"Analog F" L313-381 (colorFn3D/nodeValFn3D/linkColorFn3D hex-only + neighborIds memo + hoveredNodeId), §"refresh() after sim stops" L389-395
    - .planning/phases/91-3d-memory-galaxy/91-UI-SPEC.md §"Node State Encoding Table" L127-149 (white selected/hover, #27272a dim, nodeVal×3) and §"Link State Encoding" L154-177
    - .planning/phases/91-3d-memory-galaxy/91-RESEARCH.md §Pattern 3 (colorFn3D hex-only L302-339), §Pattern 4 (refresh L342-355), Pitfall 1 (rgba→black L503-511), Pitfall 4 (theme switch needs refresh L533-541), Pitfall 5 (useCallback deps L543-549). NOTE the LOW-confidence Open Questions 1 & 2 (does refresh() re-apply nodeColor to cached materials; does nodeVal resize spheres) — VERIFY these empirically in the dev app during this task; if refresh()/nodeVal do not re-apply, document it in the SUMMARY and fall back to the `nodeThreeObject` material-mutation approach noted in RESEARCH Open Questions (do NOT assume they work).
  </read_first>
  <behavior>
    - colorFn3D returns "#ffffff" for the selected node id and for the hovered node id
    - colorFn3D returns "#27272a" for a non-neighbor when a selection is active
    - colorFn3D returns colors.primary (hex) for a code node and colors.vaultNode (hex) for a vault node with no selection
    - nodeValFn3D returns (node.val ?? 1) * 3 for the selected node, else node.val ?? 1
    - linkColorFn3D returns hex strings only (colors.primary / colors.vaultNode / "#94a3b8") — never an rgba() string
    - Switching 2D→3D mounts force-graph-3d; switching 3D→2D removes it and restores force-graph-canvas (SC#1)
  </behavior>
  <action>
    At module top of `CodeVaultGraph.tsx` (outside `GraphContent`, mirroring App.tsx L24-74) add `const LazyForceGraph3D = React.lazy(() => import("./ForceGraph3D"));`. Rename the existing `fgRef` (L111) to `fgRef2d` and add `const fgRef3d = useRef<ForceGraph3DHandle>(null);` (import the type from `./ForceGraph3D`). Add a `neighborIds` `useMemo` (Set<string> of selected node's neighbors, derived from `filteredData.links` + `selectedNodeId`, analogous to `neighborNodes` L260-270). Add `useCallback`-wrapped 3D callbacks per UI-SPEC/RESEARCH: `colorFn3D` (deps `[selectedNodeId, hoveredNodeId, neighborIds, colors]`) returning `"#ffffff"` for selected/hovered, `"#27272a"` for non-neighbors when a selection is active (Pitfall 1 — rgba would render black; `#27272a` is the justified dim hex), else `isVaultNode(node) ? colors.vaultNode : colors.primary`; `nodeValFn3D` (deps `[selectedNodeId]`) returning `(node.val ?? 1) * 3` when selected else `node.val ?? 1`; `linkColorFn3D` (deps `[colors]`) returning hex-only (`colors.vaultNode` for vault↔vault, `colors.primary` for code↔code, `"#94a3b8"` cross-source). At the swap point (L478-493), wrap in a `renderMode === "2d" ? (<ForceGraphCanvas ... unchanged ... />) : (<Suspense fallback={canvasClass loading "Loading 3D render…"}><LazyForceGraph3D ref={fgRef3d} data={filteredData} colorFn={colorFn3D} nodeValFn={nodeValFn3D} linkColorFn={linkColorFn3D} labelFn={labelFn} onNodeClick={(n)=>setSelectedNodeId(n.id)} onNodeHover={(n)=>setHoveredNodeId(n?.id ?? null)} onBackgroundClick={()=>setSelectedNodeId(null)} onEngineStop={()=>fgRef3d.current?.zoomToFit(400,60)} className={canvasClass} /></Suspense>)`. CRITICAL: keep the 2D `<ForceGraphCanvas>` branch's props byte-identical to the current mount (only the ref name changes to fgRef2d) — SC#1 no regression. Add the post-settle redraw effect (RESEARCH Pattern 4): `useEffect(() => { if (renderMode !== "3d") return; fgRef3d.current?.refresh(); }, [selectedNodeId, hoveredNodeId, colors, renderMode]);`.
  </action>
  <verify>
    <automated>npx vitest run src/components/graph/ForceGraph3D.test.tsx src/components/graph/CodeVaultGraph.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Source contains module-level `lazy(() => import("./ForceGraph3D"))` and a `Suspense` wrapper around `LazyForceGraph3D`
    - `colorFn3D` returns hex-only branches; `grep` for `rgba(` inside the colorFn3D/linkColorFn3D bodies finds none
    - The renamed `fgRef2d` is used by the unchanged `ForceGraphCanvas`; `fgRef3d` typed as `ForceGraph3DHandle`
    - SC#1 (toggle-restore), SC#4 (disposal-unmount), SC#5 (colorFn hex) cases in `ForceGraph3D.test.tsx` PASS
    - `npx vitest run src/components/graph/CodeVaultGraph.test.tsx` exits 0 (2D regression suite still green)
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Render surface swaps lazily; 3D colors/sizes are theme-aware hex; refresh wired; 2D path unchanged; Wave 0 tests GREEN.</done>
</task>

<task type="auto">
  <name>Task 3: Wire the ?focus= 3D centering branch</name>
  <files>src/components/graph/CodeVaultGraph.tsx</files>
  <read_first>
    - src/components/graph/CodeVaultGraph.tsx L152-160 (useFocusParam onFocus, currently 2D `centerNodeWhenReady(fgRef, ...)`)
    - src/hooks/useFocusParam.ts L39-71 (ref-agnostic onFocus contract — caller closes over its own ref)
    - .planning/phases/91-3d-memory-galaxy/91-PATTERNS.md §"Wiring in GraphContent" L613-629 (mode-conditional onFocus branch)
    - .planning/phases/91-3d-memory-galaxy/91-UI-SPEC.md §"Interaction Parity Contract" L347-369 (the `?focus=` row + ref abstraction note D-01b)
  </read_first>
  <action>
    Update the `useFocusParam` `onFocus` callback (L155-159) to a mode-conditional branch: always `setSelectedNodeId(node.id)`; then if `renderMode === "2d"` keep the existing `centerNodeWhenReady(fgRef2d, node as { x?: number; y?: number })` call unchanged, else call `centerNode3DWhenReady(fgRef3d, node as { x?: number; y?: number; z?: number })` (import from `@/lib/graph-center` / `../../lib/graph-center` matching the existing centerNodeWhenReady import). Because `useFocusParam` is one-shot and ref-agnostic, no change to the hook is needed. Confirm `renderMode` is in scope at the onFocus closure (it is — declared in Task 1).
  </action>
  <verify>
    <automated>npx tsc --noEmit && grep -Eq "centerNode3DWhenReady\(fgRef3d" src/components/graph/CodeVaultGraph.tsx && npx vitest run src/components/graph/CodeVaultGraph.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `onFocus` calls `centerNodeWhenReady(fgRef2d, ...)` in the 2D branch and `centerNode3DWhenReady(fgRef3d, ...)` in the 3D branch
    - `centerNode3DWhenReady` is imported from the graph-center module
    - `npx vitest run src/components/graph/CodeVaultGraph.test.tsx` exits 0
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Deep-link focus centers via cameraPosition in 3D mode and via centerAt/zoom in 2D mode.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| IndexedDB → app state | Persisted `codepulse:render-mode` value is read back into render state on mount |
| graph data → WebGL | Same client-side snapshot drives both render surfaces |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-91-IDB | Tampering | idb-keyval `codepulse:render-mode` value | mitigate | Read value is coerced: only the literal `"3d"` flips to 3D, anything else (tampered/garbage/missing) defaults to `"2d"` (ASVS V5; RESEARCH §Security Domain L695-700). Impact is cosmetic only (which renderer mounts) — no PII, no privilege. |
| T-91-DOS | Denial of Service | 3D mounting a very large `graphData` | accept | Opt-in, no runtime fallback (D-03); counts bounded upstream by `graphSnapshots` truncation; FPS validated as a manual gate (plan 05). |

No HIGH-severity threats. The toggle writes one non-PII enum to local IndexedDB; the coercion closes the only realistic (cosmetic) tampering vector.
</threat_model>

<verification>
- `npx vitest run src/components/graph/ForceGraph3D.test.tsx src/components/graph/CodeVaultGraph.test.tsx` green (Wave 0 tests now GREEN; 2D regression suite still green)
- `npx tsc --noEmit` clean
- No `rgba(` in the 3D color callbacks
</verification>

<success_criteria>
Opt-in 3D mode is fully integrated (toggle + persistence + lazy swap + theme colors + focus branch) with the 2D path unchanged and the Wave 0 Nyquist tests GREEN.
</success_criteria>

<output>
Create `.planning/phases/91-3d-memory-galaxy/91-03-SUMMARY.md` when done
</output>
