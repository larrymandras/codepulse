---
phase: 91-3d-memory-galaxy
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - src/components/graph/ForceGraph3D.test.tsx
autonomous: true
requirements: [G3D-01, G3D-02]

must_haves:
  truths:
    - "react-force-graph-3d is installed and three is present transitively (not a separate top-level dep)"
    - "ForceGraph3D.test.tsx exists and exercises the 3D toggle/color/disposal behaviors via a mocked react-force-graph-3d"
    - "The new test RED-fails cleanly until the Wave 1/2 implementation lands (no Vite transform or tsc break)"
  artifacts:
    - path: "src/components/graph/ForceGraph3D.test.tsx"
      provides: "Nyquist Wave 0 scaffold covering SC#1 toggle-restore, SC#4 disposal-mock, SC#5 colorFn hex"
      contains: "vi.mock(\"react-force-graph-3d\""
    - path: "package.json"
      provides: "react-force-graph-3d production dependency"
      contains: "react-force-graph-3d"
  key_links:
    - from: "src/components/graph/ForceGraph3D.test.tsx"
      to: "react-force-graph-3d"
      via: "vi.mock capturing lastForceGraph3DProps"
      pattern: "vi\\.mock\\(\"react-force-graph-3d\""
---

<objective>
Install the locked 3D rendering library and author the Wave 0 Nyquist test scaffold for the 3D render mode, so all downstream implementation is test-driven.

Purpose: `react-force-graph-3d` (which transitively pulls `three`) must exist before the lazy 3D wrapper can import it (G3D-01), and the validation contract (91-VALIDATION.md) requires `ForceGraph3D.test.tsx` to exist in Wave 0 covering SC#1 (toggle restore), SC#4 (disposal mock), and SC#5 (colorFn hex). The test RED-fails until the implementation lands in plans 02–03.
Output: `react-force-graph-3d` in `package.json` + `src/components/graph/ForceGraph3D.test.tsx` (RED).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/91-3d-memory-galaxy/91-RESEARCH.md
@.planning/phases/91-3d-memory-galaxy/91-PATTERNS.md
@.planning/phases/91-3d-memory-galaxy/91-VALIDATION.md

<interfaces>
<!-- ThemeColors fields the colorFn3D test asserts against (src/hooks/useThemeColors.ts L14-27) -->
<!-- colors.primary (hex, code nodes), colors.vaultNode (hex, vault nodes) are the only safe 3D colors. -->
<!-- The mocked useThemeColors returns: primary "#10b981", vaultNode "#8b5cf6" (matches CodeVaultGraph.test.tsx L49-64). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install react-force-graph-3d (three comes transitively)</name>
  <files>package.json, package-lock.json</files>
  <read_first>
    - .planning/phases/91-3d-memory-galaxy/91-RESEARCH.md §"Standard Stack" (install command, L129-143) and §"Package Legitimacy Audit" (L147-161 — both packages slopcheck [OK], no postinstall)
    - package.json (confirm react-force-graph-2d@^1.29.1 already present; three NOT present)
  </read_first>
  <action>
    Run `npm install react-force-graph-3d` (per G3D-01, library locked in CONTEXT.md D / RESEARCH §Standard Stack). Do NOT run `npm install three` separately — `three` is a transitive dependency of `3d-force-graph` (which `react-force-graph-3d` depends on) per RESEARCH (the `three: >=0.179 <1` constraint is owned by `3d-force-graph`). Confirm `react-force-graph-3d` lands in `package.json` dependencies and `three` appears in `package-lock.json` as a resolved transitive dep. Both packages passed the RESEARCH Package Legitimacy Audit ([OK], no postinstall scripts) — no operator legitimacy checkpoint required.
  </action>
  <verify>
    <automated>node -e "const p=require('./package.json'); if(!p.dependencies['react-force-graph-3d']) process.exit(1); if(p.dependencies['three']) {console.error('three should not be a top-level dep'); process.exit(1)}" && node -e "require.resolve('three')"</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` dependencies contains `react-force-graph-3d`
    - `package.json` dependencies does NOT contain a top-level `three` entry
    - `node -e "require.resolve('three')"` exits 0 (three resolvable transitively)
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>react-force-graph-3d installed; three resolvable transitively; no top-level three dep; tsc clean.</done>
</task>

<task type="auto">
  <name>Task 2: Author ForceGraph3D.test.tsx Wave 0 RED scaffold</name>
  <files>src/components/graph/ForceGraph3D.test.tsx</files>
  <read_first>
    - src/components/graph/CodeVaultGraph.test.tsx L20-134 (mock declaration block: convex/react, _generated/api, useThemeColors L49-64 verbatim, useKnowledgeGraph, ForceGraphCanvas prop-capture mock L79-98, Tooltip mock, MemoryRouter render helper) AND L378-396 (the colorFn capture+assert pattern)
    - .planning/phases/91-3d-memory-galaxy/91-PATTERNS.md §"src/components/graph/ForceGraph3D.test.tsx (test)" L441-531 (react-force-graph-3d mock capturing lastForceGraph3DProps, idb-keyval mock, SC#5 color assertion)
    - .planning/phases/91-3d-memory-galaxy/91-VALIDATION.md §"Wave 0 Requirements" L54-59
  </read_first>
  <action>
    Create `src/components/graph/ForceGraph3D.test.tsx` that renders `CodeVaultGraph` (import after mocks) inside `MemoryRouter`. Mirror the mock block from `CodeVaultGraph.test.tsx`: reuse the `convex/react`, `../../convex/_generated/api`, `../../hooks/useThemeColors` (verbatim — primary `#10b981`, vaultNode `#8b5cf6`), and `../../hooks/useKnowledgeGraph` mocks, plus the project graph fixture helper `makeProjectGraphFixture` / `mockGetProjectGraph`. Add `vi.mock("react-force-graph-3d", ...)` whose `default` export captures props into a module-level `lastForceGraph3DProps` and returns a `<div data-testid="force-graph-3d" data-node-count={...}/>` (mirror the ForceGraphCanvas mock). Add `vi.mock("idb-keyval", () => ({ get: vi.fn().mockResolvedValue(undefined), set: vi.fn().mockResolvedValue(undefined) }))`. Write three RED test cases:
      (a) SC#1 toggle-restore: render with a fixture, query the "3D" toggle button (`aria-label="Render mode"` group, button text `3D`), click it, `await` the lazy 3D chunk via `findByTestId("force-graph-3d")`, then click "2D" and assert `force-graph-3d` is removed and `force-graph-canvas` is back.
      (b) SC#4 disposal-mock: after toggling 3D→2D, assert the `force-graph-3d` testid is unmounted (gone from the DOM) without throwing.
      (c) SC#5 colorFn-hex: after switching to 3D, read `lastForceGraph3DProps.nodeColor`; assert it is a function; assert it returns `"#10b981"` for a code node (`{ source: "codepulse", id: "graphify:..." }`) and `"#8b5cf6"` for a vault node (`{ source: "vault", id: "vault:Note.md" }`).
    Use `act`/`findBy*` for the Suspense+lazy boundary. These tests are EXPECTED to fail now (no toggle, no 3D path yet) — that is the intended RED state; do not stub the implementation to make them pass.
  </action>
  <verify>
    <automated>npx vitest run src/components/graph/ForceGraph3D.test.tsx 2>&1 | grep -Eq "fail|FAIL|3 (failed|tests)" && echo RED-OK</automated>
  </verify>
  <acceptance_criteria>
    - File `src/components/graph/ForceGraph3D.test.tsx` exists
    - Source contains `vi.mock("react-force-graph-3d"` and `vi.mock("idb-keyval"`
    - Source contains exactly three `it(` cases tagged for SC#1, SC#4, SC#5
    - `npx vitest run src/components/graph/ForceGraph3D.test.tsx` RED-fails (tests run, assertions fail — NOT a Vite transform error or tsc parse break)
    - `npx tsc --noEmit` exits 0 (test compiles even while RED)
  </acceptance_criteria>
  <done>ForceGraph3D.test.tsx exists, compiles, and RED-fails with running assertions (not a transform error).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → build | New third-party packages enter the dependency tree |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-91-SC | Tampering | `react-force-graph-3d` + transitive `three` install | mitigate | Both packages slopcheck `[OK]` in 91-RESEARCH.md §Package Legitimacy Audit (vasturiano + mrdoob, established OSS, no postinstall scripts). Install the named package only; verify via package-lock. No `[ASSUMED]`/`[SUS]` packages → no blocking-human legitimacy checkpoint per the package-legitimacy gate. |

No HIGH-severity threats in this plan. The only surface is a dependency install of two audited, postinstall-free packages.
</threat_model>

<verification>
- `npx tsc --noEmit` clean
- `react-force-graph-3d` present, `three` transitive only
- `ForceGraph3D.test.tsx` RED-fails with running assertions
</verification>

<success_criteria>
Package installed (SC#2 prerequisite) and the Nyquist Wave 0 test scaffold exists covering SC#1/SC#4/SC#5 (RED).
</success_criteria>

<output>
Create `.planning/phases/91-3d-memory-galaxy/91-01-SUMMARY.md` when done
</output>
