---
phase: 83-graph-snapshot-receiver
plan: 02
type: execute
wave: 2
depends_on: ["83-01"]
files_modified:
  - convex/graphSnapshots.ts
  - convex/runtimeIngest.ts
  - convex/crons.ts
  - convex/graphSnapshots.test.ts
autonomous: true
requirements: [GH-01]
nyquist_compliant: true

must_haves:
  truths:
    - "A graph_snapshot event POSTed to /runtime-ingest writes node/link rows and flips activeVersion to a complete new version"
    - "Re-POSTing the same snapshotId increments version and never produces two active versions (idempotent full-replacement)"
    - "Links whose source or target is not among stored nodes are dropped before persisting (no dangling refs)"
    - "getProjectGraph returns null before any snapshot, and the active version's {meta, nodes, links} after"
    - "The retention sweep keeps the last 7 versions and deletes at most one stale version's rows per invocation"
  artifacts:
    - path: "convex/graphSnapshots.ts"
      provides: "upsertGraphSnapshot + getProjectGraph + listSnapshots + sweepGraphSnapshotVersions + selectVersionDeletes"
      exports: ["upsertGraphSnapshot", "getProjectGraph", "listSnapshots", "sweepGraphSnapshotVersions", "selectVersionDeletes", "GRAPH_SNAPSHOT_KEEP_VERSIONS"]
    - path: "convex/graphSnapshots.test.ts"
      provides: "Pure-logic unit tests for retention selection, dispatch mapping, dangling-link guard"
      min_lines: 40
    - path: "convex/runtimeIngest.ts"
      provides: "case graph_snapshot dispatch"
      contains: "case \"graph_snapshot\""
    - path: "convex/crons.ts"
      provides: "daily retention sweep registration"
      contains: "sweep-graph-snapshot-versions"
  key_links:
    - from: "convex/runtimeIngest.ts case graph_snapshot"
      to: "internal.graphSnapshots.upsertGraphSnapshot"
      via: "ctx.runMutation"
      pattern: "internal\\.graphSnapshots\\.upsertGraphSnapshot"
    - from: "convex/crons.ts"
      to: "internal.graphSnapshots.sweepGraphSnapshotVersions"
      via: "crons.daily"
      pattern: "sweepGraphSnapshotVersions"
---

<objective>
Build the receiver module `convex/graphSnapshots.ts` and wire it: add the `case "graph_snapshot"` dispatch to `runtimeIngest.ts`, register the daily retention sweep in `crons.ts`, and add pure-logic Vitest coverage in `graphSnapshots.test.ts`. This is the core of GH-01 — it turns the currently-dropped nightly `graph_snapshot` event into persisted, versioned, idempotently-replaced graph data with a public read API for Phase 84.

Purpose: The `graph_snapshot` event is absent from the `runtimeIngest` switch today, so every nightly emit is silently discarded. This plan fixes that live bug and exposes `getProjectGraph` / `listSnapshots` for the rendering phase.
Output: One new module, one new dispatch case, one new cron entry, one new test file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/83-graph-snapshot-receiver/83-CONTEXT.md
@.planning/phases/83-graph-snapshot-receiver/83-RESEARCH.md
@.planning/phases/83-graph-snapshot-receiver/83-PATTERNS.md
@.planning/phases/83-graph-snapshot-receiver/83-01-SUMMARY.md

<interfaces>
<!-- Table names + index + producer payload are defined by plan 01 (schema) and 83-RESEARCH.md. -->
<!-- Module/export names are LOCKED in 83-RESEARCH.md Pattern 5 (lines 287-297). -->

Tables (from plan 01): graphSnapshots (index by_snapshotId), graphSnapshotNodes + graphSnapshotLinks (index by_snapshot_version on [snapshotId, version]).

Locked Convex api paths consumed by Phase 84:
- api.graphSnapshots.getProjectGraph({ snapshotId? })
- api.graphSnapshots.listSnapshots()

Dispatch wiring: runtimeIngest.ts already exposes `data` and `timestamp` in scope (lines 39-40); the switch ends ~L880.

Convex limits (83-RESEARCH.md Research Target 2): 16,000 docs/mutation, 16 MiB/mutation, 32,000 docs/query. Worst case ~13,500 rows fits ONE mutation/query. The ONLY hard batching constraint: the sweep must delete at most one stale version (~13,500 rows) per invocation — two would be ~27,000 > 16,000 (Pitfall 5 / Anti-pattern).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create convex/graphSnapshots.ts — receiver mutation, read queries, sweep, pure helper</name>
  <files>convex/graphSnapshots.ts</files>
  <read_first>
    - convex/forge.ts — `appendLogChunk` (~L623-651) for the `internalMutation` + idempotency shape; `selectCapDeletes` (~L561-577) for the exported pure-helper pattern; `sweepForgeLogChunks` (~L579-617) for the two-pass sweep + module-level const + `internal.*` registration shape
    - convex/kg.ts — `upsertSummary` (~L20-50) for patch-or-insert, `latestSummary` (~L52-58) for the public graceful-skip read query (returns null when absent, NO Clerk gating)
    - convex/schema.ts — the three Phase 83 tables added in plan 01 (field names + index names this module references)
    - .planning/phases/83-graph-snapshot-receiver/83-RESEARCH.md — Pattern 2 (versioned swap, lines 199-234), Pattern 3 (read queries, lines 236-250), Pattern 4 (sweep + selectVersionDeletes, lines 252-285), Code Examples getProjectGraph (lines 476-520) and selectVersionDeletes (lines 523-535); Pitfalls 1-6 (lines 356-393)
    - .planning/phases/83-graph-snapshot-receiver/83-PATTERNS.md — graphSnapshots.ts section Parts A-F (lines 136-313), Shared Patterns (lines 402-455)
  </read_first>
  <behavior>
    - selectVersionDeletes([], 7) → []; selectVersionDeletes([1,2,3], 7) → []; selectVersionDeletes([1..9], 7) → [1,2] (oldest dropped, newest 7 kept); order-independent (sorts internally)
    - upsertGraphSnapshot first ingest → activeVersion becomes 1; second ingest of same snapshotId → activeVersion becomes 2 (monotonic, no duplicate meta row)
    - Dangling-link guard: a link whose source or target nodeId is not in the incoming node set is excluded; storedLinkCount reflects the post-filter count
    - activeVersion meta flip happens AFTER all node/link inserts (readers never see a half-written version)
    - getProjectGraph returns null when no meta doc exists; otherwise returns active version's mapped nodes/links + meta counts
  </behavior>
  <action>
    Create `convex/graphSnapshots.ts`. Import `{ internalMutation, query }` from `./_generated/server` and `{ v }` from `convex/values` (no `mutation` — there are no public mutations here). Add a module JSDoc noting Phase 83 / GH-01 and that the two writers are `internalMutation` (httpAction has no Clerk identity) while the two readers are public graceful-skip queries.

    Export module-level `GRAPH_SNAPSHOT_KEEP_VERSIONS = 7` and the pure function `selectVersionDeletes(versions: number[], keepN: number): number[]` — return `[]` when `versions.length <= keepN`, else sort ascending and slice off the oldest `length - keepN` (mirror `selectCapDeletes` structure). Export both at module level so the test imports them directly.

    `upsertGraphSnapshot` = `internalMutation`. Args: `snapshotId: v.string()`, `nodes: v.array(v.object({ id, label, type, community: v.optional(v.union(v.float64(), v.null())), source }))`, `links: v.array(v.object({ source, target, relation }))`, `sources: v.array(v.object({...the seven producer fields}))`, `nodeCount: v.float64()`, `linkCount: v.float64()`, `generatedAt: v.float64()`, `receivedAt: v.float64()`. Handler (versioned swap — D-02): (1) read existing meta via `by_snapshotId` index `.unique()`; (2) `newVersion = (existing?.activeVersion ?? 0) + 1`; (3) build `Set<string>` of incoming node ids; (4) filter `links` to those whose `source` AND `target` are both in the set (dangling-link guard, D-05); (5) insert node rows into `graphSnapshotNodes` under `newVersion`, coercing incoming `community` of `null`/`undefined` to `undefined` for the optional field; (6) insert filtered link rows into `graphSnapshotLinks` under `newVersion`; (7) LAST, patch-or-insert the `graphSnapshots` meta doc with `activeVersion: newVersion`, `sources` verbatim, `nodeCount`, `linkCount`, `storedNodeCount` = inserted node count, `storedLinkCount` = filtered link count, `generatedAt`, `updatedAt: receivedAt`. Loop inserts in chunks of 1,000 (defensive headroom — single mutation still fits ~13,500 rows per Research). Do NOT flip the pointer before the row inserts.

    `getProjectGraph` = public `query`. Args `{ snapshotId: v.optional(v.string()) }`, default `"astridr-project-graph"` (Discretion → Research Open Question 3 recommends the literal). Read meta via `by_snapshotId` `.unique()`; return `null` if absent (graceful-skip). Else read `graphSnapshotNodes` and `graphSnapshotLinks` via `by_snapshot_version` (`.eq("snapshotId", id).eq("version", meta.activeVersion)`) `.collect()`. Return `{ snapshotId, sources, nodeCount, linkCount, storedNodeCount, storedLinkCount, generatedAt, nodes: mapped {id,label,type,community,source}, links: mapped {source,target,relation} }` (strip `_id`/`_creationTime`).

    `listSnapshots` = public `query`, args `{}`. `.collect()` `graphSnapshots`, map to `[{ snapshotId, nodeCount, linkCount, generatedAt, updatedAt }]`.

    `sweepGraphSnapshotVersions` = `internalMutation`, args `{}`. For each meta doc: derive the full stored-version list (e.g. distinct `version` values seen via the `by_snapshot_version` index for that snapshotId — scope the collect to the index, never collect the whole table unconditionally, Pitfall 5). Call `selectVersionDeletes(versions, GRAPH_SNAPSHOT_KEEP_VERSIONS)`. Delete rows for AT MOST ONE stale version per invocation (Pitfall 5 / Anti-pattern: two versions ≈ 27,000 deletes > 16,000 limit); rely on subsequent nightly runs to clear any remaining backlog (Research A2). Track a delete counter and stop before 16,000.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `convex/graphSnapshots.ts` exports `selectVersionDeletes`, `GRAPH_SNAPSHOT_KEEP_VERSIONS`, `upsertGraphSnapshot`, `getProjectGraph`, `listSnapshots`, `sweepGraphSnapshotVersions`
    - `upsertGraphSnapshot` and `sweepGraphSnapshotVersions` are declared with `internalMutation` (NOT `mutation`); `getProjectGraph` and `listSnapshots` with `query`
    - `convex/graphSnapshots.ts` contains the `by_snapshotId` read and two `by_snapshot_version` reads keyed on `meta.activeVersion`
    - The meta-doc patch/insert with `activeVersion` is the LAST write in `upsertGraphSnapshot` (after the node and link insert loops)
    - `convex/graphSnapshots.ts` contains a links filter against a `Set` of incoming node ids (dangling-link guard)
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Module compiles with the five locked exports + the constant; writers are internalMutation, readers are graceful-skip queries; versioned-swap, dangling-link guard, and one-version-per-sweep cap implemented.</done>
</task>

<task type="auto">
  <name>Task 2: Wire dispatch case in runtimeIngest.ts and retention cron in crons.ts</name>
  <files>convex/runtimeIngest.ts, convex/crons.ts</files>
  <read_first>
    - convex/runtimeIngest.ts — the `kg_summary` case (~L692-712) and `kits_snapshot` case (~L673-690) to mirror; the in-scope `data`/`timestamp` vars (lines 38-40); the switch's closing brace (~L880) where the new case is inserted; confirm `internal` is already imported for the `ctx.runMutation(internal.*)` call
    - convex/crons.ts — the `sweep-forge-log-chunks` (~L112-116) and `sweep-forge-file-records` (~L118-124) daily entries to mirror, and the `export default crons` (~L126) the new entry must precede
    - .planning/phases/83-graph-snapshot-receiver/83-PATTERNS.md — runtimeIngest.ts section (lines 69-132) and crons.ts section (lines 317-349)
    - .planning/phases/83-graph-snapshot-receiver/83-RESEARCH.md — runtimeIngest dispatch case (lines 453-473), sweep cron shape (lines 271-278)
  </read_first>
  <action>
    In `runtimeIngest.ts`, add `case "graph_snapshot":` before the switch's closing brace, mirroring the `kg_summary` case. Body: a comment noting Phase 83 / GH-01 + the row-based-storage rationale; `const d = data as any;`; a single `await ctx.runMutation(internal.graphSnapshots.upsertGraphSnapshot, {...})` with defensive access — `snapshotId: d.snapshotId ?? "astridr-project-graph"`, `nodes: Array.isArray(d.nodes) ? d.nodes : []`, `links: Array.isArray(d.links) ? d.links : []`, `sources: Array.isArray(d.sources) ? d.sources : []`, `nodeCount: d.nodeCount ?? 0`, `linkCount: d.linkCount ?? 0`, `generatedAt: d.generatedAt ?? timestamp`, `receivedAt: timestamp`; trailing `break;`. Use `internal.graphSnapshots.*` (NOT `api.*` — httpAction has no Clerk identity, Pitfall 1). Do not modify any existing case; the bearer auth at lines 19-22 is already enforced before the switch — leave it untouched.

    In `crons.ts`, add one `crons.daily("sweep-graph-snapshot-versions", { hourUTC: 4, minuteUTC: 30 }, internal.graphSnapshots.sweepGraphSnapshotVersions)` entry after the `sweep-forge-file-records` (04:00) entry and before `export default crons`, with a Phase 83 / D-03 comment and a note that 04:30 is offset from the 04:00 file sweep to avoid scheduler contention.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `convex/runtimeIngest.ts` contains `case "graph_snapshot"` calling `internal.graphSnapshots.upsertGraphSnapshot` (NOT `api.graphSnapshots.*`)
    - The new case ends with `break;` and is the only modification to the switch (no existing case altered)
    - `convex/crons.ts` contains `sweep-graph-snapshot-versions` registered via `crons.daily` with `internal.graphSnapshots.sweepGraphSnapshotVersions` at `{ hourUTC: 4, minuteUTC: 30 }`, placed before `export default crons`
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>The dispatch case routes graph_snapshot to the internal upsert and the retention sweep is scheduled daily; type-check clean; no existing dispatch path changed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Pure-logic Vitest coverage in convex/graphSnapshots.test.ts</name>
  <files>convex/graphSnapshots.test.ts</files>
  <read_first>
    - convex/kg.test.ts — pure-logic test structure, the `mapKgSummaryEvent` dispatch-mirror pattern, and `it.todo` for DB round-trips
    - convex/forge.test.ts — how exported pure helpers (`selectCapDeletes`) are imported and asserted without a Convex runtime
    - convex/graphSnapshots.ts — the actual `selectVersionDeletes` signature and the dispatch-mapping/dangling-guard logic the test mirrors (created in Task 1)
    - .planning/phases/83-graph-snapshot-receiver/83-RESEARCH.md — Validation Architecture test map GH-01a..g (lines 611-621)
    - .planning/phases/83-graph-snapshot-receiver/83-PATTERNS.md — graphSnapshots.test.ts section (lines 353-399)
    - .planning/phases/83-graph-snapshot-receiver/83-VALIDATION.md — the per-task verification map + manual-only round-trip row to honor
  </read_first>
  <behavior>
    - GH-01a selectVersionDeletes: under-keepN → []; over-keepN → oldest versions only, newest 7 retained; unsorted input handled
    - GH-01b dispatch mapping mirror: missing snapshotId → "astridr-project-graph"; non-array nodes/links/sources → []; missing nodeCount/linkCount → 0; missing generatedAt → falls back to timestamp
    - GH-01c dangling-link guard mirror: link with an endpoint not in the node-id set is dropped; both-endpoints-present link is kept
    - GH-01d community: null and a numeric community both survive the node mapping (coerced for the optional field)
    - GH-01e generatedAt float (e.g. 1750312345.678901) passes through unchanged as a number
  </behavior>
  <action>
    Create `convex/graphSnapshots.test.ts`. `import { describe, it, expect } from "vitest"` and `import { selectVersionDeletes, GRAPH_SNAPSHOT_KEEP_VERSIONS } from "./graphSnapshots"`. No Convex runtime imports. Add module-level mirror functions replicating the dispatch defensive-access (`mapGraphSnapshotEvent`) and the dangling-link guard (`filterDanglingLinks`) so the logic is exercised without a live backend — mirror `kg.test.ts`'s `mapKgSummaryEvent` approach. Write `describe` blocks covering GH-01a (selectVersionDeletes incl. the empty, under-keepN, and over-keepN + unsorted cases), GH-01b (dispatch mapping fallbacks), GH-01c (dangling-link drop/keep), GH-01d (community null + numeric), GH-01e (generatedAt float passthrough). Mark the live DB round-trip (idempotent re-POST, real activeVersion increment) as `it.todo(...)` — it is exercised manually in plan 03. Keep assertions concrete (`toEqual`/`toBe`), no subjective language.
  </action>
  <verify>
    <automated>npx vitest run convex/graphSnapshots.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `convex/graphSnapshots.test.ts` imports `selectVersionDeletes` and `GRAPH_SNAPSHOT_KEEP_VERSIONS` from `./graphSnapshots`
    - Test file contains `describe`/`it` blocks for selectVersionDeletes, dispatch-mapping fallbacks, dangling-link guard, community null+numeric, and generatedAt float
    - `npx vitest run convex/graphSnapshots.test.ts` exits 0 with all non-todo tests passing
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Pure-logic tests for GH-01a..e are green; DB round-trip deferred to plan 03 via it.todo; type-check clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Ástríðr producer → /runtime-ingest httpAction | Untrusted-shaped (though bearer-authed, server-to-server) payload crosses here and is dispatched to the internal upsert |
| Browser → public read queries | Unauthenticated browser reads getProjectGraph / listSnapshots (intentional graceful-skip surface) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-83-01 | Spoofing | /runtime-ingest ingest | mitigate | Existing `validateIngestAuth` bearer gate (runtimeIngest.ts L19-22) runs BEFORE the switch — the new `graph_snapshot` case adds no bypass; reuse only, no new auth surface |
| T-83-02 | Elevation of Privilege | upsertGraphSnapshot / sweepGraphSnapshotVersions | mitigate | Both declared `internalMutation` (`internal.*`), not callable from the browser; only reachable from the httpAction and the cron scheduler (Pitfall 1) |
| T-83-03 | Tampering | Dangling-link references in payload | mitigate | Receiver builds a Set of stored node ids and drops links with a missing endpoint before insert (D-05); prevents a corrupt graph that would crash Phase 84's renderer (Pitfall 3) |
| T-83-06 | Denial of Service | Retention sweep delete volume | mitigate | Sweep deletes at most one stale version (~13,500 rows) per invocation with a doc-count guard, staying under the 16,000-doc/mutation write limit (Pitfall 5) |
| T-83-07 | Information Disclosure | Public read queries (no Clerk gating) | accept | Read path is intentionally public graceful-skip, consistent with `kg.latestSummary` / `forge.listJobs`; whole surface sits behind AuthGuard; graph data is non-secret operational telemetry. Accepted, low severity (per planning_context security note). |
| T-83-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this phase (RESEARCH "Package Legitimacy Audit: not applicable") — no install task to gate |
</threat_model>

<verification>
- `npx vitest run convex/graphSnapshots.test.ts` green (GH-01a..e).
- `npx tsc --noEmit` clean across all four modified files.
- `npm test` green at wave merge.
- Behavioral note: the live idempotent re-POST / activeVersion-increment round-trip is intentionally deferred to plan 03 (requires a Convex backend + bearer POST) and is recorded as `it.todo` here.
</verification>

<success_criteria>
- graph_snapshot dispatch routes to `internal.graphSnapshots.upsertGraphSnapshot`; the event is no longer dropped.
- Versioned swap, dangling-link guard, public graceful-skip reads, and one-version-per-sweep retention all implemented and type-clean.
- Pure-logic tests green; cron registered.
</success_criteria>

<output>
Create `.planning/phases/83-graph-snapshot-receiver/83-02-SUMMARY.md` when done.
</output>
