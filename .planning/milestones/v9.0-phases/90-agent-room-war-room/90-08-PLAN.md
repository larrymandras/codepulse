---
phase: 90-agent-room-war-room
plan: 08
type: execute
wave: 6
depends_on: ["90-07"]
files_modified: []
autonomous: false
requirements: [ROOM-03, ROOM-04]
user_setup: []

must_haves:
  truths:
    - "The live POST /api/war-room/{room}/token endpoint returns a valid { token, url } for an existing room under Bearer auth"
    - "The operator can join a live war room and hear agents; after explicit unmute, agents hear the operator (two-way voice)"
    - "Transcript ordering is monotonic by seq under genuinely concurrent ingest"
  artifacts: []
  key_links:
    - from: "CodePulse /war-room/:roomId Join"
      to: "live LiveKit room"
      via: "real two-way audio"
      pattern: "connected"
---

<objective>
Operator sign-off on the three manual-only verifications from 90-VALIDATION.md that cannot run in jsdom: the live cross-repo token endpoint, real two-way LiveKit audio, and transcript ordering under concurrent ingest. These confirm ROOM-03 and the ROOM-04 ordering guarantee against the live stack.

Purpose: jsdom mocks the WebRTC media path; only a live LiveKit room + real mic can prove the headline Join feature works.
Output: Operator confirmation (or defect report) for the three manual checks.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/90-agent-room-war-room/90-VALIDATION.md
@.planning/phases/90-agent-room-war-room/90-RESEARCH.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Live-stack manual verification of Join, token endpoint, and ordering</name>
  <action>PAUSE for operator. Ensure the prerequisites below are running, then walk the operator through the three manual checks and wait for sign-off; do not mark the phase verified until all three pass or defects are recorded.</action>
  <what-built>
    Plans 01-07 shipped: the astridr-repo `POST /api/war-room/{room_name}/token` endpoint (validated, Bearer-protected), the `useWarRoomVoice` LiveKit hook (join muted, subscribe agent audio, clean teardown), the connection-state VoiceControlBar, real identity, bounded listing, deep-link, and seq-ordered transcript. The full Vitest suite is green. These three behaviors require the live stack and could not be automated in jsdom.
  </what-built>
  <how-to-verify>
    Prerequisite: Ástríðr running with a reachable LiveKit server; `LIVEKIT_PUBLIC_URL` browser-reachable; `VITE_ASTRIDR_API_KEY` set in CodePulse.

    1. Token endpoint (ROOM-03 / D-02): start a war room via Ástríðr, then
       `curl -X POST "$VITE_ASTRIDR_API_URL/api/war-room/<live-room>/token" -H "Authorization: Bearer $VITE_ASTRIDR_API_KEY" -H "Content-Type: application/json" -d '{"identity":"operator"}'`
       → expect HTTP 200 + JSON `{ token, url }`. Also confirm a malformed name (e.g. `../foo`) returns HTTP 400.
    2. Two-way voice (ROOM-03): open `/war-room/<live-room>` in CodePulse, click "Join Voice". Confirm the connection-state reaches `connected` (status dot), you HEAR the agents, and you join MUTED (amber mic). Click unmute and confirm the agents respond to your speech. Select a different room and confirm the previous room's audio STOPS (no leak).
       Identity check (ROOM-01 / Assumption A2): confirm at least one participant card shows a REAL roster agent name (e.g. "astridr"/"hervor"), NOT the "Agent #xxxx" fallback. If every card is "Agent #xxxx", the `participantId`→roster mapping (D-04) is mismatched — record as a defect and route to `/gsd:plan-phase 90 --gaps`.
    3. Transcript ordering (ROOM-04): with the room active, let several transcript chunks arrive (or fire concurrent `/transcript-ingest` posts). Confirm the rendered transcript order is monotonic and shows no duplicated utterances.
  </how-to-verify>
  <resume-signal>Type "approved" if all three pass, or describe defects (which step, observed vs expected) to spawn gap-closure.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → live LiveKit room | Real two-way audio into a live agent boardroom |
| CodePulse → astridr token endpoint | Live Bearer-authed cross-repo call |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-90-PATH | Elevation of Privilege | live token endpoint | mitigate | Manual step 1 confirms malformed room_name returns 400 against the live endpoint (validates Plan 04 mitigation end-to-end). |
| T-90-MIC | Spoofing / unintended broadcast | live join | mitigate | Manual step 2 confirms join is muted by default and only broadcasts after explicit unmute (validates D-03 end-to-end). |
| T-90-ORD | Tampering | live concurrent ingest | mitigate | Manual step 3 confirms monotonic seq order + no duplicates under real concurrency (validates Plan 03 OCC seq end-to-end). |
</threat_model>

<verification>
- Operator confirms all three manual checks pass against the live Ástríðr + LiveKit stack.
- Any defect routes to `/gsd:plan-phase 90 --gaps`.
</verification>

<success_criteria>
Operator has confirmed: live token endpoint returns {token,url} and rejects malformed names; real two-way audio works with muted-by-default join and no cross-room leak; transcript ordering is monotonic under concurrent ingest. ROOM-03 + ROOM-04 verified end-to-end.
</success_criteria>

<output>
Create `.planning/phases/90-agent-room-war-room/90-08-SUMMARY.md` when done.
</output>
