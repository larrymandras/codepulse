---
phase: 90-agent-room-war-room
plan: 07
type: execute
wave: 5
depends_on: ["90-04", "90-06"]
files_modified:
  - src/pages/WarRoom.tsx
autonomous: true
requirements: [ROOM-03, ROOM-04]
user_setup: []

must_haves:
  truths:
    - "Clicking Join triggers a real LiveKit connection via useWarRoomVoice (not the cosmetic setIsJoined(true))"
    - "Selecting a different room disconnects the prior LiveKit room (no leaked agent audio)"
    - "VoiceControlBar receives the live connectionState; mute/leave map to the hook"
    - "A closed/non-existent room renders the read-only 'Room Ended' state with Join disabled, full transcript preserved"
    - "Transcript renders deterministically by seq, merging persisted + live chunks without duplicates"
  artifacts:
    - path: "src/pages/WarRoom.tsx"
      provides: "real Join wiring + closed-room read-only state + seq transcript merge"
      contains: "useWarRoomVoice"
  key_links:
    - from: "src/pages/WarRoom.tsx"
      to: "src/hooks/useWarRoomVoice.ts"
      via: "join/leave/toggleMute + connectionState"
      pattern: "useWarRoomVoice"
    - from: "src/pages/WarRoom.tsx room-change effect"
      to: "voice.leave()"
      via: "disconnect LiveKit on selectedRoomId change"
      pattern: "leave"
---

<objective>
Complete the War Room (same file as Plan 06, sequential): wire the genuine operator Join via `useWarRoomVoice` (ROOM-03) including disconnect-on-room-change, render the closed-room read-only "Room Ended" state (ROOM-04 / D-06), and finalize deterministic seq-ordered transcript merge with live-chunk dedup (ROOM-04 / D-07). Turns the closed-room case of `src/pages/WarRoom.test.tsx` GREEN and completes the phase's automated surface.

Purpose: Replace cosmetic join with a real, leak-free LiveKit connection and preserve archived transcripts on dead links.
Output: Real Join wiring, Room Ended state, seq transcript merge.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/90-agent-room-war-room/90-PATTERNS.md
@.planning/phases/90-agent-room-war-room/90-RESEARCH.md
@.planning/phases/90-agent-room-war-room/90-UI-SPEC.md

<interfaces>
<!-- Consumed contracts from Plan 04/03/06. -->
useWarRoomVoice() → { connectionState, isMuted, join(roomName), leave(), toggleMute() } (Plan 04).
VoiceControlBarProps now include connectionState + onRetry (Plan 04).
WarRoom.tsx (post Plan 06): handleJoin/Leave/ToggleMute (112-114) still cosmetic; room-change reset effect (43-47) sets flags only; transcript merge consumes seq-ordered roomEvents.
Surface D (UI-SPEC): closed room → StatusBadge "ended", "Room Ended" notice bar (bg-(--status-warn)/10, AlertCircle), cards opacity-50 + non-interactive + isSpeaking false, disabled "Join Voice" with Tooltip "This room has ended", TranscriptPanel live={false}.
Live chunk dedup (RESEARCH Transcript Merge): exclude a live chunk if a persisted event shares timestamp + speakerId.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire real Join via useWarRoomVoice + disconnect on room change</name>
  <read_first>
    - src/pages/WarRoom.tsx (handlers 112-114; room-change effect 43-47; VoiceControlBar usage)
    - src/hooks/useWarRoomVoice.ts (Plan 04 hook)
    - src/components/VoiceControlBar.tsx (connectionState + onRetry props from Plan 04)
    - .planning/phases/90-agent-room-war-room/90-PATTERNS.md § WarRoom room reset effect (Pitfall 1); RESEARCH Pattern 1 + Pitfall 1
  </read_first>
  <files>src/pages/WarRoom.tsx</files>
  <action>Call `const voice = useWarRoomVoice()`. Replace cosmetic `handleJoin`/`handleLeave`/`handleToggleMute` (112-114): `handleJoin` → `voice.join(selectedRoom.roomId)`; `handleLeave` → `voice.leave()`; `handleToggleMute` → `voice.toggleMute()`. Drive `VoiceControlBar` from the hook: `isJoined = voice.connectionState !== 'disconnected'`, `isMuted = voice.isMuted`, `connectionState={voice.connectionState}`, `onRetry` re-invokes join. In the room-change effect (43-47), call `void voice.leave()` (in addition to clearing live chunks) so changing rooms disconnects the LiveKit Room — fixes the Pitfall 1 audio leak; remove the now-unused `isJoined`/`isMuted` local state if fully replaced by the hook. Gate Join so it cannot fire when no room is selected or the room is closed.</action>
  <verify>
    <automated>grep -nE "useWarRoomVoice|voice.join|voice.leave|connectionState" src/pages/WarRoom.tsx && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Join/Leave/Mute map to `useWarRoomVoice`; `VoiceControlBar` receives live `connectionState`.
    - Room-change effect calls `voice.leave()` (no leaked audio on room switch).
    - No cosmetic `setIsJoined(true)` path remains.
    - `npx tsc --noEmit` passes.
  </acceptance_criteria>
  <done>Real Join wired; room-change disconnects LiveKit; tsc clean.</done>
</task>

<task type="auto">
  <name>Task 2: Closed-room read-only "Room Ended" state (D-06 / Surface D)</name>
  <read_first>
    - src/pages/WarRoom.tsx (room detail render; empty-state 150-153; StatusBadge usage)
    - src/components/ui/tooltip.tsx (Radix Tooltip primitive)
    - .planning/phases/90-agent-room-war-room/90-UI-SPEC.md § Surface D + § Copywriting Contract
  </read_first>
  <files>src/pages/WarRoom.tsx</files>
  <action>When the selected room's `status !== "active"` (closed/idle/ended) OR a deep-link `roomId` resolves to no room, render the read-only archive view (NOT a redirect/404 — D-06): a "Room Ended" notice bar between the room header and the cards grid (`py-2 px-4 bg-(--status-warn)/10 border-b border-(--status-warn)/20`, `AlertCircle` h-4 w-4 in `text-(--status-warn)`, copy "Room Ended" + " — This session has ended. The transcript is preserved below."); agent cards at `opacity-50`, `isSpeaking` forced false, non-interactive; `VoiceControlBar` shows a DISABLED "Join Voice" wrapped in a Radix `Tooltip` "This room has ended" (no "You'll join muted" sub-label, no connection-state UI); `TranscriptPanel` with `live={false}` and auto-scroll to newest on load. Use `var(--status-*)` tokens and Lucide icons only. For a non-existent deep-link room, render the same ended view with the room name from the URL param.</action>
  <verify>
    <automated>grep -nE "Room Ended|This room has ended|status-warn" src/pages/WarRoom.tsx && npx vitest run src/pages/WarRoom.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Closed/non-existent room renders "Room Ended" banner + disabled Join (tooltip "This room has ended"); transcript preserved, `live={false}`.
    - Cards are dimmed and non-interactive; no speaking ring.
    - `WarRoom.test.tsx` closed-room case GREEN; only `var(--status-*)` tokens + Lucide icons used.
  </acceptance_criteria>
  <done>Read-only ended state renders; closed-room test green.</done>
</task>

<task type="auto">
  <name>Task 3: Deterministic seq transcript merge + live-chunk dedup</name>
  <read_first>
    - src/pages/WarRoom.tsx (transcript merge 93-105; live chunks subscription)
    - .planning/phases/90-agent-room-war-room/90-PATTERNS.md § WarRoom transcript merge with seq; RESEARCH § Transcript Merge with Seq Dedup + Anti-patterns
  </read_first>
  <files>src/pages/WarRoom.tsx</files>
  <action>Finalize the transcript merge: map persisted `roomEvents` (already seq-ordered from getRoomEvents) to `TranscriptChunk` carrying `seq: e.seq`; append only live chunks that are NOT already persisted (exclude a live chunk where some persisted event shares the same `timestamp` AND `speakerId` — RESEARCH dedup). Persisted events are the source of truth on reload (live chunks cleared on room change). Ensure the rendered order is persisted-by-seq first, then un-persisted live chunks appended (live chunks have no seq). Keep `agentColor` from `resolveAgentColor` (Plan 06).</action>
  <verify>
    <automated>grep -nE "seq:|filter\(lc|some\(" src/pages/WarRoom.tsx && npx tsc --noEmit && npm test 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Transcript merges persisted (seq-ordered) + live chunks with timestamp+speakerId dedup; no duplicate utterances.
    - `npx tsc --noEmit` passes; full suite green (all five Wave-0 test files now GREEN).
  </acceptance_criteria>
  <done>Seq-deterministic transcript merge with dedup; full suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator mic → live agent room | Audio broadcast after explicit unmute |
| room switch → LiveKit Room lifecycle | Stale connection could leak audio across rooms |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-90-MIC | Spoofing / unintended broadcast | Join wiring | mitigate | Join goes through the muted-by-default hook (Plan 04); WarRoom never forces unmute; closed rooms disable Join entirely (Task 1/2, D-03). |
| T-90-LEAK | Information Disclosure | room change while joined | mitigate | Room-change effect calls `voice.leave()` → `room.disconnect()` + audio-element teardown (Task 1, Pitfall 1) so prior-room agent audio stops. |
| T-90-ORD | Tampering | transcript render order | mitigate | Render by server-assigned seq with timestamp+speakerId dedup (Task 3); concurrent ingest cannot reorder or duplicate. |
</threat_model>

<verification>
- `npx vitest run src/pages/WarRoom.test.tsx` GREEN (auto-select + closed-room).
- `npm test` full suite GREEN (all Wave-0 tests turned green across Plans 03-07).
- `npx tsc --noEmit` passes; grep confirms useWarRoomVoice wiring, voice.leave() on room change, Room Ended state, seq merge.
</verification>

<success_criteria>
Join is a real leak-free LiveKit connection driven by useWarRoomVoice; closed/non-existent deep-links render the read-only Room Ended archive with Join disabled; transcript renders deterministically by seq with dedup. ROOM-03 + ROOM-04 WarRoom surface complete; full automated suite green. Live two-way audio verified in Plan 08.
</success_criteria>

<output>
Create `.planning/phases/90-agent-room-war-room/90-07-SUMMARY.md` when done.
</output>
