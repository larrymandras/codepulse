---
phase: 90-agent-room-war-room
plan: 03
type: execute
wave: 3
depends_on: ["90-01", "90-02"]
files_modified:
  - convex/warRoom.ts
  - convex/v6Mutations.ts
autonomous: true
requirements: [ROOM-02, ROOM-04]
user_setup: []

must_haves:
  truths:
    - "listRooms returns a bounded { active, closed, hasMore } structure — no unbounded .collect() on closed history"
    - "Active rooms always appear; idle rooms are treated as closed in the bounded section"
    - "insertWarRoomEvent assigns a monotonic per-room seq computed server-side (race-free under Convex OCC)"
    - "getRoomEvents returns events ordered by seq via by_room_seq"
  artifacts:
    - path: "convex/warRoom.ts"
      provides: "bounded listRooms + seq-ordered getRoomEvents"
      contains: "hasMore"
    - path: "convex/v6Mutations.ts"
      provides: "seq-assigning insertWarRoomEvent"
      contains: "by_room_seq"
  key_links:
    - from: "convex/v6Mutations.ts insertWarRoomEvent"
      to: "warRoomEvents by_room_seq index"
      via: "read max seq then insert seq=max+1"
      pattern: "by_room_seq"
    - from: "convex/warRoom.ts listRooms"
      to: "warRooms by_status index"
      via: "withIndex(by_status).take(limit+1)"
      pattern: "by_status"
---

<objective>
Harden the War Room Convex layer: replace the unbounded `listRooms` `.collect()` with a bounded `{ active, closed, hasMore }` query (ROOM-02), and add deterministic ordering (ROOM-04) by computing a monotonic per-room `seq` in `insertWarRoomEvent` and switching `getRoomEvents` to the `by_room_seq` index. Turns `convex/warRoom.test.ts` and `convex/v6Mutations.test.ts` GREEN.

Purpose: Eliminate the 16 MiB read-scan risk on a growing table and guarantee transcript order under concurrent ingest.
Output: Bounded listing, seq-assigning mutation, seq-ordered reads.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/90-agent-room-war-room/90-RESEARCH.md
@.planning/phases/90-agent-room-war-room/90-PATTERNS.md

<interfaces>
<!-- Bounded-read idiom (self-analog) + OCC seq precedent. -->
getRoomEvents (convex/warRoom.ts:13-21) uses `.withIndex(...).order(...).take(N)` — copy this idiom into listRooms.
warRooms.by_status index = ["status","createdAt"] (schema.ts:1288). Status domain: "active" | "idle" | "closed".
Forge OCC seq precedent: convex/forge.ts:634-641 reads max via by_host_job_seq then inserts (serializable; conflicting concurrent reads retry → gap-free).
warRoomEvents.by_room_seq index = ["roomId","seq"] (added in Plan 01). seq is v.optional(v.number()); missing-seq legacy rows sort before seq=0 under order("asc").
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Bounded listRooms { active, closed, hasMore }</name>
  <read_first>
    - convex/warRoom.ts (listRooms 4-11 unbounded; getRoomEvents 13-21 bounded idiom)
    - .planning/phases/90-agent-room-war-room/90-PATTERNS.md § convex/warRoom.ts (replacement listRooms) + Critical Note N6
    - .planning/phases/90-agent-room-war-room/90-RESEARCH.md Pattern 5 + Pitfall 4
  </read_first>
  <files>convex/warRoom.ts</files>
  <behavior>
    - listRooms() returns `{ active: Room[], closed: Room[], hasMore: boolean }`.
    - active = all rooms with status "active" (collect; expected < 20 concurrent).
    - closed section = non-active rooms ("closed" AND "idle", per N6) ordered most-recent first, bounded to `closedLimit` (default 20, hard cap 200).
    - hasMore = true iff more closed/idle rooms exist beyond the limit (take limit+1 to detect).
  </behavior>
  <action>Replace `listRooms` (lines 4-11). Add arg `closedLimit: v.optional(v.float64())` defaulting to 20; clamp with `Math.min(closedLimit, 200)`. Query active via `withIndex("by_status", q => q.eq("status","active")).order("desc").collect()`. For the bounded closed section, query BOTH `"closed"` and `"idle"` via `by_status` (each `.order("desc").take(limit+1)`), merge, sort by `createdAt` desc, then slice to `limit`; compute `hasMore` from whether the merged pre-slice length exceeds `limit` (treat idle as closed — Critical Note N6 / Open Question 2). Return `{ active, closed, hasMore }`. Remove the `.collect()` over the whole table.</action>
  <verify>
    <automated>npx vitest run convex/warRoom.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `convex/warRoom.test.ts` listing-shape + listing-hasmore tests GREEN.
    - No unbounded `.collect()` over the full warRooms table remains in listRooms (active collect of status="active" only is allowed).
    - Idle rooms appear in the closed section.
  </acceptance_criteria>
  <done>Bounded listRooms returns {active,closed,hasMore}; tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Seq-assigning insertWarRoomEvent + seq-ordered getRoomEvents</name>
  <read_first>
    - convex/v6Mutations.ts (insertWarRoomEvent 151-164)
    - convex/forge.ts:623-649 (OCC read-max-then-insert precedent)
    - convex/warRoom.ts getRoomEvents (13-21)
    - .planning/phases/90-agent-room-war-room/90-PATTERNS.md § insertWarRoomEvent (seq-computing replacement) + § getRoomEvents (seq index)
  </read_first>
  <files>convex/v6Mutations.ts, convex/warRoom.ts</files>
  <behavior>
    - insertWarRoomEvent computes seq server-side: read the latest event for roomId via by_room_seq (order desc, first); seq = (last.seq ?? -1) + 1; insert with that seq. seq is NOT an input arg.
    - First event in a room → seq 0; subsequent → strictly increasing unique seq per room.
    - getRoomEvents returns events ordered ascending by seq via by_room_seq.
  </behavior>
  <action>In `insertWarRoomEvent`, do not add `seq` to args; in the handler, query `warRoomEvents` `.withIndex("by_room_seq", q => q.eq("roomId", args.roomId)).order("desc").first()`, compute `seq = (lastEvent?.seq ?? -1) + 1`, and insert `{ ...args, seq }` (OCC makes read-max-then-insert race-free — mirror forge.ts; no idempotency dedup needed since each transcript chunk is distinct). Switch `getRoomEvents` to `.withIndex("by_room_seq", q => q.eq("roomId", roomId)).order("asc").take(limit ?? 500)` so reads are seq-ordered (legacy rows with missing seq sort before seq=0, acceptable per Pitfall 5).</action>
  <verify>
    <automated>npx vitest run convex/v6Mutations.test.ts convex/warRoom.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `convex/v6Mutations.test.ts` seq-assign + seq-unique tests GREEN.
    - `convex/warRoom.test.ts` getRoomEvents ascending-seq ordering test GREEN.
    - `insertWarRoomEvent` reads max via `by_room_seq` and inserts seq=max+1; seq is not a public arg.
    - `getRoomEvents` orders by `by_room_seq`.
    - `npx tsc --noEmit` passes.
  </acceptance_criteria>
  <done>seq assigned race-free; reads seq-ordered; tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ingest httpAction → mutation | Untrusted Ástríðr event payload reaches the seq-assigning mutation |
| client useQuery → Convex | Read-only room/transcript queries |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-90-DOS | Denial of Service | listRooms unbounded scan | mitigate | Bounded `take(limit+1)` with hard cap 200 replaces full-table `.collect()` (Task 1). |
| T-90-ORD | Tampering | concurrent transcript ingest order | mitigate | Server-side monotonic seq under Convex OCC; conflicting concurrent reads retry → gap-free ordering (Task 2). |
| T-90-INJ | Tampering | insertWarRoomEvent args | accept | Args are `v.`-validated; seq is server-computed (not client-supplied), so clients cannot forge ordering. |
</threat_model>

<verification>
- `npx vitest run convex/warRoom.test.ts convex/v6Mutations.test.ts` all GREEN.
- `npx tsc --noEmit` passes.
- `grep by_room_seq convex/v6Mutations.ts convex/warRoom.ts` confirms index usage.
</verification>

<success_criteria>
listRooms bounded to {active,closed,hasMore} with idle-as-closed; insertWarRoomEvent assigns race-free monotonic per-room seq; getRoomEvents seq-ordered. ROOM-02 + ROOM-04 backend tests green.
</success_criteria>

<output>
Create `.planning/phases/90-agent-room-war-room/90-03-SUMMARY.md` when done.
</output>
