---
phase: 84-graphs-hub-code-vault-render
plan: 03
type: execute
wave: 3
depends_on: ["84-02"]
files_modified:
  - src/pages/GraphsHub.tsx
  - src/pages/GraphsHub.test.tsx
  - src/App.tsx
  - src/layouts/DashboardLayout.tsx
autonomous: true
requirements: [GH-03]

must_haves:
  truths:
    - "Navigating to /graphs renders a page with three live summary tiles (Tool Galaxy, MCP Inventory, KG Explorer) above the code/vault hero"
    - "Each summary tile click-navigates to its standalone route and shows a live metric from its existing hook"
    - "The 'Graphs Hub' nav entry is no longer a placeholder:true stub — it routes to /graphs and auto-registers in the CommandPalette"
    - "The code/vault hero (CodeVaultGraph) is the inline hero of the page, wrapped in SectionErrorBoundary + GlassPanel"
  artifacts:
    - path: "src/pages/GraphsHub.tsx"
      provides: "Route page: summary tile row + CodeVaultGraph hero, thin composition (HivePage pattern)"
      contains: "CodeVaultGraph"
      min_lines: 60
    - path: "src/App.tsx"
      provides: "Lazy /graphs route"
      contains: "/graphs"
    - path: "src/layouts/DashboardLayout.tsx"
      provides: "Real Graphs Hub nav entry (placeholder flipped)"
      contains: "to: \"/graphs\""
  key_links:
    - from: "src/pages/GraphsHub.tsx"
      to: "CodeVaultGraph"
      via: "hero composition inside SectionErrorBoundary + GlassPanel"
      pattern: "<CodeVaultGraph"
    - from: "src/pages/GraphsHub.tsx"
      to: "useToolGalaxySources / useMcpHealthSources / useKgSummary"
      via: "tile metric derivation"
      pattern: "useKgSummary\\("
    - from: "src/App.tsx"
      to: "GraphsHub"
      via: "lazy route /graphs"
      pattern: "path=\"/graphs\""
    - from: "src/layouts/DashboardLayout.tsx"
      to: "/graphs"
      via: "nav entry placeholder flip"
      pattern: "to: \"/graphs\""
---

<objective>
Assemble and wire the hub (GH-03). Compose the `GraphsHub` page (three live summary tiles + the `CodeVaultGraph` hero), register the lazy `/graphs` route in `App.tsx`, and flip the `placeholder:true` "Graphs Hub" nav entry in `DashboardLayout.tsx` to a real `to: "/graphs"` route.

Purpose: This turns the four graph surfaces into one reachable hub and removes the dead nav stub — the unification that defines GH-03. It depends on `CodeVaultGraph` (plan 02) as the hero.
Output: 1 page + its filled-in test (turns plan 01 GraphsHub todos green) + 2 one-line wiring edits.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/84-graphs-hub-code-vault-render/84-CONTEXT.md
@.planning/phases/84-graphs-hub-code-vault-render/84-RESEARCH.md
@.planning/phases/84-graphs-hub-code-vault-render/84-PATTERNS.md
@.planning/phases/84-graphs-hub-code-vault-render/84-UI-SPEC.md
@.planning/phases/84-graphs-hub-code-vault-render/84-VALIDATION.md
@src/components/graph/CodeVaultGraph.tsx

<interfaces>
<!-- Tile hooks — VERIFIED. -->
<!-- useToolGalaxySources(): { tools, mcpServers, edges, kits, loading } (src/hooks/useToolGalaxy.ts) -->
<!--   counts: call buildGalaxy({tools,mcpServers,edges,kits,agentFilter:null,mcpFilter:null,now:Date.now()/1000}).stats -->
<!--   → stats.toolCount, stats.orphanCount  (src/lib/tool-galaxy.ts) -->
<!-- useMcpHealthSources(): { mcpServers, tools, edges, governance, loading } (src/hooks/useMcpHealth.ts) -->
<!--   counts: buildMcpHealth(...) → model.serverCount, model.errorCount (src/lib/mcp-health.ts:117-127), -->
<!--   or simpler mcpServers.length / mcpServers.filter(s => s.status === "error").length -->
<!-- useKgSummary(): { summary: { totalEntities, currentTripleCount, ... } | null, loading } (src/hooks/useKgSummary.ts) -->

<!-- MetricCard — VERIFIED src/components/MetricCard.tsx:53-64. value: string|number; onClick?: () => void. -->

<!-- Nav GRAPHS group — VERIFIED src/layouts/DashboardLayout.tsx ~L156. -->
<!-- Before: { label: "Graphs Hub", icon: "network", group: "GRAPHS", placeholder: true } -->
<!-- After:  { to: "/graphs", label: "Graphs Hub", icon: "network", group: "GRAPHS" } -->
<!-- "network" → Network is already in iconComponents (~L105); navItems auto-excludes placeholder:true (~L204-216). -->

<!-- App.tsx lazy graph routes ~L51-71 (declarations) and ~L118-123 (routes); add /graphs FIRST in the GRAPHS cluster. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: GraphsHub page — summary tile row + CodeVaultGraph hero + tile test</name>
  <files>src/pages/GraphsHub.tsx, src/pages/GraphsHub.test.tsx</files>
  <read_first>
    - src/pages/HivePage.tsx (canonical new-page composition — imports L1-17, page structure L19-63, SectionErrorBoundary + GlassPanel usage)
    - src/pages/ToolGalaxy.tsx (MetricCard usage with value/onClick; InfoTooltip placement)
    - src/components/MetricCard.tsx lines 53-64 (props: label, value, onClick)
    - src/hooks/useToolGalaxy.ts, src/hooks/useMcpHealth.ts, src/hooks/useKgSummary.ts (tile metric sources)
    - src/lib/mcp-health.ts lines 117-127 (serverCount/errorCount on the built model) — or derive from mcpServers array
    - src/components/graph/CodeVaultGraph.tsx (the hero this page mounts)
    - src/test/projectGraphFixture.ts (mock for the hero so the page test renders)
    - .planning/phases/84-graphs-hub-code-vault-render/84-UI-SPEC.md (Layout Contract, Summary tile copy table, Icon Assignments)
    - .planning/phases/84-graphs-hub-code-vault-render/84-RESEARCH.md (Pitfall 1 — tile metric derivation; summary tile composition example)
  </read_first>
  <behavior>
    - Test 1 (GH-03 tiles): the page renders three MetricCard tiles labelled TOOL GALAXY, MCP INVENTORY, KG EXPLORER.
    - Test 2 (GH-03 nav): clicking each tile triggers navigation to `/tool-galaxy`, `/mcp-inventory`, `/knowledge-graph` respectively (assert via a mocked `useNavigate`).
  </behavior>
  <action>
    Create `src/pages/GraphsHub.tsx` as the default export (HivePage structure). Page wrapper `div className="space-y-6 p-6"`. Header row: H1 `text-[10px] font-mono uppercase tracking-widest font-bold text-primary` with `Network` icon (h-5 w-5 text-primary) + label "GRAPHS HUB" + `InfoTooltip` with the UI-SPEC body ("Ástríðr's code, vault, and tool graphs — unified..."). Summary tile row: `grid grid-cols-1 md:grid-cols-3 gap-4`, each tile its OWN `SectionErrorBoundary` (named per UI-SPEC) wrapping a `MetricCard` — NOT a shared GlassPanel (MetricCard has its own surface). Derive tile metrics per Pitfall 1: Tool Galaxy via `useToolGalaxySources()` → `buildGalaxy({tools,mcpServers,edges,kits,agentFilter:null,mcpFilter:null,now:Date.now()/1000}).stats` for `toolCount`/`orphanCount`, value string ``${toolCount} tools · ${orphanCount} orphans``, onClick → `navigate("/tool-galaxy")`; MCP Inventory via `useMcpHealthSources()` → `serverCount`/`errorCount` (from `buildMcpHealth` model or `mcpServers.length` + `mcpServers.filter(s => s.status === "error").length`), value ``${serverCount} servers · ${errorCount} errors``, onClick → `navigate("/mcp-inventory")`; KG Explorer via `useKgSummary()` → `summary?.totalEntities ?? 0` / `summary?.currentTripleCount ?? 0`, value ``${entities} entities · ${triples} triples``, onClick → `navigate("/knowledge-graph")`. Use `useNavigate` from `react-router-dom`. Hero: `SectionErrorBoundary name="Code/Vault Graph"` → `GlassPanel className="rounded-xl"` → `<CodeVaultGraph />` (the hero reads its own data via useProjectGraph — pass no graph props). Tiles must keep rendering independently when the snapshot is null (D-12 — they use their own hooks). All icons Lucide (Network, Boxes, Server, Share2 per UI-SPEC Icon Assignments).
    Fill in `src/pages/GraphsHub.test.tsx` (created in plan 01) — convert the GraphsHub todos to real assertions for the two behaviors above. Mock `react-router-dom`'s `useNavigate`; mock the tile hooks (or let them resolve to empty via the Convex mock); mock `useProjectGraph`/`getProjectGraph` via `src/test/projectGraphFixture.ts` so the hero renders without a live backend.
  </action>
  <verify>
    <automated>npx vitest run src/pages/GraphsHub.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `src/pages/GraphsHub.tsx` contains `<CodeVaultGraph` and three `MetricCard` usages with labels `TOOL GALAXY`, `MCP INVENTORY`, `KG EXPLORER`.
    - File contains `navigate("/tool-galaxy")`, `navigate("/mcp-inventory")`, and `navigate("/knowledge-graph")`.
    - Each tile is wrapped in its own `SectionErrorBoundary`; the hero is wrapped in `SectionErrorBoundary` + `GlassPanel`.
    - `npx vitest run src/pages/GraphsHub.test.tsx` exits 0 with both GH-03 behaviors passing (no remaining `it.todo`/`skip` for them).
    - `npx tsc --noEmit` reports no new errors.
  </acceptance_criteria>
  <done>GraphsHub renders three live click-through tiles above the CodeVaultGraph hero; both GH-03 tile behaviors green.</done>
</task>

<task type="auto">
  <name>Task 2: Wire /graphs route + flip the nav placeholder</name>
  <files>src/App.tsx, src/layouts/DashboardLayout.tsx</files>
  <read_first>
    - src/App.tsx lines 51-71 (lazy graph-route declarations) and lines 118-123 (graph-route registrations) — add /graphs FIRST in the GRAPHS cluster
    - src/layouts/DashboardLayout.tsx ~L154-161 (GRAPHS group) + ~L105 (iconComponents map, confirm "network" → Network) + ~L204-216 (navItems placeholder auto-exclude)
    - .planning/phases/84-graphs-hub-code-vault-render/84-PATTERNS.md (§ src/App.tsx edit + § src/layouts/DashboardLayout.tsx edit — exact before/after)
  </read_first>
  <action>
    In `src/App.tsx`: add `const GraphsHub = lazy(() => import("./pages/GraphsHub"));` alongside the sibling graph lazy declarations (after the KnowledgeGraph/HivePage block ~L71), with a `// Phase 84: Graphs Hub` comment. Add a `<Route path="/graphs" element={<Suspense fallback={<div className="text-muted-foreground text-sm p-8 text-center">Loading Graphs Hub...</div>}><GraphsHub /></Suspense>} />` immediately before the existing GRAPHS-cluster routes (~L118) so the hub is first in the cluster.
    In `src/layouts/DashboardLayout.tsx`: change the GRAPHS group entry (~L156) from `{ label: "Graphs Hub", icon: "network", group: "GRAPHS", placeholder: true }` to `{ to: "/graphs", label: "Graphs Hub", icon: "network", group: "GRAPHS" }` — drop the `placeholder: true` flag and add `to: "/graphs"`. Confirm `iconComponents` already maps `"network"` → `Network` (~L105); if missing, add it. The `navItems` auto-exclude (checks `placeholder`/`!to`) then auto-registers the route for the CommandPalette — no further edit. Keep the entry first in the GRAPHS group.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `src/App.tsx` contains `lazy(() => import("./pages/GraphsHub"))` and `path="/graphs"`.
    - `src/layouts/DashboardLayout.tsx` GRAPHS group contains `to: "/graphs"` for the "Graphs Hub" entry and that entry no longer contains `placeholder: true`.
    - `src/layouts/DashboardLayout.tsx` `iconComponents` maps `"network"` → `Network`.
    - `npx tsc --noEmit` clean; `npm run build` succeeds (route + lazy import resolve).
  </acceptance_criteria>
  <done>/graphs is a real lazy route; the Graphs Hub nav stub is a live link that auto-registers in CommandPalette; build green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Convex queries → browser page | The hub composes public read queries only (tile hooks + getProjectGraph via the hero). No user input crosses to the backend; no secrets; no mutation; no new route guard. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-84-04 | Information Disclosure | /graphs page + summary tiles | accept | Read-only composition of public operational telemetry (tile counts, graph snapshot) — same public-read posture as existing graph pages (KnowledgeGraph, ToolGalaxy, McpInventory are already unauthenticated). The nav flip exposes no new data class; the three target routes already exist and are reachable. No L1 high-severity threat. No auth gating introduced (none exists on the sibling routes). |
| T-84-SC | Tampering | npm/pip/cargo installs | mitigate | Zero new packages (RESEARCH.md Package Legitimacy Audit: none). No install step. |
</threat_model>

<verification>
- `npx vitest run src/pages/GraphsHub.test.tsx` exits 0.
- `npx tsc --noEmit` clean; `npm run build` succeeds.
- `npm test` green (full suite) before `/gsd-verify-work`.
- Manual (per 84-VALIDATION.md): in `npm run dev`, click the "Graphs Hub" nav entry → `/graphs` loads; open CommandPalette → "Graphs Hub" listed; tiles click through; hero renders the code/vault graph (or the null explainer with tiles still live).
</verification>

<success_criteria>
- `/graphs` renders three live summary tiles + the code/vault hero.
- Each tile click-navigates to its standalone route.
- The "Graphs Hub" nav entry routes to `/graphs` and is no longer a placeholder stub.
- All four graph surfaces (Tool Galaxy, MCP Inventory, KG Explorer, code/vault) are reachable from the hub.
</success_criteria>

<output>
Create `.planning/phases/84-graphs-hub-code-vault-render/84-03-SUMMARY.md` when done.
</output>
