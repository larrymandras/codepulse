---
phase: 84-graphs-hub-code-vault-render
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/hooks/useProjectGraph.ts
  - src/hooks/useProjectGraph.test.ts
  - src/components/graph/CodeVaultGraph.test.tsx
  - src/pages/GraphsHub.test.tsx
  - src/test/projectGraphFixture.ts
autonomous: true
requirements: [GH-02, GH-03]

must_haves:
  truths:
    - "useProjectGraph returns undefined while Convex loads, null when no snapshot, the data object once a snapshot exists"
    - "A reusable fixture matching the getProjectGraph return shape is available to all three Phase 84 test files"
    - "The three Phase 84 test files exist and run (RED until their implementations land in plans 02/03)"
  artifacts:
    - path: "src/hooks/useProjectGraph.ts"
      provides: "Thin useQuery wrapper over api.graphSnapshots.getProjectGraph; ProjectGraphData type export"
      contains: "useProjectGraph"
    - path: "src/hooks/useProjectGraph.test.ts"
      provides: "Hook three-state coverage (undefined/null/object)"
      contains: "useProjectGraph"
    - path: "src/components/graph/CodeVaultGraph.test.tsx"
      provides: "Wave 0 scaffold for GH-02 render/filter/truncation/freshness/integrity/detail/colorFn"
      contains: "CodeVaultGraph"
    - path: "src/pages/GraphsHub.test.tsx"
      provides: "Wave 0 scaffold for GH-03 tile render + click nav"
      contains: "GraphsHub"
    - path: "src/test/projectGraphFixture.ts"
      provides: "Faithfully-shaped getProjectGraph fixture + getProjectGraph mock helper"
      contains: "makeProjectGraphFixture"
  key_links:
    - from: "src/hooks/useProjectGraph.ts"
      to: "api.graphSnapshots.getProjectGraph"
      via: "useQuery"
      pattern: "useQuery\\(\\s*api\\.graphSnapshots\\.getProjectGraph"
    - from: "src/test/projectGraphFixture.ts"
      to: "convex/_generated/api getProjectGraph shape"
      via: "fixture mirrors snapshotId/sources/nodeCount/.../nodes/links"
      pattern: "storedNodeCount"
---

<objective>
Establish the data-access seam and the Wave 0 test scaffolding for Phase 84. Create the `useProjectGraph` hook (the only net-new data wrapper this phase needs), a faithfully-shaped `getProjectGraph` fixture + mock that all three test files share, and the three empty-but-running test files named in 84-VALIDATION.md.

Purpose: Plans 02 (CodeVaultGraph) and 03 (GraphsHub) build against a defined hook contract and a shared fixture, with their test files already present so verification commands resolve from the first commit (Nyquist sampling continuity).
Output: 1 hook, 1 fixture/mock module, 3 test files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/84-graphs-hub-code-vault-render/84-CONTEXT.md
@.planning/phases/84-graphs-hub-code-vault-render/84-RESEARCH.md
@.planning/phases/84-graphs-hub-code-vault-render/84-PATTERNS.md
@.planning/phases/84-graphs-hub-code-vault-render/84-VALIDATION.md

<interfaces>
<!-- getProjectGraph return shape — VERIFIED from convex/graphSnapshots.ts:236-282. -->
<!-- Returns null before any ingest (graceful-skip). useQuery returns undefined while loading. -->
getProjectGraph({ snapshotId?: string }) -> null | {
  snapshotId: string;            // default "astridr-project-graph"
  sources: Array<{ source: string; kind: string; nodeCount: number; linkCount: number;
                   emittedNodeCount: number; emittedLinkCount: number; truncated: boolean }>;
  nodeCount: number;             // producer-reported total (for "X of Y")
  linkCount: number;
  storedNodeCount: number;       // actual rows stored (after dangling-link drop)
  storedLinkCount: number;
  generatedAt: number;           // Unix SECONDS (float64) — ×1000 before Date.now()
  nodes: Array<{ id: string; label: string; type: string; community: number | null | undefined; source: string }>;
  links: Array<{ source: string; target: string; relation: string }>;
}

<!-- Hook analog — VERIFIED src/hooks/useKgSummary.ts:1-2,27. Do NOT copy the {summary,loading} wrapper; -->
<!-- useProjectGraph returns the RAW useQuery result so callers see all three states (undefined/null/object). -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create useProjectGraph hook + three-state test</name>
  <files>src/hooks/useProjectGraph.ts, src/hooks/useProjectGraph.test.ts</files>
  <read_first>
    - src/hooks/useKgSummary.ts (analog — import lines 1-2; do NOT replicate the {summary,loading} wrapper)
    - src/hooks/useToolGalaxy.ts (secondary analog — useQuery import + Convex undefined→loading convention)
    - convex/graphSnapshots.ts lines 236-282 (the getProjectGraph handler — source of truth for the return shape and the `null` graceful-skip)
    - .planning/phases/84-graphs-hub-code-vault-render/84-PATTERNS.md (§ useProjectGraph "Copy-ready shape")
  </read_first>
  <behavior>
    - Test 1: when useQuery returns `undefined`, the hook returns `undefined` (loading — NOT coerced to null).
    - Test 2: when useQuery returns `null`, the hook returns `null` (no snapshot ingested).
    - Test 3: when useQuery returns a snapshot object, the hook returns that same object reference.
    - Test 4: passing a `snapshotId` forwards `{ snapshotId }` as the query arg; passing nothing forwards `{}`.
  </behavior>
  <action>
    Create `src/hooks/useProjectGraph.ts`. Import `useQuery` from `convex/react` and `api` from `../../convex/_generated/api`. Export `useProjectGraph(snapshotId?: string)` that returns `useQuery(api.graphSnapshots.getProjectGraph, snapshotId ? { snapshotId } : {})` — return the raw result, do NOT coerce `undefined`→`null` (CodeVaultGraph needs to distinguish loading from empty per D-12). Export `type ProjectGraphData = NonNullable<ReturnType<typeof useQuery<typeof api.graphSnapshots.getProjectGraph>>>`. Mirror the useKgSummary import style but NOT its `{ summary, loading }` return wrapper.
    Create `src/hooks/useProjectGraph.test.ts` covering the four behaviors above by mocking `convex/react`'s `useQuery` (`vi.mock("convex/react")`) and asserting the hook returns the mock value verbatim for `undefined`, `null`, and a fixture object, and that the query arg is forwarded correctly.
  </action>
  <verify>
    <automated>npx vitest run src/hooks/useProjectGraph.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/hooks/useProjectGraph.ts` contains `useQuery(api.graphSnapshots.getProjectGraph` and exports `useProjectGraph` and `ProjectGraphData`.
    - `useProjectGraph.ts` does NOT contain a `?? null` or `?? []` coercion on the useQuery result (raw three-state passthrough preserved).
    - `npx vitest run src/hooks/useProjectGraph.test.ts` exits 0.
    - `npx tsc --noEmit` reports no errors introduced by these files.
  </acceptance_criteria>
  <done>Hook returns raw three-state Convex result; four-behavior test green; type exported.</done>
</task>

<task type="auto">
  <name>Task 2: Create shared fixture/mock + scaffold the two component/page test files</name>
  <files>src/test/projectGraphFixture.ts, src/components/graph/CodeVaultGraph.test.tsx, src/pages/GraphsHub.test.tsx</files>
  <read_first>
    - convex/graphSnapshots.ts lines 260-281 (exact field names the fixture must mirror)
    - src/components/forge/ForgeLogPane.test.tsx OR src/pages/KnowledgeGraph (any existing *.test.tsx) for the project's vitest + @testing-library/react + vi.mock layout convention
    - src/test/setup.ts (global mocks already registered — do not duplicate)
    - .planning/phases/84-graphs-hub-code-vault-render/84-VALIDATION.md (§ Wave 0 Requirements + Per-Task Verification Map — the behaviors each file must eventually cover)
  </read_first>
  <action>
    Create `src/test/projectGraphFixture.ts` exporting `makeProjectGraphFixture(overrides?)` that returns an object exactly matching the getProjectGraph shape in `<interfaces>`: at minimum 2 graphify-source nodes (ids `graphify:codepulse:src/a.ts`, `graphify:codepulse:src/b.ts`, source `graphify:codepulse:`) and 1 vault-source node (id `vault:Note.md`, source `vault:`), links among them, a `sources[]` array with one graphify entry (truncated:false) and one vault entry (allow override to set `truncated:true` and mismatched emitted/total counts), and `generatedAt` defaulting to `Date.now()/1000` (fresh) — overridable to a stale value. Include `storedNodeCount`/`storedLinkCount` equal to `nodeCount`/`linkCount` by default, overridable lower for the integrity-warning case. Also export a `mockGetProjectGraph(value)` helper that sets up `vi.mock("convex/react")`'s `useQuery` to return `value` (the pattern the two consumer test files import). Per 84-VALIDATION.md the mock targets `api.graphSnapshots.getProjectGraph`.
    Create `src/components/graph/CodeVaultGraph.test.tsx` and `src/pages/GraphsHub.test.tsx` as runnable scaffolds: import the fixture, render against it, and include `it.todo(...)` (or skipped) placeholders enumerating each behavior row from 84-VALIDATION.md (CodeVaultGraph: render-with-data, loading-on-undefined, empty-on-null, source-filter-drops-vault+dangling, truncation X-of-Y, stale-badge, integrity-warning, detail-panel-on-click, colorFn emerald/violet; GraphsHub: three tiles render, tiles click-navigate). The files MUST run clean (exit 0) now — implemented assertions arrive in plans 02/03. Do not import CodeVaultGraph/GraphsHub components yet if they would fail to resolve; gate real renders behind `it.todo`.
  </action>
  <verify>
    <automated>npx vitest run src/test/projectGraphFixture.ts src/components/graph/CodeVaultGraph.test.tsx src/pages/GraphsHub.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `src/test/projectGraphFixture.ts` contains `makeProjectGraphFixture` and the strings `storedNodeCount`, `generatedAt`, `vault:`, and `graphify:`.
    - `src/components/graph/CodeVaultGraph.test.tsx` contains `CodeVaultGraph` and at least 9 `it`/`it.todo` entries (one per CodeVaultGraph row in 84-VALIDATION.md).
    - `src/pages/GraphsHub.test.tsx` contains `GraphsHub` and at least 2 `it`/`it.todo` entries.
    - `npx vitest run src/test/projectGraphFixture.ts src/components/graph/CodeVaultGraph.test.tsx src/pages/GraphsHub.test.tsx` exits 0 (no red — todos are pending, not failing).
  </acceptance_criteria>
  <done>Shared fixture + mock helper exist and match the live shape; both consumer test files run clean with enumerated pending behaviors.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Convex query → browser | `getProjectGraph` is a public, read-only Convex query consumed via `useQuery`. No user input crosses into the query (only an optional fixed `snapshotId`); no secrets; no mutation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-84-01 | Information Disclosure | `getProjectGraph` public read | accept | Matches the established public-read telemetry posture (KG summary cards, forge.listJobs) per RESEARCH.md Security Domain. Data is operational graph metadata, no PII/secrets. No L1 high-severity threat identified. No auth gating introduced (none exists on the analog reads). |
| T-84-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages this phase (RESEARCH.md Package Legitimacy Audit: none added). No install step; no slopcheck checkpoint needed. |
</threat_model>

<verification>
- `npx vitest run src/hooks/useProjectGraph.test.ts src/test/projectGraphFixture.ts src/components/graph/CodeVaultGraph.test.tsx src/pages/GraphsHub.test.tsx` exits 0.
- `npx tsc --noEmit` clean.
</verification>

<success_criteria>
- `useProjectGraph` exposes the raw three-state Convex result (undefined/null/object).
- A faithfully-shaped fixture + `getProjectGraph` mock helper exist and are imported by both consumer test files.
- All three Phase 84 test files run clean, with pending behaviors enumerated per 84-VALIDATION.md.
</success_criteria>

<output>
Create `.planning/phases/84-graphs-hub-code-vault-render/84-01-SUMMARY.md` when done.
</output>
