---
phase: 86-kg-full-text-search-clustering-layout
plan: 02
type: execute
wave: 2
depends_on: ["86-01"]
files_modified:
  - src/pages/KnowledgeGraph.tsx
autonomous: true
requirements: [KG-09]
must_haves:
  truths:
    - "The KG Explorer graph draws a community halo around any node whose community is non-null, using the community palette (layered under the selection ring)"
    - "The floating legend shows a 'Communities' section listing present clusters with palette swatches ONLY when ≥1 node has a non-null community (auto-hide)"
    - "When no KG node carries community (the case today, until Ástríðr emits it), the KG graph renders force-directed with no halo and no community legend section — no regression"
  artifacts:
    - path: "src/pages/KnowledgeGraph.tsx"
      provides: "community halo in paintNode + clusterForce wiring + auto-hide community legend section"
      contains: "communityColor"
  key_links:
    - from: "src/pages/KnowledgeGraph.tsx"
      to: "src/lib/kg-graph.ts communityColor"
      via: "import + paintNode halo + legend swatch"
      pattern: "communityColor"
    - from: "src/pages/KnowledgeGraph.tsx"
      to: "src/components/graph/ForceGraphCanvas.tsx clusterForce/communityColorFn"
      via: "props on the KG ForceGraphCanvas"
      pattern: "clusterForce|communityColorFn"
---

<objective>
Integrate the Plan 01 community-cluster primitives into the KG Explorer page: draw community halos, engage the cluster force, and add the auto-hide community legend section. KG entities resolve `community: null` today (Ástríðr's `/api/kg/overview` does not emit `community` yet — D-10 deferred), so this surface renders force-directed now and lights up automatically the moment Ástríðr ships the field — exactly the data-gated activation in D-04/D-05.

Purpose: complete KG-09 on the KG surface (SC#3 wiring present, SC#4 fallback preserved). Code/vault (Plan 01) is where clustering visibly demos today; this plan ensures the KG graph is correctly wired with zero further CodePulse work once the API emits community.
Output: KG Explorer paintNode halo, clusterForce prop, and the auto-hide Communities legend.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/86-kg-full-text-search-clustering-layout/86-RESEARCH.md
@.planning/phases/86-kg-full-text-search-clustering-layout/86-PATTERNS.md
@.planning/phases/86-kg-full-text-search-clustering-layout/86-UI-SPEC.md
@.planning/phases/86-kg-full-text-search-clustering-layout/86-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 (already implemented) + live KnowledgeGraph source. -->

From src/lib/kg-graph.ts (Plan 01):
- communityColor(community: number | null | undefined): string | null
- COMMUNITY_PALETTE: string[8]
- KgNode.community?: number | null (threaded via toGraphData/upsertNode)

From src/components/graph/ForceGraphCanvas.tsx (Plan 01):
- ForceGraphCanvasProps now has: clusterForce?: boolean; communityColorFn?: (node) => string | null
- The shared `paint` wrapper draws the halo when communityColorFn returns non-null — so passing communityColorFn is sufficient; the page's own paintNode does NOT need to redraw the halo IF it relies on the shared wrapper. (Choose ONE halo path — see action.)

From src/pages/KnowledgeGraph.tsx (live):
- paintNode callback L128-172 (fill → selection ring → label; layer order documented).
- legendTypes useMemo L182-185 (present entity types from graph.nodes).
- legend DOM block L254-283 (floating top-3 left-3 panel).
- useFocusParam wiring L113-122 (do not disturb).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Community halo + cluster force on the KG Explorer graph</name>
  <files>src/pages/KnowledgeGraph.tsx</files>
  <read_first>
    - src/pages/KnowledgeGraph.tsx (the file being modified — paintNode L128-172, the ForceGraphCanvas call site, imports)
    - src/components/graph/ForceGraphCanvas.tsx (the clusterForce + communityColorFn props from Plan 01)
    - src/lib/kg-graph.ts (communityColor from Plan 01)
    - 86-PATTERNS.md § "src/pages/KnowledgeGraph.tsx" → paintNode callback (lines 537-583) for the exact halo layer order
    - 86-UI-SPEC.md § Color (halo geometry) + § Interaction Contract (halo under selection ring; honor opts.dimmed)
  </read_first>
  <action>
    Import `communityColor` from `@/lib/kg-graph`. On the KG `<ForceGraphCanvas>` call site, pass `clusterForce={true}` and `communityColorFn={(node) => communityColor(node.community)}` — reusing the shared halo + force path built in Plan 01 (single halo path: let the shared `paint` wrapper draw the halo via communityColorFn; do NOT also draw the halo inside the page's `paintNode`, to avoid a double-stroke). The page's existing `paintNode` keeps drawing fill + selection ring + label unchanged. Because KG nodes resolve `community: null` today, the gate `nodes.some(n => n.community != null)` is false → cluster force is not registered and no halo draws (SC#4 — current KG behavior unchanged), and it activates automatically when the API emits community.
  </action>
  <verify>
    <automated>npx vitest run src/pages/KnowledgeGraph.test.tsx 2>/dev/null; npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Grep assertion: `grep -n "clusterForce" src/pages/KnowledgeGraph.tsx` and `grep -n "communityColorFn" src/pages/KnowledgeGraph.tsx` each return a match on the ForceGraphCanvas call site.
    - Grep assertion: `grep -c "communityColor" src/pages/KnowledgeGraph.tsx` (filtered `grep -v '^#'`) ≥ 1.
    - `npx tsc --noEmit` clean; existing KnowledgeGraph behavior unchanged on today's (community-less) data.
  </acceptance_criteria>
  <done>KG graph wired for clustering via the shared Plan 01 path; renders identically to today on community-less data; will cluster + halo once the API emits community.</done>
</task>

<task type="auto">
  <name>Task 2: Auto-hide Communities legend section</name>
  <files>src/pages/KnowledgeGraph.tsx</files>
  <read_first>
    - src/pages/KnowledgeGraph.tsx (the file being modified — legendTypes useMemo L182-185, legend DOM block L254-283)
    - src/lib/kg-graph.ts (communityColor from Plan 01)
    - 86-PATTERNS.md § "src/pages/KnowledgeGraph.tsx" → legendTypes / legend DOM patterns (lines 585-633)
    - 86-UI-SPEC.md § Component Inventory ("Community legend row" — auto-hide Q4-A) + § Copywriting Contract (heading "Communities", each entry "Cluster {id}")
  </read_first>
  <action>
    Add a `presentCommunities` `useMemo` (mirroring `legendTypes`) computing the sorted unique non-null `community` ids in `graph.nodes`. In the floating legend DOM block, after the entity-type rows (and any current/superseded/contradiction rows), append a `Communities` section that renders ONLY when `presentCommunities.length > 0` (auto-hide — no "no clusters" copy per Q4-A). Each entry: a `h-2.5 w-2.5` rounded swatch colored via `communityColor(c)` (the inherited legend-dot dimension) + the label `Cluster {c}`. Use the exact heading copy `Communities` and entry copy `Cluster {id}` from the UI-SPEC. Keep all spacing/typography tokens consistent with the existing legend rows (font-mono, text-muted-foreground, border-t divider).
  </action>
  <verify>
    <automated>npx vitest run src/pages/KnowledgeGraph.test.tsx 2>/dev/null; npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Grep assertion: `grep -n "presentCommunities" src/pages/KnowledgeGraph.tsx` returns the useMemo + the conditional render.
    - Grep assertion: `grep -n "Communities" src/pages/KnowledgeGraph.tsx` shows the auto-hide heading; the section is wrapped in `presentCommunities.length > 0 &&` (no unconditional render).
    - `npx tsc --noEmit` clean.
    - MANUAL (real-data, per global verification bar — record in SUMMARY): on today's KG data (community-less) the Communities legend section is ABSENT (SC#4 observed); the section appears only when community data is present.
  </acceptance_criteria>
  <done>Communities legend section is present in code, auto-hides when no node carries community, and matches the UI-SPEC copy + legend-dot dimensions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Ástríðr `/api/kg/overview` → KG render | Entity `community` (when emitted) flows into halo palette + legend label. Numeric-only; rendered as React text (`Cluster {id}`). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-86-04 | Tampering (XSS) | community legend `Cluster {id}` label | mitigate | `id` is a number rendered as React text content (auto-escaped); no `dangerouslySetInnerHTML`. communityColor clamps the palette index (T-86-01). |
| T-86-05 | Denial of Service | KG cluster force | mitigate | Inherits Plan 01's gate (`nodes.some(n => n.community != null)`) + bounded `cooldownTicks`. KG is community-less today → force never registers. |
| T-86-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs this plan (all libs already in node_modules). |
</threat_model>

<verification>
- `npx tsc --noEmit` clean; KnowledgeGraph tests (if present) green.
- Manual on-real-data: KG graph unchanged on community-less data, Communities legend absent (SC#4 observed); halo + cluster path confirmed wired (will activate when API emits community).
</verification>

<success_criteria>
- KG-09 SC#3 wiring complete on the KG surface (halo + cluster force engaged via the shared path).
- KG-09 SC#4 preserved on the KG surface (no regression on today's community-less data; legend auto-hides).
</success_criteria>

<output>
Create `.planning/phases/86-kg-full-text-search-clustering-layout/86-02-SUMMARY.md` when done. Record the SC#4 observation (KG unchanged / legend absent on community-less data).
</output>
