---
phase: 92-voice-activated-command-palette-jarvis-mode
plan: 06
type: execute
wave: 5
depends_on: ["92-05"]
files_modified:
  - scripts/validate_wakeword_model.py
  - public/openwakeword/README.md
  - .planning/phases/92-voice-activated-command-palette-jarvis-mode/92-VERIFICATION.md
autonomous: true
requirements: [VOX-01]

must_haves:
  truths:
    - "hey_astrid.onnx is confirmed to be a non-degenerate, contract-compatible classifier — not an empty/stub/constant-output export — with the exact I/O contract wakeWordWorker.ts expects (input [1, seqLen, embDim] with seqLen ∈ {16,22}, output a single score). (Training QUALITY — accuracy 0.78 / recall 0.55 — is documented in README provenance from the Colab run; the structural script proves the model is real and wired-compatible, not the gradient-descent training itself.)"
    - "The 13.8KB file size is explained by a counted parameter total and an op/initializer inventory, compared against the hey_jarvis_v0.1.onnx fallback"
    - "A repeatable validation script exists so the model can be re-checked after any future retrain, with a clear PASS/FAIL verdict printed and a non-zero exit on FAIL"
    - "VOX-01 model-dependency sign-off is recorded honestly in 92-VERIFICATION.md: the structural model dependency is marked resolved (with script evidence) if it validates, or flagged for retraining with concrete failing-check evidence if it does not — WITHOUT closing the 7 live-mic/browser human-QA items"
  artifacts:
    - path: "scripts/validate_wakeword_model.py"
      provides: "standalone ONNX validation: graph inspection + non-degenerate inference smoke test + PASS/FAIL verdict for hey_astrid.onnx vs the worker contract"
      contains: "hey_astrid.onnx"
    - path: "public/openwakeword/README.md"
      provides: "model status updated from PENDING to the validated/flagged verdict"
      contains: "hey_astrid.onnx"
  key_links:
    - from: "scripts/validate_wakeword_model.py"
      to: "public/openwakeword/hey_astrid.onnx"
      via: "onnx.load + onnxruntime.InferenceSession over the model file"
      pattern: "hey_astrid.onnx"
    - from: "scripts/validate_wakeword_model.py"
      to: "src/workers/wakeWordWorker.ts"
      via: "asserts the same [1, seqLen∈{16,22}, embDim] input / single-score output contract the worker probes"
      pattern: "seqLen"
---

<objective>
Validate that the newly-trained `public/openwakeword/hey_astrid.onnx` (13.8 KB, vs. the 1.27 MB `hey_jarvis_v0.1.onnx` fallback) is a genuine, non-degenerate trained wake-word classifier that satisfies the exact contract `src/workers/wakeWordWorker.ts` enforces — Stage-3 input `[1, seqLen, embDim]` with `seqLen ∈ {16, 22}` and `embDim` matching the embedding model's output (≈96), producing a single finite score. Resolve the one genuine external dependency the Phase 92 verification flagged ("hey_astrid.onnx PENDING — VOX-01 live-detection QA blocked on it"), and record an honest PASS/FAIL verdict.

Purpose: VOX-01's wake-detection success criterion depends on this model. The wiring is already shipped and 83 tests pass; the model file now exists and README provenance records the Colab training metrics (accuracy 0.78 / recall 0.55), but `92-VERIFICATION.md` still names the untrained model as "the one genuine external dependency that must be resolved before full VOX-01 sign-off." The suspiciously-small file size (13.8 KB vs 1.27 MB) makes it worth proving — with counted parameters and a varying-output inference test — that the file is a real, contract-compatible classifier and not a broken export, before the verification doc declares the dependency resolved. The script is left committed so any future retrain re-validates in one command.

Output: `scripts/validate_wakeword_model.py` (new), `public/openwakeword/README.md` (add the structural-validation numbers to the provenance section), and `92-VERIFICATION.md` (mark the model-dependency resolved or re-opened with evidence).

Note: the README "PENDING" text and the ROADMAP "Porcupine" line were already corrected in commits `028641e` / `b62f0bd` — this plan does NOT redo those; it adds the structural-validation evidence the docs still lack.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/92-voice-activated-command-palette-jarvis-mode/92-RESEARCH.md
@.planning/phases/92-voice-activated-command-palette-jarvis-mode/92-VERIFICATION.md
@src/workers/wakeWordWorker.ts
@public/openwakeword/README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the ONNX validation script (graph inspection + inference smoke test)</name>
  <files>scripts/validate_wakeword_model.py</files>
  <read_first>
    - src/workers/wakeWordWorker.ts (lines 64-141 `loadModels` — the EXACT contract to mirror: classifier input `[1, seqLen, embDim]`, the `candidateSeqLens = [16, 22]` probe at lines 118-133, `embDim` derived from embedding_model output last dim, output read as `data[0]` so output must hold ≥1 score; THRESHOLD=0.5 at line 38)
    - .planning/phases/92-voice-activated-command-palette-jarvis-mode/92-RESEARCH.md §"Three-Stage ONNX Pipeline" and any "Pitfall 6" shape-inspection notes (mel_bins ≈32, embDim ≈96, seqLen 16 or 22)
    - public/openwakeword/README.md (current PENDING status text being superseded)
  </read_first>
  <behavior>
    - Running `python scripts/validate_wakeword_model.py` inspects and tests `public/openwakeword/hey_astrid.onnx`, prints a structured report, and exits 0 only if every contract + non-degeneracy check passes (exits non-zero with a clear FAIL summary otherwise).
    - The script is dependency-self-healing: if `onnx` / `onnxruntime` import fails, it prints the exact `pip install onnx onnxruntime` hint and exits non-zero rather than tracebacking.
    - It also loads `hey_jarvis_v0.1.onnx` and `embedding_model.onnx` for a side-by-side contract comparison, so a divergence (different input rank, different embDim, no usable seqLen) is reported explicitly.
  </behavior>
  <action>Create `scripts/validate_wakeword_model.py`. Resolve the model dir relative to the repo root (`public/openwakeword`) so it runs from any CWD. Wrap the `import onnx` / `import onnxruntime as ort` in a try/except that, on ImportError, prints `Missing deps — run: pip install onnx onnxruntime` and `sys.exit(2)`.

  Implement three sections:

  (A) **Graph inspection** — for each of `hey_astrid.onnx` and `hey_jarvis_v0.1.onnx`: `onnx.load`, run `onnx.checker.check_model`, and print: opset version, graph input names + declared shapes, graph output names + declared shapes, the set of op `node.op_type` values, and the total trained-parameter count summed from `model.graph.initializer` (use `onnx.numpy_helper.to_array(init).size` per initializer). This is what explains the 13.8 KB — print the param total for both models side by side.

  (B) **Embedding-dim resolution** — load `embedding_model.onnx` with onnxruntime, run a zeros input of shape `[76, mel_bins, 1]` (derive `mel_bins` by first running `melspectrogram.onnx` on `np.zeros(1280, float32)` and taking the last output dim, mirroring wakeWordWorker.ts lines 91-98), and take the embedding output's last dim as `embDim`. Print `mel_bins` and `embDim`.

  (C) **Classifier contract + non-degeneracy test** — load `hey_astrid.onnx` with onnxruntime. Probe `seqLen` exactly as the worker does: for `seqLen in (16, 22)`, try running input `[1, seqLen, embDim]` of zeros; record the first that succeeds as `resolved_seq_len`. Then run the classifier on **N=8 independent `np.random.RandomState(0)` random inputs** of the resolved shape and collect the scalar scores (read element 0 of the single output, matching `data[0]`). Assert/report:
    - PASS-1 contract: exactly one of `{16, 22}` produced a runnable input AND the output contains ≥1 value (so the worker's `data[0]` read is valid).
    - PASS-2 embDim match: the classifier's accepted last input dim equals the `embDim` from section (B).
    - PASS-3 non-degenerate weights: total classifier parameter count > 0 AND not all initializer values are zero.
    - PASS-4 non-constant output: the 8 random-input scores are not all identical (stdev > 1e-6) — a stub/untrained head that ignores its input would emit a constant; varied output is evidence of trained, input-sensitive weights.
    - INFO: print min/max/mean of the 8 scores and whether any cross THRESHOLD=0.5 (informational only — random noise is not expected to wake, so do NOT fail on this).

  Print a final `VERDICT: PASS` / `VERDICT: FAIL` line listing which of PASS-1..4 failed, and `sys.exit(0 if pass else 1)`. Keep it a single self-contained file, stdlib + numpy + onnx + onnxruntime only, no project imports.</action>
  <verify>
    <automated>python -c "import ast,sys; ast.parse(open('scripts/validate_wakeword_model.py').read()); print('syntax-ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/validate_wakeword_model.py` exists and parses (the `ast.parse` check prints `syntax-ok`).
    - File contains the literals `hey_astrid.onnx`, `hey_jarvis_v0.1.onnx`, `embedding_model.onnx`, the `(16, 22)` seqLen probe, and a `VERDICT:` print.
    - Missing-dependency path prints the `pip install onnx onnxruntime` hint and exits non-zero (no raw ImportError traceback) — verifiable by reading the try/except.
    - The script reads only files under `public/openwakeword/` and imports no `src/` project code.
  </acceptance_criteria>
  <done>A standalone, repeatable validation script exists that inspects the model graph, counts parameters, resolves the embedding/seqLen dims the way the worker does, and runs a non-degeneracy inference smoke test with a PASS/FAIL verdict.</done>
</task>

<task type="auto">
  <name>Task 2: Run the validation, capture the verdict, and record VOX-01 sign-off status</name>
  <files>public/openwakeword/README.md, .planning/phases/92-voice-activated-command-palette-jarvis-mode/92-VERIFICATION.md</files>
  <read_first>
    - scripts/validate_wakeword_model.py (the script from Task 1 — run it and read its full stdout)
    - public/openwakeword/README.md (already says hey_astrid.onnx SHIPPED with Colab provenance — accuracy 0.78 / recall 0.55; this task ADDS a structural-validation line to the "Model provenance" section, it does NOT remove a PENDING line — there is none)
    - .planning/phases/92-voice-activated-command-palette-jarvis-mode/92-VERIFICATION.md (the `human_verification` "Live Wake Detection (VOX-01 primary)" entry whose `why_human` still says "hey_astrid.onnx is also PENDING training", and the "Gaps Summary" paragraph naming hey_astrid.onnx as "the one genuine external dependency that must be resolved before full VOX-01 sign-off")
  </read_first>
  <behavior>
    - Run `pip install onnx onnxruntime` if the script reports missing deps, then run `python scripts/validate_wakeword_model.py` and capture the verdict + the param-count/embDim/seqLen/score numbers it prints.
    - Update the docs to reflect the ACTUAL verdict — do not assume PASS. If the model FAILs (e.g. constant output, wrong shape, zero weights), record the failure evidence and explicitly flag that hey_astrid.onnx must be retrained, leaving the fallback (hey_jarvis) as the active classifier.
  </behavior>
  <action>Execute the script (installing `onnx onnxruntime` into the active Python first if needed). Capture the printed VERDICT, the hey_astrid vs hey_jarvis parameter counts, resolved `embDim`, resolved `seqLen`, and the 8-sample score min/max/mean.

  In `public/openwakeword/README.md` "Model provenance" section: APPEND a dated structural-validation line (the README already documents training quality, not structure). On PASS: `Structural validation <date> (\`python scripts/validate_wakeword_model.py\`): <param-count> trained params, input [1, <seqLen>, <embDim>], single-score output, non-degenerate (score stdev <value> > 1e-6 across random inputs) — contract-compatible with wakeWordWorker.ts. Re-run after any retrain.` On FAIL: a `Structural validation <date>: NEEDS RETRAIN — <failing check>` line, noting hey_jarvis_v0.1.onnx stays the active fallback. Keep the existing accuracy/recall provenance lines intact (they are the training-quality evidence the script cannot supply).

  In `92-VERIFICATION.md`: edit the "Live Wake Detection" `why_human` text and the "Gaps Summary" so the **structural** model dependency is marked RESOLVED (with the validation numbers + a pointer to the script) on PASS, or RE-OPENED with the failure evidence on FAIL. Do NOT change the `status:` frontmatter value, and do NOT close the live-audio human items — note explicitly that live wake-detection still needs real-mic QA (the 0.55 recall means missed triggers are plausible in the field); this task closes only the "is the file a real classifier" dependency.</action>
  <verify>
    <automated>python scripts/validate_wakeword_model.py; echo "exit=$?"; grep -ci "structural validation" public/openwakeword/README.md; grep -ci "structural validation" .planning/phases/92-voice-activated-command-palette-jarvis-mode/92-VERIFICATION.md</automated>
  </verify>
  <acceptance_criteria>
    - `python scripts/validate_wakeword_model.py` runs to completion and prints a `VERDICT:` line (exit code captured and recorded in the SUMMARY).
    - `public/openwakeword/README.md` gains a dated `Structural validation` line citing the param count and resolved input shape `[1, <seqLen>, <embDim>]`; the existing accuracy/recall provenance lines are preserved (`grep -ci "structural validation"` ≥ 1 AND `grep -c "recall 0.55" README.md` still ≥ 1).
    - `92-VERIFICATION.md` "Live Wake Detection" entry and "Gaps Summary" reflect the real verdict (RESOLVED + evidence on PASS, or RE-OPENED + failing-check evidence on FAIL); `grep -ci "structural validation"` ≥ 1.
    - The `status: human_needed` frontmatter is unchanged and the 7 live-audio human items are NOT marked closed.
    - The recorded verdict, param counts, embDim, seqLen, and score stats are written into `92-06-SUMMARY.md`.
  </acceptance_criteria>
  <done>The model is validated against the worker contract with real numbers; the README and VERIFICATION docs state the honest VOX-01 model-dependency status (resolved or flagged-for-retrain) with reproducible evidence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Model file → wake engine | `hey_astrid.onnx` is loaded and executed by the in-browser Worker; a malformed or mis-shaped model is the only new artifact this plan introduces to that boundary (read-only validation, no new runtime code) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-92-18 | Spoofing / false sign-off | hey_astrid.onnx | mitigate | The non-degeneracy test (varied output across random inputs + non-zero counted weights) prevents an empty/stub model from being signed off as a working wake classifier; verdict is exit-code enforced |
| T-92-19 | Denial of Service / crash | classifier shape mismatch | mitigate | Script asserts the model accepts exactly the `[1, seqLen∈{16,22}, embDim]` shape the worker probes; a mismatch fails the verdict before any live-QA claim, and the worker already falls back to hey_jarvis on a load failure |
| T-92-20 | Tampering | committed validation script | accept | The script is read-only diagnostic tooling (loads/infers models, prints a verdict); it modifies no app code or runtime path; bounded to local dev validation |
</threat_model>

<verification>
- `python scripts/validate_wakeword_model.py` runs and prints a `VERDICT:` line; exit code recorded in the SUMMARY.
- `public/openwakeword/README.md` gains a dated `Structural validation` line (param count + input shape `[1, seqLen, embDim]`) appended to the provenance section; existing accuracy/recall lines preserved.
- `92-VERIFICATION.md` Live-Wake-Detection human item + Gaps Summary reflect the real verdict (RESOLVED or RE-OPENED with evidence); `status: human_needed` frontmatter unchanged.
- No `src/` runtime code modified; full `npx vitest run` remains green (this plan touches no app code — confirm no regressions).
- MANUAL (still open, unchanged by this plan): the 7 live-audio/browser human-verification items in 92-VERIFICATION.md (real mic wake detection, live STT→chat.send, audible TTS, feedback guard, graceful-degradation reload, toggle persistence, ⌘K coexistence, continuous turn loop) still require a running browser + backend.
</verification>

<success_criteria>
The one genuine external dependency Phase 92 verification flagged — the unvalidated hey_astrid.onnx — is resolved with reproducible evidence: a committed validation script proves the model is a real, contract-compatible, non-degenerate classifier (or flags it for retrain with specific failing checks), and the README + VERIFICATION docs state the honest VOX-01 model-readiness status. The remaining open items are exclusively the live-audio human-QA behaviors that always required a real browser (and the 0.55-recall caveat that live-mic QA must still confirm).
</success_criteria>

<output>
Create `.planning/phases/92-voice-activated-command-palette-jarvis-mode/92-06-SUMMARY.md` when done
</output>
