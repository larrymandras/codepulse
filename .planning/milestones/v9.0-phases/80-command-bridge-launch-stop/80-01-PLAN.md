---
phase: 80-command-bridge-launch-stop
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - convex/schema.ts
  - convex/forge.ts
  - convex/forgeCommands.ts
  - convex/http.ts
  - convex/crons.ts
  - convex/forge.test.ts
autonomous: true
requirements: [FI-06, FI-08]
must_haves:
  truths:
    - "A Clerk-authenticated operator can enqueue a launch command; an unauthenticated caller is rejected (throws)"
    - "A Clerk-authenticated operator can enqueue a stop command; an unauthenticated caller is rejected (throws)"
    - "The daemon can atomically claim queued, non-expired commands for its host via a bearer-authed httpAction, and the claim updates host liveness"
    - "The daemon can ack a command, recording done/failed and the resolved forgeJobId for optimistic-row reconciliation"
    - "Queued commands past their TTL are marked expired by a scheduled cron and will never be claimed"
    - "A dangerous-mode capability in a launch payload is stripped before the command is stored"
  artifacts:
    - path: "convex/schema.ts"
      provides: "forgeCommands + forgeHosts table definitions with claim/reconcile/TTL/liveness indexes"
      contains: "forgeCommands"
    - path: "convex/forge.ts"
      provides: "enqueueLaunch, enqueueStop (Clerk fail-closed mutations); claimAndUpsertHost, ackCommand, expireStaleCommands (internalMutation); listForgeCommands, listHosts (queries)"
      exports: ["enqueueLaunch", "enqueueStop", "claimAndUpsertHost", "ackCommand", "expireStaleCommands", "listForgeCommands", "listHosts"]
    - path: "convex/forgeCommands.ts"
      provides: "forgeCommandsClaim + forgeCommandsAck httpActions (bearer-authed, CORS)"
      exports: ["forgeCommandsClaim", "forgeCommandsAck"]
    - path: "convex/http.ts"
      provides: "POST+OPTIONS routes for /forge-commands-claim and /forge-commands-ack"
      contains: "/forge-commands-claim"
    - path: "convex/crons.ts"
      provides: "expire-stale-forge-commands interval entry"
      contains: "expire-stale-forge-commands"
    - path: "convex/forge.test.ts"
      provides: "Pure-logic tests for FI-06 + FI-08 (auth fail-closed, dangerous strip, TTL, claim, ack)"
      contains: "Authentication required"
  key_links:
    - from: "convex/forgeCommands.ts"
      to: "internal.forge.claimAndUpsertHost / internal.forge.ackCommand"
      via: "ctx.runMutation from httpAction"
      pattern: "runMutation\\(internal\\.forge\\."
    - from: "convex/forgeCommands.ts"
      to: "validateForgeIngestAuth (convex/ingestAuth.ts)"
      via: "bearer auth gate (D-14)"
      pattern: "validateForgeIngestAuth"
    - from: "convex/forge.ts enqueueLaunch/enqueueStop"
      to: "ctx.auth.getUserIdentity()"
      via: "fail-closed null check (D-13)"
      pattern: "getUserIdentity"
    - from: "convex/crons.ts"
      to: "internal.forge.expireStaleCommands"
      via: "crons.interval"
      pattern: "expireStaleCommands"
---

<objective>
Build the entire Convex backend for the Forge command bridge: the `forgeCommands` + `forgeHosts` tables, the Clerk-gated (fail-closed) enqueue mutations, the daemon-facing claim/ack httpActions (bearer-authed), the TTL-expiry cron entry, and the pure-logic test suite.

Purpose: This is the foundation Wave 2 (UI) and the cross-repo daemon (80-02) build against. It opens the first controlled write-back path in the Forge integration while keeping the engine local — Convex never touches localhost.
Output: Deployable Convex schema + functions + routes + cron + tests covering FI-06 (queue, claim, ack, TTL) and FI-08 (fail-closed auth).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/80-command-bridge-launch-stop/80-CONTEXT.md
@.planning/phases/80-command-bridge-launch-stop/80-RESEARCH.md
@.planning/phases/80-command-bridge-launch-stop/80-PATTERNS.md
@.planning/phases/80-command-bridge-launch-stop/80-VALIDATION.md

<interfaces>
<!-- Contracts the executor needs — extracted from the live codebase. Use directly, no exploration needed. -->

From convex/forge.ts (existing — extend, do NOT rewrite):
- `upsertJob`, `upsertWorkspaces` — internalMutation, read-then-patch-or-insert via .withIndex(...).unique()
- `listJobs`, `getJob`, `listWorkspaces` — public query, NO auth check (graceful-skip convention; D-13 deliberately diverges from this for the new write mutations)
- JOB_LIST_LIMIT = 1000 (cap pattern for list queries)
- Current imports: `import { internalMutation, query } from "./_generated/server";` — extend to add `mutation`

From convex/ingestAuth.ts (use as-is, already exported — no changes):
- `validateForgeIngestAuth(request: Request): boolean` — fail-closed bearer check using FORGE_INGEST_API_KEY (D-14)
- `getCorsHeaders(request: Request): Record<string,string>`
- `unauthorizedResponse(): Response` — 401 JSON

From convex/forgeIngest.ts (structural analog for the new httpActions):
- `forgeIngest = httpAction(async (ctx, request) => {...})` — OPTIONS→CORS, validateForgeIngestAuth→401, JSON parse→400, dispatch via ctx.runMutation(internal.forge.*), success 200 with CORS headers

From convex/schema.ts:1464 (forgeJobs/forgeWorkspaces — copy conventions):
- Nullable fields use `v.union(v.T, v.null())` (NOT v.optional)
- status is `v.string()` (NOT a literal union)
- capabilities passthrough is `v.string()` JSON
- Multi-column indexes named `by_<cols>`

From convex/crons.ts (existing, has 14 crons — add ONE entry):
- `crons.interval("name", { minutes: N }, internal.domain.fn)` then `export default crons` at the bottom
- Closest analog entry: "docker-health-cleanup" interval (lines 83-88)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Schema + cron entry for forgeCommands / forgeHosts</name>
  <read_first>
    - convex/schema.ts (around line 1464 — the `// FORGE INTEGRATION (Phase 78)` block: forgeJobs + forgeWorkspaces defs and their indexes)
    - convex/crons.ts (lines 83-104 — the docker-health-cleanup interval analog + the `export default crons` tail)
    - .planning/phases/80-command-bridge-launch-stop/80-PATTERNS.md (§"convex/schema.ts — add forgeCommands + forgeHosts tables" and §"convex/crons.ts — add expire-stale-forge-commands cron")
    - .planning/phases/80-command-bridge-launch-stop/80-RESEARCH.md (§RQ2 schema + state machine, §RQ4 TTL, §RQ6 host liveness)
  </read_first>
  <behavior>
    - forgeCommands table exists with fields: hostId, commandId, commandType (launch|stop), launchPayload (object|null), stopPayload (object|null), status, issuedBy, createdAt, expiresAt, claimedAt, executedAt, completedAt, resolvedForgeJobId, error
    - forgeHosts table exists with fields: hostId, lastSeenAt, hostname (optional)
    - forgeCommands has index by_host_status_created (["hostId","status","createdAt"]), by_commandId (["commandId"]), by_expires (["expiresAt"])
    - forgeHosts has index by_hostId (["hostId"]), by_lastSeenAt (["lastSeenAt"])
    - crons.ts registers an interval named "expire-stale-forge-commands" at { minutes: 1 } → internal.forge.expireStaleCommands
  </behavior>
  <action>
    Add a new section `// FORGE COMMAND BRIDGE (Phase 80)` to convex/schema.ts after the forgeWorkspaces def. Define `forgeCommands` with: `hostId: v.string()`, `commandId: v.string()` (client-generated ULID for optimistic reconciliation), `commandType: v.union(v.literal("launch"), v.literal("stop"))`, `launchPayload: v.union(v.object({ agent: v.string(), workspaceId: v.string(), mode: v.string(), prompt: v.union(v.string(), v.null()), model: v.union(v.string(), v.null()), capabilities: v.union(v.string(), v.null()) }), v.null())`, `stopPayload: v.union(v.object({ forgeJobId: v.string() }), v.null())`, `status: v.string()` (values: queued | executing | done | failed | expired), `issuedBy: v.string()` (Clerk identity.subject), `createdAt: v.number()`, `expiresAt: v.number()`, `claimedAt: v.union(v.number(), v.null())`, `executedAt: v.union(v.number(), v.null())`, `completedAt: v.union(v.number(), v.null())`, `resolvedForgeJobId: v.union(v.string(), v.null())`, `error: v.union(v.string(), v.null())`. Add indexes `by_host_status_created` on ["hostId","status","createdAt"], `by_commandId` on ["commandId"], `by_expires` on ["expiresAt"]. Define `forgeHosts` with `hostId: v.string()`, `lastSeenAt: v.number()`, `hostname: v.optional(v.string())`; indexes `by_hostId` on ["hostId"] and `by_lastSeenAt` on ["lastSeenAt"]. Then in convex/crons.ts add, before `export default crons`, a `// Phase 80: Forge command bridge — expire unclaimed commands past TTL (D-12)` comment and `crons.interval("expire-stale-forge-commands", { minutes: 1 }, internal.forge.expireStaleCommands)`. The cron will reference an internalMutation created in Task 2 — type errors against internal.forge.expireStaleCommands resolve once Task 2 lands; both ship in this same plan.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -v -E "expireStaleCommands|internal\.forge" | grep -c "error TS" | grep -qx 0 && echo SCHEMA_OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "forgeCommands: defineTable" convex/schema.ts` returns 1
    - `grep -c "forgeHosts: defineTable" convex/schema.ts` returns 1
    - `grep -c "by_host_status_created" convex/schema.ts` returns 1
    - `grep -c "expire-stale-forge-commands" convex/crons.ts` returns 1
    - No new tsc errors except temporary unresolved `internal.forge.expireStaleCommands` (resolved by Task 2)
  </acceptance_criteria>
  <done>Both tables defined with the three+two indexes; cron entry registered; tsc clean apart from the Task-2-pending internal reference.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: forge.ts mutations, internalMutations, queries + pure-logic tests (FI-06, FI-08)</name>
  <read_first>
    - convex/forge.ts (FULL file — the upsertJob read-then-patch-or-insert handler shape, listJobs withIndex+order+take pattern, file header comment)
    - convex/forge.test.ts (FULL file — pure-logic extraction pattern: extract decision fn, test without live Convex, .todo for DB round-trips)
    - convex/ingestAuth.ts (lines 88-108 — validateForgeIngestAuth fail-closed model the D-13 mutation gate mirrors in spirit)
    - .planning/phases/80-command-bridge-launch-stop/80-PATTERNS.md (§"convex/forge.ts — extend with mutations, queries, internalMutations" — has the exact handler scaffolds; §"convex/forge.test.ts — extend with Phase 80 tests")
    - .planning/phases/80-command-bridge-launch-stop/80-RESEARCH.md (§RQ2 claim/ack mechanics, §RQ5 Clerk fail-closed, §Pitfall 7 dangerous strip, §Code Examples claimAndUpsertHost)
  </read_first>
  <behavior>
    - enqueueLaunch throws "Authentication required to issue Forge commands" when getUserIdentity() returns null (FI-08, D-13)
    - enqueueStop throws the same when getUserIdentity() returns null (FI-08, D-13)
    - enqueueLaunch strips a `dangerous` key from the capabilities JSON before inserting (Pitfall 7, D-06)
    - The launch row is built with status "queued", issuedBy = identity.subject, expiresAt = createdAt + 5*60*1000
    - claimAndUpsertHost upserts forgeHosts.lastSeenAt and atomically sets status "executing" + claimedAt on up to 10 queued, non-expired commands for the host, returning them
    - ackCommand patches the command: status (done|failed), resolvedForgeJobId (when present), completedAt, error
    - expireStaleCommands marks status "expired" on commands whose status is "queued" and expiresAt < now; never touches executing/done/failed
    - Pure-logic helpers (shouldExpireCommand, stripDangerousCapability, buildLaunchRow) are unit-tested; auth-fail-closed is tested via the extracted guard
  </behavior>
  <action>
    Extend convex/forge.ts. Change the import to `import { internalMutation, mutation, query } from "./_generated/server";`. Extend the file header comment to describe the Phase 80 command-bridge functions and the D-13 fail-closed divergence. Define exported pure helpers so tests can import them: `stripDangerousCapability(capabilities: string | null): string | null` (parse JSON, delete `.dangerous`, re-serialize; return null if empty/unparseable-safe), `shouldExpireCommand(status: string, expiresAt: number, now: number): boolean` (true only when status === "queued" && expiresAt < now), and `buildLaunchRow(args, subject, now, ttlMs)` returning the insert object. Add `enqueueLaunch = mutation({...})`: args hostId, commandId, agent, workspaceId, mode, prompt (v.union v.string v.null), model (v.union v.string v.null), capabilities (v.union v.string v.null); handler calls `const identity = await ctx.auth.getUserIdentity(); if (identity === null) throw new Error("Authentication required to issue Forge commands");` with a prominent `// D-13: Fail-closed — DELIBERATE divergence from read-query graceful-skip. DO NOT change to graceful-skip; this is a write/control path.` comment, then inserts a forgeCommands row using buildLaunchRow with capabilities run through stripDangerousCapability, commandType "launch", stopPayload null. Add `enqueueStop = mutation({...})`: args hostId, commandId, forgeJobId; same fail-closed guard + comment; inserts a forgeCommands row commandType "stop", stopPayload { forgeJobId }, launchPayload null, status "queued", expiresAt = now + TTL. Define `const FORGE_COMMAND_TTL_MS = 5 * 60 * 1000;` (D-12). Add `claimAndUpsertHost = internalMutation({ args: { hostId: v.string(), now: v.number() } })`: upsert forgeHosts via by_hostId .unique() then patch-or-insert lastSeenAt; query forgeCommands by_host_status_created (eq hostId, eq status "queued"), `.filter(q => q.gt(q.field("expiresAt"), args.now))`, `.take(10)`; patch each to status "executing", claimedAt now, executedAt now; return the claimed docs. Add a code comment on the return: `// W1: Returned docs still show status:"queued" (pre-patch snapshot in this closure); they have been atomically claimed server-side (queued→executing) — the daemon must treat every returned command as to-be-executed, NOT skip on status.` Add `ackCommand = internalMutation({ args: { commandId: v.string(), status: v.string(), resolvedForgeJobId: v.union(v.string(), v.null()), error: v.union(v.string(), v.null()), now: v.number() } })`: find by by_commandId .unique(), patch status, resolvedForgeJobId, error, completedAt: args.now. Add `expireStaleCommands = internalMutation({ args: {} })`: query forgeCommands by_expires (`q.lt("expiresAt", Date.now())`).collect(), for each call shouldExpireCommand and patch status "expired" when true. Add `listForgeCommands = query({ args: { hostId: v.optional(v.string()) } })` (NO auth check — read-only, graceful-skip convention): when hostId, withIndex by_host_status_created (eq hostId) order desc take JOB_LIST_LIMIT; else collect newest-first via a suitable index/order; return rows. Add `listHosts = query({ args: {} })` (NO auth check): query forgeHosts by_lastSeenAt order desc collect. Then extend convex/forge.test.ts: import the pure helpers and add describe blocks testing stripDangerousCapability (removes dangerous, keeps maxTurns, handles null), shouldExpireCommand (queued+past=true, executing+past=false, queued+future=false), buildLaunchRow field mapping, and a fail-closed test asserting the guard throws when identity is null (test the extracted/inline guard logic, not a live Convex runtime). Add `.todo` stubs for the DB round-trips (enqueueLaunch insert, claimAndUpsertHost atomic claim, ackCommand resolve, expireStaleCommands) per the existing repo convention.
  </action>
  <verify>
    <automated>npx vitest run convex/forge.test.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run convex/forge.test.ts` exits 0 with the new describe blocks green
    - `grep -c "Authentication required to issue Forge commands" convex/forge.ts` returns 2 (enqueueLaunch + enqueueStop)
    - `grep -c "getUserIdentity" convex/forge.ts` returns at least 2
    - `grep -c "D-13" convex/forge.ts` returns at least 1 (the divergence comment is present)
    - `grep -c "stripDangerousCapability\|expireStaleCommands\|claimAndUpsertHost\|ackCommand\|listForgeCommands\|listHosts" convex/forge.ts` confirms all six+helper symbols exist
    - `npx tsc --noEmit` reports no errors in convex/forge.ts
  </acceptance_criteria>
  <done>All seven new functions + three pure helpers exist; D-13 comment present on both enqueue mutations; dangerous strip implemented; forge.test.ts green for the new logic; DB round-trips stubbed .todo.</done>
</task>

<task type="auto">
  <name>Task 3: httpActions (claim/ack) + http.ts route registration</name>
  <read_first>
    - convex/forgeIngest.ts (FULL file — the exact httpAction skeleton: OPTIONS→CORS 200, validateForgeIngestAuth→401, JSON parse→400, required-field 400, ctx.runMutation dispatch, 200 success with CORS)
    - convex/http.ts (lines 24, 71-75 — forgeIngest import + the /forge-ingest POST+OPTIONS registration pair, and the `export default http` tail)
    - convex/ingestAuth.ts (the three exported helpers used by the skeleton)
    - .planning/phases/80-command-bridge-launch-stop/80-PATTERNS.md (§"convex/forgeCommands.ts (new httpAction file)" and §"convex/http.ts — add /forge-commands-claim and /forge-commands-ack routes")
  </read_first>
  <action>
    Create convex/forgeCommands.ts. Import `httpAction` from "./_generated/server", `internal` from "./_generated/api", and `getCorsHeaders, validateForgeIngestAuth, unauthorizedResponse` from "./ingestAuth". Export `forgeCommandsClaim = httpAction(async (ctx, request) => {...})`: OPTIONS → `new Response(null, { status: 200, headers: getCorsHeaders(request) })`; `if (!validateForgeIngestAuth(request)) return unauthorizedResponse();` (D-14 bearer reuse of FORGE_INGEST_API_KEY); parse body in try/catch returning 400 JSON on failure; validate `hostId` present else 400 JSON "Missing required field: hostId"; `const result = await ctx.runMutation(internal.forge.claimAndUpsertHost, { hostId, now: Date.now() });`; return 200 `JSON.stringify({ commands: result })` with `{ "Content-Type": "application/json", ...getCorsHeaders(request) }`. Export `forgeCommandsAck = httpAction(...)` with the same auth/CORS/parse scaffold: require `commandId` and `status` in the body (400 if missing), then `await ctx.runMutation(internal.forge.ackCommand, { commandId, status, resolvedForgeJobId: body.forgeJobId ?? null, error: body.error ?? null, now: Date.now() });` and return 200 `JSON.stringify({ ok: true })` with CORS headers. Then edit convex/http.ts: add `import { forgeCommandsClaim, forgeCommandsAck } from "./forgeCommands";` near the forgeIngest import; add, after the Phase 78 forge-ingest routes, a `// Phase 80: Forge command bridge — claim + ack (daemon-facing, bearer-authed)` comment and four routes — `/forge-commands-claim` POST+OPTIONS → forgeCommandsClaim and `/forge-commands-ack` POST+OPTIONS → forgeCommandsAck.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -c "error TS" | grep -qx 0 && grep -c "forge-commands-claim" convex/http.ts && npx vitest run convex/forge.test.ts 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run convex/forge.test.ts` exits 0 — re-run here as the Nyquist sampling gate, since this task wires the Task-2 internalMutations (claimAndUpsertHost, ackCommand) into the httpAction routes (keeps unit-test coverage in the 3-task window; no two consecutive tasks rely on tsc/grep alone)
    - `grep -c "forge-commands-claim" convex/http.ts` returns 2 (POST + OPTIONS)
    - `grep -c "forge-commands-ack" convex/http.ts` returns 2 (POST + OPTIONS)
    - `grep -c "validateForgeIngestAuth" convex/forgeCommands.ts` returns at least 2 (both actions gate on bearer)
    - `grep -c "runMutation(internal.forge.claimAndUpsertHost\|runMutation(internal.forge.ackCommand" convex/forgeCommands.ts` confirms both dispatches
    - `npx tsc --noEmit` reports 0 errors across convex/
  </acceptance_criteria>
  <done>forgeCommands.ts exports both bearer-authed httpActions dispatching to the Task-2 internalMutations; http.ts registers all four POST+OPTIONS routes; whole convex/ tree type-checks clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → Convex mutation | Clerk-authenticated operator issues launch/stop. Untrusted until getUserIdentity() succeeds. |
| local daemon → Convex httpAction | Server-to-server claim/ack. Untrusted until FORGE_INGEST_API_KEY bearer validates. |
| Convex command store → daemon execution | A claimed command becomes a local Forge action; must be idempotent + TTL-bounded. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-80-01 | Elevation of Privilege | enqueueLaunch / enqueueStop | mitigate | Fail-closed `getUserIdentity()` null-check throws (D-13). Tested in forge.test.ts. Deliberately diverges from read-query graceful-skip; divergence documented in a code comment so it is not "fixed" back. |
| T-80-02 | Tampering | launchPayload.capabilities | mitigate | `stripDangerousCapability` removes `dangerous` before insert (D-06, Pitfall 7). Defense-in-depth on top of the UI never sending it. Unit-tested. |
| T-80-03 | Tampering | claim path (double-execution / replay) | mitigate | Single atomic `claimAndUpsertHost` internalMutation reads + patches status queued→executing in one serializable transaction. Concurrent polls cannot double-claim. |
| T-80-04 | Tampering | stale command execution | mitigate | `expiresAt = createdAt + 5min` (D-12); claim filters `expiresAt > now`; `expireStaleCommands` cron @ 1min marks queued-past-TTL as expired. A laptop waking hours later finds only expired commands. |
| T-80-05 | Spoofing | daemon claim/ack endpoints | mitigate | `validateForgeIngestAuth` bearer (FORGE_INGEST_API_KEY, D-14), fail-closed when key unset (no FORGE_INGEST_ALLOW_ANON). Server-to-server only; key never in browser. |
| T-80-06 | Elevation of Privilege | dangerous-mode / inline workspace creation reaching cloud control plane | accept (out of scope, structurally absent) | The enqueueLaunch arg surface has no dangerous toggle and no workspace-create path (D-06/D-07); the only writable command types are launch + stop. Nothing to mitigate beyond the stripped capability (T-80-02). |
| T-80-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages installed this phase (RESEARCH §Package Legitimacy Audit confirms zero installs). slopcheck not triggered. |
</threat_model>

<verification>
- `npx vitest run convex/forge.test.ts` green (FI-06 logic + FI-08 fail-closed)
- `npx tsc --noEmit` clean across convex/
- `npx convex dev` deploys the schema (manual/cross-task gate; Convex applies schema via dev/deploy — NOT a manual migration)
- Routes registered: 4 new POST+OPTIONS entries in http.ts
- Cron registered: expire-stale-forge-commands @ 1min
</verification>

<success_criteria>
- forgeCommands + forgeHosts tables exist with claim/reconcile/TTL/liveness indexes
- enqueueLaunch + enqueueStop are Clerk fail-closed (throw on null identity) — FI-08
- claimAndUpsertHost atomically claims + bumps host liveness; ackCommand reconciles; expireStaleCommands TTL-expires — FI-06
- forgeCommandsClaim + forgeCommandsAck are bearer-authed httpActions wired into http.ts — FI-06
- dangerous capability stripped before storage — D-06
- forge.test.ts green; convex/ type-checks
</success_criteria>

<output>
Create `.planning/phases/80-command-bridge-launch-stop/80-01-SUMMARY.md` when done
</output>
