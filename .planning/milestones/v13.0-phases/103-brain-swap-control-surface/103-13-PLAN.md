---
phase: 103-brain-swap-control-surface
plan: 13
type: execute
wave: 3
depends_on: ["103-09", "103-10", "103-11", "103-12"]
gap_closure: true
files_modified:
  - .planning/phases/103-brain-swap-control-surface/103-VALIDATION.md
  - .planning/REQUIREMENTS.md
autonomous: false
requirements: [BSC-05, BSC-01, BSC-02, BSC-04]
user_setup: []

must_haves:
  truths:
    - "Steps 4b, 5 and 6 of the 103-08-T2 live checkpoint are re-run against the running Ástríðr WS and a CodePulse dev server started WITHOUT VITE_BRAINS_STUB, and each step's real outcome is recorded verbatim"
    - "With one global override genuinely in force, the header badge, the Chat composer pill and the Control Center BrainControl are observed simultaneously and agree — the 2026-07-28 three-way disagreement does not reproduce"
    - "A live global swap's result surface reports the outcome of the swap.set that actually fired, not the deferred per-profile axis's failures"
    - "D-15: the confirm gate still holds live — selecting an engine in All profiles scope dispatches zero WS frames until the confirm button is clicked"
    - "D-11: the live confirm modal lists every affected profile current → new with an accurate pinned-default count"
    - "The live stack is left on the operator's intended engine, with the restored engine recorded"
    - "REQUIREMENTS.md's BSC markers state the post-gap-closure truth and cite the evidence, with no marker asserting more than was observed"
  artifacts:
    - path: ".planning/phases/103-brain-swap-control-surface/103-VALIDATION.md"
      provides: "A second live-checkpoint results table for the gap-closure cycle, alongside (not replacing) the original 103-08-T2 table"
      contains: "103-13-T1"
    - path: ".planning/REQUIREMENTS.md"
      provides: "BSC-01/02/04/05 markers restated from observed evidence"
  key_links:
    - from: ".planning/phases/103-brain-swap-control-surface/103-VALIDATION.md"
      to: "the running Astridr + CodePulse stack"
      via: "operator-attended per-step observations recorded verbatim"
      pattern: "103-13-T1"
---

<objective>
Close gap Group E (BSC-05): steps 4b, 5 and 6 of the 103-08-T2 live checkpoint failed or were not
exercisable, blocked by the Group A and Group B defects. With those closed, re-run them for real.

Purpose: this is the gap where the whole phase's honesty claim is settled. `103-VALIDATION.md`
records that the global axis's READ path (catalogue, 331 live engines) and its FRICTION gate (D-15,
zero dispatch before explicit confirm) are genuinely live-verified, and that the dispatch →
readback → revert leg is NOT. The verifier treats that per-step table as authoritative. Nothing
short of a fresh operator-attended observation against the live stack can change it — and the six
defects this cycle fixes were all invisible to a green 2,813-test suite because `VITE_BRAINS_STUB`
masked them. A green suite is therefore explicitly NOT accepted as evidence for this plan.

Output: a second live-checkpoint results table in `103-VALIDATION.md`, and requirement markers
restated from what was actually observed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/103-brain-swap-control-surface/103-VALIDATION.md
@.planning/phases/103-brain-swap-control-surface/103-VERIFICATION.md
@.planning/phases/103-brain-swap-control-surface/103-09-SUMMARY.md
@.planning/phases/103-brain-swap-control-surface/103-10-SUMMARY.md
@.planning/phases/103-brain-swap-control-surface/103-11-SUMMARY.md
@.planning/phases/103-brain-swap-control-surface/103-12-SUMMARY.md
</context>

<interfaces>
<!-- Prerequisites for the checkpoint:                                                              -->
<!--   Ástríðr running and reachable at ws://127.0.0.1:8181/ws/telemetry                            -->
<!--   CodePulse dev server on :5173 started WITHOUT VITE_BRAINS_STUB=true                          -->
<!-- Note (memory: feedback-ps51-empty-env-var-deletes): in PowerShell 5.1 `$env:VAR=''` DELETES     -->
<!-- the variable and the child then falls back to .env.local. To be certain the stub is OFF,       -->
<!-- confirm the picker shows NO stub banner and the badge shows NO STUB chip once loaded, rather   -->
<!-- than trusting the shell invocation.                                                            -->

<!-- The original per-step table this checkpoint re-runs lives at 103-VALIDATION.md:85-107.         -->
<!-- Step 4a (swap.set genuinely changes the live brain) already PASSED and is not re-litigated.    -->
</interfaces>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Re-run the live global-axis checkpoint, steps 4b / 5 / 6 (103-13-T1)</name>
  <read_first>
    - .planning/phases/103-brain-swap-control-surface/103-VALIDATION.md lines 76-127 — the original 103-08-T2 per-step table, the headline three-surfaces-three-answers finding, and exactly what was and was not proven.
    - .planning/phases/103-brain-swap-control-surface/103-08-PLAN.md lines 139-187 — Task 2's original `<how-to-verify>` steps 1-7 and its acceptance criteria; this checkpoint re-runs the same script.
    - The four gap-closure summaries (103-09 through 103-12) — so the observer knows which behavior each step is now expected to exhibit and can name the plan responsible if a step still fails.
  </read_first>
  <what-built>
    103-09 — one shared resolution order (`src/hooks/useResolvedBrain.ts`) that requests a
    `swap.get_state` snapshot on every connect and is consumed by the header badge, the Chat
    composer pill, and (via the page-level state feeding `ControlCenterPanel`) `BrainControl`.

    103-10 — `convex/activeEngine.ts`'s `recordRouting` converted to an `internalMutation`,
    invoked only through `internal.activeEngine.recordRouting`, and deployed to the self-hosted
    instance.

    103-11 — `CommandItem.onSelect` wired through a shared `handleActivate` so keyboard
    search→arrow→Enter works and still routes through both the cost-confirm branch and the global
    confirm gate; the nested focusable health-dot span removed; a generation guard on the
    scope-driven catalogue fetch.

    103-12 — `GlobalSwapModal` now awaits the real `swap.set`, reports its ack plus the `swap.state`
    readback, sends zero `gateway.model.set` calls for global scope, and survives "Done" so the
    Revert toast action renders a real result.
  </what-built>
  <how-to-verify>
    Prerequisites: Ástríðr up and reachable on its WS telemetry endpoint, and `npm run dev` on
    :5173 started WITHOUT `VITE_BRAINS_STUB=true`.

    A. Pre-flight (confirm you are genuinely live, not stubbed)
       1. Load the dashboard. Open the picker. Confirm there is NO stub banner in the popover and
          NO STUB chip on the badge. If either is present, the stub is on — stop and fix the
          environment before recording anything.

    B. Step 4b — the badge updates from the readback (the step that FAILED on 2026-07-28)
       2. Before touching CodePulse, set a global override from the Control Center (or confirm one
          is already in force). Then RELOAD the page with that override already active.
       3. Read all three surfaces in the same sitting and record each verbatim:
            · the header active-brain badge (top status cluster)
            · the Chat composer pill on `/chat`
            · Control Center `BrainControl`
          Expected: all three name the same engine, and the badge and pill show a "Global"
          qualifier. Any disagreement is a FAIL — record which surface disagreed and what it said.
       4. Now perform a fresh "All profiles" swap to a different engine from the picker. Confirm
          the badge updates to the new engine from the `swap.state` push (the readback), not from
          the ack. Record the engine before and after.

    C. Step 4b, second half — the modal reports the swap it actually fired
       5. During the same swap, read the modal's result surface. Expected: it reports the outcome
          of the `swap.set` that fired. It must NOT show per-profile `union_tag_invalid` rows, and
          it must NOT report 0/N failure for a swap that succeeded. Record the exact text shown.

    D. Step 5 — the revert affordance is offered
       6. Click "Done". Confirm the summary toast appears and offers a "Revert global swap" action.
          Record the toast text.

    E. Step 6 — revert renders and the badge reverts
       7. Click "Revert global swap". Confirm a dialog becomes visible with a revert result row
          (not a silent command). Then confirm the badge returns to the prior engine. Record both.

    F. Carried-over human verifications from 103-VERIFICATION.md
       8. Confirm the live confirm modal lists every affected profile `current → new` and that the
          pinned-default count is accurate against real profile data (VERIFICATION human check #2).
       9. Load `/chat` at a narrow viewport and confirm the composer pill does not displace the send
          affordance (VERIFICATION human check #1).

    G. Confirm the D-15 gate did not regress, and this time do it from the keyboard
      10. Open the picker, move scope to "All profiles", use ONLY the keyboard — type to filter,
          ArrowDown, Enter — and confirm the confirm modal opens with ZERO commands dispatched
          before you click the confirm button. Record it.

    H. Leave the stack clean
      11. Restore the engine the operator wants running and record which engine that is.

    Record which of the eleven observations passed. Any failure here is a real integration failure,
    not a stub limitation — do not write it off, do not soften it, and do not substitute a green
    unit run for an observation you did not make.
  </how-to-verify>
  <action>
Drive the live verification in `<how-to-verify>` against the running stack. Do NOT stub any part of
this run and do NOT set `VITE_BRAINS_STUB=true`.

If an observation fails, capture the exact symptom verbatim — including which surface showed what —
and stop rather than proceeding to sign-off. Name the gap-closure plan (103-09 / 103-10 / 103-11 /
103-12) whose behavior the failure implicates, so a follow-up cycle has a starting point.

Steps that cannot be exercised must be recorded as NOT VERIFIED with the reason, never as passes.
  </action>
  <verify>
    <human-check>All eleven observations in `<how-to-verify>` performed against a running Ástríðr + CodePulse stack with the stub OFF, each result recorded verbatim.</human-check>
  </verify>
  <acceptance_criteria>
    - The pre-flight confirmed no stub banner and no STUB chip — the run is genuinely live.
    - With one global override in force before page load, all three surfaces named the SAME engine (the 2026-07-28 three-way disagreement did not reproduce), and the badge/pill carried the "Global" qualifier.
    - After a live "All profiles" swap, the badge updated to the new engine from the `swap.state` readback; the before and after engine ids are recorded.
    - The modal's result surface reported the `swap.set` outcome and showed no per-profile `union_tag_invalid` rows.
    - The summary toast offered "Revert global swap"; clicking it produced a VISIBLE dialog with a revert result row; the badge then returned to the prior engine.
    - Keyboard-only selection in "All profiles" scope opened the confirm modal with zero commands dispatched pre-confirm.
    - The confirm modal's per-profile `current → new` list and pinned-default count were checked against real profile data.
    - The engine the stack was left running is recorded.
    - ANTI-STUB-MASKING PROOF: this entire task is an operator-attended live observation with the stub OFF, which is the only class of evidence `103-VALIDATION.md` treats as authoritative for BSC-05. Its expected observable is stated per step above. No unit or E2E result may be substituted for any observation.
  </acceptance_criteria>
  <resume-signal>Type "approved" with the per-step results (all eleven), or describe which observation failed, what was displayed instead, and on which surface.</resume-signal>
  <done>The dispatch → readback → revert leg has a real, recorded live outcome — pass or fail — replacing the blocked 2026-07-28 result.</done>
</task>

<task type="auto">
  <name>Task 2: Record the live results and restate the requirement markers (103-13-T2)</name>
  <files>.planning/phases/103-brain-swap-control-surface/103-VALIDATION.md, .planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/phases/103-brain-swap-control-surface/103-VALIDATION.md (whole file) — especially the 103-08-T2 section (76-193), the "Manual-Only Verifications" table (289-294), the sign-off list (297-307), and "What This Phase Does NOT Claim" (326-334).
    - .planning/REQUIREMENTS.md lines 9-19 — the Phase 103 requirement block and the CURRENT wording of the BSC-01/BSC-02 markers.
    - .planning/phases/103-brain-swap-control-surface/103-VERIFICATION.md — the gap list this cycle set out to close, and its "Note on REQUIREMENTS.md drift".
    - Task 1's recorded per-step results.
  </read_first>
  <action>
Record the outcome; do not rewrite history.

In `103-VALIDATION.md`, ADD a new section "Gap-Closure Live Re-Verification (103-13-T1,
{date})" immediately after the existing "Live Global-Axis Verification (103-08-T2)" section. Do NOT
edit, delete, or soften the original 103-08-T2 table — the two runs sit side by side so the
before/after is legible. The new section contains:
  - a per-observation table (the eleven observations from Task 1) with verbatim results,
  - a replacement for the "three surfaces, three answers" headline finding stating what the three
    surfaces showed THIS time,
  - an updated "Manual-Only Verifications" result column for the global-swap row and the composer-pill
    row, and
  - an updated sign-off bullet for the global-axis manual verification stating exactly what is now
    proven and what still is not.

Then update `.planning/REQUIREMENTS.md`'s Phase 103 block. IMPORTANT — verify before you edit: the
stale "✅ satisfied 2026-07-28" markers on BSC-01 and BSC-02 that `103-VERIFICATION.md` flagged were
ALREADY corrected in commit `0b354131` ("correct false BSC-01/02 markers"); both lines currently
read "⚠ NOT satisfied / partial — gap closure required". Read the live file before changing
anything and do not re-apply a correction that has already landed. The remaining work is to restate
BSC-01, BSC-02, BSC-04 and BSC-05 from THIS cycle's observed evidence:
  - Each marker must cite what was observed, on which surface, and where the evidence lives
    (`103-VALIDATION.md`'s new section).
  - A requirement whose live observation FAILED or was not exercisable stays unsatisfied, with the
    reason. Do not mark anything satisfied that Task 1 did not observe.
  - BSC-05's per-profile half stays explicitly deferred to astridr Phase 184.1 — that deferral is
    by design and is not a defect. Do not close it and do not report it as one.
  - BSC-03 was already verified and is untouched by this cycle except that 103-11 and 103-12 both
    carry regression guards on its D-15 confirm gate; note that the gate was re-confirmed live
    (observation 10) rather than re-marking the requirement.

Every claim added to either file must be traceable to a recorded observation or a named command
output. Nothing derived from a SUMMARY-level assertion — writing markers from SUMMARY claims before
the live checkpoint ran is exactly what produced the stale markers this cycle exists to prevent.
  </action>
  <verify>
    <automated>grep -c "103-13-T1" .planning/phases/103-brain-swap-control-surface/103-VALIDATION.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Live Global-Axis Verification (103-08-T2)" .planning/phases/103-brain-swap-control-surface/103-VALIDATION.md` still returns 1 — the original section survives untouched.
    - `grep -c "103-13-T1" .planning/phases/103-brain-swap-control-surface/103-VALIDATION.md` returns at least 1 — the new section exists.
    - The new section contains one row per observation from Task 1, each with a verbatim result, and no row is omitted.
    - `grep -c "satisfied 2026-07-28" .planning/REQUIREMENTS.md` returns 0 — no revived stale marker.
    - Every BSC marker in `.planning/REQUIREMENTS.md`'s Phase 103 block cites `103-VALIDATION.md`'s new section or a named command output as its evidence; no marker asserts a status Task 1 did not observe.
    - BSC-05's per-profile half is still recorded as deferred to astridr Phase 184.1 and is not reported as a defect.
    - ANTI-STUB-MASKING PROOF: this task records evidence rather than producing behavior. Its guard is provenance — every marker traces to an operator-attended stub-off observation from Task 1 or to a named command output, and no marker may be written from a SUMMARY-level claim. That provenance rule is the direct countermeasure to the failure that produced the original stale markers.
  </acceptance_criteria>
  <done>The gap-closure cycle's live outcome is recorded honestly, and the requirement markers state only what was observed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator → live Ástríðr process | The checkpoint deliberately mutates the running reasoning engine, process-wide, on a stack the operator uses for real work |
| Planning artifacts → future phases | `103-VALIDATION.md` and `REQUIREMENTS.md` are read as ground truth by later verification and milestone audits |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-103-50 | Tampering | live process state left on an unintended engine after the checkpoint | mitigate | Observation 11 requires restoring the operator's intended engine and recording which engine that is, so the session cannot silently leave the stack on a test model. |
| T-103-51 | Repudiation | recording a pass for an observation that was not made | mitigate | Every acceptance criterion demands a verbatim recorded result; steps that could not be exercised must be written as NOT VERIFIED with a reason. No unit or E2E result may substitute for an observation — the same rule that made the original 103-08-T2 table trustworthy. |
| T-103-52 | Tampering | a stub-on run recorded as live verification | mitigate | Observation 1 is a pre-flight that checks for the absence of the stub banner and STUB chip in the rendered UI, rather than trusting the shell invocation — PowerShell 5.1's `$env:VAR=''` deletes the variable and silently falls back to `.env.local`. |
| T-103-53 | Information Disclosure | recording live payloads into planning artifacts | mitigate | Record engine IDs, profile names, and UI copy only. Do not paste raw WS frames, bearer tokens, or `.env` values into `103-VALIDATION.md`; the repo is under version control. |
| T-103-54 | Tampering | overwriting the original failing checkpoint record | mitigate | The new section is ADDED after the 103-08-T2 section, which stays byte-intact; an acceptance criterion asserts the original heading still exists exactly once. |
| T-103-SC | Tampering | npm/pip/cargo installs | mitigate | This plan installs nothing. Any install proposal stops the plan for a blocking human checkpoint. |
</threat_model>

<verification>
- Task 1's eleven observations are recorded verbatim, including any failures.
- `103-VALIDATION.md` contains both the original 103-08-T2 table and the new 103-13-T1 table.
- `.planning/REQUIREMENTS.md`'s Phase 103 markers each cite observed evidence.
- The live stack was left on a recorded, intended engine.
</verification>

<success_criteria>
- Steps 4b, 5 and 6 of the live checkpoint have a real recorded outcome for the first time.
- Three brain surfaces agree on one live global override, or the disagreement is recorded verbatim with the offending surface named.
- No requirement marker asserts more than was observed.
- BSC-05's per-profile half remains honestly deferred.
</success_criteria>

<output>
Create `.planning/phases/103-brain-swap-control-surface/103-13-SUMMARY.md` when done.
</output>
