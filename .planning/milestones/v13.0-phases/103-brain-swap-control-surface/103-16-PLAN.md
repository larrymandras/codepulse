---
phase: 103-brain-swap-control-surface
plan: 16
type: execute
wave: 4
depends_on: ["103-12", "103-14"]
gap_closure: true
files_modified:
  - src/components/brains/GlobalSwapModal.tsx
  - src/components/brains/GlobalSwapModal.test.tsx
  - src/components/brains/BrainPicker.tsx
  - src/components/brains/BrainPicker.test.tsx
autonomous: true
requirements: [BSC-02, BSC-04]
user_setup: []

must_haves:
  truths:
    - "Reselecting the SAME brain after a completed global swap opens a FRESH confirm prompt, not the stale result of the previous swap"
    - "Reselecting the same brain after a FAILED global swap restores the retry path — a confirm button, not a dead 'Done'"
    - "103-12/CR-03 is NOT regressed: the modal instance still survives \"Done\", so a \"Revert global swap\" toast clicked later still reopens a LIVE instance and renders a real result"
    - "The reset is keyed to a fresh user SELECTION, not to a change in `target.id`"
    - "D-14 and D-15 are unchanged in strength: no optimistic confirmation, and zero commands dispatched before the explicit confirm click"
  artifacts:
    - path: "src/components/brains/GlobalSwapModal.tsx"
      provides: "A reset keyed to selection identity rather than target identity"
      contains: "selection"
    - path: "src/components/brains/BrainPicker.test.tsx"
      provides: "Coverage for the reselect-same-brain path with a REAL GlobalSwapModal, not the mock"
      contains: "reselect"
  key_links:
    - from: "src/components/brains/BrainPicker.tsx"
      to: "src/components/brains/GlobalSwapModal.tsx"
      via: "a per-selection nonce that changes on every activation, including a repeat of the same brain"
      pattern: "selection"
---

<objective>
Close code-review finding CR-01 (Critical), confirmed LIVE against the running stack on 2026-07-29.

`GlobalSwapModal`'s reset effect (`GlobalSwapModal.tsx:197-209`) early-returns when
`target.id === prevTargetIdRef.current`. 103-12's CR-03 fix intentionally stopped nulling
`globalTarget` on close (`BrainPicker.tsx:190-194, 504-511`) so the instance survives "Done" and a
later revert can reopen it. Together those two facts mean reselecting the SAME brain never resets
the modal.

Live evidence (2026-07-29): swap all profiles to Claude Haiku 4.5 → confirm → Done → reselect
Claude Haiku 4.5 again. Observed dialog:

    Swap all profiles to Claude Haiku 4.5?
    Switched to Claude Haiku 4.5.
    Profiles now governed by the global override:
    consulting / business / personal
    Done

Measured: `confirm button = 0, cancel = 0, done = 1`. The user is shown the PREVIOUS swap's result
as though it were the current one, with no way to proceed. After a FAILED swap the same path
removes the retry affordance entirely.

The fix must close this WITHOUT regressing CR-03 — the modal must still survive "Done" so a
"Revert global swap" toast clicked minutes later reopens a live instance.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
@$HOME/.claude/get-shit-done/references/tdd.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/103-brain-swap-control-surface/103-CONTRACT.md
@.planning/phases/103-brain-swap-control-surface/103-REVIEW.md
@.planning/phases/103-brain-swap-control-surface/103-12-SUMMARY.md
@.planning/phases/103-brain-swap-control-surface/103-14-SUMMARY.md
</context>

<interfaces>
<!-- src/components/brains/GlobalSwapModal.tsx                                                   -->
<!--   :197-209  reset effect, guarded on `target.id === prevTargetIdRef.current` — the defect    -->
<!--   :211-220  D-14 readback effect (modelOverride === confirmTarget) — DO NOT weaken           -->
<!-- src/components/brains/BrainPicker.tsx                                                        -->
<!--   :188-194  pendingTarget / globalTarget (MOUNT guard) / globalDialogOpen (VISIBILITY)        -->
<!--   :504-511  `{globalTarget && <GlobalSwapModal target open={globalDialogOpen} .../>}`         -->
<!--   handleActivate / handleSelect — where a global-scope activation is turned into a modal open -->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Key the modal reset to a fresh selection, not to target identity (103-16-T1)</name>
  <files>src/components/brains/BrainPicker.tsx, src/components/brains/GlobalSwapModal.tsx</files>
  <action>
Introduce a per-selection identity that changes on EVERY global-scope activation, including a
repeat activation of the same brain — for example a monotonically incrementing nonce in
`BrainPicker` set in the same handler that assigns `globalTarget` and opens the dialog, passed to
`GlobalSwapModal` as a prop.

In `GlobalSwapModal`, reset on a change to that selection identity instead of on `target.id`.
Keep everything the reset currently clears (phase, outcome, isBusy, lastAction, snapshot,
revertRestoredName, priorOverrideRef, priorOverrideDisplayNameRef, confirm timeout).

CRITICAL — do NOT regress 103-12/CR-03. The reset must still NOT fire on a mere `open`
transition. A revert triggered from the toast reopens the modal WITHOUT a new selection, so the
selection identity is unchanged and the snapshot/outcome the revert result depends on must
survive. Changing the guard from `target.id` to `open` (the pre-103-12 behavior) reintroduces the
exact bug CR-03 closed — do not do that.

Read `103-12-SUMMARY.md` before writing this so the CR-03 invariant is understood, not guessed.
  </action>
  <verify>
    <automated>npx vitest run src/components/brains/GlobalSwapModal.test.tsx src/components/brains/BrainPicker.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Reselecting the same brain after a completed swap yields `phase: "confirm"` with a confirm button present.
    - Reopening WITHOUT a new selection (the toast-revert path) does NOT reset — snapshot and outcome survive, and the revert still renders a real result row.
    - MUTATION CHECK: revert the guard to `target.id` equality and confirm the new reselect test fails; then revert the guard to `open` and confirm a CR-03 regression test fails. Restore afterwards. Both mutations must be run for real, not reasoned about.
  </acceptance_criteria>
  <done>A repeat selection of the same brain behaves like a first selection.</done>
</task>

<task type="auto">
  <name>Task 2: Cover the reselect path against a REAL modal (103-16-T2)</name>
  <files>src/components/brains/BrainPicker.test.tsx, src/components/brains/GlobalSwapModal.test.tsx</files>
  <action>
The reviewer noted this scenario was invisible because `BrainPicker.test.tsx` fully MOCKS
`GlobalSwapModal` (see its `data-testid="global-swap-modal"` stub), so no existing test could ever
observe the modal's internal phase.

Add coverage that exercises the real component for this path — either an integration-style test in
`BrainPicker.test.tsx` that does not mock `GlobalSwapModal` for these cases, or an equivalent
`GlobalSwapModal.test.tsx` test driving the selection-identity prop directly. Whichever you choose,
the test must fail if Task 1 is reverted.

Cover:
  (a) select brain X → confirm → Done → select X again → fresh confirm phase, confirm button present
  (b) select X → confirm → swap FAILS → Done → select X again → retry is available (confirm button, not a dead Done)
  (c) select X → confirm → Done → toast revert (no new selection) → the instance still renders a real revert result (CR-03 guard)
  (d) select X → Done → select Y → fresh confirm (the pre-existing behavior still works)

Also state explicitly in the SUMMARY that `BrainPicker.test.tsx`'s modal mock is why the original
defect shipped, so the next reviewer does not have to rediscover it.
  </action>
  <verify>
    <automated>npx vitest run src/components/brains/BrainPicker.test.tsx src/components/brains/GlobalSwapModal.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - All four scenarios covered and passing.
    - At least one of the new tests exercises the REAL `GlobalSwapModal`, not the mock.
    - `npx tsc --noEmit` clean; full suite passes with no reduction from the 2854-test baseline.
  </acceptance_criteria>
  <done>The reselect path can never silently regress again.</done>
</task>

</tasks>

<verification>
- Both mutation checks in Task 1 performed for real and restored.
- `npx tsc --noEmit` clean; full suite >= 2854 passing.
- Live re-verification (reselect the same brain after a completed swap against the running stack)
  is performed by the orchestrator after this plan lands. This plan does not claim it; a green
  suite is not accepted as evidence for it.
</verification>

<success_criteria>
- Reselecting the same brain gives a fresh confirm prompt.
- A failed swap keeps its retry path.
- CR-03 stays closed.
</success_criteria>

<output>
Create `.planning/phases/103-brain-swap-control-surface/103-16-SUMMARY.md` when done.
</output>
