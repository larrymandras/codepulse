---
phase: 103-brain-swap-control-surface
plan: 09
type: execute
wave: 1
depends_on: []
gap_closure: true
files_modified:
  - src/hooks/useResolvedBrain.ts
  - src/hooks/useResolvedBrain.test.tsx
  - src/components/brains/BrainHeaderBadge.tsx
  - src/components/brains/BrainHeaderBadge.test.tsx
  - src/pages/Chat.tsx
autonomous: true
requirements: [BSC-01]
user_setup: []

must_haves:
  truths:
    - "With a global override already in force BEFORE the page loads, the header badge shows that override's model id, not 'No brain reported'"
    - "With a global override already in force BEFORE the page loads, the Chat composer pill shows that override's model id, not 'Auto'"
    - "The header badge, the Chat composer pill, and the Control Center BrainControl all read the SAME global-override value from ONE module — three surfaces cannot render three different answers for one live override"
    - "The resolution order is global override first, then the per-profile telemetry snapshot, then an explicit no-reading fallback — matching 103-CONTRACT.md §9's router.py precedence where the global override (rung 2) shadows the per-profile default (rung 4)"
    - "A global-override reading is always visibly and accessibly qualified as global, never presented as an honest per-profile reading"
    - "D-14: the displayed engine comes only from server-reported state (swap.get_state ack / swap.state push / Convex telemetry), never from a client assertion or an optimistic guess"
    - "The global snapshot is re-requested on every reconnect, so a swap made while this tab's socket was down does not leave a stale reading"
  artifacts:
    - path: "src/hooks/useResolvedBrain.ts"
      provides: "The single shared what-brain-is-actually-running resolution order: swap.get_state snapshot + swap.state subscription + pure resolveActiveBrain()"
      exports: ["useGlobalBrainOverride", "resolveActiveBrain", "useResolvedBrain"]
      min_lines: 60
    - path: "src/hooks/useResolvedBrain.test.tsx"
      provides: "Proof that swap.get_state is sent on connect and that a pre-existing override resolves with zero swap.state events"
    - path: "src/components/brains/BrainHeaderBadge.tsx"
      provides: "Badge reading through the shared resolver instead of its own subscribe-only fallback"
      contains: "useResolvedBrain"
    - path: "src/pages/Chat.tsx"
      provides: "Composer pill and page-level swap state both reading through the shared resolver"
      contains: "useGlobalBrainOverride"
  key_links:
    - from: "src/hooks/useResolvedBrain.ts"
      to: "Astridr WS swap.get_state"
      via: "sendCommand snapshot request on every connected transition"
      pattern: "swap\\.get_state"
    - from: "src/components/brains/BrainHeaderBadge.tsx"
      to: "src/hooks/useResolvedBrain.ts"
      via: "useResolvedBrain() replaces useGlobalEngineFallback()"
      pattern: "useResolvedBrain"
    - from: "src/pages/Chat.tsx"
      to: "src/hooks/useResolvedBrain.ts"
      via: "useGlobalBrainOverride() replaces the inline swapState useState + two effects; its value still feeds ControlCenterPanel's swapModelOverride prop"
      pattern: "useGlobalBrainOverride"
---

<objective>
Close gap Group A (defects 6a + 6b, BSC-01): every brain surface this phase built is blind to a
global override that was already active when the page loaded.

Purpose: on 2026-07-28 the operator had `claude-haiku-4-5-20251001` genuinely in force as a global
override and watched three surfaces report three different answers — `BrainControl` correct,
`BrainHeaderBadge` "No brain reported", the Chat composer pill "Auto". That is precisely the
"stale read presented as live state" failure BSC-01 exists to eliminate, and this phase added two
of the three wrong voices. The root cause is one shared class of bug: `swap.state` is a CHANGE
event, and nobody except `Chat.tsx:309` ever asks for a SNAPSHOT with `swap.get_state`.

The fix is explicitly NOT two independent patches. `103-VALIDATION.md`'s own gap-closure guidance:
"resolve 6a and 6b together via one shared source of truth ... consumed by the badge, the composer
pill, and `BrainControl` alike. Fixing them independently reproduces the three-way disagreement in
a new shape."

Output: `src/hooks/useResolvedBrain.ts` (+ test), and three consumers rewired onto it.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/103-brain-swap-control-surface/103-CONTEXT.md
@.planning/phases/103-brain-swap-control-surface/103-CONTRACT.md
@.planning/phases/103-brain-swap-control-surface/103-VERIFICATION.md
@.planning/phases/103-brain-swap-control-surface/103-VALIDATION.md
@.planning/phases/103-brain-swap-control-surface/103-UI-SPEC.md
</context>

<interfaces>
<!-- The WS context API the shared hook must use (src/contexts/AstridrWSContext.tsx):            -->
<!--   status: "connected" | "reconnecting" | "disconnected"                                      -->
<!--   sendCommand: (cmd: Record<string, unknown>) => Promise<AckResponse>                         -->
<!--   subscribeEvent: (eventType: string, cb: (event: unknown) => void) => () => void  (returns   -->
<!--     the unsubscribe function directly — return it straight from useEffect)                    -->
<!--   useAstridrWS() THROWS when rendered outside AstridrWSProvider (AstridrWSContext.tsx:105).   -->

<!-- The snapshot ack shape (already consumed verbatim at Chat.tsx:309-314):                       -->
<!--   ack.status === "ok"; ack.model_override: string | null; ack.voice_override_name: string|null-->
<!-- The push shape (already consumed verbatim at Chat.tsx:342-348):                               -->
<!--   event.data.model_override; event.data.voice_override_name                                   -->

<!-- The per-profile axis (src/hooks/useActiveEngine.ts, unchanged by this plan):                  -->
<!--   useActiveEngine(): ActiveEngineMap  // Record<profileId, ActiveEngine | null>               -->
<!--   deriveMixedState(map): { mixed: boolean; distinctModels: string[]; single?: ActiveEngine }  -->
<!--   ActiveEngine carries at least: model, mode ("pinned"|"inherited"|"session"), expiresAt?     -->

<!-- 103-CONTRACT.md §9 — router.py's real resolution precedence, which this hook must mirror:     -->
<!--   1 explicit_model · 2 GLOBAL override (swap.set) · 3 session override · 4 per-profile default -->
<!-- The global override genuinely governs every turn while in force, so global-first is the       -->
<!-- HONEST order, not a shortcut.                                                                 -->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create the one shared brain-resolution module (103-09-T1)</name>
  <files>src/hooks/useResolvedBrain.ts, src/hooks/useResolvedBrain.test.tsx</files>
  <read_first>
    - src/pages/Chat.tsx lines 290-355 — the EXISTING, CORRECT `swap.get_state` snapshot + `swap.state` subscription pair this module generalizes. Line 309 is the named precedent; do not invent a new idiom.
    - src/contexts/AstridrWSContext.tsx lines 23-115 and 392-470 — `status`, `sendCommand`, `subscribeEvent` signatures and the out-of-provider throw at line 105.
    - src/hooks/useActiveEngine.ts (whole file) — `ActiveEngineMap`, `deriveMixedState`, `MixedState`, and its D-14 docstring on never fabricating a reading.
    - src/components/brains/BrainHeaderBadge.tsx lines 51-91 — `useGlobalEngineFallback`, the subscribe-only hook being replaced, including its rules-of-hooks-safe try/catch degrade which must be preserved.
    - src/hooks/useActiveEngine.test.ts — the in-repo hook-test pattern to follow.
    - .planning/phases/103-brain-swap-control-surface/103-CONTRACT.md §9 (lines 232-258) — the router.py precedence justifying global-first.
  </read_first>
  <action>
Create `src/hooks/useResolvedBrain.ts` exporting exactly three things.

1. `useGlobalBrainOverride(): { modelOverride: string | null; voiceOverride: string | null }`
   - Calls `useAstridrWS()` inside a try/catch, mirroring `useGlobalEngineFallback`'s existing
     degrade: on throw, set the local `subscribeEvent`/`sendCommand`/`status` locals to null/
     undefined and return `{ modelOverride: null, voiceOverride: null }`. Keep the existing
     rules-of-hooks safety comment — the hook count per render must not change between branches.
   - Effect A (THE FIX): when `status === "connected"`, `await sendCommand({ type: "swap.get_state" })`
     and, on `ack.status === "ok"`, set state from `ack.model_override` and `ack.voice_override_name`.
     Depend on `status` so it re-hydrates on every reconnect, not just first mount — a swap made
     while this tab's socket was down must not leave a stale reading (the WR-07 lesson already
     recorded at Chat.tsx:303-306). On a non-ok ack or a throw, `console.warn` and leave the prior
     value; do not silently clear it.
   - Effect B: `subscribeEvent("swap.state", ...)` reading `event.data.model_override` and
     `event.data.voice_override_name`, returning the unsubscribe directly.
   - Coerce both fields defensively: anything that is not a non-empty `string` becomes `null`.
     Never render a non-string payload field.

2. `resolveActiveBrain(args: { globalOverride: string | null; activeEngines: ActiveEngineMap; profileId?: string }): ResolvedBrain`
   — PURE, no React, exported for direct unit test. `ResolvedBrain` is
   `{ source: "global" | "profile" | "mixed" | "none"; model: string | null; mode?: ActiveEngine["mode"]; expiresAt?: number; distinctModels: string[] }`.
   Resolution order, in this exact precedence (103-CONTRACT.md §9):
     a. `globalOverride` non-null → `{ source: "global", model: globalOverride, distinctModels: [] }`.
        This wins even when per-profile telemetry exists, because the global override genuinely
        shadows the per-profile default at rung 2 vs rung 4 — showing the per-profile value while a
        global override governs the turn IS the BSC-01 trap.
     b. `profileId` supplied → that profile's `ActiveEngine` → `{ source: "profile", model, mode, expiresAt }`;
        if the entry is null/absent → `{ source: "none", model: null }`.
     c. `profileId` omitted → `deriveMixedState(activeEngines)`; `mixed` → `{ source: "mixed", model: null, distinctModels }`;
        a single agreed engine → `{ source: "profile", ... }`; nothing reported → `{ source: "none" }`.

3. `useResolvedBrain(profileId?: string): ResolvedBrain` — composes `useGlobalBrainOverride()` +
   `useActiveEngine()` + `resolveActiveBrain`, memoized on its inputs.

Add `src/hooks/useResolvedBrain.test.tsx` with `renderHook`/`render` and a mocked
`@/contexts/AstridrWSContext`, covering at minimum:
  - `sendCommand` is called with exactly `{ type: "swap.get_state" }` once `status` is `"connected"`,
    and is NOT called while status is `"disconnected"`.
  - With the mocked ack returning `{ status: "ok", model_override: "claude-haiku-4-5-20251001" }`
    and ZERO `swap.state` events ever emitted, `useResolvedBrain()` resolves
    `{ source: "global", model: "claude-haiku-4-5-20251001" }`. This is the exact live-observed
    failure, reproduced as a regression test.
  - A `status` transition `connected → disconnected → connected` issues a SECOND `swap.get_state`.
  - `resolveActiveBrain` unit cases: global-beats-profile, profile-when-no-global,
    mixed-when-no-global-and-disagreeing, none-when-nothing-reported.
  - Rendering outside the provider (mock `useAstridrWS` to throw) returns nulls and does not throw.
  </action>
  <verify>
    <automated>npx vitest run src/hooks/useResolvedBrain.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "swap.get_state" src/hooks/useResolvedBrain.ts` returns at least 1.
    - `npx vitest run src/hooks/useResolvedBrain.test.tsx` passes with at least 6 tests.
    - `npx tsc --noEmit` exits 0.
    - ANTI-STUB-MASKING PROOF: the "pre-existing override with zero swap.state events" test asserts on the mocked WS `sendCommand` frame (`{ type: "swap.get_state" }`) and its ack payload — the global axis directly. `VITE_BRAINS_STUB` gates only `src/lib/brainsApi.ts` (module-scope read at brainsApi.ts:212), and this module imports nothing from `brainsApi`. Confirm with `grep -c "brainsApi" src/hooks/useResolvedBrain.ts` returning 0 — the stub cannot fabricate, mask, or satisfy this test either way.
    - The test asserting a second `swap.get_state` after a reconnect fails if the effect's dependency array is narrowed to `[]` (mount-only) — mutate it locally to confirm before claiming the guard works.
  </acceptance_criteria>
  <done>One module owns the global-override snapshot + subscription and the full resolution order, with a regression test reproducing the exact live-observed blindness.</done>
</task>

<task type="auto">
  <name>Task 2: Rewire BrainHeaderBadge onto the shared resolver (103-09-T2)</name>
  <files>src/components/brains/BrainHeaderBadge.tsx, src/components/brains/BrainHeaderBadge.test.tsx</files>
  <read_first>
    - src/components/brains/BrainHeaderBadge.tsx (whole file) — current state, including the `usingGlobalFallback` label/chip/aria logic and the `isConfirmedLive` pulse gate at line 164.
    - src/hooks/useResolvedBrain.ts (as written in Task 1).
    - src/components/brains/BrainHeaderBadge.test.tsx (whole file) — existing mocks and assertions that must keep passing.
    - .planning/phases/103-brain-swap-control-surface/103-UI-SPEC.md §2 (lines 134-140) — the badge contract: aria-label always present, Mixed-brains stacked dots, confirmed-live pulse only when genuinely server-confirmed and not pending/stub.
  </read_first>
  <action>
Delete `useGlobalEngineFallback` from `BrainHeaderBadge.tsx` entirely (it is now the shared module's
job) and consume `useResolvedBrain()` with no `profileId` — the badge is dashboard-wide.

Drive every existing visual from `resolved.source`:
  - `source === "global"` → `baseLabel = resolved.model`; keep the existing
    `data-testid="brain-header-badge-global-chip"` "Global" chip and keep folding " (global)" into
    the `aria-label`, because an explicit `aria-label` replaces all descendant text for assistive
    tech. The global reading must never present itself as an honest per-profile reading.
  - `source === "mixed"` → "Mixed brains" + the existing stacked `distinctModels` dots.
  - `source === "profile"` → `resolved.model` + the existing single provider dot, and keep the
    session/pinned suffixes keyed off `resolved.mode` / `resolved.expiresAt`.
  - `source === "none"` → "No brain reported".

Update `isConfirmedLive` (the trust-signal pulse dot): render it when `source` is `"global"` OR
`"profile"`, and never while `pendingLabel` is set. Keep the `!BRAINS_STUB_ACTIVE` guard for
`source === "profile"` only — `VITE_BRAINS_STUB` gates the per-profile seam, and the global axis is
live regardless (D-16 amendment: labelling live global data as stub is itself an honesty failure).

Pass `entryScope="global"` to `BrainPicker` when `source` is `"global"` or `"mixed"` — opening the
picker from a badge that is showing a global reading should land on "All profiles" scope. This is
D-08's one-time contextual entry default, already consumed at most once by `BrainPicker`'s
`consumedEntryScope` ref; it is not a persisted preference.

Extend `BrainHeaderBadge.test.tsx` with a test that mounts the badge with an EMPTY `useActiveEngine`
map and a mocked `swap.get_state` ack carrying `model_override: "claude-haiku-4-5-20251001"`, and
asserts the button's accessible name is exactly
`Active brain: claude-haiku-4-5-20251001 (global)` and that the "Global" chip is present. Also
assert the badge still reads "Mixed brains" when there is NO global override and two profiles
disagree (the global path must not swallow the honest mixed reading).
  </action>
  <verify>
    <automated>npx vitest run src/components/brains/BrainHeaderBadge.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "useGlobalEngineFallback" src/components/brains/BrainHeaderBadge.tsx` returns 0.
    - `grep -c "useResolvedBrain" src/components/brains/BrainHeaderBadge.tsx` returns at least 1.
    - A test asserts the accessible name `Active brain: claude-haiku-4-5-20251001 (global)` with an empty per-profile map and zero `swap.state` events emitted.
    - A test asserts `Mixed brains` still renders when `globalOverride` is null and two profiles report different models.
    - `npx vitest run src/components/brains/BrainHeaderBadge.test.tsx` passes; `npx tsc --noEmit` exits 0.
    - ANTI-STUB-MASKING PROOF: the new "already-active override" test supplies its value through the mocked `useAstridrWS().sendCommand` ack — the live global axis — not through `brainsApi`. Run the test file with `VITE_BRAINS_STUB` unset and confirm it passes; the assertion does not read `BRAINS_STUB_ACTIVE` at all, so a stub build cannot satisfy it.
  </acceptance_criteria>
  <done>The dashboard badge reports an override that was active before page load, correctly qualified as global.</done>
</task>

<task type="auto">
  <name>Task 3: Rewire Chat.tsx — composer pill and page-level swap state onto the same module (103-09-T3)</name>
  <files>src/pages/Chat.tsx</files>
  <read_first>
    - src/pages/Chat.tsx lines 89-180 — `BrainComposerPill`, including the D-05 host-correction docstring and the `title` string that currently claims the pill reflects the per-profile default.
    - src/pages/Chat.tsx lines 290-355 — the inline `swapState` useState and its two effects (`swap.get_state` hydration, `swap.state` subscription) being replaced.
    - src/pages/Chat.tsx line 771 — `swapModelOverride={swapState.modelOverride}` and `swapVoiceOverride={swapState.voiceOverride}` feeding `ControlCenterPanel`.
    - src/components/control-center/ControlCenterPanel.tsx line 278 — `<BrainControl override={swapModelOverride} ... />`, the pre-existing surface that was the only correct one and must stay correct.
    - src/hooks/useResolvedBrain.ts (as written in Task 1).
  </read_first>
  <action>
Two edits in `src/pages/Chat.tsx`, both pointing at the same module so the page cannot hold two
copies of the truth.

1. Page level: replace the inline `swapState` `useState` and BOTH inline effects with
   `const swapState = useGlobalBrainOverride();`. Keep the existing prop wiring at line 771
   unchanged — `swapModelOverride={swapState.modelOverride}` and
   `swapVoiceOverride={swapState.voiceOverride}` must still feed `ControlCenterPanel`, so
   `BrainControl` keeps rendering the same correct value it renders today, now sourced from the
   shared module rather than its own private copy. Preserve the existing in-memory-only /
   never-persisted comment block (lines 290-296) — that reasoning still applies; only the
   implementation moves.

2. `BrainComposerPill`: replace `const activeEngines = useActiveEngine(); const engine = activeEngines[profileId] ?? null;`
   with `const resolved = useResolvedBrain(profileId);`. Derive `baseLabel` from `resolved.model`,
   falling back to "Auto" only when `resolved.source === "none"`. When `resolved.source === "global"`,
   append the same "Global" qualifier treatment the badge uses so the pill cannot present a global
   override as the per-profile default, and REPLACE the now-false `title` string
   ("This surface reflects the per-profile default, distinct from any active global override") with
   copy that states which axis is actually being shown, keyed off `resolved.source`. Keep the
   provider-color dot resolution reading `resolved.model` against the catalogue, and keep the
   deliberate non-reuse of `BrainControl`'s `Brain` icon (T-103-29).

Do not touch the pill's DOM position in the composer — layout is a separate open human-verification
item (103-VERIFICATION.md human_verification #1) and is out of scope here.
  </action>
  <verify>
    <automated>npx vitest run src/pages/Chat.test.tsx src/components/control-center/ControlCenterPanel.test.tsx && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "useGlobalBrainOverride\|useResolvedBrain" src/pages/Chat.tsx` returns at least 2.
    - `grep -c 'type: "swap.get_state"' src/pages/Chat.tsx` returns 0 — the raw snapshot call now lives only in the shared module. Confirm the module still has it: `grep -c "swap.get_state" src/hooks/useResolvedBrain.ts` >= 1.
    - `grep -c 'subscribeEvent("swap.state"' src/pages/Chat.tsx` returns 0.
    - `grep -c "swapModelOverride={swapState.modelOverride}" src/pages/Chat.tsx` returns 1 — BrainControl's wiring is preserved, not rewritten.
    - `grep -c "distinct from any active global override" src/pages/Chat.tsx` returns 0 — the false pill title is gone.
    - `npx tsc --noEmit` exits 0 and the whole suite is green: `npm test`.
    - ANTI-STUB-MASKING PROOF: the pill's engine value no longer originates from `brainsApi.getDefaultProfileId()`'s deferred per-profile adapter. Confirm structurally — inside `BrainComposerPill`, `resolved.model` is the only source feeding `baseLabel`, and `brainsApi` is used solely for catalogue display metadata (the provider dot color). With the stub OFF and a global override in force, the pill's value comes from the `swap.get_state` ack; the stub has no code path that can produce it. Group E (103-13) confirms this observationally on the live stack.
  </acceptance_criteria>
  <done>All three surfaces — badge, composer pill, and Control Center BrainControl — read one global-override value from one module.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Ástríðr WS → browser | `swap.get_state` acks and `swap.state` pushes are untrusted input rendered directly into the accessible name of a dashboard-wide control |
| Convex → browser | `activeEngineSnapshots` rows (per-profile axis) render with a trust-signal pulse dot |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-103-31 | Information Disclosure | `swap.get_state` ack | accept | The ack carries only the active model id and voice name — already displayed by the pre-existing `BrainControl` on the same socket. No new endpoint, no secret, no PII; nothing is added to the exposure surface. |
| T-103-32 | Spoofing | `swap.state` / `swap.get_state` payload fields | mitigate | `useGlobalBrainOverride` coerces `model_override` and `voice_override_name` to `string \| null` and treats any non-string as `null` before it can reach a label or `aria-label`. Values are rendered as text nodes only — no `dangerouslySetInnerHTML` anywhere in the touched files. |
| T-103-33 | Tampering (stale state) | reconnect handling | mitigate | The snapshot effect keys on `status`, re-issuing `swap.get_state` on every `connected` transition, so a swap performed while this tab's socket was down cannot leave a stale reading presented as live (the exact BSC-01 failure class). |
| T-103-34 | Repudiation | global-vs-profile provenance | mitigate | A `source === "global"` reading always carries the "Global" chip AND the " (global)" suffix inside the `aria-label`, so an operator can never mistake a process-wide override for that profile's own configured brain. |
| T-103-SC | Tampering | npm/pip/cargo installs | mitigate | This plan adds ZERO new dependencies. If any package install becomes necessary, stop and raise a blocking human checkpoint; `103-RESEARCH.md` carries no Package Legitimacy Audit row for a new package, so any `[ASSUMED]` package must be verified on npmjs.com before install. |
</threat_model>

<verification>
- `npx vitest run src/hooks/useResolvedBrain.test.tsx src/components/brains/BrainHeaderBadge.test.tsx` passes.
- `npm test` green, `npx tsc --noEmit` exits 0.
- `grep -rn "subscribeEvent(\"swap.state\"" src/ | grep -v useResolvedBrain.ts` returns no rows outside the shared module — one subscription owner, not three.
- Group E (103-13) confirms the behavior observationally against the live stack; a green suite alone is explicitly NOT accepted as proof for this gap.
</verification>

<success_criteria>
- With a global override active before page load, the header badge and the Chat composer pill both display that override's model id, qualified as global.
- `BrainControl` still displays the same value, now from the same module.
- `swap.get_state` appears in exactly one source file (`src/hooks/useResolvedBrain.ts`).
- A regression test reproduces the 2026-07-28 live failure (override in force, zero `swap.state` frames) and fails if the snapshot request is removed.
</success_criteria>

<output>
Create `.planning/phases/103-brain-swap-control-surface/103-09-SUMMARY.md` when done.
</output>
