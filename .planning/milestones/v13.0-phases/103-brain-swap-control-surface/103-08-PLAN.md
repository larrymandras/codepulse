---
phase: 103-brain-swap-control-surface
plan: 08
type: execute
wave: 5
depends_on: ["103-06", "103-07"]
files_modified:
  - e2e/brain-swap.spec.ts
  - .planning/phases/103-brain-swap-control-surface/103-VALIDATION.md
autonomous: false
requirements: [BSC-02, BSC-04, BSC-05]
user_setup: []

must_haves:
  truths:
    - "D-16: an end-to-end run against the stub adapter proves the picker-open to swap to toast round trip without any live per-profile backend"
    - "D-08: the global axis is exercised for real against the running stack — the shipped swap.set path is not stubbed and its live verification genuinely closes BSC-05's global half"
    - "D-10: a live global swap can be reverted with the shipped restore idiom"
    - "A green stub suite is never reported as live BSC-05 verification; the per-profile axis stays explicitly deferred to astridr Phase 184.1"
    - "Every row of the VALIDATION.md per-task verification map is bound to a real task ID"
  artifacts:
    - path: "e2e/brain-swap.spec.ts"
      provides: "Playwright round-trip against the stub adapter"
    - path: ".planning/phases/103-brain-swap-control-surface/103-VALIDATION.md"
      provides: "Bound task IDs, realized fixtures, sign-off"
      contains: "nyquist_compliant: true"
  key_links:
    - from: "e2e/brain-swap.spec.ts"
      to: "VITE_BRAINS_STUB"
      via: "the spec asserts against stub fixture data and skips honestly when the stub is not active"
      pattern: "VITE_BRAINS_STUB|STUB"
---

<objective>
Close the phase: an end-to-end stub round trip, a genuine live verification of the global axis
against the running stack, and an honest sign-off that names exactly what was and was not proven.

Purpose: BSC-05 is split by axis. The global half is live and its original wording — "verified
working end-to-end on the running stack" — is satisfiable here and MUST be satisfied. The
per-profile half stays contract-first and deferred. This plan is where that distinction is
recorded rather than blurred.

Output: `e2e/brain-swap.spec.ts`, a live global-axis verification, and a completed
`103-VALIDATION.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/103-brain-swap-control-surface/103-CONTEXT.md
@.planning/phases/103-brain-swap-control-surface/103-VALIDATION.md
@.planning/phases/103-brain-swap-control-surface/103-CONTRACT.md
@.planning/phases/103-brain-swap-control-surface/103-06-SUMMARY.md
@.planning/phases/103-brain-swap-control-surface/103-07-SUMMARY.md
</context>

<interfaces>
<!-- E2E analog: e2e/analytics-cache-tile.spec.ts (34 lines) — the Clerk-skip guard idiom:      -->
<!--   if (await page.getByText('Sign in to access the telemetry dashboard').count())            -->
<!--     test.skip(true, 'Clerk auth gate present — run e2e without VITE_CLERK_PUBLISHABLE_KEY');-->
<!-- Assert STRUCTURE, never exact live-data values.                                             -->

<!-- The stub is active only when VITE_BRAINS_STUB === "true" (module-scope read in             -->
<!-- src/lib/brainsApi.ts). The dev server serving Playwright must be started with it set.       -->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Playwright round trip against the stub (103-08-T1)</name>
  <files>e2e/brain-swap.spec.ts</files>
  <read_first>
    - e2e/analytics-cache-tile.spec.ts (full file — the Clerk-skip guard and structural-assertion idiom to copy)
    - e2e/navigation.spec.ts (the routing/goto conventions in this suite)
    - playwright config (the web-server command and env, so `VITE_BRAINS_STUB=true` reaches the served build)
    - src/lib/brainsFixtures.ts (the fixture names the spec asserts against)
    - src/components/brains/BrainPicker.tsx and src/components/brains/BrainHeaderBadge.tsx (the DOM the spec drives)
    - .planning/phases/103-brain-swap-control-surface/103-UI-SPEC.md (§ Copywriting Contract — the exact toast and banner strings to assert)
  </read_first>
  <action>
Create `e2e/brain-swap.spec.ts` following `e2e/analytics-cache-tile.spec.ts`'s structure, including
its Clerk-skip guard.

The spec must ensure the served build runs with `VITE_BRAINS_STUB=true` — set it on the Playwright
web-server command's env rather than relying on the developer's shell. Add a second honest guard:
if the STUB banner is absent from the opened picker, `test.skip` with a message saying the stub
adapter is not active, rather than asserting against whatever live data happens to be present.

Cover the full round trip against stub fixture data:
1. Navigate to the dashboard and assert the active-brain badge is present in the header cluster.
2. Click the badge, assert the picker opens and the STUB banner renders.
3. Assert the three group headings render in the order Subscription, API, Local.
4. Assert the scope selector reads "This profile" on open.
5. Type into the search input and assert the visible row set narrows.
6. Select a normal-tier fixture row and assert the per-profile success toast appears with the
   Copywriting Contract's per-profile copy.
7. Reopen the picker and assert the scope selector has reset to "This profile".

Assert STRUCTURE and fixture-derived strings only — never a specific live engine name.
`VITE_BRAINS_STUB=true` is the only mode this phase can drive end to end for the per-profile axis.

Add a header comment stating plainly that a green run here proves the stub round trip and UI
honesty, NOT live per-profile integration.
  </action>
  <verify>
    <automated>npm run test:e2e -- brain-swap.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `e2e/brain-swap.spec.ts` exists and contains the Clerk-skip guard.
    - It contains a stub-absent skip guard keyed on the STUB banner.
    - `npm run test:e2e -- brain-swap.spec.ts` passes (or skips with an explicit reason, never fails silently).
    - The spec asserts the group order, the scope-selector default, a search narrowing the row set, the success toast, and the scope reset on reopen.
    - The spec contains no assertion on a live engine name.
    - The file's header comment states a green run is not live per-profile verification.
  </acceptance_criteria>
  <done>The picker-open to swap to toast round trip is proven end to end against the stub.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Live global-axis verification against the running stack (103-08-T2)</name>
  <files>src/components/brains/BrainPicker.tsx, src/components/brains/GlobalSwapModal.tsx, src/components/brains/BrainHeaderBadge.tsx</files>
  <read_first>
    - .planning/phases/103-brain-swap-control-surface/103-VALIDATION.md (§ "The BSC-05 Honesty Boundary" and § "Manual-Only Verifications")
    - .planning/phases/103-brain-swap-control-surface/103-CONTEXT.md (the `<blocker_reframing>` CORRECTION block — the global axis is live and MUST be genuinely verified)
    - src/components/control-center/BrainControl.tsx (the shipped global picker whose live path this shares — its "Restore usual brain" affordance is the revert idiom)
  </read_first>
  <what-built>
    Plans 103-01 through 103-07 built the picker, the global-swap modal, the header badge, the
    composer pill and the Settings rows. The "All profiles" branch dispatches the LIVE, shipped
    `swap.set` path (astridr Phase 185/186) — it is not stubbed. Its live end-to-end behavior has
    not yet been exercised by a human against a running stack, and "endpoint exists ≠ integration
    works" is a lesson this project has already paid for once in Phase 90.
  </what-built>
  <how-to-verify>
    Prerequisites: Ástríðr running and reachable on the WS telemetry endpoint, and the CodePulse
    dev server up (`npm run dev`, port 5173) WITHOUT `VITE_BRAINS_STUB=true` so the live branch is
    genuinely live.

    1. Load the dashboard. Confirm the header active-brain badge renders and is NOT showing a STUB
       chip (the global axis must never be marked stub).
    2. Click the badge to open the picker. Confirm the real catalogue loads — expect roughly 300+
       entries. Confirm rows wrap without truncation and provider grouping holds. Confirm no STUB
       banner is present.
    3. Move the scope selector to "All profiles". Choose a different brain. Confirm the
       confirmation modal lists every affected profile with `current → new` and flags the
       pinned-default count.
    4. Confirm the swap. Confirm the `swap.state` push updates the badge to the new engine —
       this is the readback, not the ack.
    5. Confirm the summary toast appears with a "Revert global swap" action.
    6. Click Revert. Confirm the badge returns to the prior engine.
    7. Also verify the composer pill on `/chat` renders in the composer without displacing the send
       button at a narrow window width.

    Record which of steps 1-7 passed. Any failure here is a real integration failure, not a stub
    limitation — do not write it off.
  </how-to-verify>
  <action>
Drive the live global-axis verification listed in `<how-to-verify>` against the running stack.
This is the one part of BSC-05's original wording that is satisfiable in this phase and it must be
genuinely satisfied — a green stub suite is not a substitute.

Do NOT stub any part of this run. Do NOT set `VITE_BRAINS_STUB=true` for it.

If a step fails, capture the exact symptom and stop rather than proceeding to sign-off. The
per-profile axis is expected to degrade honestly (error ack, engine unchanged) in this
non-stub mode — that is the correct behavior and is not a failure of this checkpoint.
  </action>
  <verify>
    <human-check>Steps 1-7 above performed against a running Ástríðr + CodePulse stack, with each step's result recorded.</human-check>
  </verify>
  <acceptance_criteria>
    - The badge rendered a real engine with no STUB chip.
    - The real catalogue loaded with rows wrapping and grouped, no truncation.
    - The confirm modal listed every affected profile with `current → new`.
    - After confirming, the badge updated from the `swap.state` readback.
    - The summary toast offered "Revert global swap" and Revert returned the badge to the prior engine.
    - The composer pill rendered without displacing the send affordance at a narrow width.
    - Any failure is recorded verbatim rather than attributed to the stub.
  </acceptance_criteria>
  <resume-signal>Type "approved" with the per-step results, or describe which step failed and how.</resume-signal>
  <done>BSC-05's global half is genuinely closed against the running stack, per its original wording.</done>
</task>

<task type="auto">
  <name>Task 3: Complete 103-VALIDATION.md and record the honesty boundary (103-08-T3)</name>
  <files>.planning/phases/103-brain-swap-control-surface/103-VALIDATION.md</files>
  <read_first>
    - .planning/phases/103-brain-swap-control-surface/103-VALIDATION.md (the whole file — the per-task map, the fixtures list, the sign-off checklist)
    - All eight plan files in .planning/phases/103-brain-swap-control-surface/ (the task IDs to confirm against what actually shipped)
    - .planning/phases/103-brain-swap-control-surface/103-01-SUMMARY.md through 103-07-SUMMARY.md
  </read_first>
  <action>
Finalize `103-VALIDATION.md`:

- Confirm every row of the Per-Task Verification Map is bound to a real task ID that actually
  shipped, and update any row whose automated command changed during execution. Two rows were
  re-pointed at planning time to the files that genuinely contain their assertions — row 7
  (scope-selector reset) to `BrainPicker.test.tsx` and row 9 (partial-failure result rows) to
  `GlobalSwapModal.test.tsx`. Verify those commands pass as written.
- Tick each realized fixture in "Fixtures That Actually Exercise the Behavior", naming the export
  in `src/lib/brainsFixtures.ts` that realizes it.
- Tick the Wave 0 Requirements list.
- Tick the Validation Sign-Off checklist, including the schema-push line (landed in 103-02-T2) and
  the global-axis live-verification line (performed in 103-08-T2).
- Set `status: complete` and `nyquist_compliant: true` in the frontmatter, and set
  `wave_0_complete: true`.
- Under the honesty boundary, record explicitly what is NOT closed: live per-profile
  `gateway.model.set` behavior, live per-profile active-engine telemetry into the new Convex
  table, real CLI-gateway health and quota, and any observed interaction between the per-profile
  mechanism and the live Phase-185/186 global override. These remain deferred to astridr
  Phase 184.1 and must not be reported as satisfied.
- Also record the two out-of-scope follow-ups this phase deliberately did not fix: the dead
  `gateway.provider.set_enabled` dispatch at `src/components/ProviderControls.tsx:188` (zero
  server-side handlers), and the astridr-side registration and implementation of Phase 184.1
  against `103-CONTRACT.md`.

Then run the full suite and the type check as the phase-level gate.
  </action>
  <verify>
    <automated>npm test &amp;&amp; npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '_TBD_' .planning/phases/103-brain-swap-control-surface/103-VALIDATION.md` returns 0.
    - The frontmatter contains `nyquist_compliant: true`, `wave_0_complete: true`, and `status: complete`.
    - Every one of the five mandatory fixtures is ticked and names its realizing export.
    - The file records the four not-closed items and the two out-of-scope follow-ups by name.
    - The file does NOT claim BSC-05 is verified for the per-profile axis.
    - `npm test` passes with zero failures.
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>The phase's validation record binds every row to real work and states plainly what remains unproven.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| stub build → verification claim | A green stub suite could be mistaken for live integration proof |
| operator → live global swap | The live verification genuinely mutates process-wide runtime state on a running Ástríðr |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-103-30 | Repudiation | verification claim scope | mitigate | The E2E spec header comment, the stub-absent skip guard, and the VALIDATION honesty boundary each state that a green stub run is not live per-profile verification; the four not-closed items are named explicitly |
| T-103-31 | Denial of Service | live global swap on a running stack | mitigate | The verification's own step 6 reverts via the shipped `restore: true` idiom, returning the process to its prior brain; the checkpoint is blocking so a human is present |
| T-103-32 | Information Disclosure (of a wrong state) | stub build reaching the live verification | mitigate | Step 1 of the live verification asserts the STUB chip is ABSENT before anything else is trusted |
| T-103-SC | Tampering | npm/pip/cargo installs | accept | Zero new packages this phase |
</threat_model>

<verification>
- `npm run test:e2e -- brain-swap.spec.ts` passes or skips with an explicit reason.
- The live global-axis checkpoint recorded per-step results against a running stack.
- `npm test` and `npx tsc --noEmit` clean.
- `103-VALIDATION.md` contains no `_TBD_` and claims nothing it did not prove.
</verification>

<success_criteria>
- The per-profile axis is proven end to end against the stub.
- The global axis is proven end to end against the running stack, satisfying BSC-05's original
  wording for that half.
- What remains unproven is named, not blurred.
</success_criteria>

<output>
Create `.planning/phases/103-brain-swap-control-surface/103-08-SUMMARY.md` when done
</output>
