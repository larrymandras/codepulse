---
phase: 103-brain-swap-control-surface
plan: 07
type: execute
wave: 4
depends_on: ["103-03", "103-05"]
files_modified:
  - src/pages/Chat.tsx
  - src/pages/Settings.tsx
  - src/components/brains/BrainFallbackNotice.tsx
  - src/components/brains/BrainFallbackNotice.test.tsx
autonomous: true
requirements: [BSC-01, BSC-02]
user_setup: []

must_haves:
  truths:
    - "D-05: the chat composer brain pill lives on src/pages/Chat.tsx, not on the unrelated ChatInput.tsx consumed only by InsightsChat"
    - "D-06: Settings Agents rows replace the stale p.model read in place with the live reactive engine, sourced from profileConfigs rather than the empty agentProfiles table"
    - "D-04: CLI brains ship text-mode only and a silent CLI to API fallback is surfaced honestly as a warn-toned toast"
    - "D-02: each per-profile row and the composer pill say whether a session override or a pinned default is in force"
    - "D-15: the pill and the Settings rows keep showing the real engine while a swap is in flight and simply drop the pending suffix on failure"
    - "No parallel per-profile view is left on screen — the stale row is removed, not supplemented"
  artifacts:
    - path: "src/pages/Settings.tsx"
      provides: "Live per-profile engine rows with a swap affordance"
      contains: "useActiveEngine"
    - path: "src/pages/Chat.tsx"
      provides: "Composer brain pill opening the picker"
      contains: "BrainPicker"
    - path: "src/components/brains/BrainFallbackNotice.tsx"
      provides: "CLI to API text-mode fallback toast"
      exports: ["useBrainFallbackNotice"]
  key_links:
    - from: "src/pages/Settings.tsx"
      to: "src/hooks/useActiveEngine.ts"
      via: "live engine lookup keyed by profileId, replacing p.model"
      pattern: "useActiveEngine"
    - from: "src/pages/Chat.tsx"
      to: "src/components/brains/BrainPicker.tsx"
      via: "composer pill trigger"
      pattern: "BrainPicker"
---

<objective>
Wire the picker into its two remaining hosts and remove the stale read BSC-01 exists to kill: the
Chat composer brain pill, the Settings → Agents per-profile rows, and the CLI-to-API fallback
notice.

Purpose: D-06 is the actual removal of a live instance of the v9.0 VitalsRail trap — `Settings.tsx`
line 663 renders `p.model`, a synced config field, as if it were live state. This plan replaces it
in place rather than adding a second view beside it.

Output: composer pill on `Chat.tsx`, replaced Settings rows, fallback-notice hook + tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/103-brain-swap-control-surface/103-CONTEXT.md
@.planning/phases/103-brain-swap-control-surface/103-RESEARCH.md
@.planning/phases/103-brain-swap-control-surface/103-PATTERNS.md
@.planning/phases/103-brain-swap-control-surface/103-UI-SPEC.md
@.planning/phases/103-brain-swap-control-surface/103-CONTRACT.md
@.planning/phases/103-brain-swap-control-surface/103-03-SUMMARY.md
@.planning/phases/103-brain-swap-control-surface/103-05-SUMMARY.md
</context>

<host_correction>
**D-05's host was corrected on 2026-07-28 and the UI-SPEC's §1 citation is stale.**

The original decision named `src/components/ChatInput.tsx`. That component is imported by exactly
one file, `src/pages/InsightsChat.tsx`, an unrelated LLM-over-telemetry Q&A surface with no profile
concept. The real Ástríðr chat is `src/pages/Chat.tsx`, which has its own inline composer and does
not import `ChatInput.tsx`. **Do NOT touch `ChatInput.tsx` or `InsightsChat.tsx`.**

UI-SPEC §1 also cites "the skill-chip-row precedent in Chat.tsx" around line 494. There is no such
row. Build the pill as a new row directly above `Chat.tsx`'s existing `{/* Input */}` block
(near line 511) — do not go hunting for a precedent that does not exist.

**Which profile does the composer pill scope to?** `Chat.tsx` is single-persona and carries no
profile switcher; it only forwards a `profile` on `chat.send` when a launch handoff supplies one.
The pill therefore scopes to the contract's `default_profile_id` (`103-CONTRACT.md` §3), supplied
by the adapter — not to an invented CodePulse-side active-profile mechanism.
</host_correction>

<interfaces>
<!-- Chat.tsx composer, lines 511-539 (insert the pill row ABOVE this block):                   -->
<!--   <div className="border-t border-border/60 p-3">                                          -->
<!--     <div className="flex items-end gap-2"> <textarea .../> <button ...><Send/></button>     -->
<!--     </div> <p className="mt-2 font-mono text-[10px] ...">{status caption}</p> </div>        -->
<!-- Chat.tsx lines 171-248 already own the live swap.get_state seed + swap.state subscription. -->

<!-- Settings.tsx Agent Profiles section, lines 630-678, already inside                          -->
<!--   <SectionErrorBoundary name="Agent Profiles"> (line 632).                                  -->
<!-- Line 663 is the stale read: {p.profileId} {p.model ? `/ ${p.model}` : ""}                   -->

<!-- Real per-profile source: api.profiles.listConfigs via src/hooks/useProfileConfigs.ts.       -->
<!-- agentProfiles has zero rows in production (convex/profiles.ts:113) — use it only as an      -->
<!-- optional join for displayName/avatarId.                                                     -->

<!-- Fallback event from 103-CONTRACT.md §5: WS event `brain.fallback` carrying                  -->
<!--   { profile_id, cli_model, fallback_model, reason }.                                        -->
<!-- AstridrWSContext exposes subscribeEvent(name, handler) — see Chat.tsx:223.                  -->
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace the stale Settings per-profile rows in place (103-07-T1)</name>
  <files>src/pages/Settings.tsx</files>
  <read_first>
    - src/pages/Settings.tsx (lines 620-690 — the whole Agent Profiles section, its `SectionErrorBoundary` at 632, and the stale `p.model` read at 663)
    - src/hooks/useProfileConfigs.ts (the real populated source)
    - src/hooks/useActiveEngine.ts (written in 103-03)
    - src/components/brains/BrainPicker.tsx (written in 103-05 — the profile-scoped invocation)
    - convex/profiles.ts (lines 105-130 and 188-197 — the audit-trail side effect and `listConfigs`; and the line-113 comment on agentProfiles being empty)
    - .planning/phases/103-brain-swap-control-surface/103-UI-SPEC.md (§9 Settings rows, §8 session-vs-pinned distinction)
  </read_first>
  <behavior>
    - Test: a profile with a reported live engine renders that engine name, not the `p.model` config field.
    - Test: a profile whose config `model` differs from its reported live engine renders the LIVE engine — proving the stale read is genuinely gone.
    - Test: a profile with no reported engine renders an honest unknown state rather than falling back to the config field.
    - Test: the row renders a session-override secondary line for a session mode and a pinned-default secondary line for a pinned mode, never both.
    - Test: the row's Swap affordance is distinct from the existing Edit button and opens the picker scoped to that profile with the scope selector locked to profile scope.
    - Test: the existing Edit button still renders and still opens the profile metadata editor.
  </behavior>
  <action>
Replace the Agent Profiles row body in `src/pages/Settings.tsx` IN PLACE (D-06). Do not add a
parallel per-profile view alongside the existing one — leaving the stale row on screen is
explicitly forbidden.

Source the profile list from `useProfileConfigs()` (backed by `profileConfigs`, real and
populated). Use `agentProfiles` only as an optional join for `displayName` / `avatarId` when
present — it has zero rows in production, so nothing may depend on it.

Delete the `p.model` render at line 663. Replace it with a live lookup keyed by `p.profileId` into
`useActiveEngine()`'s map, rendering per UI-SPEC §9: the profileId in muted Body, the engine
display name from live reactive telemetry, the session-override / pinned-default secondary line in
Label typography, a pending suffix when in flight, the dashed STUB chip when
`BRAINS_STUB_ACTIVE`, and a Swap button.

The Swap button opens `BrainPicker` scoped to that profile with the scope selector hidden or
locked — the context is already profile-specific. Keep the existing Edit button untouched and
visually separate: swapping the brain is not profile editing.

Confirm the new `useActiveEngine()` call sits INSIDE the existing
`<SectionErrorBoundary name="Agent Profiles">` at line 632, not above it. No new boundary is
needed; a misplaced hook call outside it would defeat the existing isolation.

Add or extend the Settings test file to cover every behavior above, including the decisive case:
a fixture where the config `model` and the live reported engine DIFFER, asserting the live value
renders. A fixture where they agree cannot prove the stale read was removed.
  </action>
  <verify>
    <automated>npx vitest run src/pages/Settings.test.tsx &amp;&amp; npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE 'p\.model' src/pages/Settings.tsx` returns 0.
    - `grep -c 'useActiveEngine' src/pages/Settings.tsx` returns at least 1.
    - `grep -c 'useProfileConfigs' src/pages/Settings.tsx` returns at least 1.
    - The `useActiveEngine` call site is inside the `SectionErrorBoundary name="Agent Profiles"` subtree.
    - A test asserts that when config `model` and live engine differ, the LIVE engine renders.
    - A test asserts a profile with no reported engine does NOT render its config `model`.
    - A test asserts the Swap affordance and the Edit button are distinct controls.
    - `npx vitest run src/pages/Settings.test.tsx` passes.
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>A live instance of the v9.0 VitalsRail stale-read trap is removed from the codebase, not merely supplemented.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Chat composer brain pill (103-07-T2)</name>
  <files>src/pages/Chat.tsx</files>
  <read_first>
    - src/pages/Chat.tsx (lines 160-250 for the existing swap.get_state seed and swap.state subscription; lines 500-545 for the composer block the pill row sits above)
    - src/components/brains/BrainPicker.tsx (written in 103-05)
    - src/hooks/useActiveEngine.ts (written in 103-03)
    - src/lib/brainsApi.ts (`getDefaultProfileId`, `BRAINS_STUB_ACTIVE`)
    - src/components/voice/SwapBadge.tsx (the existing Chat-page badge — the pill must be visually distinguishable from it)
    - .planning/phases/103-brain-swap-control-surface/103-UI-SPEC.md (§1 composer pill in full, § Spacing exception for the h-8 pill, § Copywriting Contract)
    - .planning/phases/103-brain-swap-control-surface/103-CONTEXT.md (the D-05 correction in `<decisions>`)
  </read_first>
  <behavior>
    - Test: the pill renders in a NEW row above the existing composer flex row and does not displace the send button.
    - Test: the pill's label is the live reported engine for the contract's default profile.
    - Test: clicking the pill opens the picker scoped to that profile.
    - Test: while a swap is in flight the pill's base label is byte-identical and a "switching to" suffix is present; after an error ack the suffix is gone and the base label is still the original engine.
    - Test: a session override renders the clock plus TTL affordance and a pinned default renders the pin affordance — never both.
    - Test: with `BRAINS_STUB_ACTIVE` true the dashed STUB chip renders on the pill; with it false it does not.
  </behavior>
  <action>
Add the composer brain pill to `src/pages/Chat.tsx` as a NEW row directly above the existing
`{/* Input */}` block near line 511 — inside the same `border-t border-border/60 p-3` container or
immediately preceding it. Do not crowd the existing 40px mic/send row and do not modify the
textarea or send button.

Do NOT touch `src/components/ChatInput.tsx` or `src/pages/InsightsChat.tsx` — see
`<host_correction>` above.

Pill geometry per UI-SPEC §1: `h-8` button, `rounded-full`, `border border-(--border)` idle and
`border-(--primary)` on hover-open, `px-2` horizontal padding. Content left to right: a 6px
provider-color dot, the engine display name at Body 14px, a `ChevronDown` at 12px.

Scope: the pill reads and swaps the contract's `default_profile_id` obtained from
`brainsApi.getDefaultProfileId()`. Do not invent a CodePulse-side active-profile mechanism.

Session override vs pinned default (D-02): the primary label is always whichever is ACTUALLY in
force. A session override appends a `Clock` icon plus the remaining countdown in Label typography,
`--status-info` tint. A pinned default appends a `Pin` icon, `--muted-foreground` tint. Never both.

Pending (D-15): the pill keeps its truthful current label; a `--status-info` pulsing dot replaces
the chevron and a " · switching to {X}…" suffix appends. On failure the suffix simply disappears —
no error styling on the pill itself; the failure lives in the toast.

Stub chip: the dashed `STUB` chip appended after the pending slot when `BRAINS_STUB_ACTIVE`.

Clicking opens `BrainPicker` anchored to the pill.

Note the coexistence: this page already renders `SwapBadge` for the LIVE global runtime axis
(seeded at lines 171-248). The two can legitimately disagree — the pill's tooltip must say it
reflects the per-profile default, distinct from any active global override. Leave the existing
`SwapBadge` wiring untouched.

Extend `src/pages/Chat.test.tsx` (or add it if absent) covering every behavior above.
  </action>
  <verify>
    <automated>npx vitest run src/pages/Chat.test.tsx &amp;&amp; npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'BrainPicker' src/pages/Chat.tsx` returns at least 2 (import plus usage).
    - `git diff --name-only` does NOT include `src/components/ChatInput.tsx` or `src/pages/InsightsChat.tsx`.
    - `git diff src/pages/Chat.tsx` shows no change to the textarea element or the send button.
    - `git diff src/pages/Chat.tsx` shows no change to the `swap.get_state` / `swap.state` wiring at lines 171-248.
    - `grep -c 'getDefaultProfileId' src/pages/Chat.tsx` returns at least 1.
    - A test asserts the pill's base label is unchanged during an in-flight swap AND after an error ack.
    - `npx vitest run src/pages/Chat.test.tsx` passes.
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>The chat composer carries an honest per-profile brain pill that opens the picker, on the correct host file.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: CLI-to-API text-mode fallback notice (103-07-T3)</name>
  <files>src/components/brains/BrainFallbackNotice.tsx, src/components/brains/BrainFallbackNotice.test.tsx</files>
  <read_first>
    - .planning/phases/103-brain-swap-control-surface/103-CONTRACT.md (§5 — the `brain.fallback` event field names)
    - .planning/phases/103-brain-swap-control-surface/103-UI-SPEC.md (§12 fallback notice, § Copywriting Contract line 116, § Color line 88)
    - src/contexts/AstridrWSContext.tsx (`subscribeEvent` signature)
    - src/pages/Chat.tsx (lines 218-235 — the existing `subscribeEvent("swap.state", ...)` subscription idiom to copy, including its cleanup)
    - src/test/setup.ts (the sonner mock)
  </read_first>
  <behavior>
    - Test: receiving a `brain.fallback` event fires exactly one sonner toast whose text matches the Copywriting Contract, interpolating the CLI engine and the fallback engine display names.
    - Test: the toast is warn-toned, not error-toned — this is a graceful degrade, not a failure.
    - Test: unmounting unsubscribes; a subsequent event fires no toast.
    - Test: an event missing `fallback_model` does not throw and does not fire a half-formed toast.
  </behavior>
  <action>
Create `src/components/brains/BrainFallbackNotice.tsx` exporting a `useBrainFallbackNotice()` hook
that subscribes to the contract's `brain.fallback` WS event and fires a `sonner` toast per
UI-SPEC §12: `--status-warn` tone (NOT `--status-error` — this is a graceful degrade), an
`ArrowRightLeft` Lucide icon, and the Copywriting Contract's copy interpolating the CLI engine
display name and the fallback engine display name.

Copy the subscription and cleanup idiom from `Chat.tsx`'s existing
`subscribeEvent("swap.state", ...)` block, including its unsubscribe on unmount. Guard against a
malformed payload — a missing `fallback_model` must produce no toast rather than a half-formed one.

Mount the hook once on `src/pages/Chat.tsx` alongside the pill from Task 2.

Explicitly OUT of scope per UI-SPEC §12: badging the individual chat response bubble with a
"(fallback)" tag. Make no `ChatBubble` changes.

Note in a file comment that D-04 defers the `--agentic` per-CLI-brain hybrid mode; this phase ships
CLI brains in text mode only, and this notice is what keeps that degrade honest.

Write `src/components/brains/BrainFallbackNotice.test.tsx` covering every behavior above.
  </action>
  <verify>
    <automated>npx vitest run src/components/brains/BrainFallbackNotice.test.tsx &amp;&amp; npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/brains/BrainFallbackNotice.tsx` exports `useBrainFallbackNotice`.
    - `grep -c 'brain.fallback' src/components/brains/BrainFallbackNotice.tsx` returns at least 1.
    - `grep -c 'ArrowRightLeft' src/components/brains/BrainFallbackNotice.tsx` returns at least 1.
    - `grep -c 'toast.error' src/components/brains/BrainFallbackNotice.tsx` returns 0 — warn tone only.
    - `git diff --name-only` does not include any `ChatBubble` file.
    - `npx vitest run src/components/brains/BrainFallbackNotice.test.tsx` passes with at least 4 tests, including the unsubscribe-on-unmount case and the malformed-payload case.
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>A silent CLI-to-API fallback is surfaced honestly, keeping D-04's text-mode-only scope from becoming a hidden degradation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| astridr WS events → Chat page | `brain.fallback` payloads arrive untrusted-shaped |
| config field vs live telemetry | The Settings row is where a stale config value could be presented as live state |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-103-26 | Information Disclosure (of a wrong state) | Settings per-profile row | mitigate | The `p.model` config read is deleted, not supplemented; an unreported profile renders unknown rather than falling back to the config field, and a test asserts the live value wins when the two differ |
| T-103-27 | Tampering | `brain.fallback` payload | mitigate | The handler guards a missing `fallback_model` and fires no half-formed toast; React escapes the interpolated names |
| T-103-28 | Denial of Service | engine query inside Settings | accept | Already covered by the existing `SectionErrorBoundary name="Agent Profiles"`; the task requires the new hook call to sit inside it |
| T-103-29 | Information Disclosure (of a wrong state) | pill vs SwapBadge disagreement on the Chat page | mitigate | The pill's tooltip states it reflects the per-profile default, distinct from any active global runtime override; the two surfaces are visually differentiated (no shared `Brain` icon) |
| T-103-SC | Tampering | npm/pip/cargo installs | accept | Zero new packages this phase |
</threat_model>

<verification>
- `npx vitest run src/pages/Settings.test.tsx src/pages/Chat.test.tsx src/components/brains/BrainFallbackNotice.test.tsx` green.
- `npx tsc --noEmit` clean.
- `ChatInput.tsx`, `InsightsChat.tsx` and any `ChatBubble` file are untouched.
</verification>

<success_criteria>
- The stale `p.model` read no longer exists anywhere in `Settings.tsx`.
- The composer pill lives on `Chat.tsx` and scopes to the contract's default profile.
- A CLI-to-API fallback is visible to the operator instead of silent.
</success_criteria>

<output>
Create `.planning/phases/103-brain-swap-control-surface/103-07-SUMMARY.md` when done
</output>
