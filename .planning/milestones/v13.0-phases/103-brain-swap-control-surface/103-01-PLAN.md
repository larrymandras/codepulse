---
phase: 103-brain-swap-control-surface
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/103-brain-swap-control-surface/103-CONTRACT.md
  - src/lib/brainsApi.ts
  - src/lib/brainsFixtures.ts
  - src/lib/brainsApi.test.ts
autonomous: true
requirements: [BSC-02, BSC-05]
user_setup: []

must_haves:
  truths:
    - "D-17: 103-CONTRACT.md ships as a real deliverable specifying gateway.model.set, the brain-catalogue payload, and the active-engine telemetry event"
    - "D-13: the per-profile command is modeled on SwapSetCommand (type Literal + request_id + discriminator), never on the dead gateway.provider.set_enabled"
    - "D-16: all per-profile brain traffic goes through one src/lib/brainsApi.ts adapter with stub and live implementations selected by VITE_BRAINS_STUB, read once at module scope"
    - "D-02: the contract carries both a session override (TTL) and a sticky pinned default, and the active-engine payload says which of the two is in force"
    - "D-03: Ástríðr owns the persisted default; CodePulse dispatches and reads back, holding no authoritative copy"
    - "D-04: CLI brains are text-mode only this phase, and the contract defines a CLI-to-API fallback event so the silent fallback can be surfaced honestly"
    - "A developer can flip VITE_BRAINS_STUB and get live or stub per-profile behavior with no other code change"
    - "The stub adapter rejects malformed command shapes in dev, catching contract drift before Phase 184.1 exists"
  artifacts:
    - path: ".planning/phases/103-brain-swap-control-surface/103-CONTRACT.md"
      provides: "The client contract astridr Phase 184.1 implements against"
      contains: "gateway.model.set"
    - path: "src/lib/brainsApi.ts"
      provides: "BrainsAdapter interface + stub/live impls + BRAINS_STUB_ACTIVE flag"
      exports: ["BrainsAdapter", "brainsApi", "BRAINS_STUB_ACTIVE"]
    - path: "src/lib/brainsFixtures.ts"
      provides: "The five mandatory fixtures VALIDATION.md requires"
    - path: "src/lib/brainsApi.test.ts"
      provides: "Contract-conformance + malformed-shape-rejection tests"
  key_links:
    - from: "src/lib/brainsApi.ts"
      to: "import.meta.env.VITE_BRAINS_STUB"
      via: "module-scope read, never per-call"
      pattern: "VITE_BRAINS_STUB"
    - from: "src/lib/brainsApi.ts"
      to: "src/lib/brainsFixtures.ts"
      via: "stub implementation imports fixtures"
      pattern: "brainsFixtures"
---

<objective>
Publish `103-CONTRACT.md` (D-17) and build the single adapter seam (D-16) every per-profile
brain interaction in this phase goes through, plus the fixtures that make the rest of the
phase's tests capable of failing.

Purpose: astridr Phase 184.1 implements against this document instead of guessing, and every
component built in waves 2-5 is written against the real contract so going live is a flag flip
plus one module — not a rewrite.

Output: `103-CONTRACT.md`, `src/lib/brainsApi.ts`, `src/lib/brainsFixtures.ts`,
`src/lib/brainsApi.test.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/103-brain-swap-control-surface/103-CONTEXT.md
@.planning/phases/103-brain-swap-control-surface/103-RESEARCH.md
@.planning/phases/103-brain-swap-control-surface/103-PATTERNS.md
@.planning/phases/103-brain-swap-control-surface/103-UI-SPEC.md
@.planning/phases/103-brain-swap-control-surface/103-VALIDATION.md
</context>

<interfaces>
<!-- The real, shipped astridr command shape the new command must imitate. -->
<!-- Source: astridr-repo/astridr/api/ws_commands.py:224-240 (SwapSetCommand). -->
<!-- Fields: type Literal["swap.set"], request_id: str = "", target: Literal["brain","voice"], -->
<!-- value: str | None = None, restore: bool = False. Dispatcher :419, handler :1075. -->

<!-- Existing CodePulse env-flag idiom to copy (src/lib/astridrApi.ts:1-2): -->
<!-- module-scope `const X = import.meta.env.VITE_...` — read once, never per-call. -->

<!-- Existing dispatch surface (src/hooks/useCommandDispatch.ts, 33 lines): -->
<!-- dispatch(cmd: Record<string, unknown>, successMsg?: string): Promise<AckResponse> -->
<!-- AckResponse has `status: "ok" | "error"` and an optional `error` string. -->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Write 103-CONTRACT.md (103-01-T1)</name>
  <files>.planning/phases/103-brain-swap-control-surface/103-CONTRACT.md</files>
  <read_first>
    - .planning/phases/103-brain-swap-control-surface/103-CONTEXT.md (the CORRECTION block in `<blocker_reframing>` first, then D-01..D-17)
    - .planning/phases/103-brain-swap-control-surface/103-RESEARCH.md (§ "Critical Discovery", § "Architecture Patterns", § "Security Domain", § "Open Questions")
    - .planning/phases/103-brain-swap-control-surface/103-PATTERNS.md (§ "Shared Patterns" → "Real WS command shape to model the new per-profile command after")
    - C:\Users\mandr\astridr-repo\astridr\api\ws_commands.py (lines 190-260 for SwapSetCommand/SwapGetStateCommand/SwapCatalogueGetCommand, 1075-1200 for _handle_swap_set and _handle_swap_catalogue)
    - C:\Users\mandr\astridr-repo\astridr\security\command_auth.py (lines 1-40 — the ADMIN_COMMANDS set)
    - C:\Users\mandr\astridr-repo\astridr\providers\router.py (lines 420-475 — _resolve_model resolution order, the global-swap-override rung)
    - convex/runtimeIngest.ts (the `case "profile_config"` block near line 512 and `case "provider_health"` near line 835 — the dual snake/camelCase coalescing convention any new event must satisfy)
  </read_first>
  <action>
Author `103-CONTRACT.md` as the client contract astridr Phase 184.1 implements against. It is a
deliverable, not documentation-after-the-fact. Required sections, each with concrete field names
and types (TypeScript for the client side, Pydantic-shaped for the astridr side):

1. **Scope and status header** — state that the GLOBAL axis is already live and shipped
   (`swap.set` / `swap.catalogue` / `swap.state`, astridr Phase 185/186) and this contract adds
   ONLY the per-profile axis. State explicitly that `gateway.provider.set_enabled` has zero
   handlers in `astridr/` and must not be used as a reference shape (D-13 correction).

2. **Write command `gateway.model.set`** — modeled on `SwapSetCommand`: a discriminated-union
   member with `type: Literal["gateway.model.set"]`, `request_id: str = ""`, and these fields:
   `scope: Literal["profile"]` (the only value this phase's contract defines — global swaps use
   the existing `swap.set`), `profile_id: str`, `model: str | None`, `mode:
   Literal["session","default"]` (D-02: session override vs sticky pinned default),
   `restore: bool = False` (mirrors `SwapSetCommand.restore`). Document the ack shape:
   `status: "ok" | "error"`, `request_id`, optional `error`. State the D-14 rule inline —
   the ack means ACCEPTED, never SWITCHED; clients must not render engine state from it.

3. **Catalogue read** — choose WS over REST and record the rationale (avoids a second auth path;
   the working `swap.catalogue` is a real precedent). Command `models.catalog` with
   `type` Literal + `request_id`. Ack entry shape: `id` (unique, stable — REQUIRED because cmdk
   selection is value-keyed), `name` (display, NOT unique), `vendor`, `group:
   "subscription" | "api" | "local"` (D-07's three fixed groups), `billing: "api" | "sub"`,
   `costTier: "normal" | "expensive" | "unknown"`, optional `quotaRemainingPct: number` (0-1),
   optional `health: "reachable" | "degraded" | "unreachable"`. Also carry
   `default_profile_id: str` on the ack — the profile the Ástríðr chat channel resolves against,
   so the Chat composer pill has a defined scope without CodePulse inventing one.
   Document the duplicate-exclusion rule: the server excludes OpenRouter-native `anthropic/...`
   duplicates exactly as `_handle_swap_catalogue` already does, and `id` uniqueness is a hard
   requirement of this contract.

4. **Active-engine telemetry event** — the D-14 per-profile readback. Event name `model_routing`,
   delivered on the existing `/runtime-ingest` HTTP path (no second ingest endpoint). Payload:
   `profile_id`, `model`, `mode: "session" | "pinned" | "inherited"`, `selection_path`
   (mirrors `router.py`'s own selection_path vocabulary), optional `expires_at` (epoch seconds,
   set only for `mode: "session"`), `timestamp`. Require the emitter to send it after every
   resolution change AND on process start so the table is never empty-by-default. Note the
   consumer must tolerate both snake_case and camelCase per this repo's ingest convention.

5. **CLI-to-API fallback event (D-04)** — text-mode-only CLI brains. WS event `brain.fallback`
   with `profile_id`, `cli_model`, `fallback_model`, `reason`. State that the `--agentic` hybrid
   mode is deferred and NOT part of this contract.

6. **Authorization tier (ASVS V4)** — `gateway.model.set` and `models.catalog` are regular,
   NON-admin commands, matching `swap.set` and `config.update`. Cite
   `astridr/security/command_auth.py:15`, where only `estop.activate` / `estop.deactivate` are
   admin-gated. State this explicitly so 184.1 neither over- nor under-gates it.

7. **Input validation (ASVS V5)** — the server MUST validate `model` and `profile_id` via
   Pydantic exactly as every other command in `ws_commands.py` does; unknown/absent `profile_id`
   is an error ack, never a silent global apply.

8. **No server-side batch command** — a global swap is N client-dispatched single-profile
   commands aggregated with `Promise.allSettled` (D-12's per-row honesty falls out for free).
   184.1 must NOT design batch semantics in its first cut.

9. **Known interaction: the global override shadows the per-profile default** — `router.py`'s
   resolution order puts the live global swap override at rung 1b, ahead of the per-profile
   default at rung 3, so a Phase-103 per-profile default can be silently inactive while a global
   override is in force. Record this so 184.1 does not rediscover it, and state that Phase 103's
   UI surfaces the per-profile default and does not attempt to render the shadowing.

10. **Conformance obligations** — the stub adapter in `src/lib/brainsApi.ts` conforms to every
    shape above and is the executable check that this document is internally consistent.
  </action>
  <verify>
    <automated>test -f .planning/phases/103-brain-swap-control-surface/103-CONTRACT.md &amp;&amp; grep -v '^#' .planning/phases/103-brain-swap-control-surface/103-CONTRACT.md | grep -c 'gateway.model.set' | grep -qv '^0$' &amp;&amp; grep -v '^#' .planning/phases/103-brain-swap-control-surface/103-CONTRACT.md | grep -c 'models.catalog' | grep -qv '^0$' &amp;&amp; grep -v '^#' .planning/phases/103-brain-swap-control-surface/103-CONTRACT.md | grep -c 'model_routing' | grep -qv '^0$' &amp;&amp; grep -v '^#' .planning/phases/103-brain-swap-control-surface/103-CONTRACT.md | grep -c 'brain.fallback' | grep -qv '^0$'</automated>
  </verify>
  <acceptance_criteria>
    - `103-CONTRACT.md` exists in the phase directory.
    - It contains all ten numbered sections listed in the action.
    - It names `gateway.model.set`, `models.catalog`, `model_routing`, and `brain.fallback` as literal identifiers.
    - It contains the string `default_profile_id` (the Chat composer pill's scope source).
    - It states that `gateway.provider.set_enabled` has zero astridr handlers and must not be modeled on.
    - It cites `astridr/security/command_auth.py` and states the command is NON-admin.
    - It states the ack means "accepted", not "switched".
    - It does NOT propose a server-side batch swap command.
  </acceptance_criteria>
  <done>103-CONTRACT.md is a self-contained document a Phase-184.1 implementer could build from without reading CodePulse source.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build the brains adapter seam and the five mandatory fixtures (103-01-T2)</name>
  <files>src/lib/brainsApi.ts, src/lib/brainsFixtures.ts</files>
  <read_first>
    - src/lib/astridrApi.ts (the module-scope env-read idiom at lines 1-2, and `authHeaders()`)
    - src/contexts/AstridrWSContext.tsx (the `AckResponse` type and `sendCommand` signature the live impl must accept)
    - src/lib/providers.ts (PROVIDER_COLORS, PROVIDER_BILLING, PROVIDER_DISPLAY_NAMES, GATEWAY_PROVIDERS — the identity source the fixtures must use)
    - .planning/phases/103-brain-swap-control-surface/103-VALIDATION.md (§ "Fixtures That Actually Exercise the Behavior" — all five are mandatory)
    - .planning/phases/103-brain-swap-control-surface/103-CONTRACT.md (written in Task 1 — the shapes this file must implement)
  </read_first>
  <behavior>
    - `BRAINS_STUB_ACTIVE` is `true` only when `import.meta.env.VITE_BRAINS_STUB === "true"`; unset, `"false"`, or any other value yields `false`.
    - `brainsApi.getCatalogue()` in stub mode resolves the fixture catalogue; in live mode it issues a `models.catalog` command through the injected sender.
    - `brainsApi.dispatchSwap()` rejects a command whose shape violates the contract (missing `profile_id`, missing `model` when `restore` is false, `scope` other than `"profile"`) with an error ack, not a thrown crash.
    - The stub's `dispatchSwap` fails for any profileId present in its configurable failure deny-list and succeeds otherwise.
  </behavior>
  <action>
Create `src/lib/brainsFixtures.ts` first — it is what makes every downstream test capable of
failing. Export:

- `STUB_CATALOGUE: CatalogueEntry[]` containing, at minimum: at least one entry per `group`
  (`subscription`, `api`, `local`); **at least one `costTier: "expensive"` and at least one
  `costTier: "unknown"`** (without these the expensive-tier inline-confirm path is unreachable and
  its test proves nothing); **two entries sharing an identical `name` but different `id`s** (the
  cmdk value-keyed duplicate-selection regression guard); at least one `billing: "sub"` entry with
  no `quotaRemainingPct` (the "∞" branch) and at least one `billing: "api"` entry with a
  `quotaRemainingPct` below 0.05 and one at or above 0.20 (the quota-threshold branches).
- `STUB_PROFILE_ENGINES: Record<string, ActiveEngine>` seeding **at least two profiles with two
  DISTINCT engine values** (the "Mixed brains" stacked-dot path is unreachable from an all-agree
  fixture) and covering **at least one `mode: "pinned"` and at least one `mode: "inherited"`**
  (D-11's pin-status-preserving revert cannot be distinguished from a naive restore otherwise).
- `STUB_DEFAULT_PROFILE_ID: string` — the `default_profile_id` the Chat composer pill scopes to.
- `makeStubFailureSet(profileIds: string[])` or an equivalent explicit constructor for the
  partial-failure deny-list.

Then create `src/lib/brainsApi.ts`:

- Export the contract TypeScript types mirroring `103-CONTRACT.md` exactly: `CatalogueEntry`,
  `ActiveEngine`, `GatewayModelSetCommand`, `BrainsAck`.
- Export `interface BrainsAdapter` with `isStub: boolean`, `getCatalogue()`,
  `dispatchSwap(cmd)`, `getDefaultProfileId()`.
- Read the flag ONCE at module scope following `astridrApi.ts:1-2`'s idiom:
  `const BRAINS_STUB = import.meta.env.VITE_BRAINS_STUB === "true"`. Export
  `BRAINS_STUB_ACTIVE` as the single source of truth for every STUB chip and banner (D-16) —
  no component may derive stub-ness any other way.
- Export a shared `validateGatewayModelSet(cmd)` used by BOTH implementations before dispatch.
  It returns an error `BrainsAck` (never throws) for: absent/empty `profile_id`; `scope` other
  than `"profile"`; absent `model` when `restore` is false; `mode` outside
  `"session" | "default"`. This is the ASVS V5 contract-drift canary — the stub must be at least
  as strict as the server will be.
- `stubBrainsAdapter`: resolves `STUB_CATALOGUE` / `STUB_DEFAULT_PROFILE_ID`, and
  `dispatchSwap` returns an error ack for profileIds in its failure set and an ok ack otherwise.
  Keep the failure set injectable so tests can construct partial-failure scenarios without
  monkey-patching.
- `liveBrainsAdapter`: takes the WS sender as an argument (a factory function, so the module has
  no React dependency and stays unit-testable) and issues `models.catalog` /
  `gateway.model.set` commands per the contract. If any catalogue read ends up going over REST
  instead, it MUST use `authHeaders()` from `src/lib/astridrApi.ts` per CLAUDE.md.
- Export `brainsApi: BrainsAdapter` selected once: `BRAINS_STUB ? stubBrainsAdapter :
  liveBrainsAdapter`.

Do NOT add any per-call flag reads, and do NOT route the GLOBAL `swap.set` path through this
module — the global axis is live and must never be marked stub (D-08/D-16 amendments).
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'VITE_BRAINS_STUB' src/lib/brainsApi.ts` returns exactly 1 (read once, module scope).
    - `src/lib/brainsApi.ts` exports `BrainsAdapter`, `brainsApi`, `BRAINS_STUB_ACTIVE`, and `validateGatewayModelSet`.
    - `grep -c 'swap.set' src/lib/brainsApi.ts` returns 0 — the global path is not routed through the stub seam.
    - `STUB_CATALOGUE` contains at least one entry each with `costTier: "expensive"` and `costTier: "unknown"`.
    - `STUB_CATALOGUE` contains two entries with the same `name` and different `id`.
    - `STUB_PROFILE_ENGINES` contains at least two profiles whose `model` values differ, and covers both `mode: "pinned"` and `mode: "inherited"`.
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>The adapter seam compiles, the flag is read once, and all five mandatory fixtures exist as real exported data.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Contract-conformance and malformed-shape tests (103-01-T3)</name>
  <files>src/lib/brainsApi.test.ts</files>
  <read_first>
    - src/lib/brainsApi.ts (written in Task 2)
    - src/lib/brainsFixtures.ts (written in Task 2)
    - src/components/control-center/BrainControl.test.tsx (the module-level `vi.mock` + `mockSendCommand.mockResolvedValue` idiom to copy)
    - src/test/setup.ts (what is already globally mocked)
    - .planning/phases/103-brain-swap-control-surface/103-CONTRACT.md
  </read_first>
  <behavior>
    - Test: `validateGatewayModelSet` returns an error ack (not a throw) for each of: missing `profile_id`, `scope: "global"`, missing `model` with `restore: false`, invalid `mode`.
    - Test: a well-formed command passes validation and reaches the sender.
    - Test: `stubBrainsAdapter.getCatalogue()` resolves entries matching the `CatalogueEntry` contract shape — every entry has a non-empty unique `id`, a `group` in the three-value set, a `billing` in the two-value set, and a `costTier` in the three-value set.
    - Test: catalogue `id` values are unique even though two entries share a `name` (the cmdk regression guard's data precondition).
    - Test: `stubBrainsAdapter.dispatchSwap` returns an error ack for a profileId in the failure set and an ok ack for one outside it.
    - Test: stub and live adapters both satisfy the same `BrainsAdapter` interface — every method name present on both, with matching arity.
    - Test: `BRAINS_STUB_ACTIVE` is false when `VITE_BRAINS_STUB` is unset and true only for the exact string `"true"` (use `vi.stubEnv` + `vi.resetModules` and re-import).
  </behavior>
  <action>
Write `src/lib/brainsApi.test.ts` covering every behavior above. Copy the mocking structure from
`BrainControl.test.tsx` (module-level `vi.mock`, `mockResolvedValue` per test, exact-shape
assertions via `toHaveBeenCalledWith`). Assert the dispatched command object exactly — field
names must match `103-CONTRACT.md` character for character, since this test is the only mechanical
check that the document and the code have not drifted. For the env-flag tests, use `vi.stubEnv`
with `vi.resetModules()` and a dynamic re-import so the module-scope read is re-evaluated.
Include a comment at the top of the file stating that a green run here proves contract conformance
and UI honesty only — it is NOT live BSC-05 verification.
  </action>
  <verify>
    <automated>npx vitest run src/lib/brainsApi.test.ts &amp;&amp; npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/lib/brainsApi.test.ts` passes with at least 7 tests.
    - A test asserts an error ack (not a thrown exception) for each of the four malformed-shape cases.
    - A test asserts catalogue `id` uniqueness across entries that share a `name`.
    - A test asserts the stub fails for a deny-listed profileId and succeeds otherwise.
    - A test asserts `BRAINS_STUB_ACTIVE === false` with `VITE_BRAINS_STUB` unset.
    - `npx tsc --noEmit` exits 0.
    - The file contains a comment stating a green run is not live BSC-05 verification.
  </acceptance_criteria>
  <done>The stub provably conforms to 103-CONTRACT.md and rejects malformed shapes, closing the redefined BSC-05's contract half.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → astridr WS | Operator-issued swap commands cross here; bearer subprotocol handshake already gates the connection |
| astridr → CodePulse `/runtime-ingest` | Untrusted-shaped telemetry crosses into Convex |
| build env → shipped bundle | `VITE_BRAINS_STUB` decides whether the operator sees real or fabricated data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-103-01 | Tampering | `gateway.model.set` payload | mitigate | `validateGatewayModelSet` rejects malformed shapes client-side (ASVS V5 canary); `103-CONTRACT.md` §7 requires server-side Pydantic validation in 184.1 |
| T-103-02 | Elevation of Privilege | command auth tier | mitigate | Contract §6 pins the command to the NON-admin tier matching `swap.set`/`config.update`, citing `command_auth.py:15`, so 184.1 cannot silently over- or under-gate it |
| T-103-03 | Information Disclosure (of a wrong state) | stub build shipped to prod | mitigate | `BRAINS_STUB_ACTIVE` is the single source of truth for the persistent, non-dismissible STUB chip and banner (D-16, UI-SPEC §10); no component may derive stub-ness independently |
| T-103-04 | Spoofing | WS connection | accept | Already mitigated at transport — the bearer subprotocol handshake gates the whole connection; no new auth surface added |
| T-103-SC | Tampering | npm/pip/cargo installs | accept | Zero new packages this phase (RESEARCH § Package Legitimacy Audit: not applicable); no install task exists, so no legitimacy checkpoint is triggered |
</threat_model>

<verification>
- `npx vitest run src/lib/brainsApi.test.ts` green.
- `npx tsc --noEmit` clean.
- `103-CONTRACT.md` present and containing every identifier the downstream plans import.
</verification>

<success_criteria>
- `103-CONTRACT.md` fully specifies the per-profile write command, the catalogue read, the
  active-engine telemetry event, the fallback event, the auth tier, and the validation rule.
- One adapter module, one env-flag read, two implementations, one exported flag constant.
- All five VALIDATION.md-mandated fixtures exist as real exported data, not as inline test literals.
</success_criteria>

<output>
Create `.planning/phases/103-brain-swap-control-surface/103-01-SUMMARY.md` when done
</output>
