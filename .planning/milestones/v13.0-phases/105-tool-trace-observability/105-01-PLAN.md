---
phase: 105-tool-trace-observability
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - convex/toolExecutions.ts
  - convex/llm.ts
  - convex/toolExecutions.test.ts
  - convex/llm.test.ts
  - src/pages/SessionDetail.tsx
  - src/components/TraceWaterfall.tsx
  - src/components/TraceWaterfall.test.tsx
  - src/components/ToolExecutionPanel.tsx
  - src/components/PermissionDecisionsChart.tsx
  - src/components/PermissionDecisionsChart.test.tsx
autonomous: true
requirements: [OBS-01, OBS-03]

must_haves:
  truths:
    - "D-12: llm.sessionCalls and toolExecutions.listBySession are row-capped, never an unbounded .collect()"
    - "D-12: toolExecutions.successRate and toolExecutions.avgDuration are row-capped over their 24h window (the plan-phase EXTENDED clause)"
    - "D-12: when a cap is hit, the trace waterfall states truncation on screen instead of rendering a partial waterfall that looks complete"
    - "D-12: TraceWaterfall is confirmed to sit inside a SectionErrorBoundary so a query timeout cannot blank SessionDetail"
    - "D-15: ToolExecutionPanel and PermissionDecisionsChart produce byte-identical output after Astridr rows exist in toolExecutions"
  artifacts:
    - path: "convex/toolExecutions.ts"
      provides: "Capped listBySession/successRate/avgDuration + excludeProvider arg"
      contains: "excludeProvider"
    - path: "convex/llm.ts"
      provides: "Capped sessionCalls returning { rows, truncated, cap }"
      contains: "SESSION_CALLS_READ_CAP"
    - path: "convex/toolExecutions.test.ts"
      provides: "Cap + exclude-filter control tests"
  key_links:
    - from: "src/components/ToolExecutionPanel.tsx"
      to: "api.toolExecutions.recentExecutions"
      via: "useQuery with excludeProvider"
      pattern: "excludeProvider"
    - from: "src/components/TraceWaterfall.tsx"
      to: "api.llm.sessionCalls"
      via: "useQuery reading { rows, truncated }"
      pattern: "truncated"
---

<objective>
Close every unbounded read in this phase's blast radius BEFORE D-01 starts writing Ástríðr's tool
volume into `toolExecutions`, and guard the two existing panels that read that table unfiltered so
their output does not silently shift.

Purpose: `convex/llm.ts`'s `sessionCalls` and `convex/toolExecutions.ts`'s `listBySession`,
`successRate` and `avgDuration` are all unbounded `.collect()` today (verified live). CLAUDE.md
forbids that shape on the self-hosted single-node SQLite backend — it is the exact class that
caused the 2026-07-21/22 outage and the 2026-08-02 Analytics blackout. This plan runs FIRST so the
guard exists before the volume arrives.

Output: four bounded, truncation-honest queries; an `excludeProvider` argument that preserves
today's two-panel behaviour byte-for-byte; a truncation banner in `TraceWaterfall`; and the tests
that prove it (`convex/toolExecutions.test.ts` is a Wave 0 gap this plan closes).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/105-tool-trace-observability/105-CONTEXT.md
@.planning/phases/105-tool-trace-observability/105-UI-SPEC.md
@.planning/phases/105-tool-trace-observability/105-VALIDATION.md
@CLAUDE.md

<planner_findings>
Read these before starting. Each was verified live against the code on 2026-08-03 and each
CONTRADICTS or CORRECTS something in an upstream artifact. Do not "fix" them back.

**F1 — D-15's stated mechanism (an include-filter on `provider`) would REGRESS both panels.**
CONTEXT.md's plan-phase correction says "add an optional `provider` arg ... and have both panels
request Claude Code rows only." Verified live: `provider` is NOT set on a large share of existing
`toolExecutions` rows.

| Writer | Sets `provider`? | Value |
|---|---|---|
| `convex/ingest.ts:146` — `PostToolUse` (Claude Code SUCCESS) | yes | `data.provider ?? "claude-cli"` |
| `convex/ingest.ts:168` — `PostToolUseFailure` (Claude Code FAILURE) | **NO — field absent** | `undefined` |
| `convex/otelLogs.ts:169` — `tool_result` | **NO — field absent** | `undefined` |
| `convex/runtimeIngest.ts:761` — execution terminal state | **NO — field absent** | `undefined` |
| `convex/runtimeIngest.ts:1029/1078/1092` — `gateway.task_*` | yes | `d.provider ?? "unknown"` (`codex`, `antigravity`, `claude-sdk`, `claude-cli`, …) |

An include-filter of `provider === "claude-cli"` therefore drops **every Claude Code tool failure
row** and every gateway row — `ToolExecutionPanel`'s success rate would read 100% for every tool.
**Implement an EXCLUDE filter instead: `excludeProvider: "astridr"`.** It delivers D-15's actual
intent (zero regression today, no mixed ranking once D-01 lands) because no row carries
`provider === "astridr"` until plan 105-03 ships.

**F2 — the UI-SPEC's truncation copy contains a value that cannot be computed honestly.**
UI-SPEC's Copywriting Contract reads `"Showing the most recent {cap} of {total} — older rows aren't
loaded."` Computing `{total}` requires counting every row for the session, which is exactly the
unbounded read this plan exists to remove. Per this project's honesty rule, **drop the `of {total}`
clause**; do not fabricate or estimate a total. Use the wording given in Task 2.

**F3 — `TraceWaterfall` already sits inside a `SectionErrorBoundary`.** `src/pages/SessionDetail.tsx:175`
mounts it as `<SectionErrorBoundary name="Trace">`. D-12's "confirm" step is satisfied by
observation; **do not rename it to `"Trace Waterfall"`** — the UI-SPEC row for this explicitly says
"unchanged from existing mount", and `"Trace"` is the existing mount.

**F4 — `api.toolExecutions.avgDuration` has ZERO consumers.** Grepped across `src/` and `convex/`:
nothing calls it (`ToolExecutionPanel.tsx:117`'s `avgDuration` is a local `useMemo`, not this
query). Bound it anyway — D-12's extended clause names it explicitly and a dead export is a live
attack/DoS surface on a public Convex deployment. Do **not** delete it (deploy-order hazard, the
104-08 `getBudgetConfig` precedent); flag it in the SUMMARY as a Phase-106 dead-code candidate.

**F5 — consumer inventory (complete, verified by grep).** Change these and nothing else:
`llm.sessionCalls` → `src/components/TraceWaterfall.tsx:184` only.
`toolExecutions.listBySession` → `src/pages/SessionDetail.tsx:47-50` only.
`toolExecutions.recentExecutions` → `ToolExecutionPanel.tsx:43`, `PermissionDecisionsChart.tsx:7`.
`toolExecutions.successRate` → `ToolExecutionPanel.tsx:44` only.
(`src/hooks/useNavCounts.ts` reads `api.commandExecutions.listExecutions`, a DIFFERENT query — leave it alone.)

**F6 — out of scope, do NOT fix here.** `convex/ingest.ts:168` (`PostToolUseFailure`) omitting
`provider` is a real pre-existing provenance gap. It is not in any D-NN decision, and touching it
would alter the very baseline Task 3's control test asserts against. Record it in the SUMMARY as a
finding; do not change it.
</planner_findings>

<interfaces>
<!-- Extracted live 2026-08-03. Use these directly; no codebase exploration needed. -->

The bounded-read shape to copy — `convex/aggregates.ts:31-46` (already in this repo):
```typescript
const LLM_WINDOW_READ_CAP = 4000;
async function fetchLlmRowsForWindow(ctx, windowStart, windowEnd) {
  const rows = await ctx.db
    .query("llmMetrics")
    .withIndex("by_timestamp", (q) => q.gte("timestamp", windowStart).lt("timestamp", windowEnd))
    .filter((q) => q.neq(q.field("archived"), true))
    .take(LLM_WINDOW_READ_CAP);
  return { rows, truncated: rows.length >= LLM_WINDOW_READ_CAP };
}
```

Existing indexes available (`convex/schema.ts:573-576`), no new index in this plan:
`toolExecutions`: `by_session(["sessionId"])`, `by_tool(["toolName","timestamp"])`,
`by_timestamp(["timestamp"])`, `by_provider(["provider"])`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Bound the four unbounded toolExecutions/llm reads and add excludeProvider</name>
  <read_first>
    - convex/toolExecutions.ts (all 101 lines — the four queries you are changing)
    - convex/llm.ts lines 119-137 (`sessionCalls`, the fourth query)
    - convex/aggregates.ts lines 9-46 (the `{ rows, truncated }` shape and the doc comment on why a cursor loop is forbidden)
    - convex/schema.ts lines 561-576 (`toolExecutions` fields + indexes)
  </read_first>
  <files>convex/toolExecutions.ts, convex/llm.ts</files>
  <behavior>
    - `sessionCalls` returns `{ rows, truncated: false, cap: 1000 }` when the session has fewer rows than the cap.
    - `sessionCalls` returns `truncated: true` when the session has exactly `cap` or more rows.
    - `listBySession` behaves identically with its own cap constant.
    - `successRate` with `excludeProvider: "astridr"` omits astridr rows and returns every other row's tool, including rows whose `provider` is `undefined`.
    - `successRate` with no args returns the same tool set it returns today (no behavioural change when the arg is absent).
    - `avgDuration` reports `truncated: true` once the 24h window read hits its cap.
    - `recentExecutions` with `excludeProvider: "astridr"` returns at most `limit` rows and never an astridr row.
  </behavior>
  <action>
In `convex/toolExecutions.ts`:

Add three module-level constants: `SESSION_TOOLS_READ_CAP = 1000`, `TOOL_WINDOW_READ_CAP = 4000`,
`RECENT_SCAN_CAP = 500`. Export all three so the tests can assert against the real values rather
than a duplicated literal.

Add an exported pure helper `excludeByProvider<T extends { provider?: string }>(rows: T[], excludeProvider?: string): T[]`
that returns `rows` unchanged when `excludeProvider` is undefined, and otherwise filters out rows
whose `provider` strictly equals `excludeProvider`. A row with `provider === undefined` is NEVER
excluded — that is the whole point (see finding F1).

`recentExecutions` — add args `excludeProvider: v.optional(v.string())` and
`limit: v.optional(v.float64())` (default 100). Read `by_timestamp` descending with
`.take(RECENT_SCAN_CAP)`, apply `excludeByProvider`, then `.slice(0, limit)`. Return an ARRAY
(unchanged shape — both consumers destructure it as an array). Add a doc comment stating the
tradeoff: the scan is bounded by construction at `RECENT_SCAN_CAP`, so if excluded rows ever
dominate the recent window the caller may receive fewer than `limit` rows; that is the intended
bounded-by-construction behaviour, not a bug.

`successRate` — add `excludeProvider: v.optional(v.string())`. Replace `.collect()` with
`.take(TOOL_WINDOW_READ_CAP)` over the existing 24h `by_timestamp` range, apply
`excludeByProvider`, and change the return from a bare array to
`{ tools: [...same per-tool objects as today...], truncated: boolean, cap: number }` where
`truncated = rawRows.length >= TOOL_WINDOW_READ_CAP` (computed BEFORE the provider filter — the cap
was hit at the read, not after filtering). Keep the per-tool object fields exactly as today:
`toolName`, `success`, `failure`, `total`, `rate`.

`avgDuration` — same treatment: `excludeProvider` arg, `.take(TOOL_WINDOW_READ_CAP)`, return
`{ tools: [...], truncated, cap }` with the per-tool fields unchanged (`toolName`, `avgDurationMs`,
`count`).

`listBySession` — replace `.collect()` with `.take(SESSION_TOOLS_READ_CAP)` and return
`{ rows, truncated, cap }`.

In `convex/llm.ts`:

Add `export const SESSION_CALLS_READ_CAP = 1000;`. Change `sessionCalls` to
`.take(SESSION_CALLS_READ_CAP)` (keeping the existing `by_session` index, `.order("asc")` and the
`archived` filter) and return `{ rows, truncated, cap }`. Update the existing doc comment above it
so it no longer claims "no rolling-window cutoff is applied — the Trace tab shows the whole
session"; state the cap and cite D-12.

Do NOT wrap any of these in a `while` pagination loop — `convex/aggregates.ts:9-30` documents that
Convex permits exactly one paginated query per invocation and that a cursor loop already broke
`backfillTokenSplit` in Phase 104.
  </action>
  <acceptance_criteria>
    - `grep -n "\.collect()" convex/toolExecutions.ts` returns ZERO matches.
    - `grep -c "collect()" convex/llm.ts` counts do not include `sessionCalls`: `sed -n '119,145p' convex/llm.ts | grep -c "collect()"` returns `0`.
    - `grep -c "excludeProvider" convex/toolExecutions.ts` returns at least `4` (helper + three query arg lists).
    - `npx tsc --noEmit` exits 0 (consumers are updated in Task 2 — run tsc at the END of Task 2, not here; a mid-task type error is expected).
    - `SESSION_TOOLS_READ_CAP`, `TOOL_WINDOW_READ_CAP`, `RECENT_SCAN_CAP` and `SESSION_CALLS_READ_CAP` are all `export`ed.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run convex/llm.test.ts</automated>
  </verify>
  <done>All four queries are bounded by an explicit exported cap constant, report `truncated`, and accept `excludeProvider`; no `.collect()` remains in `convex/toolExecutions.ts`.</done>
</task>

<task type="auto">
  <name>Task 2: Update the four consumers and render the D-12 truncation notice</name>
  <read_first>
    - src/pages/SessionDetail.tsx lines 40-60 and 165-180 (the `listBySession` consumer and the existing `SectionErrorBoundary name="Trace"` mount)
    - src/components/TraceWaterfall.tsx lines 180-235 (the `sessionCalls` consumer, the loading/empty branches, and the summary strip)
    - src/components/ToolExecutionPanel.tsx lines 40-120 (the `recentExecutions` + `successRate` consumers)
    - src/components/PermissionDecisionsChart.tsx lines 1-25 (the `recentExecutions` consumer)
    - .planning/phases/105-tool-trace-observability/105-UI-SPEC.md § Copywriting Contract and § Color
  </read_first>
  <files>src/pages/SessionDetail.tsx, src/components/TraceWaterfall.tsx, src/components/ToolExecutionPanel.tsx, src/components/PermissionDecisionsChart.tsx</files>
  <action>
`src/pages/SessionDetail.tsx` — the `useQuery(api.toolExecutions.listBySession, ...)` at lines 47-50
now returns an object. Change the local to read `.rows`, defaulting to `[]` while the query is
`undefined`. Everything downstream of `toolExecutions` in this file keeps working on an array.
Leave the `SectionErrorBoundary name="Trace"` mount at line 175 EXACTLY as it is (finding F3).

`src/components/TraceWaterfall.tsx` — `useQuery(api.llm.sessionCalls, { sessionId })` now returns
`{ rows, truncated, cap } | undefined`. Destructure it: keep `rows === undefined` as the loading
branch (returns `null`) and `rows.length === 0` as the existing empty branch (copy unchanged).
Add a truncation banner rendered ABOVE the summary strip, only when `truncated` is true. Copy
(finding F2 — no `{total}` clause):
`Showing the most recent {cap} calls — older calls in this session aren't loaded.`
Style it as a token-driven inline banner: `var(--status-warn)` for the text/border via
`color-mix(in srgb, var(--status-warn) 40%, transparent)` for the border and
`color-mix(in srgb, var(--status-warn) 12%, transparent)` for the background, `text-sm`, `rounded-md`,
`px-3 py-2`. ZERO hardcoded hex.

`src/components/ToolExecutionPanel.tsx` — pass `{ excludeProvider: "astridr" }` to BOTH
`useQuery(api.toolExecutions.recentExecutions, ...)` and `useQuery(api.toolExecutions.successRate, ...)`.
`recentExecutions` still returns an array (no shape change). `successRate` now returns an object —
read `.tools` with `?? []` while loading. Add a one-line comment citing D-15 and D-02 explaining
WHY the exclusion exists (this panel is the operator's own Claude Code session view; Ástríðr's
tools live on `/tools`). Make no other change to this component — no layout, no copy, no ranking
logic.

`src/components/PermissionDecisionsChart.tsx` — pass `{ excludeProvider: "astridr" }` to
`useQuery(api.toolExecutions.recentExecutions, ...)`. Same one-line D-15 comment. No other change.

Run `npx tsc --noEmit` at the end of this task and fix any remaining shape mismatch.
  </action>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0.
    - `grep -c 'excludeProvider: "astridr"' src/components/ToolExecutionPanel.tsx` returns `2`.
    - `grep -c 'excludeProvider: "astridr"' src/components/PermissionDecisionsChart.tsx` returns `1`.
    - `grep -n 'SectionErrorBoundary name="Trace"' src/pages/SessionDetail.tsx` still matches (the boundary was not renamed or removed).
    - `grep -Ei '#[0-9a-f]{3,8}' src/components/TraceWaterfall.tsx | grep -v '^\s*//'` returns zero matches (no hex introduced).
    - The truncation banner string in `TraceWaterfall.tsx` does NOT contain the substring `of ` followed by a total-count expression — assert by `grep -c "older calls in this session aren't loaded" src/components/TraceWaterfall.tsx` returning `1`.
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run src/components/TraceWaterfall.test.tsx</automated>
  </verify>
  <done>All four consumers compile against the new shapes; the truncation notice renders only when a cap is hit, with no fabricated total and no hardcoded hex.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Tests — caps, truncation, and the D-15 zero-regression CONTROL</name>
  <read_first>
    - convex/llm.test.ts lines 30-150 (the `sessionCallsLogic` mirror-function convention this repo uses instead of convex-test)
    - convex/runtimeIngest.test.ts lines 1-60 (the same convention, and the "convex-test is not installed" note)
    - convex/toolExecutions.ts (post-Task-1 state)
    - .planning/phases/105-tool-trace-observability/105-VALIDATION.md § Per-Task Verification Map (the two rows naming `convex/toolExecutions.test.ts`)
  </read_first>
  <files>convex/toolExecutions.test.ts, convex/llm.test.ts, src/components/TraceWaterfall.test.tsx, src/components/PermissionDecisionsChart.test.tsx</files>
  <behavior>
    - CONTROL (D-15): given a fixture of Claude-Code rows (`provider: "claude-cli"`), provider-less rows (`provider: undefined`) and gateway rows, `successRate({excludeProvider:"astridr"})` returns output DEEPLY EQUAL to `successRate({})` run against the same fixture with the astridr rows removed. Two runs, one variable.
    - NEGATIVE CONTROL: the same assertion FAILS if the filter is written as an include-filter on `provider === "claude-cli"` — encode this as an explicit test asserting that an include-filter would drop the provider-less and gateway rows (so a future editor cannot silently swap the mechanism back).
    - `truncated` is `false` below the cap and `true` at/above the cap, for `sessionCalls`, `listBySession`, `successRate` and `avgDuration`.
    - `excludeByProvider` never removes a row whose `provider` is `undefined`.
  </behavior>
  <action>
Create `convex/toolExecutions.test.ts` (Wave 0 gap). Follow the established convention exactly:
`convex-test` is NOT installed, so mirror each query handler's logic as a local pure function over
an in-memory `store` array (the `sessionCallsLogic` pattern in `convex/llm.test.ts:37-60`), and
import the genuinely-pure exports (`excludeByProvider`, the four cap constants) directly from
`./toolExecutions` so the constants under test are the real ones.

Cover, at minimum:
1. `excludeByProvider` unit behaviour including the `provider: undefined` case.
2. The D-15 CONTROL described in `<behavior>` — build ONE fixture containing all four row classes
   (`claude-cli`, `undefined`, `gateway:*` with `provider: "codex"`, and `astridr`), then assert
   `toEqual` between the excluded run and the astridr-free baseline run.
3. The negative control proving an include-filter would drop rows (assert the include-filtered set
   is strictly smaller than the excluded set on the same fixture).
4. Cap/truncation boundaries for `listBySession`, `successRate` and `avgDuration`: exactly
   `cap - 1` rows → `truncated === false`; exactly `cap` rows → `truncated === true`.
5. `truncated` for `successRate` is computed from the RAW read count, not the post-filter count —
   feed a fixture of `cap` rows that are ALL astridr and assert `truncated === true` while `tools`
   is empty.

Extend `convex/llm.test.ts`'s existing `describe("sessionCalls ...")` block with the same
cap/truncation boundary cases against its existing `sessionCallsLogic` mirror (update that mirror
to `.slice(0, cap)` + `truncated` so it stays a faithful mirror of the real handler).

Extend `src/components/TraceWaterfall.test.tsx` with a component test asserting the truncation
banner: it is ABSENT when the mocked `sessionCalls` returns `truncated: false`, and PRESENT with
the exact Task-2 copy when it returns `truncated: true`. Mock `api.llm.sessionCalls` via the
existing mock scaffold at the top of that file (lines ~10-30).

Add `src/components/PermissionDecisionsChart.test.tsx` if it does not exist, asserting the
component passes `excludeProvider: "astridr"` to its query (spy on the mocked `useQuery` args).

Every test must be mutation-verified before you call it done: temporarily revert one production
change (e.g. remove the `excludeProvider` argument from `successRate`'s handler) and confirm the
corresponding test FAILS, then restore. Record which mutation you ran in the SUMMARY.
  </action>
  <acceptance_criteria>
    - `npx vitest run convex/toolExecutions.test.ts convex/llm.test.ts src/components/TraceWaterfall.test.tsx src/components/PermissionDecisionsChart.test.tsx` exits 0 with at least 12 new assertions across the four files.
    - `convex/toolExecutions.test.ts` imports `excludeByProvider` and at least two cap constants FROM `./toolExecutions` (assert: `grep -c 'from "./toolExecutions"' convex/toolExecutions.test.ts` >= 1).
    - The D-15 control test uses `toEqual` between two runs (assert the test name contains the word `control`).
    - `npx vitest run` (full suite) exits 0.
    - The SUMMARY records the exact mutation used to prove the control test is not vacuous, and the observed failure output.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run convex/toolExecutions.test.ts convex/llm.test.ts src/components/TraceWaterfall.test.tsx</automated>
  </verify>
  <done>The D-15 zero-regression claim is proven by a two-run control, not by the presence of an argument; every cap reports truncation at its exact boundary; the full suite is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → Convex query | `recentExecutions`, `successRate`, `avgDuration`, `listBySession`, `sessionCalls` are all PUBLIC queries callable by anyone with the deployment URL |
| Convex query → self-hosted SQLite | single writer, single node; an unbounded read starves ingest for every other caller |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-105-01 | Denial of Service | `llm.sessionCalls`, `toolExecutions.listBySession` | mitigate | Replace `.collect()` with `.take(1000)`; an anonymous caller can no longer force a full-session scan. Task 1. |
| T-105-02 | Denial of Service | `toolExecutions.successRate`, `avgDuration` | mitigate | `.take(4000)` over the existing 24h `by_timestamp` range. Task 1. |
| T-105-03 | Denial of Service | `toolExecutions.recentExecutions` with a filter | mitigate | Bound the SCAN at `RECENT_SCAN_CAP = 500` and filter in JS, so a filter that matches nothing cannot walk the table. Task 1. |
| T-105-04 | Information Disclosure | the same public queries | accept | Pre-existing posture: every OBSERVE-tier query in this deployment is unauthenticated; this plan changes no auth. Tracked for Phase 106 (DEBT). |
| T-105-05 | Tampering | `api.toolExecutions.insert` is a PUBLIC mutation | accept | Pre-existing (4 ingest paths already call it). Not widened by this plan. Recorded as a Phase-106 candidate; do not convert it here — 4 live call sites and a deploy-order hazard (the 104-08 `getBudgetConfig` precedent). |
| T-105-SC | Tampering | package installs | mitigate | This plan installs ZERO packages. `npm install` must not appear in any commit; if a dependency turns out to be needed, stop and re-run the Package Legitimacy Gate. |
</threat_model>

<verification>
- `npx vitest run` full suite green.
- `npx tsc --noEmit` clean.
- `npm run build` clean.
- `grep -rn "\.collect()" convex/toolExecutions.ts` → zero matches.
- Manual reasoning check recorded in the SUMMARY: name every consumer of the four changed queries and confirm each was updated (the F5 inventory is the checklist).
</verification>

<success_criteria>
- Four previously unbounded queries are bounded by exported cap constants and report `truncated`.
- `TraceWaterfall` shows a token-driven truncation banner when, and only when, a cap is hit — with no fabricated total.
- `ToolExecutionPanel` and `PermissionDecisionsChart` are proven byte-identical under a two-run control that seeds astridr rows.
- `convex/toolExecutions.test.ts` exists (Wave 0 gap closed).
- Full Vitest suite, `tsc --noEmit`, and `npm run build` all green.
</success_criteria>

<output>
Create `.planning/phases/105-tool-trace-observability/105-01-SUMMARY.md` when done. It MUST record:
the exact cap values chosen, the mutation used to prove the D-15 control is non-vacuous, the F6
`PostToolUseFailure` provenance finding (left unfixed, with the reason), and the F4 dead
`avgDuration` query as a Phase-106 candidate.
</output>
