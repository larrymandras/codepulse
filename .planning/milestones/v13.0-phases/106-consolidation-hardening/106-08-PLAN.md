---
phase: 106-consolidation-hardening
plan: 08
type: execute
wave: 3
depends_on: ["106-03", "106-04", "106-05", "106-07"]
files_modified:
  - .planning/phases/106-consolidation-hardening/106-HUMAN-UAT.md
  - .planning/REQUIREMENTS.md
  - .planning/STATE.md
autonomous: false
requirements: [DEBT-01, DEBT-02, DEBT-03, DEBT-04]

user_setup:
  - service: forge-daemon
    why: "The drag round-trip must run against a REAL Forge daemon — the whole point of the deferred check is that the daemon actually moves directories on disk. A daemon-less run would only exercise the optimistic UI."
    env_vars: []
    dashboard_config:
      - task: "Confirm the Forge daemon is running before the session, and be ready to stop it mid-command to exercise the honest-failure path"
        location: "the machine running the daemon"

must_haves:
  truths:
    - "D-08: The live-Forge-daemon drag round-trip — archive, move to a project workspace, restore — is exercised in the browser against a real daemon, plus a deliberately induced failure that clears the pending overlay and surfaces the real refusal instead of a false success."
    - "Phase 100's genuinely-open item — the CR-02 shadowed-row Cold Storage no-op, previously accepted as unit-verified only because the live catalog held zero shadowed-merged skills — is exercised live using the staging technique this project already proved works."
    - "STATE.md's claim that Phase 100's drag round-trip 'remains outstanding' is corrected, because 100-HUMAN-UAT.md records it as operator-verified live on 2026-07-25 — a day after the STATE.md note was written."
    - "REQUIREMENTS.md's DEBT-01..04 markers reflect what this phase actually evidenced, with any unclosed item marked PARTIAL and its blocker named rather than rounded up to satisfied."
    - "106-HUMAN-UAT.md is closed with a Summary whose counts reconcile against every test section in the file across all three UAT sessions."
  artifacts:
    - path: ".planning/phases/106-consolidation-hardening/106-HUMAN-UAT.md"
      provides: "the final UAT log for the phase, closed with reconciled counts"
      contains: "## Summary"
    - path: ".planning/REQUIREMENTS.md"
      provides: "DEBT-01..04 markers reflecting this phase's recorded evidence"
      contains: "DEBT-01"
  key_links:
    - from: "the Skills page drag gesture"
      to: "a real Forge daemon moving directories on disk"
      via: "an enqueued lifecycle command reconciled back to done/failed"
      pattern: "enqueueLifecycle"
    - from: "each plan's evidence artifact"
      to: "the DEBT-0N marker in REQUIREMENTS.md"
      via: "a marker is written only from a cited artifact section, never from a plan having run"
      pattern: "DEBT-0"
---

<objective>
UAT session C of three, plus the phase's close-out.

## A correction the planner found and this plan must carry through

CONTEXT.md D-08 and STATE.md both describe Phase 100's "live-Forge-daemon drag round-trip" as outstanding. `100-HUMAN-UAT.md` says otherwise: its Test 1 records `PASSED (operator-verified live 2026-07-25)` with concrete detail — `agent-browser` dragged onto Cold Storage and archived, the dormant row dragged back onto Global and restored, and an honest daemon refusal observed on `test-driven-development` ("…no longer exists at its source location … Nothing changed on disk") with no false success. The STATE.md note asserting otherwise is dated 2026-07-24, one day BEFORE that verification.

What is genuinely still open from Phase 100 is its **Test 2**: the CR-02 shadowed-row Cold Storage no-op. It is marked `UNIT-VERIFIED` and `Accepted` explicitly because "the live catalog contains 0 shadowed-merged skills (every cold skill is dormant-only) — fabricating one was deferred by operator". Plan 106-06 has since demonstrated that a shadowed-merged skill CAN be staged reliably on disk, so the reason it was deferred no longer holds.

This plan therefore does both: re-runs the round-trip (D-08 locks it, and it is cheap once the stack is up) AND closes the item that was actually left open, then corrects the stale claim per this repo's Stale Docs rule.

Purpose: finish DEBT-04, and close the phase honestly.
Output: the final sections of `106-HUMAN-UAT.md`, corrected STATE.md and REQUIREMENTS.md markers.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/106-consolidation-hardening/106-CONTEXT.md
@.planning/phases/106-consolidation-hardening/106-HUMAN-UAT.md
@.planning/milestones/v11.0-phases/100-control-surface-ux-menu-drag-lanes-optimistic-reconcile/100-HUMAN-UAT.md
@.planning/REQUIREMENTS.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Stage a shadowed-merged skill and write the two test stubs</name>
  <files>.planning/phases/106-consolidation-hardening/106-HUMAN-UAT.md</files>
  <read_first>
    - `.planning/milestones/v11.0-phases/100-control-surface-ux-menu-drag-lanes-optimistic-reconcile/100-HUMAN-UAT.md` — read it in full. Test 1's recorded PASS and Test 2's `UNIT-VERIFIED` / `Accepted` status are the factual basis for this plan's scoping, and Test 2's `expected:` line is the exact behaviour to re-state.
    - `.planning/phases/106-consolidation-hardening/106-HUMAN-UAT.md` — the running artifact from plans 106-06 and 106-07; continue its numbering and its `expected:`/`result:`/`notes:` shape
    - `.planning/phases/106-consolidation-hardening/106-06-SUMMARY.md` — the staging technique as actually executed this phase, including the real scan-root paths, so this task does not re-derive them
    - `src/components/skills/SkillControlSurfaceProvider.tsx` and `src/components/skills/ScopeRail.tsx` (or whichever files own drag lanes and the scope rail — locate them by searching for the drop resolution logic) — read the lane matrix so each test's `expected:` names the real lanes and the real no-op condition
  </read_first>
  <action>
**A. Bring the stack up and confirm a REAL daemon.** Start the dev server, confirm the Forge daemon is running, and record its identity/version and the Convex backend the UI targets. This test is worthless against an absent daemon — the optimistic overlay would paint and never reconcile, which looks like a pass at a glance. Record the proof the daemon is live (a completed rescan, or a real command reaching `done`), not merely that a process exists.

**B. Stage the fixtures.** Using the same disk-backed technique plan 106-06 used (directories under the real scan roots, then a rescan; DB-only rows get pruned):

  - `uat106-drag` — a plain active global skill, the subject of the archive → move → restore round-trip.
  - A project workspace to move it into — reuse an existing throwaway workspace if one exists, otherwise create one and record its path.
  - `uat106-shadow` — the shadowed-merged fixture Phase 100 could not obtain: the SAME skill name present as an active global copy AND a dormant cold copy, which a rescan merges into one shadowed row. This is exactly the shape `98-HUMAN-UAT.md` Test 4's notes describe.

  Confirm each fixture registered with its intended origin shape before the session, and record every created path. In particular, confirm `uat106-shadow` genuinely renders as a shadowed-merged row — if it does not, the Test-2 re-run cannot happen and that must be recorded honestly rather than substituting a dormant-only row.

**C. Append two test stubs** to `## Tests` in `106-HUMAN-UAT.md`, continuing the numbering:

  - `### N. Live Forge daemon drag round-trip (Phase-100 Test 1 re-verification, D-08)` — expected: dragging `uat106-drag` onto Cold Storage enqueues an archive against the real daemon, paints the pending overlay immediately, and reconciles to `done` with the directory actually moved on disk; dragging it onto a project scope opens the workspace picker and completes the move; dragging the cold row back onto Global restores it. Then, with the daemon stopped mid-command, the pending overlay clears and the real refusal or expiry surfaces — never a false success. Note in the stub that `100-HUMAN-UAT.md` records this as already PASSED live on 2026-07-25 and that this run is a re-verification D-08 asked for, so a pass is the expected outcome.
  - `### N+1. CR-02 shadowed-row Cold Storage no-op (Phase-100 Test 2 — the item genuinely left open)` — expected: dragging the shadowed-merged `uat106-shadow` row out of the Cold Storage view and dropping it back on the Cold Storage rail entry fires NO mutation (same-scope no-op) and, critically, does NOT archive the skill's live ACTIVE copy. Expect the explicit "already in Cold Storage" toast that Phase 100 observed for the dormant case. State in the stub that Phase 100 accepted this as unit-verified only because no shadowed-merged skill existed live, and that this run supplies the missing live gesture.

**D.** Update `## Current Test` and `updated:`. Leave `status: in-progress` — Task 3 closes the file.

No source file is modified by this task.
  </action>
  <verify>
    <automated>test $(grep -c '^### [0-9]\+\.' .planning/phases/106-consolidation-hardening/106-HUMAN-UAT.md) -ge 9 &amp;&amp; grep -q "^status: in-progress" .planning/phases/106-consolidation-hardening/106-HUMAN-UAT.md &amp;&amp; git diff --quiet -- src convex</automated>
  </verify>
  <acceptance_criteria>
    - Two new `### N.` sections exist continuing the file's numbering, each with a non-empty `expected:` and an empty `result:`.
    - The round-trip stub cites `100-HUMAN-UAT.md`'s recorded 2026-07-25 PASS and frames this run as a D-08 re-verification.
    - The shadowed-no-op stub cites Phase 100's `UNIT-VERIFIED`/`Accepted` status and its stated reason (zero shadowed-merged skills in the live catalog).
    - `## Environment` is extended with every fixture path created here, the workspace path used for the move leg, and the daemon-liveness proof.
    - `uat106-shadow` is confirmed rendering as a shadowed-merged row before the session, and that confirmation is recorded; if it could not be achieved, the artifact records that plainly and the Test-2 section is marked `blocked` with the reason.
    - `git diff --quiet -- src convex` succeeds.
  </acceptance_criteria>
  <done>A real daemon is proven live, a shadowed-merged fixture exists for the first time in this project's live catalog, and both test stubs carry code-derived and precedent-cited expectations.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Live guided session — drag round-trip and the shadowed-row no-op</name>
  <files>.planning/phases/106-consolidation-hardening/106-HUMAN-UAT.md</files>
  <action>Drive the five legs with Larry and STOP for his confirmation after each. Confirm he is Clerk-signed-in before the first drag. For every leg that claims something moved, have him check the actual folder on disk — only a real daemon can prove it, which is exactly why this test was deferred. Record each observed outcome into its `result:` line quoting real toast/status text, including the on-disk outcome at the named path. For the honest-failure leg, record the exact refusal or expiry text and state explicitly that no false success appeared. For the shadowed no-op, record that the ACTIVE copy survived, verified both in the UI and on disk. Confirm the Forge daemon is restarted and healthy before ending the session. Assign each section a `pass` / `issue` / `blocked` verdict. Change no source file — task 3 owns cleanup and close-out.</action>
  <what-built>
The dev server is up against a Forge daemon proven live (not merely running), and two fixtures are staged: `uat106-drag`, a plain active global skill for the round-trip, and `uat106-shadow`, a shadowed-merged row — the exact fixture Phase 100 could not obtain, which is why its CR-02 no-op test was accepted as unit-verified only.

`100-HUMAN-UAT.md` records the round-trip itself as already operator-verified live on 2026-07-25; STATE.md's note calling it outstanding predates that by a day and will be corrected in the next task.
  </what-built>
  <how-to-verify>
Sign in with Clerk first — the lifecycle mutations are auth-gated, and an unauthenticated drag surfaces a redacted "Server Error" from the auth guard that reads like a feature bug.

**Round-trip (`uat106-drag`):**
1. Drag it onto Cold Storage. Expect the pending overlay immediately, then reconciliation to done, and the directory actually gone from the active skills folder on disk.
2. Drag the now-dormant row onto a Project scope; pick the staged workspace in the dialog. Expect the move to complete and the directory to exist in that workspace on disk.
3. Drag it back onto Global. Expect a restore back to active.
4. **Honest-failure leg:** stop the Forge daemon, then issue one more drag. Expect the pending overlay to clear and a real refusal or expiry to surface — a queued command that visibly expires, or a real daemon refusal. What must NOT happen is a false success state. Restart the daemon afterwards.

**Shadowed no-op (`uat106-shadow`) — this is the leg Phase 100 never got to run:**
5. In the Cold Storage view, drag the shadowed row out and drop it back onto the Cold Storage rail entry. Expect: nothing mutates, the "already in Cold Storage" toast appears, and — the part that matters — the skill's live ACTIVE copy is untouched. Confirm afterwards that the active copy is still present both on the Skills page and on disk.

For each leg, tell Claude what you actually saw, including the toast text. Where a leg claims something moved on disk, check the folder — the whole reason this test was deferred is that only a real daemon can prove it.
  </how-to-verify>
  <verify>
    <automated>test $(grep -c '^result: *$' .planning/phases/106-consolidation-hardening/106-HUMAN-UAT.md) -eq 0</automated>
    <human-check>Larry confirms per leg what happened on screen and on disk, including the honest-failure leg's actual message and whether the shadowed row's active copy survived.</human-check>
  </verify>
  <acceptance_criteria>
    - Both new `result:` lines are filled with what was actually observed, quoting real toast/status text rather than summarising.
    - The round-trip result records, per leg, the on-disk outcome (directory present/absent at the named path) — not only the UI state.
    - The honest-failure leg's result records the exact refusal or expiry text observed and states explicitly that no false success appeared.
    - The shadowed-no-op result records that the ACTIVE copy survived, verified both in the UI and on disk.
    - Each section carries a `pass` / `issue` / `blocked` verdict.
    - The Forge daemon is confirmed restarted and healthy at the end of the session.
  </acceptance_criteria>
  <resume-signal>Report each leg's outcome (screen and disk), or type "blocked" with what stopped you.</resume-signal>
  <done>Both tests carry a real recorded result with on-disk verification, the honest-failure leg’s actual message is captured, the shadowed row’s active copy is proven untouched, and the daemon is confirmed healthy.</done>
</task>

<task type="auto">
  <name>Task 3: Clean up, correct the stale claims, mark the requirements, and close the UAT log</name>
  <files>.planning/phases/106-consolidation-hardening/106-HUMAN-UAT.md, .planning/REQUIREMENTS.md, .planning/STATE.md</files>
  <read_first>
    - `.planning/phases/106-consolidation-hardening/106-HUMAN-UAT.md` — every test section across all three sessions; the Summary must reconcile against all of them, not just this plan's two
    - `.planning/phases/106-consolidation-hardening/106-DEBT-VERIFICATION.md` — DEBT-01's verdict and DEBT-02's export/archive verdicts
    - `.planning/phases/106-consolidation-hardening/106-BUNDLE-ANALYSIS.md` — DEBT-03's chunk verdict
    - `.planning/phases/106-consolidation-hardening/106-TAILSCALE-CHECKLIST.md` — DEBT-03's D-10 half
    - `.planning/REQUIREMENTS.md` § "Phase 106 — Consolidation & Hardening" — the four DEBT bullets to mark, and the marker vocabulary used by the satisfied Phase 103/104/105 bullets above them (`✓ SATISFIED` / `⚠ PARTIAL` with the reason stated inline)
    - `.planning/STATE.md` — locate the comment line asserting that Phase 100's manual live-Forge-daemon drag verification "remains outstanding". Read the surrounding block before editing; this file has a documented history of being clobbered.
    - `CLAUDE.md` § "Stale Docs" — corrections land in the same commit as the discovery
  </read_first>
  <action>
**A. Clean up.** Remove every fixture created by this plan (`uat106-drag`, `uat106-shadow`, and the staged workspace if this plan created it), trigger a rescan, and confirm zero `uat106-*` rows remain in the registry and zero directories remain on disk. Extend the artifact's `## Cleanup` section with the commands and the post-cleanup verification result. Use the daemon's normal rescan reconciliation — never a bulk delete or patch against the live self-hosted Convex.

**B. Correct STATE.md's stale Phase-100 claim — carefully.**

  `.planning/STATE.md` carries a note asserting that Phase 100's own manual live-Forge-daemon drag verification "remains outstanding, same category as Phase 98/99's own deferred manual checks." `100-HUMAN-UAT.md` Test 1 records it as `PASSED (operator-verified live 2026-07-25)` — a day after that note was written. Correct it to state that the round-trip was operator-verified live on 2026-07-25, that Phase 100's genuinely-outstanding item was its Test 2 shadowed-row no-op, and what this phase did about it.

  This file has been clobbered repeatedly in this project, including by concurrent sessions that ran no state verb at all. Follow the established protocol exactly:
  - Make the edit as a single asserted string replacement against the exact existing sentence. Do not rewrite the surrounding block.
  - Immediately run `git diff .planning/STATE.md` and account for EVERY deleted line. If any deletion is not one you made — in particular a regressed `stopped_at`/`last_activity`, an inflated `total_phases`, or a recomputed `percent` — do not commit. Restore with `git show HEAD:.planning/STATE.md > .planning/STATE.md` and re-apply only your own edit.
  - Do not run any `gsd-sdk` state/roadmap/phase/milestone verb in this plan. Every one of them has a recorded history of miscounting or clobbering this file.
  - Record the `git diff .planning/STATE.md` output in the SUMMARY.

**C. Mark REQUIREMENTS.md's DEBT-01..04 from evidence, not from completion.**

  This is the phase's last plan, which by this project's convention is where requirement markers are written (Phase 105's plan 105-09 did the same). Write each marker ONLY from a cited artifact section, and cite it inline:

  - **DEBT-01** — mark from `106-DEBT-VERIFICATION.md` § DEBT-01's `VERDICT:` line. Note in the marker that this was a verify-and-close with zero source changes, and that the scope was codepulse only (D-06).
  - **DEBT-02** — mark from § DEBT-02's `ARCHIVE VERDICT:` and `### Cancel handoff`. If Larry deferred the cancel, the marker is PARTIAL with "export verified and archived; subscription cancel pending Larry" as the named blocker — never SATISFIED on an uncancelled subscription.
  - **DEBT-03** — mark from `106-BUNDLE-ANALYSIS.md`'s `DEBT-03 CHUNK VERDICT:` AND `106-TAILSCALE-CHECKLIST.md`'s frontmatter `status:`. Both halves must be closed for a SATISFIED marker; if either is short, mark PARTIAL and name which half. Where a chunk is documented as an accepted exception rather than reduced, say so in the marker with its size — do not let "below the warning threshold" imply chunks that are still above it.
  - **DEBT-04** — mark from `106-HUMAN-UAT.md`'s `## Summary`. SATISFIED only if every one of the three deferred sequences has a `pass`; any `issue` or `blocked` makes it PARTIAL with the specific test named.

  Do not round any marker up. A marker that overstates is worse than one that admits a gap: this project has repeatedly found that a green artifact hid a live defect, and every honest PARTIAL in REQUIREMENTS.md above these bullets was written for that reason.

**D. Close the UAT log.** Update `## Summary` so total/passed/issues/pending/blocked reconcile against EVERY `### N.` section in the file across all three sessions — recount them, do not extrapolate from the previous plans' numbers. Set frontmatter `status:` to `passed`, `partial`, or `failed` to match, set `updated:`, and set `## Current Test` to `[complete]`.

**E.** If any UAT test recorded an `issue`, state plainly in the SUMMARY that DEBT-04 is not fully closed, name the test, and recommend `/gsd-plan-phase 106 --gaps`.
  </action>
  <verify>
    <automated>grep -qE "^status: (passed|partial|failed)" .planning/phases/106-consolidation-hardening/106-HUMAN-UAT.md &amp;&amp; grep -q "\[complete\]" .planning/phases/106-consolidation-hardening/106-HUMAN-UAT.md &amp;&amp; test $(grep -c '^result: *$' .planning/phases/106-consolidation-hardening/106-HUMAN-UAT.md) -eq 0 &amp;&amp; grep -qE "DEBT-01.*(SATISFIED|PARTIAL)" .planning/REQUIREMENTS.md &amp;&amp; grep -qE "DEBT-02.*(SATISFIED|PARTIAL)" .planning/REQUIREMENTS.md &amp;&amp; grep -qE "DEBT-03.*(SATISFIED|PARTIAL)" .planning/REQUIREMENTS.md &amp;&amp; grep -qE "DEBT-04.*(SATISFIED|PARTIAL)" .planning/REQUIREMENTS.md &amp;&amp; git diff --quiet -- src convex</automated>
  </verify>
  <acceptance_criteria>
    - No `uat106-*` row remains in the live registry and no `uat106-*` directory remains on disk; the `## Cleanup` section records the post-cleanup verification result.
    - `.planning/STATE.md` no longer claims Phase 100's drag round-trip is outstanding, and instead records the 2026-07-25 operator verification plus what this phase did about Test 2.
    - The SUMMARY contains the verbatim `git diff .planning/STATE.md` output, and every deleted line in it is one this task made. No `gsd-sdk` state/roadmap/phase/milestone verb was run.
    - Each of DEBT-01, DEBT-02, DEBT-03, DEBT-04 in `.planning/REQUIREMENTS.md` carries a `✓ SATISFIED` or `⚠ PARTIAL` marker that cites the specific artifact section it was written from (by filename and section heading).
    - Any PARTIAL marker names its blocker concretely; no marker asserts a closure the artifacts do not evidence.
    - DEBT-02 is not marked SATISFIED if the subscription cancel is still pending.
    - DEBT-03's marker states, with sizes, any chunk that remains above 500 kB as an accepted exception rather than implying all chunks are under threshold.
    - `106-HUMAN-UAT.md` has `status:` set to a terminal value, `## Current Test` set to `[complete]`, zero blank `result:` lines, and `## Summary` counts that equal the actual number of `### N.` sections in the file.
    - `git diff --quiet -- src convex` succeeds — this task changes no source.
  </acceptance_criteria>
  <done>The live catalog is clean, the two stale planning claims are corrected in the same session that found them, every DEBT marker is written from a cited artifact rather than from a plan having run, and the phase's UAT log is closed with counts that reconcile.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| drag gesture → Forge daemon → filesystem | A UI gesture causes a real daemon to move real directories, including the ability to archive a live active skill |
| planning artifacts → STATE.md / REQUIREMENTS.md | Shared, clobber-prone files edited from a session that may be concurrent with another |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-106-27 | Tampering | a real user skill archived by the shadowed-row no-op regression | mitigate | Every fixture is a throwaway `uat106-*` name; the shadowed test's whole point is asserting the ACTIVE copy survives, verified in UI and on disk. No pre-existing skill is used as a subject. |
| T-106-28 | Tampering | `.planning/STATE.md` clobbered by a concurrent session | mitigate | Single asserted string replacement, `git diff .planning/STATE.md` reviewed with every deletion accounted for before commit, restore-from-HEAD path documented, and zero `gsd-sdk` state verbs run. The diff is recorded in the SUMMARY. |
| T-106-29 | Repudiation | a requirement marked SATISFIED beyond its evidence | mitigate | Each marker must cite the artifact filename and section it was written from; PARTIAL is mandatory where an artifact is short, and DEBT-02 is explicitly barred from SATISFIED while the cancel is pending. |
| T-106-30 | Tampering | live self-hosted Convex during cleanup | mitigate | Cleanup goes through the daemon's normal rescan reconciliation; no bulk delete or patch, per `CLAUDE.md` § "Self-Hosted Convex — Operational Rules". |
| T-106-31 | Denial of service | Forge daemon left stopped after the honest-failure leg | mitigate | The checkpoint's acceptance requires the daemon confirmed restarted and healthy before the session ends. |
| T-106-SC | Tampering | npm/pip/cargo installs | mitigate | This plan installs nothing. |
</threat_model>

<verification>
- `106-HUMAN-UAT.md`: terminal `status:`, `## Current Test` = `[complete]`, zero blank `result:` lines, `## Summary` counts equal to the number of `### N.` sections.
- Zero `uat106-*` rows in the registry and zero `uat106-*` directories on disk.
- `.planning/STATE.md` no longer claims Phase 100's drag round-trip is outstanding; its `git diff` is recorded and fully accounted for.
- All four DEBT bullets in `.planning/REQUIREMENTS.md` carry an evidence-cited SATISFIED or PARTIAL marker.
- `git diff --quiet -- src convex` succeeds.
</verification>

<success_criteria>
- The drag round-trip is re-verified live against a real daemon, including the honest-failure leg, with on-disk outcomes recorded.
- Phase 100's genuinely-open CR-02 shadowed-row no-op is finally exercised live, with the active copy proven untouched.
- Both stale planning claims found during this phase are corrected in the session that found them.
- Every DEBT-0N marker is traceable to a cited artifact section, with nothing rounded up.
</success_criteria>

<output>
Create `.planning/phases/106-consolidation-hardening/106-08-SUMMARY.md` when done.
</output>
