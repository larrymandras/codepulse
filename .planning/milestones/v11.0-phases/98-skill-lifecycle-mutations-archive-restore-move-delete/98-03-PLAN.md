---
phase: 98-skill-lifecycle-mutations-archive-restore-move-delete
plan: 03
type: execute
wave: 2
depends_on: ["98-01"]
files_modified:
  - src/components/ui/dropdown-menu.tsx
  - src/hooks/useLifecycle.ts
  - src/hooks/useLifecycle.test.ts
  - src/components/skills/MoveToProjectDialog.tsx
  - src/components/skills/MoveToProjectDialog.test.tsx
  - src/components/skills/DeleteSkillDialog.tsx
  - src/components/skills/DeleteSkillDialog.test.tsx
autonomous: true
requirements: [LIFE-03, LIFE-04, LIFE-06]
user_setup: []

must_haves:
  truths:
    - "A DropdownMenu primitive exists in src/components/ui and imports from the installed radix-ui meta-package"
    - "useLifecycle exposes lifecycle command rows keyed by (skillName, action) with the reused IntakeRowStatus model, so a row can show its in-flight/queued/expired badge (LIFE-06)"
    - "MoveToProjectDialog offers the synced-workspace picker (no class filter) and enqueues a move on confirm (LIFE-03)"
    - "DeleteSkillDialog's destructive button stays disabled until the typed name exactly (case-sensitive) matches the skill name (LIFE-04/D-06)"
  artifacts:
    - path: "src/components/ui/dropdown-menu.tsx"
      provides: "shadcn dropdown-menu primitive"
      contains: "radix-ui"
    - path: "src/hooks/useLifecycle.ts"
      provides: "lifecycle command row hook + adapter, reusing IntakeRowStatus"
      exports: ["useLifecycleCommands", "LifecycleCommandRow"]
    - path: "src/components/skills/MoveToProjectDialog.tsx"
      provides: "workspace-picker move dialog"
      contains: "listWorkspaces"
    - path: "src/components/skills/DeleteSkillDialog.tsx"
      provides: "type-to-confirm permanent-delete AlertDialog"
      contains: "AlertDialog"
  key_links:
    - from: "src/hooks/useLifecycle.ts"
      to: "api.forge.listLifecycleCommands"
      via: "useQuery"
      pattern: "listLifecycleCommands"
    - from: "src/components/skills/DeleteSkillDialog.tsx"
      to: "confirm gate"
      via: "trimmed exact case-sensitive match"
      pattern: "=== skillName"
---

<objective>
Build the reusable UI building blocks for the lifecycle control surface: install the one new shadcn primitive (`dropdown-menu`), add a `useLifecycle` hook (a sibling of `useIntake`, keyed by skill name so a row can find its in-flight command and render the reused status badge), and the two self-contained dialogs — a small workspace-picker Move dialog and the GitHub-style type-to-confirm permanent-delete AlertDialog. These are independent, individually testable components with no cross-wiring; Plan 98-04 assembles them into the ⋯ menu.

Purpose: The Browser/Client tier's leaf components. Splitting them from the menu assembly keeps each within peak-quality context budget and lets them be unit-tested in isolation (DeleteSkillDialog's confirm gate and MoveToProjectDialog's picker are the two pieces with real logic).
Output: One primitive + one hook + two dialogs, all test-covered.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/98-skill-lifecycle-mutations-archive-restore-move-delete/98-CONTEXT.md
@.planning/phases/98-skill-lifecycle-mutations-archive-restore-move-delete/98-UI-SPEC.md
@.planning/phases/98-skill-lifecycle-mutations-archive-restore-move-delete/98-PATTERNS.md

<interfaces>
<!-- Extracted from live codepulse code this session. Executor uses these directly. -->

useIntake.ts (src/hooks/useIntake.ts) — the shape to clone into useLifecycle.ts:
  type IntakeRowStatus = "pending"|"queued"|"executing"|"done"|"failed"|"expired"   (IMPORT this type; do not redefine)
  mapIntakeStatus(raw): IntakeRowStatus                                             (equivalent mapLifecycleStatus)
  adaptIntakeCommand(doc) → row ; useIntakeCommandsRaw() wraps useQuery in useMemo (MANDATORY — unmemoized .map() causes "Maximum update depth exceeded" against live Convex)
  EMPTY_INTAKE_ROWS module constant (stable identity — mirror as EMPTY_LIFECYCLE_ROWS)
  NOTE: do NOT reuse useIntakeCommands() — its query is hardcoded to commandType "intake" and its row has no skillName. New sibling hook + new query (api.forge.listLifecycleCommands, from Plan 98-01) required.

lifecyclePayload on the Convex doc (from Plan 98-01 schema): { action, skillName, sourceOrigin, destination, workspaceId }
  → LifecycleCommandRow must carry skillName (mandatory — lifecycle acts on an existing named skill) and action, so SkillRow can look up "is there an in-flight command for me".

IntakeModal.tsx workspace picker (src/components/skills/IntakeModal.tsx ~379-419) — reuse verbatim, NO class filter (Pitfall 2):
  workspaces = useQuery(api.forge.listWorkspaces, hostId ? { hostId } : "skip")
  <Select value={workspaceId} onValueChange={setWorkspaceId} disabled={workspaces.length===0}> ... </Select>
  empty state copy: "No workspaces synced from this host yet."

ForgeStopConfirmDialog.tsx (src/components/forge/ForgeStopConfirmDialog.tsx) — AlertDialog structure to copy; DeleteSkillDialog adds an Input type-to-confirm gate on top.

RowStatusBadge (src/components/skills/IntakeStatusBadge.tsx ~66-122) — reuse VERBATIM for lifecycle badges (pending/queued/executing/failed/expired). Do not fork.

enqueueLifecycle mutation (from Plan 98-01): api.forge.enqueueLifecycle, args { hostId, commandId, action, skillName, sourceOrigin, destination, workspaceId }.
</interfaces>

<copywriting>
<!-- VERBATIM from 98-UI-SPEC.md Copywriting Contract — do not improvise -->
Move dialog title: `Move "{name}" to Project`
Move dialog body: `Choose a synced workspace. The skill moves immediately once you confirm.`
Move confirm button: `Move skill`  · Move cancel button: `Cancel Move`
Move empty-state: `No workspaces synced from this host yet.`
Delete title: `Delete "{name}" permanently?`
Delete body: `This removes the file from disk. There is no undo and no git history for .claude/ — type the skill name to confirm.`
Delete input placeholder: `Type "{name}" to confirm`
Delete confirm button: `Delete Permanently` (disabled until exact case-sensitive match; --status-error/destructive variant)
Delete cancel button: `Cancel` (AlertDialogCancel default — house precedent per ForgeStopConfirmDialog)
</copywriting>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install dropdown-menu primitive + useLifecycle hook</name>
  <read_first>
    - src/hooks/useIntake.ts (full — the exact shape to clone: types, adapter, useMemo discipline, EMPTY constant)
    - src/components/ui/select.tsx (radix-ui meta-package import convention, in case the shadcn CLI is unavailable)
    - src/hooks/useIntake.test.ts (if present — mirror its test conventions) ; otherwise the intake adapter tests
    - .planning/phases/98-.../98-PATTERNS.md (useLifecycle section ~367-404)
  </read_first>
  <behavior>
    - mapLifecycleStatus passes "queued"/"executing"/"done"/"failed"/"expired" through unchanged; unknown raw → "queued" (defensive, never throws)
    - adaptLifecycleCommand maps a Convex doc → LifecycleCommandRow carrying commandId, status, skillName, action, error, createdAt, expiresAt
    - useLifecycleCommandsRaw returns undefined while loading, mapped rows once loaded (useMemo-wrapped, stable identity)
    - a lookup helper resolves the newest in-flight row for a given skillName
  </behavior>
  <action>
    Install the primitive: run `npx shadcn add dropdown-menu` from the codepulse repo root (writes src/components/ui/dropdown-menu.tsx from the official shadcn registry). If the CLI is unavailable, hand-author the file mirroring select.tsx's `import { DropdownMenu as DropdownMenuPrimitive } from "radix-ui"` convention (the radix-ui v1.4.3 meta-package is already installed — no npm install). Verify the file uses the `radix-ui` meta-package import, not a per-primitive `@radix-ui/react-dropdown-menu` install.

    Create src/hooks/useLifecycle.ts as a sibling of useIntake.ts. IMPORT `IntakeRowStatus` from useIntake (do not redefine the enum). Define `LifecycleAction = "archive"|"restore"|"move"|"delete"` and `LifecycleCommandRow` carrying commandId, status (IntakeRowStatus), skillName, action, destination, error, createdAt, expiresAt. Add `mapLifecycleStatus` (same switch as mapIntakeStatus), `adaptLifecycleCommand(doc)` reading `doc.lifecyclePayload`, `useLifecycleCommandsRaw()` calling `useQuery(api.forge.listLifecycleCommands, {})` wrapped in useMemo (the wrap is mandatory — an unmemoized .map() loops "Maximum update depth exceeded" against live Convex), `EMPTY_LIFECYCLE_ROWS` module constant, `useLifecycleCommands()` returning `?? EMPTY_LIFECYCLE_ROWS`, and a `latestLifecycleForSkill(rows, skillName)` helper returning the newest matching row or null. Create src/hooks/useLifecycle.test.ts (Wave 0) covering mapLifecycleStatus, adaptLifecycleCommand, and the skill-name lookup.
  </action>
  <verify>
    <automated>cd C:/Users/mandr/codepulse && npx vitest run src/hooks/useLifecycle.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - src/components/ui/dropdown-menu.tsx exists and contains `radix-ui` (meta-package import, not `@radix-ui/react-dropdown-menu`)
    - src/hooks/useLifecycle.ts exports `useLifecycleCommands`, `LifecycleCommandRow`, and a per-skill lookup helper; imports `IntakeRowStatus` from useIntake
    - useLifecycleCommandsRaw wraps the query result in `useMemo`
    - `npx vitest run src/hooks/useLifecycle.test.ts` exits 0 and `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>The dropdown-menu primitive is available and useLifecycle mirrors useIntake's status model keyed by skill name, ready for the menu/badge to consume.</done>
</task>

<task type="auto">
  <name>Task 2: MoveToProjectDialog — workspace picker (LIFE-03)</name>
  <read_first>
    - src/components/skills/IntakeModal.tsx (imports ~24-50, workspace Select block ~379-419 — reuse the picker, NO class filter)
    - src/components/ui/dialog.tsx, src/components/ui/select.tsx, src/components/ui/button.tsx
    - .planning/phases/98-.../98-UI-SPEC.md (§4 + Copywriting Contract) and this plan's <copywriting> block
  </read_first>
  <behavior>
    - renders the synced-workspace Select from api.forge.listWorkspaces with no class-based filter (both synced and local-only appear)
    - shows "No workspaces synced from this host yet." when the list is empty and disables confirm
    - on confirm with a selected workspace, calls enqueueLifecycle with action "move", destination "project", the chosen workspaceId, the skill's sourceOrigin, and a fresh client commandId
    - confirm is disabled until a workspace is selected
    - title/body/buttons render the verbatim Copywriting Contract strings
  </behavior>
  <action>
    Create src/components/skills/MoveToProjectDialog.tsx — a small controlled `Dialog` (open/onOpenChange props), NOT a port of IntakeModal (no host picker, no destination toggle, no file/URL XOR). Props: skillName, sourceOrigin, hostId, open, onOpenChange, and an onMoved callback (optional). Reuse IntakeModal's workspace `Select` block verbatim (workspaces from `useQuery(api.forge.listWorkspaces, hostId ? { hostId } : "skip")`, no `class` filter — Pitfall 2), including the empty-state copy. On confirm ("Move skill"), generate a client commandId (ULID/crypto.randomUUID as elsewhere) and call `useMutation(api.forge.enqueueLifecycle)` with action "move", destination "project", workspaceId, skillName, sourceOrigin. Use the verbatim title/body/button copy from the <copywriting> block. "Move to Global" is NOT handled here (it has no sub-target — Plan 98-04 enqueues it directly). Create MoveToProjectDialog.test.tsx (Wave 0) covering the picker render, empty-state, disabled-confirm, and the enqueue args on confirm (mock the mutation).
  </action>
  <verify>
    <automated>cd C:/Users/mandr/codepulse && npx vitest run src/components/skills/MoveToProjectDialog.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - MoveToProjectDialog.tsx renders a Select fed by `api.forge.listWorkspaces` with NO `.class ===` filter
    - Confirm calls enqueueLifecycle with `action: "move"`, `destination: "project"`, and the selected workspaceId + sourceOrigin
    - Empty-state renders "No workspaces synced from this host yet." and disables confirm
    - Title/body/buttons match the verbatim copy ("Move skill", "Cancel Move")
    - `npx vitest run src/components/skills/MoveToProjectDialog.test.tsx` exits 0
  </acceptance_criteria>
  <done>An operator can pick a synced workspace and fire a move-to-project command from a small dialog with honest empty-state.</done>
</task>

<task type="auto">
  <name>Task 3: DeleteSkillDialog — type-to-confirm permanent delete (LIFE-04/D-06)</name>
  <read_first>
    - src/components/forge/ForgeStopConfirmDialog.tsx (full — AlertDialog structure to copy)
    - src/components/ui/alert-dialog.tsx, src/components/ui/input.tsx, src/components/ui/button.tsx
    - .planning/phases/98-.../98-UI-SPEC.md (§5 + Copywriting Contract) and this plan's <copywriting> block
    - .planning/phases/98-.../98-CONTEXT.md (D-05 cold-only, D-06 type-to-confirm)
  </read_first>
  <behavior>
    - the destructive button is disabled while the input is empty or does not exactly match skillName
    - matching is case-sensitive and trimmed: "  legal  " matches "legal"; "Legal" does NOT match "legal"
    - the input is not pre-filled and there is no autofocus-and-select-all
    - on confirm, calls enqueueLifecycle with action "delete", destination "cold", the skill's sourceOrigin (the dormant origin), and a fresh commandId
    - title/body/placeholder/buttons render the verbatim Copywriting Contract strings
  </behavior>
  <action>
    Create src/components/skills/DeleteSkillDialog.tsx copying ForgeStopConfirmDialog's AlertDialog structure and adding the D-06 type-to-confirm gate: local `confirmText` state, `canDelete = confirmText.trim() === skillName` (case-sensitive), an `Input` with the placeholder copy, and `<AlertDialogAction disabled={!canDelete}>` using the destructive/`--status-error` variant. No pre-fill, no autofocus-select-all. On confirm, generate a client commandId and call `useMutation(api.forge.enqueueLifecycle)` with action "delete", destination "cold", skillName, sourceOrigin. Use the verbatim title/body/placeholder/button copy from the <copywriting> block; AlertDialogCancel keeps its default "Cancel" label (house precedent). This dialog is only ever mounted for cold-storage/dormant rows (Plan 98-04 gates it). Create DeleteSkillDialog.test.tsx (Wave 0) covering: button disabled until exact case-sensitive match, trimming, no case-fold, and the enqueue args on confirm.
  </action>
  <verify>
    <automated>cd C:/Users/mandr/codepulse && npx vitest run src/components/skills/DeleteSkillDialog.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - DeleteSkillDialog.tsx contains `confirmText.trim() === skillName` (case-sensitive gate) driving `disabled` on the destructive action
    - The input is not pre-filled (initial confirmText is empty)
    - Confirm calls enqueueLifecycle with `action: "delete"`, `destination: "cold"`, and the skill's sourceOrigin
    - Title/body/placeholder/button match verbatim copy ("Delete Permanently", "Delete \"{name}\" permanently?")
    - `npx vitest run src/components/skills/DeleteSkillDialog.test.tsx` exits 0
  </acceptance_criteria>
  <done>Permanent deletion requires typing the exact skill name; the destructive button cannot fire on a misclick or stray paste-mismatch.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator input → destructive enqueue | The type-to-confirm gate is the last client-side guard before a permanent-delete command is issued |
| browser → Convex mutation | Dialogs issue enqueueLifecycle; all server-side validation still applies (Plan 98-01) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-98-07 | Tampering (misclick) | DeleteSkillDialog confirm gate | mitigate | Destructive AlertDialogAction disabled until exact case-sensitive trimmed name match; no pre-fill/autofocus-select (D-06) — `.claude/` has no git safety net |
| T-98-05 | Spoofing/Elevation | enqueueLifecycle from dialogs | mitigate | Mutations inherit Plan 98-01's Clerk fail-closed auth; dialogs do not bypass it |
| T-98-SC | Tampering | dropdown-menu install | mitigate | `dropdown-menu` is generated from the already-installed, already-vetted first-party `radix-ui` v1.4.3 meta-package (RESEARCH Package Legitimacy Audit: nothing new to install); verify the generated file imports `radix-ui`, not a new per-primitive package |
</threat_model>

<verification>
- `npx vitest run src/hooks/useLifecycle.test.ts src/components/skills/MoveToProjectDialog.test.tsx src/components/skills/DeleteSkillDialog.test.tsx` green
- `npx tsc --noEmit` clean
- Full codepulse suite `npm test` green at wave merge
</verification>

<success_criteria>
- The dropdown-menu primitive is installed from the first-party registry with no new npm dependency
- useLifecycle mirrors the intake status model keyed by skill name (LIFE-06 badge substrate)
- The Move dialog offers the synced-workspace picker (LIFE-03) and the Delete dialog enforces type-to-confirm (LIFE-04/D-06)
</success_criteria>

<output>
Create `.planning/phases/98-skill-lifecycle-mutations-archive-restore-move-delete/98-03-SUMMARY.md` when done
</output>
