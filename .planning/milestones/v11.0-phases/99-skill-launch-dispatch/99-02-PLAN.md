---
phase: 99-skill-launch-dispatch
plan: 02
type: execute
wave: 2
depends_on: [99-01]
files_modified:
  - src/pages/Chat.tsx
  - src/pages/Chat.test.tsx
autonomous: true
requirements: [LAUNCH-01, LAUNCH-03]
must_haves:
  truths:
    - "D-05: a Run→Chat handoff results in an ACTUAL executed chat.send — the composer is NEVER left prefilled-and-waiting"
    - "D-06: the send happens on the Chat page so the user watches the turn stream live"
    - "D-12: recordSkillLaunch fires exactly once, on confirmed send, for both the Chat and Ástríðr targets"
    - "The auto-send effect fires at most once per navigation even under React StrictMode double-mount"
    - "A handoff that arrives while the WS is not connected surfaces an honest toast, never a silent drop (Pitfall 3, house honest-state rule)"
  artifacts:
    - path: "src/pages/Chat.tsx"
      provides: "mount-triggered auto-send effect reading router-state AutoSendHandoff"
      contains: "autoSend"
    - path: "src/pages/Chat.test.tsx"
      provides: "unit coverage for the auto-send-on-mount effect"
      exports: []
  key_links:
    - from: "src/pages/Chat.tsx"
      to: "useAstridrChat().sendMessage"
      via: "auto-send effect on mount when handoff present + connected"
      pattern: "sendMessage\\("
    - from: "src/pages/Chat.tsx"
      to: "api.registry.recordSkillLaunch"
      via: "useMutation, called after send resolves"
      pattern: "recordSkillLaunch"
---

<objective>
Build the receiving half of the Chat launch (LAUNCH-01 and the Ástríðr variant of LAUNCH-03): when the Skills page navigates to `/chat` with an `AutoSendHandoff` in router state, Chat.tsx fires the real `chat.send` on mount (with the persona `profile` if present), then records the launch — instead of leaving a prefilled composer.

Purpose: `useAstridrChat()` is instantiated ONLY inside `Chat.tsx` (verified), so the popovers cannot send; the actual send must live here. This is where D-05's "always executed, never waiting" guarantee is enforced.
Output: a StrictMode-safe, connection-guarded auto-send effect + `recordSkillLaunch` on confirmed execution, with a new `Chat.test.tsx`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/99-skill-launch-dispatch/99-CONTEXT.md
@.planning/phases/99-skill-launch-dispatch/99-RESEARCH.md
@.planning/phases/99-skill-launch-dispatch/99-PATTERNS.md

<interfaces>
<!-- Consumed from Plan 99-01 (do not redefine) -->
From src/lib/skillRun.ts:
```typescript
export interface AutoSendHandoff { text: string; skillName: string; profile?: ProfileId; }
```
Router-state shape read here: `useLocation().state?.autoSend as AutoSendHandoff | undefined`.

From src/hooks/useAstridrChat.ts (Plan 99-01):
```typescript
sendMessage(text: string, opts?: { ...; profile?: string }): Promise<void>
// guards internally: if (!text.trim() || isStreamingRef.current || status !== "connected") return;
```
recordSkillLaunch: `useMutation(api.registry.recordSkillLaunch)` — args `{ name: string }`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Mount-triggered auto-send effect in Chat.tsx</name>
  <files>src/pages/Chat.tsx</files>
  <read_first>
    - src/pages/Chat.tsx (lines 1-40 imports; lines ~82 `const chat = useAstridrChat()`; the `submit()` guard-then-send at ~261-266; the composer disabled state `disabled={!draft.trim() || isStreaming || disconnected}` ~536, and `const disconnected = status !== "connected"` ~254)
    - src/contexts/AstridrWSContext.tsx (lines 363-375) — the established StrictMode-safe guarded-effect pattern (mount→cleanup→remount fires twice; guard with a ref)
    - .planning/phases/99-skill-launch-dispatch/99-PATTERNS.md § "src/pages/Chat.tsx (MODIFY — new mount-triggered auto-send effect)" and § "No Analog Found" (first row)
    - .planning/phases/99-skill-launch-dispatch/99-RESEARCH.md Pitfalls 1 & 3 (router-state handoff, connection-not-ready silent no-op)
  </read_first>
  <action>
    Import `useLocation` from `react-router-dom` (confirm not already imported), `toast` from `sonner` (add — same import used in SkillLifecycleMenu.tsx:19), `useMutation` from `convex/react` + `api` from `../../convex/_generated/api`, and the `AutoSendHandoff` type from `@/lib/skillRun`.
    Read the handoff once: `const location = useLocation(); const handoff = location.state?.autoSend as AutoSendHandoff | undefined;`.
    Add a `firedRef = useRef(false)` one-shot guard (StrictMode double-mount safe, per AstridrWSContext precedent). In a `useEffect` keyed on `[handoff, chat.status]`:
      - return early if `firedRef.current` is true or `handoff` is absent;
      - if `chat.status !== "connected"`, do NOT set the ref and do NOT toast yet (wait for the status to flip via the dep) — but if it is still not connected after the WS has settled to a terminal non-connecting state (`"disconnected"`/error), set `firedRef.current = true` and `toast.error("Couldn't send — Ástríðr isn't connected. Try again.")` so the launch is never silently dropped (Pitfall 3);
      - when connected: set `firedRef.current = true`, then `await chat.sendMessage(handoff.text, handoff.profile ? { profile: handoff.profile } : undefined)`.
    Do NOT prefill `draft`/the composer with the handoff text — the send is programmatic; leaving text in the composer is exactly the D-05-rejected dead end. After firing, clear the router state so a manual refresh does not re-send: `navigate(location.pathname, { replace: true, state: {} })` (import `useNavigate` if not present). Keep the existing composer `submit()` path untouched.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Source: Chat.tsx reads `location.state?.autoSend` and never assigns the handoff text into `draft`/`setDraft` (grep confirms no `setDraft(handoff` / `setDraft(...text)`).
    - Source: the effect is guarded by a `useRef` one-shot (`firedRef`) so it cannot fire twice under StrictMode.
    - Source: the non-connected branch calls `toast.error(...)` (no silent early-return that drops the launch).
    - Source: after firing, router state is cleared via `navigate(..., { replace: true, state: {} })`.
    - `npx tsc --noEmit` clean.
  </acceptance_criteria>
  <done>Chat.tsx auto-sends the handoff on mount when connected, toasts honestly when not, never prefills the composer, and cannot double-fire.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: recordSkillLaunch on confirmed send + Chat.test.tsx</name>
  <files>src/pages/Chat.tsx, src/pages/Chat.test.tsx</files>
  <read_first>
    - src/pages/Chat.tsx (the effect added in Task 1)
    - convex/registry.ts (lines 678-691) — `recordSkillLaunch` mutation signature (`{ name: string }`)
    - src/hooks/useAstridrChat.test.ts — the render harness + how the WS context / status is mocked; reuse its mocking approach so Chat.test.tsx can drive `status` and a spy `sendMessage`
    - src/test/setup.ts — global mocks (Clerk, Three.js, Tone.js, etc.) already in place for jsdom
  </read_first>
  <behavior>
    - Test 1: mounting Chat with router state `{ autoSend: { text: "/x 1", skillName: "x" } }` and a connected WS calls `sendMessage("/x 1", undefined)` once, then `recordSkillLaunch({ name: "x" })` once.
    - Test 2: with `profile: "business"` in the handoff, `sendMessage` is called with `{ profile: "business" }`.
    - Test 3: mounting with NO autoSend state calls neither `sendMessage` nor `recordSkillLaunch`.
    - Test 4: StrictMode double-mount fires `sendMessage` exactly once (not twice).
    - Test 5: mounting while the WS reports a settled non-connected state calls `toast.error` and does NOT call `sendMessage`.
  </behavior>
  <action>
    After `await chat.sendMessage(...)` resolves without the internal guard bailing, call `recordSkillLaunch({ name: handoff.skillName })` — this is D-12's "confirmed execution only" recording point for BOTH the Chat and Ástríðr targets (both navigate here). Add `const recordSkillLaunch = useMutation(api.registry.recordSkillLaunch)`.
    Create `src/pages/Chat.test.tsx` covering all five `<behavior>` cases. Mock `useAstridrChat` (spy `sendMessage`, controllable `status`), `useMutation`/`recordSkillLaunch` (spy), and `sonner`'s `toast`. Render inside a `MemoryRouter` with an `initialEntries` entry carrying the `autoSend` state (react-router v7). Use `React.StrictMode` for Test 4.
    Note the honest-state constraint (D-14a): there is NO assertion that "a persona voice answered" — Test 2 asserts only that the `profile` field was forwarded.
  </action>
  <verify>
    <automated>npx vitest run src/pages/Chat.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Behavior (tests): all five `<behavior>` cases pass.
    - Source: `recordSkillLaunch({ name: handoff.skillName })` is called only after `sendMessage` resolves (not before navigating, not on mount unconditionally).
    - No test asserts persona-voice behavior — only `profile` forwarding (D-09/D-14a honesty).
    - `npx vitest run src/pages/Chat.test.tsx` green.
  </acceptance_criteria>
  <done>The Chat receiver records exactly one launch per confirmed send for both Chat and Ástríðr targets; Chat.test.tsx proves single-fire, profile-forwarding, no-op-without-handoff, and honest disconnect handling.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Router state → chat.send `message` | The handoff text becomes an executed agent turn |
| Chat client → astridr WS | `profile` scopes SecurityContext.profile_id server-side |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-99-04 | Tampering / EoP | Chat.tsx auto-send `message` | accept | The handoff text is constructed client-side from the user's OWN skill selection + the arg-step the user confirmed (Plan 03); it lands in the same `message` field as any typed chat text and passes astridr's `security_pipeline.process_inbound` upstream (RESEARCH). No new bypass. |
| T-99-05 | Spoofing | `profile` in handoff | mitigate | `profile` is typed `ProfileId` (constrained union from Plan 03's picker); astridr treats unknown values defensively (`default_profile_id`). No privilege escalation. |
| T-99-06 | Repudiation / DoS | double-fire under StrictMode | mitigate | `firedRef` one-shot guard + router-state clear ensure at most one send per navigation; a refresh cannot replay the launch. |
</threat_model>

<verification>
- `npx tsc --noEmit` clean.
- `npx vitest run src/pages/Chat.test.tsx` green.
- Manual (deferred to live UAT — jsdom cannot exercise a real WS): Run→Send to Chat from Skills navigates to /chat and the turn streams; a fresh app-open navigation (WS still reconnecting) shows the honest toast instead of a silent no-op.
</verification>

<success_criteria>
- A Run→Chat/Ástríðr handoff always produces an executed `chat.send` (never a waiting composer) and exactly one `recordSkillLaunch`.
- The effect is StrictMode-safe and honest when disconnected.
</success_criteria>

<output>
Create `.planning/phases/99-skill-launch-dispatch/99-02-SUMMARY.md` when done.
</output>
