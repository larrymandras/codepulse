---
phase: 99-skill-launch-dispatch
plan: 06
type: execute
wave: 5
depends_on: [99-04, 99-05]
files_modified:
  - src/pages/Skills.tsx
  - src/components/skills/SkillRow.tsx
  - src/components/skills/SkillRow.test.tsx
  - src/components/skills/SkillCommandPalette.tsx
  - src/components/skills/SkillCommandPalette.test.tsx
  - src/components/skills/ColdStorageView.tsx
  - src/components/skills/ColdStorageView.test.tsx
  - src/components/skills/AllSkillsOverview.tsx
  - src/components/skills/AllSkillsOverview.test.tsx
  - src/components/skills/SkillsInCategory.tsx
  - src/components/skills/SkillsInCategory.test.tsx
  - src/components/skills/__tests__/SkillsInCategory.test.tsx
autonomous: true
requirements: [LAUNCH-04]
must_haves:
  truths:
    - "D-12: every real launch (Chat/Ástríðr via Chat.tsx, Forge via the provider) records useCount/lastUsedAt — and ONLY real launches do"
    - "D-13: clipboard copy no longer counts as a launch anywhere — the recordSkillLaunch-on-copy calls are removed at every leaf (SkillRow, SkillCommandPalette; QuickDeck done in Plan 05)"
    - "The passive `/chat?skill=` open-in-chat dead-end (a confirmed no-op) is fully retired — no component navigates to it and no recordSkillLaunch fires for it (Pitfall 1/2)"
    - "D-02: the SkillCommandPalette keeps its Enter-to-copy behavior and gains NO Run item; only its broken open-in-chat + copy-recording side effects are removed"
    - "Skills.tsx wraps the skills UI in SkillLaunchProvider so Run works on every ⋯ menu and QuickDeck tile across the page"
  artifacts:
    - path: "src/pages/Skills.tsx"
      provides: "SkillLaunchProvider wrapper; retired handleOpenInChat/handleRecordUse + prop passes"
      contains: "SkillLaunchProvider"
    - path: "src/components/skills/SkillRow.tsx"
      provides: "no inline chat button, no copy-recording, no onRecordUse/onOpenInChat props"
      contains: "SkillLifecycleMenu"
  key_links:
    - from: "src/pages/Skills.tsx"
      to: "SkillLaunchProvider"
      via: "wraps QuickDeck + category/overview/cold-storage views"
      pattern: "SkillLaunchProvider"
    - from: "src/components/skills/SkillRow.tsx"
      to: "recordSkillLaunch"
      via: "REMOVED — copy no longer records"
      pattern: "onRecordUse"
---

<objective>
Final integration + honest-recording cleanup (LAUNCH-04, D-12/D-13). Mount `SkillLaunchProvider` at the Skills page so every Run entry point works, and retire the two dead paths across the shared row/container/palette components: (1) copy no longer records a launch anywhere, and (2) the passive `/chat?skill=` open-in-chat affordance — a confirmed no-op — is removed entirely. After this plan, `useCount` reflects real runs only.

**Locked resolutions (planner decisions, per the two flagged ambiguities):**
- **Ambiguity 1 — SkillCommandPalette recording:** The palette STOPS recording on copy (D-13 consistency) AND its broken open-in-chat is retired. D-02's "left as-is" is honored for the palette's genuinely-working behavior (Enter-to-copy stays; no Run item is added), but is narrowed for the provably-broken `/chat?skill=` open-in-chat sub-behavior, which RESEARCH confirmed is a silent no-op that must not keep recording a fake launch.
- **Ambiguity 2 — handleOpenInChat call sites:** `handleOpenInChat` and the `onOpenInChat` prop are DELETED everywhere (QuickDeck done in Plan 05; SkillRow, SkillCommandPalette, ColdStorageView, AllSkillsOverview, SkillsInCategory here). The real Chat launch is reached only via the Run chooser (with the D-04 arg step).

Output: `Skills.tsx` wrapped + de-wired; the 5 shared skills components + their tests de-propped.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/99-skill-launch-dispatch/99-CONTEXT.md
@.planning/phases/99-skill-launch-dispatch/99-RESEARCH.md
@.planning/phases/99-skill-launch-dispatch/99-PATTERNS.md

<interfaces>
<!-- Consumed from Plans 99-04/99-05 -->
From src/components/skills/SkillLaunchProvider.tsx: `SkillLaunchProvider`.
QuickDeck (Plan 05) no longer accepts `onUse`/`onOpenInChat`. SkillLifecycleMenu (Plan 05) reaches Run internally via context — SkillRow passes it NO new props.

Confirmed D-13 leaf call sites to remove (grep-verified):
- src/components/skills/SkillRow.tsx:82  `onRecordUse(skill.name)` (copy) ; :130 `onClick={() => onOpenInChat(skill.name)}` (inline chat button)
- src/components/skills/SkillCommandPalette.tsx:100 `onRecordUse(skill.name)` ; :104 `onOpenInChat(skill.name)`
- Prop-drilled from Skills.tsx to: QuickDeck (done Plan 05), ColdStorageView, AllSkillsOverview, SkillsInCategory, SkillCommandPalette (all `onRecordUse`/`onOpenInChat`)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: SkillRow + SkillCommandPalette — remove copy-recording + retire open-in-chat</name>
  <files>src/components/skills/SkillRow.tsx, src/components/skills/SkillRow.test.tsx, src/components/skills/SkillCommandPalette.tsx, src/components/skills/SkillCommandPalette.test.tsx</files>
  <read_first>
    - src/components/skills/SkillRow.tsx (props 16-17, 51-52; the copy handler calling `onRecordUse(skill.name)` at 82; the inline chat button at ~130 calling `onOpenInChat`; note SkillRow already renders SkillLifecycleMenu which now carries Run internally)
    - src/components/skills/SkillCommandPalette.tsx (props 33-34, 48-49; `onRecordUse(skill.name)` at 100; `onOpenInChat(skill.name)` at 104 and the key binding that calls it)
    - src/components/skills/SkillRow.test.tsx (33-34, 45, 55, 70) and SkillCommandPalette.test.tsx (41-53, 95-150) — the assertions that copy records + that open-in-chat fires; these invert/remove
    - .planning/phases/99-skill-launch-dispatch/99-RESEARCH.md Pitfall 2 + Open Question 1
  </read_first>
  <action>
    **SkillRow.tsx:** delete the `onRecordUse` and `onOpenInChat` props from the interface + signature; in the copy handler remove the `onRecordUse(skill.name)` line (copy just copies — keep any transient copied-state feedback); remove the inline chat button (~130) that called `onOpenInChat`. Run remains reachable via the already-rendered `SkillLifecycleMenu` (needs no new prop). **SkillCommandPalette.tsx:** delete the `onRecordUse` and `onOpenInChat` props; remove the `onRecordUse(skill.name)` copy-recording call (100); remove the open-in-chat action (104) and its key binding (Ctrl+Enter) — keep Enter-to-copy intact (D-02: copy behavior preserved). Do NOT add a Run item to the palette (D-02). Update both test files: remove the `onRecordUse`/`onOpenInChat` mock props and their call assertions; add a positive assertion that copy still copies (clipboard mock) and a negative assertion that copy does NOT record (`onRecordUse` no longer exists / no record spy fires).
  </action>
  <verify>
    <automated>npx vitest run src/components/skills/SkillRow.test.tsx src/components/skills/SkillCommandPalette.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Source: neither SkillRow.tsx nor SkillCommandPalette.tsx references `onRecordUse` or `onOpenInChat` (grep returns zero in both files).
    - Source: SkillCommandPalette retains its Enter-to-copy path (grep for the clipboard write) and has NO Run/target-chooser code (D-02).
    - Source: SkillRow renders `SkillLifecycleMenu` (Run reachable) and has no inline chat button.
    - Behavior (tests): copy copies but records nothing in both components; the open-in-chat assertions are gone.
  </acceptance_criteria>
  <done>SkillRow + palette copy no longer record; the broken open-in-chat is retired; palette keeps copy and gains no Run; tests updated and green.</done>
</task>

<task type="auto">
  <name>Task 2: Container pass-throughs — drop onRecordUse/onOpenInChat</name>
  <files>src/components/skills/ColdStorageView.tsx, src/components/skills/ColdStorageView.test.tsx, src/components/skills/AllSkillsOverview.tsx, src/components/skills/AllSkillsOverview.test.tsx, src/components/skills/SkillsInCategory.tsx, src/components/skills/SkillsInCategory.test.tsx, src/components/skills/__tests__/SkillsInCategory.test.tsx</files>
  <read_first>
    - src/components/skills/ColdStorageView.tsx (11-12, 26-27, 57-58), AllSkillsOverview.tsx (20-21, 33-34, 82-83), SkillsInCategory.tsx (21-22, 36-37, 126-127) — each declares `onRecordUse`/`onOpenInChat` only to forward them to SkillRow
    - the matching test files (incl. the second copy at src/components/skills/__tests__/SkillsInCategory.test.tsx:57-58, 90-95) — mock props + the onOpenInChat assertion to remove
  </read_first>
  <action>
    In each of ColdStorageView, AllSkillsOverview, SkillsInCategory: remove `onRecordUse` and `onOpenInChat` from the props interface, the destructured signature, and the `<SkillRow …>` forwarding (SkillRow no longer accepts them, Task 1). No other behavior changes — these are pure forwarders. Update all four test files (note SkillsInCategory has TWO test files) to drop the removed mock props and delete the `onOpenInChat`-fired assertion (e.g. `__tests__/SkillsInCategory.test.tsx:90-95`).
  </action>
  <verify>
    <automated>npx vitest run src/components/skills/ColdStorageView.test.tsx src/components/skills/AllSkillsOverview.test.tsx src/components/skills/SkillsInCategory.test.tsx src/components/skills/__tests__/SkillsInCategory.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Source: none of ColdStorageView.tsx / AllSkillsOverview.tsx / SkillsInCategory.tsx reference `onRecordUse` or `onOpenInChat` (grep zero across all three).
    - Behavior (tests): all four test files pass without the removed props; the open-in-chat assertion is gone.
  </acceptance_criteria>
  <done>The three container components no longer thread the retired props; their tests pass.</done>
</task>

<task type="auto">
  <name>Task 3: Skills.tsx — mount SkillLaunchProvider + retire dead handlers/prop passes</name>
  <files>src/pages/Skills.tsx</files>
  <read_first>
    - src/pages/Skills.tsx (132-139 `handleRecordUse`/`handleOpenInChat`; the `recordLaunch = useMutation(api.registry.recordSkillLaunch)` binding; 270-275 QuickDeck; 371-411 ColdStorageView/AllSkillsOverview/SkillsInCategory; 418-425 SkillCommandPalette — all pass `onUse`/`onRecordUse`/`onOpenInChat`)
    - src/components/skills/SkillLaunchProvider.tsx (Plan 99-04) — `SkillLaunchProvider`
    - src/main.tsx — the app provider-stack convention (where to nest a page-scoped provider)
  </read_first>
  <action>
    Delete `handleOpenInChat` and `handleRecordUse` (132-139) and, if now unused, the `recordLaunch`/`useMutation(api.registry.recordSkillLaunch)` binding (recording now happens in Chat.tsx and SkillLaunchProvider, not here — grep to confirm no remaining reference before removing). Remove the `onUse=`/`onRecordUse=`/`onOpenInChat=` props from `<QuickDeck>`, `<ColdStorageView>`, `<AllSkillsOverview>`, `<SkillsInCategory>`, and `<SkillCommandPalette>` (their signatures dropped these in Plan 05 + Tasks 1-2). Wrap the skills UI subtree — at minimum everything that renders QuickDeck and the row-bearing views (and the SkillCommandPalette) — in `<SkillLaunchProvider>…</SkillLaunchProvider>` so every ⋯ menu and QuickDeck tile resolves `useSkillLaunch()` at runtime (the provider also mounts the single page-level ForgeLaunchModal). Leave all lifecycle/category/intake wiring otherwise unchanged.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run</automated>
  </verify>
  <acceptance_criteria>
    - Source: Skills.tsx renders `<SkillLaunchProvider>` around the QuickDeck + row views; `handleOpenInChat`/`handleRecordUse` are gone; no `onUse=`/`onRecordUse=`/`onOpenInChat=` prop passes remain (grep zero).
    - Source: no dangling `recordLaunch` reference (removed if unused).
    - `npx tsc --noEmit` is clean repo-wide (all removed-prop callers reconciled).
    - `npx vitest run` (full suite) is green — zero regressions across the ~204 test files.
  </acceptance_criteria>
  <done>Skills.tsx mounts the launch provider, records nothing directly, and passes no retired props; repo-wide tsc + full test suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Skills page → launch surfaces | Provider scopes the Forge modal + recording to the page subtree |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-99-15 | Repudiation (metric integrity) | copy-recording removal | mitigate | Removing every copy-triggered `recordSkillLaunch` (SkillRow, palette; QuickDeck in Plan 05) makes `useCount`/`lastUsedAt` reflect real launches only (D-13/LAUNCH-04); a broken no-op navigate can no longer log a fake launch (Pitfall 2). |
| T-99-16 | Tampering (dead path) | `/chat?skill=` retirement | mitigate | The confirmed no-op open-in-chat is fully removed rather than re-pointed, eliminating a misleading affordance that looked like it launched but did nothing (RESEARCH drift finding). |
</threat_model>

<verification>
- `npx tsc --noEmit` clean repo-wide.
- `npx vitest run` (full suite) green.
- Manual (live UAT, deferred): from the Skills page, Run via the ⋯ menu and via a QuickDeck tile each open the chooser; Chat/Ástríðr stream a real turn; Forge opens the prefilled modal; copying a chip does NOT bump `useCount`.
</verification>

<success_criteria>
- `useCount` records real launches only; copy records nothing anywhere; the passive open-in-chat dead-end is gone.
- The Skills page is wrapped in SkillLaunchProvider so Run works on every surface; full suite + tsc green.
</success_criteria>

<output>
Create `.planning/phases/99-skill-launch-dispatch/99-06-SUMMARY.md` when done.
</output>
