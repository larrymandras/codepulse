---
phase: 97-skill-lifecycle-management
plan: 03
type: execute
wave: 2
depends_on: [97-01, 97-02]
files_modified:
  - C:\Users\mandr\forge\src\emit\command-poller.ts
  - C:\Users\mandr\forge\src\emit\command-poller.test.ts
autonomous: true
requirements: [DAEMON-03, DAEMON-04]
must_haves:
  truths:
    - "After an intake write resolves status 'done', executeIntake fires the rescan-and-sync fire-and-forget, so the Skills page reflects the install without a manual refresh (DAEMON-03, INTAKE-03)"
    - "A validation-only failure (nothing written) never triggers a rescan — the trigger is gated on a successful write outcome (DAEMON-03)"
    - "The rescan trigger never blocks or fails the ack: the serial intake queue drains and the ack fires even if the rescan silently fails (mirrors emitJob's never-delay-the-caller invariant)"
    - "The daemon advertises 'intake' in supportedTypes only when the intake CLI probe passed, so an older daemon is never handed an intake command it cannot run (DAEMON-04, regression guard)"
  artifacts:
    - path: "C:\\Users\\mandr\\forge\\src\\emit\\command-poller.ts"
      provides: "post-successful-write rescan trigger inside executeIntake"
      contains: "rescanAndSync"
    - path: "C:\\Users\\mandr\\forge\\src\\emit\\command-poller.test.ts"
      provides: "rescan-trigger gating test + DAEMON-04 supportedTypes regression guard"
  key_links:
    - from: "command-poller executeIntake"
      to: "skill-rescan rescanAndSync"
      via: "fire-and-forget void call on status 'done'"
      pattern: "void rescanAndSync"
    - from: "probeIntakeCli"
      to: "supportedTypes intake"
      via: "capability gate"
      pattern: "intake"
---

<objective>
Wire Plan 02's rescan module into the command poller so a successful install auto-syncs the registry (DAEMON-03), and lock the already-shipped capability negotiation (DAEMON-04) behind a regression guard so it can't silently regress.

Purpose: `executeIntake` in `command-poller.ts` already has the structural hook point (97-PATTERNS.md § command-poller.ts insertion point). This plan connects the two Wave-1 forge deliverables (the real-write result from Plan 01 and the rescan function from Plan 02) at that seam, and pins DAEMON-04's probe-gated `supportedTypes` so the "old daemon never claims intake" guarantee survives future edits.

Output: the DAEMON-03 trigger + a DAEMON-04 regression test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/97-skill-lifecycle-management/97-PATTERNS.md
@.planning/phases/97-skill-lifecycle-management/97-RESEARCH.md

<interfaces>
<!-- Wave-1 outputs this plan composes. -->

From Plan 01 — C:\Users\mandr\forge\src\process\intake-exec.ts:
  mapExitCodeToResult now resolves status 'done' for exit 0 AND 8/9 (skill on disk), 'failed' for 1-7. Rescan must fire on status 'done' only.

From Plan 02 — C:\Users\mandr\forge\src\emit\skill-rescan.ts:
  export rescanAndSync(deps) — builds the snapshot and POSTs to /scan; fire-and-forget; never throws.

Insertion point — C:\Users\mandr\forge\src\emit\command-poller.ts executeIntake (~L287-319):
  after `const result = await this.intakeFn(...)` and status assignment, BEFORE the unconditional ack. Serial queue: enqueueIntake/drainIntakeQueue (~L255-274) must keep draining one-at-a-time — do NOT await the rescan's retry backoff.

DAEMON-04 (already shipped) — C:\Users\mandr\forge\src\index.ts + src\emit\intake-probe.ts:
  probeIntakeCli() gates `supportedTypes: intakeAvailable ? ['launch','stop','intake'] : ['launch','stop']`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fire rescan after a successful intake write</name>
  <files>C:\Users\mandr\forge\src\emit\command-poller.ts, C:\Users\mandr\forge\src\emit\command-poller.test.ts</files>
  <read_first>
    - C:\Users\mandr\forge\src\emit\command-poller.ts (executeIntake ~L287-319, enqueueIntake/drainIntakeQueue ~L255-274)
    - C:\Users\mandr\forge\src\emit\skill-rescan.ts (rescanAndSync — from Plan 02)
    - 97-PATTERNS.md § command-poller.ts (insertion point + serial-queue constraint) + 97-RESEARCH.md Pitfall 4
  </read_first>
  <behavior>
    - intakeFn resolves status 'done' → rescanAndSync is invoked exactly once
    - intakeFn resolves status 'failed' → rescanAndSync is NOT invoked (validation-only failure wrote nothing)
    - exit-8/9 done-with-warning (status 'done') → rescanAndSync IS invoked (skill is on disk)
    - rescanAndSync rejecting/hanging → the ack still fires (rescan is not awaited on the ack path)
  </behavior>
  <action>
    At the insertion point in executeIntake, after the intake result's status is known, call the Plan 02 rescan as a fire-and-forget `void rescanAndSync(...)` ONLY when status === 'done'. Do not await it inside drainIntakeQueue's `await this.executeIntake(cmd)` — the serial intake queue must keep draining and the ack must fire unconditionally even if the rescan silently fails, mirroring codepulse-emitter.ts's never-delay-the-caller invariant. Inject rescanAndSync (or its config) through the poller's existing config seam so the test can spy on it without real network/filesystem I/O. Do not gate the rescan on verdict — status 'done' already means the write committed (including the 8/9 loud-not-rolled-back cases where the skill is genuinely on disk).
  </action>
  <verify>
    <automated>cd /c/Users/mandr/forge && npx vitest run src/emit/command-poller.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Test asserts rescan spy called once on status 'done', zero times on status 'failed'.
    - Test asserts the ack POST still fires when the rescan spy rejects (rescan not awaited on the ack path).
    - Source assertion: `grep -n "void rescanAndSync\|rescanAndSync(" src/emit/command-poller.ts` matches inside executeIntake.
  </acceptance_criteria>
  <done>A successful install triggers exactly one non-blocking registry rescan; a failed validation never does; the ack is never gated on the rescan.</done>
</task>

<task type="auto">
  <name>Task 2: DAEMON-04 supportedTypes regression guard</name>
  <files>C:\Users\mandr\forge\src\emit\command-poller.test.ts</files>
  <read_first>
    - C:\Users\mandr\forge\src\emit\intake-probe.ts (probeIntakeCli)
    - C:\Users\mandr\forge\src\index.ts (supportedTypes gate: intakeAvailable ? [...,'intake'] : [...])
    - C:\Users\mandr\forge\src\emit\command-poller.test.ts (existing describe blocks to extend)
    - 97-RESEARCH.md § DAEMON-04 (already shipped) + 97-VALIDATION.md (DAEMON-04 = regression guard only)
  </read_first>
  <action>
    Add a regression guard that pins DAEMON-04's already-shipped behavior: when the intake CLI probe is unavailable (intakeFn undefined / probe failed), 'intake' is absent from the advertised supportedTypes and the poller never claims/executes an intake command; when available, 'intake' is present. Test at the most unit-testable seam — assert probeIntakeCli's gate result drives the supportedTypes array, and that a poller constructed without intakeFn throws/ignores an intake command rather than running it (executeIntake's existing `if (!this.intakeFn) throw` path). Do not refactor the shipped wiring; this task only adds coverage so the capability negotiation cannot silently regress in Phase 98 when new command types are added to the same array.
  </action>
  <verify>
    <automated>cd /c/Users/mandr/forge && npx vitest run src/emit/command-poller.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Test asserts supportedTypes excludes 'intake' when the probe/intakeFn is unavailable and includes it when available.
    - Test asserts an intake command handed to a poller without intakeFn does not perform a write (hits the existing not-supported guard).
    - Mutation check: removing 'intake' from the available-branch supportedTypes fails the guard.
  </acceptance_criteria>
  <done>DAEMON-04's capability negotiation is regression-pinned; an old-daemon build is provably never handed an intake command it cannot run.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Convex claim → daemon | command-type dispatch: a daemon must only claim types it can execute |
| daemon write → registry sync | a rescan side-effect must never corrupt the ack/terminal state |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-97-08 | Denial of Service | serial intake queue | mitigate | Rescan is fire-and-forget (`void`), never awaited inside drainIntakeQueue — a slow/failing rescan cannot stall the queue or delay the ack. |
| T-97-09 | Repudiation | ack integrity | mitigate | The ack fires unconditionally regardless of rescan outcome, so command terminal state is always recorded (tested). |
| T-97-04 | Elevation of privilege | capability negotiation | mitigate | supportedTypes gates on probeIntakeCli; a daemon that cannot run intake never advertises it (DAEMON-04 regression guard). |
| T-97-SC | Tampering | package installs | accept | No installs this phase. |
</threat_model>

<verification>
- `cd /c/Users/mandr/forge && npx vitest run src/emit/command-poller.test.ts` green.
- Full forge suite green before wave merge: `cd /c/Users/mandr/forge && npm test`.
</verification>

<success_criteria>
- Successful installs auto-rescan; failed validations do not; the ack is never blocked by the rescan.
- DAEMON-04 capability negotiation is regression-guarded.
</success_criteria>

<output>
Create `.planning/phases/97-skill-lifecycle-management/97-03-SUMMARY.md` when done.
</output>
