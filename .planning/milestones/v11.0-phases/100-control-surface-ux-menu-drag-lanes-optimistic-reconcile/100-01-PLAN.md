---
phase: 100-control-surface-ux-menu-drag-lanes-optimistic-reconcile
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/skills.ts
  - src/lib/skills.test.ts
  - src/components/skills/SkillLifecycleMenu.tsx
autonomous: true
requirements: [UX-01, UX-02]
must_haves:
  truths:
    - "The ⋯ menu and the drag path derive their allowed actions from ONE shared function — they cannot diverge (D-02)"
    - "resolveLifecycleActions returns identical scope-state for every existing SkillLifecycleMenu fixture (activeGlobal/activeProject/dormant/multiScope/shadowedMerged)"
    - "resolveScopeDrop encodes the full drag matrix (including multi-scope reject) as a pure, testable decision"
    - "The drag matrix can never produce a delete action; there is no trash/delete drop target anywhere in the scope rail (D-04)"
    - "The refactored SkillLifecycleMenu still passes its existing test suite unchanged (behavior-preserving)"
  artifacts:
    - path: "src/lib/skills.ts"
      provides: "resolveLifecycleActions + resolveScopeDrop pure helpers"
      contains: "export function resolveLifecycleActions"
    - path: "src/lib/skills.test.ts"
      provides: "5-fixture parity tests + drag-matrix cell tests"
      contains: "resolveScopeDrop"
    - path: "src/components/skills/SkillLifecycleMenu.tsx"
      provides: "menu consuming the shared predicate instead of its inline block"
      contains: "resolveLifecycleActions"
  key_links:
    - from: "src/components/skills/SkillLifecycleMenu.tsx"
      to: "src/lib/skills.ts"
      via: "resolveLifecycleActions import"
      pattern: "resolveLifecycleActions"
---

<objective>
Extract the per-row scope predicate that currently lives inline inside `SkillLifecycleMenu.tsx:141-148` into a shared pure function `resolveLifecycleActions()` in `src/lib/skills.ts`, and add a second pure helper `resolveScopeDrop()` that encodes the complete drag matrix. This is the foundation that makes D-02's "the drag matrix mirrors the ⋯ menu exactly" a STRUCTURAL guarantee (one function, two callers) rather than a convention two code paths must remember to keep in sync.

Purpose: The menu is the source of truth for allowed lifecycle actions (D-02, D-06). Both the existing ⋯ menu and the new scope-rail drop handler (Plan 04) must read the same predicate. Without this extraction the drop handler would duplicate the menu's logic and the two copies could drift.
Output: `resolveLifecycleActions` + `resolveScopeDrop` in `src/lib/skills.ts`, exhaustive unit tests, and a behavior-preserving refactor of `SkillLifecycleMenu` to consume `resolveLifecycleActions`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/100-control-surface-ux-menu-drag-lanes-optimistic-reconcile/100-CONTEXT.md
@.planning/phases/100-control-surface-ux-menu-drag-lanes-optimistic-reconcile/100-RESEARCH.md
@.planning/phases/100-control-surface-ux-menu-drag-lanes-optimistic-reconcile/100-PATTERNS.md

<interfaces>
<!-- Extracted this session. The executor uses these directly — no codebase exploration needed. -->

From src/lib/skills.ts (existing helper style to match — pure, no React/Convex):
- export const DORMANT_ORIGIN = "claude-code:available";
- const PROJECT_PREFIX = "claude-code:project:";
- export type SkillLike = { name; displayName?; origins?: string[]; source?; ... };
- export function isDormant(skill: SkillLike): boolean
- export function isShadowing(skill: SkillLike): boolean
- export function hasDormantCopy(skill: SkillLike): boolean

From src/components/skills/SkillLifecycleMenu.tsx:141-148 (the EXACT inline block to extract — verified this session):
  const dormant = isDormant(skill) || lane === "cold";
  const shadowed = isShadowing(skill);
  const nonDormantOrigins = (skill.origins ?? []).filter((o) => o !== DORMANT_ORIGIN);
  const multiScope = nonDormantOrigins.length > 1;
  const activeOrigin = nonDormantOrigins.length === 1 ? nonDormantOrigins[0] : undefined;
  const moveDestinationIsProject = activeOrigin === "claude-code";

The 5 canonical fixtures already in SkillLifecycleMenu.test.tsx: activeGlobal, activeProject, dormant, multiScope, shadowedMerged.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add resolveLifecycleActions + resolveScopeDrop to src/lib/skills.ts with parity + matrix tests</name>
  <files>src/lib/skills.ts, src/lib/skills.test.ts</files>
  <read_first>
    - src/lib/skills.ts (the file being modified — match the existing pure-helper style of isDormant/isShadowing/hasDormantCopy)
    - src/lib/skills.test.ts (the test file to extend — match its describe/expect conventions)
    - src/components/skills/SkillLifecycleMenu.tsx:130-190 (extraction source — the inline predicate at :141-148 and the enqueue() action shapes at :150-185)
    - src/components/skills/SkillLifecycleMenu.test.tsx (the 5 canonical fixtures: activeGlobal/activeProject/dormant/multiScope/shadowedMerged — replicate these fixtures in skills.test.ts)
    - .planning/phases/100-control-surface-ux-menu-drag-lanes-optimistic-reconcile/100-RESEARCH.md § "The Drag Matrix — resolved cell-by-cell" (the literal spec for resolveScopeDrop)
  </read_first>
  <behavior>
    resolveLifecycleActions(skill, lane="active") returns { dormant, shadowed, multiScope, activeOrigin?, moveDestinationIsProject } computed EXACTLY as the inline block: dormant = isDormant(skill) || lane === "cold"; shadowed = isShadowing(skill); nonDormantOrigins = origins minus DORMANT_ORIGIN; multiScope = nonDormantOrigins.length > 1; activeOrigin = the single non-dormant origin or undefined; moveDestinationIsProject = activeOrigin === "claude-code".
    - Parity: for each of the 5 fixtures, output equals the values the menu computes today (activeGlobal → activeOrigin "claude-code", multiScope false; activeProject → activeOrigin "claude-code:project:<hash>"; dormant → dormant true, activeOrigin undefined; multiScope → multiScope true, activeOrigin undefined; shadowedMerged → shadowed true).
    resolveScopeDrop(skill, targetScope) returns a discriminated result: { kind: "noop" } | { kind: "reject", hint } | { kind: "dialog" } | { kind: "enqueue", action, sourceOrigin, destination }. Matrix cells:
    - active-global (activeOrigin === "claude-code"): →global = noop; →project = dialog; →cold = enqueue{archive, sourceOrigin:"claude-code", destination:"cold"}.
    - active-project (activeOrigin startsWith "claude-code:project:"): →global = enqueue{move, sourceOrigin:activeOrigin, destination:"global"}; →project = noop; →cold = enqueue{archive, sourceOrigin:activeOrigin, destination:"cold"}.
    - dormant && !shadowed: →global = enqueue{restore, sourceOrigin:DORMANT_ORIGIN, destination:"global"}; →project = reject{hint:"Restore to Global first, then move."}; →cold = noop.
    - dormant && shadowed: →global = reject (shadow-blocked); →project = reject; →cold = noop.
    - multiScope: →global/→project/→cold all = reject{hint:"Active in multiple scopes — disambiguation ships in a later release."}.
    - Delete is NEVER a possible result (D-04 — drag never deletes).
  </behavior>
  <action>Add `resolveLifecycleActions(skill: SkillLike, lane: "active" | "cold" = "active"): LifecycleActionState` and `resolveScopeDrop(skill: SkillLike, targetScope: "global" | "project" | "cold"): ScopeDropResult` to `src/lib/skills.ts`, alongside `isDormant`/`isShadowing`/`hasDormantCopy`, keeping them pure (no React, no Convex import). Export the `LifecycleActionState` and `ScopeDropResult` types. `resolveLifecycleActions` must reproduce the inline block at `SkillLifecycleMenu.tsx:141-148` byte-for-behavior. `resolveScopeDrop` calls `resolveLifecycleActions` internally and returns the discriminated result per the matrix in the behavior block — active-project is detected via `activeOrigin?.startsWith("claude-code:project:")`, active-global via `activeOrigin === "claude-code"`. Extend `src/lib/skills.test.ts` with a `resolveLifecycleActions` describe block asserting parity against all 5 fixtures replicated from `SkillLifecycleMenu.test.tsx`, and a `resolveScopeDrop` describe block asserting every matrix cell above (including all three multi-scope reject cells and the cold→project reject hint string). Do NOT change any existing export signature in skills.ts.</action>
  <verify>
    <automated>npx vitest run src/lib/skills.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/lib/skills.test.ts` passes with the new describe blocks present.
    - `grep -n "export function resolveLifecycleActions" src/lib/skills.ts` returns a match.
    - `grep -n "export function resolveScopeDrop" src/lib/skills.ts` returns a match.
    - Test file asserts the exact reject hint `"Restore to Global first, then move."` for the dormant→project cell.
    - Test file asserts multi-scope rows reject for ALL three target scopes.
    - `npx tsc --noEmit` reports no new errors.
  </acceptance_criteria>
  <done>Both helpers exist, are pure, are exported with their result types, and are covered by parity + full-matrix unit tests that pass.</done>
</task>

<task type="auto">
  <name>Task 2: Refactor SkillLifecycleMenu to consume resolveLifecycleActions (behavior-preserving)</name>
  <files>src/components/skills/SkillLifecycleMenu.tsx</files>
  <read_first>
    - src/components/skills/SkillLifecycleMenu.tsx (the file being modified — the inline block at :141-148 and every downstream reference to dormant/shadowed/multiScope/activeOrigin/moveDestinationIsProject)
    - src/components/skills/SkillLifecycleMenu.test.tsx (the contract that must stay green — do not modify assertions)
    - src/lib/skills.ts (the newly-added resolveLifecycleActions from Task 1)
  </read_first>
  <action>In `SkillLifecycleMenu.tsx`, replace the inline computation at lines 141-148 with a single call: destructure `{ dormant, shadowed, multiScope, activeOrigin, moveDestinationIsProject }` from `resolveLifecycleActions(skill, lane)` imported from `@/lib/skills`. Delete the now-dead local `nonDormantOrigins`/`isDormant`/`isShadowing` inline usage that only fed this block (keep any other independent uses intact). Leave `enqueue`, `handleArchive`, `handleRestore`, `handleMoveToGlobal`, the multi-scope disabled branch, and all JSX untouched — this is a pure substitution of the derivation source. Do NOT alter behavior; the existing test suite is the contract.</action>
  <verify>
    <automated>npx vitest run src/components/skills/SkillLifecycleMenu.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/components/skills/SkillLifecycleMenu.test.tsx` passes with ZERO changes to the test file's assertions.
    - `grep -n "resolveLifecycleActions" src/components/skills/SkillLifecycleMenu.tsx` returns a match.
    - The inline `const nonDormantOrigins = (skill.origins ?? []).filter` block no longer appears in SkillLifecycleMenu.tsx (`grep -c "nonDormantOrigins" src/components/skills/SkillLifecycleMenu.tsx` returns 0, unless a genuinely separate later use exists — if so, document it in the SUMMARY).
    - `npx tsc --noEmit` reports no new errors.
  </acceptance_criteria>
  <done>SkillLifecycleMenu derives its scope-state from the shared helper; its existing test suite is green and unmodified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client drag/menu UI → `enqueueLifecycle` mutation | The client predicate decides what the UI OFFERS; it is never the enforcement layer |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-100-01 | Elevation of Privilege | `resolveLifecycleActions`/`resolveScopeDrop` (client predicate) | accept (mitigated server-side) | This predicate is UX-only. `validateLifecyclePreflight` (`convex/forge.ts`) re-checks every rule server-side regardless of what the client offers; a client that fires `enqueueLifecycle` with any payload is still rejected by the same LAYER-1 checks that gate the menu. This plan adds NO new server call site. |
| T-100-SC | Tampering | package installs | n/a | This plan installs zero packages (pure TS + tests). No supply-chain surface. |
</threat_model>

<verification>
- `npx vitest run src/lib/skills.test.ts src/components/skills/SkillLifecycleMenu.test.tsx` — both green.
- `npx tsc --noEmit` — clean.
</verification>

<success_criteria>
- A single shared `resolveLifecycleActions` is the source of truth for the ⋯ menu's scope-state.
- `resolveScopeDrop` encodes the complete drag matrix (Plan 03/04 consume it) including the multi-scope reject case that D-02's prose omitted.
- The menu refactor is behavior-preserving (existing suite unchanged and green).
</success_criteria>

<output>
Create `.planning/phases/100-control-surface-ux-menu-drag-lanes-optimistic-reconcile/100-01-SUMMARY.md` when done
</output>
