---
phase: 100-control-surface-ux-menu-drag-lanes-optimistic-reconcile
plan: 05
type: execute
wave: 2
depends_on: ["100-01", "100-02"]
files_modified:
  - src/components/skills/SkillRow.tsx
  - src/components/skills/SkillRow.test.tsx
  - src/components/skills/AllSkillsOverview.test.tsx
  - src/components/skills/SkillsInCategory.test.tsx
  - src/components/skills/ColdStorageView.test.tsx
autonomous: true
requirements: [UX-01, UX-03, UX-04]
must_haves:
  truths:
    - "A row with a pending move renders an unmistakable 'pending, unconfirmed' treatment (70% opacity + pulsing --status-info bar) — never confusable with the dormant (50%) or active/selected (--primary) states (D-05)"
    - "SkillRow.onDragStart records the dragged skill (setDraggingSkill) so the scope rail can compute validity; onDragEnd clears it"
    - "The scope-gated ⋯ menu renders identically across AllSkillsOverview, SkillsInCategory, ColdStorageView, reconciled with QuickDeck (UX-01, D-06)"
    - "No /manage-skills terminal-instruction string survives anywhere in the Cold Storage restore path (UX-04)"
  artifacts:
    - path: "src/components/skills/SkillRow.tsx"
      provides: "pending overlay via context + dragging-skill reporting on dragStart/dragEnd"
      contains: "usePendingMove"
    - path: "src/components/skills/ColdStorageView.test.tsx"
      provides: "preserved no-/manage-skills assertion (do not weaken)"
      contains: "manage-skills"
  key_links:
    - from: "src/components/skills/SkillRow.tsx"
      to: "src/hooks/usePendingLifecycleMoves.ts"
      via: "usePendingMove + useDraggingSkill context readers"
      pattern: "usePendingMove|useDraggingSkill"
---

<objective>
Give `SkillRow` its optimistic-pending visual treatment (D-05) and wire the transient dragging-skill identity, then AUDIT (D-06) that the scope-gated ⋯ menu is present and correct on every row surface (UX-01) and that in-app Cold Storage restore has no `/manage-skills` terminal dead-end (UX-04). The pending overlay reads the row's entry from `usePendingMove(skill.name)` via context (Plan 02) — no prop-drilling through the intermediary list components. `SkillRow.onDragStart` additionally records the dragged skill through `useDraggingSkill().setDraggingSkill` so `ScopeRail` (Plan 03) can compute per-entry validity during a native drag.

Purpose: UX-03's visible-but-honest pending state, plus UX-01/UX-04 which are completeness/consistency audits of already-shipped 98/99 work, not net-new construction (D-06). The pending row must be visually distinct from both dormant (opacity-50) and active/selected (--primary) so it can never read as a false success.
Output: `SkillRow.tsx` pending overlay + drag reporting; a `SkillRow` pending test; and audit-confirming tests across the row surfaces (extend only if a real gap is found).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/100-control-surface-ux-menu-drag-lanes-optimistic-reconcile/100-CONTEXT.md
@.planning/phases/100-control-surface-ux-menu-drag-lanes-optimistic-reconcile/100-PATTERNS.md
@.planning/phases/100-control-surface-ux-menu-drag-lanes-optimistic-reconcile/100-UI-SPEC.md

<interfaces>
<!-- Verified this session. -->

From src/hooks/usePendingLifecycleMoves.ts (Plan 02):
- export function usePendingMove(skillName: string): PendingMove | undefined
- export function useDraggingSkill(): { draggingSkill; setDraggingSkill }
- export type PendingMove = { commandId; action; destination }

Existing SkillRow overlay/drag pattern (SkillRow.tsx — verified):
- :24-31 optional pass-through props (hostId, lane) — new pending state does NOT need a new required prop (read from context)
- :82-86 onDragStart sets e.dataTransfer.setData("text/plain", skill.name) + effectAllowed="move"  ← ADD setDraggingSkill(skill) here; add onDragEnd={() => setDraggingSkill(null)}
- :87-89 row wrapper: `... ${dormant ? "opacity-50" : ""}`  ← pending adds opacity-70 (a DIFFERENT value, so pending ≠ dormant)
- :100-104 dormant badge span — the visual precedent for an additive, non-conflicting indicator

UI-SPEC pending treatment (§ Interaction States → 2):
- opacity 70% (NOT the dormant 50%)
- pulsing left-edge bar: bg-[var(--status-info)] shadow-[var(--glow-sm)] animate-pulse  (info color, never --primary)
- the existing RowStatusBadge chip (rendered by SkillLifecycleMenu) already shows Queued…/Executing… — reused, not restyled

UX-04 existing assertion to KEEP GREEN (do not weaken): ColdStorageView.test.tsx:61-64 asserts no "/manage-skills" string.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SkillRow pending overlay (context) + dragging-skill reporting + test</name>
  <files>src/components/skills/SkillRow.tsx, src/components/skills/SkillRow.test.tsx</files>
  <read_first>
    - src/components/skills/SkillRow.tsx (the file being modified — the drag payload at :82-86, the row-wrapper className at :87-89, the dormant badge at :100-104, and the optional-prop discipline at :24-31)
    - src/components/skills/SkillRow.test.tsx (the test file to extend — match its render + mock conventions)
    - src/hooks/usePendingLifecycleMoves.ts (Plan 02 — usePendingMove, useDraggingSkill)
    - .planning/phases/100-control-surface-ux-menu-drag-lanes-optimistic-reconcile/100-UI-SPEC.md § "2. Optimistic pending row" (exact opacity/bar/color contract + the visual-hierarchy rule that primary out-ranks info)
  </read_first>
  <behavior>
    SkillRow reads pending = usePendingMove(skill.name). When pending is defined: the row wrapper gets opacity-70 (NOT the dormant opacity-50) and renders a pulsing left-edge indicator bar (bg-[var(--status-info)] shadow-[var(--glow-sm)] animate-pulse). When pending is undefined: no pending treatment, existing appearance unchanged. Pending and dormant use different opacities so they are never identical; the pending bar uses --status-info, never --primary (so "pending, unconfirmed" is never confusable with "active/selected"). onDragStart continues to set dataTransfer text/plain = skill.name AND now also calls useDraggingSkill().setDraggingSkill(skill); a new onDragEnd handler calls setDraggingSkill(null). No new REQUIRED prop is added (pending is read from context so existing call sites keep compiling).
    Test cases: (1) with usePendingMove mocked to return a PendingMove, the row wrapper has opacity-70 and a pulsing --status-info bar element is present; (2) with usePendingMove mocked to undefined, neither the opacity-70 class nor the info bar is present; (3) a dormant row (opacity-50) with NO pending does not get opacity-70 (states are distinct); (4) firing dragStart calls setDraggingSkill with the skill and dragEnd calls it with null.
  </behavior>
  <action>In `src/components/skills/SkillRow.tsx`, read `const pending = usePendingMove(skill.name)` and `const { setDraggingSkill } = useDraggingSkill()` from `@/hooks/usePendingLifecycleMoves`. Add `pending ? "opacity-70" : ""` to the row wrapper className (alongside, not replacing, the existing `dormant ? "opacity-50" : ""` — dormant and pending are mutually distinct; if both were ever true, pending's 70% is fine as the visible state). When `pending` is defined, render an absolutely-positioned pulsing left-edge bar using `bg-[var(--status-info)] shadow-[var(--glow-sm)] animate-pulse` (mirror CategoryGrid's active glow-bar geometry but in the info token). In `onDragStart`, after setting dataTransfer, call `setDraggingSkill(skill)`; add `onDragEnd={() => setDraggingSkill(null)}`. Use only CSS tokens, never hex. Do NOT restyle the existing `RowStatusBadge` chip. Extend `src/components/skills/SkillRow.test.tsx` with the 4 cases, mocking `@/hooks/usePendingLifecycleMoves` (usePendingMove + useDraggingSkill).</action>
  <verify>
    <automated>npx vitest run src/components/skills/SkillRow.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/components/skills/SkillRow.test.tsx` passes all 4 cases.
    - `grep -n "opacity-70" src/components/skills/SkillRow.tsx` and `grep -n "var(--status-info)" src/components/skills/SkillRow.tsx` return matches.
    - `grep -n "setDraggingSkill" src/components/skills/SkillRow.tsx` shows both the onDragStart set and the onDragEnd clear.
    - No hardcoded hex added: `grep -nE "#[0-9a-fA-F]{3,6}" src/components/skills/SkillRow.tsx` returns nothing new vs. baseline.
    - The existing SkillRow test assertions remain green (no weakening).
    - `npx tsc --noEmit` reports no new errors.
  </acceptance_criteria>
  <done>SkillRow shows a distinct, token-driven pending treatment read from context and reports the dragged skill on dragStart/dragEnd; its test suite is green and no existing call site needs a new prop.</done>
</task>

<task type="auto">
  <name>Task 2: UX-01 menu-consistency + UX-04 no-/manage-skills completeness audit across row surfaces</name>
  <files>src/components/skills/AllSkillsOverview.test.tsx, src/components/skills/SkillsInCategory.test.tsx, src/components/skills/ColdStorageView.test.tsx, src/components/skills/ColdStorageView.tsx</files>
  <read_first>
    - src/components/skills/AllSkillsOverview.tsx and AllSkillsOverview.test.tsx
    - src/components/skills/SkillsInCategory.tsx and SkillsInCategory.test.tsx (create the test only if none exists and a gap is found)
    - src/components/skills/ColdStorageView.tsx and ColdStorageView.test.tsx (esp. :61-64 — the no-/manage-skills assertion to preserve)
    - src/components/skills/QuickDeck.tsx (reconcile the ⋯/Run affordance against the row surfaces — Phase 99 D-02)
    - src/components/skills/SkillLifecycleMenu.tsx (the scope-gated menu that must appear consistently on each surface)
    - .planning/phases/100-control-surface-ux-menu-drag-lanes-optimistic-reconcile/100-UI-SPEC.md § "3. ⋯ menu completeness audit"
  </read_first>
  <action>Audit (D-06) that each of `AllSkillsOverview`, `SkillsInCategory`, and `ColdStorageView` renders a `SkillRow` that hosts the scope-gated `SkillLifecycleMenu` (Archive / Restore / Move / Delete / Run gated by scope), and that `QuickDeck` is consistent with them (Run = default gesture per Phase 99 D-02; drag is ADDITIVE, never replacing the menu). If any surface is missing a menu-render test case, add one asserting the ⋯ trigger is present for a representative row on that surface. Re-run and KEEP GREEN `ColdStorageView.test.tsx`'s existing no-`/manage-skills` assertion (:61-64) — do NOT weaken or delete it; if any `/manage-skills` string is found anywhere in the cold-storage restore path, remove it and note it in the SUMMARY. If the audit finds NO gaps (menus already render consistently and no terminal string survives), record that explicitly in the SUMMARY as the audit result — do not invent changes. This task is verification-first: net-new code only if a real gap is found.</action>
  <verify>
    <automated>npx vitest run src/components/skills/AllSkillsOverview.test.tsx src/components/skills/ColdStorageView.test.tsx src/components/skills/SkillLifecycleMenu.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/components/skills/AllSkillsOverview.test.tsx src/components/skills/ColdStorageView.test.tsx src/components/skills/SkillLifecycleMenu.test.tsx` is green.
    - `grep -rn "manage-skills" src/components/skills/` returns matches ONLY inside `ColdStorageView.test.tsx` (the preserved negative assertion) — no `/manage-skills` instruction string in any non-test source file. If a match exists in a source file, it must be removed.
    - Each of AllSkillsOverview / SkillsInCategory / ColdStorageView has at least one test asserting the ⋯ menu (or its trigger) renders for a row — added if previously missing.
    - The SUMMARY states the audit outcome (gaps found + fixed, or no gaps) explicitly.
    - `npx tsc --noEmit` reports no new errors.
  </acceptance_criteria>
  <done>The scope-gated ⋯ menu is verified present and consistent on every row surface; the no-/manage-skills invariant holds and its test stays green; the audit result is recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client optimistic overlay → server truth | The pending overlay is a client-only hint; the row's real membership comes from the reconciled `skills` query after daemon rescan |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-100-02 | Repudiation / false-success | SkillRow pending overlay | mitigate | The pending treatment is visibly UNCONFIRMED (70% opacity + pulsing info bar, never the steady --primary "active" style) and is cleared by the Plan 02 reconcile — it never claims success. Overlay state is context-only ephemeral, never persisted. |
| T-100-SC | Tampering | package installs | n/a | Zero package installs. No supply-chain surface. |
</threat_model>

<verification>
- `npx vitest run src/components/skills/SkillRow.test.tsx src/components/skills/AllSkillsOverview.test.tsx src/components/skills/ColdStorageView.test.tsx src/components/skills/SkillLifecycleMenu.test.tsx` — green.
- `grep -rn "manage-skills" src/components/skills/` — only the preserved test assertion.
- `npx tsc --noEmit` — clean.
</verification>

<success_criteria>
- The pending row treatment is unmistakably distinct from dormant and from active/selected — no false-success reading.
- SkillRow reports the dragged skill so the scope rail can compute validity.
- The ⋯ menu is consistent across every row surface (UX-01) and the Cold Storage restore path has no terminal dead-end (UX-04), both verified.
</success_criteria>

<output>
Create `.planning/phases/100-control-surface-ux-menu-drag-lanes-optimistic-reconcile/100-05-SUMMARY.md` when done
</output>
