---
phase: 07-intelligence-layer
plan: 05
type: execute
wave: 3
depends_on:
  - 07-01
  - 07-03
files_modified:
  - convex/memoryQuality.ts
  - convex/memoryQuality.test.ts
  - src/components/MemoryQualityTab.tsx
  - src/pages/Memory.tsx
autonomous: true
requirements:
  - INT-07

must_haves:
  truths:
    - "Memory page shows deduplication rate, stale count, and contradiction count as StatCards"
    - "Quality tab displays detailed breakdown of stale memories and contradiction pairs"
    - "Memory quality cron evaluates episodicEvents for staleness and deduplication"
    - "Contradiction detection uses LLM via the same primary/backup provider config from Plan 03"
  artifacts:
    - path: "convex/memoryQuality.ts"
      provides: "Memory quality evaluation cron + queries"
      exports: ["evaluateInternal", "getLatestQuality", "computeDeduplicationRate", "identifyStaleMemories"]
    - path: "src/components/MemoryQualityTab.tsx"
      provides: "Quality tab with dedup rate, stale list, contradiction pairs"
      contains: "MemoryQualityTab"
    - path: "src/pages/Memory.tsx"
      provides: "Memory page with Quality tab and quality StatCards"
      contains: "MemoryQualityTab"
  key_links:
    - from: "convex/crons.ts"
      to: "convex/memoryQuality.ts"
      via: "internal.memoryQuality.evaluateInternal"
      pattern: "evaluateInternal"
    - from: "src/components/MemoryQualityTab.tsx"
      to: "convex/memoryQuality.ts"
      via: "useQuery(api.memoryQuality.getLatestQuality)"
      pattern: "api\\.memoryQuality\\.getLatestQuality"
    - from: "convex/memoryQuality.ts"
      to: "convex/briefings.ts"
      via: "internal.briefings.getLLMConfigInternal"
      pattern: "getLLMConfigInternal"
---

<objective>
Implement memory quality metrics: deduplication rate, staleness detection, LLM-based contradiction detection, and the Quality tab on the Memory page.

Purpose: Operators see memory health at a glance (per D-13, D-14, D-15) — dedup rate, stale memory count, and contradiction count with detailed breakdowns in a new Quality tab.
Output: convex/memoryQuality.ts (evaluation cron + queries), MemoryQualityTab component, Memory page integration.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-intelligence-layer/07-CONTEXT.md
@.planning/phases/07-intelligence-layer/07-RESEARCH.md
@.planning/phases/07-intelligence-layer/07-UI-SPEC.md
@.planning/phases/07-intelligence-layer/07-03-SUMMARY.md

<interfaces>
<!-- From convex/schema.ts — episodicEvents table (existing) -->
episodicEvents: defineTable({
  agentId: v.optional(v.string()),
  sessionId: v.optional(v.string()),
  eventType: v.string(),    // "memory_stored" | "memory_recalled" | "memory_pruned"
  data: v.optional(v.any()),
  timestamp: v.float64(),
}).index("by_type", ["eventType", "timestamp"])

<!-- From convex/schema.ts — memoryQuality table (Plan 01) -->
memoryQuality: defineTable({
  evaluatedAt: v.float64(),
  deduplicationRate: v.float64(),     // 0.0 to 1.0
  staleCount: v.float64(),
  contradictionCount: v.float64(),
  staleMemoryIds: v.optional(v.array(v.string())),
  contradictionPairs: v.optional(v.array(v.object({
    memoryA: v.string(),
    memoryB: v.string(),
    reason: v.optional(v.string()),
  }))),
}).index("by_evaluated", ["evaluatedAt"])

<!-- From convex/briefings.ts (Plan 03) — LLM config internal query -->
export const getLLMConfigInternal = internalQuery({
  args: { key: v.string() },
  handler: async (ctx, { key }) => { /* returns { provider, model, apiKey } | null */ },
});

<!-- From src/pages/Memory.tsx — existing tab system -->
type TabId = "timeline" | "tiers" | "reflections";
// Tab navigation: <div className="flex gap-1 border-b border-gray-700/50">
// StatCard component: <div className="bg-gray-800/50 border border-gray-700/50 rounded-xl p-4">

<!-- From 07-UI-SPEC.md — Memory Quality tab spec -->
// New "Quality" tab: Timeline | Tiers | Reflections | Quality
// Stats row: Dedup Rate (%) | Stale Memories (count) | Contradictions (count)
// Accordion sections: "Duplicate Flags", "Stale Memories", "Contradictions"
// EntityRow pattern for each item: leading icon, memory summary, age/flag date
// Empty: "No issues detected" in text-xs text-muted-foreground
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Memory quality evaluation backend — dedup rate, staleness, contradiction detection</name>
  <files>convex/memoryQuality.ts, convex/memoryQuality.test.ts</files>
  <read_first>
    - convex/schema.ts (episodicEvents table — eventType values, data field structure, timestamp)
    - convex/memoryQuality.test.ts (existing test.todo stubs to fill)
    - convex/briefings.ts (getLLMConfigInternal query — for LLM contradiction detection)
    - convex/alertRulesConfig.ts (agentConfigs read pattern for staleness_days config)
  </read_first>
  <behavior>
    - computeDeduplicationRate(total=100, pruned=15) returns 0.15
    - computeDeduplicationRate(total=0, pruned=0) returns 0
    - identifyStaleMemories(events, thresholdDays=30, now) returns IDs of memories with most recent access > 30 days ago
    - identifyStaleMemories with all recent memories returns empty array
  </behavior>
  <action>
Create `convex/memoryQuality.ts` with:

**Pure helper functions (exported for testing):**

```typescript
export function computeDeduplicationRate(totalStored: number, prunedCount: number): number {
  if (totalStored === 0) return 0;
  return prunedCount / totalStored;
}

export function identifyStaleMemories(
  events: Array<{ eventType: string; data?: any; timestamp: number }>,
  thresholdDays: number,
  nowEpoch: number
): string[] {
  // Build a map: memoryId -> most recent access timestamp
  const lastAccess: Record<string, number> = {};
  for (const e of events) {
    const memoryId = e.data?.memoryId || e.data?.id;
    if (!memoryId) continue;
    if (e.eventType === "memory_stored" || e.eventType === "memory_recalled") {
      lastAccess[memoryId] = Math.max(lastAccess[memoryId] ?? 0, e.timestamp);
    }
  }
  
  const cutoff = nowEpoch - thresholdDays * 86400;
  return Object.entries(lastAccess)
    .filter(([, ts]) => ts < cutoff)
    .map(([id]) => id);
}
```

**Convex functions:**

1. `evaluateInternal` — `internalMutation` (cron target at 03:00 UTC).
   - Read staleness threshold from agentConfigs key `"intelligence.staleness_days"` (default 30).
   - Query `episodicEvents` table: all events with `eventType` in `["memory_stored", "memory_recalled", "memory_pruned"]`.
   - Compute dedup rate: count `memory_pruned` events / count `memory_stored` events.
   - Identify stale memories via `identifyStaleMemories`.
   - For contradiction detection: Schedule `ctx.scheduler.runAfter(0, internal.memoryQuality.detectContradictionsAction, { recentMemoryIds: last24hStoredIds.slice(0, 20) })`. Cap at 20 memory IDs per batch per RESEARCH.md Pitfall 5.
   - Store preliminary results (dedup + stale, contradiction count = 0) in `memoryQuality` table. The contradiction action will patch the row.

2. `detectContradictionsAction` — `internalAction` args: `{ recentMemoryIds: v.array(v.string()) }`.
   - Read LLM config via `ctx.runQuery(internal.briefings.getLLMConfigInternal, { key: "intelligence.llm_primary" })`.
   - If no LLM configured, skip contradiction detection (store 0 contradictions).
   - For each pair of recent memory IDs (limit to ~10 pairs from 20 IDs to cap cost), read memory content from `episodicEvents` table.
   - Send batch to LLM with system prompt: "You are a memory consistency checker. Given pairs of memories, identify if any contradict each other. Return JSON: { contradictions: [{ memoryA: string, memoryB: string, reason: string }] }. Return empty array if no contradictions."
   - Store results via `ctx.runMutation(internal.memoryQuality.updateContradictions, { evaluatedAt, contradictionPairs, contradictionCount })`.

3. `updateContradictions` — `internalMutation` that patches the latest `memoryQuality` row with contradiction results.

4. `getLatestQuality` — public `query`. Returns the most recent `memoryQuality` row (ordered by `evaluatedAt` descending, take 1). Also returns the stale memories detail and contradiction pairs for the Quality tab breakdown.

5. `getQualityHistory` — public `query`. Returns last 10 `memoryQuality` rows for trend display (optional, used if Quality tab shows trend).

Fill `convex/memoryQuality.test.ts`: implement tests for `computeDeduplicationRate` and `identifyStaleMemories` pure functions.
  </action>
  <verify>
    <automated>npx vitest run convex/memoryQuality.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - convex/memoryQuality.ts contains `export function computeDeduplicationRate(`
    - convex/memoryQuality.ts contains `export function identifyStaleMemories(`
    - convex/memoryQuality.ts contains `export const evaluateInternal = internalMutation(`
    - convex/memoryQuality.ts contains `export const detectContradictionsAction = internalAction(`
    - convex/memoryQuality.ts contains `export const getLatestQuality = query(`
    - convex/memoryQuality.ts contains `intelligence.staleness_days`
    - convex/memoryQuality.ts contains `getLLMConfigInternal` (uses Plan 03's LLM config)
    - convex/memoryQuality.ts contains `memory_pruned` (dedup rate from pruned events)
    - convex/memoryQuality.test.ts has at least 3 non-todo tests
    - `npx vitest run convex/memoryQuality.test.ts` exits 0
  </acceptance_criteria>
  <done>Memory quality evaluation cron computes dedup rate and staleness from episodicEvents, runs LLM contradiction detection on recent memories (capped at 20 per batch). Pure function tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: MemoryQualityTab component and Memory page integration</name>
  <files>src/components/MemoryQualityTab.tsx, src/pages/Memory.tsx</files>
  <read_first>
    - src/pages/Memory.tsx (full file — tab system: TabId type, activeTab state, tab button rendering, StatCard component, existing stats grid)
    - .planning/phases/07-intelligence-layer/07-UI-SPEC.md (Memory Quality tab spec: stats, accordion sections, empty state, copywriting)
  </read_first>
  <action>
**MemoryQualityTab.tsx:**

```typescript
import { useQuery } from "convex/react";
import { api } from "../../convex/_generated/api";
import { ChevronDown, AlertTriangle, Clock, Copy } from "lucide-react";
import { useState } from "react";
```

- Uses `useQuery(api.memoryQuality.getLatestQuality)` for data.
- Stats row: 3 stat cards (matching existing StatCard pattern from Memory.tsx):
  - "Dedup Rate" — `${(data.deduplicationRate * 100).toFixed(1)}%`
  - "Stale Memories" — `data.staleCount`
  - "Contradictions" — `data.contradictionCount`
  - Stats use `text-2xl font-semibold tabular-nums` value + `text-xs text-muted-foreground uppercase tracking-wide` label (per MetricCard pattern in UI-SPEC).

- Three accordion sections below stats. Each accordion:
  - Header: `<button className="flex items-center justify-between w-full py-3 border-b border-border" onClick={toggle}>`
    - Left: icon (Copy for Duplicate Flags, Clock for Stale Memories, AlertTriangle for Contradictions) + section title in `text-sm font-medium`
    - Right: count badge + ChevronDown with rotate-180 when open
  - Content: list of items using EntityRow-like pattern:
    - **Stale Memories:** Each row: Clock icon (w-4 h-4 text-muted-foreground) + memory ID (truncated, font-mono text-xs) + "Last accessed X days ago" in text-xs text-muted-foreground
    - **Contradiction Pairs:** Each row: AlertTriangle icon + "Memory A vs Memory B" + reason text
    - **Duplicate Flags:** Show dedup rate summary text (no individual items — just the rate stat)
  - Empty state per section: "No issues detected" in `text-xs text-muted-foreground py-4`

- Overall empty state (when no quality data): heading "No quality issues detected" + body "Run memory quality evaluation to check for duplicates, stale entries, and contradictions." in `text-sm text-muted-foreground text-center py-8`.

**Memory.tsx modifications:**

1. Expand TabId type: `type TabId = "timeline" | "tiers" | "reflections" | "quality";`

2. Add "Quality" tab button to the tab navigation row (after Reflections):
   ```
   <button className={`px-4 py-2 text-sm ... ${activeTab === "quality" ? "border-b-2 border-primary text-foreground" : "text-muted-foreground"}`} onClick={() => setActiveTab("quality")}>Quality</button>
   ```

3. Add quality stats to the top stats grid. Import and call `useQuery(api.memoryQuality.getLatestQuality)`. Add 3 more StatCards to the existing grid:
   - "Dedup Rate" showing `${((quality?.deduplicationRate ?? 0) * 100).toFixed(1)}%`
   - "Stale Memories" showing `quality?.staleCount ?? 0`
   - "Contradictions" showing `quality?.contradictionCount ?? 0`

4. Add tab content for "quality": `{activeTab === "quality" && <MemoryQualityTab />}`

5. Import `MemoryQualityTab` from `"../components/MemoryQualityTab"`.

6. Wrap the quality tab content in `<SectionErrorBoundary name="Memory Quality">`.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - src/pages/Memory.tsx contains `"quality"` as a valid TabId value
    - src/pages/Memory.tsx contains `MemoryQualityTab`
    - src/pages/Memory.tsx contains `api.memoryQuality.getLatestQuality`
    - src/pages/Memory.tsx contains `Dedup Rate` or `deduplicationRate`
    - src/pages/Memory.tsx contains `Stale Memories`
    - src/pages/Memory.tsx contains `Contradictions`
    - src/components/MemoryQualityTab.tsx contains `api.memoryQuality.getLatestQuality`
    - src/components/MemoryQualityTab.tsx contains `No quality issues detected`
    - src/components/MemoryQualityTab.tsx contains `Stale Memories` or `staleCount`
    - src/components/MemoryQualityTab.tsx contains `text-2xl font-semibold tabular-nums` (MetricCard pattern)
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Memory page has Quality tab with StatCards for dedup rate, stale count, and contradiction count. Accordion sections show detailed breakdowns. Empty state displays when no quality data exists.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Convex action -> external LLM API | Memory content pairs sent to LLM for contradiction detection |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-11 | Information Disclosure | Memory content sent to LLM for contradiction check | accept | Memory content already flows through LLM during normal Astridr operation. Contradiction check is same trust level. Batch capped at 20 IDs. |
| T-07-12 | Denial of Service | Contradiction detection LLM costs | mitigate | Cap batch size at 20 memory IDs (~10 pairs) per cron run. Only check memories added/updated in last 24h, not full corpus (per RESEARCH.md Pitfall 5). |
</threat_model>

<verification>
- `npx vitest run convex/memoryQuality.test.ts` exits 0
- `npx tsc --noEmit` exits 0
- Memory page shows Quality tab with 3 quality StatCards
- Quality tab accordion sections render with empty state when no data
</verification>

<success_criteria>
Memory page displays dedup rate, stale count, and contradiction count. Quality tab shows detailed breakdowns with accordion sections. Memory quality evaluation cron runs daily with LLM contradiction detection capped at 20 memories per batch.
</success_criteria>

<output>
After completion, create `.planning/phases/07-intelligence-layer/07-05-SUMMARY.md`
</output>
