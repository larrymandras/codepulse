---
phase: 5
slug: data-pipeline
status: draft
shadcn_initialized: true
preset: new-york / neutral / cssVariables / no-prefix
created: 2026-04-14
---

# Phase 5 — UI Design Contract

> Visual and interaction contract for frontend phases. Generated by gsd-ui-researcher, verified by gsd-ui-checker.
>
> **Scope note:** Phase 5 is primarily a backend-only data pipeline phase. The only UI surface is a single retention threshold control added to the existing Settings page, and "Load more" triggers added to all list views. All other design tokens are inherited from Phase 1 (UI Foundation) and are recorded here for executor reference.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | shadcn/ui |
| Preset | new-york, neutral, cssVariables, no-prefix |
| Component library | Radix UI (via shadcn/ui) |
| Icon library | Lucide React (16px / 4x4 sizing per UI-08) |
| Font | Geist Variable (body), JetBrains Mono (code/mono) |
| Radius | `--radius: 0rem` — no rounded corners anywhere |

Source: `components.json`, `src/index.css`

---

## Spacing Scale

Declared values (multiples of 4 — inherited from Phase 1):

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Icon gaps, inline padding |
| sm | 8px | Compact element spacing, input padding |
| md | 16px | Default element spacing, section content padding |
| lg | 24px | Section padding, card internal spacing |
| xl | 32px | Layout gaps between major sections |
| 2xl | 48px | Major section breaks |
| 3xl | 64px | Page-level spacing |

Exceptions: none for this phase.

Source: Phase 1 established 8-point scale. Applied consistently across all existing pages.

---

## Typography

Inherited from Phase 1. All new Phase 5 UI elements use these exact values.

| Role | Size | Weight | Line Height | Notes |
|------|------|--------|-------------|-------|
| Body | 14px | 400 (regular) | 1.5 | All form labels, table content, list rows |
| Label | 12px | 400 (regular) | 1.4 | Input labels, muted metadata, section headers |
| Heading | 16px | 600 (semibold) | 1.3 | Settings section headings, panel titles |
| Display | 20px | 600 (semibold) | 1.2 | Page-level titles only |

Font: `var(--font-geist)` / `'Geist Variable', sans-serif`

Tabular numerals: apply `font-variant-numeric: tabular-nums` to all metric values and numeric inputs. Source: Phase 1 MetricCard pattern.

---

## Color

Phase 1 established a monochromatic oklch palette (dark theme primary). All Phase 5 UI inherits these tokens.

| Role | CSS Token | oklch (dark mode) | Usage |
|------|-----------|-------------------|-------|
| Dominant (60%) | `--background` | `oklch(0.145 0 0)` | Page background, primary surfaces |
| Secondary (30%) | `--card` / `--sidebar` | `oklch(0.205 0 0)` | Settings card surface, sidebar, nav |
| Accent (10%) | `--primary` | `oklch(0.922 0 0)` | "Load more" button, "Update Retention" button, active input ring |
| Destructive | `--destructive` | `oklch(0.704 0.191 22.216)` | No destructive actions in this phase |
| Muted text | `--muted-foreground` | `oklch(0.708 0 0)` | Input helper text, empty state body, "no data yet" labels |
| Status OK | `--status-ok` | `oklch(0.72 0.17 142)` | Aggregate cron last-run success indicator (if displayed) |
| Status Error | `--status-error` | `oklch(0.72 0.20 27)` | Cron failure state indicator (if displayed) |

Accent reserved for: "Load more" button (all paginated list views), "Update Retention" button on retention threshold control. No other elements use the primary accent.

Source: `src/index.css` dark mode variables — Phase 1 established.

---

## Component Inventory

### New UI Elements (Phase 5 only)

#### 1. Retention Threshold Control (Settings page)

A single numeric input with inline save, placed in a new "Data Retention" section on `src/pages/Settings.tsx`.

- **Section header:** uppercase `tracking-wide text-xs text-muted-foreground` with a horizontal separator below — matches existing SectionHeader pattern
- **Label:** "Retention period" at 12px / weight 400 / `text-muted-foreground`
- **Input:** Number input, right-aligned value, `w-20` width, displays integer days (1–365). Uses `--input` border color, `--background` fill, no rounded corners.
- **Suffix:** "days" text label immediately right of input, 12px / weight 400 / muted
- **Save button:** "Update Retention" label, primary accent styling, width auto. Disabled when value is unchanged from stored value. Shows loading spinner (Lucide `Loader2` 16px) while mutation is pending.
- **Helper text:** 14px / `text-muted-foreground` / line-height 1.5 — "Events older than this threshold are automatically archived. Minimum 1 day, maximum 365 days."
- **Validation:** Input clamped to min=1 max=365. If user enters out-of-range value, input border switches to `--destructive` and save button is disabled. No toast — inline error only.
- **Inline error:** 12px / `--status-error` color — "Enter a value between 1 and 365 days."

#### 2. "Load more" Trigger (all paginated list views)

A button rendered at the bottom of every paginated list. Rendered conditionally only when `status === "CanLoadMore"`.

- **Label:** "Load more" (12 items) — shows count of items about to load in parentheses
- **Styling:** `text-sm text-muted-foreground underline-offset-4 hover:underline` — text-link style, no button frame, no border, no background
- **Loading state:** Replace label with Lucide `Loader2` 16px spinning when `status === "LoadingMore"`
- **Exhausted state:** Render nothing — do not show "You've reached the end" or similar copy
- **Position:** Centered horizontally, `mt-4 mb-2` margin, below the last list row

Pages affected: Events, Sessions, Agents, Executions, Alerts, LLM Calls, Security Events.

---

## Interaction States

### Retention Input

| State | Visual |
|-------|--------|
| Default | `--input` border, `--background` fill |
| Focus | `--ring` outline at 50% opacity |
| Invalid | `--destructive` border + inline error text |
| Saving | "Update Retention" button shows `Loader2` spinner, input disabled |
| Saved | "Update Retention" button returns to default; no toast required |

### "Load more" Button

| State | Visual |
|-------|--------|
| Available | Text-link: "Load more (25)" — muted, underline on hover |
| Loading | `Loader2` 16px spinner, centered in place of text |
| Exhausted | Component not rendered |

---

## Copywriting Contract

| Element | Copy |
|---------|------|
| Primary CTA (retention) | "Update Retention" |
| Primary CTA (pagination) | "Load more" |
| Empty state heading | "No data yet" |
| Empty state body | "Events will appear here as Ástríðr runs." |
| Retention helper | "Events older than this threshold are automatically archived. Minimum 1 day, maximum 365 days." |
| Retention input label | "Retention period" |
| Retention input suffix | "days" |
| Settings section title | "Data Retention" |
| Validation error | "Enter a value between 1 and 365 days." |
| Error state (list load fail) | "Failed to load. Refresh to try again." |
| Destructive confirmation | None — no destructive actions in this phase |

Source: D-07 (30-day default), D-08 (infinite scroll / Load more pattern), CONTEXT.md.

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | No new blocks — existing shadcn components only | not required |
| Third-party | None | not applicable |

No new shadcn components are installed in this phase. All UI uses existing hand-rolled Tailwind components and established patterns from Phases 1-4.

Source: RESEARCH.md — "No new dependencies required." Components.json shows `"registries": {}`.

---

## Design Decisions Rationale

| Decision | Rationale |
|----------|-----------|
| Text-link style for "Load more" | Matches Paperclip information-dense UX; avoids visual weight of a bordered button at the bottom of every list |
| Inline validation on retention input (no toast) | Settings page already uses inline error patterns; toast would be disproportionate for a single numeric field |
| No "You've reached the end" exhausted state | Convex `usePaginatedQuery` `Exhausted` status — silently render nothing; clean list termination |
| Retention section in Settings (not a separate page) | D-07 explicitly targets Settings page; single config value does not warrant a new route |
| No visual aggregate status dashboard | Phase 5 is backend-only; cron job status visible in Convex dashboard, not CodePulse UI |
| Label weight collapsed to 400 (from 500) | Typography contract allows only 2 weights. Label role uses muted styling via `text-muted-foreground` — color conveys hierarchy without a third weight |

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
