---
phase: 108-per-profile-engine-telemetry-astridr-backend
plan: 03
type: execute
wave: 2
depends_on: ["108-02"]
repos: [codepulse]
files_modified:
  - codepulse:convex/runtimeIngest.ts
  - codepulse:convex/runtimeIngest.test.ts
autonomous: true
requirements: [TELE-02, ENGINE-01]

must_haves:
  truths:
    - "D-13: a control_verb_swap event routes to the controlVerbSwaps domain table, not only to the generic runtime_events log."
    - "D-14: brain and voice swap events both route through the one ingest case, discriminated by verb, with no event kind silently dropped."
    - "A model_routing event with status failed does not write an activeEngineSnapshots row, so a model that never served a response is never reported as a profile's current engine."
    - "Every field read from an astridr payload coalesces camelCase and snake_case, and an unusable event breaks rather than throwing, so one bad event cannot poison an ingest batch."
    - "The new case routes through internal.controlVerbSwaps.record and never through api."
  artifacts:
    - path: "codepulse:convex/runtimeIngest.ts"
      provides: "case control_verb_swap + failed-status skip in case model_routing"
      contains: "case \"control_verb_swap\""
    - path: "codepulse:convex/runtimeIngest.test.ts"
      provides: "first-ever test coverage of the model_routing and control_verb_swap ingest cases"
  key_links:
    - from: "convex/runtimeIngest.ts"
      to: "convex/controlVerbSwaps.ts"
      via: "ctx.runMutation(internal.controlVerbSwaps.record, ...)"
      pattern: "internal\\.controlVerbSwaps\\.record"
---

<objective>
Route `control_verb_swap` into the `controlVerbSwaps` domain table, and stop `model_routing` events
that report a FAILED resolution from being stored as a profile's current engine.

**Repo: `codepulse` only** (`C:\Users\mandr\codepulse`, branch `master`).

Purpose: TELE-02's "routed to a domain table" half, plus the ENGINE-01 correctness gap research
Item 6 identified — `latestByProfile` returns the newest row per profile with no status filter, so a
stored `status:"failed"` row would report a model that never actually served a response as the
profile's live engine. That is the same fabricated-reading class D-14 exists to prevent, with a
plausible model name instead of a sentinel string.

Output: one new ingest case, one guard line, and the first test coverage either case has ever had.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/108-per-profile-engine-telemetry-astridr-backend/108-CONTEXT.md
@.planning/phases/108-per-profile-engine-telemetry-astridr-backend/108-RESEARCH.md
@.planning/phases/108-per-profile-engine-telemetry-astridr-backend/108-PATTERNS.md
@CLAUDE.md
</context>

<interfaces>
<!-- The existing model_routing case, convex/runtimeIngest.ts:717-748, re-verified 2026-08-07. -->

```typescript
case "model_routing": {
  const d = data as any;
  const routedProfileId = d.profileId ?? d.profile_id;
  const routedModel = d.model;
  if (isUnresolvedRouting({ profileId: routedProfileId, model: routedModel })) {
    break;
  }
  await ctx.runMutation(internal.activeEngine.recordRouting, {
    profileId: routedProfileId,
    model: routedModel,
    mode: d.mode ?? "inherited",
    selectionPath: d.selectionPath ?? d.selection_path,
    expiresAt: d.expiresAt ?? d.expires_at,
    timestamp,
  });
  break;
}
```

Already correct and NOT to be changed: `routedModel = d.model` reads only `d.model` with no
`selectedModel` fallback — that is D-11's REJECTED alternative, correctly already not implemented.
`mode: d.mode ?? "inherited"` also stays as-is; plan 108-01 derives `mode` astridr-side, and this
fallback only guards a payload that omits it entirely.

<!-- Write path created by plan 108-02 -->
`internal.controlVerbSwaps.record({ verb, target?, resolved?, providerAffinity?, voiceId?, path,
reason?, scope?, sessionId?, channel, timestamp })`
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: case "control_verb_swap" + the model_routing failed-status skip</name>
  <repo>codepulse</repo>
  <files>convex/runtimeIngest.ts</files>
  <read_first>
    - `convex/runtimeIngest.ts:717-748` — `case "model_routing"`, **the exact analog**
      (108-PATTERNS.md §6), including its D-14 comment block and the WR-06/168-06 rationale for why
      dual coalescing is load-bearing rather than decoration.
    - `convex/runtimeIngest.ts:749-757` — `case "git_commit"`, a simpler sibling showing the same
      `const d = data as any` → coalesce → `ctx.runMutation` → `break` shape.
    - `convex/runtimeIngest.ts:200-240` — the generic path that lands EVERY runtime event in
      `runtime_events` before the domain switch; adding a case must not remove that generic row.
    - `convex/controlVerbSwaps.ts` — the `record` mutation's exact arg names (created by plan 108-02).
    - `convex/activeEngineFilters.ts` — `isUnresolvedRouting`, and its header explaining why the
      write path and the read path share one predicate.
  </read_first>
  <action>
**(a) Add `case "control_verb_swap":`** to the same switch, placed immediately after
`case "model_routing"` so the two per-profile engine-axis cases sit together.

Body follows the `model_routing` case's shape exactly:
`const d = data as any;` then dual snake/camelCase coalescing for every field —
`d.verb`, `d.target`, `d.resolved`, `d.providerAffinity ?? d.provider_affinity`,
`d.voiceId ?? d.voice_id`, `d.path`, `d.reason`, `d.scope ?? d.profile_id ?? d.profileId`,
`d.sessionId ?? d.session_id`, `d.channel`, `timestamp` — then
`await ctx.runMutation(internal.controlVerbSwaps.record, {...}); break;`.

Two rules from this file's own header comments, both non-negotiable:
- coalesce camelCase AND snake_case on every field — a single unhandled `null` here previously
  poisoned an 8-event production batch (WR-06/168-06);
- an unusable event must `break`, never `throw`.

Guard only on the two fields the schema declares non-optional (`verb`, `path`) plus `channel`: if
any is missing/empty, `break` without inserting. There is deliberately **no** `isUnresolvedRouting`
equivalent for this table — per D-13 a refusal IS a valid row here (that is the whole point: the
refusal path is where the affinity guard and the resolver fail, and a history storing only successes
claims every swap worked). Write that rationale as a comment so a future reader does not "harden" it
into dropping refusals.

Note in a comment that this case does not remove the event's generic `runtime_events` row — this
file's established behaviour is that every runtime event lands in `runtime_events` AND routes to a
domain table when a case exists.

**(b) Add the failed-status skip** inside `case "model_routing"`, as one guard line placed BEFORE the
existing `isUnresolvedRouting` check:

`if (d.status === "failed") break;`

with a comment giving research Item 6's reasoning: `latestByProfile` returns the newest row per
profile with no status filter, so a failed-resolution row would be rendered as the profile's current
engine — a model that never served a response. The guard belongs here rather than astridr-side
because `_emit_model_routing` is one helper shared by four call sites including the failure path, and
branching it there would complicate a function three other paths depend on.

Do NOT change `routedModel = d.model` (no `selectedModel` fallback — D-11's rejected alternative) and
do NOT change `mode: d.mode ?? "inherited"`.
  </action>
  <verify>
    <automated>cd /c/Users/mandr/codepulse && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'case "control_verb_swap"' convex/runtimeIngest.ts` returns `1`.
    - `grep -c "internal\.controlVerbSwaps\.record" convex/runtimeIngest.ts` returns `1` and
      `grep -c "api\.controlVerbSwaps" convex/runtimeIngest.ts` returns `0`.
    - `grep -c 'd.status === "failed"' convex/runtimeIngest.ts` returns `1`, and it appears at a
      lower line number than the `isUnresolvedRouting(` call in the same case.
    - `grep -c "selectedModel" convex/runtimeIngest.ts` returns `0` (the rename stays producer-side).
    - The `control_verb_swap` case body contains no `throw` statement.
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>`control_verb_swap` reaches its domain table with dual-coalesced fields, and a failed resolution never becomes a stored current-engine reading.</done>
</task>

<task type="auto">
  <name>Task 2: first test coverage for the model_routing and control_verb_swap ingest cases</name>
  <repo>codepulse</repo>
  <files>convex/runtimeIngest.test.ts</files>
  <read_first>
    - `convex/runtimeIngest.test.ts` — the whole file. It uses TWO styles: pure-helper unit tests
      (e.g. the `llm_call` goalId/traceId extraction blocks at `:249-292`) and bounded static
      source-check tests via `readFileSync` (e.g. `:330-364`, `:412+`). Pick the style each new test
      needs and follow it; do not invent a third.
    - `convex/activeEngine.test.ts:126-155` — the bounded-window source-guard idiom: locate the case
      via `source.indexOf('case "..."')`, slice a bounded window, and assert on that window ONLY.
      Its own rationale is verbatim reusable: "asserting on the whole file would fail on their
      correct usage, not on this defect."
    - `convex/runtimeIngest.ts` — the two cases as edited in Task 1.
  </read_first>
  <action>
`convex/runtimeIngest.test.ts` exists but a targeted grep for `model_routing|control_verb_swap`
returns **zero hits** — verified 2026-08-07. Neither case has ever had a test. Close that.

Add a `describe("runtimeIngest — model_routing case", ...)` block and a
`describe("runtimeIngest — control_verb_swap case", ...)` block. Every source-check assertion must be
scoped to a bounded window around its own `case "..."` (the `activeEngine.test.ts:126-155` idiom),
never the whole file, and must run against comment-stripped source so a case's own explanatory
comment cannot satisfy or break a grep.

Cases to cover:

`model_routing`:
1. The case body reads `d.model` and does NOT contain the string `selectedModel` — the D-11 rename is
   producer-side and the consumer must not grow a compatibility fallback.
2. The case body contains `d.status === "failed"` and the `break` for it precedes the
   `isUnresolvedRouting(` call.
3. The case body reads `d.profileId ?? d.profile_id` (dual coalescing preserved).

`control_verb_swap`:
4. The case body calls `internal.controlVerbSwaps.record` and does not reference `api.controlVerbSwaps`.
5. The case body coalesces snake_case for every multi-word field: assert the window contains
   `provider_affinity`, `voice_id`, and `session_id`.
6. The case body contains no `throw`.
7. The case body contains no `isUnresolvedRouting` call — a refusal is a valid row here (D-13), and
   this test is what stops a future reader from "hardening" refusals away.

For each source-check test, prove the assertion is not vacuous the way `activeEngine.test.ts` does:
assert the sliced window is non-empty and that `source.indexOf('case "control_verb_swap"')` is
`> -1` before asserting anything about its contents. An `indexOf` miss returning `-1` silently
produces a window that trivially satisfies every negative assertion.
  </action>
  <verify>
    <automated>cd /c/Users/mandr/codepulse && npx vitest run convex/runtimeIngest.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run convex/runtimeIngest.test.ts` exits 0 with `>= 7` new passing tests across the
      two new describe blocks.
    - `grep -c "model_routing" convex/runtimeIngest.test.ts` returns `>= 1` (was 0 before this task).
    - `grep -c "control_verb_swap" convex/runtimeIngest.test.ts` returns `>= 1` (was 0 before).
    - Each source-check test asserts `indexOf(...) > -1` (or equivalent) before asserting on the
      sliced window.
    - Mutation check, performed and its result stated in the SUMMARY: temporarily delete the
      `if (d.status === "failed") break;` line from `convex/runtimeIngest.ts` and confirm test 2 goes
      RED; then restore.
  </acceptance_criteria>
  <done>Both engine-axis ingest cases have regression coverage, and the failed-status guard has been proven to fail when removed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Ástríðr `/runtime-ingest` → Convex domain tables | Bearer-gated httpAction; the payload body is otherwise untrusted, arbitrarily-shaped JSON |
| ingest batch → per-event handler | One event's failure must not affect its siblings |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-108-14 | Denial of Service | ingest batch poisoning | mitigate | The new case follows the file's own "must `break`, never `throw`" rule, guarded by an explicit test (case 6). A single unhandled `null` here previously poisoned an 8-event production batch (WR-06/168-06) — that is the recorded precedent, not a hypothetical. |
| T-108-15 | Tampering | fabricated current-engine reading | mitigate | `if (d.status === "failed") break;` — a resolution that threw never becomes a stored current-engine row, because `latestByProfile` has no status filter and would render it as live. Mutation-checked in Task 2. |
| T-108-03 | Spoofing | `controlVerbSwaps` write path | mitigate | Reached only via `internal.controlVerbSwaps.record`; asserted by test case 4 that no `api.controlVerbSwaps` reference exists in this file. |
| T-108-16 | Information Disclosure | untrusted strings stored verbatim (`target`, `reason`, `channel`) | accept | These are stored as data and rendered by React, which escapes by default — no `dangerouslySetInnerHTML` is introduced by this phase. The values originate from Ástríðr's own control-verb resolver, and the ingest endpoint is bearer-gated, so the untrusted surface is a compromised Ástríðr process, which already has strictly greater capability than injecting a string into a history row. |
| T-108-SC | Tampering | package installs | accept | No packages installed by this plan. |
</threat_model>

<verification>
1. `cd /c/Users/mandr/codepulse && npx tsc --noEmit` exits 0.
2. `npx vitest run convex/runtimeIngest.test.ts convex/controlVerbSwaps.test.ts convex/activeEngine.test.ts` exits 0.
3. `npm test` — full suite green (wave-merge gate).
</verification>

<success_criteria>
- A `control_verb_swap` event with either `verb` reaches `controlVerbSwaps` with all fields
  dual-coalesced.
- A `model_routing` event with `status:"failed"` produces no `activeEngineSnapshots` row.
- Neither case can throw out of the switch.
- Both cases have test coverage where previously there was none.
</success_criteria>

<output>
Create `.planning/phases/108-per-profile-engine-telemetry-astridr-backend/108-03-SUMMARY.md` when done.
Include the Task 2 mutation-check result.
</output>
