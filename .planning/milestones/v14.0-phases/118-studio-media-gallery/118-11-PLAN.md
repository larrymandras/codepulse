---
phase: 118-studio-media-gallery
plan: 11
type: execute
wave: 6
depends_on: ["118-10"]
files_modified:
  - src/components/studio/MediaDetailSheet.tsx
  - src/components/studio/StylesPanel.tsx
  - src/components/studio/ModelsPanel.tsx
  - src/pages/Studio.tsx
  - src/pages/Studio.test.tsx
autonomous: true
requirements: [D-02, D-07, D-08, D-12]

must_haves:
  truths:
    - "D-07 holds at field level: in the detail panel every absent provenance field renders the exact string 'No provenance recorded' in a visually distinct muted italic style, while a populated field renders as non-italic mono foreground text — the two are distinguishable side by side in one panel."
    - "D-02 holds in the detail panel: it displays the same bounded thumbnail already in the grid at a larger bounded size and never fetches or renders the original file — this is explicitly not a lightbox."
    - "D-08's UI holds: Move to Trash takes no modal confirmation, the row leaves the Gallery grid immediately with no host round-trip, the Trash tab shows it with a 'Deletes automatically in N days' caption, and Restore returns it."
    - "D-12 holds in the Models panel: `recipeMd` renders as plain text inside a `<pre>` with `font-mono` and is never passed to `dangerouslySetInnerHTML`, and no create/edit UI exists for `mediaModels` or `mediaStyles`."
  artifacts:
    - path: "src/components/studio/MediaDetailSheet.tsx"
      provides: "Right-side detail Sheet: bounded thumb, field-level recipe block, copy recipe/path, trash/restore, star"
      contains: "No provenance recorded"
    - path: "src/components/studio/ModelsPanel.tsx"
      provides: "Collapsible mediaModels list with plain-text recipeMd rendering"
      contains: "<pre"
    - path: "src/pages/Studio.test.tsx"
      provides: "Component tests for the D-07 field pair, the trash countdown and the recipeMd rendering rule"
      contains: "No provenance recorded"
  key_links:
    - from: "src/components/studio/MediaDetailSheet.tsx"
      to: "api.media.softDelete / api.media.restore / api.media.toggleStar"
      via: "useMutation calls from the SheetFooter action row"
      pattern: "useMutation"
---

<objective>
Finish the `/studio` surface: the detail Sheet where the full recipe lives, the Trash view with its
visible grace period, and the collapsible Styles and Models panels.

Purpose: this plan realises D-07's control pair at the FIELD level (a populated value and an absent
one rendered side by side, visually distinct) and D-08's full three-state loop (hidden from Gallery,
present in Trash, restorable) — the design doc's own gate stops after the first two.

Output: the complete page, plus component tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/118-studio-media-gallery/118-UI-SPEC.md
@.planning/phases/118-studio-media-gallery/118-PATTERNS.md
@.planning/phases/118-studio-media-gallery/118-CONTEXT.md
@.planning/phases/118-studio-media-gallery/118-10-SUMMARY.md

**Scope guard:** the v15.0 "Borealis Console" overhaul, including its quick-wins, stays out of this
phase per CLAUDE.md.

**Treat this plan's prose as a DRAFT, never a spec.** `118-UI-SPEC.md` is the contract.
</context>

<tasks>

<task type="auto">
  <name>Task 1: MediaDetailSheet — the recipe panel and its actions</name>
  <files>src/components/studio/MediaDetailSheet.tsx, src/pages/Studio.tsx</files>
  <read_first>
    - `.planning/phases/118-studio-media-gallery/118-UI-SPEC.md` § "Media Detail Sheet Contract" in
      full — the five top-to-bottom sections, the exact button variants and labels, the
      icon-only-control rule (only the Star toggle is icon-only; Copy Recipe, Copy Path, Move to
      Trash and Restore all carry visible text), the disabled-Copy-Recipe tooltip copy, and the
      explicit statement that this Sheet is NOT the forbidden lightbox.
    - `src/components/galdr/PromptEditorDrawer.tsx:186-339` — the Sheet shape to reuse:
      `Sheet` / `SheetContent side="right" className="w-full sm:max-w-2xl overflow-y-auto"` /
      `SheetHeader` / `SheetFooter className="flex-row items-center justify-between"`. Also read
      `:258-259`'s inline plain-text-only rendering rule and its rationale.
    - `src/pages/Bifrost.tsx:88-127` — the single-click, no-`AlertDialog` destructive action
      precedent that the UI-SPEC cites for Move to Trash.
    - `src/components/studio/MediaCard.tsx` from plan `118-10` — the broken-thumbnail fallback block
      to reuse verbatim, and the `data-testid` naming scheme to extend.
  </read_first>
  <action>
Create `MediaDetailSheet.tsx` as a right-side shadcn `Sheet`, contents top to bottom per the
UI-SPEC:

1. **Thumbnail** — the SAME bounded ≤200 KB thumbnail already in the grid, rendered larger at
   `max-h-80 object-contain`, reusing the card's broken-thumbnail fallback component. Add a comment
   stating plainly that this is not a lightbox: it never fetches the original, which is exactly why
   D-02 could forbid the lightbox without making a second HTTP origin load-bearing.
2. **Filename** plus `mediaType` and `kind` badges.
3. **Recipe block** — `prompt`, `model`, `provider`, `style`, `project`, `params`, each as a labelled
   row. **This is D-07's control pair at field level and the styling difference is the mechanism, not
   decoration:** a populated value renders `text-foreground font-mono`, NOT italic; an absent field
   renders the exact string `No provenance recorded` in `text-muted-foreground italic`. A reader must
   be able to tell the two apart in one glance within a single panel. Never render blank, never
   render `Unknown`, never derive a value from the filename.
4. **Technical facts** — dimensions or duration, size, created date, and the absolute path in small
   `font-mono` with a **Copy Path** button beside it.
5. **Actions row** in `SheetFooter`, matching `PromptEditorDrawer.tsx:328`'s layout:
   - **Copy Recipe** — primary `Button variant="default"`; copies one formatted plain-text block
     (prompt/model/provider/style/params) sized to paste straight into `/studio-generate`. When the
     row has no provenance the button is DISABLED with the tooltip
     `No recipe to copy — this file has no sidecar.` A disabled button with an explaining tooltip is
     the point: an enabled button that copies an empty block would be a silent failure.
   - **Copy Path** — `variant="outline"`, `Copy` icon plus the visible text `Copy Path`.
   - **Move to Trash** — `variant="ghost"`, muted icon plus the visible text `Move to Trash`.
     Deliberately NOT `variant="destructive"`, and deliberately no modal confirmation: the 30-day
     Trash plus a visible Restore path is the safety net, and a confirm dialog on an already
     reversible action is theatre. Omitted entirely when viewing a Trash row.
   - **Restore** — `variant="outline"`, `RotateCcw` icon plus the visible text `Restore`. Shown only
     for a Trash row. No confirmation.
   - **Star toggle** — the ONE icon-only control in this row, same `Star` glyph as the card overlay,
     `aria-label={\`Toggle favorite ${filename}\`}`, filled state `var(--status-warn)`.

Wire the actions to `useMutation(api.media.toggleStar / softDelete / restore)`. After `softDelete`
the Sheet closes and the row leaves the Gallery grid immediately — that is the Convex subscription
updating, with no host round-trip, which is exactly what D-08's mutation-flags-then-watcher-moves
split buys.

Wire the Sheet into `Studio.tsx`'s card-click callback for both tabs.

Give every action button and every recipe field row a stable `data-testid` so plan `118-15`'s E2E
spec can assert the field-level control pair. List them in the SUMMARY.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0 and `npm run build` succeeds.
    - The file contains the exact strings `No provenance recorded`, `Copy Recipe`, `Copy Path`,
      `Move to Trash`, `Restore` and `No recipe to copy — this file has no sidecar.`
    - The absent-field style includes `italic` and `text-muted-foreground`; the populated-field style
      includes `font-mono` and does NOT include `italic`. Quote both class strings — the difference
      is the D-07 mechanism.
    - `grep -c "AlertDialog" src/components/studio/MediaDetailSheet.tsx` returns 0 — no modal
      confirmation on either destructive action.
    - Copy Recipe is disabled when `hasProvenance` is false — quote the condition.
    - No original-file fetch: `grep -n "absPath" src/components/studio/MediaDetailSheet.tsx`
      inspected line by line and reported; `absPath` appears only as displayed/copied text.
    - `grep -c "dangerouslySetInnerHTML" src/components/studio/MediaDetailSheet.tsx` returns 0.
    - `grep -nE "#[0-9a-fA-F]{3,8}|rgba?\(" src/components/studio/MediaDetailSheet.tsx` returns no
      match, run with a known-positive control so the zero is meaningful.
  </acceptance_criteria>
  <done>The detail panel shows the full recipe with absent fields visually distinct from populated ones, offers copy/trash/restore/star without a modal, and never touches the original file.</done>
</task>

<task type="auto">
  <name>Task 2: Trash view — dimmed cards, visible countdown, restore</name>
  <files>src/pages/Studio.tsx, src/components/studio/MediaDetailSheet.tsx</files>
  <read_first>
    - `.planning/phases/118-studio-media-gallery/118-UI-SPEC.md` § "Trash View Contract" — the
      `opacity-60` (explicitly NOT grayscale, because grayscale would fight broken-thumbnail
      detection since a broken thumbnail is already colourless), the caption replacing the
      model/type row, the `var(--status-error)` threshold at N ≤ 3 days with muted styling above it,
      and the statement that no manual "delete forever" control exists in this phase.
    - `.planning/phases/118-studio-media-gallery/118-CONTEXT.md` D-08 and `<specifics>` — the gate
      requires all three of: absent from Gallery, present in Trash, restorable.
    - `convex/media.ts`'s `listTrash` — it already returns `daysUntilPurge` computed server-side, so
      no component recomputes the deadline.
    - `src/components/studio/MediaCard.tsx` — the card being reused with a `trashVariant` prop.
  </read_first>
  <action>
Extend the Trash tab in `Studio.tsx` and add a `trashVariant` mode to the existing card rather than
duplicating it:

- Thumbnail renders at `opacity-60`. **Not grayscale** — a broken thumbnail is already colourless,
  so grayscale would make the two states indistinguishable, and the comment in the code should say
  that so nobody "improves" it later.
- The model/type row is replaced by the caption `Deletes automatically in N days`, using the
  server-computed `daysUntilPurge` from `listTrash` — no component recomputes `deletedAt + 30d`.
  Styling: `var(--status-error)` when N ≤ 3, otherwise `text-muted-foreground`. No red until the
  deadline is actually close; a permanently red trash view trains the operator to ignore it.
- Card click still opens the detail Sheet (read-only recipe view); the Sheet's action row swaps
  Move to Trash for Restore.
- The Trash tab is a flat list sorted most-recently-deleted first, with Search only — no filter
  chips, no Selects, no Styles/Models panels.
- **Ship no "delete forever" control.** Permanent deletion is the 30-day janitor's job only
  (plan `118-06`). If a future editor wants one, it is a new decision, not a gap.
- Empty state: `Trash is empty` with the body
  `Deleted media appears here for 30 days before it's permanently removed.`

Give the countdown caption and the Restore button `data-testid` values; plan `118-15`'s E2E spec
asserts the three-state D-08 control pair through them.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0 and `npm run build` succeeds.
    - The Trash card uses `opacity-60` and contains ZERO occurrences of `grayscale` — with a comment
      explaining why.
    - The countdown string `Deletes automatically in` is present, and the value comes from
      `daysUntilPurge` rather than a client-side date computation — quote the expression and confirm
      no `+ 30` or `2592000000` literal appears in `src/`.
    - The `var(--status-error)` styling applies only when N ≤ 3 — quote the condition.
    - No "delete forever" / "permanently delete" control exists — `grep -inE "delete forever|permanently delete|purge now" src/`
      returns nothing.
    - The Trash tab renders no filter chips and no Selects — quote the conditional.
    - Both trash empty-state strings are present verbatim.
  </acceptance_criteria>
  <done>Trash is a structurally distinct view with a visible, server-computed grace period and a restore path, and no way to bypass the janitor.</done>
</task>

<task type="auto">
  <name>Task 3: Styles and Models collapsible panels, plus component tests</name>
  <files>src/components/studio/StylesPanel.tsx, src/components/studio/ModelsPanel.tsx, src/pages/Studio.tsx, src/pages/Studio.test.tsx</files>
  <read_first>
    - `.planning/phases/118-studio-media-gallery/118-UI-SPEC.md` § "Collapsible Panels — Styles &
      Models" — both default COLLAPSED, placed between the filter bar and the grid; Styles is a
      small FIXED-ASPECT card grid (not masonry — it is a curated table, not a content feed); Models
      is a list with `name`, `provider`, a `type` Badge, an enabled/disabled dot, `docsUrl` as an
      external link, and `recipeMd` rendered as plain text in a `<pre>` with `font-mono`.
    - `src/components/galdr/PromptEditorDrawer.tsx:258-259` — the plain-text-only rendering rule and
      its stated rationale: agent-authored free text rendered as HTML is a stored-XSS surface for
      zero benefit.
    - `src/pages/Bifrost.tsx` — the `ExternalLink` icon-plus-mono-URL treatment to reuse for
      `docsUrl`.
    - `src/test/setup.ts` — what this repo's jsdom setup already mocks, so the component test does
      not re-mock things unnecessarily.
    - `src/components/ui/collapsible.tsx` — the installed primitive.
  </read_first>
  <action>
**`StylesPanel.tsx`** — a `Collapsible`, default collapsed, rendering `mediaStyles` rows from
`useQuery(api.media.listStyles)` in a small fixed-aspect card grid (NOT masonry). Each card shows the
`previewMediaId`-resolved thumbnail — or the same `ImageOff` placeholder the main grid uses when the
pointer is absent or dangling — plus `name` and `slug`.

**`ModelsPanel.tsx`** — a `Collapsible`, default collapsed, rendering `mediaModels` rows from
`useQuery(api.media.listModels)` as a list: `name`, `provider`, `type` as a small `Badge`, an
enabled/disabled dot, `docsUrl` as an external link reusing Bifröst's treatment, and `recipeMd`
rendered as **plain text inside a `<pre>` with `font-mono`**. Add the inline rationale comment
mirroring `PromptEditorDrawer.tsx:258-259`: `recipeMd` is agent-authored free text and rendering it
as HTML would be a stored-XSS surface for zero benefit. **`dangerouslySetInnerHTML` must not appear
in this file**, and no markdown-to-HTML renderer may be introduced.

**Neither panel gets create or edit UI** (D-12): cards are seeded only by the agent after an actual
proven run, never authored blind in the UI. `enabled` is displayed, not editable.

Place both panels between the filter bar and the masonry grid in the Gallery tab only.

**`src/pages/Studio.test.tsx`** — component tests under this repo's jsdom Vitest setup:

1. **D-07 field-level control pair (mandatory).** Render the detail Sheet twice in one test — once
   with a fully populated row, once with a provenance-absent row — and assert: the absent one shows
   `No provenance recorded` with an italic muted class, the populated one shows the real value with a
   mono non-italic class, and the two class strings DIFFER. Asserting only the absent case would pass
   against a component that renders the sentinel unconditionally.
2. **D-07 card badge control pair.** Render a grid containing one provenance-absent and one
   provenance-bearing row; assert exactly ONE `No provenance recorded` badge is present. Asserting
   "at least one" would pass against a component that badges everything.
3. **Copy Recipe disabled state.** Provenance-absent row → the button is disabled and carries the
   tooltip copy. Control: provenance-bearing row → enabled.
4. **Thumbnail fallback.** `thumbnailUrl: null` → `Thumbnail unavailable` renders and the card is
   still clickable and star-able. Control: a non-null URL renders an `<img>`. Also fire the image's
   `onError` and assert the fallback replaces it.
5. **Audio placeholder** renders with a solid border and NOT the dashed error treatment — control:
   a broken image renders the dashed one. The two must be distinguishable.
6. **Trash countdown.** `daysUntilPurge: 2` renders with the `--status-error` styling;
   `daysUntilPurge: 20` renders muted. Both in one test.
7. **`recipeMd` is not HTML.** Feed a `recipeMd` containing `<img src=x onerror=alert(1)>` and assert
   the literal text appears in the document and NO `img` element was created. This is the assertion
   that actually guards the XSS rule; asserting the absence of `dangerouslySetInnerHTML` in source is
   a weaker, secondary check — do both.
8. **No create/edit UI** in either panel — assert no button with create/edit/save semantics renders.

Break two implementation rules (e.g. render `recipeMd` through `dangerouslySetInnerHTML`; make the
absent-field style identical to the populated one), confirm the corresponding tests go RED, revert.
  </action>
  <verify>
    <automated>npx vitest run src/pages/Studio.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/pages/Studio.test.tsx` passes.
    - `grep -c "dangerouslySetInnerHTML" src/components/studio/*.tsx src/pages/Studio.tsx` returns 0.
    - `git diff package.json` shows no dependency added — no markdown renderer was introduced.
    - Both panels default to collapsed — quote the `defaultOpen`/`open` state.
    - Test 7 asserts on the rendered DOM (literal text present, zero `img` elements created), not
      only on source text.
    - Tests 1, 2, 3, 5 and 6 each assert BOTH sides of their pair in a single test.
    - **Mutation proof:** two implementation rules broken, observed red, reverted — quote both
      failure messages.
    - `npm test` full suite at or above the pre-plan baseline (quote both); `npm run build` succeeds.
  </acceptance_criteria>
  <done>The curated panels render read-only with `recipeMd` proven inert as HTML, and the D-07 pairs are guarded at both card and field level by tests proven to fail when the distinction is removed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `mediaModels.recipeMd` → rendered DOM | Agent-authored free text from a curated table, rendered in a collapsible panel. |
| sidecar-derived provenance fields → rendered DOM | Generator-authored text arriving through the ingest route. |
| detail Sheet actions → public Convex mutations | Single-click destructive action with no confirmation dialog. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-118-05 | Tampering (stored XSS) | `recipeMd` rendered in the Models panel | mitigate | Plain text inside `<pre className="font-mono">`; no `dangerouslySetInnerHTML` anywhere; no markdown-to-HTML renderer introduced (`git diff package.json` clean). Guarded by a DOM-level test feeding `<img src=x onerror=alert(1)>` and asserting the literal text renders with zero `img` elements created — a source-text grep alone would be a weaker check, so both are required. Same rule and rationale as `PromptEditorDrawer.tsx:258-259`. |
| T-118-35 | Spoofing | an absent provenance field reading as a populated one | mitigate | The absent case renders the exact sentinel in a DIFFERENT class string (muted italic) from a populated value (mono, non-italic), asserted by a test that requires the two class strings to differ. This is 117 D-03's rule applied at field level. |
| T-118-03 | Denial of Service | the detail Sheet fetching the original file | mitigate | The Sheet renders the same bounded ≤200 KB thumbnail at `max-h-80 object-contain`; `absPath` appears only as displayed and copied text, verified by inspecting every occurrence. D-02's no-lightbox decision holds precisely because this panel never needs a second origin. |
| T-118-36 | Tampering / data loss | one-click Move to Trash with no confirmation | accept | Deliberate per the UI-SPEC: the action is reversible for 30 days with a visible Restore path and a visible countdown, matching Bifröst's existing archive precedent. A confirm dialog on an already-reversible action is theatre. The irreversible step is the janitor's, and this phase ships no manual "delete forever" control at all — asserted by a grep. |
| T-118-37 | Tampering | a curated `mediaModels` card authored blind in the UI | mitigate | D-12: neither panel ships create or edit UI, asserted by a test. Cards are seeded only after a proven end-to-end run in plans `118-12` through `118-14`. |
| T-118-04 | Information Disclosure | a credential value pasted into `recipeMd` and rendered | mitigate | D-12 restricts `recipeMd` to environment-variable NAMES only; enforcement is at authoring time in plans `118-12`/`118-13`/`118-14`. This plan's contribution is that the panel is read-only, so no value can be introduced through the UI. |
| T-118-SC | Tampering | npm/pip/cargo installs | accept | Zero new packages; all primitives already installed. |
</threat_model>

<verification>
1. `npx vitest run src/pages/Studio.test.tsx` green with two proven mutation failures.
2. `grep -c "dangerouslySetInnerHTML" src/components/studio/*.tsx src/pages/Studio.tsx` → 0.
3. `git diff package.json` shows no dependency added.
4. `grep -inE "delete forever|permanently delete|purge now" src/` returns nothing.
5. `npx tsc --noEmit` clean; `npm run build` succeeds; `npm test` at or above baseline.
</verification>

<success_criteria>
- The detail panel makes absent provenance visually distinct from populated provenance at field
  level, guarded by a test that fails when the distinction is removed.
- Trash shows a server-computed, visible grace period and restores; nothing bypasses the janitor.
- `recipeMd` is proven inert as HTML at the DOM level, not merely by a source grep.
</success_criteria>

<output>
Create `.planning/phases/118-studio-media-gallery/118-11-SUMMARY.md` when done. It MUST list every
`data-testid` introduced (plan `118-15` depends on them), quote the two differing class strings that
implement D-07's field-level pair, quote both mutation-proof failure messages, and quote
before/after `npm test` counts.
</output>
