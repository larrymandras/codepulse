---
phase: 118-studio-media-gallery
plan: 15
type: execute
wave: 10
depends_on: ["118-11", "118-14"]
files_modified:
  - e2e/studio.spec.ts
  - .planning/phases/118-studio-media-gallery/118-GATE-EVIDENCE.md
  - .planning/phases/118-studio-media-gallery/118-VALIDATION.md
autonomous: true
requirements: [D-02, D-04, D-07, D-08, D-14, D-16]

must_haves:
  truths:
    - "D-16 is proven by a real click-through: navigating from the COMMAND nav group reaches `/studio` and the page renders, rather than a route merely existing in the router config."
    - "D-07's control pair is proven in the browser: a complete-recipe card and a 'No provenance recorded' card render in the SAME grid view, and the detail panel distinguishes populated from absent fields."
    - "D-08's three-state control pair is proven in the browser: a soft-deleted row is absent from Gallery, present in Trash with its countdown, and returns to Gallery after Restore — the design doc's own gate stops after the first two."
    - "D-14 is proven by an observable artifact: `G:\\My Drive\\media-vault` holds the mirrored content and `backup.log` is present locally but absent from the mirror."
    - "Every decision D-01 through D-16 has a named evidence pointer, and any decision that is not fully proven is recorded as OPEN rather than assumed closed."
  artifacts:
    - path: "e2e/studio.spec.ts"
      provides: "Playwright reachability and control-pair proofs against the live instance"
      contains: "No provenance recorded"
    - path: ".planning/phases/118-studio-media-gallery/118-GATE-EVIDENCE.md"
      provides: "Full-suite gate results and the D-01..D-16 coverage roll-up"
      contains: "D-16"
  key_links:
    - from: "e2e/studio.spec.ts"
      to: "src/components/studio/*"
      via: "data-testid selectors introduced by plans 118-10 and 118-11"
      pattern: "getByTestId"
---

<objective>
Guard the phase against regression with an end-to-end spec, run the full gate, and produce a single
artifact that says — with a pointer to evidence for each — whether every one of D-01 through D-16 is
actually proven.

Purpose: three of this phase's gates were verified by direct probe during execution, which leaves
them unguarded against regression. This spec closes that hole the same way `e2e/loom.spec.ts` closed
Phase 119's. The roll-up exists because the phase's acceptance-bearing units are decisions, not
requirement IDs, and a verification pass needs one place to check them against.

Output: `e2e/studio.spec.ts`, a gate-evidence artifact, and an updated `118-VALIDATION.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/118-studio-media-gallery/118-VALIDATION.md
@.planning/phases/118-studio-media-gallery/118-CONTEXT.md
@.planning/phases/118-studio-media-gallery/118-10-SUMMARY.md
@.planning/phases/118-studio-media-gallery/118-11-SUMMARY.md
@.planning/phases/118-studio-media-gallery/118-D09-EVIDENCE.md

**Treat this plan's prose as a DRAFT, never a spec.** The `data-testid` values are whatever plans
`118-10` and `118-11` actually shipped — read their SUMMARYs, which were required to list them.
</context>

<tasks>

<task type="auto">
  <name>Task 1: e2e/studio.spec.ts — reachability and both browser control pairs</name>
  <files>e2e/studio.spec.ts</files>
  <read_first>
    - `e2e/loom.spec.ts` (whole file) — the pattern to follow: reads the LIVE self-hosted instance,
      creates nothing, seeds `codepulse_onboarding_complete` in `addInitScript`, and — the key
      point, stated in its own header — **DELIBERATELY FAILS rather than skips when its fixture is
      missing**, because a spec that skips itself when its fixture is absent passes vacuously and
      reports green while proving nothing.
    - `e2e/bifrost.spec.ts` — the live-Convex-data reachability and control-pair assertion pattern.
    - `e2e/navigation.spec.ts` — how nav click-throughs are asserted in this repo.
    - `.planning/phases/118-studio-media-gallery/118-10-SUMMARY.md` and `118-11-SUMMARY.md` — the
      complete list of `data-testid` values introduced. Use them; do not invent selectors.
    - `.planning/phases/118-studio-media-gallery/118-D09-EVIDENCE.md` — which real rows exist, and
      the filename of the deliberately sidecar-less control file that makes the D-07 pair possible.
    - `package.json` scripts and `playwright.config.ts` — how E2E is actually invoked here, including
      the `dev:noauth` caveats documented in the `test:e2e:noauth:help` script.
  </read_first>
  <action>
Create `e2e/studio.spec.ts` with a header comment, in `loom.spec.ts`'s voice, stating that it reads
the live instance, creates no rows, and fails rather than skips when its fixtures are missing.

**Test 1 — D-16 reachability by real click-through.** From the dashboard, open the COMMAND nav group
and CLICK the `Studio` entry; assert the URL is `/studio` and the page's `PageHeader` title renders.
Do not assert that a route exists in configuration — the decision is about reachability from the nav
a human uses. Also assert the sync caption
`Auto-syncs every 5 min · run /studio-sync for an instant sync` is visible, since the ≤5-minute
latency being visible is part of the page's contract.

**Test 2 — D-07 control pair in one grid view (mandatory).** In the Gallery tab, assert BOTH in the
same assertion block: at least one card WITHOUT a `No provenance recorded` badge (the generated
asset from plan `118-12`) and at least one card WITH that badge (the sidecar-less control file).
Then open each one's detail Sheet and assert the recipe block shows a populated `prompt` for the
first and the exact string `No provenance recorded` for the second, with DIFFERENT class strings.
A single "the image appeared" assertion is explicitly insufficient — it would look identical if the
sidecar reader were broken and silently returned nothing. If either fixture row is missing, FAIL with
a message naming which, rather than skipping.

**Test 3 — D-08 three-state control pair (mandatory).** Operating on the sidecar-less control row
(chosen deliberately: it carries no provenance worth risking):
1. Record the Gallery card count and assert the row is present.
2. Open its Sheet and click `Move to Trash`. Assert NO confirmation dialog appeared (`AlertDialog`
   never renders) — the no-modal behaviour is itself part of D-08's UI contract.
3. Assert the row is ABSENT from the Gallery grid and the count decreased by exactly one.
4. Switch to the Trash tab; assert the row is PRESENT and its `Deletes automatically in N days`
   caption renders.
5. Click `Restore`; assert the row returns to the Gallery and the count is back to its original
   value.
Add an `afterEach` that restores the row if the test failed part-way, so a failing run does not leave
live data trashed. Note in a comment that the watcher will move the file to `trash\` within one cycle
if the row is left flagged, which is exactly why the cleanup exists.

**Test 4 — D-02 no original fetch.** Instrument the page's network requests and assert that no
request is made for the original media file. Concretely: capture all requests during a card click
and detail-Sheet open, and assert none targets a URL derived from `absPath` or carries a
non-thumbnail media extension from the vault. Control, in the same test: assert at least one
thumbnail request WAS made — otherwise a page that loaded nothing at all would pass.

Use only `data-testid` selectors from the two SUMMARYs. Where a needed testid does not exist, add it
to the component and say so in this plan's SUMMARY rather than falling back on a brittle text or
CSS selector.
  </action>
  <verify>
    <automated>npx playwright test e2e/studio.spec.ts --project=chromium</automated>
  </verify>
  <acceptance_criteria>
    - `npx playwright test e2e/studio.spec.ts --project=chromium` passes.
    - `grep -c "test.skip\|it.skip" e2e/studio.spec.ts` returns 0 — the spec fails rather than skips
      when a fixture is missing.
    - Test 2 asserts BOTH the badged and un-badged card in the same block, and asserts the two
      field class strings DIFFER.
    - Test 3 asserts all five steps including the count decreasing by exactly one and returning to
      its original value, and asserts no confirmation dialog rendered.
    - Test 4's control asserts at least one thumbnail request was made.
    - An `afterEach` restores the row on failure.
    - **Mutation proof:** temporarily remove the `No provenance recorded` badge from `MediaCard.tsx`,
      confirm Test 2 goes RED, revert. Then temporarily make `softDelete` a no-op, confirm Test 3
      goes RED, revert. Quote both failure messages.
  </acceptance_criteria>
  <done>The two mandatory browser control pairs and the nav reachability are guarded by a spec proven to fail when either mechanism is broken.</done>
</task>

<task type="auto">
  <name>Task 2: Run the full phase gate and capture the evidence</name>
  <files>.planning/phases/118-studio-media-gallery/118-GATE-EVIDENCE.md</files>
  <read_first>
    - `.planning/phases/118-studio-media-gallery/118-VALIDATION.md` § "Sampling Rate" — the phase
      gate is the full Vitest suite green AND `e2e/studio.spec.ts` green AND `npx tsc --noEmit`
      clean, and the repo's ~310-file / ~4,100-test baseline where a DROP in count is itself a
      signal.
    - `.planning/phases/118-studio-media-gallery/118-09-SUMMARY.md` — the scheduled-task proofs, and
      whether the D-14 mirror was already verified there.
    - `CLAUDE.md` § "Self-Hosted Convex — Operational Rules".
  </read_first>
  <action>
Run the full gate and write `.planning/phases/118-studio-media-gallery/118-GATE-EVIDENCE.md`
recording each command, its exit code and its headline numbers:

1. `npx tsc --noEmit` — must be clean.
2. `npm test` (full Vitest suite) — record files and tests passed. **Compare against the pre-phase
   baseline recorded in the earliest plan SUMMARY.** A count that DROPPED is a signal even if
   everything shown is green, because a suite that silently stopped collecting a file reports green
   while covering less.
3. `npm run build` — must succeed.
4. `npx playwright test e2e/studio.spec.ts --project=chromium` — must pass.
5. **D-14 mirror verification, control-paired.** Assert `G:\My Drive\media-vault\gen\` contains the
   assets generated in plans `118-12` through `118-14`; assert `media-vault\backup.log` exists
   locally and gained a line; and assert `G:\My Drive\media-vault\backup.log` does NOT exist (the
   exclusion working). The third assertion is the control — without it, "the mirror has files" is
   consistent with a mirror that copied everything indiscriminately.
6. **D-04 cadence verification.** Confirm `StudioWatch` has run at least once since plan `118-09`'s
   registration by an observable side effect — a timestamped line in `media-vault\studio-watch.log`
   later than the registration time. `LastTaskResult` is not acceptable evidence.
7. **Secret scan before this artifact is committed, run LAST.** Scan every file this phase created or
   modified, plus this artifact itself and all evidence files, for credential-shaped strings and for
   the literal environment-variable values of `STUDIO_API_KEY` and `FAL_KEY`. Use FIXED-STRING
   matching for anything containing backslashes or a Windows path — a hand-escaped backslash pattern
   silently returns zero and this repo has been bitten by that exact false negative more than once.
   Pair the scan with a known-present control line so a zero result is meaningful. **Run this step
   after every other write in this task, not before** — a disclosure check that runs before the last
   write passes vacuously.

If any gate fails, record the failure faithfully and do NOT write a passing verdict. A wrong green
in a durable artifact is what the next session reads as ground truth.
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <acceptance_criteria>
    - `118-GATE-EVIDENCE.md` records all seven items with their commands, exit codes and numbers.
    - `npm test`'s file and test counts are quoted alongside the pre-phase baseline, and any drop is
      called out explicitly rather than glossed.
    - `npx tsc --noEmit` clean; `npm run build` succeeds; the Playwright spec passes.
    - The D-14 check includes the `backup.log`-absent-from-mirror control.
    - The D-04 check cites a log line timestamp, not `LastTaskResult`.
    - The secret scan ran LAST, used fixed-string matching for backslash-containing literals, was
      paired with a known-present control, and found nothing. Quote the control's result.
  </acceptance_criteria>
  <done>The full gate has run and its results — including any failure — are recorded with the numbers that make them checkable.</done>
</task>

<task type="auto">
  <name>Task 3: D-01..D-16 coverage roll-up and validation sign-off</name>
  <files>.planning/phases/118-studio-media-gallery/118-GATE-EVIDENCE.md, .planning/phases/118-studio-media-gallery/118-VALIDATION.md</files>
  <read_first>
    - `.planning/phases/118-studio-media-gallery/118-CONTEXT.md` `<decisions>` — all sixteen
      decisions and both AMENDMENT blocks, so the roll-up is checked against the decisions as
      AMENDED rather than as originally written.
    - Every `118-*-SUMMARY.md` produced by this phase, plus `118-D01-EVIDENCE.md`,
      `118-OPENART-EVIDENCE.md` and `118-D09-EVIDENCE.md`.
    - `.planning/phases/118-studio-media-gallery/118-VALIDATION.md` § "Validation Sign-Off" and
      § "Control-Pair Requirement" — the checklist to complete and the three mandatory pairs.
  </read_first>
  <action>
Append a `## Decision coverage roll-up` section to `118-GATE-EVIDENCE.md`: one row per decision
`D-01` through `D-16`, each with the plan that implemented it, the file or artifact that evidences
it, and a status of `PROVEN`, `PARTIAL` or `OPEN`.

Rules for the status column, and they are the whole point of this task:
- `PROVEN` requires evidence that would have looked DIFFERENT had the mechanism been broken. A green
  test whose control was never checked, or an assertion that would pass vacuously, is not PROVEN.
- `PARTIAL` for anything implemented but not exercised against reality — say exactly what is missing.
- `OPEN` for anything skipped, including a skipped generator leg or a skipped checkpoint. **Do not
  round OPEN up to PROVEN because everything else passed.**

Then add a `## Mandatory control pairs` section explicitly confirming all three from
`118-VALIDATION.md`, each with a pointer to where the pair is asserted:
1. D-01 — the successful upload round-trip paired with the orphaned `avatars` storage ID resolving
   `null` in the same run.
2. D-07 — a complete-recipe card rendered in the same grid view as a sidecar-less card rendering
   `No provenance recorded`.
3. D-08 — soft-deleted row absent from Gallery, present in Trash, and restored.

Finally, update `118-VALIDATION.md`:
- Fill the `Plan` and `Wave` columns of the Per-Task Verification Map from the plans as executed.
- Set each row's Status from the roll-up.
- Tick the `## Validation Sign-Off` checklist items that are genuinely satisfied, and leave unticked
  — with a note — any that are not.
- Set `nyquist_compliant: true` in the frontmatter **only if** every row has an automated verify or a
  satisfied Wave 0 dependency and no three consecutive tasks lack an automated verify. If that is not
  true, leave it `false` and say why. A frontmatter flag is a durable claim; do not set it on the
  basis that the phase is otherwise finished.
  </action>
  <verify>
    <automated>node --input-type=module -e "import fs from 'node:fs'; const g=fs.readFileSync('.planning/phases/118-studio-media-gallery/118-GATE-EVIDENCE.md','utf8'); const missing=[]; for(let i=1;i<=16;i++){const d='D-'+String(i).padStart(2,'0'); if(!new RegExp('\\\\b'+d+'\\\\b').test(g)) missing.push(d);} if(missing.length) throw new Error('roll-up missing decisions: '+missing.join(', ')); if(!/## Mandatory control pairs/.test(g)) throw new Error('mandatory control pairs section missing'); console.log('all 16 decisions present in roll-up');"</automated>
  </verify>
  <acceptance_criteria>
    - The roll-up contains a row for every one of `D-01` through `D-16` — asserted automatically.
    - Every row names a concrete evidence file or test, not a plan number alone.
    - Every `PROVEN` row's evidence includes a control, or a stated reason why the assertion could
      not have passed vacuously. Spot-check three rows in the SUMMARY by naming their controls.
    - Any `OPEN` or `PARTIAL` row names exactly what is missing.
    - The `## Mandatory control pairs` section confirms all three with pointers.
    - `118-VALIDATION.md`'s Per-Task Verification Map has its Plan, Wave and Status columns filled.
    - `nyquist_compliant` is set to `true` only if the stated conditions hold; otherwise it remains
      `false` with a written reason.
  </acceptance_criteria>
  <done>One artifact states, decision by decision and with a control named for each, what this phase actually proved — and what it did not.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| E2E spec → live self-hosted Convex data | The D-08 test performs a real soft-delete and restore against live rows. |
| gate evidence artifacts → public repo | Committed content that may echo paths, log lines and command output. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-118-46 | Tampering / data loss | the E2E spec leaving a live row soft-deleted after a failing run | mitigate | Test 3 operates deliberately on the sidecar-less control row (no provenance worth risking) and an `afterEach` restores it on failure. The 30-day grace means even a missed cleanup is recoverable, and the janitor is the only irreversible path. |
| T-118-04 | Information Disclosure | credential values in gate or evidence artifacts committed to a public repo | mitigate | Task 2's secret scan runs LAST — after every other write in the task — because a disclosure check that runs before the final write passes vacuously. It uses FIXED-STRING matching for backslash-containing literals (a hand-escaped pattern silently returns zero, a false negative this repo has hit repeatedly) and is paired with a known-present control so the zero is meaningful. |
| T-118-45 | Spoofing | recording a decision as PROVEN when its evidence would look identical if broken | mitigate | The roll-up's `PROVEN` status is defined as requiring evidence that would have differed under failure; `OPEN` and `PARTIAL` are first-class outcomes and explicitly must not be rounded up. `nyquist_compliant: true` may be set only on stated conditions, because a frontmatter flag is a durable claim the next session reads as ground truth. |
| T-118-47 | Spoofing | a vacuously-passing E2E spec that skips when its fixture is missing | mitigate | `grep -c "test.skip\|it.skip"` returning 0 is an acceptance criterion, and the spec fails with a message naming the missing fixture — the same hole `e2e/loom.spec.ts` was written to close. Two mutation proofs confirm Tests 2 and 3 go red when their mechanisms break. |
| T-118-03 | Denial of Service | the browser fetching an original media file | mitigate | Test 4 instruments network requests and asserts no original is fetched, paired with a control asserting at least one thumbnail request WAS made — without which a page that loaded nothing would pass. |
| T-118-SC | Tampering | npm/pip/cargo installs | accept | No packages installed; Vitest and Playwright are already configured. |
</threat_model>

<verification>
1. `npx playwright test e2e/studio.spec.ts --project=chromium` green with two proven mutation
   failures.
2. `npx tsc --noEmit` clean; `npm run build` succeeds; `npm test` at or above the pre-phase baseline
   with counts quoted.
3. `118-GATE-EVIDENCE.md` contains all sixteen decision rows (asserted automatically) and the
   mandatory-control-pairs section.
4. The D-14 mirror control (`backup.log` absent from the mirror) and the D-04 log-timestamp check
   both recorded.
5. The final secret scan found nothing, with its known-present control quoted.
</verification>

<success_criteria>
- The two mandatory browser control pairs and nav reachability are guarded against regression by a
  spec proven to fail when broken.
- The full gate has run and its numbers are recorded against the pre-phase baseline.
- Every decision D-01 through D-16 has a status backed by a named control, and nothing unproven is
  recorded as proven.
</success_criteria>

<output>
Create `.planning/phases/118-studio-media-gallery/118-15-SUMMARY.md` when done. It MUST quote: the
two mutation-proof failure messages, the full-suite before/after counts against the pre-phase
baseline, the three mandatory control pairs with their evidence pointers, the final `D-09 status`
line, the secret scan's control result, and the value written to `nyquist_compliant` with its
reason.
</output>
