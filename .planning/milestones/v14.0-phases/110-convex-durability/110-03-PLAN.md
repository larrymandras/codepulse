---
phase: 110-convex-durability
plan: 03
type: execute
wave: 2
depends_on: ["110-01"]
files_modified:
  - convex/retention.ts
  - convex/retention.test.ts
  - convex/aggregates.ts
  - convex/dataRetention.ts
autonomous: true
requirements: [DUR-01, DUR-02]

must_haves:
  truths:
    - "D-01: aggregates is bounded period-aware — period:hourly rows are prunable and period:daily rows can never be deleted by the nightly chain"
    - "D-02: the period filter lives in the existing sequential chain under the one batch cap, not a parallel chain, and the by_creation_time cursor seek is unchanged"
    - "D-03: the keep-forever guard in retention.test.ts is narrowed to llmMetrics/sessions/alerts and replaced for aggregates by a positive guard asserted against the predicate function itself, not against a comment"
    - "D-04: RETENTION_DAYS.aggregates is 90 days, the same build/history tier as events and cronExecutions"
    - "D-05: the nightly chain resumes at the table index where the previous capped run stopped, instead of always restarting at index 0"
    - "D-06: the rotation cursor is a single patched row in agentConfigs that never grows, written only at the two chain-terminal actions"
    - "The terminal prune log line states the index the run started at and how many of the table count it covered, so a completed pass is distinguishable from a partial one"
    - "Every comment in the repo that describes aggregates as kept-forever or immutable is corrected in the same change that makes it prunable"
  artifacts:
    - path: "convex/retention.ts"
      provides: "RETENTION_DAYS.aggregates, PRUNE_PREDICATES, predicate-gated delete loop, rotation cursor read/write, listRetentionPolicy"
      exports: ["RETENTION_DAYS", "PRUNE_PREDICATES", "startNightlyPrune", "pruneBatchV3", "listRetentionPolicy"]
      contains: "PRUNE_PREDICATES"
    - path: "convex/retention.test.ts"
      provides: "narrowed keep-forever guard, positive aggregates-predicate guard, 90-day window assertion, predicate-key table-existence guard"
      contains: "PRUNE_PREDICATES"
  key_links:
    - from: "convex/retention.ts"
      to: "convex/retentionCursor.ts"
      via: "named import of partitionBatchForPrune, resolveRotationStart, planRotationWrite"
      pattern: "partitionBatchForPrune"
    - from: "convex/retention.ts"
      to: "agentConfigs"
      via: "get-existing-or-patch on configKey retention.rotationCursor"
      pattern: "retention\\.rotationCursor"
---

<objective>
Wire Phase 110's prune-chain changes into `convex/retention.ts`: `aggregates` joins `RETENTION_DAYS` at 90 days with a period-aware predicate, the delete loop moves to the tested `partitionBatchForPrune` helper, the chain rotates its start index nightly through a single `agentConfigs` row, and a live-readable `listRetentionPolicy` replaces the health check's hand-copied policy table. Then sweep every comment in the repo that says `aggregates` is kept forever.

Purpose: `aggregates` is the largest unbounded table on an instance that has already gone down twice from read growth. Bounding it is DUR-01. But the same run must not break the two invariants this module has already paid for in production incidents — the `by_creation_time` cursor seek (2026-07-30) and the batch-capped sequential chain (2026-07-21/22). Everything here rides the existing machinery; nothing forks it.

Output: a prune chain that prunes `aggregates` hourly rows, resumes where it stopped, publishes its policy map, and logs a terminal line an operator can read as evidence.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@.planning/phases/110-convex-durability/110-CONTEXT.md
@.planning/phases/110-convex-durability/110-RESEARCH.md
@.planning/phases/110-convex-durability/110-PATTERNS.md
@.planning/phases/110-convex-durability/110-VALIDATION.md
@.planning/phases/110-convex-durability/110-01-SUMMARY.md

<interfaces>
<!-- Contracts the executor needs. Extracted live this session; current as of 2026-08-10. -->

From `convex/retentionCursor.ts` after plan 110-01 (consume these — do not reimplement):

  export function partitionBatchForPrune<T extends { _id: unknown; _creationTime: number }>(
    batch: readonly T[],
    predicate?: ((doc: T) => boolean) | undefined
  ): { toDelete: T[]; lastCreationTime: number | null };

  export function resolveRotationStart(rawValue: unknown, tableCount: number): number;
  export function planRotationWrite(action: PruneAction, tableIndex: number): number | null;
  export function planNextPruneStep(args: PlanPruneStepArgs): PruneStep;   // unchanged

From `convex/schema.ts:261-266` — the `agentConfigs` row shape the rotation cursor writes:

  agentConfigs: { configKey: string; value: any; source?: string; updatedAt: number }
  index: by_key on configKey

  `updatedAt` is written as `Date.now() / 1000` — epoch SECONDS, per the four existing
  call sites in convex/webhookDelivery.ts. Do not write milliseconds here.

From `convex/schema.ts:953-990` — the `aggregates` row shape:

  period: "hourly" | "daily"
  bucket_start: number   // epoch SECONDS — the prune cuts on _creationTime in MILLISECONDS.
                         // The predicate must never read bucket_start.

Current `pruneBatchV3` args (`convex/retention.ts:119-130`):

  { tableIndex: number, nowMs: number, deletedSoFar: number, batchesUsed: number,
    cursorMs?: number }

  `cursorMs` was added as OPTIONAL in the 2026-07-30 fix specifically so already-scheduled
  jobs on the old signature still validate. That is the established idiom in this file for
  extending the signature — a required new arg would fail validation on in-flight jobs.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the aggregates entry, the predicate map, and predicate-aware batch bookkeeping</name>
  <files>convex/retention.ts</files>

  <read_first>
    - `convex/retention.ts` (full file, 216 lines) — especially the header comment at lines 6-27, the `prompts`/`promptVersions` D-13 exemption comment at lines 79-95 (the in-file-rationale precedent this entry must follow), the batch query at lines 151-157, and the delete loop at lines 159-165.
    - `convex/retentionCursor.ts` — `partitionBatchForPrune`'s exact signature as built in plan 110-01.
    - `convex/analyticsRollup.ts:271-292` — `clearHistoricalBucketsPage`, the second, pre-existing deleter over `aggregates`. It cuts on `bucket_start` via `by_type_period_bucket`, a different index and field than the nightly prune's `_creationTime` seek, and is called only from a manual operator-gated backfill action (no cron entry references it — confirm in `convex/crons.ts`).
    - `convex/costDerived.ts:405-424` — `computePeriodSpend`'s daily-for-past-days / hourly-for-today read strategy, described in-file as DISJOINT BY CONSTRUCTION. This is the architectural precedent D-01 rests on and the reason daily rows must survive.
    - `.planning/phases/110-convex-durability/110-PATTERNS.md` §`convex/retention.ts` Analogs 1-3.
  </read_first>

  <action>
Three edits to `convex/retention.ts`.

**A. `RETENTION_DAYS` gains `aggregates: 90`** (D-04), placed in the build/history 90-day tier alongside `events`/`cronExecutions`/`jobLifecycle`. It carries an in-file rationale comment in the same shape as the `prompts`/`promptVersions` D-13 block at lines 79-95 — that is this file's established convention for a non-obvious retention decision, and `retention.test.ts` already asserts such rationales exist. The comment must state: `aggregates` is bounded period-aware (`D-01`), only `period:"hourly"` rows are prunable and `period:"daily"` rows are kept forever because Phase 104 re-prices dollars from daily buckets on every read; the measured split as of 2026-08-10 was ~1,455 hourly + 138 daily rows/day with exactly 90 rows older than 90 days, so the first pass carries effectively no backlog; and that the eligibility cut is on `_creationTime` in milliseconds like every other table, never on `bucket_start`, which is epoch seconds.

**B. Add an exported `PRUNE_PREDICATES` map** immediately after `RETENTION_DAYS`, before `const PRUNED_TABLES`:

  `export const PRUNE_PREDICATES: Partial<Record<string, (doc: any) => boolean>>`

with exactly one entry, `aggregates`, returning `doc.period !== "daily"`. A table absent from this map is pruned unconditionally, exactly as today — do not add entries for any other table.

Its comment must cite `D-01`, `D-02` and `D-03`, state that this function IS the positive guard (`retention.test.ts` asserts against it directly rather than against prose), and name `convex/analyticsRollup.ts`'s `clearHistoricalBucketsPage` as a second, pre-existing writer over `aggregates` that deletes by `bucket_start` through `by_type_period_bucket` — a different index and field, so the two do not collide — so a future editor does not rediscover it as a surprise.

**C. Replace the delete loop at lines 159-165** with a call to `partitionBatchForPrune(batch, PRUNE_PREDICATES[table])`, deleting each doc in the returned `toDelete` and passing the returned `lastCreationTime` straight into the existing `planNextPruneStep` call. `deleted` becomes `toDelete.length`.

The batch query at lines 151-157 is unchanged — the predicate applies AFTER the query returns and must never be folded into it. Folding it in would mean querying `aggregates` through a different index and would break the `by_creation_time` monotonic-seek guarantee that `retentionCursor.test.ts` exists to protect, which D-02 explicitly requires reusing unmodified.

**D. Update the file header comment at lines 12-14.** It currently reads that "Aggregates, llmMetrics (cost history), sessions, alerts, and config/audit tables are kept forever". This sentence is the authority `retention.test.ts:94-95` explicitly cites, so it is load-bearing, not decorative. Rewrite it so `aggregates` is described as partially bounded — `period:"hourly"` aged out at 90 days, `period:"daily"` kept forever — while `llmMetrics`, `sessions`, `alerts` and the config/audit tables remain kept forever. Do not delete the sentence; correct it.
  </action>

  <verify>
    <automated>npx vitest run convex/retentionCursor.test.ts && npx tsc --noEmit</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "aggregates: 90" convex/retention.ts` returns `1`.
    - `grep -c "^export const PRUNE_PREDICATES" convex/retention.ts` returns `1`, and the map has exactly one key: no table other than `aggregates` appears as a `PRUNE_PREDICATES` entry.
    - `grep -c "partitionBatchForPrune" convex/retention.ts` returns at least `1`, and `git diff HEAD -- convex/retention.ts` shows the four lines of the old `for (const doc of batch)` loop removed.
    - The batch query is byte-unchanged: `git diff HEAD -- convex/retention.ts` shows no modification to lines containing `withIndex("by_creation_time"`, `.order("asc")`, or `.take(BATCH_SIZE)`.
    - `grep -c "bucket_start" convex/retention.ts` returns a count matching only comment lines — the predicate body must not read `bucket_start`: `grep -v '^\s*//' convex/retention.ts | grep -v '^\s\*' | grep -c "bucket_start"` returns `0`.
    - The header comment no longer asserts `aggregates` is kept forever: `sed -n '1,30p' convex/retention.ts | grep -c "Aggregates, llmMetrics"` returns `0`, while `sed -n '1,30p' convex/retention.ts | grep -c "llmMetrics"` returns at least `1` as the control proving the sentence was rewritten rather than deleted.
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>

  <done>`aggregates` is a 90-day `RETENTION_DAYS` entry with a documented rationale, `PRUNE_PREDICATES.aggregates` exempts daily rows, the delete loop routes through the tested helper, the cursor-seek query is untouched, and the header comment tells the truth.</done>
</task>

<task type="auto">
  <name>Task 2: Add the nightly rotation cursor, the listRetentionPolicy read, and a self-describing terminal log line</name>
  <files>convex/retention.ts</files>

  <read_first>
    - `convex/retention.ts` — `startNightlyPrune` at lines 102-114 (currently hardcodes `tableIndex: 0`), the `cap-reached` branch at 179-187, the `next-table` branch at 202-211, and the terminal `console.log("retention: all tables pruned")` at line 213.
    - `convex/webhookDelivery.ts:36-65` (`setChannel`) — the get-existing-or-patch idiom to copy verbatim in shape, including `updatedAt: Date.now() / 1000`.
    - `convex/aggregates.ts:625-708` and `:713-834` — the two insert-only backfill cursors. This is the ANTI-PATTERN for D-06: they `ctx.db.insert` a new row every call and read back the last by insertion order, so the row count grows forever. Correct for an operator-run one-shot backfill; wrong for a nightly job, which would add ~365 rows/year to `agentConfigs` — the exact unbounded growth this phase exists to prevent.
    - `convex/retentionCursor.ts` — `resolveRotationStart` and `planRotationWrite` signatures from plan 110-01.
    - `C:/Users/mandr/convex-selfhost/retention-health-check.ps1` lines 95-130 — the `npx convex run` invocation shape the new query must be callable through (plan 110-04 consumes it).
  </read_first>

  <action>
Four edits to `convex/retention.ts`.

**A. Rotation cursor read in `startNightlyPrune` (D-05/D-06).** Define a module constant `const ROTATION_CURSOR_KEY = "retention.rotationCursor";`. In the handler, read the single `agentConfigs` row via `.withIndex("by_key", q => q.eq("configKey", ROTATION_CURSOR_KEY)).first()` — `.first()`, matching `webhookDelivery.ts`, never `.collect()` — and pass its `value` through `resolveRotationStart(existing?.value, PRUNED_TABLES.length)`. Schedule `pruneBatchV3` with that result as `tableIndex` instead of the hardcoded `0`. Log the resolved start index alongside the existing `retention: nightly prune started` message so a capped-run resume is visible in the log.

**B. Thread a `startIndex` through the chain.** Add `startIndex: v.optional(v.number())` to `pruneBatchV3`'s args — optional for the same reason `cursorMs` is optional (an already-scheduled job on the old signature must still validate; a required arg would fail them). Resolve it as `args.startIndex ?? args.tableIndex` and pass it forward on every `ctx.scheduler.runAfter` reschedule, in both the `continue-table` and `next-table` branches. `startNightlyPrune` passes the resolved rotation start.

**C. Rotation cursor writes at the two terminal branches (D-06).** Call `planRotationWrite(next.action, args.tableIndex)`. When it returns a number, write it with the get-existing-or-patch idiom: query `agentConfigs` by `by_key` on `ROTATION_CURSOR_KEY`, `ctx.db.patch(existing._id, { value, updatedAt: Date.now() / 1000 })` when a row exists, otherwise `ctx.db.insert("agentConfigs", { configKey: ROTATION_CURSOR_KEY, value, source: "runtime", updatedAt: Date.now() / 1000 })`. When it returns `null`, write nothing. Do this once, in one place, before the `cap-reached` early return and before the terminal `done` log — not duplicated per branch.

`updatedAt` is epoch SECONDS (`Date.now() / 1000`), matching the four `webhookDelivery.ts` call sites. Do not write milliseconds; the value would be indistinguishable from a valid seconds timestamp 55,000 years in the future and nothing would catch it.

**D. Make the two chain-terminal log lines self-describing.** Rotation means the chain no longer always starts at index 0, so `retention: all tables pruned` on its own would claim a complete pass for a run that covered only the tail of the table list — and DUR-02's evidence leg rests on exactly that line. Keep the existing prefix byte-identical (the health check and any operator grep key on it) and append the run's shape:

  `retention: all tables pruned (start index ${startIndex}, covered ${PRUNED_TABLES.length - startIndex} of ${PRUNED_TABLES.length} tables)`

Do the same for the `cap-reached` line at 183-185, appending `(start index ${startIndex})` after the existing message so a capped run records where it began as well as where it stopped. Both existing message prefixes must remain unchanged.

**E. Add `listRetentionPolicy` (D-07's read target).** Export a no-argument function that returns `RETENTION_DAYS` verbatim — a read of the same object `pruneBatchV3` iterates, not a copy. Implement it as an `internalQuery` first, so this phase adds no new publicly-callable endpoint. Verify it is reachable through the exact CLI shape `retention-health-check.ps1` uses (`npx convex run retention:listRetentionPolicy --env-file <path>`); if `npx convex run` refuses internal functions with the credentials that script holds, change it to a public `query` and record in the task summary which one shipped and the error that forced it. Do not guess — run the invocation.

Its comment cites `D-07` and states that it exists so `retention-health-check.ps1` reads the live policy instead of hand-copying it, and that a copy in any other file is a bug.
  </action>

  <verify>
    <automated>npx vitest run convex/retentionCursor.test.ts convex/retention.test.ts && npx tsc --noEmit</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "retention.rotationCursor" convex/retention.ts` returns at least `1`, and `grep -c "ROTATION_CURSOR_KEY" convex/retention.ts` returns at least `4` (constant, read, patch, insert).
    - `startNightlyPrune` no longer hardcodes the start: `grep -c "tableIndex: 0," convex/retention.ts` returns `0`.
    - The rotation write uses patch-or-insert, never insert-only: `grep -c "ctx.db.patch" convex/retention.ts` returns at least `1`, and `grep -c "\.collect()" convex/retention.ts` returns `0`.
    - `updatedAt` is seconds: every `updatedAt:` occurrence in `convex/retention.ts` is followed by `Date.now() / 1000`. `grep -c "updatedAt: Date.now()," convex/retention.ts` returns `0`.
    - `startIndex` is optional and threaded: `grep -c "startIndex: v.optional(v.number())" convex/retention.ts` returns `1`, and `grep -c "startIndex" convex/retention.ts` returns at least `5`.
    - The terminal log prefix is preserved and extended: `grep -c 'retention: all tables pruned (start index' convex/retention.ts` returns `1`.
    - `listRetentionPolicy` returns the live object, not a literal: `grep -c "handler: async () => RETENTION_DAYS" convex/retention.ts` returns `1`, and no second copy of the table list exists anywhere in the file.
    - The CLI invocation was actually run and its raw output recorded in the task summary, showing a JSON object whose key count equals `Object.keys(RETENTION_DAYS).length`. A summary that asserts it works without the transcript does not satisfy this criterion.
    - `npx tsc --noEmit` exits 0 and `npx vitest run convex/retentionCursor.test.ts` still exits 0.
  </acceptance_criteria>

  <done>The chain resumes at a persisted, bounds-checked index held in one patched `agentConfigs` row, both terminal log lines state the run's start index and coverage, and `listRetentionPolicy` is proven callable through the health check's own CLI shape.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Narrow the keep-forever guard, add the positive predicate guard, and sweep the stale aggregates comments</name>
  <files>convex/retention.test.ts, convex/aggregates.ts, convex/dataRetention.ts</files>

  <read_first>
    - `convex/retention.test.ts` (full file, 103 lines) — the harness liveness check at lines 29-35, the `gatewayQuotaSnapshots` bound assertion at 49-54, the Phase 108 bound assertion at 65-68, the D-13 exemption test at 70-90 (the shape the positive guard must follow), the comment at lines 56-64 that describes "a fixed keep-forever list", and the guard being narrowed at lines 92-102.
    - `convex/retention.ts` after Tasks 1-2 — for `PRUNE_PREDICATES`'s exact export shape.
    - `convex/aggregates.ts:743-745` — a comment whose subject is `llmMetrics`, not `aggregates`. Read it before editing: `llmMetrics` stays kept-forever, so its substance is correct. What needs checking is its parenthetical claim that "only the runtime-firehose/build-history tables are physically deleted there."
    - `convex/dataRetention.ts:5-9` — the D-12 comment describing the `aggregates` rollups as "immutable historical buckets" that are "never read, patched, or deleted here."
    - `convex/costBudgetEval.ts:281` and `convex/costBudgetEval.test.ts:815` — verified NOT affected; both are about `alerts`, which stays kept-forever. Read them only to confirm, and leave them unmodified.
  </read_first>

  <behavior>
    - The keep-forever guard passes for `llmMetrics`, `sessions`, `alerts` and no longer names `aggregates`.
    - `PRUNE_PREDICATES.aggregates({ period: "daily" })` is `false` and `PRUNE_PREDICATES.aggregates({ period: "hourly" })` is `true`.
    - `RETENTION_DAYS.aggregates` is exactly `90`.
    - Every key of `PRUNE_PREDICATES` is a real table in `schema.ts` and is also a key of `RETENTION_DAYS` — a predicate for a table that is never pruned is a silent no-op of the same class the existing table-existence guard was written to catch.
  </behavior>

  <action>
**A. Narrow the D-03 guard.** In `convex/retention.test.ts:92-102`, change the loop's array from `["aggregates", "llmMetrics", "sessions", "alerts"]` to `["llmMetrics", "sessions", "alerts"]`, and rewrite its comment so it justifies the three remaining tables without claiming `aggregates` is among them. The class-level protection must survive in a form that matches the new design.

**B. Add the positive guard (D-03).** A new `it(...)` asserting `PRUNE_PREDICATES.aggregates` directly — the two behavioral assertions in `<behavior>` — plus `expect(RETENTION_DAYS.aggregates).toBe(90)` for D-04. Follow the D-13 exemption test's shape at lines 70-90: include a known-present control so the assertion cannot pass vacuously against an empty or mis-imported map, and assert the decision rationale is documented in place by reading `convex/retention.ts`'s own source for `D-01` the way that test reads it for `D-13`. Import `PRUNE_PREDICATES` alongside the existing `RETENTION_DAYS` import.

**C. Extend the silent-no-op guard over the predicate map.** A new `it(...)` asserting every `Object.keys(PRUNE_PREDICATES)` entry is in `schemaTables` AND in `Object.keys(RETENTION_DAYS)`. Reuse the file's existing `schemaTables` set — do not re-parse `schema.ts`.

**D. Sweep the stale comment sites.** Four sites beyond the header comment already fixed in Task 1:

  1. `convex/retention.test.ts:56-64` — the Phase 108 comment listing what the existing tests assert, including "a fixed keep-forever list". Update that phrase so it describes the narrowed list, leaving the rest of the paragraph's substance intact.
  2. `convex/aggregates.ts:743-745` — verify before editing. Its subject is `llmMetrics`, which remains kept-forever, so the main claim stands. The parenthetical "only the runtime-firehose/build-history tables are physically deleted there" is what to check against the post-Task-1 state: `aggregates: 90` sits in the build/history tier, so decide whether the sentence is still accurate as written, and if it now misleads, amend the parenthetical minimally to note that `aggregates` hourly rows are also physically deleted there while daily rows are not. Do not touch the `llmMetrics` substance or the `agentConfigs["retention_days"]` pointer.
  3. `convex/dataRetention.ts:5-9` — the "never read, patched, or deleted **here**" scoping stays correct and D-01 does not relax D-12; this module still must not touch `aggregates`. What goes stale is the unqualified "immutable historical buckets". Amend it to say the daily rollups are immutable historical buckets kept forever while hourly rollups now age out at 90 days via `convex/retention.ts`'s nightly prune, and keep the existing prohibition sentence verbatim.
  4. Re-grep the class after editing rather than trusting this list: `grep -rn "kept forever\|keep-forever\|keepForever\|immutable historical" convex/ src/ --include=*.ts --include=*.tsx`. Every remaining hit must be either about `llmMetrics`/`sessions`/`alerts` (still true) or already corrected. Record the full post-edit hit list in the task summary. Leave `convex/costBudgetEval.ts:281` and `convex/costBudgetEval.test.ts:815` unmodified — both are about `alerts`.
  </action>

  <verify>
    <automated>npx vitest run convex/retention.test.ts convex/retentionCursor.test.ts</automated>
  </verify>

  <acceptance_criteria>
    - `npx vitest run convex/retention.test.ts` exits 0 with a test count strictly greater than HEAD's, and every pre-existing test other than the narrowed guard passing unchanged.
    - `grep -c '"aggregates", "llmMetrics"' convex/retention.test.ts` returns `0`; `grep -c '"llmMetrics", "sessions", "alerts"' convex/retention.test.ts` returns `1`.
    - `grep -c "PRUNE_PREDICATES.aggregates" convex/retention.test.ts` returns at least `2` (the daily and hourly assertions).
    - Mutation check, run and recorded in the task summary: temporarily change `PRUNE_PREDICATES.aggregates` to `() => true`, confirm `npx vitest run convex/retention.test.ts` FAILS on the daily assertion, then restore. A guard that stays green when the predicate is made to delete daily rows is guarding nothing.
    - Second mutation check: temporarily add a bogus key `notATable: () => true` to `PRUNE_PREDICATES`, confirm the new silent-no-op test FAILS, then restore.
    - The post-edit sweep `grep -rn "kept forever\|keep-forever\|keepForever\|immutable historical" convex/ src/ --include=*.ts --include=*.tsx` is recorded verbatim in the summary, and no remaining hit asserts `aggregates` is kept forever or immutable.
    - `git diff HEAD -- convex/costBudgetEval.ts convex/costBudgetEval.test.ts` is empty.
    - `npx vitest run` (full suite) exits 0.
  </acceptance_criteria>

  <done>The keep-forever guard is narrowed rather than deleted, `aggregates` is protected by a mutation-tested positive guard against the predicate function itself, predicate keys inherit the silent-no-op protection, and no comment in the repo still describes `aggregates` as kept forever or immutable.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cron scheduler → `pruneBatchV3` | The only entry point. Fires from `convex/crons.ts:165-168` at 09:00 UTC with no arguments and no external input. Rescheduled invocations carry only integers this module itself produced. |
| `agentConfigs` row → chain control flow | New in this plan. The rotation cursor is operator-editable persisted state that is read back into the chain's start index — the one place in this phase where stored data influences what gets deleted. |
| `listRetentionPolicy` → CLI caller | New in this plan. A read of the policy map, callable from outside the mutation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-110-03-01 | Tampering | `agentConfigs["retention.rotationCursor"]` → `startNightlyPrune` | mitigate | A hand-edited or corrupt cursor could point past the end of the table array (silently pruning nothing every night) or at a negative index. Every read passes through `resolveRotationStart` (plan 110-01), which resolves any non-integer, negative, or `>= PRUNED_TABLES.length` value to `0` and never throws — bounded against whatever `RETENTION_DAYS` currently holds, evaluated fresh each run. Unit-tested across all seven malformed inputs. |
| T-110-03-02 | Denial of Service | `pruneBatchV3` delete loop | mitigate | Data loss and instance availability share a mechanism here. `BATCH_SIZE=200`, `RESCHEDULE_DELAY_MS=3000` and `MAX_BATCHES_PER_NIGHT=600` are unchanged, and the `by_creation_time` cursor-seek query is byte-unchanged — asserted in Task 1's criteria. Widening any of these is forbidden: per upstream `get-convex/convex-backend#495`, this backend's `index_scan` materializes the whole matched range before truncating, so a wider interval costs memory proportional to every row in it, not to the 200 kept. |
| T-110-03-03 | Tampering | `PRUNE_PREDICATES.aggregates` | mitigate | An inverted or removed predicate deletes Phase 104's re-priceable daily cost history irreversibly on the next nightly run, with no error and no operator signal. Mitigated by a positive guard asserted against the callable predicate rather than a comment, and by a required mutation check proving the guard fails when the predicate is made to delete daily rows. Backed by the DUR-01 live before/after count in plan 110-05, where a shrinking daily count is a hard phase-blocking fail. |
| T-110-03-04 | Information Disclosure | `listRetentionPolicy` | mitigate | Adds a new callable surface. Implemented as `internalQuery` by default so no new publicly-reachable endpoint is created; a downgrade to public `query` is permitted only if the CLI provably cannot reach the internal form, and must be recorded with the error that forced it. The returned data is a map of table names to day counts — no PII, no credentials, no row contents — so the residual exposure if it does go public is the retention policy itself, which is already documented in a git-tracked source file. |
| T-110-03-05 | Repudiation | terminal prune log line | mitigate | With rotation, `retention: all tables pruned` alone would attest to a complete pass for a run that covered only the tail of the table list — and DUR-02's evidence rests on that line. The appended start index and covered-of-total counts make the claim self-describing and falsifiable rather than trusting the reader's assumption that runs start at 0. |
| T-110-03-SC | Tampering | npm/pip/cargo installs | accept | No packages installed; no `package.json` or lockfile change. Asserted by an empty `git diff HEAD -- package.json package-lock.json` at plan close. |
</threat_model>

<verification>
- `npx vitest run` (full suite) exits 0.
- `npx tsc --noEmit` exits 0.
- `git diff HEAD -- package.json package-lock.json` is empty.
- `git diff HEAD -- convex/crons.ts` is empty — the cron entry and its 09:00 UTC schedule are unchanged by this phase.
- `git diff HEAD -- convex/analyticsRollup.ts` is empty — the second `aggregates` writer is documented, not modified.
- `grep -c "BATCH_SIZE = 200\|RESCHEDULE_DELAY_MS = 3000\|MAX_BATCHES_PER_NIGHT = 600" convex/retention.ts` returns `3` — the batch-capping constants are untouched.
- Both mutation checks from Task 3 recorded with their failing output.
</verification>

<success_criteria>
- `aggregates` is a 90-day `RETENTION_DAYS` entry whose hourly rows are prunable and whose daily rows are provably unreachable by the chain.
- The chain rides the existing sequential, batch-capped, cursor-seeked machinery — no second chain, no second batch budget, no index change.
- The rotation cursor is one `agentConfigs` row, patched not inserted, bounds-checked on every read.
- `listRetentionPolicy` is proven callable through the health check's own CLI invocation shape, with the transcript recorded.
- The terminal log line states its start index and coverage.
- No comment anywhere in the repo still claims `aggregates` is kept forever or immutable, proven by a recorded post-edit grep.
</success_criteria>

<output>
Create `.planning/phases/110-convex-durability/110-03-SUMMARY.md` when done. Include: the `listRetentionPolicy` CLI transcript, both mutation-check failures verbatim, whether `internalQuery` or `query` shipped, and the full post-edit comment sweep hit list.
</output>
