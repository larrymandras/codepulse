# Phase 110 Plan Review - Verdict

## VERIFICATION PASSED

Phase: 110 - Convex Durability
Plans checked: 6 (110-01 through 110-06)
Issues: 0 blocker(s), 2 warning(s)

This is a re-verification pass. All seven priority checks from the dispatch were traced against the live source (convex/retention.ts, convex/retentionCursor.ts, convex/retention.test.ts, convex/retentionCursor.test.ts, convex/schema.ts, convex/aggregates.ts, convex/dataRetention.ts, convex/analyticsRollup.ts, convex/crons.ts, convex/costBudgetEval.ts, convex/webhookDelivery.ts) and against live GitHub state (gh issue view / gh pr view on get-convex/convex-backend #495, #525, PR #522), not accepted from plan prose.

### Priority check results (all confirmed, no blockers found)

1. Cursor-stall fix ordering (Pitfall 1). Confirmed convex/retention.ts:159-165 today sets lastCreationTime unconditionally per-doc only because every doc is currently deleted unconditionally (no predicate yet) - so todays code coincidentally has no bug, but the moment a predicate is added, the fix must move lastCreationTime = doc._creationTime outside the delete-conditional. Plan 110-01 Task 1 specifies partitionBatchForPrune sets lastCreationTime from every doc, deleted or skipped (110-01-PLAN.md:109). Plan 110-01 Task 2 requires a mutation-tested all-skipped-batch regression that must fail if lastCreationTime is sourced from toDelete only (110-01-PLAN.md:183). Plan 110-03 Task 1.C wires partitionBatchForPrune into the delete loop in the same task that adds PRUNE_PREDICATES (110-03-PLAN.md:126-136) - the predicate and the fix ship atomically in wave 2, so there is no window where the predicate exists without the fix. Confirmed correct.

2. D-03 guard narrowing with justifying comments. Live-read convex/retention.test.ts:92-102 confirms the exact array containing aggregates, llmMetrics, sessions, alerts that plan 110-03 Task 3.A narrows to llmMetrics, sessions, alerts. Live-read convex/retention.ts:12-14 (header comment cited by the tests own docstring) confirms the kept forever sentence plan 110-03 Task 1.D corrects. Live-read convex/aggregates.ts:743-745 confirms the exact parenthetical text about only the runtime-firehose/build-history tables being physically deleted there, which plan 110-03 Task 3.D.2 flags for review. Live-read convex/dataRetention.ts:5-9 confirms the immutable historical buckets, never read patched or deleted here text plan 110-03 Task 3.D.3 amends. Live-grep confirmed convex/costBudgetEval.ts:281 and its neighboring lines concern alerts only, matching the plans explicit leave-unmodified instruction and its git diff emptiness check. All four sweep sites verified accurate.

3. DUR-03 three-state record. Live gh issue view 495 shows state OPEN, stateReason empty, author santigamo (non-maintainer). Live gh issue view 525 shows state OPEN, stateReason empty, author Sjotie (non-maintainer). Live gh pr view 522 shows state OPEN, mergedAt null (open, unmerged). All three match what plan 110-02 expects to find and instructs re-verifying at write-up time rather than hardcoding (110-02-PLAN.md:84, :100, :128). grep -c searchIndex-or-vectorIndex against convex/schema.ts returns 0, and grep -c for dot-index-open-paren against the same file returns 288 - confirms the number-525 ruling-out control the plan requires (110-02-PLAN.md:86, :99) produces exactly the result the plan expects.

4. Live-evidence controls. Confirmed present: DUR-01 opposite-failure control (hourly _creationTime moving forward or a nonzero aggregates-done-pruned count) in 110-06-PLAN.md:151. DUR-02 leg 2 three-way table-count cross-check in 110-05-PLAN.md:151, :168. The docker logs leg requires both --tail and --since explicitly, with a positive control and a known-absent control, in 110-06-PLAN.md:101-109, enforced in acceptance criteria at :121-124.

5. Sequencing realism. Deploy (wave 3, plan 110-04) and live-pass observation (wave 5, plan 110-06) are separated by wave 4 (plan 110-05, the health-check edit), so no plan observes a prune pass in the same wave it deploys. Plans 110-02, 110-04, 110-05, 110-06 are all autonomous:false with blocking human-verify checkpoints. Plan 110-06 objective explicitly states it cannot start until the 09:00 UTC cron has fired at least once (110-06-PLAN.md:37). Confirmed correct.

6. Threat model and secret hygiene. All 6 plans carry a threat_model block with STRIDE registers scoped to real components (verified non-boilerplate: for example, 110-01 DoS entry names the specific stall mechanism, not a generic template line). The redaction-preamble precedent from 108-ENGINE-05-EVIDENCE.md is cited by name in plans 110-02 and 110-04. npx convex env list is explicitly forbidden by name in the constraints and action text of plans 110-02 and 110-04, and referenced again in 110-05 threat model - confirmed no plan invokes it.

7. Live-instance safety. No plan issues a bulk-delete, bulk-patch, or import --replace-all. All aggregates probes are capped at take(1000) or lower (110-04-PLAN.md:103, :121; 110-06-PLAN.md:155), consistent with the live SystemTimeoutError observed at take(5001) for a greater-than-30-day window recorded in 110-CONTEXT.md:119. Plan 110-05 Task 1 requires a hashed pre-110.bak backup of the unversioned PS1 before editing, with the hash and a bounded diff captured as evidence (110-05-PLAN.md:105, :125-131) - matches CLAUDE.md operational rules and the fact convex-selfhost/ is not under git until Phase 113.

### Cross-checks beyond the priority list

- Requirement coverage: DUR-01 in 110-01/03/04/06 frontmatter; DUR-02 in 110-03/04/05/06; DUR-03 in 110-02. All three requirement IDs from REQUIREMENTS.md:40-42 and ROADMAP.md Phase 110 stub are covered, matching the already-verified decision-coverage gate result.
- Dependency graph: 110-01 (wave1, deps none) leads to 110-03 (wave2, depends on 110-01) leads to 110-04 (wave3, depends on 110-03) leads to 110-05 (wave4, depends on 110-04) leads to 110-06 (wave5, depends on 110-05); 110-02 (wave1, deps none) is independent, confirmed by its own objective text stating it shares no file with any of them. No cycles, no forward references, wave numbers consistent with max(deps)+1.
- Live-code fidelity of every quoted line: every file:line citation checked against a live read of convex/schema.ts (aggregates table 953-990, agentConfigs 261-266), convex/crons.ts (retention-prune at 165-168, 09:00 UTC, and confirmed no cron references analyticsRollup), convex/analyticsRollup.ts:273-292 (clearHistoricalBucketsPage), convex/webhookDelivery.ts:36-65 (setChannel, the get-existing-or-patch idiom), and convex/aggregates.ts:625-834 (the two insert-only backfill cursors named as the anti-pattern) all matched CONTEXT.md, RESEARCH.md and PATTERNS.md citations exactly - no drift found.
- Rotation-write placement (110-03 Task 2.C): traced the actual pruneBatchV3 control flow (cap-reached returns immediately; continue-table returns immediately; next-table and done both fall through the per-table log line first). The plan instruction to call planRotationWrite once, immediately after next is computed and before any branch, is correct: planRotationWrite returns null for continue-table and next-table (no write happens) and a value for cap-reached and done (write happens once), so no duplication and no per-batch write growth. No ambiguity found for an executor.
- Architectural tier compliance (Dim 7c): RESEARCH.md Architectural Responsibility Map places DUR-01 in API/Backend (Convex internalMutation), DUR-02 in Ops tooling plus API/Backend (Convex query), DUR-03 in Database/Storage (native Rust process). All six plans actual file changes land in the tier the map assigns. No mismatch.
- Cross-plan data contracts (Dim 9): the only shared data path is partitionBatchForPrune output feeding planNextPruneStep across plans 110-01 to 110-03; the consuming plan (110-03) uses the exact signature the producing plan (110-01) built, verified against 110-03 own interfaces block. No transform conflict.

### Warnings (should fix, execution may proceed)

1. [research_resolution] 110-RESEARCH.md Open Questions section is not marked resolved
- File: .planning/phases/110-convex-durability/110-RESEARCH.md:286-297
- The section heading reads "## Open Questions" with no "(RESOLVED)" suffix, and neither of the two listed questions carries an inline RESOLVED marker. Per Dimension 11, this is a real gap. Practical impact is low: Open Question 1 (whether the planner wants a new pure helper extracted for the rotation-cursor value computation) was answered by plan 110-01 actually extracting resolveRotationStart and planRotationWrite as pure, tested helpers - the recommended path. Open Question 2 (evidence file name) was answered by naming it 110-DUR-EVIDENCE.md, exactly RESEARCH.md own recommendation. Both questions were framed as discretion, not blocking decisions, and the plans took the recommended branch in both cases.
- Fix: add "(RESOLVED)" to the section heading, or inline RESOLVED markers per question citing the plans that resolved them, so a future reader does not have to re-derive that these were closed.
- Confidence: real (verified by direct read), low severity.

2. [nyquist_compliance] 110-VALIDATION.md own compliance frontmatter and sign-off checklist were never updated to reflect the finished plan set
- File: .planning/phases/110-convex-durability/110-VALIDATION.md:5 (nyquist_compliant: false) and :88-98 (all sign-off checkboxes unchecked, Approval: pending)
- Independently re-derived Dimension 8 (checks 8a through 8d) against all six PLAN.md files: every task, both auto and checkpoint types, carries an automated verify command (confirmed by direct read of every verify block across 110-01 through 110-06), no watch-mode flags appear anywhere, and no window of 3 consecutive implementation tasks lacks an automated verify. Substantively, the plan set satisfies Nyquist sampling. But the document own frontmatter and checklist still read as if written before planning happened (status: draft, wave_0_complete: false at lines 4-6 also unchanged), and nothing in this phase tasks updates it. If any downstream automation gates on this frontmatter literally rather than re-deriving compliance, it would either wrongly block execution or, worse, treat a stale false as authoritative.
- Fix: update 110-VALIDATION.md frontmatter to nyquist_compliant: true, wave_0_complete: true, status: planned or equivalent, and tick the sign-off checklist now that the plans exist and satisfy it - this is a documentation housekeeping item, not a plan defect.
- Confidence: real (verified by direct read), low severity - does not block execution since no plan task or gate in this set actually reads this frontmatter to make a go or no-go decision.

### Dropped findings

None. Every candidate issue considered during this review was substantiated against live code, live GitHub state, or live file content, so none were dropped as speculative. The two items above were kept as warnings rather than blockers because in both cases the underlying substance (research questions actually resolved by plan choices; Nyquist requirements actually satisfied by the plans) is sound - only the tracking artifact is stale.

### Recommendation

No blockers. Plans are ready for execution as-is. The two warnings are documentation hygiene items that can be fixed in passing (for example, during plan 110-01 execution) or deferred without risk to the phase goal.
