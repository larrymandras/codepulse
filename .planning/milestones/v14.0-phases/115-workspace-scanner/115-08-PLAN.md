---
phase: 115-workspace-scanner
plan: 08
type: execute
wave: 4
depends_on: ["115-01", "115-03", "115-07"]
files_modified:
  - hooks/workspaceScan.mjs
  - hooks/__tests__/workspaceScan.test.mjs
autonomous: true
requirements: []            # No mapped REQ-IDs for Phase 115 — traceability is via D-NN.
decisions: [D-04, D-05, D-12]

must_haves:
  truths:
    - "D-12's refusal is proven at the INTEGRATION level, not only as a pure function: with an invalid or absent approval, an injected postSnapshot spy is NEVER called, so no byte can leave the host before the gate passes."
    - "D-12's refusal has a passing integration control: with a valid approval the same entry point DOES call postSnapshot, so the never-called assertion is not satisfiable by an entry point that never posts at all."
    - "D-04 holds end to end: the workspace scanner POSTs through the shared hooks/ingestPost.mjs helper rather than re-implementing the bearer/timeout block."
    - "D-05's on-demand flag exists: the scanner runs as node hooks/workspaceScan.mjs with --dry-run, --approve and a default ingest mode, and works both as a direct run and as an ESM import."
    - "The nightly path never exits 0 on a silent failure: a refusal, an unreachable backend or a non-2xx response each produce a distinct non-zero exit code, because a hidden scheduled task swallows stdout."
  artifacts:
    - path: "hooks/workspaceScan.mjs"
      provides: "runWorkspaceScan entry point + isDirectRun CLI branch wiring config load, walk, report, gate and POST"
      exports: ["runWorkspaceScan"]
      contains: "postSnapshot"
  key_links:
    - from: "hooks/workspaceScan.mjs"
      to: "hooks/ingestPost.mjs"
      via: "postSnapshot, injectable via deps for the case-5 spy"
      pattern: "from \"\\./ingestPost\\.mjs\""
    - from: "hooks/workspaceScan.mjs"
      to: "hooks/workspaceApproval.mjs"
      via: "canonicalReportHash + isDryRunApproved gate before any network call"
      pattern: "isDryRunApproved"
---

<objective>
Wire the entry point: load config → walk → classify → build report → **gate** → POST. Then prove case 5
of `115-VALIDATION.md`'s mandatory five-case table — that the refusal happens *before* the network call.

Purpose: plan 115-03 proved the gate's pure functions refuse. That is necessary and not sufficient. D-12
requires the ingest path to hard-refuse, and the only way to know the refusal is actually wired ahead of
the `fetch` is to inject a `postSnapshot` spy and assert it was never called. `115-VALIDATION.md` fixes
this as case 5 and it is a hard acceptance requirement, not a suggestion.

Output: `hooks/workspaceScan.mjs` (entry point added), `hooks/__tests__/workspaceScan.test.mjs`
(integration suites added).

This plan completes the five-case table: cases 1-4 live in `hooks/__tests__/workspaceApproval.test.mjs`
(plan 115-03), case 5 lives here. Neither half alone satisfies D-12.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/115-workspace-scanner/115-CONTEXT.md
@.planning/phases/115-workspace-scanner/115-VALIDATION.md
@.planning/phases/115-workspace-scanner/115-RESEARCH.md

**Treat this plan as a DRAFT.** Verify every signature against the live modules from plans 115-01,
115-02, 115-03, 115-05 and 115-07 and correct this plan where it is wrong.
</context>

<interfaces>
<!-- Verified live 2026-08-12 for scanner.mjs; the rest are this phase's own earlier plans. -->

From plan 115-01: `postSnapshot(endpointUrl, ingestKey, body, deps = {})` in `hooks/ingestPost.mjs` —
takes the FULL endpoint URL, never throws, returns `{ ok, status }`.
(If plan 115-01 took its sanctioned fallback and left `scanner.mjs` untouched, `hooks/ingestPost.mjs`
still exists as a standalone module — import it either way. Read `115-01-SUMMARY.md` to find out which
happened before assuming.)

From plan 115-03: `canonicalReportHash`, `isDryRunApproved`, `buildApprovalMarkerContents`,
`REPORT_RELPATH` (`config/workspace-scan-report.json`), `APPROVAL_MARKER_RELPATH`
(`config/workspace-scan.approved.sha256`) in `hooks/workspaceApproval.mjs`. All three paths are
gitignored by plan 115-02.

From plan 115-07: `walkRoot`, `rollupRootResults`, `loadMountedSet`, `buildSnapshot`,
`buildDryRunReport`, `hashableView` in `hooks/workspaceScan.mjs`.

The `isDirectRun` idiom to copy, verified exact at `hooks/scanner.mjs:280-286`:

```
const __scanner_dirname = dirname(fileURLToPath(import.meta.url));
const isDirectRun = process.argv[1] && process.argv[1].replace(/\\/g, "/").includes("scanner.mjs");
if (isDirectRun) {
  const sessionId = process.argv[2] || "manual-scan";
  const dryRun = process.argv.includes("--dry-run");
```

Swap `"scanner.mjs"` for `"workspaceScan.mjs"` in the `.includes(...)` check. This is what lets the module
work both as `node hooks/workspaceScan.mjs` (D-05's scheduled task) and as an ESM import (its tests).

The URL-resolution idiom at `hooks/scanner.mjs:288-318` — `process.env.CODEPULSE_URL` first, then a
regex-matched read of `.env.local` for `CONVEX_SITE_URL`, falling back to `VITE_CONVEX_URL` with
`.convex.cloud` swapped to `.convex.site`. Reuse that resolution for continuity. **Note:** `.env.local`
is an env file, and this project's absolute rule is never to read `.env` files. `hooks/scanner.mjs`
already does exactly this read today for exactly this purpose, so following the existing precedent is
correct — but **never log, echo or print any value read from it**, and prefer
`process.env.CODEPULSE_URL` when set. If the env-file-guard hook blocks the read at runtime, treat a
failure to resolve a URL as a distinct non-zero exit with a clear message, not a crash.

**D-04's constraint, restated and STRONGER than CONTEXT.md's original wording.** `hooks/codepulse-hook.mjs:142-145`
does `const { runScan } = await import(scannerPath); await runScan(...)` on `SessionStart` — the call is
**awaited inline in the dispatcher**. Nothing in this plan may put a workspace walk on that path.
`hooks/workspaceScan.mjs` must not be imported by `hooks/codepulse-hook.mjs`, must not be referenced from
`hooks/scanner.mjs`, and must not register itself in any `settings.json` hook binding. This plan adds a
scheduled-task entry point only; plan 115-10 registers the task.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Wire runWorkspaceScan and the CLI branch with the gate ahead of the POST</name>
  <files>hooks/workspaceScan.mjs</files>
  <read_first>
    - `hooks/workspaceScan.mjs` — plan 115-07's walk/report functions. Read the whole file; you are
      appending to it.
    - `hooks/scanner.mjs:275-338` — the `isDirectRun` branch, the URL resolution, the key resolution, and
      the dry-run branch. Note that `scanner.mjs`'s dry-run only logs to stdout (`:320-332`), which is NOT
      sufficient for D-12 — a report that exists only in a terminal scrollback cannot be diffed or
      approved later. Treat `scanner.mjs`'s dry-run as the flag CONVENTION to reuse, not the behavior.
    - `hooks/ingestPost.mjs` and `115-01-SUMMARY.md` — the shared POST helper and whether the extraction
      or the fallback landed.
    - `hooks/workspaceApproval.mjs` — the gate functions and the two gitignored paths.
    - `hooks/workspaceConfig.mjs` — `loadWorkspaceConfig`.
  </read_first>
  <action>
Add to `hooks/workspaceScan.mjs`:

`export async function runWorkspaceScan(options = {}, deps = {})` following the injectable-deps pattern
at `hooks/scanner.mjs:33-38`. `options` is `{ mode: "ingest" | "dry-run" | "approve", codepulseUrl, ingestKey }`.
`deps` destructures with real defaults: `{ loadConfig = loadWorkspaceConfig, readdirSync, statSync, readFileSync, writeFileSync, existsSync, homedir, yamlLoad, postSnapshot, now = () => Math.floor(Date.now()/1000), logger = console }`.
**`postSnapshot` MUST be injectable** — that injection point is what makes case 5 testable at all
(the same shape as `scanner.mjs`'s `collectSkills` override).

Flow, in this exact order:

1. `const config = deps.loadConfig();` — if it throws (tracked config absent/malformed, which plan
   115-02 makes the one legitimate throw), catch it and return `{ status: "config-error", exitCode: 2 }`.
   Never let it propagate: the nightly launch swallows stdout, so an uncaught throw is invisible.
2. `const { mounted, ok: mountedOk } = loadMountedSet(config, deps);`
3. Walk every declared root; `rollupRootResults`.
4. `const report = buildDryRunReport({ config, rollup, mountedOk, generatedAt: deps.now() });`
5. `const reportHash = canonicalReportHash(hashableView(report));`
6. **Always write the report to `REPORT_RELPATH`**, in every mode including `ingest`. Two reasons:
   D-12's report must be diffable rather than scrollback-only, and after an unattended nightly run the
   written report is the evidence of what was classified. Write it with a 2-space `JSON.stringify` so a
   diff is readable, and include `reportHash` as a field in the WRITTEN file (it is excluded from
   `hashableView`, so writing it does not affect the hash — assert that in a test).
7. **Mode branch:**
   - `"dry-run"`: log a human summary (department counts, `totals.dirs`, `totals.withheldFiles`, the
     Unclassified root list with counts, every `warnings` entry, and the report path), print the
     `reportHash`, print the exact `--approve` command to run next, and return
     `{ status: "dry-run", reportHash, exitCode: 0 }`. **Do NOT call `postSnapshot`.**
   - `"approve"`: write `buildApprovalMarkerContents(reportHash, { approvedAt })` to
     `APPROVAL_MARKER_RELPATH` and return `{ status: "approved", reportHash, exitCode: 0 }`.
     **Do NOT call `postSnapshot`.** Refuse with a non-zero exit if `REPORT_RELPATH` does not already
     exist on disk — approving a report nobody wrote is approving nothing.
   - `"ingest"`: continue to step 8.
8. **THE GATE. This block must appear in source BEFORE any reference to `postSnapshot`, and there must be
   no code path from step 7 to step 9 that bypasses it.**
   - `const approvalRaw = deps.existsSync(APPROVAL_MARKER_RELPATH) ? deps.readFileSync(APPROVAL_MARKER_RELPATH, "utf8") : null;`
   - `if (!isDryRunApproved(reportHash, approvalRaw)) { logger.error(...); return { status: "refused", reportHash, exitCode: 3 }; }`
   - The error message must state: the report has not been approved, or has CHANGED since approval; the
     expected hash; the marker's current first line (or `<absent>`); and the two commands to run
     (`--dry-run` then `--approve`). Do **not** print any directory path or the report body in this
     message.
   - Return, do not `process.exit`, from inside `runWorkspaceScan` — the exit-code translation belongs in
     the CLI branch, so tests can assert the return value without the process dying.
9. `const resp = await deps.postSnapshot(\`${options.codepulseUrl}/workspace-ingest\`, options.ingestKey, buildSnapshot({ config, rollup, mountedOk, dryRunReportHash: reportHash, generatedAt: report.generatedAt }));`
   Return `{ status: resp.ok ? "ingested" : "post-failed", httpStatus: resp.status, reportHash, exitCode: resp.ok ? 0 : 4 }`.

Then the `isDirectRun` CLI branch, copying `hooks/scanner.mjs:280-338`'s shape:
- `const isDirectRun = process.argv[1] && process.argv[1].replace(/\\/g, "/").includes("workspaceScan.mjs");`
- Flags: `--dry-run`, `--approve`; default mode `ingest`. **D-05's on-demand flag.** If both `--dry-run`
  and `--approve` are present, refuse with a usage error and exit 2 rather than picking one.
- Resolve `codepulseUrl` and `ingestKey` using `hooks/scanner.mjs:288-318`'s existing idiom. Never print
  either value. If no URL resolves, exit 2 with a message naming `CODEPULSE_URL`.
- `runWorkspaceScan(...).then(r => { log a one-line result; process.exit(r.exitCode); })` and a `.catch`
  that logs and `process.exit(5)` — an unhandled rejection must not exit 0.
- **Distinct exit codes, because a hidden scheduled task swallows stdout and `LastTaskResult` is the only
  signal Larry gets:** `0` success, `2` config/usage error, `3` D-12 refusal, `4` POST failed
  (unreachable or non-2xx), `5` unexpected throw. Document the table in a comment AND in the SUMMARY —
  plan 115-10's log file and Larry's overnight check read these.
- **No shebang.**

Do not touch `hooks/codepulse-hook.mjs`, `hooks/scanner.mjs`, `.claude/settings.json` or
`.claude-alt/settings.json`. D-04's whole point is that this walk stays off the awaited SessionStart path.
  </action>
  <verify>
    <automated>npx vitest run hooks/__tests__/workspaceScan.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -Fc 'from "./ingestPost.mjs"' hooks/workspaceScan.mjs` returns `1` — D-04's shared helper is
      imported, not re-implemented.
    - `grep -c "fetch(\|AbortController" hooks/workspaceScan.mjs` returns `0` — no second POST
      implementation exists in this file.
    - **Source ordering:** the line number of `isDryRunApproved` is LOWER than the line number of the
      `postSnapshot` CALL (the import may appear earlier). State both line numbers in the SUMMARY. Verify
      with `grep -n "isDryRunApproved" hooks/workspaceScan.mjs` and
      `grep -n "deps.postSnapshot(\|postSnapshot(\`" hooks/workspaceScan.mjs`.
    - `postSnapshot` is reached from exactly ONE call site: `grep -c "postSnapshot(" hooks/workspaceScan.mjs`
      returns `2` (the destructured default plus the single call) — more than one call site means more
      than one path to bypass the gate.
    - `git diff --stat` does NOT list `hooks/codepulse-hook.mjs`, `hooks/scanner.mjs`,
      `.claude/settings.json` or `.claude-alt/settings.json` (D-04).
    - `node hooks/workspaceScan.mjs --dry-run` runs to completion against Larry's real tree, exits 0,
      writes `config/workspace-scan-report.json`, and does NOT post. Confirm no post by checking the
      output contains no HTTP status line and by confirming `config/workspace-scan.approved.sha256` does
      not exist yet (`git check-ignore` already proved both paths are ignored).
      **Record `totals.dirs`, `totals.files` and `totals.withheldFiles` from that run in the SUMMARY** —
      plan 115-09's attended review needs those numbers, and `115-RESEARCH.md`'s Pitfall 3 / Assumption A2
      depend on the directory count.
    - `node hooks/workspaceScan.mjs` (default ingest mode, no approval marker present) exits **3** and
      prints a refusal naming both commands. This is the real-world case-5 observation to pair with the
      unit-level spy assertion in Task 2.
    - `node hooks/workspaceScan.mjs --dry-run --approve` exits 2 with a usage error.
    - `head -c 2 hooks/workspaceScan.mjs` is not `#!`.
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>`runWorkspaceScan` loads config, walks, builds and writes the report, and refuses with exit 3 before any POST when the approval is absent or stale; `--dry-run` and `--approve` work as on-demand flags; the POST goes through the shared helper from a single call site; and no SessionStart-path file was touched.</done>
</task>

<task type="auto">
  <name>Task 2: D-12 case 5 — the integration control proving the refusal precedes the network call</name>
  <files>hooks/__tests__/workspaceScan.test.mjs</files>
  <read_first>
    - `hooks/workspaceScan.mjs` — the entry point you just wrote.
    - `.planning/phases/115-workspace-scanner/115-VALIDATION.md` § "Mandatory mutation test — D-12's
      refusal (BLOCKING)" — case 5's exact wording: "inject a `deps.postSnapshot` spy; run the real entry
      point with approval invalid → spy **never called** — refusal happens *before* `fetch()`".
    - `hooks/__tests__/workspaceApproval.test.mjs` — cases 1-4, so you know what is already covered and do
      not duplicate it.
    - `hooks/__tests__/scanner.test.mjs:33-70` — the `mkdtempSync` fixture idiom.
  </read_first>
  <action>
Add an integration suite to `hooks/__tests__/workspaceScan.test.mjs`. Build a `mkdtempSync` fixture tree
and a synthetic config pointing at it (inject `loadConfig` returning that config, so no real root is
touched). Inject a `postSnapshot` spy recording its call count and arguments.

Required cases — case (a) is the passing CONTROL:

- **(a) CONTROL — a VALID approval reaches the POST.** Run `--dry-run` mode to get `reportHash`, write a
  valid marker into the fixture, then run `ingest` mode. Assert the spy call count is `1`, the returned
  `status` is `"ingested"`, and the posted body's `dryRunReportHash` equals that hash.
  **Without this control, every never-called assertion below is satisfiable by an entry point that never
  posts at all.** Label it in a comment as load-bearing so nobody removes it.
- **(b) CASE 5 — marker ABSENT → spy NEVER called.** `existsSync` returns false for the marker. Assert
  spy call count is `0`, returned `status` is `"refused"`, `exitCode` is `3`.
- **(c) CASE 5 — marker STALE → spy NEVER called.** Approve report A, then mutate the fixture tree so the
  report content changes (add a visible file, which changes `fileCount` and therefore the hash), then run
  `ingest`. Assert spy call count `0` and `status: "refused"`. **This is the case that matters most in
  practice** — it is the "someone widened the allowlist and re-ran the nightly task" scenario.
- **(d) CASE 5 — marker CORRUPTED → spy NEVER called.** Marker contains `not-a-hash`. Spy count `0`.
- **(e) The refusal does not throw.** Every refusal case returns a value; assert none of (b)-(d) rejects.
  A throw under the nightly hidden-task launch is silently swallowed and would read as success.
- **(f) The refusal message leaks nothing.** Capture `logger.error` output in (b)-(d) and assert with
  fixed-string `.includes()` checks that it contains NEITHER the fixture's absolute tmp path NOR any
  fixture filename NOR the ingest key. Use `.includes(...)` with the literal string, never a
  hand-escaped backslash regex.
- **(g) `--dry-run` never posts even with a valid marker present.** Spy count `0`, `status: "dry-run"`.
  This proves the mode branch, not the gate, is what suppresses the post in dry-run.
- **(h) `--approve` never posts.** Spy count `0`, `status: "approved"`, and the marker file was written
  with the current report's hash on its first line.
- **(i) `--approve` refuses when no report exists on disk.** Non-zero exit; no marker written.
- **(j) `reportHash` is excluded from its own hash.** Read the written report file, confirm it contains a
  `reportHash` field, and confirm `canonicalReportHash(hashableView(parsedReport))` equals that field's
  value — i.e. including the hash in the written artifact did not change the hash.
- **(k) A failed POST is not reported as success.** Spy returns `{ ok: false, status: 500 }`; assert
  `status: "post-failed"` and `exitCode: 4`. **CONTROL:** the spy returning `{ ok: true, status: 200 }`
  yields `exitCode: 0` — so the non-zero assertion is not satisfiable by an always-failing path.

**Mutation proofs**, each recorded in the SUMMARY with the failing test names, each restored from a `cp`
backup (never `git checkout -- <file>`, which would discard this plan's other uncommitted edits):
1. Move the gate block to AFTER the `postSnapshot` call → cases (b), (c) and (d) must FAIL. This is the
   single most important mutation in the phase: it proves the tests measure ORDERING, not merely the
   returned status.
2. Change the gate to `if (false)` (never refuse) → cases (b), (c) and (d) must FAIL.
3. Change `isDryRunApproved`'s call to pass a constant `true` → same three must FAIL.
4. Change the refusal to `throw` instead of returning → case (e) must FAIL.
  </action>
  <verify>
    <automated>npx vitest run hooks/__tests__/workspaceScan.test.mjs hooks/__tests__/workspaceApproval.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run hooks/__tests__/workspaceScan.test.mjs hooks/__tests__/workspaceApproval.test.mjs`
      passes, and together they cover all FIVE cases of `115-VALIDATION.md`'s table. State the mapping
      case→test-name in the SUMMARY.
    - Case (a), the passing control, exists and asserts a spy call count of `1`.
    - Cases (b), (c), (d) each assert the spy call count is exactly `0` — not merely that `status` is
      `"refused"`. A status assertion alone does not prove the network call was prevented.
    - Mutation proof 1 (gate moved after the POST) was performed and cases (b)/(c)/(d) FAILED. **If they
      stayed green, the suite is not measuring ordering and must be strengthened before this task is
      done.** Record the failing test names.
    - All four mutation proofs recorded with failure counts; all four files restored (`git diff` shows
      only intended content, no leftover mutation).
    - Case (f)'s leak checks use `.includes(...)` with literal strings.
    - `npm test` exits 0.
    - `git status --porcelain` shows no stray fixture directories, `.bak` files, or an accidentally-created
      `config/workspace-scan.approved.sha256` left over from a test (tests must write markers into their
      own tmp fixtures, not into the repo's `config/`). If a real `config/workspace-scan.approved.sha256`
      exists at the end of this plan, delete it — plan 115-09 owns creating the real one, after Larry
      reviews the real report.
  </acceptance_criteria>
  <done>All five cases of the mandatory D-12 table are green across the two test files, the never-called assertions are on the spy's call count, and moving the gate after the POST has been shown to break exactly those cases.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| an unreviewed classification result → the network | The gate is the last control before bytes leave the host |
| the nightly hidden scheduled task → the operator | stdout is swallowed; only the exit code and the log file reach Larry |

## STRIDE Threat Register (ASVS L1, block on high)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-115-08-01 | Information Disclosure (HIGH) | an unreviewed or config-drifted snapshot transmitted | mitigate | The gate returns `refused` before the single `postSnapshot` call site; proven by an injected spy's call count of `0` in three refusal cases with a passing `1`-call control, and by a mutation proof that moving the gate after the POST breaks exactly those cases. Exactly one call site exists, asserted by grep. |
| T-115-08-02 | Tampering | a gate that reports green without evaluating anything | mitigate | Four mutation proofs: gate moved after the POST, `if (false)`, constant `true`, and refusal-as-throw. A green suite under any of these means the suite is not a gate. |
| T-115-08-03 | Information Disclosure | the refusal message echoing directory paths, filenames or the bearer key | mitigate | Case (f) asserts the fixture's absolute path, its filenames and the ingest key are all absent from `logger.error` output, using fixed-string checks. The URL/key resolution never logs either value. |
| T-115-08-04 | Information Disclosure | the written dry-run report committed to a PUBLIC repo | mitigate | `REPORT_RELPATH` and `APPROVAL_MARKER_RELPATH` are gitignored by plan 115-02 (`git check-ignore` verified there); tests write markers only into their own tmp fixtures; an acceptance criterion requires deleting any real marker created accidentally during this plan. |
| T-115-08-05 | Denial of Service | a workspace walk landing on the awaited SessionStart path | mitigate | D-04: `git diff --stat` must not list `hooks/codepulse-hook.mjs`, `hooks/scanner.mjs` or either `settings.json`. `hooks/codepulse-hook.mjs:142-145` awaits `runScan` inline, so anything slow there blocks the hook dispatcher on both launchers. |
| T-115-08-06 | Tampering / availability | a silent failure exiting 0 under the hidden nightly task | mitigate | Distinct non-zero exit codes (2 config/usage, 3 refusal, 4 POST failed, 5 unexpected throw), a `.catch` that exits 5 rather than letting an unhandled rejection exit 0, and case (k) with a success control. `LastTaskResult` is Larry's only unattended signal — but note plan 115-10 forbids treating it as proof the task RAN. |
| T-115-08-07 | Information Disclosure | reading `.env.local` for the Convex URL | accept | `hooks/scanner.mjs:288-318` already performs exactly this read for exactly this purpose; following the established precedent is correct rather than inventing a second mechanism. Mitigated by never logging any value read from it and by preferring `process.env.CODEPULSE_URL`. |
| T-115-08-SC | Tampering | npm installs | accept | Nothing installed; no `package.json` change. |
</threat_model>

<verification>
- `npx vitest run hooks/__tests__/` — all hook tests green.
- `npm test` — full suite green.
- `npx tsc --noEmit` — clean.
- `node hooks/workspaceScan.mjs --dry-run` — exits 0, writes the report, posts nothing. Record
  `totals.dirs` / `totals.files` / `totals.withheldFiles`.
- `node hooks/workspaceScan.mjs` with no marker — exits **3**.
- Source-ordering evidence: the `isDryRunApproved` line number and the `postSnapshot` call line number,
  both stated in the SUMMARY, former lower than latter.
- Four mutation proofs recorded with failing test names, all restored.
- `git status --porcelain` — no stray fixtures, no `.bak`, no real approval marker.
- `git diff --stat` — exactly `hooks/workspaceScan.mjs` and `hooks/__tests__/workspaceScan.test.mjs`.
</verification>

<success_criteria>
- All five cases of `115-VALIDATION.md`'s mandatory table are green.
- The refusal is proven to precede the network call by spy call count, not by returned status.
- Moving the gate after the POST demonstrably breaks the suite.
- `--dry-run` and `--approve` work as on-demand flags and neither posts.
- Distinct non-zero exit codes exist for every failure mode, and nothing on the SessionStart path was
  touched.
</success_criteria>

<output>
Create `.planning/phases/115-workspace-scanner/115-08-SUMMARY.md` when done. Record: the case→test-name
mapping for all five D-12 cases; the gate and POST line numbers; all four mutation-proof results with
failing test names; the exit-code table; and the real `--dry-run` numbers (`totals.dirs`, `totals.files`,
`totals.withheldFiles`) that plan 115-09 needs.
</output>
