---
phase: 115-workspace-scanner
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - convex/schema.ts
  - convex/workspace.ts
  - convex/workspace.test.ts
autonomous: true
requirements: []            # No mapped REQ-IDs for Phase 115 — traceability is via D-NN.
decisions: [D-10, D-11, D-13]

must_haves:
  truths:
    - "D-10 holds: new workspaceSnapshots + workspaceDirs tables copy graphSnapshots' versioned shape — entity rows keyed by (snapshotId, version) and the activeVersion pointer patched LAST — and graphSnapshotNodes is NOT reused or overloaded."
    - "D-10's crash-safety guarantee holds: a mid-ingest failure leaves the previous version still active and complete, because no reader can see the new version until the pointer flip."
    - "D-11 holds: version growth is bounded by an INLINE, batch-capped prune inside the same ingest mutation — never a cron, and never a mass delete on the self-hosted instance."
    - "D-11's prune candidate-selection performs no unbounded table read: the live version list is read from the meta document, so the failure mode that disabled sweepGraphSnapshotVersions cannot recur here."
    - "D-13 holds: one row per DIRECTORY, never per file; a directory carries aggregate counts and no filenames."
    - "The write path is an internalMutation, not a public mutation, so it is unreachable by an unauthenticated POST /api/mutation even from inside the tailnet."
  artifacts:
    - path: "convex/schema.ts"
      provides: "workspaceSnapshots (meta, by_snapshotId) + workspaceDirs (entity rows, by_snapshot_version)"
      contains: "workspaceDirs: defineTable"
    - path: "convex/workspace.ts"
      provides: "upsertWorkspaceSnapshot (internalMutation), getWorkspaceMap (query), selectVersionDeletes, WORKSPACE_KEEP_VERSIONS, WORKSPACE_DELETE_CAP"
      exports: ["upsertWorkspaceSnapshot", "getWorkspaceMap", "WORKSPACE_KEEP_VERSIONS"]
    - path: "convex/workspace.test.ts"
      provides: "Pure-logic mirror tests for version selection and prune bookkeeping, plus it.todo markers for DB-round-trip cases"
      contains: "selectVersionDeletes"
  key_links:
    - from: "convex/workspace.ts"
      to: "workspaceDirs by_snapshot_version index"
      via: "withIndex-scoped query — never a bare collect across versions"
      pattern: "withIndex\\(\"by_snapshot_version\""
    - from: "convex/workspace.ts"
      to: "internalMutation"
      via: "write path declared internal, per graphSnapshots.ts and loom.ts"
      pattern: "internalMutation\\("
---

<objective>
Add the versioned `workspace*` storage tables and the `internalMutation` that ingests a snapshot into
them with an inline, batch-capped prune.

Purpose: D-10 requires the proven versioned-write pattern (pointer flipped LAST) so a mid-scan crash
can never show a partial map, and explicitly rejects reusing `graphSnapshotNodes` — whose fixed fields
(`nodeId, label, type, community, source`, verified at `convex/schema.ts:1914-1921`) have nowhere for
`department`/`access`/`size`/`mtime`/`withheldCount`, so reuse would mean a lossy overload of a
contract Phase 83-87's `/graphs` hub already depends on. D-11 requires growth be bounded by
construction rather than by a schedule, because the schedule-based equivalent is **currently broken on
this exact backend**.

Output: `convex/schema.ts` (edited), `convex/workspace.ts` (new), `convex/workspace.test.ts` (new).

**Nothing in this plan is live until plan 115-09 deploys it.** `npx tsc --noEmit` and `npm test` pass
whether or not a deploy landed, because Convex types are generated from the config rather than read
from the live database. Do not report the tables as existing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/115-workspace-scanner/115-CONTEXT.md
@.planning/phases/115-workspace-scanner/115-RESEARCH.md
@.planning/phases/115-workspace-scanner/115-PATTERNS.md
@./CLAUDE.md

**`./CLAUDE.md` § "Self-Hosted Convex — Operational Rules" is BINDING here, from a real multi-day
outage.** Never `import --replace-all`; never bulk-delete or bulk-patch a large table on the live
instance — its MVCC tombstone GC cannot absorb one. D-11's cap is not a performance nicety.

**Treat the table shapes and the prune sketch in this plan as a DRAFT.** Field names and index choice
are explicitly Claude's Discretion per CONTEXT.md. Read the live `graphSnapshots.ts` and correct this
plan where it is wrong; say so in the SUMMARY.
</context>

<interfaces>
<!-- All verified live 2026-08-12. -->

`convex/graphSnapshots.ts` exports, confirmed:
- `:23` `export const GRAPH_SNAPSHOT_KEEP_VERSIONS = 7;`
- `:30` `export function selectVersionDeletes(versions: number[], keepN: number): number[]` — pure,
  zero-dependency, generically named. Sorts ascending and returns everything except the newest `keepN`.
- `:55` `export const upsertGraphSnapshot = internalMutation({`
- `:168` `export const sweepGraphSnapshotVersions = internalMutation({`

The 7-step write ordering to copy (`graphSnapshots.ts:84-155`):
1. Read the existing meta doc via the `by_snapshotId` index (`:86-89`).
2. `newVersion = (existing?.activeVersion ?? 0) + 1` (`:92`) — monotonic, never reused, never supplied
   by the producer (`upsertGraphSnapshot`'s args carry no `version` field, verified `:56-83`).
3-6. Insert entity rows for the new version in chunks of `CHUNK = 1000` (`:102-121`).
7. **LAST:** patch-or-insert the meta doc with the new `activeVersion` (`:137-153`). Its own comment:
   *"Step 7 is last: readers continue to see the complete previous version until the pointer flips."*
   This is the entire crash-safety guarantee. Never move it earlier.

**The defect in the analog you must NOT copy.** `sweepGraphSnapshotVersions`' candidate selection
(`graphSnapshots.ts:176-185`) derives its version list by collecting **every row for the snapshotId
across all versions**:

`ctx.db.query("graphSnapshotNodes").withIndex("by_snapshot_version", q => q.eq("snapshotId", meta.snapshotId)).collect()`

That is bounded only by total stored rows, and it is the exact read that makes the sweep time out on
this backend — `convex/crons.ts:145-151` has its registration commented out with
`DISABLED 2026-07-14 — times out on self-hosted Convex`, and `crons.ts:39-44` records the root cause:
*"markStaleArchived, evaluateInternal, and sweepGraphSnapshotVersions all hit the 15s syscall cap on
self-hosted Convex (single node, SQLite, 3.2M docs) and NEVER complete."*
`115-RESEARCH.md`'s Assumption A2 flags that this same read shape in the workspace prune could time out
if the directory count is large. **Task 2 eliminates that read entirely** by keeping the live version
list on the meta document. Do not carry the full-scan shape over.

The anti-pattern for the write path: `convex/registry.ts:130` declares `syncInventory` as a **public**
`mutation`, and `convex/scan.ts:23` calls it via `api.registry.syncInventory`. Per `./CLAUDE.md`, every
public Convex function is callable with no credential by anything that can route to the host — the
bearer check in `scan.ts:17` gates the HTTP route, not the mutation. Follow `graphSnapshots.ts`
(`internalMutation`, header comment: *"Writers are internalMutation — called from the /runtime-ingest
httpAction which has no Clerk identity"*) and `convex/loom.ts:141,242` (both `internalMutation`, citing
"v14.0 audit INT-03"). Do **not** add `ctx.auth` gating — `./CLAUDE.md`'s SEED-008 decision explicitly
rejected per-module auth hardening as the shape that was rejected; the tailnet is the auth boundary.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add the workspaceSnapshots and workspaceDirs tables</name>
  <files>convex/schema.ts</files>
  <read_first>
    - `convex/schema.ts:1877-1935` — the `graphSnapshots`/`graphSnapshotNodes`/`graphSnapshotLinks`
      block IN FULL, including the comment header at `:1880-1889` recording the 8,192-element
      array-field and ~1 MiB document limits that forced row-based storage. You are adding a sibling
      block; copy the commenting convention, not the fields.
    - `.planning/phases/115-workspace-scanner/115-RESEARCH.md` § "Standard Stack — the versioned-write
      contract to copy (D-10)" — the proposed field list and the Pitfall 1 side-channel constraint.
    - `.planning/phases/115-workspace-scanner/115-CONTEXT.md` § D-03, D-10, D-13.
  </read_first>
  <action>
Add a new table block to `convex/schema.ts`, placed as its own section with a comment header in the
same style as `:1877-1889`. The header must record: Phase 115; row-per-DIRECTORY not per-file (D-13,
citing the measured 21,029 files across just three roots); the versioned pointer-last contract (D-10);
the inline-prune-not-cron rationale (D-11, citing `crons.ts:145-151`); and Pitfall 1's side-channel
rule (`totalSize`/`fileCount` count VISIBLE files only, and there is deliberately no `withheldBytes`
field because a byte total is a far higher-resolution side channel than a count).

`workspaceSnapshots` — one meta row per `snapshotId`, indexed `.index("by_snapshotId", ["snapshotId"])`:

- `snapshotId: v.string()` — fixed, `"larry-workspace"`, matching `config/workspace.json`'s `snapshotId`.
- `activeVersion: v.number()` — monotonic, incremented per ingest, never reused.
- `storedVersions: v.array(v.number())` — **the versions whose `workspaceDirs` rows are still present.**
  Bounded by `WORKSPACE_KEEP_VERSIONS + 1` (≈4 elements), so nowhere near the 8,192-element limit. This
  field exists specifically so the prune never needs to scan entity rows to discover which versions
  exist — see Task 2 and Assumption A2.
- `generatedAt: v.float64()` — host scan time, epoch **seconds** (matching `graphSnapshots.ts`'s
  convention at `schema.ts:1907`). Record the unit in the field comment; this project has been bitten
  twice by seconds/millis confusion producing 1970 dates and vacuously-passing cutoff comparisons.
- `receivedAt: v.float64()` — Convex ingest time, epoch seconds.
- `rootCount: v.number()`.
- `coveredRoots: v.array(v.string())` — root ids whose walk actually, successfully enumerated.
- `scannedRootsComplete: v.boolean()` — false when any declared root failed to enumerate. **This pair is
  a load-bearing implied requirement, not polish**: it follows `hooks/skillScan.mjs:107-118`'s
  coverage-honest convention (*"Coverage now requires BOTH that at least one plugin was actually read
  AND that no plugin's read failed"*) and the `scannedOrigins`/`scannedOriginsComplete` pair already on
  the `/scan` wire. Without it, Phase 114 renders a partial map as if it were complete.
- `unclassifiedRootIds: v.array(v.string())` — D-14/D-15/D-16; small, bounded by root count.
- `accessDerivationOk: v.boolean()` — false when the compose parse failed and every directory fell back
  to `local-only` (D-09 / Pitfall 4). Without this, a silent parse failure is indistinguishable from a
  real change in Ástríðr's mount surface.
- `localConfigStatus: v.string()` — `"merged" | "absent" | "version-mismatch"`, from
  `hooks/workspaceConfig.mjs`. Makes D-17's fail-closed path visible rather than silent.
- `totalDirs: v.number()`, `totalFiles: v.number()`, `totalWithheldFiles: v.number()`,
  `totalBytes: v.float64()`.
- `dryRunReportHash: v.string()` — D-12; the approved report hash this ingest was gated on, recorded so
  the stored snapshot is traceable to a reviewed report.
- `prunedVersion: v.optional(v.number())` — the version the last ingest's inline prune targeted.
  Optional with a documented reason: absent on the very first ingest, when there is nothing to prune.
  (`schema.ts:1913`'s `community` comment is the precedent that every optional needs its own stated
  reason, not a defensive blanket.)
- `pruneIncomplete: v.boolean()` — true when the prune hit `WORKSPACE_DELETE_CAP` and left rows behind
  for the next ingest.

`workspaceDirs` — one row per DIRECTORY, indexed `.index("by_snapshot_version", ["snapshotId", "version"])`:

- `snapshotId: v.string()`, `version: v.number()`.
- `rootId: v.string()` — which declared root this directory belongs to (D-06).
- `dirPath: v.string()` — forward-slash relative to the root; `""` means the root itself.
- `department: v.string()` — one of `Work | Consulting | Personal | Unclassified` (D-07/D-14).
- `access: v.string()` — `"astridr-reachable" | "local-only"` (D-09). Named `access`, deliberately NOT
  `astridrCanRead`: it reflects DECLARED bind-mount coverage, not a live reachability probe
  (RESEARCH Pitfall 2). State that in the field comment so Phase 114 does not overclaim liveness.
- `fileCount: v.number()` — **VISIBLE files directly in this directory only**, never including withheld
  files and never including subdirectories' files.
- `totalSize: v.float64()` — bytes of VISIBLE files only (Pitfall 1).
- `latestMtime: v.float64()` — epoch seconds; max over visible files.
- `withheldCount: v.number()` — D-03; secret-classified files whose paths are never transmitted.
  Deliberately count-only.

Do NOT add a `withheldBytes` field, do NOT add per-file arrays, and do NOT add a `files` array field of
any kind — `schema.ts:1880-1883` records the exact limits that forced row-based storage here.

Add nothing to `convex/crons.ts`. D-11 rejects a cron outright.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0.
    - `grep -c "workspaceSnapshots: defineTable" convex/schema.ts` returns `1`; same for
      `workspaceDirs: defineTable`.
    - `grep -Fc 'index("by_snapshotId", ["snapshotId"])' convex/schema.ts` increased by 1 versus HEAD
      (`git show HEAD:convex/schema.ts | grep -Fc ...` as the baseline) — confirming the meta table got
      the pointer-read index.
    - `grep -Fc 'index("by_snapshot_version", ["snapshotId", "version"])' convex/schema.ts` increased by
      1 versus HEAD.
    - `grep -c "withheldBytes\|withheldPaths\|secretPaths" convex/schema.ts` returns `0` — D-03/Pitfall 1.
    - The `workspaceDirs` definition contains no `v.array(` — one row per directory means no array
      fields on the entity table: `node -e` or manual read confirming; state the check used.
    - `git diff --stat convex/crons.ts` is empty — no cron was registered (D-11).
    - `git diff convex/schema.ts` touches only the added block; no existing table definition is modified.
      Read the full diff and account for every deletion line; there should be none.
  </acceptance_criteria>
  <done>`convex/schema.ts` defines `workspaceSnapshots` (meta, `by_snapshotId`) and `workspaceDirs` (one row per directory, `by_snapshot_version`), with a comment header recording D-10/D-11/D-13 and the side-channel rule, and no cron was added.</done>
</task>

<task type="auto">
  <name>Task 2: Write upsertWorkspaceSnapshot with the inline batch-capped prune</name>
  <files>convex/workspace.ts</files>
  <read_first>
    - `convex/graphSnapshots.ts` IN FULL (301 lines). You need `:23-34` (the keep-versions constant and
      `selectVersionDeletes`), `:40-155` (`upsertGraphSnapshot`'s 7-step ordering), and `:160-224` (the
      sweep, whose candidate-selection read you must NOT copy — see this plan's `<interfaces>`).
    - `convex/loom.ts:135-170` — the `internalMutation` discipline and its INT-03 header comment.
    - `convex/crons.ts:39-51` and `:145-151` — the recorded root cause of the timeout and the disabled
      registration. Read this before deciding any read shape in the prune.
    - `convex/schema.ts` — your Task 1 block, so the mutation's validators match the table exactly.
    - `./CLAUDE.md` § "Self-Hosted Convex — Operational Rules".
  </read_first>
  <action>
Create `convex/workspace.ts`.

Constants:
- `export const WORKSPACE_SNAPSHOT_ID = "larry-workspace";`
- `export const WORKSPACE_KEEP_VERSIONS = 3;` — deliberately not `graphSnapshots`' 7. The 7 exists for
  Phase 87's temporal diffing, which actually reads old versions; nothing in Phase 115 or 114's scope
  reads a version other than the active one (Phase 114 renders "the map", singular, present tense). 3
  gives one rollback buffer past active while minimising both steady-state rows and the size of the
  single-version delete this runs nightly. Record that rationale in a comment.
- `export const WORKSPACE_DELETE_CAP = 4000;` — headroom under the ~16,000-doc per-mutation write
  ceiling this repo's own comments record (`graphSnapshots.ts:161,193`), leaving room for THIS ingest's
  own inserts, which happen first in the same invocation. That shared budget is why the cap is lower
  than the sweep's 15,000: the sweep had the whole budget to itself.
- `const CHUNK = 1000;` — matching `graphSnapshots.ts:102`.
- Re-export or copy `selectVersionDeletes`. Prefer `import { selectVersionDeletes } from "./graphSnapshots";`
  — it is already exported, pure, zero-dependency and generically named. Do NOT import
  `GRAPH_SNAPSHOT_KEEP_VERSIONS`; the constant must not be shared. If importing across domain modules
  is undesirable on reading the file, copy the four-line function verbatim with an attribution comment
  and re-export it so `convex/workspace.test.ts` can test the copy actually used.

`export const upsertWorkspaceSnapshot = internalMutation({ args: {...}, handler: ... })`:

Args, all with `v.*` validators (V5 input validation) — mirroring `graphSnapshots.ts:56-83`'s style:
`snapshotId: v.string()`, `generatedAt: v.float64()`, `rootCount: v.number()`,
`coveredRoots: v.array(v.string())`, `scannedRootsComplete: v.boolean()`,
`unclassifiedRootIds: v.array(v.string())`, `accessDerivationOk: v.boolean()`,
`localConfigStatus: v.string()`, `dryRunReportHash: v.string()`, and
`dirs: v.array(v.object({ rootId, dirPath, department, access, fileCount, totalSize, latestMtime, withheldCount }))`
with the matching `v.*` types. **The producer sends NO `version` field** — version allocation is
server-side only, exactly as `graphSnapshots.ts` does it.

Handler, in this order — the ORDER is the crash-safety contract:

1. Read the existing meta doc: `ctx.db.query("workspaceSnapshots").withIndex("by_snapshotId", q => q.eq("snapshotId", args.snapshotId)).unique()`.
2. `const newVersion = (existing?.activeVersion ?? 0) + 1;`
3. Insert `workspaceDirs` rows for `newVersion` in chunks of `CHUNK`. Skip `graphSnapshots.ts:94-100`'s
   dangling-reference filter — directories have no cross-reference integrity concern the way links
   reference nodes, and both D-10 and RESEARCH say this step is a no-op here. Do not add one just to
   mirror the shape.
4. Compute derived totals from `args.dirs`: `totalDirs = args.dirs.length`,
   `totalFiles = sum(fileCount)`, `totalWithheldFiles = sum(withheldCount)`, `totalBytes = sum(totalSize)`.
   Derive them here rather than trusting producer-supplied totals, so a producer bug cannot make the
   stored aggregate disagree with the stored rows.
5. **LAST (the pointer flip):** patch-or-insert the meta doc with `activeVersion: newVersion` and
   `storedVersions: [...(existing?.storedVersions ?? []), newVersion]` plus every other meta field.
   Nothing before this point is visible to a reader; a crash anywhere in steps 1-4 leaves the previous
   version still active and complete. Never move this earlier.
6. **INLINE PRUNE (D-11), after the flip:**
   - `const toDelete = selectVersionDeletes(storedVersionsAfterFlip, WORKSPACE_KEEP_VERSIONS);`
     `storedVersionsAfterFlip` is the array you just wrote — **read from the meta document, NOT from a
     query over `workspaceDirs`.** This is the whole A2 mitigation: the candidate-selection read is
     O(keepN) array elements, not O(stored rows), so the read that times out
     `sweepGraphSnapshotVersions` has no analogue here.
   - If `toDelete.length === 0`, patch `prunedVersion: undefined`/`pruneIncomplete: false` as
     appropriate and return.
   - Otherwise take `const versionToDelete = toDelete[0]` — **exactly ONE version per ingest, never
     more.** Query only that slice:
     `ctx.db.query("workspaceDirs").withIndex("by_snapshot_version", q => q.eq("snapshotId", args.snapshotId).eq("version", versionToDelete))`.
     Iterate and `ctx.db.delete` up to `WORKSPACE_DELETE_CAP` docs, counting as you go and breaking at
     the cap.
   - If the cap was hit: leave `versionToDelete` IN `storedVersions` and set `pruneIncomplete: true`, so
     the next ingest re-selects the same version and finishes it. **Never raise the cap to "finish it
     this time"** — that is the mass delete `./CLAUDE.md` forbids on this instance.
   - If the version was fully deleted: patch the meta doc a SECOND time with `versionToDelete` removed
     from `storedVersions`, `prunedVersion: versionToDelete`, `pruneIncomplete: false`.
   - Document the crash window explicitly in a comment: a failure between the delete loop and this
     second patch leaves a stale entry in `storedVersions` pointing at a partially-deleted version. That
     is SAFE and self-healing — the next ingest's `selectVersionDeletes` selects it again and finishes
     it, and `activeVersion` is never affected. Idempotent by construction.

Also add `export const getWorkspaceMap = query({...})` returning the ACTIVE version's rows plus the meta
doc, or `null` before any snapshot has been ingested (graceful-skip, matching
`graphSnapshots.ts:236`'s `getProjectGraph`). Query `workspaceDirs` only through
`by_snapshot_version` with BOTH `snapshotId` and `version` pinned — never a bare `.collect()` across
versions. This is Phase 114's read contract; a public `query` is correct here (reads are public and
graceful-skip throughout this repo), only the WRITE must be internal.

Add a header comment recording: writers are `internalMutation` because the httpAction has no Clerk
identity (same rule as `graphSnapshots.ts` / `forge.appendLogChunk`); do NOT change it to a public
`mutation` the way `registry.syncInventory` is; and do NOT add `ctx.auth` gating (SEED-008 rejected
per-module hardening — the tailnet is the boundary).
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0.
    - `grep -c "internalMutation(" convex/workspace.ts` returns `1` and
      `grep -vc '^\s*[/*]' <(grep -n "= mutation(" convex/workspace.ts)` finds nothing — i.e.
      `grep -c "= mutation(" convex/workspace.ts` returns `0`. No public write path exists.
    - `grep -Fc 'internal' convex/workspace.ts` > 0 and the file imports `internalMutation` from
      `./_generated/server`.
    - The prune's candidate selection reads `storedVersions` from the meta doc. Verify by source
      assertion: within the prune block there is **no** `ctx.db.query("workspaceDirs")` call whose
      `withIndex` pins only `snapshotId`. Concretely, every `withIndex("by_snapshot_version", ...)` in
      the file must chain `.eq("version",` — check with
      `grep -c 'by_snapshot_version' convex/workspace.ts` and confirm the same count of `.eq("version"` occurrences.
    - `grep -Fc 'WORKSPACE_DELETE_CAP' convex/workspace.ts` >= 2 (definition plus the cap check).
    - `grep -c "WORKSPACE_KEEP_VERSIONS = 3" convex/workspace.ts` returns `1`.
    - `grep -c "GRAPH_SNAPSHOT_KEEP_VERSIONS" convex/workspace.ts` returns `0` — the constant must not
      be shared.
    - The pointer-flip patch precedes the prune in source order. State the two line numbers in the
      SUMMARY as evidence.
    - `git diff --stat convex/crons.ts` is empty.
    - `grep -c "ctx.auth" convex/workspace.ts` returns `0` (SEED-008).
  </acceptance_criteria>
  <done>`upsertWorkspaceSnapshot` is an `internalMutation` that chunk-inserts the new version, flips the pointer LAST, then prunes exactly one old version capped at 4,000 deletes with the remainder deferred, selecting candidates from the meta doc rather than from any table scan.</done>
</task>

<task type="auto">
  <name>Task 3: Pure-logic mirror tests plus it.todo markers for the DB-round-trip cases</name>
  <files>convex/workspace.test.ts</files>
  <read_first>
    - `convex/graphSnapshots.test.ts` — the whole file. It is the exact style template: pure mirror
      functions, plain `expect()`, zero DB round-trip, plus `it.todo(...)` markers at `:275-279` for the
      cases that genuinely need a live Convex.
    - `convex/workspace.ts` — the module under test.
    - `.planning/phases/115-workspace-scanner/115-VALIDATION.md` § "Deferred to live verification".
  </read_first>
  <action>
Create `convex/workspace.test.ts`. `convex-test` is NOT installed in this repo, so no DB round-trip is
available; the established pattern is pure mirror tests plus `it.todo` markers deferred to the attended
live wave (plan 115-09).

Real, runnable tests:

1. **`selectVersionDeletes` against `WORKSPACE_KEEP_VERSIONS`** — a near-verbatim swap of the 7-case
   table at `graphSnapshots.test.ts:48-83`: empty input; exactly `keepN` versions; below `keepN`;
   exceeds `keepN`; unsorted input; keeps the N NEWEST; `keepN = 1`. Assert against `3`, not `7`, and
   assert `WORKSPACE_KEEP_VERSIONS === 3` explicitly so a silent constant change fails a test.
2. **Prune bookkeeping mirror function** — write a pure helper in the test file mirroring step 6's
   `storedVersions` arithmetic, e.g.
   `nextStoredVersions(stored, versionToDelete, deletedCount, cap)`, and test:
   - Full delete (`deletedCount < cap`) → `versionToDelete` removed, `pruneIncomplete === false`.
   - Cap hit (`deletedCount === cap`) → `versionToDelete` RETAINED, `pruneIncomplete === true`.
   - Idempotence: applying the cap-hit result again with a smaller remaining count completes the
     removal — proving the deferred remainder self-heals rather than accumulating.
   - **Control:** with `stored.length <= keepN`, nothing is selected and `storedVersions` is unchanged.
     Without this the other three assertions are satisfiable by a function that always defers.
3. **Derived totals mirror** — a pure `deriveTotals(dirs)` mirroring step 4; assert
   `totalFiles`/`totalWithheldFiles`/`totalBytes`/`totalDirs` over a small fixture, and assert that
   `withheldCount` contributes to `totalWithheldFiles` and NOT to `totalFiles` or `totalBytes`
   (Pitfall 1's side-channel rule, tested rather than assumed).
4. **Version monotonicity mirror** — `nextVersion(existingActiveVersion | undefined)` returns 1 from
   `undefined` and `n+1` otherwise; assert it never returns a value already in `storedVersions`.

`it.todo(...)` markers, one per line, worded so plan 115-09 can convert each into a live probe:
- `"upsertWorkspaceSnapshot re-POST same snapshotId → activeVersion increments to 2, never two active versions (DB round-trip)"`
- `"the inline prune deletes the OLDEST version's rows and never the active version's rows (DB round-trip)"`
- `"a crash between the delete loop and the second meta patch self-heals on the next ingest (DB round-trip)"`
- `"getWorkspaceMap returns null before any ingest and the active version's rows after (DB round-trip)"`
  </action>
  <verify>
    <automated>npx vitest run convex/workspace.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run convex/workspace.test.ts` passes with 0 failures and reports the `it.todo` count.
    - `grep -c "it.todo(" convex/workspace.test.ts` returns `>= 4`.
    - `grep -Fc "WORKSPACE_KEEP_VERSIONS" convex/workspace.test.ts` >= 1 — the constant is imported from
      the module under test, not re-typed as a literal.
    - The prune-bookkeeping suite contains the "nothing selected" control (criterion 2's control), and
      the totals suite asserts `withheldCount` does NOT reach `totalFiles`/`totalBytes`.
    - **Mutation proof:** temporarily change `WORKSPACE_KEEP_VERSIONS` from `3` to `7` in
      `convex/workspace.ts`, re-run `npx vitest run convex/workspace.test.ts`, confirm it FAILS, then
      restore from a `cp` backup (not `git checkout -- <file>`, which would discard this plan's other
      uncommitted edits to that file). Record the failure count in the SUMMARY.
    - `npm test` exits 0.
  </acceptance_criteria>
  <done>Pure mirror tests cover version selection, prune bookkeeping including the deferred-remainder self-heal, derived totals including the side-channel rule, and version monotonicity; four DB-dependent cases are recorded as `it.todo` for plan 115-09; and the suite is proven to fail when the keep-versions constant changes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Convex public function namespace → the database | Every PUBLIC Convex function is callable with no credential by anything that can route to the host (`./CLAUDE.md`, measured 2026-08-11) |
| a single ingest mutation → the single-node self-hosted SQLite backend | An unbounded write or delete here takes the whole dashboard down; this has happened |

## STRIDE Threat Register (ASVS L1, block on high)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-115-04-01 | Tampering (V4 Access Control — the load-bearing control) | `upsertWorkspaceSnapshot` | mitigate | Declared `internalMutation`, reachable only via `ctx.runMutation(internal.workspace.*)` from the httpAction. Acceptance asserts `grep -c "= mutation(" convex/workspace.ts` is `0`. This is strictly better than `convex/scan.ts`'s existing shape, where `api.registry.syncInventory` is public and the bearer check gates only the route. |
| T-115-04-02 | Denial of Service | mass delete / tombstone storm on the self-hosted instance | mitigate | ONE old version per ingest, capped at `WORKSPACE_DELETE_CAP = 4000`, remainder deferred to the next ingest, never expanded. Inline, no cron, no scheduler hop. Enforces `./CLAUDE.md`'s "never bulk-delete on the live instance". |
| T-115-04-03 | Denial of Service | the prune's own candidate-selection read timing out (RESEARCH Assumption A2) | mitigate | `storedVersions` on the meta doc replaces the full-index `.collect()` that `graphSnapshots.ts:176-185` uses and that `crons.ts:145-151` records as timing out on this backend. Acceptance asserts every `by_snapshot_version` query pins `version`. |
| T-115-04-04 | Information Disclosure | directory aggregate size as a side channel to a withheld file | mitigate | `fileCount`/`totalSize` cover VISIBLE files only; `withheldCount` is deliberately count-only and no `withheldBytes` field exists. Asserted by a schema grep AND by the `deriveTotals` test. |
| T-115-04-05 | Tampering | a partial ingest rendered as a complete map | mitigate | Pointer-last ordering (step 5 after step 3); acceptance requires the two source line numbers as evidence. Plus `scannedRootsComplete`/`coveredRoots` so a partial WALK is also visible rather than silent. |
| T-115-04-06 | Spoofing | a producer supplying its own version number to overwrite history | mitigate | The mutation's args carry no `version` field; version allocation is server-side `activeVersion + 1` only, mirroring `graphSnapshots.ts`. |
| T-115-04-07 | Input Validation (V5) | malformed rows from a compromised or buggy producer | mitigate | `v.*` validators on every arg including the nested `dirs` object shape; totals are DERIVED server-side rather than trusted from the producer, so a producer bug cannot make the aggregate disagree with the rows. |
| T-115-04-08 | Elevation of Privilege | adding `ctx.auth` gating to "harden" this module | accept | Deliberately NOT done. `./CLAUDE.md` SEED-008 explicitly rejected per-module auth hardening as the shape that was rejected; the tailnet plus the LAN firewall block is the boundary. Acceptance asserts `grep -c "ctx.auth"` is `0`. |
| T-115-04-SC | Tampering | npm installs | accept | Nothing installed; no `package.json` change. |
</threat_model>

<verification>
- `npx tsc --noEmit` — clean.
- `npx vitest run convex/workspace.test.ts` — green, with `it.todo` count reported.
- `npm test` — full suite green.
- `git diff --stat` — exactly `convex/schema.ts`, `convex/workspace.ts`, `convex/workspace.test.ts`.
  `convex/crons.ts` must be absent from the diff.
- Read the full `git diff convex/schema.ts` and account for every deletion line. There should be none —
  this plan only appends a block.
- **Do NOT run `npx convex deploy` here.** The deploy is plan 115-09's [BLOCKING] task, and running it
  without `--env-file` can target the retired cloud deployment. Also do NOT run `npx convex env list`
  at any point — against the self-hosted backend it prints full `NAME=VALUE` pairs into the transcript.
</verification>

<success_criteria>
- The two `workspace*` tables exist in `convex/schema.ts` with the correct indexes and no array field
  on the entity table.
- `upsertWorkspaceSnapshot` is internal, flips the pointer last, and prunes one capped version inline
  with candidate selection that reads no entity rows.
- Pure mirror tests are green and four DB-dependent behaviors are recorded as `it.todo`.
- No cron was registered and no public write path exists.
</success_criteria>

<output>
Create `.planning/phases/115-workspace-scanner/115-04-SUMMARY.md` when done. Include the pointer-flip
and prune line numbers, both mutation-proof failure counts, and any place this plan's draft field list
was wrong. State explicitly that the schema is NOT yet live and names plan 115-09 as the deploy owner.
</output>
