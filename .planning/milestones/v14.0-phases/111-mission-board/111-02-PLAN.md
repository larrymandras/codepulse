---
phase: 111-mission-board
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/control-center/ActiveAgentsPanel.tsx
  - src/components/control-center/ActiveAgentsPanel.test.tsx
  - src/pages/Chat.tsx
  - src/pages/Chat.test.tsx
autonomous: true
requirements: [MISSION-03]

must_haves:
  truths:
    - "D-01: the fix stays frontend-only — no astridr-repo file and no file under convex/ is modified by this plan"
    - "D-07: ActiveAgentsPanel no longer exists anywhere in src/, and the Chat page no longer renders the unfalsifiable claim 'No agents running.'"
    - "D-13: MissionTimelinePanel's mount at Chat.tsx:1124 and its SectionErrorBoundary are untouched — the deletion does not reach the neighbouring panel that merely shares the word Mission"
    - "The cc-left-rail contains exactly one child — Intelligence Feed — with no empty SectionErrorBoundary, placeholder card, disabled panel, or coming-soon tile in the removed panel's place"
    - "Chat.test.tsx asserts six command-center panels and contains no seven-panel count language, so no test claims a panel count the page no longer has"
    - "Operator opening the Chat page in command-center mode sees no panel asserting agent liveness that subagentJobs cannot back"
  artifacts:
    - path: "src/pages/Chat.tsx"
      provides: "Command-center left rail with the permanently-false liveness panel removed"
      contains: "cc-left-rail"
    - path: "src/pages/Chat.test.tsx"
      provides: "Six-panel command-center mount coverage, with no stale seven-panel claim"
      contains: "COMMAND_CENTER_PANEL_LABELS"
  key_links:
    - from: "src/pages/Chat.tsx"
      to: "IntelligenceFeedPanel"
      via: "SectionErrorBoundary name=\"Intelligence Feed\" — the rail's sole remaining child"
      pattern: "SectionErrorBoundary name=\"Intelligence Feed\""
---

<objective>
Delete `ActiveAgentsPanel` outright — the component, its test, its import, and its mount plus the
`SectionErrorBoundary` wrapping it — and repair every downstream reference the deletion exposes in
`Chat.test.tsx`.

Purpose (D-07): `ActiveAgentsPanel.tsx:35` filters `job.status === "running"` against a table that
structurally never receives a `running` row (`convex/runtimeIngest.ts:594-596` routes terminal-state
events only, and the live probe found zero `running` rows in 7 lifetime rows). It therefore renders
"No agents running." permanently and unconditionally — an unfalsifiable positive claim, which is a
stronger version of the defect MISSION-03 forbids than a merely zeroed figure. UI-SPEC resolves
D-07 as **delete, not re-source**: the only other candidate feed (`useSwarmGraph`) is goal-scoped
and identity-less, and no feed carrying genuine agent liveness exists without astridr-repo work
that D-01 places out of scope.

Output: two files deleted, two files edited, zero replacement affordance.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/111-mission-board/111-CONTEXT.md
@.planning/phases/111-mission-board/111-UI-SPEC.md
@.planning/phases/111-mission-board/111-PATTERNS.md

@src/components/control-center/ActiveAgentsPanel.tsx

<interfaces>
<!-- The complete verified reference set. Extracted from the repo — do not go hunting for more,
     but DO re-run the grep in the acceptance criteria to prove none was missed. -->

Repo-wide `grep -rn ActiveAgentsPanel src/` (8 hits, verified 2026-08-10):
  src/components/control-center/ActiveAgentsPanel.tsx   :2, :33, :72   — the component (73 lines)
  src/components/control-center/ActiveAgentsPanel.test.tsx :9, :24, :36, :50, :59, :67 — its test (72 lines)
  src/pages/Chat.tsx                                    :41            — the import
  src/pages/Chat.tsx                                    :1054          — the mount
  src/pages/Chat.test.tsx                               :762           — a doc comment

Plus the label-array fallout in src/pages/Chat.test.tsx, which does NOT contain the string
"ActiveAgentsPanel" and is therefore invisible to that grep (verified separately):
  :779  const SEVEN_PANEL_LABELS = [        — the constant declaration
  :781    "ACTIVE AGENTS",                  — the entry to remove
  :852, :869, :950                          — THREE usage sites of the constant
  :842, :857, :948                          — THREE test titles whose prose says "seven"

Current Chat.tsx:1046-1056 (the rail — `flex flex-col gap-2` handles a single child unaided):
  <div data-testid="cc-left-rail" className="flex flex-col gap-2 lg:flex-row … xl:col-span-1 …">
    <SectionErrorBoundary name="Intelligence Feed"><IntelligenceFeedPanel /></SectionErrorBoundary>
    <SectionErrorBoundary name="Active Agents"><ActiveAgentsPanel /></SectionErrorBoundary>
  </div>

Deletion precedent in this repo — commit cff6d866 deleted BrainsWsRegistrar.tsx +
BrainsWsRegistrar.test.tsx + its single App.tsx mount in ONE commit, and fixed every downstream
test the deletion broke rather than leaving them red. That is the discipline to follow here.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete ActiveAgentsPanel, its test, its import, and its mount plus wrapper (D-07)</name>

  <files>src/components/control-center/ActiveAgentsPanel.tsx, src/components/control-center/ActiveAgentsPanel.test.tsx, src/pages/Chat.tsx</files>

  <read_first>
    - `src/components/control-center/ActiveAgentsPanel.tsx` (all 73 lines) — read it before
      deleting, per the project rule that a "duplicate/dead" verdict is a claim until the contents
      are inspected. Confirm for yourself that line 35's `status === "running"` filter is the whole
      of its data logic and that nothing else in it is worth preserving.
    - `src/components/control-center/ActiveAgentsPanel.test.tsx` (all 72 lines) — confirm all four
      `it()` blocks assert only behavior that ceases to exist (`agent-tile` testid,
      `No agents running.`, `ACTIVE AGENTS` header)
    - `src/pages/Chat.tsx` lines 35-45 (import block) and 1040-1060 (the rail)
    - `.planning/phases/111-mission-board/111-UI-SPEC.md` — the "D-07 resolution" section
    - `.planning/phases/111-mission-board/111-PATTERNS.md` — the `cff6d866` deletion-precedent diff
  </read_first>

  <action>
Delete `src/components/control-center/ActiveAgentsPanel.tsx` and
`src/components/control-center/ActiveAgentsPanel.test.tsx` with `git rm` so the removal is staged
as a deletion rather than an untracked absence.

In `src/pages/Chat.tsx`, remove line 41 in full:
`import { ActiveAgentsPanel } from "@/components/control-center/ActiveAgentsPanel";`

In `src/pages/Chat.tsx`, remove all three lines at 1053-1055 — the
`<SectionErrorBoundary name="Active Agents">` opening tag, the `<ActiveAgentsPanel />` child, and
the closing `</SectionErrorBoundary>`. Removing only the inner component is explicitly forbidden by
UI-SPEC: an empty boundary is a styled void, and MISSION-03 requires absence.

`IntelligenceFeedPanel` (with its own `SectionErrorBoundary name="Intelligence Feed"`, lines
1050-1052) becomes the rail's sole child. Apply **no layout compensation** — do not add a spacer,
do not change the rail's `className`, do not adjust `gap-2`/`lg:gap-3`/`xl:gap-2`, do not touch
`min-h-0`/`xl:max-h-[70vh]`/`xl:overflow-y-auto`, and do not alter the `lg:col-span-3` /
`xl:col-span-1` spans. The existing `flex flex-col gap-2` already renders a single child correctly,
and the long comment at Chat.tsx:1036-1045 explains why this rail's height caps are load-bearing
for the command-center grid row — changing them would reopen a previously-fixed layout defect.

Introduce **no replacement affordance of any kind**: no empty card, no "coming soon" tile, no
disabled panel, no placeholder text, no commented-out mount left in place as a marker.

**Prohibitions:** do not touch `MissionTimelinePanel` or its `SectionErrorBoundary name="Mission
Timeline"` at Chat.tsx:1124 (D-13 — it renders reminders and events, not `subagentJobs`, and merely
shares a word with this phase's subject). Do not touch any other panel, mount, or import in
`Chat.tsx`. Do not modify `src/hooks/useSubagentJobs.ts` — `JobsPanel` still consumes it. Do not
modify anything under `convex/`, and do not modify any file in `astridr-repo` (D-01).
  </action>

  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>

  <acceptance_criteria>
    - `git ls-files src/components/control-center/ActiveAgentsPanel.tsx` returns empty
    - `git ls-files src/components/control-center/ActiveAgentsPanel.test.tsx` returns empty
    - `grep -rn "ActiveAgentsPanel" src/pages/Chat.tsx` returns no matches
    - `grep -rn "Active Agents" src/pages/Chat.tsx` returns no matches (the boundary name went too)
    - `grep -c "SectionErrorBoundary" src/pages/Chat.tsx` returns exactly two fewer than it did
      before this task — capture the before value first and record both numbers in the SUMMARY
    - `grep -c "SectionErrorBoundary name=\"Intelligence Feed\"" src/pages/Chat.tsx` returns 1
    - `grep -c "SectionErrorBoundary name=\"Mission Timeline\"" src/pages/Chat.tsx` returns 1 (D-13)
    - `grep -c "cc-left-rail" src/pages/Chat.tsx` returns 1 and `git diff src/pages/Chat.tsx` shows
      NO change to the line carrying `data-testid="cc-left-rail"` or the `className` line beneath it
    - `grep -riE "coming soon|placeholder (panel|card|tile)|TODO: restore|no agents running" src/pages/Chat.tsx`
      returns no matches. The bare word `placeholder` **must not** be one of the alternatives:
      it is structurally incapable of returning 0 on correct work, because `src/pages/Chat.tsx:733-734`
      already carry the message textarea's own `placeholder={…}` attribute and the
      `placeholder:text-muted-foreground/70` Tailwind variant — both pre-existing, both untouched
      by this plan, and both matched by the bare form (verified 2026-08-10: the bare-word form
      returns 2 hits against the file as it stands before this task runs).
    - `git diff -U0 src/pages/Chat.tsx | grep -ciE "^\+.*(coming soon|placeholder|TODO|disabled)"`
      returns 0 — this is the residue check the bare alternative was reaching for, scoped to this
      task's own ADDED lines so pre-existing attributes elsewhere in the file cannot trip it
    - `npx tsc --noEmit` exits 0
    - `git status --porcelain` shows no modified file under `convex/` and none outside
      `src/pages/Chat.tsx` plus the two deletions
  </acceptance_criteria>

  <done>
    The component and its test are staged as deletions, `Chat.tsx` has one fewer rail child and no
    residue of the removed panel, and the type check passes. `Chat.test.tsx` is expected to be RED
    at this point — Task 2 owns it.
  </done>
</task>

<task type="auto">
  <name>Task 2: Repair Chat.test.tsx's seven-panel claims left behind by the deletion</name>

  <files>src/pages/Chat.test.tsx</files>

  <read_first>
    - `src/pages/Chat.test.tsx` lines 755-800 (the doc comment, the helper, and the label constant)
    - `src/pages/Chat.test.tsx` lines 835-880 and 940-960 (the three usage sites and three titles)
    - `.planning/phases/111-mission-board/111-PATTERNS.md` — the "No Analog Found" note recording
      that no prior commit shrank a label-array constant like this, so it is guided-but-novel work
  </read_first>

  <action>
Six edit sites in `src/pages/Chat.test.tsx`. All of them; missing any one leaves a red or a lying
test.

**(1) Doc comment, lines 762-763.** It enumerates "The seven panel components
(IntelligenceFeedPanel/ActiveAgentsPanel/MissionTimelinePanel/LlmStatusPanel/SystemMonitorPanel/
VoiceStatusPanel/QuickCommandsPanel)". Drop `ActiveAgentsPanel/` from the list and change "seven"
to "six". Leave the rest of that comment block (lines 764-770, explaining that the panels mount for
real through the shared `useQuery` mocks and that this proves `SectionErrorBoundary` coverage
end-to-end) intact — it is still true.

**(2) The constant, line 779.** Remove the `"ACTIVE AGENTS",` entry at line 781, leaving six
labels: `INTELLIGENCE FEED`, `MISSION TIMELINE`, `LLM STATUS`, `SYSTEM MONITOR`, `VOICE STATUS`,
`QUICK COMMANDS`. Rename the constant from `SEVEN_PANEL_LABELS` to `COMMAND_CENTER_PANEL_LABELS`.
Use the count-free name deliberately: a count-bearing identifier is the same class of self-
invalidating claim this phase exists to remove, and it would need renaming again the next time a
panel is added or removed.

**(3-5) The three usage sites**, lines 852, 869 and 950 — every `for (const label of
SEVEN_PANEL_LABELS)` becomes `COMMAND_CENTER_PANEL_LABELS`. There are THREE, not one: 852 and 950
assert the labels are ABSENT while command-center mode is off, and 869 asserts they are all PRESENT
while it is on. Do not change the assertions themselves, only the identifier.

**(6) The three test titles**, lines 842, 857 and 948. Their prose reads "mounts none of the seven
panels", "all seven panels render", and "mounts none of the seven command-center panels". Each
becomes "six". Change only the count word; leave the rest of each title byte-identical so the test
names stay greppable against their history.

Make no other change to this file. In particular do not touch the intermittent brain-pill test
tracked as DEBT-06 — it is a separate requirement owned by Phase 113, and masking or editing it
here would bury a known-unstable signal inside an unrelated diff.
  </action>

  <verify>
    <automated>npx vitest run src/pages/Chat.test.tsx</automated>
  </verify>

  <acceptance_criteria>
    - `grep -rn "ActiveAgentsPanel" src/` returns no matches (whole-tree, the primary gate)
    - `grep -rn "ACTIVE AGENTS" src/` returns no matches
    - `grep -c "SEVEN_PANEL_LABELS" src/pages/Chat.test.tsx` returns 0
    - `grep -c "COMMAND_CENTER_PANEL_LABELS" src/pages/Chat.test.tsx` returns 4 (one declaration
      plus three usage sites)
    - `grep -in "seven" src/pages/Chat.test.tsx` returns no matches
    - `grep -c "six" src/pages/Chat.test.tsx` returns at least 4 (the comment plus three titles)
    - `npx vitest run src/pages/Chat.test.tsx` exits 0
    - Mutation control (run, record, revert): re-add `"ACTIVE AGENTS"` to
      `COMMAND_CENTER_PANEL_LABELS` and confirm `npx vitest run src/pages/Chat.test.tsx` FAILS on
      the mode-ON test at line ~857. A green run means the label array is not actually asserted and
      the whole edit proved nothing — investigate before proceeding.
    - `npx tsc --noEmit` exits 0
    - `npx vitest run` exits 0 (full suite — no other file referenced the deleted panel)
  </acceptance_criteria>

  <done>
    `Chat.test.tsx` asserts six command-center panels under a count-free constant name, contains no
    "seven" and no "ACTIVE AGENTS", passes, and the mutation control was observed RED before being
    reverted — with the captured output in the SUMMARY.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `subagentJobs` row → Chat page DOM | The deleted panel rendered emitter-controlled `agentTypeId` / `taskSnippet` strings; deleting it removes a rendering sink, it does not open one |
| React render tree → `SectionErrorBoundary` | A removed boundary could widen a crash blast radius — assessed below |

This plan deletes code and edits a test. It adds no input handling, no network call, no auth
surface, no persistence, and no new data path. The register is short because the surface is; a
padded model would be the same fabrication this phase removes from the UI.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-111-04 | Denial of service (availability) | Removing `<SectionErrorBoundary name="Active Agents">` from `Chat.tsx` | accept | The removed boundary wrapped only the removed component; no other panel loses coverage. `IntelligenceFeedPanel` retains its own `SectionErrorBoundary name="Intelligence Feed"`, asserted as an acceptance criterion, and `Chat.test.tsx`'s command-center block mounts every remaining panel for real, so a mount-time throw in any of them fails the suite loudly rather than silently. |
| T-111-05 | Information disclosure | Deletion of a component that read `api.subagentJobs.listRecent` | accept | No auth check, gate, redaction, or filter is removed with it — the panel was a pure consumer of a query `JobsPanel` still issues unchanged. Query surface and permissions are identical before and after. |
| T-111-06 | Repudiation | A partial deletion leaving a stale reference that compiles but lies (e.g. a label array still asserting a panel that no longer mounts) | mitigate | The whole-tree `grep -rn "ActiveAgentsPanel" src/` and `grep -in "seven"` gates in Task 2, plus a mutation control proving the label array is genuinely asserted rather than decorative. |
| T-111-SC | Tampering | npm/pip/cargo installs | accept — not applicable | This plan installs no packages and removes none from `package.json`. The Package Legitimacy Gate is not triggered; `git status --porcelain` must not list `package.json` or `package-lock.json`. |
</threat_model>

<verification>
- `grep -rn "ActiveAgentsPanel" src/` returns no matches
- `grep -in "seven" src/pages/Chat.test.tsx` returns no matches
- `npx tsc --noEmit` exits 0
- `npx vitest run` exits 0 (full suite)
- `git status --porcelain` shows exactly two deletions (`ActiveAgentsPanel.tsx`,
  `ActiveAgentsPanel.test.tsx`) and two modifications (`Chat.tsx`, `Chat.test.tsx`) — nothing under
  `convex/`, nothing in `package.json`, nothing outside `src/`
- The Task 2 mutation control was executed and observed RED, with its output in the SUMMARY
</verification>

<success_criteria>
- No file in `src/` mentions `ActiveAgentsPanel` or renders "No agents running."
- The `cc-left-rail` has one child, with no empty boundary, placeholder, or coming-soon tile
- `MissionTimelinePanel`'s mount and boundary at `Chat.tsx:1124` are byte-identical to before
- The full Vitest suite and the type check both pass
</success_criteria>

<output>
Create `.planning/phases/111-mission-board/111-02-SUMMARY.md` when done. Record the before/after
`SectionErrorBoundary` counts in `Chat.tsx`, the mutation-control output, and confirm by direct
quotation of the grep results — not by assertion — that the whole-tree `ActiveAgentsPanel` search
is empty.
</output>
