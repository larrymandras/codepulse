---
quick_id: 260629-ow5
slug: memory-event-deeplink
description: Make Memory honor ?event= so KG provenance cross-nav links land on the right episodic memory
status: in-progress
date: 2026-06-29
mode: quick
---

# Quick Task 260629-ow5: Memory ?event= deep-link focus

## Context
"Finish the parked deep-link work" investigation found that of the 3 surfaces
not yet in the focus system, two need nothing:
- **Capabilities** already reads `?try=` from CommandPalette (Capabilities.tsx:351).
- **Hive** has no inbound deep-link and no consumer → speculative, deferred.

The one real gap: **KGDetailsPanel builds `/memory?event=<sourceEventId>`
provenance links** ("Open the episodic memory that taught this fact"), but
`Memory.tsx` never read `?event=` — the link landed on the timeline with no
focus. This closes that dead cross-nav link.

## Task
- **files:** `src/pages/Memory.tsx`, `src/pages/Memory.test.tsx` (new)
- **action:** Read `?event=` via `useSearchParams`; force the inner `timeline`
  tab; tag each timeline event with `data-event-id` / `data-focused`; highlight
  (ring/border) and `scrollIntoView` the matching event. Graceful no-op if the
  event isn't in the loaded window.
- **verify:** `npx tsc --noEmit`; `npx vitest run` (new Memory test asserts
  highlight + scroll on `?event=`, none without).
- **done:** Arriving at `/memory?event=<id>` focuses that event.

## Out of scope
- Hive `?goal=` deep-link (no consumer) — deferred by design.
- Capabilities (already wired).
