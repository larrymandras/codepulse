---
phase: 260603-or6
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/modelPricing.ts
  - src/components/AgentProfileEditor.tsx
autonomous: true
requirements: []
quick: true

must_haves:
  truths:
    - "Opus 4.x spend in the dashboard reports at correct $5/$25 per MTok rates (not the old 3x-inflated $15/$75)"
    - "claude-opus-4-8 and claude-opus-4-7 exist in the PRICING table at correct Opus 4.x rates"
    - "The agent profile model dropdown can select claude-opus-4-8"
  artifacts:
    - path: "src/lib/modelPricing.ts"
      provides: "Corrected Opus 4.x pricing + two new Opus entries"
      contains: "claude-opus-4-8"
    - path: "src/components/AgentProfileEditor.tsx"
      provides: "MODELS dropdown including new Opus entries"
      contains: "claude-opus-4-8"
  key_links:
    - from: "src/lib/modelPricing.ts"
      to: "estimateCost()"
      via: "PRICING[model] lookup"
      pattern: "PRICING\\[model\\]"
---

<objective>
Register Opus 4.8 (and 4.7) in CodePulse's cost model and the agent-profile UI dropdown, and fix a latent over-pricing bug where Opus 4.x has been priced at Opus 3 rates ($15/$75 per MTok instead of $5/$25), inflating all Opus spend reporting by 3x.

Purpose: Dashboard cost accounting must reflect real Anthropic Opus 4.x pricing, and operators must be able to select the new Opus model on agent profiles.
Output: Corrected + extended `PRICING` table in modelPricing.ts; extended `MODELS` array in AgentProfileEditor.tsx.

Note: These model ids are PRICING DATA + a UI dropdown — CodePulse is a telemetry dashboard, not an API caller. This change is display/cost-accounting only with no request-shape risk.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md

<interfaces>
<!-- Current state of the two files. Executor edits these directly — no exploration needed. -->

src/lib/modelPricing.ts — current PRICING record (lines 1-13) and estimateCost (lines 15-24):
```typescript
const PRICING: Record<string, { input: number; output: number }> = {
  "claude-opus-4-5":       { input: 15.00 / 1_000_000, output: 75.00 / 1_000_000 },  // WRONG (Opus 3 pricing)
  "claude-opus-4-6":       { input: 15.00 / 1_000_000, output: 75.00 / 1_000_000 },  // WRONG
  "claude-sonnet-4-5":     { input:  3.00 / 1_000_000, output: 15.00 / 1_000_000 },
  "claude-sonnet-4-6":     { input:  3.00 / 1_000_000, output: 15.00 / 1_000_000 },
  "claude-haiku-3-5":      { input:  0.80 / 1_000_000, output:  4.00 / 1_000_000 },
  "claude-haiku-4-5":      { input:  0.80 / 1_000_000, output:  4.00 / 1_000_000 },
  "gpt-4o":                { input:  2.50 / 1_000_000, output: 10.00 / 1_000_000 },
  "gpt-4o-mini":           { input:  0.15 / 1_000_000, output:  0.60 / 1_000_000 },
  "gemini-2.5-pro":        { input:  1.25 / 1_000_000, output: 10.00 / 1_000_000 },
  "gemini-2.5-flash":      { input:  0.30 / 1_000_000, output:  2.50 / 1_000_000 },
  "default":               { input:  3.00 / 1_000_000, output: 15.00 / 1_000_000 },
};
// estimateCost(): const rates = PRICING[model] ?? PRICING["default"];
```

src/components/AgentProfileEditor.tsx — current MODELS array (lines 10-19):
```typescript
const MODELS = [
  "claude-opus-4-6",
  "claude-sonnet-4-6",
  "claude-haiku-4-5",
  "gpt-4o",
  "gpt-4o-mini",
  "gemini-2.5-pro",
  "gemini-2.5-flash",
  "other",
];
```
Default model state (line 34) is `useState(profile?.model ?? "claude-sonnet-4-6")` — DO NOT change this.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix Opus 4.x pricing bug and register Opus 4.7/4.8 in PRICING</name>
  <files>src/lib/modelPricing.ts</files>
  <action>
Edit the `PRICING` record in src/lib/modelPricing.ts ONLY. Do NOT touch `estimateCost()` or any non-Opus rows.

(a) FIX the latent over-pricing bug — Opus 4.x real pricing is $5 input / $25 output per MTok (the old $15/$75 was Opus 3 pricing and has been inflating all Opus dashboard spend by 3x). Change BOTH existing Opus rows to:
```typescript
  "claude-opus-4-5":       { input:  5.00 / 1_000_000, output: 25.00 / 1_000_000 },
  "claude-opus-4-6":       { input:  5.00 / 1_000_000, output: 25.00 / 1_000_000 },
```

(b) ADD two new Opus entries with the same correct Opus 4.x pricing, placed in the Opus group (e.g. immediately after the claude-opus-4-6 row):
```typescript
  "claude-opus-4-7":       { input:  5.00 / 1_000_000, output: 25.00 / 1_000_000 },
  "claude-opus-4-8":       { input:  5.00 / 1_000_000, output: 25.00 / 1_000_000 },
```

Keep ALL sonnet, haiku, gpt, gemini, and default entries EXACTLY as-is. Do not reformat unrelated lines.
  </action>
  <verify>
    <automated>cd C:/Users/mandr/codepulse && npx tsc --noEmit</automated>
    Then run a repro to assert pricing values. Create a throwaway check (e.g. `npx tsx -e`) that imports `estimateCost` from `./src/lib/modelPricing.ts` and asserts:
    - `estimateCost(1_000_000, 0, "claude-opus-4-8") === 5.00` and `estimateCost(0, 1_000_000, "claude-opus-4-8") === 25.00`
    - same for `"claude-opus-4-7"`
    - `estimateCost(1_000_000, 0, "claude-opus-4-6") === 5.00` and `estimateCost(0, 1_000_000, "claude-opus-4-6") === 25.00` (bug fixed — no longer 15/75)
    - same assertion for `"claude-opus-4-5"`
    Report exact computed values. Delete the throwaway file/command after.
  </verify>
  <done>PRICING contains claude-opus-4-7 and claude-opus-4-8 at 5/25; claude-opus-4-5 and claude-opus-4-6 now equal 5/25 (no longer 15/75); all other rows unchanged; tsc --noEmit passes.</done>
</task>

<task type="auto">
  <name>Task 2: Add Opus 4.8/4.7 to the agent profile model dropdown</name>
  <files>src/components/AgentProfileEditor.tsx</files>
  <action>
Edit the `MODELS` array (lines ~10-19) in src/components/AgentProfileEditor.tsx so the Anthropic group leads with the new Opus models. Put `claude-opus-4-8` first, then `claude-opus-4-7`, then keep `claude-opus-4-6`, then leave the rest unchanged:
```typescript
const MODELS = [
  "claude-opus-4-8",
  "claude-opus-4-7",
  "claude-opus-4-6",
  "claude-sonnet-4-6",
  "claude-haiku-4-5",
  "gpt-4o",
  "gpt-4o-mini",
  "gemini-2.5-pro",
  "gemini-2.5-flash",
  "other",
];
```
DO NOT change the default model state on line ~34 (`useState(profile?.model ?? "claude-sonnet-4-6")` stays). Do NOT touch any other part of the file.
  </action>
  <verify>
    <automated>cd C:/Users/mandr/codepulse && npx tsc --noEmit</automated>
    Confirm the MODELS array includes "claude-opus-4-8" and "claude-opus-4-7" and still includes "claude-opus-4-6"; confirm line ~34 default is still "claude-sonnet-4-6".
  </verify>
  <done>MODELS array includes claude-opus-4-8 and claude-opus-4-7; claude-opus-4-6 retained; sonnet default on line ~34 unchanged; tsc --noEmit passes.</done>
</task>

</tasks>

<verification>
After both tasks:
1. `cd C:/Users/mandr/codepulse && npx tsc --noEmit` — passes (no type errors).
2. Pricing repro (Task 1 verify) confirms 4-7/4-8 at 5/25 and 4-5/4-6 corrected to 5/25.
3. `cd C:/Users/mandr/codepulse && npm test` — run the full vitest suite (or at minimum any modelPricing / cost tests if present). Report pass/fail honestly; investigate any failure rather than dismissing as pre-existing.
4. Run `graphify update .` from the repo root per CodePulse CLAUDE.md to keep the knowledge graph current (AST-only, no API cost).

Out of scope — DO NOT touch: seedTeams.ts, seedGateway.ts, profiles.ts seed data (existing DB records stay on their models), the raw fetch call sites (memoryQuality.ts / briefings.ts — payloads already clean), estimateCost(), and any non-Anthropic pricing rows.
</verification>

<success_criteria>
- claude-opus-4-8 and claude-opus-4-7 priced at $5/$25 per MTok in PRICING.
- claude-opus-4-5 and claude-opus-4-6 corrected from $15/$75 to $5/$25 (3x over-pricing bug fixed).
- MODELS dropdown in AgentProfileEditor.tsx offers claude-opus-4-8 (and 4-7).
- Sonnet remains the default profile model.
- `npx tsc --noEmit` passes; `npm test` results reported honestly; `graphify update .` run.
</success_criteria>

<output>
After completion, create `.planning/quick/260603-or6-codepulse-register-opus-4-8-in-cost-mode/260603-or6-SUMMARY.md`
</output>
