# Requirements

**Active milestone: v13.0 — Brain-Swap Control, Cost Intelligence & Consolidation** (Phases 103–106, formalized 2026-07-27 via `/gsd-new-milestone`).

Prior milestone v11.0 (Skills Command Center) shipped 2026-07-25 → [milestones/v11.0-REQUIREMENTS.md](milestones/v11.0-REQUIREMENTS.md) (22/22). v12.0 (Reminders & Calendar) shipped 2026-07-23 → [milestones/v12.0-REQUIREMENTS.md](milestones/v12.0-REQUIREMENTS.md). The three post-v11.0 follow-ups (display-name fix, bridge coverage, Chat command-center) all shipped 2026-07-26; Phase-98 UAT tests 5 + 4-shadowed verified live 2026-07-27.

---

## Phase 103 — Brain-Swap Control Surface

A CodePulse surface to switch Ástríðr's reasoning engine (keyed API models + subscription CLIs) on the fly. This is the CodePulse half of the astridr brain-swap thread (astridr Phase 184.1, which explicitly scopes "+ CodePulse controls").

- **BSC-01** — Live view of the **current reasoning engine** per agent AND globally, reactive from Convex/telemetry (not a stale config read — the v9.0 "VitalsRail active-profile" trap). — ⚠ **PARTIAL (global axis SATISFIED; per-agent axis deferred, not a defect)** (restated 2026-07-29 after `103-13-T1` live re-verification; see `103-VALIDATION.md` "Gap-Closure Live Re-Verification (103-13-T1)"). **The "AND globally" half is now genuinely satisfied.** With `claude-haiku-4-5-20251001` genuinely in force live, the header badge, the Chat composer pill, and Control Center `BrainControl` all named the same engine, both surfaces this phase built carrying the required "Global" qualifier (OBS 3) — the 2026-07-28 three-way disagreement did not reproduce, closed by 103-09's shared `useResolvedBrain` resolution order. **The "per agent" half remains NOT satisfied by design**: `activeEngineSnapshots` carries zero live rows for the real consulting/business/personal profiles — `activeEngine:latestByProfile` returned exactly one row, `{profileId:"unknown", model:"unknown"}` (OBS 8) — correctly deferred to astridr Phase 184.1, not a defect. **One related defect found live, now fixed at the unit-test level**: the D-11 confirm modal (which is supposed to show each profile's *current* engine before a swap) read from the same empty `activeEngineSnapshots` axis, so it showed "Auto"/pinnedCount=0 for all three profiles despite each carrying a real pinned `modelPreferences.primary` in Convex (OBS 8). **Closed by Plan 103-17 (2026-07-29, unit-tested, NOT yet live-re-verified)**: `GlobalSwapModal`'s pinned-default count and shadowing warning now read a config-derived `hasConfiguredDefault` signal (`profileConfigs.modelPreferences.primary`) instead of `mode`; the current-engine column itself remains telemetry-only per this bullet's own "not a stale config read" mandate (D-14, regression-tested). Marker stays PARTIAL pending an operator-attended live re-check of this fix — see `103-17-SUMMARY.md`. **RE-MARKED 2026-07-30 after the operator-attended live UAT** (`103-UAT.md`, 16/16 tests resolved against the running Ástríðr + self-hosted Convex stack, Clerk-disabled ephemeral server, the operator's own :5173 untouched): the live re-verification this marker was explicitly waiting on has now been performed. OBS 8's confirm-modal fix (103-17) is now LIVE-CONFIRMED: the warning reads "3 profiles have a pinned default (Claude Sonnet 5) that will be shadowed…" with 3 Pin icons, while the current-engine column still reads "Auto" — i.e. the count became honest WITHOUT back-filling the telemetry column from config (D-14 boundary intact). **The marker stays PARTIAL only because of the by-design deferral**, and this UAT produced harder evidence for it than before: pruning `activeEngineSnapshots` found **93 rows, ALL 93 of them unresolved `{profileId:"unknown", model:"unknown"}` sentinels** — the per-profile telemetry axis has never carried a single valid row, which is exactly astridr Phase 184.1's scope. A related honesty defect found and fixed during the UAT: those sentinels were being rendered dashboard-wide as a real engine named "unknown" (with the confirmed-live pulse lit); the ingest now refuses to write them and the read path refuses to trust them (`ddb51d96`, deployed 2026-07-30, guard proven live — a real swap now creates zero rows).
- **BSC-02** — **Switch the engine on the fly**: choose from keyed API models (Claude family — Opus 4.8 / Sonnet 5 / Fable 5 / Haiku 4.5 — and any others astridr exposes) and subscription CLIs (Claude Code / Codex / Antigravity); dispatched to astridr's brain-swap endpoint via authenticated `/api/*` (bearer per CLAUDE.md). — ✓ **SATISFIED (global axis; per-profile axis deferred to astridr Phase 184.1)** (restated 2026-07-29 after `103-13-T1`; see `103-VALIDATION.md`). **Global axis: SATISFIED.** `BrainPicker` lists **331 real engines** from live `swap.catalogue` (re-confirmed this session), the global branch's `swap.set` dispatch changes the running brain end-to-end (OBS 4/5), and the component's designed primary interaction — search → arrow-navigate → Enter — now genuinely dispatches with **zero** mutating WS frames before the explicit confirm click (OBS 10, closes the prior `BrainPicker.tsx:414-419` `CommandItem.onSelect` gap via 103-11). **Per-profile axis: NOT satisfied, correctly deferred.** Verified live this session that `models.catalog` is absent from Ástríðr's entire accepted command union (read verbatim off a live validation error) — the picker's "This profile" scope genuinely cannot dispatch live, matching astridr Phase 184.1's scope, not a defect. **RE-MARKED 2026-07-30 after the operator-attended live UAT** (`103-UAT.md`, 16/16 tests resolved against the running Ástríðr + self-hosted Convex stack, Clerk-disabled ephemeral server, the operator's own :5173 untouched): the live re-verification this marker was explicitly waiting on has now been performed. The last unit-covered-only path — 103-16's FAILED-swap retry branch — is now live-verified (`103-UAT.md` test 11): a genuine error ack was induced by intercepting the Ástríðr socket (0 frames reached the live backend, nothing mutated), the failure rendered honestly, `Done` stayed enabled, and reselecting the same brain produced a fresh confirm that genuinely re-dispatched. Mouse and keyboard paths both re-confirmed live with 0 mutating frames before the explicit confirm.
- **BSC-03** — **Scope control**: per-agent swap vs global swap; a global swap requires an explicit confirm (irreversible-ish, affects all agents). — *(Not re-marked this cycle; already ✓ satisfied per `103-VERIFICATION.md`.)* The D-15 confirm gate was re-confirmed live 2026-07-29: keyboard-only selection in "All profiles" scope dispatched zero commands before the confirm click (`103-VALIDATION.md` OBS 10).
- **BSC-04** — **Honest live status**: show swap in-flight → success/failure → the *resulting* active engine reconciled back from astridr (server-confirmed, not optimistic-only). — ✓ **SATISFIED** (restated 2026-07-29 after `103-13-T1`; see `103-VALIDATION.md`). **The dispatch → readback → revert leg is now genuinely live-verified and honest.** The modal reports the real `swap.set` ack + `swap.state` readback with no per-profile `union_tag_invalid` rows and no false 0/N failure (OBS 5, closes the prior `GlobalSwapModal.tsx:154,159-169` result-discarding defect via 103-12). Both revert paths work and render a real, visible result: GlobalSwapModal's "Revert global swap" toast action (OBS 7 — FAILED on the first attempt this session because it cleared instead of restoring the prior engine; **fixed live mid-session by Plan 103-14**, then re-verified PASS) and Control Center `BrainControl`'s independent "Restore usual brain" (OBS 12, exactly one dispatched frame, badge cleared correctly). **Pre-swap side: fixed at the unit-test level, pending live re-verification.** The D-11 confirm modal shown *before* a swap is dispatched previously did not honestly reflect each profile's pinned status (OBS 8) — **closed by Plan 103-17 (2026-07-29)**: the pinned-default count/warning now derive from `profileConfigs.modelPreferences.primary` (config), not `activeEngineSnapshots` (telemetry), and name the configured default being shadowed. Marker stays PARTIAL: this fix has not yet been re-run against the live running stack, matching this cycle's "a green unit suite is not accepted as proof of a live fix" pattern — see `103-17-SUMMARY.md`. Also now closed at the unit-test level: review finding WR-01 (`103-18-PLAN.md`, complete 2026-07-29) — the page-scoped revert host could fire a real command with no visible UI after a route change (a separate honesty gap from OBS 8); global-swap ownership is now hoisted into an app-level `GlobalSwapContext` above the router outlet so the modal instance survives navigation — see `103-18-SUMMARY.md`. Not yet re-run against the live running stack. **RE-MARKED 2026-07-30 after the operator-attended live UAT** (`103-UAT.md`, 16/16 tests resolved against the running Ástríðr + self-hosted Convex stack, Clerk-disabled ephemeral server, the operator's own :5173 untouched): the live re-verification this marker was explicitly waiting on has now been performed. Both blockers cited above are now live-verified closed: OBS 8's pre-swap confirm modal (see BSC-01) and WR-01's page-scoped revert (`103-UAT.md` test 9 — a swap started from a routed, page-scoped host, then Done, then navigating away, then clicking the toast's revert: the hoisted modal reopened with a real visible result and exactly one real `swap.set`). **Four further honesty defects were found and fixed during the UAT**, all live-verified: (1) the picker CRASHED from 2 of its 3 entry points, so most operators could not swap at all (`a9ef7e16`); (2) a never-settling dispatch trapped the operator in a modal showing false in-flight progress with no escape (`fc9828ff`); (3) a FAILED swap's row header still claimed "Profiles now governed by the global override", contradicting its own outcome line (`025d7502`); (4) raw model ids leaked into four operator-facing strings where a real display name existed (`35d16d5a`, `025d7502`).
- **BSC-05 (integration gate)** — Before building any UI against them, **verify astridr's brain-swap endpoints work end-to-end on the running stack** (list-engines + swap + read-current). "Endpoint exists ≠ integration works" (Phase-90 War Room lesson). Close this gate *during* execution, not after. — ✓ **SATISFIED (global axis; per-profile axis remains correctly deferred)** (restated 2026-07-29 after `103-13-T1`; see `103-VALIDATION.md` "Gap-Closure Live Re-Verification (103-13-T1)"). **Global axis: read (catalogue), confirm gate (D-15), and now write/readback/revert are ALL genuinely live-verified end-to-end** (`103-13-T1` OBS 1-7, 9-12) — the dispatch/readback/revert leg that blocked BSC-05 at `103-08-T2` is closed. **Still not a full pass** for these reasons, none of which is the per-profile axis: (1) OBS 8's D-11 confirm-modal defect is now fixed at the unit-test level by Plan 103-17 (2026-07-29) but has not yet been re-run against the live running stack; (2) review finding WR-01 is now closed at the unit-test level by Plan 103-18 (2026-07-29, see `103-18-SUMMARY.md`) — the page-scoped revert host could fire a real command with no visible UI after a route change; global-swap ownership is now hoisted into an app-level `GlobalSwapContext` above the router outlet — but has not yet been re-run against the live running stack; (3) the per-profile axis (list-engines + swap + read-current for "This profile" scope) remains correctly and by-design deferred to astridr Phase 184.1 — confirmed live this session that Ástríðr's accepted command union does not include `models.catalog`. That deferral is not a defect and is not counted against this gate. **All three gap-closure code fixes (103-16/103-17/103-18) are now CODE-complete; an operator-attended live re-verification pass is the single remaining item before this gate can close.** **RE-MARKED 2026-07-30 after the operator-attended live UAT** (`103-UAT.md`, 16/16 tests resolved against the running Ástríðr + self-hosted Convex stack, Clerk-disabled ephemeral server, the operator's own :5173 untouched): the live re-verification this marker was explicitly waiting on has now been performed. This marker named its own closing condition: *"an operator-attended live re-verification pass is the single remaining item before this gate can close."* That pass is `103-UAT.md` (2026-07-29/30). All three conditions it listed are discharged: (1) OBS 8's fix live-verified; (2) WR-01's fix live-verified (test 9); (3) the per-profile deferral is unchanged and still not counted against this gate. The Convex ingest guard was additionally deployed to the live self-hosted instance 2026-07-30 (`✔ No indexes are deleted by this push`) and proven live.

## Phase 104 — Cost Intelligence

- **COST-01** — **Per-model / per-provider cost breakdown over time** (chart + table), correctly attributing the current model mix (sonnet-5 / opus-4.8 / fable-5 etc.). — ✓ **SATISFIED** (2026-08-03, Phase 104; live-verified, see `104-VALIDATION.md` + `104-VERIFICATION.md` 12/12). `CostBreakdownTable` (per-provider/per-model, 24h/7d/30d windows) and `CostTrendChart` (with D-08's Billed / Billed + Covered toggle) both read `convex/costDerived.ts`, the single place tokens become money. **The model-mix attribution this requirement names is the part that was actually broken:** `src/lib/modelPricing.ts` had no entry for sonnet-5/opus-5/fable-5, so they fell to a default that under-priced Opus-class calls ~5x. The seeded `modelPricing` table now carries 23 rates including all three, and D-03's no-default-fallback rule means an unpriced model renders as its own `Unpriced` row with real token counts, never $0 inside a total. Read-time derivation proven live: `billedUsd` equals `promptTokens × inputPerToken + completionTokens × outputPerToken` to within 1e-9 on every row, so a rate correction re-prices history with no rollup re-run (D-04). D-05 holds live — `billedTotal` and `coveredTotal` are separate fields and no response merges them. Gateway/subscription turns land as real `llmMetrics` rows (D-18, proven with one real turn: `{calls:0,tokens:0}` → `{calls:1,tokens:36141}`, `cost` a true $0 with the shadow figure in `coveredUsd`). **Closure scope stated precisely:** every AGGREGATE spend surface derives dollars (`22a1733f` + `7e278003` moved the last four — SDKSpendGuard, CostForecastPanel, Analytics' API Spend card and LlmAnalyticsPanel's Model Breakdown — off the legacy raw `llmMetrics.cost`; measured live, derived $2.8136 vs legacy $2.4895, a ~13% under-report). Three PRE-EXISTING per-call/per-trace debug readouts (Recent LLM Calls row, `TraceWaterfall`, `chat/VitalsRail`) still show the reported value; all predate this phase, none is an aggregate spend figure, recorded as out-of-scope informational in `104-VERIFICATION.md`.
- **COST-02** — **Budget thresholds**, configurable per-model and/or global (and per-goal where the data supports it), persisted in Convex. — ✓ **SATISFIED** (2026-08-03, Phase 104). One `costBudgets` Convex table scoped `global` / `model` / `provider` / `quota`, each row carrying its own `daily|weekly|monthly` period and `warnFraction`, with UTC period boundaries and Clerk-gated writes (`ctx.auth.getUserIdentity()` on create/update/remove; scope/scopeKey/period immutable after create, surfaced in the UI). Operator surface is Settings → Cost & Budgets (`CostBudgetsAdmin` + `ModelPricingAdmin`, each independently error-isolated). **Exactly one cap source is live (D-12/D-19):** `SDKSpendGuard`'s hardcoded `DAILY_CAP = 5.00` export is gone, `forecasts.costForecast` reads its monthly cap from `costBudgets` instead of `agentConfigs["intelligence.budget_cap"]`, and the legacy Settings cap form is retired — verified by sweep, with the only surviving legacy reads inside two deprecated, caller-less functions. Live-verified end to end: editing the limit in Settings changed the gauge, and a **disabled** budget correctly stops being a cap (`b26b22f4` — both read paths had ignored `enabled` while the evaluator honoured it, so the UI had asserted a cap that could never alert). No monthly budget was invented where none existed: `seedFromLegacyCaps` refused it explicitly and the panel shows its honest "No monthly budget set." state.
- **COST-03** — **Anomaly / budget alerts** when spend spikes or crosses a threshold, delivered through the existing alert-routing layer (no new channel plumbing). — ✓ **SATISFIED** (2026-08-03, Phase 104; live-verified on a REAL fired alert). `convex/costBudgetEval.ts` projects the current burn rate to the period boundary (D-13), classifies warn vs breach, dedups per `(budgetId, level, periodStart)` (D-15) and inserts through the existing delivering path — `alerts` insert + `internal.webhookDelivery.sendAlertWebhook` — so **no new channel plumbing was added**, exactly as this requirement requires. **D-14 was the hard constraint and it holds:** evaluation runs at the TAIL of the existing `computeHourly` cron with NO new `convex/crons.ts` entry (verified: untouched by every Phase 104 commit) and `evaluateInternal` still disabled; measured live at ~250-290 ms server-side against the 15 000 ms syscall cap, wrapped in try/catch so a failure cannot take the rollup down, with zero retry/backoff entries. Live proof: a budget set just under the day's spend fired — *"Global daily budget at 111% ($3.0629 of $2.7600) — projected to hit $2.7600 by ~08:03 PM."* — a concrete figure and time, **zero enforcement wording** (D-16: alert-only, no throttle/swap/stop; the config surface says so too, "Budgets raise alerts. They don't stop work."), `webhookStatus` moved to `delivered`, and a second `computeHourly` returned `fired: 0, skippedDeduped: 1` with still exactly one alert row.

## Phase 105 — Tool & Trace Observability

- **OBS-01** — **Tool-usage analytics**: per-tool call frequency + success/failure rates over time. — ✓ **SATISFIED** (2026-08-04, live-verified, see `105-VALIDATION.md` Task 3). The core mechanism — `/tools` page, source filter (Ástríðr/Claude Code/Gateway/All), hourly aggregate buckets — is fully live-verified with real data (real Ástríðr `weather` tool calls landed as `toolExecutions` rows with populated `durationMs`/`traceId`/`round`; switching source filters genuinely changes the ranking). **A real D-15 regression was found and fully closed**: `runtimeIngest.ts`'s pre-existing `command_execution` case (predates Phase 105) inserted a second, untagged `toolExecutions` row for every Ástríðr tool call, bypassing the `excludeProvider: "astridr"` filter and leaking Ástríðr activity into `ToolExecutionPanel`/`PermissionDecisionsChart` — confirmed live (one real induction bumped the Claude-Code-only `weather` count from 2→3) and root-caused to `execution_tracker.py` wrapping every tool call, not just automation. Fixed going forward (`resolveCommandExecutionToolRow` now tags `provider: "astridr"`, live-verified against a second real induction) AND the historical backlog closed: querying by session ID found this leak had existed since **2026-07-22**, spanning most of Ástríðr's tool catalog (91 untagged rows) — `convex/migrations.ts`'s `backfillAstridrProviderTag` (dry-run verified, then applied) retagged all 91, live-verified on the actual `/` Dashboard's Tool Executions panel (now purely genuine Claude Code activity) and via query (zero Ástríðr tool names remain under `excludeProvider: "astridr"`).
- **OBS-02** — **Surface the astridr tool-filter signals** in CodePulse: ingest + a view for `agent_loop.tool_call_leaked_as_text` / `tool_policy_event` (the silent-filter-trap detector shipped astridr `b7e4a534`), so a leaked/off-turn tool is visible with the offending tool name. — ✓ **SATISFIED** (2026-08-04, live-verified, see `105-VALIDATION.md` Task 2). All 4 `tool_policy_event` kinds proven live against the running self-hosted stack: `malformed_policy_reload_rejected` and `malformed_policy_boot` via real, deliberate, immediately-reversed policy-file corruption (config watcher live-reload + container reboot); `execution_denied` and `tool_call_leaked_as_text` via the sanctioned synthetic fallback after real induction attempts were exhausted (`execution_denied` proved structurally unreachable from the commander chat session — every tool cluster is offered to it; `tool_call_leaked_as_text` survived two real prompt attempts the model correctly refused). D-06 alert isolation control passes: exactly 2 alerts (`malformed_policy_boot` severity `error`, `malformed_policy_reload_rejected` severity `warning`), zero for the other two kinds, dedup confirmed on re-run, zero enforcement wording, delivery mechanism investigated to root cause (`warning` severity correctly routes to digest mode, not stuck). The `/tools` policy feed matches its UI-SPEC exactly: correct locked per-kind colors, exactly 2 Bell markers on the two malformed kinds, sensible "received Nm ago" relative time, expand-row renders real `tool_was_offered`/`tools_offered_count`/`round`/`agentId` values. Policy file and astridr container confirmed restored to the real, unmalformed policy.
- **OBS-03** — **Deeper trace waterfall**: extend the existing `TraceWaterfall` with nested spans, per-tool timings, and cache-hit visibility per turn. — ✓ **SATISFIED** (2026-08-04, live-verified, see `105-VALIDATION.md` Task 3). Nesting, per-tool timing bars, and the per-turn cache ratio are all live-verified correct against a real 2-round Ástríðr session: `Round 1`/`Round 2` sub-sections, an LLM-call bar followed by a nested tool bar under Round 1, and the turn header's cache percentage hand-cross-checked against the raw `llm:sessionCalls` rows (`27980 / (27980 + 27980 + 946) = 49.17%` → displayed `49% cached`, exact match). The D-12 per-session truncation banner (`SESSION_TOOLS_READ_CAP`/`SESSION_CALLS_READ_CAP`, both 1000) and the "unattributed tool rows" render path — neither organically reproducible (no live session remotely approaches 1000 calls; no real session combines LLM calls with an untraced tool row) — were both live-verified against `convex/migrations.ts`'s `seedTruncationTestData`, exactly 1000 synthetic rows under a dedicated, clearly-fake test sessionId (never a real Ástríðr session), per explicit operator direction. The live UI rendered the exact expected banner text ("Showing the most recent 1000 tool executions — older tool calls in this session aren't loaded"), the "TOOL CALLS WITH NO REPORTED ROUND" section, and the "UNTRACED TOOL CALLS" section — neither guessed onto a round. Seed data (1000 `toolExecutions` + 1 `llmMetrics` row) fully cleaned up immediately after verification (`cleanupTruncationTestData`, re-queried empty); zero residue in the live table.

## Phase 106 — Consolidation & Hardening

- **DEBT-01** — **Typed-api sweep**: eliminate remaining `anyApi` usages (e.g. `Ideation.tsx`) → type-safe Convex calls.
- **DEBT-02** — **Retire cloud Convex `tidy-whale-981`**: export pre-2026-07-15 history (~56 GB peak — plan it), confirm nothing reads it, then cancel. Fully closes the cloud→self-hosted migration.
- **DEBT-03** — **Build/chunk cleanup**: code-split the >500 kB chunks (`react-force-graph-3d`, `WarRoom`, `useSpeechRecognition`) below the warning threshold; laptop Tailscale set-up.
- **DEBT-04** — **Finish deferred manual UAT**: Phase-98 Test-4 remaining menu sub-cases (active-single / dormant-non-shadowed / multi-scope), the full "Hey Ástríðr" wake → "stop" barge-in → "goodbye" re-arm voice sequence, and Phase-100's manual live-Forge-daemon drag round-trip.

---

### Out of scope (v13.0)
- Astridr-side brain-swap *backend* (owned by astridr Phase 184.1 — CodePulse consumes it; BSC-05 gates on it being live).
- New alert delivery channels (COST-03 reuses existing routing).
- Any mass mutation of the live self-hosted Convex (DEBT-02 exports/cancels the *cloud* instance only).
