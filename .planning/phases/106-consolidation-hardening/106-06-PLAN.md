---
phase: 106-consolidation-hardening
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/106-consolidation-hardening/106-HUMAN-UAT.md
  - convex/skillSync.ts
  - convex/__tests__/skillSync.test.ts
  - src/components/skills/SkillLifecycleMenu.tsx
autonomous: false
requirements: [DEBT-04]

user_setup:
  - service: clerk
    why: "D-08: the skill-lifecycle mutations are Clerk-gated; an unauthenticated click tests the auth gate, not the feature (Phase-98 UAT test 5 proved this — enqueueLifecycle's auth guard throws a plain Error that Convex redacts to 'Server Error'). The session must be signed in by Larry."
    env_vars: []
    dashboard_config:
      - task: "Sign in to CodePulse with Clerk in the browser session used for this UAT"
        location: "the running CodePulse UI"

must_haves:
  truths:
    - "D-08: The three browser-pending Phase-98 Test-4 menu sub-cases — active-single-scope, dormant-non-shadowed, multi-scope — are each exercised live in a Clerk-signed-in browser session, with the observed menu state recorded."
    - "D-07 (as corrected): The stale project-origin-row behaviour is re-verified live against the CURRENT code, confirming the 98-05 fix still holds — moving the last skill out of a project workspace prunes the declared-but-empty origin and leaves Archive/Move enabled."
    - "Every staged test skill is removed from disk and from the registry afterwards, leaving zero residue in the live catalog."
    - "A UAT artifact records each test with its expected result, its actual result, and a pass/issue verdict — structured the same way as 98-HUMAN-UAT.md so it reads as a continuation, not a new format."
  artifacts:
    - path: ".planning/phases/106-consolidation-hardening/106-HUMAN-UAT.md"
      provides: "the phase's UAT log; this plan writes its frontmatter, the skills-lifecycle test section, and the running Summary"
      contains: "### 1."
  key_links:
    - from: "the live Skills page ⋯ menu"
      to: "convex/skillSync.ts origin reconciliation"
      via: "a Forge daemon rescan that prunes declared-but-empty origins"
      pattern: "scannedOrigins"
---

<objective>
UAT session A of three: close the Phase-98 skill-lifecycle items that were left browser-pending, and re-verify that the stale project-origin bug is genuinely fixed.

## Read this before planning any code change

CONTEXT.md D-07 originally described the stale project-origin-row bug as an open major defect and instructed a fix-with-regression-test. **That framing was corrected on 2026-08-04 during pattern-mapping and the correction is recorded in D-07 itself.** The evidence:

- `98-HUMAN-UAT.md`'s own Gaps section records `status: resolved`, closed by gap-closure plan 98-05 on 2026-07-22.
- `98-HUMAN-UAT.md` Test 6, dated 2026-07-23, is a live post-deploy re-repro that PASSED — the `claude-code:project:559ce8ebf812` row was fully pruned and no skill rendered multi-scope from that workspace.
- Current `convex/skillSync.ts` already contains `sanitizeScannedOrigins` plus the per-origin `computeSkillPrunes` logic with the `scannedOrigins` manifest.

So this plan's job for that bug is **live re-verification that the fix holds**, not writing a fix. Task 3 exists only as a contingency and must be a documented no-op unless the live re-repro produces a genuinely NEW failure.

Purpose: DEBT-04 is "finish deferred manual UAT". These are the Phase-98 items that a green unit suite can never close.
Output: `.planning/phases/106-consolidation-hardening/106-HUMAN-UAT.md` (created by this plan, extended by plans 106-07 and 106-08).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/106-consolidation-hardening/106-CONTEXT.md
@.planning/phases/106-consolidation-hardening/106-PATTERNS.md
@.planning/milestones/v11.0-phases/98-skill-lifecycle-mutations-archive-restore-move-delete/98-HUMAN-UAT.md
@src/components/skills/SkillLifecycleMenu.tsx
@convex/skillSync.ts
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Stage the fixtures and create the UAT artifact</name>
  <files>.planning/phases/106-consolidation-hardening/106-HUMAN-UAT.md</files>
  <read_first>
    - `.planning/milestones/v11.0-phases/98-skill-lifecycle-mutations-archive-restore-move-delete/98-HUMAN-UAT.md` — read it in full. It is the structural template for the artifact (frontmatter `status`/`phase`/`source`/`started`/`updated`; a `## Current Test` marker; numbered `### N. <test name>` sections carrying `expected:` / `result:` / `notes:`; a closing `## Summary` with total/passed/issues/pending/skipped/blocked counts). Its Test 4 `notes:` also documents the reusable staging technique this task depends on.
    - `src/components/skills/SkillLifecycleMenu.tsx` — read the actual scope-gating branches (dormant / shadowed / multiScope) so each test's `expected:` line quotes the real label and the real disabled-reason string this component renders, rather than a paraphrase.
    - `convex/skillSync.ts` — read `sanitizeScannedOrigins` and `computeSkillPrunes` so the stale-origin test's `expected:` line describes the mechanism that is actually in the code today.
    - `CLAUDE.md` § Commands — the dev-server command and port for the session.
  </read_first>
  <action>
**A. Bring the stack up and record what is running.** Start the CodePulse dev server per `CLAUDE.md` § Commands and confirm the Forge daemon is running (its rescan is what reconciles the registry — without it, no lifecycle test can reach its honest end state). Record the dev-server URL, the daemon's status, and which Convex backend the UI is pointed at. Per project LESSONS, probe both `localhost` and `127.0.0.1` before declaring anything down: this project's Vite server has been misreported as down from an IPv4-only probe.

**B. Stage the fixtures on disk, not in the database.** Per the technique recorded in `98-HUMAN-UAT.md` Test 4 `notes:` — DB-only skill rows are pruned by the daemon's post-command rescan, which reconciles the registry against disk, so a database insert will not survive. Create throwaway skill directories on disk and let a rescan register them:

  - `uat106-active-single` — a directory under the global active skills path only. Registers as a single active origin.
  - `uat106-dormant-plain` — a directory under the cold-storage/available path only, with NO same-named active copy. Registers as dormant, non-shadowed. (Phase 98 verified the SHADOWED variant; the non-shadowed one is one of the three still pending.)
  - `uat106-multiscope` — the same skill name present under BOTH the global active path and a project workspace's `.claude/skills/` path, so it registers with two origins and renders multi-scope.
  - `uat106-lastout` — a skill present ONLY inside a project workspace, and the ONLY skill in that workspace. This is the fixture for the stale-project-origin re-verification: moving it out leaves the workspace origin declared-but-empty, which is precisely the condition the 98-05 fix reconciles.

  Derive the actual paths from the daemon's configured scan roots rather than hardcoding them; record every path you create in the artifact so Task 2's cleanup can be exact. Give each directory the minimum content a real skill needs (a `SKILL.md`) so the scanner treats it as genuine.

  Trigger a rescan and confirm each fixture appears in the Skills page registry with the intended origin shape BEFORE the live session starts. A fixture that did not register the way you intended would silently invalidate its test.

**C. Create `.planning/phases/106-consolidation-hardening/106-HUMAN-UAT.md`** following `98-HUMAN-UAT.md`'s structure exactly:

  - Frontmatter: `status: in-progress`, `phase: 106-consolidation-hardening`, `source: [106-CONTEXT.md, 98-HUMAN-UAT.md, 100-HUMAN-UAT.md]`, `started: <ISO timestamp>`, `updated: <ISO timestamp>`.
  - `## Current Test` — a one-line marker naming where the session is.
  - `## Environment` — the dev-server URL, Convex backend target, daemon status, and every staged fixture path from step B.
  - `## Tests` — four stub sections, each with a real `expected:` line written from the code read in `read_first` and an empty `result:`:
    - `### 1. Active single-scope row ⋯ menu (Phase-98 Test 4, pending sub-case)`
    - `### 2. Dormant non-shadowed row ⋯ menu (Phase-98 Test 4, pending sub-case)`
    - `### 3. Multi-scope row ⋯ menu (Phase-98 Test 4, pending sub-case)`
    - `### 4. Stale project-origin re-verification (D-07 as corrected — re-verify the 98-05 fix holds)`
  - `## Summary` — the counts table, zeroed for now.

  In section 4's `expected:`, state the corrected framing explicitly: this is a re-verification that the 98-05 fix holds, and a PASS is the expected outcome; a failure here would be a NEW regression, not the original bug resurfacing.
  </action>
  <verify>
    <automated>test -f .planning/phases/106-consolidation-hardening/106-HUMAN-UAT.md &amp;&amp; grep -q "^status: in-progress" .planning/phases/106-consolidation-hardening/106-HUMAN-UAT.md &amp;&amp; test $(grep -c '^### [0-9]\.' .planning/phases/106-consolidation-hardening/106-HUMAN-UAT.md) -ge 4 &amp;&amp; test $(grep -c '^expected:' .planning/phases/106-consolidation-hardening/106-HUMAN-UAT.md) -ge 4 &amp;&amp; grep -q "## Summary" .planning/phases/106-consolidation-hardening/106-HUMAN-UAT.md</automated>
  </verify>
  <acceptance_criteria>
    - `106-HUMAN-UAT.md` exists with frontmatter fields `status`, `phase`, `source`, `started`, `updated`, matching `98-HUMAN-UAT.md`'s field set.
    - It contains exactly four `### N.` test sections, each with a non-empty `expected:` line and an empty `result:` line.
    - Test 1/2/3 `expected:` lines quote the real menu item labels and disabled-reason strings as they appear in `src/components/skills/SkillLifecycleMenu.tsx` — a grep of any quoted reason string against that file returns a match.
    - Test 4's `expected:` states that this re-verifies the 98-05 fix and that a failure would be a NEW regression.
    - `## Environment` lists every staged fixture path created in step B.
    - All four fixtures are confirmed present in the live registry with their intended origin shapes before the session, and that confirmation is recorded in `## Environment`.
    - No production source file is modified by this task (`git status --porcelain src convex` is empty).
  </acceptance_criteria>
  <done>The stack is up, four disk-backed fixtures are registered with verified origin shapes, and a UAT artifact structured like `98-HUMAN-UAT.md` is ready to receive real results.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Live Claude-guided session — the three menu sub-cases + the stale-origin re-verification</name>
  <files>.planning/phases/106-consolidation-hardening/106-HUMAN-UAT.md</files>
  <action>Drive the session with Larry step by step, one test at a time, and STOP for his confirmation after each. Confirm he is Clerk-signed-in BEFORE the first click — an unauthenticated lifecycle click throws a plain Error that Convex redacts to "Server Error", which reads like a feature defect (the Phase-98 precedent). Record each observed outcome into the matching `result:` line in `106-HUMAN-UAT.md` quoting the real on-screen strings (menu labels, tooltip text) rather than summarising, and assign each section a `pass` / `issue` / `blocked` verdict using `98-HUMAN-UAT.md`’s vocabulary. For test 4, record the registry origins the skill carried BEFORE and AFTER the rescan, not just a verdict word. Update `## Summary` counts and the `## Current Test` marker. Do not fix anything during the session — task 3 owns the contingency branch.</action>
  <what-built>
The dev server and Forge daemon are running, and four throwaway skills are staged on disk and registered: `uat106-active-single` (active, one origin), `uat106-dormant-plain` (dormant, no active twin), `uat106-multiscope` (global + project workspace), and `uat106-lastout` (the only skill in a project workspace). `106-HUMAN-UAT.md` holds four tests, each with the expected result already written from the current source.

These are the Phase-98 items a green unit suite cannot close: what the ⋯ menu actually renders, and whether the origin-pruning fix still holds against a real daemon rescan.
  </what-built>
  <how-to-verify>
You need to be **signed in with Clerk** before touching anything. The lifecycle mutations are auth-gated and an unauthenticated click throws a plain Error that Convex redacts to "Server Error" — that would test the auth gate, not the feature. This exact trap cost a pass during Phase 98.

Claude will drive the steps and read the results with you. Work through the tests in order:

1. **Active single-scope** — open the ⋯ menu on `uat106-active-single`. Expect Archive plus exactly one Move item, both enabled, no disabled-reason tooltip.
2. **Dormant non-shadowed** — open the ⋯ menu on `uat106-dormant-plain` in Cold Storage. Expect Restore ENABLED (this is the non-shadowed case — Phase 98 verified the shadowed one, where Restore is correctly disabled) plus Delete Permanently. The Skills page must not blank.
3. **Multi-scope** — open the ⋯ menu on `uat106-multiscope`. Expect Archive and Move disabled, each with the honest reason showing which scopes it spans.
4. **Stale project-origin re-verification** — move `uat106-lastout` out of its project workspace (it is the last skill there), let the daemon rescan, then check: the now-empty project origin should disappear from the registry, the skill should NOT render multi-scope, and its Archive/Move should stay enabled. **A pass is expected here** — this is re-verifying a fix that shipped in 98-05 and already passed a live re-repro on 2026-07-23. If it FAILS, that is a new regression and Claude will take it to Task 3.

For each test, confirm out loud what you actually see so it can be written into `result:` verbatim rather than paraphrased. If a menu shows something unexpected, say what it shows — do not round it to "wrong".
  </how-to-verify>
  <verify>
    <automated>test $(grep -c '^result: *$' .planning/phases/106-consolidation-hardening/106-HUMAN-UAT.md) -eq 0 &amp;&amp; grep -q "## Summary" .planning/phases/106-consolidation-hardening/106-HUMAN-UAT.md</automated>
    <human-check>Larry confirms, per test, exactly what the ⋯ menu rendered and what happened to the project origin after the rescan.</human-check>
  </verify>
  <acceptance_criteria>
    - All four `result:` lines are filled in with what was actually observed, quoting real on-screen strings (menu labels, tooltip text) rather than summarising them.
    - Each test section carries a verdict of `pass`, `issue`, or `blocked`, following `98-HUMAN-UAT.md`'s vocabulary.
    - Test 4's section records the registry state before and after the rescan (which origins the skill carried each time), not just a pass/fail word.
    - `## Summary` counts are updated and reconcile with the four sections (total 4; passed + issues + pending + blocked = 4).
    - The `## Current Test` marker is updated to reflect where the session ended.
    - If any test is an `issue`, its section records the exact on-screen text or error, and the SUMMARY states DEBT-04 is not closed by this plan.
  </acceptance_criteria>
  <resume-signal>Confirm the observed result for each of the four tests, or type "blocked" with what stopped you.</resume-signal>
  <done>All four tests carry a real recorded result and verdict, test 4 records the before/after origin state, and `## Summary` reconciles.</done>
</task>

<task type="auto">
  <name>Task 3: Clean up fixtures, and fix a genuine regression only if test 4 failed</name>
  <files>.planning/phases/106-consolidation-hardening/106-HUMAN-UAT.md, convex/skillSync.ts, convex/__tests__/skillSync.test.ts, src/components/skills/SkillLifecycleMenu.tsx</files>
  <read_first>
    - `.planning/phases/106-consolidation-hardening/106-HUMAN-UAT.md` — the recorded results from Task 2; this task branches on Test 4's verdict
    - `## Environment` in that same file — the exact fixture paths to remove
    - `convex/skillSync.ts` — `sanitizeScannedOrigins` and `computeSkillPrunes`, the functions 98-05 introduced; read them before concluding anything about a failure
    - `convex/__tests__/skillSync.test.ts` — the existing test conventions (describe/it/expect with table-style fixtures) to follow if a regression test is needed
    - `CLAUDE.md` § "Error Triage" — a confirmed bug found during a phase is fixed in the phase, with a test; it is never filed as pre-existing
  </read_first>
  <action>
**A. Clean up, unconditionally.** Remove every fixture directory listed in `## Environment` from disk, trigger a rescan, and confirm the registry no longer carries any `uat106-*` row and no orphaned project origin from the staging. Record the cleanup commands and the post-cleanup verification query/result in the artifact under a `## Cleanup` section. Phase 98's own UAT recorded "zero residue" as an explicit outcome — match that bar. Do not use any bulk/mass delete against the live self-hosted Convex; removing a handful of rows via the normal rescan reconciliation is the intended path, per `CLAUDE.md` § "Self-Hosted Convex — Operational Rules".

**B. Branch on Test 4's verdict.**

*If Test 4 passed* (the expected outcome): make NO change to `convex/skillSync.ts`, `convex/__tests__/skillSync.test.ts`, or `src/components/skills/SkillLifecycleMenu.tsx`. Record in the artifact and the SUMMARY that D-07's code-fix branch was a documented no-op, citing the live evidence: the 98-05 fix is present in the current source and held under a fresh live re-repro. State the `git status --porcelain convex src` output as proof no source was touched.

*If Test 4 failed* — a NEW regression, since the 98-05 fix is present in the source and previously passed a live re-repro — then per `CLAUDE.md` § "Error Triage" fix it in this phase rather than deferring:
  1. Root-cause it first. Determine whether the failure is registry-side (`convex/skillSync.ts` origin reconciliation) or UI-side (`src/components/skills/SkillLifecycleMenu.tsx` scope gating). Do not patch both on suspicion; name the mechanism before editing.
  2. Write a failing unit test in `convex/__tests__/skillSync.test.ts` (or the appropriate component test) that reproduces the observed behaviour, and confirm it fails against the current source.
  3. Make the minimal fix, and confirm the test passes.
  4. Mutation-verify: revert the fix, confirm the test fails again, restore the fix byte-identically. A test that passes with the fix removed is not a guard.
  5. Apply the defect-class sweep this project requires: state the defect pattern abstractly, grep the whole repo — including tests, fixtures and config, not just source — for other occurrences, and record the full hit list with file:line in the SUMMARY before concluding.
  6. Re-run the live re-repro with Larry to confirm the fix holds against a real daemon rescan, and append a `### 5.` section to the artifact recording it.

**C. Close the artifact section.** Update `## Summary` counts and the `updated:` frontmatter timestamp. Leave `status:` as `in-progress` — plans 106-07 and 106-08 add more tests to this same file, and only the last of them may close it.
  </action>
  <verify>
    <automated>npx vitest run convex/__tests__/skillSync.test.ts &amp;&amp; npx tsc --noEmit &amp;&amp; grep -q "## Cleanup" .planning/phases/106-consolidation-hardening/106-HUMAN-UAT.md &amp;&amp; grep -q "^status: in-progress" .planning/phases/106-consolidation-hardening/106-HUMAN-UAT.md</automated>
  </verify>
  <acceptance_criteria>
    - A `## Cleanup` section records the removal commands and a post-cleanup registry query showing zero `uat106-*` rows and zero orphaned staging origins.
    - No `uat106-*` directory remains at any path listed in `## Environment`.
    - If Test 4 passed: `git status --porcelain convex src` is empty, and the artifact plus SUMMARY both state that D-07's code-fix branch was a documented no-op with the reason.
    - If Test 4 failed: a new test exists that fails without the fix and passes with it (mutation proof recorded in the SUMMARY), the defect-class grep sweep is recorded with its full file:line hit list, and a `### 5.` live re-verification section records the fix holding against a real rescan.
    - `npx vitest run convex/__tests__/skillSync.test.ts` and `npx tsc --noEmit` both exit 0.
    - Frontmatter `status:` is still `in-progress` and `updated:` carries a fresh timestamp.
    - No bulk/mass delete or patch was issued against the live self-hosted Convex instance.
  </acceptance_criteria>
  <done>The live catalog is back to zero residue, and either D-07's code branch is recorded as a proven no-op or a genuine new regression is fixed, tested, class-swept, and live-re-verified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser session → Clerk-gated Convex mutations | Lifecycle mutations require a real signed-in identity |
| staged fixtures → live skills registry | Test data is written into the production self-hosted registry and must be fully reclaimed |
| Forge daemon → local filesystem | Lifecycle commands move real directories on disk |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-106-19 | Tampering | live skills registry and real skill directories on disk | mitigate | Every fixture is a throwaway `uat106-*` name created for this session; no pre-existing skill is archived, moved, or deleted. Task 3's cleanup is unconditional and asserts zero residue by query, matching Phase 98's own bar. |
| T-106-20 | Tampering | live self-hosted Convex | mitigate | Cleanup goes through the daemon's normal rescan reconciliation, never a bulk delete/patch — the tombstone-GC hazard in `CLAUDE.md` applies to exactly this instance. |
| T-106-21 | Spoofing | Clerk auth gate masquerading as a feature failure | mitigate | The checkpoint states up front that the session must be signed in and explains that an unauthenticated click surfaces a redacted "Server Error" from the auth guard, which would be misread as a feature defect (the Phase-98 precedent). |
| T-106-22 | Repudiation | UAT results recorded as paraphrase | mitigate | Acceptance requires `result:` lines to quote real on-screen strings, and Test 4 to record registry state before and after — not just a verdict word. |
| T-106-SC | Tampering | npm/pip/cargo installs | mitigate | This plan installs nothing. |
</threat_model>

<verification>
- `106-HUMAN-UAT.md` exists with four `### N.` sections, zero blank `result:` lines, a reconciling `## Summary`, and a `## Cleanup` section.
- No `uat106-*` row remains in the live registry and no `uat106-*` directory remains on disk.
- If Test 4 passed: `git status --porcelain convex src` is empty.
- `npx vitest run convex/__tests__/skillSync.test.ts` and `npx tsc --noEmit` exit 0.
- Frontmatter `status:` remains `in-progress` for plans 106-07 / 106-08.
</verification>

<success_criteria>
- The three Phase-98 Test-4 menu sub-cases are each exercised live and recorded with real on-screen text.
- The stale project-origin fix is re-verified live, and D-07's corrected framing (re-verify, don't rewrite) is honoured — with a no-op explicitly recorded if it passes.
- Zero test residue remains in the live catalog or on disk.
</success_criteria>

<output>
Create `.planning/phases/106-consolidation-hardening/106-06-SUMMARY.md` when done.
</output>
