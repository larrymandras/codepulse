---
phase: 106-consolidation-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/106-consolidation-hardening/106-DEBT-VERIFICATION.md
autonomous: true
requirements: [DEBT-01, DEBT-02]

must_haves:
  truths:
    - "D-05: A reader can see, from evidence in the repo, that zero real `anyApi` usages remain in src/ or convex/ (excluding generated code), and that every surviving textual hit is a comment, not an import."
    - "D-05: `npx tsc --noEmit` passes with zero errors, so the typed-api claim is backed by the type checker, not only by grep."
    - "D-06: The DEBT-01 sweep is scoped to codepulse only and says so explicitly."
    - "D-03: A reader can see the exact grep invocations and their verbatim output proving that neither codepulse nor astridr-repo reads cloud Convex `tidy-whale-981`."
    - "D-03: Files that could not be machine-read (`.env*`, blocked by the env-file-guard hook) are named individually with a manual check line for Larry, rather than being silently reported as clean."
  artifacts:
    - path: ".planning/phases/106-consolidation-hardening/106-DEBT-VERIFICATION.md"
      provides: "DEBT-01 verify-and-close evidence + DEBT-02 pre-flight reference sweep evidence"
      contains: "## DEBT-01"
  key_links:
    - from: "106-DEBT-VERIFICATION.md § DEBT-02"
      to: "106-03-PLAN.md (cloud export)"
      via: "the go/no-go gate — the export plan must not run until this sweep is clean"
      pattern: "DEBT-02 pre-flight"
---

<objective>
Two read-only verification sweeps that produce one evidence artifact and change zero source files.

1. **DEBT-01 (verify-and-close, D-05):** re-confirm that no real `anyApi` usage remains anywhere in `src/` or `convex/` outside generated code, backed by a filtered grep gate plus a clean `npx tsc --noEmit`.
2. **DEBT-02 pre-flight (D-03):** sweep BOTH `codepulse` and `astridr-repo` for any reference to the cloud Convex deployment `tidy-whale-981` or a cloud Convex URL, so plan 106-03 can export-and-retire it knowing nothing reads it.

Purpose: DEBT-01 closes on evidence, not assumption; DEBT-02's export/cancel is irreversible, so "nothing reads it" must be proven before, not after.
Output: `.planning/phases/106-consolidation-hardening/106-DEBT-VERIFICATION.md`.

No source file is modified by this plan. If the sweeps find real hits, DO NOT fix them here — record them in the artifact and stop; a real `anyApi` import or a live cloud-URL reference changes the shape of plans 106-03 and would need re-planning.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/106-consolidation-hardening/106-CONTEXT.md
@.planning/phases/106-consolidation-hardening/106-PATTERNS.md
@CLAUDE.md

Reference for the PASS/FAIL evidence convention this repo already uses in verification output:
@scripts/verify-intake-claim.mjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: DEBT-01 typed-api verify-and-close</name>
  <files>.planning/phases/106-consolidation-hardening/106-DEBT-VERIFICATION.md</files>
  <read_first>
    - `.planning/phases/106-consolidation-hardening/106-CONTEXT.md` — decisions D-05 (verify-and-close, expect zero source changes) and D-06 (codepulse-only scope)
    - `.planning/phases/106-consolidation-hardening/106-PATTERNS.md` § "DEBT-01 — Typed-api sweep" — the pattern-mapping pass already ran this grep and got 2 comment-only hits
    - `convex/costBudgetEval.test.ts` and `convex/evalScores.test.ts` — the two files expected to carry the surviving comment hits; read the actual lines so the classification in the artifact is quoted, not asserted
    - `src/pages/Ideation.tsx` — the file REQUIREMENTS.md DEBT-01 names by example; confirm its Convex calls go through the typed `api.*` object
    - `scripts/verify-intake-claim.mjs` — the repo's PASS/FAIL evidence convention
  </read_first>
  <action>
Create `.planning/phases/106-consolidation-hardening/106-DEBT-VERIFICATION.md` with a `## DEBT-01 — Typed-api sweep (verify-and-close)` section and fill it from real command output captured this session. Do not paraphrase output; paste it.

Run and record each of these, in this order, from the codepulse repo root:

1. Raw hit count, generated code excluded:
   `grep -rn "anyApi" src convex --include=*.ts --include=*.tsx | grep -v "^convex/_generated/"`
   Record the full verbatim output and the hit count.

2. Comment-filtered gate (grep-gate hygiene — a bare count over an unfiltered file counts comments and is self-invalidating). Filter out lines whose content begins with `//`, `*`, or `/*`:
   `grep -rn "anyApi" src convex --include=*.ts --include=*.tsx | grep -v "^convex/_generated/" | grep -vE ':[0-9]+:[[:space:]]*(//|\*|/\*)'`
   This filtered command MUST return zero lines. Record the command, its output (expected: empty), and its exit status.

3. Import-shaped gate — an `anyApi` that is actually imported would appear on an import line, which the comment filter would not remove. Confirm zero import sites:
   `grep -rn "anyApi" src convex --include=*.ts --include=*.tsx | grep -v "^convex/_generated/" | grep -E "^[^:]+:[0-9]+:[[:space:]]*import|from \"convex/server\"|require\("`
   MUST return zero lines. Record command and output.

4. Type checker: `npx tsc --noEmit`. Record the command, the full output, and the exit code. Exit code MUST be 0.

5. `src/pages/Ideation.tsx` spot-check: quote the file's Convex import line(s) and one `useQuery`/`useMutation` call site showing it references the typed `api.<module>.<fn>` object rather than an `anyApi`-derived one.

For each of the surviving textual hits from step 1, write one classification row: `file:line` — verbatim line content — classification (`comment` / `generated` / `real usage`). If ANY row classifies as `real usage`, do NOT fix it: record it, set the section's verdict to `FAIL`, and state in the SUMMARY that DEBT-01 needs a code-change plan that this phase does not currently have.

State the scope boundary explicitly in the section, per D-06: the sweep covers codepulse only; astridr-repo POSTs to CodePulse's HTTP ingest endpoints and does not call the Convex API object at all, so it is out of DEBT-01's scope.

Close the section with a verdict line in the repo's PASS/FAIL convention, e.g. `VERDICT: PASS — 0 real anyApi usages, tsc --noEmit clean (exit 0)`.
  </action>
  <verify>
    <automated>grep -rn "anyApi" src convex --include=*.ts --include=*.tsx | grep -v "^convex/_generated/" | grep -vE ':[0-9]+:[[:space:]]*(//|\*|/\*)' ; test $? -eq 1 &amp;&amp; npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/106-consolidation-hardening/106-DEBT-VERIFICATION.md` exists and contains a heading matching `## DEBT-01`.
    - The comment-filtered grep in step 2 returns zero lines (grep exit status 1).
    - The import-shaped grep in step 3 returns zero lines.
    - `npx tsc --noEmit` exits 0 and the artifact records that exit code.
    - The artifact contains a classification row for every hit returned by the unfiltered step-1 grep, each with a verbatim quoted line, and none classified `real usage`.
    - The artifact quotes at least one real line from `src/pages/Ideation.tsx` showing a typed `api.` reference.
    - The artifact contains the literal string `codepulse only` (or an equivalent explicit scope statement naming astridr-repo as out of scope) in the DEBT-01 section.
    - The DEBT-01 section ends with a line beginning `VERDICT:`.
    - `git status --porcelain src convex` is empty — this task modifies no source file.
  </acceptance_criteria>
  <done>DEBT-01 is closed on recorded evidence: zero real `anyApi` usages, a clean type check, and an explicit scope statement — with zero source files changed.</done>
</task>

<task type="auto">
  <name>Task 2: DEBT-02 pre-flight — cross-repo cloud-Convex reference sweep</name>
  <files>.planning/phases/106-consolidation-hardening/106-DEBT-VERIFICATION.md</files>
  <read_first>
    - `.planning/phases/106-consolidation-hardening/106-CONTEXT.md` — D-03 (sweep BOTH repos; a Vercel env check alone is not sufficient), D-01 (`.planning/AVATAR-HANDOFF.md` is stale and claims prod reads the cloud instance)
    - `.planning/AVATAR-HANDOFF.md` — read it so the artifact can quote the exact stale claim; do NOT edit it here (plan 106-03 owns that correction)
    - `CLAUDE.md` § "Self-Hosted Convex — Operational Rules" and § "Environment Variables" — confirms `VITE_CONVEX_URL` is the variable that would carry a cloud URL, and that the self-hosted instance is a DIFFERENT target that this sweep must not conflate with the cloud one
    - Root `CLAUDE.md` (user-global, already loaded) § "Secrets & Auth" — `.env` files are hook-blocked from Read/Write/Edit and from `cat`/`source` via Bash; they cannot be machine-swept
  </read_first>
  <action>
Append a `## DEBT-02 — Pre-flight reference sweep (D-03)` section to `106-DEBT-VERIFICATION.md`.

Sweep both repos — codepulse at `C:\Users\mandr\codepulse` and astridr at `C:\Users\mandr\astridr-repo` — for every reference to the retired cloud deployment. Run each of these against BOTH repo roots and record the command, the repo, and the verbatim output (including empty output):

1. Deployment name: search for `tidy-whale-981`.
2. Cloud Convex hostnames: search for `convex.cloud` and `convex.site`.
3. The frontend env var that would carry a cloud URL: search for `VITE_CONVEX_URL` and `CONVEX_DEPLOYMENT`, and for each hit record whether the value is a literal cloud URL, a literal self-hosted/tailnet URL, or a variable reference.
4. Deploy/CI surfaces specifically — do not rely on the repo-wide grep alone. Enumerate and inspect: `vercel.json`, `.github/workflows/**`, `netlify.toml`, `convex.json`, `package.json` scripts, and any `Dockerfile`/`docker-compose*.yml`. Record which of these exist and, for each that exists, whether it names a cloud Convex target.
5. Forge daemon config in astridr-repo — search for `convex` in daemon/config files under `astridr/` and any `.forge`/daemon config, and record which Convex URL the daemon is configured against.

Use the Grep tool for the text sweeps (it is the repo-preferred search path per the graphify rule in CLAUDE.md), and record the exact pattern used for each.

Handle the un-sweepable surface honestly. `.env`, `.env.local` and siblings are blocked by the env-file-guard hook, so they CANNOT be searched by any tool available here. Do not report them as clean. Instead:
  - List, by path, every `.env*` file that EXISTS in either repo (a directory listing is not blocked; only reading the contents is).
  - For each, write a one-line manual check for Larry to run himself, of the form: open `<path>` and confirm no line's value contains `tidy-whale-981`, `.convex.cloud`, or `.convex.site`.
  - Mark this sub-item `MANUAL — PENDING LARRY` in the section, not `PASS`.

Quote `.planning/AVATAR-HANDOFF.md`'s stale claim verbatim (the line asserting Vercel prod reads cloud `tidy-whale-981`) and record it as the single known documentation-only reference, flagged for correction by plan 106-03 per D-01 and this repo's Stale Docs rule. A documentation reference is not a functional reader and must not be counted as a blocker.

Close with a go/no-go verdict line for the export plan: `VERDICT: GO — no functional reader of tidy-whale-981 in either repo (N doc-only references, M manual .env checks pending)` or `VERDICT: NO-GO — <the functional reader found>`. If NO-GO, stop and say so in the SUMMARY; plan 106-03 must not run.
  </action>
  <verify>
    <automated>test -f .planning/phases/106-consolidation-hardening/106-DEBT-VERIFICATION.md &amp;&amp; grep -q "## DEBT-02" .planning/phases/106-consolidation-hardening/106-DEBT-VERIFICATION.md &amp;&amp; grep -q "^VERDICT:" .planning/phases/106-consolidation-hardening/106-DEBT-VERIFICATION.md &amp;&amp; grep -q "MANUAL — PENDING LARRY" .planning/phases/106-consolidation-hardening/106-DEBT-VERIFICATION.md</automated>
  </verify>
  <acceptance_criteria>
    - The artifact contains a `## DEBT-02 — Pre-flight reference sweep` section.
    - The section records results for BOTH `C:\Users\mandr\codepulse` and `C:\Users\mandr\astridr-repo`, each named explicitly, for every one of the five sweep patterns.
    - Every recorded sweep shows the literal pattern used and its verbatim output (empty output is written as `(no matches)`, never omitted).
    - The section enumerates which of `vercel.json`, `.github/workflows/**`, `netlify.toml`, `convex.json`, `package.json`, `Dockerfile`/`docker-compose*.yml` exist per repo, and states per existing file whether it names a cloud Convex target.
    - Every `.env*` file present in either repo is listed by path with a per-file manual check line, and the sub-item is labelled `MANUAL — PENDING LARRY` (not PASS).
    - The section quotes `.planning/AVATAR-HANDOFF.md`'s stale cloud claim verbatim and classifies it `doc-only, not a functional reader`.
    - The section ends with a line beginning `VERDICT: GO` or `VERDICT: NO-GO`.
    - `git status --porcelain src convex scripts` is empty — no source file changed.
  </acceptance_criteria>
  <done>A reader can see, from quoted commands and verbatim output across both repos plus an explicit list of the surfaces that could not be machine-checked, whether anything still reads cloud Convex `tidy-whale-981` — and plan 106-03 has a recorded GO/NO-GO.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo files → verification artifact | Only read; nothing untrusted crosses into executable code |
| `.env*` secrets → transcript | Secret values must never enter the artifact or the transcript |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-106-01 | Information disclosure | `.env*` files containing Convex URLs/admin keys | mitigate | Do not read `.env*` (hook-blocked anyway). Record only the file PATH and a manual check instruction; never a value. |
| T-106-02 | Information disclosure | grep output pasted into the artifact | mitigate | If any grep hit's line contains a token/key-shaped value (`sb_`, `eyJ`, `admin-key`, a bearer string), record the `file:line` and redact the value as `<redacted>` rather than pasting it. |
| T-106-03 | Tampering | npm/pip/cargo installs | mitigate | This plan installs nothing. No package-manager command is permitted in either task. |
</threat_model>

<verification>
- `grep -rn "anyApi" src convex --include=*.ts --include=*.tsx | grep -v "^convex/_generated/" | grep -vE ':[0-9]+:[[:space:]]*(//|\*|/\*)'` returns zero lines.
- `npx tsc --noEmit` exits 0.
- `.planning/phases/106-consolidation-hardening/106-DEBT-VERIFICATION.md` contains both `## DEBT-01` and `## DEBT-02` sections, each ending in a `VERDICT:` line.
- `git status --porcelain src convex scripts vite.config.ts` is empty.
</verification>

<success_criteria>
- DEBT-01 is closed on recorded evidence with zero source changes, or explicitly marked FAIL with the real usage named.
- DEBT-02 has a recorded GO/NO-GO for plan 106-03, covering both repos, with the un-sweepable `.env*` surface listed rather than assumed clean.
- No secret value appears anywhere in the artifact or the transcript.
</success_criteria>

<output>
Create `.planning/phases/106-consolidation-hardening/106-01-SUMMARY.md` when done.
</output>
