---
phase: 106-consolidation-hardening
plan: 03
type: execute
wave: 2
depends_on: ["106-01"]
files_modified:
  - .planning/phases/106-consolidation-hardening/106-DEBT-VERIFICATION.md
  - .planning/AVATAR-HANDOFF.md
autonomous: false
requirements: [DEBT-02]

user_setup:
  - service: convex-cloud
    why: "Exporting the retired cloud deployment tidy-whale-981 requires an authenticated Convex cloud CLI session, and cancelling the subscription is a billing-level dashboard action Claude must not automate (D-04)."
    env_vars: []
    dashboard_config:
      - task: "Authenticate the Convex CLI against the cloud account that owns tidy-whale-981 (`npx convex login`) if the export step reports an auth failure"
        location: "terminal + browser OAuth flow"
      - task: "Cancel the Convex cloud subscription / delete the tidy-whale-981 deployment after the export is verified"
        location: "Convex dashboard → the project owning tidy-whale-981 → Settings → (deployment delete) and account Billing → cancel plan"

must_haves:
  truths:
    - "D-02: A complete, readable archive of the cloud deployment tidy-whale-981 exists on local disk on the office PC, and its completeness is proven by reading data out of the archive — not by the exporter's exit code alone."
    - "D-02: The archive is proven to have come from the CLOUD deployment and not the live self-hosted instance, by a content check on data only the cloud instance holds (history frozen at 2026-07-15)."
    - "D-01: `.planning/AVATAR-HANDOFF.md`'s stale claim that Vercel prod reads cloud tidy-whale-981 is corrected in place, in the same commit that discovers it, per this repo's Stale Docs rule."
    - "D-04: Larry receives the exact dashboard step to cancel the subscription himself; nothing in this plan attempts the cancel."
    - "CLAUDE.md § Self-Hosted Convex: the live self-hosted instance is never written to, never mass-mutated, and never imported into by this plan."
  artifacts:
    - path: ".planning/phases/106-consolidation-hardening/106-DEBT-VERIFICATION.md"
      provides: "DEBT-02 export transcript, archive verification evidence, and the cancel handoff instructions"
      contains: "## DEBT-02 — Export"
    - path: ".planning/AVATAR-HANDOFF.md"
      provides: "corrected, non-stale statement of which Convex backend production actually reads"
  key_links:
    - from: "106-DEBT-VERIFICATION.md § DEBT-02 — Pre-flight reference sweep"
      to: "the export step"
      via: "the recorded GO verdict from plan 106-01 gates this plan"
      pattern: "VERDICT: GO"
---

<objective>
Finish the cloud→self-hosted migration by taking a complete, verified local archive of the retired cloud Convex deployment `tidy-whale-981`, correcting the one stale document that still claims production reads it, and handing Larry the exact manual step to cancel the subscription.

Purpose: the cancel is irreversible. Once the subscription is gone the ~56 GB of pre-2026-07-15 history cannot be re-exported. The archive must therefore be verified by reading real rows out of it, and the archive's provenance must be proven to be the cloud deployment rather than the live self-hosted one.
Output: a verified export directory/zip on local scratch disk, an appended `## DEBT-02 — Export & verification` section in `106-DEBT-VERIFICATION.md`, a corrected `.planning/AVATAR-HANDOFF.md`, and a blocking checkpoint handing Larry the cancel step.

## Hard constraints

- **Do not run the cancel.** D-04: the subscription cancellation is Larry's, manually, in the dashboard. This plan stops at "export verified + instructions in hand".
- **Do not touch the self-hosted instance.** Per `CLAUDE.md` § "Self-Hosted Convex — Operational Rules": no `convex import`, never `--replace-all`, no bulk delete or bulk patch, against `convex-backend` / the tailnet URL / `127.0.0.1:3210`. This plan is read-only against Convex and its only Convex write path is *none*. The self-hosted tombstone-GC hazards documented there apply to a different target and must not be conflated with this export.
- **Do not proceed if plan 106-01 recorded `VERDICT: NO-GO`.** Read that verdict first; if it is NO-GO, stop and report.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/106-consolidation-hardening/106-CONTEXT.md
@.planning/phases/106-consolidation-hardening/106-PATTERNS.md
@.planning/phases/106-consolidation-hardening/106-DEBT-VERIFICATION.md
@.planning/AVATAR-HANDOFF.md
@CLAUDE.md

<interfaces>
<!-- Verified against the installed CLI this session (convex 1.42.1). Use these flags; do NOT
     guess. Note PATTERNS.md's suggested `--deployment-name` does NOT exist in this version. -->

`npx convex export --help` (convex 1.42.1) accepts exactly:
  --path <zipFilePath>       export into a directory or an unoccupied .zip path
  --include-file-storage     include stored files under a _storage folder
  --prod                     export from the project's default production deployment
  --deployment <deployment>  export from a specific deployment; accepts a deployment NAME
                             (e.g. `tidy-whale-981`), a reference (`dev`, `prod`, `local`),
                             or `team-slug:project-slug:reference`
  -h, --help

Export archive layout (Convex snapshot export): one directory per table containing
`documents.jsonl` (one JSON document per line), plus `generated_schema.jsonl` per table.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Export tidy-whale-981 to local scratch disk, proving the target before and after</name>
  <files>.planning/phases/106-consolidation-hardening/106-DEBT-VERIFICATION.md</files>
  <read_first>
    - `.planning/phases/106-consolidation-hardening/106-DEBT-VERIFICATION.md` — plan 106-01's `## DEBT-02 — Pre-flight reference sweep` section and its `VERDICT:` line. This plan must not proceed on NO-GO.
    - `.planning/phases/106-consolidation-hardening/106-CONTEXT.md` — D-02 (local disk on this office PC, verify complete/readable, deletable afterwards — no long-term retention or G:\ cold-storage requirement), D-04 (do not automate the cancel)
    - `CLAUDE.md` § "Self-Hosted Convex — Operational Rules" — the hazards that apply to the OTHER target, so the distinction is explicit in the transcript
    - `CLAUDE.md` § "Environment Variables" — `VITE_CONVEX_URL` is the frontend's Convex URL; the self-hosted CLI target is configured through `CONVEX_SELF_HOSTED_URL` / `CONVEX_SELF_HOSTED_ADMIN_KEY`, which is exactly what must NOT capture this export
  </read_first>
  <action>
**Step 1 — gate.** Read the `VERDICT:` line in `106-DEBT-VERIFICATION.md` § DEBT-02 pre-flight. If it is `NO-GO`, stop the task, record why, and report in the SUMMARY. Only continue on `GO`.

**Step 2 — pick and size the scratch path.** Use a scratch directory outside the repo and outside any synced drive: `C:\convex-cloud-archive\tidy-whale-981\`. Create it. Before exporting, record free space on `C:` (`df -h /c` under Git Bash, or `Get-PSDrive C` under PowerShell) into the artifact. D-02 puts the archive at ~56 GB; require at least 120 GB free before starting. If free space is below 120 GB, do NOT start the export — stop and raise it in the SUMMARY for the Task 3 checkpoint.

**Step 3 — steer the CLI away from the self-hosted instance.** The repo's env configures the CLI against the self-hosted backend. Run the export from **Git Bash**, not PowerShell, with the self-hosted variables explicitly emptied on the invocation — PowerShell 5.1 deletes a variable when assigned `''` (project memory `feedback-ps51-empty-env-var-deletes`), which is the opposite of what is wanted here:

  `CONVEX_SELF_HOSTED_URL= CONVEX_SELF_HOSTED_ADMIN_KEY= npx convex export --deployment tidy-whale-981 --include-file-storage --path /c/convex-cloud-archive/tidy-whale-981`

Include `--include-file-storage`: this archive can never be retaken after the cancel, so stored files must be in it.

**Step 4 — prove the target BEFORE letting it run to completion.** The CLI prints the deployment it is exporting from. Read that line. If it names anything other than `tidy-whale-981` — in particular if it names a self-hosted URL, a tailnet host, `127.0.0.1:3210`, or `convex-backend` — abort the command immediately and do not retry blind. Record the actual resolved target in the artifact and stop for the Task 3 checkpoint. A wrong-target export is not merely useless: it would read the live instance under load, which `CLAUDE.md` warns against.

If the CLI instead fails with an authentication error, that is expected on a machine whose CLI session is scoped to the self-hosted backend. Record the verbatim error, run `npx convex login`, and retry once. If it still fails, stop and record it for the checkpoint — do not attempt alternate auth mechanisms and never introduce an API-key fallback.

**Step 5 — record the transcript.** Append a `## DEBT-02 — Export & verification` section to `106-DEBT-VERIFICATION.md` containing: the free-space figure before the run, the exact command (with the emptied env vars shown), the CLI's verbatim target line, the verbatim completion output, the wall-clock duration, the resulting on-disk size of the export path (`du -sh`), and the free-space figure after.

Do not delete the archive in this plan. D-02 permits deletion once confirmed, but confirmation is Task 2 and the cancel is still pending; deleting before the cancel would destroy the only copy.
  </action>
  <verify>
    <automated>test -d /c/convex-cloud-archive/tidy-whale-981 &amp;&amp; du -sh /c/convex-cloud-archive/tidy-whale-981 &amp;&amp; grep -q "## DEBT-02 — Export &amp; verification" .planning/phases/106-consolidation-hardening/106-DEBT-VERIFICATION.md</automated>
  </verify>
  <acceptance_criteria>
    - The artifact records plan 106-01's pre-flight `VERDICT:` line verbatim and it reads `GO`.
    - The artifact records a pre-export free-space figure for `C:` of at least 120 GB.
    - The artifact contains the exact export command including `--deployment tidy-whale-981` and `--include-file-storage`, and shows the self-hosted env vars emptied on the invocation.
    - The artifact contains the CLI's own verbatim line naming the deployment it exported from, and that line names `tidy-whale-981`.
    - `C:\convex-cloud-archive\tidy-whale-981` exists and is non-empty, and its `du -sh` size is recorded in the artifact.
    - No `convex import`, `convex run`, `convex deploy`, or any mutation command appears anywhere in this task's transcript. `grep -riE "convex (import|deploy)|--replace-all" .planning/phases/106-consolidation-hardening/106-DEBT-VERIFICATION.md` returns only lines that are explicit prohibitions, not executed commands.
    - The archive is still present on disk at the end of the task (not deleted).
  </acceptance_criteria>
  <done>A complete cloud export of `tidy-whale-981` sits on local scratch disk, and the transcript proves — from the CLI's own output — that it came from the cloud deployment and not the live self-hosted instance.</done>
</task>

<task type="auto">
  <name>Task 2: Verify the archive is complete and readable, and correct the stale AVATAR-HANDOFF claim</name>
  <files>.planning/phases/106-consolidation-hardening/106-DEBT-VERIFICATION.md, .planning/AVATAR-HANDOFF.md</files>
  <read_first>
    - `.planning/AVATAR-HANDOFF.md` — read it in full; locate the exact line(s) claiming Vercel prod (`codepulse-jade-omega.vercel.app`) reads cloud `tidy-whale-981`
    - `.planning/phases/106-consolidation-hardening/106-CONTEXT.md` — D-01 (that claim is stale; correct or flag it during execution, don't trust it) and D-02 ("verify the archive is complete/readable")
    - `.planning/phases/106-consolidation-hardening/106-DEBT-VERIFICATION.md` — Task 1's export transcript
    - `convex/schema.ts` — the table list the archive is checked against; the archive should contain a directory per table that had rows
    - `CLAUDE.md` § "Stale Docs" — corrections go in the same commit as the discovery
  </read_first>
  <action>
**A. Verify the archive by reading real data out of it — not by trusting the exit code.**

Append the results to `## DEBT-02 — Export & verification` in `106-DEBT-VERIFICATION.md`:

1. **Structure.** List the top-level entries of the export path. Record the table-directory count. Cross-check against the table names declared in `convex/schema.ts`: record which schema tables are present in the archive and which are absent, and note that an absent table means it held zero rows on the cloud instance (state that explicitly rather than treating absence as corruption).
2. **Readability.** For at least five substantial tables — including `events` and `sessions` — read the first and last line of `documents.jsonl`, confirm each parses as JSON, and record the row count per table (`wc -l`). Any table whose final line fails to parse indicates a truncated export: that is a hard FAIL, record it and stop.
3. **Provenance proof (this is the load-bearing check).** From the archive's `events` table, compute the maximum `timestamp` across all rows. Convert it to UTC and record it. Per project memory `convex-topology-all-local`, the cloud deployment's telemetry stopped and its newest `events` row is frozen at approximately `2026-07-15T00:04Z`. Assert the archive's newest `events` timestamp is on or before 2026-07-16 UTC. If instead it is recent (within days of today), the export came from the LIVE self-hosted instance, not the cloud one — record that as a hard FAIL, do not proceed to Task 3, and say so plainly in the SUMMARY.
4. **History coverage.** Record the archive's *oldest* `events` timestamp too, so the section shows the actual span of history captured, and state whether pre-2026-07-15 history (DEBT-02's stated subject) is present.
5. **File storage.** Record whether a `_storage` folder is present and its entry count / size, so it is on the record whether stored files were captured.
6. Close with `ARCHIVE VERDICT: COMPLETE & READABLE` (plus the row/table totals) or `ARCHIVE VERDICT: FAIL — <reason>`.

**B. Correct the stale document (D-01, Stale Docs rule).**

Edit `.planning/AVATAR-HANDOFF.md` in place. Replace the stale assertion that production reads cloud `tidy-whale-981` with the current, evidence-backed statement: production reads the self-hosted Convex backend; the cloud deployment `tidy-whale-981` has been retired since 2026-07-15 and, as of this phase, exported and pending cancellation. Add a short dated correction note (`Corrected 2026-08-04 (Phase 106, DEBT-02): …`) naming what the previous text claimed and what the evidence is, rather than silently overwriting it — the previous claim's existence is itself useful history. Do not rewrite unrelated parts of the file; the diff should touch only the stale claim and add the correction note.
  </action>
  <verify>
    <automated>grep -q "ARCHIVE VERDICT:" .planning/phases/106-consolidation-hardening/106-DEBT-VERIFICATION.md &amp;&amp; grep -qi "tidy-whale-981" .planning/AVATAR-HANDOFF.md &amp;&amp; grep -qi "retired\|Corrected 2026-08-04" .planning/AVATAR-HANDOFF.md &amp;&amp; ! grep -rqiE "prod(uction)? (still )?(reads|points (at|to)) .{0,40}tidy-whale-981" .planning/AVATAR-HANDOFF.md</automated>
  </verify>
  <acceptance_criteria>
    - The artifact records the archive's table-directory count and an explicit present/absent cross-check against the table names in `convex/schema.ts`.
    - The artifact records, for at least five tables including `events` and `sessions`, a row count and a confirmation that the first AND last `documents.jsonl` lines parse as JSON.
    - The artifact records the archive's maximum `events` timestamp in UTC, and that value is on or before 2026-07-16T00:00Z.
    - The artifact records the archive's minimum `events` timestamp and states whether pre-2026-07-15 history is present.
    - The artifact records whether `_storage` is present, with its entry count.
    - The artifact contains a line beginning `ARCHIVE VERDICT:`.
    - `.planning/AVATAR-HANDOFF.md` no longer asserts that production reads `tidy-whale-981`, and contains a dated correction note naming Phase 106 / DEBT-02.
    - `git diff --stat .planning/AVATAR-HANDOFF.md` shows a change confined to the stale claim plus the added note (fewer than 25 changed lines).
  </acceptance_criteria>
  <done>The archive is proven complete, readable, and provably sourced from the cloud deployment; the one document that still claimed production reads the cloud instance now says what is true and records that it was wrong.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Hand Larry the cancel step (D-04 — Claude must not automate this)</name>
  <files>.planning/phases/106-consolidation-hardening/106-DEBT-VERIFICATION.md</files>
  <action>Present the checkpoint below to Larry verbatim and STOP. Do not issue any cancel, delete, or billing call yourself under any circumstance (D-04). While waiting, do not delete the archive at `C:convex-cloud-archive	idy-whale-981` — after the cancel it is the only copy of that history. When Larry responds, append his reply verbatim to `106-DEBT-VERIFICATION.md` under a new `### Cancel handoff` subheading inside `## DEBT-02 — Export &amp; verification`, recording which `.env*` files he checked and their results, and what he did (or deferred) in the Convex dashboard. If he defers, say so plainly in the SUMMARY and mark DEBT-02 as export-complete / cancel-pending rather than closed.</action>
  <what-built>
The cloud deployment `tidy-whale-981` has been exported to `C:\convex-cloud-archive\tidy-whale-981\`, and the archive has been verified by reading real rows out of it (table coverage, JSON parse of first/last lines, and a provenance check proving the newest `events` row is frozen pre-2026-07-15, i.e. the cloud instance and not the live self-hosted one). Plan 106-01 separately proved that neither codepulse nor astridr-repo has a functional reader of that deployment, and `.planning/AVATAR-HANDOFF.md`'s stale claim to the contrary has been corrected.

Cancelling the subscription is a billing/account action and is deliberately NOT automated (D-04).
  </what-built>
  <how-to-verify>
Before you cancel anything:

1. Read the `## DEBT-02 — Export &amp; verification` section of `.planning/phases/106-consolidation-hardening/106-DEBT-VERIFICATION.md`. Confirm the `ARCHIVE VERDICT:` line reads `COMPLETE & READABLE`, and that the recorded newest `events` timestamp is pre-2026-07-15 (proof the archive came from the cloud instance).
2. Confirm the archive exists and is the expected size: open `C:\convex-cloud-archive\tidy-whale-981\` and check the on-disk size against the figure recorded in the artifact.
3. Complete the `MANUAL — PENDING LARRY` `.env*` checks listed in the § DEBT-02 pre-flight section of the same file — Claude cannot read those files. For each listed `.env*` path, confirm no line's value contains `tidy-whale-981`, `.convex.cloud`, or `.convex.site`. Report back which files you checked and the result.

Then, when you are satisfied:

4. Open the Convex dashboard at https://dashboard.convex.dev and select the project that owns the `tidy-whale-981` deployment.
5. Delete / decommission the `tidy-whale-981` deployment from that project's Settings, and then cancel the plan under account Billing so the deployment stops being charged.
6. Confirm back here what you did, and whether the dashboard reported anything unexpected.

Note: after the cancel, the export at `C:\convex-cloud-archive\tidy-whale-981\` is the only remaining copy of that history. Per D-02 you may delete it whenever you like — it is not required to be retained — but nothing in this phase deletes it for you.
  </how-to-verify>
  <verify>
    <automated>grep -q "ARCHIVE VERDICT: COMPLETE" .planning/phases/106-consolidation-hardening/106-DEBT-VERIFICATION.md &amp;&amp; test -d /c/convex-cloud-archive/tidy-whale-981</automated>
    <human-check>Larry confirms the .env* manual checks are clean and reports the outcome of the dashboard deployment-delete + subscription cancel.</human-check>
  </verify>
  <acceptance_criteria>
    - The checkpoint presents the archive path, its recorded size, and the `ARCHIVE VERDICT` line before asking for any destructive action.
    - The checkpoint lists every `MANUAL — PENDING LARRY` `.env*` path from plan 106-01 by name.
    - No cancel, delete, or billing API call is issued by Claude anywhere in this plan.
    - After Larry responds, his reported outcome (which `.env*` files he checked, and what he did in the dashboard) is appended verbatim to `## DEBT-02 — Export &amp; verification` under a `### Cancel handoff` subheading.
  </acceptance_criteria>
  <resume-signal>Reply with the results of the `.env*` checks and what you did in the Convex dashboard, or "deferred" if you want to cancel later.</resume-signal>
  <done>Larry has the exact dashboard steps and the archive evidence in front of him, his reply is recorded verbatim under `### Cancel handoff`, and Claude issued no billing or deletion action.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Convex CLI → deployment selection | The CLI resolves a target from ambient env; a mis-resolution would point a heavy read at the LIVE self-hosted instance |
| exported archive → local disk | ~56 GB of production history, including any PII in telemetry, lands unencrypted on the office PC |
| Claude → billing account | Explicitly not crossed: the cancel is human-only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-106-07 | Denial of service | live self-hosted `convex-backend` | mitigate | Empty `CONVEX_SELF_HOSTED_URL`/`CONVEX_SELF_HOSTED_ADMIN_KEY` on the invocation (Git Bash, not PS 5.1); assert the CLI's own printed target names `tidy-whale-981` before letting the run finish; abort on any other target. Post-hoc content proof: newest `events` timestamp must be pre-2026-07-15. |
| T-106-08 | Tampering | live self-hosted Convex data | mitigate | This plan issues zero Convex write commands. `convex import`, `--replace-all`, bulk delete/patch are all forbidden per `CLAUDE.md`; acceptance asserts none appear as executed commands. |
| T-106-09 | Information disclosure | `C:\convex-cloud-archive\` containing production telemetry | accept | Local disk on Larry's own office PC, outside the repo and outside any synced drive (so it cannot be committed or auto-uploaded); D-02 explicitly permits deletion once verified. Path is deliberately NOT under `C:\Users\mandr\codepulse` or a Google Drive mount. |
| T-106-10 | Elevation of privilege | Convex cloud account | mitigate | Auth via `npx convex login` OAuth only. No API-key fallback is introduced (root CLAUDE.md § Secrets & Auth). No credential value is printed to the transcript or the artifact. |
| T-106-11 | Repudiation | irreversible subscription cancel | mitigate | Blocking human checkpoint with the archive verdict shown first; Larry's reported outcome recorded verbatim in the artifact. |
| T-106-SC | Tampering | npm/pip/cargo installs | mitigate | This plan installs nothing; `npx convex` runs the already-installed `convex@1.42.1` from `node_modules`. No new package is added. |
</threat_model>

<verification>
- `C:\convex-cloud-archive\tidy-whale-981\` exists, is non-empty, and its size is recorded.
- The archive's newest `events` timestamp is on or before 2026-07-16T00:00Z (cloud-provenance proof).
- `106-DEBT-VERIFICATION.md` contains `## DEBT-02 — Export & verification` with an `ARCHIVE VERDICT:` line and a `### Cancel handoff` subheading.
- `.planning/AVATAR-HANDOFF.md` no longer claims production reads `tidy-whale-981`.
- No Convex write command was executed at any point in this plan.
</verification>

<success_criteria>
- A complete, readable, provably-cloud-sourced archive of `tidy-whale-981` exists on local disk.
- Nothing in either repo functionally reads the cloud deployment, and the one document that said otherwise is corrected.
- Larry has the exact dashboard steps to cancel, and did not have any part of that action taken for him.
- The live self-hosted Convex instance was never written to.
</success_criteria>

<output>
Create `.planning/phases/106-consolidation-hardening/106-03-SUMMARY.md` when done.
</output>
