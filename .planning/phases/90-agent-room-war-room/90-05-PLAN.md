---
phase: 90-agent-room-war-room
plan: 05
type: execute
wave: 3
depends_on: ["90-01", "90-02"]
files_modified:
  - src/lib/warRoomIdentity.ts
autonomous: true
requirements: [ROOM-01]
user_setup: []

must_haves:
  truths:
    - "A known participantId resolves to the roster agent's name, avatarData, color, and tier role badge"
    - "An unknown participantId resolves to a deterministic 'Agent #xxxx' name + a distinct generated avatar/color — never the raw id, never an identical placeholder"
    - "The operator's own card resolves to name 'You', accent avatar, role 'Operator'"
    - "Transcript agentColor resolves to the agent color (or a deterministic hash for unknowns)"
  artifacts:
    - path: "src/lib/warRoomIdentity.ts"
      provides: "resolveParticipant + resolveAgentColor pure helpers"
      exports: ["resolveParticipant", "resolveAgentColor"]
  key_links:
    - from: "src/lib/warRoomIdentity.ts"
      to: "src/components/AgentAvatar.tsx getColor"
      via: "deterministic color for unknown participants"
      pattern: "getColor"
    - from: "src/lib/warRoomIdentity.ts"
      to: "src/hooks/useRosterAgents.ts RosterAgent"
      via: "agents.find(a => a.id === pid || a.name === pid)"
      pattern: "find"
---

<objective>
Implement the pure identity-resolution helper (ROOM-01) that maps a `participantId` to real `AgentVoiceCard` props + transcript color from `useRosterAgents()` data, with a deterministic generated fallback for unknown participants and a dedicated operator-self case. Turns `src/components/AgentVoiceCard.test.tsx` GREEN. WarRoom.tsx consumes this in Plan 06.

Purpose: Isolate ROOM-01 logic into a unit-testable pure module so the convergence file (WarRoom.tsx) only wires it.
Output: `src/lib/warRoomIdentity.ts` with `resolveParticipant` + `resolveAgentColor`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/90-agent-room-war-room/90-PATTERNS.md
@.planning/phases/90-agent-room-war-room/90-RESEARCH.md
@.planning/phases/90-agent-room-war-room/90-UI-SPEC.md

<interfaces>
<!-- Resolution contracts (RESEARCH Pattern 6 / UI-SPEC Surface C). -->
RosterAgent (src/hooks/useRosterAgents.ts): { id, name, tier?, status, avatarData?:{color,emoji,imageStorageId} }.
AgentVoiceCardProps (src/components/AgentVoiceCard.tsx:15-23): { profileId, name, avatar?:{name,emoji?,color?}|null, roleBadge, currentTask?, joinDurationMs?, isSpeaking }.
getColor (src/components/AgentAvatar.tsx, exported in Plan 02): deterministic 8-brand-color hash from a string.
Surface C copy: unknown name = "Agent #"+pid.slice(-4); operator name="You", avatar {name:"You",color:"var(--primary)"}, roleBadge="Operator".
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement resolveParticipant + resolveAgentColor</name>
  <read_first>
    - src/lib/warRoomIdentity.ts (skeleton from Plan 02)
    - src/components/AgentVoiceCard.test.tsx (RED — the contract to satisfy)
    - src/hooks/useRosterAgents.ts (RosterAgent shape)
    - src/components/AgentAvatar.tsx (exported getColor)
    - .planning/phases/90-agent-room-war-room/90-PATTERNS.md § Pattern Assignments (resolveParticipant) + RESEARCH Pattern 6; UI-SPEC § Surface C
  </read_first>
  <files>src/lib/warRoomIdentity.ts</files>
  <behavior>
    - resolveParticipant(pid, agents, isOperatorSelf):
      - isOperatorSelf → { name:"You", avatar:{name:"You",color:"var(--primary)"}, roleBadge:"Operator" }.
      - match agents.find(a => a.id===pid || a.name===pid) → { name: agent.name, avatar: agent.avatarData ?? {name:agent.name}, roleBadge: agent.tier ?? "Agent" }.
      - unknown → { name: "Agent #"+pid.slice(-4), avatar: {name: pid}, roleBadge: "Agent" } (avatar.name=pid drives deterministic getColor; display name never the raw pid).
      - all return profileId: pid; isSpeaking left to the caller (accept an isSpeaking arg or default false).
    - resolveAgentColor(speakerId, agents): matched agent → agent.avatarData?.color ?? getColor(speakerId); unknown/undefined speakerId → getColor(speakerId ?? "").
  </behavior>
  <action>Fill in the Plan 02 skeleton with the pure functions above (no React, no hooks — accept `agents: RosterAgent[]` as a param so it stays unit-testable). Import `getColor` from `@/components/AgentAvatar`. Keep signatures matching what `AgentVoiceCard.test.tsx` imports. Two unknown participants with different ids MUST produce different avatars (never collapse to one placeholder — D-05); never return the raw pid as the display `name`.</action>
  <verify>
    <automated>npx vitest run src/components/AgentVoiceCard.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - identity-known + identity-unknown tests GREEN.
    - Known agent → name/avatarData/tier; unknown → "Agent #xxxx" + `{name:pid}` avatar; operator → "You"/Operator.
    - resolveAgentColor returns agent color or deterministic getColor hash.
    - `npx tsc --noEmit` passes; functions are pure (no React imports).
  </acceptance_criteria>
  <done>Identity helper implemented; ROOM-01 tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| participantId (from ingest) → rendered identity | Untrusted id string influences displayed name/avatar |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-90-ID | Information Disclosure / Spoofing | unknown participant rendering | mitigate | Never render the raw participantId; map to "Agent #"+last4 with a deterministic generated avatar (D-05) so a crafted id cannot impersonate a known agent's name unless it actually matches the roster. |
| T-90-XSS | Tampering | name/badge text | accept | Values render as React text children (auto-escaped); no `dangerouslySetInnerHTML`. |
</threat_model>

<verification>
- `npx vitest run src/components/AgentVoiceCard.test.tsx` GREEN.
- `npx tsc --noEmit` passes.
- `grep getColor src/lib/warRoomIdentity.ts` confirms reuse of the shared hash.
</verification>

<success_criteria>
resolveParticipant + resolveAgentColor resolve known agents, unknown participants (deterministic, distinct, never raw id), and operator-self; ROOM-01 unit tests green. WarRoom.tsx can now wire identity in Plan 06.
</success_criteria>

<output>
Create `.planning/phases/90-agent-room-war-room/90-05-SUMMARY.md` when done.
</output>
