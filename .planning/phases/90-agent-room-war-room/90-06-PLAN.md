---
phase: 90-agent-room-war-room
plan: 06
type: execute
wave: 4
depends_on: ["90-02", "90-03", "90-05"]
files_modified:
  - src/pages/WarRoom.tsx
  - src/App.tsx
autonomous: true
requirements: [ROOM-01, ROOM-02, ROOM-04]
user_setup: []

must_haves:
  truths:
    - "/war-room/:roomId deep-links auto-select the matching room once rooms load"
    - "Participant cards show real identity (name/avatar/color/role badge) via resolveParticipant — no hardcoded name=pid/avatar=null/roleBadge='Agent'"
    - "Transcript chunks carry a resolved agentColor (no more hardcoded undefined)"
    - "Room listing consumes the bounded { active, closed, hasMore } query with a 'Show older rooms' control"
  artifacts:
    - path: "src/App.tsx"
      provides: "/war-room/:roomId route"
      contains: "war-room/:roomId"
    - path: "src/pages/WarRoom.tsx"
      provides: "deep-link auto-select + real identity + bounded listing wiring"
      contains: "resolveParticipant"
  key_links:
    - from: "src/pages/WarRoom.tsx"
      to: "src/lib/warRoomIdentity.ts resolveParticipant"
      via: "map participantIds → AgentVoiceCard props"
      pattern: "resolveParticipant"
    - from: "src/pages/WarRoom.tsx"
      to: "api.warRoom.listRooms"
      via: "useQuery({ closedLimit }) → { active, closed, hasMore }"
      pattern: "closedLimit"
---

<objective>
Wire the War Room data layer into `WarRoom.tsx` (and add the deep-link route in `App.tsx`): real participant identity (ROOM-01), the bounded `{active,closed,hasMore}` listing + "Show older rooms" (ROOM-02), and the `/war-room/:roomId` deep-link with auto-select (ROOM-04). Turns the deep-link-select case of `src/pages/WarRoom.test.tsx` GREEN. Voice join + closed-room state come next in Plan 07 (same file, sequential).

Purpose: Replace the four hardcoded identity placeholders and the unbounded listing consumption with real data, and make rooms bookmarkable.
Output: Deep-link route, identity-wired cards, bounded listing UI.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/90-agent-room-war-room/90-PATTERNS.md
@.planning/phases/90-agent-room-war-room/90-RESEARCH.md
@.planning/phases/90-agent-room-war-room/90-UI-SPEC.md

<interfaces>
<!-- Contracts produced by earlier plans, consumed here. -->
api.warRoom.listRooms({ closedLimit }) → { active: Room[], closed: Room[], hasMore: boolean } (Plan 03).
api.warRoom.getRoomEvents → seq-ordered events (Plan 03).
resolveParticipant(pid, agents, isOperatorSelf) → AgentVoiceCardProps; resolveAgentColor(speakerId, agents) → string (Plan 05, src/lib/warRoomIdentity.ts).
useRosterAgents() → RosterAgent[] (src/hooks/useRosterAgents.ts).
App.tsx deep-link analog: /hr/roster + /hr/roster/:agentId (App.tsx:131-134) both render the same component.
WarRoom.tsx current: rooms = useQuery(listRooms) ?? [] (line 26); activeRooms/closedRooms filter (108-109); hardcoded AgentVoiceCard props (190-199); transcript agentColor: undefined (line 63, 93-105).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: /war-room/:roomId route + deep-link auto-select</name>
  <read_first>
    - src/App.tsx (existing /war-room route line 118; /hr/roster/:agentId pattern 131-134)
    - src/pages/WarRoom.tsx (selectedRoomId state line 27; room-change effect 43-47)
    - src/pages/hr/Roster.tsx (useParams shape lines 24-25)
    - .planning/phases/90-agent-room-war-room/90-PATTERNS.md § src/App.tsx + § WarRoom deep-link; RESEARCH Pitfall 6
  </read_first>
  <files>src/App.tsx, src/pages/WarRoom.tsx</files>
  <action>In `App.tsx`, add a `<Route path="/war-room/:roomId">` rendering the SAME `<WarRoom />` (same Suspense fallback) immediately after the existing `/war-room` route (line 118). In `WarRoom.tsx`, import `useParams` from `react-router-dom`, read `const { roomId: deepLinkRoomId } = useParams<{ roomId?: string }>()`, and add an auto-select effect that sets `selectedRoomId` to `deepLinkRoomId` ONLY when `deepLinkRoomId && rooms.length > 0 && !selectedRoomId` (guard on rooms loaded — Pitfall 6 race). Lean toward auto-scroll transcript to newest on deep-link load (Claude's discretion D-06). Do not change the room-change reset effect here (Plan 07 owns the voice.leave() addition).</action>
  <verify>
    <automated>grep -n "war-room/:roomId" src/App.tsx && grep -n "useParams" src/pages/WarRoom.tsx && npx vitest run src/pages/WarRoom.test.tsx -t "auto-select"</automated>
  </verify>
  <acceptance_criteria>
    - `App.tsx` has `/war-room/:roomId` rendering `<WarRoom />`.
    - WarRoom reads `useParams` roomId and auto-selects only when rooms have loaded.
    - The deep-link auto-select test in `WarRoom.test.tsx` is GREEN.
  </acceptance_criteria>
  <done>Deep-link route + guarded auto-select working; select test green.</done>
</task>

<task type="auto">
  <name>Task 2: Real participant identity + transcript agentColor</name>
  <read_first>
    - src/pages/WarRoom.tsx (hardcoded AgentVoiceCard props 190-199; transcript merge 93-105; live-chunk agentColor undefined line 63)
    - src/lib/warRoomIdentity.ts (resolveParticipant, resolveAgentColor — Plan 05)
    - src/hooks/useRosterAgents.ts (RosterAgent)
    - .planning/phases/90-agent-room-war-room/90-PATTERNS.md § WarRoom identity props; UI-SPEC § Surface C
  </read_first>
  <files>src/pages/WarRoom.tsx</files>
  <action>Import `useRosterAgents` and `resolveParticipant`/`resolveAgentColor` from `@/lib/warRoomIdentity`. Call `const agents = useRosterAgents()`. Replace the hardcoded `<AgentVoiceCard>` props at lines 190-199 by mapping each `pid` through `resolveParticipant(pid, agents, /* isOperatorSelf */ pid === operatorIdentity)` and spreading the resolved `{name, avatar, roleBadge}` (keep `isSpeaking={speakingAgents.has(pid)}`); the operator's own identity is `"operator"` (matching the Join identity from Plan 04). In BOTH the persisted-event map (93-105) and the live-chunk subscription (line 63), set `agentColor: resolveAgentColor(speakerId, agents)` instead of `undefined`. Do not change ordering logic here (getRoomEvents is already seq-ordered from Plan 03; full seq merge/dedup is Plan 07).</action>
  <verify>
    <automated>grep -nE "resolveParticipant|resolveAgentColor|useRosterAgents" src/pages/WarRoom.tsx && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - No `name={pid}` / `avatar={null}` / hardcoded `roleBadge="Agent"` placeholders remain at the card call site.
    - Cards render resolved identity; unknown participants show "Agent #xxxx" + deterministic avatar.
    - Transcript chunks (persisted + live) carry a resolved `agentColor` (no `undefined`).
    - `npx tsc --noEmit` passes.
  </acceptance_criteria>
  <done>Real identity + agentColor wired; tsc clean.</done>
</task>

<task type="auto">
  <name>Task 3: Bounded listing consumption + "Show older rooms"</name>
  <read_first>
    - src/pages/WarRoom.tsx (rooms useQuery line 26; activeRooms/closedRooms 108-109; empty-state 150-153)
    - .planning/phases/90-agent-room-war-room/90-PATTERNS.md § WarRoom bounded room listing state; UI-SPEC § Surface E
  </read_first>
  <files>src/pages/WarRoom.tsx</files>
  <action>Add `const [closedLimit, setClosedLimit] = useState(20)`. Replace line 26 with `const roomsData = useQuery(api.warRoom.listRooms, { closedLimit }) ?? { active: [], closed: [], hasMore: false }`. Derive `activeRooms = roomsData.active`, `closedRooms = roomsData.closed`, `hasMore = roomsData.hasMore` (remove the client-side `.filter()` at 108-109). Update `rooms` usages (e.g. `selectedRoom` lookup and the deep-link `rooms.length>0` guard from Task 1) to use the combined `[...activeRooms, ...closedRooms]` set. Render Surface E: after the last closed `RoomListItem`, when `hasMore`, a ghost `Button` "Show older rooms" (`text-sm text-muted-foreground w-full justify-center`) whose handler is `setClosedLimit(p => p + 20)`; show `Loader2 animate-spin` + "Loading…" while the larger query resolves; hide when `!hasMore`. Add the "No rooms yet…" empty copy from the Copywriting Contract when both lists are empty.</action>
  <verify>
    <automated>grep -nE "closedLimit|hasMore|Show older rooms" src/pages/WarRoom.tsx && npx tsc --noEmit && npm test 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `listRooms` is queried with `{ closedLimit }` and consumed as `{active,closed,hasMore}`; no client-side full-list filter remains.
    - "Show older rooms" appears only when `hasMore`, increments the limit, shows a loading state.
    - `npx tsc --noEmit` passes; full suite has no new failures introduced by this plan.
  </acceptance_criteria>
  <done>Bounded listing + pagination wired; tsc clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL path param :roomId → room selection | Attacker-influenced deep-link value |
| ingest participantId → rendered identity | Untrusted id influences displayed identity |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-90-ID | Spoofing | participant identity rendering | mitigate | resolveParticipant maps unknown ids to "Agent #xxxx" + deterministic avatar; only true roster matches show a known name (Task 2, D-05). |
| T-90-DEEP | Tampering | :roomId deep-link to arbitrary room | accept | Deep-link only selects an already-listed room; non-existent/closed rooms render the read-only ended state (Plan 07). No privileged action is taken from the param alone. |
| T-90-DOS | Denial of Service | room listing | mitigate | Consumes the bounded query (Plan 03); pagination increments are operator-driven (Task 3). |
</threat_model>

<verification>
- `npx vitest run src/pages/WarRoom.test.tsx -t "auto-select"` GREEN.
- `npx tsc --noEmit` passes; `npm test` no new failures.
- grep confirms resolveParticipant/resolveAgentColor/closedLimit/hasMore wired; no hardcoded `name={pid}`/`avatar={null}` at the card call site.
</verification>

<success_criteria>
WarRoom deep-links auto-select, render real identity (ROOM-01), wire transcript agentColor, and consume the bounded listing with "Show older rooms" (ROOM-02); deep-link select test green. Voice + closed-room state follow in Plan 07.
</success_criteria>

<output>
Create `.planning/phases/90-agent-room-war-room/90-06-SUMMARY.md` when done.
</output>
