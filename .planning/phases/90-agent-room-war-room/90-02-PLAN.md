---
phase: 90-agent-room-war-room
plan: 02
type: execute
wave: 2
depends_on: ["90-01"]
files_modified:
  - src/__mocks__/livekit-client.ts
  - src/test/setup.ts
  - src/components/AgentAvatar.tsx
  - src/lib/warRoomIdentity.ts
  - src/hooks/useWarRoomVoice.ts
  - src/components/AgentVoiceCard.test.tsx
  - convex/warRoom.test.ts
  - convex/v6Mutations.test.ts
  - src/hooks/useWarRoomVoice.test.ts
  - src/pages/WarRoom.test.tsx
autonomous: true
requirements: [ROOM-01, ROOM-02, ROOM-03, ROOM-04]
user_setup: []

must_haves:
  truths:
    - "A shared livekit-client mock exists and is registered so War Room tests run in jsdom without WebRTC"
    - "AgentAvatar.getColor is exported for reuse by the identity helper and transcript color"
    - "Interface skeletons exist for useWarRoomVoice and warRoomIdentity so test imports resolve and fail RED on behavior, not on transform"
    - "All five Wave-0 test files exist and fail RED against unimplemented behavior"
  artifacts:
    - path: "src/__mocks__/livekit-client.ts"
      provides: "stub Room/RoomEvent/Track/ConnectionState for tests"
      exports: ["Room", "RoomEvent", "Track", "ConnectionState"]
    - path: "src/lib/warRoomIdentity.ts"
      provides: "resolveParticipant + resolveAgentColor contracts (skeleton)"
      exports: ["resolveParticipant", "resolveAgentColor"]
    - path: "src/hooks/useWarRoomVoice.ts"
      provides: "useWarRoomVoice contract (skeleton)"
      exports: ["useWarRoomVoice"]
  key_links:
    - from: "src/test/setup.ts"
      to: "src/__mocks__/livekit-client.ts"
      via: "vi.mock('livekit-client')"
      pattern: "livekit-client"
    - from: "src/lib/warRoomIdentity.ts"
      to: "src/components/AgentAvatar.tsx getColor"
      via: "import getColor"
      pattern: "getColor"
---

<objective>
Create the Wave-0 test scaffolding and the interface contracts later plans implement against: the shared `livekit-client` mock (registered in the existing test setup), the `getColor` export, the `warRoomIdentity` + `useWarRoomVoice` skeletons (interface-first), and the five RED test files from 90-VALIDATION.md. After this plan, every later task has an automated RED test to turn GREEN.

Purpose: Nyquist compliance — give every downstream requirement a fast automated gate before implementation.
Output: Mock + setup registration, `getColor` export, two interface skeletons, five RED test files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/90-agent-room-war-room/90-VALIDATION.md
@.planning/phases/90-agent-room-war-room/90-PATTERNS.md
@.planning/phases/90-agent-room-war-room/90-RESEARCH.md

<interfaces>
<!-- Consumer contracts the skeletons must expose (from RESEARCH Pattern 2 + Pattern 6). -->
useWarRoomVoice() returns: { connectionState: 'disconnected'|'connecting'|'connected'|'reconnecting'|'failed'; isMuted: boolean; join(roomName): Promise<void>; leave(): Promise<void>; toggleMute(): Promise<void> }.
resolveParticipant(pid, agents, isOperatorSelf) → AgentVoiceCardProps { profileId, name, avatar, roleBadge, isSpeaking }.
resolveAgentColor(speakerId, agents) → string (agent.avatarData.color, else getColor(speakerId)).
AgentVoiceCardProps (src/components/AgentVoiceCard.tsx): { profileId, name, avatar?:{name,emoji?,color?}|null, roleBadge, currentTask?, joinDurationMs?, isSpeaking }.
RosterAgent (src/hooks/useRosterAgents.ts): { id, name, tier?, status, avatarData?:{color,emoji,imageStorageId} }.
Cross-plan RED-scaffold precedent: STATE.md Phase 88 Wave 0 — not-yet-built Convex shapes loaded so tests RED cleanly without breaking Vite transform or tsc.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: livekit-client mock + getColor export + interface skeletons</name>
  <read_first>
    - src/test/setup.ts (existing vi.fn class-stub pattern, e.g. WorkerMock ~lines 53-61)
    - src/components/AgentAvatar.tsx (getColor at lines 33-40 — currently file-private)
    - src/hooks/useTtsPlayback.ts (ref-lifecycle hook shape to mirror in the skeleton)
    - .planning/phases/90-agent-room-war-room/90-PATTERNS.md § src/__mocks__/livekit-client.ts (target stub) + § Deterministic Color Hash
  </read_first>
  <files>src/__mocks__/livekit-client.ts, src/test/setup.ts, src/components/AgentAvatar.tsx, src/lib/warRoomIdentity.ts, src/hooks/useWarRoomVoice.ts</files>
  <action>(a) Create `src/__mocks__/livekit-client.ts` exporting stub `ConnectionState`, `RoomEvent`, `Track` const objects and a `Room` class with `vi.fn()` `connect`/`disconnect`/`off`, a `localParticipant.setMicrophoneEnabled` `vi.fn()`, an `on(event,handler)` recorder and an `emit(event,...args)` test helper (per PATTERNS target stub). (b) Register it in `src/test/setup.ts` alongside the existing externals via `vi.mock('livekit-client')`. (c) In `src/components/AgentAvatar.tsx`, change `function getColor` to `export function getColor` (Critical Note N1 — required by ROOM-01 agentColor and the identity helper); do not change its behavior. (d) Create `src/lib/warRoomIdentity.ts` as an interface-first SKELETON: export `resolveParticipant` and `resolveAgentColor` with correct signatures and types, importing `getColor` from `@/components/AgentAvatar`, but with stub bodies that throw `new Error("not implemented")` (filled in Plan 05). (e) Create `src/hooks/useWarRoomVoice.ts` as an interface-first SKELETON: export `VoiceConnectionState` type, `UseWarRoomVoiceReturn` interface, and a `useWarRoomVoice()` whose `join/leave/toggleMute` reject with "not implemented" and `connectionState` is `'disconnected'` (filled in Plan 04). Skeletons exist solely so test imports resolve and tsc stays clean.</action>
  <verify>
    <automated>grep -n "export function getColor" src/components/AgentAvatar.tsx && grep -n "vi.mock('livekit-client')" src/test/setup.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `src/__mocks__/livekit-client.ts` exports `Room`, `RoomEvent`, `Track`, `ConnectionState`; `Room` has `connect`/`disconnect`/`on`/`emit`/`localParticipant.setMicrophoneEnabled`.
    - `src/test/setup.ts` registers the mock via `vi.mock('livekit-client')`.
    - `AgentAvatar.tsx` exports `getColor` (behavior unchanged).
    - `warRoomIdentity.ts` and `useWarRoomVoice.ts` export the documented contracts with stub bodies; `npx tsc --noEmit` passes.
  </acceptance_criteria>
  <done>Mock registered, getColor exported, both skeletons compile, tsc clean.</done>
</task>

<task type="auto">
  <name>Task 2: RED tests — convex backend (ROOM-02 + ROOM-04)</name>
  <read_first>
    - convex/warRoom.ts (current listRooms lines 4-11 returns array; getRoomEvents 13-21)
    - convex/v6Mutations.ts (insertWarRoomEvent lines 151-164, no seq today)
    - .planning/phases/90-agent-room-war-room/90-VALIDATION.md § Per-Task Verification Map (rows listing-shape/listing-hasmore/seq-assign/seq-unique)
    - existing convex/*.test.ts for the convex-test harness import shape
  </read_first>
  <files>convex/warRoom.test.ts, convex/v6Mutations.test.ts</files>
  <action>Create `convex/warRoom.test.ts` asserting the TARGET listRooms contract: returns `{ active, closed, hasMore }`; active rooms always listed; closed section bounded to `closedLimit` (default 20) with `hasMore: true` when closed rooms exceed the limit (ROOM-02). Create `convex/v6Mutations.test.ts` asserting `insertWarRoomEvent` assigns `seq = (max seq for roomId) + 1`, that the first event in a room gets `seq = 0`, and that sequential inserts produce strictly increasing unique seq values per room (ROOM-04). Use the project's existing convex test harness. These MUST fail RED now (listRooms returns an array; insertWarRoomEvent assigns no seq). Where the not-yet-built shape would break transform, follow the Phase 88 RED-scaffold pattern (STATE.md) rather than importing a non-existent symbol.</action>
  <verify>
    <automated>npx vitest run convex/warRoom.test.ts convex/v6Mutations.test.ts 2>&1 | grep -Eq "fail|FAIL|✗|×|expected"</automated>
  </verify>
  <acceptance_criteria>
    - `convex/warRoom.test.ts` exists with assertions for `{active,closed,hasMore}` shape and bounded-closed/hasMore behavior.
    - `convex/v6Mutations.test.ts` exists with assertions for seq=max+1, first-seq-0, and uniqueness.
    - Both files RUN (no transform/import crash) and FAIL on assertions (RED), not on missing modules.
  </acceptance_criteria>
  <done>Two convex RED test files run and fail on behavior.</done>
</task>

<task type="auto">
  <name>Task 3: RED tests — identity, voice hook, deep-link page (ROOM-01/03/04)</name>
  <read_first>
    - src/components/AgentVoiceCard.tsx (props interface lines 15-23)
    - src/hooks/useTtsPlayback.test.ts (renderHook + vi.mock pattern)
    - src/pages/WarRoom.tsx (current cosmetic join + room lists + transcript merge)
    - .planning/phases/90-agent-room-war-room/90-VALIDATION.md § Per-Task Verification Map (identity-*/voice-*/deeplink-* rows)
    - .planning/phases/90-agent-room-war-room/90-UI-SPEC.md § Surface C, Surface D (copy: "Agent #xxxx", "Room Ended", disabled Join)
  </read_first>
  <files>src/components/AgentVoiceCard.test.tsx, src/hooks/useWarRoomVoice.test.ts, src/pages/WarRoom.test.tsx</files>
  <action>Create `src/components/AgentVoiceCard.test.tsx`: import `resolveParticipant` from `@/lib/warRoomIdentity`; assert a KNOWN participant resolves to `agent.name` + `agent.avatarData` + `tier` roleBadge, and an UNKNOWN participant resolves to name `"Agent #" + pid.slice(-4)`, an avatar carrying `{ name: pid }` (deterministic, never the raw id as display), and roleBadge `"Agent"` — plus a render smoke test of `AgentVoiceCard` with each (ROOM-01, Surface C). Create `src/hooks/useWarRoomVoice.test.ts`: `vi.mock('livekit-client')`; assert `join(roomName)` first fetches `POST /api/war-room/{room}/token` with an `Authorization: Bearer` header (mock global fetch; T-90-AUTH) then calls `Room.connect`; assert mic is NOT enabled on connect — `setMicrophoneEnabled` is not called with `true` during join (join muted, T-90-MIC); assert `toggleMute()` calls `setMicrophoneEnabled` (ROOM-03). Create `src/pages/WarRoom.test.tsx` (React Testing Library): assert `/war-room/:roomId` auto-selects the matching room once rooms load, and that a closed/non-existent room renders a "Room Ended" banner with a disabled Join control (ROOM-04, Surface D). All three FAIL RED against the skeletons/cosmetic implementation.</action>
  <verify>
    <automated>npx vitest run src/components/AgentVoiceCard.test.tsx src/hooks/useWarRoomVoice.test.ts src/pages/WarRoom.test.tsx 2>&1 | grep -Eq "fail|FAIL|✗|×|expected"</automated>
  </verify>
  <acceptance_criteria>
    - AgentVoiceCard.test.tsx asserts known + unknown identity resolution and never asserts a raw-id display name.
    - useWarRoomVoice.test.ts asserts Bearer-authed token fetch, no `setMicrophoneEnabled(true)` on join, and toggleMute behavior.
    - WarRoom.test.tsx asserts deep-link auto-select and closed-room "Room Ended" + disabled Join.
    - All three files RUN and FAIL on assertions (RED).
  </acceptance_criteria>
  <done>Three RED test files run and fail on behavior; full suite import-clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → mocked SDK | Tests substitute a stub for `livekit-client`; no real WebRTC/network |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-90-AUTH | Spoofing | token fetch in voice hook | mitigate | useWarRoomVoice.test.ts asserts the token fetch carries `Authorization: Bearer` (locks the contract Plan 04 must satisfy). |
| T-90-MIC | Spoofing/Repudiation | mic on join | mitigate | useWarRoomVoice.test.ts asserts mic is NOT enabled on connect (join-muted contract for Plan 04). |
| T-90-TEST | Tampering | jsdom test env | accept | Stub SDK has no media/network access; nothing leaves the test process. |
</threat_model>

<verification>
- `npx tsc --noEmit` passes (skeletons + getColor export compile).
- `npm test` runs the full suite without transform/import crashes; the five new files are RED on behavior.
</verification>

<success_criteria>
Shared livekit mock registered; getColor exported; warRoomIdentity + useWarRoomVoice skeletons compile; five Wave-0 test files exist and fail RED on behavior (not imports). Downstream plans now have automated gates.
</success_criteria>

<output>
Create `.planning/phases/90-agent-room-war-room/90-02-SUMMARY.md` when done.
</output>
