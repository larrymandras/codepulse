---
phase: 90-agent-room-war-room
plan: 04
type: execute
wave: 3
depends_on: ["90-01", "90-02"]
files_modified:
  - C:/Users/mandr/astridr-repo/astridr/api/war_room_routes.py
  - src/hooks/useWarRoomVoice.ts
  - src/components/VoiceControlBar.tsx
autonomous: true
requirements: [ROOM-03]
user_setup:
  - service: livekit
    why: "Operator two-way voice join requires a reachable LiveKit server"
    env_vars:
      - name: LIVEKIT_PUBLIC_URL
        source: "astridr-repo Docker Compose / deployment env — browser-reachable LiveKit ws URL (defaults to ws://localhost:7880 in dev)"

must_haves:
  truths:
    - "POST /api/war-room/{room_name}/token mints a room-scoped LiveKit token for an existing room, behind the web channel Bearer middleware"
    - "The token endpoint rejects malformed room_name (path traversal / non-alphanumeric-hyphen)"
    - "useWarRoomVoice.join fetches the token with Authorization: Bearer then connects, joining MUTED (mic never enabled on connect)"
    - "useWarRoomVoice disconnects cleanly (room + attached audio elements) on leave/unmount"
    - "VoiceControlBar renders connection-state UI driven by a connectionState prop"
  artifacts:
    - path: "src/hooks/useWarRoomVoice.ts"
      provides: "LiveKit Room lifecycle hook (join-muted, subscribe audio, leave)"
      exports: ["useWarRoomVoice"]
    - path: "C:/Users/mandr/astridr-repo/astridr/api/war_room_routes.py"
      provides: "POST /{room_name}/token join-token endpoint"
      contains: "/{room_name}/token"
  key_links:
    - from: "src/hooks/useWarRoomVoice.ts"
      to: "POST /api/war-room/{room}/token"
      via: "fetch with authHeaders()"
      pattern: "war-room/.*token"
    - from: "src/hooks/useWarRoomVoice.ts"
      to: "livekit-client Room.connect"
      via: "room.connect(url, token) without setMicrophoneEnabled(true)"
      pattern: "connect"
---

<objective>
Build the genuine operator Join (ROOM-03): the cross-repo astridr-repo token endpoint (D-02), the `useWarRoomVoice` LiveKit lifecycle hook (join-muted per D-03, subscribe agent audio, clean teardown), and the `VoiceControlBar` connection-state UI. Turns `src/hooks/useWarRoomVoice.test.ts` GREEN. WarRoom.tsx wiring is Plan 07.

Purpose: Replace the cosmetic `setIsJoined(true)` with a real LiveKit voice connection.
Output: Token endpoint, voice hook, connection-state-aware control bar.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/90-agent-room-war-room/90-RESEARCH.md
@.planning/phases/90-agent-room-war-room/90-PATTERNS.md
@.planning/phases/90-agent-room-war-room/90-UI-SPEC.md

<interfaces>
<!-- Hook contract (RESEARCH Pattern 2) + auth + LiveKit API. -->
useWarRoomVoice() → { connectionState: 'disconnected'|'connecting'|'connected'|'reconnecting'|'failed'; isMuted; join(roomName):Promise<void>; leave():Promise<void>; toggleMute():Promise<void> }.
authHeaders() (src/lib/astridrApi.ts:117-121) → adds Content-Type + Authorization: Bearer ${VITE_ASTRIDR_API_KEY}; astridrApiBase() → base URL. DO NOT re-implement.
LiveKit (livekit-client): new Room({adaptiveStream,dynacast}); RoomEvent.ConnectionStateChanged|TrackSubscribed|TrackUnsubscribed|Disconnected; ConnectionState enum; Track.Kind.Audio; track.attach()/detach(); room.localParticipant.setMicrophoneEnabled(bool); room.connect(url,token); room.disconnect().
astridr token: generate_participant_token(room_name, identity) (token.py) → room-scoped, room_join+can_publish+can_subscribe, TTL 3600. web.py:677-694 auth_check Bearer middleware auto-protects /api/* (no per-route auth code).
Existing VoiceControlBarProps: { isJoined, isMuted, onJoin, onLeave, onToggleMute }.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: astridr-repo POST /{room_name}/token endpoint (D-02)</name>
  <read_first>
    - C:/Users/mandr/astridr-repo/astridr/api/war_room_routes.py (existing POST create + DELETE; imports list; _require_war_room guard lines 35-39)
    - C:/Users/mandr/astridr-repo/astridr/channels/war_room/token.py (generate_participant_token)
    - C:/Users/mandr/astridr-repo/astridr/channels/war_room/dispatcher.py:119-125 (return shape { token, url })
    - .planning/phases/90-agent-room-war-room/90-PATTERNS.md § astridr/api/war_room_routes.py + Critical Note N2
  </read_first>
  <files>C:/Users/mandr/astridr-repo/astridr/api/war_room_routes.py</files>
  <action>Add `import os` near the top of the module (Pitfall 3 / N2 — currently missing). Append a `JoinTokenRequest(BaseModel)` with optional `identity: str | None = None`, and a route `@router.post("/{room_name}/token")` `async def get_join_token(room_name, req)` that: (1) calls `_require_war_room()` first (503 guard, same as siblings); (2) VALIDATES `room_name` — reject if it contains `..`, `/`, or any character outside `[A-Za-z0-9-]` by raising `HTTPException(status_code=400, detail="invalid room name")` BEFORE calling token mint (V5 Input Validation / T-90-PATH path-traversal mitigation — this is a security requirement, add it even though the RESEARCH sample omits it); (3) sets `identity = req.identity or "operator"`; (4) imports and calls `generate_participant_token(room_name, identity)` (do NOT weaken its grants/TTL — room-scoped, 3600s, room_join+can_publish+can_subscribe); (5) reads `os.environ.get("LIVEKIT_PUBLIC_URL", "ws://localhost:7880")`; (6) logs `api.war_room.token_issued` with room_name + identity but NEVER the token (T-90-TOK); (7) returns `{ "token": token, "url": public_lk_url }`. No per-route auth code — the web channel Bearer middleware covers it.</action>
  <verify>
    <automated>cd C:/Users/mandr/astridr-repo && python -c "import ast,sys; ast.parse(open('astridr/api/war_room_routes.py').read())" && grep -nE "/\{room_name\}/token|import os|invalid room name" astridr/api/war_room_routes.py</automated>
  </verify>
  <acceptance_criteria>
    - `import os` present; `@router.post("/{room_name}/token")` defined.
    - room_name validation rejects `..`, `/`, and non-`[A-Za-z0-9-]` with HTTP 400 before token mint.
    - Returns `{ token, url }`; `generate_participant_token` grants/TTL unchanged; token never logged.
    - File parses (ast) without syntax error. (Live curl is in Plan 08 manual checkpoint.)
  </acceptance_criteria>
  <done>Token endpoint added with path-param validation; parses clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: useWarRoomVoice LiveKit lifecycle hook (join muted)</name>
  <read_first>
    - src/hooks/useWarRoomVoice.ts (skeleton from Plan 02)
    - src/hooks/useWarRoomVoice.test.ts (RED — the contract to satisfy)
    - src/hooks/useTtsPlayback.ts (ref-for-stateful-resource + cleanup-on-unmount pattern)
    - src/lib/astridrApi.ts:117-136 (authHeaders, astridrApiBase, apiRequest)
    - .planning/phases/90-agent-room-war-room/90-PATTERNS.md § src/hooks/useWarRoomVoice.ts + § Ref-Lifecycle Cleanup; RESEARCH Pattern 1 + Pitfalls 1/Anti-patterns
  </read_first>
  <files>src/hooks/useWarRoomVoice.ts</files>
  <behavior>
    - join(roomName): fetch POST /api/war-room/{encoded room}/token with authHeaders() (Bearer) + body {identity:"operator"}; on ok, new Room({adaptiveStream,dynacast}), wire events, room.connect(url,token); set connectionState via ConnectionStateChanged; DO NOT call setMicrophoneEnabled(true) (join muted, isMuted starts true).
    - join is gated: no-op if connectionState !== 'disconnected' (prevents double-connect).
    - TrackSubscribed(audio): track.attach() → append element, track refs; TrackUnsubscribed: detach; Disconnected: remove all attached audio elements.
    - leave(): room.disconnect(), clear refs, connectionState 'disconnected'. Unmount cleanup disconnects too.
    - toggleMute(): setMicrophoneEnabled(!current) and flip isMuted.
    - token fetch failure → connectionState 'failed'.
  </behavior>
  <action>Implement the hook replacing the Plan 02 skeleton. Store the `Room` in a `useRef` (never in render body — Anti-pattern), keep an audio-elements ref array for cleanup. Map the SDK `ConnectionState` enum to the `VoiceConnectionState` string union (SignalReconnecting → 'reconnecting'; connect throw / token failure → 'failed'). Use `authHeaders()` + `astridrApiBase()` from `@/lib/astridrApi` (do not re-implement). Gate `join()` on `connectionState === 'disconnected'`. Provide unmount cleanup via `useEffect(()=>()=>{ roomRef.current?.disconnect() },[])`. Never log the token.</action>
  <verify>
    <automated>npx vitest run src/hooks/useWarRoomVoice.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - voice-join-flow, voice-join-muted, voice-toggle-mute tests GREEN.
    - Token fetch uses `authHeaders()` (Bearer); `setMicrophoneEnabled(true)` is never called during `join`.
    - Room stored in a ref; leave/unmount call `room.disconnect()` and clean up audio elements.
    - `npx tsc --noEmit` passes.
  </acceptance_criteria>
  <done>Hook joins muted, subscribes audio, tears down cleanly; tests green.</done>
</task>

<task type="auto">
  <name>Task 3: VoiceControlBar connection-state UI + muted-default affordance</name>
  <read_first>
    - src/components/VoiceControlBar.tsx (full current file — props lines 15-21, render 52-88)
    - .planning/phases/90-agent-room-war-room/90-UI-SPEC.md § Surface A (Connection-State UI) + § Surface B (Muted-by-Default) + § Copywriting Contract
  </read_first>
  <files>src/components/VoiceControlBar.tsx</files>
  <action>Extend `VoiceControlBarProps` with `connectionState?: "connecting"|"connected"|"reconnecting"|"failed"` and `onRetry?: () => void`. Render Surface A inline (left `flex-1` region when joined): connecting → `Loader2 animate-spin` + "Connecting to voice…" with mute/leave disabled (`opacity-50 pointer-events-none`); connected → 2px filled dot `bg-(--status-ok)`, no text; reconnecting → pulsing dot + "Reconnecting…" (controls enabled); failed → `AlertCircle` + "Connection failed — check your network and try again" + a ghost "Retry Connection" button calling `onRetry`. Add Surface B muted-default affordance: pre-join show "You'll join muted" (`text-sm text-muted-foreground`, centered below the Join button); post-join, drive the existing mute toggle icon color per UI-SPEC (amber `MicOff` highlight first 5s via a local `useState` timer, then default `MicOff` muted / `Mic` `text-(--status-ok)` live). Use Lucide icons only and `var(--status-*)` tokens (no hex). Respect `prefers-reduced-motion` (static icons). Keep the existing 44px touch targets and 3s leave-confirm.</action>
  <verify>
    <automated>npx tsc --noEmit && grep -nE "connectionState|You'll join muted|Retry Connection|status-ok|status-warn" src/components/VoiceControlBar.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Props include `connectionState` + `onRetry`; all four states render per Surface A with correct copy from the Copywriting Contract.
    - "You'll join muted" sub-label shown pre-join; 5s amber muted highlight window post-join.
    - Only Lucide icons + `var(--status-*)` tokens (no hardcoded hex); reduced-motion honored; 44px targets + 3s confirm preserved.
    - `npx tsc --noEmit` passes.
  </acceptance_criteria>
  <done>VoiceControlBar renders connection-state + muted-default UI; tsc clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CodePulse browser → astridr /api token endpoint | Authenticated cross-repo HTTP; room_name is attacker-influenced path input |
| browser → LiveKit server | Token-scoped WebRTC media join |
| operator mic → live agent room | Audio broadcast into a live boardroom |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-90-PATH | Elevation of Privilege | `room_name` path param in token endpoint | mitigate | Reject `..`, `/`, non-`[A-Za-z0-9-]` with HTTP 400 before mint (Task 1); LiveKit token stays room-scoped. |
| T-90-AUTH | Spoofing | token fetch from CodePulse | mitigate | `authHeaders()` sends `Authorization: Bearer ${VITE_ASTRIDR_API_KEY}`; endpoint behind web.py Bearer middleware (Task 1/2). |
| T-90-SCOPE | Elevation of Privilege | LiveKit grant/TTL | mitigate | Reuse `generate_participant_token` unchanged (room_join+can_publish+can_subscribe, no admin, 3600s) — explicitly not weakened (Task 1). |
| T-90-MIC | Spoofing / unintended broadcast | mic on join | mitigate | Join muted: `setMicrophoneEnabled(true)` never called on connect; explicit unmute only; 5s amber warning (Task 2/3, D-03). |
| T-90-TOK | Information Disclosure | LiveKit token | mitigate | Never log the token (endpoint logs room_name/identity only; hook never logs token). |
</threat_model>

<verification>
- `npx vitest run src/hooks/useWarRoomVoice.test.ts` GREEN.
- `npx tsc --noEmit` passes.
- `python -c "import ast; ast.parse(open(...war_room_routes.py).read())"` succeeds; grep confirms route + validation + `import os`.
</verification>

<success_criteria>
Token endpoint mints a validated room-scoped token behind Bearer auth; useWarRoomVoice joins muted, subscribes agent audio, and tears down cleanly; VoiceControlBar surfaces connection state + muted-default affordance. ROOM-03 hook tests green. (Live two-way audio verified in Plan 08.)
</success_criteria>

<output>
Create `.planning/phases/90-agent-room-war-room/90-04-SUMMARY.md` when done.
</output>
