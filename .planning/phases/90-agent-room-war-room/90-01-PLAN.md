---
phase: 90-agent-room-war-room
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - convex/schema.ts
  - convex/_generated/api.d.ts
  - convex/_generated/dataModel.d.ts
autonomous: false
requirements: [ROOM-03, ROOM-04]
user_setup: []

must_haves:
  truths:
    - "livekit-client@2.20.0 is installed at a pinned exact version and npm audit is clean"
    - "warRoomEvents has an optional seq field and a by_room_seq index in the live Convex deployment"
    - "Generated Convex types include warRoomEvents.seq so downstream code typechecks"
  artifacts:
    - path: "convex/schema.ts"
      provides: "warRoomEvents.seq + by_room_seq index"
      contains: "by_room_seq"
    - path: "package.json"
      provides: "pinned livekit-client dependency"
      contains: "livekit-client"
  key_links:
    - from: "convex/schema.ts warRoomEvents"
      to: "convex/_generated/dataModel"
      via: "npx convex dev / codegen regenerates types"
      pattern: "seq"
---

<objective>
Lay the foundation the whole phase builds on: install the one new dependency (`livekit-client`, behind a supply-chain legitimacy gate) and make the additive Convex schema change that unblocks deterministic transcript ordering (ROOM-04) — the `seq` field and `by_room_seq` index on `warRoomEvents` — then regenerate/deploy Convex types so every later plan typechecks against the live deployment.

Purpose: Both changes are upstream dependencies of multiple later plans; doing them first removes cross-plan churn.
Output: Pinned `livekit-client`, `warRoomEvents.seq` + `by_room_seq` index live, regenerated `_generated` types.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/90-agent-room-war-room/90-RESEARCH.md
@.planning/phases/90-agent-room-war-room/90-PATTERNS.md

<interfaces>
<!-- Forge precedent for the seq pattern (schema.ts:1541-1549) — the exact shape to mirror. -->
forgeLogChunks defines `seq: v.number()` (required) + index `by_host_job_seq: ["hostId","forgeJobId","seq"]`.
warRoomEvents (schema.ts:1290-1300) currently has NO seq, indexes: `by_room: ["roomId","timestamp"]`, `by_timestamp: ["timestamp"]`.
Critical Note N3 (PATTERNS.md): seq MUST be `v.optional(v.number())` not `v.number()` — existing rows have no seq; a required field rejects existing data (Pitfall 5).
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Supply-chain legitimacy gate for livekit-client</name>
  <action>PAUSE before any install. Present the npmjs.com legitimacy evidence below and wait for operator approval; do NOT run `npm install livekit-client` until approved. This gate is never auto-approvable (ignore auto_advance).</action>
  <what-built>Nothing yet — this gate runs BEFORE any install. `livekit-client` is tagged `[ASSUMED]` in 90-RESEARCH.md § Package Legitimacy Audit (slopcheck was unavailable at research time).</what-built>
  <how-to-verify>
    1. Open https://www.npmjs.com/package/livekit-client
    2. Confirm: publisher is the `livekit` org; license Apache-2.0; repository `github.com/livekit/client-sdk-js`; version 2.20.0 exists; substantial weekly download count; continuous release history.
    3. Confirm this matches 90-RESEARCH.md § Package Legitimacy Audit (official LiveKit org, Apache-2.0, created 2021-01-24).
  </how-to-verify>
  <resume-signal>Type "approved" to authorize `npm install livekit-client@2.20.0`, or describe concerns.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Install pinned livekit-client + audit</name>
  <read_first>
    - package.json (current dependency block; confirm livekit-client absent)
    - .planning/phases/90-agent-room-war-room/90-RESEARCH.md § Standard Stack + Package Legitimacy Audit
  </read_first>
  <files>package.json, package-lock.json</files>
  <action>Install `livekit-client` pinned to an EXACT version 2.20.0 (no caret) — add `"livekit-client": "2.20.0"` to dependencies (supply-chain pin per T-90-SC). Run `npm install livekit-client@2.20.0 --save-exact`. Then run `npm audit` and record the result in the SUMMARY; if any HIGH/CRITICAL advisory names `livekit-client`, stop and surface it. Do NOT add `@livekit/components-react` (RESEARCH: overkill — we wire existing War Room components).</action>
  <verify>
    <automated>node -e "const p=require('./package.json'); if(p.dependencies['livekit-client']!=='2.20.0'){process.exit(1)}" && npm ls livekit-client</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` dependencies has `"livekit-client": "2.20.0"` (exact, no `^`/`~`).
    - `npm ls livekit-client` resolves to 2.20.0.
    - `npm audit` output captured; no unaddressed HIGH/CRITICAL advisory attributable to livekit-client.
  </acceptance_criteria>
  <done>livekit-client@2.20.0 pinned and installed; audit recorded.</done>
</task>

<task type="auto">
  <name>Task 3: Add warRoomEvents.seq + by_room_seq index, redeploy Convex types</name>
  <read_first>
    - convex/schema.ts (warRoomEvents at lines 1290-1300; forgeLogChunks seq precedent at 1541-1549)
    - .planning/phases/90-agent-room-war-room/90-PATTERNS.md § convex/schema.ts (TARGET block) + Critical Note N3
  </read_first>
  <files>convex/schema.ts, convex/_generated/api.d.ts, convex/_generated/dataModel.d.ts</files>
  <action>In `warRoomEvents`, add field `seq: v.optional(v.number())` (optional, per N3 / Pitfall 5 — existing rows have no seq). Add index `by_room_seq` on `["roomId", "seq"]`. KEEP the existing `by_room` and `by_timestamp` indexes unchanged (legacy reads / backcompat). Then push the schema to the live deployment and regenerate generated types: run `npx convex dev --once` (or `npx convex codegen` if a deploy is not desired in this environment — prefer the deploy so the new index exists before later waves query it). Verify `convex/_generated/dataModel.d.ts` reflects the optional `seq` field on warRoomEvents. This is the ROOM-04 schema foundation; the seq-computing mutation and seq-ordered reads come in Plan 03.</action>
  <verify>
    <automated>grep -n "by_room_seq" convex/schema.ts && npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - `convex/schema.ts`: warRoomEvents declares `seq: v.optional(v.number())` and `.index("by_room_seq", ["roomId", "seq"])`; `by_room` and `by_timestamp` still present.
    - `npx tsc --noEmit` passes (generated types regenerated; no stale-type errors).
    - `npx convex dev --once` (or codegen) completed without schema-validation errors against existing data.
  </acceptance_criteria>
  <done>seq field + by_room_seq index live and reflected in generated types; tsc clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → local build | Third-party package code enters the build (supply chain) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-90-SC | Tampering | `npm install livekit-client` (new 2.20.0 release) | mitigate | Blocking-human legitimacy checkpoint (Task 1) verifying npmjs.com listing BEFORE install; pin exact 2.20.0 (no range); run `npm audit` (Task 2). Checkpoint is never auto-approvable. |
| T-90-DATA | Information Disclosure | additive schema migration | accept | `seq` is non-PII ordering metadata; optional field is additive and cannot reject/expose existing rows. |
</threat_model>

<verification>
- `npm ls livekit-client` → 2.20.0; package.json pin is exact.
- `grep by_room_seq convex/schema.ts` matches; `by_room` + `by_timestamp` retained.
- `npx tsc --noEmit` passes with regenerated types.
</verification>

<success_criteria>
livekit-client@2.20.0 pinned + audited; warRoomEvents has optional `seq` + `by_room_seq` index live in Convex with regenerated types; tsc clean. Phase foundation ready for Wave 2 test scaffolding.
</success_criteria>

<output>
Create `.planning/phases/90-agent-room-war-room/90-01-SUMMARY.md` when done.
</output>
