---
phase: 115-workspace-scanner
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - hooks/ingestPost.mjs
  - hooks/scanner.mjs
autonomous: true
requirements: []            # Phase 115 has NO mapped REQ-IDs (ROADMAP: "Requirements: TBD").
                            # Traceability runs through D-NN decision ids — see must_haves.truths.
decisions: [D-04]

must_haves:
  truths:
    - "D-04 is satisfiable: a single shared POST-with-bearer helper exists that both hooks/scanner.mjs and the future hooks/workspaceScan.mjs import, rather than two copies of the same 20 lines."
    - "The SessionStart path still works: hooks/scanner.mjs's runScan still POSTs the same body to the same /scan URL with the same bearer header and the same 3s abort timeout, proven by the pre-existing wire tests staying green untouched."
    - "hooks/scanner.mjs contains no fetch( call of its own after the extraction."
  artifacts:
    - path: "hooks/ingestPost.mjs"
      provides: "postSnapshot(endpointUrl, ingestKey, body, deps) — the single POST-with-bearer site for host-side scanners"
      exports: ["postSnapshot"]
      min_lines: 30
    - path: "hooks/scanner.mjs"
      provides: "runScan, now delegating its POST to hooks/ingestPost.mjs"
      contains: "from \"./ingestPost.mjs\""
  key_links:
    - from: "hooks/scanner.mjs"
      to: "hooks/ingestPost.mjs"
      via: "static ESM import of postSnapshot"
      pattern: "import \\{ postSnapshot \\} from \"\\./ingestPost\\.mjs\""
---

<objective>
Extract `hooks/scanner.mjs`'s inline POST-with-bearer block into a small shared module
`hooks/ingestPost.mjs`, so D-04's "import shared helpers rather than copying them" is actually
possible for the workspace scanner built in later plans.

Purpose: D-04 as written in CONTEXT.md rests on a premise that was measured FALSE at planning time —
`hooks/scanner.mjs` exports exactly ONE symbol (`runScan`, verified at `hooks/scanner.mjs:33`, the
only `^export` line in the file) and the POST block is inline inside `runScan` at
`hooks/scanner.mjs:220-241`. There are no shared helpers to import yet. This plan creates the one it
needs.

Output: `hooks/ingestPost.mjs` (new), `hooks/scanner.mjs` (edited to delegate).

**This is the highest-risk edit in Phase 115 and the ONLY plan that touches the awaited SessionStart
path.** `hooks/codepulse-hook.mjs:142-145` does `const { runScan } = await import(scannerPath); await
runScan(...)` when `resolvedEventType === "SessionStart"` — the call is **awaited inline in the
dispatcher**, so a regression here degrades every single session start on both launchers
(`codepulse-hook.mjs` is wired at *user* scope in `.claude/settings.json` AND
`.claude-alt/settings.json`). Behavior preservation is the entire acceptance bar.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/115-workspace-scanner/115-CONTEXT.md
@.planning/phases/115-workspace-scanner/115-PATTERNS.md

**Treat every code shape quoted in this plan as a DRAFT, not a spec.** Verify against the live files
in `<read_first>` and correct this plan where it is wrong. A prior 3,500-line plan in this ecosystem
carried 14 real defects, and every implementer who corrected it rather than transcribing it was
right. If you correct something, say so in the SUMMARY.
</context>

<interfaces>
<!-- The exact block being extracted. Verified live 2026-08-12 at hooks/scanner.mjs:220-241. -->
Current inline shape inside `runScan` (`hooks/scanner.mjs:220-241`), abbreviated:

- `const controller = new AbortController();`
- `const timeout = setTimeout(() => controller.abort(), 3000);`
- `try { const headers = { "Content-Type": "application/json" };`
- `if (ingestKey) headers["Authorization"] = \`Bearer ${ingestKey}\`;`
- `else console.warn("[codepulse-scanner] no ASTRIDR_INGEST_API_KEY set — posting unauthenticated (server may reject)");`
- `const resp = await fetch(\`${codepulseUrl}/scan\`, { method: "POST", headers, body: JSON.stringify(snapshot), signal: controller.signal });`
- `if (!resp.ok) console.error(\`[codepulse-scanner] /scan responded ${resp.status}: ${await resp.text()}\`);`
- `} catch (err) { console.error(\`[codepulse-scanner] /scan failed: ${err.message}\`); }`
- `} finally { clearTimeout(timeout); }`

Note it **never throws** — every failure path is caught and logged. That non-throwing contract is
load-bearing: `codepulse-hook.mjs` awaits `runScan` inline and a thrown error would surface on every
session start. Preserve it exactly.

The existing regression net, `hooks/__tests__/scanner.test.mjs`, stands up a REAL `node:http` server
on `127.0.0.1:0`, calls `runScan("sess-wire-1", url, "test-key", { home, cwd })`, and asserts on the
**actual received POST body bytes** (`body.scannedOrigins`, `body.scannedOriginsComplete`,
`body.skills`). So it does genuinely exercise the extracted path end-to-end — a broken extraction
fails these tests rather than passing silently. Its header records that `scanner.mjs` had ZERO test
coverage before Phase 113 added it, so this suite is the only net that exists.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Extract postSnapshot into hooks/ingestPost.mjs</name>
  <files>hooks/ingestPost.mjs, hooks/scanner.mjs</files>
  <read_first>
    - `hooks/scanner.mjs` — read the whole file. You must see the real current state of `runScan`, its
      parameters, and where the POST block sits relative to the snapshot construction. Pay attention
      to the header comment at `:5-10`.
    - `hooks/codepulse-hook.mjs:130-155` — the awaited SessionStart call site you must not regress.
    - `hooks/__tests__/scanner.test.mjs` — the regression net. Understand exactly what it asserts
      before you change anything.
    - `.planning/phases/115-workspace-scanner/115-PATTERNS.md` § `hooks/workspaceScan.mjs` — the
      analog analysis for this extraction, including the no-shebang landmine.
  </read_first>
  <action>
Create `hooks/ingestPost.mjs` exporting exactly one function:

`export async function postSnapshot(endpointUrl, ingestKey, body, deps = {})`

- `endpointUrl` is the **full** URL including the path (e.g. `http://host/scan`). Do NOT re-append a
  path inside the helper — the caller owns the route, because plan 115-09 will pass
  `${codepulseUrl}/workspace-ingest` through this same helper.
- `deps` destructures with defaults: `{ timeoutMs = 3000, logPrefix = "codepulse-scanner", fetchImpl = fetch }`.
  Injectable `fetchImpl` follows the established injectable-deps pattern at `hooks/scanner.mjs:33-38`.
- Behavior must be byte-for-byte identical to the current inline block for the `scanner.mjs` call
  site, including the exact console strings. Concretely, with `logPrefix = "codepulse-scanner"` the
  helper must emit:
  - on missing key: `[codepulse-scanner] no ASTRIDR_INGEST_API_KEY set — posting unauthenticated (server may reject)` via `console.warn`
  - on non-ok response: `[codepulse-scanner] /scan responded {status}: {text}` via `console.error` — build
    this from the endpoint's pathname, NOT a hardcoded `/scan`, so `/workspace-ingest` logs correctly
    later. Deriving the pathname via `new URL(endpointUrl).pathname` is acceptable; if that throws on
    a malformed URL, fall back to the raw `endpointUrl` string rather than throwing.
  - on thrown error: `[codepulse-scanner] {pathname} failed: {err.message}` via `console.error`
- **The function must NEVER throw.** Wrap everything in try/catch/finally exactly as the current block
  does, and `clearTimeout` in `finally`.
- Return `{ ok: boolean, status: number | null }` — additive; the current call site ignores the
  return value, so adding one cannot regress it, and 115-09's entry point will use it.
- **Do NOT add a shebang.** `hooks/scanner.mjs:5-10` documents why: Vite/Rolldown's SSR module
  transform (used by `hooks/__tests__/*.test.mjs`) hoists imports above line 1 and a shebang there
  breaks parsing with `Invalid Character '!'`. This module will be imported by tests the same way.
- Write a header comment recording that this module was extracted from `hooks/scanner.mjs:220-241` in
  Phase 115 per D-04, that it is on the awaited SessionStart path via
  `hooks/codepulse-hook.mjs:142-145`, and that it must never throw.

Then edit `hooks/scanner.mjs`:
- Add a static top-level `import { postSnapshot } from "./ingestPost.mjs";` alongside the existing
  imports at `:12-18`. Static, not dynamic — a dynamic import inside `runScan` would add resolution
  work to the awaited hook path.
- Replace the `:220-241` block with a single `await postSnapshot(\`${codepulseUrl}/scan\`, ingestKey, snapshot);`
- Change NOTHING else in the file. No reordering, no comment rewrites, no snapshot-shape edits.
  `git diff hooks/scanner.mjs` must show only the added import line and the replaced block.
  </action>
  <verify>
    <automated>npx vitest run hooks/__tests__/scanner.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run hooks/__tests__/scanner.test.mjs` passes with **zero** edits to
      `hooks/__tests__/scanner.test.mjs` (`git diff --name-only` must not list that file).
    - `grep -c "fetch(" hooks/scanner.mjs` returns `0` — no POST site remains in `scanner.mjs`.
      (If a comment mentions `fetch(`, filter comments: `grep -v '^\s*[/*]' hooks/scanner.mjs | grep -c "fetch("` must be `0`.)
    - `grep -c "AbortController" hooks/scanner.mjs` returns `0`.
    - `grep -Fc 'import { postSnapshot } from "./ingestPost.mjs"' hooks/scanner.mjs` returns `1`.
    - `hooks/ingestPost.mjs` first line is NOT `#!` — `head -c 2 hooks/ingestPost.mjs` is not `#!`.
    - `grep -c "^export" hooks/ingestPost.mjs` returns `1` and that export is `postSnapshot`.
    - `git diff --numstat hooks/scanner.mjs` shows a net line reduction (deletions exceed additions).
    - `npx tsc --noEmit` exits 0 (no new type errors introduced repo-wide).
  </acceptance_criteria>
  <done>`hooks/ingestPost.mjs` exists exporting only `postSnapshot`; `hooks/scanner.mjs` delegates to it and its pre-existing wire tests pass unmodified.</done>
</task>

<task type="auto">
  <name>Task 2: Prove behavior preservation, or take D-04's sanctioned fallback</name>
  <files>hooks/__tests__/ingestPost.test.mjs</files>
  <read_first>
    - `hooks/ingestPost.mjs` — the module you just wrote.
    - `hooks/scanner.mjs` — the edited file.
    - `.planning/phases/115-workspace-scanner/115-CONTEXT.md` § D-04 correction 2 — the fallback
      authorization and its conditions.
  </read_first>
  <action>
Add a dedicated test file `hooks/__tests__/ingestPost.test.mjs` covering `postSnapshot` directly
against a real `node:http` server on `127.0.0.1:0` (same fixture idiom as
`hooks/__tests__/scanner.test.mjs:33-41`), with these cases:

1. **Bearer present:** server receives `Authorization: Bearer test-key` and the exact JSON body;
   `postSnapshot` returns `{ ok: true, status: 200 }`.
2. **Bearer absent (control):** call with `ingestKey` empty/undefined; server receives NO
   `authorization` header. Assert the request arrived anyway (the current code warns but still POSTs
   — do not turn this into a refusal, that would be a behavior change).
3. **Non-2xx:** server replies 401; `postSnapshot` returns `{ ok: false, status: 401 }` and does NOT
   throw.
4. **Unreachable endpoint:** point at a closed port; `postSnapshot` returns `{ ok: false, status: null }`
   and does NOT throw. This is the load-bearing non-throwing assertion for the SessionStart path.
5. **Timeout:** inject `deps.timeoutMs = 20` against a server that deliberately never responds;
   assert `postSnapshot` resolves (does not hang, does not throw) within the test timeout.
6. **Path is caller-owned (control for the later reuse):** call with an endpoint ending
   `/workspace-ingest` and assert the server's `req.url` is `/workspace-ingest`, proving the helper
   does not hardcode `/scan`.

**Fallback authorization (D-04, explicit).** If after two genuine correction attempts
`hooks/__tests__/scanner.test.mjs` cannot be made green by the extraction, STOP extracting and take
D-04's sanctioned fallback instead: revert `hooks/scanner.mjs` to HEAD
(`git checkout HEAD -- hooks/scanner.mjs`), keep `hooks/ingestPost.mjs` as a standalone module used
only by the workspace scanner, and record in the SUMMARY that the ~20 lines are now duplicated
because "duplicating 20 lines is a smaller risk than regressing every session start" (CONTEXT.md
D-04). Do not spend a third attempt, and do not weaken or edit `scanner.test.mjs` to make the
extraction pass — editing the only regression net to accommodate the change is the failure mode this
task exists to prevent.
  </action>
  <verify>
    <automated>npx vitest run hooks/__tests__/ingestPost.test.mjs hooks/__tests__/scanner.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run hooks/__tests__/ingestPost.test.mjs` passes with all 6 cases present (case 1 is
      the passing control that makes cases 3/4/5's failure assertions meaningful).
    - `npx vitest run hooks/__tests__/scanner.test.mjs` still passes; `git diff HEAD --name-only` does
      NOT list `hooks/__tests__/scanner.test.mjs`.
    - `npm test` exits 0 (full suite; no collateral regression elsewhere).
    - Case 4 asserts on the RETURN VALUE, not on a caught exception — a `expect(...).rejects` shape
      would mean the non-throwing contract was broken.
    - If the fallback was taken, the SUMMARY states so explicitly and `git diff HEAD --stat` shows
      `hooks/scanner.mjs` unchanged.
  </acceptance_criteria>
  <done>`postSnapshot` is directly tested for all six behaviors including the non-throwing contract, and the pre-existing scanner wire tests are green without having been touched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host script → Convex HTTP route | Bearer-authenticated write crosses the network here |
| SessionStart hook dispatcher → scanner module | An exception or a slow call here degrades every session start on both launchers |

## STRIDE Threat Register (ASVS L1, block on high)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-115-01-01 | Information Disclosure | `postSnapshot`'s error logging | mitigate | Log only `err.message`, `resp.status` and the response text — never the request `body`, never the `ingestKey`. An accepted response text could echo input, so do not add body echoing to the log. |
| T-115-01-02 | Information Disclosure | the bearer token | mitigate | `ingestKey` appears only in the `Authorization` header value; never in a log line, never in the returned object. Assert this by reading the final module — no template string interpolating `ingestKey` outside the header. |
| T-115-01-03 | Denial of Service | awaited SessionStart path | mitigate | Preserve the 3s `AbortController` default and the never-throws contract; case 4 and case 5 tests are the proof. A hang here blocks the hook dispatcher directly (`codepulse-hook.mjs:142-145` awaits it). |
| T-115-01-04 | Tampering | the only regression net | mitigate | `hooks/__tests__/scanner.test.mjs` must not be edited by this plan; an acceptance criterion asserts it is absent from `git diff --name-only`. |
| T-115-01-SC | Tampering | npm installs | accept | This plan installs nothing. No `package.json` change; no Package Legitimacy Gate applies. |
</threat_model>

<verification>
- `npx vitest run hooks/__tests__/` — all hook tests green.
- `npm test` — full suite green.
- `npx tsc --noEmit` — clean.
- `git diff HEAD --stat` — exactly `hooks/ingestPost.mjs` (new), `hooks/scanner.mjs` (net reduction),
  `hooks/__tests__/ingestPost.test.mjs` (new). Nothing else.
- Manual sanity, cheap: `node hooks/scanner.mjs manual-scan --dry-run` still runs without throwing
  (the `isDirectRun` branch at `hooks/scanner.mjs:280-338` is untouched by this plan; confirm it still
  reaches its dry-run output).
</verification>

<success_criteria>
- A single shared `postSnapshot` exists and is the only POST site for host-side scanner traffic.
- `hooks/scanner.mjs`'s wire behavior is unchanged, proven by its pre-existing tests passing
  unmodified.
- `postSnapshot` has its own tests including a passing control and an explicit never-throws
  assertion.
- OR: the D-04 fallback was taken, `scanner.mjs` is byte-identical to HEAD, and the SUMMARY says so.
</success_criteria>

<output>
Create `.planning/phases/115-workspace-scanner/115-01-SUMMARY.md` when done. Record explicitly
whether the extraction landed or the fallback was taken, and any place this plan's draft was wrong.
</output>
