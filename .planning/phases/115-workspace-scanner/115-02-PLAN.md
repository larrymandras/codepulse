---
phase: 115-workspace-scanner
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - config/workspace.json
  - config/workspace.local.json
  - .gitignore
  - hooks/workspaceConfig.mjs
  - hooks/__tests__/workspaceConfig.test.mjs
autonomous: true
requirements: []            # No mapped REQ-IDs for Phase 115 — traceability is via D-NN.
decisions: [D-06, D-08, D-14, D-15, D-16, D-17]

must_haves:
  truths:
    - "D-08 holds: the classification rules live in a JSON config checked into this repo, reviewable in a diff, and the classifier can be a pure function of (path, config) because the config is data not code."
    - "D-17 holds: the config is SPLIT — a tracked rules file plus a gitignored local root list — and .gitignore is updated in the same commit that creates the local file, so the sensitive root names are never published to this PUBLIC repo."
    - "D-17's fail-closed rule holds: a missing or malformed config/workspace.local.json yields exactly the tracked roots and never throws; it never yields 'scan everything unclassified'."
    - "D-06 holds: the walk's roots are a named explicit list plus an excludeDirs set, never discovered."
    - "D-14 holds: root-to-department assignment is an explicit map and an unmapped root resolves to the literal department 'Unclassified', never silently into Work, Consulting or Personal."
    - "D-15 holds: the vault, .claude and .claude-alt are each declared with department 'Unclassified' at the root level."
    - "D-16 holds: every ambiguous root under C:/Users/mandr is a DECLARED root mapped to Unclassified in the gitignored local file — never omitted, never guessed into a real department."
  artifacts:
    - path: "config/workspace.json"
      provides: "Tracked rules: schemaVersion, snapshotId, departments, excludeDirs, excludeFiles, shareableAllowlist, non-sensitive roots, composeFile"
      contains: "\"schemaVersion\""
    - path: "config/workspace.local.json"
      provides: "Gitignored local root list — the sensitive/ambiguous roots and their department mapping"
      contains: "\"roots\""
    - path: "hooks/workspaceConfig.mjs"
      provides: "loadWorkspaceConfig / mergeWorkspaceConfig — fail-closed tracked-then-local merge"
      exports: ["loadWorkspaceConfig", "mergeWorkspaceConfig", "DEPARTMENTS", "UNCLASSIFIED"]
    - path: ".gitignore"
      provides: "Ignore rules for the local config, the dry-run report and the approval marker"
      contains: "config/workspace.local.json"
  key_links:
    - from: "hooks/workspaceConfig.mjs"
      to: "config/workspace.json"
      via: "existsSync + readFileSync + JSON.parse, wrapped"
      pattern: "workspace\\.json"
    - from: ".gitignore"
      to: "config/workspace.local.json"
      via: "ignore rule preventing publication to a public repo"
      pattern: "config/workspace\\.local\\.json"
---

<objective>
Define the split classification config (D-08 + D-17) and the fail-closed loader that merges its two
halves, and add the `.gitignore` rules that keep the sensitive half out of this **public** repo.

Purpose: this is the contract every later plan reads. The classifier (115-05) is a pure function of
`(path, config)` only because the config is declarative data; the walk (115-07) gets its root list
and exclude set from here; the dry-run report (115-07) reports root→department from here so Larry can
re-map in one local edit. Getting the shape right first is what lets those plans run without
inventing it.

Output: `config/workspace.json` (tracked), `config/workspace.local.json` (gitignored),
`.gitignore` (edited), `hooks/workspaceConfig.mjs` + its tests.

**`larrymandras/codepulse` is a PUBLIC repo** (`gh repo view` → `"visibility":"PUBLIC"`, measured at
planning time). D-16's root list is an inventory of Larry's project directory names, several of which
read like client engagements, and 7 of those ~10 names appear in **zero** tracked files at HEAD.
Committing them would be a new, permanent, one-way disclosure — git history survives deletion. The
`.gitignore` edit is not hygiene here; it is the control.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/115-workspace-scanner/115-CONTEXT.md
@.planning/phases/115-workspace-scanner/115-RESEARCH.md
@.planning/phases/115-workspace-scanner/115-PATTERNS.md
@./CLAUDE.md

**Treat the JSON shapes below as a DRAFT.** They are a concrete proposal within Claude's Discretion
(CONTEXT.md explicitly defers "the config file's exact schema and location" and "the concrete
allowlist patterns" to the planner, validated by D-12's dry-run before first ingest). If you find a
better shape while reading the analogs, take it and say so in the SUMMARY — but do not weaken any
fail-closed property.
</context>

<interfaces>
<!-- Verified live 2026-08-12. `config/` does not exist in this repo yet (`ls config` → No such
     file or directory), so this is a genuinely new location; there is no in-repo copy source. -->

The `.gitignore` analog, verified exact at `.gitignore:3-6`:

```
# Secrets: ignore all env files except the committed template (CSO-95-03)
.env
.env.*
!.env.example
```

Copy the **comment-then-pattern** convention including a decision-id citation, the way that block
cites `CSO-95-03`.

The fail-closed control-flow analog, `convex/ingestAuth.ts:76-85` (`validateIngestAuth`): absent
input returns the SAFE value, never throws, never widens scope, and relaxing it requires an explicit
opt-in. Copy that shape; the return type differs (this loader returns a merged config object plus a
mode flag, not a boolean).

The defensive-read analog, `hooks/scanner.mjs:288-304`: every config-file read is wrapped in
`existsSync` + `try/catch` so a missing or malformed file never crashes the scan. Copy the wrapping;
replace the regex-match approach with `JSON.parse` + explicit shape validation.

Departments vocabulary is fixed by D-07/D-14/D-15/D-16:
`["Work", "Consulting", "Personal", "Unclassified"]` — exactly these four strings, `"Unclassified"`
being the mandatory fallback, never a real department.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Write the tracked rules config and the .gitignore rules in one commit</name>
  <files>.gitignore, config/workspace.json</files>
  <read_first>
    - `.gitignore` — read the whole file. You are editing it; see the existing comment-then-pattern
      convention at `:3-6` and where new blocks are placed.
    - `.planning/phases/115-workspace-scanner/115-CONTEXT.md` § D-02, D-06, D-08, D-15, D-16, D-17 —
      the binding decisions. D-17 AMENDS D-08; read both.
    - `.planning/phases/115-workspace-scanner/115-RESEARCH.md` § "Recommended Project Structure" and
      § "Architectural Responsibility Map — Notes on D-09's actual mount surface".
    - `C:/Users/mandr/astridr-repo/docker-compose.yml` lines 320-390 and 527-554 — confirm the
      `composeFile` path you record is the real file and note that mount sources use
      `${VAR:-default}` syntax (115-05 parses it; you only record the path).
  </read_first>
  <action>
**Do BOTH edits in this one task and commit them together.** If `config/workspace.local.json` is
created before `.gitignore` names it, the very next `git add -A` in any concurrent session publishes
it. Order within the task: `.gitignore` FIRST, then the config files.

**Step 1 — `.gitignore`.** Append a new block using the existing comment-then-pattern convention,
citing the decision ids the way `:3` cites `CSO-95-03`:

```
# Phase 115 workspace scanner: the local root list holds Larry's project
# directory names (several read as client engagements) and this repo is PUBLIC —
# committing them is a one-way disclosure (D-17). The dry-run report and its
# approval marker are machine-local derived state (D-12), regenerated per run.
config/workspace.local.json
config/workspace-scan-report.json
config/workspace-scan.approved.sha256
```

Three explicit filenames, NOT a `config/*.local.json` glob — D-17 names exactly one local config and
PATTERNS.md's `.gitignore` analysis says do not glob unless a second file is anticipated.

**Step 2 — `config/workspace.json`** (tracked). Keys, exactly:

- `schemaVersion`: `1` (number). The loader refuses an unknown version — fail closed.
- `snapshotId`: `"larry-workspace"` (string). The fixed Convex snapshot id; must match the value
  `convex/workspace.ts` defaults to in plan 115-04.
- `departments`: `["Work", "Consulting", "Personal", "Unclassified"]`.
- `excludeDirs`: the donor's set adopted by D-06 — `node_modules`, `.git`, `.venv`, `__pycache__`,
  `.next`, `.cache`, `dist`, `coverage`, `.turbo`, `.pytest_cache`, `.idea`, `.vscode-server`, plus
  `graphify-out`, `.worktrees`, `node_modules.bak`, `venv`, `.mypy_cache`, `.ruff_cache`,
  `.gradle`, `build`, `out`, `target`, `.svelte-kit`, `.parcel-cache`, `.nuxt`. **No depth cap** —
  D-06 explicitly declines one, because a silent depth truncation presents as a mysteriously
  incomplete map.
- `excludeFiles`: `[".DS_Store", "Thumbs.db", "desktop.ini"]`.
- `shareableAllowlist`: an object with `default` and optional `byRoot`:
  - `default.requireNonDotBasename`: `true` — a basename beginning with `.` is never shareable. This
    single rule is what fails CLOSED on `.claude.json` and `.mcp.json`, both of which the donor's
    `SECRET_RE` measurably returned PUBLIC for.
  - `default.extensions`: documents, source and media ONLY —
    `.md .markdown .txt .rst .adoc .ts .tsx .js .jsx .mjs .cjs .py .rb .go .rs .java .kt .c .h .cpp .cs .swift .css .scss .less .html .vue .svelte .svg .png .jpg .jpeg .gif .webp .avif .ico .bmp .mp3 .wav .flac .mp4 .mov .webm .pdf .docx .xlsx .pptx .odt .woff .woff2 .ttf .otf .eot .ipynb .lock`
  - **The following extensions must NOT appear in the allowlist, and this is deliberate, not an
    omission to fill:** `.json .yml .yaml .toml .ini .cfg .conf .env .envfile .pem .key .crt .cer .p12 .pfx .ppk .sh .bash .zsh .ps1 .psm1 .bat .cmd .vbs .sql .db .sqlite .sqlite3 .log .csv .tsv .bak .keychain .kdbx`
    plus any extensionless file. Rationale: the classification decides whether a FILENAME is
    transmitted, and a config/script/credential filename is itself a disclosure —
    `generate_admin_key.sh` and `selfhosted.envfile` are the two measured proofs. Add a
    `"_excludedFromAllowlistRationale"` string field carrying that sentence so a future editor does
    not "helpfully" add `.json` back.
  - `byRoot`: `{}` for now. When present for a root id, the entry REPLACES `default` for that root
    (does not merge) — record that semantics in a `"_byRootSemantics"` string field.
- `roots`: an array of `{ id, path, department, evidence? }`. Only the **non-sensitive** roots go
  here, per D-17. Paths use FORWARD slashes (Pitfall 5: normalize to `/` everywhere; also avoids JSON
  backslash escaping). Exactly these five:
  - `{ "id": "vault", "path": "C:/Users/mandr/Mandras", "department": "Unclassified", "evidence": "D-15: demonstrably contains Work, Consulting and Personal material mixed together; any single root-level department label would be an assertion the scanner inferred rather than knew." }`
  - `{ "id": "claude", "path": "C:/Users/mandr/.claude", "department": "Unclassified", "evidence": "D-15" }`
  - `{ "id": "claude-alt", "path": "C:/Users/mandr/.claude-alt", "department": "Unclassified", "evidence": "D-15" }`
  - `{ "id": "codepulse", "path": "C:/Users/mandr/codepulse", "department": "Personal", "evidence": "CodePulse is the telemetry dashboard for Astridr, which Larry's global CLAUDE.md lists under Personal as 'Astridr (personal AI assistant framework)'." }`
  - `{ "id": "astridr-repo", "path": "C:/Users/mandr/astridr-repo", "department": "Personal", "evidence": "Larry's global CLAUDE.md lists 'Astridr (personal AI assistant framework)' under Personal." }`

  The two `Personal` assignments are **declared inferences with cited evidence**, not silent
  defaults — which is what D-14 forbids. D-12's dry-run report (115-07) must list every root's
  department so Larry can correct either in one local edit before first ingest.
- `composeFile`: `"C:/Users/mandr/astridr-repo/docker-compose.yml"` — the D-09 mount source.
- `localConfigPath`: `"config/workspace.local.json"` — repo-relative.

Add a top-level `"_readme"` string explaining: this file is TRACKED and PUBLIC; sensitive root names
belong in `config/workspace.local.json` which is gitignored (D-17); the allowlist is
deny-by-default (D-02) so anything unmatched is withheld, not shared.

Home paths of the form `C:/Users/mandr` are explicitly fine in tracked files — D-17 records the
precedent (many tracked `.md` files already contain that path), so this decision is not about home
paths.
  </action>
  <verify>
    <automated>node -e "const c=require('./config/workspace.json'); const bad=['.json','.yml','.yaml','.sh','.ps1','.env','.envfile','.pem','.key','.sql','.log','.csv','.bat','.vbs']; const hit=bad.filter(e=>c.shareableAllowlist.default.extensions.includes(e)); if(hit.length){console.error('FAIL forbidden extensions allowlisted:',hit);process.exit(1);} if(c.shareableAllowlist.default.requireNonDotBasename!==true){console.error('FAIL requireNonDotBasename not true');process.exit(1);} if(c.roots.length!==5){console.error('FAIL expected 5 tracked roots, got',c.roots.length);process.exit(1);} const ok=c.roots.every(r=>c.departments.includes(r.department)); if(!ok){console.error('FAIL a root department is outside the vocabulary');process.exit(1);} console.log('PASS tracked config invariants');"</automated>
  </verify>
  <acceptance_criteria>
    - `git check-ignore -v config/workspace.local.json` prints a match citing `.gitignore` (i.e. the
      ignore rule is live BEFORE the local file is created in Task 2).
    - `git check-ignore config/workspace-scan-report.json` and
      `git check-ignore config/workspace-scan.approved.sha256` both exit 0.
    - `git check-ignore config/workspace.json` exits **1** (non-zero) — the tracked rules file must NOT
      be ignored. This is the control that proves the ignore rules are specific rather than swallowing
      the whole `config/` directory.
    - The Task 1 `<verify>` node command prints `PASS tracked config invariants` and exits 0.
    - `node -e "JSON.parse(require('fs').readFileSync('config/workspace.json','utf8'))"` exits 0.
    - `config/workspace.json` contains the literal string `Unclassified` at least 3 times (the three
      D-15 roots) — `grep -Fc Unclassified config/workspace.json` >= 3.
    - `grep -Fc '"evidence"' config/workspace.json` >= 5 — every root carries a declared basis.
    - No backslash appears in any `path` value: `node -e "const c=require('./config/workspace.json'); if(c.roots.some(r=>r.path.includes(String.fromCharCode(92)))){process.exit(1)}"` exits 0.
  </acceptance_criteria>
  <done>`.gitignore` ignores the local config, the report and the marker (and provably does NOT ignore the tracked rules file); `config/workspace.json` exists with a deny-by-default allowlist that excludes every measured-leaky extension and five declared roots each carrying evidence.</done>
</task>

<task type="auto">
  <name>Task 2: Write the gitignored local root list (D-16)</name>
  <files>config/workspace.local.json (gitignored — never committed)</files>
  <read_first>
    - `.gitignore` — confirm your Task 1 block is present before creating this file.
    - `config/workspace.json` — the tracked half, so the local half uses the identical `roots` entry
      shape.
    - `.planning/phases/115-workspace-scanner/115-CONTEXT.md` § D-16, D-17.
  </read_first>
  <action>
Enumerate the directories directly under `C:/Users/mandr/` (`ls -d /c/Users/mandr/*/`) and identify
every one that is a real project/work root — i.e. contains a `.git` directory, or a `package.json`, or
a `pyproject.toml`, or is otherwise plainly a working directory rather than a Windows shell folder.

**EXCLUDE** from consideration: Windows profile folders (`Desktop`, `Documents`, `Downloads`,
`Pictures`, `Music`, `Videos`, `Favorites`, `Links`, `Contacts`, `Searches`, `Saved Games`,
`OneDrive*`, `AppData`, `3D Objects`, `ansel`), tool caches (`.cache`, `.npm`, `.cargo`, `.vscode`,
`.docker`, `.ollama`, `.nuget`, `.gradle`, `.m2`, `.ssh`, `.gnupg`, `.aws`, `.azure`, `.kube`), and
the five roots already declared in the tracked config.

**Never open, read or transcribe the CONTENTS of any of these directories** — you need the directory
NAME and nothing else. Do not `cat` anything under them.

Write `config/workspace.local.json`:

- `schemaVersion`: `1` — must match the tracked file; the loader refuses a mismatch.
- `roots`: one entry per discovered root, each `{ "id": "<kebab-case-of-dirname>", "path": "C:/Users/mandr/<dirname>", "department": "Unclassified", "evidence": "D-16: declared root, department not inferable from the directory name alone; re-map here after reviewing the D-12 dry-run report." }`

**Every single one gets `"Unclassified"`.** D-16 is explicit: never omitted (a narrowed root list
makes real work invisible), never guessed into a real department (a wrong department landing silently
is D-14's forbidden failure). Even if a directory name looks obviously like a ProtectAll/Work repo,
it still ships Unclassified here — Larry re-maps after seeing the dry-run report's file counts and
sizes. Do not exercise judgment about which ones are "obviously" Work.

**Do NOT record any of these directory names in any tracked file** — not in the SUMMARY, not in
`config/workspace.json`, not in a commit message, not in a code comment. Report only the COUNT of
roots added. If you need to describe a specific root in the SUMMARY, refer to it by index
(`local root #3`), never by name.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const l=JSON.parse(fs.readFileSync('config/workspace.local.json','utf8')); const t=JSON.parse(fs.readFileSync('config/workspace.json','utf8')); if(l.schemaVersion!==t.schemaVersion){console.error('FAIL schemaVersion mismatch');process.exit(1);} if(!Array.isArray(l.roots)||l.roots.length===0){console.error('FAIL no local roots declared');process.exit(1);} const wrong=l.roots.filter(r=>r.department!=='Unclassified'); if(wrong.length){console.error('FAIL '+wrong.length+' local root(s) not Unclassified — violates D-16');process.exit(1);} const dupe=l.roots.filter(r=>t.roots.some(x=>x.id===r.id)); if(dupe.length){console.error('FAIL local root id collides with a tracked root id');process.exit(1);} console.log('PASS local config invariants; roots='+l.roots.length);"</automated>
  </verify>
  <acceptance_criteria>
    - The Task 2 `<verify>` command prints `PASS local config invariants; roots=N` with `N >= 1`.
    - `git status --porcelain config/workspace.local.json` produces **no output** — the file is ignored
      and therefore invisible to git. (Control: `git status --porcelain config/workspace.json` DOES
      produce output, proving the check discriminates rather than always being empty.)
    - `git ls-files config/` lists `config/workspace.json` and NOT `config/workspace.local.json`.
    - Every `department` in the local file is the literal string `Unclassified`:
      `node -e "const l=require('./config/workspace.local.json'); process.exit(l.roots.every(r=>r.department==='Unclassified')?0:1)"` exits 0.
    - No local root id or path appears anywhere in a tracked file. Verify with a fixed-string search
      per root id — use `git grep -F "<id>"` (the `-F` is mandatory; a hand-escaped pattern
      containing backslashes silently returns 0 hits, which is the exact false negative that has bitten
      this project repeatedly). Every one must return no hits.
    - `115-02-SUMMARY.md` contains no local root directory name — only a count.
  </acceptance_criteria>
  <done>Every ambiguous root under `C:/Users/mandr` is declared with department `Unclassified` in a file git provably cannot see, and none of their names appear in any tracked file.</done>
</task>

<task type="auto">
  <name>Task 3: Write the fail-closed loader and its mutation tests</name>
  <files>hooks/workspaceConfig.mjs, hooks/__tests__/workspaceConfig.test.mjs</files>
  <read_first>
    - `config/workspace.json` and `config/workspace.local.json` — the two halves you are merging.
    - `convex/ingestAuth.ts:63-95` — the fail-closed idiom to model the control flow on
      (`validateIngestAuth`: absent input returns the SAFE value, never throws).
    - `hooks/scanner.mjs:280-320` — the `existsSync` + `try/catch` defensive-read wrapping and the
      `isDirectRun`/`__dirname` resolution idiom for locating repo-relative files from a `hooks/`
      module.
    - `hooks/__tests__/scanner.test.mjs:1-45` — the `mkdtempSync` real-tmp-fixture test idiom.
  </read_first>
  <action>
Create `hooks/workspaceConfig.mjs`. **No shebang** (same reason as `hooks/scanner.mjs:5-10` — its
test file imports it and Vite/Rolldown's SSR transform hoists imports above line 1).

Exports:

- `export const DEPARTMENTS = ["Work", "Consulting", "Personal", "Unclassified"];`
- `export const UNCLASSIFIED = "Unclassified";`
- `export const CONFIG_SCHEMA_VERSION = 1;`
- `export function mergeWorkspaceConfig(tracked, local)` — **pure**, no I/O. This is the unit under
  test.
  - `tracked` must be a valid parsed object; if it is null/malformed/`schemaVersion !== 1`, THROW —
    the tracked file is a repo artifact and its absence is a build defect, not a runtime condition.
    This is the one place a throw is correct.
  - `local` may be `null` (absent or malformed). When `null`: return the tracked config unchanged with
    `localConfigStatus: "absent"`. **This is D-17's fail-closed path: tracked roots only. Never
    "scan everything unclassified", never a throw.**
  - When `local` is present but `local.schemaVersion !== tracked.schemaVersion`: treat it exactly as
    `null`, with `localConfigStatus: "version-mismatch"`. Fail closed, do not attempt a partial merge.
  - When `local` is present and valid: merge at the KEY level with **local winning on collision**
    (D-17). For `roots` specifically, merge by `id`: a local root with the same `id` as a tracked root
    REPLACES it; a local root with a new `id` is appended. Set `localConfigStatus: "merged"`.
  - Normalize on the way out: every root's `path` gets `.replace(/\\/g, "/")` and any trailing `/`
    stripped (Pitfall 5 / the `skillScan.mjs:158-164` `samePath` normalization idiom). Every root whose
    `department` is missing or not in `DEPARTMENTS` is coerced to `UNCLASSIFIED` — **never** to a real
    department (D-14).
  - Return `{ ...merged, localConfigStatus }`.
- `export function loadWorkspaceConfig(deps = {})` — the I/O wrapper. Destructure with defaults
  `{ repoRoot = <dirname of this module>/.., readFileSync, existsSync }` following the injectable-deps
  pattern at `hooks/scanner.mjs:33-38`. Read `config/workspace.json` (throw if absent/malformed) and
  `config/workspace.local.json` (each read wrapped in `existsSync` + `try/catch`; on ANY failure pass
  `null` into `mergeWorkspaceConfig`). Return `mergeWorkspaceConfig`'s result.

Create `hooks/__tests__/workspaceConfig.test.mjs`. `mergeWorkspaceConfig` is pure so most cases need
zero I/O; use `mkdtempSync` fixtures only for `loadWorkspaceConfig`. Required cases — note case 1 is
the passing CONTROL without which the four refusal cases are indistinguishable from a function that
always fails closed:

1. **Control (merge works):** valid tracked + valid local with one new root id → result has
   `localConfigStatus === "merged"`, `roots.length === tracked + 1`, and the local root is present.
2. **Local absent (`null`):** → `localConfigStatus === "absent"`, `roots` deep-equals the tracked
   roots exactly. Assert the LENGTH and the ids, not just that it did not throw.
3. **Local malformed JSON on disk:** write a fixture `workspace.local.json` containing `{not json`;
   `loadWorkspaceConfig` returns `localConfigStatus === "absent"` and does NOT throw.
4. **Local schemaVersion mismatch:** local with `schemaVersion: 99` → `"version-mismatch"` and tracked
   roots only.
5. **Local wins on collision:** local declares a root with the SAME `id` as a tracked root but a
   different `path`/`department` → the merged root carries the LOCAL values and `roots.length` is
   unchanged (replaced, not duplicated).
6. **Unknown department is coerced to Unclassified, not to a real department:** local root with
   `department: "Engineering"` → merged department is exactly `"Unclassified"`. Pair with a control in
   the same test: a local root with `department: "Work"` stays `"Work"` — proving the coercion is
   selective, not blanket.
7. **Path normalization:** a local root path with backslashes and a trailing separator normalizes to
   forward slashes with no trailing separator.
8. **Tracked absent/malformed THROWS:** `mergeWorkspaceConfig(null, {...})` and
   `mergeWorkspaceConfig({schemaVersion: 99}, null)` both throw. This is the deliberate asymmetry —
   assert it so a future editor does not "harmonize" it into a silent fallback.
  </action>
  <verify>
    <automated>npx vitest run hooks/__tests__/workspaceConfig.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run hooks/__tests__/workspaceConfig.test.mjs` passes with all 8 cases present.
    - Case 1 (the control) is present and passing — a suite of only refusal cases would be satisfied by
      a function that always fails closed, which is the exact defect class this project's standing rule
      forbids.
    - `mergeWorkspaceConfig` performs no I/O: `grep -v '^\s*[/*]' hooks/workspaceConfig.mjs | grep -c "readFileSync\|existsSync"` is `>= 1` overall (the loader needs them) but those calls appear ONLY
      inside `loadWorkspaceConfig` — verify by reading the function bodies, and assert in a test that
      `mergeWorkspaceConfig` succeeds when called with `readFileSync` unavailable (pass plain objects; no
      `deps` at all).
    - Case 3 asserts on the RETURNED `localConfigStatus`, not on a caught exception.
    - `node -e "import('./hooks/workspaceConfig.mjs').then(m=>{const c=m.loadWorkspaceConfig(); console.log('status='+c.localConfigStatus+' roots='+c.roots.length); if(c.roots.length<6){process.exit(1)}})"`
      prints `status=merged` and a root count greater than 5 (5 tracked + at least 1 local), proving the
      real on-disk merge works and not just the fixtures.
    - `head -c 2 hooks/workspaceConfig.mjs` is not `#!`.
    - `npm test` exits 0.
  </acceptance_criteria>
  <done>`loadWorkspaceConfig` merges tracked←local with local winning, returns tracked-only on any local failure without throwing, never coerces an unknown department into a real one, and all eight cases including the passing control are green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local filesystem → tracked git repo | This repo is PUBLIC; anything committed is a permanent one-way disclosure |
| config file → classifier behavior | A widened allowlist silently widens what leaves the host |

## STRIDE Threat Register (ASVS L1, block on high)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-115-02-01 | Information Disclosure | `config/workspace.local.json` in a PUBLIC repo | mitigate | `.gitignore` rule written FIRST, in the same task, before the file exists. Verified by `git check-ignore` returning a match AND by `git status --porcelain` on the file producing no output, with `config/workspace.json` as the discriminating control. Git history survives deletion, so a single accidental commit is unrecoverable. |
| T-115-02-02 | Information Disclosure | local root names leaking via the SUMMARY, a commit message or a code comment | mitigate | Task 2 forbids recording any local root name in any tracked artifact; acceptance requires a `git grep -F` per root id returning no hits. `-F` is mandatory — a hand-escaped backslash pattern returns a false 0. |
| T-115-02-03 | Information Disclosure | over-broad ignore rule hiding real source | mitigate | Three explicit filenames, not a `config/` or `config/*.json` glob. `git check-ignore config/workspace.json` must exit non-zero as the control. |
| T-115-02-04 | Tampering | allowlist drift re-adding `.json`/`.sh`/`.yaml` | mitigate | An automated invariant check in Task 1's `<verify>` fails on any of 14 forbidden extensions, plus a `_excludedFromAllowlistRationale` field recording WHY, so the next editor sees the reason in the diff. D-12's hash gate (115-03/115-08) additionally invalidates any prior approval when the config changes the report. |
| T-115-02-05 | Elevation of Privilege / fail-open | missing local config interpreted as "scan everything" | mitigate | `mergeWorkspaceConfig(tracked, null)` returns tracked roots only, asserted on root LENGTH and ids (case 2), plus a version-mismatch case that also falls closed. |
| T-115-02-06 | Denial of Service | a malformed local config crashing a nightly hidden task that swallows output | mitigate | Every local read is `existsSync` + `try/catch`; case 3 asserts the returned status rather than a caught throw, so a crash cannot pass as a refusal. |
| T-115-02-SC | Tampering | npm installs | accept | This plan installs nothing; no `package.json` change. |
</threat_model>

<verification>
- `npx vitest run hooks/__tests__/workspaceConfig.test.mjs` — green, 8 cases.
- `npm test` — full suite green.
- `git check-ignore -v config/workspace.local.json config/workspace-scan-report.json config/workspace-scan.approved.sha256` — all three matched.
- `git check-ignore config/workspace.json` — exits non-zero (control).
- `git status --porcelain` immediately before commit — must NOT list `config/workspace.local.json`.
  Per this project's concurrent-session rule, run this as its own tool call and READ it, then run
  `git show --stat HEAD` AFTER the commit and confirm the file list is exactly what you intended.
- `git ls-files config/` — exactly `config/workspace.json`.
</verification>

<success_criteria>
- The tracked config is a complete, reviewable rules file whose allowlist provably excludes every
  extension family that leaked in the donor measurement.
- The local root list exists, declares every ambiguous root as `Unclassified`, and is invisible to git.
- The loader merges tracked←local with local winning, and every failure mode falls closed to
  tracked-only without throwing.
- No local root name exists anywhere in tracked content.
</success_criteria>

<output>
Create `.planning/phases/115-workspace-scanner/115-02-SUMMARY.md` when done. Report the local root
COUNT only — never a name. Record any place this plan's draft schema was wrong.
</output>
