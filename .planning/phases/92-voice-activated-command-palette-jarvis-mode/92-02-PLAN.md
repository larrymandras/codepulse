---
phase: 92-voice-activated-command-palette-jarvis-mode
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/hooks/useSpeechRecognition.ts
  - src/hooks/useSpeechRecognition.test.ts
  - src/hooks/useTtsPlayback.ts
  - src/hooks/useTtsPlayback.test.ts
  - src/components/ChatInput.tsx
  - src/pages/Chat.tsx
autonomous: true
requirements: [VOX-02, VOX-03]

must_haves:
  truths:
    - "useSpeechRecognition is a shared hook consumed by both ChatInput and the palette voice mode (no duplicate Web Speech logic)"
    - "useTtsPlayback is a shared hook consumed by both Chat and the palette voice mode (no duplicate audio logic)"
    - "useTtsPlayback exposes isPlaying so the feedback guard can pause recognition during TTS"
    - "ChatInput still works exactly as before after extraction; Chat TTS still plays after extraction"
  artifacts:
    - path: "src/hooks/useSpeechRecognition.ts"
      provides: "shared Web Speech recognition hook with continuous/interim options"
      exports: ["useSpeechRecognition"]
      min_lines: 60
    - path: "src/hooks/useTtsPlayback.ts"
      provides: "shared TTS audio playback hook exposing isPlaying"
      exports: ["useTtsPlayback"]
      min_lines: 30
    - path: "src/hooks/useSpeechRecognition.test.ts"
      provides: "unit coverage for final/interim result + onEnd"
    - path: "src/hooks/useTtsPlayback.test.ts"
      provides: "unit coverage for isPlaying lifecycle + URL construction"
  key_links:
    - from: "src/components/ChatInput.tsx"
      to: "src/hooks/useSpeechRecognition.ts"
      via: "useSpeechRecognition({ continuous: false, interimResults: false })"
      pattern: "useSpeechRecognition"
    - from: "src/pages/Chat.tsx"
      to: "src/hooks/useTtsPlayback.ts"
      via: "useTtsPlayback()"
      pattern: "useTtsPlayback"
---

<objective>
Extract the two pieces of existing browser-audio logic into shared hooks so the palette voice mode (92-04) reuses them with zero duplication: `useSpeechRecognition` (from `ChatInput.tsx` Web Speech logic) and `useTtsPlayback` (from `Chat.tsx` `run.tts` playback). Refactor `ChatInput.tsx` and `Chat.tsx` to consume the extracted hooks — do NOT leave two copies. `useTtsPlayback` gains an `isPlaying` flag the feedback guard needs.

Purpose: VOX-02 requires the palette to reuse ChatInput's recognition; VOX-03 mandates a single shared `useTtsPlayback` (no duplicate logic). Extracting now, with the originals refactored to consume the extractions, guarantees one source of truth.

Output: `src/hooks/useSpeechRecognition.ts` (+test), `src/hooks/useTtsPlayback.ts` (+test), refactored `ChatInput.tsx` and `Chat.tsx`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/92-voice-activated-command-palette-jarvis-mode/92-RESEARCH.md
@.planning/phases/92-voice-activated-command-palette-jarvis-mode/92-PATTERNS.md
@.planning/phases/92-voice-activated-command-palette-jarvis-mode/92-VALIDATION.md

<interfaces>
<!-- Target hook contracts the executor must implement (from 92-PATTERNS + RESEARCH). -->

useSpeechRecognition options/return (RESEARCH Pattern 4, PATTERNS lines 217-232):
  options: { continuous?: boolean; interimResults?: boolean; lang?: string;
             onFinalResult: (t: string) => void; onInterimResult?: (t: string) => void; onEnd?: () => void }
  returns: { start: () => void; stop: () => void; abort: () => void; isListening: boolean; speechAvailable: boolean }

useTtsPlayback return (RESEARCH Pattern 5, PATTERNS lines 296-310):
  returns: { play: (url: string) => void; stop: () => void; isPlaying: boolean }
  - play() normalizes relative audio_url against ASTRIDR_API_URL internally
  - isPlaying: true before audio.play(), false in audio.onended and in stop()
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extract useSpeechRecognition + refactor ChatInput</name>
  <files>src/hooks/useSpeechRecognition.ts, src/hooks/useSpeechRecognition.test.ts, src/components/ChatInput.tsx</files>
  <read_first>
    - src/components/ChatInput.tsx lines 16-40 (SpeechRecognition type decls), 54-56 (getSpeechRecognitionClass), 62-86 (state + unmount cleanup), 119-170 (start/stopListening lifecycle), 158-165 (onerror) — the exact extraction source
    - .planning/phases/92-voice-activated-command-palette-jarvis-mode/92-PATTERNS.md §useSpeechRecognition.ts (lines 128-246 — verbatim lift instructions + generalization to options)
    - .planning/phases/92-voice-activated-command-palette-jarvis-mode/92-RESEARCH.md §"Pattern 4" (continuous:true + interimResults:true for palette; ChatInput stays continuous:false/interimResults:false)
    - src/test/setup.ts (SpeechRecognition mock added by 92-01 — but this plan is wave 1 parallel; if running before 92-01's mock lands, add a local vi.stubGlobal in the test per PATTERNS lines 739-743)
  </read_first>
  <behavior>
    - onFinalResult fires with the transcript when recognition.onresult delivers a final result.
    - onInterimResult fires with interim transcript when interimResults:true and a non-final result arrives.
    - onEnd fires when recognition.onend fires; isListening becomes false.
    - speechAvailable is false when getSpeechRecognitionClass() returns null (no SpeechRecognition global).
    - Calling start() when already listening does not throw (guard on isListening).
  </behavior>
  <action>Create `src/hooks/useSpeechRecognition.ts`. Move the `SpeechRecognitionEvent`/`SpeechRecognitionInstance` interfaces and the `declare global` Window augmentation (ChatInput.tsx lines 16-40) into this file and export the instance type. Lift `getSpeechRecognitionClass()` (ChatInput line 54). Implement `useSpeechRecognition(options)` per the `<interfaces>` contract: the constructor sets `continuous = options.continuous ?? false`, `interimResults = options.interimResults ?? false`, `lang = options.lang ?? "en-US"`; `onresult` reads `event.results[event.resultIndex][0].transcript` and routes final results to `onFinalResult` and (when `interimResults`) non-final results to `onInterimResult`; `onend` sets isListening false + calls `onEnd?.()`; `onerror` mirrors ChatInput lines 158-165 (ignore "aborted"/"no-speech", console.warn others). Keep a `recognitionRef`, expose `start`/`stop`/`abort`/`isListening`/`speechAvailable`, and copy the unmount `abort()` cleanup verbatim (PATTERNS lines 235-245). Guard `start()` to no-op if already listening (RESEARCH anti-pattern: start() while active throws InvalidStateError). Then refactor `ChatInput.tsx` to delete its inline recognition logic and `declare global`, importing and calling `useSpeechRecognition({ continuous: false, interimResults: false, onFinalResult: <existing auto-send handler>, onEnd: ... })`, wiring its existing mic button to the hook's `start`/`stop`/`isListening`/`speechAvailable`. Behavior of ChatInput must be unchanged. Write the test file covering the five behaviors using the mocked SpeechRecognition.</action>
  <verify>
    <automated>npx vitest run src/hooks/useSpeechRecognition.test.ts src/components/ChatInput.test.tsx 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/hooks/useSpeechRecognition.test.ts` exits 0 and covers final/interim/onEnd/speechAvailable.
    - `ChatInput.tsx` imports `useSpeechRecognition` and no longer contains its own `new SpeechRecognitionClass()` setup or `declare global` block (grep "webkitSpeechRecognition" in ChatInput returns 0 outside the import).
    - Existing `src/components/ChatInput.test.tsx` still passes (ChatInput behavior unchanged).
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>Single shared recognition hook; ChatInput consumes it; no duplicate Web Speech logic; ChatInput behavior preserved.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extract useTtsPlayback + refactor Chat</name>
  <files>src/hooks/useTtsPlayback.ts, src/hooks/useTtsPlayback.test.ts, src/pages/Chat.tsx</files>
  <read_first>
    - src/pages/Chat.tsx lines 63-86 (audioRef + playAudio + unmount cleanup), 253-288 (run.tts handler), 262-264 (relative→absolute URL construction), 283-287 (ttsEnabled guard — stays in Chat)
    - .planning/phases/92-voice-activated-command-palette-jarvis-mode/92-PATTERNS.md §useTtsPlayback.ts (lines 250-310 — extraction + isPlaying additions)
    - .planning/phases/92-voice-activated-command-palette-jarvis-mode/92-RESEARCH.md §"Pattern 5" (isPlaying feedback-guard contract) and §"Open Question 4" (run.tts sessionId routing stays in the consumer)
  </read_first>
  <behavior>
    - play(url) sets isPlaying true and calls audio.play(); audio.onended sets isPlaying false.
    - play(relativeUrl) constructs the full URL as `${ASTRIDR_API_URL}${relativeUrl}` when the url does not start with "http"; passes absolute http(s) URLs through unchanged.
    - stop() pauses the current audio and sets isPlaying false.
    - Calling play() while audio is already playing replaces the prior audio (pauses the old one first).
  </behavior>
  <action>Create `src/hooks/useTtsPlayback.ts` exporting `useTtsPlayback()` per the `<interfaces>` contract. Move `audioRef`, the `playAudio` mechanics (Chat.tsx lines 63-86), and the URL construction (lines 262-264) into the hook; define `ASTRIDR_API_URL = import.meta.env.VITE_ASTRIDR_API_URL ?? "http://localhost:8181"` inside the hook. Add `const [isPlaying, setIsPlaying] = useState(false)`: set true immediately before `audio.play()`, set false in `audio.onended`, set false in `stop()` after `audio.pause()`. Copy the unmount cleanup (pause + null the ref). Expose `play`, `stop`, `isPlaying`. The `play()` arg accepts a relative or absolute url and normalizes internally. Then refactor `Chat.tsx` to consume `useTtsPlayback()` — delete the local `audioRef`/`playAudio`/URL-build and call `play(data.audio_url)` from its `run.tts` handler; the `ttsEnabled` guard and `sessionId` routing logic REMAIN in Chat.tsx (the hook is transport-agnostic; the consumer decides which event to play). Write the test covering the four behaviors using the mocked Audio.</action>
  <verify>
    <automated>npx vitest run src/hooks/useTtsPlayback.test.ts 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/hooks/useTtsPlayback.test.ts` exits 0 and asserts isPlaying true→false across play→onended, plus relative/absolute URL handling.
    - `Chat.tsx` imports `useTtsPlayback` and no longer constructs `new Audio(...)` directly (grep "new Audio" in Chat.tsx returns 0).
    - `Chat.tsx` still applies its `ttsEnabled` guard and sessionId routing (those lines remain).
    - `npx tsc --noEmit` exits 0; existing Chat tests (if any) still pass.
  </acceptance_criteria>
  <done>Single shared TTS hook exposing isPlaying; Chat consumes it; no duplicate audio logic; feedback-guard signal available to 92-04.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Mic audio → Web Speech API | Browser sends post-wake mic audio to the platform speech service (Chrome → Google) for transcription |
| run.tts audio_url → <audio> | Audio URL from the Ástríðr backend is fetched and played |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-92-03 | Information Disclosure | Web Speech API transcription | accept | This is pre-existing behavior in ChatInput (already shipped); extraction does not change what audio is sent or when. Mic only opens on explicit user action; no new exposure introduced by refactor |
| T-92-04 | Spoofing | audio_url origin (relative → ASTRIDR_API_URL) | mitigate | URL normalization only prefixes the configured `VITE_ASTRIDR_API_URL` to relative paths; absolute http(s) URLs pass through as today — no new origin surface vs. existing Chat.tsx |
| T-92-05 | Tampering | shared hooks consumed by two surfaces | accept | Pure refactor; behavior of existing surfaces asserted unchanged by their existing tests |
</threat_model>

<verification>
- `npx vitest run src/hooks/useSpeechRecognition.test.ts src/hooks/useTtsPlayback.test.ts` green.
- `npx vitest run src/components/ChatInput.test.tsx` green (no regression).
- `npx tsc --noEmit` exits 0.
- Grep confirms ChatInput no longer hand-rolls SpeechRecognition and Chat no longer hand-rolls Audio.
</verification>

<success_criteria>
Both hooks extracted, unit-tested, and consumed by their original surfaces with behavior preserved; `useTtsPlayback.isPlaying` available for the feedback guard. Ready for 92-04 to reuse both.
</success_criteria>

<output>
Create `.planning/phases/92-voice-activated-command-palette-jarvis-mode/92-02-SUMMARY.md` when done
</output>
