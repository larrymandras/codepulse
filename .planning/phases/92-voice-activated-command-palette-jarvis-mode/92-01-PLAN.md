---
phase: 92-voice-activated-command-palette-jarvis-mode
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - vite.config.ts
  - src/lib/melNormalize.ts
  - src/lib/melNormalize.test.ts
  - src/test/setup.ts
  - public/openwakeword/README.md
autonomous: false
requirements: [VOX-01]
user_setup: []

must_haves:
  truths:
    - "onnxruntime-web is installed and Vite serves its WASM without pre-bundling corruption"
    - "Mel normalization (value/10)+2 is a pure, unit-tested function"
    - "Vitest mocks exist for Worker, AudioWorkletNode, SpeechRecognition, and HTMLAudioElement.play so downstream voice tests can run in jsdom"
    - "The hey_astrid.onnx training dependency is explicitly surfaced and tracked (not silently assumed present)"
  artifacts:
    - path: "vite.config.ts"
      provides: "optimizeDeps.exclude + viteStaticCopy WASM copy"
      contains: "viteStaticCopy"
    - path: "src/lib/melNormalize.ts"
      provides: "pure mel normalization helper"
      exports: ["normalizeMelFrame"]
      min_lines: 5
    - path: "src/lib/melNormalize.test.ts"
      provides: "unit test for (v/10)+2 transform"
    - path: "src/test/setup.ts"
      provides: "Worker/AudioWorklet/SpeechRecognition/Audio mocks"
  key_links:
    - from: "vite.config.ts"
      to: "node_modules/onnxruntime-web/dist/*.wasm"
      via: "viteStaticCopy target"
      pattern: "onnxruntime-web/dist/\\*\\.wasm"
    - from: "src/workers/wakeWordWorker.ts (downstream)"
      to: "src/lib/melNormalize.ts"
      via: "import normalizeMelFrame"
      pattern: "normalizeMelFrame"
---

<objective>
Lay the foundation for browser wake-word detection: install `onnxruntime-web`, configure Vite to serve its WASM correctly, extract the mandatory mel-normalization transform as a pure unit-tested function, and add the jsdom test mocks every downstream voice test depends on. Also surface the `hey_astrid.onnx` custom-model training as an explicit external prerequisite (it blocks live wake detection but NOT any code/tests, which use the bundled "hey jarvis" stand-in).

Purpose: Every other Phase 92 plan depends on this. Without `optimizeDeps.exclude` + `viteStaticCopy`, ONNX session creation serves error-HTML instead of WASM (RESEARCH Pitfall 7). Without the pure `normalizeMelFrame`, the `(v/10)+2` transform (RESEARCH Pitfall 1) is untestable. Without the mocks, no voice hook test runs in jsdom.

Output: Installed deps, configured Vite, `src/lib/melNormalize.ts` (+test), extended `src/test/setup.ts`, updated `public/openwakeword/README.md` tracking the pending model.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/92-voice-activated-command-palette-jarvis-mode/92-RESEARCH.md
@.planning/phases/92-voice-activated-command-palette-jarvis-mode/92-PATTERNS.md
@.planning/phases/92-voice-activated-command-palette-jarvis-mode/92-VALIDATION.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 0: Package legitimacy gate + custom-model prerequisite</name>
  <action>STOP for human verification before installing. Verify both npm packages at their npmjs.com listings against RESEARCH slopcheck [OK], and have the operator acknowledge the pending hey_astrid.onnx custom-model dependency (live VOX-01 detection is blocked on it; all code/tests use the bundled "hey jarvis" stand-in). Do not proceed to install until approved.</action>
  <what-built>Pre-install verification of the two new npm packages and a tracking note for the missing custom wake-word model.</what-built>
  <read_first>
    - .planning/phases/92-voice-activated-command-palette-jarvis-mode/92-RESEARCH.md §"Package Legitimacy Audit" (both packages [OK]) and §"Pitfall 5" (hey_astrid.onnx PENDING)
    - public/openwakeword/README.md (current state — marks hey_astrid.onnx PENDING)
  </read_first>
  <how-to-verify>
    1. Confirm `onnxruntime-web` at npmjs.com/package/onnxruntime-web is the Microsoft/onnxruntime port (RESEARCH lists 1.27.0, published 2026-06-19, slopcheck [OK]).
    2. Confirm `vite-plugin-static-copy` at npmjs.com/package/vite-plugin-static-copy is sapphi-red/vite-plugin-static-copy (RESEARCH lists 4.1.1, slopcheck [OK]).
    3. Acknowledge the `hey_astrid.onnx` classifier is NOT yet trained. Decide: (a) you will train it via openWakeWord's Colab synthetic-data pipeline before QA sign-off, and (b) all code/tests in this phase use the bundled "hey jarvis" model as the integration stand-in. This is a known, surfaced dependency — VOX-01 live-detection QA is blocked on it; nothing else is.
  </how-to-verify>
  <resume-signal>Type "approved" to install both packages, or describe a concern.</resume-signal>
</task>

<task type="auto">
  <name>Task 1: Install onnxruntime-web + configure Vite for WASM</name>
  <files>package.json, vite.config.ts</files>
  <read_first>
    - vite.config.ts (current 16-line baseline — analog is self per 92-PATTERNS §vite.config.ts lines 605-642)
    - .planning/phases/92-voice-activated-command-palette-jarvis-mode/92-RESEARCH.md §"Pattern 6: Vite Configuration Changes" (lines 399-425) and §"Pitfall 3"/§"Pitfall 7"
    - .planning/phases/92-voice-activated-command-palette-jarvis-mode/92-PATTERNS.md §vite.config.ts (the exact extended config block)
  </read_first>
  <action>Run `npm install onnxruntime-web` and `npm install --save-dev vite-plugin-static-copy` (versions per RESEARCH: onnxruntime-web 1.27.0, vite-plugin-static-copy 4.1.1 — accept the resolved current within those majors). Edit `vite.config.ts` per 92-PATTERNS §vite.config.ts: import `viteStaticCopy` from `vite-plugin-static-copy`; add it to the `plugins` array with one target `{ src: 'node_modules/onnxruntime-web/dist/*.wasm', dest: '.' }`; add `optimizeDeps: { exclude: ['onnxruntime-web'] }`. Preserve the existing `react()`, `tailwindcss()`, `resolve.alias` `@`→`./src`, and `server.port: 5173`. Do NOT add COOP/COEP headers — the Worker sets `ort.env.wasm.numThreads = 1` (downstream 92-03), which deliberately avoids cross-origin-isolation (RESEARCH §Summary, D-03b rationale). Do NOT add any Picovoice config or env var — that path is rejected (D-03a).</action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | tail -5 && grep -c "viteStaticCopy" vite.config.ts</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` lists `onnxruntime-web` in dependencies and `vite-plugin-static-copy` in devDependencies.
    - `vite.config.ts` contains `viteStaticCopy` (grep count ≥ 1) with the `onnxruntime-web/dist/*.wasm` target and `optimizeDeps.exclude: ['onnxruntime-web']`.
    - `npx tsc --noEmit` exits 0.
    - No string `Picovoice`, `.ppn`, `VITE_PICOVOICE`, or `Cross-Origin-Embedder-Policy` appears in vite.config.ts.
  </acceptance_criteria>
  <done>onnxruntime-web installed; Vite configured to exclude it from pre-bundling and copy its WASM; type check clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Mel-normalization pure util + test</name>
  <files>src/lib/melNormalize.ts, src/lib/melNormalize.test.ts</files>
  <read_first>
    - .planning/phases/92-voice-activated-command-palette-jarvis-mode/92-RESEARCH.md §"Pattern 1" mel normalization line ("output = (value / 10.0) + 2.0") and §"Pitfall 1"
    - .planning/phases/92-voice-activated-command-palette-jarvis-mode/92-VALIDATION.md §"Wave 0 Requirements" (mel-normalization helper must be a pure assertable function)
    - src/lib/formatters.ts (analog for a pure util module in src/lib with co-located behavior)
  </read_first>
  <behavior>
    - normalizeMelFrame([0]) returns [2] (0/10 + 2).
    - normalizeMelFrame([10, -10]) returns [3, 1].
    - normalizeMelFrame applied to a Float32Array preserves length and applies (v/10)+2 elementwise.
    - Empty input returns an empty array (no throw).
  </behavior>
  <action>Create `src/lib/melNormalize.ts` exporting a pure function `normalizeMelFrame(frame: number[] | Float32Array): number[]` that maps each element to `(value / 10.0) + 2.0`. No side effects, no ONNX import — this is the exact transform the downstream Worker (92-03) applies between melspectrogram output and embedding input (RESEARCH Pitfall 1). Add a JSDoc one-liner citing the transform is mandatory for trained-model compatibility (filter the header out of any future grep gates per planner hygiene). Write `src/lib/melNormalize.test.ts` covering the four behaviors above with Vitest.</action>
  <verify>
    <automated>npx vitest run src/lib/melNormalize.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/lib/melNormalize.test.ts` exits 0.
    - `src/lib/melNormalize.ts` exports `normalizeMelFrame`.
    - Test asserts normalizeMelFrame([0]) === [2] and normalizeMelFrame([10,-10]) === [3,1].
    - Module imports nothing from `onnxruntime-web` (pure).
  </acceptance_criteria>
  <done>normalizeMelFrame is a pure, exported, fully-tested function; downstream Worker can import it.</done>
</task>

<task type="auto">
  <name>Task 3: jsdom mocks for voice browser APIs + README tracking</name>
  <files>src/test/setup.ts, public/openwakeword/README.md</files>
  <read_first>
    - src/test/setup.ts (current mocks for Clerk/Recharts/Three.js/React Flow/Tone.js — extend, do not replace)
    - .planning/phases/92-voice-activated-command-palette-jarvis-mode/92-PATTERNS.md §"Testing: vi.mock" (lines 716-754 — exact stubGlobal shapes for SpeechRecognition / Audio / getUserMedia)
    - .planning/phases/92-voice-activated-command-palette-jarvis-mode/92-VALIDATION.md §"Wave 0 Requirements" (mocks needed: AudioWorkletNode / Worker / SpeechRecognition / HTMLAudioElement.play)
  </read_first>
  <action>Extend `src/test/setup.ts` with global stubs (do not remove any existing mock) so downstream voice hook/component tests run in jsdom: (1) `window.SpeechRecognition` / `window.webkitSpeechRecognition` constructors returning a mock instance with `continuous`, `interimResults`, `lang`, `start`/`stop`/`abort` as `vi.fn()`, and assignable `onresult`/`onend`/`onerror`; (2) `window.Audio` constructor returning a mock with `play()` resolving a Promise, `pause()`, and assignable `onended`; (3) ensure `HTMLAudioElement.prototype.play` is a `vi.fn()` resolving (jsdom lacks audio output); (4) a minimal `Worker` mock (constructor storing `onmessage`, `postMessage`/`terminate` as `vi.fn()`) and an `AudioWorkletNode` stub plus `AudioContext.prototype.audioWorklet.addModule` resolving — these let `useWakeWord` mount without real WASM. Guard each stub so it only installs if the global is undefined (avoid clobbering a real jsdom impl). Update `public/openwakeword/README.md` to record: melspectrogram.onnx + embedding_model.onnx present (v0.5.1); `hey_astrid.onnx` PENDING (train via openWakeWord Colab synthetic-data pipeline before VOX-01 QA sign-off); "hey jarvis" bundled model is the dev/integration stand-in.</action>
  <verify>
    <automated>npx vitest run src/lib/melNormalize.test.ts 2>&1 | tail -5 && grep -c "SpeechRecognition" src/test/setup.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/test/setup.ts` defines stubs for SpeechRecognition, Audio, Worker, and AudioWorkletNode/addModule (grep "SpeechRecognition" count ≥ 1).
    - Existing mocks (Tone.js, Three.js, Recharts, Clerk, React Flow) remain present.
    - The full existing suite still passes: `npx vitest run` exits 0 (no regression from added globals).
    - `public/openwakeword/README.md` states hey_astrid.onnx is PENDING and names the "hey jarvis" stand-in.
  </acceptance_criteria>
  <done>Voice-API mocks installed without breaking the existing suite; the pending custom model is documented.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → build | Two new third-party packages enter the supply chain |
| node_modules WASM → served asset | onnxruntime-web `.wasm` binaries are copied into the served bundle |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-92-SC | Tampering | onnxruntime-web, vite-plugin-static-copy installs | mitigate | Blocking human checkpoint (Task 0) verifies both at npmjs.com against RESEARCH slopcheck [OK] before install |
| T-92-01 | Tampering | Vite WASM serving (Pitfall 7) | mitigate | `optimizeDeps.exclude` + `viteStaticCopy` ensure the genuine `.wasm` is served, not pre-bundled error-HTML |
| T-92-02 | Information Disclosure | ONNX asset origin | accept | All ONNX assets are first-party in `public/openwakeword/`; no remote model fetch; no audio leaves machine at this layer (no mic opened in this plan) |
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0 after Vite + util changes.
- `npx vitest run src/lib/melNormalize.test.ts` green.
- `npx vitest run` (full suite) green — added globals cause no regression.
- Grep confirms no Picovoice / COOP-COEP strings introduced.
</verification>

<success_criteria>
onnxruntime-web installed and Vite-configured for correct WASM serving; `normalizeMelFrame` pure + tested; jsdom voice mocks added without regression; hey_astrid.onnx prerequisite surfaced in README. Foundation ready for 92-02 and 92-03.
</success_criteria>

<output>
Create `.planning/phases/92-voice-activated-command-palette-jarvis-mode/92-01-SUMMARY.md` when done
</output>
