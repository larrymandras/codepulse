---
phase: 92-voice-activated-command-palette-jarvis-mode
plan: 04
type: execute
wave: 3
depends_on: ["92-02", "92-03"]
files_modified:
  - src/components/voice/voiceState.ts
  - src/components/voice/voiceState.test.ts
  - src/components/voice/VoiceModePanel.tsx
  - src/components/voice/VoiceModePanel.test.tsx
  - src/components/CommandPalette.tsx
  - src/index.css
autonomous: true
requirements: [VOX-02, VOX-03]

must_haves:
  truths:
    - "On wake, the palette opens in voice mode showing a live transcript; a final spoken result is sent via sendCommand({type:'chat.send', message}) over AstridrWSContext — no new transport"
    - "The streamed reply (run.text) renders in the palette and the run.tts audio_url auto-plays via the shared useTtsPlayback hook"
    - "The feedback guard pauses speech recognition while TTS isPlaying and resumes it after, so Ástríðr's own reply is not self-transcribed"
    - "The turn loop continues after run.completed + TTS end (next turn without re-wake) and exits on an end-phrase or ~30s silence timeout"
  artifacts:
    - path: "src/components/voice/voiceState.ts"
      provides: "pure 6-state machine: transitions + end-phrase detection"
      exports: ["voiceReducer", "isEndPhrase", "VoiceState"]
    - path: "src/components/voice/VoiceModePanel.tsx"
      provides: "voice UI inside CommandDialog: badge, transcript, reply stream, waveform, close"
      exports: ["VoiceModePanel"]
      min_lines: 80
    - path: "src/components/CommandPalette.tsx"
      provides: "voiceMode prop + conditional VoiceModePanel render"
      contains: "VoiceModePanel"
  key_links:
    - from: "src/components/voice/VoiceModePanel.tsx"
      to: "src/contexts/AstridrWSContext"
      via: "sendCommand({type:'chat.send'}) + subscribeEvent(run.text/run.tts/run.completed)"
      pattern: "chat\\.send"
    - from: "src/components/voice/VoiceModePanel.tsx"
      to: "src/hooks/useTtsPlayback.ts"
      via: "useTtsPlayback().play + isPlaying feedback guard"
      pattern: "useTtsPlayback"
    - from: "src/components/voice/VoiceModePanel.tsx"
      to: "src/hooks/useSpeechRecognition.ts"
      via: "useSpeechRecognition({ continuous:true, interimResults:true })"
      pattern: "useSpeechRecognition"
---

<objective>
Build the palette voice mode: a pure 6-state machine (`idle → listening → transcribing → processing → speaking → error-disabled`), and `VoiceModePanel` rendered inside the existing `CommandDialog` when `voiceMode=true`. The panel reuses `useSpeechRecognition` (continuous + interim) for live transcript, sends the final transcript through the existing `sendCommand({type:'chat.send', message})`, renders the streamed `run.text` reply, auto-plays `run.tts` via the shared `useTtsPlayback`, and runs the continuous turn loop with the feedback guard (pause STT while `isPlaying`), end-phrase exit, and ~30s silence timeout.

Purpose: VOX-02 (transcribe → live transcript → chat.send over the existing transport) and VOX-03 (streamed reply renders + run.tts auto-plays via the shared hook, no duplicate logic). Mirrors Ástríðr's server-side `voice.py` state machine client-side (D-01/D-02).

Output: `src/components/voice/voiceState.ts` (+test), `src/components/voice/VoiceModePanel.tsx` (+test), modified `CommandPalette.tsx`, voice keyframes/reduced-motion in `src/index.css`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/92-voice-activated-command-palette-jarvis-mode/92-RESEARCH.md
@.planning/phases/92-voice-activated-command-palette-jarvis-mode/92-PATTERNS.md
@.planning/phases/92-voice-activated-command-palette-jarvis-mode/92-UI-SPEC.md
@.planning/phases/92-voice-activated-command-palette-jarvis-mode/92-VALIDATION.md
@src/hooks/useSpeechRecognition.ts
@src/hooks/useTtsPlayback.ts
@src/components/CommandPalette.tsx
@src/pages/Chat.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pure voice state machine + end-phrase detection</name>
  <files>src/components/voice/voiceState.ts, src/components/voice/voiceState.test.ts</files>
  <read_first>
    - .planning/phases/92-voice-activated-command-palette-jarvis-mode/92-UI-SPEC.md §"Voice-Mode State Machine (6 states)" (state definitions + triggers table)
    - .planning/phases/92-voice-activated-command-palette-jarvis-mode/92-RESEARCH.md §"Pattern 7: Turn-Loop State Machine" (lines 427-458 — transition order, feedback guard, END_PHRASES list mirroring voice.py:136-138)
    - .planning/phases/92-voice-activated-command-palette-jarvis-mode/92-VALIDATION.md (state-machine transitions are unit-testable; live audio is manual)
  </read_first>
  <behavior>
    - voiceReducer('listening', {type:'INTERIM_RESULT'}) → 'transcribing'.
    - voiceReducer('transcribing', {type:'FINAL_RESULT'}) → 'processing'.
    - voiceReducer('processing', {type:'TTS_START'}) → 'speaking'.
    - voiceReducer('speaking', {type:'TTS_END'}) → 'listening' (next turn, no re-wake).
    - voiceReducer(any, {type:'END'}) → 'idle' (end-phrase or silence timeout or close).
    - voiceReducer(any, {type:'ERROR'}) → 'error-disabled'.
    - isEndPhrase("stop") / "goodbye" / "thanks" / "that's all" → true; isEndPhrase("show me agents") → false; case-insensitive + trimmed.
  </behavior>
  <action>Create `src/components/voice/voiceState.ts` exporting `type VoiceState = 'idle'|'listening'|'transcribing'|'processing'|'speaking'|'error-disabled'`, a pure `voiceReducer(state, action)` implementing the transitions per UI-SPEC + RESEARCH Pattern 7, and `isEndPhrase(text)` using `END_PHRASES = ["stop","goodbye","thanks","that's all"]` (lowercased, trimmed compare). Action types: `WAKE`, `INTERIM_RESULT`, `FINAL_RESULT`, `TTS_START`, `TTS_END`, `END`, `ERROR`. No React, no side effects — this is the pure logic the panel's `useReducer` drives. Write `src/components/voice/voiceState.test.ts` covering every behavior above.</action>
  <verify>
    <automated>npx vitest run src/components/voice/voiceState.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/components/voice/voiceState.test.ts` exits 0 and covers all seven behaviors.
    - `voiceState.ts` exports `voiceReducer`, `isEndPhrase`, `VoiceState`; imports nothing from React.
    - END_PHRASES includes at least "stop" and "goodbye"; isEndPhrase is case-insensitive.
  </acceptance_criteria>
  <done>Pure, exported, fully-tested 6-state machine + end-phrase detector ready to drive the panel.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: VoiceModePanel — turn loop, transcript, reply, feedback guard</name>
  <files>src/components/voice/VoiceModePanel.tsx, src/components/voice/VoiceModePanel.test.tsx, src/index.css</files>
  <read_first>
    - .planning/phases/92-voice-activated-command-palette-jarvis-mode/92-UI-SPEC.md §"Palette voice-mode layout", §"State → visual rendering table", §"Accessibility Contract" (aria-live regions table), §"Copywriting Contract", §"prefers-reduced-motion" — implement the panel exactly to spec
    - .planning/phases/92-voice-activated-command-palette-jarvis-mode/92-PATTERNS.md §VoiceModePanel.tsx (lines 356-428 — inline subcomponents, streamed-text append, subscribeEvent cleanup, CommandGroup composition, aria-live) and §"Shared Patterns" (subscribeEvent cleanup, sendCommand+session_id tracking lines 661-683)
    - .planning/phases/92-voice-activated-command-palette-jarvis-mode/92-RESEARCH.md §"Pattern 7" feedback guard (stop STT on TTS start, start on audio end) + §"Open Question 4" (track activeSessionId from sendCommand ack for run.tts routing)
    - src/hooks/useSpeechRecognition.ts, src/hooks/useTtsPlayback.ts (the shared hooks to consume)
    - src/pages/Chat.tsx lines 129-181 (sendCommand + session_id ack pattern), 186-308 (subscribeEvent multi-unsub)
    - src/index.css line ~376 (existing @media prefers-reduced-motion block) and ~432-442 (.eq-bar-* classes the waveform reuses)
  </read_first>
  <behavior>
    - On final transcript, sendCommand is called with `{ type: 'chat.send', message: <final transcript> }` and activeSessionId is captured from the ack.
    - A run.text event for the active session appends to the reply stream (replyText grows).
    - A run.tts event for the active session calls useTtsPlayback.play(audio_url).
    - When useTtsPlayback.isPlaying flips true, recognition.stop() is called (feedback guard); when it flips false (TTS ended), recognition.start() is called for the next turn.
    - A final transcript matching an end-phrase ("stop") dispatches END (does not send a chat.send) and closes voice mode.
    - The panel renders the correct state label + aria-live regions per UI-SPEC for each VoiceState.
  </behavior>
  <action>Create `src/components/voice/VoiceModePanel.tsx` exporting `VoiceModePanel` (props: `voiceState`/initial state, `onClose`). Drive state with `useReducer(voiceReducer, ...)`. Consume `useSpeechRecognition({ continuous: true, interimResults: true, onInterimResult, onFinalResult, onEnd })` and `useTtsPlayback()`. Turn loop: on `onFinalResult(text)`, if `isEndPhrase(text)` dispatch END + call `onClose()`; else `dispatch(FINAL_RESULT)` and `sendCommand({ type:'chat.send', message:text })`, capturing `session_id` from the ack into an `activeSessionRef` (PATTERNS lines 674-683). Subscribe to `run.text`/`run.tts`/`run.completed` in one `useEffect` with multi-unsub cleanup (PATTERNS §subscribeEvent), filtering by `activeSessionRef`. On `run.text` append the chunk to a `replyText` state and dispatch as needed; on `run.tts` call `play(audio_url)` and dispatch TTS_START; on `run.completed` prepare the next turn. Feedback guard via a `useEffect` on `useTtsPlayback().isPlaying`: when true → `recognition.stop()` + dispatch TTS_START (speaking); when it transitions to false → `recognition.start()` + dispatch TTS_END (back to listening, next turn) — RESEARCH Pattern 7. Silence timeout: a ~30s timer (reset on each interim/final result) that dispatches END + `onClose()` when it elapses (UI-SPEC: palette closes, returns to listening at the top-bar level — `onClose` handles palette close). Render per UI-SPEC: inline `VoiceStateBadge` (status dot + label, `aria-live="assertive"`), `VoiceTranscriptArea` (interim muted-italic + final foreground, `aria-live="polite"`), `VoiceReplyStream` ("ÁSTRÍÐR" label + 14px streamed text, `aria-live="polite" aria-atomic="false"`), `VoiceWaveform` (3× `.eq-bar` reusing existing CSS), and the close `X` button (focus lands here on open per UI-SPEC focus management). Use ONLY `var(--primary)` as the voice accent and the exact tokens/copy from UI-SPEC; Lucide icons `Volume2`/`Loader2`/`X`/`AlertCircle`. Add to `src/index.css` (NOT a new CSS file — Tailwind 4 rule): any voice keyframe used (e.g. `voice-listen-pulse`) plus a `@media (prefers-reduced-motion: reduce)` override that disables the voice dot pulse and waveform animation per UI-SPEC. Decision: BARGE-IN is DEFERRED to a follow-on (RESEARCH Open Question 2 — avoids the recognition.start()-while-isPlaying race); implement the simpler "mic not listening during speaking" behavior (recognition paused while isPlaying) and note the deferral in the SUMMARY. Write `src/components/voice/VoiceModePanel.test.tsx` (mocks per setup.ts + AstridrWSContext mock per PATTERNS lines 720-735) covering the behaviors above: final-transcript→sendCommand with chat.send; run.text→reply append; run.tts→play; isPlaying true→recognition.stop and false→recognition.start (feedback guard); end-phrase→END+no-send; per-state label rendering.</action>
  <verify>
    <automated>npx vitest run src/components/voice/VoiceModePanel.test.tsx 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/components/voice/VoiceModePanel.test.tsx` exits 0.
    - Test asserts a final transcript triggers `sendCommand` with `{ type: 'chat.send', message: ... }` (grep "chat.send" present in panel).
    - Test asserts the feedback guard: recognition.stop() when isPlaying true, recognition.start() when isPlaying false.
    - Test asserts end-phrase "stop" dispatches END and does NOT call sendCommand.
    - Test asserts run.text appends to the reply stream and run.tts calls useTtsPlayback.play.
    - Panel uses `useSpeechRecognition` and `useTtsPlayback` (no inline `new SpeechRecognitionClass()` / `new Audio()` — grep both return 0).
    - aria-live regions present per UI-SPEC (assertive on state label, polite on transcript + reply).
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>VoiceModePanel runs the continuous turn loop over the existing transport, renders transcript + streamed reply, auto-plays TTS via the shared hook, and enforces the feedback guard + end-phrase/silence exit. Barge-in deferred (documented). Live audio behaviors verified per 92-VALIDATION manual checklist.</done>
</task>

<task type="auto">
  <name>Task 3: Wire VoiceModePanel into CommandPalette</name>
  <files>src/components/CommandPalette.tsx</files>
  <read_first>
    - src/components/CommandPalette.tsx lines 80-83 (props interface), 96-100 (CommandDialog + CommandInput + CommandList structure) — the additive insertion point
    - .planning/phases/92-voice-activated-command-palette-jarvis-mode/92-PATTERNS.md §CommandPalette.tsx (lines 504-537 — props extension + conditional render block)
    - .planning/phases/92-voice-activated-command-palette-jarvis-mode/92-UI-SPEC.md §"Wake-word → palette open transition" (voice panel replaces CommandInput+CommandList contents; no separate animation)
    - src/components/voice/VoiceModePanel.tsx (the component to render)
  </read_first>
  <action>Modify `src/components/CommandPalette.tsx` per 92-PATTERNS §CommandPalette.tsx: extend `CommandPaletteProps` with optional `voiceMode?: boolean`, `voiceState?: VoiceState`, `onVoiceClose?: () => void`. Inside the existing `<CommandDialog open onOpenChange>`, branch: when `voiceMode` is true render `<VoiceModePanel voiceState={voiceState ?? 'listening'} onClose={() => { onVoiceClose?.(); onOpenChange(false); }} />` in place of the `<CommandInput> + <CommandList>` block; otherwise render the existing content unchanged (purely additive — text mode is untouched). Import `VoiceModePanel` and `VoiceState` from `@/components/voice/...`. Do NOT alter the existing Pages/Agents/Sessions/Commands groups.</action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | tail -5 && grep -c "VoiceModePanel" src/components/CommandPalette.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `CommandPalette.tsx` `CommandPaletteProps` includes `voiceMode?`, `voiceState?`, `onVoiceClose?`.
    - When `voiceMode` is true the palette renders `VoiceModePanel` (grep count ≥ 1); when false the existing CommandInput/CommandList content is unchanged.
    - `npx tsc --noEmit` exits 0; existing CommandPalette behavior/tests unaffected.
  </acceptance_criteria>
  <done>CommandPalette conditionally renders VoiceModePanel in voice mode, additively, with text mode untouched. Ready for 92-05 to pass voiceMode from DashboardLayout.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Post-wake mic → Web Speech API | Mic audio sent to the browser speech service for transcription (only after explicit wake) |
| Final transcript → chat.send WS | Transcribed text crosses to the Ástríðr backend over the existing authenticated WS |
| TTS audio_url → <audio> | Server-provided audio URL fetched and played |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-92-10 | Information Disclosure | self-transcription feedback loop | mitigate | Feedback guard pauses recognition while `useTtsPlayback.isPlaying` is true, resumes on audio end (RESEARCH Pitfall 4) — Ástríðr's own reply is never sent back as a command |
| T-92-11 | Elevation of Privilege | speakable destructive commands | mitigate | Voice mode does NOT expose Emergency Stop as a speakable command (UI-SPEC Copywriting Contract); the existing Zap button stays the only surface for it |
| T-92-12 | Tampering | chat.send transport | accept | Reuses the existing authenticated `AstridrWSContext.sendCommand`; no new transport or auth surface introduced |
| T-92-13 | Denial of Service | runaway turn loop / stuck listening | mitigate | ~30s silence timeout + end-phrase exit dispatch END and close voice mode; cooldown in the wake engine bounds re-triggers |
</threat_model>

<verification>
- `npx vitest run src/components/voice/voiceState.test.ts src/components/voice/VoiceModePanel.test.tsx` green.
- `npx tsc --noEmit` exits 0.
- Grep: panel uses the shared hooks (no inline SpeechRecognition/Audio); "chat.send" present; CommandPalette renders VoiceModePanel.
- MANUAL (92-VALIDATION): live transcript shows; chat.send fires with the spoken text; reply renders + TTS plays once in persona voice; Ástríðr's reply is not self-transcribed.
</verification>

<success_criteria>
VoiceModePanel delivers the continuous turn loop, live transcript, streamed reply, shared-hook TTS playback, and feedback guard via a pure tested state machine, wired additively into CommandPalette. VOX-02 + VOX-03 satisfied at the palette layer; barge-in explicitly deferred.
</success_criteria>

<output>
Create `.planning/phases/92-voice-activated-command-palette-jarvis-mode/92-04-SUMMARY.md` when done
</output>
