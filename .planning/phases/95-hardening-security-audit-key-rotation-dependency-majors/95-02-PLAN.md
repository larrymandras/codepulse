---
phase: 95-hardening-security-audit-key-rotation-dependency-majors
plan: 02
type: execute
wave: 2
depends_on: [95-01]
files_modified: []
autonomous: false
requirements: [HARD-03]

must_haves:
  truths:
    - "The four folded majors (diff@8, js-yaml@5, jsdom@29, react-easy-crop@6) are confirmed at target versions on master"
    - "All six stale remote dependabot branches are deleted"
    - "react-easy-crop@6 cropper UI (AvatarUploader) renders and interacts correctly"
  artifacts: []
  key_links:
    - from: "src/components/AvatarUploader.tsx"
      to: "react-easy-crop@6"
      via: "import Cropper from 'react-easy-crop'"
      pattern: "from ['\"]react-easy-crop['\"]"
---

<objective>
Close out D-10 as the retrospective verification it actually is (RESEARCH Pitfall 1: the four "folded majors" already merged to master 2026-07-04 — they are NOT four fresh bumps). Confirm each is genuinely at target, delete the six lingering stale remote dependabot branches, and do the one manual item D-10 mandates: verify the `react-easy-crop@6` cropper UI surface (`AvatarUploader.tsx`, its sole consumer).

Purpose: Satisfies D-10's "each verified independently" bar without creating no-op re-bump commits, and completes dependency verification so Plan 03's audit certifies the settled tree (D-11).
Output: Confirmed-green majors, clean remote branch list, operator-verified cropper UI.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/95-hardening-security-audit-key-rotation-dependency-majors/95-RESEARCH.md
@.planning/phases/95-hardening-security-audit-key-rotation-dependency-majors/95-CONTEXT.md

<interfaces>
<!-- Already-merged majors (VERIFIED from git log + lockfile). Verify only — do NOT re-bump. -->
diff@8.0.3 (merged 142cc7c), js-yaml@5.2.1 (merged ec42253), jsdom@29.1.1 (merged ab2eab4), react-easy-crop@6.0.2 (merged c0c7bac). Master is currently green on `tsc --noEmit`.

Six stale remote branches to delete (confirmed present via `git branch -r`):
origin/dependabot/npm_and_yarn/diff-8.0.3, js-yaml-5.2.1, jsdom-29.1.1, react-day-picker-10.0.1, react-easy-crop-6.0.2, typescript-6.0.3

react-easy-crop sole consumer: src/components/AvatarUploader.tsx (`import Cropper from "react-easy-crop"`).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify already-merged majors at target and delete the six stale dependabot branches</name>
  <files>package.json, package-lock.json</files>
  <read_first>
    - package.json (diff ^8.0.3, js-yaml ^5.2.1, jsdom ^29.1.1, react-easy-crop ^6.0.2)
    - .planning/phases/95-hardening-security-audit-key-rotation-dependency-majors/95-RESEARCH.md (Pitfall 1, Standard Stack version table)
  </read_first>
  <action>
    Do NOT re-bump these four packages — they already merged to master 2026-07-04 (Pitfall 1: re-bumping creates no-op commits and confusion). Verify each installed version equals target with `node -e "console.log(require('./node_modules/<pkg>/package.json').version)"` for diff (expect 8.0.3), js-yaml (expect 5.2.1), jsdom (expect 29.1.1), react-easy-crop (expect 6.0.2). Confirm the tree is green by running `npx tsc --noEmit && npx vitest run && npm run build` (this doubles as D-10's "each verified independently" evidence since all four are already installed on the branch after Plan 01). Then delete the six stale remote dependabot branches with `git push origin --delete dependabot/npm_and_yarn/<branch>` for each of: diff-8.0.3, js-yaml-5.2.1, jsdom-29.1.1, react-day-picker-10.0.1, react-easy-crop-6.0.2, typescript-6.0.3. Optionally leave a closing comment on the old dependabot PRs (Claude's discretion per D-09/D-10) — not required. jsdom@29 needs no vitest-environment config change (vitest 4.1.9 peer jsdom = `*`; environment already `jsdom` in vitest.config.ts).
  </action>
  <verify>
    <automated>node -e "['diff','js-yaml','jsdom','react-easy-crop'].forEach(p=>console.log(p, require('./node_modules/'+p+'/package.json').version))"</automated>
  </verify>
  <acceptance_criteria>
    - diff=8.0.3, js-yaml=5.2.1, jsdom=29.1.1, react-easy-crop=6.0.2 all confirmed
    - `git branch -r | grep dependabot` returns no matches (all six deleted)
    - `npx tsc --noEmit && npx vitest run && npm run build` all exit 0
    - No new package.json version changes were introduced for these four (git diff shows no re-bump)
  </acceptance_criteria>
  <done>Four majors confirmed at target, tree green, six stale dependabot branches deleted.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Operator verifies react-easy-crop@6 cropper UI (D-10 manual check)</name>
  <files>src/components/AvatarUploader.tsx</files>
  <read_first>
    - src/components/AvatarUploader.tsx (sole react-easy-crop consumer — the cropper surface to verify)
    - .planning/phases/95-hardening-security-audit-key-rotation-dependency-majors/95-VALIDATION.md (Manual-Only Verifications table)
  </read_first>
  <action>Pause for operator verification of the react-easy-crop@6 cropper UI in AvatarUploader.tsx (D-10 mandates this manual check; no automated harness mounts the cropper). Present the how-to-verify steps below and wait for the resume signal before proceeding. Do not auto-approve.</action>
  <acceptance_criteria>
    - Operator confirms the crop frame renders, zoom works, drag-to-reposition works, and cropped output is produced without console errors
    - Any react-easy-crop@6 prop-API change reachable by AvatarUploader is confirmed non-breaking
  </acceptance_criteria>
  <what-built>
    Plan 01 landed TS 6.0.3 + removed dead deps; react-easy-crop@6.0.2 was already merged to master. No automated harness mounts the avatar cropper, so D-10 mandates a manual UI check of its sole consumer, `AvatarUploader.tsx`.
  </what-built>
  <how-to-verify>
    1. Start the dev server: `npm run dev` (Vite, port 5173).
    2. Navigate to the surface that mounts `AvatarUploader` (the avatar/profile image upload flow).
    3. Load an image into the cropper.
    4. Confirm: the crop frame renders, zoom control works, drag-to-reposition works, and a cropped output is produced without console errors.
    5. If the react-easy-crop@6 changelog notes any prop-API change reachable by AvatarUploader, confirm the component still passes the expected props (skim the changelog per RESEARCH note A3/A4).
  </how-to-verify>
  <resume-signal>Type "approved" if the cropper renders and interacts correctly, or describe the specific breakage observed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan verifies already-installed deps and deletes remote branches — no new code path or data boundary is introduced |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-95-02-DC | Tampering | react-easy-crop@6 (already merged) | accept | Mainstream established package in committed lockfile; `npm audit` 0 vulns; UI behavior confirmed by the manual checkpoint. |

Honest note: minimal threat surface. No installs, no code edits, no auth logic. Branch deletion is remote-repo hygiene. The one risk (a runtime UI regression in react-easy-crop@6 that tsc/vitest cannot catch) is covered by the Task 2 human-verify checkpoint.
</threat_model>

<verification>
- Four majors confirmed at target versions
- `npx tsc --noEmit && npx vitest run && npm run build` green
- Six stale dependabot branches deleted (`git branch -r | grep dependabot` empty)
- Operator approves react-easy-crop cropper UI
</verification>

<success_criteria>
D-10 closed as retrospective verification: majors confirmed green, stale branches cleaned up, cropper UI operator-verified. Dependency work fully settled ahead of the audit.
</success_criteria>

<output>
Create `.planning/phases/95-hardening-security-audit-key-rotation-dependency-majors/95-02-SUMMARY.md` when done.
</output>
