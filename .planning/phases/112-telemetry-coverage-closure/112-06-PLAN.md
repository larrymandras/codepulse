---
phase: 112-telemetry-coverage-closure
plan: 06
type: execute
wave: 4
depends_on: ["112-04"]
files_modified:
  - convex/telemetryDispositions.ts
  - convex/telemetryDispositions.test.ts
autonomous: true
requirements: [TELE-03]

must_haves:
  truths:
    - "D-10 is satisfied: the TELE-03 dispositions exist as a machine-readable const checked into CodePulse, not prose, and a test fails when a Group B kind is undisposed or its entry is deleted."
    - "D-11 is satisfied: every entry carries both its reason and its measurement date, and the test asserts the date is present rather than trusting the author."
    - "D-01's bar is recorded as the criterion each disposition was judged against - live arrival inside the 14-day retention window, established by a control-paired probe, not by emitter presence in astridr source."
    - "D-03's measurement of record is the source of the recorded dispositions, and its scope caveat survives: the four silent kinds are recorded as having no row in the 14-day window as of 2026-08-12, never as never emitted."
    - "D-05's resolved gate is recorded: message_routed is disposed routed on a measured 0.7-1.2 rows/day figure, which is the low-volume branch of D-05, not an exception to it."
    - "D-12 is recorded as deliberately out of scope: the astridr-repo-side emitter probe is named in this file as a considered-and-deferred guard, so a future reader can tell a deferral from an oversight."
    - "The guard is mutation-proven, not asserted: the test was observed failing with an entry deleted and with a generic-by-design reason rewritten as never emitted, then observed passing on restore."
  artifacts:
    - path: "convex/telemetryDispositions.ts"
      provides: "GROUP_B_DISPOSITIONS - one entry per Group B kind with disposition, table, reason and measurement date"
      exports: ["GROUP_B_DISPOSITIONS"]
      contains: "generic-table-by-design"
    - path: "convex/telemetryDispositions.test.ts"
      provides: "three-layer drift guard - harness liveness, per-kind specific assertions, source-text reason/date assertions"
  key_links:
    - from: "convex/telemetryDispositions.ts"
      to: "convex/runtimeIngest.ts dispatch switch"
      via: "test cross-check that every routed kind has a case and every generic kind does not"
      pattern: "case \"[a-z_.]+\""
    - from: "convex/telemetryDispositions.ts"
      to: "convex/schema.ts"
      via: "test cross-check that each routed kind's named table is a real defineTable"
      pattern: "defineTable\\("
---

<objective>
Turn this phase's dispositions into a machine-readable record with a guard that fails when a Group
B kind drifts out of it.

Purpose: TELE-03's actual requirement is not "route some kinds" - it is that every remaining Group
B kind has a stated, justified disposition and none is left ambiguous. Prose in a SUMMARY decays;
`convex/retention.ts`'s own header comment records why (a typo'd table name there is a permanent
SILENT no-op, which is exactly why `RETENTION_DAYS` has a test). This const is that same shape for
dispositions.

Output: one const module, one test module. No schema change, no ingest change, no UI.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/112-telemetry-coverage-closure/112-CONTEXT.md
@.planning/phases/112-telemetry-coverage-closure/112-RESEARCH.md
@.planning/phases/112-telemetry-coverage-closure/112-PATTERNS.md
@.planning/phases/112-telemetry-coverage-closure/112-04-SUMMARY.md
@.planning/REQUIREMENTS.md
</context>

<decided_shapes>
File and shape are Claude's Discretion per CONTEXT.md and are DECIDED here.

`convex/telemetryDispositions.ts` exports `GROUP_B_DISPOSITIONS`, a record keyed by the event-kind
string exactly as the ingest switch and the live data spell it. Each value carries `disposition`
(either `"routed"` or `"generic-table-by-design"`), `table` (present only when routed), `reason`
and `measured` (an ISO date string).

Seven entries, one per Group B kind. The reasons are FIXED here - the executor writes these, it
does not re-derive them:

| Kind | disposition | table | reason (verbatim intent) | measured |
|---|---|---|---|---|
| `governor_decision` | routed | `governorDecisions` | Clears D-01's bar: measured 83.3 rows/day, 1,168 rows across the full 14.02-day window (uncapped read). Routed AND surfaced per D-04 - the Governor Decisions section on the Settings notifications tab. | 2026-08-12 |
| `control_verb_swap` | routed | `controlVerbSwaps` | Already disposed by TELE-02 in Phases 108/109 - routed to a domain table and surfaced as per-profile swap history. Not re-litigated by Phase 112; it is the precedent this phase follows. | 2026-08-07 |
| `message_routed` | routed | `messageRoutes` | Clears D-01's bar and D-05's volume gate resolved to the low-volume branch: 10 rows in the entire 14-day window, roughly 0.7-1.2 rows/day, bursty. Routed per D-13 but deliberately NOT surfaced this phase - its channel/sender/session routing metadata needs its own UI design pass, not a reskin of the governor_decision surface. The surface is a recorded follow-up, not an ambiguity. | 2026-08-12 |
| `prompt_assembly` | generic-table-by-design | - | Emitter exists in astridr, no row in the 14-day window as of 2026-08-12. Fails D-01's bar, so a dedicated table would be built purely for switch-coverage symmetry, which REQUIREMENTS.md:57 forbids. | 2026-08-12 |
| `structured_output_exhausted` | generic-table-by-design | - | (same reason text as above) | 2026-08-12 |
| `vision.capture` | generic-table-by-design | - | (same reason text as above) | 2026-08-12 |
| `control_verb_focus` | generic-table-by-design | - | (same reason text as above) | 2026-08-12 |

**D-03's scope caveat is binding on the wording.** "No row in the 14-day window" means not in the
retention window - it does NOT mean never emitted. All four silent kinds have real emitters per the
Phase 105 grep. No reason string may say "never fires", "never emitted", "no emitter" or
equivalent. The test enforces this, so it is not a style note.

**File-level docstring must also record:**
- D-01's bar itself, in one sentence, so the criterion travels with the verdicts.
- D-02's re-measurability rule and the exact safe probe form (`events:listByType` with
  `--env-file`, never `events:countByType`, result count must come back UNDER the requested limit
  or it is a truncated sample), plus the control pair used - `llm_call` known-present returned
  1,261 rows, `definitely_not_a_real_kind_9x7q2` known-absent returned 0 - so a future
  re-measurement can be run the same way.
- D-12: an astridr-repo-side emitter probe (a guard failing when the contract documents a kind no
  code emits) was CONSIDERED and deliberately NOT taken into this phase - new guard, second repo,
  beyond TELE-01/TELE-03's letter. It is recorded in Deferred Ideas, not dropped.
- That timestamps in this domain are epoch SECONDS, confirmed against wall clock at measurement.

Keep the whole thing scannable on roughly one screen of const plus its docstring, the way
`RETENTION_DAYS` reads - CONTEXT.md names that file as the model to imitate explicitly.
</decided_shapes>

<tasks>

<task type="auto">
  <name>Task 1: Write convex/telemetryDispositions.ts (D-10, D-11, D-01, D-03, D-05, D-12)</name>
  <files>convex/telemetryDispositions.ts</files>
  <read_first>
    - convex/retention.ts lines 30-105 (the model this file imitates: exported const, in-line reason per entry, header comment stating what the test enforces and why)
    - .planning/phases/112-telemetry-coverage-closure/112-CONTEXT.md (D-01, D-02, D-03, D-05, D-10, D-11, D-12, D-13 in full)
    - .planning/phases/112-telemetry-coverage-closure/112-RESEARCH.md, Primary Research Question and Secondary Research Question 3 (the measured figures and the confirmed 7-kind Group B enumeration with exact spellings)
    - convex/runtimeIngest.ts - the two new cases added by plan 04, to confirm the kind strings are spelled identically
  </read_first>
  <action>
    Create `convex/telemetryDispositions.ts` with the exported `GROUP_B_DISPOSITIONS` record and a
    discriminated-union type for its values, populated exactly as fixed in `<decided_shapes>`.

    Spell each key exactly as the ingest switch and the live data spell it:
    `governor_decision`, `control_verb_swap`, `message_routed`, `prompt_assembly`,
    `structured_output_exhausted`, `vision.capture`, `control_verb_focus`. Note the dot in
    `vision.capture` - it is not an underscore.

    Write the file-level docstring covering the five points listed in `<decided_shapes>` (D-01's
    bar, D-02's re-measurement recipe with its control pair, D-12's deferral, the epoch-seconds
    note, and what the companion test enforces). State in that docstring, as `retention.ts` does
    for its own map, that a kind missing from this record is a silent drift and that
    `telemetryDispositions.test.ts` is what makes it loud.

    This module must not import `internalMutation`, `query`, `convex/values` or anything from
    `./_generated/*`. It is a plain const module, like `controlVerbSwapsFilters.ts`, so it stays
    importable from either bundle without dragging a runtime across.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `GROUP_B_DISPOSITIONS` has exactly 7 keys, matching the spellings above including the dot in `vision.capture`.
    - The three routed entries name `governorDecisions`, `controlVerbSwaps` and `messageRoutes` respectively.
    - Every entry has a non-empty `reason` and a `measured` value matching `/^\d{4}-\d{2}-\d{2}$/`.
    - No reason string contains `never emitted`, `never fires` or `no emitter`.
    - Every generic-by-design reason contains the phrase `no row in the 14-day window`.
    - The file source contains `D-01`, `D-02`, `D-03`, `D-05`, `D-10`, `D-11`, `D-12` and `D-13`.
    - The file contains zero `import` statements.
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>Seven disposed kinds with reasons and dates, and the criterion, the re-measurement recipe and the deferral all recorded in place.</done>
</task>

<task type="auto">
  <name>Task 2: Write the three-layer drift guard test</name>
  <files>convex/telemetryDispositions.test.ts</files>
  <read_first>
    - convex/retention.test.ts (full file, 231 lines - lines 14-27 source-level parsing, 29-36 the harness liveness check and its "guard the guard" comment, 57-71 the specific-value assertion and the mutation-testing rationale it records, 73-93 the known-present/known-absent pairing and the source-text `.toContain` assertion for a documented exemption)
    - .planning/phases/112-telemetry-coverage-closure/112-PATTERNS.md seam 7 (the three-layer model spelled out)
    - convex/telemetryDispositions.ts (as written in Task 1)
    - convex/runtimeIngest.ts and convex/schema.ts (the two sources the test parses)
  </read_first>
  <action>
    Create `convex/telemetryDispositions.test.ts` following `retention.test.ts`'s mechanism: read
    `convex/runtimeIngest.ts` and `convex/schema.ts` as raw source with `readFileSync` +
    `resolve(process.cwd(), ...)`, and parse them with regexes rather than importing generated
    Convex artefacts.

    **Layer 1 - guard the guard.** Before asserting anything, prove both parsers found a plausible
    non-empty set: the set of `case "<kind>":` strings extracted from `runtimeIngest.ts` must have
    more than 20 members and must contain a known-present control (`llm_call`) and not contain a
    known-absent control (`definitely_not_a_real_kind_9x7q2`); the set of `defineTable` names
    extracted from `schema.ts` must have more than 20 members and contain `alerts`. Without this,
    a regex that silently stops matching makes every downstream assertion pass vacuously.

    **Layer 2 - per-kind specific assertions.** Declare the canonical seven Group B kinds as a
    literal array IN THE TEST FILE, independent of the const under test, and assert set equality
    against `Object.keys(GROUP_B_DISPOSITIONS)` in both directions - no missing kind, no extra
    kind. Then write one explicit assertion block PER KIND naming that kind and its expected
    disposition (and its expected table, for the three routed ones). Do NOT replace these with a
    loop over the object's own keys: `retention.test.ts`'s own comment records the 2026-08-07
    adversarial finding that deleting an entry outright was caught by nothing generic, because a
    loop over the object cannot notice something absent from the object.

    Then cross-check the record against reality:
    - Every kind whose disposition is `routed` MUST have a matching `case "<kind>":` in
      `runtimeIngest.ts`'s parsed case set, AND its named `table` must be in the parsed
      `schema.ts` table set.
    - Every kind whose disposition is `generic-table-by-design` MUST NOT appear in the parsed case
      set. This is the assertion that catches someone routing a kind without updating the record.

    **Layer 3 - source-text reason/date assertions (D-11).** Read `convex/telemetryDispositions.ts`'s
    own source and assert: it contains `2026-08-12`; it contains `D-12` (the deferral is recorded,
    not dropped); it does NOT contain `never emitted`, `never fires` or `no emitter` anywhere - the
    D-03 scope caveat, enforced rather than trusted. Additionally assert against the imported
    object that every entry's `measured` matches an ISO date and every generic-by-design entry's
    `reason` contains `no row in the 14-day window`.

    Carry a comment in the test file stating exactly which mutations it is designed to catch, in
    the style `retention.test.ts` uses for its own.
  </action>
  <verify>
    <automated>npx vitest run convex/telemetryDispositions.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run convex/telemetryDispositions.test.ts` passes.
    - The suite contains a harness-liveness test asserting both parsed sets have more than 20 members and contain their named known-present controls, and that the case set does NOT contain the bogus control kind.
    - The canonical seven-kind list is declared in the TEST file, and set equality is asserted in both directions.
    - There is one explicitly-named assertion per kind - `grep -c "governor_decision" convex/telemetryDispositions.test.ts` and the same for each of the other six kinds each return at least 1.
    - The routed/case and generic/no-case cross-checks are both present.
    - The `never emitted` / `never fires` / `no emitter` negative source assertions are present.
  </acceptance_criteria>
  <done>All three layers implemented, with the enumeration proven live before anything is asserted against it.</done>
</task>

<task type="auto">
  <name>Task 3: Mutation-prove the guard - observe it fail on two distinct drifts</name>
  <files>convex/telemetryDispositions.ts (temporarily mutated then restored), convex/runtimeIngest.ts (temporarily mutated then restored)</files>
  <read_first>
    - convex/telemetryDispositions.ts and convex/telemetryDispositions.test.ts (as written in Tasks 1 and 2)
    - convex/retention.test.ts lines 48-71 (the comment recording how the equivalent guard was mutation-tested and what generic assertions failed to catch)
  </read_first>
  <action>
    A green that was never observed going red is not a guard. Run three mutations, one at a time,
    restoring fully between each, and capture the output of every run.

    Mutation A - deletion. Delete the `governor_decision` entry from `GROUP_B_DISPOSITIONS`. Run
    `npx vitest run convex/telemetryDispositions.test.ts`. It MUST fail, and the failure must come
    from the set-equality assertion and/or the per-kind assertion for `governor_decision` - not
    from a type error. Restore. Re-run. MUST pass.

    Mutation B - the D-03 scope caveat. Rewrite one generic-by-design entry's reason to say
    "never emitted" instead of "no row in the 14-day window". Run the suite. It MUST fail on the
    Layer 3 negative source assertion. Restore. Re-run. MUST pass.

    Mutation C - the record/reality cross-check. Change `message_routed`'s disposition from
    `routed` to `generic-table-by-design` while leaving its `case "message_routed"` in
    `runtimeIngest.ts` untouched. Run the suite. It MUST fail on the generic-kinds-have-no-case
    cross-check. Restore. Re-run. MUST pass.

    After all three, confirm `git diff convex/telemetryDispositions.ts` is byte-identical to its
    Task 1 state and `git diff convex/runtimeIngest.ts` is empty relative to plan 04's commit -
    restore means byte-identical, not "looks right".

    Quote all six run outputs (three red, three green) in the SUMMARY. If any mutation does NOT
    turn the suite red, the guard is insufficient - strengthen the test and re-run rather than
    accepting it.
  </action>
  <verify>
    <automated>npx vitest run convex/telemetryDispositions.test.ts && npm test</automated>
  </verify>
  <acceptance_criteria>
    - All three mutations were observed turning the suite RED, and all three restores were observed turning it GREEN. Six outputs quoted in the SUMMARY, each identifying the specific failing assertion.
    - After restore, `git diff convex/runtimeIngest.ts` is empty (no residue from Mutation C).
    - After restore, `convex/telemetryDispositions.ts` matches its Task 1 content byte-for-byte.
    - `npm test` (full suite) passes; totals quoted in the SUMMARY.
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>Three distinct drift classes each observed failing the guard and each observed passing after restore.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Disposition record -> future reader | This file is the answer to "why was this kind not routed?"; a stale or wrongly-worded entry misleads a future phase into rebuilding or wrongly deleting a route |
| Test regex -> source files it parses | A regex that stops matching turns every downstream assertion vacuous, which reads identically to a passing guard |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-112-19 | Repudiation | A guard that passes vacuously | mitigate | Layer 1 harness-liveness check asserts both parsed sets are non-trivially populated and contain a named known-present control while excluding a known-absent one, before any downstream assertion runs - the pattern `retention.test.ts:29-36` documents for exactly this failure |
| T-112-20 | Tampering | Silent deletion of a disposition entry | mitigate | Per-kind explicit assertions plus two-directional set equality against a canonical list declared independently in the test file; mutation A in Task 3 proves the deletion is caught |
| T-112-21 | Repudiation | A disposition reason overstating the evidence ("never emitted") | mitigate | Layer 3 negative source assertions on `never emitted` / `never fires` / `no emitter`; mutation B in Task 3 proves the assertion fires. D-03's caveat is enforced by test, not by author discipline |
| T-112-22 | Tampering | A kind routed in code but still recorded generic (or vice versa) | mitigate | Cross-check of the record against the parsed `runtimeIngest.ts` case set and the parsed `schema.ts` table set in both directions; mutation C in Task 3 proves it |
| T-112-23 | Information Disclosure | Recording a live probe recipe in a committed file | accept | The recorded recipe names `events:listByType` and `--env-file` precisely so no credential is ever placed on a command line; no key, URL secret or admin key is written into the file |
| T-112-SC | Tampering | npm/pip/cargo installs | accept | No package installed - two new source files in an already-configured repo |
</threat_model>

<verification>
1. `npx vitest run convex/telemetryDispositions.test.ts` passes.
2. `npm test` full suite passes; totals quoted.
3. Six mutation-proof outputs (3 red, 3 green) quoted in the SUMMARY.
4. `git diff convex/runtimeIngest.ts` is empty after Task 3.
5. `npx tsc --noEmit` exits 0.
</verification>

<success_criteria>
- Every Group B kind has exactly one recorded disposition with a reason and a measurement date (D-10, D-11).
- D-01's bar, D-02's re-measurement recipe, D-03's scope caveat, D-05's resolved gate and D-12's deferral are all recorded in the file itself, not only in planning documents.
- The guard was observed failing on deletion, on caveat-violating wording, and on record/reality divergence.
</success_criteria>

<output>
Create `.planning/phases/112-telemetry-coverage-closure/112-06-SUMMARY.md` when done, quoting all
six mutation-proof run outputs and the full-suite totals.
</output>
