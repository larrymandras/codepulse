---
phase: 112-telemetry-coverage-closure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - C:/Users/mandr/astridr-repo/docs/astridr-contract.md
autonomous: true
requirements: [TELE-01]

must_haves:
  truths:
    - "D-07 is satisfied: sections 2.20 through 2.24 of astridr-repo/docs/astridr-contract.md each carry a dated NOT EMITTED banner, and all five sections keep their original section numbers and original file positions - nothing deleted, nothing relocated, 2.25 through 2.40 numbering untouched."
    - "D-08 is satisfied: the three critical-events rows for worktree_lifecycle, batch_execution and loop_lifecycle are removed from the critical-events table, and the adjacent subagent_job row is left intact."
    - "D-09 is satisfied: each banner states WHY the section exists - v1.6.0 (2026-03-09), Claude Code Feb/Mar 2026 release alignment, sourced from docs/new_claude_capabilities.md - not merely that the kind is unimplemented."
    - "The banner's zero-emitter claim is backed by a search run inside this plan and paired with a known-present control, not asserted from memory."
    - "The astridr-repo commit contains only docs/astridr-contract.md - no CodePulse file, and no file another concurrent session left in the working tree."
  artifacts:
    - path: "C:/Users/mandr/astridr-repo/docs/astridr-contract.md"
      provides: "Group A sections marked aspirational in place; three unfirable critical-events rows removed"
      contains: "NOT EMITTED"
  key_links:
    - from: "docs/astridr-contract.md sections 2.20-2.24"
      to: "docs/new_claude_capabilities.md"
      via: "banner citation of the originating PRD (D-09)"
      pattern: "new_claude_capabilities\\.md"
---

<objective>
Make `astridr-repo/docs/astridr-contract.md` stop presenting the five Group A event kinds
(`instructions_loaded`, `loop_lifecycle`, `worktree_lifecycle`, `batch_execution`,
`auto_memory`) as live behaviour, and remove the three operator-alerting rows that promise
alerts which can never fire.

Purpose: TELE-01. The contract currently asserts behaviour no code produces. That is the same
defect class section 2.25 already records ("contract claims a route that doesn't exist"), and it
is what made the Group A / Group B split necessary in the first place.

Output: one edited markdown file in a second repository, one commit in that repository. No code,
no build, no CodePulse change.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/112-telemetry-coverage-closure/112-CONTEXT.md
@.planning/phases/112-telemetry-coverage-closure/112-RESEARCH.md
</context>

<working_directory>
**This plan operates in `C:\Users\mandr\astridr-repo`, NOT in CodePulse.**

Every path below starting with `docs/` is relative to `C:\Users\mandr\astridr-repo`. That
checkout is on branch `feature/brain-swap` and is SHARED with other sessions. Hard rules:

- Never `git add -A` or `git add .` there. Name `docs/astridr-contract.md` explicitly.
- Never `git commit --amend` there.
- After committing, run `git show --stat HEAD` and READ it. If any file other than
  `docs/astridr-contract.md` appears, do not rewrite history - disclose it in the SUMMARY.
- At least one untracked file in that repo belongs to another session
  (`.planning/phases/188.5-*/.review-fix-recovery-pending.json`). Do not stage, move or delete it.
- Do not build, restart, deploy or rebuild anything in astridr-repo. This is a prose edit.
- No CodePulse source file is modified by this plan. If you are editing anything under
  `C:\Users\mandr\codepulse` other than this phase's SUMMARY, stop.
</working_directory>

<banner_template>
The exact banner to insert. Substitute `<KIND>` with that section's own event-type name
(`instructions_loaded`, `loop_lifecycle`, `worktree_lifecycle`, `batch_execution`,
`auto_memory`). Everything else is byte-identical across all five insertions.

Insert as a markdown blockquote immediately BELOW the `### 2.NN` heading line and immediately
ABOVE that section's existing `Emitted by:` line, separated from both by one blank line.

    > **NOT EMITTED - aspirational (verified 2026-08-12).** Nothing in this repository emits
    > `<KIND>`. A fixed-string search of the source tree for this event name finds zero emitter
    > call sites; the same search in the same run finds call sites for `governor_decision`, a kind
    > that *is* emitted - so the zero result discriminates rather than reflecting a broken search.
    > Treat the fields below as a forward contract, not as observed behaviour.
    >
    > **Why this section exists:** it was added in **v1.6.0 (2026-03-09)** under "Claude Code
    > Feb/Mar 2026 release alignment" (see the changelog), one of five event types (2.20-2.24)
    > documented *ahead of* implementation, by intent, and sourced from the implementation PRD at
    > `docs/new_claude_capabilities.md`. It is retained in place deliberately: do not delete it and
    > do not renumber it - writing the emitter is what makes it true.
    >
    > Recorded by CodePulse Phase 112 (TELE-01, decisions D-07/D-08/D-09).

The literal string `NOT EMITTED` must appear on exactly one line per banner - five lines total in
the file, and nowhere else in the file.

**This template is a planner-authored DRAFT, not a spec.** If the live section structure you read
in `read_first` contradicts the placement described here, follow the live file and note the
deviation in the SUMMARY.
</banner_template>

<tasks>

<task type="auto">
  <name>Task 1: Establish the zero-emitter fact with a paired control</name>
  <files>C:/Users/mandr/astridr-repo/docs/astridr-contract.md (READ ONLY in this task — no write)</files>
  <read_first>
    - C:/Users/mandr/astridr-repo/docs/astridr-contract.md lines 651-830 (section 2.19 through 2.25 - the five Group A sections and the boundary you must not disturb)
    - .planning/phases/112-telemetry-coverage-closure/112-CONTEXT.md (D-07, D-09, and the Phase 105 emitter-grep provenance under Canonical References)
  </read_first>
  <action>
    From `C:\Users\mandr\astridr-repo`, search the source tree for each of the five Group A
    event-type strings - `instructions_loaded`, `loop_lifecycle`, `worktree_lifecycle`,
    `batch_execution`, `auto_memory` - and for the known-present control `governor_decision`, in
    the SAME run and with the SAME command form.

    Use a fixed-string matcher (`grep -rF` or `git grep -F`), restrict to source extensions
    (`--include=*.py --include=*.ts --include=*.js`), and exclude `docs/` so the contract's own
    prose is not counted as an emitter. Do not hand-escape the patterns.

    Read the CONTROL result first. If `governor_decision` returns zero, the search is broken
    (wrong CWD, wrong include filter, wrong path) - fix it and re-run before interpreting any
    subject result. A zero for the five subjects is evidence only once the control is non-zero in
    that same run.

    Record all six counts verbatim in the SUMMARY along with the exact command. If ANY of the five
    Group A kinds returns a non-zero emitter count, STOP the plan and report it - the banner's
    factual claim would be false for that kind and D-07's premise needs revisiting before any
    prose is written.
  </action>
  <verify>
    <automated>cd /c/Users/mandr/astridr-repo && grep -rF --include=*.py --include=*.ts --include=*.js -l "governor_decision" . | head -5</automated>
  </verify>
  <acceptance_criteria>
    - The control `governor_decision` returns at least one matching source file outside `docs/`; that file list is quoted in the SUMMARY.
    - Each of the five Group A event-type strings returns zero matching source files outside `docs/`.
    - The exact command and all six counts appear in the SUMMARY - a bare conclusion ("confirmed zero emitters") is not acceptable.
    - `git status --short` in astridr-repo shows no modification from this task (search only).
  </acceptance_criteria>
  <done>Six counts recorded with the control non-zero and all five subjects zero, or the plan is stopped and the discrepancy reported.</done>
</task>

<task type="auto">
  <name>Task 2: Insert the five dated NOT EMITTED banners in place (D-07, D-09)</name>
  <files>C:/Users/mandr/astridr-repo/docs/astridr-contract.md</files>
  <read_first>
    - C:/Users/mandr/astridr-repo/docs/astridr-contract.md lines 651-830 (heading style and the exact section boundaries)
    - C:/Users/mandr/astridr-repo/docs/astridr-contract.md lines 1878-1889 (the changelog, to confirm the v1.6.0 wording the banner cites)
    - The `<banner_template>` block in this plan file
  </read_first>
  <action>
    Before editing, capture the baseline: the count of lines containing `NOT EMITTED` in
    `docs/astridr-contract.md` must be 0. If it is not 0, stop and report - a prior banner exists
    and the target count of 5 would be wrong.

    Edit `docs/astridr-contract.md`. Insert the `<banner_template>` banner five times, once under
    each of `### 2.20 instructions_loaded`, `### 2.21 loop_lifecycle`,
    `### 2.22 worktree_lifecycle`, `### 2.23 batch_execution`, `### 2.24 auto_memory`,
    substituting `<KIND>` with that section's own event name.

    Per D-07 this is IN-PLACE only. Do not delete a section, do not move a section, do not change
    any `### 2.NN` number anywhere in the file, and do not touch field tables, JSON examples,
    `Emitted by:` / `When:` lines, or the file header. Everything from `### 2.25 prompt_assembly`
    onward must be byte-identical except for its line offsets shifting.

    Per D-09 the "Why this section exists" paragraph is the load-bearing part - the v1.6.0 /
    2026-03-09 / "Claude Code Feb/Mar 2026 release alignment" / `docs/new_claude_capabilities.md`
    citation must survive verbatim. A banner reduced to "not implemented" fails this task.

    Use the Edit tool. Do NOT use a shell heredoc, and do NOT use a PowerShell
    `Get-Content`/`Set-Content` round-trip on this file: it contains non-ASCII characters
    (section signs, em dashes, check/cross marks) that a PS 5.1 round-trip mojibakes across the
    whole file. If `git diff --stat` reports far more changed lines than the banners you inserted,
    that is corruption - revert with `git checkout -- docs/astridr-contract.md` and redo with Edit.
  </action>
  <verify>
    <automated>cd /c/Users/mandr/astridr-repo && grep -cF "NOT EMITTED" docs/astridr-contract.md && grep -nE "^### 2\.(19|20|21|22|23|24|25|40) " docs/astridr-contract.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cF "NOT EMITTED" docs/astridr-contract.md` outputs exactly `5` (baseline before the edit was `0`).
    - `grep -cF "new_claude_capabilities.md" docs/astridr-contract.md` is at least `6` (five banners plus the pre-existing changelog citation).
    - `grep -nE "^### 2\.(20|21|22|23|24) " docs/astridr-contract.md` lists exactly the five original headings, in their original order, with their original event names.
    - `grep -nE "^### 2\.40 " docs/astridr-contract.md` still finds section 2.40 `governor_decision` - it was neither renumbered nor removed.
    - `git diff --numstat docs/astridr-contract.md` shows deletions of `0` (banners are pure insertions).
    - Each of the five banners contains the strings `v1.6.0`, `2026-03-09` and `new_claude_capabilities.md`.
  </acceptance_criteria>
  <done>Five in-place banners present, zero deletions, section numbering provably unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: Remove the three unfirable critical-events rows (D-08), verify, commit</name>
  <files>C:/Users/mandr/astridr-repo/docs/astridr-contract.md</files>
  <read_first>
    - C:/Users/mandr/astridr-repo/docs/astridr-contract.md - the critical-events table. After Task 2 the banners shifted every line below them, so locate the table by CONTENT, not by the pre-edit line numbers 1785-1787: search for the row beginning with a pipe followed by a backtick-quoted `worktree_lifecycle`.
    - .planning/phases/112-telemetry-coverage-closure/112-CONTEXT.md (D-08 and its rationale)
  </read_first>
  <action>
    In the critical-events table of `docs/astridr-contract.md`, delete exactly three consecutive
    table rows - the ones whose first cell is `worktree_lifecycle`, `batch_execution` and
    `loop_lifecycle`. Before Task 2's insertions these were lines 1785, 1786 and 1787; locate them
    now by content.

    Do NOT delete the `subagent_job` row that immediately follows them (Phase 168, a real emitter),
    the `mcp_connection` row that immediately precedes them, or the table's closing "Framework
    implementation: Check `_is_critical()` in ConvexHandler before buffering." note. Delete the
    three rows only - no header change, no surrounding prose change.

    Rationale to keep in mind while editing (D-08): all three are Group A kinds with zero
    emitters, so the table currently documents operator alerts that can never fire. Marking the
    sections aspirational while leaving live-looking alert rows would leave the defect half-fixed.

    Then commit in `C:\Users\mandr\astridr-repo` naming the file explicitly - `git add
    docs/astridr-contract.md` then `git commit`. Do NOT use `git add -A`. Do NOT use
    `git commit --amend`. Immediately after committing run `git show --stat HEAD` and read the
    file list. Suggested subject: `docs(contract): mark Group A event kinds aspirational, drop
    unfirable critical-event rows (CodePulse TELE-01)`.
  </action>
  <verify>
    <automated>cd /c/Users/mandr/astridr-repo && grep -cE "^\| \`(worktree_lifecycle|batch_execution|loop_lifecycle)\` \|" docs/astridr-contract.md; grep -cE "^\| \`(subagent_job|mcp_connection)\` \|" docs/astridr-contract.md; git show --stat HEAD</automated>
  </verify>
  <acceptance_criteria>
    - The critical-events table contains zero rows whose first cell is `worktree_lifecycle`, `batch_execution` or `loop_lifecycle` (first grep above outputs `0`).
    - The `subagent_job` and `mcp_connection` critical-events rows still exist (second grep outputs `2`).
    - The string "Framework implementation: Check `_is_critical()`" is still present in the file.
    - `git show --stat HEAD` lists exactly one file: `docs/astridr-contract.md`. The stat line and the commit hash are quoted in the SUMMARY.
    - `git log -1 --format=%H` is quoted in the SUMMARY as the created commit; no `--amend` was run.
    - `grep -cF "NOT EMITTED" docs/astridr-contract.md` is still exactly `5` after the deletions.
  </acceptance_criteria>
  <done>Three rows removed, neighbours intact, single-file commit created and its stat output read and quoted.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CodePulse session -> astridr-repo working tree | A CodePulse-scoped agent writes into a second, shared git checkout that other sessions are concurrently committing into |
| Contract prose -> future implementer | The document is the only source of truth for what astridr emits; a wrong claim here propagates into other repos' designs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-112-06 | Tampering | astridr-repo git index (shared checkout) | mitigate | Explicit single-file `git add docs/astridr-contract.md`; `git add -A` and `git commit --amend` are both forbidden by this plan; mandatory post-commit `git show --stat HEAD` read, with disclose-not-rewrite as the recovery |
| T-112-07 | Repudiation | Banner factual claim ("nothing emits this") | mitigate | Task 1 requires a control-paired search whose command and six counts are quoted in the SUMMARY; a non-zero subject halts the plan rather than shipping a false claim |
| T-112-08 | Tampering | File-wide encoding corruption | mitigate | Edit tool only; PowerShell `Get-Content`/`Set-Content` round-trips explicitly forbidden on this non-ASCII file; `git diff --numstat` deletions must be `0` in Task 2 |
| T-112-09 | Information Disclosure | Banner prose committed to a repo with a gitleaks CI scan | accept | The banner is dated prose citing a doc path; it contains no credential-shaped content, and RESEARCH.md confirms `gitleaks-scan.yml` scans for secrets only and no doc linter touches `docs/*.md` |
| T-112-SC | Tampering | npm/pip/cargo installs | accept | This plan installs nothing - it is a prose edit in a second repo with no build step |
</threat_model>

<verification>
1. `grep -cF "NOT EMITTED" docs/astridr-contract.md` = `5`, baseline was `0`.
2. `grep -nE "^### 2\.(20|21|22|23|24|25|40) " docs/astridr-contract.md` shows the original headings with original numbers and original event names.
3. Zero rows for the three Group A kinds remain in the critical-events table; `subagent_job` and `mcp_connection` survive.
4. `git show --stat HEAD` in astridr-repo lists exactly one file.
5. `git status --short` in `C:\Users\mandr\codepulse` shows no source change from this plan.
</verification>

<success_criteria>
- All five Group A sections carry a dated banner citing v1.6.0 / 2026-03-09 / `docs/new_claude_capabilities.md` (D-07, D-09).
- The three unfirable critical-events rows are gone; the two neighbouring rows are not (D-08).
- Section numbering 2.20 through 2.40 is provably unchanged.
- One commit in astridr-repo touching exactly one file, with its stat output read after the fact.
</success_criteria>

<output>
Create `.planning/phases/112-telemetry-coverage-closure/112-01-SUMMARY.md` when done. It must
quote: the six search counts from Task 1 with the command used, the pre-edit and post-edit
`NOT EMITTED` counts, the section-heading listing, and the `git show --stat HEAD` output.
</output>
