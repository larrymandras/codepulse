---
phase: 02-bidirectional-telemetry
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - src/hooks/useLiveState.test.ts
  - src/components/ConnectionPopover.test.tsx
  - src/index.css
autonomous: true
requirements:
  - RT-03
  - RT-04
  - RT-08
  - RT-02

must_haves:
  truths:
    - "Test stubs exist for useLiveState covering disconnect-clear, agent status update, and event-driven state change"
    - "Test stubs exist for ConnectionPopover covering auth error display and disconnected state rendering"
    - "live-update-pulse CSS animation keyframe is defined in index.css"
  artifacts:
    - path: "src/hooks/useLiveState.test.ts"
      provides: "Nyquist-compliant test stubs for useLiveState hook"
      contains: "test.todo"
    - path: "src/components/ConnectionPopover.test.tsx"
      provides: "Nyquist-compliant test stubs for ConnectionPopover"
      contains: "test.todo"
    - path: "src/index.css"
      provides: "live-update-pulse animation keyframe and .live-update-flash class"
      contains: "live-update-pulse"
  key_links: []
---

<objective>
Create Wave 0 test stubs for useLiveState and ConnectionPopover (Nyquist compliance), add the live-update-pulse CSS animation keyframe to index.css, and install the shadcn Popover component.

Purpose: Establish test infrastructure before implementation (Wave 0 pattern from Phase 01) and add the CSS animation that will be consumed by all page wiring in Plan 03.
Output: Two test stub files, updated index.css with animation, shadcn popover component installed.
</objective>

<execution_context>
@C:\Users\mandr\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\mandr\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-bidirectional-telemetry/02-CONTEXT.md
@.planning/phases/02-bidirectional-telemetry/02-UI-SPEC.md
@.planning/phases/02-bidirectional-telemetry/02-VALIDATION.md

<interfaces>
<!-- From src/contexts/AstridrWSContext.tsx -->
```typescript
export type WSStatus = "connected" | "reconnecting" | "disconnected";
interface AstridrWSContextValue {
  status: WSStatus;
  sendCommand: (cmd: Record<string, unknown>) => Promise<AckResponse>;
  subscribe: (topic: string, callback: TopicCallback) => () => void;
  subscribeEvent: (eventType: string, callback: TopicCallback) => () => void;
}
export function useAstridrWS(): AstridrWSContextValue;
```

<!-- From src/hooks/useCommandCatalog.ts — reference test mock pattern -->
```typescript
// Mocking pattern: vi.mock("@/contexts/AstridrWSContext") with vi.fn() returns
// for status, subscribeEvent, sendCommand, subscribe
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wave 0 test stubs and CSS animation</name>
  <files>src/hooks/useLiveState.test.ts, src/components/ConnectionPopover.test.tsx, src/index.css</files>
  <read_first>
    - src/index.css (current state — find insertion point for new keyframe)
    - src/hooks/useCommandCatalog.ts (reference pattern for mocking useAstridrWS)
    - src/test/setup.ts (test infrastructure configuration)
    - .planning/phases/02-bidirectional-telemetry/02-VALIDATION.md (Wave 0 requirements)
    - .planning/phases/02-bidirectional-telemetry/02-UI-SPEC.md (exact CSS keyframe definition)
  </read_first>
  <action>
1. Create `src/hooks/useLiveState.test.ts` with `test.todo` stubs (no implementation). Mock `useAstridrWS` using `vi.mock("@/contexts/AstridrWSContext")`. Include these stubs:
   - `test.todo("updates agentStatus when agent_status_change event received")` — RT-08
   - `test.todo("clears all state to null/empty when wsStatus transitions to disconnected")` — RT-04, D-05
   - `test.todo("clears all state when wsStatus transitions to reconnecting")` — RT-04
   - `test.todo("sets connectionHealth from wsStatus")` — RT-03
   - `test.todo("subscribes to agent_status_change event via subscribeEvent")` — RT-03
   - `test.todo("updates liveMetricDeltas when metric delta event received")` — RT-03
   - `test.todo("returns isLive=true only when connectionHealth is connected")` — RT-03
   - `test.todo("validates payload shape before updating state")` — T-02-01

2. Create `src/components/ConnectionPopover.test.tsx` with `test.todo` stubs. Include:
   - `test.todo("renders CONNECTION DETAILS header")` — RT-02
   - `test.todo("shows Disconnected status with --status-error dot when disconnected")` — RT-02
   - `test.todo("shows auth error message when auth failure detected")` — RT-02, D-07
   - `test.todo("shows Reconnect button only when disconnected")` — D-11
   - `test.todo("hides Reconnect button when connected")` — D-11
   - `test.todo("renders URL, uptime, latency, topics, last event rows")` — D-10

3. Add the `live-update-pulse` CSS keyframe and `.live-update-flash` class to `src/index.css`. Insert AFTER the existing `@keyframes slide-in-entry` block (or at the end of the keyframes section). Exact CSS:

```css
@keyframes live-update-pulse {
  0%   { background-color: oklch(0.205 0 0 / 0.08); }
  100% { background-color: transparent; }
}
.live-update-flash {
  animation: live-update-pulse 600ms ease-out forwards;
}
```

Note: Use hardcoded `oklch(0.205 0 0 / 0.08)` instead of `oklch(from var(--primary) l c h / 0.08)` — the relative color syntax has limited browser support. The hardcoded value matches `--primary` in dark mode. This is a single-operator dashboard so dark mode value is sufficient.
  </action>
  <verify>
    <automated>cd C:/Users/mandr/codepulse && npx vitest run src/hooks/useLiveState.test.ts src/components/ConnectionPopover.test.tsx --reporter=verbose 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - src/hooks/useLiveState.test.ts exists and contains `test.todo("updates agentStatus when agent_status_change event received")`
    - src/hooks/useLiveState.test.ts contains `test.todo("clears all state to null/empty when wsStatus transitions to disconnected")`
    - src/hooks/useLiveState.test.ts contains at least 8 test.todo entries
    - src/components/ConnectionPopover.test.tsx exists and contains `test.todo("renders CONNECTION DETAILS header")`
    - src/components/ConnectionPopover.test.tsx contains `test.todo("shows auth error message when auth failure detected")`
    - src/components/ConnectionPopover.test.tsx contains at least 6 test.todo entries
    - src/index.css contains `@keyframes live-update-pulse`
    - src/index.css contains `.live-update-flash`
    - src/index.css contains `animation: live-update-pulse 600ms ease-out forwards`
    - All test.todo stubs pass (vitest reports them as skipped/todo, not failed)
  </acceptance_criteria>
  <done>Both test stub files exist with all required todo entries. CSS animation keyframe added to index.css. Vitest runs without errors (all tests are .todo so none fail).</done>
</task>

<task type="auto">
  <name>Task 2: Install shadcn Popover component</name>
  <files>src/components/ui/popover.tsx</files>
  <read_first>
    - src/components/ui/ (verify popover.tsx does not already exist)
    - components.json (shadcn config — confirm preset)
  </read_first>
  <action>
Run `npx shadcn add popover` to install the Popover component from the official shadcn registry. This will create `src/components/ui/popover.tsx` using the Radix Popover primitive.

After install, verify the file exists and exports `Popover`, `PopoverTrigger`, `PopoverContent`.

Run `npx tsc --noEmit` to confirm no type errors introduced.
  </action>
  <verify>
    <automated>cd C:/Users/mandr/codepulse && npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - src/components/ui/popover.tsx exists
    - src/components/ui/popover.tsx contains `PopoverContent`
    - src/components/ui/popover.tsx contains `PopoverTrigger`
    - `npx tsc --noEmit` exits with code 0
  </acceptance_criteria>
  <done>shadcn Popover component installed at src/components/ui/popover.tsx. TypeScript compilation passes.</done>
</task>

</tasks>

<threat_model>
## Threat Model

| ID | Threat | STRIDE | Severity | Mitigation | Task |
|----|--------|--------|----------|------------|------|
| T-02-01 | Malformed WS payload crashing React state in useLiveState | Tampering | medium | Test stub includes "validates payload shape before updating state" — implementation will validate in Plan 02 | Task 1 |

No trust boundaries crossed in this plan (test stubs + CSS only). Threat register entries are forward-looking for Plan 02 implementation.
</threat_model>

<verification>
- `npx vitest run src/hooks/useLiveState.test.ts src/components/ConnectionPopover.test.tsx` completes with all todos reported
- `npx tsc --noEmit` passes
- `grep "live-update-pulse" src/index.css` returns a match
- `ls src/components/ui/popover.tsx` confirms file exists
</verification>

<success_criteria>
Wave 0 test infrastructure is in place. CSS animation is ready for consumption. shadcn Popover is installed. No test failures, no type errors.
</success_criteria>

<output>
After completion, create `.planning/phases/02-bidirectional-telemetry/02-01-SUMMARY.md`
</output>
