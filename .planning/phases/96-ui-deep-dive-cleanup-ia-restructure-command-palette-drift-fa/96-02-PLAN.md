---
phase: 96-ui-deep-dive-cleanup-ia-restructure-command-palette-drift-fa
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/layouts/DashboardLayout.tsx
  - src/layouts/__tests__/DashboardLayout.test.tsx
autonomous: true
requirements: [F1, D-03, F3, D-04, F2]
must_haves:
  truths:
    - "The CONSOLE nav cluster is removed from navGroups (D-03): Forge sits in COMMAND, the duplicate Live Run CONSOLE entry is gone, Executions and Build sit in OBSERVE"
    - "Live Run appears exactly once in the flat nav (no duplicate route)"
    - "Header SYS readout shows real systemResources.current.cpu and is HIDDEN entirely when cpu is null/absent (D-04, honesty-first — never a fabricated number)"
    - "Header LAT readout shows a real WS round-trip latency (ConnectionPopover ping pattern) and is hidden when unavailable (D-04)"
    - "iconComponents is exported from DashboardLayout alongside navItems so CommandPalette can resolve string-keyed icons (F2 enabler)"
  artifacts:
    - path: "src/layouts/DashboardLayout.tsx"
      provides: "Restructured navGroups, real header telemetry, iconComponents export"
      contains: "export { iconComponents"
  key_links:
    - from: "src/layouts/DashboardLayout.tsx header"
      to: "api.systemResources.current"
      via: "useQuery + null-per-field guard"
      pattern: "systemResources.current"
    - from: "src/layouts/DashboardLayout.tsx"
      to: "CommandPalette"
      via: "export { iconComponents, navItems }"
      pattern: "export \\{ *iconComponents"
---

<objective>
Restructure the sidebar IA and make the header telemetry honest — all inside the single file that owns them (`DashboardLayout.tsx`). Three changes: (1) F1/D-03 dissolve the CONSOLE cluster in `navGroups`; (2) F3/D-04 replace the fabricated `SYS: 14%` / `LAT: 12ms` literals with real data that hides when absent; (3) F2 enabler — export `iconComponents` so CommandPalette can resolve string-keyed icons in Plan 05.

Purpose: Wave 1 foundation. Plan 05 (CommandPalette) depends on the `iconComponents` export; nothing else touches this file.
Output: Restructured nav, live-or-hidden header telemetry, exported icon map, extended layout test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/96-ui-deep-dive-cleanup-ia-restructure-command-palette-drift-fa/FINDINGS.md
@.planning/phases/96-ui-deep-dive-cleanup-ia-restructure-command-palette-drift-fa/96-PATTERNS.md
@.planning/phases/96-ui-deep-dive-cleanup-ia-restructure-command-palette-drift-fa/96-RESEARCH.md
@CLAUDE.md

<interfaces>
Current navGroups shape (src/layouts/DashboardLayout.tsx): flat TS array of `{ group, items: [{ to, label, icon, group }] }`. CONSOLE group currently holds: `{to:"/live-run"}` (DUPLICATE of the COMMAND entry), `{to:"/executions"}`, `{to:"/build"}`, `{to:"/forge"}`.

Target navGroups edits:
- Add `{ to: "/forge", label: "Forge", icon: "flame", group: "COMMAND" }` into the COMMAND group.
- Move `{ to: "/executions", ... }` and `{ to: "/build", ... }` into the OBSERVE group (update their `group` field to "OBSERVE").
- Delete the entire CONSOLE group object (including its duplicate `/live-run` entry). COMMAND keeps its existing `/live-run` entry.
- Note: Mission Control stays in OBSERVE for now; its merge/removal is Plan 04, not this plan.

systemResources query (convex/systemResources.ts): `api.systemResources.current` returns `null` OR an object with per-field-nullable `{ cpu, ram, disk }` — NO latency field. Guard the FIELD: `const showSys = resources?.cpu != null`. Never `resources ? … : "—"` and never `resources?.cpu ?? 0`.

LAT source (src/components/ConnectionPopover.tsx:90-113): WS ping RTT pattern —
`const start = performance.now(); await sendCommand({ type: "ping" }).catch(()=>{}); setLatencyMs(Math.round(performance.now()-start));` on a 30s interval, only while status === "connected". Lift this into the header scope for a real LAT value; hide LAT when not connected / no measurement yet.

Fabricated literals to replace (src/layouts/DashboardLayout.tsx:~709-718): `SYS: <span className="text-primary font-bold">14%</span>` and `LAT: <span className="text-primary font-bold">12ms</span>`.

iconComponents (src/layouts/DashboardLayout.tsx:79-119): `const iconComponents: Record<string, React.ElementType> = { grid: LayoutDashboard, cpu: Cpu, … }`. Currently NOT exported. navItems is already exported near line 795.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dissolve CONSOLE cluster + export iconComponents</name>
  <files>src/layouts/DashboardLayout.tsx</files>
  <read_first>
    - src/layouts/DashboardLayout.tsx (read the navGroups array ~140-210, the iconComponents map ~79-119, and the existing `export { navItems }` near 795)
  </read_first>
  <action>Edit `navGroups`: (1) add a Forge item `{ to: "/forge", label: "Forge", icon: "flame", group: "COMMAND" }` to the COMMAND group's items; (2) change the `/executions` and `/build` items' `group` to "OBSERVE" and move them into the OBSERVE group's items array; (3) delete the entire CONSOLE group object (its `/live-run` entry is the duplicate — COMMAND already has `/live-run`, keep that one). Then add `export { iconComponents };` alongside the existing `export { navItems };`. Do not change Mission Control (Plan 04 owns it). Verify `navItems` (the derived deduped flat list) now contains `/forge`, `/executions`, `/build` exactly once and no CONSOLE group remains.</action>
  <verify>
    <automated>cd . && grep -c 'group: "CONSOLE"' src/layouts/DashboardLayout.tsx | grep -qx 0 && grep -q 'export { iconComponents' src/layouts/DashboardLayout.tsx && npx tsc --noEmit 2>&1 | grep -v '^$' | grep -qi "DashboardLayout" && echo "TSC-ERR" || echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'group: "CONSOLE"' src/layouts/DashboardLayout.tsx` returns 0
    - `grep -c '"/live-run"' src/layouts/DashboardLayout.tsx` returns 1 (duplicate removed)
    - `grep -q 'export { iconComponents' src/layouts/DashboardLayout.tsx` matches
    - The `/forge` item's group is "COMMAND"; `/executions` and `/build` items' group is "OBSERVE"
    - `npx tsc --noEmit` reports no new errors in DashboardLayout.tsx
  </acceptance_criteria>
  <done>CONSOLE cluster gone, Forge in COMMAND, Executions/Build in OBSERVE, Live Run deduped, iconComponents exported.</done>
</task>

<task type="auto">
  <name>Task 2: Wire real header telemetry (SYS real/hidden, LAT via WS ping) + test</name>
  <files>src/layouts/DashboardLayout.tsx, src/layouts/__tests__/DashboardLayout.test.tsx</files>
  <read_first>
    - src/layouts/DashboardLayout.tsx (the header telemetry block ~703-718)
    - src/components/ConnectionPopover.tsx (WS ping RTT pattern ~90-113 — the LAT source)
    - convex/systemResources.ts (confirm `current` return shape; no latency field)
    - src/layouts/__tests__/DashboardLayout.test.tsx (existing test to extend; see how it mocks useQuery / Convex)
  </read_first>
  <action>Replace the `SYS: 14%` literal with `useQuery(api.systemResources.current)` and render the SYS span ONLY when `resources?.cpu != null`, showing `SYS: {Math.round(resources.cpu)}%` (keep the existing `text-primary font-bold` treatment). Replace the `LAT: 12ms` literal by lifting the ConnectionPopover ping pattern: measure WS round-trip RTT via `sendCommand({type:"ping"})` on a 30s interval while connected, store in state, and render the LAT span ONLY when a measurement exists and the socket is connected — show `LAT: {rttMs}ms`. When SYS/LAT data is absent, render nothing for that span (no "—", no "N/A", no zero — honesty rule D-04). Extend `DashboardLayout.test.tsx` with: (a) SYS hidden when `systemResources.current` returns null; (b) SYS shows the rounded cpu when present; (c) LAT hidden when not connected / no measurement. Mock `useQuery` and the WS context per the existing test's mocking approach.</action>
  <verify>
    <automated>npx vitest run src/layouts/__tests__/DashboardLayout.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -E "14%|12ms" src/layouts/DashboardLayout.tsx` returns no matches (fabricated literals gone)
    - `grep -q "systemResources.current" src/layouts/DashboardLayout.tsx` matches
    - SYS span render is guarded by a `!= null` field check (not `?? 0`, not whole-object ternary)
    - DashboardLayout.test.tsx has a test asserting SYS is absent from the DOM when systemResources returns null, and present with the cpu value when populated
    - `npx vitest run src/layouts/__tests__/DashboardLayout.test.tsx` exits 0
  </acceptance_criteria>
  <done>Header shows real CPU and WS latency or nothing — never a fabricated number.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| client → Convex query | `systemResources.current` read (untrusted-to-client data rendered as telemetry) |
| client → Ástríðr WS | `sendCommand({type:"ping"})` round-trip for LAT measurement |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-96-02-01 | Repudiation (false telemetry) | Header SYS/LAT readouts | mitigate | Render only real values with a null-per-field guard; hide entirely when data absent — the fabricated-number class of bug is the thing being removed |
| T-96-02-02 | Information Disclosure | ping command | accept | `ping` carries no payload/PII; RTT is a local timing measurement, same as existing ConnectionPopover usage |
| T-96-02-SC | Tampering | package installs | accept | No packages installed this phase (96-RESEARCH.md: audit N/A) |
</threat_model>

<verification>
- `grep -c 'group: "CONSOLE"' src/layouts/DashboardLayout.tsx` == 0
- `npx vitest run src/layouts/__tests__/DashboardLayout.test.tsx` green
- `npx tsc --noEmit` clean
</verification>

<success_criteria>
CONSOLE dissolved per D-03; header telemetry real-or-hidden per D-04; iconComponents exported for Plan 05.
</success_criteria>

<output>
Create `.planning/phases/96-ui-deep-dive-cleanup-ia-restructure-command-palette-drift-fa/96-02-SUMMARY.md` when done.
</output>
