---
phase: 96-ui-deep-dive-cleanup-ia-restructure-command-palette-drift-fa
plan: 03
type: execute
wave: 2
depends_on: ["96-01"]
files_modified:
  - src/pages/Chat.tsx
  - src/pages/Inbox.tsx
  - src/components/ApprovalActions.tsx
  - src/components/BlockRenderer.tsx
  - src/components/InboxCard.tsx
  - src/pages/__tests__/Chat.test.tsx
  - src/pages/__tests__/Inbox.test.tsx
autonomous: true
requirements: [F6, D-11, F7]
must_haves:
  truths:
    - "Chat approve/reject sends the server-correct payload { type:'approval.respond', request_id_target, decision } — NOT the broken { requestId, approved } shape (F6)"
    - "Chat awaits the sendCommand ack and surfaces toast.error when ack.status !== 'ok' — no more unconditional success toast (F6 repudiation fix)"
    - "A single shared approval component/hook is consumed by BOTH Chat and Inbox (D-11), standardized on the async Promise<void> signature"
    - "Chat and Inbox page headers use the shared <PageHeader> and their anomalous max-h-[500px] caps are removed (F7)"
  artifacts:
    - path: "src/components/ApprovalActions.tsx"
      provides: "Shared approve/reject component used by Chat and Inbox"
      contains: "request_id_target"
    - path: "src/pages/__tests__/Chat.test.tsx"
      provides: "Regression test for the payload-shape + ack-error fix"
  key_links:
    - from: "src/pages/Chat.tsx"
      to: "Ástríðr WS approval.respond"
      via: "sendCommand({request_id_target, decision}) with ack check"
      pattern: "request_id_target"
    - from: "src/components/ApprovalActions.tsx"
      to: "both Chat and Inbox"
      via: "shared component import"
      pattern: "ApprovalActions"
---

<objective>
Fix the confirmed live bug: Chat.tsx sends a malformed `approval.respond` payload (`{requestId, approved}`) that the Ástríðr Pydantic `ApprovalRespondCommand` model rejects, and Chat shows a false success toast because it fires-and-forgets the command. Make Chat match Inbox's verified-correct shape (`{request_id_target, decision}` + ack check), then extract ONE shared approval component (D-11) consumed by both Chat and Inbox. While in these two files, migrate their headers to `<PageHeader>` and remove their `max-h-[500px]` caps (F7).

Purpose: This is the phase's one access-control (V4) fix — an agent tool call is blocked until a human approves, and today that gate is unreachable from Chat. RESEARCH already verified the server contract (no further cross-repo work needed before extraction).
Output: Correct Chat sender, shared `ApprovalActions`, ack-checked flow, regression tests, F7 headers.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/96-ui-deep-dive-cleanup-ia-restructure-command-palette-drift-fa/96-RESEARCH.md
@.planning/phases/96-ui-deep-dive-cleanup-ia-restructure-command-palette-drift-fa/96-PATTERNS.md
@.planning/phases/96-ui-deep-dive-cleanup-ia-restructure-command-palette-drift-fa/96-UI-SPEC.md

<interfaces>
Server ground truth (astridr-repo/astridr/api/ws_commands.py:95-100) — do NOT relax this, conform to it:
`ApprovalRespondCommand`: `type: "approval.respond"`, `request_id: str` (WS correlation id, auto-injected by sendCommand — do not set), `request_id_target: str` (the HITL UUID), `decision: "approve"|"reject"`, `comment: str|None`.

CORRECT client pattern already in Inbox.tsx:185-206 (copy this shape):
```typescript
const ack = await sendCommand({ type: "approval.respond", request_id_target: requestId, decision: "approve" });
if (ack.status !== "ok") { toast.error(ack.error ?? "Approval failed"); return; }
toast.success("Approval sent.");
```

WRONG code currently in Chat.tsx:303-311 (remove, do not preserve):
```typescript
const handleApprove = useCallback((requestId: string) => {
  void sendCommand({ type: "approval.respond", requestId, approved: true });
  toast.success("Approved — sent to Ástríðr");
}, [sendCommand]);
```

Consumer prop signatures to standardize on the async shape:
- src/components/BlockRenderer.tsx:33-34 — `onApprove?: (requestId: string) => void` (Chat's, sync — make async)
- src/components/InboxCard.tsx:48-49 — `onApprove?: (requestId: string) => Promise<void>` (Inbox's, already async — the target)

Chat.tsx header (src/pages/Chat.tsx:330): `text-base font-semibold` → migrate to <PageHeader>. Chat is in the max-h-[500px] group.
Inbox.tsx header (src/pages/Inbox.tsx:363): `text-xl font-semibold` → migrate to <PageHeader>. Inbox is in the max-h-[500px] group.

RE-VERIFY before building the shared component (RESEARCH "Valid until ~14 days"): confirm `ApprovalRespondCommand`'s fields still match by reading `C:\Users\mandr\astridr-repo\astridr\api\ws_commands.py` — if the model changed, conform to the new shape.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED test for Chat approval payload + ack handling</name>
  <files>src/pages/__tests__/Chat.test.tsx</files>
  <read_first>
    - src/pages/Chat.tsx (current broken handleApprove ~303-311; how sendCommand + AstridrWS context are consumed)
    - src/pages/Inbox.tsx (the correct pattern ~185-229)
    - src/pages/__tests__/Inbox.test.tsx (existing test — mirror how it mocks sendCommand / the WS context)
  </read_first>
  <behavior>
    - Test 1: clicking Approve calls sendCommand with an object containing `type:"approval.respond"`, `request_id_target: <the HITL id>`, `decision:"approve"` — and NOT keys `requestId` or `approved`.
    - Test 2: clicking Reject sends `decision:"reject"`.
    - Test 3: when the mocked sendCommand resolves `{ status: "error", error: "bad" }`, a toast.error is shown and NO success toast fires.
    - Test 4: when sendCommand resolves `{ status: "ok" }`, a success toast fires.
  </behavior>
  <action>Create Chat.test.tsx (no coverage exists today). Mock `sendCommand` as a vi.fn resolving a configurable ack; mock `toast` from sonner (vi.fn on success/error). Render Chat with an approval block/prop that exposes an Approve and Reject affordance, click them, and assert on the sendCommand argument shape and toast calls per the behaviors. This MUST fail against the current Chat.tsx (which sends `{requestId, approved}` and never checks the ack).</action>
  <verify>
    <automated>npx vitest run src/pages/__tests__/Chat.test.tsx 2>&1 | grep -qE "[0-9]+ failed" && echo RED-OK</automated>
  </verify>
  <acceptance_criteria>
    - src/pages/__tests__/Chat.test.tsx exists
    - Asserts sendCommand is called with `request_id_target` and `decision` and not with `requestId`/`approved`
    - Fails against current Chat.tsx (RED confirmed before Task 2)
  </acceptance_criteria>
  <done>Failing test encodes the correct approval contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extract shared ApprovalActions; fix Chat + rewire Inbox (GREEN)</name>
  <files>src/components/ApprovalActions.tsx, src/pages/Chat.tsx, src/pages/Inbox.tsx, src/components/BlockRenderer.tsx, src/components/InboxCard.tsx</files>
  <read_first>
    - src/pages/Inbox.tsx (correct handleApprove/handleReject ~185-229 — extraction source)
    - src/pages/Chat.tsx (broken sender + its BlockRenderer wiring)
    - src/components/BlockRenderer.tsx (onApprove/onReject prop types ~33-34)
    - src/components/InboxCard.tsx (onApprove prop type ~48-49)
    - C:\Users\mandr\astridr-repo\astridr\api\ws_commands.py (re-verify ApprovalRespondCommand shape before coding)
    - src/pages/__tests__/Chat.test.tsx (the contract to make green)
  </read_first>
  <action>Create `src/components/ApprovalActions.tsx` exporting a shared approve/reject affordance (component and/or a `useApprovalActions(sendCommand)` hook) whose handlers send `{ type:"approval.respond", request_id_target: requestId, decision:"approve"|"reject", comment? }`, await the ack, and on `ack.status !== "ok"` call `toast.error(ack.error ?? "Approval failed")` and return early, else `toast.success("Approval sent.")` — mirroring Inbox.tsx verbatim. Add a shared TS type mirroring the Pydantic model 1:1 (`request_id_target: string; decision: "approve"|"reject"; comment?: string`). Replace Chat.tsx's broken `handleApprove` (and its reject path) to consume the shared piece; make the handler `async` returning `Promise<void>`. Update `BlockRenderer.tsx` (and any ChatBubble prop) `onApprove`/`onReject` types to `(requestId: string) => Promise<void>` to match Inbox's async signature. Refactor Inbox.tsx to consume the same shared component/hook (behavior unchanged — it was already correct). Approve/reject copy follows the already-correct UI (do not invent new wording, per UI-SPEC).</action>
  <verify>
    <automated>npx vitest run src/pages/__tests__/Chat.test.tsx src/pages/__tests__/Inbox.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "request_id_target" src/components/ApprovalActions.tsx` matches
    - `grep -E "requestId, approved|approved: true" src/pages/Chat.tsx` returns no matches (broken shape gone)
    - `grep -q "ApprovalActions" src/pages/Chat.tsx` and `grep -q "ApprovalActions" src/pages/Inbox.tsx` both match (shared consumption)
    - Chat's approve handler awaits the ack and branches on status (no unconditional success toast)
    - `npx vitest run src/pages/__tests__/Chat.test.tsx src/pages/__tests__/Inbox.test.tsx` exits 0
    - `npx tsc --noEmit` clean for these files
  </acceptance_criteria>
  <done>Chat sends the correct, ack-checked payload via a shared component that Inbox also uses.</done>
</task>

<task type="auto">
  <name>Task 3: Migrate Chat + Inbox headers to PageHeader; remove max-h caps (F7)</name>
  <files>src/pages/Chat.tsx, src/pages/Inbox.tsx</files>
  <read_first>
    - src/components/PageHeader.tsx (the shared component from Plan 01)
    - src/pages/Chat.tsx (h1 at ~330; find its `max-h-[500px]` wrapper)
    - src/pages/Inbox.tsx (h1 at ~363; find its `max-h-[500px]` wrapper)
  </read_first>
  <action>In Chat.tsx and Inbox.tsx, replace the bespoke `<h1>` (Chat `text-base font-semibold`, Inbox `text-xl font-semibold`) with `<PageHeader title="Chat" />` / `<PageHeader title="Inbox" />` (preserve any existing right-aligned header controls by passing them through the `actions` prop). Remove the anomalous `max-h-[500px]` class from each page's wrapper div so the panels fill the full page height (F7). Import PageHeader from "@/components/PageHeader".</action>
  <verify>
    <automated>grep -L 'max-h-\[500px\]' src/pages/Chat.tsx src/pages/Inbox.tsx | wc -l | grep -qx 2 && grep -q 'PageHeader' src/pages/Chat.tsx && grep -q 'PageHeader' src/pages/Inbox.tsx && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'max-h-\[500px\]' src/pages/Chat.tsx` returns 0 and same for Inbox.tsx
    - Both files import and render `<PageHeader`
    - `npx vitest run src/pages/__tests__/Chat.test.tsx src/pages/__tests__/Inbox.test.tsx` still green
  </acceptance_criteria>
  <done>Chat and Inbox use the standard header and no longer cap panel height.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| client → Ástríðr WS (approval.respond) | Human-in-the-loop approval gate crosses here; server Pydantic-validates the command |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-96-03-01 | Repudiation (false confirmation) | Chat approval sender | mitigate | Fix payload to match server model AND await/branch on ack — the UI can no longer claim success when the server rejected (the confirmed live bug) |
| T-96-03-02 | Elevation of Privilege (V4 access control) | HITL approval gate | mitigate | Conform client to the server's strict `ApprovalRespondCommand`; do NOT loosen server validation from the client — fix the client to match (V5) |
| T-96-03-03 | Tampering | shared approval type | mitigate | Introduce a compile-time TS type mirroring the Pydantic model 1:1 so a future shape drift fails at build, not silently at runtime |
| T-96-03-SC | Tampering | package installs | accept | No packages installed this phase |
</threat_model>

<verification>
- `npx vitest run src/pages/__tests__/Chat.test.tsx src/pages/__tests__/Inbox.test.tsx` green
- `grep -E "requestId, approved" src/pages/Chat.tsx` empty
- `npx tsc --noEmit` clean
- Manual (per 96-VALIDATION Manual-Only): live Chat approval round-trip against running astridr confirms decision lands with no 300s timeout
</verification>

<success_criteria>
Chat approvals work server-side via the correct ack-checked payload; one shared approval component serves Chat and Inbox; Chat/Inbox headers standardized and uncapped.
</success_criteria>

<output>
Create `.planning/phases/96-ui-deep-dive-cleanup-ia-restructure-command-palette-drift-fa/96-03-SUMMARY.md` when done.
</output>
