---
phase: 96-ui-deep-dive-cleanup-ia-restructure-command-palette-drift-fa
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/PageHeader.tsx
  - src/components/__tests__/PageHeader.test.tsx
autonomous: true
requirements: [F7]
must_haves:
  truths:
    - "A shared <PageHeader> component exists and renders an h1 with exactly text-2xl font-bold text-foreground (F7 standard)"
    - "PageHeader accepts a title, an optional leading Lucide icon, and an optional right-aligned actions slot (needed by icon'd headers and the Tasks view toggle)"
    - "PageHeader.test.tsx asserts the typography contract and passes"
  artifacts:
    - path: "src/components/PageHeader.tsx"
      provides: "F7 shared page-title component"
      contains: "text-2xl font-bold text-foreground"
    - path: "src/components/__tests__/PageHeader.test.tsx"
      provides: "Typography contract regression test"
  key_links:
    - from: "src/components/PageHeader.tsx"
      to: "h1 class string"
      via: "className literal"
      pattern: "text-2xl font-bold text-foreground"
---

<objective>
Create the shared `<PageHeader>` component that 31+ pages migrate to in this phase (F7). This is the single typography standard the phase enforces. It must emit `text-2xl font-bold text-foreground` on its `<h1>` and support the two shapes existing headers use: an optional leading Lucide icon (KnowledgeGraph, ToolGalaxy, McpInventory, hr/*, GraphsHub, HivePage all render an icon beside the title) and an optional right-aligned actions slot (the merged Tasks board seats its "By Status"/"By Agent" segmented control here).

Purpose: 31 pages depend on this being correct before they migrate — it is a Wave 1 foundation with no dependencies.
Output: `src/components/PageHeader.tsx` + a green contract test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/96-ui-deep-dive-cleanup-ia-restructure-command-palette-drift-fa/96-UI-SPEC.md
@.planning/phases/96-ui-deep-dive-cleanup-ia-restructure-command-palette-drift-fa/96-PATTERNS.md
@CLAUDE.md

<interfaces>
Sibling component to mirror (src/components/SectionHeader.tsx, full file — the h2 counterpart of this exact pattern):

```typescript
interface SectionHeaderProps {
  title: string;
  action?: React.ReactNode;
}
export function SectionHeader({ title, action }: SectionHeaderProps) {
  return (
    <div className="space-y-2 mb-4">
      <div className="flex items-center justify-between">
        <h2 className="text-base font-semibold uppercase tracking-wide text-muted-foreground">{title}</h2>
        {action}
      </div>
      <Separator />
    </div>
  );
}
```

Gold-standard target typography already shipped (src/pages/BuildProgress.tsx:24, src/pages/Analytics.tsx:86):
`<h1 className="text-2xl font-bold text-foreground">…</h1>`

Lucide icons are imported as components (e.g. `import { Network } from "lucide-react"`); many pages render `<h1 className="... flex items-center gap-2"><Icon .../> Title</h1>`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write PageHeader contract test (RED)</name>
  <files>src/components/__tests__/PageHeader.test.tsx</files>
  <read_first>
    - src/components/PageHeader.tsx (does not exist yet — the test defines its contract)
    - src/components/__tests__/CommandPalette.test.tsx (existing test in this folder — mirror its render/import conventions, jsdom + @testing-library/react)
    - src/components/SectionHeader.tsx (the sibling prop shape)
  </read_first>
  <behavior>
    - Test 1: renders an h1 whose className contains "text-2xl", "font-bold", and "text-foreground" when given title="Build Progress".
    - Test 2: renders the provided title text content ("Build Progress").
    - Test 3: when passed an `actions` node (e.g. a button with text "By Agent"), that node is rendered.
    - Test 4: when passed an `icon` (a Lucide component), an svg is rendered inside the header (getByRole/container query for svg).
  </behavior>
  <action>Create the test file using `@testing-library/react` render + screen queries, importing `PageHeader` from "../PageHeader". Assert on the h1 element's className string (query via `container.querySelector("h1")`) that it includes the three required tokens `text-2xl`, `font-bold`, `text-foreground`. Assert title text, the actions node, and an svg presence for the icon prop. Do not assert on any other classes so the component keeps layout freedom. This test MUST fail initially (module does not exist).</action>
  <verify>
    <automated>npx vitest run src/components/__tests__/PageHeader.test.tsx 2>&1 | grep -qE "Cannot find module|Failed to resolve import|failed" && echo RED-OK</automated>
  </verify>
  <acceptance_criteria>
    - src/components/__tests__/PageHeader.test.tsx exists and imports PageHeader from "../PageHeader"
    - Running the test before Task 2 fails (module missing) — RED confirmed
    - Test asserts the h1 className contains all of: text-2xl, font-bold, text-foreground
  </acceptance_criteria>
  <done>The contract test exists and fails because PageHeader is not yet implemented.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement PageHeader component (GREEN)</name>
  <files>src/components/PageHeader.tsx</files>
  <read_first>
    - src/components/SectionHeader.tsx (prop-shape analog)
    - src/pages/BuildProgress.tsx (compliant h1 usage at line 24)
    - src/components/__tests__/PageHeader.test.tsx (the contract to satisfy — written in Task 1)
  </read_first>
  <action>Create `src/components/PageHeader.tsx` exporting a named `PageHeader` function component with props `{ title: React.ReactNode; icon?: LucideIcon; actions?: React.ReactNode; className?: string }` (type the icon as `import("lucide-react").LucideIcon`). Render a wrapper div (flex row, items-center, justify-between, sensible bottom margin such as `mb-4`) containing: a left group with the optional icon (`gap-2` flex, icon sized `h-6 w-6` or similar) and an `<h1 className="text-2xl font-bold text-foreground">` wrapping `title`; and the optional `actions` node on the right. Merge any passed `className` onto the outer wrapper. Do NOT reintroduce any `max-h-[...]` cap (F7/UI-SPEC prohibition). Keep it presentational — no data fetching.</action>
  <verify>
    <automated>npx vitest run src/components/__tests__/PageHeader.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - src/components/PageHeader.tsx exports `PageHeader`
    - `grep -n "text-2xl font-bold text-foreground" src/components/PageHeader.tsx` returns a match on the h1
    - `npx vitest run src/components/__tests__/PageHeader.test.tsx` exits 0 (all tests green)
    - `npx tsc --noEmit` reports no new errors for this file
  </acceptance_criteria>
  <done>PageHeader renders the F7 standard h1 with optional icon and actions slot; contract test green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| (none new) | Pure presentational component; renders props, no input parsing, no data fetch, no network |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-96-01-01 | Tampering | PageHeader title/actions as React nodes | accept | Consumers pass trusted static strings/components; React escapes text children by default — no dangerouslySetInnerHTML introduced |
| T-96-01-SC | Tampering | package installs | accept | No packages installed this phase (96-RESEARCH.md: Package Legitimacy Audit N/A) |
</threat_model>

<verification>
- `npx vitest run src/components/__tests__/PageHeader.test.tsx` green
- `npx tsc --noEmit` clean
</verification>

<success_criteria>
PageHeader component exists, enforces `text-2xl font-bold text-foreground`, supports icon + actions, and its contract test passes.
</success_criteria>

<output>
Create `.planning/phases/96-ui-deep-dive-cleanup-ia-restructure-command-palette-drift-fa/96-01-SUMMARY.md` when done.
</output>
