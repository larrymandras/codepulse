---
phase: 96-ui-deep-dive-cleanup-ia-restructure-command-palette-drift-fa
plan: 12
type: execute
wave: 2
depends_on: ["96-01"]
files_modified:
  - src/pages/Executions.tsx
  - src/pages/Ideation.tsx
  - src/pages/ConfigPage.tsx
  - src/pages/InsightsChat.tsx
  - src/pages/LiveRun.tsx
  - src/pages/WhatsApp.tsx
  - src/pages/HivePage.tsx
  - src/pages/GraphsHub.tsx
  - src/pages/McpInventory.tsx
  - src/pages/Quality.tsx
  - src/pages/QualityDetail.tsx
  - src/pages/SessionDetail.tsx
autonomous: true
requirements: [F7]
must_haves:
  truths:
    - "Executions, Ideation, ConfigPage, InsightsChat, LiveRun, WhatsApp, HivePage, GraphsHub, McpInventory, Quality, QualityDetail render their title via <PageHeader> (F7)"
    - "LiveRun's anomalous max-h-[500px] cap is removed (F7)"
    - "SessionDetail gains a <PageHeader> if it is a top-level route view (F7); otherwise it is left as a sub-panel and noted"
    - "No migrated page keeps a bespoke h1 (terminal micro-headers on Hive/GraphsHub and the font-semibold/text-xl/text-lg/text-base variants are all replaced)"
  artifacts:
    - path: "src/pages/LiveRun.tsx"
      provides: "Standard header + no panel cap"
      contains: "PageHeader"
  key_links:
    - from: "these pages"
      to: "src/components/PageHeader.tsx"
      via: "import + render"
      pattern: "PageHeader"
---

<objective>
F7 header sweep, batch B: migrate the remaining leftover pages to `<PageHeader>` — Executions, Ideation, ConfigPage, InsightsChat, LiveRun, WhatsApp, HivePage, GraphsHub, McpInventory, Quality, QualityDetail, and SessionDetail. Also remove LiveRun's anomalous `max-h-[500px]` cap (it is in the Chat/LiveRun/Inbox/Tasks group; Chat/Inbox/Tasks are handled in other plans, LiveRun here). These pages are owned by no other plan.

Purpose: The other half of the 35-page header standardization. Depends only on Plan 01 (PageHeader).
Output: Remaining leftover pages on the standard header; LiveRun uncapped.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/96-ui-deep-dive-cleanup-ia-restructure-command-palette-drift-fa/96-PATTERNS.md
@.planning/phases/96-ui-deep-dive-cleanup-ia-restructure-command-palette-drift-fa/96-UI-SPEC.md

<interfaces>
PageHeader (Plan 01): `<PageHeader title icon? actions? />` emits `<h1 className="text-2xl font-bold text-foreground">`.

Current headers to replace (audited tree):
- Executions.tsx: `text-2xl font-semibold text-foreground` (semibold → bold).
- Ideation.tsx: `text-2xl font-semibold`.
- ConfigPage.tsx: `text-lg font-semibold text-(--foreground)`.
- InsightsChat.tsx: `text-base font-semibold text-foreground`.
- LiveRun.tsx: `text-xl font-semibold text-(--foreground)` AND an anomalous `max-h-[500px]` wrapper — remove the cap too.
- WhatsApp.tsx: `text-xl font-semibold`.
- HivePage.tsx: terminal micro-header `text-xs font-mono uppercase tracking-widest text-primary flex items-center gap-2` (has a leading icon → pass to PageHeader icon prop).
- GraphsHub.tsx: terminal micro-header `text-xs font-mono uppercase tracking-widest font-bold text-primary flex items-center gap-2` (leading icon → icon prop).
- McpInventory.tsx: `text-2xl font-bold flex items-center gap-2` (missing text-foreground; leading icon → icon prop).
- Quality.tsx, QualityDetail.tsx: `text-2xl font-bold` (missing text-foreground).
- SessionDetail.tsx: NO h1 — if it is a standalone route view, add `<PageHeader title="Session Detail" />`; if it is only ever rendered as a sub-panel/modal inside another page, leave it and note that in the SUMMARY (do not force a duplicate header).

Preserve each page's existing title text and icon; pass any header-right controls via `actions`. Do NOT reintroduce `max-h-[...]` caps.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate 6 headers (Executions, Ideation, ConfigPage, InsightsChat, LiveRun, WhatsApp) + LiveRun uncap</name>
  <files>src/pages/Executions.tsx, src/pages/Ideation.tsx, src/pages/ConfigPage.tsx, src/pages/InsightsChat.tsx, src/pages/LiveRun.tsx, src/pages/WhatsApp.tsx</files>
  <read_first>
    - src/pages/Executions.tsx, src/pages/Ideation.tsx, src/pages/ConfigPage.tsx, src/pages/InsightsChat.tsx, src/pages/WhatsApp.tsx (each header block)
    - src/pages/LiveRun.tsx (header ~206 AND the max-h-[500px] wrapper)
    - src/components/PageHeader.tsx (Plan 01 API)
  </read_first>
  <action>In each page, replace the bespoke `<h1>` with `<PageHeader title="<existing title>" actions={<existing right controls, if any>} />`, keeping the title text. In LiveRun.tsx additionally remove the `max-h-[500px]` class from its wrapper so the panel fills the page. Import PageHeader from "@/components/PageHeader" in each.</action>
  <verify>
    <automated>for f in Executions Ideation ConfigPage InsightsChat LiveRun WhatsApp; do grep -q 'PageHeader' "src/pages/$f.tsx" || { echo "MISSING $f"; exit 1; }; done && grep -c 'max-h-\[500px\]' src/pages/LiveRun.tsx | grep -qx 0 && npx tsc --noEmit && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - All 6 files import and render `<PageHeader`
    - `grep -c 'max-h-\[500px\]' src/pages/LiveRun.tsx` == 0
    - `npx tsc --noEmit` clean; existing tests for these pages stay green
  </acceptance_criteria>
  <done>Six pages migrated; LiveRun uncapped.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate 6 headers (HivePage, GraphsHub, McpInventory, Quality, QualityDetail, SessionDetail)</name>
  <files>src/pages/HivePage.tsx, src/pages/GraphsHub.tsx, src/pages/McpInventory.tsx, src/pages/Quality.tsx, src/pages/QualityDetail.tsx, src/pages/SessionDetail.tsx</files>
  <read_first>
    - src/pages/HivePage.tsx, src/pages/GraphsHub.tsx (terminal micro-headers with leading icons)
    - src/pages/McpInventory.tsx (text-2xl with icon)
    - src/pages/Quality.tsx, src/pages/QualityDetail.tsx (text-2xl font-bold, missing text-foreground)
    - src/pages/SessionDetail.tsx (no h1 — determine if standalone route)
    - src/App.tsx (check whether SessionDetail is a routed page or a sub-panel)
    - src/components/PageHeader.tsx (Plan 01 API)
  </read_first>
  <action>Replace the bespoke h1 in HivePage/GraphsHub with `<PageHeader title="<existing title>" icon={<existing Lucide icon>} />` (drop the terminal mono/uppercase styling, keep the icon). Replace McpInventory's h1 with `<PageHeader title="MCP Inventory" icon={<existing icon>} />`. Replace Quality/QualityDetail h1s with `<PageHeader title="…" />`. For SessionDetail: check App.tsx — if it is a standalone routed view, add `<PageHeader title="Session Detail" />`; if it only renders as a sub-panel, leave it unchanged and record the decision in the SUMMARY. Import PageHeader where added.</action>
  <verify>
    <automated>for f in HivePage GraphsHub McpInventory Quality QualityDetail; do grep -q 'PageHeader' "src/pages/$f.tsx" || { echo "MISSING $f"; exit 1; }; done && npx tsc --noEmit && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - HivePage, GraphsHub, McpInventory, Quality, QualityDetail each import and render `<PageHeader`
    - Terminal micro-header classes (`text-xs font-mono uppercase tracking-widest`) no longer style the h1 title on Hive/GraphsHub
    - SessionDetail either renders `<PageHeader` (if routed) or the SUMMARY records why it was left as a sub-panel
    - `npx tsc --noEmit` clean; existing tests stay green
  </acceptance_criteria>
  <done>Remaining leftover pages migrated to the standard header.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| (none new) | Header-only presentational migration; no data/network change |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-96-12-01 | Tampering | divergent header styling | mitigate | Single shared component removes per-page header drift |
| T-96-12-SC | Tampering | package installs | accept | No packages installed this phase |
</threat_model>

<verification>
- Each migrated page imports and renders `<PageHeader`
- `grep -c 'max-h-\[500px\]' src/pages/LiveRun.tsx` == 0
- `npx tsc --noEmit` clean; full `npm test` green after the wave
- Phase gate: `grep -rn 'text-2xl' src/pages | grep -v 'text-foreground'` shows no bespoke page-title h1 outside PageHeader
</verification>

<success_criteria>
The remaining ~12 leftover pages migrated to the standard PageHeader; LiveRun uncapped; the 35-page F7 migration is complete across all plans.
</success_criteria>

<output>
Create `.planning/phases/96-ui-deep-dive-cleanup-ia-restructure-command-palette-drift-fa/96-12-SUMMARY.md` when done.
</output>
