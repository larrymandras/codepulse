---
phase: 96-ui-deep-dive-cleanup-ia-restructure-command-palette-drift-fa
plan: 05
type: execute
wave: 2
depends_on: ["96-02"]
files_modified:
  - src/components/CommandPalette.tsx
  - src/components/HeroStatsBar.tsx
  - src/components/__tests__/CommandPalette.test.tsx
autonomous: true
requirements: [F2]
must_haves:
  truths:
    - "CommandPalette lists pages by importing navItems from DashboardLayout, not the hardcoded NAV_PAGES (F2) — all ~15 previously-unreachable routes (/forge, /hive, /skills, /quality, /doc-comments, /channels/whatsapp, /graphs, /tool-galaxy, /mcp-inventory, /knowledge-graph, /hr/*) are now ⌘K-reachable"
    - "CommandPalette resolves each item's string icon key via the imported iconComponents map, falling back to LayoutDashboard (Pitfall 1)"
    - "Stale deep links to redirected routes are fixed: CommandPalette /agents and /profiles → /hr/roster; HeroStatsBar /agents → /hr/roster (F2)"
  artifacts:
    - path: "src/components/CommandPalette.tsx"
      provides: "Nav list sourced from the single navItems registry"
      contains: "navItems"
  key_links:
    - from: "src/components/CommandPalette.tsx"
      to: "src/layouts/DashboardLayout.tsx navItems + iconComponents"
      via: "import"
      pattern: "iconComponents"
---

<objective>
Kill the drift: CommandPalette maintains its own hardcoded `NAV_PAGES` list that has fallen ~15 routes behind the sidebar and still links to redirected routes. Replace it with the single-source `navItems` (+ `iconComponents`) export that DashboardLayout exposes (Plan 02 added the `iconComponents` export), and fix the stale `/agents`/`/profiles` deep links here and in HeroStatsBar.

Purpose: Makes every real route reachable via ⌘K and removes a whole class of manual-sync bugs. Depends on Plan 02's `iconComponents` export.
Output: Registry-sourced palette, fixed deep links, extended palette test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/96-ui-deep-dive-cleanup-ia-restructure-command-palette-drift-fa/96-PATTERNS.md
@.planning/phases/96-ui-deep-dive-cleanup-ia-restructure-command-palette-drift-fa/96-RESEARCH.md

<interfaces>
Import target (from Plan 02): `import { navItems, iconComponents } from "../layouts/DashboardLayout";`
- `navItems`: `{ to, label, icon, group }[]` — icon is a STRING KEY (e.g. "flame", "kanban"), not a component.
- `iconComponents`: `Record<string, React.ElementType>` — resolve via `iconComponents[item.icon] ?? LayoutDashboard` (mirror DashboardLayout's own fallback).

Pitfall 1 (RESEARCH): a naive swap that treats `item.icon` as a component renders no icon. Must look up the string key in iconComponents.

Current drift to delete (src/components/CommandPalette.tsx:53-80): hardcoded `NAV_PAGES` with component `Icon` fields, missing ~15 routes, includes stale /agents & /profiles. Render site at ~126-134 maps NAV_PAGES → CommandItem.
Stale deep link inside CommandPalette Agents group (~140): `navigate("/agents")` → change to `/hr/roster`.
HeroStatsBar stale link (src/components/HeroStatsBar.tsx:54): `navigate("/agents")` → `/hr/roster` (matches App.tsx redirect target).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Source CommandPalette pages from navItems + iconComponents; fix stale links</name>
  <files>src/components/CommandPalette.tsx, src/components/HeroStatsBar.tsx</files>
  <read_first>
    - src/components/CommandPalette.tsx (NAV_PAGES block ~53-80, render map ~126-134, Agents-group link ~140)
    - src/layouts/DashboardLayout.tsx (confirm `export { navItems, iconComponents }` from Plan 02; the string-keyed icon map)
    - src/components/HeroStatsBar.tsx (stale /agents link ~54)
  </read_first>
  <action>In CommandPalette.tsx: delete the hardcoded NAV_PAGES array; `import { navItems, iconComponents } from "../layouts/DashboardLayout"` and also keep `LayoutDashboard` imported from lucide-react for the fallback. Update the Pages CommandGroup render to map over `navItems`, resolving the icon as `const Icon = iconComponents[item.icon] ?? LayoutDashboard;` then `<Icon className="mr-2 h-4 w-4" />` + `item.label`, navigating to `item.to`. Fix the stale `navigate("/agents")` in the Agents group to `navigate("/hr/roster")` and any `/profiles` link likewise. In HeroStatsBar.tsx change `navigate("/agents")` (line 54) to `navigate("/hr/roster")`.</action>
  <verify>
    <automated>grep -q 'navItems' src/components/CommandPalette.tsx && grep -q 'iconComponents' src/components/CommandPalette.tsx && ! grep -q 'NAV_PAGES' src/components/CommandPalette.tsx && ! grep -q '"/agents"' src/components/HeroStatsBar.tsx && npx tsc --noEmit && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'NAV_PAGES' src/components/CommandPalette.tsx` == 0
    - CommandPalette imports and maps `navItems`, resolving icons via `iconComponents[item.icon] ?? LayoutDashboard`
    - `grep -c '"/agents"' src/components/CommandPalette.tsx` == 0 and `grep -c '"/agents"' src/components/HeroStatsBar.tsx` == 0
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Palette pages come from the registry with resolved icons; no stale entity links remain.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend CommandPalette test — coverage + no stale links</name>
  <files>src/components/__tests__/CommandPalette.test.tsx</files>
  <read_first>
    - src/components/__tests__/CommandPalette.test.tsx (existing test — extend it)
    - src/components/CommandPalette.tsx (post-Task-1 state)
  </read_first>
  <behavior>
    - Test A: previously-missing routes render in the palette — assert at least "/forge", "/graphs", and one "/hr/" route are present (by label or link).
    - Test B: no palette item navigates to "/agents" or "/profiles" (stale routes are gone).
    - Test C: each rendered nav item shows an icon (svg present), proving the string-key → component resolution works.
  </behavior>
  <action>Extend the existing CommandPalette test to render the palette (open state) with the real `navItems`/`iconComponents` (or a representative mock matching their shapes) and assert the three behaviors above. Guard against Pitfall 1 with the icon-presence assertion.</action>
  <verify>
    <automated>npx vitest run src/components/__tests__/CommandPalette.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Test asserts /forge (and other formerly-missing routes) appear
    - Test asserts no item links to /agents or /profiles
    - Test asserts icon svg presence per item
    - `npx vitest run src/components/__tests__/CommandPalette.test.tsx` exits 0
  </acceptance_criteria>
  <done>Palette coverage + no-stale-link + icon-resolution regression-guarded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| (none new) | Client-side nav wiring; no data/network boundary changed |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-96-05-01 | Tampering | dual nav lists drifting | mitigate | Single source of truth (navItems) removes the manual-sync surface where drift/dead-links accrue |
| T-96-05-SC | Tampering | package installs | accept | No packages installed this phase |
</threat_model>

<verification>
- `npx vitest run src/components/__tests__/CommandPalette.test.tsx` green
- `npx tsc --noEmit` clean
</verification>

<success_criteria>
CommandPalette is registry-sourced with resolved icons; stale /agents and /profiles links removed here and in HeroStatsBar.
</success_criteria>

<output>
Create `.planning/phases/96-ui-deep-dive-cleanup-ia-restructure-command-palette-drift-fa/96-05-SUMMARY.md` when done.
</output>
