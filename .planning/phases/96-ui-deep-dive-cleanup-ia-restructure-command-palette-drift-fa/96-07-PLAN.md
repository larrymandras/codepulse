---
phase: 96-ui-deep-dive-cleanup-ia-restructure-command-palette-drift-fa
plan: 07
type: execute
wave: 2
depends_on: ["96-01"]
files_modified:
  - src/components/FactsTable.tsx
  - src/components/__tests__/FactsTable.test.tsx
  - src/pages/Memory.tsx
  - src/pages/Dreaming.tsx
autonomous: true
requirements: [D-09, F9, D-10, F7]
must_haves:
  truths:
    - "A shared <FactsTable> component renders the facts table + search/category filter, consumed by BOTH Memory ('Durable Facts') and Dreaming ('Facts') (D-09) — the duplicated block is gone from both pages"
    - "Dreaming dead UI is removed: the disabled 'Start Backfill' stub and the unused AnimatedNumber import (F9/D-10)"
    - "Memory and Dreaming headers use <PageHeader>; each page keeps its own existing empty/error copy (F7, UI-SPEC)"
  artifacts:
    - path: "src/components/FactsTable.tsx"
      provides: "Shared facts table for Memory + Dreaming"
      contains: "TableHeader"
    - path: "src/components/__tests__/FactsTable.test.tsx"
      provides: "Render/filter contract test"
  key_links:
    - from: "src/pages/Memory.tsx and src/pages/Dreaming.tsx"
      to: "src/components/FactsTable.tsx"
      via: "shared component import"
      pattern: "FactsTable"
---

<objective>
Extract the near-identical "facts table" block duplicated between Memory's "Durable Facts" tab and Dreaming's "Facts" tab into one shared `<FactsTable>` (D-09), then remove Dreaming's dead UI (disabled "Start Backfill" stub, unused `AnimatedNumber` import — F9/D-10). Migrate both page headers to `<PageHeader>` (F7). Each page keeps its own query + filter state and empty/error copy — only the presentational table is shared.

Purpose: Collapses accidental duplication; both surfaces stay per D-09.
Output: Shared FactsTable + test, both pages consuming it, Dreaming dead-code removed, standard headers.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/96-ui-deep-dive-cleanup-ia-restructure-command-palette-drift-fa/96-PATTERNS.md
@.planning/phases/96-ui-deep-dive-cleanup-ia-restructure-command-palette-drift-fa/96-UI-SPEC.md

<interfaces>
Duplicated block to extract (src/pages/Dreaming.tsx:163-235 and src/pages/Memory.tsx:705-780 — functionally identical). Both compute filtered facts + categories locally from `useQuery(api.dreaming.recentFacts, { limit: 100 })`.

FactsTable props (keep query/filter in each page; pass computed data in):
`{ facts: Fact[] | undefined; search: string; onSearchChange: (v: string) => void; category: string; onCategoryChange: (v: string) => void; categories: string[]; sectionName: string }`

Table columns (verbatim from the analog): Fact (factText), Category (StatusBadge, uppercased when present), Confidence (right, `${(confidence*100).toFixed(0)}%` or dash), Created (timestamp). Search Input + optional category select above; empty-state and no-match copy already IDENTICAL in both pages — reuse the existing copy, do not invent new wording (UI-SPEC).

Dreaming dead UI to remove: "Start Backfill" disabled stub (src/pages/Dreaming.tsx:345-352) and unused `AnimatedNumber` import (src/pages/Dreaming.tsx:20).

Headers (F7): Memory h1 `text-2xl font-bold` (missing text-foreground) → PageHeader; Dreaming h1 `text-xl font-semibold` → PageHeader.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build shared FactsTable + contract test</name>
  <files>src/components/FactsTable.tsx, src/components/__tests__/FactsTable.test.tsx</files>
  <read_first>
    - src/pages/Dreaming.tsx (facts block ~163-235 — extraction source)
    - src/pages/Memory.tsx (facts block ~705-780 — the duplicate)
    - src/components/__tests__/PageHeader.test.tsx (test conventions in this folder)
  </read_first>
  <behavior>
    - Test 1: given a facts array, renders one row per fact with the fact text and a formatted confidence (e.g. "80%").
    - Test 2: given facts=[], renders the empty-state copy (existing wording passed in).
    - Test 3: typing in the search input calls onSearchChange.
    - Test 4: the category select (when categories provided) calls onCategoryChange.
  </behavior>
  <action>Write FactsTable.test.tsx first (RED), then implement src/components/FactsTable.tsx as a presentational component with the props listed in the interfaces block, extracting the table/filter markup verbatim from the Dreaming analog (Input + optional category select + Table with Fact/Category/Confidence/Created columns, wrapped in SectionErrorBoundary using sectionName). Keep the exact existing empty/no-match copy. No data fetching inside the component.</action>
  <verify>
    <automated>npx vitest run src/components/__tests__/FactsTable.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - src/components/FactsTable.tsx exports FactsTable and contains a Table/TableHeader
    - Component does not call useQuery (presentational only) — `grep -c 'useQuery' src/components/FactsTable.tsx` == 0
    - `npx vitest run src/components/__tests__/FactsTable.test.tsx` exits 0
  </acceptance_criteria>
  <done>Shared FactsTable renders facts + filters with the existing copy; test green.</done>
</task>

<task type="auto">
  <name>Task 2: Consume FactsTable in Memory + Dreaming; remove Dreaming dead UI; headers</name>
  <files>src/pages/Memory.tsx, src/pages/Dreaming.tsx</files>
  <read_first>
    - src/pages/Memory.tsx (Durable Facts block ~705-780; header)
    - src/pages/Dreaming.tsx (Facts block ~163-235; Start Backfill stub ~345-352; AnimatedNumber import ~20; header)
    - src/components/FactsTable.tsx (the new component)
    - src/components/PageHeader.tsx (Plan 01)
  </read_first>
  <action>In both pages, replace the inline facts table/filter markup with a FactsTable element passing facts={filteredFacts}, search/onSearchChange, category/onCategoryChange, categories={allCategories}, and sectionName ("Durable Facts" for Memory, "Dreaming Facts" for Dreaming), keeping each page's existing useQuery(api.dreaming.recentFacts,{limit:100}) + filter state + computed filteredFacts/allCategories. In Dreaming.tsx also delete the disabled "Start Backfill" stub (:345-352) and remove the unused AnimatedNumber import (:20). Replace each page's h1 with PageHeader title="Memory" / title="Dreaming" (preserve any right-side controls via actions).</action>
  <verify>
    <automated>npx vitest run src/components/__tests__/FactsTable.test.tsx && npx tsc --noEmit && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'FactsTable' src/pages/Memory.tsx` and `grep -q 'FactsTable' src/pages/Dreaming.tsx`
    - `grep -c 'AnimatedNumber' src/pages/Dreaming.tsx` == 0
    - `grep -ci 'Start Backfill' src/pages/Dreaming.tsx` == 0
    - Both pages import and render `<PageHeader`
    - `npx tsc --noEmit` exits 0 (no dangling imports after dead-code removal)
  </acceptance_criteria>
  <done>Both pages share FactsTable; Dreaming dead UI gone; headers standardized.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| (none new) | Presentational extraction over an unchanged read query (api.dreaming.recentFacts) |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-96-07-01 | Tampering | duplicated facts markup drifting | mitigate | Single shared component removes the divergence surface between Memory and Dreaming |
| T-96-07-SC | Tampering | package installs | accept | No packages installed this phase |
</threat_model>

<verification>
- `npx vitest run src/components/__tests__/FactsTable.test.tsx` green
- `npx tsc --noEmit` clean (dead-import removal verified)
</verification>

<success_criteria>
FactsTable shared by Memory + Dreaming; Dreaming dead UI removed; both headers standardized.
</success_criteria>

<output>
Create `.planning/phases/96-ui-deep-dive-cleanup-ia-restructure-command-palette-drift-fa/96-07-SUMMARY.md` when done.
</output>
