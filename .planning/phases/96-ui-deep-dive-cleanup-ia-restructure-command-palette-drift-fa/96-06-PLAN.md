---
phase: 96-ui-deep-dive-cleanup-ia-restructure-command-palette-drift-fa
plan: 06
type: execute
wave: 2
depends_on: ["96-01"]
files_modified:
  - src/pages/Security.tsx
  - src/pages/Automation.tsx
  - src/pages/Infrastructure.tsx
  - src/pages/__tests__/Security.test.tsx
  - src/pages/__tests__/Automation.test.tsx
autonomous: true
requirements: [F4, D-05, D-06, D-07, F7]
must_haves:
  truths:
    - "Security page shows NO hardcoded 'Valid' Audit Chain integrity badge; the entry count is relabeled honestly as loaded events (D-05)"
    - "Automation shows the computed CRON_SCHEDULES.length as 'Configured Schedules' — no `totalJobs ?? 12` fallback, no fake `enabled: true` live indicators (D-06)"
    - "Both empty Provider Allowlist / Network Policy placeholders are removed: the Security allowlist sub-block AND the Infrastructure Network Policy section — while Security's live Network Access Log is preserved (D-07)"
    - "Security, Automation, and Infrastructure headers use <PageHeader> (F7)"
  artifacts:
    - path: "src/pages/Security.tsx"
      provides: "Honest audit label; allowlist placeholder removed; access log kept"
      contains: "PageHeader"
    - path: "src/pages/Automation.tsx"
      provides: "Computed schedule count, honest cron labeling"
      contains: "CRON_SCHEDULES.length"
  key_links:
    - from: "src/pages/Automation.tsx"
      to: "CRON_SCHEDULES"
      via: "computed length"
      pattern: "CRON_SCHEDULES.length"
---

<objective>
Enforce the honesty-first rule across the three pages that fabricate trust signals (F4): remove Security's hardcoded "Valid" audit badge and the empty Provider Allowlist placeholder (D-05/D-07), replace Automation's `totalJobs ?? 12` magic-number fallback and fake `enabled: true` cron indicators with the computed catalog count honestly labeled (D-06), and remove Infrastructure's empty Network Policy placeholder (D-07). Migrate all three headers to `<PageHeader>` (F7). Preserve Security's LIVE Network Access Log — only the placeholder allowlist sub-block goes.

Purpose: Removes every fabricated/hardcoded value on these pages; regression-guards them so the fake values can't return.
Output: Three honest pages, two regression tests, standard headers.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/96-ui-deep-dive-cleanup-ia-restructure-command-palette-drift-fa/96-PATTERNS.md
@.planning/phases/96-ui-deep-dive-cleanup-ia-restructure-command-palette-drift-fa/96-RESEARCH.md
@.planning/phases/96-ui-deep-dive-cleanup-ia-restructure-command-palette-drift-fa/96-UI-SPEC.md

<interfaces>
D-05 Security Audit Chain block (src/pages/Security.tsx:215-230): remove the "Chain integrity: Valid" row entirely; relabel "Entry count" → "{n} events loaded" honest proxy (UI-SPEC Copywriting). Keep `mergedEvents.length` as the number, only the LABEL changes.

D-07 Security Network Policy tab (src/pages/Security.tsx:422-481): remove ONLY the first sub-div — the Provider Allowlist placeholder (:425-444). KEEP the second sub-div — the Network Access Log (:446-479), which renders real `networkPolicyEvents`. Do NOT delete the whole TabsContent (RESEARCH Anti-Pattern 3).

D-06 Automation (src/pages/Automation.tsx): line 89 `<MetricCard label="Cron Jobs" value={summary?.totalJobs ?? 12} />` → `<MetricCard label="Configured Schedules" value={CRON_SCHEDULES.length} />` (CRON_SCHEDULES already imported at :15, 12 entries today). Also in `schedulesToCronJobs()` (:34-40) drop the hardcoded `enabled: true` live indicator and present as configured (not live-enabled) — the honest static catalog until Ástríðr emits real cron state (Deferred).

D-07 Infrastructure (src/pages/Infrastructure.tsx:255-266): remove the ENTIRE `<div className="md:col-span-12">…Network Policy placeholder…</div>` — no live sibling to preserve here (unlike Security).

Headers (F7): Security h1 `text-2xl font-bold` (missing text-foreground) → PageHeader; Automation h1 `text-2xl font-bold` → PageHeader; Infrastructure h1 `text-2xl font-bold` → PageHeader.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED regression tests for F4 honesty (Security + Automation)</name>
  <files>src/pages/__tests__/Security.test.tsx, src/pages/__tests__/Automation.test.tsx</files>
  <read_first>
    - src/pages/Security.tsx (Audit Chain block ~215-230; Network Policy tab ~422-481)
    - src/pages/Automation.tsx (MetricCard ~89; schedulesToCronJobs ~34-40)
    - src/pages/__tests__/Inbox.test.tsx (Convex mocking conventions)
  </read_first>
  <behavior>
    - Security Test 1: the rendered page contains no "Valid" chain-integrity text and no "Chain integrity" row.
    - Security Test 2: the audit entry count is labeled as loaded events (e.g. matches /events loaded/), not "Entry count" as an integrity proxy.
    - Security Test 3: no "Provider Allowlist" placeholder text; the "Network Access Log" section still renders.
    - Automation Test 1: the schedules metric shows the computed CRON_SCHEDULES.length (mock the list to a known length N and assert N is shown), and the literal fallback 12 is not used when the query returns undefined.
    - Automation Test 2: no fake `enabled` live badge is shown for the static catalog.
  </behavior>
  <action>Create both test files. Mock Convex `useQuery` for the security/automation queries (return controllable values including undefined for the cron summary to prove no `?? 12`), mock CRON_SCHEDULES length if needed. Assert the behaviors above. These MUST fail against current code (which shows "Valid", "Entry count", and the `?? 12` fallback).</action>
  <verify>
    <automated>npx vitest run src/pages/__tests__/Security.test.tsx src/pages/__tests__/Automation.test.tsx 2>&1 | grep -qi fail && echo RED-OK</automated>
  </verify>
  <acceptance_criteria>
    - Both test files exist and assert the honesty behaviors
    - Fail against current Security.tsx/Automation.tsx (RED confirmed)
  </acceptance_criteria>
  <done>Failing tests encode the no-fabricated-value contract.</done>
</task>

<task type="auto">
  <name>Task 2: Apply F4 honesty fixes + headers (GREEN)</name>
  <files>src/pages/Security.tsx, src/pages/Automation.tsx, src/pages/Infrastructure.tsx</files>
  <read_first>
    - src/pages/Security.tsx (Audit Chain block; Network Policy tab; keep the Access Log)
    - src/pages/Automation.tsx (MetricCard; schedulesToCronJobs)
    - src/pages/Infrastructure.tsx (Network Policy placeholder ~255-266)
    - src/components/PageHeader.tsx (Plan 01)
    - src/pages/__tests__/Security.test.tsx, src/pages/__tests__/Automation.test.tsx (contracts to satisfy)
  </read_first>
  <action>Security.tsx: delete the "Chain integrity: Valid" row; relabel the entry-count line to "{mergedEvents.length} events loaded" (honest proxy). Remove ONLY the Provider Allowlist placeholder div (:425-444) inside the network-policy TabsContent; keep the Network Access Log sub-block intact. Automation.tsx: replace the `?? 12` MetricCard with `label="Configured Schedules" value={CRON_SCHEDULES.length}`; in `schedulesToCronJobs()` drop the hardcoded `enabled: true` live indicator (present as configured, not live). Infrastructure.tsx: remove the entire Network Policy placeholder `<div className="md:col-span-12">…</div>` (:255-266). All three: replace the bespoke h1 with `<PageHeader title="…" />` (titles: "Security", "Automation", "Infrastructure"; preserve any existing header-right controls via the actions prop).</action>
  <verify>
    <automated>npx vitest run src/pages/__tests__/Security.test.tsx src/pages/__tests__/Automation.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '>Valid<' src/pages/Security.tsx` == 0 (no hardcoded Valid badge) and Network Access Log block still present (`grep -q 'Network Access Log' src/pages/Security.tsx`)
    - `grep -q 'CRON_SCHEDULES.length' src/pages/Automation.tsx` and `grep -c '?? 12' src/pages/Automation.tsx` == 0
    - Infrastructure.tsx no longer contains the Network Policy placeholder copy (`grep -c 'Per-provider network policy rules will appear here' src/pages/Infrastructure.tsx` == 0)
    - All three files import and render `<PageHeader`
    - `npx vitest run src/pages/__tests__/Security.test.tsx src/pages/__tests__/Automation.test.tsx` exits 0
    - `npx tsc --noEmit` clean for the three files
  </acceptance_criteria>
  <done>No fabricated trust signals remain; live Access Log preserved; headers standardized.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| client render → operator trust | These readouts inform security/ops decisions; fabricated values mislead the operator |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-96-06-01 | Repudiation (false trust signal) | Security "Valid" badge, Automation `?? 12`, fake `enabled` | mitigate | Remove hardcoded trust signals; show only computed/real values or nothing — a fabricated "Valid" is the exact anti-pattern being removed |
| T-96-06-02 | Information Disclosure | Network Access Log (kept) | accept | Pre-existing live Convex-backed panel, unchanged; out of this change's scope |
| T-96-06-SC | Tampering | package installs | accept | No packages installed this phase |
</threat_model>

<verification>
- `npx vitest run src/pages/__tests__/Security.test.tsx src/pages/__tests__/Automation.test.tsx` green
- `grep -c '?? 12' src/pages/Automation.tsx` == 0
- `npx tsc --noEmit` clean
</verification>

<success_criteria>
Security/Automation/Infrastructure show only honest values, the live Access Log is preserved, and all three use PageHeader.
</success_criteria>

<output>
Create `.planning/phases/96-ui-deep-dive-cleanup-ia-restructure-command-palette-drift-fa/96-06-SUMMARY.md` when done.
</output>
