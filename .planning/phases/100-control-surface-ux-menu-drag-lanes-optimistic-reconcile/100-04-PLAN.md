---
phase: 100-control-surface-ux-menu-drag-lanes-optimistic-reconcile
plan: 04
type: execute
wave: 3
depends_on: ["100-01", "100-02", "100-03"]
files_modified:
  - src/pages/Skills.tsx
  - src/components/skills/MoveToProjectDialog.tsx
  - src/pages/__tests__/Skills.test.tsx
autonomous: true
requirements: [UX-02, UX-03]
must_haves:
  truths:
    - "Dragging a skill onto Global/Cold fires the correct enqueueLifecycle (action/sourceOrigin/destination) with no dialog, and paints a pending entry BEFORE awaiting (D-05)"
    - "Dragging a movable row onto Project opens the existing MoveToProjectDialog and does NOT enqueue at drop time — the mutation + pending paint begin only in the dialog's confirm (D-03, Pitfall 2/4)"
    - "Invalid drops (cold/dormant→Project, multi-scope→any, own-scope→same) fire ZERO mutations (D-02)"
    - "A synchronous LAYER-1 refusal clears the pending entry via .catch() and toasts the daemon's real reason — not left stranded (Pitfall 3)"
    - "The Scope rail renders below Categories with its OWN dropTargetScope state, never sharing the category dropTarget state (Pitfall 5)"
  artifacts:
    - path: "src/pages/Skills.tsx"
      provides: "ScopeRail render, dropTargetScope state, handleDropOnScope dispatch, SkillControlSurfaceProvider wrap, page-level MoveToProjectDialog for drag-to-Project"
      contains: "handleDropOnScope"
    - path: "src/components/skills/MoveToProjectDialog.tsx"
      provides: "onMoved widened to (commandId) and invoked with the generated commandId"
      contains: "onMoved?.("
    - path: "src/pages/__tests__/Skills.test.tsx"
      provides: "integration drop tests (enqueue / dialog / invalid no-op)"
      contains: "handleDropOnScope"
  key_links:
    - from: "src/pages/Skills.tsx"
      to: "src/components/skills/ScopeRail.tsx"
      via: "ScopeRail render below Categories"
      pattern: "ScopeRail"
    - from: "src/pages/Skills.tsx"
      to: "convex/forge.ts"
      via: "enqueueLifecycle (existing mutation, new trigger)"
      pattern: "enqueueLifecycle"
    - from: "src/pages/Skills.tsx"
      to: "src/lib/skills.ts"
      via: "resolveScopeDrop dispatch"
      pattern: "resolveScopeDrop"
---

<objective>
Wire the whole control surface together on the Skills page: render `<ScopeRail>` below Categories with its own independent `dropTargetScope` state, host the `SkillControlSurfaceProvider`, resolve the daemon host, and implement `handleDropOnScope` that dispatches through `resolveScopeDrop` (Plan 01) — no-op / visible-reject / direct `enqueueLifecycle` with paint-before-await + LAYER-1 `.catch()` / open the existing `MoveToProjectDialog` for the Project target (D-03). Widen `MoveToProjectDialog`'s already-declared-but-unused `onMoved` prop to carry the `commandId` so the Project-target pending paint begins inside the dialog's confirm (never at drop time — Pitfall 2/4).

Purpose: This is the integration wave that makes UX-02 (drag lanes) and UX-03 (optimistic reconcile) actually work end-to-end. It reuses `enqueueLifecycle` exactly as the ⋯ menu already calls it — a new trigger, not a new operation (D-02). The two hardest correctness points are the two rollback paths (Pitfall 3: LAYER-1 synchronous throw clears via `.catch()`; LAYER-2 async `failed` clears via the Plan 02 reconcile effect) and the Project-lane sequencing (the pending paint must begin in the dialog confirm, not the drop).
Output: `Skills.tsx` integration, `MoveToProjectDialog.tsx` `onMoved` widening, and integration drop tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/100-control-surface-ux-menu-drag-lanes-optimistic-reconcile/100-CONTEXT.md
@.planning/phases/100-control-surface-ux-menu-drag-lanes-optimistic-reconcile/100-RESEARCH.md
@.planning/phases/100-control-surface-ux-menu-drag-lanes-optimistic-reconcile/100-PATTERNS.md

<interfaces>
<!-- Verified this session. -->

Skills.tsx (existing, 502 lines — the shape to extend):
- :26 export default function Skills()   // becomes SkillsBody + a provider-wrapping default export
- :33 const [dropTarget, setDropTarget] = useState<string|null>(null)   // DO NOT reuse for scope — add a sibling dropTargetScope
- :41 const enrichedSkills = useQuery(api.skillCategories.getSkillsWithOverrides) ?? []   // resolve dropped skillName against this
- :134-139 handleDropOnCategory(categoryName, e) — the shape to mirror (dataTransfer.getData("text/plain") → mutation → setDropTarget(null))
- :302-311 the <CategoryGrid ... dropTargetCategory={dropTarget} .../> block — mount <ScopeRail> as a sibling section immediately below

From src/lib/skills.ts (Plan 01):
- resolveScopeDrop(skill, targetScope): { kind:"noop" } | { kind:"reject", hint } | { kind:"dialog" } | { kind:"enqueue", action, sourceOrigin, destination }
- resolveLifecycleActions(skill).activeOrigin   // sourceOrigin for the dialog (active-global → "claude-code")

From src/hooks/usePendingLifecycleMoves.ts (Plan 02):
- SkillControlSurfaceProvider   // wrap the page body; owns the pending map + dragging identity (single subscription)
- useSkillControlSurface(): { beginPending, clearPending, ... }

Reuse-as-is (do NOT reimplement):
- resolveHostId (exported from SkillLifecycleMenu.tsx:76-84) + useForgeHostsRaw   // host = resolveHostId(useForgeHostsRaw() ?? [], undefined)
- lifecycleRefusalMessage (src/hooks/useLifecycle.ts:85-90)
- enqueueLifecycle = useMutation(api.forge.enqueueLifecycle); arg shape:
    { hostId, commandId, skillName, workspaceId, action, sourceOrigin, destination }

MoveToProjectDialog.tsx (existing):
- :44 onMoved?: () => void   // declared, never passed today — WIDEN to (commandId: string) => void
- :79-103 handleConfirm generates commandId at :82, calls enqueueLifecycle(...move...project...), then closes on success — ADD onMoved?.(commandId) after the await resolves
- props already include skillName, sourceOrigin, hostId, open, onOpenChange (the dialog picks workspaceId internally)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Skills.tsx — ScopeRail render, dropTargetScope, handleDropOnScope dispatch, provider wrap, page-level Project dialog</name>
  <files>src/pages/Skills.tsx</files>
  <read_first>
    - src/pages/Skills.tsx (the file being modified — read the full render + handleDropOnCategory + enrichedSkills + the CategoryGrid mount block)
    - src/components/skills/ScopeRail.tsx (Plan 03 — its dropTargetScope/onDragOverScope/onDragLeaveScope/onDropOnScope prop contract)
    - src/hooks/usePendingLifecycleMoves.ts (Plan 02 — SkillControlSurfaceProvider + useSkillControlSurface)
    - src/lib/skills.ts (Plan 01 — resolveScopeDrop + resolveLifecycleActions)
    - src/components/skills/SkillLifecycleMenu.tsx:76-84,150-168,347-355 (resolveHostId export; the enqueue() arg shape to copy; how MoveToProjectDialog is rendered/props)
    - src/components/skills/MoveToProjectDialog.tsx (its prop contract for the page-level instance)
    - .planning/phases/100-control-surface-ux-menu-drag-lanes-optimistic-reconcile/100-RESEARCH.md § "Pattern 4", "Pitfall 2", "Pitfall 3", "Pitfall 5"
  </read_first>
  <action>Wrap the Skills page body in `SkillControlSurfaceProvider`: rename `export default function Skills()` to `function SkillsBody()` and add `export default function Skills() { return <SkillControlSurfaceProvider><SkillsBody/></SkillControlSurfaceProvider>; }`. Inside `SkillsBody`, add `const { beginPending, clearPending } = useSkillControlSurface()`, a sibling `const [dropTargetScope, setDropTargetScope] = useState<string | null>(null)` (NEVER reuse `dropTarget`), a page-level move-dialog state `const [scopeMoveDialog, setScopeMoveDialog] = useState<{ skillName: string; sourceOrigin: string } | null>(null)`, and resolve the host via `resolveHostId(useForgeHostsRaw() ?? [], undefined)`. Mount `<ScopeRail dropTargetScope={dropTargetScope} onDragOverScope={setDropTargetScope} onDragLeaveScope={() => setDropTargetScope(null)} onDropOnScope={handleDropOnScope} />` as a sibling section immediately below the existing CategoryGrid block (D-01, same `gap-4` rail rhythm). Implement `handleDropOnScope(scope, e)`: read `skillName = e.dataTransfer.getData("text/plain")`; `setDropTargetScope(null)`; return if no skillName; resolve `skill = enrichedSkills.find(s => s.name === skillName)`, return if not found (Tampering guard — an unrecognized name no-ops); compute `resolveScopeDrop(skill, scope)`; switch on `.kind`: `"noop"` → return (silent, zero mutation); `"reject"` → return (zero mutation; the visible not-allowed feedback was already shown by ScopeRail during dragover); `"dialog"` → `setScopeMoveDialog({ skillName, sourceOrigin: resolveLifecycleActions(skill).activeOrigin! })` (opens the picker — do NOT enqueue here); `"enqueue"` → generate `const commandId = crypto.randomUUID()`, call `beginPending(skillName, { commandId, action: result.action, destination: result.destination })` BEFORE awaiting, then `enqueueLifecycle({ hostId, commandId, skillName, workspaceId: null, action: result.action, sourceOrigin: result.sourceOrigin, destination: result.destination }).catch(err => { clearPending(skillName); toast.error(lifecycleRefusalMessage(err)); })`. Render a page-level `<MoveToProjectDialog open={!!scopeMoveDialog} skillName={scopeMoveDialog?.skillName ?? ""} sourceOrigin={scopeMoveDialog?.sourceOrigin ?? ""} hostId={host} onOpenChange={(o) => !o && setScopeMoveDialog(null)} onMoved={(commandId) => { if (scopeMoveDialog) beginPending(scopeMoveDialog.skillName, { commandId, action: "move", destination: "project" }); }} />`. Leave `handleDropOnCategory` and the category `dropTarget` state completely untouched (independent surfaces, Pitfall 5).</action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run src/pages/__tests__/Skills.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "dropTargetScope" src/pages/Skills.tsx` shows a state variable distinct from `dropTarget`; `grep -c "setDropTarget(null)" src/pages/Skills.tsx` still shows the ORIGINAL category resets unchanged (no scope logic reuses `dropTarget`).
    - `grep -n "ScopeRail" src/pages/Skills.tsx` and `grep -n "SkillControlSurfaceProvider" src/pages/Skills.tsx` and `grep -n "handleDropOnScope" src/pages/Skills.tsx` all return matches.
    - The enqueue branch calls `beginPending(...)` BEFORE `enqueueLifecycle(...)` and the enqueue call has a `.catch` that calls `clearPending` + `toast.error(lifecycleRefusalMessage(...))` (verified by reading the handler).
    - The dialog branch calls `setScopeMoveDialog` and does NOT call `enqueueLifecycle` (Pitfall 2 — no enqueue on Project drop).
    - `npx tsc --noEmit` clean; `npx vitest run src/pages/__tests__/Skills.test.tsx` green.
  </acceptance_criteria>
  <done>The Scope rail renders below Categories, drags dispatch correctly through resolveScopeDrop (no-op/reject/enqueue-with-paint-before-await+catch/dialog), the category surface is untouched, and the page hosts the control-surface provider + a page-level Project dialog wired to beginPending on confirm.</done>
</task>

<task type="auto">
  <name>Task 2: Widen MoveToProjectDialog.onMoved to (commandId) and invoke it on successful move</name>
  <files>src/components/skills/MoveToProjectDialog.tsx</files>
  <read_first>
    - src/components/skills/MoveToProjectDialog.tsx (the file being modified — the onMoved prop at :44 and handleConfirm at :79-103, esp. the commandId generated at :82)
    - src/components/skills/MoveToProjectDialog.test.tsx (the contract to keep green; extend if it asserts onMoved)
    - src/components/skills/SkillLifecycleMenu.tsx:347-355 (the existing caller that passes NO onMoved — must keep compiling since onMoved is optional)
  </read_first>
  <action>In `MoveToProjectDialog.tsx`, widen the prop signature `onMoved?: () => void` to `onMoved?: (commandId: string) => void`. In `handleConfirm`, after `await enqueueLifecycle(...)` resolves successfully (before/at the existing success-close), call `onMoved?.(commandId)` passing the `commandId` already generated at line 82. Do not change any other behavior (still close only on success, still toast the refusal on catch). The existing `SkillLifecycleMenu` caller passes no `onMoved` and must continue to compile unchanged (prop stays optional).</action>
  <verify>
    <automated>npx vitest run src/components/skills/MoveToProjectDialog.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "onMoved?: (commandId: string) => void" src/components/skills/MoveToProjectDialog.tsx` returns a match.
    - `grep -n "onMoved?.(commandId)" src/components/skills/MoveToProjectDialog.tsx` returns a match, placed after the successful `await enqueueLifecycle`.
    - `npx vitest run src/components/skills/MoveToProjectDialog.test.tsx` is green (existing assertions preserved).
    - `npx tsc --noEmit` clean (the existing menu caller still compiles).
  </acceptance_criteria>
  <done>onMoved carries the commandId and fires only on a successful move; existing callers keep compiling and the dialog's test suite is green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Skills integration drop tests — enqueue / dialog / invalid no-op</name>
  <files>src/pages/__tests__/Skills.test.tsx</files>
  <read_first>
    - src/pages/__tests__/Skills.test.tsx (extend; if absent, create following SkillLifecycleMenu.test.tsx's Convex/module-mock convention)
    - src/components/skills/SkillLifecycleMenu.test.tsx:21-105 (the mock convention: mock convex/react useQuery/useMutation + the generated api module; jsdom ResizeObserver shim; fireEvent.drop with a plain-object dataTransfer mock)
    - src/components/skills/SkillRow.test.tsx:100 (dataTransfer mock proof)
    - .planning/phases/100-control-surface-ux-menu-drag-lanes-optimistic-reconcile/100-VALIDATION.md (the UX-02 test-map rows this task satisfies)
  </read_first>
  <behavior>
    Rendering Skills (with enrichedSkills mocked via useQuery to include an active-global skill, an active-project skill, a dormant skill, and a multi-scope skill), and firing a drop on a ScopeRail entry:
    - drop active-global skill onto Cold → enqueueLifecycle called once with { action:"archive", sourceOrigin:"claude-code", destination:"cold" }; no MoveToProjectDialog opened.
    - drop active-project skill onto Global → enqueueLifecycle called once with { action:"move", sourceOrigin: the project origin, destination:"global" }.
    - drop active-global skill onto Project → MoveToProjectDialog opens (rendered) AND enqueueLifecycle is NOT called at drop time.
    - drop dormant skill onto Project → enqueueLifecycle NOT called (invalid reject); no dialog opens.
    - drop multi-scope skill onto any lane → enqueueLifecycle NOT called (invalid reject).
    - drop active-global skill onto Global (own scope) → enqueueLifecycle NOT called (noop).
  </behavior>
  <action>Extend (or create) `src/pages/__tests__/Skills.test.tsx` with an integration describe block covering the 6 drop cases in the behavior block. Mock `convex/react` (`useQuery` returning the fixture `enrichedSkills`, `useMutation` returning a spy for `enqueueLifecycle`), the generated `api` module, `useForgeHostsRaw`/`resolveHostId` to yield a host, and `sonner`. Use `fireEvent.dragOver`/`fireEvent.drop` on the ScopeRail entries with a plain-object `dataTransfer` whose `getData("text/plain")` returns the fixture skill name. Assert the `enqueueLifecycle` spy call args (or absence) per case, and assert MoveToProjectDialog presence for the Project-target-open case. Include the jsdom `ResizeObserver` shim required by Radix.</action>
  <verify>
    <automated>npx vitest run src/pages/__tests__/Skills.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/pages/__tests__/Skills.test.tsx` passes all 6 drop cases.
    - The archive case asserts exact args `{ action:"archive", sourceOrigin:"claude-code", destination:"cold" }`.
    - The Project-open case asserts `enqueueLifecycle` was NOT called at drop (Pitfall 2) and the dialog rendered.
    - The three invalid cases (dormant→Project, multi-scope→any, own-scope) each assert the `enqueueLifecycle` spy was called zero times.
    - `npx tsc --noEmit` clean.
  </acceptance_criteria>
  <done>The Skills-surface drop behavior is covered by its first-ever integration drop tests, all green, proving the matrix fires the right mutation (or none) per cell.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser drag payload → `enqueueLifecycle` mutation | Untrusted `dataTransfer` text crosses here; the dropped name is resolved against the live `enrichedSkills` query before any mutation |
| client → Convex (`enqueueLifecycle`) | Existing fail-closed Clerk auth boundary — unchanged |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-100-01 | Elevation of Privilege | client drag matrix as sole gate | accept (mitigated server-side) | `handleDropOnScope` routes to the SAME `enqueueLifecycle` the menu uses; `validateLifecyclePreflight` re-validates every rule (shadow/collision/not-cold/global-only) server-side regardless of what the client offered. No new call site bypasses the existing Clerk auth check (`convex/forge.ts:1091-1094`). |
| T-100-02 | Tampering | spoofed `dataTransfer` payload | mitigate | The dropped `skillName` is looked up against the live `enrichedSkills` query before any mutation — an unrecognized name no-ops. Even a resolved-but-wrong name passes through the unchanged server-side `isSafeSkillName` + preflight gates. `skill.name` always originates from an already-validated query result, never raw user text. |
| T-100-03 | Repudiation / false-success | LAYER-1 synchronous refusal | mitigate | The enqueue `.catch()` clears the pending entry and surfaces the daemon's real refusal (`lifecycleRefusalMessage`) — no row is ever inserted for a LAYER-1 reject, so the reconcile effect alone would strand it (Pitfall 3); the `.catch()` is the mandatory second rollback path. |
| T-100-SC | Tampering | package installs | n/a | Zero package installs. No supply-chain surface. |
</threat_model>

<verification>
- `npx vitest run src/pages/__tests__/Skills.test.tsx src/components/skills/MoveToProjectDialog.test.tsx` — green.
- `npm test` (full suite, ~2447+ tests) — stays green after this integration wave merges.
- `npx tsc --noEmit` — clean.
- Manual (deferred, live Forge daemon — same category as Phase 98/99 outstanding checks): drag active→Cold, active-global→Project (pick workspace), cold→Global; confirm each reconciles to done and rolls back honestly on a forced failure/expiry.
</verification>

<success_criteria>
- Drag across scope lanes fires the correct existing mutation (or opens the Project picker, or honestly no-ops) per the shared matrix — a new trigger over Phase 98's `enqueueLifecycle`, no new operation.
- Both rollback paths work: LAYER-1 synchronous throw via `.catch()`, LAYER-2 async `failed`/`expired` via the Plan 02 reconcile.
- The Project-lane pending paint begins only in the dialog confirm; the category surface is untouched; the scope rail owns independent drop state.
</success_criteria>

<output>
Create `.planning/phases/100-control-surface-ux-menu-drag-lanes-optimistic-reconcile/100-04-SUMMARY.md` when done
</output>
