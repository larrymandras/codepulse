---
phase: 125-signature-layers
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - convex/events.ts
  - convex/eventsWindow.test.ts
autonomous: true
requirements: [SIGNAL-02]

must_haves:
  truths:
    - "D-05: exactly ONE bounded Convex read backs the ECG hero — a new `listRecentRuntimeWindow` query range-bound on the EXISTING `by_timestamp` index, not `listRecentUnified` at a larger limit and not a new every-route subscription"
    - "D-05: the query reads `runtime_events` only, never the `events` build-time table, and projects `_id`/`eventType`/`timestamp` only — `data` (a `v.any()` of arbitrary size) is never returned"
    - "D-05: the guard is shown to go RED when the window bound is widened, proving it measures the range and not merely that rows come back"
    - "D-17: the query is the TRACE's backfill only; nothing in it feeds the 40px numeral, because `run.*`/`chat.response` reach Convex through no path at all (`send_live()`, RESEARCH R-1)"
    - "No schema change and no index addition is made — `by_timestamp` already exists on `runtime_events` (`convex/schema.ts:17`, RESEARCH R-5)"
  artifacts:
    - path: "convex/events.ts"
      provides: "listRecentRuntimeWindow — bounded 60s runtime_events read"
      contains: "listRecentRuntimeWindow"
    - path: "convex/eventsWindow.test.ts"
      provides: "Range-bound + projection + index-usage guard for the window query"
      min_lines: 80
  key_links:
    - from: "convex/events.ts"
      to: "runtime_events.by_timestamp"
      via: "withIndex range bound"
      pattern: "withIndex\\(\"by_timestamp\""
    - from: "convex/eventsWindow.test.ts"
      to: "convex/events.ts"
      via: "._handler on the registered query"
      pattern: "listRecentRuntimeWindow"
---

<objective>
Write the one bounded Convex read the Pulse ECG takes on mount, and prove it is bounded.

Purpose: D-05 permits the ECG exactly one read, and Phase 124 shipped an unbounded `collect()` on
an every-route subscription that is still open as Phase 126 work. This query is the deliberate
avoidance of that shape. RESEARCH R-5 confirmed a usable `by_timestamp` index already exists, so
this is plain query authoring — no schema change, no index addition, no self-hosted schema deploy.
Output: `listRecentRuntimeWindow` in `convex/events.ts` plus `convex/eventsWindow.test.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/125-signature-layers/125-CONTEXT.md
@.planning/phases/125-signature-layers/125-RESEARCH.md
@.planning/phases/125-signature-layers/125-PATTERNS.md
@CLAUDE.md

**The planning documents are CLAIMS. The live code is EVIDENCE.** If anything below contradicts
what you read in the repository, the repository wins — correct the plan in your SUMMARY rather
than transcribing a wrong instruction.

<interfaces>
Contract this plan CREATES, consumed by plan 125-09:

```typescript
// convex/events.ts
export const listRecentRuntimeWindow = query({
  args: { windowSeconds: v.optional(v.float64()) },
  // returns, newest first:
  //   Array<{ _id: Id<"runtime_events">; eventType: string; timestamp: number }>
});
```

Live schema, read 2026-08-21 (`convex/schema.ts:8-18`) — DO NOT modify:

```
runtime_events: { eventType, data, timestamp: v.float64(), critical, receivedAt, archived? }
  .index("by_type", ["eventType"])
  .index("by_timestamp", ["timestamp"])        // <- the index this query uses; already exists
  .index("by_critical", ["critical", "timestamp"])
```

`timestamp` is PRODUCER-SUPPLIED fractional SECONDS (`convex/events.ts:252` args, stored verbatim;
producer origin `astridr/engine/telemetry.py:214`). It is NOT milliseconds and it is NOT floored.
The house convention for deriving a bound is `Date.now() / 1000`, as at `convex/events.ts:204`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the bounded 60s runtime_events window query</name>
  <read_first>
    - `convex/events.ts:185-262` — `listRecentUnified` in full (the range-bound `.withIndex`
      idiom at `:210`/`:217`, its 2026-07-22 incident comment at `:200-203`, and `insertEvent`)
    - `convex/schema.ts:8-18` — the `runtime_events` table and its three indexes
    - `.planning/phases/125-signature-layers/125-RESEARCH.md` §R-5 — the recommended query shape
      and the explicit instruction not to project `data`
    - `CLAUDE.md` §"Convex & Frontend Lessons" — the 4,096-read limit, the "an index cannot speed
      an unfiltered count" rule, and the `useQuery`-throws-blanks-the-tree lesson
  </read_first>
  <files>convex/events.ts</files>
  <action>
    Add `listRecentRuntimeWindow` to `convex/events.ts`, immediately after `listRecentUnified`.
    Signature exactly as declared in `<interfaces>`.

    Handler: clamp `windowSeconds` to the inclusive range 1..300 (a caller-supplied window is
    untrusted input and an unclamped value is an unbounded scan wearing an argument's costume —
    default 60 when absent, and reject-by-clamp rather than throwing, because a throwing Convex
    query unmounts the React tree per this repo's own lesson). Derive `nowSec = Date.now() / 1000`
    and `lo = nowSec - windowSec`; upper-bound at `nowSec + 60` so a producer clock a minute ahead
    does not silently drop live rows while still excluding millisecond-scale junk rows.

    Query `runtime_events` with `.withIndex("by_timestamp", q => q.gte("timestamp", lo)
    .lte("timestamp", hi))`, `.order("desc")`, `.filter(q => q.neq(q.field("archived"), true))`,
    then `.collect()`. `.collect()` is correct here and is NOT the Phase 124 defect: the read is
    bounded by the index range itself, not by a `.take()` cap. Add a comment saying exactly that,
    and naming the 4,096-READ ceiling as the reason the window is clamped rather than left open.

    Map the result to `{ _id, eventType, timestamp }` and return it. Do NOT return `data` — it is
    `v.any()` and can carry whole tool-argument payloads; D-06's hue map keys on `eventType` alone.
    Do NOT merge in the `events` table. Do NOT modify `listRecentUnified`, `insertEvent`, the
    schema, or any index.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "listRecentRuntimeWindow" convex/events.ts` returns 1.
    - `grep -c 'withIndex("by_timestamp"' convex/events.ts` returns ≥ 2 (the pre-existing use in
      `listRecentUnified` plus the new one).
    - `git diff convex/schema.ts` is empty.
    - The new handler body contains no occurrence of `q.query("events")` — confirmed by reading the
      added block, and stated in the SUMMARY with the line range.
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>A single-table, index-range-bound, `data`-free 60s window query exists next to its analog, with the window clamped so no caller can turn it into a full scan.</done>
</task>

<task type="auto">
  <name>Task 2: Prove the window is actually bounded, projected, and index-driven</name>
  <read_first>
    - `convex/events.test.ts:24-70` — the fake `db.query().withIndex()` store that HONOURS
      `eq`/`gte`/`lte` constraints, and the `._handler` convention for invoking the really-
      registered function rather than a re-implementation
    - `convex/events.ts` — the function just written
    - `.planning/phases/125-signature-layers/125-VALIDATION.md` — the SIGNAL-02 row for this query
  </read_first>
  <files>convex/eventsWindow.test.ts</files>
  <action>
    Create `convex/eventsWindow.test.ts`. Build a local fake store modelled on
    `events.test.ts`'s `makeEventsStore()` — it must record which index name was passed to
    `withIndex` and must APPLY the `gte`/`lte` constraints, not ignore them. Invoke the real
    registered query via `listRecentRuntimeWindow._handler(ctx, args)`, never a copy of the logic.

    Seed rows with fractional-second timestamps spanning the boundary: at `now`, `now - 10`,
    `now - 59.5`, `now - 60.5`, `now - 3600`, one `archived: true` row at `now - 5`, and one row
    at `now + 600` (a bogus far-future stamp).

    Assert:
    (a) the returned set is exactly the in-window, non-archived rows — the `now - 60.5`,
        `now - 3600`, `now + 600` and archived rows are all absent, each asserted individually so
        a failure names which bound broke;
    (b) `withIndex` was called with the literal string `by_timestamp` — assert on the recorded
        index name, not on the source text of `events.ts`;
    (c) every returned object has exactly the keys `_id`, `eventType`, `timestamp` (assert
        `Object.keys(row).sort()`), so a future edit that starts leaking `data` fails here;
    (d) `windowSeconds: 100000` returns the same in-60s-clamp result set as the default — i.e. the
        clamp holds and an oversized argument cannot widen the scan;
    (e) results are ordered newest-first.

    Then run the mutation proof: temporarily remove the `.lte(...)` upper bound (a syntactically
    valid edit), re-run, and confirm assertion (a)'s future-row case goes RED. Separately, change
    the mapper to spread the whole row, re-run, and confirm (c) goes RED. Revert both and record
    all four verbatim outputs (two RED, then GREEN) in your SUMMARY.
  </action>
  <verify>
    <automated>npx vitest run convex/eventsWindow.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run convex/eventsWindow.test.ts` exits 0 with ≥ 5 passing tests.
    - `grep -c "_handler" convex/eventsWindow.test.ts` returns ≥ 1.
    - `grep -c "by_timestamp" convex/eventsWindow.test.ts` returns ≥ 1.
    - The SUMMARY contains the verbatim RED output for the removed upper bound AND for the leaked
      `data` projection, plus the GREEN output after revert.
    - `npm test` exits 0.
  </acceptance_criteria>
  <done>The window query's boundedness, projection and index usage are each asserted on an observable, and each assertion has been shown to fail under a valid mutation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → public Convex query | `listRecentRuntimeWindow` is a PUBLIC Convex function; per `CLAUDE.md`'s SEED-008 decision anything that can route to the host can call it with no credential |
| producer-supplied `timestamp` → index range | Ástríðr supplies the stored timestamp; a wrong-unit or far-future value crosses into a range bound |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-125-02-01 | Denial of Service | `listRecentRuntimeWindow` `windowSeconds` arg | mitigate | Clamp `windowSeconds` to 1..300 inside the handler. Unclamped, a caller could request a multi-day window and drive a self-hosted single-node instance into the read ceiling — the shape of the 2026-07-22 incident. Asserted by test case (d) with `windowSeconds: 100000`. |
| T-125-02-02 | Information disclosure | returned row shape | mitigate | `data` (`v.any()`) may carry tool arguments, prompts or paths. The projection returns `_id`/`eventType`/`timestamp` only, asserted by an exact `Object.keys` check that a future spread cannot slip past. |
| T-125-02-03 | Denial of Service | `.collect()` on the range | mitigate | Bounded by a ≤300s index range with an upper bound, not by an arbitrary `.take()`. Documented in-code against the 4,096-READ ceiling. |
| T-125-02-04 | Elevation of privilege | making this an `internalQuery` | accept | Deliberately NOT taken. Per `CLAUDE.md`, hardening one module's functions in isolation is the exact shape SEED-008 REJECTED (215 public mutations, 85 files with no `ctx.auth`); the tailnet is the auth boundary and the LAN firewall rule enforces it. This query returns event types and timestamps only, no payload. |
| T-125-02-05 | Tampering | producer-supplied `timestamp` | mitigate | An upper bound of `nowSec + 60` excludes far-future and millisecond-scale rows from the window, asserted by the `now + 600` seed row. |
| T-125-02-SC | Tampering | npm/pip/cargo installs | mitigate | This plan installs nothing. No package-manager command may run here. |
</threat_model>

<verification>
- `npm test` green.
- `npx tsc --noEmit` exits 0.
- `git diff --stat` confined to `convex/events.ts` and `convex/eventsWindow.test.ts`.
- `git diff convex/schema.ts` empty — this plan must not cause a schema deploy.
- NO deploy is performed here. Deploying this function is plan 125-07's job, under its own
  working-tree check, and `npx convex deploy` must never be run without
  `--env-file C:\Users\mandr\convex-selfhost\selfhosted.envfile`.
</verification>

<success_criteria>
- `listRecentRuntimeWindow` exists, single-table, index-range-bound, clamped, `data`-free.
- Its bound, projection, index and ordering are each asserted and each mutation-proven.
- No schema or index change was made.
</success_criteria>

<output>
Create `.planning/phases/125-signature-layers/125-02-SUMMARY.md` when done.
</output>
