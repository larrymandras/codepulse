---
phase: 01-ui-redesign
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/index.css
  - src/components/MetricCard.tsx
  - src/lib/utils.ts
  - src/components/ui/separator.tsx
  - src/components/ui/badge.tsx
  - src/components/ui/tooltip.tsx
  - src/components/ui/button.tsx
  - components.json
  - package.json
autonomous: true
requirements:
  - UI-01
  - UI-02

must_haves:
  truths:
    - "oklch CSS variables are the sole color source — no hex, rgb, or indigo-* anywhere in index.css"
    - "--radius is 0rem globally"
    - "CRT overlay CSS is completely absent"
    - "Cinzel font-face and usage are completely absent"
    - "MetricCard renders without border, background, or rounded corners"
    - "MetricCard values use tabular-nums class"
    - "shadcn/ui is initialized with New York style and components are installable"
  artifacts:
    - path: "src/index.css"
      provides: "Token layer with oklch palette, operational tokens, slide-in animation keyframe, privacy rules"
      contains: "--background"
    - path: "src/components/MetricCard.tsx"
      provides: "Borderless metric display with tabular-nums and Lucide trend icons"
      contains: "tabular-nums"
    - path: "src/lib/utils.ts"
      provides: "cn() helper from shadcn/ui"
      exports: ["cn"]
    - path: "components.json"
      provides: "shadcn/ui configuration"
      contains: "new-york"
  key_links:
    - from: "src/index.css"
      to: "all components"
      via: "CSS custom properties"
      pattern: "--background.*oklch"
    - from: "src/components/MetricCard.tsx"
      to: "9 page imports"
      via: "unchanged prop API {label, value, trend}"
      pattern: "MetricCardProps"
---

<objective>
Establish the design token layer and shadcn/ui foundation for the Paperclip design system.

Purpose: Every subsequent plan depends on the CSS variable tokens and shadcn/ui primitives being installed and correct. This is Wave 0 — the foundation everything else builds on.
Output: Fully replaced index.css with oklch tokens, shadcn/ui initialized with New York style, cn() utility, four shadcn primitives installed (separator, badge, tooltip, button), MetricCard redesigned to borderless tabular-nums pattern.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-ui-redesign/01-CONTEXT.md
@.planning/phases/01-ui-redesign/01-RESEARCH.md
@.planning/phases/01-ui-redesign/01-UI-SPEC.md

<interfaces>
<!-- Current MetricCard prop API — FROZEN, do not change -->
From src/components/MetricCard.tsx:
```typescript
interface MetricCardProps {
  label: string;
  value: string | number;
  trend?: "up" | "down" | "neutral";
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize shadcn/ui and replace index.css token layer</name>
  <files>src/index.css, src/lib/utils.ts, components.json, package.json</files>
  <read_first>
    - src/index.css (current CRT overlay, font declarations, privacy rules, eq-bar animations — must preserve privacy + eq-bar rules)
    - package.json (current dependencies)
    - tsconfig.json (verify @/ path alias exists)
    - vite.config.ts (verify @/ path alias exists)
    - .planning/phases/01-ui-redesign/01-UI-SPEC.md (exact oklch values in CSS Custom Properties section)
  </read_first>
  <action>
1. Run `npx shadcn@latest init` from the codepulse root. When prompted: Style = "New York", Base color = "Neutral", CSS variables = "Yes". This creates `components.json`, `src/lib/utils.ts` (with `cn()` helper), and modifies `src/index.css`.

2. After init completes, FULLY REPLACE `src/index.css` with the following structure (shadcn init may have written partial content — overwrite it completely):

```css
@import "tailwindcss";

@theme {
  --font-geist: "Geist", system-ui, sans-serif;
  --font-mono: "JetBrains Mono", monospace;
}

@layer base {
  :root {
    --background: oklch(1 0 0);
    --foreground: oklch(0.145 0 0);
    --card: oklch(1 0 0);
    --card-foreground: oklch(0.145 0 0);
    --muted: oklch(0.97 0 0);
    --muted-foreground: oklch(0.556 0 0);
    --border: oklch(0.922 0 0);
    --input: oklch(0.922 0 0);
    --primary: oklch(0.205 0 0);
    --primary-foreground: oklch(0.985 0 0);
    --accent: oklch(0.97 0 0);
    --accent-foreground: oklch(0.205 0 0);
    --destructive: oklch(0.577 0.245 27.325);
    --ring: oklch(0.708 0 0);
    --popover: oklch(1 0 0);
    --popover-foreground: oklch(0.145 0 0);
    --secondary: oklch(0.97 0 0);
    --secondary-foreground: oklch(0.205 0 0);

    --status-ok: oklch(0.65 0.15 142);
    --status-error: oklch(0.65 0.18 27);
    --status-warn: oklch(0.75 0.12 85);
    --chart-bar: oklch(0.45 0 0);
    --chart-bar-accent: oklch(0.65 0.08 220);

    --radius: 0rem;
  }

  .dark {
    --background: oklch(0.145 0 0);
    --foreground: oklch(0.985 0 0);
    --card: oklch(0.205 0 0);
    --card-foreground: oklch(0.985 0 0);
    --muted: oklch(0.269 0 0);
    --muted-foreground: oklch(0.708 0 0);
    --border: oklch(0.269 0 0);
    --input: oklch(0.269 0 0);
    --primary: oklch(0.922 0 0);
    --primary-foreground: oklch(0.205 0 0);
    --accent: oklch(0.269 0 0);
    --accent-foreground: oklch(0.985 0 0);
    --destructive: oklch(0.704 0.191 22.216);
    --ring: oklch(0.439 0 0);
    --popover: oklch(0.205 0 0);
    --popover-foreground: oklch(0.985 0 0);
    --secondary: oklch(0.269 0 0);
    --secondary-foreground: oklch(0.985 0 0);

    --status-ok: oklch(0.72 0.17 142);
    --status-error: oklch(0.72 0.20 27);
    --status-warn: oklch(0.80 0.14 85);
    --chart-bar: oklch(0.55 0 0);
    --chart-bar-accent: oklch(0.70 0.10 220);
  }
}

body {
  font-family: var(--font-geist);
}

code, pre, .font-mono {
  font-family: var(--font-mono);
}

/* Privacy modes — PRESERVED from original */
.privacy-demo [data-sensitive] {
  filter: blur(4px);
  transition: filter 0.2s;
}
.privacy-demo [data-sensitive]:hover {
  filter: blur(0);
}
.privacy-screenshot [data-sensitive] {
  visibility: hidden;
}
.privacy-screenshot .metric-card {
  opacity: 0.3;
}

/* Ambient audio equalizer bars — PRESERVED from original (copy all eq-bar keyframes) */
/* Copy eq-bounce-1, eq-bounce-2, eq-bounce-3 keyframes and .eq-bar-* classes from original */

/* Activity feed slide-in animation (UI-07, D-14) */
@keyframes slide-in-entry {
  from {
    opacity: 0;
    transform: translateY(-14px) scale(0.985);
    filter: blur(4px);
  }
  to {
    opacity: 1;
    transform: translateY(0) scale(1);
    filter: blur(0);
  }
}
.activity-entry-new {
  animation: slide-in-entry 520ms ease-out forwards;
  box-shadow: inset 2px 0 0 var(--primary);
}
```

CRITICAL: Read the original index.css fully and copy ALL eq-bounce keyframes and eq-bar classes verbatim. Do NOT drop them.

REMOVED (per D-01): `.crt-overlay`, `.crt-overlay::before`, `.crt-overlay::after`, `@keyframes crt-flicker`.
REMOVED (per D-02): `--font-cinzel`, `h1, h2 { font-family: var(--font-cinzel) }`.

3. Install shadcn/ui primitives:
```bash
npx shadcn@latest add separator tooltip badge button
```

4. Install lucide-react (needed by MetricCard in Task 2 and all subsequent plans):
```bash
npm install lucide-react
```

5. Verify `src/lib/utils.ts` exists and exports `cn`. If shadcn init created it, confirm content. If not, create:
```typescript
import { type ClassValue, clsx } from "clsx";
import { twMerge } from "tailwind-merge";

export function cn(...inputs: ClassValue[]) {
  return twMerge(clsx(inputs));
}
```

6. Ensure dark mode works with class strategy. If shadcn init did not add it, add `@variant dark (.dark &);` after the `@import "tailwindcss"` line (Tailwind 4 syntax for class-based dark mode). Check if shadcn's init already handled this.
  </action>
  <verify>
    <automated>cd C:/Users/mandr/codepulse && grep -c "oklch" src/index.css | xargs test 20 -le && grep -q "\-\-radius: 0rem" src/index.css && grep -qv "crt-overlay" src/index.css && grep -qv "cinzel" src/index.css && grep -q "slide-in-entry" src/index.css && grep -q "eq-bounce" src/index.css && test -f src/lib/utils.ts && test -f components.json && grep -q "new-york" components.json && test -f src/components/ui/separator.tsx && test -f src/components/ui/badge.tsx && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - grep "oklch" src/index.css returns 20+ matches (light + dark mode tokens)
    - grep "--radius: 0rem" src/index.css returns a match
    - grep "crt-overlay" src/index.css returns NO matches
    - grep "cinzel" src/index.css returns NO matches (case-insensitive)
    - grep "slide-in-entry" src/index.css returns a match
    - grep "eq-bounce" src/index.css returns matches (preserved from original)
    - grep "privacy-demo" src/index.css returns a match (preserved from original)
    - components.json exists and contains "new-york"
    - src/lib/utils.ts exists and contains "export function cn"
    - src/components/ui/separator.tsx exists
    - src/components/ui/badge.tsx exists
    - src/components/ui/tooltip.tsx exists
    - src/components/ui/button.tsx exists
    - grep "lucide-react" package.json returns a match
  </acceptance_criteria>
  <done>shadcn/ui initialized with New York style. index.css fully replaced with oklch token layer. CRT and Cinzel removed. Privacy and eq-bar rules preserved. Slide-in animation keyframe added. Four shadcn primitives installed. lucide-react installed. cn() utility available.</done>
</task>

<task type="auto">
  <name>Task 2: Redesign MetricCard to borderless tabular-nums pattern</name>
  <files>src/components/MetricCard.tsx</files>
  <read_first>
    - src/components/MetricCard.tsx (current implementation — prop API is FROZEN)
    - .planning/phases/01-ui-redesign/01-UI-SPEC.md (MetricCard section in Component Inventory + Typography section for sizing)
    - .planning/phases/01-ui-redesign/01-RESEARCH.md (Pattern 2: MetricCard Redesign)
  </read_first>
  <action>
Replace the visual implementation of MetricCard while keeping the exact same prop API (`{ label, value, trend }: MetricCardProps`) and the `memo` wrapper with default export.

New implementation per UI-02, D-03, UI-SPEC Typography section:

```tsx
import { memo } from "react";
import { TrendingUp, TrendingDown } from "lucide-react";

interface MetricCardProps {
  label: string;
  value: string | number;
  trend?: "up" | "down" | "neutral";
}

function MetricCardInner({ label, value, trend }: MetricCardProps) {
  const trendColor =
    trend === "up" ? "text-(--status-ok)"
    : trend === "down" ? "text-(--status-error)"
    : "text-muted-foreground";

  return (
    <div className="p-4">
      <p className="text-xs text-muted-foreground uppercase tracking-wide">{label}</p>
      <div className="mt-1 flex items-baseline gap-2">
        <span className="text-2xl font-semibold tabular-nums">{value}</span>
        {trend === "up" && <TrendingUp className={`w-3 h-3 ${trendColor}`} />}
        {trend === "down" && <TrendingDown className={`w-3 h-3 ${trendColor}`} />}
      </div>
    </div>
  );
}

const MetricCard = memo(MetricCardInner);
export default MetricCard;
```

Key changes from current:
- REMOVED: `bg-gray-800/50 border border-gray-700/50 rounded-xl` (no background, no border, no rounding)
- REMOVED: ASCII trend arrows (`^`, `v`) replaced with Lucide `TrendingUp`/`TrendingDown` icons at `w-3 h-3`
- REMOVED: `text-green-400`/`text-red-400` replaced with `text-(--status-ok)`/`text-(--status-error)` (Tailwind 4 CSS variable syntax)
- ADDED: `tabular-nums` class on the value span (UI-02)
- KEPT: `memo` wrapper, default export, exact same `MetricCardProps` interface
  </action>
  <verify>
    <automated>cd C:/Users/mandr/codepulse && grep -q "tabular-nums" src/components/MetricCard.tsx && grep -q "TrendingUp" src/components/MetricCard.tsx && grep -qv "rounded" src/components/MetricCard.tsx && grep -qv "border" src/components/MetricCard.tsx && grep -qv "bg-gray" src/components/MetricCard.tsx && grep -q "export default MetricCard" src/components/MetricCard.tsx && npx tsc --noEmit --pretty false 2>&1 | head -5 && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - grep "tabular-nums" src/components/MetricCard.tsx returns a match
    - grep "TrendingUp" src/components/MetricCard.tsx returns a match (Lucide icon import)
    - grep "TrendingDown" src/components/MetricCard.tsx returns a match
    - grep "rounded" src/components/MetricCard.tsx returns NO matches
    - grep "border" src/components/MetricCard.tsx returns NO matches
    - grep "bg-gray" src/components/MetricCard.tsx returns NO matches
    - grep "status-ok" src/components/MetricCard.tsx returns a match
    - grep "status-error" src/components/MetricCard.tsx returns a match
    - grep "export default MetricCard" src/components/MetricCard.tsx returns a match
    - grep "MetricCardProps" src/components/MetricCard.tsx returns matches (interface preserved)
    - npx tsc --noEmit passes without errors related to MetricCard
  </acceptance_criteria>
  <done>MetricCard renders borderless with large tabular-nums values, tiny muted labels, and Lucide trend icons using oklch operational status colors. Prop API unchanged — all 9 page imports continue to work without modification.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry -> local install | shadcn CLI and lucide-react installed from npm |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | shadcn CLI init | accept | Official shadcn registry only; no third-party registries declared. Low risk — installs source files, not runtime deps. |
| T-01-02 | Information Disclosure | index.css | accept | No secrets in CSS tokens. Privacy mode rules preserved for PII masking. |
</threat_model>

<verification>
- `npm run build` completes without errors
- `npx tsc --noEmit` passes
- No `recharts` imports broken (not touched in this plan)
- All existing tests pass: `npm test`
</verification>

<success_criteria>
- oklch token layer is the sole color source in index.css
- shadcn/ui New York style initialized with separator, badge, tooltip, button
- MetricCard is borderless with tabular-nums and Lucide trend icons
- CRT overlay and Cinzel font completely removed
- Privacy and eq-bar CSS rules preserved
- All 9 MetricCard consumers compile without changes
</success_criteria>

<output>
After completion, create `.planning/phases/01-ui-redesign/01-01-SUMMARY.md`
</output>
