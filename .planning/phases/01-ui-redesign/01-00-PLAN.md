---
phase: 01-ui-redesign
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - src/components/__tests__/theme.test.ts
  - src/components/__tests__/MetricCard.test.tsx
  - src/components/__tests__/SectionHeader.test.tsx
  - src/layouts/__tests__/DashboardLayout.test.tsx
  - src/components/__tests__/FlexBarChart.test.tsx
  - src/components/__tests__/EntityRow.test.tsx
  - src/components/__tests__/ActivityAnimation.test.tsx
autonomous: true
requirements:
  - UI-01
  - UI-02
  - UI-03
  - UI-04
  - UI-05
  - UI-06
  - UI-07

must_haves:
  truths:
    - "All 7 test files exist and are importable by Vitest"
    - "npm test -- --run exits cleanly (all stubs pass or are skipped)"
    - "Each stub contains at least one describe block and one test.todo or placeholder test"
  artifacts:
    - path: "src/components/__tests__/theme.test.ts"
      provides: "Stubs for UI-01 CSS variable validation"
      contains: "describe"
    - path: "src/components/__tests__/MetricCard.test.tsx"
      provides: "Updated stubs for borderless MetricCard with tabular-nums"
      contains: "tabular-nums"
    - path: "src/components/__tests__/SectionHeader.test.tsx"
      provides: "Stubs for UI-03 section header pattern"
      contains: "describe"
    - path: "src/layouts/__tests__/DashboardLayout.test.tsx"
      provides: "Stubs for UI-04 sidebar sections, icons, badges"
      contains: "describe"
    - path: "src/components/__tests__/FlexBarChart.test.tsx"
      provides: "Stubs for UI-05 flex bar chart"
      contains: "describe"
    - path: "src/components/__tests__/EntityRow.test.tsx"
      provides: "Stubs for UI-06 entity row pattern"
      contains: "describe"
    - path: "src/components/__tests__/ActivityAnimation.test.tsx"
      provides: "Stubs for UI-07 slide-in animation"
      contains: "describe"
  key_links: []
---

<objective>
Create all Wave 0 test stubs required by VALIDATION.md so that every subsequent plan can run `npm test -- --run` as its feedback mechanism.

Purpose: VALIDATION.md declares wave_0_complete: false and lists 6 missing test files plus 1 needing update. Without these stubs, no plan can include `npm test` in its verify blocks. This is the prerequisite for Nyquist-compliant verification.
Output: 7 test files (6 new, 1 updated) all passing or skipped under `npm test -- --run`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-ui-redesign/01-CONTEXT.md
@.planning/phases/01-ui-redesign/01-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create 6 new test stub files</name>
  <files>src/components/__tests__/theme.test.ts, src/components/__tests__/SectionHeader.test.tsx, src/layouts/__tests__/DashboardLayout.test.tsx, src/components/__tests__/FlexBarChart.test.tsx, src/components/__tests__/EntityRow.test.tsx, src/components/__tests__/ActivityAnimation.test.tsx</files>
  <read_first>
    - src/components/__tests__/MetricCard.test.tsx (existing test file — understand the testing patterns, imports, render helpers used in this project)
    - .planning/phases/01-ui-redesign/01-VALIDATION.md (Wave 0 Requirements section — exact file paths and what each stub must cover)
    - vitest.config.ts (test configuration)
  </read_first>
  <action>
Create 6 test stub files. Each stub uses `describe` with `test.todo` placeholders describing the behaviors that will be validated after implementation. Stubs MUST be syntactically valid and pass `npm test -- --run` (todo tests are counted as skipped, not failures).

**src/components/__tests__/theme.test.ts** (UI-01 — CSS tokens):
```typescript
import { describe, test } from "vitest";

describe("Design Tokens (UI-01)", () => {
  test.todo("index.css contains oklch values for --background in :root");
  test.todo("index.css contains oklch values for --background in .dark");
  test.todo("--radius is set to 0rem");
  test.todo("no hex or rgb color values in index.css");
  test.todo("CRT overlay classes are absent");
  test.todo("Cinzel font references are absent");
});
```

**src/components/__tests__/SectionHeader.test.tsx** (UI-03):
```typescript
import { describe, test } from "vitest";

describe("SectionHeader (UI-03)", () => {
  test.todo("renders title with uppercase tracking-wide styling");
  test.todo("renders Separator below title");
  test.todo("renders optional action slot");
});
```

**src/layouts/__tests__/DashboardLayout.test.tsx** (UI-04):
Create the `src/layouts/__tests__/` directory if it does not exist.
```typescript
import { describe, test } from "vitest";

describe("DashboardLayout Sidebar (UI-04)", () => {
  test.todo("renders 5 section groups: OVERVIEW, OPERATIONS, SYSTEM, INSIGHTS, ADMIN");
  test.todo("each nav item renders a Lucide icon, not ASCII text");
  test.todo("nav items with countQuery display a Badge with numeric count");
  test.todo("collapsed state shows icon-only with Tooltip labels");
  test.todo("collapsed icons have aria-label for accessibility");
  test.todo("sidebar width is 240px (w-60) when expanded");
});
```

**src/components/__tests__/FlexBarChart.test.tsx** (UI-05):
```typescript
import { describe, test } from "vitest";

describe("FlexBarChart (UI-05)", () => {
  test.todo("renders one bar per data item");
  test.todo("bar heights are proportional to values");
  test.todo("hover shows tooltip with label and value");
  test.todo("click calls onSegmentClick with label and value");
  test.todo("uses --chart-bar token for bar color");
});
```

**src/components/__tests__/EntityRow.test.tsx** (UI-06):
```typescript
import { describe, test } from "vitest";

describe("EntityRow (UI-06)", () => {
  test.todo("renders icon, primary text, and optional secondary text");
  test.todo("renders optional trailing content");
  test.todo("applies hover state only when onClick is provided");
  test.todo("renders border-b divider between rows");
  test.todo("icon is sized at w-4 h-4");
});
```

**src/components/__tests__/ActivityAnimation.test.tsx** (UI-07):
```typescript
import { describe, test } from "vitest";

describe("Activity Feed Animation (UI-07)", () => {
  test.todo("new entries receive activity-entry-new class");
  test.todo("slide-in-entry keyframe exists in index.css");
  test.todo("activity-entry-new class applies inset box-shadow accent");
});
```
  </action>
  <verify>
    <automated>cd C:/Users/mandr/codepulse && test -f src/components/__tests__/theme.test.ts && test -f src/components/__tests__/SectionHeader.test.tsx && test -f src/layouts/__tests__/DashboardLayout.test.tsx && test -f src/components/__tests__/FlexBarChart.test.tsx && test -f src/components/__tests__/EntityRow.test.tsx && test -f src/components/__tests__/ActivityAnimation.test.tsx && npm test -- --run 2>&1 | tail -5 && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>All 6 new test stub files exist, contain describe blocks with test.todo placeholders, and npm test -- --run completes without failures.</done>
</task>

<task type="auto">
  <name>Task 2: Update MetricCard.test.tsx for borderless redesign</name>
  <files>src/components/__tests__/MetricCard.test.tsx</files>
  <read_first>
    - src/components/__tests__/MetricCard.test.tsx (current test content — understand what assertions exist)
  </read_first>
  <action>
Read the existing MetricCard.test.tsx. Update or add test.todo stubs for the new borderless, tabular-nums, Lucide icon design:

Add these test.todo entries (keep any existing passing tests that are still valid):
```typescript
test.todo("renders value with tabular-nums class");
test.todo("renders TrendingUp icon when trend is up");
test.todo("renders TrendingDown icon when trend is down");
test.todo("does not render border or rounded corners");
test.todo("does not render bg-gray background");
test.todo("uses --status-ok color for up trend");
test.todo("uses --status-error color for down trend");
```

If existing tests assert old styles (rounded-xl, bg-gray-800, ASCII arrows), comment them out or replace with the new todo stubs. The goal is: `npm test -- --run` passes cleanly.
  </action>
  <verify>
    <automated>cd C:/Users/mandr/codepulse && grep -q "tabular-nums" src/components/__tests__/MetricCard.test.tsx && npm test -- --run 2>&1 | tail -5 && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>MetricCard.test.tsx updated with stubs for new borderless design. npm test -- --run passes cleanly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | Test stubs only — no runtime code, no external input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-00-01 | N/A | Test stubs | accept | No attack surface — test files are dev-only artifacts |
</threat_model>

<verification>
- `npm test -- --run` completes without failures
- All 7 test files exist at their VALIDATION.md paths
- No test file has syntax errors
</verification>

<success_criteria>
- 6 new test stub files created at exact paths from VALIDATION.md
- MetricCard.test.tsx updated with borderless design stubs
- All stubs use test.todo (skipped, not failing)
- npm test -- --run exits cleanly
- wave_0_complete can be set to true in VALIDATION.md
</success_criteria>

<output>
After completion, create `.planning/phases/01-ui-redesign/01-00-SUMMARY.md`
</output>
