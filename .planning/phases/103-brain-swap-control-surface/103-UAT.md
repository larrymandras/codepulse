---
status: complete
phase: 103-brain-swap-control-surface
source: [103-01-SUMMARY.md, 103-02-SUMMARY.md, 103-03-SUMMARY.md, 103-04-SUMMARY.md, 103-05-SUMMARY.md, 103-06-SUMMARY.md, 103-07-SUMMARY.md, 103-08-SUMMARY.md, 103-09-SUMMARY.md, 103-10-SUMMARY.md, 103-11-SUMMARY.md, 103-12-SUMMARY.md, 103-13-SUMMARY.md, 103-14-SUMMARY.md, 103-15-SUMMARY.md, 103-16-SUMMARY.md, 103-17-SUMMARY.md, 103-18-SUMMARY.md]
started: 2026-07-29T21:39:07Z
updated: 2026-07-30T00:50:00Z
run_conditions: |
  Isolated ephemeral Vite server on 127.0.0.1:5199 (Clerk disabled via an empty
  VITE_CLERK_PUBLISHABLE_KEY set from Git Bash, since PS 5.1 deletes empty env vars), pointed at
  the SAME live backends as the running stack: self-hosted Convex 127.0.0.1:3210 and Ástríðr
  127.0.0.1:8181. Playwright/Chromium headless, 1600x1000 (420x900 for the narrow-width test).
  The operator's own :5173 stack was never touched. Mirrors the 103-13-T1 recipe.
  Larry authorized real swap.set/restore dispatches, with every swap reverted after observation.
  He did NOT authorize inducing a swap failure (test 11).
  END STATE VERIFIED CLEAN: no global override left in force (badge shows no "GLOBAL" chip);
  the final act of the run was a real restore:true that cleared the override set during testing.
  Every live swap.set dispatched across the whole UAT, in order:
    value=claude-sonnet-5 (t7) / value=claude-haiku-4-5-20251001 (t8 leg2) /
    value=claude-sonnet-5 (t8) / value=claude-haiku-4-5-20251001 (t8 revert-to-prior) /
    restore=true (t12 clear) — plus one hung confirm that dispatched nothing (see test 16).
---

## Current Test

[testing complete — all 16 tests resolved, all 4 gaps fixed and re-verified live]

## Fix Round (2026-07-29, post-UAT)

All 4 logged gaps were fixed, each TDD'd and mutation-checked, then RE-VERIFIED LIVE against the
same stack on a fresh ephemeral server:

| Gap | Fix commit | Live re-verification |
|---|---|---|
| test 15 (blocker) — picker crashed from routed hosts | `a9ef7e16` | Chat pill: 331 rows, 0 Tooltip errors, 0 "went wrong" (was: popover destroyed, 2 errors). Settings Swap: 331 rows, 0 errors (was: destroyed, 2 errors). Header badge control unchanged: 331 rows, 0 errors. |
| test 2 (major) — "unknown" shown as a live engine | `ddb51d96` | Badge reads "No brain reported" at t+0/4/9s, aria-label "Active brain: No brain reported". Confirmed with the sentinel row STILL in the table — the read-path guard makes the UI honest with no deploy. |
| test 16 (major) — never-settling dispatch trapped the operator | `fc9828ff` | Unit-verified both failure shapes on both legs (never-settles, rejects); the original hang did not recur in any live run after the fix. |
| test 3 (minor) — transience blamed for a permanent gap | `6afd67dd` | Picker now shows "Per-profile swapping isn't available yet" (1 match); old "try again in a moment" copy absent (0 matches) on that axis. |

Test 9 (WR-01), previously BLOCKED by test 15, is now **PASS** — see its entry below.

### Round 2 (2026-07-29, "fix all of it")

| Item | Status |
|---|---|
| Test 11 — failed-swap retry | **PASS** (see its entry). Induced via WS interception; 0 frames reached the live backend. |
| Cosmetic raw-model-id leak (4 sites) | **FIXED** (`35d16d5a`, `025d7502`) and verified live: badge + pill "Claude Sonnet 5", warning "(Claude Sonnet 5)", "Reverted to Claude Sonnet 5." |
| NEW — failed swap claimed the override applied | **FIXED** (`025d7502`) — found while inducing the test-11 failure. |
| `pruneUnresolved` cleanup mutation | **WRITTEN + committed** (`35d16d5a`), batch-capped, internal-only — but NOT yet deployed or run (see below). |
| Convex ingest-guard deploy | **BLOCKED — needs Larry.** See below. |

**The deploy is blocked, not skipped.** `npx convex deploy` against the live self-hosted
instance was denied by the permission classifier (the same guard that blocked 103-08's deploy
attempt). Everything is staged and pre-verified for it:
  - `--dry-run` against a forced local target confirmed `✔ No indexes are deleted by this push`
    and `Would have deployed Convex functions to http://127.0.0.1:3210` — additive, function-only.
  - A bare `npx convex deploy` may target the RETIRED CLOUD deployment; the target must be forced:
    `CONVEX_SELF_HOSTED_URL=http://127.0.0.1:3210` +
    `CONVEX_SELF_HOSTED_ADMIN_KEY=$(docker exec convex-backend ./generate_admin_key.sh | tail -1)`
  - After deploying, run the cleanup:
    `npx convex run activeEngine:pruneUnresolved '{}' --url http://127.0.0.1:3210 --admin-key <key>`
    It reports `{scanned, unresolvedFound, deleted, moreRemaining}`; re-run while `moreRemaining`.

**The sentinel row is actively REGENERATING**, which raises the deploy's priority above
"cleanup": mid-session it reappeared with a new `_id` and a later timestamp
(`selectionPath: "codepulse-default"`), i.e. Ástríðr emits unresolved `model_routing` events —
apparently on swap — so new sentinel rows keep being written until the ingest guard is deployed.
The frontend read guard keeps the UI honest regardless, which is why the badge still reads
"No brain reported" today.

### Outstanding after round 1 (superseded above)

1. **Convex deploy not performed.** The write-path half of the test-2 fix lives in
   `convex/runtimeIngest.ts` and only takes effect after deploying to the live self-hosted
   instance. Not done — that is a live-infra action for Larry to authorize. The frontend read-path
   guard already makes the UI honest without it.
2. **The existing sentinel row is still in `activeEngineSnapshots`** (1 row,
   `{profileId:"unknown", model:"unknown"}`). Inert now thanks to the read guard, but worth
   deleting once the ingest guard is deployed.
3. **Test 11 (failed-swap retry) still unverified** — inducing a live swap failure was not
   authorized. Note test 16 observed an adjacent real failure mode by accident, and it did NOT
   recover gracefully before the fix.
4. **Cosmetic raw-model-id leak not fixed** — deliberately left out of scope; see Cosmetic Notes.
5. Unrelated, blocking `/chat` at the time of re-verification: the concurrent phase-188 session's
   UNCOMMITTED `src/hooks/useAstridrVoice.ts` imports `reportRealtimeUsage` from
   `@/lib/astridrApi`, which does not export it — `SyntaxError` trips the page ErrorBoundary. Left
   untouched (another session's in-flight work). This is why test 9 was verified through the
   Settings host rather than the Chat pill.

## Tests

### 1. Cold Start Smoke Test
expected: Vite dev server on :5173 is DOWN right now (probe = 000); Convex :3210 and Ástríðr :8181 are up. Start the app from scratch, load the dashboard: boots without errors, header shows the Active Brain badge, dashboard shows live data. The new `GlobalSwapProvider` now wraps DashboardLayout's whole tree (103-18) and `activeEngineSnapshots` is a new Convex table (103-02) — a fresh boot is the honest test of both.
result: pass
evidence: |
  Corrected premise: :5173 was NOT down. Vite binds only to ::1, so the 127.0.0.1 probe returned
  000 while http://localhost:5173 returned 200 and the CodePulseUI supervisor task was running
  (PID 21724 since 16:32).
  Fresh boot on the ephemeral server: title "CodePulse", header renders, Active Brain badge
  present in the status cluster, 424 numeric tokens on the dashboard of which 369 non-zero (live
  data, not zeros), 0 page errors, 0 console errors on the dashboard route. GlobalSwapProvider
  mounting in DashboardLayout did not break the boot; activeEngineSnapshots resolves live.
  Noted, not a phase-103 defect: a fresh browser profile gets the first-run "Welcome to
  CodePulse" onboarding tour, whose full-screen overlay intercepts every pointer event until
  dismissed.

### 2. Header Badge Shows the Live Current Engine
expected: The Active Brain badge sits in the header status cluster (leftmost after the E-Stop button, before the notification bell). It shows a provider-color dot + the real engine name. If a global override is active it also shows a "Global" qualifier; if nothing has been reported and no override exists it honestly reads "No brain reported" rather than inventing a model. No dashed "STUB" chip (that only appears with VITE_BRAINS_STUB set).
result: issue
outcome: fixed
fix: "ddb51d96 — convex/activeEngineFilters.ts owns the guard once; the write path refuses to store an unresolved row and the read path refuses to trust ones already stored. Both halves mutation-checked."
fix_verified_live: "Badge reads 'No brain reported' at t+0/4/9s; aria-label 'Active brain: No brain reported'. Verified with the sentinel row STILL present in activeEngineSnapshots, proving the read-path guard alone restores honesty with no deploy."
reported: "With no global override in force the badge renders the literal sentinel string 'unknown' as if it were a real engine name — aria-label 'Active brain: unknown' — instead of the honest 'No brain reported', and lights the confirmed-live pulse on that fabricated reading."
severity: major
evidence: |
  Position and chips are correct: the badge is header button[3], immediately after E-Stop and
  before Notifications; no STUB chip. With an override active it reads correctly
  ('claude-sonnet-5' + GLOBAL, 'claude-haiku-4-5-20251001' + GLOBAL) — the defect is specific to
  the no-override fleet-wide fallback path.
  Reproduced 3 separate times (leg 1 recon, leg 3 sampling t+0..12s, leg 4 post-restore). The
  honest "No brain reported" flashes first and flips to "unknown" once the Convex query resolves.
  Mechanism traced live end-to-end, not inferred:
  1. `activeEngine:latestByProfile` returns exactly ONE row — {profileId:"unknown",
     model:"unknown", mode:"inherited", selectionPath:"default"} — verified twice via
     npx convex run against 127.0.0.1:3210, and still present after all testing.
  2. `convex/runtimeIngest.ts:535` coalesces a model_routing event's missing fields to the literal
     string "unknown", so the sentinel is written by the NORMAL live ingest path — not a stray
     manual probe row like the 103-08 incident, so deleting the row does not fix it.
  3. `useActiveEngine.ts:88-93`'s defensive join deliberately preserves a telemetry row whose
     profileId is absent from profileConfigs, so map["unknown"] = {model:"unknown"}.
  4. `BrainHeaderBadge` calls `useResolvedBrain()` with no profileId → the fleet-wide branch →
     `deriveMixedState`. The 3 real profiles are null and excluded, leaving the sentinel as the
     ONLY reported entry → distinctModels ["unknown"], length 1 → mixed:false → source:"profile".
  5. Badge renders baseLabel = "unknown"; since source==="profile" and the stub flag is off,
     `isConfirmedLive` is true — a server-confirmed pulse on a value meaning "nothing reported".
  Settings' Agent Profiles rows do NOT share this bug — they correctly read "Not reported" (test
  13), because they look up per-profile keys rather than the fleet-wide agreement.

### 3. Brain Picker Opens With the Live Catalogue
expected: Clicking the header badge opens a wide popover with a search box focused, rows grouped under provider/section headers, ~331 real engines from Ástríðr's live `swap.catalogue`. Each row shows a provider dot, a full non-truncated engine name, a billing chip, a health dot, and a quota bar (or an infinity label). No truncated names, no empty list.
result: issue
outcome: fixed
fix: "6afd67dd — the profile axis now says per-profile swapping isn't available yet and points at 'All profiles'; the global axis keeps the retry wording, where a failure genuinely is transient. Default scope deliberately NOT flipped to global, which would make the broader scope the default to solve a wording problem."
fix_verified_live: "'Per-profile swapping isn't available yet' present (1 match); the old 'try again in a moment' copy absent (0 matches) on the profile axis."
reported: "On open the picker defaults to 'This profile' scope, which dispatches the deferred models.catalog command and renders \"Couldn't load the brain catalogue — try again in a moment.\" with zero rows. The 331 live engines only appear after manually switching to 'All profiles'. The copy promises transience for a condition that is structurally permanent until astridr Phase 184.1 ships."
severity: minor
evidence: |
  Default scope on open = "This profile" (data-state this:on / all:off) → 0 rows, fetch-error text
  present, models.catalog frames sent and unaccepted by Ástríðr's command union (the documented
  184.1 deferral). Reproduced from all three entry points (header badge, Chat composer pill,
  Settings Swap button).
  After switching to "All profiles": 331 rows load from the live swap.catalogue — the catalogue
  itself works exactly as specified. Row anatomy verified on live rows, e.g.
  "Claude Sonnet 5 / API / ∞" with billing chip and unlimited-quota label; names not truncated;
  provider group headings present.
  The deferred per-profile axis failing is EXPECTED and is not counted against the phase. What is
  logged is that the picker opens onto that dead axis by default and blames transience.

### 4. Scope Selector — This Profile vs All Profiles
expected: The picker has an explicit "This profile" / "All profiles" scope toggle. Switching scope re-fetches the catalogue for that axis. Closing and reopening the picker resets the scope back to its default rather than remembering your last choice. Note: "This profile" cannot dispatch live yet — that axis is deliberately deferred to Ástríðr Phase 184.1 — so a per-profile attempt failing is expected, not a bug.
result: pass
evidence: |
  Both toggle items present. Initial state this:on / all:off. Clicking "All profiles" flips to
  this:off / all:on AND re-fetches — row count 0 → 331, confirming the catalogue is genuinely
  scope-sourced rather than reused across axes (the 103-11 scope-blind fix holds).
  Escape closes the popover (rows → 0); reopening resets to this:on / all:off, so D-08's
  reset-on-open rule holds.

### 5. Keyboard-Only Selection Reaches the Confirm Gate
expected: With "All profiles" scope selected, type into the search box to narrow the list, press ArrowDown to move the highlight, press Enter. Enter alone must NOT swap anything — it opens the "Swap all profiles to X?" confirm dialog. Keyboard and mouse selection behave identically. Nothing changes on the server until you click the explicit confirm button.
result: pass
evidence: |
  Typed "sonnet": 331 → 17 rows. ArrowDown moved the highlight to exactly one aria-selected row
  ("~Anthropic/Claude Sonnet Latest"). Enter opened the dialog titled "Swap all profiles to
  ~Anthropic/Claude Sonnet Latest?".
  The confirm gate held: 0 swap.set frames between starting the selection and the dialog opening;
  across the entire read-only leg, 31 WS frames sent (6 models.catalog, 4 swap.get_state,
  2 swap.catalogue) and swap.set count = 0. CR-02's keyboard path works; BSC-03's
  deliberate-confirm requirement re-confirmed live.

### 6. Confirm Modal Shows Honest Pinned-Default State (OBS 8 fix, 103-17)
expected: In the "Swap all profiles to X?" confirm dialog, the shadowing warning reports a real count AND names what is being shadowed — e.g. "3 profile(s) have a pinned default (Sonnet 5) that will be shadowed…" — for the 3 real profiles (consulting/business/personal) that each carry a configured `modelPreferences.primary`. A Pin icon appears on those rows. The "current engine" column still reads "Auto" for profiles with no live telemetry (that column is deliberately telemetry-only — it must NOT be back-filled from config). Before 103-17 this said pinnedCount 0 with no names; that is the defect being re-checked live.
result: pass
evidence: |
  OBS 8 is CLOSED — live-confirmed for the first time. Verbatim dialog text:
    "3 profiles have a pinned default (anthropic/claude-sonnet-5) that will be shadowed while
     this global override is in force."
  then rows: consulting / Auto / → / <target>, likewise business and personal. 3 Pin icons.
  Both halves hold at once: the count is 3 (was 0 pre-103-17) and it names the shadowed default,
  AND the current-engine column still reads "Auto" for all three — the D-14 boundary was NOT
  back-filled from config to make the count work. Reproduced on every confirm dialog opened
  during the run (4 separate opens).
  Cosmetic nit: names the raw model id "anthropic/claude-sonnet-5" rather than a display name like
  "Sonnet 5" — `configuredDefaultDisplayName` fell through to its raw-id fallback. See test 15's
  note; copy blemish, not an honesty failure.

### 7. Global Swap Dispatches and Reports a Server-Confirmed Result
expected: Clicking confirm fires exactly one real `swap.set`. The dialog shows a result only after the server's ack AND the `swap.state` readback agree with the requested target — no optimistic "switched". If the readback never arrives it falls back within ~4s to an honest "accepted, unconfirmed" reading, never a fabricated success or a fake failure. After the swap, the header badge and the Chat composer pill both show the new engine with the "Global" qualifier — all surfaces agree.
result: pass
evidence: |
  Confirm click sent EXACTLY 1 frame:
    {"type":"swap.set","target":"brain","value":"claude-sonnet-5","restore":false,...}
  Dialog then read "Switched to Claude Sonnet 5." — the `confirmed` status, i.e. the swap.state
  readback agreed with the requested target, not the "accepted, unconfirmed" fallback and not an
  optimistic pre-ack success. Repeated for Claude Haiku 4.5: 1 frame, "Switched to Claude Haiku
  4.5.". No gateway.model.set fan-out (contract §8 holds).
  Three-surface agreement re-confirmed live: header badge ('claude-sonnet-5', GLOBAL), Chat
  composer pill (chat-brain-pill-label 'claude-sonnet-5', chat-brain-pill-global-chip 'GLOBAL'),
  and the Chat Control Center BRAIN row ('claude-sonnet-5') all matched simultaneously.
  Cosmetic: the badge/pill show the raw model id ('claude-sonnet-5') while the dialog shows the
  display name ('Claude Sonnet 5') — inconsistent but not dishonest.

### 8. Revert Restores the Previous Engine (Not Just Clears It)
expected: After a successful swap, dismissing the dialog arms a "Revert global swap" toast action. Clicking it reopens the dialog, dispatches a real restore, and puts the engine back to the engine that was active BEFORE the swap — not simply cleared to Auto. The header badge follows the revert.
result: pass
evidence: |
  103-14 confirmed live by frame audit, which is the decisive evidence: with Haiku 4.5 as the
  prior override, swapping to Sonnet 5 then clicking "Revert global swap" dispatched
    {"type":"swap.set","target":"brain","value":"claude-haiku-4-5-20251001","restore":false,...}
  — a restore-to-PRIOR (value + restore:false), NOT the clear-to-Auto form (restore:true) that the
  pre-103-14 behavior would have sent.
  Dialog stayed visible throughout the revert (count 1), titled "Revert global swap" and reading
  "Reverted to claude-haiku-4-5-20251001." with "Profiles still governed by the global override:".
  Follow-up toast: "Reverted to claude-haiku-4-5-20251001." Badge followed: ('claude-haiku-4-5-
  20251001', GLOBAL). CR-03 therefore also still holds — the toast action found a live instance.
  Cosmetic: raw model id again instead of the display name.

### 9. Revert Survives Navigating Away From /chat (WR-01 fix, 103-18)
expected: Go to /chat, start an "All profiles" swap from the composer brain pill, confirm it, click Done (arming the revert toast), then navigate away from /chat (e.g. to the dashboard) WITHOUT clicking revert. Now click "Revert global swap" in the still-visible toast. The dialog must reopen with a visible result. Before 103-18 the command fired for real with zero UI feedback because the composer's modal had unmounted with the page. This is the fix's first live check.
result: pass
evidence: |
  Initially BLOCKED by test 15 — the picker crashed from every routed host, so no swap could be
  started from a page-scoped entry point at all. After fix a9ef7e16 this became reachable and
  PASSES: WR-01's first live verification.
  Exercised through Settings' per-profile "Swap" button rather than the Chat composer pill, because
  /chat was unrenderable at re-verification time due to the concurrent phase-188 session's
  uncommitted useAstridrVoice.ts import error (left untouched — another session's work). Settings'
  Swap button is the equivalent host for this invariant: also rendered inside the routed
  <Outlet/>, also page-scoped, also unmounted by a route change. The property under test —
  "a page-scoped host unmounts while the hoisted app-level modal survives" — is exercised exactly.
  Sequence and measurements:
    - 331 rows loaded from the page-scoped host, 0 Tooltip errors.
    - Keyboard activation (search "sonnet" -> 17 rows -> Enter on row 0, "Claude Sonnet 5"),
      with 0 swap.set frames before the explicit confirm.
    - Confirm -> exactly 1 frame, "Switched to Claude Sonnet 5.", badge ('claude-sonnet-5', GLOBAL).
    - Done -> revert toast armed.
    - Clicked "Dashboard" -> url became "/", unmounting the Settings host.
    - Clicked "Revert global swap" in the still-visible toast.
  >>> ASSERTION: the dialog REOPENED (count 1) with a real visible result — "Revert global swap /
      Global override cleared — profiles are back on their own defaults." — and exactly 1 real
      swap.set frame ({"restore":true}). Pre-103-18 this fired a real command into a dead component
      with zero UI feedback.
  Badge returned to ('No brain reported', '-'), so this run also left the live stack clean.
  Note: no override preceded this swap, so the revert took its CLEAR branch; the restore-to-prior
  branch is separately live-verified in test 8.

### 10. Reselecting the Same Brain Gets a Fresh Confirm (103-16)
expected: Complete a global swap to brain X, click Done, then reopen the picker and select brain X again. You should get a fresh "Swap all profiles to X?" confirm prompt — never the previous swap's stale result screen with a Done button.
result: pass
evidence: |
  Completed a global swap to Claude Sonnet 5, clicked Done, reopened the picker from the header
  badge and reselected Claude Sonnet 5. The dialog opened in the CONFIRM phase (Cancel button
  present, "Swap all profiles to Claude Sonnet 5" action present) with the full pinned-default
  warning and profile rows — not the previous swap's result screen.
  0 swap.set frames were dispatched across the reselect-then-Cancel sequence, so the fresh prompt
  is not achieved by re-firing anything. This is 103-16's success path, live-verified. Its
  failed-swap retry sub-case remains unverified (test 11).

### 11. Failed-Swap Retry Path (unit-covered only, never live-observed)
expected: Force a swap to fail (e.g. dispatch a deliberately invalid engine id, or take Ástríðr's WS down mid-swap), then reselect that same brain from the picker. The retry must genuinely re-dispatch — you get a fresh confirm prompt, not a frozen error screen with no way forward.
result: pass
evidence: |
  Initially skipped (inducing a failure was not authorized). Larry then authorized fixing
  everything, so this was exercised — and it PASSES, closing the phase's last
  unit-covered-only path.
  Induced the failure WITHOUT touching the live backend or taking Ástríðr down: Playwright's
  route_web_socket intercepted the Ástríðr socket, answered `swap.set` with a real error ack
  ("engine unavailable (induced for UAT test 11)"), and passed every other frame through
  untouched. The swap.set was never forwarded, so 0 frames reached the live backend and the
  badge stayed ('No brain reported', '-') throughout — a strictly better induction than
  stopping the backend.
  Failure leg:
    - Outcome line: "Failed — engine unavailable (...). Every profile is still on its prior
      engine." — honest, and explicitly states nothing changed.
    - Done ENABLED (test 16's guarantee holding on the error path too).
    - Badge unchanged — no optimistic switch.
    - Failure toast fired: "Swap to Claude Sonnet 5 failed — ...".
  Retry leg (the actual 103-16 failure-branch question):
    - Reselecting the SAME brain produced a FRESH confirm prompt (phase "confirm"), not the
      frozen error screen.
    - 0 frames dispatched merely by reselecting.
    - Clicking confirm on the retry dispatched 1 NEW frame >>> the retry genuinely re-dispatches.
  This run also surfaced a new defect, fixed in 025d7502: the result row-list header said
  "Profiles now governed by the global override:" on a FAILED swap, contradicting its own
  outcome line. It now reads "Profiles unchanged — still on their prior engine:".

### 12. Restore Usual Brain From the Control Center
expected: With a global override active, the Control Center's Brain control shows a "Restore usual brain" button (only when an override exists). Clicking it fires one command and clears the override back to Auto — the header badge, Chat pill, and this panel all agree afterwards.
result: pass
evidence: |
  The Control Center is a panel ON /chat (not a /control-center route). Its BRAIN row opens a
  popover containing "Restore usual brain", correctly gated on an active override: present (1)
  while Haiku was in force, and absent (0) after clearing.
  Clicking it dispatched exactly 1 frame: {"type":"swap.set","target":"brain","restore":true,...}
  — the clear form, one command, no fan-out. Badge dropped the GLOBAL chip immediately; reopening
  the popover showed the button gone. This was also the run's cleanup: it returned the live stack
  to the no-override state it started in.

### 13. Settings → Agent Profiles Rows Are Live
expected: Settings' Agent Profiles section lists the real profiles from live config (consulting/business/personal), each with a secondary line showing EITHER a session override (Clock icon + TTL) OR a pinned default (Pin icon) — never both at once. Each row has a "Swap" button that opens the brain picker, visually and functionally distinct from the existing "Edit" button. No stale `profileId / model` text from the old synced-config read.
result: pass
evidence: |
  My first two attempts wrongly reported the section missing — /settings is TABBED (General,
  Agents, LLM Providers, Notifications) and Agent Profiles lives under "Agents", which I had not
  clicked. Not a defect; my navigation error.
  On the Agents tab: "Agent Profiles" heading present, all 3 real profiles from live config
  (consulting, business, personal), each with its own "Swap" button AND "Edit" button — 3 of each,
  distinct controls. 0 matches for the stale "/ anthropic" model text, so the old
  synced-config read (`p.model`) is genuinely gone. No STUB chips (live mode). No error fallback.
  Secondary line reads "Not reported" per profile — the honest D-14 empty state, correct given
  activeEngineSnapshots holds no rows for real profiles. The session(Clock)/pinned(Pin) branch
  therefore could not be exercised live; only the honest-empty branch is confirmed.
  Note: these rows get the empty state RIGHT where the header badge gets it wrong (test 2).

### 14. Chat Composer Pill — Placement and Narrow Width
expected: On /chat, a brain pill row sits directly above the composer input row, showing a provider-color dot + the current engine name (distinct from the Brain icon the Control Center already uses on that page). Narrow the window to roughly 420px wide: the pill still fits and the send button stays fully visible and in-bounds — it must not be displaced or clipped.
result: pass
evidence: |
  Measured bounding boxes at both widths.
  1600px: pill at x=298,y=755 (30x20); send button at x=668,y=796 (44x44), right edge 712 ≤ 1600.
  420px:  pill at x=58,y=251 (30x20);  send button at x=339,y=292 (44x44), right edge 383 ≤ 420 —
          fully in-bounds, not clipped.
  At both widths the pill's y is above the send control's y, so it occupies its own row above the
  input rather than displacing the send affordance. OBS 9's regression watch-item holds.

### 15. Brain Picker Is Usable From Every Entry Point (found during this UAT)
expected: The brain picker should render the live catalogue and dispatch a swap from any of the three entry points the phase built — the header badge, the Chat composer pill, and Settings' per-profile Swap buttons.
result: issue
outcome: fixed
fix: "a9ef7e16 — BrainPicker now owns a local TooltipProvider around its PopoverContent, so it is safe from ANY host instead of requiring each host to remember (the documented CodeVaultGraph/SkillLifecycleMenu pattern). Two new tests render the picker with NO TooltipProvider ancestor — the host shape every other test in the file hid by wrapping its renders. Mutation-checked."
fix_verified_live: "Chat composer pill: 331 rows, 0 Tooltip errors, 0 'went wrong' (was: popover 1->0, 2 errors). Settings Swap: 331 rows, 0 Tooltip errors (was: popover 1->0, 2 errors). Header-badge control: 331 rows, 0 errors, unchanged. This also unblocked test 9."
reported: "The picker CRASHES whenever catalogue rows render from a host mounted inside the routed page — the Chat composer pill and Settings' Swap buttons. Switching to 'All profiles' destroys the popover, logs `Error: \\`Tooltip\\` must be used within \\`TooltipProvider\\``, and trips an ErrorBoundary. Only the header badge works. 2 of the phase's 3 swap entry points are unusable on the only axis that can actually dispatch."
severity: blocker
evidence: |
  Isolated with a control, which is what makes the mechanism certain rather than suspected:
    CASE 1 — /chat composer pill → click "All profiles":
      popover-content 1 → 0, rows 0, "went wrong" fallback appears, 2 TooltipProvider errors.
    CASE 2 (CONTROL) — dashboard header badge → click "All profiles":
      popover-content stays 1, rows 0 → 331, 0 TooltipProvider errors.
    CASE 3 — Settings (Agents tab) → a profile's "Swap" button → click "All profiles":
      popover-content 1 → 0, rows 0, 2 TooltipProvider errors.
  Verbatim error (full stack captured): "Error: `Tooltip` must be used within `TooltipProvider`
  at useContext2 ... at Tooltip (radix-ui.js)" followed by "ErrorBoundary caught: ...".
  Mechanism: `BrainPickerRow` wraps each row's button in a Radix `<Tooltip>` — that wrap is
  103-11's own WR-03 fix ("the Tooltip wraps the button itself"). Radix context follows the REACT
  tree, not the DOM, so a portaled popover inherits its host's providers. `BrainHeaderBadge` is
  rendered inside DashboardLayout's `<TooltipProvider delayDuration={300}>`
  (DashboardLayout.tsx:588-603) and is fine. The Chat pill and the Settings rows are rendered
  inside the routed `<Outlet/>`, which neither of DashboardLayout's TooltipProviders wraps — a
  boundary this repo has already been bitten by and documented verbatim at
  SkillLifecycleMenu.tsx:186-190 ("neither of DashboardLayout's TooltipProviders wraps the routed
  <Outlet /> — a bare <Tooltip> here throws at render time and the page ErrorBoundary blanks the
  whole page"), with the same local-provider workaround applied in CodeVaultGraph.tsx.
  Why the 2815-test suite missed it: unit tests render BrainPicker directly, and 103-18's own
  mechanical fix wrapped those renders in providers, so no test reproduces the routed-host
  provider boundary.
  Related and probably the same hunt: the working tree has an UNCOMMITTED change to
  BrainHeaderBadge.tsx adding a local TooltipProvider — redundant there (DashboardLayout already
  supplies one) but strongly suggesting this bug family was being chased in a prior session and
  patched on the wrong surface.
  This is what blocks test 9.

### 16. A Never-Settling Dispatch Must Not Trap the Operator (found during this UAT)
expected: If the swap command cannot be delivered, the confirm dialog should report an honest failure and remain dismissible.
result: issue
outcome: fixed
fix: "fc9828ff — both legs dispatch through one dispatchBounded() seam that always settles (rejection -> honest error; never-settling -> bounded by GLOBAL_SWAP_DISPATCH_TIMEOUT_MS = 15s, deliberately above AstridrWSContext's own 10s ACK_TIMEOUT_MS so the real ack timeout still wins when the socket is open), plus a finally that clears isBusy unconditionally. Bounded in the component, NOT in the shared sendCommand queue — making that queue reject globally would turn silent hangs into unhandled rejections in every other command panel that awaits it without a catch."
fix_verified_live: "3 new unit tests cover both failure shapes (never-settles, rejects) across both the swap and revert legs; mutation-checked — restoring runSwap's bare await fails exactly its 2 tests. The original hang did not recur in any live run after the fix, but since the trigger is intermittent that absence is NOT claimed as proof; the unit coverage is the real evidence."
reported: "Observed once: clicking confirm sent ZERO WS frames, and the dialog sat on 'Switching to Claude Haiku 4.5…' indefinitely with the Done button permanently disabled — no Cancel, no close X, no escape. The operator sees false in-flight progress for a command that never left the browser, and the only way out is a page reload."
severity: major
evidence: |
  Observed live in leg 1: "swap.set frames fired by this confirm: 0", dialog stuck on
  "Switching to Claude Haiku 4.5…", Done resolved to `<button disabled>` and stayed disabled
  through 30s of retries. Confirmed structurally in a later run that the result phase offers no
  alternative exit: Cancel count 0, close-X count 0 (GlobalSwapModal renders
  `showCloseButton={false}`).
  Did NOT reproduce on retry (the same swap then sent 1 frame and read "Switched to Claude Haiku
  4.5."), so the trigger is intermittent — most likely a WS reconnect window; the run recorded
  repeated OPEN/CLOSE cycles on 127.0.0.1:8181 across route changes.
  The code path is unambiguous regardless of trigger frequency:
  - `AstridrWSContext.tsx:396-404` — when the socket is not OPEN, sendCommand pushes the command
    onto a queue and returns a promise that is NEITHER resolved NOR rejected, with NO timeout. If
    the connection never returns, `await dispatch(...)` never settles.
  - `GlobalSwapModal.tsx:300-340` — `runSwap` sets phase="result"/outcome="pending" BEFORE
    dispatching, then `const ack = await dispatch(...)` with no try/catch and no finally. If the
    promise never settles (queued) or rejects ("Command timeout" / "Command queue full"),
    `setIsBusy(false)` never runs, so Done stays disabled forever. `runRevert` has the identical
    shape.
  Two consequences worth separating: (a) the operator is trapped in a modal showing false
  progress; (b) a queued command can still fire LATER when the socket returns — a real
  state-mutating swap landing with no visible surface, which is the same hazard class as WR-01
  reached through the queue instead of an unmount.

## Summary

total: 16
passed: 11
issues: 4
issues_fixed: 4
pending: 0
skipped: 0
blocked: 0

## Gaps

- truth: "BSC-01/BSC-04 — the current engine is shown honestly on every surface; a profile with no reported telemetry reads as unreported, never as a fabricated engine value"
  status: fixed
  reason: "User reported: With no global override in force the badge renders the literal sentinel string 'unknown' as a real engine name instead of 'No brain reported', and lights the confirmed-live pulse on that fabricated reading."
  severity: major
  test: 2
  root_cause: "convex/runtimeIngest.ts:535 coalesces a model_routing event's missing profileId/model to the literal string \"unknown\" and inserts it; useActiveEngine.ts:88-93's defensive join keeps that non-profile row in the map; deriveMixedState then sees it as the single agreed reading (the 3 real profiles being null are excluded), so resolveActiveBrain returns source:\"profile\" with model \"unknown\" and every fleet-wide surface renders the sentinel as a real, confirmed-live engine."
  artifacts:
    - path: "convex/runtimeIngest.ts:535"
      issue: "Coalesces missing profileId/model to the literal string \"unknown\" and writes it as a telemetry row, manufacturing a reading where none exists."
    - path: "src/hooks/useActiveEngine.ts:88-93"
      issue: "Defensive join preserves telemetry rows whose profileId is absent from profileConfigs — correct for a real profile racing the join, but it also admits the \"unknown\" sentinel."
    - path: "src/hooks/useResolvedBrain.ts:165-177"
      issue: "deriveMixedState/resolveActiveBrain treat the sentinel as a legitimate single agreed model, so source becomes \"profile\" rather than \"none\"."
  missing:
    - "Reject sentinel rows at the ingest boundary (skip the insert when profileId or model is unresolved rather than writing \"unknown\"), and/or filter them out of the read path so an unresolvable reading can never satisfy deriveMixedState."
    - "Do not light the confirmed-live pulse for a reading not attributable to a known profile."
    - "Delete the existing {profileId:\"unknown\"} row from activeEngineSnapshots once the ingest guard lands (batch-capped, per the self-hosted-Convex rules)."
  debug_session: ""

- truth: "BSC-02/BSC-04 — an operator can change the engine on the fly from the brain-swap surfaces this phase built"
  status: fixed
  reason: "User reported: The picker crashes whenever catalogue rows render from a host inside the routed page (Chat composer pill, Settings Swap buttons) — the popover is destroyed with `Tooltip` must be used within `TooltipProvider` and an ErrorBoundary trip. Only the header badge works."
  severity: blocker
  test: 15
  root_cause: "BrainPickerRow wraps each row's button in a Radix <Tooltip> (103-11's WR-03 fix). Radix context follows the React tree, so a portaled popover inherits its HOST's providers. BrainHeaderBadge sits inside DashboardLayout's TooltipProvider (DashboardLayout.tsx:588-603) and works; the Chat composer pill and Settings' AgentProfileRows are rendered inside the routed <Outlet/>, which neither DashboardLayout TooltipProvider wraps — the exact boundary already documented at SkillLifecycleMenu.tsx:186-190. Unit tests miss it because they render BrainPicker under their own providers."
  artifacts:
    - path: "src/components/brains/BrainPickerRow.tsx:179"
      issue: "Row button wrapped in a Radix <Tooltip> that requires a TooltipProvider ancestor in the React tree."
    - path: "src/layouts/DashboardLayout.tsx:588-603"
      issue: "The TooltipProvider covers only the header cluster, not the routed <Outlet/> — so only the header-badge host is safe."
    - path: "src/pages/Chat.tsx:154-215"
      issue: "BrainComposerPill mounts a BrainPicker inside the routed page, with no TooltipProvider ancestor."
    - path: "src/pages/Settings.tsx:832"
      issue: "AgentProfileRows mounts a BrainPicker inside the routed page, with no TooltipProvider ancestor."
  missing:
    - "Give BrainPicker (or BrainPickerRow) its own local TooltipProvider so it is safe from any host, mirroring the documented CodeVaultGraph.tsx / SkillLifecycleMenu.tsx pattern — one fix covers all present and future hosts."
    - "Alternatively hoist a single app-level TooltipProvider above the router outlet, which also retires the three existing local-provider workarounds."
    - "Add a regression test that renders BrainPicker with catalogue rows and NO TooltipProvider ancestor — the current suite cannot catch this."
    - "Decide the fate of the uncommitted, redundant TooltipProvider wrap in BrainHeaderBadge.tsx."
  debug_session: ""

- truth: "BSC-04 — honest live status: no state-mutating command fires with zero visible operator feedback, and the operator is never shown optimistic progress"
  status: fixed
  reason: "User reported: Observed once — clicking confirm sent zero WS frames and the dialog sat on 'Switching to…' indefinitely with Done permanently disabled, no Cancel and no close X; only a page reload escapes."
  severity: major
  test: 16
  root_cause: "AstridrWSContext.tsx:396-404 queues a command when the socket is not OPEN and returns a promise that is neither resolved nor rejected and carries no timeout. GlobalSwapModal.tsx:300-340's runSwap (and runRevert identically) sets phase=result/outcome=pending BEFORE awaiting dispatch, with no try/catch and no finally, so a never-settling or rejecting dispatch leaves isBusy true forever — and the result phase renders no Cancel and showCloseButton={false}. A queued command may also fire later with no visible surface."
  artifacts:
    - path: "src/contexts/AstridrWSContext.tsx:396-404"
      issue: "Queued commands get a promise with no timeout and no rejection path; if the socket never reopens the caller awaits forever."
    - path: "src/components/brains/GlobalSwapModal.tsx:300-340"
      issue: "runSwap/runRevert await dispatch with no try/catch/finally, so setIsBusy(false) is skipped on a hang or rejection, disabling the only exit control."
    - path: "src/components/brains/GlobalSwapModal.tsx:397"
      issue: "showCloseButton={false} plus no Cancel in the result phase means a stuck dialog has no escape hatch."
  missing:
    - "Wrap the dispatch in try/catch/finally so isBusy always clears and a rejection renders an honest error outcome."
    - "Bound the queued-command path with a timeout (or reject immediately when disconnected) so a dispatch can never hang indefinitely."
    - "Keep a dismissal affordance reachable in the result phase, or gate the pending state on a bounded timer."
    - "Decide whether a queued global swap should fire at all on reconnect, given it would land with no visible surface (same hazard class as WR-01)."
  debug_session: ""

- truth: "BSC-04 — honest live status: the operator is never shown a failure message that misrepresents why the failure happened"
  status: fixed
  reason: "User reported: On open the picker defaults to 'This profile' scope, which dispatches the deferred models.catalog command and renders \"Couldn't load the brain catalogue — try again in a moment.\" with zero rows; the 331 live engines only appear after manually switching to 'All profiles'."
  severity: minor
  test: 3
  root_cause: "BrainPicker's initial scope state is \"profile\", and the per-profile catalogue source (models.catalog) is absent from Ástríðr's accepted command union until astridr Phase 184.1 — so the default-scope fetch can only ever fail, and the generic fetch-error copy attributes a permanent structural gap to a transient one."
  artifacts:
    - path: "src/components/brains/BrainPicker.tsx (initial scope state; fetchError copy ~line 500)"
      issue: "Default scope lands on the deferred axis; \"try again in a moment\" implies transience for a condition that cannot resolve until an external phase ships."
  missing:
    - "Default the picker's scope to \"global\" while the per-profile axis is deferred, or replace the generic fetch-error copy on the profile axis with an honest 'per-profile swapping isn't available yet' message."
  debug_session: ""

## Deferred / Not Counted

- Per-profile axis of BSC-01/02/04/05 (models.catalog, real gateway.model.set, per-profile
  telemetry ingest) — astridr Phase 184.1. Re-confirmed live this run: models.catalog frames are
  dispatched and not accepted. Not counted as a phase-103 defect, per the phase's own deferral.

## Cosmetic Notes (not logged as gaps)

- Raw model ids leak into user-facing copy where a display name exists: the shadowing warning says
  "anthropic/claude-sonnet-5", the badge/pill say "claude-sonnet-5", and the revert result says
  "Reverted to claude-haiku-4-5-20251001" — while the dialog titles correctly say "Claude Sonnet 5"
  / "Claude Haiku 4.5". One display-name resolution helper applied consistently would fix all four.
- A fresh browser profile gets the first-run onboarding tour, whose overlay blocks all interaction
  until skipped. Expected, but it does gate any automated UAT.
