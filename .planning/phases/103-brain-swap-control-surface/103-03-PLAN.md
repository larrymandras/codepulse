---
phase: 103-brain-swap-control-surface
plan: 03
type: execute
wave: 2
depends_on: ["103-01", "103-02"]
files_modified:
  - src/hooks/useActiveEngine.ts
  - src/hooks/useActiveEngine.test.ts
  - src/components/brains/BrainPickerRow.tsx
  - src/components/brains/BrainPickerRow.test.tsx
autonomous: true
requirements: [BSC-01]
user_setup: []

must_haves:
  truths:
    - "D-01: the reactive hook is keyed by profileId, sourced from profileConfigs and the active-engine telemetry table, never from agents.agentId"
    - "D-14: the hook returns only what the server reported; it computes no engine value of its own"
    - "D-07: each picker row renders color dot, engine name, billing chip, health dot and quota bar"
    - "D-02: a row and an engine reading distinguish a session override from a pinned default and never present both as current"
    - "The health and quota thresholds are re-tokenized to --status-* CSS vars, not copied as Tailwind color literals"
    - "A long model name wraps instead of truncating, and the hook never returns undefined"
  artifacts:
    - path: "src/hooks/useActiveEngine.ts"
      provides: "Per-profile active-engine map + mixed-state derivation"
      exports: ["useActiveEngine", "deriveMixedState"]
    - path: "src/components/brains/BrainPickerRow.tsx"
      provides: "One catalogue row: dot, name, billing chip, health dot, quota bar"
      min_lines: 60
  key_links:
    - from: "src/hooks/useActiveEngine.ts"
      to: "api.activeEngine.latestByProfile"
      via: "useQuery with a non-undefined fallback"
      pattern: "activeEngine\\.latestByProfile"
    - from: "src/components/brains/BrainPickerRow.tsx"
      to: "src/lib/providers.ts"
      via: "PROVIDER_COLORS / PROVIDER_BILLING identity lookup"
      pattern: "PROVIDER_COLORS"
---

<objective>
Build the two reusable primitives every brain surface composes: the reactive per-profile engine
hook (BSC-01's read half) and the picker row component (D-07's row anatomy).

Purpose: the header badge, the Settings row, the composer pill and the picker all read the same
hook, so they cannot disagree with themselves; and the row's health/quota threshold logic is
re-tokenized once here rather than being copy-pasted as hardcoded colors into four places.

Output: `useActiveEngine` + tests, `BrainPickerRow` + tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/103-brain-swap-control-surface/103-CONTEXT.md
@.planning/phases/103-brain-swap-control-surface/103-RESEARCH.md
@.planning/phases/103-brain-swap-control-surface/103-PATTERNS.md
@.planning/phases/103-brain-swap-control-surface/103-UI-SPEC.md
@.planning/phases/103-brain-swap-control-surface/103-CONTRACT.md
@.planning/phases/103-brain-swap-control-surface/103-01-SUMMARY.md
@.planning/phases/103-brain-swap-control-surface/103-02-SUMMARY.md
</context>

<interfaces>
<!-- Hook idiom to copy — src/hooks/useProviderHealth.ts (full file):                         -->
<!--   export function useProviderHealth() {                                                   -->
<!--     return useThrottledQuery(api.providerHealth.latest, {}, 5000) ?? {};                   -->
<!--   }                                                                                       -->
<!-- src/hooks/useProfileConfigs.ts (full file):                                                -->
<!--   export function useProfileConfigs() { return useQuery(api.profiles.listConfigs) ?? []; } -->

<!-- Contract types available from 103-01: CatalogueEntry { id, name, vendor, group,           -->
<!--   billing, costTier, quotaRemainingPct?, health? }; ActiveEngine { model, mode,            -->
<!--   selectionPath?, expiresAt?, timestamp }.                                                 -->

<!-- Threshold logic to RE-TOKENIZE (do NOT copy the literals) —                                -->
<!-- src/components/ProviderHealthPanel.tsx:45-51 uses bg-red-500 / bg-yellow-500 /             -->
<!-- bg-emerald-500 at <5% / <20% / else. Keep the thresholds, replace the colors with          -->
<!-- bg-(--status-error) / bg-(--status-warn) / bg-(--status-ok).                               -->
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: useActiveEngine reactive hook (103-03-T1)</name>
  <files>src/hooks/useActiveEngine.ts, src/hooks/useActiveEngine.test.ts</files>
  <read_first>
    - src/hooks/useProviderHealth.ts (full file — the `useQuery(...) ?? {}` wrapper idiom)
    - src/hooks/useProfileConfigs.ts (full file — the profileConfigs reader this hook joins against)
    - src/hooks/useThrottledQuery.ts (decide throttle vs plain useQuery)
    - convex/activeEngine.ts (written in 103-02 — the `latestByProfile` return shape)
    - convex/profiles.ts (lines 188-197 — `listConfigs`, the real populated per-profile source; and the line-113 comment stating `agentProfiles` has zero rows)
    - src/lib/brainsFixtures.ts (written in 103-01 — `STUB_PROFILE_ENGINES`, the ≥2-distinct-engine fixture)
  </read_first>
  <behavior>
    - Test: with a query result covering two profiles on different models, `deriveMixedState` reports mixed=true and returns both distinct model values.
    - Test: with all profiles on the same model, `deriveMixedState` reports mixed=false and returns that single model.
    - Test: with an empty query result, `deriveMixedState` reports mixed=false and returns no engine — never throws, never returns undefined.
    - Test: `useActiveEngine` returns an empty object (not `undefined`) while the query is loading.
    - Test: a profile present in `profileConfigs` but absent from the telemetry table appears in the map with a null/unknown engine, not as a missing key — the surface must be able to say "not reported" instead of guessing.
  </behavior>
  <action>
Create `src/hooks/useActiveEngine.ts` following `useProviderHealth.ts`'s shape exactly.

- `useActiveEngine()` reads `api.activeEngine.latestByProfile` and MUST coalesce to a
  non-`undefined` value (`?? {}`). This is load-bearing: the header badge built in 103-06 renders
  on every page via `DashboardLayout`, and a throwing or undefined `useQuery` here unmounts the
  React tree and blanks the whole dashboard.
- It joins the telemetry rows against `useProfileConfigs()` (backed by `profileConfigs`, the real
  populated table) so every known profile has an entry. Do NOT source the profile list from
  `agentProfiles` — that table has zero rows in production per `convex/profiles.ts:113`.
- Export a separate PURE function `deriveMixedState(engines)` returning at minimum
  `{ mixed: boolean, distinctModels: string[], single?: ActiveEngine }`. Keeping it pure and
  exported is what makes the Mixed-brains behavior testable without a Convex runtime.
- Return per profile the server-reported `model`, `mode`, `expiresAt` and `timestamp` unchanged.
  The hook computes NO engine value of its own (D-14) and performs no fallback to
  `profileConfigs.modelPreferences` — an unreported profile reads as unknown, not as its config
  field. Reporting a config value as if it were live state is precisely the v9.0 VitalsRail trap
  BSC-01 exists to remove.
- Use a plain `useQuery` rather than `useThrottledQuery`: swap state must feel closer to real time
  than provider-health polling. Note the choice in a comment.

Then write `src/hooks/useActiveEngine.test.ts` covering every behavior above, testing
`deriveMixedState` directly as a pure function and mocking `convex/react`'s `useQuery` for the
hook-level cases (follow the existing hook-test mocking style in this repo).
  </action>
  <verify>
    <automated>npx vitest run src/hooks/useActiveEngine.test.ts &amp;&amp; npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `src/hooks/useActiveEngine.ts` exports `useActiveEngine` and `deriveMixedState`.
    - `grep -c 'agentProfiles' src/hooks/useActiveEngine.ts` returns 0.
    - `grep -c 'modelPreferences' src/hooks/useActiveEngine.ts` returns 0 — no fallback to the config field.
    - The `useQuery` call is coalesced with `??` so the hook can never return `undefined`.
    - `npx vitest run src/hooks/useActiveEngine.test.ts` passes with at least 5 tests, including the two-distinct-models mixed case and the empty case.
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>Every brain surface can read one shared, server-reported, never-undefined per-profile engine map.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: BrainPickerRow with tokenized health and quota (103-03-T2)</name>
  <files>src/components/brains/BrainPickerRow.tsx, src/components/brains/BrainPickerRow.test.tsx</files>
  <read_first>
    - src/components/ProviderHealthPanel.tsx (lines 1-60 — the row composition to imitate AND the hardcoded color literals at 8-12 / 29-35 / 45-51 that must NOT be carried forward)
    - src/lib/providers.ts (full file — PROVIDER_COLORS, PROVIDER_BILLING, PROVIDER_DISPLAY_NAMES, GATEWAY_PROVIDERS)
    - src/hooks/useProviderHealth.ts (the health source)
    - src/components/ui/progress.tsx (the shadcn primitive to compose — do NOT hand-roll a div bar)
    - src/components/ui/badge.tsx and src/components/ui/tooltip.tsx
    - src/components/control-center/BrainControl.tsx (lines 260-285 — the non-truncating wrapping `<button>` row markup; and its docstring recording the three live checkpoint rounds)
    - src/components/control-center/BrainControl.test.tsx (lines 200-235 — the "never truncates" regression test to copy)
    - .planning/phases/103-brain-swap-control-surface/103-UI-SPEC.md (§3 row anatomy, § Color, § Typography, § Spacing)
  </read_first>
  <behavior>
    - Test: a row with `quotaRemainingPct` of 0.5 renders the quota indicator with the `--status-ok` token; 0.10 renders `--status-warn`; 0.02 renders `--status-error`.
    - Test: a `billing: "sub"` row with no `quotaRemainingPct` renders the infinity label instead of a Progress bar.
    - Test: the billing chip renders the literal text "API" or "SUB" and carries no accent or status color class.
    - Test: the health dot renders with a `--status-*` token and its tooltip content contains the health status word ("reachable" / "degraded" / "unreachable").
    - Test (regression guard, copied from BrainControl.test.tsx): a row whose engine name is very long renders the full name and the row element carries no `truncate` class.
    - Test: an `costTier: "expensive"` row renders its confirm affordance only after being activated, not on first render.
  </behavior>
  <action>
Create `src/components/brains/BrainPickerRow.tsx`, a presentational row for one `CatalogueEntry`.
Props at minimum: the entry, the current selection state, an `onSelect` callback, and an
`isExpanded`/`onExpandChange` pair for the expensive-tier inline confirm. It owns no dispatch
logic — the picker in 103-05 owns that.

Layout, left to right, per UI-SPEC §3: an 8px provider-color dot sourced from `PROVIDER_COLORS`
(the one documented hex exception — reuse the const, never invent a second identity-color source);
the engine display name at Body 14px, `flex-1`; a billing chip; a 6px health dot; a quota bar.

Hard requirements carried over from `BrainControl.tsx`'s three live operator checkpoint rounds —
re-learning these costs another checkpoint round, so treat them as fixed:
- The row is a `<button>` with `whitespace-normal break-words`. It must NEVER carry `truncate`.
  The real catalogue is ~300+ entries with long model names.
- Copy the "never truncates" regression test from `BrainControl.test.tsx` lines 209-228 verbatim,
  adapting only the selector.

Billing chip: `Badge variant="outline"` at `text-xs uppercase px-1 py-0` reading "API" or "SUB",
with a neutral `--border` color. Never accent, never a status color — billing is identity
metadata, not a state signal. Do NOT use `text-[10px]`; `text-xs` is the smallest declared size.

Health dot: sourced from `useProviderHealth()`, rendered with `bg-(--status-ok)` /
`bg-(--status-warn)` / `bg-(--status-error)`. RE-TOKENIZE `ProviderHealthPanel.tsx:45-51`'s
threshold logic — keep the thresholds (at or above 20% ok, 5-20% warn, below 5% error), replace
`bg-green-500` / `bg-yellow-500` / `bg-red-500` / `bg-emerald-500` with the CSS-var tokens.
Copying the literals violates CLAUDE.md § Styling. Color must never be the sole differentiator:
wrap the dot in a `Tooltip` revealing the Label-typography status word.

Quota bar: compose the shadcn `Progress` primitive at `h-1 w-10` — do NOT hand-roll the div bar
`GatewayQuotaPanel` uses. Subscription-billed rows render a static full-width `--status-ok` bar or
the mono infinity label instead.

Expensive/unknown-tier inline confirm (UI-SPEC §3/§11): when the row's tier is `"expensive"` or
`"unknown"`, activation expands the row in place rather than firing — border becomes
`--status-warn`, the confirm copy from the UI-SPEC Copywriting Contract renders, with a ghost
`Cancel` and a `Confirm swap` button (never a bare "Confirm"). Normal-tier rows fire immediately.

Then write `src/components/brains/BrainPickerRow.test.tsx` covering every behavior above, using
`STUB_CATALOGUE` from `src/lib/brainsFixtures.ts` — the fixture already carries the expensive,
unknown, subscription and low-quota entries these tests require.
  </action>
  <verify>
    <automated>npx vitest run src/components/brains/BrainPickerRow.test.tsx &amp;&amp; npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE 'bg-(green|red|yellow|emerald|gray)-[0-9]' src/components/brains/BrainPickerRow.tsx` returns 0.
    - `grep -c 'truncate' src/components/brains/BrainPickerRow.tsx` returns 0.
    - `grep -c 'text-\[10px\]' src/components/brains/BrainPickerRow.tsx` returns 0.
    - `grep -c 'PROVIDER_COLORS' src/components/brains/BrainPickerRow.tsx` returns at least 1.
    - The file imports `Progress` from `@/components/ui/progress`.
    - `npx vitest run src/components/brains/BrainPickerRow.test.tsx` passes with at least 6 tests, including the three quota-threshold cases and the never-truncates regression guard.
    - A test asserts the expensive-tier row does NOT show its confirm affordance on first render and DOES after activation — the assertion checks rendered content, not merely that a click did not throw.
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>One row component renders D-07's full anatomy with tokenized status colors and non-truncating names.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Convex query → dashboard-wide React tree | A throwing or undefined query here blanks every page rendering the badge |
| catalogue payload → row render | Server-supplied names/ids render into the DOM |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-103-09 | Denial of Service | `useActiveEngine` | mitigate | `useQuery(...) ?? {}` coalescing so the hook can never return `undefined`; consumers in 103-06/07 additionally wrap in `SectionErrorBoundary` |
| T-103-10 | Information Disclosure (of a wrong state) | engine reading fallback | mitigate | The hook performs NO fallback to `profileConfigs.modelPreferences`; an unreported profile reads as unknown rather than showing a config value as live state |
| T-103-11 | Tampering | catalogue-supplied display strings | accept | React escapes text content by default; rows render text only, no `dangerouslySetInnerHTML` |
| T-103-SC | Tampering | npm/pip/cargo installs | accept | Zero new packages this phase; all primitives already installed |
</threat_model>

<verification>
- `npx vitest run src/hooks/useActiveEngine.test.ts src/components/brains/BrainPickerRow.test.tsx` green.
- `npx tsc --noEmit` clean.
- No hardcoded Tailwind status color literals in the new row component.
</verification>

<success_criteria>
- One shared reactive engine map that never returns undefined and never invents a value.
- One row component whose thresholds are tokenized and whose names never truncate.
</success_criteria>

<output>
Create `.planning/phases/103-brain-swap-control-surface/103-03-SUMMARY.md` when done
</output>
