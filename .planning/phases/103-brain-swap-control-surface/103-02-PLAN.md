---
phase: 103-brain-swap-control-surface
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - convex/schema.ts
  - convex/activeEngine.ts
  - convex/activeEngine.test.ts
  - convex/runtimeIngest.ts
autonomous: false
requirements: [BSC-01]
user_setup: []

must_haves:
  truths:
    - "D-01: the swap unit is the profile — the active-engine table is keyed by profileId, never by agentId"
    - "D-14: the per-profile active engine comes from server-reported telemetry landing in Convex, never from a client assertion or an ack payload"
    - "D-03: this table holds the live resolved engine only; the persisted default stays Ástríðr-owned and is never written here"
    - "An operator's per-profile engine reading is reactive from a Convex table fed by astridr telemetry, not a stale config field"
    - "The new table exists on the live self-hosted Convex instance, not merely in the TypeScript types"
  artifacts:
    - path: "convex/schema.ts"
      provides: "activeEngineSnapshots table definition"
      contains: "activeEngineSnapshots"
    - path: "convex/activeEngine.ts"
      provides: "latest-per-profile query + ingest write mutation"
      exports: ["deduplicateByProfile", "latestByProfile", "recordRouting"]
    - path: "convex/runtimeIngest.ts"
      provides: "model_routing event case routing into activeEngine"
      contains: "model_routing"
  key_links:
    - from: "convex/runtimeIngest.ts"
      to: "api.activeEngine.recordRouting"
      via: "case \"model_routing\" in the existing event switch"
      pattern: "activeEngine\\.recordRouting"
    - from: "convex/activeEngine.ts"
      to: "activeEngineSnapshots"
      via: "indexed query + dedup-latest-per-profile"
      pattern: "activeEngineSnapshots"
---

<objective>
Build the Convex-reactive substrate that makes BSC-01 structurally true for the per-profile axis:
a new append-only snapshot table fed by an astridr telemetry event, read as latest-per-profile.

Purpose: the UI must be a READER of server-reported engine state (D-14). Without this table
every downstream surface would have to assert the engine itself — exactly the v9.0 VitalsRail
stale-read trap BSC-01 exists to kill.

Output: `activeEngineSnapshots` table, `convex/activeEngine.ts`, a `model_routing` ingest case,
unit tests, and a schema push landed on the live self-hosted instance.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/103-brain-swap-control-surface/103-CONTEXT.md
@.planning/phases/103-brain-swap-control-surface/103-RESEARCH.md
@.planning/phases/103-brain-swap-control-surface/103-PATTERNS.md
@.planning/phases/103-brain-swap-control-surface/103-VALIDATION.md
</context>

<interfaces>
<!-- The exact table shape to imitate — convex/schema.ts:1540-1551 (gatewayQuotaSnapshots): -->
<!-- defineTable({ provider, billingType, usedToday, dailyLimit?, spendUsd, spendCapUsd?,      -->
<!--   remainingPct, timestamp }).index("by_provider", ["provider","timestamp"])                -->
<!--   .index("by_timestamp", ["timestamp"])                                                     -->

<!-- The dedup + latest query idiom — convex/gatewayQuota.ts:15-25 and :123-134:                -->
<!-- deduplicateByProvider<T extends {provider,timestamp}>(rows): keeps first-seen per key       -->
<!-- latestByProvider: query .withIndex("by_timestamp").order("desc").take(100) then dedupe      -->

<!-- The ingest case idiom — convex/runtimeIngest.ts:512 (profile_config) and :835              -->
<!-- (provider_health): dual snake/camelCase coalescing, e.g.                                    -->
<!-- d.profileId ?? d.profile_id ?? "unknown", then ctx.runMutation(api.X.y, {...}), then break. -->

<!-- Contract shape from 103-CONTRACT.md §4 (model_routing):                                    -->
<!-- profile_id, model, mode: "session"|"pinned"|"inherited", selection_path, expires_at?, timestamp -->
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the activeEngineSnapshots table and convex/activeEngine.ts (103-02-T1)</name>
  <files>convex/schema.ts, convex/activeEngine.ts</files>
  <read_first>
    - convex/schema.ts (lines 500-520 for `profileConfigs`, lines 1535-1555 for `gatewayQuotaSnapshots` — the verbatim shape to follow)
    - convex/gatewayQuota.ts (full file — `deduplicateByProvider` at 15-25, `insertSnapshot` at 103-117, `latestByProvider` at 123-134)
    - convex/profiles.ts (lines 84-148 — `personaConfigChangeKey` + `upsertConfig`, including the comment at line 113 stating agentProfiles has zero rows; and `listConfigs` at 188-197)
    - .planning/phases/103-brain-swap-control-surface/103-CONTRACT.md (§4 active-engine telemetry event — the field names this table must carry)
    - CLAUDE.md (§ "Self-Hosted Convex — Operational Rules")
  </read_first>
  <behavior>
    - `deduplicateByProfile` keeps the FIRST row seen per `profileId` (rows arrive newest-first from a descending index scan), so the result is latest-per-profile.
    - `latestByProfile` returns an object/array covering every profile that has ever reported, with no duplicates.
    - `recordRouting` inserts one append-only row; it never patches or deletes an existing row.
  </behavior>
  <action>
Append a new `activeEngineSnapshots` table to `convex/schema.ts`, modeled field-for-field on
`gatewayQuotaSnapshots` but keyed by profile: `profileId: v.string()`, `model: v.string()`,
`mode: v.string()` (contract values `"session" | "pinned" | "inherited"` — keep it `v.string()`
to match the defensive-boundary convention used across this schema, and validate at the ingest
edge instead), `selectionPath: v.optional(v.string())`, `expiresAt: v.optional(v.float64())`,
`timestamp: v.float64()`. Add two indexes exactly mirroring the analog:
`.index("by_profileId", ["profileId", "timestamp"])` and `.index("by_timestamp", ["timestamp"])`.
This is a PURE ADDITIVE schema change — do not modify or reindex any existing table.

Create `convex/activeEngine.ts` as a near-verbatim rename of `convex/gatewayQuota.ts`'s shape:

- `export function deduplicateByProfile<T extends { profileId: string; timestamp: number }>(rows: T[]): T[]`
  — a straight rename of `deduplicateByProvider`, keeping the same first-seen-wins Map logic.
  Export it so it is unit-testable in isolation (the `personaConfigChangeKey` precedent in
  `profiles.ts`).
- `export const latestByProfile = query({ args: {}, ... })` — query `activeEngineSnapshots`
  `.withIndex("by_timestamp").order("desc").take(200)` then `deduplicateByProfile`. Use 200 rather
  than gatewayQuota's 100 because the profile cardinality is higher than the provider cardinality.
  Carry forward the same "current-only, no history" comment `gatewayQuota.ts:121` has.
- `export const recordRouting = mutation({ ... })` — a plain insert of one row. Per D-14 the UI
  must NEVER call this to assert an engine; add a comment saying so explicitly. Use `mutation`
  (not `internalMutation`) only if the ingest path requires it — check how
  `runtimeIngest.ts` calls its sibling mutations and match that call convention exactly.

Do NOT write a `configChanges` audit row here — the audit belongs to the persisted-default write
path in `profiles.upsertConfig`, and this table holds the separate live-telemetry axis (D-03/D-14).
Do NOT touch `agentProfiles` — it has zero rows and is not the real path.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'activeEngineSnapshots' convex/schema.ts` returns at least 1.
    - The table definition carries `profileId`, `model`, `mode`, `timestamp` and the two named indexes.
    - `convex/activeEngine.ts` exports `deduplicateByProfile`, `latestByProfile`, `recordRouting`.
    - `grep -c 'configChanges' convex/activeEngine.ts` returns 0.
    - `grep -c 'agentProfiles' convex/activeEngine.ts` returns 0.
    - No existing table definition in `convex/schema.ts` is modified (`git diff convex/schema.ts` shows only additions).
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>The reactive table and its latest-per-profile query exist in source, additively, with no audit-write duplication.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: [BLOCKING] Push the Convex schema to the live self-hosted instance (103-02-T2)</name>
  <files>convex/schema.ts</files>
  <read_first>
    - CLAUDE.md (§ "Self-Hosted Convex — Operational Rules" — the hard prohibitions)
    - convex/schema.ts (the table added in Task 1)
    - .planning/phases/103-brain-swap-control-surface/103-VALIDATION.md (§ "Wave 0 Requirements" schema-push note)
  </read_first>
  <what-built>
    Task 1 added the `activeEngineSnapshots` table to `convex/schema.ts`. Convex generates its
    TypeScript types FROM that file, so `npm run build` and `npx tsc --noEmit` pass whether or
    not the table exists on the live instance. That is a false-positive verification state: every
    downstream reactive-path claim in Plans 103-03, 103-06, 103-07 and 103-08 would be green
    against a table that does not exist. This checkpoint closes that gap before any of them run.
  </what-built>
  <action>
Run `npx convex deploy --yes` against the self-hosted deployment to land the additive schema
change. This is a pure table addition — it creates a new empty table and touches no existing rows.

HARD PROHIBITIONS (CLAUDE.md § Self-Hosted Convex): never run
`npx convex import --replace-all` against the live instance; never bulk-delete or bulk-patch a
large table. Both have taken this deployment down for days. If the deploy errors, STOP and report
— do not attempt a data-side workaround.

After the deploy returns, confirm the table is genuinely present on the instance (not just in the
generated types) by running a query against it — e.g. `npx convex run activeEngine:latestByProfile`
— and confirming it returns an empty result rather than an unknown-table error.
  </action>
  <how-to-verify>
    1. Run `npx convex deploy --yes` from `C:\Users\mandr\codepulse` and confirm it completes without error.
    2. Run `npx convex run activeEngine:latestByProfile` and confirm it returns an empty array/object — NOT a "table not found" or "unknown function" error.
    3. Confirm no existing table was dropped or reindexed: the deploy output must list only an addition.
  </how-to-verify>
  <verify>
    <automated>npx convex run activeEngine:latestByProfile</automated>
  </verify>
  <acceptance_criteria>
    - `npx convex deploy --yes` exits 0.
    - `npx convex run activeEngine:latestByProfile` returns an empty result, proving the function and table resolve on the live instance.
    - No `import --replace-all`, bulk delete, or bulk patch was run.
    - Deploy output shows an additive schema change only.
  </acceptance_criteria>
  <resume-signal>Type "approved" once the deploy has landed and the live query returned an empty result, or describe the error.</resume-signal>
  <done>The reactive table exists on the live self-hosted instance; downstream reactive-path verification is no longer a false positive.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wire the model_routing ingest case and test the dedup path (103-02-T3)</name>
  <files>convex/runtimeIngest.ts, convex/activeEngine.test.ts</files>
  <read_first>
    - convex/runtimeIngest.ts (lines 120-145 for the batch-poisoning comment, the `case "profile_config"` block near 512, and the `case "provider_health"` block near 835 — the dual-coalescing convention)
    - convex/activeEngine.ts (written in Task 1)
    - convex/profiles.ts (lines 84-90 — the exported-pure-helper testing idiom)
    - .planning/phases/103-brain-swap-control-surface/103-CONTRACT.md (§4 — the exact event field names, snake_case on the wire)
  </read_first>
  <behavior>
    - Test: `deduplicateByProfile` given rows for two profiles in descending-timestamp order returns exactly one row per profile, and the one it returns is the newest.
    - Test: `deduplicateByProfile` given a single profile with three snapshots returns one row.
    - Test: `deduplicateByProfile` given an empty array returns an empty array (the badge must never receive `undefined`).
    - Test: given a fixture with two profiles on DIFFERENT models, the dedup result contains two distinct `model` values — the data precondition the "Mixed brains" badge depends on.
  </behavior>
  <action>
Add a `case "model_routing"` to the existing event switch in `convex/runtimeIngest.ts`, alongside
the roughly forty cases already there. Do NOT create a second ingest endpoint. Follow the exact
convention of the neighbouring cases: read the payload defensively with dual snake/camelCase
coalescing (`d.profileId ?? d.profile_id ?? "unknown"`, `d.selectionPath ?? d.selection_path`,
`d.expiresAt ?? d.expires_at`), default `mode` to `"inherited"` when absent, then
`ctx.runMutation(api.activeEngine.recordRouting, {...})` and `break`. The defensive coalescing is
load-bearing, not decoration — this file's own comments record a single unhandled `null` poisoning
an eight-event batch in production.

Then write `convex/activeEngine.test.ts` exercising `deduplicateByProfile` in isolation as a pure
function (the `personaConfigChangeKey` precedent), covering every behavior listed above. Do not
attempt to spin up a Convex runtime in this test file.
  </action>
  <verify>
    <automated>npx vitest run convex/activeEngine.test.ts &amp;&amp; npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'model_routing' convex/runtimeIngest.ts` returns at least 1.
    - `grep -c 'activeEngine.recordRouting' convex/runtimeIngest.ts` returns at least 1.
    - The new case reads `profile_id` via the dual-coalescing idiom (a `?? d.profile_id` appears in the case body).
    - `npx vitest run convex/activeEngine.test.ts` passes with at least 4 tests.
    - One test asserts a two-profile/two-distinct-model dedup result (the Mixed-brains precondition).
    - One test asserts an empty input yields an empty array, never `undefined`.
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>Astridr's `model_routing` event has a landing path into the reactive table, and the dedup logic is proven correct in isolation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| astridr → `/runtime-ingest` | Untrusted-shaped JSON telemetry crosses into Convex mutations |
| Convex table → every dashboard page | A throwing query here blanks every page rendering the header badge |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-103-05 | Tampering | `case "model_routing"` payload | mitigate | Dual snake/camelCase coalescing with explicit defaults at the ingest edge, matching the convention that exists because one unhandled `null` poisoned an eight-event batch in production |
| T-103-06 | Denial of Service | `latestByProfile` unbounded read | mitigate | Bounded `.take(200)` over the `by_timestamp` descending index — never `.collect()` on an append-only snapshot table |
| T-103-07 | Repudiation | who changed a profile's model | accept | Already covered — `convex/profiles.ts:109-127` writes a `configChanges` row on every `modelPreferences` change with `changedBy` distinguishing `"dashboard"` from `"astridr-sync"`; this plan deliberately does not duplicate it |
| T-103-08 | Denial of Service | live self-hosted Convex instance | mitigate | Schema push is a pure additive table creation; `import --replace-all` and bulk delete/patch are prohibited by the [BLOCKING] checkpoint's own action text |
| T-103-SC | Tampering | npm/pip/cargo installs | accept | Zero new packages this phase; no install task exists |
</threat_model>

<verification>
- `npx vitest run convex/activeEngine.test.ts` green.
- `npx convex run activeEngine:latestByProfile` resolves on the live instance.
- `git diff convex/schema.ts` shows additions only.
</verification>

<success_criteria>
- A profile's active engine can be read reactively from Convex, latest-per-profile, bounded.
- The table exists on the live self-hosted instance, not only in generated types.
- Nothing in this plan writes an engine value the client asserted.
</success_criteria>

<output>
Create `.planning/phases/103-brain-swap-control-surface/103-02-SUMMARY.md` when done
</output>
