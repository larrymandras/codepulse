---
phase: 103-brain-swap-control-surface
plan: 10
type: execute
wave: 1
depends_on: []
gap_closure: true
files_modified:
  - convex/activeEngine.ts
  - convex/runtimeIngest.ts
  - convex/activeEngine.test.ts
autonomous: true
requirements: [BSC-01, BSC-04]
user_setup: []

must_haves:
  truths:
    - "A holder of the shipped VITE_CONVEX_URL cannot call the active-engine write path from browser devtools — recordRouting is no longer reachable through the public api. namespace"
    - "The model_routing telemetry ingest still writes active-engine snapshot rows, now through internal.activeEngine.recordRouting"
    - "The badge's confirmed-live pulse dot can only be driven by a genuinely server-reported telemetry row, never by a client-fabricated one"
    - "D-14: the active-engine axis has exactly one write path and it is reachable only from server-side telemetry ingest, never from a client action or an ack payload"
    - "No other Convex mutation in this phase's surface carries the same public-callable exposure"
  artifacts:
    - path: "convex/activeEngine.ts"
      provides: "latestByProfile query (public, read-only) + recordRouting as an internalMutation"
      contains: "internalMutation"
    - path: "convex/runtimeIngest.ts"
      provides: "model_routing case invoking the internal namespace"
      contains: "internal.activeEngine.recordRouting"
    - path: "convex/activeEngine.test.ts"
      provides: "deduplicateByProfile coverage plus a source-level guard that recordRouting is not publicly callable"
  key_links:
    - from: "convex/runtimeIngest.ts"
      to: "convex/activeEngine.ts"
      via: "ctx.runMutation(internal.activeEngine.recordRouting, ...)"
      pattern: "internal\\.activeEngine\\.recordRouting"
---

<objective>
Close gap Group C (CR-01, BSC-01 + BSC-04): `convex/activeEngine.ts:69-81` declares `recordRouting`
with the PUBLIC `mutation` builder and `convex/runtimeIngest.ts:534` calls it through the public
`api.` namespace.

Purpose: this is a live authorization defect, not a style nit. The file's own docstring states the
UI must never call this to assert an engine — but nothing server-side enforces it. Any holder of
the shipped `VITE_CONVEX_URL` can open devtools and call `api.activeEngine.recordRouting` to insert
a fabricated row, which `BrainHeaderBadge` then renders with its `isConfirmedLive` trust-signal
pulse dot. The write path that is supposed to be this feature's trust anchor is forgeable by anyone
who loads the page. The in-repo precedent the original plan cited — `convex/gatewayQuota.ts:103`'s
`insertSnapshot` — already uses `internalMutation`; this file deviated from it.

One fix closes both BSC-01's "server-confirmed" claim and BSC-04's "not optimistic-only" claim.

Output: `recordRouting` as an `internalMutation`, invoked only via `internal.` from the ingest path,
deployed to the self-hosted instance, plus an audit confirming no sibling exposure.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/103-brain-swap-control-surface/103-CONTEXT.md
@.planning/phases/103-brain-swap-control-surface/103-CONTRACT.md
@.planning/phases/103-brain-swap-control-surface/103-VERIFICATION.md
@.planning/phases/103-brain-swap-control-surface/103-REVIEW.md
@CLAUDE.md
</context>

<interfaces>
<!-- The in-repo precedent, convex/gatewayQuota.ts:1 and :103 —                                    -->
<!--   import { internalAction, internalMutation, query } from "./_generated/server";               -->
<!--   export const insertSnapshot = internalMutation({ args: {...}, handler: async (ctx, args)=>…});-->
<!-- and its caller uses internal.gatewayQuota.insertSnapshot, imported from "./_generated/api".    -->

<!-- Current call site, convex/runtimeIngest.ts (case "model_routing"):                             -->
<!--   await ctx.runMutation(api.activeEngine.recordRouting, { profileId, model, mode,              -->
<!--     selectionPath, expiresAt, timestamp });                                                    -->
<!-- The dual snake/camelCase coalescing in that case is load-bearing (WR-06/168-06 lesson) —       -->
<!-- change ONLY the namespace and the builder, never the coalescing.                               -->

<!-- Self-hosted Convex operational rules (CLAUDE.md):                                              -->
<!--   NEVER `npx convex import --replace-all`. NEVER bulk-delete/bulk-patch the live instance.     -->
<!--   A function-only deploy is safe and is what this plan needs.                                   -->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Convert recordRouting to internalMutation and rewire the ingest call (103-10-T1)</name>
  <files>convex/activeEngine.ts, convex/runtimeIngest.ts</files>
  <read_first>
    - convex/activeEngine.ts (whole file, 81 lines) — the current `mutation` import at line 1, the D-14 docstring at lines 58-68, and the `recordRouting` declaration at lines 69-81.
    - convex/gatewayQuota.ts lines 1-10 and 95-130 — THE NAMED PRECEDENT: `internalMutation` import, `insertSnapshot` declaration, and how its internal caller references it.
    - convex/runtimeIngest.ts — the `case "model_routing"` block (around line 529-542) and the file's existing imports of `api` / `internal` from `./_generated/api`.
    - .planning/phases/103-brain-swap-control-surface/103-REVIEW.md lines 65-124 (CR-01) — the full finding and its cited evidence.
  </read_first>
  <action>
In `convex/activeEngine.ts`:
  - Change the import on line 1 from `{ mutation, query }` to `{ internalMutation, query }`.
  - Change `export const recordRouting = mutation({` to
    `export const recordRouting = internalMutation({`. The `args` validators and the handler body
    are unchanged — this is an authorization change, not a behavior change.
  - Keep `latestByProfile` as a public `query`; it is a read-only, bounded, non-PII read consumed by
    `useActiveEngine` in the browser and must stay public.
  - Update the D-14 docstring above `recordRouting` so it states the rule is now ENFORCED by the
    builder rather than merely asserted in prose, and cite `gatewayQuota.ts`'s `insertSnapshot` as
    the precedent it now matches.

In `convex/runtimeIngest.ts`, `case "model_routing"`:
  - Change `ctx.runMutation(api.activeEngine.recordRouting, {...})` to
    `ctx.runMutation(internal.activeEngine.recordRouting, {...})`.
  - Add `internal` to the existing `./_generated/api` import if it is not already imported. Leave
    every other `api.*` call in the file alone — this plan changes exactly one call site.
  - Do NOT touch the dual snake/camelCase coalescing (`d.profileId ?? d.profile_id`, etc.). That
    defensiveness is load-bearing per this file's own WR-06/168-06 lesson.

Then deploy the function change to the self-hosted instance so the internal function actually
exists at runtime: run the repo's documented deploy (`npx convex deploy --yes`). This is a
function-only deploy. Do NOT run `npx convex import --replace-all`, and do not bulk-delete or
bulk-patch any table (CLAUDE.md § Self-Hosted Convex). If the deploy cannot complete
non-interactively, record the exact error verbatim in the summary rather than claiming the change
landed live.

Finally, audit the phase's Convex surface for the same exposure: enumerate every non-comment
`mutation(` declaration in the files this phase created or modified (`convex/activeEngine.ts`,
`convex/runtimeIngest.ts`) and record the result in the summary. `runtimeIngest.ts`'s HTTP ingest
entry point is public by design (it is the astridr ingest endpoint) and is out of scope here; name
it explicitly as reviewed-and-accepted rather than leaving it unmentioned.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run convex/activeEngine.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -v '^\s*[*/]' convex/activeEngine.ts | grep -c "= mutation("` returns 0 (comment lines filtered, since the file's own docstring uses the word "mutation").
    - `grep -v '^\s*[*/]' convex/activeEngine.ts | grep -c "internalMutation"` returns at least 2 (the import and the declaration).
    - `grep -c "internal.activeEngine.recordRouting" convex/runtimeIngest.ts` returns 1.
    - `grep -c "api.activeEngine.recordRouting" convex/runtimeIngest.ts` returns 0.
    - `npx tsc --noEmit` exits 0 — this is the load-bearing proof that `internal.activeEngine.recordRouting` genuinely resolves in the regenerated Convex API types, i.e. that the function moved namespaces rather than merely being renamed in a string.
    - `npx convex deploy --yes` completed, or its exact failure text is recorded verbatim in the summary. No `--replace-all` was used, and no table was bulk-deleted or bulk-patched.
    - The summary contains the sibling-exposure audit: every non-comment `mutation(` declaration in `convex/activeEngine.ts` and `convex/runtimeIngest.ts`, with `runtimeIngest.ts`'s public HTTP ingest endpoint explicitly named as reviewed and accepted.
    - ANTI-STUB-MASKING PROOF: this defect lives entirely on the Convex server. `VITE_BRAINS_STUB` gates only `src/lib/brainsApi.ts` and has no effect on any Convex function — confirm with `grep -rc "BRAINS_STUB" convex/` returning 0 across the directory. The proof here is a source assertion plus the `tsc` namespace resolution, both of which are stub-independent by construction.
  </acceptance_criteria>
  <done>The active-engine write path is server-only; the public forgery route into the confirmed-live trust signal is closed.</done>
</task>

<task type="auto">
  <name>Task 2: Lock the authorization boundary with a test (103-10-T2)</name>
  <files>convex/activeEngine.test.ts</files>
  <read_first>
    - convex/activeEngine.test.ts (whole file) — the existing `deduplicateByProfile` coverage and its import style.
    - convex/activeEngine.ts (as edited in Task 1).
  </read_first>
  <action>
Add a guard test to `convex/activeEngine.test.ts` that reads `convex/activeEngine.ts` from disk
(via `node:fs` `readFileSync`, resolved relative to the test file) and asserts, on the source with
comment lines stripped:
  - the file declares `recordRouting` with `internalMutation`, and
  - the file contains no `= mutation(` declaration at all.

Strip comment lines before matching (the file's docstrings legitimately contain the words
"mutation" and "recordRouting"; an unfiltered count is self-invalidating and would pass or fail for
the wrong reason).

Add a second assertion reading `convex/runtimeIngest.ts` and requiring `internal.activeEngine.recordRouting`
to be present and `api.activeEngine.recordRouting` to be absent. Name the test after the defect
(CR-01) so a future reader knows why it exists.

Keep the existing `deduplicateByProfile` tests untouched and passing.
  </action>
  <verify>
    <automated>npx vitest run convex/activeEngine.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run convex/activeEngine.test.ts` passes, including the new CR-01 guard tests and every pre-existing `deduplicateByProfile` test.
    - MUTATION CHECK (mandatory before claiming the guard works): temporarily change `internalMutation` back to `mutation` in `convex/activeEngine.ts`, re-run the test file, confirm it FAILS, then restore. Record both outcomes in the summary. A guard test that has never been seen to fail proves nothing.
    - The comment-stripping is real: the test must still pass when the file's docstring mentions the word "mutation", and must still fail when a real `= mutation(` declaration is present.
    - ANTI-STUB-MASKING PROOF: the assertion reads the Convex source file from disk. It has no React render, no `brainsApi` import, and no dependence on any `VITE_` flag — a stub build and a live build produce byte-identical results for this test.
  </acceptance_criteria>
  <done>The authorization boundary is enforced by the builder AND regression-guarded by a test proven to fail when the boundary is removed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → Convex public API | Anyone holding the shipped `VITE_CONVEX_URL` can invoke any function exported with a public builder; this is the boundary CR-01 leaves open |
| Ástríðr → Convex HTTP ingest | `/runtime-ingest` accepts telemetry over HTTP; it is the intended, reviewed public entry point for `model_routing` events |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-103-35 | Elevation of Privilege | `convex/activeEngine.ts` `recordRouting` (public `mutation`) | mitigate | Convert to `internalMutation`, matching `convex/gatewayQuota.ts:103`'s `insertSnapshot`. The function disappears from the client-callable `api.` namespace entirely — removing the capability rather than validating it. Enforced by the Convex builder, guarded by the 103-10-T2 source test. |
| T-103-36 | Spoofing | forged "server-confirmed" `activeEngineSnapshots` row | mitigate | A forged row is indistinguishable from a genuine `model_routing`-sourced snapshot and renders with `BrainHeaderBadge`'s `isConfirmedLive` pulse dot — the UI's strongest trust signal. Closing the public write path is the only mitigation that works, since the row's content cannot be authenticated after insertion. |
| T-103-37 | Tampering | data integrity of the append-only table | mitigate | `recordRouting` remains insert-only with no patch or delete path; converting the builder does not widen it. No bulk-delete or bulk-patch is performed against the live self-hosted instance (CLAUDE.md operational rule). |
| T-103-38 | Information Disclosure | `latestByProfile` staying a public `query` | accept | Read-only, bounded to `.take(200)` over the `by_timestamp` index, and returns only profile ids and model ids — no PII, no secrets. It is required by the browser (`useActiveEngine`) and matches `gatewayQuota.ts:123`'s public `latestByProvider`. |
| T-103-39 | Elevation of Privilege | sibling public mutations in the phase surface | mitigate | Task 1 audits every non-comment `mutation(` declaration in `convex/activeEngine.ts` and `convex/runtimeIngest.ts` and records the result; `runtimeIngest.ts`'s HTTP ingest entry point is named explicitly as reviewed-and-accepted rather than silently omitted. |
| T-103-SC | Tampering | npm/pip/cargo installs | mitigate | This plan adds ZERO new dependencies. Any install proposal stops the plan and raises a blocking human checkpoint; no `103-RESEARCH.md` Package Legitimacy Audit row covers a new package here. |
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0 (proves the internal namespace resolves in regenerated types).
- `npx vitest run convex/activeEngine.test.ts` passes, and the mutation check proved the guard fails when reverted.
- `npx convex deploy --yes` landed the function change on the self-hosted instance (or its failure is recorded verbatim).
- `grep -v '^\s*[*/]' convex/activeEngine.ts | grep -c "= mutation("` returns 0.
</verification>

<success_criteria>
- `api.activeEngine.recordRouting` no longer exists; `internal.activeEngine.recordRouting` does.
- Telemetry ingest still writes snapshot rows through the internal namespace.
- The sibling-exposure audit is recorded, naming every public mutation in the phase surface.
- The guard test has been demonstrated to fail when the fix is reverted.
</success_criteria>

<output>
Create `.planning/phases/103-brain-swap-control-surface/103-10-SUMMARY.md` when done.
</output>
