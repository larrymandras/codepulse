---
phase: 108-per-profile-engine-telemetry-astridr-backend
plan: 02
type: execute
wave: 1
depends_on: []
repos: [codepulse]
files_modified:
  - codepulse:convex/schema.ts
  - codepulse:convex/controlVerbSwaps.ts
  - codepulse:convex/controlVerbSwaps.test.ts
  - codepulse:convex/retention.ts
autonomous: true
requirements: [TELE-02]

must_haves:
  truths:
    - "D-13: every control_verb_swap emit — restore, unresolved, affinity-refused, success — is storable, so a swap history cannot claim every swap worked."
    - "D-13: the table carries a scope column holding the explicit profile id when a swap was scoped and nothing when it was global."
    - "D-14: one table holds brain AND voice swaps, discriminated by the existing verb field, so no live event kind is left with no queryable surface."
    - "D-10: activeEngineSnapshots is bounded by RETENTION_DAYS in this phase, before it starts growing, rather than after."
    - "The only write path into the new table is an internalMutation, absent from the client-callable api. namespace."
    - "The swap-history read is bounded by an explicit .take() cap, never .collect()."
  artifacts:
    - path: "codepulse:convex/schema.ts"
      provides: "controlVerbSwaps table definition with by_scope and by_timestamp indexes"
      contains: "controlVerbSwaps"
    - path: "codepulse:convex/controlVerbSwaps.ts"
      provides: "record internalMutation + bounded per-scope read query"
      exports: ["record", "listByScope"]
    - path: "codepulse:convex/controlVerbSwaps.test.ts"
      provides: "CR-01 authorization-boundary regression guard + bounded-read guard"
    - path: "codepulse:convex/retention.ts"
      provides: "RETENTION_DAYS entries for activeEngineSnapshots and controlVerbSwaps"
      contains: "controlVerbSwaps: 30"
  key_links:
    - from: "convex/retention.ts"
      to: "convex/schema.ts"
      via: "RETENTION_DAYS keys must be real schema tables (retention.test.ts guard)"
      pattern: "controlVerbSwaps"
---

<objective>
Stand up the CodePulse receiving half for `control_verb_swap`: a `controlVerbSwaps` table, an
internal-only write path, one bounded read query, and retention bounds for BOTH new/newly-growing
per-profile engine tables.

**Repo: `codepulse` only** (`C:\Users\mandr\codepulse`, branch `master`).

Purpose: TELE-02 requires `control_verb_swap` to reach a queryable domain table rather than only the
generic `runtime_events` log. D-10 requires `activeEngineSnapshots` — permanently empty until this
phase, about to start growing — to be bounded before it can ever need a mass delete.

Output: schema + domain module + retention entries + tests. The ingest case that feeds this table is
plan 108-03; the readout that consumes it is plan 108-06.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/108-per-profile-engine-telemetry-astridr-backend/108-CONTEXT.md
@.planning/phases/108-per-profile-engine-telemetry-astridr-backend/108-RESEARCH.md
@.planning/phases/108-per-profile-engine-telemetry-astridr-backend/108-PATTERNS.md
@CLAUDE.md
</context>

<interfaces>
<!-- The six live control_verb_swap emit sites, read from astridr-repo 2026-08-07. The Convex
     schema must accommodate all six shapes; every site-specific field is v.optional(). -->

| Site | astridr file:line | `verb` | Fields present |
|---|---|---|---|
| restore | `swap_model.py:444` | `swap_model` | `target:null, resolved:null, provider_affinity:null, path:"restore", session_id, channel` |
| unresolved | `swap_model.py:472` | `swap_model` | `target, resolved:null, provider_affinity, path, reason, session_id, channel` |
| affinity-refused | `swap_model.py:483` | `swap_model` | `target, resolved, provider_affinity:null, path:"refused", reason:"affinity_guard", session_id, channel` |
| success | `swap_model.py:495` | `swap_model` | `target, resolved, provider_affinity, path, reason, session_id, channel` |
| restore | `swap_voice.py:211` | `swap_voice` | `target:null, resolved:null, voice_id:null, path:"restore", session_id, channel` |
| success/unresolved | `swap_voice.py:232` | `swap_voice` | `target, resolved, voice_id, path, session_id, channel` |

This matches `docs/astridr-contract.md:1192-1214`'s existing documented field contract field-for-field.
`scope` is the ONE new column this phase adds.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: controlVerbSwaps table + retention bounds for both engine-axis tables</name>
  <repo>codepulse</repo>
  <files>convex/schema.ts, convex/retention.ts</files>
  <read_first>
    - `convex/schema.ts:2048-2072` — the `activeEngineSnapshots` block, **the exact analog**
      (108-PATTERNS.md §4): section-header comment block, doc-comment on the table, `v.string()`
      rather than a Literal union with the note that validation happens at the ingest edge.
    - `convex/retention.ts:28-71` — `RETENTION_DAYS` and its three bands (14 / 30 / 90).
    - `convex/retention.ts:39-48` (`gatewayQuotaSnapshots`) and `:58-65` (`toolPolicyEvents`) — the
      two comment-style precedents for "bound a table BEFORE it grows", including the "only the
      latest row is ever read, so N days is pure headroom" phrasing.
    - `convex/retention.test.ts:37-47` — the two guards that auto-cover any new key: "every pruned
      table name is a real table in schema.ts" and "every window is a positive whole number".
    - `CLAUDE.md` § "Self-Hosted Convex — Operational Rules".
  </read_first>
  <action>
**(a) `convex/schema.ts`** — add a `controlVerbSwaps` table immediately after the
`activeEngineSnapshots` block, using that block's exact commenting convention (a `====` section
header, then a doc comment on the table itself). Fields, matching all six emit sites above and
`docs/astridr-contract.md:1192-1214` field-for-field:

`verb: v.string()` · `target: v.optional(v.string())` · `resolved: v.optional(v.string())` ·
`providerAffinity: v.optional(v.string())` · `voiceId: v.optional(v.string())` · `path: v.string()` ·
`reason: v.optional(v.string())` · `scope: v.optional(v.string())` ·
`sessionId: v.optional(v.string())` · `channel: v.string()` · `timestamp: v.float64()`

Indexes: `.index("by_scope", ["scope", "timestamp"])` and `.index("by_timestamp", ["timestamp"])`.

Keep `verb` and `path` as `v.string()` (not Literal unions) — this schema's stated defensive-boundary
convention is that the ingest edge validates, not the schema; a Literal union here would turn an
unexpected astridr value into a hard ingest failure rather than a stored, diagnosable row. State the
D-13/D-14 rationale in the doc comment: every emit is stored INCLUDING refusals, because the refusal
path is exactly where the affinity guard and the resolver fail, and a history storing only successes
claims every swap worked; and one table holds brain and voice discriminated by `verb`, because both
verbs emit the same event name on the same channel.

**(b) `convex/retention.ts`** — add TWO `RETENTION_DAYS` entries in the 30-day band, each with a
comment in the `gatewayQuotaSnapshots`/`toolPolicyEvents` register (why it is bounded pre-emptively,
and why the window is headroom rather than a functional limit):

- `activeEngineSnapshots: 30` — D-10. Verified this session: `grep -c "activeEngineSnapshots"
  convex/retention.ts` returns `0`. The table is append-only and unbounded and this is the phase
  that turns it from permanently-empty into growing. Only the latest row per profile is ever read
  (`activeEngine.latestByProfile`), so any window is pure headroom. 14 days would age out
  "what was this profile's engine yesterday" faster than an operator would ask; 90 is unwarranted
  for a latest-row-per-key consumer.
- `controlVerbSwaps: 30` — D-14. Same tier deliberately, so both of this phase's per-profile
  engine-axis tables share one mental model. A swap is a manual/rare operator action, so volume is
  low; the 90-day tier is reserved for higher-value long-horizon tables.

Do not write any new prune code. `retention.ts`'s existing batch-capped, cursor-seeked sweep already
covers every `RETENTION_DAYS` key — that is the whole point of adding keys rather than a bespoke
sweep, per CLAUDE.md's self-hosted rules (the 2026-07-21/22 tombstone-storm incident).
  </action>
  <verify>
    <automated>cd /c/Users/mandr/codepulse && npx tsc --noEmit && npx vitest run convex/retention.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "controlVerbSwaps" convex/schema.ts` returns `>= 1` and the definition contains all 11
      field names listed above.
    - `grep -n "by_scope" convex/schema.ts` shows the index declared on `controlVerbSwaps`.
    - `grep -v '^\s*//' convex/retention.ts | grep -c "activeEngineSnapshots: 30"` returns `1`.
    - `grep -v '^\s*//' convex/retention.ts | grep -c "controlVerbSwaps: 30"` returns `1`.
    - `npx vitest run convex/retention.test.ts` exits 0 — in particular the "every pruned table name
      is a real table in schema.ts" guard passes for both new keys (it would fail on a typo).
    - `npx tsc --noEmit` exits 0.
    - No new prune function was added to `convex/retention.ts` (`git diff` shows only
      `RETENTION_DAYS` additions plus comments).
  </acceptance_criteria>
  <done>Both tables are bounded by the existing batch-capped machinery, and `controlVerbSwaps` exists with the indexes its read paths need.</done>
</task>

<task type="auto">
  <name>Task 2: controlVerbSwaps domain module — internal-only write, bounded read</name>
  <repo>codepulse</repo>
  <files>convex/controlVerbSwaps.ts</files>
  <read_first>
    - `convex/activeEngine.ts` (full file, 141 lines) — **the exact analog** (108-PATTERNS.md §1):
      the header-comment convention, `latestByProfile`'s bounded `.take(200)`, and `recordRouting`'s
      `internalMutation` + the CR-01 rationale docstring at `:59-78`.
    - `convex/activeEngineFilters.ts` — the dependency-free shared-predicate convention, if a
      validity guard is needed.
    - `convex/schema.ts` — the `controlVerbSwaps` definition added in Task 1.
  </read_first>
  <action>
Create `convex/controlVerbSwaps.ts` modelled on `convex/activeEngine.ts`.

**Header comment** — state the TELE-02/D-13/D-14 framing: this is the per-profile swap-history audit
trail; `swap_model.py` and `swap_voice.py` emit one event per swap attempt
(restore / unresolved / affinity-refused / success) and this table stores every one, discriminated by
`verb`, so a refusal is queryable rather than only a spoken/toast notice; one table holds brain and
voice, and the D-15 readout filters to `verb === "swap_model"` while voice rows are captured but not
yet surfaced (deferred, not dropped).

**`record`** — an `internalMutation` whose `args` mirror the Task 1 schema field-for-field
(`verb: v.string()`, `path: v.string()`, `channel: v.string()`, `timestamp: v.float64()`, all others
`v.optional(v.string())`), handler `await ctx.db.insert("controlVerbSwaps", { ...args })`. Copy
`recordRouting`'s CR-01 docstring rationale in this file's own words: it is declared
`internalMutation` so it does not exist in the client-callable `api.` namespace at all, closing the
devtools-forgeable write path a plain `mutation` would leave open on a telemetry table. It is
reachable ONLY from the `control_verb_swap` ingest case (plan 108-03), never from client code.

**`listByScope`** — a public `query` taking `{ profileId: v.string() }`, reading
`.withIndex("by_scope", q => q.eq("scope", args.profileId)).order("desc").take(20)`. A `query`, not
an `internalMutation`, because D-15's modal legitimately reads it from the client. `.take(20)` and
never `.collect()` — this repo's standing rule, and 20 is right-sized for a modal history section
(vs `latestByProfile`'s 200 for a dashboard-wide read).

Export the row-cap as a named constant (e.g. `export const SWAP_HISTORY_CAP = 20;`) and use it in the
`.take()` call, so plan 108-06's on-screen truncation caption and this query cannot drift apart.

If a pure helper is useful for the readout (e.g. a `verb === "swap_model"` filter), export it from
this module so it can be unit-tested directly — following `deduplicateByProfile`'s precedent
(`activeEngine.ts:24-34`), which is exported solely so `activeEngine.test.ts` can test it as a pure
function.
  </action>
  <verify>
    <automated>cd /c/Users/mandr/codepulse && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `convex/controlVerbSwaps.ts` exists and exports `record` and `listByScope`.
    - `grep -c "= internalMutation(" convex/controlVerbSwaps.ts` returns `1` and
      `grep -c "= mutation(" convex/controlVerbSwaps.ts` returns `0`.
    - `grep -c "\.collect()" convex/controlVerbSwaps.ts` returns `0`.
    - `grep -n "SWAP_HISTORY_CAP" convex/controlVerbSwaps.ts` shows both an `export const` and its use
      inside `.take(`.
    - `grep -n 'withIndex("by_scope"' convex/controlVerbSwaps.ts` returns a hit.
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>The table has exactly one write path (internal-only) and one bounded read path.</done>
</task>

<task type="auto">
  <name>Task 3: controlVerbSwaps.test.ts — authorization-boundary and bounded-read guards</name>
  <repo>codepulse</repo>
  <files>convex/controlVerbSwaps.test.ts</files>
  <read_first>
    - `convex/activeEngine.test.ts` (full file, 156 lines) — **the exact analog** and the harness
      style this repo uses for Convex modules: source-level parsing with `readFileSync`, NOT a
      `convex-test` runtime harness. Specifically: the `stripCommentLines` helper at `:14-19`, the
      CR-01 authorization guard at `:93-116`, the pure-function tests at `:39-84`, and the
      bounded-window ingest-case guard at `:126-155`.
  </read_first>
  <action>
Create `convex/controlVerbSwaps.test.ts` mirroring `convex/activeEngine.test.ts`'s structure.

**Copy `stripCommentLines` verbatim** from `activeEngine.test.ts:14-19`. It exists precisely so a
docstring that legitimately says "mutation" cannot make a negative source-grep pass or fail
vacuously — a bare `grep -c` on an unfiltered file counts comment text, which is self-invalidating
when the file's own header explains the rule being tested.

Tests to include:

1. **CR-01 authorization boundary** (copy `activeEngine.test.ts:93-116`'s shape): on the
   comment-stripped source of `convex/controlVerbSwaps.ts`, assert `internalMutation(` IS present and
   `= mutation(` is NOT. Then assert on the RAW (unstripped) source that the word `mutation` still
   appears — this sanity-checks the stripper itself, so the negative assertion cannot pass because
   the stripper ate everything. Copy that self-check rationale; it is the difference between a real
   guard and a vacuous green.

2. **Bounded read**: assert the stripped source of `controlVerbSwaps.ts` contains `.take(` and does
   NOT contain `.collect(`.

3. **Cap consistency**: assert `SWAP_HISTORY_CAP` is exported and that the numeric literal in the
   `.take(...)` call is the constant, not a duplicated number.

4. **Pure-helper tests**, if Task 2 exported one: table-driven cases including an empty-input case
   that must return `[]`, never `undefined` — matching `deduplicateByProfile`'s test block.

Do NOT add an ingest-case assertion here; that belongs to plan 108-03, which owns
`convex/runtimeIngest.ts`.
  </action>
  <verify>
    <automated>cd /c/Users/mandr/codepulse && npx vitest run convex/controlVerbSwaps.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run convex/controlVerbSwaps.test.ts` exits 0 with `>= 3` passing tests.
    - The file contains a `stripCommentLines` helper and a raw-source sanity assertion proving the
      stripper is not vacuously passing.
    - Mutation check, performed and its result stated in the SUMMARY: temporarily change
      `internalMutation(` to `mutation(` in `convex/controlVerbSwaps.ts` and confirm test 1 goes RED;
      then restore. A guard that stays green under this mutation is not a guard.
  </acceptance_criteria>
  <done>The internal-only write path and the bounded read are protected by regression tests that have been proven to fail when violated.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser client → Convex `api.*` namespace | Any holder of the shipped `VITE_CONVEX_URL` can call anything in `api.*` from devtools |
| Ástríðr `/runtime-ingest` → Convex tables | Bearer-gated httpAction; payload contents are otherwise unvalidated strings |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-108-03 | Spoofing | `controlVerbSwaps` write path | mitigate | Declared `internalMutation`, so it is absent from the client-callable `api.` namespace entirely — the CR-01 rule `activeEngine.ts` already enforces. Guarded by a source-level regression test that has been mutation-checked (Task 3), because the defect it prevents (a forged "server-confirmed" swap-history row rendered as truth) is invisible at runtime. |
| T-108-05 | Denial of Service | `activeEngineSnapshots`, `controlVerbSwaps` growth | mitigate | Both added to `RETENTION_DAYS` at 30 days in Task 1, riding the existing batch-capped cursor-seeked prune. No bespoke sweep and no bulk delete — CLAUDE.md's hard rule after the 2026-07-21/22 tombstone-storm outage. |
| T-108-12 | Denial of Service | `listByScope` unbounded read | mitigate | `.take(SWAP_HISTORY_CAP)` on an indexed range, never `.collect()`. Asserted by a source-level test, since an unbounded read on a growing table is exactly what breached the 16 MiB limit in Phase 88. |
| T-108-13 | Tampering | Unexpected `verb`/`path` values from astridr | accept | `verb` and `path` stay `v.string()` rather than Literal unions, matching this schema's stated defensive-boundary convention. An unexpected value is stored and diagnosable rather than throwing at the ingest edge and poisoning the batch (the WR-06/168-06 lesson). The D-15 readout filters positively on `verb === "swap_model"`, so an unknown verb renders nothing rather than something wrong. |
| T-108-SC | Tampering | package installs | accept | No packages installed by this plan; every symbol used already exists in the repo. |
</threat_model>

<verification>
1. `cd /c/Users/mandr/codepulse && npx tsc --noEmit` exits 0.
2. `npx vitest run convex/controlVerbSwaps.test.ts convex/retention.test.ts convex/activeEngine.test.ts` exits 0.
3. `npm test` — full suite green (wave-merge gate).
4. `git diff` on `convex/retention.ts` shows only `RETENTION_DAYS` key additions and comments — no
   change to `BATCH_SIZE`, the prune loop, or the cursor logic.
</verification>

<success_criteria>
- `controlVerbSwaps` exists in the schema with `by_scope` and `by_timestamp` indexes and can hold all
  six live emit-site payload shapes.
- Its only write path is an `internalMutation`; its only read path is `.take()`-bounded.
- `activeEngineSnapshots` and `controlVerbSwaps` both appear in `RETENTION_DAYS`.
- The CR-01 guard has been proven to fail when the write path is downgraded to a public `mutation`.
</success_criteria>

<output>
Create `.planning/phases/108-per-profile-engine-telemetry-astridr-backend/108-02-SUMMARY.md` when done.
Include the Task 3 mutation-check result.
</output>
