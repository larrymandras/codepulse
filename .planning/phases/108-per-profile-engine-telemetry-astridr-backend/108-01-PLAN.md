---
phase: 108-per-profile-engine-telemetry-astridr-backend
plan: 01
type: execute
wave: 1
depends_on: []
repos: [astridr-repo]
files_modified:
  - astridr-repo:astridr/channels/agent_processor.py
  - astridr-repo:astridr/engine/bootstrap/wiring.py
  - astridr-repo:astridr/providers/router.py
  - astridr-repo:docs/astridr-contract.md
  - astridr-repo:tests/unit/providers/test_router.py
  - astridr-repo:tests/unit/channels/test_agent_processor_profile_context.py
autonomous: true
requirements: [ENGINE-01]

must_haves:
  truths:
    - "D-01: ModelRouter reads the profile from the pre-existing _current_profile_id ContextVar via get_profile_context() — no second, parallel ContextVar is created."
    - "D-01: every live set-point pairs set_profile_context with reset_profile_context in a finally block, so the profile never leaks between units of work."
    - "D-02: a model_routing event with no profile in context, or with no resolved model, is refused at emit — no row is produced at all, no sentinel is written."
    - "D-09: the emitter dedups per profile on (model, mode, selectionPath, status) and sends only on change, so a hot resolution path costs zero telemetry per call."
    - "D-11: the emitted field is named model, not selectedModel, and docs/astridr-contract.md documents the same name in the same commit."
    - "D-12: mode is derived once, at the single emit helper, from selection_path via an explicit lookup with an inherited default — every live selection_path value maps, including codepulse-default and advisor."
    - "A CodePulse-originated chat turn attributes to a real profile id, not to None."
  artifacts:
    - path: "astridr-repo:astridr/providers/router.py"
      provides: "_emit_model_routing with profileId, refuse-to-emit, mode derivation, emit-on-change memo"
      contains: "get_profile_context"
    - path: "astridr-repo:astridr/channels/agent_processor.py"
      provides: "try/finally profile ContextVar lifecycle at the inbound-message set-point"
      contains: "reset_profile_context"
    - path: "astridr-repo:astridr/engine/bootstrap/wiring.py"
      provides: "profile ContextVar set-point for the CodePulse chat/task launch path"
      contains: "set_profile_context"
    - path: "astridr-repo:tests/unit/channels/test_agent_processor_profile_context.py"
      provides: "regression proof that the profile context is set during and cleared after a turn"
  key_links:
    - from: "astridr/providers/router.py"
      to: "astridr/engine/telemetry.py"
      via: "from astridr.engine.telemetry import get_profile_context"
      pattern: "get_profile_context"
    - from: "astridr/channels/agent_processor.py"
      to: "astridr/engine/telemetry.py"
      via: "set_profile_context / reset_profile_context token pairing"
      pattern: "reset_profile_context"
---

<objective>
Make Ástríðr's `model_routing` telemetry carry a real profile identity and a contract-correct
payload, and make the profile ContextVar it depends on reliable.

**Repo: `astridr-repo` only** (`C:\Users\mandr\astridr-repo`, branch `feature/brain-swap`).

Purpose: today `_emit_model_routing` sends `{selectedModel, selectionPath, taskCategory, status}` —
no `profileId`, wrong field name, no `mode`. CodePulse's `case "model_routing"` reads `d.profileId`
and `d.model`, gets `undefined` for both, and `isUnresolvedRouting()` `break`s every event. That is
the entire mechanism behind "93 rows, all `{profileId:"unknown", model:"unknown"}`". This plan fixes
the producer.

Output: a `model_routing` payload with a real `profileId`, `model`, and derived `mode`; a
refuse-to-emit guard; an emit-on-change memo; and the two ContextVar set-points repaired.
</objective>

<execution_constraints>
- **Do NOT run this plan in a git worktree.** `astridr-repo` is `pip install -e`-installed: a
  worktree's `src`/package path is shadowed by the original checkout's `.pth`, so edits to EXISTING
  modules test green against code you did not write (LESSONS 2026-07-31). Execute in the main
  checkout at `C:\Users\mandr\astridr-repo`.
- **A concurrent session is committing to `feature/brain-swap`.** HEAD moved three times during
  planning (`0d23f06e` → `645e8f83` → `5b8bbde1`). Task 1 re-verifies every line number below before
  editing. Never `git commit --amend` in this checkout — always a fresh commit. `git add` only your
  own explicit paths, and after each commit run `git show --stat HEAD` and disclose anything swept
  in. Do not switch branches.
</execution_constraints>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/108-per-profile-engine-telemetry-astridr-backend/108-CONTEXT.md
@.planning/phases/108-per-profile-engine-telemetry-astridr-backend/108-RESEARCH.md
@.planning/phases/108-per-profile-engine-telemetry-astridr-backend/108-PATTERNS.md
@.planning/phases/108-per-profile-engine-telemetry-astridr-backend/108-VALIDATION.md
</context>

<interfaces>
<!-- Extracted live 2026-08-07 from astridr-repo @ 5b8bbde1. Re-verify per Task 1. -->

astridr/engine/telemetry.py (:95, :670-686) — the ContextVar that ALREADY EXISTS. Do not create another.
```python
_current_profile_id: contextvars.ContextVar[str | None] = contextvars.ContextVar(...)  # default=None

def set_profile_context(profile_id: str | None) -> contextvars.Token[str | None]: ...
def reset_profile_context(token: contextvars.Token[str | None]) -> None: ...
def get_profile_context() -> str | None: ...
```

astridr/providers/router.py (:399) — the single emit helper, shared by 3 call sites (:302 advisor,
:375 success, :393 failed). No call-site changes are needed in this plan.
```python
async def _emit_model_routing(
    self,
    resolved_model: str | None,
    selection_path: str,
    task_category: TaskCategory | None,
    complexity_result: Any | None,
    status: str = "success",
    error: str | None = None,
) -> None:
```

Live `selection_path` vocabulary (7 values — CONTEXT.md's D-12 list of 5 is INCOMPLETE):
`"override"` (:438) · `"global-swap-override"` (:447) · `"session-override"` (:450-453) ·
`"codepulse-default"` (:462) · `"category-rule"` (:465-469) · `"default"` (:471) ·
`"advisor"` — a hardcoded literal passed straight to `_emit_model_routing` from inside `chat()`
at `:302-304`, which never flows through `_resolve_model` at all.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Repair the profile ContextVar lifecycle at both live set-points</name>
  <repo>astridr-repo</repo>
  <files>astridr/channels/agent_processor.py, astridr/engine/bootstrap/wiring.py, tests/unit/channels/test_agent_processor_profile_context.py</files>
  <read_first>
    - `astridr/channels/agent_processor.py` — the whole `process()` method (set-point at `:116-117`).
    - `astridr/channels/router.py:508-525` — **the idiom to copy verbatim**: `_goal_token =
      set_goal_context(...)` / `try: ... except Exception: ... finally: reset_goal_context(_goal_token)`.
      108-PATTERNS.md §7 names this as the exact analog.
    - `astridr/engine/telemetry.py:670-686` — the three profile-context functions and their token type.
    - `astridr/engine/bootstrap/wiring.py:332-500` — `_ws_agent_launcher`; note
      `_profile_or_default = payload.get("profile") or default_profile_id` at `:399`, computed today
      only inside the `if security_pipeline is not None:` branch.
    - `tests/unit/engine/bootstrap/test_wiring_chat_persistence.py` — the precedent for exercising
      `_ws_agent_launcher` from a test.
  </read_first>
  <action>
FIRST, before any edit: re-verify the line anchors this plan cites, because a concurrent session is
committing to this branch. Run from `C:\Users\mandr\astridr-repo`:
`grep -n "set_profile_context\|async def process" astridr/channels/agent_processor.py`,
`grep -n "_emit_model_routing\|selectedModel\|def _resolve_model\|_global_model_override" astridr/providers/router.py`,
`grep -n "_ws_agent_launcher\|_profile_or_default" astridr/engine/bootstrap/wiring.py`.
If a citation has drifted, use the live line and note the drift in the SUMMARY. Do not proceed on a
stale anchor.

`reset_profile_context()` is called NOWHERE in astridr-repo today (verified: zero call sites, versus
`reset_goal_context`/`reset_trace_context`/`reset_round_context`, all of which are reset in `finally:`
blocks). That is harmless only because each inbound message currently runs in its own
`asyncio.Task`. It becomes load-bearing the moment `providers/router.py` reads this ContextVar —
which is exactly what Task 2 does. Fix it now, per D-01/D-02.

(a) `astridr/channels/agent_processor.py`, `AgentProcessor.process()`: capture the token
(`_profile_token = set_profile_context(profile.id)`) and add a `finally: reset_profile_context(
_profile_token)` covering everything from the set call through every `return` and the
`except Exception` handler. Match `channels/router.py:508-525`'s nesting shape rather than inventing
a new one. Keep the existing local import of `set_profile_context`; add `reset_profile_context` to it.

(b) `astridr/engine/bootstrap/wiring.py`, `_ws_agent_launcher`: **this path has no profile set-point
at all today** — verified live this session. It is the launcher behind `chat.send`
(`ws_commands.py:618` `_handle_chat_send` → `_ws_agent_launcher`), i.e. every CodePulse chat and
task turn. Without a set-point there, D-02's refuse-to-emit would silence the axis on the operator's
own primary surface and would make this phase's own ENGINE-05 gate unreachable without an inbound
WhatsApp/Telegram/email message. Hoist `_profile_or_default = payload.get("profile") or
default_profile_id` OUT of the `if security_pipeline is not None:` branch so it is computed
unconditionally, then wrap the launcher body in
`_profile_token = set_profile_context(_profile_or_default)` / `finally: reset_profile_context(
_profile_token)`, set BEFORE `agent_type_registry.create_agent_loop()`/`agent_loop_cls()` and before
any `sub_loop.run(...)` await, so the ContextVar is live for the whole turn and for any
`asyncio.create_task` child that copies the context at creation. Add a comment stating that
`default_profile_id` here is Ástríðr's own resolved chat-channel profile (the same value the
security pipeline already attributes to and that 103-CONTRACT.md §3 calls `default_profile_id`) —
a real resolution, not a guess, so D-02 is not being circumvented.

(c) New test file `tests/unit/channels/test_agent_processor_profile_context.py`: a behavioral test
(not a source grep) that constructs `AgentProcessor` with a stub `_agent_loop` whose `process()`
records `get_profile_context()`, then asserts (i) the recorded value equals `profile.id` DURING the
turn, and (ii) `get_profile_context() is None` AFTER `process()` returns, and (iii) it is also `None`
after a turn in which the stub loop raises. Assertion (iii) is the one that proves the `finally`
rather than the happy path. Mutation-check your own test before committing: temporarily delete the
`finally` and confirm assertions (ii) and (iii) go RED, then restore.
  </action>
  <verify>
    <automated>cd /c/Users/mandr/astridr-repo && pytest tests/unit/channels/test_agent_processor_profile_context.py tests/unit/engine/bootstrap/test_wiring_chat_persistence.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "reset_profile_context" astridr/channels/agent_processor.py` returns `>= 1`.
    - `grep -c "set_profile_context" astridr/engine/bootstrap/wiring.py` returns `>= 1` AND
      `grep -c "reset_profile_context" astridr/engine/bootstrap/wiring.py` returns `>= 1`.
    - In `wiring.py`, `_profile_or_default = payload.get("profile") or default_profile_id` appears
      at a lower indentation level than the `if security_pipeline is not None:` block that used to
      contain it (i.e. it is computed unconditionally).
    - `pytest tests/unit/channels/test_agent_processor_profile_context.py -x -q` exits 0 with 3
      passing cases, one of which exercises the exception path.
    - Removing the `finally:` line in `agent_processor.py` makes the new test FAIL (mutation check
      performed and its result stated in the SUMMARY).
  </acceptance_criteria>
  <done>Both live set-points set AND reset the profile ContextVar with a token pairing, proven by a behavioral test that covers the exception path.</done>
</task>

<task type="auto">
  <name>Task 2: profileId, refuse-to-emit, and the selectedModel→model rename</name>
  <repo>astridr-repo</repo>
  <files>astridr/providers/router.py, docs/astridr-contract.md, tests/unit/providers/test_router.py</files>
  <read_first>
    - `astridr/providers/router.py:399-427` — `_emit_model_routing`, the full body including the
      payload dict at `:411-423` and `"selectedModel"` at `:413`.
    - `astridr/providers/router.py:295-310, :370-398` — the 3 call sites, to confirm none need changing.
    - `astridr/engine/telemetry.py:684-686` — `get_profile_context()`.
    - `docs/astridr-contract.md:1170-1180` — the field table row documenting `selectedModel`.
    - `tests/unit/providers/test_router.py:650-725` — the 4 existing assertions on
      `payload["selectedModel"]` at `:664, :686, :703, :719`.
    - `convex/activeEngineFilters.ts` (codepulse, read-only reference) — `isUnresolvedRouting`
      treats `""`, `null` and the literal `"unknown"` as unresolved, but NOT the literal `"default"`.
  </read_first>
  <action>
In `_emit_model_routing`, at the top of the function body:

1. Read the profile: `profile_id = get_profile_context()` (import
   `from astridr.engine.telemetry import get_profile_context` at module scope alongside the existing
   telemetry imports, or locally if that is this module's convention — match what is already there).

2. **D-02 refuse-to-emit guard, two conditions, both early-`return`:**
   - `profile_id` is falsy → return without sending. `structlog` debug line
     `router.model_routing_skipped` with `reason="no_profile_context"`.
   - `resolved_model` is falsy → return without sending, `reason="unresolved_model"`.
     This second condition is NOT optional and is not in CONTEXT.md's text: today the payload sends
     `resolved_model or "default"`, and the literal string `"default"` passes CodePulse's
     `isUnresolvedRouting()` guard (which only rejects `""`/`null`/`"unknown"`). Emitting it would
     store `model:"default"` as a profile's current engine — a fabricated reading of exactly the
     class ENGINE-01 exists to kill, just with a plausible-looking name instead of a sentinel string.
     Record in a code comment that the accepted cost is: a profile resolving purely to the
     `FailoverProvider` fallback (`_resolve_model`'s `return None, "default"` rung) reports nothing at
     runtime, and is covered instead by plan 108-05's boot seed, which carries that profile's real
     configured `model_default`.

3. **Payload changes** (keep every existing key not named below unchanged):
   - ADD `"profileId": profile_id`.
   - RENAME `"selectedModel"` → `"model"`, and drop the `or "default"` coalesce (guard 2 above now
     makes it unreachable) so the value is the real resolved model id. This is D-11. Do NOT emit both
     names: the mandatory D-11 grep was run and is clean — no dashboard, log parser, war-room agent
     or other telemetry consumer reads `selectedModel` (only `router.py:413` itself, this file's own
     tests, `docs/astridr-contract.md:1175`, and two historical planning docs). The dual-emit
     fallback is therefore NOT needed.

4. **`docs/astridr-contract.md:1175` — same commit, non-negotiable.** That row currently documents
   `selectedModel` as the current, correct field name in Ástríðr's own telemetry contract. Change the
   field name to `model` and update the description to drop the `"default"` fallback wording. This is
   the identical defect class D-08 fixes one repo over, and TELE-01 fixes for the Group A kinds: a
   contract doc describing a field the code no longer sends. It must not be deferred to a trailing
   docs task.

5. **`tests/unit/providers/test_router.py`:** update the 4 assertions at `:664, :686, :703, :719` from
   `payload["selectedModel"]` to `payload["model"]` in this same commit (they go red otherwise), and
   ADD cases: (a) `profileId` in the payload equals the value set via `set_profile_context`;
   (b) with no profile in context, `telemetry.send` is NOT called at all for `model_routing` — assert
   on the mock's call list, not on a flag the code under test sets; (c) with a profile in context but
   `resolved_model=None`, `telemetry.send` is likewise not called. Name (b) so that
   `pytest -k refuse_to_emit` selects it (108-VALIDATION.md's Wave 0 entry references that selector).
  </action>
  <verify>
    <automated>cd /c/Users/mandr/astridr-repo && pytest tests/unit/providers/test_router.py -x -q && pytest tests/unit/providers/test_router.py -k refuse_to_emit -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "selectedModel" astridr/providers/router.py docs/astridr-contract.md tests/unit/providers/test_router.py` returns `0` for all three files.
    - `grep -n '"model"' astridr/providers/router.py` shows the key inside the `_emit_model_routing` payload dict.
    - `grep -n '"profileId"' astridr/providers/router.py` shows the key inside the same payload dict.
    - `pytest tests/unit/providers/test_router.py -k refuse_to_emit -q` selects at least 1 test and exits 0.
    - The refuse-to-emit tests assert `telemetry.send` was not called (e.g. no call whose first
      positional arg is `"model_routing"`), NOT that a boolean/flag was set.
    - The `docs/astridr-contract.md` edit and the `router.py` rename are in the SAME commit
      (`git show --stat HEAD` lists both paths).
  </acceptance_criteria>
  <done>The emitted payload carries a real `profileId` and a `model` key; unresolved profile or model produces no event at all; Ástríðr's own contract doc matches the code in the same commit.</done>
</task>

<task type="auto">
  <name>Task 3: derive mode from selection_path, and emit only on change</name>
  <repo>astridr-repo</repo>
  <files>astridr/providers/router.py, tests/unit/providers/test_router.py</files>
  <read_first>
    - `astridr/providers/router.py:429-472` — the full `_resolve_model` precedence chain and every
      `selection_path` string literal it returns.
    - `astridr/providers/router.py:295-310` — the `"advisor"` literal passed directly to
      `_emit_model_routing`, bypassing `_resolve_model` entirely.
    - `108-RESEARCH.md` Item 5 — the full 7-value `selection_path` → `mode` table.
    - `.planning/milestones/v13.0-phases/103-brain-swap-control-surface/103-CONTRACT.md` §4 — the
      `mode: "session" | "pinned" | "inherited"` vocabulary this must produce.
  </read_first>
  <action>
**(a) D-12 mode derivation.** Add a module-level constant next to `ModelRouter` and use it inside
`_emit_model_routing` only (one source of truth, so `mode` can never disagree with the
`selectionPath` shipped alongside it):

`_MODE_BY_SELECTION_PATH` mapping `"profile-swap-override" -> "pinned"`,
`"global-swap-override" -> "pinned"`, `"session-override" -> "session"`. Derive with
`_MODE_BY_SELECTION_PATH.get(selection_path, "inherited")`.

The `.get(..., "inherited")` default arm is **mandatory and must not be replaced by an exhaustive
`match`/`if-elif` chain without a catch-all.** CONTEXT.md's D-12 lists 5 `selection_path` values; the
live code has **7** — it omits `"codepulse-default"` (`router.py:462`) and `"advisor"` (the
hardcoded literal at `:302-304` that never flows through `_resolve_model`). Both are live and
reachable today. A match with no default arm would raise or produce `None` for them.
`"profile-swap-override"` is the literal plan 108-04 will introduce; pre-registering it here means
108-04 adds no mapping code.

Add `"mode": mode` to the payload.

**(b) D-09 emit-on-change.** Add `self._last_routing_emit: dict[str, tuple] = {}` in
`ModelRouter.__init__`, keyed by profile id. In `_emit_model_routing`, after both refuse-to-emit
guards pass, build `key = (model, mode, selection_path, status)` and return early without sending if
it equals `self._last_routing_emit.get(profile_id)`; otherwise send and then store it. `status` is
part of the key deliberately: a repeated identical failure is suppressed, but a success following a
failure differs and emits. Log the suppression at debug (`reason="unchanged"`), not info.

This is contract-literal, not a deviation — 103-CONTRACT.md §4 says "after every resolution
**change**", and the only consumer (`activeEngine.latestByProfile`) reads exactly the newest row per
profile. It is also what keeps `activeEngineSnapshots` from becoming the highest-frequency table on a
single-node self-hosted instance that has already gone down twice from read growth.

**(c) Tests** in `tests/unit/providers/test_router.py`: a table-driven case asserting all 7 live
`selection_path` values plus `"profile-swap-override"` map to a value in
`{"pinned","session","inherited"}` with no exception and no `None` — explicitly including
`"codepulse-default"` and `"advisor"`; a case asserting `"global-swap-override"` → `"pinned"` and
`"session-override"` → `"session"`; and a dedup case asserting that two identical consecutive
resolutions for the same profile produce exactly ONE `telemetry.send("model_routing", ...)` call,
while a third with a different model produces a second call.
  </action>
  <verify>
    <automated>cd /c/Users/mandr/astridr-repo && pytest tests/unit/providers/test_router.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "_MODE_BY_SELECTION_PATH" astridr/providers/router.py` shows both the definition and a
      `.get(` call whose second argument is the string `"inherited"`.
    - `grep -c "codepulse-default" tests/unit/providers/test_router.py` and
      `grep -c '"advisor"' tests/unit/providers/test_router.py` both return `>= 1`.
    - `grep -n "_last_routing_emit" astridr/providers/router.py` shows an `__init__` assignment and at
      least one read inside `_emit_model_routing`.
    - The dedup test asserts a CALL COUNT on the telemetry mock (e.g. `== 1` then `== 2`), not a flag.
    - `pytest tests/unit/providers/test_router.py -x -q` exits 0.
  </acceptance_criteria>
  <done>`mode` is derived once from `selection_path` with a proven default arm covering all 7 live values, and repeated identical resolutions emit exactly one event.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| inbound channel / CodePulse WS → agent turn | Untrusted message text crosses here; the profile identity attached at this boundary governs every downstream attribution |
| Ástríðr process → CodePulse `/runtime-ingest` | Telemetry payload crosses a network boundary into a persisted Convex table |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-108-07 | Tampering | `_current_profile_id` ContextVar (`telemetry.py:95`) | mitigate | Token-paired `set_profile_context`/`reset_profile_context` in a `finally:` at both live set-points (Task 1), proven by a behavioral test that covers the exception path. Without the reset, a long-lived or reused task misattributes one profile's engine to another. |
| T-108-08 | Spoofing | `model_routing` payload's `profileId` | mitigate | `profileId` is read from the server-set ContextVar only — never from message content, never from a client-supplied field. A caller cannot assert a profile it is not running as. |
| T-108-09 | Information Disclosure | `structlog` debug lines added for the refuse/dedup paths | accept | Profile ids are not secrets — `telemetry.py:95` already carries the explicit `contextvar-ok: profile ID, not a secret` annotation. Model ids are public catalogue names. No key, token, or message text is logged by the new lines. |
| T-108-10 | Denial of Service | `activeEngineSnapshots` write volume | mitigate | D-09's per-profile emit-on-change memo (Task 3) keeps the highest-frequency code path in the system from producing one Convex write per LLM call on a single-node SQLite instance that has already gone down twice from read growth. Retention bound lands in plan 108-02. |
| T-108-11 | Repudiation | Silent refuse-to-emit | accept | Both refusal branches emit a `structlog` debug record with a `reason`, so "the axis is quiet" is diagnosable rather than invisible. Debug rather than warn because the no-profile case is the normal, expected state for cron and background work (D-02's accepted cost). |
| T-108-SC | Tampering | package installs | accept | This plan installs nothing — Python `contextvars` is stdlib and every other symbol already exists in the repo. 108-RESEARCH.md's Package Legitimacy Audit records "not applicable" for the whole phase. |
</threat_model>

<verification>
1. `cd /c/Users/mandr/astridr-repo && pytest tests/unit/providers/test_router.py tests/unit/channels/test_agent_processor_profile_context.py -x -q` exits 0.
2. `cd /c/Users/mandr/astridr-repo && pytest tests/ -q` — full suite green (wave-merge gate).
3. `grep -rc "selectedModel" astridr/ docs/ tests/` returns 0 outside `.planning/`.
4. Every commit made by this plan: `git show --stat HEAD` inspected, anything swept in from the
   concurrent session disclosed in the SUMMARY.
</verification>

<success_criteria>
- A `model_routing` event fired during a profiled turn carries a non-empty `profileId`, a real
  `model` id, and a `mode` from `{"pinned","session","inherited"}`.
- A resolution with no profile in context, or with no resolved model, produces zero telemetry sends.
- Two identical consecutive resolutions for one profile produce exactly one send.
- `selectedModel` no longer exists anywhere in `astridr/`, `docs/`, or `tests/`.
</success_criteria>

<output>
Create `.planning/phases/108-per-profile-engine-telemetry-astridr-backend/108-01-SUMMARY.md` when done.
Include: the line-anchor drift check result from Task 1, the Task 1 mutation-check result, and the
`git show --stat HEAD` output for every commit.
</output>
