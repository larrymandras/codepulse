---
phase: 91-3d-memory-galaxy
plan: 05
type: execute
wave: 3
depends_on: ["91-03"]
files_modified: []
autonomous: false
requirements: [G3D-02]

must_haves:
  truths:
    - "The 3D mode sustains ≥30 FPS against the live ~4,038-node graphSnapshots data (SC#3)"
    - "Toggling 2D↔3D 3+ times leaves no orphaned WebGL context / growing heap (SC#4 leak)"
  artifacts: []
  key_links:
    - from: "live graphSnapshots (Convex tidy-whale-981)"
      to: "ForceGraph3D render"
      via: "useProjectGraph in 3D mode"
      pattern: "graphSnapshots"
---

<objective>
Run the two manual GPU-scale gates that jsdom cannot assert: the ≥30 FPS benchmark (SC#3) against the live ~4,038-node snapshot, and the WebGL no-leak check (SC#4) across repeated 2D↔3D toggles.

Purpose: D-03 ships opt-in 3D with NO runtime fallback, so the ≥30 FPS target is verified once, manually, on the operator's machine against the live `graphSnapshots` data (not a synthetic graph) before shipping. D-03a makes clean WebGL disposal mandatory regardless. Both are blocking gates.
Output: operator pass/fail sign-off recorded in the SUMMARY (no source changes unless a tuning lever is applied).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/91-3d-memory-galaxy/91-RESEARCH.md
@.planning/phases/91-3d-memory-galaxy/91-UI-SPEC.md
@.planning/phases/91-3d-memory-galaxy/91-VALIDATION.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: ≥30 FPS gate against the live ~4,038-node snapshot (SC#3)</name>
  <action>Manual benchmark gate — no autonomous code change unless a tuning lever is applied. The operator runs 3D mode against the live ~4,038-node graphSnapshots data and measures sustained FPS in DevTools Performance per the how-to-verify steps; if it misses ≥30, apply the documented prop-tuning levers in order and re-measure; record final prop values + measured FPS in the SUMMARY. Do NOT add a runtime auto-degradation path (D-03 forbids it).</action>
  <what-built>
    The opt-in 3D render mode on CodeVaultGraph (plans 02-03) — react-force-graph-3d rendering the same `useProjectGraph()` snapshot the 2D mode uses, with the UI-SPEC prop baseline (nodeResolution 6, cooldownTicks 150, nodeRelSize 4, linkOpacity 0.2).
  </what-built>
  <how-to-verify>
    1. Ensure the live snapshot is available: `useProjectGraph()` reads the Convex `graphSnapshots` table from deployment `tidy-whale-981` (~4,038 nodes). If the live deployment is unreachable, load the saved JSON snapshot fallback noted in RESEARCH §Environment Availability. Do NOT use a synthetic graph (CONTEXT.md §Specific Ideas).
    2. `npm run dev` (port 5173), open the Graphs hub `CodeVaultGraph`, confirm the node count is ~4,038 (Both filter).
    3. Click the `3D` toggle. Wait for the simulation to settle (onEngineStop → zoomToFit; ~2-3 s).
    4. Open Chrome DevTools → Performance, record ~5 s of idle + light orbit interaction AFTER settle. Read sustained FPS (Frames track / FPS meter).
    5. PASS if sustained FPS ≥ 30 after settle. FAIL if it dips below 30 sustained.
    6. If FAIL, apply the tuning levers in order and re-measure (CONTEXT.md Claude's Discretion / UI-SPEC §3D Sphere Geometry Props): lower `nodeResolution` 6→4; reduce `cooldownTicks` (settle sooner / freeze after settle, e.g. `pauseAnimation()` on engine stop); reduce `linkWidth`/`linkOpacity`; as a last resort `nodeRelSize`. Re-record after each change. Record the final prop values and the measured FPS in the SUMMARY. Do NOT add a runtime auto-degradation path (D-03 forbids it).
  </how-to-verify>
  <verify>
    <human-check>Operator confirms sustained ≥30 FPS at ~4,038 nodes after settle, and records the final prop values + measured FPS.</human-check>
  </verify>
  <resume-signal>Type "approved" with the measured FPS, or describe the dip + which levers were tried.</resume-signal>
  <acceptance_criteria>
    - Benchmarked against live (or saved) ~4,038-node `graphSnapshots` data, NOT a synthetic graph
    - Sustained FPS ≥ 30 after simulation settle, measured in DevTools Performance
    - Final prop values recorded in the SUMMARY
  </acceptance_criteria>
  <done>Operator sign-off that 3D sustains ≥30 FPS at the live snapshot scale (SC#3).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: WebGL no-leak gate on repeated 2D↔3D toggle (SC#4)</name>
  <action>Manual leak gate — no autonomous code change unless the defensive cleanup fallback is needed. The operator toggles 2D↔3D 3+ times and compares DevTools Memory heap snapshots per the how-to-verify steps; if WebGL contexts accumulate, add the documented idempotent `renderer().dispose()` / `_destructor()` cleanup-on-unmount and re-measure. Record the result in the SUMMARY.</action>
  <what-built>
    The render-surface swap (plan 03): switching to 2D unmounts `<ForceGraph3D>`, and react-force-graph-3d's `_destructor` (react-kapsule → three-render-objects → `renderer.dispose()`) runs automatically on unmount per RESEARCH §Pattern 6.
  </what-built>
  <how-to-verify>
    1. With the dev app open on CodeVaultGraph, take a Chrome DevTools → Memory heap snapshot as a baseline (2D mode).
    2. Toggle 2D→3D→2D at least 3 times (let 3D mount/settle each time).
    3. Take another heap snapshot. Confirm there is no growing/orphaned `WebGLRenderingContext` (the live WebGL context count returns to baseline; no monotonic heap growth across the toggles).
    4. PASS if no orphaned WebGL context and heap returns to ~baseline. FAIL if WebGL contexts accumulate.
    5. If FAIL, add the defensive `useEffect` cleanup calling `fgRef3d.current?.renderer()?.dispose()` / `_destructor()` on unmount (RESEARCH notes `dispose()` is idempotent) and re-measure.
  </how-to-verify>
  <verify>
    <human-check>Operator confirms no orphaned WebGLRenderingContext and no monotonic heap growth after 3+ toggles (DevTools Memory).</human-check>
  </verify>
  <resume-signal>Type "approved" (no leak), or describe the accumulating contexts.</resume-signal>
  <acceptance_criteria>
    - 3+ full 2D↔3D toggle cycles performed
    - DevTools Memory shows no orphaned WebGLRenderingContext and no monotonic heap growth
  </acceptance_criteria>
  <done>Operator sign-off that repeated toggling disposes WebGL cleanly (SC#4 leak gate).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| WebGL context lifecycle | Repeated mount/unmount must release GPU resources |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-91-LEAK | Denial of Service | Orphaned WebGL contexts on repeated toggle | mitigate | Library disposes automatically on unmount (RESEARCH §Pattern 6); this plan is the manual leak gate that confirms it, with a documented idempotent defensive cleanup fallback if it regresses. |
| T-91-DOS | Denial of Service | <30 FPS on weak GPUs | accept | Opt-in mode, no runtime fallback (D-03); the FPS gate validates the operator's machine; weaker GPUs are explicitly allowed to run slower. |

No HIGH-severity threats. Both are correctness/performance gates on a client-side, no-PII render surface.
</threat_model>

<verification>
- Operator sign-off on both blocking checkpoints (SC#3 FPS, SC#4 leak)
- Any prop tuning applied is recorded; no runtime auto-degradation added (D-03)
</verification>

<success_criteria>
SC#3 (≥30 FPS at live ~4,038-node snapshot) and SC#4 (clean WebGL disposal on repeated toggle) verified by operator sign-off.
</success_criteria>

<output>
Create `.planning/phases/91-3d-memory-galaxy/91-05-SUMMARY.md` when done
</output>
