---
phase: 91-3d-memory-galaxy
plan: 04
type: execute
wave: 3
depends_on: ["91-03"]
files_modified: []
autonomous: true
requirements: [G3D-01]

must_haves:
  truths:
    - "A production build produces a separate ForceGraph3D/three.js lazy chunk"
    - "The main entry chunk (index-*.js) contains zero three.js code (SC#2)"
  artifacts: []
  key_links:
    - from: "vite build output"
      to: "dist/assets lazy chunk"
      via: "React.lazy dynamic-import boundary"
      pattern: "ForceGraph3D"
---

<objective>
Prove SC#2: a production `vite build` isolates `three`/`react-force-graph-3d` into their own lazy chunk so the default 2D path ships zero three.js.

Purpose: G3D-01's lazy-isolation guarantee is a build-time property that unit tests cannot observe. This plan runs the real build and asserts the main bundle is three.js-free while a separate 3D chunk exists.
Output: a verified build manifest (no committed file changes).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/91-3d-memory-galaxy/91-RESEARCH.md
@.planning/phases/91-3d-memory-galaxy/91-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build and assert three.js chunk isolation (SC#2)</name>
  <files></files>
  <read_first>
    - .planning/phases/91-3d-memory-galaxy/91-VALIDATION.md L45 (SC#2 row: `npm run build && grep -c "SphereGeometry" dist/assets/index-*.js` → expect 0)
    - .planning/phases/91-3d-memory-galaxy/91-RESEARCH.md §Pitfall 2 (L513-521 — static import bloats main bundle) and §"Runtime State Inventory" L579 (new `ForceGraph3D-[hash].js` lazy chunk expected)
  </read_first>
  <action>
    Run `npm run build`. After it succeeds, assert chunk isolation against `dist/assets/`: (1) the main entry chunk `index-*.js` must contain zero three.js markers — grep it for `SphereGeometry` (and `WebGLRenderer`) and expect a count of `0`; (2) a separate lazy chunk that contains the 3D code must exist (a chunk whose name includes `ForceGraph3D` or which contains `SphereGeometry`/`WebGLRenderer`). Use filtered greps (avoid counting matches in sourcemaps). If the main chunk contains three.js markers, the isolation has regressed (a static `three`/`react-force-graph-3d` import leaked outside `ForceGraph3D.tsx`) — trace and report the offending import; do NOT mark SC#2 passed. This task modifies no source and commits no files (the `dist/` output is build-only).
  </action>
  <verify>
    <automated>npm run build && test "$(grep -c 'SphereGeometry' dist/assets/index-*.js)" = "0" && grep -rlq 'SphereGeometry\|WebGLRenderer' dist/assets/ && echo SC2-PASS</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits 0
    - `grep -c 'SphereGeometry' dist/assets/index-*.js` prints `0`
    - `grep -c 'WebGLRenderer' dist/assets/index-*.js` prints `0`
    - At least one non-index chunk in `dist/assets/` contains `SphereGeometry` or `WebGLRenderer` (the isolated 3D chunk exists)
  </acceptance_criteria>
  <done>Production build green; three.js confined to its own lazy chunk; main entry chunk three.js-free (SC#2).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build → served bundle | Rollup code-splitting decides what ships in the default chunk |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-91-SC | Tampering | three.js leaking into the main bundle | mitigate | This plan IS the build-manifest gate that detects a leaked static three.js import; a non-zero `SphereGeometry` count in `index-*.js` fails the gate. |

No HIGH-severity threats. Verification-only plan; no source or dependency changes.
</threat_model>

<verification>
- `npm run build` exits 0
- main entry chunk has zero three.js markers; a separate 3D chunk exists
</verification>

<success_criteria>
SC#2 proven: the 2D/default bundle ships zero three.js; the 3D code lives in its own lazy chunk.
</success_criteria>

<output>
Create `.planning/phases/91-3d-memory-galaxy/91-04-SUMMARY.md` when done
</output>
