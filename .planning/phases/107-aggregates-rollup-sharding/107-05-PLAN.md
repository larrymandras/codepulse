---
phase: 107-aggregates-rollup-sharding
plan: 05
type: execute
wave: 4
depends_on: ["107-04"]
files_modified:
  - .planning/phases/107-aggregates-rollup-sharding/107-OCC-EVIDENCE.md
autonomous: true
requirements: [OCC-01]

must_haves:
  truths:
    - "D-01: After the deploy, newly written `events` and `sankey_edge` rows carry an integer `shard` in the 0..7 range produced by the single `AGGREGATE_SHARD_COUNT` constant."
    - "D-02: The deploy changes only the `events` and `sankey_edge` write path. No `cost` / `tokens` / `tokens_prompt` / `tokens_completion` behavior is touched, and the post-deploy smoke check does not need to look at them."
    - "D-03: `events.ingest` still performs its three read-patch-or-insert round trips after the deploy — the deployed diff added a parameter, not a round trip."
    - "D-04: The deploy is an additive optional-field schema push. It must print `No indexes are deleted by this push`, and it must not rewrite, patch, or delete any of the ~1.94M existing rows — nothing in this plan runs an import, a bulk patch, or a bulk delete against the live self-hosted instance."
    - "D-05: The pre-deploy baseline in `107-OCC-BASELINE.md` MUST already exist and read `BASELINE COMPLETE` before this plan runs; once deployed, that evidence can no longer be captured. This plan produces the post-deploy read-total check only — the after-window OCC rate belongs to plan 107-06."
  artifacts:
    - path: ".planning/phases/107-aggregates-rollup-sharding/107-OCC-EVIDENCE.md"
      provides: "Deploy output with target and index assertions, plus the immediate post-deploy shard-presence and read-total checks"
      contains: "No indexes are deleted by this push"
      min_lines: 30
  key_links:
    - from: "107-OCC-BASELINE.md"
      to: "this plan's deploy task"
      via: "a blocking precondition — the deploy aborts unless the baseline artifact exists and reads BASELINE COMPLETE"
      pattern: "BASELINE COMPLETE"
    - from: "107-OCC-EVIDENCE.md § D"
      to: "107-06-PLAN.md (after-window measurement)"
      via: "the recorded DEPLOY_UTC timestamp, which starts the clock for the after-window"
      pattern: "DEPLOY_UTC"
---

<objective>
Push the sharding change to the live self-hosted Convex backend, prove the push was non-destructive and landed on the right instance, and immediately re-run the read-total spot-check so a "fixed contention, broke the fold" outcome is caught within minutes instead of at the end of the phase.

Purpose: OCC-01 names a live operational outcome. Nothing measured so far touches the live system's write path. This is the plan where the change becomes real, and it is also the plan where the two live-deploy risks specific to this repo are checked: landing on the cloud deployment instead of the self-hosted one, and an unexpected index change on an instance whose MVCC tombstone GC cannot absorb index churn while serving load.

**Hard precondition.** `.planning/phases/107-aggregates-rollup-sharding/107-OCC-BASELINE.md` must exist and end with `BASELINE COMPLETE`. If it does not, STOP — do not deploy. The pre-deploy OCC evidence cannot be recovered after the fact, and a phase that deploys first has destroyed the only proof it will ever have.

Output: `.planning/phases/107-aggregates-rollup-sharding/107-OCC-EVIDENCE.md`. No source file is modified — this plan deploys code that plan 107-03 already committed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/107-aggregates-rollup-sharding/107-OCC-BASELINE.md
@.planning/phases/107-aggregates-rollup-sharding/107-VALIDATION.md
@.planning/phases/107-aggregates-rollup-sharding/107-RESEARCH.md
@CLAUDE.md

<interfaces>
<!-- Everything the executor needs. Do not go exploring for these. -->

Deploy command and the two strings that must appear in its output:

    npx convex deploy --yes
    -> the printed target must resolve to the SELF-HOSTED instance: http://127.0.0.1:3210
    -> the output must contain: No indexes are deleted by this push

`package.json` also defines `npm run deploy` as `npx convex deploy && npx vite build`. Do NOT use it here:
this phase changes backend code only, and a frontend build is out of scope and adds unrelated risk.

Queries used by the post-deploy checks (same contracts plan 107-04 already exercised):

    npx convex run aggregates:eventCountsByPeriod   args { period: string, lookbackDays?: number }  // lookbackDays may be fractional
    npx convex run events:listRecent                args { limit?: number }                          // last 48h, DESC, timestamp in epoch seconds

Schema shape now live after the push (`convex/schema.ts`), both indexes unchanged:

    aggregates: { metric_type, period, bucket_start, value, dimensions?, shard? }
      .index("by_type_period_bucket", ["metric_type", "period", "bucket_start"])
      .index("by_period_bucket", ["period", "bucket_start"])
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Deploy to the self-hosted instance and assert the push was non-destructive</name>
  <files>.planning/phases/107-aggregates-rollup-sharding/107-OCC-EVIDENCE.md</files>
  <read_first>
    - `.planning/phases/107-aggregates-rollup-sharding/107-OCC-BASELINE.md` — confirm it exists, read `WINDOW_HOURS` and the closing status line. If the closing line is not `BASELINE COMPLETE`, stop here.
    - `CLAUDE.md` — the "Self-Hosted Convex — Operational Rules" section. It is the governing constraint: never `--replace-all` against the live instance, never bulk-delete or bulk-patch a large table, and treat a dashboard-wide "no data" as index rot until proven otherwise.
    - `.planning/phases/107-aggregates-rollup-sharding/107-RESEARCH.md` — the "Schema Change" section, which records the repo's established two-part deploy verification (confirm the printed target, then confirm the no-index-deletion line) and its five prior precedents across Phases 103-105.
    - `convex/schema.ts` — confirm by sight that the `aggregates` block has the added optional `shard` field and that both `.index(...)` lines are unchanged from `git show HEAD~1:convex/schema.ts`.
  </read_first>
  <action>
1. **Gate.** Verify `.planning/phases/107-aggregates-rollup-sharding/107-OCC-BASELINE.md` exists and contains a line beginning `BASELINE COMPLETE`. If it does not, abort the plan, write nothing, and report the missing baseline in the SUMMARY.

2. **Pre-flight, in this order, all recorded verbatim in the artifact:**
   - `git status --porcelain` — the working tree must be clean for `convex/`; a deploy of uncommitted code is not reproducible.
   - `git rev-parse --short HEAD` — record the commit being deployed.
   - `npx tsc --noEmit` — must exit 0.
   - `npm test` — must be fully green. Do not deploy on a red suite.
   - `docker ps --filter name=convex-backend --format '{{.Status}}'` and `docker inspect -f '{{.State.StartedAt}}' convex-backend` — record both; the second value must still match what plan 107-04 recorded, proving the log stream carrying the baseline is still intact.

3. **Deploy.** Run `npx convex deploy --yes` from the repo root and capture the COMPLETE output (stdout and stderr together) into the artifact verbatim. Do not run `npm run deploy`.

4. **Assert both halves of the deploy verification, and treat either failure as a hard stop:**
   - The printed deployment target must contain `127.0.0.1:3210`. If the output names a `.convex.cloud` host or any other target, the CLI resolved to the wrong backend — record it, do NOT proceed to Task 2, and report it as a blocker. This repo's live backend is the self-hosted instance; a cloud push would be both wrong and misleading.
   - The output must contain the literal string `No indexes are deleted by this push`. If it does not — or if the output mentions any index being added, removed, or rebuilt — record the exact wording and STOP. Index churn on this instance is the mechanism behind the 2026-07-21/22 outage and is not something to push through.
   Record each assertion as its own PASS/FAIL line with the quoted evidence.

5. Record `DEPLOY_UTC: <ISO-8601 UTC timestamp>` on its own line. Plan 107-06 uses this to know when the after-window may begin.

6. **Post-deploy liveness.** Wait 60 seconds, then confirm the backend is serving: `docker ps --filter name=convex-backend --format '{{.Status}}'` still shows the container up with the SAME `StartedAt` (the push must not have restarted it), and one `npx convex run aggregates:eventCountsByPeriod` call with `{"period":"hourly","lookbackDays":0.05}` returns a non-empty object. Per CLAUDE.md, a dashboard-wide empty result after a push is index rot or memory starvation until proven otherwise — if the query returns empty, check `docker stats --no-stream convex-backend` and record it before drawing any conclusion.

Create `.planning/phases/107-aggregates-rollup-sharding/107-OCC-EVIDENCE.md` with a section headed `## D — Deploy` containing all of the above.
  </action>
  <verify>
    <automated>grep -q 'No indexes are deleted by this push' .planning/phases/107-aggregates-rollup-sharding/107-OCC-EVIDENCE.md &amp;&amp; grep -q '127.0.0.1:3210' .planning/phases/107-aggregates-rollup-sharding/107-OCC-EVIDENCE.md &amp;&amp; grep -qE '^DEPLOY_UTC: [0-9]{4}-' .planning/phases/107-aggregates-rollup-sharding/107-OCC-EVIDENCE.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/107-aggregates-rollup-sharding/107-OCC-EVIDENCE.md` exists with a `## D — Deploy` section.
    - The artifact quotes the deploy output verbatim and it contains the literal `No indexes are deleted by this push`.
    - The artifact contains an explicit PASS line naming `127.0.0.1:3210` as the resolved deploy target, quoted from the deploy output — not asserted from configuration.
    - `DEPLOY_UTC: <ISO-8601>` is present on its own line.
    - The pre-flight block records `git rev-parse --short HEAD`, a clean `git status --porcelain` for `convex/`, `npx tsc --noEmit` exit 0, and a green `npm test`.
    - The `StartedAt` value recorded before the deploy matches the one recorded after it — the container was not restarted, so the baseline log window is still continuous with the post-deploy window.
    - The post-deploy liveness query returned a non-empty object, or the artifact records `docker stats` output and an explicit investigation instead of an unexamined empty result.
    - The artifact contains no occurrence of `--replace-all`, `convex import`, or any bulk patch/delete command.
    - `git status --porcelain convex src` is empty — this plan changed no source file.
  </acceptance_criteria>
  <done>The sharding code is live on the self-hosted backend, the push is proven non-destructive against both indexes, the target is proven to be the self-hosted instance from the CLI's own output, and the deploy moment is timestamped for the after-window.</done>
</task>

<task type="auto">
  <name>Task 2: Post-deploy shard-presence and read-total spot-check</name>
  <files>.planning/phases/107-aggregates-rollup-sharding/107-OCC-EVIDENCE.md</files>
  <read_first>
    - `.planning/phases/107-aggregates-rollup-sharding/107-OCC-BASELINE.md` — section C, the pre-deploy read-total control: the `H0`/`H1`/`A`/`B` method, the 60-second cutoff offset, the coverage guard, and the recorded caveats. This task repeats that method exactly so the two results are comparable.
    - `.planning/phases/107-aggregates-rollup-sharding/107-VALIDATION.md` — "Dangerous (False-Green) Assertions" item 5: a lower OCC count alone is not proof, because contention can be reduced while a fold is silently broken.
    - `convex/lib/aggregateShard.ts` — the shard range that live rows must fall inside.
  </read_first>
  <action>
Append a section headed `## E — Post-deploy shard presence and read totals` to `107-OCC-EVIDENCE.md`.

**Shard presence.** Wait at least 10 minutes after `DEPLOY_UTC` so live ingest has written some sharded rows, then confirm real rows carry the field:

- First establish whether the CLI subcommand exists rather than assuming it: run `npx convex data --help` and record the output. Do not build the check around a subcommand you have not confirmed in the installed version.
- If `npx convex data` is available, list recent `aggregates` rows (for example `npx convex data aggregates --limit 50`) and record: how many of the listed rows carry a `shard` field, the distinct shard values observed, and confirmation that every observed value is an integer in `0..7`. Record it as `SHARD_PRESENCE: OBSERVED <k> distinct values <list>`.
- If the subcommand is NOT available in this CLI version, do not fabricate a check and do not add a query to the codebase for it. Record `SHARD_PRESENCE: DEFERRED — npx convex data unavailable in convex <version>; to be confirmed at plan 107-06's checkpoint via the self-hosted Convex dashboard` and note the dashboard URL. Plan 107-06's human checkpoint picks it up.
- Rows written before the deploy legitimately have no `shard` field. Their presence in the listing is expected and correct (D-04) — do not treat it as a failure.

**Read-total spot-check.** Repeat section C's method against the most recent COMPLETE hour, using the current clock:

- `H0` = `floor(now/3600)*3600 - 3600`, `H1` = `H0 + 3600`.
- `A` = summed values from `npx convex run aggregates:eventCountsByPeriod` with `lookbackDays = (now - (H0 - 60)) / 86400`.
- `B` = the same with `lookbackDays = (now - (H1 - 60)) / 86400`.
- `POST_BUCKET_TOTAL = A - B`.
- `POST_RAW_EVENT_COUNT` = count of events from `npx convex run events:listRecent` with `{"limit":5000}` whose `timestamp` is in `[H0, H1)`.
- Apply the same mandatory coverage guard: the minimum returned `timestamp` must be strictly less than `H0`, otherwise raise the limit and re-run. Record the observed minimum and whether the guard passed.

Record both numbers and an explicit verdict line: `READ_TOTALS: MATCH` or `READ_TOTALS: MISMATCH — bucket <x> vs raw <y>, delta <d>`.

**Interpretation rules, to be written into the artifact rather than left to judgement:**
- A MISMATCH that did not exist in the pre-deploy control (section C) is a REGRESSION and a phase blocker — the sharded write path is undercounting or a fold is broken. Record it and stop; do not wait for the OCC measurement to "confirm" a fix on top of a broken read.
- A MISMATCH of the same size and direction as the pre-deploy control is pre-existing, not caused by sharding. Record it as such with both numbers side by side.
- If the chosen hour spans `DEPLOY_UTC`, the bucket for that hour contains both unsharded and sharded rows. That is fine and is exactly the D-04 mixed-state case worth exercising — note explicitly in the artifact whether the measured hour spans the deploy.

End the section with `POST-DEPLOY CHECK: PASS` or `POST-DEPLOY CHECK: FAIL — <reason>`.
  </action>
  <verify>
    <automated>grep -qE '^READ_TOTALS: (MATCH|MISMATCH)' .planning/phases/107-aggregates-rollup-sharding/107-OCC-EVIDENCE.md &amp;&amp; grep -qE '^SHARD_PRESENCE: (OBSERVED|DEFERRED)' .planning/phases/107-aggregates-rollup-sharding/107-OCC-EVIDENCE.md &amp;&amp; grep -qE '^POST-DEPLOY CHECK: (PASS|FAIL)' .planning/phases/107-aggregates-rollup-sharding/107-OCC-EVIDENCE.md</automated>
  </verify>
  <acceptance_criteria>
    - The artifact contains a `## E — Post-deploy shard presence and read totals` section.
    - `SHARD_PRESENCE:` is present and reads either `OBSERVED` with the distinct shard values listed, or `DEFERRED` with the reason and the confirmed `npx convex data --help` output showing the subcommand is unavailable.
    - If `OBSERVED`, every listed shard value is an integer in `0..7`, and the artifact states that pre-deploy rows lacking `shard` are expected under D-04.
    - The read-total block records `H0`, `H1`, `A`, `B`, `POST_BUCKET_TOTAL`, `POST_RAW_EVENT_COUNT`, and the observed minimum returned timestamp with the coverage-guard result.
    - `READ_TOTALS: MATCH` or `READ_TOTALS: MISMATCH` is present with both numbers.
    - If MISMATCH, the artifact compares it explicitly against the pre-deploy control in `107-OCC-BASELINE.md` section C and classifies it as REGRESSION or PRE-EXISTING.
    - The artifact states whether the measured hour spans `DEPLOY_UTC`.
    - The section ends with `POST-DEPLOY CHECK: PASS` or `POST-DEPLOY CHECK: FAIL`.
    - No new query, mutation, or log statement was added to the codebase to support this check: `git status --porcelain convex src` is empty.
  </acceptance_criteria>
  <done>Within minutes of the deploy there is recorded evidence that live rows carry a valid shard and that the analytics totals still equal the raw event counts — so a broken fold cannot hide behind a falling OCC rate later.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local repo → live self-hosted Convex backend | Crossed here for the first time in this phase. A schema push and function push reach production. |
| deploy CLI target resolution | The CLI decides which backend receives the push; a wrong resolution would push to the retired cloud deployment |
| backend output → committed artifact | Deploy output and query results are pasted into a committed markdown file |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-107-12 | Spoofing | `npx convex deploy` resolving to the cloud deployment instead of self-hosted | mitigate | The resolved target must be quoted from the CLI's own output and must contain `127.0.0.1:3210`. Any other target is a hard stop before Task 2 runs. Configuration is not accepted as evidence of target. |
| T-107-13 | Denial of service | An index add/remove/rebuild on the live instance | mitigate | The deploy must print `No indexes are deleted by this push`, and any mention of an index being added or rebuilt is a hard stop. This is the mechanism behind the 2026-07-21/22 tombstone-poisoning outage. |
| T-107-14 | Tampering | Bulk mutation of the ~1.94M existing rows | mitigate | An acceptance criterion asserts the artifact contains no `--replace-all`, `convex import`, or bulk patch/delete command. No such command may be run. |
| T-107-15 | Repudiation | A deploy of uncommitted or untested code | mitigate | Pre-flight records a clean `git status` for `convex/`, the deployed commit hash, `tsc --noEmit` exit 0, and a green `npm test`, all before the push. |
| T-107-16 | Information disclosure | Deploy output or query results containing credentials | mitigate | Inspect the captured output before pasting. Redact any admin key, bearer token, or `sb_`/`eyJ`-shaped value as `<redacted>`; never paste `.env` contents or an admin key into the artifact. |
| T-107-SC | Tampering | npm/pip/cargo installs | mitigate | This plan installs nothing; `convex ^1.42.0` is already in `node_modules`. `npm install` must not be run as part of the deploy. |
</threat_model>

<verification>
- `.planning/phases/107-aggregates-rollup-sharding/107-OCC-EVIDENCE.md` contains sections D and E.
- The deploy output is quoted verbatim and contains `No indexes are deleted by this push`.
- The resolved deploy target is quoted and contains `127.0.0.1:3210`.
- `DEPLOY_UTC`, `SHARD_PRESENCE`, `READ_TOTALS`, and `POST-DEPLOY CHECK` lines are all present.
- `docker inspect -f '{{.State.StartedAt}}' convex-backend` is unchanged across the whole plan — the container was never restarted, so plan 107-06's after-window is measurable from the same log stream.
- `git status --porcelain convex src` is empty.
</verification>

<success_criteria>
- The sharding change is live on the self-hosted backend, deployed from a clean, type-checked, fully-tested commit.
- The push is proven non-destructive to both indexes and to existing rows, from the CLI's own output rather than from expectation.
- Live analytics totals still equal raw event counts after the change, checked by the same method that was validated before it.
- The deploy moment is timestamped so the after-window in plan 107-06 starts from a known point.
</success_criteria>

<output>
Create `.planning/phases/107-aggregates-rollup-sharding/107-05-SUMMARY.md` when done. Lead with `DEPLOY_UTC`, the two deploy assertions, and the `READ_TOTALS` verdict, and state the earliest wall-clock time at which plan 107-06 may take its after-window measurement (that is `DEPLOY_UTC` plus `WINDOW_HOURS` from `107-OCC-BASELINE.md`).
</output>
