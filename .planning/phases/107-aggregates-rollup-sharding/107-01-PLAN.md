---
phase: 107-aggregates-rollup-sharding
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - convex/analyticsRollup.test.ts
  - convex/events.test.ts
autonomous: true
requirements: [OCC-01]

must_haves:
  truths:
    - "D-01: The write-path tests pass EXPLICIT shard values in the range 0..7 and reference `AGGREGATE_SHARD_COUNT`, so no test depends on a random draw — and a separate ingest-level test proves the draw itself lands in that range."
    - "D-02: Every fixture in this plan is `metric_type: \"events\"` or `metric_type: \"sankey_edge\"` only — no `cost`/`tokens`/`tokens_prompt`/`tokens_completion` fixture is added, because those metric types are out of scope."
    - "D-03: One `events.ingest` call is asserted to produce exactly THREE aggregate writes (one events bucket plus two sankey edges), so a future refactor that collapses the round trips fails the test instead of passing silently."
    - "D-04: A test proves that a pre-existing `aggregates` row with NO `shard` field is never patched by a sharded write, and that the bucket's summed total across the legacy row plus the new sharded rows is still correct."
    - "D-05: This plan produces NO live evidence. Green unit tests here must never be presented as proof that OCC contention dropped — that evidence is owned by plans 107-04 and 107-06 alone."
  artifacts:
    - path: "convex/analyticsRollup.test.ts"
      provides: "Wave-0 write-path shard contract: explicit-shard call sites, shard-split assertions, legacy-unsharded-row assertion, pickShard range contract"
      contains: "different explicit shards"
    - path: "convex/events.test.ts"
      provides: "Wave-0 ingest contract: one shard drawn per ingest call and shared by all three aggregate writes"
      contains: "_handler"
  key_links:
    - from: "convex/analyticsRollup.test.ts"
      to: "convex/analyticsRollup.ts incrementEventBucket / incrementSankeyBuckets"
      via: "the existing @vite-ignore dynamic-import guard (beforeAll)"
      pattern: "incrementEventBucket\\(ctx as any, [^)]*, [0-9]"
    - from: "convex/events.test.ts"
      to: "convex/events.ts ingest"
      via: "the repo's `._handler` convention against a fake ctx.db (aggregates.test.ts:25-33)"
      pattern: "\\(ingest as any\\)\\._handler"
---

<objective>
Write the Wave-0 write-path contract for aggregate sharding, BEFORE any production code changes.

Purpose: two independent reasons this must land first.
1. **Determinism.** The moment `incrementEventBucket` draws its own shard via `Math.random()`, the existing "increment patch-or-insert" test (`convex/analyticsRollup.test.ts:193-208`) becomes genuinely flaky — the two calls would land on different shards 7 times out of 8 and INSERT instead of PATCH, failing `toHaveLength(1)`. Making shard an explicit parameter and pinning it in the test is a correctness prerequisite, not cleanup.
2. **Regression guard.** Nothing in the suite currently proves that a bucket spanning multiple rows still totals correctly, that a legacy row with no `shard` field is handled, or that one ingest call draws exactly one shard and shares it across all three writes.

These tests are EXPECTED TO BE RED until plan 107-03 lands. That is the established Wave-0 pattern in this very file (see its Phase 88 header comment and the `beforeAll` dynamic-import guard at lines 92-104).

Output: `convex/analyticsRollup.test.ts` updated in place, plus a new `convex/events.test.ts`. No production file is modified by this plan.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/107-aggregates-rollup-sharding/107-CONTEXT.md
@.planning/phases/107-aggregates-rollup-sharding/107-RESEARCH.md
@.planning/phases/107-aggregates-rollup-sharding/107-PATTERNS.md
@.planning/phases/107-aggregates-rollup-sharding/107-VALIDATION.md
@CLAUDE.md

<interfaces>
<!-- Contracts the executor needs. Do not go exploring for these. -->

Current production signatures — `convex/analyticsRollup.ts` (plan 107-03 adds a trailing `shard: number` to all three):

    export async function incrementEventBucket(ctx: MutationCtx, eventType: string, timestamp: number): Promise<void>
    export async function incrementSankeyBuckets(ctx: MutationCtx, eventType: string, toolName: string | undefined, timestamp: number): Promise<void>
    async function incrementSankeyEdge(ctx: MutationCtx, hourStart: number, source: string, target: string): Promise<void>   // module-private

Row shapes written to the `aggregates` table by the write path today:

    { metric_type: "events",      period: "hourly", bucket_start: <hour>, value: <n>, dimensions: { event_type } }
    { metric_type: "sankey_edge", period: "hourly", bucket_start: <hour>, value: <n>, dimensions: { source, target } }

`convex/events.ts` — the `ingest` mutation under test in Task 3:

    export const ingest = mutation({
      args: { sessionId, eventType, toolName?, filePath?, payload, hookType?, timestamp, idempotencyKey? },
      handler: ...
    })
    // Flow: optional idempotencyKey dedup via events.by_idempotencyKey .first() -> early return;
    //       ctx.db.insert("events", {...});
    //       incrementEventBucket(...);  incrementSankeyBuckets(...)  // 2 sankey edge writes inside
    // => exactly THREE aggregates writes per non-deduped ingest call.

Constant module created by plan 107-03 — `convex/lib/aggregateShard.ts`:

    export const AGGREGATE_SHARD_COUNT = 8;
    export function pickShard(): number;   // integer in [0, AGGREGATE_SHARD_COUNT)

Sankey edge derivation (already live, unchanged) — `convex/lib/sankeyClassify.ts`:
one event produces exactly two edges, `categoryOf(eventType) -> (toolName ?? eventType)` and `(toolName ?? eventType) -> outcomeOf(eventType)`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Make every existing write-path call site shard-explicit</name>
  <files>convex/analyticsRollup.test.ts</files>
  <read_first>
    - `convex/analyticsRollup.test.ts` — the whole file. Specifically: the `makeStore()` harness (lines 22-53), the `eventBucket()` filter helper (56-62), the `RollupModule` type declaration (71-91), the `beforeAll` dynamic-import guard (92-104), the `ingestWithDedup` helper (109-129), and the four existing `incrementEventBucket` call sites at lines 127, 200, 204, and 224.
    - `convex/analyticsRollup.ts` lines 26-107 — the current unsharded `incrementEventBucket` / `incrementSankeyBuckets` / `incrementSankeyEdge` bodies you are writing the contract against.
    - `.planning/phases/107-aggregates-rollup-sharding/107-RESEARCH.md` — the "Pitfall 1" section, on why an implicit random draw makes this file flaky.
    - `.planning/phases/107-aggregates-rollup-sharding/107-PATTERNS.md` section 3 — the `makeStore()` convention and the note that its fake `withIndex()` IGNORES the index predicate.
  </read_first>
  <action>
Update `convex/analyticsRollup.test.ts` so every existing write-path invocation passes an explicit shard.

1. Extend the hand-written `RollupModule` type: change `incrementEventBucket` to `(ctx: any, eventType: string, timestamp: number, shard: number) => Promise<void>`, and ADD `incrementSankeyBuckets?: (ctx: any, eventType: string, toolName: string | undefined, timestamp: number, shard: number) => Promise<void>` (it is not currently declared). Leave `accumulateEvent` and `accumulateLlmTokens` untouched. This is a hand-written type on a dynamically imported module, so `npx tsc --noEmit` stays clean whether or not the production signature has landed yet.

2. Give `ingestWithDedup` an optional `shard` field on its `args` parameter, defaulting to `0`, and forward it as the fourth argument to `rollup.incrementEventBucket`. Do not change its dedup behavior — the two "idempotency invariants" tests must keep passing unchanged.

3. Pass explicit shard `0` at the three remaining call sites: the two calls inside the "increment patch-or-insert" test (currently lines 200 and 204) and the loop call inside the "backfill count-equality" test (currently line 224). Add a short comment at the patch-or-insert test explaining that the shard is pinned deliberately: with a random draw these two calls would land on different shards about 7 times in 8 and the test would flake.

4. Add a second dynamic-import guard, mirroring the existing `beforeAll` block verbatim in style (non-literal specifier plus `/* @vite-ignore */`, `try/catch` to `null`), that loads `./lib/aggregateShard` into a `shardLib` variable typed as `{ AGGREGATE_SHARD_COUNT?: number; pickShard?: () => number } | null`. The module does not exist until plan 107-03, so this must degrade to `null` rather than erroring the file.

5. Do NOT touch the `classifier` describe block or either `accumulate*` describe block. Do NOT change the `makeStore()` harness itself.

Note for anyone grepping this area: `convex/analyticsRollup.ts` embeds literal NUL bytes as accumulator-key separators (template literals at roughly lines 179, 192 and 218). Ripgrep and `grep` therefore report it as "binary file matches". Those NULs are intentional — do not "fix" them. Use `grep -a` if you need to grep that file.
  </action>
  <verify>
    <automated>npx tsc --noEmit &amp;&amp; npx vitest run convex/analyticsRollup.test.ts -t "idempotency"</automated>
  </verify>
  <acceptance_criteria>
    - `convex/analyticsRollup.test.ts` contains zero remaining three-argument `incrementEventBucket(` invocations. Verified by: `grep -aoE 'incrementEventBucket\(ctx as any,[^)]*\)' convex/analyticsRollup.test.ts` — every match ends in a numeric fourth argument.
    - The `RollupModule` type declares `shard: number` as the fourth parameter of `incrementEventBucket` and declares `incrementSankeyBuckets` with a fifth `shard: number` parameter.
    - `ingestWithDedup` accepts an optional `shard` and defaults it to `0`.
    - A `shardLib` dynamic-import guard exists that references the specifier `./lib/aggregateShard` and catches to `null`.
    - `npx tsc --noEmit` exits 0.
    - `npx vitest run convex/analyticsRollup.test.ts -t "idempotency"` passes — the two pre-existing idempotency tests are unaffected by the signature change.
    - `git status --porcelain convex/analyticsRollup.ts convex/events.ts convex/schema.ts` is empty — this task changes no production file.
  </acceptance_criteria>
  <done>Every write-path call in the test file supplies an explicit shard, and the type declaration and import guards are ready for the production signature that plan 107-03 will land.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Shard-split and legacy-unsharded write-path tests (RED until 107-03)</name>
  <files>convex/analyticsRollup.test.ts</files>
  <behavior>
    - Two `incrementEventBucket` calls with the SAME eventType, hour, and shard produce exactly ONE row, `value === 2`, `shard === 0`.
    - Two `incrementEventBucket` calls with the same eventType and hour but DIFFERENT explicit shards (0 and 3) produce exactly TWO rows, each `value === 1`, with shard values `{0, 3}`, and the summed value across both rows is 2.
    - A pre-seeded legacy row (`metric_type: "events"`, matching `dimensions.event_type`, NO `shard` key at all) is NOT patched by a subsequent shard-0 write: the legacy row's `value` is unchanged, a new row with `shard === 0` is inserted, and the sum across all matching rows equals legacy value + 1.
    - One `incrementSankeyBuckets` call writes exactly TWO `sankey_edge` rows, both carrying the shard that was passed in.
    - Two `incrementSankeyBuckets` calls for the same event with different explicit shards produce FOUR `sankey_edge` rows (2 edges x 2 shards), and each distinct `{source,target}` edge sums to 2.
    - `pickShard()` returns an integer in `[0, AGGREGATE_SHARD_COUNT)` across 200 draws, and `AGGREGATE_SHARD_COUNT === 8`.
  </behavior>
  <read_first>
    - `convex/analyticsRollup.test.ts` — the file as left by Task 1, especially the `makeStore()` harness and the `eventBucket()` helper you will build fixtures with.
    - `convex/analyticsRollup.ts` lines 26-107 — the current bodies, so the assertions describe the intended post-change behavior precisely.
    - `.planning/phases/107-aggregates-rollup-sharding/107-PATTERNS.md` sections 1 and 3 — the `billingType` legacy-default precedent and the `makeStore()` convention.
    - `.planning/phases/107-aggregates-rollup-sharding/107-VALIDATION.md` — "Dangerous (False-Green) Assertions" item 4 (row-count-only assertions).
  </read_first>
  <action>
Add a new `describe("shard", ...)` block to `convex/analyticsRollup.test.ts`, placed immediately after the existing "increment patch-or-insert" block. Every test name in this block must contain the word `shard` so `npx vitest run convex/analyticsRollup.test.ts -t "shard"` selects the whole set.

Build fixtures with the existing `makeStore()` harness — do not invent a new one. Critical harness property to design around (from 107-PATTERNS.md section 3): the fake `withIndex()` ignores the index predicate entirely and returns the whole `aggregates` array, so the production code's own JS `.find()` is what does the matching. Consequence: keep every fixture inside a SINGLE hour and a single `metric_type` per test, or rows from another hour/type will be matched by the fake.

Write these tests:

1. `"same explicit shard patches the same row"` — two `incrementEventBucket(ctx, "llm_call", 1_700_000_000, 0)` calls. Assert `store.aggregates` has length 1, its `value` is 2, and its `shard` is 0.

2. `"different explicit shards insert separate rows that still sum to the full count"` — `incrementEventBucket(ctx, "llm_call", 1_700_000_000, 0)` then the same call with shard `3`. Assert length 2; assert the set of `shard` values is exactly `{0, 3}`; assert each row's `value` is 1; and assert the SUM of `value` across both rows is 2. Assert the sum, not just the count — a count assertion alone is a false-green.

3. `"a legacy row with no shard field is not patched by a sharded write (D-04)"` — before calling anything, push a legacy row directly onto `store.aggregates`: `{ _id: "legacy", metric_type: "events", period: "hourly", bucket_start: 1_700_000_000, value: 42, dimensions: { event_type: "llm_call" } }` with NO `shard` key. Then call `incrementEventBucket(ctx, "llm_call", 1_700_000_000, 0)`. Assert: the legacy row's `value` is STILL 42; `store.aggregates` has length 2; the new row has `shard === 0` and `value === 1`; and the sum across both rows is 43. This encodes the deliberate choice that the write-path match is a STRICT `r.shard === shard` comparison rather than `(r.shard ?? 0) === shard` — strict equality retires the pre-deploy hot row instead of letting it keep absorbing 1/8 of live writes, and the read side stays correct because every reader sums all matching rows regardless of shard.

4. `"one incrementSankeyBuckets call writes exactly two edges carrying the passed shard"` — one call with `eventType: "tool_use"`, `toolName: "Read"`, `timestamp: 1_700_000_000`, shard 2. Assert exactly 2 rows with `metric_type === "sankey_edge"`, both `shard === 2`, and that their `{source, target}` pairs are the two distinct edges derived by `categoryOf`/`outcomeOf` (import those from `./lib/sankeyClassify`, which this file already imports, and derive the expected pairs rather than hardcoding strings).

5. `"different shards split each sankey edge without changing the edge totals"` — the same call twice with shards 1 and 5. Assert 4 `sankey_edge` rows total; group them by `${source}::${target}` and assert there are exactly 2 distinct edges, each summing to `value` 2.

6. `"pickShard returns an integer within AGGREGATE_SHARD_COUNT"` — guard on `shardLib` (throw a clear "not implemented (plan 107-03)" error if null, matching the existing guard style in this file). Assert `shardLib.AGGREGATE_SHARD_COUNT === 8`; run 200 `pickShard()` draws and assert every result satisfies `Number.isInteger(v) && v >= 0 && v < AGGREGATE_SHARD_COUNT`. Do NOT assert on the distribution of draws — that would be a flaky statistical test.

Every test that depends on the not-yet-shipped signature must guard on `rollup` / `shardLib` and THROW a descriptive error (the existing convention at lines 197-199), so these RED cleanly instead of erroring the file at import time.
  </action>
  <verify>
    <automated>npx tsc --noEmit &amp;&amp; npx vitest run convex/analyticsRollup.test.ts --reporter=verbose 2>&amp;1 | grep -q "different explicit shards insert separate rows"</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run convex/analyticsRollup.test.ts --reporter=verbose` lists all six new tests by name, proving they are collected and executed (they are expected to FAIL until plan 107-03 — a red result here is the correct outcome, an "0 tests collected" result is not).
    - `test $(grep -a -v '^[[:space:]]*//' convex/analyticsRollup.test.ts | grep -c 'shard') -ge 20` succeeds (comment lines excluded so the count is not self-satisfied by prose).
    - The file contains the literal string `AGGREGATE_SHARD_COUNT`.
    - Test 3's assertions include an unchanged-legacy-value check (`42`) AND a cross-row sum check (`43`) — not a row-count check alone.
    - Tests 2 and 5 each assert a SUMMED value, not only a row count.
    - No test asserts on the statistical distribution of `pickShard()` results.
    - `npx tsc --noEmit` exits 0.
    - `git status --porcelain convex/analyticsRollup.ts convex/events.ts convex/schema.ts convex/lib` is empty — this task changes no production file.
  </acceptance_criteria>
  <done>The suite now encodes, as executable assertions, exactly what sharding must do to the write path — including the two cases nothing covered before: cross-shard summing and the legacy unsharded row.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: New convex/events.test.ts — one shard per ingest call, shared by all three writes</name>
  <files>convex/events.test.ts</files>
  <behavior>
    - One `events.ingest` call produces exactly THREE `aggregates` rows: one `metric_type: "events"` and two `metric_type: "sankey_edge"`.
    - All three rows written by a single ingest call carry the SAME `shard` value — proving one draw was made and passed down, not three independent draws.
    - That shard value is an integer in `[0, 8)`.
    - Across 20 ingest calls, more than one distinct shard value is observed — proving the draw is per-call and random, not a constant.
    - An ingest call that is deduplicated by `idempotencyKey` writes no event row and no aggregate row at all.
  </behavior>
  <read_first>
    - `convex/events.ts` lines 1-48 — the whole `ingest` mutation, including the `idempotencyKey` dedup early return and the two helper calls.
    - `convex/aggregates.test.ts` lines 24-33 and 155-180 — the repo's `._handler` convention for exercising a registered mutation against a fake ctx, and the shape of that fake `db` (this is the pattern to copy; there is no `convex-test` in this repo).
    - `convex/analyticsRollup.test.ts` lines 22-53 — the `makeStore()` harness, whose `db` shape (query/withIndex/collect/first, insert, patch) is what the increment helpers need.
    - `.planning/phases/107-aggregates-rollup-sharding/107-VALIDATION.md` — the `write-02` row, which is the requirement this file satisfies.
  </read_first>
  <action>
Create `convex/events.test.ts`. This file does not exist today.

Import `ingest` from `./events` as a normal static import (the module exists and compiles now — unlike `./lib/aggregateShard`, no dynamic-import guard is needed).

Build a small fake ctx modeled on `makeStore()` from `analyticsRollup.test.ts`, backing two tables (`events` and `aggregates`) with plain arrays and supporting `query(table).withIndex(...).collect()`, `.first()`, `insert(table, doc)`, and `patch(id, doc)`. Keep it local to this file rather than exporting it from the other test file — the two harnesses serve different callers and coupling them makes both harder to change.

Invoke the real registered mutation through `await (ingest as any)._handler(ctx, args)`, exactly as `aggregates.test.ts` does for `computeHourly`.

Write these tests, all named so `-t "shard"` selects the shard ones:

1. `"one ingest call writes three aggregate rows sharing one shard"` — call the handler once with `{ sessionId: "s1", eventType: "tool_use", toolName: "Read", payload: {}, timestamp: 1_700_000_000 }`. Assert: `store.events` has length 1; `store.aggregates` has exactly 3 rows; exactly 1 has `metric_type === "events"` and exactly 2 have `metric_type === "sankey_edge"`; the set of distinct `shard` values across all three rows has size 1; and that single value is an integer satisfying `v >= 0 && v < 8`.
   This is the behavioral form of the "drawn once per ingest and passed explicitly" constraint. Do NOT settle for asserting the source text of `events.ts` — assert the observable outcome, which is that all three rows agree on one shard.

2. `"shard varies across ingest calls"` — run 20 ingest calls, each with a FRESH store so the three rows of each call can be inspected in isolation, and collect the per-call shard value. Assert every per-call value is an integer in `[0, 8)`, and assert the number of DISTINCT values across the 20 calls is greater than 1. A constant shard would pass test 1 and is exactly what this catches. (Flake risk is `(1/8)^19`, effectively zero — but do not tighten this into a distribution assertion, which would be genuinely flaky.)

3. `"a deduplicated ingest writes no event and no aggregate rows"` — call the handler twice with the same `idempotencyKey`. Assert `store.events` has length 1 and `store.aggregates` still has exactly 3 rows (the second call short-circuited before any write). This pins the ordering constraint that the shard draw and the aggregate writes both sit AFTER the dedup early return.

Until plan 107-03 lands, tests 1 and 2 are RED (the rows will have no `shard` field) and test 3 is GREEN (dedup behavior is unchanged). That split is correct and expected.
  </action>
  <verify>
    <automated>npx tsc --noEmit &amp;&amp; npx vitest run convex/events.test.ts --reporter=verbose 2>&amp;1 | grep -q "one ingest call writes three aggregate rows sharing one shard"</automated>
  </verify>
  <acceptance_criteria>
    - `convex/events.test.ts` exists and contains the literal `(ingest as any)._handler`.
    - `npx vitest run convex/events.test.ts --reporter=verbose` collects and runs all three tests by name. Tests 1 and 2 are expected RED until plan 107-03; test 3 must be GREEN now.
    - Test 1 asserts the aggregate row count is exactly 3 AND asserts the distinct-shard-value set has size 1 — both, not either.
    - Test 2 asserts the distinct shard count across 20 calls is greater than 1, and contains no assertion about how evenly the values are distributed.
    - Test 3 asserts both `store.events.length === 1` and that the aggregate row count did not grow past 3.
    - The fake ctx is defined locally in `convex/events.test.ts`; nothing is exported from `convex/analyticsRollup.test.ts` to support it.
    - `npx tsc --noEmit` exits 0.
    - `git status --porcelain convex/events.ts convex/analyticsRollup.ts convex/schema.ts convex/lib` is empty — this task changes no production file.
  </acceptance_criteria>
  <done>The "one shard per ingest, shared by all three writes" constraint — the phase's central correctness rule — is now a behavioral assertion on the real mutation handler, not a source-text grep.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test fixtures → production write path | Fixtures are in-memory only; no boundary is crossed into the live instance by this plan |
| repo → live self-hosted Convex | NOT crossed by this plan. No deploy, no `npx convex run`, no docker command. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-107-01 | Tampering | `convex/analyticsRollup.ts` NUL-byte accumulator keys (roughly lines 179/192/218) | mitigate | Do not edit that file in this plan at all; if grepped, use `grep -a` and treat the NULs as intentional separators, never as corruption to repair. |
| T-107-02 | Repudiation | A green unit suite being mistaken for OCC-01 evidence | mitigate | The plan objective and truth D-05 state explicitly that this plan produces no live evidence; the SUMMARY must repeat that. |
| T-107-22 | Repudiation | Proving the one-draw-per-ingest rule with a source grep instead of behavior | mitigate | Task 3 asserts on the observable outcome — all three written rows agreeing on one shard value — through the real `ingest._handler`, per the "verify the real outcome, not the proxy" rule. |
| T-107-SC | Tampering | npm/pip/cargo installs | mitigate | This plan installs nothing. Vitest ^4.1.9 is already wired. Any `npm install` in this plan is out of scope and must be refused. |
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0.
- `npx vitest run convex/analyticsRollup.test.ts -t "idempotency"` passes (pre-existing behavior preserved).
- `npx vitest run convex/analyticsRollup.test.ts convex/events.test.ts --reporter=verbose` collects and runs six new `shard` tests plus three ingest tests; the shard ones are RED, and each failure message names the missing shard parameter, the missing `./lib/aggregateShard` module, or an absent `shard` field on the written rows — NOT a syntax/import error that killed the file.
- `npx vitest run convex/events.test.ts -t "deduplicated"` is GREEN now.
- `git status --porcelain convex/analyticsRollup.ts convex/events.ts convex/schema.ts convex/lib` is empty.
</verification>

<success_criteria>
- Zero write-path call sites in the test suite rely on an implicit shard.
- Cross-shard summing, the D-04 legacy-unsharded row, the three-writes-per-ingest invariant, the one-shard-per-ingest rule, and the `pickShard` range contract are all expressed as assertions on observable outcomes rather than row counts or source text.
- The suite fails red for the right reasons and every file still compiles and collects.
</success_criteria>

<output>
Create `.planning/phases/107-aggregates-rollup-sharding/107-01-SUMMARY.md` when done. State explicitly which tests are RED by design and name plan 107-03 as the one that turns them green.
</output>
