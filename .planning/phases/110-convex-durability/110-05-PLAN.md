---
phase: 110-convex-durability
plan: 05
type: execute
wave: 4
depends_on: ["110-04"]
files_modified:
  - C:/Users/mandr/convex-selfhost/retention-health-check.ps1
  - .planning/phases/110-convex-durability/110-DUR-EVIDENCE.md
autonomous: false
requirements: [DUR-02]

must_haves:
  truths:
    - "D-07: retention-health-check.ps1 derives its table list from the deployed RETENTION_DAYS at runtime, and the hand-copied 14-entry hashtable with its keep-in-sync comment is deleted outright"
    - "D-08: a health-check run showing every table in RETENTION_DAYS caught up is captured verbatim as the second of DUR-02's two independent pieces of live evidence"
    - "The health check prints a table count equal to the deployed policy's key count, so a run that still covers only 14 tables is a visible failure rather than a clean pass"
    - "A failed or empty policy read makes the health check exit non-zero rather than fall back to any hardcoded list"
    - "The edit to the unversioned ops script is captured as evidence with a before/after hash and a restorable backup, since git will not record it until Phase 113"
  artifacts:
    - path: "C:/Users/mandr/convex-selfhost/retention-health-check.ps1"
      provides: "A health check whose table list is a live read of the deployed retention policy"
      contains: "listRetentionPolicy"
    - path: ".planning/phases/110-convex-durability/110-DUR-EVIDENCE.md"
      provides: "DUR-02 leg 2: a full health-check run over every table, with its count control"
      contains: "DUR-02 leg 2"
  key_links:
    - from: "C:/Users/mandr/convex-selfhost/retention-health-check.ps1"
      to: "convex/retention.ts"
      via: "npx convex run retention:listRetentionPolicy replacing the hand-copied hashtable"
      pattern: "retention:listRetentionPolicy"
---

<objective>
Make `retention-health-check.ps1` read the live retention policy instead of a hand-copied list, then capture a full run as DUR-02's second evidence leg.

Purpose: the copy in that script holds 14 entries against `RETENTION_DAYS`' 18 — `gatewayQuotaSnapshots`, `toolPolicyEvents`, `activeEngineSnapshots` and `controlVerbSwaps` have been invisible to every health check that has ever run. The 2026-08-10 05:30 log line "all tables caught up" was therefore a claim about 14 tables, not all of them, and `aggregates` would have become the fifth silent omission the moment plan 110-03 shipped. A health check that can silently cover a subset is worse than none, because it produces a green nobody re-examines.

Output: an edited ops script, and a run whose printed table count is cross-checked against the deployed policy's key count.

This script lives at `C:/Users/mandr/convex-selfhost/` and is NOT under version control until Phase 113's debt sweep, so the edit itself must be captured as evidence.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@.planning/phases/110-convex-durability/110-CONTEXT.md
@.planning/phases/110-convex-durability/110-RESEARCH.md
@.planning/phases/110-convex-durability/110-PATTERNS.md
@.planning/phases/110-convex-durability/110-VALIDATION.md
@.planning/phases/110-convex-durability/110-04-SUMMARY.md

<interfaces>
<!-- Extracted from the live script this session. -->

`retention-health-check.ps1` structure (212 lines, read live 2026-08-10):
  line 38        the "Keep in sync if that map changes" comment
  lines 39-54    the $RetentionDays hashtable, 14 entries:
                 runtime_events, toolExecutions, activeTime, selfHealingEvents, fileOps,
                 heartbeatAlerts, events, environmentSnapshots, contextSnapshots,
                 metricSnapshots, securityEvents, cronExecutions, jobLifecycle,
                 agentCoordination
  lines 95-130   the per-table probe loop — PRESERVE UNCHANGED. Its loop variables are
                 already $table and $days, which is exactly what the replacement
                 foreach produces, so no rename is needed anywhere else in the script.

Quoting constraints that are load-bearing in this file:
  - The per-table probe uses single-quoted JS string literals inside cmd /c deliberately,
    so the wrapping does not have to fight PowerShell/cmd quote parsing.
  - cmd's captured output is a String[] when it spans lines, and -match against an array
    filters elements instead of setting $matches, so output is join-flattened before matching.
  - One CLI call per table: a looped inline-query over a table-name array silently
    produced no output when tried 2026-07-30.
  - PS 5.1: Get-Content reads BOM-less UTF-8 as ANSI, so a read/replace/write round trip
    mojibakes non-ASCII. Use [IO.File]::ReadAllText / WriteAllText for in-place edits.
  - PS 5.1: assigning to an automatic variable ($input, $PID, $HOME) errors while every
    downstream check silently runs against an empty value. Use non-reserved names only.

The function form that shipped (internalQuery vs query) is recorded in 110-04-SUMMARY.md.
Write the invocation against that proven form, not against an assumption.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace the hand-copied policy hashtable with a live read of the deployed map</name>
  <files>C:/Users/mandr/convex-selfhost/retention-health-check.ps1</files>

  <read_first>
    - `C:/Users/mandr/convex-selfhost/retention-health-check.ps1` (full file, 212 lines) — the `$RetentionDays` hashtable at lines 39-54 and its comment at line 38, the per-table probe loop at 95-130, and how `$CodepulseDir` and `$EnvFile` are defined above them.
    - `.planning/phases/110-convex-durability/110-PATTERNS.md` §`retention-health-check.ps1` — the verbatim probe-loop quote and the exact replacement shape.
    - `.planning/phases/110-convex-durability/110-04-SUMMARY.md` — which function form shipped and the exact CLI invocation proven to reach it.
    - `C:/Users/mandr/convex-selfhost/retention-health.log` — the verdict-line format the script writes, so a changed table count is legible in the log rather than only on stdout.
  </read_first>

  <action>
Back the script up first: copy it to `C:/Users/mandr/convex-selfhost/retention-health-check.ps1.pre-110.bak` and record `Get-FileHash` of the original. This file is not in git until Phase 113, so the backup and the hash are the only rollback path that exists.

Delete the `$RetentionDays` hashtable (lines 39-54) and its "Keep in sync if that map changes" comment (line 38) outright — D-07 is explicit that the copy goes, not that it gets updated. Replace them with a live read of the deployed policy, using the CLI invocation form recorded in `110-04-SUMMARY.md`: build the JSON from a `cmd /c "cd /d ... && npx convex run retention:listRetentionPolicy --env-file ... 2>&1"` call, join-flatten it the way the rest of the script flattens `cmd` output, then `ConvertFrom-Json` it into a policy object.

Change the probe loop's header from `foreach ($table in $RetentionDays.Keys)` to iterate the policy object's `PSObject.Properties.Name`, with `$days` read from the corresponding property. The loop BODY stays byte-identical — it already uses `$table` and `$days`.

This invocation needs no nested quoting at all, unlike the inline-query probes below it, because there is no JS literal to quote. That is the point of the design; do not reintroduce an `--inline-query` form here.

Add a hard failure path. If the policy read fails, returns non-JSON, or yields zero properties, the script logs a distinct verdict naming the policy read as the cause and exits non-zero WITHOUT falling back to any hardcoded list. A fallback would silently recreate the exact defect D-07 removes, and a health check that degrades to a stale subset while still printing a verdict is the failure mode this plan exists to eliminate. Zero properties is a failure, not an empty policy.

Add the probed table count to the run's log line, so a run covering the wrong number of tables is visible in `retention-health.log` after the fact rather than only at the terminal.

Edit with `[IO.File]::ReadAllText` / `WriteAllText` rather than `Get-Content`/`Set-Content` — PS 5.1 reads BOM-less UTF-8 as ANSI and a round trip would mojibake the script's non-ASCII comment characters. After writing, diff the new file against the backup and confirm the only changed region is the replaced block plus the new verdict line.
  </action>

  <verify>
    <automated>powershell -NoProfile -Command "[System.Management.Automation.Language.Parser]::ParseFile('C:/Users/mandr/convex-selfhost/retention-health-check.ps1',[ref]$null,[ref]$null) | Out-Null; exit 0"</automated>
  </verify>

  <acceptance_criteria>
    - `C:/Users/mandr/convex-selfhost/retention-health-check.ps1.pre-110.bak` exists and its `Get-FileHash` matches the pre-edit hash recorded in the task summary.
    - The script parses without error under the PowerShell parser (the `<verify>` command exits 0). A parse failure here means the scheduled task would silently do nothing.
    - `Select-String -SimpleMatch '$RetentionDays' retention-health-check.ps1` returns zero hits, and `Select-String -SimpleMatch 'Keep in sync'` returns zero hits.
    - `Select-String -SimpleMatch 'retention:listRetentionPolicy' retention-health-check.ps1` returns exactly one hit.
    - No hardcoded table-name list remains anywhere in the file: none of `runtime_events`, `toolExecutions`, `agentCoordination` appears outside a comment. Run the check with comment lines filtered out, not as a bare count — a bare grep counts the comment prose describing the removal and reports a false positive.
    - A `diff` of the edited script against the `.pre-110.bak` backup shows changes only in the replaced region and the verdict line, with the 95-130 probe-loop body unchanged apart from its `foreach` header.
    - The file is still BOM-less UTF-8 and its non-ASCII comment characters are intact (spot-check the em-dashes in the header comment against the backup).
  </acceptance_criteria>

  <done>The hand-copied policy is gone, the script reads the deployed map through a proven CLI invocation, a failed read is a hard non-zero exit rather than a silent subset, and the original is backed up with a recorded hash.</done>
</task>

<task type="auto">
  <name>Task 2: Run the health check and capture DUR-02 leg 2 with its table-count control</name>
  <files>.planning/phases/110-convex-durability/110-DUR-EVIDENCE.md</files>

  <read_first>
    - The edited `C:/Users/mandr/convex-selfhost/retention-health-check.ps1`.
    - `C:/Users/mandr/convex-selfhost/retention-health.log` — the tail, for the pre-edit verdict lines. The 2026-08-10 05:30 entry reading `verdict=OK ... all tables caught up` over 14 tables is the control this run is measured against.
    - `.planning/phases/110-convex-durability/110-DUR-EVIDENCE.md` — the file this task appends to, and the deployed policy readback recorded there in plan 110-04.
    - `.planning/phases/110-convex-durability/110-VALIDATION.md` §"Manual-Only Verifications" — the binding contract for this leg, including its required control.
  </read_first>

  <action>
Run the edited script once, attended, and append the result to `110-DUR-EVIDENCE.md` under `## DUR-02 leg 2 — health check covers every table`.

Paste the full run output verbatim, plus the new `retention-health.log` line it wrote. Then record the control explicitly, as three numbers in one place: the count of tables the run probed, `Object.keys(RETENTION_DAYS).length` from the working-tree source, and the key count of the deployed `listRetentionPolicy` readback recorded in plan 110-04. All three must be equal. A run that prints 14 is the pre-existing defect; a run that prints 18 means `aggregates` did not deploy; only equality across all three is a pass.

Also record the four previously-invisible tables by name — `gatewayQuotaSnapshots`, `toolPolicyEvents`, `activeEngineSnapshots`, `controlVerbSwaps` — plus `aggregates`, each with the per-table status the run gave it. Their presence by name is what makes this leg evidence rather than a count that could be reached some other way.

Note honestly what each per-table status does and does not prove. `empty-or-caught-up` is ambiguous between pruned, empty, and nothing-aged-out — it is not evidence that a prune ran. This leg's claim is bounded to coverage: every table in the policy is now visible to the check. The claim that a complete pass actually executed belongs to leg 1, in plan 110-06, and must not be asserted here.

Any table returning `TIMEOUT` is a fail for this leg. Record it verbatim with the table name rather than re-running until it passes, and stop — a probe that times out on this instance is a signal about instance health, and repeating it is exactly the read pressure CLAUDE.md's operational rules warn against.

Do not modify the `ConvexRetentionHealthCheck` scheduled task. It already runs this script daily at 05:30; the edit takes effect on its own.
  </action>

  <verify>
    <automated>grep -c "DUR-02 leg 2" .planning/phases/110-convex-durability/110-DUR-EVIDENCE.md</automated>
  </verify>

  <acceptance_criteria>
    - The evidence file contains a `## DUR-02 leg 2` section with the full run output and the new `retention-health.log` line pasted verbatim.
    - Three counts are stated together and are equal: tables probed by the run, `Object.keys(RETENTION_DAYS).length` from source, and the deployed policy readback's key count from plan 110-04.
    - All five named tables (`gatewayQuotaSnapshots`, `toolPolicyEvents`, `activeEngineSnapshots`, `controlVerbSwaps`, `aggregates`) appear in the pasted output with a per-table status.
    - Zero tables report `TIMEOUT`.
    - The section's verdict states explicitly that this leg proves coverage, not that a prune executed, and defers the completed-pass claim to leg 1.
    - The verdict sits below the pasted output.
    - No credential value appears in the pasted transcript.
  </acceptance_criteria>

  <done>A health-check run covering every table in the deployed policy is on the record, cross-checked three ways against the source and the deployed map, with the ambiguity of a per-table "caught up" reading stated rather than papered over.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Operator confirms the health check on the live box and the scheduled run</name>
  <files>C:/Users/mandr/convex-selfhost/retention-health-check.ps1, .planning/phases/110-convex-durability/110-DUR-EVIDENCE.md</files>

  <what-built>
    `retention-health-check.ps1`'s 14-entry hand-copied policy table was deleted and replaced by a live read of the deployed `retention:listRetentionPolicy`, with a hard non-zero exit if that read fails or returns nothing — no fallback list. The original is backed up at `retention-health-check.ps1.pre-110.bak` with its hash recorded. One attended run is captured in `110-DUR-EVIDENCE.md` as DUR-02 leg 2, cross-checked three ways on table count.
  </what-built>

  <how-to-verify>
    1. Open `C:/Users/mandr/convex-selfhost/retention-health.log` and read the newest entry. Confirm it names a table count equal to the deployed policy's key count, and that the four previously-invisible tables plus `aggregates` are listed.
    2. Run the script once yourself and confirm it completes without a parse error and without any `TIMEOUT` row.
    3. Break it deliberately, once: temporarily point the policy read at a nonexistent function name, run it, and confirm it exits non-zero with the policy-read failure verdict rather than falling back to a table list. Restore. This is the only way to know the hard-failure path exists rather than being described — a health check that degrades silently is the exact defect this plan removes.
    4. Confirm `Get-ScheduledTask ConvexRetentionHealthCheck` is still enabled and unmodified, and that its next run time is tomorrow 05:30.
    5. Confirm `C:/Users/mandr/convex-selfhost/retention-health-check.ps1.pre-110.bak` exists — this script is not in git until Phase 113, so that backup is the only rollback.
  </how-to-verify>

  <resume-signal>Type "approved" if the run covers every table and the deliberate-break test exits non-zero, or describe what failed.</resume-signal>

  <action>
Pause. Present the newest `retention-health.log` entry, the run output captured in Task 2, and the location of the `.pre-110.bak` backup. Ask the operator to run the script themselves and to perform the deliberate-break test described below, since a hard-failure path that has never been seen fire is not known to exist. Do not modify the `ConvexRetentionHealthCheck` scheduled task while waiting.
  </action>

  <verify>
    <automated>grep -c "DUR-02 leg 2" .planning/phases/110-convex-durability/110-DUR-EVIDENCE.md</automated>
  </verify>

  <done>Operator has run the script, confirmed every table is covered, and confirmed the deliberate-break test exits non-zero without falling back to a hardcoded list — with the response recorded verbatim in the plan summary.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ops script → live production backend | The health check issues read-only Convex CLI calls against the live self-hosted instance with admin credentials from an env file. |
| unversioned filesystem → committed evidence file | The edited script is outside git until Phase 113, so its content and its hash reach the repository only through the evidence file. |
| scheduled task → unattended execution | The edited script runs daily at 05:30 with no operator present. A defect introduced here surfaces as a silently wrong verdict, not as an error anyone sees. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-110-05-01 | Repudiation | health-check verdict | mitigate | This is the phase's central integrity concern: a monitor that silently covers a subset produces a green nobody re-examines, which is exactly how four tables went unwatched. Mitigated by removing the hand-copied list entirely rather than updating it, by forbidding any fallback list on a failed read, by making a failed or empty policy read a non-zero exit, by logging the probed table count into the persistent log, and by an operator deliberate-break test proving the failure path fires. |
| T-110-05-02 | Information Disclosure | evidence transcript | mitigate | The script's `cmd /c` lines contain an `--env-file` path and the CLI carries an admin key. Mitigated by eliding credential arguments in the pasted transcript, by the file's existing no-secrets preamble, and by a credential-shape scan before commit. `npx convex env list` remains forbidden — against this self-hosted backend it prints full `NAME=VALUE`. |
| T-110-05-03 | Denial of Service | live `convex-backend` | mitigate | The probe loop issues one CLI call per table and the table count rises from 14 to 19, increasing read pressure on an instance already near its limits. Mitigated by preserving the existing one-call-per-table probe shape unchanged, by keeping every probe a `take(1)` oldest-doc read, and by treating a `TIMEOUT` as a recorded fail rather than something to retry. |
| T-110-05-04 | Tampering | unversioned ops script | mitigate | An edit to a file git does not track is unrecoverable if it goes wrong and invisible to review. Mitigated by a hashed `.pre-110.bak` backup taken before any edit, by a diff against that backup bounding the changed region, and by pasting the change into the evidence file so the repository holds a record until Phase 113 brings the directory into git. |
| T-110-05-05 | Denial of Service | `ConvexRetentionHealthCheck` scheduled task | mitigate | A PS 5.1 parse error makes the daily task a silent no-op — no output, no alert, and the monitor is simply gone. Mitigated by a parser check in `<verify>` that must exit 0 before the task closes, by `[IO.File]::ReadAllText`/`WriteAllText` to avoid the ANSI round-trip corruption that produces exactly this class of parse error, and by an operator run in the checkpoint. |
| T-110-05-SC | Tampering | npm/pip/cargo installs | accept | No packages installed. `npx convex` resolves the version already pinned in the repo's lockfile, and the PowerShell edit introduces no module import. |
</threat_model>

<verification>
- The script parses under `[System.Management.Automation.Language.Parser]::ParseFile` with no errors.
- `Select-String -SimpleMatch '$RetentionDays'` and `'Keep in sync'` both return zero hits.
- The evidence file's three table counts are equal and stated together.
- Zero `TIMEOUT` rows in the captured run.
- `ConvexRetentionHealthCheck` is still enabled with an unmodified action.
- `retention-health-check.ps1.pre-110.bak` exists with its hash recorded.

<human-check>
Operator ran the script, saw every table covered, and confirmed the deliberate-break test exits non-zero without falling back to a hardcoded list.
</human-check>
</verification>

<success_criteria>
- No copy of the retention policy exists outside `convex/retention.ts`.
- The health check covers every table in the deployed policy, proven by three agreeing counts and by the five formerly-omitted tables appearing by name.
- A failed policy read is a loud non-zero exit, demonstrated by an operator-run break test rather than asserted.
- This leg's claim is scoped to coverage; the completed-pass claim is left to leg 1.
- The unversioned edit is recoverable and recorded.
</success_criteria>

<output>
Append to `.planning/phases/110-convex-durability/110-DUR-EVIDENCE.md` and create `.planning/phases/110-convex-durability/110-05-SUMMARY.md` when done. Record the pre-edit and post-edit file hashes, the three table counts, and the operator's resume signal verbatim.
</output>
