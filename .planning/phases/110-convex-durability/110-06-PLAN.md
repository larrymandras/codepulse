---
phase: 110-convex-durability
plan: 06
type: execute
wave: 5
depends_on: ["110-05"]
files_modified:
  - .planning/phases/110-convex-durability/110-DUR-EVIDENCE.md
autonomous: false
requirements: [DUR-01, DUR-02]

must_haves:
  truths:
    - "D-08: the chain's own terminal 'retention: all tables pruned' line is pulled verbatim from the running container's logs, and it names a start index of 0 covering every table in the policy"
    - "D-01: the oldest period:daily aggregate row is unchanged or older after a real prune, and the oldest period:hourly row has moved forward — a shrinking daily population is a hard fail that blocks the phase"
    - "D-05: the nightly run started at the index the rotation cursor named, proven from the run's own start-index log line rather than inferred"
    - "D-06: the rotation cursor exists as exactly one agentConfigs row after the run, patched not inserted, whose absence before deploy is already on the record as the control"
    - "Operator signs off that a complete nightly prune pass was observed on the live self-hosted instance, not read from code"
  artifacts:
    - path: ".planning/phases/110-convex-durability/110-DUR-EVIDENCE.md"
      provides: "DUR-02 leg 1 and the DUR-01 live before/after confirmation, each under its raw transcript"
      contains: "DUR-02 leg 1"
  key_links:
    - from: ".planning/phases/110-convex-durability/110-DUR-EVIDENCE.md"
      to: "convex/retention.ts"
      via: "the terminal log line emitted by the deployed pruneBatchV3"
      pattern: "retention: all tables pruned"
---

<objective>
Close DUR-01 and DUR-02 on live evidence: observe a real nightly prune pass on the self-hosted instance, pull the chain's own terminal line from the container's logs, and prove against plan 110-04's baseline that daily aggregate rows survived while hourly rows aged out.

Purpose: DUR-02's whole point is that the pass is confirmed from the running system rather than read from code. A green unit suite says the predicate is correct in principle; it says nothing about whether the chain reached the end of its table list on a real instance under real load. And the DUR-01 claim that matters — Phase 104's re-priceable cost history survived — is only checkable as a before/after against a baseline captured before the first prune ran.

Output: two evidence sections and an operator sign-off.

This plan cannot start until the 09:00 UTC cron has fired at least once against the code deployed in plan 110-04. That latency is structural, not slack.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@.planning/phases/110-convex-durability/110-CONTEXT.md
@.planning/phases/110-convex-durability/110-PATTERNS.md
@.planning/phases/110-convex-durability/110-VALIDATION.md
@.planning/phases/110-convex-durability/110-04-SUMMARY.md
@.planning/phases/110-convex-durability/110-05-SUMMARY.md

<interfaces>
<!-- What the deployed code emits, from convex/retention.ts as modified by plan 110-03. -->

Terminal line, emitted once at the true end of a full chain pass:
  retention: all tables pruned (start index N, covered M of K tables)

Cap line, emitted instead when the nightly batch ceiling is reached:
  retention: nightly batch cap (600) hit at <table> after pruning <n> docs; remainder deferred to tomorrow (start index N)

Per-table line, emitted when a table drained with a nonzero count:
  retention: <table> done, pruned <n> docs

Start line:
  retention: nightly prune started   (plus the resolved start index)

Rotation cursor after a run that reached `done`:
  agentConfigs row, configKey "retention.rotationCursor", value 0, updatedAt in epoch SECONDS.
  Recorded as ABSENT in the plan 110-04 pre-deploy baseline — that absence is the control.

Cron: convex/crons.ts:165-168, `retention-prune`, daily at 09:00 UTC.

Baseline probes to re-run for comparison (plan 110-04, same commands):
  oldest period:"daily"  row via by_period_bucket, .order('asc').take(1)
  oldest period:"hourly" row via by_period_bucket, .order('asc').take(1)
  bounded daily count at .take(1000)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pull DUR-02 leg 1 — the terminal pass line — from the running container's logs</name>
  <files>.planning/phases/110-convex-durability/110-DUR-EVIDENCE.md</files>

  <read_first>
    - `.planning/phases/110-convex-durability/110-DUR-EVIDENCE.md` — the file this appends to, and plan 110-04's recorded deploy time, which bounds the log window.
    - `.planning/phases/110-convex-durability/110-VALIDATION.md` §"Manual-Only Verifications" — the binding contract for this leg, including the requirement to bound `docker logs` with BOTH `--tail` and `--since`.
    - `C:/Users/mandr/convex-selfhost/health-report.md` §7 — this project's own logging-rotation lesson, which is why `--since` alone is not acceptable.
    - `convex/retention.ts` as deployed — the exact literal prefixes of the four log lines above.
    - `CLAUDE.md` §"Self-Hosted Convex — Operational Rules".
  </read_first>

  <action>
Append `## DUR-02 leg 1 — a complete pass observed on the running instance` to `110-DUR-EVIDENCE.md`.

Pull the retention lines from the running container with `docker logs convex-backend` bounded by BOTH `--tail <N>` and `--since <window covering the 09:00 UTC fire>`. Never `--since` alone: log rotation makes an unbounded window return a misleadingly thin slice, and a thin slice reads identically to "the line was never emitted."

Paste, verbatim: the `retention: nightly prune started` line with its resolved start index, every `retention: <table> done, pruned <n> docs` line the run emitted, and the terminal line.

**The pass condition, stated precisely.** The terminal line must read `start index 0` and `covered K of K tables`, where K equals the deployed policy's key count recorded in plans 110-04 and 110-05. A terminal line reading a nonzero start index is a real completion of a partial sweep — the chain reached the end of the array from where the rotation put it — and it is NOT DUR-02's evidence. If that is what appears, record it verbatim, state why it does not close the leg, and wait for a night that starts at 0. The first post-deploy run is expected to start at 0 because the rotation row did not exist before deploy and `resolveRotationStart` defaults a missing value to 0; if it does not, something wrote that row and the file must say so.

**Controls, both required.** An absence in a log is a claim about the search window and the search string, not about the system.
1. *Positive control:* the same bounded window must contain other `retention:` lines — at minimum the start line. If the window contains no retention output at all, the window is wrong or the cron did not fire, and no conclusion about the terminal line is available yet.
2. *Known-absent control:* grep the identical window for a string that cannot be present (for example `retention: definitely-not-a-real-line-9x7q2`) and record the empty result. This is what proves the grep would have found the terminal line had it been emitted.

If the run hit the batch cap instead of completing, paste the cap line — it names the table it stopped at and the start index — record that leg 1 is not yet closed, and note that the rotation cursor should now hold that table's index so tomorrow resumes there. That is D-05 working, not a failure; it just is not this leg's evidence.

Do not hand-trigger the cron to produce a line. A manually-invoked run starts from a rotation state the operator chose, which is precisely the thing this leg is supposed to observe rather than arrange.
  </action>

  <verify>
    <automated>grep -c "DUR-02 leg 1" .planning/phases/110-convex-durability/110-DUR-EVIDENCE.md</automated>
  </verify>

  <acceptance_criteria>
    - The `docker logs` command in the transcript carries BOTH `--tail` and `--since`. A command with only one of them fails this criterion regardless of what it returned.
    - The terminal line is pasted verbatim and reads `start index 0` with `covered K of K tables`, K matching the counts recorded in plans 110-04 and 110-05.
    - The positive control is present: at least one other `retention:` line from the same bounded window appears in the transcript.
    - The known-absent control is present with its empty result.
    - Every per-table `done, pruned` line the window contained is pasted — not a selection.
    - No `docker exec`, `npx convex run`, or scheduler call in this section triggered the prune. The transcript is read-only.
    - The verdict sits below the pasted output and states which of the three outcomes occurred (complete pass from 0 / complete pass from a nonzero index / cap reached).
  </acceptance_criteria>

  <done>The chain's own terminal line is on the record from the running container, bounded correctly, with both controls, and its start index and coverage are stated rather than assumed.</done>
</task>

<task type="auto">
  <name>Task 2: Confirm daily rows survived and hourly rows aged out, against the pre-deploy baseline</name>
  <files>.planning/phases/110-convex-durability/110-DUR-EVIDENCE.md</files>

  <read_first>
    - `.planning/phases/110-convex-durability/110-DUR-EVIDENCE.md` §"Pre-deploy baseline" — the exact commands and their recorded values. Re-run the identical commands; a differently-shaped probe is not a comparison.
    - `.planning/phases/110-convex-durability/110-VALIDATION.md` §"Manual-Only Verifications" row 1 — the pass/fail contract and its required control.
    - `convex/retention.ts` as deployed — `PRUNE_PREDICATES.aggregates` and the `ROTATION_CURSOR_KEY` constant.
    - `CLAUDE.md` §"Self-Hosted Convex — Operational Rules" — bounded reads only.
  </read_first>

  <action>
Append `## DUR-01 live confirmation — daily rows survived, hourly rows aged out` to `110-DUR-EVIDENCE.md`.

Re-run the three baseline probes from plan 110-04 verbatim — same index, same predicate, same `take` — and paste each command with its output. Then state each result beside its recorded baseline value in one place, so the comparison is visible rather than asserted.

**The hard fail (D-01).** The oldest `period:"daily"` row's `_creationTime` must be unchanged, or older. The prune deletes ascending from the oldest end, so this timestamp moving FORWARD means daily rows were deleted. That blocks the phase: stop, do not continue to the checkpoint, record the reading, and treat it as a defect in `PRUNE_PREDICATES.aggregates` requiring a fix and a redeploy. The bounded daily count must likewise not have dropped.

**The control (the opposite failure).** The oldest `period:"hourly"` row's `_creationTime` must have moved FORWARD, or the transcript must show the run's own `retention: aggregates done, pruned <n> docs` line with a nonzero count. Without one of these, an unchanged daily reading is uninformative — it is equally consistent with a predicate that is skipping everything, which is the opposite defect and would leave `aggregates` unbounded while every test stays green. Note that plan 110-04's baseline recorded exactly 90 rows older than 90 days as of 2026-08-10, so a small nonzero prune count is the expected shape here, not a large one.

**Rotation cursor (D-05/D-06).** Query `agentConfigs` `by_key` for `configKey === "retention.rotationCursor"` and paste the result. Assert three things, each separately: the row EXISTS (its absence was recorded pre-deploy in plan 110-04 — that control is what makes a value of `0` meaningful rather than indistinguishable from a missing row); there is exactly ONE such row, not a growing set, which is what distinguishes the patch idiom from the insert-only anti-pattern; and its `updatedAt` falls inside the run window and reads as epoch SECONDS, not milliseconds. If the run completed, the value is `0`; if it hit the cap, the value is the index of the table it stopped at.

Bounded reads only, `take` no greater than 1000, no retry at a larger bound on a timeout.
  </action>

  <verify>
    <automated>grep -c "DUR-01 live confirmation" .planning/phases/110-convex-durability/110-DUR-EVIDENCE.md</automated>
  </verify>

  <acceptance_criteria>
    - All three baseline probes were re-run with byte-identical commands and both the new and the recorded baseline values appear side by side.
    - The oldest `period:"daily"` `_creationTime` is unchanged or older than baseline, and the bounded daily count has not dropped. Either failing halts the plan.
    - The control is satisfied by at least one of: the oldest `period:"hourly"` `_creationTime` moved forward, or a `retention: aggregates done, pruned <n> docs` line with a nonzero count appears in the leg-1 transcript. The section states which one carried it.
    - The rotation cursor query returns exactly one row, with a value consistent with the leg-1 outcome (`0` for a completed pass, the stopped-at index for a capped run), and an `updatedAt` inside the run window whose magnitude is epoch seconds.
    - The pre-deploy absence of that row is cited by reference to the plan 110-04 section, so the control is traceable rather than remembered.
    - Every verdict sits below its output. No credential value appears anywhere.
  </acceptance_criteria>

  <done>DUR-01 is confirmed on live data against a real baseline: cost-history daily rows are intact, hourly rows demonstrably aged out, and the rotation cursor is a single row holding the value the run's outcome predicts.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Operator sign-off closing DUR-01 and DUR-02</name>
  <files>.planning/phases/110-convex-durability/110-DUR-EVIDENCE.md</files>

  <what-built>
    A real 09:00 UTC nightly prune ran against the code deployed in plan 110-04. Its terminal `retention: all tables pruned` line was pulled from the running container's logs with both `--tail` and `--since` bounds and both a positive and a known-absent control, and it names the start index and how many of the policy's tables the run covered. The three baseline probes from plan 110-04 were re-run verbatim and compared side by side, and the rotation cursor row was checked for existence, uniqueness and value.
  </what-built>

  <how-to-verify>
    1. Open `.planning/phases/110-convex-durability/110-DUR-EVIDENCE.md` and read `## DUR-02 leg 1`. Confirm the terminal line reads `start index 0` and `covered K of K tables` with K matching the table count in the leg-2 section, and that both controls are present.
    2. Read `## DUR-01 live confirmation`. Confirm the oldest daily `_creationTime` did not move forward, and that the hourly control is satisfied by a concrete reading rather than by argument.
    3. Confirm the rotation cursor section shows exactly one row and cites plan 110-04's recorded absence as its control.
    4. Check the instance is healthy after the run: `docker stats --no-stream convex-backend`, and confirm the CodePulse dashboard loads and its cost charts still render history. The charts are the user-visible proof that Phase 104's re-priceable daily buckets survived — a number in a transcript is not the same as a chart that still draws.
    5. Confirm no section states a verdict above the output that produced it.
    6. Satisfy yourself that DUR-01 and DUR-02 are now observed rather than argued. If any leg rests on an absence without a control, say so — that is the standard this project holds live evidence to, and the leg does not close without it.
  </how-to-verify>

  <resume-signal>Type "approved" to close DUR-01 and DUR-02 on this evidence, or name the leg that is not yet satisfied.</resume-signal>

  <action>
Pause. Present the two evidence sections written in Tasks 1 and 2 and ask the operator to check the live dashboard's cost charts themselves — a surviving row count in a transcript and a chart that still draws history are different claims, and only the operator can make the second. Do not mark DUR-01 or DUR-02 satisfied, and do not update ROADMAP or STATE, until they respond.
  </action>

  <verify>
    <automated>grep -c "DUR-01 live confirmation" .planning/phases/110-convex-durability/110-DUR-EVIDENCE.md</automated>
  </verify>

  <done>Operator has signed off that a complete nightly pass was observed on the running instance and that daily cost history survived, with the response recorded verbatim in the plan summary.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| live production instance → committed evidence file | `docker logs` and Convex CLI output from the live self-hosted backend is pasted verbatim into a git-tracked file. Container logs are the least predictable source in this phase — they carry whatever the process chose to print. |
| observation → irreversible data claim | This plan is where the phase decides whether real cost history survived a real delete. A wrong reading here closes a requirement over data loss that has already happened. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-110-06-01 | Repudiation | DUR-02 leg 1 evidence | mitigate | A grep returning nothing is a claim about the search window and the search string, not about the system, and log rotation makes an unbounded `--since` return a misleading slice. Mitigated by requiring both `--tail` and `--since`, a positive control proving the window contains retention output at all, and a known-absent control proving the grep would have found the line had it been emitted. Reinforced by the start-index and coverage counts appended to the line in plan 110-03, so a partial sweep cannot masquerade as a complete one. |
| T-110-06-02 | Tampering | `aggregates` daily rows | mitigate | The irreversible outcome this phase risks. Detected by re-running plan 110-04's identical probes against a baseline taken before any prune ran; the oldest daily `_creationTime` moving forward is the direct signature of the predicate deleting cost history, and is a hard stop that blocks the plan rather than a note. The paired hourly control catches the opposite defect — a predicate skipping everything, which leaves the table unbounded while every reading looks reassuring. |
| T-110-06-03 | Information Disclosure | pasted `docker logs` output | mitigate | Container logs are uncontrolled output and can carry connection strings, keys, or request payloads from unrelated subsystems. Mitigated by scoping the paste to lines matching the `retention:` prefix rather than dumping the window, by the evidence file's no-secrets preamble, and by a credential-shape scan before commit. `npx convex env list` remains forbidden. |
| T-110-06-04 | Denial of Service | live `convex-backend` | mitigate | The instance has just completed a delete pass and is at its most fragile. Mitigated by read-only probes capped at `take(1000)` on the index-served `by_period_bucket`, no retry at a larger bound on a timeout, and an explicit prohibition on hand-triggering the cron to produce evidence. |
| T-110-06-05 | Spoofing | manually-arranged evidence | mitigate | A hand-triggered prune would produce a genuine-looking terminal line from a rotation state the operator chose, which is the exact thing the leg is meant to observe unarranged. Forbidden by name in Task 1, and checkable from the log timestamp against the 09:00 UTC cron window. |
| T-110-06-SC | Tampering | npm/pip/cargo installs | accept | No packages installed; this plan runs read-only observation commands only. |
</threat_model>

<verification>
- `.planning/phases/110-convex-durability/110-DUR-EVIDENCE.md` contains all five sections in order: pre-deploy baseline, deploy, deployed policy readback, DUR-02 leg 2, DUR-02 leg 1, DUR-01 live confirmation.
- Leg 1's `docker logs` command carries both `--tail` and `--since`, with both controls recorded.
- The oldest daily `_creationTime` did not move forward; the hourly control is satisfied by a concrete reading.
- Exactly one `retention.rotationCursor` row exists, with `updatedAt` in epoch seconds.
- `npx vitest run` still exits 0 against the deployed commit.
- No credential value in any committed artifact.

<human-check>
Operator confirms the dashboard's cost charts still render history after the first live prune of `aggregates`.
</human-check>
</verification>

<success_criteria>
- A complete nightly pass starting at index 0 and covering every table in the policy is observed on the running instance and pasted verbatim, with both controls.
- Daily aggregate rows are proven intact against a pre-prune baseline; hourly rows are proven to have aged out.
- The rotation cursor is exactly one row holding the value its run's outcome predicts, measured against a recorded pre-deploy absence.
- Every verdict in the evidence file sits beneath the output that produced it.
- The operator signs off that DUR-01 and DUR-02 were observed rather than argued.
</success_criteria>

<output>
Append to `.planning/phases/110-convex-durability/110-DUR-EVIDENCE.md` and create `.planning/phases/110-convex-durability/110-06-SUMMARY.md` when done. Record the operator's resume signal verbatim, the terminal line's start index and coverage counts, and the daily/hourly baseline comparison as a two-column table.
</output>
