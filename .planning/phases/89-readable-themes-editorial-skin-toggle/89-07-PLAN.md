---
phase: 89-readable-themes-editorial-skin-toggle
plan: 07
type: execute
wave: 3
depends_on: ["89-01", "89-02", "89-05", "89-06"]
files_modified:
  - e2e/theme-contrast.spec.ts
  - e2e/theme-no-fouc.spec.ts
  - e2e/theme-reduced-motion.spec.ts
autonomous: false
requirements: [TH-06, TH-02, TH-03, TH-04, TH-05]
must_haves:
  truths:
    - "axe-core reports zero WCAG-AA contrast violations on Dashboard/Live Run/Analytics/Forge/Graphs for all four themes (20 cases)"
    - "The no-FOUC e2e proves data-theme is set on domcontentloaded before React hydration"
    - "Reduced-motion e2e proves matrix-bg and the scanline bar are hidden"
    - "Manual canvas legibility + aubergine grain look are confirmed by the operator"
  artifacts:
    - path: "e2e/theme-contrast.spec.ts"
      provides: "20 passing axe WCAG-AA contrast cases (4 themes x 5 pages)"
      contains: "withTags"
    - path: "e2e/theme-no-fouc.spec.ts"
      provides: "passing pre-paint no-FOUC assertion"
    - path: "e2e/theme-reduced-motion.spec.ts"
      provides: "passing reduced-motion suppression assertion"
  key_links:
    - from: "e2e/theme-contrast.spec.ts"
      to: "the 5 live routes per theme"
      via: "addInitScript(codepulse-theme) + AxeBuilder.withTags(['wcag2a','wcag2aa'])"
      pattern: "AxeBuilder"
---

<objective>
Close out TH-06: turn the Plan-01 e2e scaffolds into fully-asserting tests and run them green — zero WCAG-AA axe violations across 4 themes x 5 surfaces (20 cases), the no-FOUC pre-paint proof, and reduced-motion suppression. Then run the operator-facing manual verifications (canvas legibility, aubergine grain) that axe cannot cover.

Purpose: TH-06 is the phase gate — every shipped theme must be WCAG-AA legible on the highest-traffic surfaces. This plan also revalidates TH-02/03/04/05 end-to-end now that tokens, switcher, and canvas are all in place.
Output: three green e2e specs + a human-verify sign-off on the manual-only behaviors.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/89-readable-themes-editorial-skin-toggle/89-RESEARCH.md
@.planning/phases/89-readable-themes-editorial-skin-toggle/89-VALIDATION.md
@.planning/phases/89-readable-themes-editorial-skin-toggle/89-UI-SPEC.md

<interfaces>
<!-- Verified route paths (src/App.tsx) and the axe contract (RESEARCH §axe-core / Playwright). -->
THEMES = ['cyan','emerald','readable','aubergine']
PAGES   = Dashboard "/", LiveRun "/live-run", Analytics "/analytics", Forge "/forge", Graphs "/graphs"
axe: new AxeBuilder({page}).withTags(['wcag2a','wcag2aa']).analyze(); expect(results.violations).toEqual([])
pre-seed theme: page.addInitScript((t)=>localStorage.setItem('codepulse-theme',t), theme) BEFORE page.goto
reduced-motion: page.emulateMedia({reducedMotion:'reduce'})
Contrast spot-checks (UI-SPEC §Contrast spot-check) that must hold: readable #e8eaf0 on #111318 (>=12:1),
readable #8892a4 on #111318 (~5.1:1), aubergine #f0e8dc on #120d18 (>=12:1), aubergine #9b8a74 on #120d18 (~4.8:1)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Complete + run the 20-case axe contrast spec</name>
  <files>e2e/theme-contrast.spec.ts</files>
  <read_first>
    e2e/theme-contrast.spec.ts (the Plan-01 scaffold to finalize)
    .planning/phases/89-readable-themes-editorial-skin-toggle/89-RESEARCH.md (§axe-core / Playwright Contrast Audit — full pattern + canvas limitation note)
    .planning/phases/89-readable-themes-editorial-skin-toggle/89-VALIDATION.md (§Per-Task Verification Map TH-06 row, §Sampling Rate)
  </read_first>
  <action>
    Finalize e2e/theme-contrast.spec.ts so the 4x5 nested loop fully asserts: addInitScript sets codepulse-theme, page.goto(path), waitForLoadState('networkidle'), then AxeBuilder withTags(['wcag2a','wcag2aa']).analyze() and expect(results.violations).toEqual([]). Remove any RED-pending comment now that tokens/switcher exist. If a genuine pre-existing (non-theme) violation surfaces on a page, do NOT mask it by narrowing the selector — per the project Error Triage rule, trace and fix the root cause (or, if it is provably independent of theming, document it in the SUMMARY with file:line and get operator confirmation before excluding it). Run the spec for all themes.
  </action>
  <verify>
    <automated>npm run test:e2e -- e2e/theme-contrast.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - e2e/theme-contrast.spec.ts asserts expect(results.violations).toEqual([]) for all 4 themes x 5 pages
    - No RED-pending comment remains
    - npm run test:e2e -- e2e/theme-contrast.spec.ts exits 0 (zero WCAG-AA violations across 20 cases)
    - Any excluded rule is documented with justification + operator confirmation in the SUMMARY
  </acceptance_criteria>
  <done>20 axe contrast cases pass green; TH-06 satisfied for all themes.</done>
</task>

<task type="auto">
  <name>Task 2: Complete + run no-FOUC and reduced-motion specs</name>
  <files>e2e/theme-no-fouc.spec.ts, e2e/theme-reduced-motion.spec.ts</files>
  <read_first>
    e2e/theme-no-fouc.spec.ts, e2e/theme-reduced-motion.spec.ts (the Plan-01 scaffolds)
    .planning/phases/89-readable-themes-editorial-skin-toggle/89-RESEARCH.md (§FOUC Verification, §prefers-reduced-motion Verification)
    .planning/phases/89-readable-themes-editorial-skin-toggle/89-VALIDATION.md (§Per-Task Verification Map TH-05 rows)
  </read_first>
  <action>
    Finalize e2e/theme-no-fouc.spec.ts: addInitScript sets codepulse-theme to 'readable', page.goto('/'), and on the domcontentloaded state assert document.documentElement has data-theme='readable' AND class contains 'dark' — proving the inline pre-paint script ran before hydration. Finalize e2e/theme-reduced-motion.spec.ts: page.emulateMedia({reducedMotion:'reduce'}), addInitScript sets 'aubergine', goto('/'), assert .matrix-bg is hidden and .crt-scanline-bar is hidden (toBeHidden). Remove RED-pending comments. Run both specs green.
  </action>
  <verify>
    <automated>npm run test:e2e -- e2e/theme-no-fouc.spec.ts e2e/theme-reduced-motion.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - theme-no-fouc.spec.ts asserts data-theme + dark class on domcontentloaded for readable
    - theme-reduced-motion.spec.ts asserts .matrix-bg and .crt-scanline-bar hidden under reducedMotion reduce
    - npm run test:e2e -- e2e/theme-no-fouc.spec.ts e2e/theme-reduced-motion.spec.ts exits 0
    - No RED-pending comments remain
  </acceptance_criteria>
  <done>No-FOUC + reduced-motion e2e green; TH-05 verified end-to-end.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Operator manual verification — canvas legibility + aubergine grain + no flash</name>
  <read_first>
    .planning/phases/89-readable-themes-editorial-skin-toggle/89-VALIDATION.md (§Manual-Only Verifications — the three rows)
  </read_first>
  <action>
    After Tasks 1-2 are green, run npm run dev and walk the operator through the manual-only checks below (canvas legibility, aubergine grain, no-flash on hard refresh, violet vault nodes, reduced-motion). This is verification AFTER all automation is complete — do not skip the automated specs to reach it. Record the operator sign-off (and any defects) in the SUMMARY.
  </action>
  <what-built>
    Four themes ship behind the switcher with full token coverage, theme-aware canvas graphs, a pre-paint no-FOUC script, and effect suppression. Automated axe + e2e are green. Three behaviors are manual-only because axe cannot read canvas pixels and grain/flash quality is perceptual (per 89-VALIDATION §Manual-Only).
  </what-built>
  <how-to-verify>
    1. Run `npm run dev`. In the header ThemeSwitcher, select each of Electric Cyan / Matrix Emerald / Readable Dark / Midnight Aubergine.
    2. Navigate to /graphs and the KG Explorer for each theme — confirm graph nodes and labels are legible against the background (especially Readable Dark and Midnight Aubergine). Confirm vault nodes are violet in every theme.
    3. On Midnight Aubergine, confirm the paper-grain is subtle (not muddy) and the ambient gradients read as warm editorial; confirm no CRT scanline bar or matrix grid is visible on Readable Dark / Aubergine.
    4. Hard-refresh (Ctrl+Shift+R) while on Readable Dark and on Midnight Aubergine — confirm NO cyan flash before the UI settles (no FOUC).
    5. Toggle OS reduced-motion on and reload — confirm no scanline/grid and suppressed aubergine pseudo-elements.
  </how-to-verify>
  <verify>
    <human-check>Operator confirms canvas legibility, aubergine grain quality, violet vault nodes, and zero flash on hard refresh across themes.</human-check>
  </verify>
  <acceptance_criteria>
    - Operator confirms each theme renders legibly on /graphs and KG (manual contrast)
    - Operator confirms aubergine grain is subtle and gradients warm; no scanline/grid on readable/aubergine
    - Operator confirms no cyan flash on hard refresh for readable and aubergine
    - Operator confirms vault nodes are violet across themes
  </acceptance_criteria>
  <resume-signal>Type "approved" if all manual checks pass, or describe what looks wrong.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Playwright test runner -> local dev server | Tests drive the real app; assert contrast/attribute/visibility state only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-89-14 | Information Disclosure | e2e axe output | accept | Tests assert violation arrays and attribute/visibility state; no secrets logged. Run against local dev only |
| T-89-15 | Repudiation | manual sign-off | mitigate | Operator approval recorded in the human-verify checkpoint + SUMMARY; documented exclusions require explicit confirmation |
</threat_model>

<verification>
- npm run test:e2e -- e2e/theme-contrast.spec.ts exits 0 (20 cases)
- npm run test:e2e -- e2e/theme-no-fouc.spec.ts e2e/theme-reduced-motion.spec.ts exits 0
- Full suite (npm run test:e2e) green before /gsd:verify-work (89-VALIDATION phase gate)
- Operator manual sign-off recorded
</verification>

<success_criteria>
- Zero WCAG-AA axe violations across all 20 theme x surface combinations
- No-FOUC + reduced-motion proven by e2e
- Manual canvas/grain/flash checks signed off
</success_criteria>

<output>
Create `.planning/phases/89-readable-themes-editorial-skin-toggle/89-07-SUMMARY.md` when done
</output>
