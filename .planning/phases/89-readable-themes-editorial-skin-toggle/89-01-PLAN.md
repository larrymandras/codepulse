---
phase: 89-readable-themes-editorial-skin-toggle
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - src/hooks/useThemeColors.ts
  - src/hooks/useThemeColors.test.ts
  - src/lib/hexToRgba.ts
  - src/lib/hexToRgba.test.ts
  - e2e/theme-contrast.spec.ts
  - e2e/theme-no-fouc.spec.ts
  - e2e/theme-reduced-motion.spec.ts
  - src/components/ThemeSwitcher.test.tsx
autonomous: false
requirements: [TH-01, TH-06]
must_haves:
  truths:
    - "useThemeColors() returns the active theme's resolved hex/rgba colors and re-resolves when data-theme changes"
    - "The contrast/FOUC/reduced-motion e2e specs and ThemeSwitcher/hook unit tests exist and run (skeleton green or documented red-pending)"
    - "@axe-core/playwright is installed and importable in e2e tests"
  artifacts:
    - path: "src/hooks/useThemeColors.ts"
      provides: "useThemeColors(): ThemeColors hook for canvas color resolution"
      exports: ["useThemeColors", "ThemeColors", "resolveThemeColors"]
      min_lines: 40
    - path: "src/lib/hexToRgba.ts"
      provides: "hexToRgba(hex, alpha) util for canvas alpha edge colors"
      exports: ["hexToRgba"]
    - path: "e2e/theme-contrast.spec.ts"
      provides: "axe-core WCAG-AA contrast spec scaffold: 4 themes x 5 pages"
      contains: "@axe-core/playwright"
    - path: "e2e/theme-no-fouc.spec.ts"
      provides: "pre-paint no-FOUC e2e scaffold"
    - path: "e2e/theme-reduced-motion.spec.ts"
      provides: "reduced-motion suppression e2e scaffold"
    - path: "src/components/ThemeSwitcher.test.tsx"
      provides: "ThemeSwitcher key-migration unit test scaffold"
  key_links:
    - from: "src/hooks/useThemeColors.ts"
      to: "getComputedStyle(document.documentElement)"
      via: "resolveThemeColors() reading CSS custom properties"
      pattern: "getComputedStyle"
    - from: "src/hooks/useThemeColors.ts"
      to: "document.documentElement data-theme attribute"
      via: "MutationObserver attributeFilter ['data-theme']"
      pattern: "MutationObserver"
---

<objective>
Wave 0 foundation for Phase 89 theming. Install the missing a11y test dependency, author the `useThemeColors()` canvas color resolver hook (TH-01) plus its `hexToRgba` helper, and seed every test file the later waves verify against (per 89-VALIDATION.md Wave 0 Requirements).

Purpose: The hook is a HARD dependency of the canvas wave (Plan 06) and of Phase 91 (3D Memory Galaxy). The test scaffolds make Nyquist sampling possible during execution — no later wave ships without a place to assert against.
Output: `@axe-core/playwright` installed; `useThemeColors.ts` + `hexToRgba.ts` with passing unit tests; 3 e2e specs + ThemeSwitcher test scaffolded (skeletons that import correctly and run, with full assertions filled in by the waves that own the behavior).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/89-readable-themes-editorial-skin-toggle/89-RESEARCH.md
@.planning/phases/89-readable-themes-editorial-skin-toggle/89-PATTERNS.md
@.planning/phases/89-readable-themes-editorial-skin-toggle/89-VALIDATION.md

<interfaces>
<!-- Canvas color contract the hook must satisfy (extracted from RESEARCH §useThemeColors). -->
<!-- Downstream canvas consumers (Plan 06) destructure these fields — keep the names stable. -->

ThemeColors shape (RESEARCH.md §useThemeColors Hook):
  primary: string           // var(--primary)        e.g. "#06b6d4"
  primaryAlpha18: string    // var(--primary) @ 0.18  for graph edges
  primaryAlpha55: string    // var(--primary) @ 0.55  for KG current node
  accent: string            // var(--accent)
  vaultNode: string         // var(--vault-node-color)  NEW token (defaults violet #8b5cf6)
  vaultNodeAlpha18: string  // var(--vault-node-color) @ 0.18  for vault link edges
  chartBar: string          // var(--chart-bar)
  chartBarAccent: string    // var(--chart-bar-accent)
  statusOk: string          // var(--status-ok)
  statusWarn: string        // var(--status-warn)
  statusError: string       // var(--status-error)
  statusInfo: string        // var(--status-info)

Existing canvas color prop interface (src/components/graph/ForceGraphCanvas.tsx):
  colorFn?: (node: any) => string
  linkColorFn?: (link: any) => string

Route paths for e2e (verified from src/App.tsx):
  Dashboard "/", LiveRun "/live-run", Analytics "/analytics", Forge "/forge", Graphs "/graphs"
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Verify and install @axe-core/playwright</name>
  <read_first>
    .planning/phases/89-readable-themes-editorial-skin-toggle/89-RESEARCH.md (§Standard Stack, §Package Legitimacy Audit)
    package.json (confirm @axe-core/playwright absent, @playwright/test ^1.58.2 present)
  </read_first>
  <what-built>
    Pending install of `@axe-core/playwright` (the Deque axe-core Playwright integration). The researcher marked this package [ASSUMED — slopcheck not run]; per the package legitimacy gate it requires a blocking human verification before the install runs. Latest npm version is 4.12.1 (verified via `npm view @axe-core/playwright version`). It is scoped under the `@axe-core` org published by Deque Systems.
  </what-built>
  <how-to-verify>
    1. Open https://www.npmjs.com/package/@axe-core/playwright and confirm: published by the `@axe-core`/Deque org, weekly downloads in the millions, repository points to dequelabs/axe-core-npm.
    2. Confirm the version to install is 4.x (4.12.1 latest).
    3. On approval, the executor runs: `npm install --save-dev @axe-core/playwright@^4.12.1` and verifies it imports.
  </how-to-verify>
  <action>
    After approval, run `npm install --save-dev @axe-core/playwright@^4.12.1`. Do NOT pin to a fabricated version — `npm view @axe-core/playwright version` returned 4.12.1. Confirm `node -e "require('@axe-core/playwright')"` resolves (the package default export is the `AxeBuilder` class).
  </action>
  <verify>
    <automated>node -e "const A=require('@axe-core/playwright'); if(!A.default && !A.AxeBuilder) process.exit(1)"</automated>
  </verify>
  <acceptance_criteria>
    - package.json devDependencies contains "@axe-core/playwright" at a ^4.x version
    - `node -e "require('@axe-core/playwright')"` exits 0
    - This checkpoint is never auto-approved (legitimacy gate); operator confirms npmjs.com listing first
  </acceptance_criteria>
  <resume-signal>Type "approved" after confirming the npmjs.com listing, or describe concerns.</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Author hexToRgba util + useThemeColors() hook with unit tests</name>
  <files>src/lib/hexToRgba.ts, src/lib/hexToRgba.test.ts, src/hooks/useThemeColors.ts, src/hooks/useThemeColors.test.ts</files>
  <read_first>
    .planning/phases/89-readable-themes-editorial-skin-toggle/89-RESEARCH.md (§useThemeColors Hook — full pattern, Pitfalls 1-7)
    .planning/phases/89-readable-themes-editorial-skin-toggle/89-PATTERNS.md (§useThemeColors.ts analog DashboardLayout.tsx:574-587; §useThemeColors.test.ts analog useLiveFlash.test.ts)
    .planning/phases/89-readable-themes-editorial-skin-toggle/89-UI-SPEC.md (§Canvas resolver, §Per-Theme Token Values for expected hex)
    src/hooks/useLiveFlash.test.ts (renderHook + act + DOM mutation analog)
    src/test/setup.ts (jsdom setup — mock getComputedStyle manually via vi.spyOn)
  </read_first>
  <behavior>
    - hexToRgba("#10b981", 0.18) returns "rgba(16, 185, 129, 0.18)"
    - hexToRgba("#5eead4", 0.55) returns "rgba(94, 234, 212, 0.55)"
    - hexToRgba trims leading whitespace from the input (getComputedStyle returns " #06b6d4")
    - hexToRgba handles 3-digit shorthand (#fff -> rgba(255, 255, 255, a)) defensively; non-hex input returns the input unchanged (so an oklch value passes through rather than corrupting)
    - resolveThemeColors() reads getComputedStyle(document.documentElement) fresh each call (NOT a cached CSSStyleDeclaration) and .trim()s each value (Pitfall 1)
    - useThemeColors() initial render returns colors resolved from the current data-theme (lazy useState initializer)
    - After document.documentElement.setAttribute("data-theme","readable") inside act(), the hook re-resolves: colors.primary updates to the readable --primary
    - The MutationObserver is disconnected on unmount (no leak)
  </behavior>
  <action>
    Create `src/lib/hexToRgba.ts` exporting `hexToRgba(hex: string, alpha: number): string` — ~10 lines, no library. Trim input, parse r/g/b, return `rgba(r, g, b, alpha)`. If the trimmed value does not match `#?[0-9a-fA-F]{3,8}`, return the trimmed input unchanged (defensive against oklch — Pitfall 3). Create `src/hooks/useThemeColors.ts` exporting the `ThemeColors` interface (exact field names from the `<interfaces>` block above, including `vaultNode` and `vaultNodeAlpha18` per locked decision D-vault), a module-level `resolveThemeColors(): ThemeColors`, and `useThemeColors(): ThemeColors`. Mirror the lazy `useState(resolveThemeColors)` + `useEffect`-with-cleanup pattern from PATTERNS analog; use a `MutationObserver` observing `document.documentElement` with `attributeFilter: ['data-theme']`, calling `setColors(resolveThemeColors())` on change, returning `() => observer.disconnect()`. `resolveThemeColors` reads tokens `--primary`, `--accent`, `--vault-node-color`, `--chart-bar`, `--chart-bar-accent`, `--status-ok/--status-warn/--status-error/--status-info` via `getComputedStyle(document.documentElement).getPropertyValue(tok).trim()`; build alpha variants via `hexToRgba`. In dev, if a resolved value starts with `oklch`, `console.warn` once (Pitfall 3). Write both `.test.ts` files mirroring the useLiveFlash.test.ts renderHook+act analog; mock `getComputedStyle` via `vi.spyOn(window,'getComputedStyle')` returning a stub whose `getPropertyValue` maps token names to UI-SPEC hex values, and flip the stub when `data-theme` changes. Use `npx vitest run` (no watch).
  </action>
  <verify>
    <automated>npx vitest run src/lib/hexToRgba.test.ts src/hooks/useThemeColors.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - src/hooks/useThemeColors.ts exports `useThemeColors`, `ThemeColors`, and `resolveThemeColors`
    - ThemeColors interface declares fields: primary, primaryAlpha18, primaryAlpha55, accent, vaultNode, vaultNodeAlpha18, chartBar, chartBarAccent, statusOk, statusWarn, statusError, statusInfo
    - useThemeColors.ts contains `MutationObserver` and `attributeFilter` with `data-theme`
    - useThemeColors.ts calls `getComputedStyle` and `.trim()` on token reads
    - src/lib/hexToRgba.ts exports `hexToRgba`; `hexToRgba("#10b981",0.18)` === "rgba(16, 185, 129, 0.18)"
    - `npx vitest run src/lib/hexToRgba.test.ts src/hooks/useThemeColors.test.ts` exits 0
    - `npx tsc --noEmit` reports no new errors for these files
  </acceptance_criteria>
  <done>Hook + util exist with named exports, MutationObserver-driven re-resolve, and green unit tests.</done>
</task>

<task type="auto">
  <name>Task 3: Seed e2e specs and ThemeSwitcher test scaffolds</name>
  <files>e2e/theme-contrast.spec.ts, e2e/theme-no-fouc.spec.ts, e2e/theme-reduced-motion.spec.ts, src/components/ThemeSwitcher.test.tsx</files>
  <read_first>
    .planning/phases/89-readable-themes-editorial-skin-toggle/89-RESEARCH.md (§axe-core / Playwright Contrast Audit — full spec shape; §FOUC Verification; §prefers-reduced-motion Verification)
    .planning/phases/89-readable-themes-editorial-skin-toggle/89-VALIDATION.md (§Wave 0 Requirements, §Per-Task Verification Map)
    .planning/phases/89-readable-themes-editorial-skin-toggle/89-PATTERNS.md (§e2e analogs alerts.spec.ts/navigation.spec.ts; §ThemeSwitcher.test analog ObsidianGraph.test.tsx)
    e2e/alerts.spec.ts (structural template: test.describe, page.goto, waitForLoadState networkidle)
    src/components/ThemeSwitcher.tsx (current 3-option component being tested)
  </read_first>
  <action>
    Create `e2e/theme-contrast.spec.ts`: import `{ test, expect }` from `@playwright/test` and `AxeBuilder` from `@axe-core/playwright`. Define `THEMES = ['cyan','emerald','readable','aubergine']` and `PAGES = [{name:'Dashboard',path:'/'},{name:'LiveRun',path:'/live-run'},{name:'Analytics',path:'/analytics'},{name:'Forge',path:'/forge'},{name:'Graphs',path:'/graphs'}]` (paths VERIFIED against src/App.tsx — do not invent). Nested loop generates 20 tests named `[${theme}] ${pg.name} — zero WCAG-AA contrast violations`; each `addInitScript` sets `localStorage.codepulse-theme`, `page.goto(path)`, `waitForLoadState('networkidle')`, then `new AxeBuilder({page}).withTags(['wcag2a','wcag2aa']).analyze()` and `expect(results.violations).toEqual([])`. Create `e2e/theme-no-fouc.spec.ts`: for theme `readable`, addInitScript sets the key, goto `/`, on `domcontentloaded` assert `document.documentElement` has `data-theme="readable"` and class `dark` (proves the inline script ran pre-paint). Create `e2e/theme-reduced-motion.spec.ts`: `page.emulateMedia({reducedMotion:'reduce'})`, set aubergine, goto `/`, assert `.matrix-bg` and `.crt-scanline-bar` are hidden. Create `src/components/ThemeSwitcher.test.tsx` mirroring ObsidianGraph.test.tsx: render `<ThemeSwitcher/>`, spy `localStorage`/`document.documentElement.setAttribute`, assert default `data-theme` is `cyan` when no key set, and (for the key-migration assertion owned by Plan 05) leave a `it.todo` or skipped placeholder referencing the `theme`->`codepulse-theme` migration so Plan 05 fills it. The contrast/no-fouc/reduced-motion specs reference behavior not yet built (token blocks, inline script) — they MAY fail until later waves; mark each describe with a comment `RED-pending: <plan that turns it green>` so executors do not mistake them for regressions. Run unit portion via `npx vitest run`.
  </action>
  <verify>
    <automated>npx vitest run src/components/ThemeSwitcher.test.tsx && npx playwright test --list e2e/theme-contrast.spec.ts e2e/theme-no-fouc.spec.ts e2e/theme-reduced-motion.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - e2e/theme-contrast.spec.ts imports `AxeBuilder` from `@axe-core/playwright` and defines exactly 4 themes x 5 pages
    - e2e/theme-contrast.spec.ts PAGES paths are exactly: "/", "/live-run", "/analytics", "/forge", "/graphs"
    - e2e/theme-no-fouc.spec.ts asserts `data-theme` on `domcontentloaded`
    - e2e/theme-reduced-motion.spec.ts calls `emulateMedia` with `reducedMotion: 'reduce'`
    - `npx playwright test --list` enumerates the new specs without import/parse error
    - src/components/ThemeSwitcher.test.tsx renders ThemeSwitcher and asserts default data-theme cyan; contains a placeholder for the key-migration test
    - Each e2e spec that depends on later waves carries a `RED-pending:` comment
  </acceptance_criteria>
  <done>All four test files exist, import cleanly, and Playwright can list the specs; ThemeSwitcher unit scaffold is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → dev machine | Installing a new dev dependency pulls third-party code into the build/test toolchain |
| Test runner → app pages | e2e tests drive the real app via Playwright; no untrusted input crosses into prod |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-89-SC | Tampering | `@axe-core/playwright` npm install | mitigate | Blocking human checkpoint (Task 1): operator verifies npmjs.com listing (Deque org, download counts, repo) before install; package marked [ASSUMED] by researcher, never auto-approved |
| T-89-01 | Tampering | `useThemeColors` reading getComputedStyle | accept | Reads only own-origin CSSOM custom properties; no external/attacker input. No mitigation needed |
| T-89-02 | Information Disclosure | e2e specs | accept | Tests run against local dev server; assert only contrast/attribute state, log no secrets |
</threat_model>

<verification>
- `node -e "require('@axe-core/playwright')"` resolves
- `npx vitest run src/lib/hexToRgba.test.ts src/hooks/useThemeColors.test.ts src/components/ThemeSwitcher.test.tsx` exits 0
- `npx playwright test --list` enumerates the 3 new e2e specs
- `npx tsc --noEmit` no new errors
</verification>

<success_criteria>
- `@axe-core/playwright@^4.12.1` in devDependencies, importable
- `useThemeColors()` + `hexToRgba()` exist with green unit tests and MutationObserver re-resolve
- 3 e2e specs + ThemeSwitcher test seeded and listable; RED-pending behaviors annotated
</success_criteria>

<output>
Create `.planning/phases/89-readable-themes-editorial-skin-toggle/89-01-SUMMARY.md` when done
</output>
