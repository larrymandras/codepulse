---
phase: 89-readable-themes-editorial-skin-toggle
plan: 06
type: execute
wave: 2
depends_on: ["89-01", "89-02"]
files_modified:
  - src/components/graph/ForceGraphCanvas.tsx
  - src/components/graph/CodeVaultGraph.tsx
  - src/pages/KnowledgeGraph.tsx
autonomous: true
requirements: [TH-01]
must_haves:
  truths:
    - "Canvas graphs render node/link colors from the active theme via useThemeColors(), not hardcoded hex"
    - "Vault nodes read --vault-node-color (violet default), kept consistent across themes, not tracking --accent"
    - "No hardcoded #06b6d4/#10b981 node or link color literal remains in the three canvas files"
  artifacts:
    - path: "src/components/graph/CodeVaultGraph.tsx"
      provides: "Theme-aware colorFn/linkColorFn via useThemeColors()"
      contains: "useThemeColors"
    - path: "src/pages/KnowledgeGraph.tsx"
      provides: "Theme-aware current-node color"
      contains: "useThemeColors"
  key_links:
    - from: "CodeVaultGraph GraphContent"
      to: "useThemeColors()"
      via: "colorFn/linkColorFn closures wrapped in useCallback([colors])"
      pattern: "useThemeColors"
    - from: "CodeVaultGraph vault nodes"
      to: "var(--vault-node-color)"
      via: "colors.vaultNode from the hook"
      pattern: "vaultNode"
---

<objective>
Route the `useThemeColors()` hook (built in Plan 01) into the three canvas-rendered graphs so node and link colors follow the active theme (TH-01). Replace the module-level hardcoded constants (`DEFAULT_COLOR`, `CODE_COLOR`, `VAULT_COLOR`, inline `rgba(...)` link colors, `COLOR_CURRENT`) with hook-resolved values. Vault nodes use the new `--vault-node-color` token (violet, consistent across themes) per the locked decision.

Purpose: Canvas APIs cannot read CSS variables — the hook resolves them to hex/rgba at runtime and re-resolves on theme switch. This is the last TH-01 surface and a hard prerequisite for Phase 91 (3D Memory Galaxy).
Output: ForceGraphCanvas default colors, CodeVaultGraph code/vault/link colors, and KnowledgeGraph current-node color all theme-driven.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/89-readable-themes-editorial-skin-toggle/89-UI-SPEC.md
@.planning/phases/89-readable-themes-editorial-skin-toggle/89-RESEARCH.md
@.planning/phases/89-readable-themes-editorial-skin-toggle/89-PATTERNS.md

<interfaces>
<!-- The hook contract (from Plan 01) and the canvas constants to replace (RESEARCH §Category B). -->

useThemeColors(): ThemeColors  (from src/hooks/useThemeColors.ts)
  fields used here: primary, primaryAlpha18, primaryAlpha55, vaultNode, vaultNodeAlpha18

Hardcoded sites to replace (verified file:line, RESEARCH §Category B):
  ForceGraphCanvas.tsx:80   DEFAULT_COLOR = "#10b981"                  -> colors.primary
  ForceGraphCanvas.tsx:272  linkColor default "rgba(16,185,129,0.18)" -> colors.primaryAlpha18
  CodeVaultGraph.tsx:60     CODE_COLOR = "#10b981"                     -> colors.primary
  CodeVaultGraph.tsx:61     VAULT_COLOR = "#8b5cf6"                    -> colors.vaultNode (var(--vault-node-color))
  CodeVaultGraph.tsx:129    inline "rgba(16,185,129,0.18)" link        -> colors.primaryAlpha18
  CodeVaultGraph.tsx:558    inline "rgba(16,185,129,0.1)"              -> colors.primaryAlpha18 (or a 0.1 variant)
  KnowledgeGraph.tsx:37     COLOR_CURRENT = "rgba(16,185,129,0.55)"    -> colors.primaryAlpha55

Wiring rule (PATTERNS §Canvas files): call useThemeColors() INSIDE the component that owns
colorFn/linkColorFn (CodeVaultGraph GraphContent ~135; KnowledgeGraph page component). Do NOT
call it at module scope. ForceGraphCanvas does NOT call the hook — it receives colors via props
from its parent (the colorFn/linkColorFn it already accepts). Wrap closures capturing colors in
useCallback with [colors] deps (consistent with the existing useFocusParam onFocus pattern,
CodeVaultGraph ~147).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Theme-aware defaults in ForceGraphCanvas</name>
  <files>src/components/graph/ForceGraphCanvas.tsx</files>
  <read_first>
    src/components/graph/ForceGraphCanvas.tsx (read DEFAULT_COLOR ~80, the colorFn/linkColorFn prop interface, and the linkColor default ~272)
    .planning/phases/89-readable-themes-editorial-skin-toggle/89-RESEARCH.md (§useThemeColors Hook pitfall 6 — colorFn passed as prop; §Category B)
    .planning/phases/89-readable-themes-editorial-skin-toggle/89-PATTERNS.md (§Canvas files — ForceGraphCanvas receives colors via props, does NOT call the hook)
    src/hooks/useThemeColors.ts (the ThemeColors contract from Plan 01)
  </read_first>
  <action>
    ForceGraphCanvas is the leaf renderer and must NOT call useThemeColors() itself (per PATTERNS). Replace the module-level DEFAULT_COLOR = "#10b981" and the inline linkColor default "rgba(16,185,129,0.18)" with theme-driven defaults sourced from props. Add optional props `defaultNodeColor?: string` and `defaultLinkColor?: string` (or reuse the existing colorFn/linkColorFn defaulting), so the parent (CodeVaultGraph) supplies hook-resolved colors. Where no parent value is provided, fall back to reading the token at call time is acceptable but prefer the parent-provided value; do not hardcode emerald. Keep the colorFn/linkColorFn prop signatures unchanged. Do not alter graph layout, physics, or node sizing.
  </action>
  <verify>
    <automated>npx tsc --noEmit && grep -L 'rgba(16, *185, *129' src/components/graph/ForceGraphCanvas.tsx</automated>
  </verify>
  <acceptance_criteria>
    - ForceGraphCanvas.tsx no longer contains the hardcoded DEFAULT_COLOR = "#10b981" or the inline "rgba(16,185,129,0.18)" link default
    - Node/link default colors come from props (parent-supplied), not module constants
    - ForceGraphCanvas does NOT import or call useThemeColors (it receives colors via props)
    - colorFn/linkColorFn prop signatures unchanged
    - npx tsc --noEmit no new errors
  </acceptance_criteria>
  <done>ForceGraphCanvas defaults are prop-driven; no emerald literal remains.</done>
</task>

<task type="auto">
  <name>Task 2: Route useThemeColors into CodeVaultGraph (code + vault + link colors)</name>
  <files>src/components/graph/CodeVaultGraph.tsx</files>
  <read_first>
    src/components/graph/CodeVaultGraph.tsx (CODE_COLOR/VAULT_COLOR ~60-61, GraphContent ~135, colorFn/linkColorFn ~120-131 and the inline rgba at ~129/558, useFocusParam useCallback pattern ~147)
    .planning/phases/89-readable-themes-editorial-skin-toggle/89-PATTERNS.md (§Canvas files — call hook in GraphContent, useCallback([colors]))
    .planning/phases/89-readable-themes-editorial-skin-toggle/89-RESEARCH.md (Pitfall 7 — vault node color; §Category B)
    src/hooks/useThemeColors.ts (ThemeColors fields: primary, primaryAlpha18, vaultNode, vaultNodeAlpha18)
  </read_first>
  <action>
    Move CODE_COLOR/VAULT_COLOR off module scope. Inside the GraphContent component (the owner of colorFn/linkColorFn, ~135), call `const colors = useThemeColors()`. In the node colorFn: code nodes use `colors.primary`; vault nodes use `colors.vaultNode` (the --vault-node-color token, violet, per locked decision — do NOT use colors.accent). In linkColorFn: replace the inline `rgba(16,185,129,0.18)` (~129) and `rgba(16,185,129,0.1)` (~558) with `colors.primaryAlpha18` (for code links) and the vault-link variant with `colors.vaultNodeAlpha18`. Wrap colorFn/linkColorFn in `useCallback` with `[colors]` (and any existing deps) so they re-create on theme switch — mirror the existing useFocusParam onFocus useCallback pattern (~147). Pass the hook-resolved default node/link colors down to ForceGraphCanvas via the props added in Task 1. Do not change graph data, layout, or the 2D render path.
  </action>
  <verify>
    <automated>npx tsc --noEmit && grep -q 'useThemeColors' src/components/graph/CodeVaultGraph.tsx && grep -q 'vaultNode' src/components/graph/CodeVaultGraph.tsx</automated>
  </verify>
  <acceptance_criteria>
    - CodeVaultGraph.tsx calls useThemeColors() inside GraphContent (not at module scope)
    - Code nodes use colors.primary; vault nodes use colors.vaultNode (NOT colors.accent)
    - colorFn/linkColorFn are wrapped in useCallback with [colors] in deps
    - No hardcoded "#10b981", "#8b5cf6", or inline rgba(16,185,129,...) node/link literal remains
    - npx tsc --noEmit no new errors
  </acceptance_criteria>
  <done>CodeVaultGraph nodes/links are theme-driven; vault nodes use the violet token consistently.</done>
</task>

<task type="auto">
  <name>Task 3: Theme-aware current-node color in KnowledgeGraph + build/test gate</name>
  <files>src/pages/KnowledgeGraph.tsx</files>
  <read_first>
    src/pages/KnowledgeGraph.tsx (COLOR_CURRENT ~37 at module scope; the KG page component that renders nodes)
    .planning/phases/89-readable-themes-editorial-skin-toggle/89-PATTERNS.md (§Canvas files — move COLOR_CURRENT inside the component)
    src/hooks/useThemeColors.ts (colors.primaryAlpha55)
  </read_first>
  <action>
    Move COLOR_CURRENT off module scope into the KnowledgeGraph page component; call `const colors = useThemeColors()` and use `colors.primaryAlpha55` for the current-node color. If COLOR_CURRENT is referenced inside a render callback, wrap that callback in useCallback with [colors] (or reference colors.primaryAlpha55 directly). Do not change KG search/clustering/layout logic. After this edit, run the full unit suite and a build to confirm no regression and that the canvas migration compiles. The graph node-label contrast itself is manual-only verification per 89-VALIDATION (axe cannot read canvas pixels).
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - KnowledgeGraph.tsx calls useThemeColors() inside the component and uses colors.primaryAlpha55 for the current node
    - No module-scope COLOR_CURRENT = "rgba(16,185,129,0.55)" literal remains
    - npx tsc --noEmit, npx vitest run, and npm run build all exit 0
    - Manual-only (documented): /graphs node/label legibility per theme is verified by eye (89-VALIDATION §Manual-Only)
  </acceptance_criteria>
  <done>KG current node is theme-driven; full suite + build green across the canvas migration.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CSS custom properties -> canvas fillStyle | Hook resolves own-origin CSSOM values to strings passed to canvas APIs; no external input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-89-12 | Tampering | getComputedStyle values into canvas fillStyle | accept | Values originate from author-controlled CSS token blocks; hook trims and (dev) warns on oklch (Pitfall 1/3). No untrusted input |
| T-89-13 | Denial of Service | invalid color string to canvas | mitigate | hexToRgba returns input unchanged on non-hex (defensive); all dark token blocks are hex so canvas receives valid colors (Pitfall 3) |
</threat_model>

<verification>
- npx tsc --noEmit clean
- npx vitest run green
- npm run build exits 0
- grep: no hardcoded #10b981/#8b5cf6/rgba(16,185,129) node/link literals across the 3 canvas files
- Manual: /graphs legibility per theme (89-VALIDATION §Manual-Only)
</verification>

<success_criteria>
- All three canvas graphs resolve node/link colors from useThemeColors()
- Vault nodes use --vault-node-color (violet, consistent)
- No hardcoded canvas color literals remain; build + tests green
</success_criteria>

<output>
Create `.planning/phases/89-readable-themes-editorial-skin-toggle/89-06-SUMMARY.md` when done
</output>
