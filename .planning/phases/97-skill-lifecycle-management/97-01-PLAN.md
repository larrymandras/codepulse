---
phase: 97-skill-lifecycle-management
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - C:\Users\mandr\forge\src\process\intake-exec.ts
  - C:\Users\mandr\forge\src\process\intake-exec.test.ts
  - C:\Users\mandr\forge\src\emit\intake-types.ts
autonomous: true
requirements: [DAEMON-01, INTAKE-04]
must_haves:
  truths:
    - "buildAdmitArgs appends --write on every invocation so a real file lands on disk, not a dry run (D-01, D-02, INTAKE-01)"
    - "buildAdmitArgs appends --allow-unrecoverable for global/project destinations only and never for cold, and never appends --allow-overwrite so a same-name collision is a clean failed install, never a silent overwrite (D-07)"
    - "mapExitCodeToResult classifies exit codes 4-9 and reads the refusal reason from stdout (not the empty stderr), surfacing the daemon's real execution/validation report (INTAKE-04)"
    - "exit codes 8-9 resolve status done (skill IS on disk, loud-not-rolled-back) and are never bucketed with 4-7 nothing-written refusals (INTAKE-04, Pitfall 4)"
    - "the daemon passes the CLI ReportEnvelope verbatim and only fills a structured error string, preserving the frozen D-P8-10 no-report-synthesis invariant"
  artifacts:
    - path: "C:\\Users\\mandr\\forge\\src\\process\\intake-exec.ts"
      provides: "real-write argv + exit-code 4-9 classification + structured write-refused error string"
      contains: "--allow-unrecoverable"
    - path: "C:\\Users\\mandr\\forge\\src\\process\\intake-exec.test.ts"
      provides: "argv-flag + exit-code-4-9 unit coverage (Wave 0 gap closed)"
  key_links:
    - from: "buildAdmitArgs"
      to: "skill-intake admit --write"
      via: "argv array"
      pattern: "--write"
    - from: "mapExitCodeToResult"
      to: "IntakeResult.error"
      via: "stdout refusal extraction"
      pattern: "write-refused:"
---

<objective>
Turn the Forge daemon's intake CLI adapter from dry-run-only into a real writer, and make its exit-code classification cover the write-specific outcomes (exit 4-9) it has never reached before.

Purpose: DAEMON-01 (validate-then-write on the host) and the daemon half of INTAKE-04 (surface the CLI's real refusal reason as an actionable error) both live entirely in `forge/src/process/intake-exec.ts`. Today `buildAdmitArgs` never passes `--write` (pure dry run) and `mapExitCodeToResult` only handles exit codes 0-3, dumping 4-9 into a generic bucket that reads an empty `stderr`.

Output: extended `buildAdmitArgs` + `mapExitCodeToResult` and the unit cases that pin them (closes the Wave 0 exit-code/argv gaps in 97-VALIDATION.md).

**Pre-flight (cheap):** confirm `forge` `master` HEAD is still `a364adf` (research baseline). If it has moved, re-read the current `intake-exec.ts` before editing — the intake harness was under active iteration when researched.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/97-skill-lifecycle-management/97-RESEARCH.md
@.planning/phases/97-skill-lifecycle-management/97-PATTERNS.md

<interfaces>
<!-- The frozen daemon seam this plan extends. Do NOT change the IntakeFn signature or reshape `report`. -->

From C:\Users\mandr\forge\src\emit\intake-types.ts (shape/signature frozen — Task 2 makes a DOC-COMMENT-ONLY edit noting error may now also be set on status 'done' for exit 8/9):
- IntakePayload.destination: 'global' | 'project' | 'cold'
- IntakeResult: { status: 'done' | 'failed'; report?: unknown; error?: string }
- INVARIANT D-P8-10: `report` is the parsed CLI JSON VERBATIM — the daemon never synthesizes or reshapes a report. Refusal text goes in `error`, never grafted into `report`.

skill-intake exit-code contract (from 97-RESEARCH.md "Code Examples"):
  0 admit (write succeeded if --write) · 1 reject · 2 bad args · 3 crash
  --- only reachable with --write ---
  4 refused: unrecoverable dest, --allow-unrecoverable not passed
  5 refused: dest exists & differs, --allow-overwrite not passed  ← D-07 collision
  6 refused: --to cold and ASTRIDR-01 marker absent
  7 refused: --to project and no git top-level
  8 placed OK, CATALOG.md regen failed after placement (loud, not rolled back)
  9 placed OK, provenance/ledger write failed after placement (loud, not rolled back)

Refusal messages print to STDOUT (typer.echo, no err=True) AFTER the first JSON line — stderr is EMPTY for exit 4-9.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Real-write argv in buildAdmitArgs</name>
  <files>C:\Users\mandr\forge\src\process\intake-exec.ts, C:\Users\mandr\forge\src\process\intake-exec.test.ts</files>
  <read_first>
    - C:\Users\mandr\forge\src\process\intake-exec.ts (buildAdmitArgs, current dry-run-only body ~L35-47)
    - 97-PATTERNS.md § intake-exec.ts (required-extension spec) + 97-RESEARCH.md Pitfall 1 + Anti-Patterns
  </read_first>
  <behavior>
    - global destination → argv contains '--write' AND '--allow-unrecoverable'; never '--allow-overwrite'
    - project destination (projectRootPath given) → argv contains '--write', '--allow-unrecoverable', '--project', the root path; never '--allow-overwrite'
    - cold destination → argv contains '--write'; does NOT contain '--allow-unrecoverable'; does NOT contain '--allow-overwrite'
    - project destination with null projectRootPath → still throws (existing guard preserved)
    - '--format', 'json' remains present in every case
  </behavior>
  <action>
    Extend buildAdmitArgs to append '--write' in every returned argv. Append '--allow-unrecoverable' only when destination is 'global' or 'project' (equivalently destination !== 'cold') — the CLI's router.py hardcodes requires_unrecoverable_override for exactly those two destinations, so omitting it refuses every real global/project write with exit 4 (Pitfall 1). Never append '--allow-unrecoverable' for cold (its gate is the ASTRIDR-01 marker, not this flag). Never append '--allow-overwrite' under any condition — its absence is what turns a collision into exit 5 instead of a silent clobber (D-07). Preserve the existing null-projectRootPath throw and the '--format json' tail.
  </action>
  <verify>
    <automated>cd /c/Users/mandr/forge && npx vitest run src/process/intake-exec.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -n "allow-unrecoverable" src/process/intake-exec.ts` returns a match guarded by a destination check; `grep -v '^\s*//' src/process/intake-exec.ts | grep -c "allow-overwrite"` is 0.
    - Test command above passes with new buildAdmitArgs cases green (global+project have --allow-unrecoverable, cold does not, none have --allow-overwrite, all have --write).
  </acceptance_criteria>
  <done>buildAdmitArgs emits a real-write argv with the correct conditional flags and the D-07 regression guard (no --allow-overwrite) is unit-pinned.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Classify exit codes 4-9 with stdout refusal extraction</name>
  <files>C:\Users\mandr\forge\src\process\intake-exec.ts, C:\Users\mandr\forge\src\process\intake-exec.test.ts, C:\Users\mandr\forge\src\emit\intake-types.ts</files>
  <read_first>
    - C:\Users\mandr\forge\src\process\intake-exec.ts (mapExitCodeToResult ~L70-96, parseIntakeStdout)
    - C:\Users\mandr\forge\src\emit\intake-types.ts (IntakeResult, D-P8-10 invariant)
    - 97-RESEARCH.md Pitfalls 3 & 4 + Code Examples (full exit-code table)
  </read_first>
  <behavior>
    - exit 4 → status 'failed'; report = parsed first-line ReportEnvelope (verbatim); error starts 'write-refused:unrecoverable:' + the extracted stdout message
    - exit 5 (collision) → status 'failed'; report verbatim; error starts 'write-refused:collision:' + extracted stdout message
    - exit 6 → status 'failed'; error starts 'write-refused:cold-marker:'
    - exit 7 → status 'failed'; error starts 'write-refused:project-git:'
    - exit 8 → status 'done' (skill IS on disk); report verbatim; error starts 'post-placement-warning:catalog:'
    - exit 9 → status 'done'; report verbatim; error starts 'post-placement-warning:ledger:'
    - exit 0/1/2/3 → unchanged from today (regression guard)
    - refusal text is read from stdout lines AFTER the JSON line, never from stderr (which is empty for 4-9)
  </behavior>
  <action>
    Extend mapExitCodeToResult to handle exit codes 4-9. For 4-7 (router refusals): parse the FIRST stdout line as the ReportEnvelope (validation always ran before the router decision, so it is present and valid) and return it VERBATIM as `report` — do not graft anything into it (D-P8-10). Extract the human-readable refusal message from the remaining stdout lines (the write-plan/outcome text emitted via typer.echo without err=True) and set `error` to a structured string `write-refused:<kind>:<message>` where kind is unrecoverable(4)/collision(5)/cold-marker(6)/project-git(7). Status is 'failed' for 4-7. For 8-9 (placed-then-secondary-step-failed): status is 'done' because the atomic placement already committed and was not rolled back — set `error` to `post-placement-warning:<catalog|ledger>:<message>` and keep the verbatim report. Never collapse 8-9 into the 4-7 nothing-written bucket. Keep 0/1/2/3 exactly as-is. The `<kind>` prefix is the machine-readable seam the Convex ackCommand adapter (Plan 05) maps to house copy — do not compose user-facing/UI copy here. Also make a DOC-COMMENT-ONLY edit to `forge/src/emit/intake-types.ts`: its IntakeResult docstring currently says `error` is populated only when status is failed — add a one-line note that `error` may now ALSO be populated on status done for the post-placement-warning (exit 8/9) case. Do not change the exported IntakeResult shape, the IntakeFn signature, or any other line — comment only.
  </action>
  <verify>
    <automated>cd /c/Users/mandr/forge && npx vitest run src/process/intake-exec.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Test cases assert each of exit 4/5/6/7 → status 'failed' with an `error` prefixed `write-refused:<kind>:` and a non-empty report parsed from the first stdout line.
    - Test cases assert exit 8 and 9 → status 'done' with an `error` prefixed `post-placement-warning:` (distinct discriminant from 4-7).
    - A regression case proves exit 0/1/2/3 mapping is unchanged.
    - Behavior assertion: a mutation that reads `stderr` instead of `stdout` for the refusal message fails the exit-5 test.
  </acceptance_criteria>
  <done>All ten exit codes (0-9) map to a correct IntakeResult; refusal reasons are extracted from stdout; 8/9 are a distinct done-with-warning outcome; report stays verbatim.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Convex claim → daemon | daemon receives an attacker-influenceable skill name / repo path in the intake payload |
| skill-intake CLI stdout → daemon | refusal text (may echo an attacker-controlled path/skill name) crosses back into the daemon's `error` string |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-97-03 | Tampering | buildAdmitArgs (D-07) | mitigate | Never append `--allow-overwrite`; exit-5 collision refusal is the enforced clean-fail path. Unit-pinned in Task 1. |
| T-97-01 | Tampering | argv construction | mitigate | destination is a fixed 3-literal union; projectRootPath comes from synced-workspace resolution only (D-04), never free-text — no new path-traversal surface added here. |
| T-97-02 | Tampering | mapExitCodeToResult error string | mitigate | Refusal text is carried as data in `error` (never interpolated into a shell command or into `report`); the copyable-CLI-string escaping (`quoteArg`) is enforced downstream in IntakeReportView (Plan 04/05 scope). Extracted text is prefixed with a machine token, not executed. |
| T-97-SC | Tampering | package installs | accept | No npm/pip installs in this phase (97-RESEARCH.md Package Legitimacy Audit: nothing to audit). |
</threat_model>

<verification>
- `cd /c/Users/mandr/forge && npx vitest run src/process/intake-exec.test.ts` green.
- Full forge suite green before wave merge: `cd /c/Users/mandr/forge && npm test`.
</verification>

<success_criteria>
- Real writes are enabled (`--write` present) with the correct conditional companion flags and no overwrite escape hatch.
- Exit codes 4-9 are classified; refusal reasons come from stdout; 8/9 are done-with-warning.
- `report` remains verbatim (D-P8-10 preserved).
</success_criteria>

<output>
Create `.planning/phases/97-skill-lifecycle-management/97-01-SUMMARY.md` when done.
</output>
