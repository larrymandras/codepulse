---
phase: 97-skill-lifecycle-management
plan: 06
type: execute
wave: 3
depends_on: [97-01, 97-02, 97-03, 97-04, 97-05]
files_modified: []
autonomous: false
requirements: [INTAKE-01, INTAKE-02, INTAKE-03]
must_haves:
  truths:
    - "The cold-storage confirmation marker ~/.claude/skill-intake.toml with [astridr] confirmed = true exists on host lmofficenew, because it is ABSENT today and every cold install refuses with exit 6 until the operator hand-creates it — no code path may ever write this key by design (INTAKE-01, Pitfall 2)"
    - "A live end-to-end round-trip proves a real SKILL.md lands on disk at each scope (global / project / cold) from both an upload and a public GitHub URL, and the Skills page auto-reflects it with correct origin/scope and no manual refresh (INTAKE-01, INTAKE-02, INTAKE-03)"
    - "A path-traversal / malformed-URL attempt is rejected before anything is written (INTAKE-02)"
  artifacts:
    - path: "~/.claude/skill-intake.toml"
      provides: "cold-storage ASTRIDR-01 confirmation marker (operator-created, never code-written)"
      contains: "confirmed = true"
  key_links:
    - from: "IntakeModal install"
      to: "host filesystem"
      via: "daemon skill-intake admit --write"
      pattern: "real file on disk"
    - from: "daemon rescan"
      to: "Skills page"
      via: "syncInventory + Convex reactivity"
      pattern: "auto-reflect"
---

<objective>
Close the two host/operator preconditions that cannot be automated or unit-tested — the missing cold-storage marker file and the live cross-repo round-trip that is the only real proof INTAKE-01/02/03 work end to end.

Purpose: 97-RESEARCH.md verified (live `cat`) that `~/.claude/skill-intake.toml` is ABSENT on `lmofficenew`; every `--to cold --write` refuses with exit 6 until it exists, and by design no code path may ever write the `[astridr] confirmed` key (the gate exists precisely so only Larry hand-edits it). And 97-VALIDATION.md marks the real-file-lands-on-disk + Skills-page-auto-reflects behaviors as manual-only (cross-repo, live daemon, real filesystem — not meaningfully unit-testable).

Output: the marker file in place + an operator-verified live install round-trip across all three scopes and both input paths.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/97-skill-lifecycle-management/97-RESEARCH.md
@.planning/phases/97-skill-lifecycle-management/97-VALIDATION.md
@.planning/phases/97-skill-lifecycle-management/97-UI-SPEC.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Operator creates the cold-storage confirmation marker</name>
  <files>~/.claude/skill-intake.toml</files>
  <read_first>
    - 97-RESEARCH.md Pitfall 2 + Environment Availability (marker absent, [VERIFIED: live check])
  </read_first>
  <action>
    This is a deliberate hand-edit-only host gate — Claude must NOT automate it (T-4-ForgedMarker: the gate is defeated if any code path ever sets the key). The operator hand-creates `~/.claude/skill-intake.toml` on host `lmofficenew` containing an `[astridr]` table with `confirmed = true`, then confirms it reads back. Until this exists, every `--to cold --write` install refuses with exit 6.
  </action>
  <what-built>
    Nothing was built by Claude for this step — this is a deliberate hand-edit-only host gate. `skill-intake`'s `astridr_marker.load_astridr_confirmation()` requires `~/.claude/skill-intake.toml` to contain a `[astridr]` table with `confirmed = true` before any `--to cold --write` install can succeed; the file is currently absent on `lmofficenew`.
  </what-built>
  <how-to-verify>
    1. Create `~/.claude/skill-intake.toml` (host `lmofficenew`) containing exactly:
       [astridr]
       confirmed = true
    2. Confirm it reads back: `cat ~/.claude/skill-intake.toml` shows the `[astridr]` table with `confirmed = true`.
    3. Do NOT commit this file or have any script generate it — it is a manual host-state precondition only.
  </how-to-verify>
  <verify>
    <human-check>Operator confirms ~/.claude/skill-intake.toml exists with [astridr] confirmed = true, hand-created.</human-check>
  </verify>
  <acceptance_criteria>
    - `~/.claude/skill-intake.toml` exists on the daemon host and contains `[astridr]` `confirmed = true`.
    - The file was hand-created (no code path writes the key).
  </acceptance_criteria>
  <done>The cold-storage marker exists on host, hand-created, so cold installs can pass the exit-6 gate.</done>
  <resume-signal>Type "approved" once the marker file exists, or describe any issue.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Live end-to-end install round-trip across all scopes</name>
  <files>(no files — live cross-repo round-trip verification)</files>
  <read_first>
    - 97-VALIDATION.md § Manual-Only Verifications
    - 97-RESEARCH.md Environment Availability (forge daemon must be running with CONVEX_FORGE_INGEST_URL + FORGE_INGEST_API_KEY)
  </read_first>
  <action>
    With the forge daemon running on `lmofficenew` (CONVEX_FORGE_INGEST_URL + FORGE_INGEST_API_KEY set) and Plans 01-05 deployed + Task 1 marker in place, the operator drives a live install round-trip through IntakeModal covering both input paths (upload + public GitHub URL), all three destinations (global / project / cold), the fan-out confirm-first flow, the path-traversal/malformed-URL guard, and a same-name collision — confirming a real file lands on disk each time, the Skills page auto-reflects with correct origin/scope and no manual refresh, guards reject before any write, and a collision is a clean failure with no overwrite. This is the only meaningful proof of INTAKE-01/02/03.
  </action>
  <what-built>
    Plans 01-05 shipped: real-write argv + exit-code 4-9 mapping (forge), the DAEMON-03 rescan module + trigger (forge), the install-language UI copy (codepulse), and the Convex refusal-reason adapter (codepulse). This checkpoint proves the whole cross-repo path end to end against the live daemon.
  </what-built>
  <how-to-verify>
    Preconditions: the forge daemon is running on `lmofficenew` with `CONVEX_FORGE_INGEST_URL` + `FORGE_INGEST_API_KEY` set; Plans 01-05 deployed; Task 1 marker in place.
    1. UPLOAD path: in IntakeModal, upload a SKILL.md, pick destination = global → submit "Install skill". Confirm (a) a real skill directory appears under `~/.claude/skills/<name>/`, and (b) the Skills page shows the new skill with origin/scope global WITHOUT a manual refresh.
    2. Repeat the upload for destination = project (pick a synced workspace) → file lands under `<workspace>/.claude/skills/<name>/`; and destination = cold → file lands under `~/.claude/skills-available/<name>/`.
    3. GITHUB URL path: paste a public GitHub URL (subpath that fans out to >1 skill) → confirm the "N skills found at this path" list appears, an explicit "Install {N} skills" click is required, and after confirm the selected skills land on disk and auto-appear on the Skills page.
    4. GUARD: attempt a malformed URL / a `../` path-traversal subpath → confirm it is rejected before anything is written (no partial directory on disk).
    5. FAILURE copy: trigger a same-name collision → confirm the row renders the actionable D-07 copy and NO overwrite occurred on disk.
  </how-to-verify>
  <verify>
    <human-check>Operator confirms all scopes + both input paths + fan-out confirm + guard + collision cases pass against the live daemon.</human-check>
  </verify>
  <acceptance_criteria>
    - A real SKILL.md file exists on disk at the correct path for each of global / project / cold after install.
    - Both the upload and public-GitHub-URL input paths land files; a subpath fan-out is confirm-first.
    - The Skills page auto-reflects each install (correct origin/scope, no manual refresh).
    - A malformed/traversal attempt is rejected pre-write with no partial state on disk.
    - A same-name collision renders the actionable error and does not overwrite.
  </acceptance_criteria>
  <done>The full cross-repo intake path is operator-verified live across every scope and input path.</done>
  <resume-signal>Type "approved" when all scopes + both input paths + the guard + collision cases pass, or describe the failure.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator hand-edit → cold gate | the ASTRIDR-01 marker is the sole authorization for cold-storage writes |
| public GitHub URL / upload → host filesystem | untrusted skill content crosses to a real on-disk write |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-97-04 | Elevation of privilege | cold-storage marker | mitigate | Marker is hand-edited only; no code path writes the `confirmed` key (verified in Task 1) — cold writes stay gated behind an explicit human act. |
| T-97-01 | Tampering | subpath / URL input | mitigate | Path-traversal + malformed-URL rejection verified live pre-write (guard step) — isSafeSubpath/isAcceptedGithubUrlShape already shipped. |
| T-97-13 | Tampering | atomicity / partial state | mitigate | skill-intake's atomic_write.place_directory (os.replace, rollback-on-exception) guarantees no partial directory; verified by the guard + collision steps leaving disk clean. |
| T-97-SC | Tampering | package installs | accept | No installs this phase. |
</threat_model>

<verification>
- Both checkpoints approved by the operator.
- Full suites green in BOTH repos before this plan runs: `cd /c/Users/mandr/forge && npm test` and (in codepulse) `npm test`.
</verification>

<success_criteria>
- The cold-storage marker exists (hand-created).
- A live round-trip proves real files land on disk at every scope from both input paths, the Skills page auto-reflects, guards reject bad input pre-write, and collisions are clean failures.
</success_criteria>

<output>
Create `.planning/phases/97-skill-lifecycle-management/97-06-SUMMARY.md` when done.
</output>
