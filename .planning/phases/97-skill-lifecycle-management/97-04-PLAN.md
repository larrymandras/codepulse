---
phase: 97-skill-lifecycle-management
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/skills/IntakeModal.tsx
  - src/components/skills/IntakeModal.test.tsx
  - src/components/skills/IntakeSheet.tsx
  - src/components/skills/IntakeSheet.test.tsx
  - src/components/skills/SkillCollectionPicker.tsx
  - src/components/skills/SkillCollectionPicker.test.tsx
autonomous: true
requirements: [INTAKE-01, INTAKE-02, INTAKE-04]
must_haves:
  truths:
    - "Every dry-run string ('Validate skill', 'Validate {N} skills', 'Validation only — nothing is written') is replaced with install-language copy, because submitting now writes to the host filesystem — keeping 'Validate' would be a false-success framing violation (INTAKE-01, INTAKE-04)"
    - "The fan-out confirm surface names the action being cancelled ('Cancel Install', not bare 'Cancel') and requires an explicit Install click — never auto-submitting on resolve (D-06)"
    - "A failed install renders an actionable error with the mandatory 'Nothing was written.' clause for clean-rollback cases, and a same-name collision renders the name+destination+next-step copy pattern — never a bare exit code or silent overwrite (D-07, INTAKE-04)"
    - "The GitHub-URL install surface stays public-repo only — no token/credential input, no private-repo auth affordance is added this phase; the existing enqueueIntake URL-shape + path-traversal guards remain the only validation (D-05, INTAKE-02)"
  artifacts:
    - path: "src/components/skills/IntakeModal.tsx"
      provides: "install-language DialogTitle/CTA/footer + Cancel Install label"
      contains: "Install skill"
    - path: "src/components/skills/IntakeSheet.tsx"
      provides: "install-language empty state + actionable failed-install copy"
  key_links:
    - from: "IntakeModal submit"
      to: "install language"
      via: "CTA + footer copy"
      pattern: "Install"
    - from: "IntakeSheet failed branch"
      to: "actionable error copy"
      via: "row.error render"
      pattern: "Nothing was written"
---

<objective>
Make the already-built intake surface tell the truth about a REAL install. Today it frames every action as a dry run ("Validate skill", "Validation only — nothing is written"); once the daemon actually writes to disk (Plans 01-03), that copy is a house-honesty violation.

Purpose: the 97-UI-SPEC.md copywriting contract mandates replacing all dry-run language with install language and specifying the fan-out-confirm and failed/collision copy. No structural/visual/component change — this is a bounded copy + data-shape delta on `IntakeModal`, `IntakeSheet`, and `SkillCollectionPicker` (and their assertion tests).

Output: install-language copy across the intake surface, with the D-06 confirm-first and D-07 actionable-error copy contracts satisfied.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/97-skill-lifecycle-management/97-UI-SPEC.md
@src/components/skills/IntakeModal.tsx
@src/components/skills/IntakeSheet.tsx
@src/components/skills/SkillCollectionPicker.tsx

<interfaces>
<!-- Exact copy sites (grep-confirmed). Token-driven styling only — never hardcode hex; reuse existing chip/badge classes. -->

IntakeModal.tsx: DialogTitle "Validate skill" (L313); footer note "Validation only — nothing is written." (L486); submit button variants "Validate {N} skills"/"Validate skill" (L504-510); Cancel button (L491-492).
IntakeSheet.tsx: empty-state body "...with "Validate skill" — reports appear here." (L63-64); failed branch `Failed: {row.error}` (L147).
SkillCollectionPicker.tsx: "{N} skills found" heading (L169), "Select all" (L172) — the D-06 confirm surface.
Tests asserting the OLD copy that MUST be updated: IntakeModal.test.tsx (L172-209), IntakeSheet.test.tsx (L62), SkillCollectionPicker.test.tsx (L64,L246).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install-language CTA + fan-out confirm copy in IntakeModal / SkillCollectionPicker</name>
  <files>src/components/skills/IntakeModal.tsx, src/components/skills/IntakeModal.test.tsx, src/components/skills/SkillCollectionPicker.tsx, src/components/skills/SkillCollectionPicker.test.tsx</files>
  <read_first>
    - src/components/skills/IntakeModal.tsx (L313 title, L486 footer, L491-510 footer buttons)
    - src/components/skills/SkillCollectionPicker.tsx (L169-172 confirm surface)
    - 97-UI-SPEC.md § Copywriting Contract + § Phase UI Deltas 2 (fan-out confirm)
  </read_first>
  <action>
    Replace the DialogTitle "Validate skill" with "Install skill". Replace the submit-button variants: "Validate {N} skills" → "Install {N} skills" and "Validate skill" → "Install skill" (both the loading-state and idle variants at ~L504-511). Replace the footer note "Validation only — nothing is written." with "Installs to the selected host and destination." (per UI-SPEC, may be omitted or greyed until a destination is chosen — pick one and apply consistently). Rename the fan-out Cancel button (L491-492) to "Cancel Install" (bare "Cancel" is banned-generic on this new confirm surface). In SkillCollectionPicker, update the heading to "{N} skills found at this path" and ensure the confirm affordance requires an explicit Install click — it must NEVER auto-submit on resolve (D-06 rationale: prevent surprise bulk-install). Do not introduce a second badge/button shape or any new component — reuse the existing DialogFooter and chip classes. Do not hardcode hex — the confirm CTA uses the existing `variant="default"` (accent) button.
  </action>
  <verify>
    <automated>npx vitest run src/components/skills/IntakeModal.test.tsx src/components/skills/SkillCollectionPicker.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -rc "Validate skill\|Validate \${" src/components/skills/IntakeModal.tsx` is 0.
    - Tests updated to assert "Install skill" / "Install {N} skills" / "Cancel Install" render, and pass.
    - Behavior assertion: the fan-out flow requires an explicit Install click (no enqueueIntake fires on resolve alone) — existing SkillCollectionPicker selection test still green.
  </acceptance_criteria>
  <done>IntakeModal + SkillCollectionPicker use install language, the confirm surface names its cancel action, and confirm-first is preserved.</done>
</task>

<task type="auto">
  <name>Task 2: IntakeSheet empty-state + actionable failed/collision copy</name>
  <files>src/components/skills/IntakeSheet.tsx, src/components/skills/IntakeSheet.test.tsx</files>
  <read_first>
    - src/components/skills/IntakeSheet.tsx (L63-64 empty state, L147 failed branch)
    - src/components/skills/IntakeSheet.test.tsx (L62 empty-state assertion)
    - 97-UI-SPEC.md § Copywriting Contract (failed/collision) + § Phase UI Deltas 3
  </read_first>
  <action>
    Update the empty-state body to reference the renamed CTA: "Drop a SKILL.md or paste a GitHub URL with "Install skill" — reports appear here." Update the failed branch (currently `Failed: {row.error}`) to render `row.error` directly as the actionable message — Plan 05's Convex adapter now composes the full kind-accurate house copy INTO `row.error` (collision -> "A skill named ... already exists at {destination}. ..."; other refused -> "Install failed: ... Nothing was written."; exit 8/9 post-placement -> "Installed, but a post-placement step failed: ..."). Do NOT prepend a universal "Install failed: ... Nothing was written." wrapper here — the "Nothing was written." clause is kind-specific (correct for collision/refused, WRONG for exit 8/9 where the file WAS written) and is already baked into `row.error` by Plan 05. This task owns only the presentational framing (a clean label + `row.error`), not the message content, and must never surface the raw internal `write-refused:`/`post-placement-warning:` token. Do not add a new visual treatment: reuse RowStatusBadge's existing `failed` config (--status-error, XCircle) and render all text via JSX (no dangerouslySetInnerHTML). Update the empty-state assertion test and add a failed-branch copy assertion.
  </action>
  <verify>
    <automated>npx vitest run src/components/skills/IntakeSheet.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -c "Validate" src/components/skills/IntakeSheet.tsx` is 0.
    - Empty-state test updated to the new copy and green.
    - A failed-row test (row.error mocked to the Plan-05 collision house copy) asserts the actionable string renders end-to-end from row.error and the raw internal `write-refused:` token never appears.
    - Security: no `dangerouslySetInnerHTML` in IntakeSheet.tsx (`grep -c dangerouslySetInnerHTML` is 0).
  </acceptance_criteria>
  <done>IntakeSheet uses install language and renders failed/collision outcomes with the mandated actionable copy through the existing failure treatment.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| daemon report/error → report UI | refusal text may contain an attacker-influenceable path/skill name |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-97-02 | Tampering (XSS-adjacent) | IntakeSheet / report copy | mitigate | All report-derived strings render via React JSX escaping; no dangerouslySetInnerHTML (asserted). Any refusal text interpolated into the copyable CLI string must pass the existing quoteArg() helper — no second unescaped interpolation site added. |
| T-97-10 | Repudiation (false success) | intake CTA copy | mitigate | Dry-run language fully removed; a real write is never framed as "validation only" (house honesty rule); grep-gated to 0 occurrences. |
| T-97-SC | Tampering | package installs | accept | No installs this phase. |
</threat_model>

<verification>
- `npx vitest run src/components/skills/IntakeModal.test.tsx src/components/skills/IntakeSheet.test.tsx src/components/skills/SkillCollectionPicker.test.tsx` green.
- Grep gate: no `Validate skill` / `Validation only` / `` `Validate ${ `` strings remain in the intake surface.
- Full codepulse suite green before wave merge: `npm test`.
</verification>

<success_criteria>
- The intake surface uses install language everywhere (no dry-run copy remains).
- Fan-out confirm names its cancel action and stays confirm-first (D-06).
- Failed/collision outcomes render the actionable copy patterns (D-07, INTAKE-04).
</success_criteria>

<output>
Create `.planning/phases/97-skill-lifecycle-management/97-04-SUMMARY.md` when done.
</output>
