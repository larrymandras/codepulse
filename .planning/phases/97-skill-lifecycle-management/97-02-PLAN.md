---
phase: 97-skill-lifecycle-management
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - C:\Users\mandr\forge\src\emit\skill-rescan.ts
  - C:\Users\mandr\forge\src\emit\skill-rescan.test.ts
autonomous: true
requirements: [INTAKE-03, DAEMON-03]
must_haves:
  truths:
    - "After a write the daemon walks all three skill roots (global, cold, per-workspace project) and builds a full host-truth inventory snapshot, not a ledger-scoped subset (D-03, DAEMON-03, Pitfall 5)"
    - "The snapshot is shaped for registry.syncInventory: { skills: [{ name, description, origin, source, upstream, command }] } with origin strings claude-code / claude-code:available / claude-code:project:<key> (D-03, D-04)"
    - "Project-scope roots are resolved from synced Forge workspaces only, never free-text host paths (D-04)"
    - "The snapshot is POSTed to the existing bearer-authed /scan endpoint fire-and-forget with the codepulse-emitter retry/logging discipline; the API key is never logged (INTAKE-03)"
  artifacts:
    - path: "C:\\Users\\mandr\\forge\\src\\emit\\skill-rescan.ts"
      provides: "filesystem walk of 3 skill roots + SKILL.md frontmatter parse + syncInventory snapshot builder + fire-and-forget /scan POST"
      min_lines: 40
    - path: "C:\\Users\\mandr\\forge\\src\\emit\\skill-rescan.test.ts"
      provides: "temp-dir walk fixtures + fetch-mock retry/log assertions (Wave 0 module gap closed)"
  key_links:
    - from: "skill-rescan buildSkillSnapshot"
      to: "registry.syncInventory skills[]"
      via: "snapshot shape { skills: [...] }"
      pattern: "origin"
    - from: "skill-rescan postSnapshot"
      to: "/scan endpoint"
      via: "bearer POST"
      pattern: "/scan"
---

<objective>
Build the one genuinely-new module this phase needs: DAEMON-03's registry rescan. After a successful install the daemon must walk the host's skill directories, parse each SKILL.md's frontmatter, and POST a full inventory snapshot to CodePulse's existing `/scan` endpoint so the Skills page reflects the new skill with no manual refresh (INTAKE-03).

Purpose: `skill_intake.provenance.report_query.scan()` is NOT reusable — it only enumerates provenance-ledger'd skills and would prune hand-copied/pre-existing skills from the registry (97-RESEARCH.md Pitfall 5). This module is a fresh filesystem walk composed from two already-shipped forge patterns.

Output: `forge/src/emit/skill-rescan.ts` (new) + its test. The wiring of this module into `command-poller.executeIntake` is Plan 03 (Wave 2); this plan delivers a standalone, unit-tested function.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/97-skill-lifecycle-management/97-RESEARCH.md
@.planning/phases/97-skill-lifecycle-management/97-PATTERNS.md

<interfaces>
<!-- Donor patterns to compose. Read both before authoring — do not invent a new walk/POST shape. -->

Walk donor — C:\Users\mandr\forge\src\workspace\enumerate.ts (enumerateWorkspace):
  recursive walk with IGNORE_DIRS skip-set, per-entry classification, try/catch around every fs call so one bad dir never kills the walk.

POST donor — C:\Users\mandr\forge\src\emit\codepulse-emitter.ts (emitWorkspaces ~L253-295):
  no-op gate `if (!ingestUrl || !apiKey) return;`, MAX_ATTEMPTS=3, exp backoff 200/400ms, 401/403 logged once via a Set, apiKey NEVER interpolated into any console message.

Workspace list — C:\Users\mandr\forge\src\store\workspaces.ts (listWorkspaces(db)) → rows carry rootPath.

Sink contract — codepulse/convex/registry.ts syncInventory + convex/scan.ts scanEndpoint:
  POST /scan (bearer via validateIngestAuth) → runMutation(registry.syncInventory, { snapshot: body }).
  snapshot.skills element shape (registry.ts upsert): { name, description, source, origin, upstream, command, useCount?, lastUsedAt? }.
  Per-origin prune fires only for origins PRESENT in snapshot.skills — the walk MUST be complete per origin it reports.

Origin-string convention (codepulse/convex/schema.ts L199, use verbatim):
  "claude-code" (global) | "claude-code:available" (cold) | "claude-code:project:<key>" (project)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Walk skill roots + build syncInventory snapshot</name>
  <files>C:\Users\mandr\forge\src\emit\skill-rescan.ts, C:\Users\mandr\forge\src\emit\skill-rescan.test.ts</files>
  <read_first>
    - C:\Users\mandr\forge\src\workspace\enumerate.ts (walk pattern + IGNORE_DIRS + defensive try/catch)
    - C:\Users\mandr\forge\src\workspace\enumerate.test.ts (temp-dir fixture pattern — glob first; author test in its shape)
    - C:\Users\mandr\forge\src\store\workspaces.ts (listWorkspaces → rootPath for project roots)
    - 97-RESEARCH.md Pitfall 5 + Architecture diagram (three walk targets) + 97-PATTERNS.md § rescan-snapshot-builder
  </read_first>
  <behavior>
    - Given a temp global root with two SKILL.md skills → snapshot.skills has 2 entries, each origin 'claude-code', name+description from frontmatter
    - Given a cold-storage root → those skills carry origin 'claude-code:available'
    - Given a synced workspace rootPath with a .claude/skills tree → those skills carry origin 'claude-code:project:<key>'
    - A directory that is missing / unreadable → skipped without throwing; other roots still enumerate
    - A dir without a parseable SKILL.md frontmatter → skipped, not emitted as a malformed skill
  </behavior>
  <action>
    Add `buildSkillSnapshot(deps)` to a new `forge/src/emit/skill-rescan.ts`. Walk three root sets: `~/.claude/skills/` (origin claude-code), `~/.claude/skills-available/` (origin claude-code:available), and for each row from listWorkspaces(db) the `<rootPath>/.claude/skills/` tree (origin claude-code:project:<key>, deriving <key> the same way existing project-origin strings do — confirm against schema.ts's comment, do not invent a new taxonomy). Reuse enumerate.ts's defensive walk shape: skip missing/unreadable dirs via try/catch, do not let one bad directory abort the whole snapshot. For each skill directory parse its SKILL.md frontmatter into { name, description, upstream, command } and assemble a `{ skills: [...] }` snapshot whose element shape matches registry.syncInventory's upsert (name, description, source, origin, upstream, command). Keep the function pure/injectable (accept the home dir, workspace list, and an fs/read seam) so it is unit-testable against temp-dir fixtures with no real host dependency. Never throw past the function boundary (discriminated/empty-array result), matching the never-throw contract every caller up the chain assumes.
  </action>
  <verify>
    <automated>cd /c/Users/mandr/forge && npx vitest run src/emit/skill-rescan.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Test builds temp-dir fixtures for all three roots and asserts the resulting snapshot.skills entries carry the correct origin string per root and correct name/description from frontmatter.
    - A missing/unreadable root does not throw and does not drop the other roots' skills.
    - Source assertion: `grep -n "claude-code:available\|claude-code:project" src/emit/skill-rescan.ts` matches (all three origin conventions present).
  </acceptance_criteria>
  <done>buildSkillSnapshot produces a complete, correctly-originned, syncInventory-shaped snapshot from a filesystem walk, resilient to bad directories.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fire-and-forget /scan POST with emitter discipline</name>
  <files>C:\Users\mandr\forge\src\emit\skill-rescan.ts, C:\Users\mandr\forge\src\emit\skill-rescan.test.ts</files>
  <read_first>
    - C:\Users\mandr\forge\src\emit\codepulse-emitter.ts (emitWorkspaces ~L253-295 — no-op gate, MAX_ATTEMPTS, backoff, once-per-process 401/403 log, apiKey never logged)
    - C:\Users\mandr\forge\src\emit\codepulse-emitter.test.ts (injectable fetchImpl, retry/log assertions)
    - codepulse/convex/scan.ts (scanEndpoint: POST /scan, bearer via validateIngestAuth) — CONFIRM the URL/auth before wiring; it is /scan, NOT /forge-ingest
  </read_first>
  <behavior>
    - 2xx response → single POST, no retry
    - 5xx response → retries up to MAX_ATTEMPTS with exponential backoff, then silently drops
    - 401/403 → logged once per process (Set-gated), not retried
    - missing ingestUrl or apiKey → no-op, no fetch call
    - apiKey never appears in any console output
  </behavior>
  <action>
    Add `postSkillSnapshot(cfg, snapshot)` to skill-rescan.ts and an exported `rescanAndSync(deps)` that composes buildSkillSnapshot (Task 1) then postSkillSnapshot. Replicate codepulse-emitter.ts's exact discipline: leading `if (!ingestUrl || !apiKey) return;` no-op gate; POST to `${ingestUrl}/scan` (confirmed endpoint — differs from emitWorkspaces' /forge-ingest) with `Authorization: Bearer ${apiKey}` and JSON body; MAX_ATTEMPTS=3 with 200ms/400ms exponential backoff on 5xx/network; 401/403 logged once per process via a module-level Set; non-auth 4xx logged per call then dropped; apiKey NEVER interpolated into any console.error. Accept an injectable fetchImpl for testing. Never throw — this runs fire-and-forget off the ack path.
  </action>
  <verify>
    <automated>cd /c/Users/mandr/forge && npx vitest run src/emit/skill-rescan.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Test asserts: single POST on 2xx; retry-count on 5xx equals MAX_ATTEMPTS; single log line across repeated 401; no fetch when apiKey/ingestUrl absent.
    - Behavior assertion: a test spying on console output confirms the apiKey string never appears.
    - Source assertion: `grep -n "/scan" src/emit/skill-rescan.ts` matches; `grep -c "/forge-ingest" src/emit/skill-rescan.ts` is 0.
  </acceptance_criteria>
  <done>rescanAndSync builds the snapshot and POSTs it to /scan fire-and-forget with the established retry/logging/secret-safety discipline.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host filesystem → daemon | SKILL.md frontmatter (attacker-influenceable content) is parsed into the snapshot |
| daemon → Convex /scan | inventory snapshot crosses to the cloud registry over an authenticated channel |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-97-05 | Spoofing | /scan POST | mitigate | /scan is bearer-authed (validateIngestAuth, FORGE_INGEST_API_KEY); no-op gate refuses to emit without a key. |
| T-97-06 | Information disclosure | emitter logging | mitigate | apiKey never interpolated into any console message (Set-gated once-per-process 401/403 log only); asserted in Task 2 test. |
| T-97-07 | Tampering | frontmatter parse | mitigate | Parsed frontmatter fields are carried as data into the snapshot (rendered later via React JSX escaping in the registry UI); the walk never executes SKILL.md content and never follows junctions into other trees (enumerate.ts skip discipline). |
| T-97-01 | Tampering | project root resolution | mitigate | Project roots come from listWorkspaces(db) synced paths only (D-04) — no free-text host path is walked. |
| T-97-SC | Tampering | package installs | accept | No installs this phase (97-RESEARCH.md Package Legitimacy Audit). |
</threat_model>

<verification>
- `cd /c/Users/mandr/forge && npx vitest run src/emit/skill-rescan.test.ts` green.
- Full forge suite green before wave merge: `cd /c/Users/mandr/forge && npm test`.
</verification>

<success_criteria>
- A full three-root walk produces a correctly-originned syncInventory snapshot resilient to bad directories.
- The snapshot POSTs to the bearer-authed /scan endpoint fire-and-forget with retry/backoff and no secret leakage.
</success_criteria>

<output>
Create `.planning/phases/97-skill-lifecycle-management/97-02-SUMMARY.md` when done.
</output>
