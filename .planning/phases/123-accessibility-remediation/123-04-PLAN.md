---
phase: 123-accessibility-remediation
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/layouts/DashboardLayout.tsx
  - .planning/phases/123-accessibility-remediation/123-SHELL-FIX.md
autonomous: true
requirements: [A11Y-02]

must_haves:
  truths:
    - "D-01: the shared app-shell chrome is fixed as a class, at the site that owns 184 of the 209 measured violation nodes, not as five instance patches"
    - "D-04: the remedy is deleting the /NN opacity modifier; where two adjacent elements would collapse to the same level the fix steps to a quieter token, and alpha is never reintroduced"
    - "D-10: all four themes are held to the same AA bar — the shell fix is verified in cyan, emerald, readable and aubergine, with no per-theme carve-out"
    - "D-15: the SYS:/LAT: header badge is fixed like any other violator and the ex-badge column is retired, because 123's target is zero and a passing badge contributes zero nodes whether it renders or not"
  artifacts:
    - path: "src/layouts/DashboardLayout.tsx"
      provides: "app-shell nav group headers, nav labels and runtime badge at full token opacity"
    - path: ".planning/phases/123-accessibility-remediation/123-SHELL-FIX.md"
      provides: "before/after node counts per theme for the shell fix, and the ex-badge column retirement statement"
  key_links:
    - from: "src/layouts/DashboardLayout.tsx"
      to: "e2e/theme-contrast.spec.ts"
      via: "20-cell axe matrix measuring the shell on all five pages"
      pattern: "theme-contrast"
---

<objective>
Fix the single highest-leverage site in the phase. `src/layouts/DashboardLayout.tsx` lines `:91`
(`text-primary/60`, nav group headers) and `:148` (`text-muted-foreground/80`, nav labels) account
for **184 of the 209** measured contrast nodes between them, and because they are shared app-shell
chrome one edit clears them across all four themes and all five measured pages at once. The
SYS:/LAT: runtime badge (`:607-616`) is the remaining genuine violator in the same file.

Purpose: D-01, D-04, D-10 and D-15.
Output: nine `/NN` deletions in one file, plus `123-SHELL-FIX.md` recording the per-theme
before/after node counts and retiring the ex-badge column.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/123-accessibility-remediation/123-CONTEXT.md
@.planning/phases/123-accessibility-remediation/123-PATTERNS.md
@.planning/phases/122-tokens-primitives-contrast-measurement/122-CONTRAST-BASELINE.md
@src/layouts/DashboardLayout.tsx
@CLAUDE.md

<interfaces>
<!-- The nine occurrences in this file, enumerated live 2026-08-20 with
     grep -nE 'text-primary/[0-9]+|text-muted-foreground/[0-9]+|text-\(--[a-zA-Z-]+\)/[0-9]+' -->
  :91   text-primary/60           nav group header <p>, text-xs (12px) font-bold  -> owes 4.5:1
  :111  text-muted-foreground/40  disabled nav item, text-sm (14px)               -> owes 4.5:1
  :117  text-primary/40           "soon" pill, text-[10px]                        -> owes 4.5:1
  :148  text-muted-foreground/80  desktop nav label, text-sm (14px)               -> owes 4.5:1
  :253  text-primary/80           text-[11px] sub-line                            -> owes 4.5:1
  :287  text-muted-foreground/80  mobile nav label, text-sm (14px)                -> owes 4.5:1
  :607  text-primary/60           SYS:/LAT: badge container, text-xs (D-15)
  :610  text-primary/80           Cpu icon (SVG, no text)                         (D-15)
  :616  text-primary/80           Server icon (SVG, no text)                      (D-15)

<!-- Measured axe evidence being remediated, re-derived from
     .planning/phases/122-.../a11y-after/*.json this session: 24 violation objects / 209 nodes
     across 20 cells; 20 objects are color-contrast, 4 are aria-prohibited-attr (all on /forge,
     one per theme — those are plan 123-06's, not this plan's).
     Worst measured shell pairs:
       #817264 on #181222 = 3.93 (aubergine, :148, 77 nodes)
       #717a8a on #171b22 = 3.98 (readable, :148, 77 nodes)
       #0b7157 on #020617 = 3.37 (emerald, :91, 15 nodes)
     Full-opacity --muted-foreground on --card already measures 6.93 / 5.20 / 5.04
     (122-FOLLOW-UPS.md A1), so NO token value needs to move. -->

<!-- NOT in scope on these same lines: bg-primary/10, hover:bg-primary/5, border-primary/20,
     bg-primary/50, and the drop-shadow oklch alpha. The sweep's families are text-* only;
     bg-*/NN and border-*/NN are a different, unmeasured population. Do not touch them. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete the nine opacity modifiers in the app shell</name>
  <files>src/layouts/DashboardLayout.tsx</files>
  <read_first>
    - src/layouts/DashboardLayout.tsx (the whole file — in particular :85-120, :140-155, :245-295 and :600-625)
    - .planning/phases/123-accessibility-remediation/123-PATTERNS.md § "The opacity-modifier sweep (D-01/D-04)" (the verbatim before/after for :91 and :148)
    - .planning/phases/123-accessibility-remediation/123-CONTEXT.md § D-04, § D-15
    - CLAUDE.md § Styling (token-driven, never hardcode hex)
  </read_first>
  <action>
    Delete the `/NN` suffix from all nine `text-*` occurrences listed in `<interfaces>`:
    `text-primary/60` -> `text-primary` (`:91`, `:607`); `text-muted-foreground/40` ->
    `text-muted-foreground` (`:111`); `text-primary/40` -> `text-primary` (`:117`);
    `text-muted-foreground/80` -> `text-muted-foreground` (`:148`, `:287`); `text-primary/80` ->
    `text-primary` (`:253`, `:610`, `:616`). Tailwind v4 already emits the full-opacity
    `color:var(--token)` rule immediately above the `@supports` `color-mix` override for every one
    of these, so deleting the modifier always falls back to a rule that already exists — there is no
    case where the base rule is absent.

    Do NOT substitute a different alpha, and do NOT introduce any hex or palette class; D-04
    forbids reintroducing alpha and Phase 122 froze the palette.

    Two sites need judgement rather than a mechanical delete, and each must be resolved explicitly
    in this task rather than deferred:
    - `:111` is the DISABLED nav item and `:148`/`:287` are the ENABLED nav labels. If both become
      bare `text-muted-foreground`, the disabled state becomes visually indistinguishable from the
      enabled one. D-04's stated remedy for exactly this is to step the quieter element to the next
      quieter TOKEN, never back to alpha. Use `text-muted` for `:111` if that token reads as
      quieter than `text-muted-foreground` in all four themes; if it does not, keep
      `text-muted-foreground` on `:111` and instead preserve the distinction with the
      already-present `cursor-not-allowed select-none` plus an `opacity-*` utility on the whole
      row (which dims background and text together and is not a text-colour alpha). State which
      option was chosen and why in the SUMMARY.
    - `:117` is a 10px "soon" pill with `border-primary/20`. Deleting `text-primary/40` leaves the
      text at full primary against a near-invisible border. That is correct for contrast; do not
      also change the border (out of family).

    Leave every `bg-*/NN`, `border-*/NN` and `drop-shadow-[...oklch(... / 0.N)]` on these lines
    untouched.
  </action>
  <verify>
    <automated>grep -cE 'text-primary/[0-9]+|text-muted-foreground/[0-9]+|text-\(--[a-zA-Z-]+\)/[0-9]+' src/layouts/DashboardLayout.tsx; npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -ohE 'text-primary/[0-9]+|text-muted-foreground/[0-9]+|text-\(--[a-zA-Z-]+\)/[0-9]+' src/layouts/DashboardLayout.tsx | wc -l` returns **0** (occurrence count, not matching lines).
    - Control that the probe discriminates: the same command run against
      `src/components/ToolExecutionPanel.tsx` (untouched by this plan) still returns **6**.
    - `grep -ohE 'bg-primary/[0-9]+|border-primary/[0-9]+' src/layouts/DashboardLayout.tsx | wc -l`
      returns the SAME number before and after this task — record both figures in the SUMMARY.
      Sweeping `bg-`/`border-` as a side effect is explicitly out of scope.
    - `grep -oE "#[0-9a-fA-F]{3,8}" src/layouts/DashboardLayout.tsx | wc -l` is unchanged from
      before the edit — no hex was introduced.
    - `npx tsc --noEmit` exits 0 and `npm test -- src/layouts` passes (or reports no matching test
      files, which is also acceptable — record which).
  </acceptance_criteria>
  <done>All nine text-family opacity modifiers removed from the app shell, with the disabled-vs-enabled nav distinction preserved by a token or a non-text-colour mechanism, and no collateral change to bg/border/shadow alpha.</done>
</task>

<task type="auto">
  <name>Task 2: Measure the shell fix across all four themes and retire the ex-badge column</name>
  <files>.planning/phases/123-accessibility-remediation/123-SHELL-FIX.md</files>
  <read_first>
    - .planning/phases/122-tokens-primitives-contrast-measurement/122-CONTRAST-BASELINE.md (the per-cell matrix and the "READ THIS BEFORE QUOTING ANY NODE-LEVEL FIGURE" determinism caveat, plus the ex-badge column this task retires)
    - .planning/phases/123-accessibility-remediation/123-CONTEXT.md § D-10, § D-15
    - e2e/theme-contrast.spec.ts (the capture mechanism — `A11Y_CAPTURE_DIR`)
  </read_first>
  <action>
    Start a `dev:noauth` server on 5181 from Git Bash (`VITE_CLERK_PUBLISHABLE_KEY= npm run
    dev:noauth`; PowerShell's empty-string assignment DELETES the variable and would leave the
    Clerk gate up, which this repo has already been bitten by). Then run the 20-cell matrix in
    measurement mode with a capture directory under
    `.planning/phases/123-accessibility-remediation/a11y-shell-fix/`:
    `A11Y_MEASURE_ONLY=1 A11Y_CAPTURE_DIR=... PW_BASE_URL=http://localhost:5181
    node_modules/.bin/playwright test e2e/theme-contrast.spec.ts`. Invoke
    `node_modules/.bin/playwright` explicitly — a different-version global Playwright on this
    machine cannot resolve repo-local modules. Note the run is expected to exit non-zero once plan
    123-03 lands D-14; that is correct, and the captures are still written.

    Write `123-SHELL-FIX.md` containing:
    - A per-theme, per-page table of violation OBJECT and NODE counts, aggregated from the new
      capture JSONs, alongside the pre-123 figures from
      `.planning/phases/122-.../a11y-after/*.json`. State the unit on every column heading.
      The pre-123 control aggregate, re-derived this session, is **24 objects / 209 nodes** across
      the 20 cells, of which 20 objects are `color-contrast` and 4 are `aria-prohibited-attr`.
    - The 4 `aria-prohibited-attr` objects are expected to REMAIN after this plan — they live on
      `/forge` and belong to plan 123-05. Say so explicitly so a non-zero residual is not read as
      this plan failing.
    - An explicit statement retiring the ex-badge column: `122-CONTRAST-BASELINE.md` carried it
      because 122 compared deltas and the badge's live-data-gated node count swung 5 to 26 across
      four captures with zero code change. 123's target is zero, so a cell passes or it does not,
      and a badge that clears AA contributes zero nodes whether it renders or not — the confound is
      dissolved rather than excluded. Record the badge's measured node count in this run.
    - D-15's residual rule, verbatim: any OTHER intermittently-failing cell is a real finding, never
      written off as noise.
    - D-10's bar: all four themes are held to the same AA threshold. `readable` carried 78 of the
      209 pre-123 nodes; record its post-fix figure in the same table and the same unit, with no
      separate threshold and no separate pass/fail.
  </action>
  <verify>
    <automated>ls .planning/phases/123-accessibility-remediation/a11y-shell-fix/*.json | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - 20 capture JSON files exist under
      `.planning/phases/123-accessibility-remediation/a11y-shell-fix/`.
    - Aggregated over those 20 files, the number of `color-contrast` nodes whose `target`
      selector matches the app-shell chrome is **0**. Re-derive by parsing the JSON (count
      `violations[].nodes.length` and bucket by `node.target`), never by grepping `"id"` — `"id"`
      also appears on every per-node check object, which is why a naive grep returns 225 against a
      209-node corpus.
    - The residual is **exactly and only** the two non-shell violators plan 123-05 owns, at
      6 nodes total: `.text-primary\/70.text-base` on `/graphs` in cyan/emerald/aubergine
      (`src/components/graph/CodeVaultGraph.tsx:892`) and `.text-\(--muted-foreground\)` on
      `/live-run` in cyan/emerald/aubergine (`src/components/RunTimeline.tsx:81`). Any OTHER
      surviving `color-contrast` node is a finding this plan must report, not absorb.
    - Aggregated `aria-prohibited-attr` objects is still **4** (unchanged), confirming this plan
      neither fixed nor broke the ARIA family that plan 123-06 owns. This is the must-differ
      control: a probe that reported 0 for everything would fail here.
    - The `readable` theme's shell `color-contrast` node count is 0, the same bar as the other
      three — no per-theme carve-out appears anywhere in the document. `readable` carries no
      instance of either residual violator, so its total should reach 0 in this plan.
    - `123-SHELL-FIX.md` contains the literal string `ex-badge column` and states it retired, and
      contains D-15's residual rule.
    - Every count in the document names its unit (objects / nodes / files / occurrences).
  </acceptance_criteria>
  <done>Zero app-shell color-contrast nodes across all 20 measured cells in all four themes, a residual of exactly the 6 nodes plan 123-05 owns, the 4 ARIA objects correctly still present, and the ex-badge column retired with its reasoning recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none introduced | This plan deletes CSS utility suffixes in a layout component and writes a planning document. No data crosses a trust boundary that it did not cross before. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-123-11 | Spoofing | "zero contrast nodes" claim | mitigate | The count is re-derived by parsing `violations[].nodes.length` from the capture JSON, not by grepping `"id"` (which over-counts 225 against a 209-node corpus because per-node check objects also carry an `id`). The `aria-prohibited-attr` residual of 4 is asserted as a must-differ control: a probe returning 0 for everything would fail it. |
| T-123-12 | Tampering | disabled-vs-enabled nav affordance | accept | Removing `text-muted-foreground/40` from the disabled nav item could make a disabled control look enabled — a usability, not a security, concern, and it is handled in Task 1 by stepping to a quieter token or a non-text-colour dim, and again by D-18's operator visual checkpoint in plan 123-11. |
| T-123-SC | Tampering | npm/pip/cargo installs | mitigate | Not applicable — zero packages added; no install task exists in this plan. `123-RESEARCH.md` § Package Legitimacy Audit records the same. |

**Concurrence with `123-RESEARCH.md` § Security Domain:** confirmed after reading the file under
change. `DashboardLayout.tsx` is presentational app-shell chrome; this plan removes opacity
modifiers from nine `className` strings and adds no logic, no data flow, no route and no dependency.
There is no authentication, authorization, input-validation or cryptography surface here. The
`AuthGuard` boundary is a separate component and is untouched.
</threat_model>

<verification>
- Zero text-family `/NN` occurrences remain in `DashboardLayout.tsx`; the control file still shows 6.
- `bg-*/NN` and `border-*/NN` occurrence counts in the same file are unchanged.
- `npx tsc --noEmit` exits 0.
- 20 capture JSONs show 0 app-shell color-contrast nodes, a residual of exactly 6 nodes (CodeVaultGraph + RunTimeline, plan 123-05's), and 4 aria-prohibited-attr objects surviving.
</verification>

<success_criteria>
- The app-shell nav headers, nav labels and runtime badge render at full token opacity in all four themes.
- Every `color-contrast` violation attributable to the app shell is gone from the 20-cell matrix,
  leaving only the 6 nodes plan 123-05 owns.
- The ex-badge column is retired with its reasoning on record, and D-15's residual rule is stated.
</success_criteria>

<output>
Create `.planning/phases/123-accessibility-remediation/123-04-SUMMARY.md` when done. Record: the
chosen remedy for `:111` and why; the before/after `bg-*/NN` + `border-*/NN` counts in the file; the
aggregated per-theme object and node counts with units; and the SYS:/LAT: badge's measured node
count in this run.
</output>
