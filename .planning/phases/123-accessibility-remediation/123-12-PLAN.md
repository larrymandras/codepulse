---
phase: 123-accessibility-remediation
plan: 12
type: execute
wave: 4
depends_on: ["123-08", "123-09"]
files_modified:
  - src/components/ActiveSessions.tsx
  - src/components/AgentTopology.tsx
  - src/components/AlertRulesEngine.tsx
  - src/components/BlackboardPanel.tsx
  - src/components/ChatBubble.tsx
  - src/components/CostBreakdown.tsx
  - src/components/DockerPanel.tsx
  - src/components/DriftTimeline.tsx
  - src/components/EventFeed.tsx
  - src/components/GitActivityWidget.tsx
  - src/components/InfoTooltip.tsx
  - src/components/JobsPanel.tsx
  - src/components/JobsPanel.test.tsx
  - src/components/ModelPricingAdmin.tsx
  - src/components/OnboardingGuide.tsx
  - src/components/ProviderControls.tsx
  - src/components/SwarmTaskDetail.tsx
  - src/components/SwarmTaskNode.tsx
  - src/components/ToolExecutionPanel.tsx
  - src/components/ToolPolicyFeed.tsx
  - src/components/WarRoomKanbanColumn.tsx
  - src/components/WarRoomTaskCard.tsx
autonomous: true
requirements: [A11Y-02]

must_haves:
  truths:
    - "D-01: bucket A's occurrences are classified against measured ratios and only sub-threshold ones change — the sweep is ratio-gated, not a blanket ban on the idiom"
    - "D-04: the /NN is deleted; a token step replaces it only where deleting would collapse two adjacent elements, and alpha is never reintroduced"
  artifacts:
    - path: ".planning/phases/123-accessibility-remediation/123-SWEEP-A-LEDGER.md"
      provides: "per-occurrence disposition for bucket A, with the JobsPanel test-selector lockstep recorded"
  key_links:
    - from: "src/components/JobsPanel.tsx"
      to: "src/components/JobsPanel.test.tsx"
      via: "the test asserts on the literal class string `text-muted-foreground/50` in three querySelectorAll selectors"
      pattern: "text-muted-foreground/50"
---

<objective>
Execute the ratio-gated sweep over bucket A: **22 files, 54 occurrences** of
`text-primary/NN` / `text-muted-foreground/NN` / `text-(--token)/NN` at the top level of
`src/components/`.

This bucket carries the sweep's one genuine trap. `src/components/JobsPanel.test.tsx` is the single
test file in the 65-file corpus, and its 4 occurrences are not styling — three of them are
`querySelectorAll('[class*="text-muted-foreground/50"]')` selectors asserting on the exact class
string `JobsPanel.tsx` renders. Deleting `/50` in the source without updating the test in the same
change turns a passing test into one that silently matches nothing.

Purpose: D-01 and D-04, bucket A.
Output: edits per the ledger, the JobsPanel test kept in lockstep, and `123-SWEEP-A-LEDGER.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/123-accessibility-remediation/123-CONTEXT.md
@.planning/phases/123-accessibility-remediation/123-PATTERNS.md
@.planning/phases/123-accessibility-remediation/123-CONTRAST-RESULT.md
@.planning/phases/123-accessibility-remediation/123-CRITERION-DECISION.md
@src/layouts/DashboardLayout.tsx
@src/components/JobsPanel.tsx
@src/components/JobsPanel.test.tsx
@CLAUDE.md

<interfaces>
<!-- The JobsPanel coupling, verified live 2026-08-20 -->
  src/components/JobsPanel.tsx:34   <CheckCircle className="h-3.5 w-3.5 text-primary/80" />
  src/components/JobsPanel.tsx:81   <ListTodo className="h-6 w-6 text-muted-foreground/50" />
  src/components/JobsPanel.tsx:96   icon={... <Clock className="h-3.5 w-3.5 text-muted-foreground/50" />}

  src/components/JobsPanel.test.tsx:172  container.querySelectorAll('[class*="text-muted-foreground/50"]').length
  src/components/JobsPanel.test.tsx:193  container.querySelectorAll('[class*="text-muted-foreground/50"]').length
  src/components/JobsPanel.test.tsx:200  // comment: "... CheckCircle, `text-primary/80`), so the fallback selector must find"
  src/components/JobsPanel.test.tsx:210  container.querySelectorAll('[class*="text-muted-foreground/50"]').length

  All three JobsPanel.tsx sites are ICON classes on SVG elements. axe's color-contrast rule does not
  apply to SVG icons, so their disposition comes from pass 2 (isolation), not pass 1 (axe) — read it
  off the ledger rather than expecting an axe verdict to exist for them.

<!-- The other `text-(--token)/NN` occurrence in the corpus lives in this bucket:
       src/components/SwarmTaskNode.tsx:107   cancelled: "text-(--status-warn)/80"
     SwarmTaskNode.tsx is ALSO D-08's named instance of raw rgba() inside shadow-[...] (its
     deliberate violet state glow). D-08 is a stated BOUNDARY, not a code change — plan 123-07 wrote
     it down and shipped no edit. Do not touch the shadow here. -->

<!-- The remedy pattern, from the already-executed src/layouts/DashboardLayout.tsx (plan 123-04):
     delete the `/NN`, leaving the bare token class. Tailwind v4 always emits the full-opacity
     `color:var(--token)` base rule immediately above the `@supports` color-mix override, so the
     deletion always falls back to a rule that already exists. -->

<!-- OUT OF FAMILY on the same elements — do NOT sweep as a side effect: bg-*/NN, border-*/NN,
     from-*/NN, to-*/NN, and drop-shadow oklch alpha. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-derive bucket A's live population and reconcile it against the ledger</name>
  <files>.planning/phases/123-accessibility-remediation/123-SWEEP-A-LEDGER.md</files>
  <read_first>
    - .planning/phases/123-accessibility-remediation/123-CONTRAST-RESULT.md § 2 and § 3 (the class-level isolation table and the file-and-line remediation list)
    - .planning/phases/123-accessibility-remediation/123-CRITERION-DECISION.md (whether the criterion widened, and by how much if so)
    - src/components/JobsPanel.test.tsx (:165-215 — all four occurrences and what each is doing)
    - .planning/phases/123-accessibility-remediation/123-CONTEXT.md § D-01, § D-04
  </read_first>
  <action>
    Re-derive the live population for THIS bucket before touching anything, with these commands and
    their units recorded:
      occurrences: `grep -ohE 'text-primary/[0-9]+|text-muted-foreground/[0-9]+|text-\(--[a-zA-Z-]+\)/[0-9]+' <files> | wc -l`
      files:       `grep -olE '...' <files> | wc -l`
    Never `grep -c` for either. Build the file list with `-print0` / `IFS= read -r -d ''` or an
    explicit list — a `for f in $(...)` loop word-splits on paths and silently drops entries, and
    the one it drops is always the one with the unusual name.

    Discrimination control, recorded alongside: the command returns **0** for
    `src/layouts/DashboardLayout.tsx` (cleaned by plan 123-04) and **non-zero** for
    `src/components/ToolExecutionPanel.tsx` (6 occurrences, this bucket's largest member).

    Classify every occurrence into `change` / `leave` / `unreconciled` exactly as plan 123-11 does,
    with file, line, measured ratio and pass label on every row, and the arithmetic shown balancing
    against the live total. `unreconciled` must be 0; a non-zero count means the ledger is
    incomplete and the plan stops and reports rather than guessing a remedy.

    Handle the four `JobsPanel.test.tsx` occurrences explicitly and separately in the ledger. They
    are NOT styling and they get no contrast disposition of their own — their disposition is
    **derived** from `JobsPanel.tsx`'s. Record the rule plainly: if `JobsPanel.tsx:81` and `:96`
    change, then `JobsPanel.test.tsx:172`, `:193` and `:210`'s selector strings and the comment at
    `:200` change in the same commit; if `JobsPanel.tsx` does not change, the test does not either.
  </action>
  <verify>
    <automated>test -f .planning/phases/123-accessibility-remediation/123-SWEEP-A-LEDGER.md</automated>
  </verify>
  <acceptance_criteria>
    - The document states the live occurrence and file counts with the exact commands and units.
    - The discrimination control is recorded: 0 for `DashboardLayout.tsx`, non-zero for
      `ToolExecutionPanel.tsx`.
    - change + leave + unreconciled = the live occurrence total, arithmetic shown; `unreconciled`
      is **0**.
    - The four `JobsPanel.test.tsx` occurrences are listed under their own heading with the derived
      lockstep rule stated, not folded into the styling dispositions.
    - Every `leave` row carries a measured ratio and a pass label.
  </acceptance_criteria>
  <done>Every occurrence in bucket A has a measured disposition, and the test-file coupling is written down as a rule rather than left to be noticed at edit time.</done>
</task>

<task type="auto">
  <name>Task 2: Apply the change list across bucket A, keeping the JobsPanel test in lockstep</name>
  <files>src/components/ActiveSessions.tsx, src/components/AgentTopology.tsx, src/components/AlertRulesEngine.tsx, src/components/BlackboardPanel.tsx, src/components/ChatBubble.tsx, src/components/CostBreakdown.tsx, src/components/DockerPanel.tsx, src/components/DriftTimeline.tsx, src/components/EventFeed.tsx, src/components/GitActivityWidget.tsx, src/components/InfoTooltip.tsx, src/components/JobsPanel.tsx, src/components/JobsPanel.test.tsx, src/components/ModelPricingAdmin.tsx, src/components/OnboardingGuide.tsx, src/components/ProviderControls.tsx, src/components/SwarmTaskDetail.tsx, src/components/SwarmTaskNode.tsx, src/components/ToolExecutionPanel.tsx, src/components/ToolPolicyFeed.tsx, src/components/WarRoomKanbanColumn.tsx, src/components/WarRoomTaskCard.tsx</files>
  <read_first>
    - .planning/phases/123-accessibility-remediation/123-SWEEP-A-LEDGER.md (Task 1's change list — the authority for every edit, including the JobsPanel lockstep rule)
    - src/components/JobsPanel.tsx (:30-100) and src/components/JobsPanel.test.tsx (:165-215) — both, together, before editing either
    - src/components/ToolExecutionPanel.tsx (6 occurrences — this bucket's densest file)
    - src/components/SwarmTaskNode.tsx (:100-115 — the `text-(--status-warn)/80` at :107, and the shadow-[...] violet glow that D-08 deliberately leaves alone)
    - src/layouts/DashboardLayout.tsx (plan 123-04's executed remedy pattern)
    - CLAUDE.md § Styling
  </read_first>
  <action>
    Apply exactly the `change` rows from Task 1's ledger — no more, no fewer. Default remedy is
    deleting the `/NN`. Where deleting would make two adjacent elements read as the same level, step
    the quieter one to the next quieter TOKEN, never back to alpha, and record each such
    substitution with the two elements it separates.

    **JobsPanel lockstep.** If `JobsPanel.tsx:81` and/or `:96` change, update
    `JobsPanel.test.tsx:172`, `:193` and `:210` to the new class string in the SAME edit, and update
    the explanatory comment at `:200` that names `text-primary/80`. Do this before running the
    suite, not after seeing it fail. A `[class*="..."]` selector that matches nothing returns an
    empty NodeList and a `.length` of 0 — which, depending on the assertion, can read as a pass.

    **SwarmTaskNode.tsx:** if `:107`'s `text-(--status-warn)/80` is a `change` row, delete the `/80`
    only. Leave the `shadow-[...]` violet glow untouched: it is D-08's named boundary instance and
    plan 123-07 shipped it as a stated boundary with no code change.

    Do NOT touch `bg-*/NN`, `border-*/NN`, `from-*/NN`, `to-*/NN` or drop-shadow oklch alpha
    anywhere in this bucket. Work file by file rather than with a repo-wide substitution — some
    occurrences of the same class string are `leave` rows, and a global `sed` would convert them too.
  </action>
  <verify>
    <automated>npx tsc --noEmit &amp;&amp; npm test -- src/components/JobsPanel.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `npm test -- src/components/JobsPanel.test.tsx` passes, AND its assertions are proven still
      meaningful: temporarily change one selector to a class string that appears nowhere
      (`text-muted-foreground/50-9x7q2`), confirm the test FAILS, then restore it. A selector-based
      test that silently matches nothing passes just as quietly as one that matches correctly, so
      this mutation is the only thing that distinguishes them. Record the result.
    - **The three selectors do not share an assertion shape, and the unmatchable-string mutation
      above is INERT on one of them.** Verified live 2026-08-20: `:172` and `:193` assert
      `.toBe(1)`, but **`:210` asserts `.toBe(0)`**. Consequences, and they differ per case:
        - `:172` / `:193` (`.toBe(1)`) — a stale selector takes them 1 → 0 and they fail LOUDLY.
          These are self-protecting; the unmatchable-string mutation is a valid proof for them.
        - `:210` (`.toBe(0)`) — a stale selector takes it 0 → 0 and it stays **green**. The
          unmatchable-string mutation cannot fail this assertion, so running it here proves
          nothing. Worse, a whole-FILE mutation turns the file red via the other two and reads as
          proof while the only vacuous assertion in it remains vacuous — this phase's own defect,
          reproduced inside its verification step.
      Required evidence, per case, not per file: for `:172`/`:193`, the unmatchable-string
      mutation must make each **fail**. For `:210`, the valid control is the opposite one — point
      its selector at a class string that IS present in that same rendered container (read one off
      the render; do not guess), and confirm `:210` then **fails**. That is what proves the
      selector can observe the DOM at all rather than being blind. Restore byte-exact afterwards
      and record all three results separately.
    - The post-edit occurrence count for this bucket equals Task 1's `leave` count exactly, with the
      arithmetic shown: `live_before - changed = live_after = leave_count`.
    - The `bg-*/NN` + `border-*/NN` occurrence count across the same 22 files is **unchanged**;
      record before and after.
    - `git diff --stat src/components/SwarmTaskNode.tsx` shows no change to any line containing
      `shadow-[`.
    - `grep -roE "#[0-9a-fA-F]{3,8}" <the 22 files> | wc -l` is unchanged — no hex introduced.
    - `grep -rohE 'text-(primary|muted-foreground)/[0-9]+' <the 22 files> | sort -u` contains only
      class strings that appear as `leave` rows in the ledger.
    - `npx tsc --noEmit` exits 0.
    - `npm test` passes with no NEW failures; record pass/fail counts before and after, and
      adjudicate the known `KnowledgeGraph.test.tsx` flake by stashing and re-running rather than
      assuming it is pre-existing.
  </acceptance_criteria>
  <done>Bucket A carries only measured-passing opacity modifiers, the JobsPanel test still tests something (proven by mutation), and the D-08 shadow boundary is intact.</done>
</task>

<task type="auto">
  <name>Task 3: Re-measure and confirm no regression</name>
  <files>.planning/phases/123-accessibility-remediation/123-SWEEP-A-LEDGER.md</files>
  <read_first>
    - .planning/phases/123-accessibility-remediation/123-SWEEP-A-LEDGER.md (Task 1's lists — the expected post-state)
    - .planning/phases/123-accessibility-remediation/123-CONTRAST-RESULT.md (the pre-sweep per-route figures to compare against)
    - .planning/phases/123-accessibility-remediation/123-CRITERION-DECISION.md (20 cells or 188)
  </read_first>
  <action>
    Start `dev:noauth` on 5181 from Git Bash (`VITE_CLERK_PUBLISHABLE_KEY= npm run dev:noauth`).
    Re-run the widened scan into `.planning/phases/123-accessibility-remediation/a11y-sweep-a/`,
    invoking `node_modules/.bin/playwright` explicitly. Append a "post-sweep" section to
    `123-SWEEP-A-LEDGER.md` with per-rule object and node counts across the criterion cells and
    across all 188, both with units, the delta from `123-CONTRAST-RESULT.md`, and any route whose
    violation count went UP, named individually.

    If plan 123-11 (bucket B) has already landed in this wave, this run measures both sweeps
    together — state that plainly rather than attributing the whole delta to bucket A. If it has
    not, state that too. Which of the two ran first is a fact about the run, not a detail to omit.
  </action>
  <verify>
    <automated>ls .planning/phases/123-accessibility-remediation/a11y-sweep-a/*.json | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - The criterion cells (20, or 188 if the operator chose `widen`) aggregate to **0**
      `color-contrast` nodes and **0** `aria-prohibited-attr` objects.
    - The capture file count is asserted (188 for a widened scan, 20 for the criterion matrix)
      BEFORE any zero is recorded as a result — an empty scan and a clean scan both report 0.
    - No route's violation node count increased relative to `123-CONTRAST-RESULT.md`; any increase
      is named in the SUMMARY.
    - The section states explicitly whether plan 123-11's edits were present in the tree during this
      run.
    - All counts derived by parsing `violations[].nodes.length`, never by grepping `"id"`.
  </acceptance_criteria>
  <done>The bucket-A sweep is measured against the pre-sweep ledger, with the co-presence of bucket B stated rather than glossed over.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| source class string → test selector | `JobsPanel.test.tsx` asserts on a literal class string rendered by `JobsPanel.tsx`. That coupling is the only place in this bucket where a styling edit can silently disarm a test. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-123-33 | Spoofing | `JobsPanel.test.tsx` green status | mitigate | The selectors are updated in the same edit as the source, and a mutation proof (a deliberately unmatchable class string must make the test fail) shows the assertions still discriminate. A `[class*=...]` selector that matches nothing returns length 0, which can read as a pass. |
| T-123-34 | Tampering | D-08's shadow boundary | mitigate | `git diff --stat` on `SwarmTaskNode.tsx` must show no change to any `shadow-[` line, so the sweep cannot quietly alter the instance plan 123-07 recorded as deliberately unchanged. |
| T-123-35 | Spoofing | "the sweep is complete" | mitigate | Post-edit occurrence count must equal the ledger's `leave` count exactly with the arithmetic shown, and the population probe is proven against a known-absent and a known-present file. |
| T-123-SC | Tampering | npm/pip/cargo installs | mitigate | Not applicable — zero packages added; no install task exists in this plan. `123-RESEARCH.md` § Package Legitimacy Audit records the same. |

**Concurrence with `123-RESEARCH.md` § Security Domain:** confirmed. This plan removes CSS opacity
modifiers from `className` strings in 21 presentational components and updates 3 test selectors and
1 comment to match. No authentication, authorization, input-validation or cryptography surface is
touched; no route, no dependency.
</threat_model>

<verification>
- change + leave + unreconciled reconciles to a live re-derived population, with `unreconciled` = 0.
- `JobsPanel.test.tsx` passes AND fails under mutation.
- `bg-*/NN` + `border-*/NN` counts unchanged; no hex introduced; no `shadow-[` line touched.
- `npx tsc --noEmit` exits 0; `npm test` has no new failures.
- The re-measured criterion cells aggregate to 0 contrast nodes and 0 ARIA objects.
</verification>

<success_criteria>
- Every occurrence in bucket A is either changed against a measured sub-threshold ratio or left
  alone against a measured passing ratio.
- The one test file in the corpus still asserts something, proven by mutation rather than assumed
  from a green run.
- D-08's shadow boundary instance is provably untouched.
</success_criteria>

<output>
Create `.planning/phases/123-accessibility-remediation/123-12-SUMMARY.md` when done. Record: the
live re-derived counts with their commands; the change / leave / unreconciled arithmetic; the
JobsPanel mutation-proof result; every token substitution made instead of a plain deletion; the
before/after `bg-*/NN` + `border-*/NN` counts; and whether plan 123-11's edits were present during
the re-measurement run.
</output>
