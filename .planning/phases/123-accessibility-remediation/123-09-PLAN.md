---
phase: 123-accessibility-remediation
plan: 09
type: execute
wave: 3
depends_on: ["123-08"]
files_modified:
  - .planning/phases/123-accessibility-remediation/123-CRITERION-DECISION.md
  - .planning/ROADMAP.md
autonomous: false
requirements: [A11Y-02]

must_haves:
  truths:
    - "D-16: the decision to widen A11Y-02's criterion past the 20 measured cells, or to ship the extra 42 routes as a sized backlog, is made by the operator at a mid-phase checkpoint against real numbers rather than committed up front against an unknown quantity"
  artifacts:
    - path: ".planning/phases/123-accessibility-remediation/123-CRITERION-DECISION.md"
      provides: "the operator's recorded decision, the numbers it was made against, and the resulting scope for plan 123-11"
  key_links:
    - from: ".planning/phases/123-accessibility-remediation/123-CRITERION-DECISION.md"
      to: ".planning/phases/123-accessibility-remediation/123-CONTRAST-RESULT.md"
      via: "the 188-cell violation figures the decision is made against"
      pattern: "123-CONTRAST-RESULT"
---

<objective>
D-16 deliberately did not commit this phase to fixing an unknown quantity on 42 routes nobody had
ever scanned. Plan 123-08 has now scanned them. This is the checkpoint where the operator decides,
against real numbers, whether A11Y-02's finish line moves from 20 cells to 188 — or whether the
extra routes ship as a sized backlog item with figures attached.

Purpose: D-16's mid-phase checkpoint.
Output: `123-CRITERION-DECISION.md` recording the decision, the numbers behind it, and the scope it
hands to plan 123-11.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/123-accessibility-remediation/123-CONTEXT.md
@.planning/phases/123-accessibility-remediation/123-CONTRAST-RESULT.md
@.planning/ROADMAP.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Prepare the decision brief from the measured ledger</name>
  <files>.planning/phases/123-accessibility-remediation/123-CRITERION-DECISION.md</files>
  <read_first>
    - .planning/phases/123-accessibility-remediation/123-CONTRAST-RESULT.md (sections 1-3 — the 188-cell per-route table, the class-level isolation table, and the remediation list)
    - .planning/phases/123-accessibility-remediation/123-CONTEXT.md § D-16 and § "Deferred Ideas" (widening the criterion is explicitly listed as deferred-to-this-checkpoint, not as out of scope)
    - .planning/ROADMAP.md § "Phase 123" success criterion 1 (the line the decision may change)
  </read_first>
  <action>
    Write the decision brief half of `123-CRITERION-DECISION.md` — everything the operator needs to
    answer in one screen, with no interpretation required and no recommendation buried in prose:

    - **The 20-cell criterion status.** Objects and nodes, by rule, with units. Expected 0/0.
    - **The extra 42 routes.** Total violation objects and total nodes across the 168 non-criterion
      cells, by rule, with units. Then the distribution: how many of the 42 routes have zero
      violations, how many have 1-5 nodes, how many have more, and the top five routes by node count
      named individually.
    - **The remediation cost if the criterion widens.** How many distinct source files and how many
      distinct occurrences would have to change beyond plan 123-11's current list, derived from the
      ledger, with units. Say plainly whether that fits inside this phase or not, and on what basis.
    - **What does not change either way.** The 42 routes are SCANNED regardless — D-16 widened the
      scan unconditionally. The decision is only about whether their violations are a gate for this
      phase or a backlog item for a later one.
    - **`/chat` is excluded from both options**, being out of scope for the whole v15.0 milestone.
      Report its numbers separately so they are visible and clearly not counted.
    - **Two named options**, each with what it costs and what it buys:
      (a) **Widen** — A11Y-02's criterion becomes all 188 cells; plan 123-11's scope grows by the
          figure above; `ROADMAP.md`'s criterion 1 is amended on disk.
      (b) **Hold and size** — A11Y-02 closes on the 20 cells as roadmapped; the extra routes become
          a follow-up phase item carrying these exact numbers, filed as a todo before this phase
          closes so it cannot evaporate.

    Do NOT pre-select an option and do NOT put a recommended value in the option text unless the
    ledger supports it — a decision UI makes a suggested figure feel verified. State which option
    the numbers point to only if the ledger makes it unambiguous, and say why.
  </action>
  <verify>
    <automated>test -f .planning/phases/123-accessibility-remediation/123-CRITERION-DECISION.md &amp;&amp; grep -c "Option" .planning/phases/123-accessibility-remediation/123-CRITERION-DECISION.md</automated>
  </verify>
  <acceptance_criteria>
    - The brief states the 20-cell aggregate, the 168-cell aggregate and the `/chat` figures
      separately, each with its unit (objects / nodes / routes / files / occurrences).
    - It names the top five non-criterion routes by node count individually.
    - It states the file count and occurrence count that option (a) would add to plan 123-11.
    - Both options are stated with their cost and their consequence; neither is marked as chosen.
    - Every figure is traceable to `123-CONTRAST-RESULT.md` — no number appears that is not in the
      ledger or derivable from it by a stated command.
  </acceptance_criteria>
  <done>A one-screen decision brief with real numbers, two costed options, and no pre-selected answer.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 2: Operator decides whether A11Y-02's criterion widens to all 47 routes</name>
  <files>.planning/phases/123-accessibility-remediation/123-CRITERION-DECISION.md</files>
  <read_first>
    - .planning/phases/123-accessibility-remediation/123-CRITERION-DECISION.md (the brief from Task 1)
  </read_first>
  <decision>Does A11Y-02's pass/fail criterion stay at the 20 theme x page cells A11Y-01 measured, or widen to all 188 cells (47 routes x 4 themes) now that the extra 42 routes have real numbers?</decision>
  <context>
    D-16 widened the SCAN unconditionally and held the CRITERION at 20 cells, with this checkpoint
    written in specifically so the choice would be made against data. Plan 123-08 has produced that
    data. Committing to all 47 up front was rejected because it would bind the phase to fixing an
    unknown quantity discovered after planning closed; that objection is now resolved either way,
    and the remaining question is whether the measured quantity fits this phase.

    Read the numbers in `123-CRITERION-DECISION.md` before answering. Whichever way this goes, the
    42 extra routes stay in the scan — a future regression on any of them still fails the suite once
    the criterion is whatever it is set to here.
  </context>
  <options>
    <option id="widen">
      <name>Widen the criterion to all 188 cells</name>
      <pros>A11Y-02 becomes a claim about the whole app rather than about 5 of 47 routes; the measurement ratchet D-17 describes covers every route from the day it ships; no follow-up phase needed.</pros>
      <cons>Plan 123-11's scope grows by the file/occurrence count in the brief; the phase closes later; some of the extra violations may be in surfaces Phase 124 or 125 is about to change anyway.</cons>
    </option>
    <option id="hold-and-size">
      <name>Hold at 20 cells and file the rest as a sized backlog</name>
      <pros>The phase closes on the scope it was roadmapped with; the extra routes get a follow-up item carrying real measured numbers instead of a guess; avoids remediating surfaces that later phases will rewrite.</pros>
      <cons>The app still ships known sub-AA sites on up to 42 routes after this phase closes; the backlog item has to actually be filed before close or it evaporates.</cons>
    </option>
  </options>
  <action>
    Present the brief and wait. Record the operator's answer verbatim in
    `123-CRITERION-DECISION.md` under a `## Decision` heading, with the date, the option id, and
    the operator's own words. Then apply the consequence in the same task:
    - If `widen`: amend `ROADMAP.md`'s Phase 123 success criterion 1 on disk to name 188 cells,
      with a dated correction note in this repo's established style, and state in the document the
      exact additional file/occurrence list plan 123-11 must now cover.
    - If `hold-and-size`: create `.planning/todos/pending/` entry for the remaining routes carrying
      the measured object/node counts, the per-route table and the derivation commands, and state in
      the document that plan 123-11's scope is unchanged. Filing the todo is part of THIS task, not
      a later one — a backlog item that depends on someone remembering later is the thing this
      option is criticised for.
  </action>
  <verify>
    <human-check>Operator has answered and the answer is recorded verbatim with a date and an option id.</human-check>
  </verify>
  <acceptance_criteria>
    - `123-CRITERION-DECISION.md` contains a `## Decision` section naming exactly one option id
      (`widen` or `hold-and-size`), the date, and the operator's verbatim words.
    - If `widen`: `grep -c "188" .planning/ROADMAP.md` is greater than its pre-edit value and the
      Phase 123 criterion 1 line names the widened cell count with a dated correction note.
    - If `hold-and-size`: a new file exists under `.planning/todos/pending/` whose body contains the
      per-route violation table and the exact command used to derive it.
    - No downstream plan begins before this section exists — this is a blocking gate.
  </acceptance_criteria>
  <resume-signal>Reply `widen` or `hold-and-size`, optionally with reasoning.</resume-signal>
  <done>The criterion for A11Y-02 is settled against measured data, the consequence is applied on disk, and plan 123-11's scope is unambiguous.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → phase scope | A human decision changes what this phase is required to deliver and what a downstream plan edits. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-123-25 | Repudiation | the scope decision | mitigate | The operator's answer is recorded verbatim with a date and an option id, alongside the exact figures it was made against, so the decision and its basis are both recoverable later. |
| T-123-26 | Tampering | the `hold-and-size` backlog | mitigate | The todo is filed inside this task rather than promised for later, and its acceptance criterion requires the derivation command to be in the file — a backlog item without its numbers is a guess wearing a filename. |
| T-123-SC | Tampering | npm/pip/cargo installs | mitigate | Not applicable — zero packages added; no install task exists in this plan. `123-RESEARCH.md` § Package Legitimacy Audit records the same. |

**Concurrence with `123-RESEARCH.md` § Security Domain:** confirmed. This plan writes planning
documents and, in one branch, a ROADMAP correction. No application code, route, dependency,
authentication, authorization, input-validation or cryptography surface is involved.
</threat_model>

<verification>
- `123-CRITERION-DECISION.md` exists with a brief section and a `## Decision` section.
- The recorded option id is one of `widen` / `hold-and-size`.
- The corresponding on-disk consequence (ROADMAP amendment or filed todo) exists.
</verification>

<success_criteria>
- The criterion decision is made against measured numbers, not a guess.
- Whichever branch was taken, its consequence is already on disk before the next wave starts.
</success_criteria>

<output>
Create `.planning/phases/123-accessibility-remediation/123-09-SUMMARY.md` when done. Record the
option chosen, the operator's verbatim words, and the resulting scope delta for plan 123-11 stated
as a file count and an occurrence count with units.
</output>
