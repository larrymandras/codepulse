---
phase: 123-accessibility-remediation
plan: 10
type: execute
wave: 3
depends_on: ["123-02", "123-08"]
files_modified:
  - e2e/contrast-isolation.spec.ts
  - src/components/IdeationRow.tsx
  - src/components/InboxCard.tsx
  - src/components/ScanResultsPanel.tsx
  - src/components/TaskDetail.tsx
autonomous: true
requirements: [A11Y-02]

must_haves:
  truths:
    - "D-05: the status-fill/foreground pairing is remedied as a class across all 8 sites in 4 files, each measured through D-02's harness and remedied per its own ratio, and the header comment that documents the defective pairing is corrected because a comment re-seeds it into new code"
  artifacts:
    - path: "src/components/ScanResultsPanel.tsx"
      provides: "AA-compliant severity badges and a header comment that no longer documents the defective pairing"
    - path: "e2e/contrast-isolation.spec.ts"
      provides: "measured ratios for all 8 status-fill pairings across 4 themes"
  key_links:
    - from: "src/components/TaskDetail.tsx"
      to: "PRIORITY_COLORS"
      via: "the pairing is assembled at :67 from a background map at :27-31 — the text colour is not in the map"
      pattern: "PRIORITY_COLORS"
---

<objective>
Fix the worst contrast ratio in the codebase. `bg-(--status-warn) text-(--foreground)` rasterises to
**~1.4–1.8:1**. axe never sees it because none of these sites render on the 5 measured routes — it
is exactly the "instance-not-class, and the class is invisible to the instrument" shape this phase
exists to remove.

But the defect is a CLASS and warn is only its measured instance. Re-derived live 2026-08-20:

```
grep -rnE 'bg-\(--status-(error|warn|ok)\)[^"]*text-\(--foreground\)' src --include=*.tsx
```

returns **7 contiguous code sites across 3 files** — `IdeationRow.tsx:30`;
`InboxCard.tsx:97,98,99`; `ScanResultsPanel.tsx:39,41,43` — plus 3 doc-comment lines
(`ScanResultsPanel.tsx:11,12,13`). `TaskDetail.tsx:67` is an **8th** site that regex structurally
cannot match. Only 3 of the 8 are `warn`.

Purpose: D-05, as widened at plan time.
Output: 8 remedied sites, one corrected header comment, and measured ratios for every one.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/123-accessibility-remediation/123-CONTEXT.md
@.planning/phases/123-accessibility-remediation/123-PATTERNS.md
@.planning/phases/122-tokens-primitives-contrast-measurement/122-BADGE-LAW.md
@src/components/StatusBadge.tsx
@src/components/IdeationRow.tsx
@src/components/InboxCard.tsx
@src/components/ScanResultsPanel.tsx
@src/components/TaskDetail.tsx
@CLAUDE.md

<interfaces>
<!-- The 8 sites, verbatim, verified live 2026-08-20 -->
  IdeationRow.tsx:30        medium: "bg-(--status-warn) text-(--foreground)",          [warn]
  InboxCard.tsx:97          high:   "bg-(--status-error) text-(--foreground)",         [error]
  InboxCard.tsx:98          medium: "bg-(--status-warn) text-(--foreground)",          [warn]
  InboxCard.tsx:99          low:    "bg-(--status-ok) text-(--foreground)",            [ok]
  ScanResultsPanel.tsx:39   return "bg-(--status-error) text-(--foreground)";          [error]
  ScanResultsPanel.tsx:41   return "bg-(--status-warn) text-(--foreground)";           [warn]
  ScanResultsPanel.tsx:43   return "bg-(--status-ok) text-(--foreground)";             [ok]
  TaskDetail.tsx:67         `... text-(--foreground) uppercase ${PRIORITY_COLORS[task.priority]}`

<!-- TaskDetail is assembled from two fragments, which is why no contiguous grep finds it -->
  TaskDetail.tsx:27-31   const PRIORITY_COLORS = { high: "bg-(--status-error)",
                                                   medium: "bg-(--status-warn)",
                                                   low: "bg-(--status-ok)" };   // NO text colour
  TaskDetail.tsx:67      className={`text-xs font-semibold px-1.5 py-0.5 rounded-sm
                                     text-(--foreground) uppercase ${PRIORITY_COLORS[task.priority]}`}
  => `:67` applies to ALL THREE priorities. Changing it repaints error and ok as well as warn.

<!-- The ONLY shipped control, src/components/StatusBadge.tsx:53-56 -->
  const strongStyles: Record<string, string> = {
    error: "bg-(--status-error-fill) text-(--status-error-on-fill)",
    warn:  "bg-(--status-warn) text-(--primary-foreground)",
  };
  StatusBadge's own comment (:50-51) records that --foreground on --status-warn rasterises to
  ~1.4-1.8:1, measured, and rejected (122-BADGE-LAW.md §8).

<!-- WHY the control does NOT transfer to the other five sites:
     - StatusBadge has NO `ok` entry at all, so there is no shipped remedy for the ok fill.
     - StatusBadge's ERROR remedy uses a DIFFERENT TOKEN PAIR entirely
       (bg-(--status-error-fill) text-(--status-error-on-fill)), not text-(--primary-foreground).
     So pattern-matching --primary-foreground across all 8 is the calculated-not-measured shape D-02
     rejects, and fixing only the 3 warn sites is the instance-not-class shape D-01 rejects. -->

<!-- Header comment that DOCUMENTS the defective pairing, ScanResultsPanel.tsx:10-14 -->
   * Severity badge colors per UI-SPEC scan color rules:
   *   HIGH   -> bg-(--status-error)  text-(--foreground)
   *   MEDIUM -> bg-(--status-warn)   text-(--foreground)
   *   LOW    -> bg-(--status-ok)     text-(--foreground)

<!-- CORRECTION to CONTEXT.md D-05's citation, verified live: InboxCard.tsx:12 does NOT hold such a
     comment — line 12 is part of an unrelated item-type doc block, and `RiskBadge` (:95-99) carries
     no preceding doc comment at all. ScanResultsPanel.tsx is the only file with a comment to
     correct. The token swap in InboxCard.tsx is unaffected. -->

<!-- OUT OF SCOPE in these files: IdeationRow.tsx:28,29,31 use `text-white`, a raw palette class
     rather than a token. It is a real CLAUDE.md violation but it is not this phase's defect class
     and Phase 122 did not measure it. Do not sweep it here; note it for a follow-up todo. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Measure all 8 status-fill pairings across all four themes</name>
  <files>e2e/contrast-isolation.spec.ts</files>
  <read_first>
    - e2e/contrast-isolation.spec.ts (the `CLASS_MATRIX` shape, the artifact writer, the sentinel discipline)
    - e2e/lib/contrast.ts (`paintedColorOfClass`, `getThemeTokenText`, `contrastRatio`, `readTextMetrics`, `wcagThresholdFor`)
    - src/components/StatusBadge.tsx (:45-60 — the shipped control pairings and the comment recording the measured ~1.4-1.8:1)
    - .planning/phases/122-tokens-primitives-contrast-measurement/122-BADGE-LAW.md § 8 (the measured badge pairings, including the control table for the old Forge `failed` pairing)
    - .planning/phases/123-accessibility-remediation/123-CONTEXT.md § D-02, § D-05
  </read_first>
  <action>
    Add a `STATUS_FILL_MATRIX` to `e2e/contrast-isolation.spec.ts` measuring, for each of the four
    themes, the rasterised contrast ratio of each of these foreground/background pairings:

      foregrounds: `--foreground` (current, defective), `--primary-foreground` (StatusBadge's warn
                   remedy), `--status-error-on-fill` (StatusBadge's error remedy)
      backgrounds: `--status-error`, `--status-warn`, `--status-ok`
      plus `--status-error-fill` as a background, since StatusBadge's error remedy pairs
      `--status-error-on-fill` with `--status-error-fill`, not with `--status-error`.

    Measure by rasterisation via the shared `paintedColorOfClass` / `sampleColor` primitives, never
    by hex+alpha arithmetic and never by scraping a `getComputedStyle` colour string — Tailwind v4
    emits `oklch()`/`oklab()` and a number-scrape reads the hue angle as a channel. Return `null`
    on any unparseable value and assert `.not.toBeNull()` rather than substituting a default.

    Emit every row into `e2e/.artifacts/123-isolation-pass2.json` with `pass: "isolation"` and a
    `family: "status-fill"` tag so the ledger can separate it from the opacity-modifier rows.

    Assertions in this task are about the INSTRUMENT, not the verdicts:
    - Assert the known-defective pairing `--foreground` on `--status-warn` measures **below 4.5**,
      reproducing StatusBadge's recorded ~1.4-1.8:1. This is the calibration case: if it does not
      reproduce, the harness disagrees with a figure Phase 122 measured and the disagreement must be
      resolved before any verdict from it is trusted.
    - Assert the known-good pairing `--primary-foreground` on `--status-warn` measures **above 4.5**
      in all four themes. One must-fail and one must-pass, so a harness that flags everything and a
      harness that flags nothing both fail.
    Do NOT assert anything about the error or ok pairings yet — that is what this task is measuring.
  </action>
  <verify>
    <automated>node_modules/.bin/playwright test e2e/contrast-isolation.spec.ts --reporter=line</automated>
  </verify>
  <acceptance_criteria>
    - The run exits 0 and `e2e/.artifacts/123-isolation-pass2.json` gains at least
      `3 foregrounds x 4 backgrounds x 4 themes = 48` rows tagged `family: "status-fill"`.
    - The `--foreground` on `--status-warn` row measures below 4.5 in every theme, and the SUMMARY
      quotes all four ratios to two decimal places against StatusBadge's recorded ~1.4-1.8:1.
    - The `--primary-foreground` on `--status-warn` row measures above 4.5 in every theme.
    - `grep -c "toBeNull" e2e/contrast-isolation.spec.ts` is at least 1 and no consumer substitutes
      a fallback colour on a `null` sample.
    - No hex token value appears in the added code — the tokens are read from the live page.
  </acceptance_criteria>
  <done>Every one of the four status-fill background tokens has a measured ratio against every candidate foreground in every theme, with the harness calibrated against a figure Phase 122 already measured.</done>
</task>

<task type="auto">
  <name>Task 2: Remedy all 8 sites per their own measured ratio and correct the re-seeding comment</name>
  <files>src/components/IdeationRow.tsx, src/components/InboxCard.tsx, src/components/ScanResultsPanel.tsx, src/components/TaskDetail.tsx</files>
  <read_first>
    - e2e/.artifacts/123-isolation-pass2.json (Task 1's measured table — the remedy for each site comes from here, not from pattern-matching StatusBadge)
    - src/components/IdeationRow.tsx (:27-33 — `SEVERITY_CLASSES`)
    - src/components/InboxCard.tsx (:92-105 — `RiskBadge` and its `styles` map)
    - src/components/ScanResultsPanel.tsx (:8-16 the header comment, :36-46 `severityBadgeClass`)
    - src/components/TaskDetail.tsx (:27-31 `PRIORITY_COLORS` and :60-72 the consuming template at :67)
    - src/components/StatusBadge.tsx (:45-60 — the two shipped remedies and why they differ)
    - CLAUDE.md § Styling (token-driven, never hardcode hex; Phase 122 froze the palette)
  </read_first>
  <action>
    For each of the 8 sites, pick the foreground token whose MEASURED ratio against that site's
    background clears 4.5:1 in all four themes, reading the answer off Task 1's table. Do not
    pattern-match `--primary-foreground` across the class — `StatusBadge` has no `ok` entry and its
    error remedy uses a different token pair entirely, so the warn control does not transfer. If a
    background has NO existing token that clears 4.5:1 against it in all four themes, stop and
    report that rather than inventing a token: Phase 122 froze the palette and minting a new
    `--status-*-on-fill` token is a token-layer change this phase is not scoped for. Say which
    background, which themes, and what the best available ratio was.

    Site-by-site:
    - `IdeationRow.tsx:30` — swap the foreground in the `medium` entry. Leave `:28`, `:29` and
      `:31`'s `text-white` alone; it is a raw palette class and a real `CLAUDE.md` violation, but it
      is a different defect class that Phase 122 did not measure. File a todo for it instead of
      sweeping it here.
    - `InboxCard.tsx:97,98,99` — swap all three foregrounds in `RiskBadge`'s `styles` map. There is
      no header comment to correct in this file; `CONTEXT.md`'s citation of `InboxCard.tsx:12` was
      stale and the file has drifted since it was written.
    - `ScanResultsPanel.tsx:39,41,43` — swap all three returns in `severityBadgeClass`. Then correct
      the header comment at `:10-14` so it documents the REMEDIED pairing. D-05 treats this comment
      as code: a comment documenting a defective pairing re-seeds it into new code. Leave the
      `SAFE -> text-(--muted-foreground) plain text "Clean"` line alone unless Task 1 measured it
      failing.
    - `TaskDetail.tsx:67` — this is the assembled site. There is no `text-(--foreground)` at `:29`
      to swap; the text colour is hardcoded in the consuming template at `:67` and applies to all
      three priorities at once. If Task 1's table shows one foreground clears 4.5:1 against all
      three of `--status-error`, `--status-warn` and `--status-ok`, change `:67` to that token. If
      it does not, move the foreground INTO the `PRIORITY_COLORS` map at `:27-31` so each priority
      carries its own measured pairing, and strip `text-(--foreground)` from the `:67` template.
      State which shape was taken and the ratios that decided it.

    No hex, no palette class, no new token, no alpha modifier anywhere in this task.
  </action>
  <verify>
    <automated>npx tsc --noEmit &amp;&amp; npm test -- src/components/IdeationRow src/components/InboxCard src/components/ScanResultsPanel src/components/TaskDetail</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rnE 'bg-\(--status-(error|warn|ok)\)[^"]*text-\(--foreground\)' src --include=*.tsx`
      returns **0** lines — down from 10 (7 code + 3 doc-comment).
    - Control that the probe discriminates: the same regex with `text-\(--primary-foreground\)`
      substituted returns a NON-zero count, proving the sites still exist and were remedied rather
      than deleted.
    - `grep -Fc "text-(--foreground)" src/components/TaskDetail.tsx` returns **0** if the token was
      swapped at `:67`, or the `PRIORITY_COLORS` map at `:27-31` contains a `text-` class for each
      of the three priorities if the map shape was taken. State which in the SUMMARY.
    - `src/components/ScanResultsPanel.tsx`'s header comment no longer contains the string
      `text-(--foreground)` on the HIGH/MEDIUM/LOW lines.
    - `grep -Fc "text-white" src/components/IdeationRow.tsx` returns **3** — unchanged. This task
      does not sweep raw palette classes.
    - `grep -oE "#[0-9a-fA-F]{3,8}" src/components/IdeationRow.tsx src/components/InboxCard.tsx src/components/ScanResultsPanel.tsx src/components/TaskDetail.tsx | wc -l`
      is unchanged from before the edit — no hex introduced.
    - `npx tsc --noEmit` exits 0 and the four components' existing unit tests pass.
    - A todo exists under `.planning/todos/pending/` for `IdeationRow.tsx`'s three `text-white`
      sites, naming the file, the three line numbers and the `CLAUDE.md` rule they violate.
  </acceptance_criteria>
  <done>All 8 sites in the class carry a measured, AA-clearing foreground; the comment that would re-seed the defect is corrected; the out-of-class `text-white` sites are filed rather than silently swept or silently ignored.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none introduced | Four presentational components change CSS token references. No data, auth or route behaviour changes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-123-27 | Spoofing | severity/priority semantics | mitigate | Changing a badge foreground must not change which severity a user reads. `TaskDetail.tsx:67` applies to all three priorities at once, so the plan requires either one token proven against all three backgrounds or a per-priority map — never a blind swap that silently repaints error and ok to fix warn. |
| T-123-28 | Spoofing | "the class is fixed" claim | mitigate | The post-fix grep is paired with a must-differ control (the same regex against the remedy token must return non-zero), so a site deleted outright cannot masquerade as a site remedied. |
| T-123-29 | Repudiation | out-of-class defects | mitigate | `IdeationRow.tsx`'s three `text-white` sites are filed as a todo with file and line numbers rather than swept as a side effect or left unmentioned. |
| T-123-SC | Tampering | npm/pip/cargo installs | mitigate | Not applicable — zero packages added; no install task exists in this plan. `123-RESEARCH.md` § Package Legitimacy Audit records the same. |

**Concurrence with `123-RESEARCH.md` § Security Domain:** confirmed after reading the four files
under change. These are badge/label components; the plan changes token references in `className`
strings and one doc comment. No authentication, authorization, input-validation or cryptography
surface, no route, no dependency.
</threat_model>

<verification>
- The class regex returns 0 while its remedy-token control returns non-zero.
- All 8 sites' chosen foregrounds have a measured ratio >= 4.5:1 in all four themes, recorded.
- `ScanResultsPanel.tsx`'s header comment documents the remedied pairing.
- `npx tsc --noEmit` exits 0; the four components' unit tests pass.
- A todo is filed for the three `text-white` sites.
</verification>

<success_criteria>
- The worst contrast pairing in the app is gone, in all four themes, at all 8 sites of its class —
  including the one no contiguous grep can find.
- Every remedy is traceable to a measured ratio, not to a pattern match against a control that does
  not transfer.
- The comment that would re-seed the defect into new code is corrected.
</success_criteria>

<output>
Create `.planning/phases/123-accessibility-remediation/123-10-SUMMARY.md` when done. Record: the
four measured `--foreground` on `--status-warn` ratios against StatusBadge's recorded ~1.4-1.8:1;
the foreground token chosen for each of the four background tokens and its worst-theme ratio; the
shape taken for `TaskDetail.tsx` and why; and the filed todo's path.
</output>
