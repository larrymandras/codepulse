---
phase: 123-accessibility-remediation
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/forge/ForgeJobList.tsx
  - src/components/skills/SkillReviewDrawer.tsx
  - src/pages/ForgePage.tsx
  - src/tokenSweep.ratchet.test.ts
autonomous: true
requirements: [A11Y-02]

must_haves:
  truths:
    - "D-06: the ARIA scope is measurement-defined — whatever wcag2a/wcag2aa non-contrast rule fires across the scanned route set is fixed, with the 2 aria-busy sites as a named floor fixed regardless of whether they render during a scan"
    - "D-09: /forge adopts PageHeader, and tokenSweep.ratchet.test.ts's KNOWN_EXEMPT entry for src/pages/ForgePage.tsx is removed in the same change so the ratchet stops being blind to that file"
  artifacts:
    - path: "src/components/forge/ForgeJobList.tsx"
      provides: "aria-prohibited-attr-free loading state"
    - path: "src/pages/ForgePage.tsx"
      provides: "PageHeader-based page header"
      contains: "PageHeader"
    - path: "src/tokenSweep.ratchet.test.ts"
      provides: "ratchet with the ForgePage pageheader exemption removed"
  key_links:
    - from: "src/pages/ForgePage.tsx"
      to: "src/components/PageHeader.tsx"
      via: "component substitution with the mobile toggle passed as `actions`"
      pattern: "PageHeader"
    - from: "src/tokenSweep.ratchet.test.ts"
      to: "src/pages/ForgePage.tsx"
      via: "KNOWN_EXEMPT no longer lists this file for the pageheader bucket"
      pattern: "KNOWN_EXEMPT"
---

<objective>
Clear the 4 `aria-prohibited-attr` violation objects — one per theme, all on `/forge` — that a
colour-only sweep structurally cannot touch, and close TOKEN-05's known partial by converting
`/forge` to `PageHeader` while the file is open.

The 4 objects are the entire non-contrast half of the measured matrix. A11Y-02's criterion is
`expect(results.violations).toEqual([])` at `e2e/theme-contrast.spec.ts:107` — an empty array, not
an empty contrast bucket — so these block the requirement by force, not by choice.

Purpose: D-06 and D-09 (including D-09's non-negotiable rider).
Output: ARIA fixes in two components, `PageHeader` adoption in `ForgePage.tsx`, and the removal of
the ratchet exemption that would otherwise leave that file permanently unguarded.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/123-accessibility-remediation/123-CONTEXT.md
@.planning/phases/123-accessibility-remediation/123-PATTERNS.md
@.planning/phases/123-accessibility-remediation/123-RESEARCH.md
@src/components/forge/ForgeJobList.tsx
@src/components/skills/SkillReviewDrawer.tsx
@src/pages/ForgePage.tsx
@src/components/PageHeader.tsx

<interfaces>
<!-- The flagged element, src/components/forge/ForgeJobList.tsx:170-175 -->
  <div className="flex flex-col gap-2 p-3" aria-busy="true" aria-label="Loading jobs">

<!-- The floor's second site, src/components/skills/SkillReviewDrawer.tsx:90-94 (no aria-label) -->
  <li key={s.name} className="rounded-lg border border-border bg-card p-3" aria-busy={busy === s.name}>

<!-- NOT a site: ForgeJobList.tsx:166 is a COMMENT mentioning aria-busy
     (`// Loading state — 3 skeleton rows with aria-busy`). CONTEXT.md's original "3 sites" figure
     counted it. The floor is 2 attribute sites in 2 files. Any scan defining this family's floor
     must exclude comment lines or it inherits a phantom third site. -->

<!-- HYPOTHESIS only, do NOT pre-fix (123-RESEARCH.md Assumption A2, Open Question 2):
     ForgeJobList.tsx:227 puts `aria-selected={isSelected}` on a bare <button>. aria-selected is
     role-restricted (option/row/tab/gridcell/...). It sits on /forge, a measured route, so the axe
     run settles it. D-06 is measurement-defined and explicitly rejects a hand-maintained census —
     fix it only if axe flags it. -->

<!-- src/components/PageHeader.tsx — the target component. Note `cn()` (twMerge), so a caller-passed
     margin BEATS the baked-in mb-4: -->
  <div className={cn("flex items-center justify-between mb-4", className)}>
    <div className="flex items-center gap-2">{Icon && <Icon className="h-6 w-6" />}
      <div>{eyebrow && <p .../>}<h1 className="text-2xl font-bold text-foreground">{title}</h1>
        {subtitle && <p .../>}</div></div>
    {actions}
  </div>

<!-- The defect being replaced, src/pages/ForgePage.tsx:150-161 -->
  <div className="flex items-center justify-between shrink-0">
    <h1 className="text-2xl font-bold text-foreground">Forge</h1>
    <button type="button" onClick={() => setListOpen(true)} aria-label="Show job list"
      className="md:hidden flex items-center justify-center size-11 rounded-md border border-border text-muted-foreground hover:text-foreground transition-colors">
      <PanelLeft className="h-5 w-5" />
    </button>
  </div>

<!-- D-09's rider, recorded verbatim in the ratchet's own KNOWN_EXEMPT at
     src/tokenSweep.ratchet.test.ts:209-221: "A straight substitution would double the vertical
     spacing above the master-detail body since PageHeader bakes in mb-4 and ForgePage's current
     header carries none." The parent is
     `<div className="flex flex-col h-full overflow-hidden space-y-4">` (ForgePage.tsx:145), so the
     4-unit gap already comes from space-y-4. RESOLUTION: pass className="mb-0" to PageHeader —
     cn()/twMerge makes the caller's margin win, and src/pages/LiveRun.tsx:209 already ships exactly
     this idiom. -->

<!-- The nearest call sites to imitate:
     src/pages/Dashboard.tsx:54   <PageHeader title="Dashboard" />                 (simplest)
     src/pages/LiveRun.tsx:207-209 <PageHeader title="Live Run" className="mb-0" actions={...} />
     src/pages/Analytics.tsx:54    <PageHeader title="Analytics" className="col-span-12 mb-6" /> -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix the measured aria-prohibited-attr violation and the named ARIA floor</name>
  <files>src/components/forge/ForgeJobList.tsx, src/components/skills/SkillReviewDrawer.tsx</files>
  <read_first>
    - src/components/forge/ForgeJobList.tsx (:160-235 — the loading branch at :166-180 and the job button at :218-232)
    - src/components/skills/SkillReviewDrawer.tsx (:86-98 — the `<li>` carrying `aria-busy`)
    - .planning/phases/122-tokens-primitives-contrast-measurement/a11y-after/cyan__Forge.json (read the `aria-prohibited-attr` violation object: its `nodes[].target`, `nodes[].html` and `nodes[].any[0].message` name the exact attribute axe objects to — read it, do not infer from the ARIA spec)
    - .planning/phases/123-accessibility-remediation/123-CONTEXT.md § D-06
    - .planning/phases/123-accessibility-remediation/123-PATTERNS.md § "D-06 — aria-busy/ARIA floor sites"
  </read_first>
  <action>
    Read the captured `aria-prohibited-attr` object in
    `.planning/phases/122-.../a11y-after/cyan__Forge.json` FIRST and let its `message` field name
    which attribute is prohibited on which implicit role. D-06 is measurement-defined and
    explicitly rejects spec-reading as the source of truth; the capture already holds the answer.

    Remedy `ForgeJobList.tsx:171-175`. A bare `<div>` has no implicit ARIA role, so a global
    attribute like `aria-label` has nothing to label. Two acceptable shapes, chosen by what the
    capture says is prohibited:
      (a) give the element an explicit role that permits both attributes — `role="status"` is the
          natural fit for a loading region and also makes the skeleton announce, or
      (b) drop the `aria-label` from the div and move the accessible name onto a wrapping labelled
          region, leaving `aria-busy` alone.
    Prefer (a) unless the capture shows `aria-busy` itself is the prohibited attribute. State which
    was chosen and quote the capture's `message` line that decided it.

    Apply the same reasoning to `SkillReviewDrawer.tsx:93`. That `<li>` carries `aria-busy` with no
    `aria-label` and sits inside a `<ul>` (so it has an implicit `listitem` role, unlike the bare
    div). It is D-06's named floor and is fixed regardless of whether it renders during a scan — but
    if the capture shows only the Forge combination is prohibited, the two sites legitimately need
    DIFFERENT remedies. Do not pattern-match one onto the other.

    `ForgeJobList.tsx:227`'s `aria-selected={isSelected}` on a bare `<button>` is a HYPOTHESIS, not
    a finding. Do not pre-fix it. Run axe against `/forge` after the two fixes above and let the
    result decide: if it fires, fix it in this task and say so; if it does not, record that it did
    not and leave it alone. Either outcome is a result; guessing is not.
  </action>
  <verify>
    <automated>npx tsc --noEmit &amp;&amp; npm test -- src/components/forge src/components/skills</automated>
  </verify>
  <acceptance_criteria>
    - With `dev:noauth` on 5181, running
      `PW_BASE_URL=http://localhost:5181 A11Y_MEASURE_ONLY=1 A11Y_CAPTURE_DIR=.planning/phases/123-accessibility-remediation/a11y-aria/ node_modules/.bin/playwright test e2e/theme-contrast.spec.ts --grep "Forge"`
      produces 4 capture JSONs in which the aggregated count of `aria-prohibited-attr` violation
      OBJECTS is **0** (was 4, one per theme). Derive by parsing `violations[].id`, not by grepping
      `"id"` — `"id"` also appears on every per-node check object.
    - Must-differ control in the same parsed output: the `color-contrast` object count is still
      reported and non-zero unless plans 123-04 and 123-05 have already landed. State which state
      was observed. A probe that returns 0 for every rule id regardless is not evidence.
    - `grep -n 'aria-busy' src/components/forge/ForgeJobList.tsx | grep -v '^\s*[0-9]*:\s*//' | wc -l`
      is used for any site count in the SUMMARY — comment lines excluded. The attribute-site floor
      is **2 sites in 2 files** (`ForgeJobList.tsx:173`, `SkillReviewDrawer.tsx:93`), never 3.
    - `npx tsc --noEmit` exits 0 and the existing forge/skills unit tests pass.
    - The SUMMARY quotes the capture's `message` string that determined the chosen remedy.
  </acceptance_criteria>
  <done>Zero `aria-prohibited-attr` objects on `/forge` in all four themes, both floor sites remedied per what axe actually objects to, and the `aria-selected` hypothesis resolved by measurement either way.</done>
</task>

<task type="auto">
  <name>Task 2: Adopt PageHeader on /forge and remove its ratchet exemption</name>
  <files>src/pages/ForgePage.tsx, src/tokenSweep.ratchet.test.ts</files>
  <read_first>
    - src/pages/ForgePage.tsx (:140-170 — the hand-rolled header and its `space-y-4` parent)
    - src/components/PageHeader.tsx (the whole component — note `cn()`/twMerge on the `className` prop)
    - src/pages/LiveRun.tsx (:205-215 — the `className="mb-0"` + `actions` idiom this conversion copies)
    - src/tokenSweep.ratchet.test.ts (:190-230 — the `KNOWN_EXEMPT` map, the `ForgePage.tsx` entry at :209-221, and `isExempt` at :224)
    - .planning/phases/123-accessibility-remediation/123-CONTEXT.md § D-09 (including the non-negotiable rider)
    - .planning/phases/122-tokens-primitives-contrast-measurement/122-RATCHET-EXEMPTIONS.md ("a record, not a blessing")
  </read_first>
  <action>
    In `src/pages/ForgePage.tsx`, replace the hand-rolled header `<div>` at `:150-161` with
    `<PageHeader title="Forge" className="mb-0 shrink-0" actions={...} />`, passing the existing
    mobile-only `<button>` (with its `aria-label="Show job list"`, its `md:hidden` class list and
    its `<PanelLeft />` child) verbatim as the `actions` prop. Import `PageHeader` from
    `@/components/PageHeader`.

    `className="mb-0"` is load-bearing and resolves D-09's rider: `PageHeader` bakes in `mb-4`,
    `ForgePage`'s current header carries none, and the 4-unit gap above the master-detail
    `GlassPanel` already comes from the parent's `space-y-4` (`:145`). Because `PageHeader` merges
    via `cn()`/twMerge, the caller's `mb-0` wins over the baked-in `mb-4` — `LiveRun.tsx:209` ships
    exactly this. Keep `shrink-0`, which the old wrapper carried and the flex column needs.

    In `src/tokenSweep.ratchet.test.ts`, DELETE the entire `"src/pages/ForgePage.tsx": [ ... ]`
    entry from `KNOWN_EXEMPT` (`:209-221`). This is non-negotiable and must land in the same change
    as the conversion: leaving it makes the ratchet permanently blind to a future `pageheader`
    regression on the one file it was written about. Leave the `src/pages/Chat.tsx` entry in place —
    that one is a genuine design exemption, not a deferral.
  </action>
  <verify>
    <automated>npx tsc --noEmit &amp;&amp; npm test -- src/tokenSweep.ratchet.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -Fc "PageHeader" src/pages/ForgePage.tsx` returns at least 2 (the import and the call).
    - `grep -c '<h1' src/pages/ForgePage.tsx` returns **0** — the hand-rolled heading is gone, and
      the `<h1>Forge</h1>` now comes from `PageHeader`.
    - `grep -Fc 'aria-label="Show job list"' src/pages/ForgePage.tsx` returns **1** — the mobile
      toggle survived the move into `actions`.
    - `grep -Fc "src/pages/ForgePage.tsx" src/tokenSweep.ratchet.test.ts` returns **0**.
      Control that the probe discriminates: `grep -Fc "src/pages/Chat.tsx" src/tokenSweep.ratchet.test.ts`
      still returns at least 1 — the genuine exemption is untouched.
    - `npm test -- src/tokenSweep.ratchet.test.ts` passes. This is the real proof of the conversion:
      with the exemption removed, the `pageheader` bucket now scans `ForgePage.tsx` and the test
      only passes if the file actually adopted the component.
    - Mutation proof, run once by hand and recorded in the SUMMARY: revert the `ForgePage.tsx`
      conversion while leaving the exemption deleted, and confirm
      `npm test -- src/tokenSweep.ratchet.test.ts` FAILS naming `ForgePage.tsx`. Re-apply the
      conversion and confirm it passes. Without this, a ratchet that no longer checks the file is
      indistinguishable from one that checks and passes it.
    - `npx tsc --noEmit` exits 0.
    - Spacing regression is NOT claimed as verified here — D-09's rider assigns the `/forge` header
      spacing check to D-18's operator checkpoint in plan 123-12 by name. Record in the SUMMARY that
      it is pending, not passed.
  </acceptance_criteria>
  <done>`/forge` renders its header through `PageHeader` with the mobile toggle intact and the gap above the body unchanged, and the ratchet now guards that file instead of exempting it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DOM → assistive technology | ARIA attributes are the contract between the app and a screen reader. A prohibited attribute is a broken contract, not an attacker path. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-123-15 | Spoofing | accessible name of the Forge loading region | mitigate | The remedy is chosen from what axe's captured `message` says is prohibited, not from a spec reading, and the post-fix count is paired with a must-differ control (`color-contrast` still reported) so an all-zero probe fails. |
| T-123-16 | Tampering | `KNOWN_EXEMPT` as a guard | mitigate | The exemption is deleted in the same change as the conversion, and a mutation proof shows the ratchet fails when the conversion is absent — closing the "guard that no longer checks anything" failure mode this repo has hit before. |
| T-123-17 | Denial of service | mobile job-list toggle | mitigate | The `actions` slot must retain the button with its exact `aria-label`; asserted by a fixed-string grep. Losing it would strip the only way to open the job list on mobile. |
| T-123-SC | Tampering | npm/pip/cargo installs | mitigate | Not applicable — zero packages added; no install task exists in this plan. `123-RESEARCH.md` § Package Legitimacy Audit records the same. |

**Concurrence with `123-RESEARCH.md` § Security Domain:** confirmed after reading the four files
under change. This plan edits ARIA attributes and swaps a presentational header component; it
touches no authentication, authorization, input-validation or cryptography surface, adds no route,
and adds no dependency. `ForgePage.tsx` does render a `ClerkAuthProbe`, but this plan does not touch
that line or the `CLERK_KEY` guard around it.
</threat_model>

<verification>
- 4 Forge capture JSONs report 0 `aria-prohibited-attr` objects, with `color-contrast` still reported as a control.
- `npm test -- src/tokenSweep.ratchet.test.ts` passes with the `ForgePage.tsx` exemption deleted, and fails when the conversion is reverted.
- `npx tsc --noEmit` exits 0.
- `/forge` has no `<h1>` of its own and one `PageHeader` call carrying the mobile toggle.
</verification>

<success_criteria>
- Zero `aria-prohibited-attr` violations on `/forge` in all four themes.
- The `aria-selected` hypothesis is resolved by measurement and the outcome recorded either way.
- `/forge` uses `PageHeader`; the ratchet guards the file instead of exempting it; the gap above the
  master-detail body is unchanged by construction (`mb-0` beats `mb-4` via twMerge) and is queued
  for D-18's visual confirmation rather than claimed as verified here.
</success_criteria>

<output>
Create `.planning/phases/123-accessibility-remediation/123-06-SUMMARY.md` when done. Record: the
capture `message` string that chose the ARIA remedy; whether the two floor sites needed the same
remedy or different ones; the `aria-selected` outcome; the ratchet mutation-proof result; and an
explicit note that `/forge` header spacing is PENDING D-18, not verified.
</output>
