---
phase: 123-accessibility-remediation
plan: 11
type: execute
wave: 4
depends_on: ["123-05", "123-08", "123-09"]
files_modified:
  - src/components/chat/VitalsRail.tsx
  - src/components/doccomments/DocViewer.tsx
  - src/components/graph/CodeVaultGraph.tsx
  - src/components/hr/AgentCard.tsx
  - src/components/hr/AgentDetailSheet.tsx
  - src/components/hr/CatalogCard.tsx
  - src/components/hr/TeamCard.tsx
  - src/components/hr/TeamEditor.tsx
  - src/components/hr/WizardShell.tsx
  - src/components/hr/WizardStepper.tsx
  - src/components/hr/detail/DetailConfigTab.tsx
  - src/components/hr/steps/ToolsStep.tsx
  - src/components/kg/KGDetailsPanel.tsx
  - src/components/kg/KGDiffControls.tsx
  - src/components/kg/KGSearchResults.tsx
  - src/components/kg/KGViewsPopover.tsx
  - src/components/reminders/CalendarOverlay.tsx
  - src/components/reminders/ReminderList.tsx
  - src/components/skills/AllSkillsOverview.tsx
  - src/components/skills/CategoryGrid.tsx
  - src/components/skills/ColdStorageView.tsx
  - src/components/skills/IntakeSheet.tsx
  - src/components/skills/IntakeStrip.tsx
  - src/components/skills/NewSkillsBanner.tsx
  - src/components/skills/ScopeRail.tsx
  - src/components/skills/SkillCommandDeck.tsx
  - src/components/skills/SkillEditPopover.tsx
  - src/components/skills/SkillFilterChips.tsx
  - src/components/skills/SkillRow.tsx
  - src/components/skills/vault/SkillKanbanView.tsx
  - src/components/skills/vault/SkillPackView.tsx
  - src/components/skills/vault/SkillRecencyView.tsx
  - src/components/skills/vault/SkillVaultView.tsx
  - src/components/workspace/AstridrLensEmptyState.tsx
  - src/pages/Chat.tsx
  - src/pages/KnowledgeGraph.tsx
  - src/pages/McpInventory.tsx
  - src/pages/Skills.tsx
  - src/pages/ToolGalaxy.tsx
  - src/pages/hr/Catalog.tsx
  - src/pages/hr/Roster.tsx
  - src/pages/hr/Teams.tsx
autonomous: true
requirements: [A11Y-02]

must_haves:
  truths:
    - "D-01: the opacity-modifier sweep is ratio-gated — every occurrence in this bucket is classified against a measured ratio and only those below threshold are changed, because blanket-banning the idiom was explicitly rejected"
    - "D-04: the remedy is deleting the /NN; where deleting collapses two adjacent elements to the same level the fix steps to a quieter token, and alpha is never reintroduced"
  artifacts:
    - path: ".planning/phases/123-accessibility-remediation/123-SWEEP-B-LEDGER.md"
      provides: "per-occurrence disposition for this bucket: changed with its measured ratio, or left alone with its measured ratio"
  key_links:
    - from: "this bucket's 42 files"
      to: ".planning/phases/123-accessibility-remediation/123-CONTRAST-RESULT.md"
      via: "section 3's file-and-line remediation list is the authority for what changes"
      pattern: "123-CONTRAST-RESULT"
---

<objective>
Execute the ratio-gated sweep over bucket B: **42 files, 113 occurrences** of
`text-primary/NN` / `text-muted-foreground/NN` / `text-(--token)/NN` in `src/components/*/` and
`src/pages/`. (One of those 113, `CodeVaultGraph.tsx:892`, is already removed by plan 123-05, so the
live count at execution time is 112 — re-derive it rather than assuming either figure.)

This is the class fix D-01 exists for: fixing only the ~5 sites axe originally flagged on 5 routes
was rejected precisely because ~170 siblings of the identical defect live on the 42 routes nobody
had scanned. Plan 123-08 has now scanned them and produced the verdicts.

Purpose: D-01 and D-04, bucket B.
Output: edits per the ledger, plus `123-SWEEP-B-LEDGER.md` giving every occurrence a disposition.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/123-accessibility-remediation/123-CONTEXT.md
@.planning/phases/123-accessibility-remediation/123-PATTERNS.md
@.planning/phases/123-accessibility-remediation/123-CONTRAST-RESULT.md
@.planning/phases/123-accessibility-remediation/123-CRITERION-DECISION.md
@src/layouts/DashboardLayout.tsx
@CLAUDE.md

<interfaces>
<!-- The remedy pattern, from the already-executed highest-leverage member of this same bucket
     (src/layouts/DashboardLayout.tsx, plan 123-04). Before:
       <p className="text-xs uppercase tracking-widest text-primary/60 font-mono font-bold ...">
     After:
       <p className="text-xs uppercase tracking-widest text-primary font-mono font-bold ...">
     Tailwind v4 compiles `/NN` on a var()-backed token into
       .text-primary\/60 { color: var(--primary) }
       @supports (color: color-mix(...)) { .text-primary\/60 { color: color-mix(in oklab, var(--primary) 60%, transparent) } }
     so deleting the modifier removes only the @supports override and falls back to a rule that is
     ALREADY compiled immediately above it. There is no case where the base rule is absent. -->

<!-- OUT OF FAMILY on the same elements — do NOT sweep as a side effect. The three sweep families
     match the `text-` prefix only. `bg-*/NN`, `border-*/NN`, `from-*/NN`, `to-*/NN` and
     drop-shadow oklch alpha are a different, unmeasured population. -->

<!-- Bucket B occurrence counts per file, re-derived live 2026-08-20 with
     `grep -ohE '<pattern>' <file> | wc -l` (occurrences; `grep -c` would count matching LINES and
     emit a path:0 row per file scanned). Top of the distribution:
       15  src/components/hr/detail/DetailConfigTab.tsx
        9  src/pages/KnowledgeGraph.tsx
        6  src/pages/ToolGalaxy.tsx
        6  src/components/kg/KGSearchResults.tsx
        6  src/components/graph/CodeVaultGraph.tsx   (5 after plan 123-05)
        5  src/components/chat/VitalsRail.tsx
     The remaining ~36 files hold 1-4 each. -->

<!-- The two `text-(--token)/NN` occurrences in the whole corpus both live in this bucket:
       src/components/chat/VitalsRail.tsx:205   text-(--status-error)/60
       src/components/SwarmTaskNode.tsx:107     text-(--status-warn)/80   <- bucket A, plan 123-12
     VitalsRail's is on a ChevronRight ICON, not text. axe's color-contrast rule does not apply to
     SVG icons, so its disposition comes from pass 2, not pass 1. -->

<!-- src/pages/Chat.tsx is in this bucket (2 occurrences). /chat is out of scope for the whole v15.0
     milestone as a SURFACE, but the class edit is mechanical and the file is part of the corpus.
     Apply the ledger's verdict for it like any other file; do not redesign anything on that page. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-derive this bucket's live population and reconcile it against the ledger</name>
  <files>.planning/phases/123-accessibility-remediation/123-SWEEP-B-LEDGER.md</files>
  <read_first>
    - .planning/phases/123-accessibility-remediation/123-CONTRAST-RESULT.md § 2 (the class-level isolation table) and § 3 (the file-and-line remediation list — this is the authority for what changes)
    - .planning/phases/123-accessibility-remediation/123-CRITERION-DECISION.md (whether the operator widened A11Y-02's criterion; if `widen`, this bucket's scope grew and the decision document names by how much)
    - .planning/phases/123-accessibility-remediation/123-CONTEXT.md § D-01, § D-04
  </read_first>
  <action>
    Re-derive the live population for THIS bucket before touching anything. Do not carry 113
    forward — plan 123-05 already removed one occurrence from `CodeVaultGraph.tsx`, and concurrent
    sessions edit this repo. Use, and record, exactly these commands with their units:
      occurrences: `grep -ohE 'text-primary/[0-9]+|text-muted-foreground/[0-9]+|text-\(--[a-zA-Z-]+\)/[0-9]+' <files> | wc -l`
      files:       `grep -olE '...' <files> | wc -l`
    Never `grep -c` for either — it counts matching LINES per file and emits a `path:0` row for
    every file scanned, so it can produce neither figure. Build the file list with `-print0` /
    `IFS= read -r -d ''` or an explicit list; a `for f in $(...)` loop word-splits on paths and
    silently drops entries.

    Control that the probe discriminates, run in the same breath: the command must return **0** for
    `src/layouts/DashboardLayout.tsx` (cleaned by plan 123-04) and **non-zero** for
    `src/components/hr/detail/DetailConfigTab.tsx` (this bucket's largest member). One known-absent
    and one known-present; a probe that cannot show both is not evidence.

    Reconcile against `123-CONTRAST-RESULT.md` § 3. Every occurrence in this bucket must appear in
    exactly one of three dispositions, and the three must sum to the live total with the arithmetic
    shown:
      - **change** — measured below threshold (pass: axe or pass: isolation); delete the `/NN`.
      - **leave** — measured at or above threshold; record the ratio and the pass label. D-01
        explicitly rejected banning the idiom, so a passing `/NN` stays.
      - **unreconciled** — present in the corpus but absent from the ledger. Any non-zero count here
        is a defect in the ledger, not a licence to guess: stop, report it, and get it measured.
        An occurrence edited without a measurement is the calculated-not-measured shape D-02 rejects
        just as much as an unmeasured occurrence left in place.

    Write `123-SWEEP-B-LEDGER.md` with the three lists, file and line for each, the measured ratio
    and pass label on every row, and the arithmetic shown balancing.
  </action>
  <verify>
    <automated>test -f .planning/phases/123-accessibility-remediation/123-SWEEP-B-LEDGER.md</automated>
  </verify>
  <acceptance_criteria>
    - The document states the live occurrence and file counts for this bucket with the exact
      commands that produced them and the unit named on each.
    - The discrimination control is recorded: 0 for `DashboardLayout.tsx`, non-zero for
      `DetailConfigTab.tsx`.
    - change + leave + unreconciled = the live occurrence total, arithmetic shown.
    - `unreconciled` is **0**. If it is not, the plan stops here and reports rather than proceeding.
    - Every row in `leave` carries a measured ratio and a pass label; none says "assumed" or
      "presumed passing".
  </acceptance_criteria>
  <done>Every occurrence in bucket B has a measured disposition, reconciled against a live re-derived population, with nothing unaccounted for.</done>
</task>

<task type="auto">
  <name>Task 2: Apply the change list across bucket B</name>
  <files>src/components/chat/VitalsRail.tsx, src/components/doccomments/DocViewer.tsx, src/components/graph/CodeVaultGraph.tsx, src/components/hr/AgentCard.tsx, src/components/hr/AgentDetailSheet.tsx, src/components/hr/CatalogCard.tsx, src/components/hr/TeamCard.tsx, src/components/hr/TeamEditor.tsx, src/components/hr/WizardShell.tsx, src/components/hr/WizardStepper.tsx, src/components/hr/detail/DetailConfigTab.tsx, src/components/hr/steps/ToolsStep.tsx, src/components/kg/KGDetailsPanel.tsx, src/components/kg/KGDiffControls.tsx, src/components/kg/KGSearchResults.tsx, src/components/kg/KGViewsPopover.tsx, src/components/reminders/CalendarOverlay.tsx, src/components/reminders/ReminderList.tsx, src/components/skills/AllSkillsOverview.tsx, src/components/skills/CategoryGrid.tsx, src/components/skills/ColdStorageView.tsx, src/components/skills/IntakeSheet.tsx, src/components/skills/IntakeStrip.tsx, src/components/skills/NewSkillsBanner.tsx, src/components/skills/ScopeRail.tsx, src/components/skills/SkillCommandDeck.tsx, src/components/skills/SkillEditPopover.tsx, src/components/skills/SkillFilterChips.tsx, src/components/skills/SkillRow.tsx, src/components/skills/vault/SkillKanbanView.tsx, src/components/skills/vault/SkillPackView.tsx, src/components/skills/vault/SkillRecencyView.tsx, src/components/skills/vault/SkillVaultView.tsx, src/components/workspace/AstridrLensEmptyState.tsx, src/pages/Chat.tsx, src/pages/KnowledgeGraph.tsx, src/pages/McpInventory.tsx, src/pages/Skills.tsx, src/pages/ToolGalaxy.tsx, src/pages/hr/Catalog.tsx, src/pages/hr/Roster.tsx, src/pages/hr/Teams.tsx</files>
  <read_first>
    - .planning/phases/123-accessibility-remediation/123-SWEEP-B-LEDGER.md (Task 1's change list — the authority for every edit)
    - src/layouts/DashboardLayout.tsx (plan 123-04's already-executed edits — the in-repo remedy pattern for this exact bucket)
    - src/components/hr/detail/DetailConfigTab.tsx (15 occurrences — read this one in full before editing; it is the densest file and most likely to hold adjacent elements whose hierarchy the deletion could collapse)
    - src/components/chat/VitalsRail.tsx (:200-210 — the `text-(--status-error)/60` on a ChevronRight icon, one of only two `text-(--token)/NN` occurrences in the whole corpus)
    - .planning/phases/123-accessibility-remediation/123-CONTEXT.md § D-04
    - CLAUDE.md § Styling
  </read_first>
  <action>
    Apply exactly the `change` rows from Task 1's ledger — no more, no fewer. The default remedy is
    deleting the `/NN` suffix, leaving the bare token class. Do not substitute a different alpha and
    do not introduce a hex or a palette class.

    Per-site judgement, applied only where the ledger flags adjacency risk or where reading the file
    shows it: if deleting a `/NN` would make two adjacent elements read as the same level (a label
    and its value, a heading and its subtitle, an enabled and a disabled row), step the quieter one
    to the next quieter TOKEN — never back to alpha. `DashboardLayout.tsx`'s disabled-nav decision
    in plan 123-04's summary is the worked precedent; follow whatever it settled on. Record every
    such token substitution individually in the SUMMARY with the two elements it separates.

    Do NOT touch `bg-*/NN`, `border-*/NN`, `from-*/NN`, `to-*/NN` or any `drop-shadow-[...oklch(...)]`
    alpha on the same lines. They are a different, unmeasured population and sweeping them as a side
    effect would put unmeasured changes in a phase whose whole claim is that every change was
    measured.

    Work file by file rather than with a single repo-wide substitution: the change list is
    per-occurrence, not per-class, and some occurrences of the same class string are `leave` rows.
    A global `sed` over the class string would silently convert `leave` rows too.
  </action>
  <verify>
    <automated>npx tsc --noEmit &amp;&amp; npm test</automated>
  </verify>
  <acceptance_criteria>
    - The post-edit occurrence count for this bucket equals `leave`-row count from Task 1's ledger,
      exactly. Re-derive with `grep -ohE ... | wc -l` over the bucket's file list and show the
      arithmetic: `live_before - changed = live_after = leave_count`.
    - The `bg-*/NN` + `border-*/NN` occurrence count across the same 42 files is **unchanged**.
      Record the before and after figures; they must be identical.
    - `grep -roE "#[0-9a-fA-F]{3,8}" <the 42 files> | wc -l` is unchanged — no hex introduced.
    - `grep -rohE 'text-(primary|muted-foreground)/[0-9]+' <the 42 files> | sort -u` contains only
      class strings that appear as `leave` rows in the ledger — no surviving occurrence is
      unexplained.
    - `npx tsc --noEmit` exits 0.
    - `npm test` passes with no NEW failures. Record the pass/fail counts before and after; this
      repo has a known ~17% flake in `KnowledgeGraph.test.tsx` (tracked separately as
      `kg-answer-sync-glxy02-test-flake.md`), so a failure there is only acceptable if it
      reproduces on unmodified source — prove it by stashing and re-running, do not assume it.
  </acceptance_criteria>
  <done>Bucket B carries only measured-passing opacity modifiers, with every deletion traceable to a measured ratio and every survivor traceable to a `leave` row.</done>
</task>

<task type="auto">
  <name>Task 3: Re-measure and confirm no regression</name>
  <files>.planning/phases/123-accessibility-remediation/123-SWEEP-B-LEDGER.md</files>
  <read_first>
    - .planning/phases/123-accessibility-remediation/123-SWEEP-B-LEDGER.md (Task 1's lists — the expected post-state)
    - e2e/theme-contrast.spec.ts (the `A11Y_SCAN_ALL` and `A11Y_MEASURE_ONLY` switches)
    - .planning/phases/123-accessibility-remediation/123-CRITERION-DECISION.md (whether the criterion is 20 cells or 188)
  </read_first>
  <action>
    Start `dev:noauth` on 5181 from Git Bash (`VITE_CLERK_PUBLISHABLE_KEY= npm run dev:noauth` —
    PowerShell's empty-string assignment DELETES the variable and would leave the Clerk gate up).
    Re-run the widened scan into a fresh capture directory
    `.planning/phases/123-accessibility-remediation/a11y-sweep-b/`, invoking
    `node_modules/.bin/playwright` explicitly.

    Compare against `123-CONTRAST-RESULT.md`'s pre-sweep figures and append a "post-sweep" section
    to `123-SWEEP-B-LEDGER.md` with: per-rule object and node counts across the criterion cells and
    across all 188 cells, both with units; the delta from the pre-sweep figures; and any route whose
    violation count went UP. A route that got worse is a real finding — D-15's residual rule says an
    intermittently-failing cell is never written off as noise, and the same discipline applies to a
    regression.
  </action>
  <verify>
    <automated>ls .planning/phases/123-accessibility-remediation/a11y-sweep-b/*.json | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - The criterion cells (20, or 188 if the operator chose `widen`) aggregate to **0**
      `color-contrast` nodes and **0** `aria-prohibited-attr` objects.
    - Must-differ control: if the criterion is still 20 cells, the full-188 aggregate is reported
      separately and its value stated. If BOTH are 0 and the pre-sweep 188 figure was non-zero,
      confirm the scan actually ran by checking the capture file count is 188 before recording the
      result — an empty scan and a clean scan both report 0.
    - No route's violation node count increased relative to `123-CONTRAST-RESULT.md`. Any increase
      is reported by name in the SUMMARY, not absorbed.
    - Derive all counts by parsing `violations[].nodes.length`, never by grepping `"id"` — `"id"`
      also appears on every per-node check object.
  </acceptance_criteria>
  <done>The bucket-B sweep is measured, not asserted, and any regression is named rather than averaged away.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none introduced | 42 presentational components change CSS utility class strings. No data, auth or route behaviour changes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-123-30 | Tampering | scope of the sweep | mitigate | Edits come from a per-occurrence change list, applied file by file rather than by a repo-wide substitution, so `leave` rows of the same class string are not silently converted. The `bg-*/NN` and `border-*/NN` counts are asserted unchanged, bounding collateral edits. |
| T-123-31 | Spoofing | "the sweep is complete" | mitigate | The post-edit occurrence count must equal the ledger's `leave` count exactly, with the arithmetic shown, and the population probe must discriminate against a known-absent and a known-present file. |
| T-123-32 | Denial of service | visual affordances | accept | Deleting an opacity modifier can flatten a deliberate de-emphasis. Mitigated in-task by the token-step rule and caught downstream by D-18's operator visual checkpoint in plan 123-13; residual risk is cosmetic. |
| T-123-SC | Tampering | npm/pip/cargo installs | mitigate | Not applicable — zero packages added; no install task exists in this plan. `123-RESEARCH.md` § Package Legitimacy Audit records the same. |

**Concurrence with `123-RESEARCH.md` § Security Domain:** confirmed. This plan removes CSS opacity
modifiers from `className` strings in presentational components and pages. It touches no
authentication, authorization, input-validation or cryptography surface, adds no route and adds no
dependency.
</threat_model>

<verification>
- change + leave + unreconciled reconciles to a live re-derived population, with `unreconciled` = 0.
- Post-edit occurrence count equals the `leave` count exactly.
- `bg-*/NN` + `border-*/NN` counts across the 42 files are unchanged; no hex introduced.
- `npx tsc --noEmit` exits 0; `npm test` has no new failures.
- The re-measured criterion cells aggregate to 0 contrast nodes and 0 ARIA objects.
</verification>

<success_criteria>
- Every occurrence in bucket B is either changed against a measured sub-threshold ratio or left
  alone against a measured passing ratio — nothing edited on assumption, nothing skipped in silence.
- No collateral change to the `bg-`/`border-` families or to any token value.
- The re-measured matrix is clean on the criterion cells with no route regressing.
</success_criteria>

<output>
Create `.planning/phases/123-accessibility-remediation/123-11-SUMMARY.md` when done. Record: the
live re-derived occurrence and file counts with their commands; the change / leave / unreconciled
arithmetic; every token substitution made instead of a plain deletion, with the two elements it
separates; the before/after `bg-*/NN` + `border-*/NN` counts; and the `npm test` counts before and
after, with the `KnowledgeGraph.test.tsx` flake explicitly adjudicated rather than assumed.
</output>
