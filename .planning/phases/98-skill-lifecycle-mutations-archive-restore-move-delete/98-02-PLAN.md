---
phase: 98-skill-lifecycle-mutations-archive-restore-move-delete
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - "C:/Users/mandr/forge/src/process/lifecycle-exec.ts"
  - "C:/Users/mandr/forge/src/process/lifecycle-exec.test.ts"
  - "C:/Users/mandr/forge/src/emit/command-poller.ts"
  - "C:/Users/mandr/forge/src/index.ts"
autonomous: true
requirements: [LIFE-01, LIFE-02, LIFE-03, LIFE-04, LIFE-05, DAEMON-02]
user_setup: []

must_haves:
  truths:
    - "The daemon executes an archive by moving the host skill dir from active root to ~/.claude/skills-available/, then fires rescanAndSync (LIFE-01/DAEMON-02)"
    - "The daemon executes restore/move/delete as native TS fs ops with no Python shell-out (D-01)"
    - "A cross-volume move (C: ↔ G:\\ project workspace) succeeds via copy+delete, never refuses (Pitfall 3)"
    - "The daemon re-checks collision/shadow against the LIVE filesystem before mutating and refuses with a lifecycle-refused:<kind>: string (D-04 layer 2)"
    - "A permanent delete verifies the skill exists ONLY at the cold root before rmSync (D-05 host-truth backstop)"
    - "A successful mutation triggers rescanAndSync; a failed one does not (DAEMON-02)"
    - "Two lifecycle commands on the same skill name in one poll cycle cannot interleave (per-name in-flight mutex, Pitfall 4)"
    - "rescan/move path resolution reads the workspace list FRESH per call, not a startup snapshot (Pitfall 5)"
  artifacts:
    - path: "C:/Users/mandr/forge/src/process/lifecycle-exec.ts"
      provides: "native-TS lifecycle fs executor with path resolution, live re-check, cross-volume move, delete"
      min_lines: 80
    - path: "C:/Users/mandr/forge/src/emit/command-poller.ts"
      provides: "executeLifecycle branch, lifecycleFn wiring, supportedTypes += lifecycle, per-name mutex"
      contains: "executeLifecycle"
    - path: "C:/Users/mandr/forge/src/index.ts"
      provides: "lifecycleFn wiring, unconditional lifecycle in supportedTypes, fresh-workspace-list fix"
      contains: "lifecycleFn"
  key_links:
    - from: "command-poller.ts executeLifecycle"
      to: "rescanAndSync"
      via: "fire-and-forget on status==='done'"
      pattern: "rescanAndSync"
    - from: "lifecycle-exec.ts move path"
      to: "getWorkspace(db, workspaceId)"
      via: "project-scope path resolution"
      pattern: "getWorkspace"
---

<objective>
Build the Local-Daemon tier of the phase (per RESEARCH Architectural Responsibility Map): a new native-TS `lifecycle-exec.ts` module in the Forge daemon repo that resolves source/dest paths for the four skill roots, performs the LAYER-2 (host-truth) collision/shadow/cold-only re-check, and executes archive/restore/move/delete as plain fs operations — including the cross-volume copy+delete fallback that is the COMMON case for any project-scope move on this host (global/cold on C:, project workspaces on G:\). Wire it into `CommandPoller` as a parallel `executeLifecycle` branch that fires the already-built `rescanAndSync` on success, and fix the daemon's stale-workspace-startup-snapshot so move-to-project resolves current workspace paths.

Purpose: The browser/Convex app cannot touch the filesystem (Surface-Substrate bridge). Every host mutation flows through `forgeCommands` and executes here. This plan delivers DAEMON-02 and the host half of LIFE-01..05.
Output: One new module + two focused modifications, all test-covered including a deterministic cross-volume-fallback test via an injected fake renameFn.

REPO NOTE: All files in this plan live in the Forge daemon repo at `C:\Users\mandr\forge` (NOT codepulse, NOT astridr-repo — the ROADMAP/context "astridr-repo" note is stale; skill-rescan.ts and promote.ts are confirmed present under C:\Users\mandr\forge this session). Run all commands with cwd `C:/Users/mandr/forge`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/98-skill-lifecycle-mutations-archive-restore-move-delete/98-CONTEXT.md
@.planning/phases/98-skill-lifecycle-mutations-archive-restore-move-delete/98-RESEARCH.md
@.planning/phases/98-skill-lifecycle-mutations-archive-restore-move-delete/98-PATTERNS.md

<interfaces>
<!-- Contracts extracted from the live forge repo this session. Executor implements against these; no codebase scavenger-hunt needed. -->

Skill roots on this host:
  global : os.homedir() + "/.claude/skills/<name>"
  cold   : os.homedir() + "/.claude/skills-available/<name>"   (DORMANT)
  project: getWorkspace(db, workspaceId).rootPath + "/.claude/skills/<name>"  (on G:\My Drive\forge-workspaces\... — cross-volume vs C:)

promote.ts (forge/src/workspace/promote.ts, lines 59-109) — ADAPT, do not call promoteWorkspace():
  CROSS_VOLUME_CODES = new Set(['EXDEV','EIO','EPERM','EACCES','ENOTSUP'])
  copyTreeReadWrite(src,dest): mkdirSync recursive; for each dirent readFileSync→writeFileSync (NOT cpSync/copyFileSync — Google Drive virtual FS rejects CopyFileEx with EIO/EPERM)
  defaultReparseScanner (lines ~133-163, lstatSync-based) — reusable pre-move symlink/junction safety check
  promoteWorkspace() THROWS ForgePromotionError on cross-volume — lifecycle needs the OPPOSITE (copy+delete, never throw)

command-poller.ts (forge/src/emit/command-poller.ts):
  ForgeCommand interface (~63-73): commandType 'launch'|'stop'|'intake' (ADD 'lifecycle'); add `lifecyclePayload: LifecyclePayload | null`
  poll() dispatch (~216-222): `if (cmd.commandType==='intake') this.enqueueIntake(cmd); else void this.execute(cmd);`  — lifecycle joins the PARALLEL else branch, NOT the serial intake queue
  executeIntake() (~316-361) — the shape to copy: mutable status/error locals, try/catch, `if (status==='done' && this.rescanCfg) void rescanAndSync(this.rescanCfg);`, fire-and-forget ack POST to this.ackUrl with Bearer this.apiKey
  PollCfg + constructor (~84-173): add optional `lifecycleFn` mirroring intakeFn's "absent = host never claims this type" convention

index.ts (forge/src/index.ts, ~140-217):
  rescanCfg.workspaces = listWorkspaces(db)  // ← STARTUP SNAPSHOT, the Pitfall-5 bug to fix
  supportedTypes: intakeAvailable ? ['launch','stop','intake'] : ['launch','stop']  // ADD 'lifecycle' unconditionally (no capability probe needed — native fs, RESEARCH A2)

workspaces store (forge/src/store/workspaces.ts): getWorkspace(db,id): Workspace|null ; listWorkspaces(db): Workspace[]

Error-string convention: mint `lifecycle-refused:collision:<raw>` and `lifecycle-refused:shadow:<raw>` (Plan 98-01's ackCommand adapter parses these). NEVER reuse intake's `write-refused:` (Pitfall 6).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: lifecycle-exec.ts — native-TS fs executor with live re-check + cross-volume move</name>
  <read_first>
    - C:/Users/mandr/forge/src/workspace/promote.ts (CROSS_VOLUME_CODES, copyTreeReadWrite, defaultReparseScanner, promoteWorkspace's throw-on-EXDEV to AVOID)
    - C:/Users/mandr/forge/src/process/intake-exec.ts (never-throw discriminated-result discipline; REFUSAL_KIND error-string shape ~107-119)
    - C:/Users/mandr/forge/src/store/workspaces.ts (getWorkspace/listWorkspaces)
    - C:/Users/mandr/forge/src/emit/skill-rescan.ts (root paths buildSkillSnapshot walks — mirror the exact global/cold/project path construction)
    - .planning/phases/98-.../98-PATTERNS.md (lifecycle-exec section ~33-117)
    - .planning/phases/98-.../98-RESEARCH.md (Open Question 2, Pitfall 3, Security Domain)
  </read_first>
  <behavior>
    - resolvePath("global","legal") === homedir + "/.claude/skills/legal"
    - resolvePath("cold","legal") === homedir + "/.claude/skills-available/legal"
    - resolvePath("project","legal", ws) uses getWorkspace(db,workspaceId).rootPath + "/.claude/skills/legal"
    - archive when a cold copy already exists returns { status:'failed', error:'lifecycle-refused:collision:...' } and moves nothing
    - restore when an active copy already exists at the target scope returns { status:'failed', error:'lifecycle-refused:shadow:...' } and moves nothing
    - delete when the skill exists at any non-cold root returns { status:'failed', error:'lifecycle-refused:collision:...' } (cold-only host-truth guard) — never rmSync
    - move where renameSync throws EXDEV falls back to copyTreeReadWrite + rmSync(src) and returns { status:'done' } (injected fake renameFn throwing EXDEV proves the fallback deterministically)
    - a skillName failing the bare-segment guard returns failed without touching fs
    - a source dir containing a reparse point (symlink/junction) is refused before move
    - ENOENT on a since-vanished source maps to a clear "already moved/deleted" failed result, never a thrown crash
  </behavior>
  <action>
    Create C:/Users/mandr/forge/src/process/lifecycle-exec.ts exporting a `runLifecycle(deps, payload)`-style function returning a discriminated `{ status: 'done' | 'failed'; error?: string }` result and NEVER throwing past its boundary (mirror intake-exec.ts's whole-file discipline so the poller's try/catch stays a backstop). Include: a local `isSafeSkillName` bare-segment guard (no separators/`..`/drive-letter — same rule as Plan 98-01, defense-in-depth); `resolvePath(destinationOrOrigin, skillName, workspace?)` building absolute paths from fixed root constants (homedir + fixed subpaths; project via getWorkspace(db,workspaceId).rootPath) — never accept a client absolute path; a standalone `existsAtRoot(root, skillName)` = `fs.existsSync(path.join(root, skillName))` used for the LAYER-2 re-check (RESEARCH Open Question 2 standalone approach); the four action handlers:
      - archive: source = active root (global or the sourceOrigin's project root), dest = cold root; refuse `lifecycle-refused:collision:` if dest exists; move.
      - restore: source = cold root, dest = active target (global or project); refuse `lifecycle-refused:shadow:` if an active copy exists at dest; move.
      - move: source = current active root (from sourceOrigin), dest = the other active scope; refuse `lifecycle-refused:collision:` if dest exists; move.
      - delete: verify the skill exists ONLY at the cold root (refuse `lifecycle-refused:collision:` if present anywhere active — never trust the Convex cached origin, Security V12/T-98-02); then `fs.rmSync(coldPath, {recursive:true, force:true})`.
    The move primitive: adapt promote.ts — try `fs.renameSync(src,dest)` first; on a code in CROSS_VOLUME_CODES fall back to `copyTreeReadWrite(src,dest)` + `fs.rmSync(src,{recursive:true,force:true})`; NEVER throw-and-refuse on those codes (opposite of promoteWorkspace). Inject the rename function and existence checks as deps so the test can force the EXDEV branch. Run a reparse-point scan (defaultReparseScanner pattern) on the source tree before any move and refuse if one is found. Optionally attempt the icacls best-effort grant on the destination after a move (memory forge-codex-sandbox-acl-blocks-reads) so the rescan can read back what it wrote — wrap in try/catch, never fatal.

    Create C:/Users/mandr/forge/src/process/lifecycle-exec.test.ts (Wave 0 — file does not exist) with the behavior cases above, using injected fake fs seams (fake renameFn throwing EXDEV, fake existsAtRoot) so cross-volume + collision + shadow + cold-only are all deterministic without a live Drive mount.
  </action>
  <verify>
    <automated>cd C:/Users/mandr/forge && npx vitest run src/process/lifecycle-exec.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - C:/Users/mandr/forge/src/process/lifecycle-exec.ts exists and exports the lifecycle runner returning `{ status, error? }` and never throwing
    - Contains a CROSS_VOLUME_CODES-based rename→copy+delete fallback (grep: `EXDEV` and `copyTreeReadWrite` usage)
    - Contains `fs.rmSync` only on the cold-verified delete path
    - Emits `lifecycle-refused:collision:` and `lifecycle-refused:shadow:` (never `write-refused:`)
    - The injected-EXDEV fallback test and the cold-only delete-guard test both pass
    - `npx vitest run src/process/lifecycle-exec.test.ts` exits 0
  </acceptance_criteria>
  <done>The daemon can perform all four lifecycle mutations natively, cross-volume-safely, with a host-truth re-check and a never-throw result contract, all deterministically tested.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: command-poller executeLifecycle branch + per-name mutex + supportedTypes</name>
  <read_first>
    - C:/Users/mandr/forge/src/emit/command-poller.ts (ForgeCommand ~63-73, poll() dispatch ~216-222, executeIntake ~316-361, PollCfg/constructor ~84-173, existing intake serial-queue for the mutex-spirit reference)
    - C:/Users/mandr/forge/src/emit/command-poller.test.ts (existing cases — extend with lifecycle dispatch/ack/rescan cases)
    - C:/Users/mandr/forge/src/process/lifecycle-exec.ts (the runner signature from Task 1)
  </read_first>
  <behavior>
    - A claimed commandType "lifecycle" command dispatches via the PARALLEL branch (void this.executeLifecycle), not the serial intake queue
    - executeLifecycle acks status "done" and fires rescanAndSync exactly once on success
    - executeLifecycle acks status "failed" with the runner's error and does NOT fire rescanAndSync
    - a daemon with no lifecycleFn never claims lifecycle (fail-safe absent convention) — or acks failed clearly, matching intakeFn's absent handling
    - two lifecycle commands for the same skillName claimed in one poll cycle do not run concurrently (the second waits/serializes via the per-name in-flight guard)
    - supportedTypes default includes "lifecycle"
  </behavior>
  <action>
    Modify C:/Users/mandr/forge/src/emit/command-poller.ts: extend the `ForgeCommand.commandType` union with `'lifecycle'` and add `lifecyclePayload: LifecyclePayload | null` (define/import the LifecyclePayload type mirroring intakePayload's shape from the schema in Plan 98-01: action/skillName/sourceOrigin/destination/workspaceId). Add a private `executeLifecycle(cmd)` copying executeIntake's exact shape (mutable status/error locals, try/catch, `if (status==='done' && this.rescanCfg) void rescanAndSync(this.rescanCfg);`, fire-and-forget ack POST to this.ackUrl with Bearer auth) but delegating the work to Task 1's runner via an injected `lifecycleFn`. Dispatch it from poll()'s parallel `else` branch: keep `if (cmd.commandType==='intake') this.enqueueIntake(cmd)` and add a case so lifecycle goes `void this.executeLifecycle(cmd)` (not the serial intake queue — independent fs ops, RESEARCH Pattern 1). Add a lightweight per-skill-name in-flight `Set<string>` mutex (Pitfall 4): before dispatching a lifecycle cmd, if its skillName is in-flight, defer to the next poll cycle; add on start, delete in a finally. Add `lifecycleFn?` to PollCfg and the constructor mirroring `intakeFn`'s absent-is-fail-safe convention. Extend the default `supportedTypes` array to include `'lifecycle'`.

    Extend command-poller.test.ts with the behavior cases (dispatch routing, ack+rescan on done, no-rescan on failed, same-name mutex).
  </action>
  <verify>
    <automated>cd C:/Users/mandr/forge && npx vitest run src/emit/command-poller.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - command-poller.ts contains `executeLifecycle` and dispatches it from the parallel (non-intake) branch
    - executeLifecycle fires `rescanAndSync` only under `status === 'done'`
    - A per-skillName in-flight guard (Set) prevents concurrent same-name execution
    - `ForgeCommand.commandType` union includes `'lifecycle'` and the interface has `lifecyclePayload`
    - default supportedTypes includes `'lifecycle'`
    - `npx vitest run src/emit/command-poller.test.ts` exits 0
  </acceptance_criteria>
  <done>The poller claims, dispatches, executes, acks, and rescans lifecycle commands in parallel, race-guarded per skill name, advertising the capability by default.</done>
</task>

<task type="auto">
  <name>Task 3: index.ts wiring + fresh-workspace-list fix (Pitfall 5)</name>
  <read_first>
    - C:/Users/mandr/forge/src/index.ts (rescanCfg + CommandPoller construction ~140-217, supportedTypes ~211, workspaces snapshot ~154-161)
    - C:/Users/mandr/forge/src/store/workspaces.ts (listWorkspaces/getWorkspace)
    - C:/Users/mandr/forge/src/emit/skill-rescan.ts (RescanAndSyncDeps/BuildSkillSnapshotDeps — confirm they accept a plain workspaces array; the fix is at the call site)
    - .planning/phases/98-.../98-RESEARCH.md (Pitfall 5)
  </read_first>
  <action>
    Modify C:/Users/mandr/forge/src/index.ts: (1) construct and pass a `lifecycleFn` (from Task 1's runner, bound to the daemon's db + homedir + rescanCfg deps) into the CommandPoller — unconditionally, with NO capability probe (native fs has no missing-CLI failure mode, RESEARCH A2). (2) Add `'lifecycle'` to the `supportedTypes` array unconditionally (both the intake-available and intake-unavailable branches, since lifecycle does not depend on the intake CLI). (3) Pitfall-5 fix: stop capturing `listWorkspaces(db)` once at module load. Make the workspace list read FRESH per rescan/execution — either pass a `() => listWorkspaces(db)` thunk into rescanCfg and the lifecycle path resolution, or re-read at each call site — so a workspace synced after daemon start is a valid move-to-project destination and appears in the rescan's project walk without a daemon restart. Keep RescanAndSyncDeps' interface accepting the array; change only the call site.
  </action>
  <verify>
    <automated>cd C:/Users/mandr/forge && npx tsc --noEmit && npx vitest run src/emit/command-poller.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - index.ts constructs and passes `lifecycleFn` into the CommandPoller
    - index.ts supportedTypes includes `'lifecycle'` in every branch
    - The workspace list is read fresh per call (no single module-load `listWorkspaces(db)` snapshot feeding rescan/lifecycle path resolution)
    - `npx tsc --noEmit` exits 0 in the forge repo
  </acceptance_criteria>
  <done>The daemon advertises and wires lifecycle execution, and move-to-project / rescan resolve current workspace paths without a restart.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Convex command row → daemon fs op | A claimed lifecyclePayload drives real renameSync/rmSync on the host filesystem |
| skill directory contents → move engine | A skill dir may contain a symlink/junction reparse point escaping the intended tree |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-98-01 | Tampering | lifecycle-exec path resolution | mitigate | Local bare-segment `isSafeSkillName` guard + paths built only from fixed root constants + getWorkspace(db,id); never a client absolute path (V12) |
| T-98-02 | Elevation/Tampering | delete cold-only | mitigate | Daemon verifies the skill exists ONLY at the cold root (existsAtRoot per active root) before rmSync — host truth, never the cached Convex origin claim |
| T-98-03 | Tampering (integrity) | concurrent same-name commands | mitigate | Per-skillName in-flight Set mutex in the poller + ENOENT-on-vanished-source mapped to a clean failed result, not a crash |
| T-98-04 | Tampering/Info Disclosure | reparse point inside skill dir | mitigate | defaultReparseScanner (lstatSync) pre-move scan refuses a directory containing a symlink/junction before copy/rename |
| T-98-SC | Tampering | package installs | accept | No package installs in the forge repo this plan; no new dependencies. |
</threat_model>

<verification>
- `cd C:/Users/mandr/forge && npx vitest run src/process/lifecycle-exec.test.ts src/emit/command-poller.test.ts` green
- `cd C:/Users/mandr/forge && npx tsc --noEmit` clean
- Full forge suite `npm test` green at wave merge
- MANUAL UAT (phase gate, cannot be proven by same-host unit test): one real archive of a global skill and one real move touching a G:\ project workspace, then confirm the Skills page reflects the new lane after rescan (see 98-VALIDATION Manual-Only)
</verification>

<success_criteria>
- The daemon executes archive/restore/move/delete natively in TS (D-01), cross-volume-safe (Pitfall 3), with a host-truth re-check (D-04 layer 2) and cold-only delete guard (D-05)
- rescanAndSync fires on success only; the Skills page reflects host truth after each mutation (DAEMON-02)
- Concurrent same-name commands cannot interleave; move-to-project resolves fresh workspace paths (Pitfall 4/5)
</success_criteria>

<output>
Create `.planning/phases/98-skill-lifecycle-mutations-archive-restore-move-delete/98-02-SUMMARY.md` when done
</output>
