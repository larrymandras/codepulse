---
phase: 98-skill-lifecycle-mutations-archive-restore-move-delete
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - convex/schema.ts
  - convex/forge.ts
  - convex/forge.test.ts
autonomous: true
requirements: [LIFE-01, LIFE-02, LIFE-03, LIFE-04, LIFE-05, LIFE-06]
user_setup: []

must_haves:
  truths:
    - "An authenticated operator can enqueue a lifecycle command (archive/restore/move/delete) that lands as a forgeCommands row a daemon can claim"
    - "A restore whose name is already active at the target scope is refused server-side (D-03/LIFE-05)"
    - "An archive is refused server-side when a dormant copy already exists in cold storage (D-02/LIFE-01)"
    - "A move is refused server-side when the destination scope already has an active same-name skill (D-02/LIFE-03)"
    - "A permanent delete is refused server-side unless the skill exists ONLY at the dormant/cold origin (D-05/LIFE-04)"
    - "A lifecycle-refused daemon error is rewritten to the UI-SPEC house copy before the row is finalized"
    - "An unauthenticated enqueue throws (Clerk fail-closed); a duplicate commandId is a silent no-op (LIFE-06 relies on the shared TTL row)"
  artifacts:
    - path: "convex/schema.ts"
      provides: "forgeCommands commandType += 'lifecycle' and lifecyclePayload optional-union field"
      contains: "lifecyclePayload"
    - path: "convex/forge.ts"
      provides: "enqueueLifecycle mutation, synthesizeLifecycleRefusalReport adapter, listLifecycleCommands query, isSafeSkillName validator"
      exports: ["enqueueLifecycle", "listLifecycleCommands", "isSafeSkillName", "synthesizeLifecycleRefusalReport"]
    - path: "convex/forge.test.ts"
      provides: "pre-flight-check + idempotency + adapter + skillName-safety test cases"
      contains: "enqueueLifecycle"
  key_links:
    - from: "convex/forge.ts enqueueLifecycle"
      to: "skills table by_name"
      via: "pre-flight registry query"
      pattern: "withIndex\\(\"by_name\""
    - from: "convex/forge.ts ackCommand"
      to: "synthesizeLifecycleRefusalReport"
      via: "commandType === 'lifecycle' dispatch"
      pattern: "lifecycle-refused"
---

<objective>
Build the Convex substrate for skill lifecycle mutations: extend the `forgeCommands` schema with a single `"lifecycle"` command type carrying an `action`-discriminated payload, add the authenticated `enqueueLifecycle` mutation with two-layer-validation LAYER 1 pre-flight checks (shadow-block, cold-collision, move-collision, delete-cold-only), extend `ackCommand`'s write-refusal adapter to recognize the new `lifecycle-refused:<kind>:` error prefix, and add a scoped `listLifecycleCommands` query for the UI hook.

Purpose: This is the API/Backend tier of the phase (per RESEARCH Architectural Responsibility Map). It gives the browser instant, honest feedback on doomed mutations (no queued command that can only fail) and gives the daemon (Plan 98-02) a claimable, TTL-governed command row on the exact same queue Phase 97 built. No new transport, no new queue.
Output: A migration-free schema extension plus three new exported functions in `convex/forge.ts`, all test-covered.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/98-skill-lifecycle-mutations-archive-restore-move-delete/98-CONTEXT.md
@.planning/phases/98-skill-lifecycle-mutations-archive-restore-move-delete/98-RESEARCH.md
@.planning/phases/98-skill-lifecycle-mutations-archive-restore-move-delete/98-PATTERNS.md
@.planning/phases/98-skill-lifecycle-mutations-archive-restore-move-delete/98-UI-SPEC.md

<interfaces>
<!-- Contracts the executor must implement against. Extracted from live code this session. -->

skills registry table (convex/schema.ts, line ~193) — one row PER (name, origin):
  name: string; origin?: string  // "claude-code" | "claude-code:available" | "claude-code:project:<key>"
  indexed `by_name` (["name"]). Pre-flight checks collect all rows for a name, then inspect the ORIGIN SET.
  DORMANT_ORIGIN = "claude-code:available" (src/lib/skills.ts:3). Project origins are prefixed "claude-code:project:". Global origin === "claude-code".

forgeCommands table (convex/schema.ts, line ~1654) — the queue to extend:
  commandType: v.union(v.literal("launch"), v.literal("stop"), v.literal("intake"))  // ADD v.literal("lifecycle")
  intakePayload: v.optional(v.union(v.object({...}), v.null()))  // the wrapper shape lifecyclePayload copies
  indexes include by_commandId, by_commandType_createdAt (reuse for the new list query)

enqueueIntake (convex/forge.ts:700) — the mutation shape to mirror exactly:
  fail-closed auth: `const identity = await ctx.auth.getUserIdentity(); if (identity === null) throw new Error("Authentication required to issue Forge commands");`
  idempotency: query by_commandId .unique(); if (existing) return;
  buildIntakeRow(args, identity.subject, Date.now(), FORGE_COMMAND_TTL_MS) then ctx.db.insert
  FORGE_COMMAND_TTL_MS = 5*60*1000 (line 25). isSafeSubpath (line ~419) is the path-safety precedent.

ackCommand adapter dispatch (convex/forge.ts, ~line 934): today gated on `if (cmd.commandType === "intake")` calling synthesizeWriteRefusalReport; parseWriteRefusalError regex is `^(write-refused|post-placement-warning):([^:]+):([\s\S]*)$`.

listIntakeCommands (convex/forge.ts:1031): `.withIndex("by_commandType_createdAt", q => q.eq("commandType","intake")).order("desc").take(INTAKE_LIST_LIMIT)` — copy this scoping technique, do NOT add a filter to it.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend forgeCommands schema, add isSafeSkillName, scaffold failing tests (Wave 0)</name>
  <read_first>
    - convex/schema.ts (forgeCommands table ~line 1654, skills table ~line 193, intakePayload wrapper ~1689)
    - convex/forge.ts (isSafeSubpath ~line 419, FORGE_COMMAND_TTL_MS line 25)
    - convex/forge.test.ts (existing intake test cases — mirror their convex-test harness usage)
    - src/lib/skills.ts (DORMANT_ORIGIN, isShadowing, isDormant — the origin-set semantics to replicate server-side)
    - .planning/phases/98-.../98-PATTERNS.md (schema section ~line 234)
  </read_first>
  <behavior>
    - isSafeSkillName("legal") === true
    - isSafeSkillName("../etc") === false (contains "..")
    - isSafeSkillName("a/b") === false and isSafeSkillName("a\\b") === false (contains separator)
    - isSafeSkillName("C:foo") === false (drive-letter prefix)
    - isSafeSkillName("") === false
    - Test scaffolds for enqueueLifecycle pre-flight checks and the ack adapter exist and FAIL RED (functions not yet implemented in this task)
  </behavior>
  <action>
    In convex/schema.ts: add `v.literal("lifecycle")` to the forgeCommands `commandType` union. Add a new `lifecyclePayload` field immediately after `intakePayload`, using the identical `v.optional(v.union(v.object({...}), v.null()))` wrapper (backward-compat: existing launch/stop/intake rows omit it and stay valid). The object shape is: `action: v.union(v.literal("archive"), v.literal("restore"), v.literal("move"), v.literal("delete"))`, `skillName: v.string()`, `sourceOrigin: v.string()`, `destination: v.union(v.literal("global"), v.literal("project"), v.literal("cold"))`, `workspaceId: v.union(v.string(), v.null())`. Reuse the existing `by_commandType_createdAt` index unchanged (it already keys on commandType). Add no new index.

    In convex/forge.ts: add and export `isSafeSkillName(name: string): boolean` — stricter than isSafeSubpath: reject empty, reject any string containing `/` or `\`, reject `..` as a full path segment, reject a leading drive-letter shape matched by `/^[A-Za-z]:/`. It validates a BARE directory-name segment because skillName drives a real fs rename/rmSync in Plan 98-02.

    Run `npx convex codegen` (offline codegen only — NOT a deploy) to regenerate `_generated/api.d.ts` for the schema change.

    In convex/forge.test.ts: add RED test scaffolds (these must fail until Tasks 2-3 land): (a) isSafeSkillName cases from the behavior block (these pass now); (b) placeholder describe-blocks for `enqueueLifecycle` pre-flight refusals and `synthesizeLifecycleRefusalReport` referencing the not-yet-exported symbols so the suite is RED. Use the same convex-test harness pattern the existing intake tests use.
  </action>
  <verify>
    <automated>cd C:/Users/mandr/codepulse && npx vitest run convex/forge.test.ts -t "isSafeSkillName" && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - convex/schema.ts contains `v.literal("lifecycle")` in the forgeCommands commandType union
    - convex/schema.ts contains `lifecyclePayload:` with an `action` union of the four literals and a `sourceOrigin: v.string()` field
    - convex/forge.ts contains `export function isSafeSkillName(`
    - `npx vitest run convex/forge.test.ts -t "isSafeSkillName"` exits 0
    - `npx tsc --noEmit` exits 0 (schema + codegen consistent)
    - convex/forge.test.ts references `enqueueLifecycle` and `synthesizeLifecycleRefusalReport` (RED scaffolds present)
  </acceptance_criteria>
  <done>Schema carries the lifecycle command type and payload; isSafeSkillName is exported and unit-passing; RED scaffolds exist for the mutation/adapter work in Tasks 2-3.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: enqueueLifecycle mutation with LAYER-1 pre-flight validation</name>
  <read_first>
    - convex/forge.ts (enqueueIntake handler lines 700-771 — copy its exact discipline; buildIntakeRow ~363; isSafeSubpath ~419)
    - convex/schema.ts (skills table by_name; forgeCommands fields for the row builder)
    - src/lib/skills.ts (DORMANT_ORIGIN constant, isShadowing/isDormant origin-set logic to replicate server-side)
    - .planning/phases/98-.../98-CONTEXT.md (D-02, D-03, D-04, D-05 — the exact refusal rules)
    - .planning/phases/98-.../98-RESEARCH.md (Security Domain V4/V5, Pitfall 1 sourceOrigin requirement)
  </read_first>
  <behavior>
    - Unauthenticated call throws "Authentication required to issue Forge commands"
    - Duplicate commandId is a silent no-op (no second row inserted)
    - action="restore" with destination="global" when a "claude-code" origin row already exists for that name throws a `lifecycle-refused:shadow:` framed error (LIFE-05/D-03)
    - action="archive" when a DORMANT_ORIGIN row already exists for that name throws a `lifecycle-refused:collision:` framed error (D-02)
    - action="move" to destination whose origin already has an active same-name row throws a `lifecycle-refused:collision:` framed error (D-02)
    - action="delete" when the skill has ANY non-dormant origin row throws (D-05: cold-only)
    - action="move"/"restore" with destination="project" and null workspaceId throws
    - skillName failing isSafeSkillName throws before any insert
    - A valid archive/restore/move/delete inserts exactly one forgeCommands row with commandType "lifecycle" and the populated lifecyclePayload
  </behavior>
  <action>
    Add `export const enqueueLifecycle = mutation({...})` to convex/forge.ts. Args: `hostId: v.string()`, `commandId: v.string()`, and the five lifecyclePayload fields (action, skillName, sourceOrigin, destination, workspaceId) using the same validators as the schema object. Handler order copies enqueueIntake exactly: (1) fail-closed `getUserIdentity()` null-check throwing "Authentication required to issue Forge commands" — never the read-query graceful-skip; (2) commandId idempotency via `by_commandId .unique()` → `if (existing) return;`; (3) `isSafeSkillName(args.skillName)` guard; (4) project-destination requires workspaceId; (5) LAYER-1 pre-flight: `const rows = await ctx.db.query("skills").withIndex("by_name", q => q.eq("name", args.skillName)).collect();` then derive the origin set and apply the action-specific rule — restore/move refuse when the destination scope's origin is already present among non-dormant rows (compose the message with prefix `lifecycle-refused:shadow:` for restore-into-active-shadow, `lifecycle-refused:collision:` for move/archive target already occupied); archive refuses when DORMANT_ORIGIN is present; delete refuses when any non-dormant origin is present (only permit when the skill exists solely at DORMANT_ORIGIN). Validate `sourceOrigin` actually appears among the skill's live rows (V5: never trust a client origin claim for a name that does not exist at that origin). (6) Build a lifecycle row (add a `buildLifecycleRow` helper mirroring buildIntakeRow: sets hostId, commandId, commandType "lifecycle", lifecyclePayload, launchPayload/stopPayload/intakePayload null, status "queued", issuedBy identity.subject, createdAt now, expiresAt now + FORGE_COMMAND_TTL_MS, claimed/executed/completed null, resolvedForgeJobId null, error null) and insert.

    Throwing the `lifecycle-refused:<kind>:<message>` string from the mutation gives the client an actionable error immediately; the identical prefix convention is what Plan 98-02's daemon emits on ack for the LAYER-2 re-check, and what Task 3's adapter parses. Do NOT reuse the literal `write-refused:` prefix (Pitfall 6).
  </action>
  <verify>
    <automated>cd C:/Users/mandr/codepulse && npx vitest run convex/forge.test.ts -t "enqueueLifecycle"</automated>
  </verify>
  <acceptance_criteria>
    - convex/forge.ts contains `export const enqueueLifecycle = mutation(`
    - Handler contains `getUserIdentity()` fail-closed throw and `by_commandId` idempotency early-return
    - Handler contains `withIndex("by_name"` skills pre-flight collect
    - Handler references `isSafeSkillName` and `FORGE_COMMAND_TTL_MS`
    - All `enqueueLifecycle` test cases from the behavior block pass: `npx vitest run convex/forge.test.ts -t "enqueueLifecycle"` exits 0
    - No `write-refused:` literal is emitted by the lifecycle path (grep: lifecycle refusals use `lifecycle-refused:`)
  </acceptance_criteria>
  <done>enqueueLifecycle refuses every doomed mutation server-side with framed errors, is auth-gated and idempotent, and inserts a valid claimable row for legal actions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: ackCommand lifecycle-refusal adapter + listLifecycleCommands query</name>
  <read_first>
    - convex/forge.ts (synthesizeWriteRefusalReport ~174-215, parseWriteRefusalError ~101, ackCommand dispatch ~934-949, listIntakeCommands ~1031, INTAKE_LIST_LIMIT ~570)
    - .planning/phases/98-.../98-UI-SPEC.md (Copywriting Contract: cold-collision, move-target-collision, shadow-block strings — compose these VERBATIM)
    - .planning/phases/98-.../98-RESEARCH.md (Pitfall 6: new prefix + sibling adapter, do not extend intake's composer)
  </read_first>
  <behavior>
    - synthesizeLifecycleRefusalReport parsing `lifecycle-refused:collision:<raw>` for an archive returns the UI-SPEC cold-collision house copy naming the skill
    - parsing `lifecycle-refused:shadow:<raw>` returns the UI-SPEC shadow-block house copy naming the skill + scope
    - a non-framed error string passes through unchanged (never crashes)
    - listLifecycleCommands returns only commandType "lifecycle" rows, newest-first, bounded
  </behavior>
  <action>
    Add `export function synthesizeLifecycleRefusalReport(report, error, payload)` to convex/forge.ts — a SIBLING of synthesizeWriteRefusalReport, not an edit to it. It parses a NEW prefix via a regex like `^lifecycle-refused:([^:]+):([\s\S]*)$`, extracts the kind (`collision` | `shadow`), and composes the exact house-copy strings from 98-UI-SPEC.md's Copywriting Contract, using the payload's skillName/destination/action to fill `{name}` and `{scope}`. Non-matching errors pass through unchanged. Extend `ackCommand`'s dispatch with `else if (cmd.commandType === "lifecycle")` calling this new adapter — leave the existing `if (cmd.commandType === "intake")` branch byte-unchanged.

    Add `export const listLifecycleCommands = query({ args: {}, handler })` copying listIntakeCommands' scoping technique verbatim: `.withIndex("by_commandType_createdAt", q => q.eq("commandType", "lifecycle")).order("desc").take(LIFECYCLE_LIST_LIMIT)`. Define `LIFECYCLE_LIST_LIMIT` (e.g. 30) as a module constant. Do NOT add a filter to listIntakeCommands (that would let lifecycle volume crowd the intake panel — Phase 97 review #6 regression).
  </action>
  <verify>
    <automated>cd C:/Users/mandr/codepulse && npx vitest run convex/forge.test.ts -t "lifecycle" && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - convex/forge.ts contains `export function synthesizeLifecycleRefusalReport(`
    - convex/forge.ts contains `export const listLifecycleCommands = query(`
    - ackCommand contains `cmd.commandType === "lifecycle"` dispatch and the intake branch is unchanged
    - Composed strings match 98-UI-SPEC.md Copywriting Contract wording for cold-collision and shadow-block (exact `{name}`/`{scope}` substitution)
    - `npx vitest run convex/forge.test.ts -t "lifecycle"` exits 0 and `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Lifecycle daemon refusals are rewritten to house copy on ack; the UI has a scoped, bounded query for lifecycle command rows without touching the intake panel's list.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → Convex mutation | Client-supplied skillName / sourceOrigin / workspaceId cross into a control path that a daemon will execute as real fs ops |
| Convex ack httpAction → forgeCommands row | Daemon-supplied error strings are rewritten into operator-facing copy |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-98-01 | Tampering | enqueueLifecycle skillName | mitigate | `isSafeSkillName` rejects separators, `..`, drive-letter prefixes before any row is inserted (Task 1/2); daemon re-validates in 98-02 |
| T-98-05 | Spoofing/Elevation | enqueueLifecycle auth | mitigate | Clerk fail-closed `getUserIdentity()` null-throw, mirroring enqueueIntake D-13 — never graceful-skip on a write path |
| T-98-06 | Tampering (Input Validation) | client-supplied sourceOrigin | mitigate | LAYER-1 pre-flight validates sourceOrigin exists among the skill's live `skills` rows; refuse a move/delete of a name/origin that does not actually exist (V5) |
| T-98-02 | Elevation/Tampering | delete cold-only rule | mitigate | Convex refuses delete when any non-dormant origin row exists; daemon LAYER-2 re-check (98-02) is the host-truth backstop |
| T-98-SC | Tampering | package installs | accept | No npm/pip installs in this plan; no `dropdown-menu` here. Nothing to audit. |
</threat_model>

<verification>
- `npx vitest run convex/forge.test.ts` green (all lifecycle + isSafeSkillName cases)
- `npx tsc --noEmit` clean (schema/codegen/mutation consistent)
- Full codepulse suite `npm test` green at wave merge
</verification>

<success_criteria>
- forgeCommands accepts a "lifecycle" command with an action-discriminated payload, migration-free
- enqueueLifecycle is auth-gated, idempotent, and refuses every D-02/D-03/D-05 violation server-side with framed errors
- ackCommand rewrites `lifecycle-refused:<kind>:` daemon errors to UI-SPEC house copy
- listLifecycleCommands is scoped to lifecycle rows and does not regress the intake panel
</success_criteria>

<output>
Create `.planning/phases/98-skill-lifecycle-mutations-archive-restore-move-delete/98-01-SUMMARY.md` when done
</output>
