---
phase: 87-saved-views-temporal-diff
plan: 03
type: execute
wave: 3
depends_on: ["87-02"]
files_modified:
  - src/hooks/useKgDiff.ts
  - src/hooks/useKgDiff.test.ts
  - src/components/kg/KGDiffControls.tsx
  - src/components/kg/KGControls.tsx
  - src/pages/KnowledgeGraph.tsx
autonomous: true
requirements: [KG-11]
must_haves:
  truths:
    - "Operator on the Temporal lens sees a Point | Diff | Animate sub-mode toggle; Point is default and preserves the existing single-as-of behavior (no regression)."
    - "In Diff sub-mode the operator picks From + To dates and clicks Compare; the page computes the diff client-side (no backend diff endpoint) — added/removed/changed node sets (node changed if attributes OR incident edges differ) and independent edge add/remove/change sets. [D-10, D-11, D-12]"
    - "Diff-state nodes render with distinct color (green added / red removed / amber changed / dimmed unchanged) and edges render with the diff stroke palette; a DIFF legend appends to the floating legend overlay."
    - "A failed snapshot fetch shows inline error copy and keeps Point + (later) Animate working — no hard block. [D-08]"
  artifacts:
    - path: "src/hooks/useKgDiff.ts"
      provides: "useKgDiff(dateA,dateB): { diff, graphB, loading, error, compare }; computeDiff pure fn"
      exports: ["useKgDiff", "computeDiff"]
    - path: "src/hooks/useKgDiff.test.ts"
      provides: "Pure computeDiff set-arithmetic tests + 404 graceful-degrade test"
    - path: "src/components/kg/KGDiffControls.tsx"
      provides: "From/To date pickers + Compare button (disabled until both set and From < To)"
      min_lines: 30
    - path: "src/components/kg/KGControls.tsx"
      provides: "Point|Diff|Animate temporal sub-mode toggle (lens stays 'temporal')"
      contains: "temporalSubMode"
    - path: "src/pages/KnowledgeGraph.tsx"
      provides: "paintNodeDiff + linkColorDiffFn swap + DIFF legend append when sub-mode is diff"
      contains: "paintNodeDiff"
  key_links:
    - from: "src/hooks/useKgDiff.ts"
      to: "fetchOverview({ asOf })"
      via: "two parallel point-in-time fetches"
      pattern: "fetchOverview\\(\\{\\s*asOf"
    - from: "src/pages/KnowledgeGraph.tsx"
      to: "paintNodeDiff"
      via: "canvas paint swap when temporalSubMode === 'diff'"
      pattern: "temporalSubMode === \"diff\""
---

<objective>
Add the Temporal Diff sub-mode (KG-11, SC#3): a `Point | Diff | Animate` toggle under the Temporal lens
(lens value stays `"temporal"` — sub-modes are UI-local), a `useKgDiff` hook that fetches two
`fetchOverview({ asOf })` snapshots and computes client-side diff sets, a `KGDiffControls` From/To picker
row, and `paintNodeDiff` + diff link coloring + a DIFF legend swapped in when diff mode is active.

Purpose: SC#3 — operator selects two as-of dates and sees added/removed/changed nodes and edges with
distinct visual treatment. Diff is client-side, node-id-based (D-12); nodes are "changed" if attributes
OR incident current-edge set differ (D-10); edges classify independently (D-11). Graceful-degrade on
fetch failure (D-08).
Output: useKgDiff + tests + KGDiffControls + KGControls sub-mode toggle + KnowledgeGraph diff paint/legend.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/87-saved-views-temporal-diff/87-CONTEXT.md
@.planning/phases/87-saved-views-temporal-diff/87-UI-SPEC.md
@.planning/phases/87-saved-views-temporal-diff/87-RESEARCH.md
@.planning/phases/87-saved-views-temporal-diff/87-PATTERNS.md

<interfaces>
<!-- src/lib/kgApi.ts (live) -->
fetchOverview(params: { asOf?: string | null; ... }): Promise<KgOverviewResponse>  // line 198
AstridrApiError (src/lib/astridrApi.ts) — has `.status: number`

<!-- src/lib/kg-graph.ts (live) -->
toGraphData(...) , normalizeOverview(resp) → KgGraphData
KgNode { id; name; entityType; agentId; val; degree; color; attributes: KgAttribute[]; synthetic; community? }
KgAttribute { predicate; value; confidence; validFrom; validTo; contradictionFlag; sourceTripleId; sourceEventId? }
KgLink { id; source; target; predicate; confidence; width; current; contradictionFlag; validFrom; validTo; agentId }
KgGraphData { nodes: KgNode[]; links: KgLink[]; stats: {...} }

<!-- src/pages/KnowledgeGraph.tsx (live) -->
paintNode (lines 212-256): canvas node painter; pattern to copy into paintNodeDiff.
linkColorFn (lines 32-36), linkLineDashFn (40-42): edge styling to copy into diff variants.
floating legend overlay (lines 379-429): append DIFF section here.
Monotonic token: useKnowledgeGraph.ts:116 (const token = ++reqRef.current; if (token !== reqRef.current) return;).

<!-- DIFF color table (UI-SPEC Color section / RESEARCH Pattern 6) -->
added:     node #22c55e / edge rgba(34,197,94,0.55) solid
removed:   node #ef4444 / edge rgba(239,68,68,0.40) dashed [4,3]
changed:   node #eab308 / edge rgba(234,179,8,0.55) solid
unchanged: node.color @ globalAlpha 0.35 / edge rgba(161,163,170,0.15) solid
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: useKgDiff hook + computeDiff pure fn + Wave 0 tests</name>
  <files>src/hooks/useKgDiff.ts, src/hooks/useKgDiff.test.ts</files>
  <read_first>
    - src/hooks/useKnowledgeGraph.ts (monotonic reqRef token line 116; fetch/error gate pattern)
    - src/lib/kgApi.ts (fetchOverview signature, line 198; OverviewParams.asOf)
    - src/lib/kg-graph.ts (KgNode/KgLink/KgGraphData/KgAttribute types lines 39-98; toGraphData, normalizeOverview)
    - .planning/phases/87-saved-views-temporal-diff/87-RESEARCH.md (Pattern 4 computeDiff; useKgDiff skeleton; Pitfall 6 edge-id stability; Assumptions A2)
    - .planning/phases/87-saved-views-temporal-diff/87-PATTERNS.md (useKgDiff.ts section; Graceful-Degrade + Monotonic-Token shared patterns)
  </read_first>
  <behavior>
    - computeDiff(graphA, graphB) returns added/removed/changed Set<nodeId> where: added = ids in B not in A; removed = ids in A not in B; changed = ids in both whose attributes differ OR whose incident current-edge set differs (D-10).
    - computeDiff returns an independent edge diff: each edge classified added/removed/changed by its own id (D-11); an edge present in both but with changed current/validTo is "changed"; a new edge between two unchanged nodes is "added".
    - Edge identity uses link.id when present, else falls back to composite key `${source}|${target}|${predicate}` (Pitfall 6 / A2).
    - compare() on a 404 (AstridrApiError.status === 404) sets a human error string and does NOT throw (D-08); a stale in-flight compare is dropped via the monotonic token.
  </behavior>
  <action>
    Create src/hooks/useKgDiff.ts. Import fetchOverview from "../lib/kgApi", AstridrApiError from
    "../lib/astridrApi", and toGraphData/normalizeOverview/type KgGraphData/KgNode/KgLink from "../lib/kg-graph".

    Export a pure `computeDiff(graphA: KgGraphData, graphB: KgGraphData): KgDiffSets` where
    `KgDiffSets = { added: Set<string>; removed: Set<string>; changed: Set<string>;
    edges: { added: Set<string>; removed: Set<string>; changed: Set<string> } }`. Implement node sets via
    Set arithmetic over node.id (RESEARCH Pattern 4). For "changed": compare attributes (a stable
    serialization of each node's `attributes` sorted by predicate+value+confidence) AND the incident
    current-edge set (build a Set of current-edge keys touching the node in each graph; differ ⇒ changed).
    For edges: define `edgeKey(link)` = `link.id || \`${src}|${tgt}|${predicate}\`` (resolve source/target to
    ids whether they are strings or node refs); classify added/removed by key membership; "changed" = same
    key in both but `current` or `validTo` differs (D-11).

    Export `useKgDiff(dateA: string | null, dateB: string | null)` returning
    `{ diff: KgDiffSets | null, graphB: KgGraphData | null, loading, error, compare }`. `compare()` uses the
    monotonic `reqRef` token (mirror useKnowledgeGraph.ts:116): bump token, set loading, clear error,
    `Promise.all([fetchOverview({asOf:dateA}), fetchOverview({asOf:dateB})])`, drop if stale, set graphA/graphB
    via toGraphData(normalizeOverview(resp)). On catch (drop if stale): if AstridrApiError && status===404 set
    "Could not load snapshot for {dateA} or {dateB}." else "Could not reach Ástríðr." (D-08 — do not throw).
    `diff` is `useMemo(() => graphA && graphB ? computeDiff(graphA, graphB) : null, [graphA, graphB])`.

    Create src/hooks/useKgDiff.test.ts (Wave 0). Pure-function tests on computeDiff with small hand-built
    KgGraphData fixtures: (1) a node only in B → added; only in A → removed; in both with differing
    attributes → changed; in both identical with identical edges → neither. (2) a node unchanged in
    attributes but gaining a current edge → changed (D-10). (3) an edge only in B between two unchanged nodes
    → edges.added contains it; an edge in both with flipped `current` → edges.changed (D-11). (4) edge keys
    fall back to composite when link.id is empty (Pitfall 6). For the 404 degrade test, mock fetchOverview to
    reject with an AstridrApiError(status 404) and assert compare() resolves with `error` set and no throw.
  </action>
  <verify>
    <automated>npx vitest run src/hooks/useKgDiff.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/hooks/useKgDiff.test.ts` passes covering added/removed/changed nodes, edge-independent classification, composite-key fallback, and 404 degrade.
    - Test asserts a node that only gained a current edge (attributes identical) lands in `changed` (D-10 behavior assertion).
    - Test asserts a new edge between two unchanged nodes lands in `edges.added` (D-11 behavior assertion).
    - Test asserts compare() with a 404 sets `error` and does not throw (behavior assertion, D-08).
    - useKgDiff.ts exports `useKgDiff` and `computeDiff` (source assertion).
    - computeDiff uses link.id OR composite `${source}|${target}|${predicate}` (source assertion for Pitfall 6 fallback).
  </acceptance_criteria>
  <done>useKgDiff + computeDiff exist with correct D-10/D-11/D-08 semantics; Wave 0 tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: KGDiffControls + Point|Diff|Animate sub-mode toggle in KGControls</name>
  <files>src/components/kg/KGDiffControls.tsx, src/components/kg/KGControls.tsx</files>
  <read_first>
    - src/components/kg/KGControls.tsx (the file being modified — temporal as-of row 125-156, lens-tab chip style 57-64, KGControlsProps)
    - src/components/kg/KGControls.test.tsx (extend with sub-mode assertions; conventions)
    - .planning/phases/87-saved-views-temporal-diff/87-PATTERNS.md (KGDiffControls.tsx + KGControls.tsx modify sections)
    - .planning/phases/87-saved-views-temporal-diff/87-UI-SPEC.md (Layout Contract; Component Inventory KG-11; Copywriting Contract diff rows)
  </read_first>
  <action>
    Create src/components/kg/KGDiffControls.tsx mirroring the KGControls temporal as-of row (125-156). Props:
    `{ dateA: string | null; dateB: string | null; onChangeA(d: string|null): void; onChangeB(d: string|null): void;
    onCompare(): void; loading: boolean; }`. Render two `Input type="date"` pickers with `From` and `To` labels
    (`text-[10px] uppercase tracking-wide`, matching the existing `As of` label) and a Compare Button
    (`variant="secondary" size="sm" font-mono text-sm`, label "Compare") disabled when `!dateA || !dateB ||
    dateA >= dateB`. When dates are equal/invalid show the inline muted copy "Select two different dates to
    compare." Use ISO-date handling consistent with the existing as-of Input (value `dateX?.slice(0,10) ?? ""`,
    onChange converts to ISO or null).

    KGControls.tsx: add to `KGControlsProps`: `temporalSubMode: "point" | "diff" | "animate"`,
    `onSubMode(m: "point"|"diff"|"animate"): void`, plus the diff-control props (`dateA, dateB, onChangeA,
    onChangeB, onCompare, diffLoading`). When `lens === "temporal"`, render a secondary control row (NOT a new
    lens tab — UI-SPEC) ABOVE the existing as-of row: three segmented chips `Point | Diff | Animate` reusing
    the lens-tab active style (`bg-primary/15 border-primary/50 text-primary` active / `bg-card/60 border-border
    text-muted-foreground` idle), `aria-pressed={temporalSubMode === m}`. Then branch the temporal body:
    `temporalSubMode === "point"` → existing as-of Input + Now (UNCHANGED, no regression — SC); `=== "diff"` →
    `<KGDiffControls .../>`; `=== "animate"` → leave a placeholder slot (Plan 04 fills it; render nothing or a
    minimal stub for now). The lens value stays "temporal" — sub-modes are UI-local only.

    Extend src/components/kg/KGControls.test.tsx: assert the three sub-mode chips render when lens==="temporal";
    clicking "Diff" calls onSubMode("diff"); the as-of row remains for "point" (regression guard).
  </action>
  <verify>
    <automated>npx vitest run src/components/kg/KGControls.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/components/kg/KGControls.test.tsx` passes including new sub-mode assertions.
    - Test asserts clicking the Diff chip calls onSubMode("diff") (behavior assertion).
    - Test asserts Point sub-mode still renders the as-of date Input (regression assertion — SC no-regression).
    - KGControls.tsx renders the Point|Diff|Animate row only when `lens === "temporal"` (source assertion).
    - The lens value is never set to "diff"/"animate" — sub-mode is separate UI state (source assertion: setLens is not called with those values).
    - Compare button disabled expression includes `dateA >= dateB` (source assertion).
  </acceptance_criteria>
  <done>Sub-mode toggle + KGDiffControls render correctly; Point mode unchanged; KGControls tests green.</done>
</task>

<task type="auto">
  <name>Task 3: Wire diff into KnowledgeGraph — paintNodeDiff, diff edges, DIFF legend</name>
  <files>src/pages/KnowledgeGraph.tsx</files>
  <read_first>
    - src/pages/KnowledgeGraph.tsx (the file being modified — paintNode 212-256, linkColorFn/linkLineDashFn 32-42, legend 379-429, SectionErrorBoundary 302-310)
    - src/hooks/useKgDiff.ts (Task 1 — the hook being consumed)
    - .planning/phases/87-saved-views-temporal-diff/87-PATTERNS.md (KnowledgeGraph.tsx paintNodeDiff + diff legend sections)
    - .planning/phases/87-saved-views-temporal-diff/87-UI-SPEC.md (Color section diff palette; Layout Contract diff legend append; Interaction Contract node-click-in-diff badge)
  </read_first>
  <action>
    KnowledgeGraph.tsx:
    1. Add page-local `const [temporalSubMode, setTemporalSubMode] = useState<"point"|"diff"|"animate">("point")`.
       Reset to "point" when `lens` changes away from / re-enters temporal (UI-SPEC: state discarded on lens change).
    2. Add `const [diffDateA, setDiffDateA] = useState<string|null>(null)` and `diffDateB` similarly; call
       `const { diff, graphB, loading: diffLoading, error: diffError, compare } = useKgDiff(diffDateA, diffDateB);`.
    3. Implement `paintNodeDiff` by copying paintNode (212-256) and swapping the color/alpha logic with the
       DIFF color table: state = added/removed/changed via `diff.added/removed/changed.has(n.id)` else
       "unchanged"; unchanged uses `n.color` at `ctx.globalAlpha = 0.35` (RESEARCH Pitfall 3 — explicit dim);
       added/removed/changed use #22c55e/#ef4444/#eab308 at alpha 1.0. Selection ring + hover white-fill behavior
       unchanged. Reset globalAlpha after paint.
    4. Implement `linkColorDiffFn(link)` and `linkLineDashDiffFn(link)` using the edge diff sets + the UI-SPEC
       edge palette (added solid green 0.55, removed dashed [4,3] red 0.40, changed solid amber 0.55, unchanged
       solid zinc 0.15). Classify each link by the same edgeKey logic used in computeDiff (import or re-derive).
    5. When `lens === "temporal" && temporalSubMode === "diff" && diff`: render the canvas from `graphB` and pass
       `paintNodeDiff` + `linkColorDiffFn` + `linkLineDashDiffFn` to ForceGraphCanvas; otherwise use the existing
       paintNode/linkColorFn (no regression in Point mode). Show the "Diffing knowledge graph…" animate-pulse
       during `diffLoading`; show `diffError` inline (red-border banner pattern, lines 321-330) without blocking;
       show "No changes between these two snapshots." when diff has empty added/removed/changed sets.
    6. Append a DIFF section to the floating legend (379-429) when `temporalSubMode === "diff" && diff`:
       a `border-t border-border mt-1 pt-1` "DIFF" header then added/removed/changed/unchanged swatch rows
       (green/red/amber/zinc-dimmed). Auto-hide otherwise.
    7. Pass `temporalSubMode`, `setTemporalSubMode` (as onSubMode), and the diff-control props
       (diffDateA/B, setDiffDateA/B as onChangeA/B, `compare` as onCompare, diffLoading) down to KGControls.
    8. (Optional, UI-SPEC) In Diff mode, when a node is selected, pass its change state to KGDetailsPanel so the
       panel header can show an added/removed/changed Badge — acceptable to defer the badge to UAT if the panel
       prop wiring is non-trivial; do not block the plan on it.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` clean.
    - `npm test` green (full suite — no regressions).
    - KnowledgeGraph.tsx defines `paintNodeDiff` and uses it only when `temporalSubMode === "diff"` (source assertion).
    - Unchanged nodes in diff paint use `globalAlpha = 0.35` (source assertion — Pitfall 3).
    - A DIFF legend section is appended conditional on `temporalSubMode === "diff"` (source assertion).
    - Point sub-mode path still uses the original paintNode/linkColorFn (regression source assertion).
  </acceptance_criteria>
  <done>
    Diff mode re-colors nodes + edges per the diff palette, shows the DIFF legend, degrades inline on fetch
    failure, and leaves Point mode unchanged; typecheck + full suite green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → Ástríðr `/api/kg/overview` | Operator-chosen as-of dates (From/To) become query params on a cross-service fetch |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-87-08 | Tampering | as-of date inputs (From/To) | mitigate | Date inputs are `<input type="date">` producing ISO strings; Compare is disabled unless both parse and `dateA < dateB`. The value is sent as an `asOf` query param to the existing, already-authed fetchOverview path — no new injection surface. |
| T-87-09 | Denial of Service | rapid repeated Compare | mitigate | Monotonic reqRef token drops stale in-flight fetches; only two parallel fetches per compare. No unbounded fan-out (animation fan-out is Plan 04's concern). |
| T-87-10 | Information Disclosure | diff fetch error copy | mitigate | Error copy names the operation/date only ("Could not load snapshot for {date}") — no stack trace, no internal IDs surfaced (D-08). |
</threat_model>

<verification>
- `npx vitest run src/hooks/useKgDiff.test.ts src/components/kg/KGControls.test.tsx` green.
- `npx tsc --noEmit` clean; `npm test` full suite green.
</verification>

<success_criteria>
- Point | Diff | Animate toggle present under Temporal lens; Point unchanged (no regression).
- Compare fetches two snapshots and computes correct added/removed/changed node + independent edge sets.
- Diff nodes/edges render with the distinct diff palette; DIFF legend appended.
- Fetch failure degrades inline; Point keeps working (SC#3 + D-08).
</success_criteria>

<output>
Create `.planning/phases/87-saved-views-temporal-diff/87-03-SUMMARY.md` when done.
</output>
