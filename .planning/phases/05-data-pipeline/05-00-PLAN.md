---
phase: 05-data-pipeline
plan: 00
type: execute
wave: 1
depends_on: []
files_modified:
  - convex/aggregates.test.ts
  - convex/archival.test.ts
  - src/hooks/useRecentEvents.test.ts
autonomous: true
requirements:
  - DP-01
  - DP-02
  - DP-03
  - DP-04

must_haves:
  truths:
    - "Test stubs exist for all four DP requirements before implementation begins"
    - "All test stubs use test.todo so the suite stays green without implementation"
  artifacts:
    - path: "convex/aggregates.test.ts"
      provides: "Test stubs for DP-01 (computeHourly, rollupDaily) and DP-02 (aggregate read queries)"
      contains: "test.todo"
    - path: "convex/archival.test.ts"
      provides: "Test stubs for DP-03 (markStaleArchived, retention config, archived filter)"
      contains: "test.todo"
    - path: "src/hooks/useRecentEvents.test.ts"
      provides: "Test stubs for DP-04 (paginated hook shape, loadMore, status values)"
      contains: "test.todo"
  key_links: []
---

<objective>
Create Nyquist-compliant test stubs for all Phase 5 requirements before any implementation begins.

Purpose: Ensures every subsequent plan has automated verification targets. All stubs use `test.todo` so the test suite stays green.
Output: Three test files with descriptive todo stubs covering DP-01 through DP-04.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-data-pipeline/05-CONTEXT.md
@.planning/phases/05-data-pipeline/05-RESEARCH.md
@.planning/phases/05-data-pipeline/05-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Wave 0 test stubs for aggregation, archival, and pagination</name>
  <files>convex/aggregates.test.ts, convex/archival.test.ts, src/hooks/useRecentEvents.test.ts</files>
  <read_first>
    convex/aggregates.test.ts (verify it does not exist yet)
    convex/archival.test.ts (verify it does not exist yet)
    src/hooks/useRecentEvents.test.ts (verify it does not exist yet — may already exist from prior phase)
    src/test/setup.ts (understand existing test setup and mocks)
  </read_first>
  <action>
Create three test files with `test.todo` stubs. No imports, no implementation — just descriptive placeholders.

**File 1: `convex/aggregates.test.ts`**
```typescript
import { describe, test } from "vitest";

describe("aggregates", () => {
  describe("computeHourly", () => {
    test.todo("writes cost aggregate rows grouped by provider+model for the last completed hour");
    test.todo("writes event count aggregate rows grouped by event_type for the last completed hour");
    test.todo("writes error rate aggregate rows grouped by error category for the last completed hour");
    test.todo("skips rows where archived === true");
    test.todo("uses bucket_start truncated to the hour boundary in epoch seconds");
  });

  describe("rollupDaily", () => {
    test.todo("sums 24 hourly aggregate rows into one daily row per metric_type+dimensions");
    test.todo("reads from aggregates table, not raw tables");
    test.todo("uses yesterday UTC midnight as dayStart");
  });

  describe("aggregate read queries", () => {
    test.todo("costByProviderAggregate returns cost grouped by provider from aggregates table");
    test.todo("errorRateTrendAggregate returns hourly error counts from aggregates table");
    test.todo("activityHeatmapAggregate returns day-hour cells from aggregates table");
  });
});
```

**File 2: `convex/archival.test.ts`**
```typescript
import { describe, test } from "vitest";

describe("archival", () => {
  describe("markStaleArchived", () => {
    test.todo("marks rows older than retention threshold as archived: true");
    test.todo("does not mark rows newer than retention threshold");
    test.todo("reads retention_days from agentConfigs table");
    test.todo("defaults to 30 days when no config exists");
    test.todo("processes events, runtime_events, llmMetrics, and toolExecutions tables");
    test.todo("limits batch to 500 rows per table per run");
  });

  describe("setRetentionDays", () => {
    test.todo("clamps input to minimum 1 day");
    test.todo("clamps input to maximum 365 days");
    test.todo("upserts retention_days in agentConfigs table");
  });
});
```

**File 3: `src/hooks/useRecentEvents.test.ts`**
If this file already exists, append the new stubs inside the existing describe block. If it does not exist, create it fresh:
```typescript
import { describe, test } from "vitest";

describe("useRecentEvents (paginated)", () => {
  test.todo("returns { events, status, loadMore } shape");
  test.todo("status is 'LoadingFirstPage' on initial render");
  test.todo("calls usePaginatedQuery with api.events.listRecentPaginated");
  test.todo("passes initialNumItems of 25 by default");
  test.todo("loadMore function is callable");
});
```
  </action>
  <verify>
    <automated>npx vitest run convex/aggregates.test.ts convex/archival.test.ts src/hooks/useRecentEvents.test.ts 2>&1 | grep -E "todo|pass|fail"</automated>
  </verify>
  <acceptance_criteria>
    - convex/aggregates.test.ts exists and contains "test.todo"
    - convex/archival.test.ts exists and contains "test.todo"
    - src/hooks/useRecentEvents.test.ts exists and contains "test.todo"
    - `npx vitest run convex/aggregates.test.ts convex/archival.test.ts src/hooks/useRecentEvents.test.ts` exits 0 (all todos are skipped, none fail)
    - No import of production code in any test file (stubs only)
  </acceptance_criteria>
  <done>Three test stub files exist, suite is green, all Phase 5 requirements have at least one test.todo placeholder</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No trust boundaries in this plan — test stubs contain no production logic or user input handling.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| (none) | — | — | — | Test-only plan, no security surface |
</threat_model>

<verification>
- `npx vitest run convex/aggregates.test.ts convex/archival.test.ts src/hooks/useRecentEvents.test.ts` exits 0
- `npm test` full suite still green
</verification>

<success_criteria>
All three test files exist with descriptive test.todo stubs covering DP-01, DP-02, DP-03, DP-04. Full test suite remains green.
</success_criteria>

<output>
After completion, create `.planning/phases/05-data-pipeline/05-00-SUMMARY.md`
</output>
