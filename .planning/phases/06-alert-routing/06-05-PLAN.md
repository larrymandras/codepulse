---
phase: 06-alert-routing
plan: 05
type: execute
wave: 3
depends_on: ["06-02", "06-03"]
files_modified:
  - src/pages/Alerts.tsx
  - src/components/AlertRulesEngine.tsx
  - src/components/AlertLifecycleActions.tsx
  - src/components/MuteDurationPicker.tsx
  - src/components/WebhookStatusBadge.tsx
  - src/components/AlertRuleForm.tsx
  - src/components/ConditionBuilder.tsx
  - src/components/InboxCard.tsx
autonomous: true
requirements: [ALR-01, ALR-04, ALR-06, ALR-07]

must_haves:
  truths:
    - "Each alert row shows Acknowledge, Mute, and Escalate action buttons"
    - "Acknowledge is single-click with optimistic UI transition to opacity-60"
    - "Mute opens a popover with duration options (15m, 1h, 4h, 24h, Indefinite)"
    - "Escalate opens a dialog pre-filled with alert title and severity-mapped priority"
    - "Each alert row shows webhook delivery status badge (delivered/retrying/failed)"
    - "AlertRulesEngine shows threshold override input and mute toggle per rule"
    - "New Custom Rule button opens AlertRuleForm Sheet"
    - "AlertRuleForm Sheet contains ConditionBuilder for compound AND/OR conditions"
    - "Alert items in Unified Inbox have acknowledge/mute inline actions"
  artifacts:
    - path: "src/components/AlertLifecycleActions.tsx"
      provides: "Row-level Acknowledge/Mute/Escalate buttons"
      contains: "AlertLifecycleActions"
    - path: "src/components/MuteDurationPicker.tsx"
      provides: "Popover with duration options"
      contains: "MuteDurationPicker"
    - path: "src/components/WebhookStatusBadge.tsx"
      provides: "Delivery status indicator (delivered/retrying/failed)"
      contains: "WebhookStatusBadge"
    - path: "src/components/AlertRuleForm.tsx"
      provides: "Sheet form for editing rules and creating custom rules"
      contains: "AlertRuleForm"
    - path: "src/components/ConditionBuilder.tsx"
      provides: "Compound AND/OR condition editor"
      contains: "ConditionBuilder"
  key_links:
    - from: "src/components/AlertLifecycleActions.tsx"
      to: "convex/alertLifecycle.ts"
      via: "useMutation for acknowledge, resolve, escalate"
      pattern: "useMutation.*alertLifecycle"
    - from: "src/components/MuteDurationPicker.tsx"
      to: "convex/alertMutes.ts"
      via: "useMutation for muteTarget"
      pattern: "useMutation.*alertMutes"
    - from: "src/components/AlertRuleForm.tsx"
      to: "convex/alertRuleCustom.ts"
      via: "useMutation for create/update custom rules"
      pattern: "useMutation.*alertRuleCustom"
    - from: "src/components/InboxCard.tsx"
      to: "convex/alertLifecycle.ts"
      via: "Acknowledge/mute inline actions on alert inbox items"
      pattern: "alertLifecycle"
---

<objective>
Alerts page lifecycle UI + AlertRuleForm + ConditionBuilder + InboxCard integration

Builds all the UI components for alert management: lifecycle action buttons on alert rows, webhook delivery status badges, AlertRuleForm sheet with ConditionBuilder, threshold overrides on AlertRulesEngine, and InboxCard alert integration.

Purpose: Operators can manage alerts (acknowledge, mute, escalate) and configure rules from the dashboard.
Output: 5 new components, 2 existing components extended, all wired to Phase 6 backend.
</objective>

<execution_context>
@C:/Users/mandr/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/mandr/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-alert-routing/06-CONTEXT.md
@.planning/phases/06-alert-routing/06-RESEARCH.md
@.planning/phases/06-alert-routing/06-UI-SPEC.md
@.planning/phases/06-alert-routing/06-02-SUMMARY.md
@.planning/phases/06-alert-routing/06-03-SUMMARY.md

<interfaces>
From convex/alertLifecycle.ts (Plan 02):
```typescript
export const acknowledgeAlert = mutation({ args: { alertId, acknowledgedBy? } });
export const resolveAlert = mutation({ args: { alertId } });
export const escalateToTask = mutation({ args: { alertId, title, description?, priority? } });
```

From convex/alertMutes.ts (Plan 02):
```typescript
export const muteTarget = mutation({ args: { targetType, targetId, duration, mutedBy? } });
export const unmuteTarget = mutation({ args: { targetType, targetId } });
export const isTargetMutedPublic = query({ args: { targetType, targetId } });
export const listActiveMutes = query({});
```

From convex/alertRuleCustom.ts (Plan 02):
```typescript
export const create = mutation({ args: { name, severity, conditions, conditionLogic, conditionGroups?, messageTemplate? } });
export const update = mutation({ args: { id, name?, severity?, conditions?, conditionLogic?, conditionGroups?, messageTemplate?, enabled? } });
export const remove = mutation({ args: { id } });
export const list = query({ args: { enabled?, severity? } });
export const setThresholdOverride = mutation({ args: { ruleId, threshold, lookbackWindow } });
export const getThresholdOverride = query({ args: { ruleId } });
```

Severity color classes (from UI-SPEC):
```
critical: text-red-400 bg-red-400/10 border-red-400/20
error: text-orange-400 bg-orange-400/10 border-orange-400/20
warning: text-yellow-400 bg-yellow-400/10 border-yellow-400/20
info: text-blue-400 bg-blue-400/10 border-blue-400/20
```

Webhook delivery status colors (from UI-SPEC):
```
delivered: --status-ok (green dot)
failed/retrying: --status-error (red dot)
pending: --status-warn (yellow dot)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: AlertLifecycleActions + MuteDurationPicker + WebhookStatusBadge + Alerts page wiring</name>
  <files>src/components/AlertLifecycleActions.tsx, src/components/MuteDurationPicker.tsx, src/components/WebhookStatusBadge.tsx, src/pages/Alerts.tsx</files>
  <read_first>src/pages/Alerts.tsx, src/components/AlertRulesEngine.tsx, src/components/ui/popover.tsx, src/components/ui/dialog.tsx, src/components/ui/button.tsx, .planning/phases/06-alert-routing/06-UI-SPEC.md</read_first>
  <action>
**Create `src/components/WebhookStatusBadge.tsx`:**
- Props: `{ status?: string; deliveredAt?: number; attempts?: number }`
- Renders:
  - `status === "delivered"`: green dot (bg-[var(--status-ok)] w-2 h-2 rounded-full) + "Delivered {relative time}" in text-xs text-muted-foreground
  - `status === "failed"`: red dot (bg-[var(--status-error)]) + "Failed after 3 attempts" in text-xs text-muted-foreground
  - `status === "pending"`: yellow dot (bg-[var(--status-warn)]) + "Retrying ({attempts}/3)" in text-xs text-muted-foreground
  - No status / undefined: render nothing
- All dots are 8px (w-2 h-2) inline-flex items

**Create `src/components/MuteDurationPicker.tsx`:**
- Props: `{ onSelect: (duration: string) => void; trigger: React.ReactNode }`
- Uses shadcn `Popover` wrapping the trigger
- Content: label "Mute for how long?" (text-sm font-medium) then 5 buttons in a vertical stack:
  - "15 minutes" (value: "15m")
  - "1 hour" (value: "1h")
  - "4 hours" (value: "4h")
  - "24 hours" (value: "24h")
  - "Indefinitely" (value: "indefinite")
- Each option is a ghost Button (text-sm, w-full, justify-start)
- Clicking an option calls `onSelect(value)` and closes the popover
- Minimum touch target: 44px height per option (accessibility)

**Create `src/components/AlertLifecycleActions.tsx`:**
- Props: `{ alertId: Id<"alerts">; alertTitle: string; alertSeverity: string; status?: string; isMuted: boolean }`
- Renders a horizontal row of action buttons:
  1. **Acknowledge** (ghost button, text-sm): visible only when `status !== "acknowledged" && status !== "resolved"`. On click: call `useMutation(api.alertLifecycle.acknowledgeAlert)({ alertId })`. Single-click, no confirmation.
  2. **Mute** (ghost button, text-sm): renders MuteDurationPicker. Trigger shows "Mute" if not muted, "Unmute" if muted. On select: call `useMutation(api.alertMutes.muteTarget)({ targetType: "alert", targetId: alertId, duration })`. On unmute: call `useMutation(api.alertMutes.unmuteTarget)({ targetType: "alert", targetId: alertId })`.
  3. **Escalate** (ghost button, text-sm): opens shadcn Dialog pre-filled with `alertTitle` as task name and severity-mapped priority. Fields: title (Input, editable), description (Textarea, optional), priority (Select: urgent/high/medium/low, default from severity map). "Create Task" primary button calls `useMutation(api.alertLifecycle.escalateToTask)({ alertId, title, description, priority })`. On success: show success toast, close dialog.

Severity to priority map: critical->"urgent", error->"high", warning->"medium", info->"low"

Copy per UI-SPEC:
- Escalate dialog button: "Create Task"
- On escalate success: alert row gains "Linked to task" badge (small text-xs bg-muted px-2 inline tag)

**Wire into `src/pages/Alerts.tsx`:**
- Import `AlertLifecycleActions`, `WebhookStatusBadge`
- In each alert row, add a right-side action cluster after existing content:
  - `<AlertLifecycleActions>` with alert props
  - `<WebhookStatusBadge>` with alert.webhookStatus, alert.webhookDeliveredAt, alert.webhookAttempts
- Acknowledged rows: add `opacity-60` class + "Acknowledged" text-xs badge
- Resolved rows: add `opacity-40` class + "Auto-resolved" text-xs badge
- Muted rows: add `opacity-50` class + clock icon (Clock from lucide-react, 4x4) + "Muted" text
- Query mute state for each visible alert using `useQuery(api.alertMutes.isTargetMutedPublic, { targetType: "alert", targetId: alert._id })`

Animation per UI-SPEC:
- Acknowledge: transition `opacity-60` 200ms ease-out
- Auto-resolved: fade to `opacity-40` then slide out after 1.5s
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/AlertLifecycleActions.tsx` exists and contains `AlertLifecycleActions`
    - `src/components/AlertLifecycleActions.tsx` contains `acknowledgeAlert` mutation call
    - `src/components/AlertLifecycleActions.tsx` contains `escalateToTask` mutation call
    - `src/components/AlertLifecycleActions.tsx` contains `Create Task`
    - `src/components/MuteDurationPicker.tsx` exists and contains `MuteDurationPicker`
    - `src/components/MuteDurationPicker.tsx` contains `Mute for how long?`
    - `src/components/MuteDurationPicker.tsx` contains `"15m"` and `"1h"` and `"4h"` and `"24h"` and `"indefinite"`
    - `src/components/WebhookStatusBadge.tsx` exists and contains `WebhookStatusBadge`
    - `src/components/WebhookStatusBadge.tsx` contains `--status-ok` and `--status-error` and `--status-warn`
    - `src/pages/Alerts.tsx` contains `AlertLifecycleActions` import
    - `src/pages/Alerts.tsx` contains `WebhookStatusBadge` import
    - `src/pages/Alerts.tsx` contains `opacity-60` (acknowledged state)
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Alert rows show lifecycle actions (acknowledge/mute/escalate) and webhook delivery status. Mute popover with 5 duration options. Escalate dialog creates Kanban task.</done>
</task>

<task type="auto">
  <name>Task 2: AlertRuleForm + ConditionBuilder + AlertRulesEngine extension + InboxCard</name>
  <files>src/components/AlertRuleForm.tsx, src/components/ConditionBuilder.tsx, src/components/AlertRulesEngine.tsx, src/components/InboxCard.tsx</files>
  <read_first>src/components/AlertRulesEngine.tsx, src/components/InboxCard.tsx, src/components/ui/sheet.tsx, src/components/ui/select.tsx, src/components/ui/accordion.tsx, src/components/ui/input.tsx, .planning/phases/06-alert-routing/06-UI-SPEC.md</read_first>
  <action>
**Create `src/components/ConditionBuilder.tsx`** (per UI-SPEC "Condition Builder" interaction pattern):
- Props: `{ conditions: Condition[]; conditionLogic: "AND" | "OR"; conditionGroups?: ConditionGroup[]; onChange: (conditions, logic, groups) => void }`
- Type `Condition = { metric: string; operator: string; threshold: number; lookbackWindow: string }`
- Type `ConditionGroup = { conditions: Condition[]; logic: "AND" | "OR" }`
- Each condition row: `[Select: metric] [Select: operator] [Input: threshold] [Select: lookbackWindow] [X button]`
  - Metric select: populated from categories in alertRules catalog (import { alertRules } from "../../convex/alertRules" or derive a static list: "cost_per_hour", "error_rate", "stall_duration", "security_blocks", "execution_failures", "latency_p95", "memory_usage")
  - Operator select: gt (>), lt (<), gte (>=), lte (<=), eq (=)
  - Threshold: numeric Input (type="number", min=0, step="any")
  - Lookback window select: "5m", "15m", "30m", "1h", "24h" (per D-03)
  - X button: removes condition row (ghost button, text-destructive, 4x4 X icon)
- Between rows: AND/OR operator pill — a small toggle button (px-2 py-xs text-xs bg-muted) that toggles `conditionLogic` between "AND" and "OR"
- "Add condition" text button at bottom (ghost, text-sm, Plus icon)
- "Add group" text button (ghost, text-sm) — one level of nesting only per UI-SPEC
- Sub-groups render indented (pl-lg) with their own AND/OR toggle and condition rows

**Create `src/components/AlertRuleForm.tsx`** (per UI-SPEC "Alert Rule Form" interaction pattern):
- Props: `{ open: boolean; onOpenChange: (open: boolean) => void; ruleId?: string; customRuleId?: Id<"alertRuleCustom">; mode: "override" | "custom" }`
- Uses shadcn Sheet (right side, 480px width via className "w-[480px]")
- **Header:** rule name (Input if custom/editable, read-only div if static override) + severity badge
- **Body:**
  - For "override" mode: Threshold override input (numeric) + lookback window Select. Shows current default as placeholder.
  - For "custom" mode: name Input, severity Select, then ConditionBuilder component, then optional message template Textarea
- **Footer:**
  - "Save Rule" primary Button — calls `setThresholdOverride` (override mode) or `create`/`update` custom rule
  - "Discard Changes" ghost Button — resets form and closes
  - "Delete Rule" destructive link (custom mode only) — shows confirmation Dialog per UI-SPEC copy: "Delete this rule? This cannot be undone. Active alerts triggered by this rule will remain in the dashboard."
- Unsaved changes: track dirty state, show dot indicator in header

**Extend `src/components/AlertRulesEngine.tsx`:**
- Add two new elements per rule row:
  1. Threshold override input: numeric Input, appears on row hover (group-hover pattern). Shows current threshold as placeholder. On blur/Enter: calls `setThresholdOverride` mutation.
  2. Mute toggle: Clock icon button (4x4, ghost) with Tooltip "Mute rule". Uses MuteDurationPicker popover. Shows muted state via opacity-50.
- Add header button: "+ New Custom Rule" (primary variant, text-sm) that opens AlertRuleForm in "custom" mode
- Add "Edit" button per row that opens AlertRuleForm in "override" mode with the rule's id
- List custom rules below static rules in a separate section with SectionHeader "CUSTOM RULES"

**Extend `src/components/InboxCard.tsx`** (per ALR-07):
- When `type === "alert"`, add two inline action buttons:
  1. "Acknowledge" ghost button (text-xs) — calls `acknowledgeAlert` mutation
  2. "Mute" ghost button (text-xs) — uses MuteDurationPicker inline
- These render in the InboxCard's trailing action slot
- Props addition: `alertId?: Id<"alerts">` for mutation calls
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/ConditionBuilder.tsx` exists and contains `ConditionBuilder`
    - `src/components/ConditionBuilder.tsx` contains `"AND"` and `"OR"` logic toggle
    - `src/components/ConditionBuilder.tsx` contains `"5m"` and `"15m"` and `"30m"` and `"1h"` and `"24h"` lookback options
    - `src/components/ConditionBuilder.tsx` contains `Add condition`
    - `src/components/AlertRuleForm.tsx` exists and contains `AlertRuleForm`
    - `src/components/AlertRuleForm.tsx` contains `Save Rule`
    - `src/components/AlertRuleForm.tsx` contains `w-[480px]` (Sheet width)
    - `src/components/AlertRulesEngine.tsx` contains `New Custom Rule`
    - `src/components/AlertRulesEngine.tsx` contains `CUSTOM RULES`
    - `src/components/AlertRulesEngine.tsx` contains `setThresholdOverride`
    - `src/components/InboxCard.tsx` contains `alertId`
    - `src/components/InboxCard.tsx` contains `acknowledgeAlert` or `alertLifecycle`
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>ConditionBuilder enables compound AND/OR rules. AlertRuleForm Sheet handles overrides and custom rules. AlertRulesEngine extended with threshold inputs, mute toggles, custom rule section. InboxCard alerts have acknowledge/mute actions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client -> Convex mutations | Operator rule configurations and lifecycle actions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-11 | Tampering | ConditionBuilder — threshold input | mitigate | Input type="number" with min=0; Convex mutation validates v.float64() |
| T-06-12 | Tampering | AlertRuleForm — custom rule injection | accept | Single operator trust model; rule names and message templates are operator-authored |
</threat_model>

<verification>
- `npx tsc --noEmit` passes
- All 5 new components exist and export named components
- AlertRulesEngine has custom rule section and threshold overrides
- InboxCard has alert action buttons
</verification>

<success_criteria>
Alert rows show full lifecycle actions. Rule management UI supports threshold overrides and custom compound rules. InboxCard alerts have inline acknowledge/mute. All components wired to Phase 6 Convex backend.
</success_criteria>

<output>
After completion, create `.planning/phases/06-alert-routing/06-05-SUMMARY.md`
</output>
