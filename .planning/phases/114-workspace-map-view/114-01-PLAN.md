---
phase: 114-workspace-map-view
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/graph/ForceGraphCanvas.tsx
  - src/components/graph/ForceGraphCanvas.test.tsx
  - src/index.css
  - src/hooks/useThemeColors.ts
  - src/hooks/useThemeColors.test.ts
autonomous: true
requirements: []   # design-doc-driven phase — traced to D-01..D-18, not REQ-IDs (Phase 116 precedent, followed by 115)

must_haves:
  truths:
    - "D-06: department fill is driven by dedicated theme tokens resolved through useThemeColors(), never a hardcoded hex in component code"
    - "D-08: ForceGraphCanvas accepts an explicit cooldownTicks override so a consumer can request a physics-off (0-tick) layout"
  artifacts:
    - path: "src/components/graph/ForceGraphCanvas.tsx"
      provides: "optional cooldownTicks prop, default 120"
      contains: "cooldownTicks"
    - path: "src/index.css"
      provides: "--dept-personal / --dept-consulting / --dept-work in :root + 4 switcher themes"
      contains: "--dept-personal"
    - path: "src/hooks/useThemeColors.ts"
      provides: "mutedForeground / deptPersonal / deptConsulting / deptWork on ThemeColors"
      contains: "deptPersonal"
  key_links:
    - from: "src/hooks/useThemeColors.ts"
      to: "src/index.css"
      via: "getComputedStyle().getPropertyValue('--dept-*')"
      pattern: "get\\(\"--dept-"
    - from: "src/components/graph/ForceGraphCanvas.tsx"
      to: "react-force-graph-2d <ForceGraph2D>"
      via: "cooldownTicks prop pass-through"
      pattern: "cooldownTicks=\\{cooldownTicks\\}"
---

<objective>
Prepare the two pieces of **shared substrate** every later Phase-114 plan depends on: an additive
`cooldownTicks` prop on `ForceGraphCanvas` (D-08 needs `0`; the file hardcodes `120` today), and the
three new department color tokens plus their `ThemeColors` fields (D-06's fill channel).

Purpose: both are edits to files other consumers already depend on (`CodeVaultGraph`, KG Explorer).
Landing them first, additively and provably non-breaking, means no later plan has to touch shared
code while also building new features.
Output: one new optional prop, three new CSS tokens across five theme blocks, four new `ThemeColors`
fields, and tests for each.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/114-workspace-map-view/114-CONTEXT.md
@.planning/phases/114-workspace-map-view/114-UI-SPEC.md
@.planning/phases/114-workspace-map-view/114-PATTERNS.md
@CLAUDE.md

**Scope guard:** the `sketch-findings-codepulse` skill and the whole v15.0 "Borealis Console"
overhaul are HELD for milestone v15.0. Phase 114 is v14.0. Do NOT load that skill and do NOT pull
in any part of that direction, quick-wins included.

**Any inline code in this plan is a DRAFT, not a spec.** If reading the real file shows the draft is
wrong, correcting it beats transcribing it — say so in the SUMMARY.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add an additive cooldownTicks prop to ForceGraphCanvas</name>
  <read_first>
    - `src/components/graph/ForceGraphCanvas.tsx` — read in full. The three edit sites are the
      exported `ForceGraphCanvasProps` interface (~`:40-86`), the props destructure block
      (~`:92-113`), and the hardcoded `cooldownTicks={120}` literal on the `<ForceGraph2D>` element
      at `:326` (verified this planning pass: `grep -n cooldownTicks` returns exactly one hit, line 326).
    - `src/components/graph/ForceGraphCanvas.test.tsx` lines 1-60 — the `vi.hoisted` + `vi.mock
      ("react-force-graph-2d", ...)` props-capturing stub and the `d3-force-3d` mock. This is the
      mechanism the new assertions use.
    - `.planning/phases/114-workspace-map-view/114-PATTERNS.md` § "The exact additive edit".
  </read_first>
  <files>src/components/graph/ForceGraphCanvas.tsx, src/components/graph/ForceGraphCanvas.test.tsx</files>
  <action>
    Add one optional prop, `cooldownTicks?: number`, to `ForceGraphCanvasProps`, documented as:
    default 120 (existing behavior, byte-identical for `CodeVaultGraph` and KG Explorer); pass 0 for a
    fully deterministic, physics-off layout per D-08. Destructure it in the props block alongside the
    other defaulted props with the default `cooldownTicks = 120`, and replace the literal at line 326
    with the pass-through `cooldownTicks={cooldownTicks}`.

    This must be **additive and backward compatible** — no existing call site changes, no other prop
    is renamed or reordered, and no other line of `ForceGraphCanvas.tsx` is touched. Do not add any
    conditional logic around the value; a consumer passing 0 gets 0, a consumer passing nothing gets 120.

    In `ForceGraphCanvas.test.tsx`, add two assertions using the existing hoisted props-capture stub:
    render with no `cooldownTicks` prop and assert the captured `h.props.cooldownTicks === 120`
    (the backward-compatibility control), then render with `cooldownTicks={0}` and assert the
    captured value is `0` — specifically `toBe(0)`, not a truthiness check, since 0 is falsy and a
    `??`/`||` default in the implementation would silently reinstate 120. That falsy-0 case is the
    only way this edit can be wrong, so the test must discriminate it.
  </action>
  <verify>
    <automated>npx vitest run src/components/graph/ForceGraphCanvas.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "cooldownTicks" src/components/graph/ForceGraphCanvas.tsx` returns exactly 3 hits:
      the interface field, the destructure default, and the JSX pass-through. No remaining bare
      `cooldownTicks={120}` literal.
    - `npx vitest run src/components/graph/ForceGraphCanvas.test.tsx` passes, including a test whose
      assertion is `expect(h.props.cooldownTicks).toBe(0)` after rendering with `cooldownTicks={0}`.
    - `npx vitest run src/components/graph/CodeVaultGraph.test.tsx` still passes unchanged (the
      backward-compatibility control — this consumer supplies no `cooldownTicks`).
    - `git diff --stat src/components/graph/ForceGraphCanvas.tsx` shows an edit of no more than ~8
      changed lines. A larger diff means something beyond the additive prop was touched.
  </acceptance_criteria>
  <done>`ForceGraphCanvas` accepts `cooldownTicks`, defaults to 120 for every existing consumer, and passes an explicitly-supplied 0 through to `<ForceGraph2D>` — proven by a `toBe(0)` assertion.</done>
</task>

<task type="auto">
  <name>Task 2: Declare the three department color tokens across :root and the four switcher themes</name>
  <read_first>
    - `src/index.css` — read the five token blocks that need the new declarations. Verified line
      anchors this planning pass: `:root` at `:62`, `[data-theme="cyan"]` at `:132`,
      `[data-theme="emerald"]` at `:190`, `[data-theme="amber"]` at `:209`, `[data-theme="readable"]`
      at `:228`, `[data-theme="aubergine"]` at `:291`. Read each block's existing
      `--muted-foreground` / `--chart-*` declarations to match the local formatting.
    - `.planning/phases/114-workspace-map-view/114-UI-SPEC.md` § Color → "Department fill — new
      dedicated tokens" — the authoritative per-theme value table, and the recorded reasoning for
      why `--chart-*` reuse is rejected (aubergine aliases all five chart slots to primary/accent/
      status colors; emerald leaves `--chart-3/4/5` undefined).
  </read_first>
  <files>src/index.css</files>
  <action>
    Add `--dept-personal`, `--dept-consulting`, and `--dept-work` to five blocks, using exactly these
    values from UI-SPEC's table:

    - `:root` (light "Paperclip") — `--dept-personal: oklch(0.65 0.12 350)`,
      `--dept-consulting: oklch(0.62 0.12 150)`, `--dept-work: oklch(0.68 0.13 55)`
    - `[data-theme="cyan"]` — `#ec4899`, `#22c55e`, `#f97316`
    - `[data-theme="emerald"]` — `#f472b6`, `#8b5cf6`, `#fb923c`
    - `[data-theme="readable"]` — `#d946ef`, `#818cf8`, `#fb923c`
    - `[data-theme="aubergine"]` — `#f472b6`, `#22d3ee`, `#fb923c`

    Place each trio next to that block's existing `--muted-foreground` declaration so it reads as
    part of the same semantic group.

    **Do NOT add these tokens to `[data-theme="amber"]`** — CLAUDE.md § Styling records that amber is
    defined in CSS but is not exposed in the theme switcher, and it already inherits its other
    undeclared tokens from `:root`. Leaving it consistent with existing practice is deliberate.

    **Do NOT add a fourth `--dept-unclassified` token.** Unclassified reuses the existing
    `--muted-foreground`, which is what "must read as muted, not a fifth peer color" (CONTEXT.md
    § Claude's Discretion) requires structurally rather than only visually.

    **Do NOT fix emerald's missing `--muted-foreground` / `--status-*` / `--chart-3/4/5`
    declarations.** UI-SPEC § "Open items for the planner" records this as a pre-existing, theme-wide
    completeness gap unrelated to Phase 114. Touching it here would widen the diff into unrelated
    themes.

    Note on CLAUDE.md's "never hardcode hex" rule: that rule governs *component code*. `src/index.css`
    is where token values are defined by design — literal hex/oklch here is correct and required.
  </action>
  <verify>
    <automated>test "$(grep -c -- '--dept-personal:' src/index.css)" = "5" && test "$(grep -c -- '--dept-consulting:' src/index.css)" = "5" && test "$(grep -c -- '--dept-work:' src/index.css)" = "5" && echo "OK: 5 declarations each"</automated>
  </verify>
  <acceptance_criteria>
    - Each of `--dept-personal:`, `--dept-consulting:`, `--dept-work:` appears exactly **5** times in
      `src/index.css` (`:root`, cyan, emerald, readable, aubergine).
    - `--dept-unclassified` appears **0** times: `grep -c -- '--dept-unclassified' src/index.css`
      returns 0.
    - The `[data-theme="amber"]` block contains no `--dept-` declaration — verify by reading the amber
      block directly, not by a whole-file grep.
    - `npm run build` succeeds (the Tailwind 4 / `@tailwindcss/vite` pipeline parses the file).
    - `git diff src/index.css` shows only added lines — zero deletions, zero modifications to existing
      declarations.
  </acceptance_criteria>
  <done>Three department tokens are declared in `:root` and all four switcher themes, amber is untouched, Unclassified has no token of its own, and the build parses the file.</done>
</task>

<task type="auto">
  <name>Task 3: Extend ThemeColors with mutedForeground and the three department fields</name>
  <read_first>
    - `src/hooks/useThemeColors.ts` — read in full (107 lines). The `ThemeColors` interface is at
      `:14-27` (12 fields today, verified this planning pass). `resolveThemeColors()` at `:41-75`
      builds the returned object via a local `get(token)` helper that calls
      `style.getPropertyValue(token).trim()`. Read the Pitfall-2 comment above it (`:36-39`): the
      `CSSStyleDeclaration` must NOT be cached, and it is not — `getComputedStyle` is called fresh
      on every `resolveThemeColors()` invocation, so new fields participate in theme-switch
      re-resolution with zero extra wiring.
    - `src/hooks/useThemeColors.test.ts` lines 1-60 — the `CYAN_TOKENS` / `READABLE_TOKENS` record
      stubs and the `makeComputedStyleStub` helper the existing tests feed through a
      `getComputedStyle` spy. New tokens must be added to those records or they resolve to `""`.
    - `src/index.css` (after Task 2) — confirm the exact token names you are reading match what was
      declared.
  </read_first>
  <files>src/hooks/useThemeColors.ts, src/hooks/useThemeColors.test.ts</files>
  <action>
    Add four fields to the `ThemeColors` interface and to the object `resolveThemeColors()` returns,
    each resolved with the existing `get("--token")` helper and nothing else:

    - `mutedForeground` ← `--muted-foreground` (Unclassified department fill)
    - `deptPersonal` ← `--dept-personal`
    - `deptConsulting` ← `--dept-consulting`
    - `deptWork` ← `--dept-work`

    Follow the interface's existing inline `// var(--token)` comment convention on each new line.
    Do NOT introduce a second `getComputedStyle` call, a module-level cache, or a memo — the whole
    point of Pitfall 2 is that resolution is re-run per invocation. Do NOT rename or reorder any
    existing field; the interface's own comment states field names are stable.

    In `useThemeColors.test.ts`, add the four new token values to both the `CYAN_TOKENS` and
    `READABLE_TOKENS` stub records (use the exact values Task 2 wrote into `src/index.css` for those
    two themes — cyan: `#ec4899`/`#22c55e`/`#f97316` + `--muted-foreground: #94a3b8`; readable:
    `#d946ef`/`#818cf8`/`#fb923c` + `--muted-foreground: #8892a4`). Then add a test asserting that
    all four new fields resolve to the cyan values on first render, **and** that they re-resolve to
    the readable values after a `data-theme` mutation — reusing the existing theme-switch test's
    mechanism. The theme-switch half is the load-bearing assertion: a field added to the interface but
    omitted from the returned object would pass a first-render-only check by resolving to `undefined`
    against a loose matcher, so assert exact string equality on each field in both states.
  </action>
  <verify>
    <automated>npx vitest run src/hooks/useThemeColors.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `ThemeColors` has exactly 16 fields (12 existing + 4 new); the 12 existing names are unchanged.
    - `grep -c 'getComputedStyle' src/hooks/useThemeColors.ts` returns 1 — no second call was added.
    - `npx vitest run src/hooks/useThemeColors.test.ts` passes, including an assertion that
      `colors.deptPersonal` equals the readable-theme value **after** a `data-theme` switch (not only
      on first render).
    - `npx tsc --noEmit` reports no error.
  </acceptance_criteria>
  <done>`useThemeColors()` exposes `mutedForeground`, `deptPersonal`, `deptConsulting`, `deptWork`; all four re-resolve on theme switch, proven by a post-mutation assertion.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CSS token layer → canvas paint code | Token values cross from stylesheet into JS strings handed to `ctx.fillStyle`. Not a security boundary (no untrusted input), but a correctness boundary — an unresolved token yields `""` and paints nothing. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-114-05 | Denial of Service (self-inflicted) | `ForceGraphCanvas` `cooldownTicks` prop | mitigate | Prop is additive with default 120; Task 1's backward-compatibility control (`CodeVaultGraph.test.tsx` still green, `toBe(120)` with no prop) proves no existing consumer's render loop changes. |
| T-114-06 | Tampering | `src/index.css` shared token blocks | mitigate | Diff must be additions only, zero deletions (Task 2 acceptance criterion) — a modified existing token would silently restyle unrelated pages. |
| T-114-SC | Tampering | npm/pip/cargo installs | accept | **Zero package installs this phase** (114-RESEARCH.md § Standard Stack: "No new packages this phase", package-legitimacy audit recorded as not applicable). No supply-chain surface to gate. |

No V2/V3/V6 surface: this plan adds no auth, session, or crypto code. No V5 surface: no client input is
parsed. V4 access control is untouched (no Convex change in this plan).
</threat_model>

<verification>
- `npx vitest run src/components/graph/ForceGraphCanvas.test.tsx src/hooks/useThemeColors.test.ts src/components/graph/CodeVaultGraph.test.tsx` — all green.
- `npx tsc --noEmit` — clean.
- `npm run build` — succeeds (proves the CSS parses).
</verification>

<success_criteria>
- `ForceGraphCanvas` has an optional `cooldownTicks` prop defaulting to 120, with a `toBe(0)` override test.
- `--dept-personal` / `--dept-consulting` / `--dept-work` each declared exactly 5 times in `src/index.css`; amber untouched; no `--dept-unclassified`.
- `ThemeColors` exposes 16 fields; the 4 new ones re-resolve on `data-theme` change.
- No existing consumer's behavior changed.
</success_criteria>

<output>
Create `.planning/phases/114-workspace-map-view/114-01-SUMMARY.md` when done.
</output>
