---
phase: 114-workspace-map-view
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - convex/graphSnapshots.ts
  - convex/graphSnapshots.test.ts
  - src/hooks/useWorkspaceMap.ts
  - src/hooks/useArmsProbe.ts
autonomous: true
requirements: []   # design-doc-driven phase — traced to D-01..D-18, not REQ-IDs

must_haves:
  truths:
    - "D-02: the workspace map is fed by a single getWorkspaceMap subscription whose three states (undefined/null/payload) reach the consumer uncoerced"
    - "D-11: the Astridr-lens empty state is driven by a live probe for kind:\"arms\" snapshot sources, never a hardcoded claim about another repo's state"
    - "D-13: graphSnapshots.listSnapshots returns a sources field, and every field it returned before is unchanged"
  artifacts:
    - path: "convex/graphSnapshots.ts"
      provides: "listSnapshots projection including sources"
      contains: "sources"
    - path: "src/hooks/useWorkspaceMap.ts"
      provides: "three-state passthrough over api.workspace.getWorkspaceMap"
      exports: ["useWorkspaceMap"]
    - path: "src/hooks/useArmsProbe.ts"
      provides: "boolean|undefined arms-presence derivation over listSnapshots"
      exports: ["useArmsProbe"]
  key_links:
    - from: "src/hooks/useWorkspaceMap.ts"
      to: "api.workspace.getWorkspaceMap"
      via: "useQuery"
      pattern: "api\\.workspace\\.getWorkspaceMap"
    - from: "src/hooks/useArmsProbe.ts"
      to: "api.graphSnapshots.listSnapshots"
      via: "useQuery + sources[].kind === \"arms\""
      pattern: "api\\.graphSnapshots\\.listSnapshots"
---

<objective>
Land the phase's **only backend change** (D-13: `listSnapshots` gains its `sources` field) and the two
thin read hooks the page consumes.

Purpose: D-11 requires the Ástríðr-lens empty state to be driven by a live probe rather than a
hardcoded sentence about another repo's state — the probe needs to know whether any snapshot source
has `kind: "arms"`, and `listSnapshots` does not currently return `sources`. D-02 requires exactly one
`getWorkspaceMap` subscription for the whole map, wrapped the way every other Convex read in this repo
is wrapped.
Output: one-field query projection change + its test, two hook files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/114-workspace-map-view/114-CONTEXT.md
@.planning/phases/114-workspace-map-view/114-PATTERNS.md
@.planning/phases/114-workspace-map-view/114-RESEARCH.md
@CLAUDE.md

**This phase does not write to Convex and does not deploy.** No `npx convex deploy` in this plan.
CLAUDE.md § "Self-Hosted Convex — Operational Rules" is why: the production backend is self-hosted,
deploys ship the whole working tree, and a shared checkout may contain another session's changes.
Deployment, if any, is decided at the operator checkpoint (plan 11), not here.

**Any inline code in this plan is a DRAFT, not a spec** — correcting it beats transcribing it.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the sources field to listSnapshots (D-13)</name>
  <read_first>
    - `convex/graphSnapshots.ts` — read the whole `listSnapshots` query. Verified this planning pass
      at `:317-329`: `args: {}`, `.collect()`, then a `.map()` projecting exactly five fields
      (`snapshotId`, `nodeCount`, `linkCount`, `generatedAt`, `updatedAt`). Also read the exported
      pure helper `selectVersionDeletes` in the same file — it is the repo's precedent for exporting
      a pure function so a test can import it directly instead of duplicating logic.
    - `convex/schema.ts:1892-1903` — the `graphSnapshots` table. `sources` is
      `v.array(v.object({ source, kind, nodeCount, linkCount, emittedNodeCount, emittedLinkCount,
      truncated }))` and already exists on every stored row, so this is a **query-projection change,
      not a schema migration**.
    - `convex/graphSnapshots.test.ts` lines 1-60 — this suite's established style: pure-logic mirrors
      and direct imports of exported helpers, **no Convex DB round-trip**.
  </read_first>
  <files>convex/graphSnapshots.ts, convex/graphSnapshots.test.ts</files>
  <action>
    Extract the row projection inside `listSnapshots`'s `.map()` into an exported pure helper in the
    same file — name it `projectSnapshotRow` — that takes a stored row and returns the six-field
    object: the five existing fields verbatim (`snapshotId`, `nodeCount`, `linkCount`, `generatedAt`,
    `updatedAt`) plus `sources: r.sources`. Have `listSnapshots` call it. Exporting the helper follows
    the `selectVersionDeletes` precedent already in this file and lets the test assert against the
    real production function rather than a hand-copied mirror, which is the stronger proof.

    Do not change `args`, do not change the `.collect()`, do not add a filter, do not add auth. D-13's
    own reasoning explicitly rejected adding a *new* public query here (CLAUDE.md § SEED-008 records
    that every public Convex function is callable with no credential, so a new public function is not
    free); extending this existing one is the whole point. Do not reopen the SEED-008 auth decision.

    Add tests to `convex/graphSnapshots.test.ts` importing `projectSnapshotRow` directly:
    (a) given a synthetic row carrying a `sources` array containing one entry with `kind: "arms"` and
    one with a different kind, the projection returns `sources` **deep-equal to the input array** —
    element order and every sub-field preserved;
    (b) the five pre-existing fields are returned with byte-identical values (the
    no-regression control);
    (c) the returned object has exactly 6 keys — assert on `Object.keys(...).sort()` so a
    stray extra field (or a dropped one) fails rather than passing a subset match.
  </action>
  <verify>
    <automated>npx vitest run convex/graphSnapshots.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "sources" convex/graphSnapshots.ts` shows `sources` present in the `listSnapshots`
      projection path (via `projectSnapshotRow`).
    - `npx vitest run convex/graphSnapshots.test.ts` passes, including an `Object.keys().sort()`
      assertion yielding exactly `["generatedAt","linkCount","nodeCount","snapshotId","sources","updatedAt"]`.
    - `npx tsc --noEmit` is clean.
    - `convex/schema.ts` is **unmodified** — `git diff --name-only` must not list it. This is a
      projection change, not a migration.
    - No `ctx.auth` call and no new `query(...)`/`mutation(...)` export was added to
      `convex/graphSnapshots.ts`.
  </acceptance_criteria>
  <done>`listSnapshots` returns `sources` alongside its five original fields, proven by a direct-import test on the exported projection helper, with the schema untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Create useWorkspaceMap and useArmsProbe</name>
  <read_first>
    - `src/hooks/useProjectGraph.ts` — the exact analog, all 28 lines. Copy its shape and its
      three-state doc comment.
    - `convex/workspace.ts:303-347` — `getWorkspaceMap`. Its return shape is this hook's contract:
      `null` when no `workspaceSnapshots` row exists (graceful skip), otherwise meta fields
      (`snapshotId`, `activeVersion`, `generatedAt`, `receivedAt`, `rootCount`, `coveredRoots`,
      `scannedRootsComplete`, `unclassifiedRootIds`, `accessDerivationOk`, `localConfigStatus`,
      `totalDirs`, `totalFiles`, `totalWithheldFiles`, `totalBytes`) plus `dirs[]` of
      `{ rootId, dirPath, department, access, fileCount, totalSize, latestMtime, withheldCount }`.
      Note `coveredRoots` is an **array of root ids, not a count** (PATTERNS.md records this
      explicitly) and `localConfigStatus` is a bare string.
    - `convex/graphSnapshots.ts` after Task 1 — the `sources` field this probe reads.
  </read_first>
  <files>src/hooks/useWorkspaceMap.ts, src/hooks/useArmsProbe.ts</files>
  <action>
    Create `src/hooks/useWorkspaceMap.ts` exporting `useWorkspaceMap(snapshotId?: string)` — a thin
    `useQuery(api.workspace.getWorkspaceMap, snapshotId ? { snapshotId } : {})` wrapper — plus an
    exported `WorkspaceMapData` type derived from the query's non-null return, following
    `useProjectGraph.ts`'s pattern. **Return the raw Convex result uncoerced**: `undefined` means the
    subscription is still resolving, `null` means resolved-but-no-snapshot (the true-empty state), an
    object means live data. Do NOT write `?? []`, `?? null`, or any other collapse — the consumer
    branches on all three, and collapsing `undefined` into `null` would render the "no snapshot yet,
    check Task Scheduler" copy during ordinary loading. Carry that rule in the doc comment.

    Create `src/hooks/useArmsProbe.ts` exporting `useArmsProbe(): boolean | undefined` —
    `useQuery(api.graphSnapshots.listSnapshots)`, returning `undefined` while the query is
    `undefined` (loading), otherwise `rows.some((r) => r.sources.some((s) => s.kind === "arms"))`.
    Guard `r.sources` against a missing/non-array value defensively (an older stored row could lack
    it) so the probe returns `false` rather than throwing — an unhandled throw here would propagate
    to the nearest error boundary. Do NOT hardcode `false`, do NOT add a comment asserting that
    Ástríðr has no ARMS inventory: D-11 exists precisely because a hardcoded claim about another
    repo's state is the line that goes stale silently and is never struck.

    Neither hook imports React state, effects, or anything from `src/components/`.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `src/hooks/useWorkspaceMap.ts` contains `api.workspace.getWorkspaceMap` and contains **no**
      `?? []` and no `?? null`: `grep -c '?? \[\]\|?? null' src/hooks/useWorkspaceMap.ts` returns 0.
    - `src/hooks/useArmsProbe.ts` contains both `api.graphSnapshots.listSnapshots` and the literal
      string `"arms"`, and contains no hardcoded `return false;` as its only return path.
    - `npx tsc --noEmit` is clean — this is the real gate, since it proves `sources` is actually on
      `listSnapshots`' generated return type (i.e. Task 1 landed) rather than the probe compiling
      against `any`.
    - Neither file imports from `src/components/`.
  </acceptance_criteria>
  <done>Both hooks exist, `useWorkspaceMap` passes all three query states through uncoerced, and `useArmsProbe` derives arms-presence live from `sources[].kind` with the type checker confirming the field exists.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Anything routable to the self-hosted Convex host → public Convex functions | `listSnapshots` is a public query. Per CLAUDE.md § SEED-008 the tailnet + LAN firewall block is the auth boundary, not per-function gating. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-114-02 | Information Disclosure | `listSnapshots` now returns `sources` (source names + counts) to any unauthenticated caller that can route to the host | accept | **Inherited, not newly introduced.** 114-RESEARCH.md § Security Domain (V4): D-13 extends an existing public query; SEED-008 already decided the tailnet + LAN firewall block is the boundary, and D-13 rejected a *new* public query precisely because a new public function is not free. `sources` carries snapshot source names and node/link counts — no path, credential, or file content. This phase does not reopen SEED-008; hardening one module's functions in isolation is the shape SEED-008 explicitly rejected. |
| T-114-07 | Denial of Service | `useArmsProbe` throwing on a row with a missing `sources` array | mitigate | Defensive array guard in the probe (Task 2) — an unhandled `useQuery`-consumer throw unmounts the React tree and blanks the page (CLAUDE.md § Patterns; Phase 110 `/analytics` and `heroStats` incidents). |
| T-114-SC | Tampering | npm/pip/cargo installs | accept | Zero package installs this phase (114-RESEARCH.md § Standard Stack). |

V2/V3/V6: no auth, session, or crypto surface added. V5: no new client-supplied query argument —
`getWorkspaceMap`'s optional `snapshotId` is pre-existing and unchanged.
</threat_model>

<verification>
- `npx vitest run convex/graphSnapshots.test.ts` — green.
- `npx tsc --noEmit` — clean (proves the generated Convex types carry `sources`).
- `git diff --name-only` does not include `convex/schema.ts`.
</verification>

<success_criteria>
- `listSnapshots` returns six fields; the original five are byte-identical.
- Both hooks exist and typecheck against the real generated API.
- No deploy was run; no schema change; no auth change.
</success_criteria>

<output>
Create `.planning/phases/114-workspace-map-view/114-02-SUMMARY.md` when done.
</output>
