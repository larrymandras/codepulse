---
phase: 86-kg-full-text-search-clustering-layout
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/kg-graph.ts
  - src/lib/kg-graph.test.ts
  - src/components/graph/ForceGraphCanvas.tsx
  - src/components/graph/ForceGraphCanvas.test.tsx
  - src/components/graph/CodeVaultGraph.tsx
autonomous: true
requirements: [KG-09]
must_haves:
  truths:
    - "The code/vault graph renders co-community nodes spatially grouped (d3-force cluster pull) AND with color-coded halos drawn around the node fill (SC#3)"
    - "A graph whose nodes carry no non-null community renders with the existing force-directed layout, no halo, no layout change (SC#4 no-regression)"
    - "communityColor(0) returns #60a5fa; communityColor(null) returns null; communityColor(8) wraps to slot 0"
  artifacts:
    - path: "src/lib/kg-graph.ts"
      provides: "COMMUNITY_PALETTE 8-slot array + communityColor() helper + KgNode.community field"
      contains: "COMMUNITY_PALETTE"
    - path: "src/components/graph/ForceGraphCanvas.tsx"
      provides: "clusterForce prop, communityColorFn halo paint, d3Force/d3ReheatSimulation on ForceGraphHandle"
      contains: "clusterForce"
    - path: "src/components/graph/CodeVaultGraph.tsx"
      provides: "clusterForce + communityColorFn wired into the code/vault ForceGraphCanvas"
      contains: "communityColor"
  key_links:
    - from: "src/components/graph/CodeVaultGraph.tsx"
      to: "src/lib/kg-graph.ts communityColor"
      via: "communityColorFn prop passed to ForceGraphCanvas"
      pattern: "communityColor"
    - from: "src/components/graph/ForceGraphCanvas.tsx"
      to: "d3-force-3d forceX/forceY/forceCollide"
      via: "useEffect-injected cluster force gated on nodes.some(n => n.community != null)"
      pattern: "forceX|forceY|forceCollide"
---

<objective>
Build the generic, data-gated community-cluster renderer (KG-09 core) inside `ForceGraphCanvas` and ship its first real value on the code/vault graph (`CodeVaultGraph`), the only surface with live `community` data today (`convex/schema.ts:1685`). Add the community palette + `communityColor()` helper + `KgNode.community` field to `kg-graph.ts`.

Purpose: KG-09 SC#3 (co-community nodes visually clustered: color halos + spatial grouping) and SC#4 (no regression for community-less graphs). This plan delivers clustering with NO cross-repo dependency — the KG graph integration (Plan 02) and the gated search lens (Plan 03) build on the primitives created here.
Output: `communityColor`/`COMMUNITY_PALETTE`, `KgNode.community`, `ForceGraphCanvas` cluster force + halo + extended `ForceGraphHandle`, and the code/vault graph rendering clusters live.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/86-kg-full-text-search-clustering-layout/86-RESEARCH.md
@.planning/phases/86-kg-full-text-search-clustering-layout/86-PATTERNS.md
@.planning/phases/86-kg-full-text-search-clustering-layout/86-UI-SPEC.md

<interfaces>
<!-- Contracts the executor needs. Extracted from live source — no codebase exploration needed. -->

ForceGraphCanvas.tsx (current, src/components/graph/ForceGraphCanvas.tsx):
- ForceGraphHandle has exactly 3 members today: centerAt, zoom, zoomToFit (L28-32).
- useImperativeHandle exposes those 3 (L96-100).
- ForceGraphCanvasProps includes paintNode?, colorFn?, onEngineStop?, focusSet?, etc. (L34-66).
- The `paint` wrapper (L141-153) delegates to a caller's `paintNode` if supplied, else `defaultPaint` (L110-139).
- BOTH callers (KnowledgeGraph + CodeVaultGraph) supply a custom paintNode — so the halo must be applied in the shared `paint` wrapper (recommended) or in each caller's paintNode. CodeVaultGraph passes paintNode at L475.

kg-graph.ts (current, src/lib/kg-graph.ts):
- ENTITY_TYPE_COLORS (10-slot, L101) + entityTypeColor() lookup (L135-137) — the SEPARATE entity-type palette; do NOT reuse for community.
- KgNode interface (L39-52): id, name, entityType, agentId, val, degree, color, attributes, synthetic.
- upsertNode (L226-250) constructs a KgNode.

d3-force-3d (installed transitive, 3.0.6): `import { forceX, forceY, forceCollide } from "d3-force-3d";`
- forceX(accessor).strength(n); forceY(accessor).strength(n); forceCollide(radiusAccessor).strength(n)

react-force-graph-2d ref accessor (confirmed in .d.ts):
- fgRef.current.d3Force(name) → get; d3Force(name, fn) → set; d3Force(name, null) → remove
- fgRef.current.d3ReheatSimulation() → restart simulation

convex/schema.ts:1685 — graphSnapshotNodes.community: v.optional(v.float64()); vault nodes emit null.
CodeVaultGraph.tsx:562-568 — already reads selectedNode.community read-only.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Community palette + communityColor() + KgNode.community in kg-graph.ts</name>
  <files>src/lib/kg-graph.ts, src/lib/kg-graph.test.ts</files>
  <read_first>
    - src/lib/kg-graph.ts (the file being modified — ENTITY_TYPE_COLORS L101, entityTypeColor L135-137, KgNode L39-52, upsertNode L226-250)
    - src/lib/kg-graph.test.ts (existing test file to extend)
    - 86-PATTERNS.md § "src/lib/kg-graph.ts" (exact additive pattern, lines 151-224)
    - 86-UI-SPEC.md § Color (the locked 8-slot community palette hex values, exact)
  </read_first>
  <behavior>
    - communityColor(0) === "#60a5fa" (slot 0)
    - communityColor(3) === "#34d399" (slot 3)
    - communityColor(8) === "#60a5fa" (wraps via community % 8)
    - communityColor(null) === null and communityColor(undefined) === null
    - communityColor(-1) returns a valid palette slot (Math.abs guards negatives)
    - COMMUNITY_PALETTE has exactly 8 entries, none equal to "#10b981" (accent) and none present in ENTITY_TYPE_COLORS (distinct encoding)
  </behavior>
  <action>
    Add `COMMUNITY_PALETTE: string[]` (the exact 8 hex values locked in 86-UI-SPEC.md § Color — slots 0..7: #60a5fa, #f472b6, #fbbf24, #34d399, #a78bfa, #22d3ee, #fb923c, #a3e635) co-located after `ENTITY_TYPE_COLORS`. Add `communityColor(community: number | null | undefined): string | null` returning `null` when `community == null`, else `COMMUNITY_PALETTE[Math.abs(community) % 8]` — mirror the module-level export style of `entityTypeColor` but null-return instead of FALLBACK_COLOR (per D-05 / RESEARCH). Add `community?: number | null` to the `KgNode` interface after `synthetic` with the documented comment (null = unclustered; undefined = API hasn't emitted). In `upsertNode`, thread `community: (seed as any)?.community ?? null` into the new-node construction so KG entities resolve to null today and light up automatically when the KG API emits it. Do NOT modify `ENTITY_TYPE_COLORS` or `entityTypeColor`. Extend `src/lib/kg-graph.test.ts` with the behavior cases above.
  </action>
  <verify>
    <automated>npx vitest run src/lib/kg-graph.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/lib/kg-graph.test.ts` passes including new communityColor + KgNode.community cases.
    - Grep assertion: `grep -n "export const COMMUNITY_PALETTE" src/lib/kg-graph.ts` returns one match; the array literal contains exactly the 8 UI-SPEC hex strings.
    - Grep assertion: `grep -n "community" src/lib/kg-graph.ts` shows the KgNode field and the upsertNode threading.
    - `npx tsc --noEmit` clean.
  </acceptance_criteria>
  <done>COMMUNITY_PALETTE + communityColor() exist and are tested; KgNode carries community threaded through upsertNode; entity-type palette untouched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ForceGraphCanvas cluster force + community halo + ForceGraphHandle d3Force members</name>
  <files>src/components/graph/ForceGraphCanvas.tsx, src/components/graph/ForceGraphCanvas.test.tsx</files>
  <read_first>
    - src/components/graph/ForceGraphCanvas.tsx (the file being modified — ForceGraphHandle L28-32, useImperativeHandle L96-100, props L34-66, paint wrapper L141-153, defaultPaint L110-139)
    - src/components/graph/ForceGraphCanvas.test.tsx (existing test file to extend)
    - src/lib/kg-graph.ts (the communityColor helper added in Task 1)
    - 86-PATTERNS.md § "src/components/graph/ForceGraphCanvas.tsx" (cluster injection pattern with all 4 CRITICAL pitfall notes, lines 228-325)
    - 86-RESEARCH.md § Pattern 1 (d3-force cluster), § Common Pitfalls 1/2/3/5, § "prefers-reduced-motion note"
    - 86-UI-SPEC.md § Color (halo geometry: stroke at size+3, lineWidth 2, globalAlpha 0.7, shadowBlur 6) + § Interaction Contract (halo under selection ring, honor opts.dimmed)
  </read_first>
  <behavior>
    - With data.nodes containing ≥1 node where community != null and clusterForce=true, the component calls fgRef.d3Force("clusterX", ...), d3Force("clusterY", ...), d3Force("clusterCollide", ...) and d3ReheatSimulation() in a useEffect (mock the ref).
    - With data.nodes where every node has community == null (or undefined) and clusterForce=true, the component does NOT register clusterX/clusterY/clusterCollide forces — it sets them to null instead (SC#4).
    - With clusterForce unset/false, no cluster force is touched regardless of community data.
    - communityColorFn returning a non-null hex causes a halo arc stroke to be drawn for that node; returning null draws no halo (assert via a mock CanvasRenderingContext2D recording arc/stroke calls).
  </behavior>
  <action>
    Extend `ForceGraphHandle` with `d3Force: (name: string, force?: any) => any` and `d3ReheatSimulation: () => void`, and expose both via `useImperativeHandle` delegating to `fgRef.current?.d3Force/d3ReheatSimulation`. Add two props to `ForceGraphCanvasProps`: `clusterForce?: boolean` and `communityColorFn?: (node: any) => string | null`. Import `forceX, forceY, forceCollide` from `d3-force-3d` (NOT `d3-force` — RESEARCH State-of-the-Art note). Add a `useEffect` keyed on `[data.nodes, clusterForce]` that: returns early if `!clusterForce` or `!fgRef.current`; computes `hasCommunity = data.nodes.some(n => n.community != null)`; when false, calls `d3Force("clusterX"|"clusterY"|"clusterCollide", null)` and returns (Pitfall 2 removal + SC#4); when true, computes per-community centroids on a ring (dynamic from the unique community ids — Pitfall: no hardcoded positions), registers `clusterX`/`clusterY` via `forceX`/`forceY` accessors that return `undefined` (NOT 0) for null-community nodes (Pitfall 3), registers `clusterCollide` via `forceCollide((node) => (node.val ?? 3) + 2)`, then calls `d3ReheatSimulation()` (Pitfall 5). Honor `prefers-reduced-motion`: when `window.matchMedia("(prefers-reduced-motion: reduce)").matches` is true, set cluster strength to 0 (or skip the reheat) — static halo color suffices. In the shared `paint` wrapper (L141-153), after delegating to `paintNode`/`defaultPaint`, if `communityColorFn` is supplied and returns a non-null hex for the node, draw the community halo: `ctx.arc(node.x, node.y, size+3, ...)` stroke with the locked geometry, honoring `opts.dimmed` alpha, drawn UNDER the selection ring layer (the caller's paintNode owns the selection ring; the halo here sits beneath the node-label layer per UI-SPEC layer order). Use `node.val ?? 3` for `size`. Reset `ctx.shadowBlur`/`ctx.globalAlpha` after the halo to avoid leaking state into the next node. Extend `ForceGraphCanvas.test.tsx` with the behavior cases (mock the imperative ref + a recording 2D context).
  </action>
  <verify>
    <automated>npx vitest run src/components/graph/ForceGraphCanvas.test.tsx && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/components/graph/ForceGraphCanvas.test.tsx` passes incl. the gate test (community present → d3Force("clusterX",...) called) AND the SC#4 test (no community → clusterX set to null, not a real force).
    - Grep assertion: `grep -n "from \"d3-force-3d\"" src/components/graph/ForceGraphCanvas.tsx` returns a match; `grep -c "d3-force\"" src/components/graph/ForceGraphCanvas.tsx` filtered with `grep -v '^#'` is 0 (must NOT import plain d3-force).
    - Grep assertion: `grep -n "d3ReheatSimulation" src/components/graph/ForceGraphCanvas.tsx` shows it on both ForceGraphHandle and useImperativeHandle.
    - `npx tsc --noEmit` clean.
  </acceptance_criteria>
  <done>ForceGraphCanvas exposes d3Force/d3ReheatSimulation, gates cluster force on community presence (SC#4 preserved), and draws community halos via communityColorFn; all pitfall guards present.</done>
</task>

<task type="auto">
  <name>Task 3: Wire clustering into the code/vault graph (first live value)</name>
  <files>src/components/graph/CodeVaultGraph.tsx</files>
  <read_first>
    - src/components/graph/CodeVaultGraph.tsx (the file being modified — imports L36-38, paintNode L264-265, ForceGraphCanvas usage L466-475, community display L562-568)
    - src/components/graph/ForceGraphCanvas.tsx (the clusterForce + communityColorFn props added in Task 2)
    - src/lib/kg-graph.ts (communityColor added in Task 1)
    - 86-PATTERNS.md § "src/components/graph/CodeVaultGraph.tsx" (lines 767-797)
    - 86-RESEARCH.md § "KgNode community Threading" (graphSnapshotNodes.community → render node)
  </read_first>
  <action>
    Confirm the project-graph node objects passed to `ForceGraphCanvas.data.nodes` carry `community` (from `graphSnapshotNodes.community`, already in schema since Phase 83). If the mapper that builds the render nodes drops `community`, thread it through so each node object exposes `community: number | null`. Import `communityColor` from `@/lib/kg-graph`. On the existing `<ForceGraphCanvas>` call site (~L470-475), add `clusterForce={true}` and `communityColorFn={(node) => communityColor(node.community)}`. Do NOT remove or alter the existing custom `paintNode` (selection ring stays caller-owned); the halo is now drawn by the shared `paint` wrapper via `communityColorFn`. Leave the read-only `community` detail-panel display (L562-568) unchanged.
  </action>
  <verify>
    <automated>npx vitest run src/components/graph/CodeVaultGraph.test.tsx && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Grep assertion: `grep -n "clusterForce" src/components/graph/CodeVaultGraph.tsx` and `grep -n "communityColorFn" src/components/graph/CodeVaultGraph.tsx` each return a match on the ForceGraphCanvas call site.
    - Grep assertion: `grep -n "communityColor" src/components/graph/CodeVaultGraph.tsx` shows the import + usage.
    - `npx tsc --noEmit` clean; existing CodeVaultGraph tests still pass.
    - MANUAL (real-data, per global verification bar — record in SUMMARY): on `/graphs` with the live code/vault snapshot, co-community nodes render spatially grouped + halo-colored; this is SC#3 observed on real data, not assumed.
  </acceptance_criteria>
  <done>The code/vault graph passes clusterForce + communityColorFn; community nodes cluster + halo on real Convex data; force-directed fallback intact for null-community (vault) nodes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Convex → CodePulse render | `graphSnapshotNodes.community` (float) flows into the cluster force + palette index. Untrusted only insofar as a malformed float could index the palette. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-86-01 | Tampering | communityColor palette index | mitigate | `Math.abs(community) % 8` clamps any int/float into [0,7]; non-numeric/null → null (no halo). No out-of-bounds read. |
| T-86-02 | Denial of Service | d3-force cluster injection | mitigate | Gate on `nodes.some(n => n.community != null)` + remove forces when absent; `d3ReheatSimulation` is bounded by existing `cooldownTicks=120`. No unbounded reflow. |
| T-86-03 | Tampering | canvas paint state leak | accept-with-control | Halo draw resets `ctx.shadowBlur`/`ctx.globalAlpha` after each node — prevents state bleed into subsequent node paints. Low risk (render-only, no data egress). |
| T-86-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs this plan (d3-force-3d + react-force-graph-2d already in node_modules — RESEARCH § Package Legitimacy Audit). |
</threat_model>

<verification>
- `npx vitest run src/lib/kg-graph.test.ts src/components/graph/ForceGraphCanvas.test.tsx src/components/graph/CodeVaultGraph.test.tsx` green.
- `npx tsc --noEmit` clean.
- Manual on-real-data: code/vault graph clusters + halos (SC#3); a community-less graph is unchanged force-directed, no halo (SC#4) — observed, recorded in SUMMARY.
</verification>

<success_criteria>
- KG-09 SC#3 satisfied on the code/vault graph (color halos + spatial clustering on real data).
- KG-09 SC#4 satisfied (no-community graphs render exactly as before — force not registered, no halo).
- communityColor / COMMUNITY_PALETTE / KgNode.community / ForceGraphHandle.d3Force primitives ready for Plan 02 (KG surface) consumption.
</success_criteria>

<output>
Create `.planning/phases/86-kg-full-text-search-clustering-layout/86-01-SUMMARY.md` when done. Record the SC#3 + SC#4 manual real-data observations explicitly.
</output>
