---
phase: 84-graphs-hub-code-vault-render
plan: 02
type: execute
wave: 2
depends_on: ["84-01"]
files_modified:
  - src/components/graph/CodeVaultGraph.tsx
  - src/components/graph/CodeVaultGraph.test.tsx
autonomous: true
requirements: [GH-02]

must_haves:
  truths:
    - "Navigating to a snapshot renders the code+vault graph via ForceGraphCanvas with code nodes emerald (#10b981) and vault nodes violet (#8b5cf6)"
    - "Code/Vault/Both filter chips narrow the rendered set client-side with no reload and never leave dangling links"
    - "A 'Showing X of Y nodes' header plus per-source chips with a 'truncated' badge show which source hit its cap"
    - "A 'stale' badge appears when the snapshot is older than 36h; an integrity warning appears only when stored counts are below emitted counts"
    - "Clicking a node opens a detail panel (id/label/type/source/community/neighbors); the null state shows the no-snapshot explainer; the loading state shows the pulse"
  artifacts:
    - path: "src/components/graph/CodeVaultGraph.tsx"
      provides: "Dual-palette code/vault graph: colorFn, source filter, truncation/freshness/integrity header, detail panel, fullscreen toggle over ForceGraphCanvas"
      contains: "CodeVaultGraph"
      min_lines: 150
  key_links:
    - from: "src/components/graph/CodeVaultGraph.tsx"
      to: "useProjectGraph"
      via: "hook call returning undefined|null|data"
      pattern: "useProjectGraph\\("
    - from: "src/components/graph/CodeVaultGraph.tsx"
      to: "ForceGraphCanvas"
      via: "data + colorFn + labelFn + onNodeClick + explicit className"
      pattern: "<ForceGraphCanvas"
---

<objective>
Build `CodeVaultGraph` — the net-new hero render (GH-02). It consumes `useProjectGraph`, renders the code/vault force graph via the existing `ForceGraphCanvas`, and owns the dual-palette `colorFn`, the Code/Vault/Both client-side filter, the per-source truncation header + freshness/integrity signals, the node-click detail panel, and the fullscreen toggle.

Purpose: This is the reason the hub exists now — the only piece of GH-02 that is construction rather than wiring. Keeping it in its own component keeps the GraphsHub page (plan 03) thin (HivePage pattern).
Output: 1 component + its filled-in test file (turns the plan 01 todos green).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/84-graphs-hub-code-vault-render/84-CONTEXT.md
@.planning/phases/84-graphs-hub-code-vault-render/84-RESEARCH.md
@.planning/phases/84-graphs-hub-code-vault-render/84-PATTERNS.md
@.planning/phases/84-graphs-hub-code-vault-render/84-UI-SPEC.md
@.planning/phases/84-graphs-hub-code-vault-render/84-VALIDATION.md
@src/hooks/useProjectGraph.ts

<interfaces>
<!-- ForceGraphCanvas props — VERIFIED src/components/graph/ForceGraphCanvas.tsx:34-63. -->
<!-- DEFAULT_COLOR = "#10b981". Default className applies h-[600px] when className is undefined — -->
<!-- so pass className EXPLICITLY for the fullscreen height switch (RESEARCH Pitfall 6). -->
ForceGraphCanvasProps {
  data: { nodes: any[]; links: any[] };
  colorFn?: (node: any) => string;
  labelFn?: (node: any) => string;
  paintNode?: (node, ctx, globalScale, opts:{hovered:boolean;dimmed:boolean}) => void;
  linkColorFn?: (link: any) => string;
  onNodeClick?: (node: any) => void;
  onNodeHover?: (node: any | null) => void;
  onBackgroundClick?: () => void;
  className?: string;   // pass explicitly for fullscreen
  backdrop?: boolean;
}
ForceGraphHandle { centerAt; zoom; zoomToFit(ms?, padding?) }

<!-- useProjectGraph (plan 01) — undefined=loading, null=no snapshot, object=ProjectGraphData. -->
<!-- ProjectGraphData fields: snapshotId, sources[], nodeCount, linkCount, storedNodeCount, -->
<!-- storedLinkCount, generatedAt (Unix SECONDS), nodes[{id,label,type,community,source}], links[{source,target,relation}]. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: CodeVaultGraph — palette, filter, states, ForceGraphCanvas integration</name>
  <files>src/components/graph/CodeVaultGraph.tsx, src/components/graph/CodeVaultGraph.test.tsx</files>
  <read_first>
    - src/pages/KnowledgeGraph.tsx (primary analog — loading L217-221, empty L223-238, error/integrity banner L159-170, detail-panel grid L182-184, legend L186-215, paintNode selection ring L59-103, ForceGraphCanvas call L240-257)
    - src/pages/ToolGalaxy.tsx (secondary analog — client-side filter pattern GAL-04, LegendDot helper L286-309, useMemo for stable graph data)
    - src/components/graph/ForceGraphCanvas.tsx lines 34-63 (props contract) + the DEFAULT_COLOR / default-className lines (Pitfall 6)
    - src/hooks/useProjectGraph.ts (the hook this component calls)
    - src/test/projectGraphFixture.ts (the fixture the test drives this with)
    - .planning/phases/84-graphs-hub-code-vault-render/84-UI-SPEC.md (Color, Layout Contract, Copywriting Contract, Legend Spec, Accessibility — the resolved discretion items)
    - .planning/phases/84-graphs-hub-code-vault-render/84-RESEARCH.md (Pitfalls 3-6 + Patterns 3-6)
  </read_first>
  <behavior>
    - Test 1 (render): with fixture data, ForceGraphCanvas is rendered (canvas region present).
    - Test 2 (loading): when the hook returns `undefined`, the "Loading graph snapshot…" pulse renders and no canvas.
    - Test 3 (empty, D-12): when the hook returns `null`, the "No graph snapshot received yet" explainer renders (Network icon + nightly-cron copy) and no canvas.
    - Test 4 (filter, D-06): selecting "Code" excludes all `vault:`-source nodes AND drops any link whose source/target is no longer present (no dangling links).
    - Test 5 (truncation, D-07): header shows "Showing {displayedNodeCount} of {nodeCount} nodes"; per-source chip text shows emitted/total; a `truncated` badge appears for a source with `truncated === true`.
    - Test 6 (freshness, D-09): with `generatedAt` older than 36h (× converted from seconds), a "stale" badge renders; with a fresh value it does not.
    - Test 7 (integrity, D-08): with `storedNodeCount < nodeCount` (or links), the dangling-drop warning banner renders; when counts match, it does NOT render.
    - Test 8 (detail panel, D-10): clicking a node opens the panel showing id, label, type, source, community (or "—"), and its direct neighbor labels.
    - Test 9 (colorFn, D-04/D-05): the color function returns `#10b981` for a `graphify:`-source node and `#8b5cf6` for a `vault:`-source node.
  </behavior>
  <action>
    Create `src/components/graph/CodeVaultGraph.tsx`. Call `useProjectGraph()` and branch on the three states: `undefined` → loading pulse (KnowledgeGraph L217-221 copy "Loading graph snapshot…"); `null` → D-12 explainer (Network h-8 w-8 text-primary/40 + heading "No graph snapshot received yet" + the nightly graph_snapshot cron body from UI-SPEC); object → the graph. Define module-level consts `CODE_COLOR = "#10b981"`, `VAULT_COLOR = "#8b5cf6"`, `STALE_THRESHOLD_MS = 36 * 60 * 60 * 1000`. `colorFn(node)` returns `VAULT_COLOR` when `node.source?.startsWith("vault:")` else `CODE_COLOR`. `labelFn(node)` returns `` `${node.label} · ${node.type} · ${node.source}` `` (D-11). Source filter: `useState<"code"|"vault"|"both">("both")` plus `filteredData` in `useMemo([snapshot, sourceFilter])` that filters nodes by `startsWith("vault:")` and then drops links whose source/target id is not in the kept-id Set (RESEARCH Pattern 4 + Pitfall: always drop dangling links). Chips: `role="group"`, each `aria-pressed`, active style `bg-primary/10 text-primary border-primary/40`. Freshness: `ageMs = Date.now() - snapshot.generatedAt * 1000` (Pitfall 4 — generatedAt is SECONDS), stale when `ageMs > STALE_THRESHOLD_MS` → amber "stale" Badge with `aria-label` per UI-SPEC. Integrity (D-08): render the red banner (KnowledgeGraph L159-170 styling, AlertTriangle text-red-500) only when `storedNodeCount < nodeCount || storedLinkCount < linkCount`; silent otherwise. Truncation header (D-07): "Showing {filteredData.nodes.length} of {snapshot.nodeCount} nodes" + map `snapshot.sources` to chips using a `sourceLabel(s)` helper that strips `graphify:` → repo segment (`s.split(":")[1]`) and `vault:` → "vault" (Pitfall 3), with a `truncated` Badge when `src.truncated`. Detail panel (D-10): `useState<string|null>(selectedNodeId)`; grid `grid-cols-1 lg:grid-cols-[1fr_320px]`; on node click set id; panel shows fields in the locked order (id mono/truncate+title, label bold, type Badge, source emerald/violet pill, community "community: {value ?? '—'}", then Neighbors list derived from links touching the node — clickable to reselect; wrap in ScrollArea if > 8); close X button MUST carry `aria-label="Close node details"`; background click clears. `paintNode` adds the selection ring keyed on `[selectedNodeId]` (KnowledgeGraph L59-103). Legend: absolute overlay (two fixed entries Code #10b981 / Vault #8b5cf6) per UI-SPEC Legend Spec. Fullscreen (D-03): `useState<boolean>(fullscreen)` + `Maximize2`/`Minimize2` ghost icon `Button` with Tooltip; pass an EXPLICIT `className` to ForceGraphCanvas derived from `fullscreen` (`h-[600px]` normal vs `h-[calc(100vh-48px)]` fullscreen, container `fixed inset-0 z-50 bg-[#09090b]` when fullscreen) — Pitfall 6; add a window `keydown` ESC `useEffect` with cleanup to exit fullscreen (let it coexist with the layout ESC handler — Pitfall 5, no stopPropagation). Use only Lucide icons (Network, Maximize2, Minimize2, AlertTriangle, Info, X) and shadcn Badge/Button/Tooltip/ScrollArea. `any` is allowed only at the ForceGraphCanvas callback boundary.
    Fill in `src/components/graph/CodeVaultGraph.test.tsx` (created in plan 01) — convert the `it.todo` placeholders to real assertions for the nine behaviors above, driving each via `makeProjectGraphFixture(...)` overrides and `mockGetProjectGraph(...)` from `src/test/projectGraphFixture.ts`. The fullscreen ESC/viewport-fill and simulation warmup behaviors are MANUAL-ONLY per 84-VALIDATION.md — do NOT assert them in jsdom; cover only the boolean state toggle (button click flips the container class string) if practical, otherwise leave fullscreen visual verification to the manual list.
  </action>
  <verify>
    <automated>npx vitest run src/components/graph/CodeVaultGraph.test.tsx src/hooks/useProjectGraph.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/graph/CodeVaultGraph.tsx` contains `const CODE_COLOR = "#10b981"`, `const VAULT_COLOR = "#8b5cf6"`, and `36 * 60 * 60 * 1000`.
    - File contains `node.source?.startsWith("vault:")` (colorFn + filter source derivation) and `snapshot.generatedAt * 1000` (Pitfall 4 seconds conversion).
    - File renders `<ForceGraphCanvas` with an explicit `className=` prop (not relying on the default) and `colorFn`, `labelFn`, `onNodeClick` props.
    - File contains `aria-label="Close node details"` and a `role="group"` filter-chip container with `aria-pressed`.
    - Integrity banner is gated by `storedNodeCount < nodeCount` / `storedLinkCount < linkCount` (grep shows the comparison; banner is not rendered unconditionally).
    - `npx vitest run src/components/graph/CodeVaultGraph.test.tsx src/hooks/useProjectGraph.test.ts` exits 0 with the nine CodeVaultGraph behaviors passing (no remaining `it.todo`/`skip` for those nine).
    - `npx tsc --noEmit` reports no new errors; no `any` outside the ForceGraphCanvas callback boundary.
  </acceptance_criteria>
  <done>Dual-palette code/vault graph renders with working filter, truncation/freshness/integrity signals, detail panel, and fullscreen toggle; nine automated behaviors green; fullscreen-visual + warmup left to the manual list.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Convex query → browser render | `CodeVaultGraph` consumes `useProjectGraph` (public read of `getProjectGraph`). All encoding (color, filter, detail) is client-side over a bounded payload. No user input crosses to the backend; no secrets; no mutation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-84-02 | Information Disclosure | code/vault graph render | accept | Read-only render of public operational graph data — same public-read posture as KG summary cards / forge.listJobs (RESEARCH.md Security Domain). No PII/secrets in node ids/labels beyond repo paths already public in this dashboard. No L1 high-severity threat. No auth gating introduced. |
| T-84-03 | Denial of Service | client-side filter / force simulation | accept | Payload is producer-capped (Phase 83 node cap); `filteredData` is O(n) memoized; ForceGraphCanvas cooldownTicks unchanged. No unbounded input. |
| T-84-SC | Tampering | npm/pip/cargo installs | mitigate | Zero new packages (RESEARCH.md Package Legitimacy Audit: none). No install step. |
</threat_model>

<verification>
- `npx vitest run src/components/graph/CodeVaultGraph.test.tsx src/hooks/useProjectGraph.test.ts` exits 0.
- `npm test` green after this wave merges.
- `npx tsc --noEmit` clean.
- Manual (per 84-VALIDATION.md): fullscreen fills viewport + ESC exits; simulation warms up and centers — verified in `npm run dev` on `/graphs` once plan 03 wires the route.
</verification>

<success_criteria>
- Code nodes render emerald, vault nodes violet, with a legend.
- Code/Vault/Both filter narrows client-side with no dangling links.
- Per-source truncation chips + "X of Y" header + freshness + integrity signals match D-07/D-08/D-09.
- Node-click detail panel + null/loading states match D-10/D-12.
</success_criteria>

<output>
Create `.planning/phases/84-graphs-hub-code-vault-render/84-02-SUMMARY.md` when done.
</output>
