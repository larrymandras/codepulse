---
phase: 122-tokens-primitives-contrast-measurement
plan: 07
type: execute
wave: 3
depends_on: ["122-03"]
files_modified:
  - src/components/TeamStatusCards.tsx
  - src/components/TokenSunburst.tsx
  - src/components/TokenWaterfall.tsx
  - src/components/ToolExecutionPanel.tsx
  - src/components/UserMenu.tsx
  - src/components/VersionHistory.tsx
  - src/components/WarRoomKanbanColumn.tsx
  - src/components/WarRoomTaskCard.tsx
  - src/components/graph/CodeVaultGraph.tsx
  - src/components/graph/ForceGraph3D.tsx
  - src/components/graph/ForceGraphCanvas.tsx
  - src/components/hr/AgentCard.tsx
  - src/components/hr/AgentDetailSheet.tsx
  - src/components/hr/CatalogCard.tsx
  - src/components/hr/RosterOrgChart.tsx
  - src/components/hr/RosterTable.tsx
  - src/components/hr/TeamCard.tsx
  - src/components/hr/TeamEditor.tsx
  - src/components/hr/detail/DetailActivityTab.tsx
  - src/components/hr/detail/DetailConfigTab.tsx
  - src/components/hr/detail/DetailVersionsTab.tsx
  - src/components/skills/RunAstridrPopover.tsx
  - src/components/skills/vault/ClusterDetailCard.tsx
  - src/components/skills/vault/SkillKanbanView.tsx
  - src/components/skills/vault/SkillVaultDetailCard.tsx
  - src/components/skills/vault/SkillVaultView.tsx
  - src/components/ui/accordion.tsx
  - src/components/ui/alert-dialog.tsx
  - src/components/ui/dialog.tsx
  - src/components/ui/sheet.tsx
  - .planning/phases/122-tokens-primitives-contrast-measurement/sweep-ledgers/122-07-LEDGER.md
autonomous: true
requirements: [TOKEN-01, TOKEN-02, TOKEN-03]

must_haves:
  truths:
    - "Slice D (src/components/ T-Z plus the graph, hr, skills and ui subtrees, 30 files) holds zero raw palette classes, zero surface hex literals, zero duration-NNN classes and zero raw violet utilities after this plan, outside entries recorded in its ledger"
    - "The four shadcn primitives in this slice keep their internal bg-popover / bg-card / border-border class strings, because D-01's whole argument is that aliasing those tokens buys all 30 primitives for free"
    - "The hr subtree's seven violet files are adjudicated per site against the violet-is-Astridr-only law rather than converted wholesale"
  artifacts:
    - path: ".planning/phases/122-tokens-primitives-contrast-measurement/sweep-ledgers/122-07-LEDGER.md"
      provides: "Per-file, per-bucket before/after counts and the per-site violet adjudication for slice D"
  key_links:
    - from: "slice D component class strings"
      to: "src/index.css theme tokens"
      via: "semantic Tailwind utilities and bracket-paren var references"
      pattern: "bg-card|bg-background|bg-popover|border-border"
---

<objective>
Sweep slice D -- the 30 files covering `src/components/` T through Z plus the `graph/`, `hr/`,
`skills/` and `ui/` subtrees -- clean of every hardcoded surface, motion literal and raw violet.

Purpose: one of five disjoint, fully parallel slices covering all 143 sweep files. This slice holds
the four shadcn primitives that still carry palette literals and the entire `hr/` violet cluster.
Output: 30 token-clean files plus a ledger recording what changed and why.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/122-tokens-primitives-contrast-measurement/122-CONTEXT.md
@.claude/skills/sketch-findings-codepulse/SKILL.md
@src/index.css
@src/components/StatusBadge.tsx
@CLAUDE.md

<interfaces>
### The conversion contract (identical across all five sweep slices)

Tokens now available from `src/index.css` (all five themes): `--surface-0/1/2/3`, `--hairline`,
`--hairline-strong`, `--astridr`, `--status-ok/warn/error/info`, `--status-error-fill`,
`--status-error-on-fill`, `--duration-fast/normal/slow`, `--ease-house`.

PREFER the existing semantic utilities: `bg-background` = `--surface-0` | `bg-card` = `--surface-1` |
`bg-popover` = `--surface-2` | `border-border` = `--hairline` | `bg-muted` / `text-muted-foreground`
for de-emphasised fills and ink. Bracket references only when no alias fits:
`bg-[var(--surface-3)]`, `border-[var(--hairline-strong)]`.

**Palette classes** -- `(bg|text|border|from|to|via)-(slate|zinc|gray|neutral|stone)-NNN`:
page -> `bg-background`; card/panel -> `bg-card`; raised/popover/dropdown/tooltip/sheet/dialog ->
`bg-popover`; hover on raised -> `bg-[var(--surface-3)]`; chip/track/skeleton -> `bg-muted`; border ->
`border-border` (emphasised -> `border-[var(--hairline-strong)]`); dim ink -> `text-muted-foreground`;
primary ink -> `text-foreground`. Opacity suffixes survive the swap.

**Hex literals** -- `(bg|border|text)-[#rrggbb]` in a className: same table. A genuinely data-driven
hex (graph node colour, chart series) is NOT a surface -- record it in the ledger as out of scope for
this bucket, naming what it is.

**Motion** -- `duration-NNN`: 75/100/150 -> `duration-fast`; 200/250 -> `duration-normal`;
300/350 -> `duration-slow`; >= 400 -> `duration-slow` AND record the old value. Add `ease-house` where
a transition carried no explicit easing or a non-house one.

**Violet** -- `(bg|text|border)-(violet|purple|fuchsia)-NNN`: adjudicate EACH site by what the element
REPRESENTS. Astridr's identity/voice/agent/memory -> `bg-[var(--astridr)]` / `text-(--astridr)` /
`border-(--astridr)/40` (the convention live in `src/components/StatusBadge.tsx:22-27`). Everything
else -> `--primary`, a `--status-*`, or a neutral. One ledger row per site.

### The shadcn boundary (D-01, load-bearing)
`src/components/ui/` holds 30 primitives; four of them (`accordion`, `alert-dialog`, `dialog`,
`sheet`) still carry a raw palette or hex class and are therefore in this slice. Convert THOSE
literals. Do NOT convert their existing `bg-popover` / `bg-card` / `border-border` class strings --
those are exactly the aliases D-01 re-pointed at the ramp, and rewriting them to
`bg-[var(--surface-N)]` is the migration D-01 explicitly rejected. The other 26 primitives are not in
this slice and must not be touched.

### Counting discipline
`grep -c` = matching LINES per file including `path:0` rows; `grep -o | wc -l` = OCCURRENCES;
`grep -l | wc -l` = FILES. Name the unit before quoting a number. Fixed-string flag for any literal
containing `#` or a backslash, typed exactly as it appears on disk -- never hand-escape.

### Files explicitly NOT in this slice
`skills/IntakeStatusBadge.tsx` is excluded (owner: plan 122-10, which rewrites its three filled
`*Map` exports under the badge law). `MetricCard.tsx`, `HeroStatsBar.tsx`, `chat/VitalsRail.tsx` and
`forge/ForgeStatusBadge.tsx` are excluded from every slice.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-derive slice D's population and open the ledger</name>
  <files>.planning/phases/122-tokens-primitives-contrast-measurement/sweep-ledgers/122-07-LEDGER.md</files>
  <read_first>
    - .planning/phases/122-tokens-primitives-contrast-measurement/122-CONTEXT.md (D-01, D-02, D-08, D-09 and the matcher-discipline note under D-25)
    - src/index.css (the five theme blocks and the `@theme`/`@utility` region)
    - src/components/ui/dialog.tsx and src/components/ui/sheet.tsx (to see which of their classes are already the aliases D-01 protects and which are raw literals to convert)
  </read_first>
  <action>
    Re-derive this slice's population from the corpus, scoped to exactly this plan's `files_modified`
    list. Do not quote CONTEXT.md's figures.

    Record per file, per bucket: the count and its UNIT. Buckets and regexes:
    palette `(bg|text|border|from|to|via)-(slate|zinc|gray|neutral|stone)-[0-9]{2,3}`;
    hex `(bg|border|text)-\[#` (fixed-string the `#`); motion `duration-[0-9]+`;
    violet `(bg|text|border)-(violet|purple|fuchsia)-[0-9]{2,3}`.

    Additionally record, for the four `ui/` files only, a fifth column: the count of existing
    `bg-popover` / `bg-card` / `border-border` occurrences. That figure must be UNCHANGED by this
    plan; it is the D-01 boundary and the ledger is where it becomes checkable.

    Create `sweep-ledgers/122-07-LEDGER.md` with a BEFORE table (one row per file, including all-zero
    files) plus empty AFTER and ADJUDICATION sections, and the regexes recorded verbatim.
  </action>
  <acceptance_criteria>
    - The BEFORE table has 30 rows, one per file in `files_modified`, including all-zero files.
    - Every count is labelled with its unit.
    - The four regexes appear verbatim, plus the fifth alias-count column for the four `ui/` files.
    - CONTROL: `src/components/graph/ForceGraph3D.tsx` is known (measured during planning) to hold a hex literal and `src/components/hr/RosterTable.tsx` a violet utility. Run those matchers against those files and confirm non-zero BEFORE trusting any zero elsewhere. Say so in the ledger.
  </acceptance_criteria>
  <verify>
    <automated>test -f .planning/phases/122-tokens-primitives-contrast-measurement/sweep-ledgers/122-07-LEDGER.md</automated>
  </verify>
  <done>Slice D's population is measured, unit-labelled, and recorded, including the shadcn alias baseline this plan must not move.</done>
</task>

<task type="auto">
  <name>Task 2: Convert every palette class and hex literal in slice D</name>
  <files>src/components/TeamStatusCards.tsx, src/components/TokenSunburst.tsx, src/components/TokenWaterfall.tsx, src/components/ToolExecutionPanel.tsx, src/components/UserMenu.tsx, src/components/VersionHistory.tsx, src/components/WarRoomKanbanColumn.tsx, src/components/WarRoomTaskCard.tsx, src/components/graph/CodeVaultGraph.tsx, src/components/graph/ForceGraph3D.tsx, src/components/graph/ForceGraphCanvas.tsx, src/components/hr/AgentCard.tsx, src/components/hr/AgentDetailSheet.tsx, src/components/hr/CatalogCard.tsx, src/components/hr/RosterOrgChart.tsx, src/components/hr/RosterTable.tsx, src/components/hr/TeamCard.tsx, src/components/hr/TeamEditor.tsx, src/components/hr/detail/DetailActivityTab.tsx, src/components/hr/detail/DetailConfigTab.tsx, src/components/hr/detail/DetailVersionsTab.tsx, src/components/skills/RunAstridrPopover.tsx, src/components/skills/vault/ClusterDetailCard.tsx, src/components/skills/vault/SkillKanbanView.tsx, src/components/skills/vault/SkillVaultDetailCard.tsx, src/components/skills/vault/SkillVaultView.tsx, src/components/ui/accordion.tsx, src/components/ui/alert-dialog.tsx, src/components/ui/dialog.tsx, src/components/ui/sheet.tsx</files>
  <read_first>
    - Each file before editing it -- read the surrounding JSX so each class is mapped by the ROLE the element plays
    - src/index.css (what each token resolves to per theme)
    - .planning/phases/120-polish-verified-defects/120-SANCTIONED-PATTERNS.md (patterns Phase 120 deliberately kept -- do not "fix" them here; `hr/CatalogCard.tsx` and `hr/TeamCard.tsx` are named there as surviving transform consumers)
  </read_first>
  <action>
    Apply the palette and hex halves of the conversion contract to all 30 files, file by file, never
    batch-sed.

    Specific care in this slice:
    - `ui/accordion.tsx`, `ui/alert-dialog.tsx`, `ui/dialog.tsx`, `ui/sheet.tsx`: convert only their
      RAW literals. Their existing `bg-popover` / `bg-card` / `border-border` strings are the D-01
      aliases and must survive byte-identical. Dialog, alert-dialog and sheet panels are raised
      surfaces -- any raw literal on their content panel maps to `bg-popover`, and their overlay
      scrim maps to a `--surface-0`-derived translucent fill, not to `bg-card`.
    - `graph/ForceGraph3D.tsx`, `graph/ForceGraphCanvas.tsx`, `graph/CodeVaultGraph.tsx`: the panel
      chrome, legend and control surfaces convert; the NODE and EDGE colours are data and are recorded
      in the ledger as out of scope for the surface bucket. Do not repaint a graph's semantics to make
      a grep go green. These three files also have per-file `vi.mock` test setups
      (`ForceGraphCanvas.test.tsx` captures props) -- run those tests specifically after editing.
    - `hr/CatalogCard.tsx` and `hr/TeamCard.tsx` are named in `120-SANCTIONED-PATTERNS.md` as
      surviving transform consumers. Their transform classes stay; only colour changes.
    - `skills/RunAstridrPopover.tsx` is a popover on an Astridr-owned surface -- its fill maps to
      `bg-popover`, and any violet in it is a strong `var(--astridr)` candidate for Task 3.

    Preserve everything that is not a colour: sizing, spacing, positioning, transform, pointer,
    visibility, opacity suffixes, `data-*`/`aria-*` attributes and conditional class logic stay
    byte-identical.

    Update the ledger's AFTER table as you go.
  </action>
  <acceptance_criteria>
    - `git grep -lE '(bg|text|border|from|to|via)-(slate|zinc|gray|neutral|stone)-[0-9]{2,3}' -- <the 30 files>` returns ZERO files, or every survivor has a dated justification row in the ledger.
    - `git grep -lE '(bg|border|text)-\[#' -- <the 30 files>` likewise returns zero, or every survivor is a ledger-recorded data-driven colour.
    - D-01 BOUNDARY CHECK: for each of the four `ui/` files, the count of `bg-popover` + `bg-card` + `border-border` occurrences is greater than or equal to its Task 1 baseline. A DECREASE means an alias was rewritten to a bracket reference -- the migration D-01 rejected -- and fails this task. Print all four before/after pairs.
    - CONTROL: run the palette command against a file OUTSIDE this slice known to still hold a palette class at this point, and confirm non-zero.
    - `npx tsc --noEmit` exits 0.
    - `npx vitest run src/components/graph` passes, and full `npx vitest run` shows zero NEW failures against the recorded pre-plan baseline; record both figures as numbers.
    - For three files chosen at random from the slice, read `git diff` in full and confirm every removed token on every changed line is a colour class. Name the three.
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run</automated>
  </verify>
  <done>Slice D holds no raw palette class and no surface hex literal outside ledger-recorded exceptions, and the shadcn alias boundary is provably intact.</done>
</task>

<task type="auto">
  <name>Task 3: Convert slice D's motion literals and adjudicate the hr violet cluster</name>
  <files>src/components/hr/AgentCard.tsx, src/components/hr/AgentDetailSheet.tsx, src/components/hr/RosterOrgChart.tsx, src/components/hr/RosterTable.tsx, src/components/hr/TeamEditor.tsx, src/components/hr/detail/DetailActivityTab.tsx, src/components/hr/detail/DetailVersionsTab.tsx, src/components/ui/accordion.tsx, src/components/ui/dialog.tsx, src/components/ui/sheet.tsx</files>
  <read_first>
    - The ledger's BEFORE table from Task 1 -- authoritative for which files hold motion or violet hits
    - src/components/StatusBadge.tsx lines 22-28 (the convention to imitate for `--astridr`)
    - .planning/phases/122-tokens-primitives-contrast-measurement/122-CONTEXT.md (D-08, D-09, D-10)
  </read_first>
  <action>
    Apply the motion and violet halves of the conversion contract to every slice-D file the ledger
    shows holding a `duration-NNN` or a raw violet utility. The `files` list above is indicative;
    the ledger is authoritative.

    Motion: map each `duration-NNN` per the contract's table and record any old value >= 400ms. The
    four `ui/` primitives carry Radix enter/exit transitions -- these are the files where `ease-house`
    matters most, because every dialog, sheet and accordion in the app inherits them.

    Violet, adjudicated per SITE: the `hr/` tree is the agent roster. An agent IS an Astridr agent, so
    a violet marking an agent's identity is a genuine `var(--astridr)` conversion; a violet used as
    one of several team/department/version accent colours is a category colour and re-hues. Do not
    convert the whole subtree on the strength of the directory name -- `hr/TeamEditor.tsx` and
    `hr/detail/DetailVersionsTab.tsx` in particular are likelier category-colour cases. Decide from
    the code. One ledger row per site: file, line, what it represents, verdict, replacement.
  </action>
  <acceptance_criteria>
    - `git grep -lE 'duration-[0-9]+' -- <the 30 slice-D files>` returns ZERO files.
    - POSITIVE control on the replacement: after `npm run build`, a fixed-string search of the built stylesheet finds a rule for whichever duration utility you used. Absence of the old classes is NOT sufficient proof -- a class matching no rule would satisfy the removal check while dropping the animation's timing.
    - `git grep -lE '(bg|text|border)-(violet|purple|fuchsia)-[0-9]{2,3}' -- <the 30 slice-D files>` returns ZERO files.
    - The ADJUDICATION section has one row per violet site found in Task 1; the row count equals the Task 1 violet occurrence count for this slice. State both numbers.
    - At least one `hr/` site is adjudicated CONVERT and at least one RE-HUE, or the ledger explains why the split is one-sided.
    - `npx tsc --noEmit` exits 0; `npx vitest run` shows zero new failures.
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run</automated>
  </verify>
  <done>Slice D's motion reads the shared tokens (proven by a rule present in the built CSS), the shadcn primitives ease on the house curve, and every hr violet site carries an individual verdict.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none introduced | Class-string edits in presentational components. No input crosses any boundary; no data flow changes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-122-07-A | Tampering | `src/components/ui/alert-dialog.tsx` | mitigate | `alert-dialog` is the primitive behind destructive confirmations (design law: destructive confirms live in dialogs, never toasts). A colour edit must not reduce the destructive affordance's distinguishability from a neutral confirm. Map its destructive action to the error tokens the token layer now provides, and read that file's full diff rather than sampling it. |
| T-122-07-B | Tampering | the four `ui/` primitives' alias strings | mitigate | Rewriting `bg-popover`/`bg-card`/`border-border` to bracket references would silently undo D-01's leverage across all 30 primitives. Mitigated by the explicit before/after alias-count check in Task 2's acceptance criteria, which fails on a DECREASE. |
| T-122-07-SC | Tampering | npm/pip/cargo installs | mitigate | Zero packages installed. RESEARCH.md's Package Legitimacy Audit records zero new packages for this phase. |

RESEARCH.md's assessment stands: no ASVS category applies.
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0; `npm run build` exits 0
- `npx vitest run` at or above the recorded baseline, zero new failures; `npx vitest run src/components/graph` passes
- All four bucket matchers return zero files across slice D, or every survivor has a ledger row
- The four `ui/` files' alias counts did not decrease
- The duration replacement is proven by a POSITIVE rule in the built CSS
- `sweep-ledgers/122-07-LEDGER.md` carries BEFORE, AFTER and per-site ADJUDICATION sections with a mixed set of violet verdicts
</verification>

<success_criteria>
Thirty files in slice D read the Borealis token layer, the shadcn alias boundary D-01 depends on is
provably untouched, and the hr roster's violet is adjudicated site by site rather than by directory
name.
</success_criteria>

<output>
Create `.planning/phases/122-tokens-primitives-contrast-measurement/122-07-SUMMARY.md` when done
</output>
