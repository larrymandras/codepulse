---
phase: 122-tokens-primitives-contrast-measurement
plan: 10
type: execute
wave: 4
depends_on: ["122-03"]
files_modified:
  - src/components/StatusBadge.tsx
  - src/components/StatusBadge.test.tsx
  - src/components/forge/ForgeStatusBadge.tsx
  - src/components/forge/ForgeStatusBadge.test.tsx
  - src/components/skills/IntakeStatusBadge.tsx
  - src/components/WarRoomTaskCard.tsx
  - .planning/phases/122-tokens-primitives-contrast-measurement/122-BADGE-LAW.md
autonomous: true
requirements: [TOKEN-02]

must_haves:
  truths:
    - "D-07: the shared StatusBadge primitive deferred to this phase is built, and emphasis is re-keyed to OPERATIONAL SEVERITY rather than to the legacy semantic bucket -- so an execution mode no longer renders like a failure and auth_failed no longer renders quiet"
    - "D-06: the Forge failed badge keeps its filled treatment and its pairing is corrected to clear 4.5:1 measured against --card in every theme"
    - "The four-tier law holds: Strong for failed / authentication failure / regression / rejected verification / stalled; Quiet-but-unmistakable for running / queued / stopping; Quietest for succeeded / completed / inactive administrative states; a SEPARATE visual grammar for execution modes"
    - "120-BADGE-INVENTORY.md's 22 consumer files and 3 Forge files were re-checked against live code before acting, not re-derived as a discovery task and not trusted as a day-old claim"
    - "The six distinct vocabularies and the two calling conventions the shared badge serves both still work: a domain status word looked up through the legacy map, and a direct semantic literal passed with a custom label"
  artifacts:
    - path: "src/components/StatusBadge.tsx"
      provides: "Severity-keyed tier table plus a separate visual grammar for execution modes"
      contains: "strict"
    - path: ".planning/phases/122-tokens-primitives-contrast-measurement/122-BADGE-LAW.md"
      provides: "The state-to-tier assignment for every entry across both badge modules, the WebhookStatusBadge adjudication, and the measured Forge failed ratios per theme"
  key_links:
    - from: "src/components/forge/ForgeStatusBadge.tsx"
      to: "--status-error-fill / --status-error-on-fill"
      via: "the failed entry's class string"
      pattern: "status-error-fill"
---

<objective>
Build the shared badge law Phase 120's D-16 deferred to this phase: emphasis keyed to operational
severity, an execution mode rendered in its own visual grammar rather than as a failure, and the one
sub-AA badge in the app corrected to clear AA while keeping its fill.

Purpose: `StatusBadge.tsx:47` maps the execution MODE `strict` to the `error` semantic, so a mode
renders filled-red alongside genuine failures, while `auth_failed` -- which is operator-actionable --
renders quiet. That inversion is what the external design review carried into this phase.
Output: both badge modules restructured, the three still-filled `IntakeStatusBadge` maps brought
under the law, and `122-BADGE-LAW.md` recording every assignment.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/122-tokens-primitives-contrast-measurement/122-CONTEXT.md
@.planning/phases/120-polish-verified-defects/120-BADGE-INVENTORY.md
@.planning/phases/120-polish-verified-defects/120-DESIGN-REVIEW-HANDOFF.md
@.planning/phases/120-polish-verified-defects/120-SANCTIONED-PATTERNS.md
@.claude/skills/sketch-findings-codepulse/SKILL.md
@src/index.css
@CLAUDE.md

<interfaces>
### `src/components/StatusBadge.tsx` today (84 lines, read it in full)

Two-level indirection: a domain word is looked up in `legacyMap` to produce a `semantic`, and the
`semantic` selects a class string from `semanticStyles`.

`semanticStyles` (`:22-28`) after Phase 120's quiet law:
- `ok`   -> `text-(--status-ok) border border-(--status-ok)/40 bg-transparent`
- `error`-> `bg-(--status-error) text-white`   (the only filled entry)
- `warn` -> `text-(--status-warn) border border-(--status-warn)/40 bg-transparent`
- `info` -> `text-(--status-info) border border-(--status-info)/40 bg-transparent`
- `idle` -> `bg-muted text-muted-foreground`

`legacyMap` (`:39-68`) contains the defect at `:47`: `strict: { semantic: "error", label: "STRICT" }`.
`adaptive` maps to `warn`, `standard` to `ok`. These are execution MODES, not outcomes.

Note the bracket-paren token syntax already in use (`text-(--status-ok)`, `border-(--status-ok)/40`)
-- it needs no `@theme` registration and is the convention to keep.

### `120-BADGE-INVENTORY.md` -- a measured work-list, NOT a discovery task (D-07)

Re-CHECK it against live code before acting; do not re-derive it, and do not trust it blind either --
it is a claim recorded a day earlier and its own header corrects the plan that produced it (19 -> 22
consumers, because the original search pattern missed the `../components/StatusBadge` relative form).

- 22 consumer files of `StatusBadge.tsx`, listed with their import lines and the vocabulary each feeds
- SIX distinct vocabularies now share this one component: job/execution status, execution mode, voice
  call, roster, swarm task, quality
- TWO calling conventions must both keep working: (a) a domain status word resolved through
  `legacyMap`; (b) a DIRECT semantic literal (`"ok"`/`"error"`/`"warn"`/`"idle"`) passed with a custom
  `label` prop, bypassing `legacyMap` entirely -- used by `CronJobList`, `FactsTable`, `WhatsApp`,
  `Security`, `Dreaming`, `Memory`
- `src/components/forge/ForgeStatusBadge.tsx`: 3 files / 5 render sites
  (`ForgeJobList.tsx:109,235`, `ForgeJobDetail.tsx:113,135`, `ForgeMetadataPanel.tsx:95`)
- `src/components/skills/IntakeStatusBadge.tsx`: THREE still-filled maps -- `ROW_STATUS_MAP`
  (`:66-96`), `SEVERITY_MAP` (`:128-144`), `VERDICT_MAP` (`:173-189`). `DestinationBadge`
  (`:225-262`) is already quiet and is NOT a target.
- `src/components/WebhookStatusBadge.tsx` (`:45-108`): renders a coloured DOT plus plain text, not a
  filled pill. The inventory explicitly asks 122 to decide whether that counts as "filled" under the
  badge law or is a separate, already-acceptable affordance. Decide and record; do not silently skip.
- `src/components/WarRoomTaskCard.tsx:66` passes `task.priority` to `StatusBadge`. `priority` values
  appear nowhere in `legacyMap`, so this site has ALWAYS rendered through the `idle` fallback with
  `status.toUpperCase()` as its label. Flagged to this phase as a probable pre-existing bug.

### The measured contrast position (`120-DESIGN-REVIEW-HANDOFF.md`)

Phase 120's quiet law IMPROVED contrast on every badge it touched: `ok` 2.43 -> 8.29, `warn`
1.92 -> 10.50, `info` 3.68 -> 5.47, Forge `auth_failed` 7.14 -> 10.50. Do not undo it.

The one exception is Forge `failed`, at 3.92:1 on the dark themes (5.33:1 on readable/aubergine) --
below AA. Its class string `bg-red-900/60 text-[var(--status-error)]` is byte-identical before and
after Phase 120, so it is pre-existing, not a regression, and the FILL is what costs the contrast.
Those figures were taken against the PAGE background; badges sit on `--card`, so the final
measurement is against `--card`. Plan 122-03 defined `--status-error-fill` and
`--status-error-on-fill` for exactly this.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-check the inventory and write the tier assignment</name>
  <files>.planning/phases/122-tokens-primitives-contrast-measurement/122-BADGE-LAW.md</files>
  <read_first>
    - .planning/phases/120-polish-verified-defects/120-BADGE-INVENTORY.md (read in full -- all four sections)
    - src/components/StatusBadge.tsx (read in full)
    - src/components/forge/ForgeStatusBadge.tsx (read in full)
    - src/components/skills/IntakeStatusBadge.tsx (read in full)
    - src/components/WebhookStatusBadge.tsx (read in full -- the adjudication target)
  </read_first>
  <action>
    Re-check the inventory against live code, then write `122-BADGE-LAW.md`.

    Re-check means: re-run the inventory's own derivation command
    (`rg --no-heading -n "from [\"'][^\"']*\bStatusBadge[\"']" src`) and compare its output to the 22
    files the document lists. Report any difference. The document is a day older than the code and
    its own header records a prior count that was wrong by three -- if your re-run and the document
    disagree, the RE-RUN is the fact and the document is the claim.

    Then assign EVERY state entry across all four badge modules to one of D-07's four tiers, and
    record the assignment with its reason:
    - **Strong (filled):** failed, authentication failure, regression, rejected verification, stalled
    - **Quiet but unmistakable:** running, queued, stopping
    - **Quietest:** succeeded, completed, inactive administrative states
    - **Separate visual grammar:** execution modes -- strict / adaptive / standard (and `filler`)

    Note where the new law CHANGES today's rendering versus where it merely restates it. The two
    changes D-07 names explicitly are `strict` (currently filled-red as an `error`, must move to the
    mode grammar) and `auth_failed` (currently quiet, must move to Strong -- it is operator-actionable
    and the handoff says concerns about it "now concern relative salience only, not legibility").

    Carry forward `120-BADGE-INVENTORY.md` §4's exceptions register verbatim. Those labels are kept
    because a spine word would make a FALSE CLAIM -- `auth_failed` mapped to Failed would claim the
    run failed when it never started; `queued` mapped to Running would claim execution began;
    `stopping_pending` mapped to Cancelled would claim the stop completed; `expired` mapped to either
    terminal word would misstate why the job ended; swarm `done` diverges from job `completed`
    deliberately. Re-keying EMPHASIS must not re-key VOCABULARY. Nothing in §4 changes label here.

    Adjudicate the two open items and record both verdicts:
    (a) `WebhookStatusBadge.tsx` -- does a saturated dot plus plain text count as "filled" under the
        badge law, or is it a separate already-acceptable affordance (the same pattern
        `ReadinessPill` and `ConnectionPopover` use)? Decide, record the reason, and act on your own
        verdict in Task 2 rather than leaving it noted.
    (b) `WarRoomTaskCard.tsx:66` -- `task.priority` reaching a status badge. Record what it renders
        today (the `idle` fallback with an uppercased priority word), and decide: give priority its
        own grammar, or stop routing it through `StatusBadge`. It is a real mismatch flagged to this
        phase, not something to note again and defer a third time.
  </action>
  <acceptance_criteria>
    - `122-BADGE-LAW.md` lists every state entry across `StatusBadge.tsx`'s `legacyMap`, `ForgeStatusBadge.tsx`'s map, and `IntakeStatusBadge.tsx`'s three maps, each with its assigned tier and a reason.
    - The consumer re-check is reported as a comparison: the re-run's file count and file list against the inventory's 22, with any difference named. "Matches" is only acceptable if both lists are printed and compared.
    - Both open adjudications (WebhookStatusBadge, WarRoomTaskCard) carry an explicit verdict with a reason -- not "noted" or "consider in a later phase".
    - The §4 exceptions register is reproduced and every label in it is marked UNCHANGED.
    - The document names, specifically, which entries CHANGE rendering under the new law and which do not. If `strict` and `auth_failed` are not both in the change list, re-read D-07 -- they are its two named cases.
  </acceptance_criteria>
  <verify>
    <automated>test -f .planning/phases/122-tokens-primitives-contrast-measurement/122-BADGE-LAW.md</automated>
  </verify>
  <done>Every badge state across four modules has a recorded tier and reason, the inventory is re-checked against live code with the comparison shown, and both open adjudications have verdicts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Restructure StatusBadge to severity tiers with a separate mode grammar</name>
  <files>src/components/StatusBadge.tsx, src/components/StatusBadge.test.tsx, src/components/WarRoomTaskCard.tsx</files>
  <read_first>
    - src/components/StatusBadge.tsx (read in full -- the two-level `legacyMap` -> `semanticStyles` indirection)
    - src/components/StatusBadge.test.tsx (read in full -- the existing assertions, including the swarm `done` control test)
    - .planning/phases/122-tokens-primitives-contrast-measurement/122-BADGE-LAW.md (Task 1's assignment)
    - src/components/BlackboardPanel.test.tsx (it asserts swarm `done` stays "DONE" -- that assertion must keep passing)
  </read_first>
  <behavior>
    - `strict`, `adaptive` and `standard` render in the MODE grammar -- visually distinct from every outcome tier, and specifically NOT the filled error treatment
    - `auth_failed` renders in the Strong tier
    - `failed` renders in the Strong tier
    - `succeeded` / `completed` render in the Quietest tier
    - `running` / `queued` / `stopping` render in the Quiet-but-unmistakable tier
    - Both calling conventions still work: a domain word resolved through the legacy map, AND a direct semantic literal with a custom `label` prop
    - Every label in `120-BADGE-INVENTORY.md` §4's exceptions register is byte-identical to before
    - Swarm `done` still reads "DONE" while job `completed` still reads "SUCCEEDED"
    - No class string in the file contains a hex literal or a raw palette class
  </behavior>
  <action>
    Restructure `StatusBadge.tsx` so emphasis is keyed to a TIER, not to the legacy semantic bucket.

    The current two-level indirection cannot express D-07's law: "execution modes are a separate
    visual grammar" is not a different value of `semantic`, it is a different axis. Introduce that
    axis -- a tier (or equivalently named) field alongside the existing semantic, so a mode can carry
    a colour without carrying an outcome's emphasis. Keep the file's existing documentation
    discipline: the comment block at `:17-21` explains the POLISH-05 law this phase supersedes;
    replace it with an equivalent block explaining the new four-tier law and naming what it changed.

    Every class string is token-driven, using the bracket-paren convention already in the file
    (`text-(--status-ok)`, `border-(--status-ok)/40`). The Strong tier is where the token layer's
    `--status-error-fill` / `--status-error-on-fill` pair belongs. No hex, no palette class.

    Preserve both calling conventions. Six vocabularies and 22 consumer files depend on this one
    component, and the direct-semantic-literal convention (`semantic="ok"` plus a custom `label`) is
    used by six of them -- a restructure that only serves the `legacyMap` path breaks those silently.

    Act on Task 1's `WarRoomTaskCard.tsx:66` verdict here. Whichever way it went, the site must stop
    rendering a priority word through the `idle` status fallback.

    Extend `src/components/StatusBadge.test.tsx` with a test per behavior line. Keep every existing
    assertion -- particularly the swarm-`done` control that proves the Phase 120 relabel was scoped
    correctly.
  </action>
  <acceptance_criteria>
    - `npx vitest run src/components/StatusBadge.test.tsx` passes, with a named test for each behavior line.
    - A test asserts `strict` does NOT render the same class string as `failed`. This is the specific defect D-07 names; assert on the rendered class, not on an intermediate lookup value.
    - A test asserts `auth_failed` DOES render the Strong tier's class string.
    - CONTROL, non-negotiable: a test asserting swarm `done` still renders "DONE" and job `completed` still renders "SUCCEEDED" -- proving the emphasis re-key did not re-key vocabulary. `src/components/BlackboardPanel.test.tsx` must also still pass unmodified.
    - A source-level test (comment lines stripped first) asserts `StatusBadge.tsx` contains no `#` colour literal and no `/-(slate|zinc|gray|neutral|stone)-[0-9]/` class.
    - MUTATION PROOF: temporarily restore `strict: { semantic: "error", ... }` to its pre-plan form; the strict-is-not-failed test must go RED. Revert, confirm GREEN. The mutation must leave the file syntactically valid.
    - Full `npx vitest run` shows zero NEW failures against the recorded pre-plan baseline. `StatusBadge.tsx` has 22 consumer files across 17 test files -- record the baseline and post-edit figures as numbers, and if any consumer test changed status, name it and say why.
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/components/StatusBadge.test.tsx src/components/BlackboardPanel.test.tsx</automated>
  </verify>
  <done>Emphasis is keyed to operational severity, execution modes have their own grammar, both calling conventions survive, and no vocabulary changed -- each proven by a named test and the strict-mapping mutation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Correct the Forge failed pairing and bring IntakeStatusBadge under the law</name>
  <files>src/components/forge/ForgeStatusBadge.tsx, src/components/forge/ForgeStatusBadge.test.tsx, src/components/skills/IntakeStatusBadge.tsx</files>
  <read_first>
    - src/components/forge/ForgeStatusBadge.tsx (read in full -- all entries, the `colorScheme` derivation, the `data-status`/`aria-label` wiring and the `animate-spin` condition)
    - src/components/forge/ForgeStatusBadge.test.tsx (read in full -- 37 existing tests)
    - src/components/skills/IntakeStatusBadge.tsx (read in full -- the three filled maps and the already-quiet `DestinationBadge`)
    - .planning/phases/120-polish-verified-defects/120-DESIGN-REVIEW-HANDOFF.md (the 3.92:1 figure and the measure-against-`--card` instruction)
    - src/index.css (the `--status-error-fill` / `--status-error-on-fill` pair defined by plan 122-03)
  </read_first>
  <behavior>
    - Forge `failed` still renders FILLED -- the treatment is locked by D-06; only the pairing changes
    - Forge `failed`'s fill and foreground come from `--status-error-fill` / `--status-error-on-fill`, not from `bg-red-900/60` and `text-[var(--status-error)]`
    - `auth_failed` renders in the Strong tier, matching Task 1's assignment
    - `data-status`, `aria-label`, the `colorScheme` derivation, the `animate-spin` condition and every icon choice are byte-identical to before
    - All five kept labels from the §4 exceptions register are unchanged
    - `IntakeStatusBadge`'s `ROW_STATUS_MAP`, `SEVERITY_MAP` and `VERDICT_MAP` entries render at their Task 1 tier; `DestinationBadge` is untouched
  </behavior>
  <action>
    Replace Forge `failed`'s `bg-red-900/60 text-[var(--status-error)]` with the token pair
    `--status-error-fill` / `--status-error-on-fill` that plan 122-03 defined and measured. Keep the
    fill: D-06 locks the filled treatment because "filled = needs attention" is the app's only
    emphasis mechanism, and the review's carried tension -- that the highest-severity badge has the
    weakest contrast -- is resolved by fixing the PAIRING, not by going quiet.

    Move `auth_failed` to the Strong tier per Task 1. Leave every other entry's Phase 120 quiet
    treatment alone: the handoff's measured data shows the quiet law improved contrast on every badge
    it touched, and undoing it would regress `auth_failed` from 10.50:1.

    Do not disturb `colorScheme`, `data-status`, `aria-label`, the `animate-spin` condition or any
    icon. `120-BADGE-INVENTORY.md` records these as byte-identical through Phase 120 and they carry
    accessibility and test-selector weight.

    Then bring `IntakeStatusBadge.tsx`'s three filled maps under the law at their Task 1 tiers. Their
    entries currently use `bg-[var(--status-*)]/20` and `bg-zinc-800/*`; the `bg-zinc-800` literals are
    also a TOKEN-01 violation the sweep slices deliberately excluded so this plan could handle the
    whole file coherently. `DestinationBadge` already wraps `ui/badge.tsx` `variant="outline"` with no
    status colour -- leave it.

    Extend `ForgeStatusBadge.test.tsx` with a test per behavior line.
  </action>
  <acceptance_criteria>
    - `npx vitest run src/components/forge/ForgeStatusBadge.test.tsx` passes -- all 37 pre-existing tests plus the new ones. Record before and after test counts.
    - `git grep -cF 'bg-red-900/60' -- src/components/forge/ForgeStatusBadge.tsx` returns 0.
    - `git grep -cF 'status-error-fill' -- src/components/forge/ForgeStatusBadge.tsx` returns at least 1.
    - `git grep -cE 'bg-(slate|zinc|gray|neutral|stone)-[0-9]{2,3}|-\[#' -- src/components/skills/IntakeStatusBadge.tsx` returns 0.
    - A test asserts `failed` still renders a FILLED treatment -- i.e. its class string carries a background, not `bg-transparent`. The whole risk of this edit is quietly turning the one deliberately-filled badge quiet.
    - MEASURED, not asserted: re-run the rasterised probe from plan 122-03 against the NEW pairing as it actually renders (a real `failed` badge on a real `--card` surface in each of the four exposed themes) and record the four ratios. Each must exceed 4.5:1. Pair it with a CONTROL run against the OLD pairing that must report below 4.5:1 -- a probe that passes the known-failing pair is broken and none of its numbers may be quoted. Append all eight figures to `122-BADGE-LAW.md`.
    - `data-status` and `aria-label` are unchanged: `git diff -- src/components/forge/ForgeStatusBadge.tsx` shows no change to either. Read the diff; do not assert on a count.
    - `npx tsc --noEmit` exits 0; full `npx vitest run` shows zero new failures against the recorded baseline.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/components/forge/ForgeStatusBadge.test.tsx</automated>
  </verify>
  <done>The app's one sub-AA badge clears AA against `--card` in every theme while still rendering filled, proven by a rasterised measurement whose control run reports the old pairing as failing, and the three remaining filled maps are under the law.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| domain status value -> badge rendering | An untrusted-in-shape status string (a value this component does not control the vocabulary of) selects a visual treatment. `WarRoomTaskCard` proves the failure mode: an unrecognised word silently renders through the `idle` fallback. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-122-10-A | Spoofing (of system state, to the operator) | `StatusBadge.tsx` unknown-status fallback | mitigate | An unrecognised status word rendering through the quiet `idle` treatment makes an unknown state look like an inactive one -- exactly what `WarRoomTaskCard.tsx:66` has been doing with `task.priority`. Task 1 adjudicates it and Task 2 acts on the verdict, so the site stops rendering a priority word as a status. |
| T-122-10-B | Information Disclosure (failure to disclose) | Forge `failed` badge | mitigate | The highest-severity badge in the app is currently below AA at 3.92:1. Mitigated by the new token pairing, measured against `--card` in every theme, with a control run on the old pairing proving the probe can report a failure. |
| T-122-10-C | Repudiation | the §4 exceptions register | mitigate | Collapsing `auth_failed` to "Failed", `queued` to "Running", `stopping_pending` to "Cancelled" or `expired` to a terminal word would each assert a state transition that did not happen. Mitigated by re-keying EMPHASIS only and asserting every §4 label is byte-identical. |
| T-122-10-SC | Tampering | npm/pip/cargo installs | mitigate | Zero packages installed. RESEARCH.md's Package Legitimacy Audit records zero new packages for this phase. |

RESEARCH.md's assessment stands: no ASVS category applies -- presentational components, no new input
surface, no auth change, no external data ingestion, no new dependency.
</threat_model>

<verification>
- `npx vitest run` shows zero new failures against the recorded baseline; `ForgeStatusBadge.test.tsx` and `BlackboardPanel.test.tsx` pass
- `strict` no longer renders the failure treatment; `auth_failed` now renders Strong -- both proven by named tests
- The strict-mapping mutation turns its test RED and reverting turns it GREEN, and the mutation left the file syntactically valid
- Forge `failed` is still filled and measures above 4.5:1 against `--card` in all four exposed themes, with the old-pairing control reporting below it
- No §4 label changed; `data-status` and `aria-label` unchanged
- `IntakeStatusBadge.tsx` holds no palette class and no hex literal
- `122-BADGE-LAW.md` records every tier assignment, the inventory re-check comparison, both adjudications, and all eight measured ratios
</verification>

<success_criteria>
Badge emphasis reflects what an operator must act on rather than which legacy bucket a word fell into,
an execution mode is visibly a mode, and the app's one sub-AA badge clears AA against the surface it
actually sits on -- with the measurement's own probe proven able to report a failure.
</success_criteria>

<output>
Create `.planning/phases/122-tokens-primitives-contrast-measurement/122-10-SUMMARY.md` when done
</output>
