---
phase: 122-tokens-primitives-contrast-measurement
plan: 13
type: execute
wave: 5
depends_on: ["122-09"]
files_modified:
  - src/components/MetricCard.tsx
  - src/components/MetricCard.test.tsx
autonomous: true
requirements: [TOKEN-04]

must_haves:
  truths:
    - "D-13: MetricCard is rewritten IN PLACE rather than replaced by a parallel MetricTile, so all its consumer files inherit the six-state contract with no import churn and there is never a window where two metric primitives coexist"
    - "D-13: the rewrite strips glow-card, the hardcoded text-white, the hardcoded text-emerald-500 / text-red-500 trend colours and the inline rgba box-shadow as part of the same token sweep"
    - "D-14: the caller declares state and the tile is pure presentation -- it never infers, and every existing prop stays source-compatible with state added"
    - "The tile renders all six states -- loading, ready, empty, stale, unavailable, error -- reading its copy, icon and tone from the shared state module rather than defining any of its own"
    - "The loading state renders a skeleton shaped like the metric it replaces, not a generic bar and not the word Loading"
  artifacts:
    - path: "src/components/MetricCard.tsx"
      provides: "The six-state metric tile every consumer inherits"
      contains: "MetricState"
      min_lines: 80
  key_links:
    - from: "src/components/MetricCard.tsx"
      to: "src/lib/metricState.ts"
      via: "import of the shared six-state copy/icon/tone table"
      pattern: "from \"@/lib/metricState\""
---

<objective>
Rewrite `MetricCard.tsx` in place to the six-state contract, reading its state vocabulary from the
shared module and its colour from the token layer.

Purpose: D-13 chose an in-place rewrite over a parallel primitive precisely because this component is
already the shared metric surface across the app -- every consumer inherits the contract with zero
import churn, and there is never a moment where two competing metric tiles exist.
Output: one rewritten primitive, source-compatible with every existing caller, with a `state` prop
added and a test per state.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/122-tokens-primitives-contrast-measurement/122-CONTEXT.md
@.planning/phases/122-tokens-primitives-contrast-measurement/122-PATTERNS.md
@.claude/skills/sketch-findings-codepulse/SKILL.md
@src/lib/metricState.ts
@src/hooks/useMetricState.ts
@src/components/ui/skeleton.tsx
@src/index.css
@CLAUDE.md

<interfaces>
### The prop surface that must survive (`MetricCard.tsx:53-64`, confirmed by direct read)

```
interface MetricCardProps {
  label: string;
  value: string | number;
  numericValue?: number;
  trend?: "up" | "down" | "neutral";
  severity?: "critical" | "error" | "warning" | "info" | "default";
  threshold?: ThresholdConfig;
  format?: (v: number) => string;
  onClick?: () => void;
  sparklineData?: number[];
}
```
D-13 requires `state` to be ADDED and every prop above to stay source-compatible. Both import forms
are live in the codebase (`import MetricCard from "../components/MetricCard"` and
`from "@/components/MetricCard"`), so the default export must stay a default export.

### What D-13 explicitly strips, all confirmed present at these lines

- `glow-card` -- `:105`, in `className="glow-card bg-card/60 backdrop-blur-md ..."`
- hardcoded `text-white` -- `:141`, `className="text-3xl font-medium tracking-tight text-white"`
- hardcoded trend colours -- `:88-90`, `trend === "up" ? "text-emerald-500" : trend === "down" ?
  "text-red-500" : "text-muted-foreground"`
- the inline `rgba()` box-shadow -- `:100`, `const restCardShadow = "0 0 15px rgba(255,255,255,0.02)"`.
  Note `hoverCardShadow` on the next line uses `color-mix(... transparent)` and is ALREADY
  token-driven -- only `restCardShadow`'s literal is the target. Do not delete the hover shadow on the
  strength of being next to the one that goes.

The file also imports `motion/react` and drives `AnimatedNumber` (`:145`) through a
`useSpring({ duration: 400 })`. Per D-09 that 400ms literal is a motion literal like any other and
must read a token; per the design law a value counter is not an ambient loop, so it takes
`--duration-slow` rather than keeping its own number. If you judge 320ms too fast for a counting
animation, say so explicitly and record the reasoning rather than silently keeping 400.

### Counting discipline for the consumer figure
CONTEXT.md's D-13 says "36 files". Re-derive it and name the unit before quoting anything: measured
during planning, `git grep -l 'MetricCard' -- 'src/*.tsx' | grep -v '\.test\.'` gives 31 FILES that
mention it, and `git grep -lE '<MetricCard'` gives 24 FILES that actually RENDER it. Those are three
different populations. Which one you need depends on the question; state which you measured.

### Design law binding this rewrite
Depth comes from shadow, never glow -- which is exactly why `glow-card` and the rest shadow go.
Skeletons are shaped like the content they replace, never generic bars. `"no signal yet"` is the
fixed empty phrasing and it lives in the shared module, not here. Hero numerals are 40px / weight 300
/ tabular.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rewrite MetricCard to the six-state contract</name>
  <files>src/components/MetricCard.tsx, src/components/MetricCard.test.tsx</files>
  <read_first>
    - src/components/MetricCard.tsx (read the whole file before rewriting it -- 159 lines)
    - src/lib/metricState.ts and src/hooks/useMetricState.ts (created by plan 122-09 -- the vocabulary this tile renders)
    - src/components/ui/skeleton.tsx (the `bg-muted` placeholder idiom for the loading state)
    - .planning/phases/122-tokens-primitives-contrast-measurement/122-CONTEXT.md (D-13, D-14)
  </read_first>
  <behavior>
    - `state="ready"` renders the value, and is the default when no `state` prop is supplied so every existing caller keeps working untouched
    - `state="loading"` renders a skeleton shaped like the tile's own content -- a label-width block and a numeral-width block -- and renders the word "Loading" nowhere
    - `state="empty"` renders exactly "no signal yet", sourced from the shared module
    - `state="stale"` renders the value AND a stale affordance -- the figure is shown, not hidden, because a stale number is still information
    - `state="unavailable"` renders the unavailable copy and does NOT render an em dash as though it were a value
    - `state="error"` renders the error affordance without throwing and without duplicating `SectionErrorBoundary`'s own fallback chrome
    - Every one of `label`, `value`, `numericValue`, `trend`, `severity`, `threshold`, `format`, `onClick`, `sparklineData` still behaves as before
    - The component defines no copy string of its own for any state
  </behavior>
  <action>
    Rewrite `MetricCard.tsx` in place. Keep the default export and the entire existing prop surface;
    add an optional `state` prop typed as `MetricState`, defaulting to `"ready"`. The default is what
    makes the rewrite non-breaking for callers plan 122-14 has not migrated yet.

    The tile is PURE PRESENTATION (D-14). It never infers its state, never calls `useQuery`, never
    checks whether a value looks empty. The caller decides -- usually via `useMetricState`.

    Every state's copy, icon and tone come from `src/lib/metricState.ts`. This component defines no
    strings of its own. That is what makes the same backend condition read identically here at tile
    scale and in `EmptyState` at panel scale.

    Strip, in the same pass: `glow-card` (`:105`), `text-white` (`:141`), the
    `text-emerald-500`/`text-red-500` trend literals (`:88-90`), and the `restCardShadow` inline
    `rgba()` (`:100`). Keep `hoverCardShadow` -- it is already `color-mix`-driven. Trend colours read
    `--status-ok` / `--status-error` (or the trend-appropriate tokens), never a palette class. Replace
    the removed glow with a real elevation shadow if the surface needs one.

    Move the `useSpring({ duration: 400 })` literal onto the motion tokens.

    The numeral treatment follows the design law: 40px / weight 300 / tabular for the hero figure.

    Rewrite `src/components/MetricCard.test.tsx` (or create it) with a named test per behavior line,
    RED before GREEN. Mock heavy render libraries per file per this repo's convention.
  </action>
  <acceptance_criteria>
    - `npx vitest run src/components/MetricCard.test.tsx` passes with one named test per behavior line -- six state tests plus the prop-compatibility tests.
    - BACKWARD-COMPATIBILITY CONTROL: a test renders `MetricCard` with the exact pre-rewrite prop set and NO `state` prop, and asserts the value renders. This is the assertion that protects every unmigrated caller.
    - `git grep -cF 'glow-card' -- src/components/MetricCard.tsx` returns 0.
    - `git grep -cF 'text-white' -- src/components/MetricCard.tsx` returns 0.
    - `git grep -cE 'text-(emerald|red)-[0-9]{3}' -- src/components/MetricCard.tsx` returns 0.
    - `git grep -cF 'rgba(' -- src/components/MetricCard.tsx` returns 0.
    - CONTROL that the hover shadow survived: `git grep -cF 'color-mix' -- src/components/MetricCard.tsx` is UNCHANGED from its pre-edit value. State both numbers -- the risk is deleting the already-correct shadow alongside the literal one.
    - `git grep -cE 'duration: 400|duration-[0-9]+' -- src/components/MetricCard.tsx` returns 0.
    - A source-level test asserts none of the shared module's six default copy strings appear as literals in `MetricCard.tsx` -- the centralisation made checkable rather than only stated.
    - A test asserts `state="loading"` renders no text node matching `/Loading/i`.
    - MUTATION PROOF: temporarily inline `"no signal yet"` into `MetricCard.tsx` instead of reading it from the module; the centralisation test must go RED. Revert, confirm GREEN. The mutation must leave the file syntactically valid -- a parse error would read exactly like the guard firing.
    - `npx tsc --noEmit` exits 0. Full `npx vitest run` shows zero NEW failures against the recorded baseline; this component is rendered by 24 files, so record baseline and post-rewrite figures as numbers and name any consumer test whose status changed.
    - `npm run build` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/components/MetricCard.test.tsx && npx tsc --noEmit</automated>
  </verify>
  <done>One metric primitive renders all six states from the shared vocabulary, keeps every existing prop working with `state` defaulting to ready, and carries no glow, no hardcoded colour and no motion literal.</done>
</task>

<task type="auto">
  <name>Task 2: Re-derive the consumer population and record the migration surface</name>
  <files>src/components/MetricCard.test.tsx</files>
  <read_first>
    - src/components/MetricCard.tsx (as rewritten in Task 1)
    - .planning/phases/122-tokens-primitives-contrast-measurement/122-CONTEXT.md (D-13's "36 files" figure and D-15's reach)
  </read_first>
  <action>
    Re-derive the consumer population that plan 122-14 will migrate, and name the unit for each figure
    rather than quoting CONTEXT.md's number. Measure and report all three, each labelled:
    - files that MENTION `MetricCard` (`git grep -l 'MetricCard' -- 'src/*.tsx'`, non-test) -- FILES
    - files that RENDER it (`git grep -lE '<MetricCard'`, non-test) -- FILES
    - render SITES (`git grep -oE '<MetricCard' | wc -l`, non-test) -- OCCURRENCES
    State plainly which figure D-13's "36" corresponds to, or that it corresponds to none of them and
    what the discrepancy is. Do not reconcile it silently.

    Add a guard test to `MetricCard.test.tsx` that will keep this honest as callers migrate: assert
    that the component's `state` prop accepts every member of `MetricState` and rejects nothing that
    the shared module defines. Derive the list from the module at test time (`Object.keys`) rather
    than writing the six names into the test -- an enumerated list can only ratify today's six, and a
    seventh state added later would slip past it unrendered.

    Write the three figures into this plan's SUMMARY so plan 122-14 starts from a measured population
    rather than from a quoted one.
  </action>
  <acceptance_criteria>
    - Three figures reported, each with its unit named, plus an explicit statement about D-13's "36".
    - The guard test derives its state list from `Object.keys` on the shared module, not from a literal array in the test file. Prove it by reading the test source.
    - MUTATION PROOF for the guard: add a synthetic seventh state to the shared module temporarily; the guard test must FAIL (because the component renders nothing for it) rather than pass. Revert. This is the difference between a ratchet and a list -- an enumerated test would have passed. Both mutations must be syntactically valid.
    - `npx vitest run src/components/MetricCard.test.tsx` passes after the revert.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/components/MetricCard.test.tsx</automated>
  </verify>
  <done>The consumer population is measured with named units rather than quoted, and a corpus-derived guard proves the tile covers whatever states the shared module defines rather than the six that exist today.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller-declared `state` -> rendered assertion | The tile renders whatever state it is told. That is deliberate (D-14) and it moves the trust to the caller, which is where the knowledge actually is. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-122-13-A | Spoofing (of system state, to the operator) | `MetricCard` default `state` | mitigate | Defaulting `state` to `"ready"` keeps unmigrated callers working, but it also means an unmigrated tile still renders whatever value it is handed as though it were current. Mitigated by plan 122-14 migrating every render site and by plan 122-17's ratchet failing on any tile left without an explicit state -- the default is a migration bridge, not an end state, and the ratchet is what stops it becoming one. |
| T-122-13-B | Repudiation | `stale` rendering | mitigate | Hiding a stale figure would be as dishonest as presenting it as current. Mitigated by the behavior rule that `stale` renders the value AND a stale affordance, asserted by its own test. |
| T-122-13-SC | Tampering | npm/pip/cargo installs | mitigate | Zero packages installed. RESEARCH.md's Package Legitimacy Audit records zero new packages for this phase. |

RESEARCH.md's assessment stands: no ASVS category applies -- a presentational component, no new input
surface, no auth change, no external data ingestion, no new dependency.
</threat_model>

<verification>
- `npx vitest run src/components/MetricCard.test.tsx` passes with a named test per state and the backward-compatibility control
- Both mutation proofs were run, went RED, were reverted, went GREEN, and both mutations were syntactically valid
- No `glow-card`, `text-white`, palette trend colour, `rgba(` or motion literal survives in the file; the `color-mix` hover shadow count is unchanged
- `npx tsc --noEmit` exits 0; `npm run build` exits 0; full suite at or above the recorded baseline with any changed consumer test named
- Three consumer-population figures reported with units, and D-13's "36" reconciled explicitly
</verification>

<success_criteria>
The app's shared metric surface renders six explicit states from one vocabulary, never infers any of
them, breaks no existing caller, and carries no glow, no hardcoded colour and no motion literal -- with
its state coverage proven by a guard that fails on a state the module defines and the tile does not
render.
</success_criteria>

<output>
Create `.planning/phases/122-tokens-primitives-contrast-measurement/122-13-SUMMARY.md` when done
</output>
