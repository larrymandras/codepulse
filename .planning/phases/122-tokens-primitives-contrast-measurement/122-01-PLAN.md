---
phase: 122-tokens-primitives-contrast-measurement
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - e2e/theme-contrast.spec.ts
  - .planning/phases/122-tokens-primitives-contrast-measurement/122-CONTRAST-BASELINE.md
  - .planning/phases/122-tokens-primitives-contrast-measurement/a11y-before/
autonomous: true
requirements: [A11Y-01]

must_haves:
  truths:
    - "D-21: the contrast matrix is measured BEFORE any src/index.css edit lands, and that before-run is the control against which the after-run's delta is read"
    - "D-22: axe-core is the population measure; no computed colour string is ever parsed to produce a contrast number"
    - "D-24: the artifact names the sampling limit explicitly -- 5 of ~47 routes measured -- and lists the unmeasured routes by name"
    - "A11Y-01's before-capture is committed with raw per-cell violation JSON, not only a summary count"
    - "The fee96b5d Clerk gate guard survives untouched: a gated run still test.skip()s rather than reporting a vacuous pass"
  artifacts:
    - path: "e2e/theme-contrast.spec.ts"
      provides: "4x5 axe matrix with per-cell JSON capture that runs BEFORE the assertion"
      contains: "A11Y_CAPTURE_DIR"
    - path: ".planning/phases/122-tokens-primitives-contrast-measurement/122-CONTRAST-BASELINE.md"
      provides: "The before-measurement table, the sampling-limit statement and the named unmeasured routes"
      contains: "BEFORE"
    - path: ".planning/phases/122-tokens-primitives-contrast-measurement/a11y-before/"
      provides: "Raw per-cell axe violation JSON, one file per theme x page cell"
  key_links:
    - from: "e2e/theme-contrast.spec.ts"
      to: ".planning/phases/122-tokens-primitives-contrast-measurement/a11y-before/"
      via: "fs.writeFileSync keyed on process.env.A11Y_CAPTURE_DIR, executed before expect()"
      pattern: "A11Y_CAPTURE_DIR"
---

<objective>
Capture the A11Y-01 "before" contrast measurement across the full 4 themes x 5 pages matrix, on
Phase 120's clean surface, BEFORE a single byte of `src/index.css` changes.

Purpose: this run is the CONTROL. D-21 is explicit — without a before-figure, a token rewrite that
made contrast WORSE is indistinguishable from one that fixed it, which is exactly the failure mode
the withdrawn Phase 120 measurement turned on. Every later claim in this phase about contrast
improving rests on this artifact existing first.
Output: `e2e/theme-contrast.spec.ts` gains per-cell JSON capture; `a11y-before/` holds 20 raw
violation JSON files; `122-CONTRAST-BASELINE.md` carries the before table and the named sampling
limit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/122-tokens-primitives-contrast-measurement/122-CONTEXT.md
@.planning/phases/122-tokens-primitives-contrast-measurement/122-VALIDATION.md
@.planning/phases/120-polish-verified-defects/120-DESIGN-REVIEW-HANDOFF.md
@CLAUDE.md

<interfaces>
`e2e/theme-contrast.spec.ts` today (72 lines, read it in full before editing):

- `THEMES = ["cyan", "emerald", "readable", "aubergine"]` (4; `amber` is deliberately absent per D-04)
- `PAGES = [{Dashboard,"/"},{LiveRun,"/live-run"},{Analytics,"/analytics"},{Forge,"/forge"},{Graphs,"/graphs"}]`
- Per cell: `page.addInitScript` sets `localStorage["codepulse-theme"]` and
  `localStorage["codepulse_onboarding_complete"]="true"`, then `page.goto`, then
  `page.waitForLoadState("networkidle")`
- Gate guard (commit `fee96b5d`): waits on `signInText.or(appShellNav)`, then `test.skip()` if the
  Clerk sign-in text is present. DO NOT REMOVE OR WEAKEN THIS.
- Measurement: `new AxeBuilder({ page }).withTags(["wcag2a","wcag2aa"]).analyze()` then
  `expect(results.violations).toEqual([])`

Server invocation (from `package.json` script `test:e2e:noauth:help`, verbatim):
- `VITE_CLERK_PUBLISHABLE_KEY= npm run dev:noauth` -- issued from GIT BASH, never PowerShell
  (PS 5.1 silently deletes an env var assigned the empty string, leaving the Clerk gate live).
- `PW_BASE_URL=http://localhost:5181 npm run test:e2e:noauth` -- also from Git Bash.
- `dev:noauth` = `vite --port 5181 --strictPort --host 127.0.0.1`. Never drop `--host 127.0.0.1`.
- `playwright.config.ts`'s `webServer` is hardcoded to 5173 and will report itself healthy while
  these tests target 5181 -- start `dev:noauth` FIRST, in its own process.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add env-gated per-cell JSON capture to the contrast matrix</name>
  <files>e2e/theme-contrast.spec.ts</files>
  <read_first>
    - e2e/theme-contrast.spec.ts (the file being modified -- read all 72 lines)
    - .planning/phases/122-tokens-primitives-contrast-measurement/122-CONTEXT.md (D-22, D-23, D-24)
    - playwright.config.ts (to confirm the webServer port and project names before running anything)
  </read_first>
  <action>
    Add two environment switches to `e2e/theme-contrast.spec.ts`, changing nothing else about the
    matrix, the `addInitScript` idiom, or the `fee96b5d` gate guard.

    (1) `A11Y_CAPTURE_DIR` -- when `process.env.A11Y_CAPTURE_DIR` is set, after `analyze()` returns
    and BEFORE the `expect(results.violations).toEqual([])` line, write
    `${A11Y_CAPTURE_DIR}/${theme}__${pg.name}.json` via `node:fs` (`mkdirSync` with
    `{ recursive: true }`, then `writeFileSync` with `JSON.stringify(payload, null, 2)`). The payload
    object carries: `theme`, `page` (the `pg.name`), `path` (the `pg.path`), `url` (`page.url()`),
    `capturedAt` (ISO string), `violationCount` (`results.violations.length`), `violations`
    (`results.violations` verbatim -- the full axe objects including `id`, `impact`, `nodes[].target`,
    `nodes[].failureSummary`, so Phase 123 can work element-by-element per D-23), and
    `axeVersion` (`results.testEngine`).

    Ordering is load-bearing: the write MUST precede the assertion, or a failing cell captures
    nothing and the baseline silently has holes. State that reason in a comment on the write.

    (2) `A11Y_MEASURE_ONLY` -- when set to `"1"`, skip the `expect(results.violations).toEqual([])`
    assertion so the whole 20-cell matrix completes in one pass. A11Y-01 is a SIZING requirement, not
    a remediation requirement; failing at cell 1 measures 1 of 20. Remediation is Phase 123's A11Y-02
    and that phase runs this same spec with the switch UNSET. When the switch is unset the spec's
    behaviour is byte-for-byte what it is today.

    Do NOT touch the gate guard, the `test.skip()` message, `THEMES`, `PAGES`, the `addInitScript`
    body, or the `withTags` list. `amber` stays out of `THEMES` per D-04 (an unexposed theme cannot be
    measured against a rendered page).
  </action>
  <acceptance_criteria>
    - `git grep -cF 'A11Y_CAPTURE_DIR' -- e2e/theme-contrast.spec.ts` reports at least 2 matching lines.
    - `git grep -cF 'A11Y_MEASURE_ONLY' -- e2e/theme-contrast.spec.ts` reports at least 1 matching line.
    - CONTROL that the guard survived: `git grep -cF 'Clerk auth gate present' -- e2e/theme-contrast.spec.ts`
      still reports 1, and `git diff -- e2e/theme-contrast.spec.ts` shows ZERO deletions inside the
      gate-check block (verify by reading the diff, not by asserting on a count).
    - CONTROL that ordering is right: in the edited file, the line number of the `writeFileSync` call
      is strictly LESS than the line number of `expect(results.violations)`. Prove it by printing both
      with `git grep -nF` and comparing the two integers in the summary.
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>The spec captures full per-cell violation JSON when `A11Y_CAPTURE_DIR` is set, completes the whole matrix when `A11Y_MEASURE_ONLY=1`, and behaves exactly as before when neither is set.</done>
</task>

<task type="auto">
  <name>Task 2: Run the before-matrix against dev:noauth and commit the raw JSON</name>
  <files>.planning/phases/122-tokens-primitives-contrast-measurement/a11y-before/</files>
  <read_first>
    - package.json (the `dev:noauth`, `test:e2e:noauth` and `test:e2e:noauth:help` scripts, verbatim)
    - e2e/theme-contrast.spec.ts (as edited by Task 1)
  </read_first>
  <action>
    Start the keyless dev server in the background from GIT BASH (never PowerShell -- PS 5.1 deletes
    an env var assigned the empty string, silently leaving the Clerk gate live and making every cell
    skip):
    `VITE_CLERK_PUBLISHABLE_KEY= npm run dev:noauth`
    Wait until `http://127.0.0.1:5181` answers. Probe `localhost` AND `127.0.0.1` -- a Vite server can
    bind one and not the other, and a single failed probe is not evidence the server is down.

    Then run the full matrix from Git Bash:
    `A11Y_CAPTURE_DIR=.planning/phases/122-tokens-primitives-contrast-measurement/a11y-before A11Y_MEASURE_ONLY=1 PW_BASE_URL=http://localhost:5181 npx playwright test e2e/theme-contrast.spec.ts`

    Then assert the run actually measured something rather than skipping:
    - Exactly 20 JSON files must exist in `a11y-before/` (4 themes x 5 pages). Fewer means cells
      skipped behind the gate or crashed; investigate before proceeding, do not proceed with holes.
    - Zero cells may report `test.skip()`. A skipped cell is a NOT-MEASURED cell. If any cell skips,
      the server is gated -- stop, fix the invocation, re-run.
    - A cell reporting `violationCount: 0` is only believable alongside a cell reporting a non-zero
      count in the same run. The known figure is 234 violations on `[cyan] Dashboard`, so if EVERY
      cell reports 0, the probe measured an unrendered page -- treat that as a failed run, not a
      clean baseline.

    Commit the 20 JSON files. They are the raw input Phase 123 consumes per D-23.
    Record the wall-clock runtime of the matrix -- `122-VALIDATION.md` currently carries "Unmeasured"
    for E2E latency and this run is the first real figure.
  </action>
  <acceptance_criteria>
    - `ls .planning/phases/122-tokens-primitives-contrast-measurement/a11y-before/*.json | wc -l` returns exactly 20.
    - Every one of the 20 filenames matches `{cyan,emerald,readable,aubergine}__{Dashboard,LiveRun,Analytics,Forge,Graphs}.json` -- print the sorted list and compare against the 20 expected names by name, not by count alone.
    - CONTROL (the run measured a rendered page, not the sign-in screen): the sum of `violationCount`
      across all 20 files is > 0, AND `cyan__Dashboard.json` reports `violationCount` > 0. Print both
      numbers. A total of 0 is a failed run.
    - CONTROL (the guard is still capable of firing): re-run ONE cell against the GATED 5173 server
      (`PW_BASE_URL=http://localhost:5173 npx playwright test e2e/theme-contrast.spec.ts -g "cyan. Dashboard"`)
      and confirm Playwright reports it as SKIPPED with the "Clerk auth gate present" reason -- not
      passed, not failed. This proves the 20 green captures above came from a genuinely keyless
      server rather than from a guard that stopped working.
    - The measured wall-clock runtime of the 20-cell matrix is recorded as a number in the summary.
  </acceptance_criteria>
  <verify>
    <automated>ls .planning/phases/122-tokens-primitives-contrast-measurement/a11y-before/*.json | wc -l</automated>
  </verify>
  <done>20 raw per-cell axe violation JSON files exist and are committed; a control cell against the gated server still skips.</done>
</task>

<task type="auto">
  <name>Task 3: Write 122-CONTRAST-BASELINE.md with the before table and the named sampling limit</name>
  <files>.planning/phases/122-tokens-primitives-contrast-measurement/122-CONTRAST-BASELINE.md</files>
  <read_first>
    - The 20 JSON files produced by Task 2
    - .planning/phases/122-tokens-primitives-contrast-measurement/122-CONTEXT.md (D-23, D-24)
    - .planning/phases/120-polish-verified-defects/120-DESIGN-REVIEW-HANDOFF.md (the withdrawn-measurement correction and why the before-run exists)
  </read_first>
  <action>
    Create `122-CONTRAST-BASELINE.md` with these sections, leaving the AFTER half explicitly stubbed
    as `PENDING -- filled by 122-19` so the file is never mistaken for complete:

    1. `## BEFORE (control, measured <date>, git <sha> -- pre-token-layer)` -- a 4-row x 5-column
       table of `violationCount` per theme x page cell, plus row/column totals and a grand total,
       every number re-derived from the committed JSON rather than transcribed from a test log.
       State the unit: these are axe VIOLATION objects, not violating ELEMENTS; if you also quote an
       element count, derive it separately as the sum of `violations[].nodes.length` and label it as
       a different unit.
    2. `## Rule breakdown (before)` -- violation counts grouped by axe rule `id` across all cells, so
       Phase 123 can work rule-by-rule per D-23.
    3. `## Sampling limit (D-24)` -- state plainly that A11Y-01 measures 5 routes of the ~47 source
       pages in `src/pages/` (re-derive both figures: `ls src/pages/*.tsx src/pages/*/*.tsx | grep -v
       '\.test\.' | wc -l` for the denominator, and name the unit as FILES). Then LIST BY NAME every
       page file that is NOT one of the 5 measured routes. Do not write "and others"; the whole point
       of D-24 is that an unmeasured route can be broken while Phase 123 reports green, so the
       unmeasured set has to be enumerable. Also record that `amber` is out of the matrix by D-04
       because it is not reachable from `ThemeSwitcher`.
    4. `## AFTER -- PENDING` and `## Delta -- PENDING` stubs naming plan 122-19 as their owner.
    5. `## Named-pair ratios -- PENDING` stub naming plan 122-18 as owner (D-22's rasterised probe
       for Forge `failed` and the `--status-ok` decouple, measured against `--card` per D-06).
    6. `## Method` -- axe-core resolves colour itself and is immune to the oklch string-scraping trap
       that produced Phase 120's withdrawn numbers; no computed colour string was parsed anywhere in
       this measurement. Name the invocation used, including the port and the shell.
  </action>
  <acceptance_criteria>
    - `git grep -cF 'PENDING' -- .planning/phases/122-tokens-primitives-contrast-measurement/122-CONTRAST-BASELINE.md` reports at least 3 matching lines (AFTER, Delta, Named-pair stubs present).
    - The grand total in section 1 equals the sum of `violationCount` across the 20 JSON files. Compute both independently and show the two numbers agreeing.
    - Section 3 names the measured-route denominator, its UNIT (files), and lists the unmeasured page files individually -- `grep -c '^- ' <file>` in that section is >= 40.
    - The file contains no parsed-colour-string figure. `git grep -cE 'rgb\(|getComputedStyle' -- .planning/phases/122-tokens-primitives-contrast-measurement/122-CONTRAST-BASELINE.md` reports 0 matching lines (or, if it reports non-zero, every hit is prose explaining why that method was NOT used -- read them, do not assume).
  </acceptance_criteria>
  <verify>
    <automated>test -f .planning/phases/122-tokens-primitives-contrast-measurement/122-CONTRAST-BASELINE.md && grep -c 'PENDING' .planning/phases/122-tokens-primitives-contrast-measurement/122-CONTRAST-BASELINE.md</automated>
  </verify>
  <done>The baseline artifact carries the full before table, the rule breakdown, the enumerated sampling limit, and explicit PENDING stubs for the after-run.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness -> local keyless dev server | `dev:noauth` runs with the Clerk gate disabled. It is pinned to `--host 127.0.0.1` precisely so an auth-disabled server is never exposed on the tailnet or LAN. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-122-01 | Information Disclosure | `dev:noauth` on port 5181 | mitigate | Never drop `--host 127.0.0.1` from the `dev:noauth` script; `vite.config.ts`'s `server.host:true` would otherwise bind an auth-disabled server to every interface. Stop the server when the matrix run completes. |
| T-122-02 | Information Disclosure | committed `a11y-before/*.json` | mitigate | axe violation nodes carry DOM selectors and page text. Before committing, scan the JSON for absolute home paths with a FIXED-STRING matcher typed exactly as it appears on disk (`grep -rF 'C:\Users\mandr'`), never a hand-escaped pattern, and pair the scan with a known-present control line so a zero is believable. |
| T-122-SC | Tampering | npm/pip/cargo installs | mitigate | No packages are installed by this plan. `@axe-core/playwright` and `@playwright/test` are already declared in `package.json` and installed. RESEARCH.md's Package Legitimacy Audit records zero new packages for this phase. |

RESEARCH.md's security assessment: no ASVS category applies. This plan adds no input surface, no
auth change, no external data ingestion, and no new dependency. The two threats above are operational
(exposure of a keyless server, disclosure via a committed artifact), not application-level.
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0
- 20 JSON files present in `a11y-before/`, named exactly per the theme x page matrix
- Grand violation total > 0 and `[cyan] Dashboard` > 0 (the run measured a rendered page)
- A control cell against the gated 5173 server still SKIPS with the `fee96b5d` reason
- `122-CONTRAST-BASELINE.md` exists with BEFORE filled and AFTER/Delta stubbed
- No `src/index.css` change is included in this plan's commit -- `git diff --name-only` for this plan must NOT list `src/index.css`
</verification>

<success_criteria>
The A11Y-01 control exists on disk and in git before any token edit: a per-cell violation count for
all 20 cells, the raw axe JSON Phase 123 consumes, and an explicitly enumerated statement of which
routes were NOT measured.
</success_criteria>

<output>
Create `.planning/phases/122-tokens-primitives-contrast-measurement/122-01-SUMMARY.md` when done
</output>
