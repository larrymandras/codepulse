---
phase: 105-tool-trace-observability
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - astridr-repo:astridr/engine/telemetry.py
  - astridr-repo:astridr/agent/loop.py
  - astridr-repo:astridr/providers/anthropic_provider.py
  - astridr-repo:astridr/providers/ollama.py
  - astridr-repo:astridr/providers/openrouter.py
  - astridr-repo:docs/astridr-contract.md
  - astridr-repo:tests/unit/test_round_context.py
  - astridr-repo:tests/unit/agent/test_loop_tool_executed_emit.py
  - astridr-repo:tests/unit/agent/test_loop.py
autonomous: true
requirements: [OBS-01, OBS-02, OBS-03]

must_haves:
  truths:
    - "D-03: astridr's tool_executed payload carries durationMs and traceId, sourced from the already-computed _duration_ms and the existing trace ContextVar"
    - "D-08: astridr's tool_call_leaked_as_text payload carries tool_was_offered, tools_offered_count, round and agentId — the four values the logger.warning already computes and the telemetry.send drops"
    - "D-10: a per-round ContextVar exists in astridr/engine/telemetry.py, is set by the agent loop where round_num lives, and is read by the three provider llm_call emits and the tool_executed emit — attribution is reported, never inferred"
  artifacts:
    - path: "C:/Users/mandr/astridr-repo/astridr/engine/telemetry.py"
      provides: "set_round_context / reset_round_context / get_round_context"
      contains: "_current_round"
    - path: "C:/Users/mandr/astridr-repo/tests/unit/test_round_context.py"
      provides: "ContextVar roundtrip + isolation tests"
    - path: "C:/Users/mandr/astridr-repo/docs/astridr-contract.md"
      provides: "Updated §2.26 and §2.34 including the previously-undocumented tool_call_leaked_as_text kind"
      contains: "tool_call_leaked_as_text"
  key_links:
    - from: "astridr/agent/loop.py"
      to: "astridr/engine/telemetry.py"
      via: "set_round_context at the round increment"
      pattern: "set_round_context"
    - from: "astridr/providers/anthropic_provider.py"
      to: "astridr/engine/telemetry.py"
      via: "get_round_context read in the llm_call payload"
      pattern: "get_round_context"
---

<objective>
The single cross-repo Ástríðr commit for Phase 105: widen two telemetry payloads with values the
code already computes and throws away, add the per-round ContextVar that makes trace nesting
*reported* rather than guessed, and update the contract doc so it stops omitting a live event kind.

Purpose: CodePulse cannot render OBS-01's per-tool timings (no `durationMs`), OBS-03's nesting (no
`traceId`, no `round` on tool rows and no `round` on `llm_call`), or OBS-02's actual diagnosis (no
`tool_was_offered`) because Ástríðr does not send them. Every added field is an existing local
variable at its emit site — this is additive telemetry only, with zero change to tool-filter
behaviour, policy semantics, or agent-loop control flow.

Output: one astridr-repo commit on `feature/brain-swap` touching 5 Python modules, 1 contract doc,
and 3 test files. No CodePulse file is touched by this plan.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/105-tool-trace-observability/105-CONTEXT.md
@.planning/phases/105-tool-trace-observability/105-RESEARCH.md
@.planning/phases/105-tool-trace-observability/105-PATTERNS.md

<repo_scope>
## ⚠ THIS PLAN OPERATES IN A DIFFERENT REPOSITORY

Every path in this plan is relative to **`C:/Users/mandr/astridr-repo`**, NOT the CodePulse
checkout you are probably sitting in. `astridr/agent/loop.py` means
`C:/Users/mandr/astridr-repo/astridr/agent/loop.py`. There is no `astridr/` directory in CodePulse
— if a path does not resolve, you are in the wrong repo. `cd` to the astridr repo explicitly in
every command (Bash tool CWD drift is a documented failure mode in this project).

- Validation framework here is **pytest**, not Vitest. `asyncio_mode = "auto"`, `testpaths = ["tests"]`
  (verified in `pyproject.toml:160-175`).
- Current branch: `feature/brain-swap` (verified 2026-08-03). This is the LIVE branch — the running
  containers are built from it (104-11 established this; no merge to `main` was needed or done).
  Commit here; do NOT merge or deploy in this plan. Deployment is plan 105-09's job.
- Do NOT touch any CodePulse file from this plan. Its `files_modified` list is astridr-only, which
  is why it can run in the same wave as plan 105-01.
</repo_scope>

<planner_findings>
Verified live in astridr-repo on 2026-08-03. These correct or extend the upstream artifacts.

**F1 — the round ContextVar's set/reset site.** PATTERNS.md suggests setting the round token at the
increment (`loop.py:1145`) "with a matching reset in the loop's existing per-round cleanup/finally".
There is no such per-round cleanup: the `while True:` loop at `loop.py:1144` runs to line **1765**
(620 lines, verified by indentation analysis) with many `break`/`continue` paths and no per-iteration
`finally`. Wrapping it would be a 620-line reindentation.

Use the TURN-level site instead, mirroring `traceId` exactly. `loop.py:938-942` already reads:
```python
_trace_token = set_trace_context(str(uuid.uuid4()))
try:
    return await self._process_inner_body(run_state, session, message)
finally:
    reset_trace_context(_trace_token)
```
Add `_round_token = set_round_context(None)` beside it and `reset_round_context(_round_token)` in
the same `finally`. Then at `loop.py:1145` (`round_num += 1`) add a bare
`set_round_context(round_num)` — no per-iteration token, because the turn-level reset restores the
pre-turn value on every exit path including exceptions. This answers RESEARCH.md's Open Question 2:
the discipline is the same one `_current_trace_id` already uses, at the same choke point.

**F2 — `ollama.py` and `openrouter.py` were confirmed this session** (PATTERNS.md flagged them as
un-re-verified). `ollama.py:214-230` and `openrouter.py:340-359` both build a local
`_llm_payload: dict[str, Any]`, read `_get_trace_ctx()`, and conditionally attach `traceId` before
`await self._telemetry.send("llm_call", _llm_payload)`. `anthropic_provider.py:592-615` is the same
shape plus a `goalId` read. All three take the identical 3-line addition. Note `ollama.py` imports
only `get_trace_context` (no `get_goal_context`) — add the round import there too.

**F3 — the contract doc gap is real and slightly wider than RESEARCH.md said.** `docs/astridr-contract.md`
§2.34 (line 1075) documents exactly three subtypes and omits `tool_call_leaked_as_text` entirely
(verified by grep: the string appears nowhere in that file). §2.26 (line 854) documents
`tool_executed`'s five fields and states the ingestion path is `callGraphEdges.upsertEdge` only —
which plan 105-03 is about to make false. Both sections need updating in THIS commit.

**F4 — `execution_denied` and the two malformed-policy emits need NO astridr change.**
`loop.py:2099-2104` already sends `{event, tool, sessionId}`; `bootstrap/core.py` already forwards
`{"event": ..., **signal}` where `signal` is `{"field", "error"}` from `config.py:1166-1187`. Do not
widen them. Their shapes are the contract CodePulse's ingest case (105-03) is written against.

**F5 — existing test files to EXTEND, not duplicate** (VALIDATION.md's Wave 0 item, now closed):
- `tests/unit/agent/test_loop_tool_executed_emit.py` — 4 existing tests on the `tool_executed` emit.
- `tests/unit/agent/test_loop.py` — already contains `tool_policy_event` coverage.
- `tests/unit/test_trace_context.py` — the ContextVar roundtrip/isolation template to mirror.
</planner_findings>

<interfaces>
<!-- Verified live 2026-08-03. Use directly. -->

The ContextVar trio to mirror — `astridr/engine/telemetry.py:87-89, 610-625`:
```python
_current_trace_id: contextvars.ContextVar[str | None] = contextvars.ContextVar(  # contextvar-ok: trace ID, not a secret
    "telemetry_trace_id", default=None
)

def set_trace_context(trace_id: str | None) -> contextvars.Token[str | None]:
    return _current_trace_id.set(trace_id)

def reset_trace_context(token: contextvars.Token[str | None]) -> None:
    _current_trace_id.reset(token)

def get_trace_context() -> str | None:
    return _current_trace_id.get()
```

Current `tool_executed` emit — `astridr/agent/loop.py:2050-2056` (inside the `finally:` of `_execute_tool`,
where `_duration_ms` was computed at line 2030 and is currently used only by `_persistence.insert_tool_invocation`):
```python
if self.telemetry is not None:
    await self.telemetry.send("tool_executed", {
        "agentId": self._active_profile or "astridr",
        "toolName": tool_call.name,
        "sessionId": self._active_session_id or "",
        "success": _tool_success,
        "timestamp": _end_dt.timestamp(),
    })
```

Current leak emit — `astridr/agent/loop.py:1457-1476`:
```python
_leaked_tool = _detect_leaked_tool_call(response.content)
if _leaked_tool is not None:
    _permitted = run_state.tools_for_turn_names if run_state is not None else None
    logger.warning(
        "agent_loop.tool_call_leaked_as_text",
        leaked_tool=_leaked_tool,
        round=round_num,
        task_category=self._task_category,
        tools_offered_count=len(_permitted) if _permitted is not None else None,
        tool_was_offered=(_leaked_tool in _permitted) if _permitted else False,
        session_id=self._active_session_id or "",
        agent_id=self._active_profile or "",
    )
    if self.telemetry is not None:
        await self.telemetry.send("tool_policy_event", {
            "event": "tool_call_leaked_as_text",
            "tool": _leaked_tool,
            "taskCategory": self._task_category or "",
            "sessionId": self._active_session_id or "",
        })
```

Provider emit shape — `astridr/providers/anthropic_provider.py:607-615`:
```python
    _gid = _get_goal_ctx()
    if _gid:
        _llm_payload["goalId"] = _gid
    _tid = _get_trace_ctx()
    if _tid:
        _llm_payload["traceId"] = _tid
    await self._telemetry.send("llm_call", _llm_payload)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the per-round ContextVar trio to astridr/engine/telemetry.py (D-10)</name>
  <read_first>
    - C:/Users/mandr/astridr-repo/astridr/engine/telemetry.py lines 75-115 (the ContextVar definitions block, including the `# contextvar-ok` marker convention) and lines 585-630 (the goal/trace setter/resetter/getter functions)
    - C:/Users/mandr/astridr-repo/tests/unit/test_trace_context.py (the full file — this is the test template you mirror)
  </read_first>
  <files>C:/Users/mandr/astridr-repo/astridr/engine/telemetry.py, C:/Users/mandr/astridr-repo/tests/unit/test_round_context.py</files>
  <behavior>
    - `get_round_context()` returns `None` when nothing has been set.
    - `set_round_context(3)` then `get_round_context()` returns `3`.
    - `reset_round_context(token)` restores the value that was current before that `set_round_context` call, including restoring `None`.
    - Round and trace are INDEPENDENT ContextVars: setting one never changes the other, in either direction (the "never alias" assertion `test_trace_context.py` already makes for trace vs goal).
    - The round ContextVar is task-local: two concurrently-running `asyncio` tasks that each set a different round see only their own value.
  </behavior>
  <action>
In `C:/Users/mandr/astridr-repo/astridr/engine/telemetry.py`:

Define `_current_round: contextvars.ContextVar[int | None]` with ContextVar name
`"telemetry_round"` and `default=None`, placed immediately after the existing `_current_trace_id`
definition. Carry the same trailing `# contextvar-ok:` marker comment the two siblings use, with a
reason of the same form ("round number, not a secret") — that marker is enforced by this repo's own
lint/guard convention, so omitting it is a real failure, not cosmetics.

Add `set_round_context(round_num: int | None) -> contextvars.Token[int | None]`,
`reset_round_context(token: contextvars.Token[int | None]) -> None`, and
`get_round_context() -> int | None`, placed immediately after the existing
`set_trace_context`/`reset_trace_context`/`get_trace_context` trio, with docstrings in the same
voice. The type parameter is `int | None`, not `str | None` — this is the one intentional
difference from the trace sibling.

Verified: `_current_round`, `round_id` and `telemetry_round` currently appear NOWHERE in the
`astridr/` package (grepped) — you are adding, not renaming.

Create `C:/Users/mandr/astridr-repo/tests/unit/test_round_context.py` mirroring
`tests/unit/test_trace_context.py`'s structure, covering every case in `<behavior>` above. The
task-locality case uses `asyncio.gather` over two coroutines that each set a distinct round and
assert their own value after an `await asyncio.sleep(0)` interleave.
  </action>
  <acceptance_criteria>
    - `cd /c/Users/mandr/astridr-repo && python -c "from astridr.engine.telemetry import set_round_context, reset_round_context, get_round_context; t=set_round_context(7); print(get_round_context()); reset_round_context(t); print(get_round_context())"` prints `7` then `None`.
    - `grep -c "contextvar-ok" astridr/engine/telemetry.py` increased by exactly 1 versus HEAD (`git diff -U0 astridr/engine/telemetry.py | grep -c "^+.*contextvar-ok"` returns `1`).
    - `cd /c/Users/mandr/astridr-repo && python -m pytest tests/unit/test_round_context.py -q` exits 0 with at least 5 tests.
    - The independence test asserts BOTH directions (set round → trace unchanged, AND set trace → round unchanged).
  </acceptance_criteria>
  <verify>
    <automated>cd /c/Users/mandr/astridr-repo && python -m pytest tests/unit/test_round_context.py tests/unit/test_trace_context.py -q</automated>
  </verify>
  <done>The round ContextVar trio exists, is independent of trace and goal, is task-local, and has its own pytest file.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Widen the two loop.py emits and wire the round set/reset (D-03, D-08, D-10)</name>
  <read_first>
    - C:/Users/mandr/astridr-repo/astridr/agent/loop.py lines 40-50 (the existing `from astridr.engine.telemetry import ...` block)
    - C:/Users/mandr/astridr-repo/astridr/agent/loop.py lines 920-950 (`_process_inner`'s traceId try/finally — the exact site the round token joins)
    - C:/Users/mandr/astridr-repo/astridr/agent/loop.py lines 1140-1150 (`round_num = 0` / `while True:` / `round_num += 1`)
    - C:/Users/mandr/astridr-repo/astridr/agent/loop.py lines 1450-1480 (the leak detector: the rich `logger.warning` and the narrow `telemetry.send` three lines below)
    - C:/Users/mandr/astridr-repo/astridr/agent/loop.py lines 2025-2060 (the `finally:` block where `_duration_ms` is computed and `tool_executed` is emitted)
    - C:/Users/mandr/astridr-repo/tests/unit/agent/test_loop_tool_executed_emit.py (full file — the 4 existing tests you extend)
  </read_first>
  <files>C:/Users/mandr/astridr-repo/astridr/agent/loop.py, C:/Users/mandr/astridr-repo/tests/unit/agent/test_loop_tool_executed_emit.py, C:/Users/mandr/astridr-repo/tests/unit/agent/test_loop.py</files>
  <behavior>
    - The `tool_executed` payload contains `durationMs` equal to the `_duration_ms` the same `finally` block passed to `_persistence.insert_tool_invocation` — the SAME value, not a recomputation.
    - The `tool_executed` payload contains `traceId` when a trace context is set, and the key is present with value `None` when it is not (do not conditionally omit — CodePulse coalesces with `??` and an explicit null is more honest than an absent key for a field that is always meaningful).
    - The `tool_executed` payload contains `round` equal to the loop's current `round_num` when inside a turn.
    - All four pre-existing `tool_executed` assertions in `test_loop_tool_executed_emit.py` still pass unchanged (success path, failure path, error-string path, telemetry-is-None path).
    - The leak `tool_policy_event` payload contains `tool_was_offered`, `tools_offered_count`, `round` and `agentId` with values EQUAL to the corresponding kwargs the `logger.warning` call three lines above passes (`tool_was_offered`, `tools_offered_count`, `round`, `agent_id`) — asserted by comparing against the captured log record, not against a hand-written expectation.
    - `tools_offered_count` is `None` (not `0`) when `_permitted` is `None` — the existing expression already distinguishes "no filter active" from "filter offered zero tools"; preserve that.
    - Exiting a turn restores the round context to its pre-turn value even when `_process_inner_body` raises.
  </behavior>
  <action>
All edits in `C:/Users/mandr/astridr-repo/astridr/agent/loop.py`.

**(a) Imports.** Extend the existing `from astridr.engine.telemetry import (...)` block at lines
45-47 to also import `get_round_context`, `reset_round_context`, `set_round_context`. Keep the
block alphabetically ordered as it is today. `get_trace_context` is already imported — do not
re-import it.

**(b) Round set/reset at the turn choke point (finding F1).** In `_process_inner` (lines 938-942),
add `_round_token = set_round_context(None)` immediately after the existing
`_trace_token = set_trace_context(...)` line, and `reset_round_context(_round_token)` inside the
existing `finally:` immediately after `reset_trace_context(_trace_token)`. Extend that method's
docstring with a sentence explaining the round ContextVar's purpose and citing D-10 — specifically
that `llm_call` is emitted from inside each provider, which has no access to the loop's `round_num`,
so a ContextVar is the only mechanism reaching both emit sites.

**(c) Round update per round.** At line 1145 (`round_num += 1`), add `set_round_context(round_num)`
on the following line, with a short comment noting there is deliberately no per-iteration reset —
the turn-level `finally` in `_process_inner` restores the pre-turn value on every exit path,
including `break`, `return` and exceptions. Do NOT wrap the `while True:` body (lines 1144-1765) in
a new `try/finally`.

**(d) Widen `tool_executed` (D-03/D-10).** In the emit at lines 2051-2056 add three keys:
`"durationMs": _duration_ms`, `"traceId": get_trace_context()`, `"round": get_round_context()`.
Update the comment block above it (currently "M1.P1: Emit an agent↔tool call-graph edge…") to state
that the payload now also feeds CodePulse's `toolExecutions` per-call rows and the trace waterfall's
nesting, citing D-01/D-03/D-10.

**(e) Widen the leak payload (D-08).** In the emit at lines 1471-1476 add four keys, each reusing
the IDENTICAL expression the `logger.warning` above already evaluates — do not recompute them
differently:
`"tool_was_offered": (_leaked_tool in _permitted) if _permitted else False`,
`"tools_offered_count": len(_permitted) if _permitted is not None else None`,
`"round": round_num`,
`"agentId": self._active_profile or ""`.
Add a comment stating that `tool_was_offered` is the actual diagnosis — it separates "the model
tried to call a tool it was never given" (the silent-filter trap this detector exists for) from "it
leaked one it had" — citing D-08.

**(f) Do NOT touch** the `execution_denied` emit (lines 2099-2104) or anything in
`astridr/engine/bootstrap/core.py` / `astridr/engine/config.py` (finding F4).

**Tests.** Extend `tests/unit/agent/test_loop_tool_executed_emit.py` with cases for `durationMs`
(a number `>= 0`), `traceId` (present when set via `set_trace_context`, `None` when unset) and
`round`. Extend `tests/unit/agent/test_loop.py`'s existing `tool_policy_event` coverage with the
leak-payload cases from `<behavior>`, including the `tools_offered_count is None` case. Add one
test asserting the round context is restored after `_process_inner` raises.
  </action>
  <acceptance_criteria>
    - `cd /c/Users/mandr/astridr-repo && git diff -U0 astridr/agent/loop.py | grep -c "^+"` is under 40 (this is a targeted widening, not a restructure — a large diff means the `while True:` body was reindented, which is explicitly forbidden).
    - `grep -n "set_round_context(round_num)" astridr/agent/loop.py` returns exactly 1 match.
    - `grep -n "reset_round_context" astridr/agent/loop.py` returns exactly 1 match, and it is inside the same `finally:` as `reset_trace_context`.
    - `grep -n "tool_was_offered" astridr/agent/loop.py` returns exactly 2 matches (the logger call and the telemetry payload).
    - `cd /c/Users/mandr/astridr-repo && python -m pytest tests/unit/agent/test_loop_tool_executed_emit.py tests/unit/agent/test_loop.py -q` exits 0.
    - MUTATION PROOF (record in the SUMMARY): temporarily change `"durationMs": _duration_ms` to `"durationMs": 0` and confirm the new duration test FAILS, then restore. A test that passes against a constant proves nothing.
  </acceptance_criteria>
  <verify>
    <automated>cd /c/Users/mandr/astridr-repo && python -m pytest tests/unit/agent/ tests/unit/test_round_context.py tests/unit/test_trace_context.py -q</automated>
  </verify>
  <done>Both emits carry every added field, the round ContextVar is set per round and reset per turn on all exit paths, and the pre-existing four `tool_executed` tests still pass.</done>
</task>

<task type="auto">
  <name>Task 3: Attach round to the three provider llm_call emits and update the contract doc</name>
  <read_first>
    - C:/Users/mandr/astridr-repo/astridr/providers/anthropic_provider.py lines 592-616
    - C:/Users/mandr/astridr-repo/astridr/providers/ollama.py lines 212-232
    - C:/Users/mandr/astridr-repo/astridr/providers/openrouter.py lines 338-360
    - C:/Users/mandr/astridr-repo/docs/astridr-contract.md lines 854-875 (§2.26 `tool_executed`) and lines 1075-1094 (§2.34 `tool_policy_event`)
  </read_first>
  <files>C:/Users/mandr/astridr-repo/astridr/providers/anthropic_provider.py, C:/Users/mandr/astridr-repo/astridr/providers/ollama.py, C:/Users/mandr/astridr-repo/astridr/providers/openrouter.py, C:/Users/mandr/astridr-repo/docs/astridr-contract.md</files>
  <action>
**Providers (D-10).** In each of the three files, inside the `if self._telemetry:` block that builds
`_llm_payload`, add exactly three lines mirroring the existing `traceId` idiom that sits immediately
above:
- a function-local import `from astridr.engine.telemetry import get_round_context as _get_round_ctx`
  placed beside the existing `get_trace_context as _get_trace_ctx` import (these are deliberately
  function-local in all three files — keep them local, do not hoist to module scope),
- `_rnd = _get_round_ctx()`,
- `if _rnd is not None: _llm_payload["round"] = _rnd`.

Note the guard is `is not None`, NOT truthiness — `round == 0` must not be silently dropped. The
sibling `traceId`/`goalId` guards use truthiness because an empty string is meaningless there;
copying that idiom for an integer would be a real bug.

Place the block immediately before the `await self._telemetry.send("llm_call", _llm_payload)` line
in each file. `ollama.py` currently imports only `get_trace_context` (no goal import) — add the
round import there too.

**Contract doc (finding F3).** In `docs/astridr-contract.md`:

§2.26 `tool_executed` (line 854) — add three rows to the Fields table:
`durationMs` (float, no, "Wall-clock tool execution time in milliseconds; the same value persisted to `tool_invocations`"),
`traceId` (str|null, no, "Per-turn trace id from the telemetry trace ContextVar; groups this tool call with the LLM calls of the same turn"),
`round` (int|null, no, "Agent-loop round number this tool ran in; null outside a turn"). Then update
the **Ingestion** line, which currently claims the only sink is `api.callGraphEdges.upsertEdge`, to
also name the `api.toolExecutions.insert` per-call row (CodePulse Phase 105, OBS-01/D-01) tagged
`provider: "astridr"`, and update **Dashboard** to name the CodePulse Tools page and trace waterfall.

§2.34 `tool_policy_event` (line 1075) — add a fourth row to the Subtypes table:
`tool_call_leaked_as_text` | `agent/loop.py` (post-response scan) | `tool`, `taskCategory`, `sessionId`, `tool_was_offered`, `tools_offered_count`, `round`, `agentId` | "The model emitted a tool call as literal text instead of a native tool_use block — the tool-filter silent-filter trap. `tool_was_offered` distinguishes a tool the model was never given from one it had. Logging/telemetry only; the response is never altered."
Update **Dashboard** from "None this phase." to name the CodePulse Tools page policy feed. Add a
note under the Subtypes table stating that `malformed_policy_boot` degrades to a fully-permissive
policy while `malformed_policy_reload_rejected` retains the last-known-good policy, and that the two
must never be collapsed (astridr Phase 182's own warning, and the reason CodePulse colours them
differently).

Do not renumber any section or reflow unrelated content.
  </action>
  <acceptance_criteria>
    - `cd /c/Users/mandr/astridr-repo && grep -c "get_round_context" astridr/providers/anthropic_provider.py astridr/providers/ollama.py astridr/providers/openrouter.py` returns `1` for each file.
    - `grep -c '_rnd is not None' astridr/providers/anthropic_provider.py astridr/providers/ollama.py astridr/providers/openrouter.py` returns `1` for each file (the `is not None` guard, not truthiness).
    - `grep -c "tool_call_leaked_as_text" docs/astridr-contract.md` returns at least `1` (it returned `0` before this task).
    - `grep -n "durationMs" docs/astridr-contract.md | head` shows a match inside the §2.26 field table (line number between 854 and 880).
    - `cd /c/Users/mandr/astridr-repo && python -m pytest tests/unit/providers -q` exits 0 (or reports "no tests ran" — either is acceptable, an ERROR is not).
    - `cd /c/Users/mandr/astridr-repo && python -m pytest tests/unit -q -m "not live"` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd /c/Users/mandr/astridr-repo && python -m pytest tests/unit -q -m "not live"</automated>
  </verify>
  <done>All three providers attach `round` under an `is not None` guard; the contract doc documents the fourth policy kind and every widened field.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| astridr process → CodePulse `/runtime-ingest` | Bearer-authenticated HTTP POST; astridr is the trusted emitter, but its payloads carry model-influenced strings |
| model output → telemetry payload | `_leaked_tool` is derived from LLM-generated text via `_detect_leaked_tool_call` — untrusted content crossing into a persisted field |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-105-06 | Information Disclosure | the widened leak payload | mitigate | Only the four values already written to the structured log are added — no new data source, no message body, no tool arguments. `tool_was_offered` is a bool, `tools_offered_count` an int, `round` an int, `agentId` an existing profile id. Nothing new leaves the process. |
| T-105-07 | Tampering | `_leaked_tool` (model-generated) reaching a persisted CodePulse field | transfer | Bounding and escaping happen at the CodePulse ingest boundary (plan 105-03, T-105-11) — this repo emits, the receiver validates. Do not add a second truncation here; a divergent limit on the two sides is worse than one enforced limit. |
| T-105-08 | Denial of Service | round ContextVar leaking across concurrent turns | mitigate | ContextVars are task-local in asyncio, and the reset is in `_process_inner`'s existing `finally` so it fires on every exit path including exceptions. Proven by the task-locality test (Task 1) and the raise-restores test (Task 2). |
| T-105-09 | Repudiation | telemetry emit failure silently losing a policy signal | accept | Pre-existing: `bootstrap/core.py` already wraps its sends in try/except-and-log. Unchanged by this plan; the receiving side's D-07 "last received" clock (plan 105-07) is the compensating control. |
| T-105-SC | Tampering | package installs | mitigate | Zero packages installed. `pyproject.toml` dependencies must be byte-identical to HEAD — assert with `git diff --stat pyproject.toml` showing no change. |
</threat_model>

<verification>
- `cd /c/Users/mandr/astridr-repo && python -m pytest tests/unit -q -m "not live"` green.
- `git diff --stat` in astridr-repo lists exactly the 9 files in `files_modified` and nothing else.
- `git diff --stat pyproject.toml` shows no change (no dependency was added).
- No CodePulse file appears in `cd /c/Users/mandr/codepulse && git status --porcelain` as a result of this plan.
</verification>

<success_criteria>
- `tool_executed` carries `durationMs`, `traceId` and `round`; `tool_call_leaked_as_text` carries `tool_was_offered`, `tools_offered_count`, `round` and `agentId`.
- A per-round ContextVar exists, is set at the round increment, is reset once per turn on every exit path, and is read by all three provider `llm_call` emits under an `is not None` guard.
- `docs/astridr-contract.md` documents all four `tool_policy_event` kinds and every widened field.
- astridr unit suite green; zero dependency changes; zero CodePulse files touched.
</success_criteria>

<output>
Create `.planning/phases/105-tool-trace-observability/105-02-SUMMARY.md` (in the CODEPULSE repo —
this phase's planning artifacts live there) when done. It MUST record: the astridr commit SHA and
branch, the exact `git diff --stat` output, the mutation used to prove the `durationMs` test is not
vacuous, and an explicit note that the commit is NOT yet deployed (plan 105-09 owns the
`docker compose --profile prod up -d --build` step).
</output>
