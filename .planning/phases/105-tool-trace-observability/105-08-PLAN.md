---
phase: 105-tool-trace-observability
plan: 08
type: execute
wave: 5
depends_on: ["105-06", "105-07"]
files_modified:
  - src/pages/Tools.tsx
  - src/pages/Tools.test.tsx
  - src/App.tsx
  - src/lib/navRegistry.ts
  - src/components/ToolBreakdown.tsx
  - src/components/ToolExecutionPanel.tsx
  - src/components/PermissionDecisionsChart.tsx
autonomous: true
requirements: [OBS-01, OBS-02]

must_haves:
  truths:
    - "D-13: a dedicated Tools page owns OBS-01's per-tool frequency and success/failure analytics and OBS-02's policy/leak feed"
    - "D-14: the Tools entry sits in the OBSERVE nav group beside Analytics, Alerts, Quality and Security — Tool Galaxy and MCP Inventory stay in GRAPHS"
    - "D-15: ToolBreakdown, ToolExecutionPanel and PermissionDecisionsChart stay mounted where they are and gain a link into the new page, with zero layout change"
    - "D-16: the Tools page is stacked sections on one scroll, not tabs, each section in its own SectionErrorBoundary, so a rare-but-important signal stays visible and an empty policy feed costs one honest line rather than a whole hidden view"
  artifacts:
    - path: "src/pages/Tools.tsx"
      provides: "The stacked Tools page shell"
      contains: "SectionErrorBoundary"
    - path: "src/lib/navRegistry.ts"
      provides: "OBSERVE → Tools entry and its Lucide icon mapping"
      contains: "/tools"
    - path: "src/App.tsx"
      provides: "Lazy /tools route"
      contains: "./pages/Tools"
  key_links:
    - from: "src/lib/navRegistry.ts"
      to: "/tools"
      via: "OBSERVE group item"
      pattern: "to: \"/tools\""
    - from: "src/components/ToolExecutionPanel.tsx"
      to: "/tools"
      via: "react-router Link"
      pattern: "View in Tools"
---

<objective>
Assemble the Tools page, give it a home in the nav, and cross-link the three existing tool panels
into it.

Purpose: D-13's rationale — OBS-01's source filter needs somewhere to live, and it is awkward to
bolt onto a Dashboard card. D-16's rationale — a rare-but-important policy signal must stay visible
on one scroll rather than hiding behind a tab nobody opens, and Settings' own tab state is local
component state rather than URL-addressable (found live in 104-09), so a D-06 alert could not
deep-link to a tab without new work. D-15's rationale — four working panels, on a milestone that has
already produced defects in exactly that class, get a link and nothing else.

Output: one page, one route, one nav entry, one icon mapping, and three one-line link additions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/105-tool-trace-observability/105-CONTEXT.md
@.planning/phases/105-tool-trace-observability/105-UI-SPEC.md
@.planning/phases/105-tool-trace-observability/105-PATTERNS.md
@CLAUDE.md

<planner_findings>
Verified live 2026-08-03.

**F1 — CONTEXT.md's nav citation is off by a few lines.** It cites `src/lib/navRegistry.ts:150-163`
for the `OBSERVE` group. The group object actually starts at line **149** and its `items` array runs
**151-167**, closing at 168. Same group, unambiguous — use the live file, not the citation.

**F2 — there is no `wrench` icon key.** `iconComponents` (`src/lib/navRegistry.ts:53-91`) maps 40
keys and none of them is `wrench`; `Wrench` is not among the 40 Lucide symbols imported at lines
12-51. So D-14 needs TWO edits, not one: add `Wrench` to the `lucide-react` import list AND add
`wrench: Wrench` to `iconComponents`. A nav item pointing at an unmapped icon key silently falls
back to `LayoutDashboard` (`src/layouts/DashboardLayout.tsx:78`) — it would look "fine" and be
wrong, so assert the mapping rather than eyeballing the sidebar.

**F3 — the three D-15 panels and their exact mount sites**, verified by grep:
| Component | Mounted at | Reads |
|---|---|---|
| `ToolBreakdown` | `src/pages/Dashboard.tsx:137` (`<ToolBreakdown events={events} />`) | the build-time `events` table via `useRecentEvents(100)` — NOT `toolExecutions`, so D-01/D-02's source mixing never reaches it |
| `ToolExecutionPanel` | `src/pages/Dashboard.tsx:159` | `toolExecutions` — already guarded with `excludeProvider: "astridr"` by plan 105-01 |
| `PermissionDecisionsChart` | `src/pages/Analytics.tsx:459` | `toolExecutions` — same guard |
Add the link INSIDE each component (so the mount sites stay untouched), not at the mount. Do not
modify `Dashboard.tsx` or `Analytics.tsx` at all.

**F4 — D-16 means two boundaries, not one.** Wrap each stacked section in its OWN
`<SectionErrorBoundary>` with the UI-SPEC's `name` props `"Tool Usage"` and `"Tool Policy Events"`.
One boundary around both would let a usage-query failure take the policy feed down with it, which is
the exact failure shape `3b31c9f4` fixed on Analytics (one timing-out query, one missing boundary,
whole page gone).

**F5 — `SectionErrorBoundary` is pre-existing hex debt.** It hardcodes `bg-gray-800/50`,
`border-red-500/30`, `text-red-400`, `text-gray-300`, `text-gray-500`, `bg-gray-700`,
`text-gray-200`. USE it (the UI-SPEC mandates reusing it verbatim, no new error copy pattern) but do
NOT modify it and do NOT copy its styling into `Tools.tsx`. Flag it as a Phase-106 DEBT candidate in
the SUMMARY.

**F6 — no tabs anywhere on this page.** D-16 is explicit. Do not import `Tabs` from
`src/components/ui/tabs.tsx` in `Tools.tsx`.
</planner_findings>

<interfaces>
<!-- Verified live 2026-08-03. Use directly. -->

Page shell analog — `src/pages/Quality.tsx:1-17` and its `return` at line 152:
```typescript
import { SectionHeader } from "../components/SectionHeader";
import SectionErrorBoundary from "../components/SectionErrorBoundary";
import { PageHeader } from "@/components/PageHeader";
...
  <div className="space-y-6">
    ...
      <PageHeader title="Quality" className="mb-0" />
```

Route analog — `src/App.tsx:29` and `:108`:
```typescript
const Quality = lazy(() => import("./pages/Quality"));
<Route path="/quality" element={<Suspense fallback={<div className="text-muted-foreground text-base p-8 text-center">Loading Quality...</div>}><Quality /></Suspense>} />
```

Nav group — `src/lib/navRegistry.ts:149-168` (live):
```typescript
{
  group: "OBSERVE",
  items: [
    { to: "/", label: "Dashboard", icon: "grid", group: "OBSERVE" },
    ...
    { to: "/quality", label: "Quality", icon: "gauge", group: "OBSERVE" },
    ...
    { to: "/insights", label: "Insights", icon: "insights", group: "OBSERVE" },
  ],
},
```
Icon map — `src/lib/navRegistry.ts:53-91`, e.g. `gauge: Gauge,   // Phase 93 — Quality page`.
Icon resolution — `src/layouts/DashboardLayout.tsx:78`: `iconComponents[item.icon] ?? LayoutDashboard`.

Components to mount (plans 105-06 / 105-07): `src/components/ToolUsagePanel.tsx` and
`src/components/ToolPolicyFeed.tsx`, both default exports taking no required props.

UI-SPEC copy: cross-link text is exactly `View in Tools →`, styled `text-primary`.
UI-SPEC spacing: `2xl` (48px) is the Usage → Policy section break; page container uses the same
`space-y-6` as `Quality.tsx`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: src/pages/Tools.tsx — stacked sections, two boundaries, no tabs</name>
  <read_first>
    - src/pages/Quality.tsx (the full file — the page shell, PageHeader usage, SectionHeader/SectionErrorBoundary composition and the `space-y-6` container)
    - src/components/ToolUsagePanel.tsx (plan 105-06's output — its props and what it already renders, so the page does not duplicate a header or a filter)
    - src/components/ToolPolicyFeed.tsx (plan 105-07's output — same)
    - src/components/SectionErrorBoundary.tsx (its `name` prop contract)
    - .planning/phases/105-tool-trace-observability/105-UI-SPEC.md § Visual Hierarchy, § Spacing Scale, § Copywriting Contract
  </read_first>
  <files>src/pages/Tools.tsx</files>
  <action>
Create `src/pages/Tools.tsx` as a default-export page component.

Structure:
```
<div className="space-y-6">
  <PageHeader title="Tools" />            {/* icon: the Lucide Wrench, matching the nav entry */}
  <section>
    <SectionErrorBoundary name="Tool Usage">
      <ToolUsagePanel />
    </SectionErrorBoundary>
  </section>
  <section className="pt-12">            {/* the UI-SPEC's 2xl / 48px major section break */}
    <SectionErrorBoundary name="Tool Policy Events">
      <ToolPolicyFeed />
    </SectionErrorBoundary>
  </section>
</div>
```
Usage FIRST, policy feed BENEATH — the order is locked by D-16.

Add a short muted sub-line under the `PageHeader` explaining what the page is and, per the UI-SPEC's
Visual Hierarchy note, cross-linking OUT to the trace waterfall's home (Sessions → a session →
Trace) so an operator can find per-turn depth from here. Keep it one sentence, `text-sm
text-muted-foreground`.

Do NOT import `Tabs` (finding F6). Do NOT add a page-level `TooltipProvider` unless a child needs
one and does not own it — `InfoTooltip` is self-contained, and `TraceWaterfall` owns its own
provider; adding a redundant one is harmless but adding one because a child crashed without it means
you have found a real bug in that child, which should be fixed there (this is the `a9ef7e16`
BrainPicker lesson: a Radix `Tooltip` with no provider ancestor crashed two of three entry points).

Do not duplicate any header, filter or empty state that `ToolUsagePanel`/`ToolPolicyFeed` already
render — this file is a shell.
  </action>
  <acceptance_criteria>
    - `grep -c "SectionErrorBoundary" src/pages/Tools.tsx` returns at least `3` (import + two usages).
    - `grep -c 'name="Tool Usage"' src/pages/Tools.tsx` returns `1` and `grep -c 'name="Tool Policy Events"' src/pages/Tools.tsx` returns `1`.
    - `grep -c "ui/tabs" src/pages/Tools.tsx` returns `0`.
    - `grep -Ei '#[0-9a-f]{3,8}' src/pages/Tools.tsx | grep -v '^\s*//'` returns zero matches.
    - The usage section's JSX appears BEFORE the policy section's: `grep -n "Tool Usage\|Tool Policy Events" src/pages/Tools.tsx` shows the Usage line at a lower line number.
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>One scrolling page with usage above the policy feed, each in its own named boundary, and no tabs.</done>
</task>

<task type="auto">
  <name>Task 2: Route + OBSERVE nav entry + the missing Wrench icon mapping</name>
  <read_first>
    - src/App.tsx lines 20-40 (the `lazy(...)` declaration block) and 100-120 (the OBSERVE-tier routes, including the Quality route's exact Suspense fallback markup)
    - src/lib/navRegistry.ts lines 12-95 (the lucide-react import list and the full `iconComponents` map)
    - src/lib/navRegistry.ts lines 149-168 (the live OBSERVE group — finding F1)
    - src/layouts/DashboardLayout.tsx lines 70-90 (how `item.icon` resolves, and the silent `?? LayoutDashboard` fallback)
  </read_first>
  <files>src/App.tsx, src/lib/navRegistry.ts</files>
  <action>
`src/lib/navRegistry.ts`:
- Add `Wrench` to the `lucide-react` import list, keeping the existing ordering style of that block.
- Add `wrench: Wrench,   // Phase 105 — Tools page` to `iconComponents`, beside the
  `gauge: Gauge,   // Phase 93 — Quality page` entry it mirrors.
- Add `{ to: "/tools", label: "Tools", icon: "wrench", group: "OBSERVE" },` to the OBSERVE `items`
  array. Place it immediately AFTER the `/quality` entry — D-14 puts it "beside Analytics, Alerts,
  Quality and Security", and that slot keeps the observability cluster contiguous.
- Do NOT touch the `GRAPHS` group: Tool Galaxy (3D viz over `callGraphEdges`) and MCP Inventory
  (governance) stay there deliberately, as genuinely different concerns (D-14).

`src/App.tsx`:
- Add `const Tools = lazy(() => import("./pages/Tools"));` in the lazy block, with a
  `// Phase 105: Tool & Trace Observability (OBS-01/OBS-02)` comment matching the file's existing
  comment style.
- Add the route copying the Quality route's markup verbatim, substituting the path, component and
  fallback text: `<Route path="/tools" element={<Suspense fallback={<div className="text-muted-foreground text-base p-8 text-center">Loading Tools...</div>}><Tools /></Suspense>} />`.
  Place it beside the other OBSERVE-tier routes.
- Confirm no existing route already claims `/tools` before adding (grep first; a duplicate path
  would shadow silently).
  </action>
  <acceptance_criteria>
    - `grep -c 'to: "/tools"' src/lib/navRegistry.ts` returns `1`.
    - `grep -c "wrench: Wrench" src/lib/navRegistry.ts` returns `1` AND `grep -c "  Wrench," src/lib/navRegistry.ts` returns `1` (both halves of finding F2 — the mapping alone silently falls back to LayoutDashboard).
    - `grep -c 'path="/tools"' src/App.tsx` returns `1`.
    - `grep -c './pages/Tools' src/App.tsx` returns `1`.
    - `git diff src/lib/navRegistry.ts | grep -c "GRAPHS"` returns `0` (the GRAPHS group is untouched).
    - `npm run build` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit && npm run build</automated>
  </verify>
  <done>`/tools` resolves to a lazily-loaded page and appears in the OBSERVE nav group with a real Wrench icon, not a silently-defaulted one.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: D-15 cross-links + page test</name>
  <read_first>
    - src/components/ToolBreakdown.tsx (full file — where its header/title sits, so the link is additive)
    - src/components/ToolExecutionPanel.tsx lines 120-235 (its header/summary region)
    - src/components/PermissionDecisionsChart.tsx (full file)
    - src/pages/Tools.tsx (Task 1's output)
  </read_first>
  <files>src/components/ToolBreakdown.tsx, src/components/ToolExecutionPanel.tsx, src/components/PermissionDecisionsChart.tsx, src/pages/Tools.test.tsx</files>
  <behavior>
    - Each of the three panels renders a `View in Tools →` link whose `href` resolves to `/tools`.
    - `Tools.tsx` renders both section boundaries' children when both child components render normally.
    - When `ToolUsagePanel` throws, the policy feed still renders — the two boundaries are independent (the D-16 / `3b31c9f4` isolation control).
    - The page renders no tab-role element.
  </behavior>
  <action>
**Cross-links (D-15).** In each of `src/components/ToolBreakdown.tsx`,
`src/components/ToolExecutionPanel.tsx` and `src/components/PermissionDecisionsChart.tsx`, add
`import { Link } from "react-router-dom";` (if not already present) and a single
`<Link to="/tools" className="text-primary text-xs font-mono tracking-widest uppercase hover:underline">View in Tools →</Link>`
placed in each component's existing header row, right-aligned via the row's existing flex layout.

ADD ONLY. Do not restructure a header, do not change a grid, do not alter any existing className on
an existing element, do not touch data flow. The UI-SPEC's Component Reuse Map is explicit: "additive
only, zero layout change to those three components." Note that `ToolExecutionPanel` and
`PermissionDecisionsChart` were already edited by plan 105-01 (the `excludeProvider` argument) — do
not revert or restyle anything from that plan.

If a component has no existing header row to hang the link on, add the smallest possible wrapper
(`<div className="flex items-center justify-between">` around the existing title element and the
link) and say so explicitly in the SUMMARY as a deviation, with the before/after markup.

**Page test.** Create `src/pages/Tools.test.tsx`. Mock `./components/ToolUsagePanel` and
`./components/ToolPolicyFeed` as trivial stubs so the page test does not re-test their internals,
and mock `react-router-dom` only as far as needed. Cover every `<behavior>` bullet. The boundary-
isolation control is the important one: make the mocked `ToolUsagePanel` throw on render and assert
that (a) `SectionErrorBoundary`'s `Tool Usage failed to load` text appears and (b) the policy feed
stub is STILL in the document. Suppress the expected React error-boundary console noise for that one
test only.

Add link assertions to `src/components/ToolExecutionPanel`'s and `PermissionDecisionsChart`'s
existing test files (both exist after plan 105-01) and to `ToolBreakdown`'s if it has one; if it does
not, assert all three links from `src/pages/Tools.test.tsx` is NOT acceptable — the links live in
the panels, so create a minimal test file for `ToolBreakdown` instead.
  </action>
  <acceptance_criteria>
    - `grep -c 'View in Tools' src/components/ToolBreakdown.tsx src/components/ToolExecutionPanel.tsx src/components/PermissionDecisionsChart.tsx` returns `1` for each of the three files.
    - `git diff --numstat src/components/ToolBreakdown.tsx src/components/ToolExecutionPanel.tsx src/components/PermissionDecisionsChart.tsx` shows fewer than 8 added lines and 0-2 deleted lines per file (additive only — a large deletion count means a header was restructured).
    - `git diff --stat src/pages/Dashboard.tsx src/pages/Analytics.tsx` produces NO output (the mount sites are untouched — finding F3).
    - `npx vitest run src/pages/Tools.test.tsx` exits 0, including a test whose name contains `isolation` or `boundary` proving one section's failure does not take the other down.
    - `npx vitest run` (full suite) exits 0; `npm run build` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/pages/Tools.test.tsx src/components/ToolExecutionPanel.test.tsx src/components/PermissionDecisionsChart.test.tsx</automated>
  </verify>
  <done>All three panels link into `/tools` with no layout change, and the page's two boundaries are proven independent.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| router → lazily-loaded page chunk | A new public route; no auth gate is added or removed |
| one section's Convex query failure → the whole page | A throwing Convex query unmounts the React tree unless a boundary contains it |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-105-36 | Denial of Service | a timing-out query on one section blanking the whole Tools page | mitigate | Two independent `SectionErrorBoundary` wrappers (D-16), proven by a render-throw isolation test. This is the direct remediation shape of `3b31c9f4`, where one timing-out query blanked all of Analytics because a single panel lacked a boundary while 35 siblings had one. |
| T-105-37 | Denial of Service | a regression on four working panels from an "additive" edit | mitigate | Additive-only constraint enforced by a `git diff --numstat` line-count gate and by asserting `Dashboard.tsx`/`Analytics.tsx` have empty diffs. This milestone has already shipped defects in exactly this class. |
| T-105-38 | Spoofing | a nav item silently rendering the wrong icon | mitigate | `DashboardLayout.tsx:78` falls back to `LayoutDashboard` for an unmapped key, so a missing map entry looks fine and is wrong. Both halves (import + map entry) are grep-asserted. |
| T-105-39 | Information Disclosure | a new unauthenticated page surface | accept | Read-only, same auth posture as every other OBSERVE page; no mutating control ships on it (the phase boundary: observes, never enforces). Tracked with T-105-04 for Phase 106. |
| T-105-40 | Elevation of Privilege | a destructive or mutating action reaching this page | mitigate | The UI-SPEC's Destructive-confirmation row is "None" by design. Assert it: `grep -c "useMutation" src/pages/Tools.tsx src/components/ToolUsagePanel.tsx src/components/ToolPolicyFeed.tsx` must return `0` for all three. |
| T-105-SC | Tampering | package installs | mitigate | Zero packages installed. `git diff --stat package.json` must be empty. |
</threat_model>

<verification>
- `npx vitest run` full suite green; `npx tsc --noEmit` clean; `npm run build` clean.
- `git diff --stat src/pages/Dashboard.tsx src/pages/Analytics.tsx` empty.
- `grep -c "useMutation" src/pages/Tools.tsx src/components/ToolUsagePanel.tsx src/components/ToolPolicyFeed.tsx` returns 0 for each (T-105-40).
- NOT verifiable here, deferred to plan 105-09: that `/tools` renders correctly against live data and across all four selectable themes. Note for that plan — `index.css` defines six theme blocks but `index.html`'s pre-paint script and `ThemeSwitcher` both hard-whitelist the same FOUR (`cyan`, `emerald`, `readable`, `aubergine`); `amber` and the light `:root` are unreachable (pre-existing since Phase 89, found live in 104-11). A "six-theme sweep" is not executable — sweep the four.
</verification>

<success_criteria>
- `/tools` exists as a lazily-loaded page in the OBSERVE nav group with a real Wrench icon.
- The page is one scroll: usage above the policy feed, each in its own named `SectionErrorBoundary`, no tabs.
- The three existing panels each link into it and are otherwise byte-for-byte unchanged apart from that link; their mount pages have empty diffs.
- No mutating control ships anywhere on this page.
</success_criteria>

<output>
Create `.planning/phases/105-tool-trace-observability/105-08-SUMMARY.md` when done. It MUST record:
any header-wrapper deviation on the three cross-linked panels (with before/after markup), the
per-file `git diff --numstat` for those three, the `SectionErrorBoundary` hex-debt flag for Phase
106, and the four-not-six theme constraint for plan 105-09.
</output>
