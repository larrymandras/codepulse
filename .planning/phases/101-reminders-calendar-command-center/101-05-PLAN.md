---
phase: 101-reminders-calendar-command-center
plan: 05
type: execute
wave: 3
depends_on: [02]
repo: astridr-repo
files_modified:
  - C:\Users\mandr\astridr-repo\astridr\automation\reminder_nudge.py
  - C:\Users\mandr\astridr-repo\tests\automation\test_reminder_nudge.py
  - C:\Users\mandr\astridr-repo\astridr\automation\jobs.py
autonomous: true
requirements: [REM-05, REM-04]
must_haves:
  truths:
    - "a cron scans Convex dueSoon/overdue (via /reminders-read per profile or a dedicated due query) and sends ONE ProactiveMessenger.send_alert per due reminder to that profile's channel, then writes notifiedAt back so it never nudges the same occurrence twice (D-04, D-11, REM-05)"
    - "the nudge targets the reminder's profile channel — a business reminder never nudges the personal channel (D-04)"
    - "recurrence-on-pass: when a recurring reminder's dueAt has passed, the next occurrence is spawned (via the reminders complete/roll path from plan 01) so the series continues even if Larry never marks it done (D-05, REM-04)"
    - "nudge scheduling lives in Astridr (not a Convex cron) because ProactiveMessenger + the outbound gate are Astridr-side; Convex only exposes the due queries (D-11)"
  artifacts:
    - path: "C:\\Users\\mandr\\astridr-repo\\astridr\\automation\\reminder_nudge.py"
      provides: "due-scan -> send_alert -> notifiedAt dedupe + recurrence roll on pass"
      contains: "send_alert"
  key_links:
    - from: "reminder_nudge cron"
      to: "ProactiveMessenger.send_alert"
      via: "per due reminder, per profile channel"
      pattern: "send_alert"
---

<objective>
Make reminders actually remind. An Ástríðr cron scans due/overdue reminders, sends a proactive nudge to the reminder's profile channel exactly once (deduped via `notifiedAt`), and rolls recurring series forward when an occurrence passes. REM-05 + the runtime half of REM-04. Scheduling is Ástríðr-side by design (D-11) — Convex only stores the due queries.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/101-reminders-calendar-command-center/101-CONTEXT.md
@.planning/phases/101-reminders-calendar-command-center/101-01-PLAN.md
@.planning/phases/101-reminders-calendar-command-center/101-02-PLAN.md

<interfaces>
From astridr/automation/proactive.py:
- ProactiveMessenger.send_alert(...) -> pushes to a channel (already has an outbound gate). Read its signature for the channel/profile targeting arg.
Due data source: reuse /reminders-read per profile and filter due locally, OR add a small `dueForNudge` read on the endpoint (only if a per-profile read is too coarse — prefer reusing /reminders-read to avoid a new endpoint).
Dedupe: after a successful send, POST /reminders-ingest op:update {id, notifiedAt: now}. A row with notifiedAt already set for its current occurrence is NOT re-nudged.
Recurrence roll: to advance a passed recurring occurrence, call op:complete (plan 01 complete() spawns the next occurrence and clears notifiedAt) — OR a dedicated roll op if complete() semantics (marking done) are wrong for an auto-roll; decide during execution and document. The invariant that MUST hold: a passed recurring series keeps producing the next open occurrence; a passed one-off does not silently vanish (it stays overdue until Larry acts).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: due-scan + single nudge + notifiedAt dedupe</name>
  <files>C:\Users\mandr\astridr-repo\astridr\automation\reminder_nudge.py, C:\Users\mandr\astridr-repo\tests\automation\test_reminder_nudge.py</files>
  <read_first>
    - astridr/automation/proactive.py (send_alert signature + channel targeting)
    - astridr/automation/heartbeat.py (how ProactiveMessenger is invoked from a periodic scan — the alert-forward precedent)
    - 101-02 /reminders-read + /reminders-ingest op:update contract
  </read_first>
  <behavior>
    - For each profile: read reminders, select due (dueAt <= now, status open|snoozed past snoozedUntil) with notifiedAt unset for the current occurrence.
    - For each selected: send_alert to THAT profile's channel with a concise reminder message; on success POST op:update {id, notifiedAt: now}.
    - A reminder already carrying notifiedAt for its current occurrence is skipped (no double-nudge). Snoozed reminders whose snoozedUntil is still in the future are skipped.
  </behavior>
  <action>
    Implement reminder_nudge.run(). Target the profile's channel (map profileId -> channel per ProactiveMessenger's contract; confirm each profile resolves to a real channel — prerequisite in CONTEXT). Dedupe strictly on notifiedAt. Tests mock send_alert + the HTTP read/update: a due personal reminder nudges the personal channel once and sets notifiedAt; a second scan does NOT nudge it again; a business reminder nudges the business channel (never personal); a future-snoozed reminder is skipped.
  </action>
  <verify>
    <automated>cd /c/Users/mandr/astridr-repo && python -m pytest tests/automation/test_reminder_nudge.py -q</automated>
  </verify>
  <acceptance_criteria>
    - Test: exactly one send_alert per due reminder; a re-scan after notifiedAt is set sends zero.
    - Test: the nudge goes to the reminder's own profile channel (business != personal).
    - Test: a future-snoozed reminder is not nudged.
  </acceptance_criteria>
  <done>Due reminders nudge once to the right channel and never repeat.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: recurrence roll on pass + job registration</name>
  <files>C:\Users\mandr\astridr-repo\astridr\automation\reminder_nudge.py, C:\Users\mandr\astridr-repo\tests\automation\test_reminder_nudge.py, C:\Users\mandr\astridr-repo\astridr\automation\jobs.py</files>
  <read_first>
    - 101-01 complete() recurrence-spawn behavior
    - astridr/automation/jobs.py / cron.py (periodic registration)
  </read_first>
  <behavior>
    - When a recurring reminder's occurrence has passed, advance the series (spawn next open occurrence) so it keeps going even unattended; a passed one-off is left overdue (not auto-completed, not vanished).
    - Register run() on a periodic schedule (named interval constant).
  </behavior>
  <action>
    Add the recurrence-roll step (call op:complete / roll per the interfaces decision). Guard: only recurring reminders roll; one-offs never auto-complete. Register the periodic job. Tests: a passed recurring reminder produces a next open occurrence after run(); a passed one-off remains a single overdue open row (no spawn, no auto-done).
  </action>
  <verify>
    <automated>cd /c/Users/mandr/astridr-repo && python -m pytest tests/automation/test_reminder_nudge.py -q</automated>
  </verify>
  <acceptance_criteria>
    - Test: a passed recurring reminder yields a next open occurrence; a passed one-off does not spawn and is not auto-completed.
    - The job is registered with a named interval constant.
  </acceptance_criteria>
  <done>Recurring series roll forward unattended; one-offs stay overdue; the scan runs on a schedule.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| reminder text -> outbound nudge | reminder title/notes (possibly from either surface) is sent to a channel |
| cron -> Convex | authed read/update (ASTRIDR_INGEST_API_KEY) |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-101-11 | Info disclosure | wrong-channel nudge | mitigate | Nudge targets the reminder's own profileId->channel; unit-pinned business!=personal. Prevents a consulting/business reminder leaking to the personal channel. |
| T-101-12 | DoS (nudge storm) | dedupe | mitigate | notifiedAt dedupe + single send per occurrence; a re-scan sends zero. Prevents the "queue of doomed jobs / repeat-fire" class (see lessons: rate-limiter must survive). |
| T-101-13 | Spoofing | outbound gate | accept | Reuses ProactiveMessenger's existing outbound gate/verification; no gate bypassed. |
</threat_model>

<verification>
- `cd /c/Users/mandr/astridr-repo && python -m pytest tests/automation/test_reminder_nudge.py -q` green.
- Manual (live): create a reminder due in 1 min via the tool; confirm exactly one Telegram nudge on the right profile and notifiedAt set; a recurring one spawns the next occurrence.
</verification>

<success_criteria>
- Due reminders nudge once, to the correct profile channel, deduped by notifiedAt.
- Recurring series roll forward unattended; one-offs stay overdue.
- Scheduling is Ástríðr-side; no Convex cron added.
</success_criteria>

<output>
Create `.planning/phases/101-reminders-calendar-command-center/101-05-SUMMARY.md` when done.
</output>
