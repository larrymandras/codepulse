---
phase: 101-reminders-calendar-command-center
plan: 03
type: execute
wave: 3
depends_on: [02]
repo: astridr-repo
files_modified:
  - C:\Users\mandr\astridr-repo\astridr\tools\reminders.py
  - C:\Users\mandr\astridr-repo\tests\tools\test_reminders.py
  - C:\Users\mandr\astridr-repo\config\tools.yaml
autonomous: true
requirements: [REM-03]
must_haves:
  truths:
    - "the reminders tool lets Astridr add/list/update/complete/snooze reminders conversationally, writing the SAME Convex store CodePulse uses via ConvexHandler.send_to('/reminders-ingest', ...) with source 'astridr' (D-01, D-07, REM-03)"
    - "list reads through the authed /reminders-read endpoint (POST + Bearer), never an anonymous GET (D-07)"
    - "the registered tool's manifest tool_id equals the tool .name exactly, or ActivationManager silently de-registers it (past gotcha) — verified by a registration test"
    - "the tool NEVER writes Google Calendar and has no calendar-write action — reminders are the only thing it mutates (D-02)"
  artifacts:
    - path: "C:\\Users\\mandr\\astridr-repo\\astridr\\tools\\reminders.py"
      provides: "RemindersTool(BaseTool) with add/list/update/complete/snooze via ConvexHandler.send_to + /reminders-read"
      contains: "send_to"
  key_links:
    - from: "RemindersTool.execute"
      to: "/reminders-ingest"
      via: "ConvexHandler.send_to"
      pattern: "reminders-ingest"
---

<objective>
Give Ástríðr a `reminder` tool so Larry can add / list / update / complete / snooze reminders by talking to her on any channel — writing the exact Convex store CodePulse writes (D-01), tagged `source:"astridr"` (D-09). This is REM-03 and makes the "feed Ástríðr reminders, she keeps track" half of the feature real.

Reads go through the authed `/reminders-read` (D-07). The tool has NO calendar-write action (D-02).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/101-reminders-calendar-command-center/101-CONTEXT.md
@.planning/phases/101-reminders-calendar-command-center/101-02-PLAN.md

<interfaces>
From astridr/tools/base.py:
- class BaseTool(ABC): `name`, parameters/`input_schema`, async `execute(**kwargs) -> ToolResult`; required_credentials per-subclass.
From astridr/engine/telemetry.py:
- ConvexHandler.send_to(endpoint: str, event_type: str, data: dict) -> None  // posts to <base>/<endpoint> reusing configured base URL + ASTRIDR_INGEST_API_KEY
  NOTE: send_to's event_type/data envelope may not match a raw ingest body 1:1 — read its impl (telemetry.py L309-341) and either reuse it or add a thin authed POST helper on the same base_url+key if the envelope doesn't fit /reminders-ingest's {op,...} contract. Do NOT invent a second api key.
Convex endpoints (plan 02): POST /reminders-ingest {op:"create"|"update"|"complete", profileId,title,...}; POST /reminders-read {profileId} -> {reminders:[...]}.
Reminder fields: see 101-01 interfaces (profileId personal|business|consulting, title, dueAt epoch-seconds, priority, recurrence rrule-lite, ...).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RemindersTool with 5 actions</name>
  <files>C:\Users\mandr\astridr-repo\astridr\tools\reminders.py, C:\Users\mandr\astridr-repo\tests\tools\test_reminders.py</files>
  <read_first>
    - astridr/tools/base.py (BaseTool contract, ToolResult)
    - astridr/engine/telemetry.py L203-341 (ConvexHandler.send_to, base_url + key)
    - an existing simple tool (e.g. astridr/tools/google_workspace.py execute dispatch) for the action-dispatch idiom
  </read_first>
  <behavior>
    - parameters: action enum [add, list, update, complete, snooze]; profileId enum [personal, business, consulting]; plus title/notes/dueAt/priority/recurrence/tags/id/snoozeUntil as needed per action.
    - add -> POST /reminders-ingest op:create (source astridr). list -> POST /reminders-read, returns a readable summary. update -> op:update. complete -> op:complete. snooze -> op:update with status snoozed + snoozedUntil.
    - dueAt accepted as epoch seconds OR an ISO string the tool converts (Astridr passes NL-resolved times); normalize to epoch seconds before sending.
    - errors from the endpoint surface as a ToolResult error, not a crash.
  </behavior>
  <action>
    Implement RemindersTool(BaseTool). Dispatch on `action`. Use ConvexHandler.send_to (or a thin authed POST on its base_url+key if the envelope doesn't fit — see interfaces note) to hit the endpoints. Never hold Google creds; no calendar action. Write tests mocking the HTTP layer: each action posts the right op/body to the right endpoint with source astridr; a non-2xx response yields a ToolResult error; dueAt ISO is normalized to epoch seconds.
  </action>
  <verify>
    <automated>cd /c/Users/mandr/astridr-repo && python -m pytest tests/tools/test_reminders.py -q</automated>
  </verify>
  <acceptance_criteria>
    - Test: add posts op:create with source:"astridr" and title to /reminders-ingest.
    - Test: list posts {profileId} to /reminders-read and summarizes the returned rows.
    - Test: an endpoint 401/500 becomes a ToolResult error (no unhandled exception).
    - Test: there is NO action that writes Google Calendar.
  </acceptance_criteria>
  <done>The tool performs all 5 reminder actions against the authed endpoints with source astridr.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Register the tool (tool_id == name)</name>
  <files>C:\Users\mandr\astridr-repo\config\tools.yaml, C:\Users\mandr\astridr-repo\tests\tools\test_reminders.py</files>
  <read_first>
    - config/tools.yaml (how google_workspace / other tools are registered; manifest tool_id shape)
    - the registration/ActivationManager path that pins tool_id to .name (see memory: manifest tool_id must equal tool .name)
  </read_first>
  <behavior>
    - The tool is registered with tool_id EXACTLY equal to RemindersTool.name; it appears in the active tool set.
  </behavior>
  <action>
    Add the reminders tool to config/tools.yaml following the existing tool entries. Add a test asserting the manifest tool_id equals the tool's .name (the mismatch that silently de-registers). Confirm required_credentials only include ASTRIDR_INGEST_API_KEY (no Google creds — this tool never touches Google).
  </action>
  <verify>
    <automated>cd /c/Users/mandr/astridr-repo && python -m pytest tests/tools/test_reminders.py -q</automated>
  </verify>
  <acceptance_criteria>
    - Test asserts config tool_id == RemindersTool().name.
    - The tool requires only the ingest key, not Google creds.
  </acceptance_criteria>
  <done>The tool registers and activates (no silent de-registration).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Larry's NL request -> tool -> /reminders-ingest | free-text title/notes crosses into the store and later renders in CodePulse |
| tool -> Convex | outbound authed write with ASTRIDR_INGEST_API_KEY |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-101-05 | EoP/Spoofing | outbound auth | mitigate | Reuse the existing ASTRIDR_INGEST_API_KEY via ConvexHandler; no new/second key, no key in logs. |
| T-101-06 | Tampering (XSS) | title/notes text | mitigate downstream | Carried as data; page renders as React text (plan 02 T-101-03). Tool does not pre-render HTML. |
| T-101-07 | EoP | Google | eliminate | The tool has no Google credential and no calendar action — it structurally cannot write Google (D-02). |
</threat_model>

<verification>
- `cd /c/Users/mandr/astridr-repo && python -m pytest tests/tools/test_reminders.py -q` green.
- Manual: `grep -n "reminders-ingest\|reminders-read" astridr/tools/reminders.py` present; no google import in the file.
</verification>

<success_criteria>
- RemindersTool add/list/update/complete/snooze work against the authed endpoints, source astridr.
- Registered with tool_id == name; requires only the ingest key.
- No Google/calendar write path in the tool.
</success_criteria>

<output>
Create `.planning/phases/101-reminders-calendar-command-center/101-03-SUMMARY.md` when done.
</output>
