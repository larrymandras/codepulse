---
status: complete
phase: 101-reminders-calendar-command-center
source: [101-01-SUMMARY.md, 101-02-SUMMARY.md, 101-03-SUMMARY.md, 101-04-SUMMARY.md, 101-05-SUMMARY.md, 101-06-SUMMARY.md]
started: 2026-07-20T21:06:31Z
updated: 2026-07-20T22:12:00Z
---

<!-- Tests 1-7, 9, 10 were executed by Claude via Playwright automation (2026-07-20, scripts in
     session scratchpad: uat-reminders-a.js / uat-reminders-b.js) against a Clerk-disabled Vite
     instance on :5174 (fresh Playwright profiles can't pass Google OAuth; identical code, only
     VITE_CLERK_PUBLISHABLE_KEY unset). Astridr-side tests verified via astridr-agent docker logs.
     Evidence per test inline. Test 8's conversational half is the only human-pending item. -->

## Current Test

[testing complete — gap diagnosed, fix plan 101-07 written & checker-verified; retest test 8's
NL half (real Ástríðr chat exchange) after executing the gap fix]

## Tests

### 1. Cold Start Smoke Test
expected: Fresh dev-server start boots without errors; /reminders loads live data with no Convex "function not found" / blank page.
result: pass
evidence: Killed Vite (pid 43388), supervisor cold-relaunched — ready in 497ms. /reminders loaded with data, ZERO console errors across two full automation runs. Browser talks to LOCAL self-hosted Convex (wss://lmofficenew.tail5bb6b3.ts.net/api/1.42.1/sync) and all reminders/calendarEvents functions are live there. Note: Vite binds ::1 only — probe via localhost, not 127.0.0.1.

### 2. Reminders in Nav + Page Load
expected: "Reminders" in COMMAND nav cluster; lazy route loads two-pane layout.
result: pass
evidence: a[href="/reminders"] visible in sidebar; click → route loaded, page non-blank, no lazy-chunk errors.

### 3. Profile Tabs — Accents, Persistence, Dual-Pane Swap
expected: Three tabs with distinct accents; selection persists across reload; switching swaps both panes.
result: pass
evidence: Selected-tab accents measured distinct — Personal var(--status-ok)→#06b6d4, Business var(--status-warn)→#eab308, Consulting var(--status-info)→#3b82f6 (cyan theme), each with matching oklch glow. localStorage["codepulse-reminders-profile"]="business" persisted; aria-selected="true" on Business after reload. Profile switch re-queried both reminder list and calendar chips (chips per profile: personal 2 / business 16 / consulting 0).

### 4. Quick-Add Bar + "N" Shortcut
expected: "N" focuses quick-add (not while typing elsewhere); Ctrl+K untouched; created reminder appears in correct group and persists.
result: pass
evidence: "N" focused [aria-label="Reminder title"]; "n" pressed inside the Due-date input did NOT steal focus. Created reminder (due +2h) appeared in section "Today" instantly and survived reload.

### 5. Reminder List Grouping & Row Details
expected: Overdue (pulse) / Today / Upcoming / Done (collapsed); relative due times, priority pip, recurrence icon, origin marker, tags.
result: pass
evidence: Overdue reminder landed in Overdue group with a running CSS animation (motion allowed) and "5m overdue" relative text; recurring row showed lucide repeat icon; Done renders as a Collapsible ("DONE 3"), collapsed by default with correct count. Ástríðr-origin marker code-verified (aria-label="Created by Ástríðr") but not exercised live — no astridr-created reminder existed during the run (see test 8).

### 6. Complete / Snooze / Edit — Optimistic + Recurring Spawn
expected: Instant optimistic actions that persist; completing recurring spawns next occurrence; one-off spawns nothing.
result: pass
evidence: Completing daily-recurring reminder spawned next open occurrence "in 1d" while original went to Done (Done count 3 matched exactly the 3 completed, i.e. one-offs spawned nothing). Snooze popover ("1 hour / This evening / Tomorrow / Set") shifted relative due "in 1h"→"in 59m". Edit-title persisted in list. All optimistic (no reload needed), all consistent after reload.

### 7. Calendar Overlay — Events + Reminder Chips, Read-Only
expected: Month/week grid; Google events as outline chips; reminders as priority chips; day-click filters; read-only.
result: pass
evidence: 35 day cells, prev/next controls, week/month toggle exercised. Real Google event chips + reminder chips rendered; clicking a chip-bearing day filtered list 3→2 rows. Read-only enforced by construction (source-scan test asserts no calendarEvents mutation import; no write handler found).

### 8. Ástríðr Conversational Reminders — Realtime Round-Trip
expected: Chat-created reminder appears on page realtime with origin marker; list/complete via chat sync both ways. Known limitation: snooze via Ástríðr shifts dueAt only (documented op:"snooze" contract gap).
result: issue
reported: "kind of working, i ad the reminders, they show up, but when i click on a furure different day in the calendar they do not persist as upcomning"
severity: major
note: Chat-add + realtime appearance CONFIRMED working by operator. The issue is scoped to the calendar day-click filter interaction with future-day reminders (repro + diagnosis below).

### 9. Calendar Cache Pipeline — All Three Profiles
expected: Per-profile real Google events, ~60-day window, refreshed by calendar:cache_refresh cron every 20 min.
result: pass
evidence: astridr-agent log 21:20:43Z — cron pushed all three profiles: personal 4 events (window-filtered from 34; mandrasle@gmail.com), business 72 (from 250; lmandras@myprotectall.com), consulting 0 (lemandras@forgedinai.ai genuinely has zero events — honest empty, not a failure). failed=[] on the run. Business chips (16) visible on the page grid.

### 10. Proactive Nudge on Due Reminder
expected: Exactly one alert to the reminder's own profile channel within ~5 min; dedupe on next scan; due recurring rolls forward.
result: pass
evidence: Overdue UAT reminder picked up by the 21:25:39Z reminder:nudge scan — nudged=1 (id kx7vj1g9...), telegram.sent chat_id=7815748065 + proactive.alert_sent, exactly once. 21:30:39Z scan: nudged=0 (notifiedAt dedupe confirmed). rolled-forward path not exercised live (no due recurring existed; covered by unit tests + same op:"complete" endpoint verified in test 6). Operator CONFIRMED on-device receipt (pasted the alert text back in session, 2026-07-20): "Reminder due: UAT nudge test — Claude (expect one Astridr alert)".

## Summary

total: 10
passed: 9
issues: 1
pending: 0
skipped: 0

## Observations (non-gap notes)

- 21:01Z one-off transient: both crons logged profile_error for all three profiles with an EMPTY error string, self-recovered by the next tick. Log-quality nit (empty error=) — candidate astridr backlog item, not a Phase 101 gap.
- Fresh browser profiles hit the Clerk→Google OAuth wall (expected; automation used the documented Clerk-off pattern) and a first-visit onboarding modal that intercepts pointer events (pre-existing, not Phase 101).
- All 5 UAT scratch reminders were removed from the live store post-test via api.reminders.remove against the local backend (verified 0 remaining).
- Known deferred follow-up (already in STATE.md): wire op:"snooze" in convex/remindersIngest.ts onto api.reminders.snooze so Ástríðr snooze can set status:"snoozed" instead of shifting dueAt.

## Gaps

```yaml
- truth: "Reminders without a due date stay visible in the list while a calendar day is selected"
  status: failed
  reason: "User reported: reminders show up (Upcoming), but clicking a future day in the calendar makes them disappear. DIAGNOSED 2026-07-20 with live repro against the user's own rows: 2 visible -> click Jul 21 -> 0 visible ('Showing Tue, Jul 21') -> deselect -> 2 visible."
  severity: major
  test: 8
  artifacts:
    - "src/components/reminders/ReminderList.tsx:472-478 — dayFiltered useMemo: `due !== undefined && startOfDaySeconds(due) === selectedDay` excludes ALL undated rows whenever selectedDay !== null"
    - "src/components/reminders/ReminderList.tsx:493-495 — upcoming group includes `due === undefined`, so undated rows live in Upcoming when no day is selected (why they 'show up' normally)"
  missing:
    - "Undated reminders must remain visible while a day filter is active (they are not tied to any day) — e.g. keep them rendered in an unfiltered 'No due date' sub-group or exempt due===undefined from dayFiltered"
  root_cause: >
    NOT an Astridr/realtime bug. The user's two reminders were created via the QuickAdd bar
    (source:"dashboard", full NL sentence as title, dueAt undefined — QuickAdd does not parse
    natural-language dates and the due field was left empty). Undated rows group under Upcoming,
    but the day-click filter requires a defined dueAt matching the selected day, so selecting any
    day hides them entirely — perceived as reminders not persisting.
```

### Secondary findings from diagnosis (not code gaps)

- **Test 8's NL round-trip is STILL unexercised**: both rows are source:"dashboard" and astridr-agent logs show zero RemindersTool invocations in the window — the add went through QuickAdd, not Ástríðr chat. Retest via actual Telegram/chat after the fix.
- The two rows are duplicates (added twice) — QuickAdd has no dedupe, by design.
- STALE NOTE CORRECTED: the op:"snooze" contract gap documented in 101-03/STATE.md was already closed — convex/remindersIngest.ts now dispatches create/update/complete/snooze/markNotified, and astridr's RemindersTool posts op:"snooze" with until. STATE.md should drop the "known limitation" line at transition.
- Enhancement candidate (backlog, not a gap): QuickAdd could NL-parse dates from the title ("tomorrow at 9am") — the user's instinct was to type a sentence.
