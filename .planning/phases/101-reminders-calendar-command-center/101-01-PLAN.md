---
phase: 101-reminders-calendar-command-center
plan: 01
type: execute
wave: 1
depends_on: []
repo: codepulse
files_modified:
  - C:\Users\mandr\codepulse\convex\schema.ts
  - C:\Users\mandr\codepulse\convex\reminders.ts
  - C:\Users\mandr\codepulse\convex\reminders.test.ts
autonomous: true
requirements: [REM-01, REM-04]
must_haves:
  truths:
    - "reminders is a Convex table and the single source of truth for reminder state — CodePulse mutations write it directly and it is realtime via useQuery (D-01, REM-01)"
    - "every write carries source ('dashboard'|'astridr') recording origin, and source is never used as a write gate — both origins can create/update any row (D-09)"
    - "complete on a recurring reminder sets status 'done'+completedAt AND spawns the next open occurrence (new row, next dueAt from rrule-lite, status 'open', notifiedAt cleared); complete on a one-off spawns nothing (D-05, REM-04)"
    - "computeNextDueAt advances dueAt by {freq,interval,byday?,until?} and returns null past `until` so a bounded recurrence terminates instead of spawning forever (D-05, REM-04)"
    - "listByProfile/dueSoon/overdue read via indexes (by_profile, by_status, by_dueAt) not full scans, and never return status 'done' from dueSoon/overdue (REM-01, REM-05 feed)"
  artifacts:
    - path: "C:\\Users\\mandr\\codepulse\\convex\\reminders.ts"
      provides: "create/update/complete/snooze/remove mutations + listByProfile/dueSoon/overdue queries + computeNextDueAt recurrence helper"
      contains: "computeNextDueAt"
    - path: "C:\\Users\\mandr\\codepulse\\convex\\schema.ts"
      provides: "reminders table definition with by_profile/by_status/by_dueAt indexes"
      contains: "reminders:"
  key_links:
    - from: "reminders.complete"
      to: "reminders (next occurrence row)"
      via: "computeNextDueAt spawn"
      pattern: "computeNextDueAt"
---

<objective>
Create the Convex `reminders` table and its CRUD + query surface — the source of truth (D-01) both CodePulse and Ástríðr write. Include the recurrence engine (D-05): `computeNextDueAt` plus the spawn-on-complete behavior, so a recurring reminder rolls to its next occurrence and a bounded recurrence terminates at `until`.

Purpose: REM-01 (the store) and the Convex half of REM-04 (recurrence) live entirely here. Nothing else in the phase works until this table + queries exist — plan 02 (ingest), 03 (tool), 05 (nudge scan) and 06 (page) all read/write these functions.

Output: `convex/schema.ts` (table + indexes), `convex/reminders.ts` (mutations/queries/helper), `convex/reminders.test.ts` (unit coverage incl. recurrence + termination).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/101-reminders-calendar-command-center/101-CONTEXT.md

<interfaces>
Reminder row shape (authored here; consumed by plans 02/03/05/06 — keep field names stable):
- profileId: "personal" | "business" | "consulting"
- title: string
- notes?: string
- dueAt?: number            // epoch SECONDS (match profiles.ts Date.now()/1000 convention)
- priority: "low" | "med" | "high"
- status: "open" | "done" | "snoozed"
- recurrence?: { freq: "daily"|"weekly"|"monthly"; interval: number; byday?: string[]; until?: number }
- tags?: string[]
- source: "dashboard" | "astridr"
- notifiedAt?: number        // set by the nudge cron (plan 05) to dedupe
- snoozedUntil?: number
- completedAt?: number
- createdAt: number
- updatedAt: number

Timestamp convention: epoch SECONDS everywhere (see convex/profiles.ts `Date.now() / 1000`). Do NOT mix ms.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: reminders table + indexes in schema.ts</name>
  <files>C:\Users\mandr\codepulse\convex\schema.ts</files>
  <read_first>
    - convex/schema.ts (defineTable/.index idiom; see `alerts` table L106-126 for a status-lifecycle analog)
    - convex/profiles.ts (epoch-seconds convention, profile IDs)
  </read_first>
  <behavior>
    - `reminders` table with the interface fields above; optional fields marked v.optional(...).
    - Indexes: by_profile ["profileId","status"], by_status ["status","dueAt"], by_dueAt ["dueAt"].
  </behavior>
  <action>
    Add the `reminders` defineTable to the schema with `v.*` validators matching the interface (recurrence as v.optional(v.object({...})) with freq as a v.union of literals). Add the three indexes. Follow the exact defineTable/.index chaining style already in the file.
  </action>
  <verify>
    <automated>cd /c/Users/mandr/codepulse && npx convex codegen && npx tsc -p convex/tsconfig.json --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "reminders:" convex/schema.ts` matches; the three indexes are present.
    - codegen + typecheck clean.
  </acceptance_criteria>
  <done>The reminders table is defined and codegen'd with the three indexes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: computeNextDueAt recurrence helper (pure, unit-first)</name>
  <files>C:\Users\mandr\codepulse\convex\reminders.ts, C:\Users\mandr\codepulse\convex\reminders.test.ts</files>
  <read_first>
    - 101-CONTEXT.md D-05 (rrule-lite shape + termination)
  </read_first>
  <behavior>
    - computeNextDueAt(dueAt, recurrence) → next epoch-seconds due:
      - daily: +interval days; weekly: +interval weeks (byday optional — if byday given, next matching weekday ≥ +1 day); monthly: +interval months (clamp end-of-month, e.g. Jan 31 +1mo → Feb 28/29).
      - returns null when the computed next is > recurrence.until (bounded recurrence terminates).
      - returns null when recurrence is undefined (one-off).
  </behavior>
  <action>
    Implement computeNextDueAt as an exported pure function (no ctx) so it is unit-testable and reused by the nudge cron logic conceptually. Keep it deterministic (no wall-clock inside — operate on the passed dueAt). Write tests FIRST covering: daily/weekly/monthly advance, byday selection, month-end clamp, `until` termination (→null), and one-off (→null).
  </action>
  <verify>
    <automated>cd /c/Users/mandr/codepulse && npx vitest run convex/reminders.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Tests assert each freq advance, month-end clamp, byday, and that a next-past-`until` returns null.
    - A mutation making computeNextDueAt ignore `until` fails the termination test.
  </acceptance_criteria>
  <done>computeNextDueAt advances correctly per freq and terminates at `until`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: CRUD mutations + queries</name>
  <files>C:\Users\mandr\codepulse\convex\reminders.ts, C:\Users\mandr\codepulse\convex\reminders.test.ts</files>
  <read_first>
    - convex/profiles.ts (mutation/query idiom, withIndex usage, epoch seconds)
    - convex/tasks.ts or convex/alerts.ts (status-lifecycle CRUD analog)
  </read_first>
  <behavior>
    - create(args): inserts with createdAt=updatedAt=now, status default "open", priority default "med", source REQUIRED (caller passes "dashboard" or "astridr"). Returns the new id.
    - update(id, patch): patches allowed mutable fields, bumps updatedAt.
    - complete(id): sets status "done", completedAt=now, updatedAt=now; if the row has recurrence, spawn the next occurrence via computeNextDueAt (new row: same title/profile/priority/recurrence/tags, dueAt=next, status "open", notifiedAt cleared, source carried); if computeNextDueAt returns null, spawn nothing.
    - snooze(id, until): status "snoozed", snoozedUntil=until, updatedAt=now.
    - remove(id): deletes the row.
    - listByProfile(profileId): all rows for a profile via by_profile, ordered sensibly (open/overdue first — or return raw and let the page group; document which).
    - dueSoon(withinSeconds): open|snoozed rows with dueAt <= now+withinSeconds and status != done, via by_dueAt/by_status. overdue(): open|snoozed rows with dueAt < now.
  </behavior>
  <action>
    Implement the mutations/queries following profiles.ts idiom. `source` is a required arg on create — do NOT gate writes on it (D-09). complete() is the recurrence spawn point (D-05) — reuse computeNextDueAt. dueSoon/overdue must exclude status "done" and read via index. Tests: create→listByProfile roundtrip; complete a one-off (no spawn); complete a recurring (spawns one next-open row with cleared notifiedAt); snooze; dueSoon/overdue exclude done and honor the window.
  </action>
  <verify>
    <automated>cd /c/Users/mandr/codepulse && npx vitest run convex/reminders.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Test: completing a recurring reminder yields exactly one additional open row with dueAt == computeNextDueAt(original) and notifiedAt undefined.
    - Test: completing a one-off yields no new row.
    - Test: dueSoon/overdue never include a status 'done' row.
    - Test: create requires source and both 'dashboard' and 'astridr' are accepted and stored verbatim.
  </acceptance_criteria>
  <done>All CRUD + queries pass, recurrence spawns correctly on complete, queries are index-backed and done-excluding.</done>
</task>

</tasks>

<verification>
- `cd /c/Users/mandr/codepulse && npx vitest run convex/reminders.test.ts` green.
- `npx convex codegen && npx tsc -p convex/tsconfig.json --noEmit` clean.
</verification>

<success_criteria>
- reminders table + 3 indexes exist and codegen.
- create/update/complete/snooze/remove + listByProfile/dueSoon/overdue implemented, index-backed.
- Recurrence spawns on complete and terminates at `until`; one-offs never respawn.
- source is stored from both origins and never gates a write.
</success_criteria>

<output>
Create `.planning/phases/101-reminders-calendar-command-center/101-01-SUMMARY.md` when done.
</output>
