---
phase: 79-forge-ui-tab-read-only-render
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/hooks/useForge.ts
  - src/components/forge/ForgeStatusBadge.tsx
  - src/components/forge/ForgeHostBadge.tsx
  - src/components/forge/ForgeStatusBadge.test.tsx
autonomous: true
requirements: [FI-05]
must_haves:
  truths:
    - "A useForge hook returns a merged newest-first list of jobs adapted to the ForgeJobRow shape"
    - "Each job row carries hostId and updatedAt in addition to the forge Job fields"
    - "All 6 JobStatus values render a distinct re-skinned badge using CodePulse tokens"
    - "auth_failed renders amber + KeyRound + 'Auth Failed'; failed renders red + XCircle + 'Failed' (SC#4 preserved)"
    - "A host badge renders the source machine name on demand"
  artifacts:
    - path: "src/hooks/useForge.ts"
      provides: "useForgeJobs (adapted rows), useForgeJobsRaw (undefined-during-load), ForgeJobRow type, adaptJob adapter"
      exports: ["useForgeJobs", "useForgeJobsRaw", "ForgeJobRow"]
    - path: "src/components/forge/ForgeStatusBadge.tsx"
      provides: "6-status re-skinned badge with data-status/data-color-scheme attributes"
      exports: ["ForgeStatusBadge"]
    - path: "src/components/forge/ForgeHostBadge.tsx"
      provides: "outline host chip"
      exports: ["ForgeHostBadge"]
    - path: "src/components/forge/ForgeStatusBadge.test.tsx"
      provides: "label + SC#4 color-distinction + aria contract tests"
  key_links:
    - from: "src/hooks/useForge.ts"
      to: "api.forge.listJobs"
      via: "useQuery(api.forge.listJobs, {})"
      pattern: "useQuery\\(api\\.forge\\.listJobs"
    - from: "src/components/forge/ForgeStatusBadge.tsx"
      to: "ForgeJobRow.status / JobStatus"
      via: "STATUS_MAP keyed by JobStatus"
      pattern: "data-status"
---

<objective>
Build the Wave-1 foundation that every other Phase 79 component imports: the
`useForge` hook (Convex `listJobs` subscription + Convex-row→`ForgeJobRow` type
adapter) and the two leaf presentational badges (`ForgeStatusBadge` re-skinned to
CodePulse tokens, `ForgeHostBadge`). This isolates the type contract and the SC#4
auth_failed≠failed test surface so downstream plans build against fixed interfaces.

Purpose: Downstream plans (79-02 list/detail, 79-03 page/nav) consume `ForgeJobRow`
and these badges directly — defining them first prevents a scavenger hunt and locks
the (hostId, forgeJobId) selection contract.
Output: `src/hooks/useForge.ts`, `src/components/forge/ForgeStatusBadge.tsx`,
`src/components/forge/ForgeHostBadge.tsx`, and a ported badge test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/79-forge-ui-tab-read-only-render/79-CONTEXT.md
@.planning/phases/79-forge-ui-tab-read-only-render/79-UI-SPEC.md
@.planning/phases/79-forge-ui-tab-read-only-render/79-PATTERNS.md

<interfaces>
<!-- Convex read-query contract (src/.../convex/forge.ts) — consumed by useForge. -->
<!-- listJobs({ hostId?: string }) → forgeJobs[] newest-first (by_updatedAt DESC). Pass {} for merged list. -->
<!-- getJob({ hostId: string, forgeJobId: string }) → row | null. Requires BOTH; not needed for P79 detail. -->
<!-- listWorkspaces({ hostId?: string }) → forgeWorkspaces[]. NOT needed in P79. -->

<!-- Convex forgeJobs row fields (from upsertJob args, convex/forge.ts:16-34): -->
<!-- forgeJobId, hostId, agent, mode, prompt(string|null), workspaceId, status, -->
<!-- pid(number|null), exitCode(number|null), startedAt(string|null), finishedAt(string|null), -->
<!-- artifactCount(number), model(string|null), capabilities(string), createdAt(string), updatedAt(string) -->
<!-- plus Convex internal _id, _creationTime (ignore both). -->

<!-- Forge JobStatus union (forge/web/src/types.ts:41-47): -->
<!-- 'queued' | 'running' | 'completed' | 'failed' | 'stopped' | 'auth_failed' -->

<!-- relativeTime(epochSeconds: number) lives in src/lib/formatters.ts — NOT used in this plan but is the formatter ForgeJobRow timestamps feed in 79-02. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: useForge hook + ForgeJobRow type + Convex-row adapter</name>
  <files>src/hooks/useForge.ts</files>
  <read_first>
    - src/hooks/useForge.ts (does not exist yet — creating)
    - src/hooks/useBuildProgress.ts (EXACT analog per PATTERNS.md §useForge.ts — the useQuery(api.X) ?? [] convention)
    - convex/forge.ts (lines 135-182: listJobs/getJob/listWorkspaces signatures — the live consumer contract this hook wraps)
    - C:\Users\mandr\forge\web\src\types.ts (the forge Job/JobStatus/JobMode shapes ForgeJobRow mirrors)
    - .planning/phases/79-forge-ui-tab-read-only-render/79-PATTERNS.md (§useForge.ts full pattern, lines 95-201; §Type Adapter Full Field Map, lines 659-685)
    - .planning/phases/79-forge-ui-tab-read-only-render/79-UI-SPEC.md (§Adapter Contract, lines 257-281)
    - src/layouts/DashboardLayout.tsx (line ~358: the `"skip"` conditional-query idiom for useForgeJob)
  </read_first>
  <behavior>
    - adaptJob maps a Convex forgeJobs doc → ForgeJobRow: doc.forgeJobId→id, plus agent/mode/prompt/workspaceId/status/pid/exitCode/startedAt/finishedAt/artifactCount/capabilities/model/createdAt passed through, plus hostId and updatedAt carried through as extra fields. logFile, _id, _creationTime are NOT mapped.
    - useForgeJobs() returns ForgeJobRow[] — the adapted, merged, newest-first list (Convex already orders by_updatedAt DESC); returns [] during load.
    - useForgeJobsRaw() returns the pre-coalesce value (ForgeJobRow[] | undefined) so the page can distinguish loading (undefined) from empty ([]).
    - useForgeJob(hostId, forgeJobId) passes "skip" to useQuery when either arg is null (optional; not consumed by the P79 detail panel which renders from the list row).
  </behavior>
  <action>
    Create `src/hooks/useForge.ts`. Export interface `ForgeJobRow` = the forge `Job`
    fields (`id` from doc.forgeJobId) plus extra `hostId: string` and `updatedAt: string`;
    do NOT include `logFile`. Define a module-local `adaptJob(doc)` per the field map in
    PATTERNS.md §Type Adapter Full Field Map. Export `useForgeJobsRaw()` wrapping
    `useQuery(api.forge.listJobs, {})` and mapping each row via `adaptJob` only when defined
    (return `undefined` while loading). Export `useForgeJobs()` = `useForgeJobsRaw() ?? []`.
    Export optional `useForgeJob(hostId: string | null, forgeJobId: string | null)` using the
    `"skip"` idiom from DashboardLayout.tsx. Import `api` from `../../convex/_generated/api`.
    Define the `JobStatus`/`JobMode` types inline (or a local `src/components/forge/forgeTypes.ts`)
    — do NOT import from forge's `@/types` alias (that path is forge's, not CodePulse's). Cast
    `doc.status` to the local `JobStatus`. Keep the adapter total: never read `logFile`.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0.
    - src/hooks/useForge.ts exports `useForgeJobs`, `useForgeJobsRaw`, and the `ForgeJobRow` type (grep: `export function useForgeJobs`, `export function useForgeJobsRaw`, `export interface ForgeJobRow`).
    - File contains `useQuery(api.forge.listJobs, {})` (merged-list, no hostId arg per D-03).
    - `ForgeJobRow` declares `hostId` and `updatedAt`; it does NOT declare `logFile` (grep: `logFile` returns no match in this file).
    - adaptJob maps `doc.forgeJobId` to `id` (grep: `forgeJobId`).
  </acceptance_criteria>
  <done>useForge.ts compiles, exports the adapted-list hooks + ForgeJobRow, wraps api.forge.listJobs with the merged-list ({}) call, and omits logFile.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ForgeStatusBadge (re-skin to CodePulse tokens) + ported test</name>
  <files>src/components/forge/ForgeStatusBadge.tsx, src/components/forge/ForgeStatusBadge.test.tsx</files>
  <read_first>
    - C:\Users\mandr\forge\web\src\components\StatusBadge.tsx (the EXACT port source — STATUS_MAP, colorScheme mapping, data-status/data-color-scheme attrs, animate-spin on running)
    - C:\Users\mandr\forge\web\src\components\StatusBadge.test.tsx (the test contract to port — labels, SC#4 distinction, aria-label, KeyRound-not-XCircle)
    - .planning/phases/79-forge-ui-tab-read-only-render/79-PATTERNS.md (§ForgeStatusBadge, lines 306-347: the color re-skin table + resulting badge span)
    - .planning/phases/79-forge-ui-tab-read-only-render/79-UI-SPEC.md (§Status Color Table, lines 99-114: bg/text Tailwind class per status, icon, label, spin)
    - src/hooks/useForge.ts (Task 1 — for the JobStatus type, if exported there)
    - src/index.css (the .dark --status-* token block referenced by the text classes)
  </read_first>
  <behavior>
    - Test (ported from forge StatusBadge.test.tsx): for each of the 6 statuses, the exact label renders ("Queued","Running","Completed","Failed","Stopped","Auth Failed").
    - auth_failed renders "Auth Failed" and NOT "Failed"; its markup matches /amber/ and the element aria-label contains "auth_failed".
    - failed renders "Failed" and NOT "Auth Failed"; its markup matches /red/ and does NOT match /amber/.
    - running has animate-spin on its icon; every badge exposes data-status and data-color-scheme.
  </behavior>
  <action>
    Create `src/components/forge/ForgeStatusBadge.tsx` by porting forge `StatusBadge.tsx`.
    Rename the export to `ForgeStatusBadge`. Keep the 6-entry `STATUS_MAP`, the `colorScheme`
    derivation, and the `data-status`/`data-color-scheme` attributes verbatim. REPLACE forge's
    inline `style={{ backgroundColor: bg, color: fg }}` and `status-${colorScheme}` class with
    the Tailwind token classes from the UI-SPEC Status Color Table: queued `bg-zinc-800/60 text-zinc-400`,
    running `bg-blue-900/60 text-[var(--status-info)]`, completed `bg-green-900/60 text-[var(--status-ok)]`,
    failed `bg-red-900/60 text-[var(--status-error)]`, stopped `bg-zinc-800/40 text-zinc-500`,
    auth_failed `bg-amber-900/60 text-[var(--status-warn)]`. Keep base span classes
    `inline-flex items-center gap-1 rounded-full px-2.5 py-0.5 text-xs font-semibold` and the
    per-status Lucide icons (Clock/Loader2/CheckCircle/XCircle/Square/KeyRound) with `animate-spin`
    only on running. Import JobStatus from the Task-1 location (NOT `@/types`).
    Then create `src/components/forge/ForgeStatusBadge.test.tsx` by porting forge's
    `StatusBadge.test.tsx`: drop the `ForgeClientConfig`/`window.__FORGE_CONFIG__` mock blocks
    (forge-only), import `ForgeStatusBadge` from `./ForgeStatusBadge`, and keep the label,
    SC#4 color-distinction (amber present for auth_failed, red present + amber absent for failed),
    aria-label, and all-6-labels test cases. The amber/red regex assertions must pass against the
    re-skinned Tailwind classes (auth_failed has `amber`, failed has `red` and no `amber`).
  </action>
  <verify>
    <automated>npx vitest run src/components/forge/ForgeStatusBadge.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/components/forge/ForgeStatusBadge.test.tsx` passes (all cases green).
    - `npx tsc --noEmit` exits 0.
    - ForgeStatusBadge.tsx contains `data-status` and `data-color-scheme` attributes (grep both).
    - Source contains `bg-amber-900/60` (auth_failed) and `bg-red-900/60` (failed) as distinct classes — SC#4 preserved.
    - Source contains `animate-spin` gated to running, and imports `KeyRound` from lucide-react.
    - Source does NOT contain `style={{` inline color (re-skin replaced inline hex with Tailwind).
  </acceptance_criteria>
  <done>ForgeStatusBadge renders all 6 statuses with CodePulse token classes, preserves data-* attrs and the auth_failed(amber)≠failed(red) distinction, and the ported test is green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: ForgeHostBadge (outline host chip)</name>
  <files>src/components/forge/ForgeHostBadge.tsx</files>
  <read_first>
    - src/components/ui/badge.tsx (the Badge primitive — variant="outline" class + base classes)
    - .planning/phases/79-forge-ui-tab-read-only-render/79-PATTERNS.md (§ForgeHostBadge, lines 559-587: the exact component pattern)
    - .planning/phases/79-forge-ui-tab-read-only-render/79-UI-SPEC.md (§ForgeHostBadge, lines 182-189: outline, 10px mono uppercase, px-2 py-0, truncate >10 chars)
  </read_first>
  <behavior>
    - Given hostId "desktop", renders "desktop" inside an outline Badge.
    - Given a hostId longer than 10 chars, renders the first 8 chars + "…".
  </behavior>
  <action>
    Create `src/components/forge/ForgeHostBadge.tsx` exporting `ForgeHostBadge({ hostId }: { hostId: string })`.
    Use the shadcn `Badge` from `@/components/ui/badge` with `variant="outline"` and className
    `text-[10px] font-mono uppercase tracking-wider px-2 py-0`. Truncate: `hostId.length > 10 ? hostId.slice(0, 8) + "…" : hostId`.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0.
    - File exports `ForgeHostBadge` (grep: `export function ForgeHostBadge`).
    - Uses `variant="outline"` on Badge and the mono className `text-[10px] font-mono uppercase` (grep both).
    - Contains the >10 char truncation `slice(0, 8)` (grep).
  </acceptance_criteria>
  <done>ForgeHostBadge renders an outline mono chip with the (truncated) host name; tsc clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Convex cloud → React UI | forgeJobs rows (agent-emitted prompt/capabilities/host strings) cross into the SPA via useQuery. Untrusted text content. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-79-01 | Tampering (XSS via rendered job strings) | ForgeStatusBadge / ForgeHostBadge text content | mitigate | Render all job-sourced strings (status label is from a fixed local STATUS_MAP, host from hostId) as JSX text children only. No `dangerouslySetInnerHTML`, no `eval`. React default-escapes — relied upon explicitly. |
| T-79-02 | Information disclosure (PII in prompt) | (N/A this plan) | accept | No prompt rendered in Wave 1 badges; prompt masking handled where prompt is displayed (79-02). Noted for traceability. |
| T-79-SC | Tampering (supply chain) | npm installs | accept | No new packages installed — all primitives (Badge, lucide-react, convex/react) already in package.json. No Package Legitimacy Gate needed. |

No high-severity threats in this plan (read-only render of fixed-label badges + a typed adapter; no user input, no mutation, no new dependency).
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0 (whole repo typechecks with the new hook + components).
- `npx vitest run src/components/forge/ForgeStatusBadge.test.tsx` passes — proves all 6 status labels and the SC#4 auth_failed≠failed color distinction behaviorally.
- `useForge.ts` wraps `api.forge.listJobs` with `{}` (merged newest-first list, D-03) and adapts rows to `ForgeJobRow` without `logFile`.
</verification>

<success_criteria>
- ForgeJobRow type + adapter + useForgeJobs/useForgeJobsRaw hooks exist and compile.
- ForgeStatusBadge re-skins all 6 statuses to CodePulse tokens, preserves data-status/data-color-scheme and auth_failed(amber)≠failed(red).
- ForgeHostBadge renders an outline mono host chip.
- Ported badge test is green; no new npm dependencies; no dangerouslySetInnerHTML.
</success_criteria>

<output>
Create `.planning/phases/79-forge-ui-tab-read-only-render/79-01-SUMMARY.md` when done.
</output>
