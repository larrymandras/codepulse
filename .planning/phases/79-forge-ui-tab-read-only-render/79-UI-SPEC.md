---
phase: 79
slug: forge-ui-tab-read-only-render
status: draft
shadcn_initialized: true
preset: new-york
created: 2026-06-15
---

# Phase 79 — UI Design Contract: Forge UI Tab (read-only render)

> Visual and interaction contract for the `/forge` route: a read-only master-detail
> job viewer porting StatusBadge / JobList / JobDetail from `forge/web/src` into the
> CodePulse shell with the Matrix-Emerald skin.
>
> Generated by gsd-ui-researcher. All locked decisions sourced from 79-CONTEXT.md —
> none were re-asked. Claude's Discretion items resolved below.

---

## Design System

| Property | Value | Source |
|----------|-------|--------|
| Tool | shadcn/ui (New York) | `components.json`, `src/components/ui/` (30 primitives) |
| Preset | New York | `package.json:76` shadcn CLI + `src/index.css:3` |
| Component library | Radix UI `^1.4.3` | `package.json:42` |
| Icon library | `lucide-react ^1.8.0` | `package.json:40` — Lucide ONLY |
| Body font | Geist (`var(--font-geist)`) | `src/index.css:9,199` |
| Code/mono font | JetBrains Mono (`var(--font-mono)`) | `src/index.css:10` |
| Heading font | Geist via `var(--font-heading)` | `src/index.css:14` — Cinzel RETIRED (Phase 71 Q2) |
| Theme | Dark Matrix-Emerald (default `<html class="dark">`) | `index.html:2` |
| Radius | `--radius: 0.5rem` | `src/index.css:102` — Phase 71 Q1 decision |

---

## Spacing Scale

Standard CodePulse 4-point rhythm. Source: Phase 71 UI-SPEC §2.8 de-facto audit.

| Token | Value | Tailwind | Usage in Phase 79 |
|-------|-------|----------|-------------------|
| xs | 4px | `gap-1 / p-1` | Icon-to-label gap inside StatusBadge, host badge gap |
| sm | 8px | `gap-2 / p-2` | Card inner item gap (agent icon → content column), skeleton row gap |
| md | 16px | `px-4 / py-4` | JobDetail header horizontal padding, detail panel field row padding |
| lg | 24px | `p-6 / py-6` | Empty state vertical padding (`py-8` = 32px is acceptable per below) |
| xl | 32px | `p-8` | Empty state padding block (matches forge's `py-8` — falls between lg/xl, allowed exception) |
| 2xl | 48px | `gap-12` | Not used in this phase |
| 3xl | 64px | n/a | Not used in this phase |

**JobList card min-height:** 72px (`min-h-[72px]`), matching forge's card spec. This is intentional and not an exception — it resolves to 18 × 4 = 72px, within the scale.

**JobList panel width:** `w-[280px]` fixed, matching forge D-11 (~280px). This is a layout constant, not a spacing exception.

**shadcn-internal component padding (exempt from the 4-point grid rule):** The stock shadcn `Badge` primitive (`src/components/ui/badge.tsx`) ships internal padding of `px-2 py-0.5`, and `ForgeStatusBadge` matches Forge's badge shape with `px-2.5 py-0.5`. These half-step values (`py-0.5` = 2px, `px-2.5` = 10px) are **component-internal padding inherited verbatim from the installed Badge primitive** — they are not layout/grid spacing tokens. They are consistent codebase-wide (every badge across all 15 CodePulse pages renders this exact padding), so diverging would break visual consistency with the entire app. They are therefore **out of scope for the 4-point grid rule** and exempt by design. All *layout* spacing in this phase (panel padding, card gaps, skeleton gaps, host-badge padding) snaps to the 4px grid.

Exceptions:
- Empty state vertical padding 32px (xl) instead of the lg 24px stop.
- Badge internal padding (`px-2.5 py-0.5` on `ForgeStatusBadge`; `px-2 py-0.5` on stock `Badge`) — exempt per the shadcn-internal padding note above (component-internal, not layout spacing).

No other exceptions.

---

## Typography

Inherits CodePulse system type scale. Source: Phase 71 UI-SPEC §2.7 + live usage audit.

| Role | Size | Weight | Line Height | Font | Usage in Phase 79 |
|------|------|--------|-------------|------|--------------------|
| Body / card content | 12px (`text-xs`) | 400 (regular) | 1.5 (`leading-relaxed`) | Geist | Prompt preview in job cards, detail field values |
| Label / secondary | 12px (`text-xs`) | 400 | 1 | Geist | Relative timestamp, agent name, muted secondary text |
| Badge label | 12px (`text-xs`) | 600 (semibold) | 1 | Geist | StatusBadge text label, host badge text |
| Detail header agent | 14px (`text-sm`) | 600 (semibold) | 1.25 | Geist | Job agent name in JobDetail header |
| Operational mono labels | 10px (`text-[10px] font-mono uppercase tracking-widest`) | 400 | 1 | JetBrains Mono | Field keys in metadata panel (e.g. "STARTED AT", "MODEL") |
| Empty state heading | 14px (`text-sm`) | 600 | 1.25 | Geist | "No jobs yet" heading |

**Two weights only:** 400 (regular) and 600 (semibold). No 500/700 usage in this phase.

---

## Color

Inherits CodePulse dark Matrix-Emerald token layer. Source: `src/index.css:116–184` (.dark block), Phase 71 UI-SPEC §2.1–2.3.

| Role | Token / Value | Usage |
|------|---------------|-------|
| Dominant (60%) | `--background: #09090b` (zinc-950) | Page background, ForgePage container |
| Secondary (30%) | `--card: #141416` + `--border: #27272a` | JobList panel bg, JobDetail panel bg, card borders |
| Accent (10%) | `--primary: #10b981` (Matrix Emerald) | Selected job card active bar / bg highlight (`bg-accent`), nav active indicator |
| Surface hover | `--accent: #27272a` | Job card hover state (`hover:bg-accent/50`), selected card (`bg-accent`) |
| Muted text | `--muted-foreground: #a1a1aa` | Agent name, timestamp, secondary labels, empty-state body text |
| Foreground | `--foreground: #ffffff` | Primary text (prompt preview, agent header) |
| Border | `--border: #27272a` | Card borders, panel dividers |
| Destructive | `--destructive` | NOT USED in Phase 79 (all action controls stripped per D-01) |

Accent (`#10b981`) reserved for: active/selected job card highlight, sidebar nav active entry indicator. Not used on any action control (none exist in Phase 79).

### Status Color Table (D-09 — full re-skin contract)

All 6 `JobStatus` values re-skinned from Forge's hardcoded hex to CodePulse tokens. SC#4 constraint: `auth_failed` (amber) MUST NOT equal `failed` (red) — preserved exactly.

| Status | Tailwind bg class | Tailwind text class | Token reference | Icon | Label | Spin? |
|--------|-------------------|---------------------|-----------------|------|-------|-------|
| `queued` | `bg-zinc-800/60` | `text-zinc-400` | `--muted: #27272a` / `--muted-foreground: #a1a1aa` | `Clock` (Lucide) | "Queued" | No |
| `running` | `bg-blue-900/60` | `text-[var(--status-info)]` = `#3b82f6` | `--status-info` | `Loader2` (Lucide) | "Running" | Yes — `animate-spin` on icon |
| `completed` | `bg-green-900/60` | `text-[var(--status-ok)]` = `#22c55e` | `--status-ok` | `CheckCircle` (Lucide) | "Completed" | No |
| `failed` | `bg-red-900/60` | `text-[var(--status-error)]` = `#ef4444` | `--status-error` | `XCircle` (Lucide) | "Failed" | No |
| `stopped` | `bg-zinc-800/40` | `text-zinc-500` | `--muted` / zinc-500 | `Square` (Lucide) | "Stopped" | No |
| `auth_failed` | `bg-amber-900/60` | `text-[var(--status-warn)]` = `#eab308` | `--status-warn` | `KeyRound` (Lucide) | "Auth Failed" | No |

**Implementation note:** Use `inline-flex items-center gap-1 rounded-full px-2.5 py-0.5 text-xs font-semibold` as the badge base class (matching Forge's shape — note `px-2.5 py-0.5` is component-internal badge padding, exempt from the grid rule per the Spacing section), substituting the Tailwind token classes above for Forge's inline `style={{ backgroundColor, color }}`. Attach `data-status={status}` and `data-color-scheme={colorScheme}` attributes (preserved from Forge) so existing tests that key on these attributes continue to pass.

`colorScheme` mapping: `queued→slate`, `running→blue`, `completed→green`, `failed→red`, `stopped→stone`, `auth_failed→amber` — unchanged from Forge.

---

## Component Inventory

All components are ported from `forge/web/src` or composed from existing CodePulse primitives. No new shadcn components are added beyond what already exists in `src/components/ui/`.

| Component | Source | Action | Primitives Used |
|-----------|--------|--------|-----------------|
| `ForgeStatusBadge` | `forge/StatusBadge.tsx` | Port + re-skin colors to CodePulse tokens (table above) | None (standalone span) |
| `ForgeJobList` | `forge/JobList.tsx` | Port structure; strip delete-X, Clear-failed, `handleDelete`, `handleClearFailed`, `onChanged`, `apiFetch`, `deleting`/`clearing` state | `ScrollArea`, `Skeleton`, `ForgeStatusBadge` |
| `ForgeJobDetail` | `forge/JobDetail.tsx` | Port header only; strip Stop Job, Tabs, LogsPanel, FilesPanel, `useJobLog`, `useWorkspaceFiles`, `useWorkspaces`, `apiFetch`, `isStopping` state | `ForgeStatusBadge` |
| `ForgeMetadataPanel` | NEW | Metadata-only fields panel (D-02): renders 13 persisted fields below the job header | None (CSS grid layout) |
| `ForgeHostBadge` | NEW | Small inline chip showing host name on each job card (D-03) | `Badge` variant="outline" |
| `ForgePage` | NEW | Page container: master-detail layout, `SectionErrorBoundary` wrapping, page header | `SectionErrorBoundary`, `GlassPanel`, `SectionHeader` |
| `useForge` | NEW | Hook: wraps `useQuery(api.forge.listJobs) ?? []`, `useQuery(api.forge.getJob)` | convex/react |
| `ForgeTypeAdapter` | NEW | Inline adapter mapping Convex `forgeJobs` row → forge `Job` shape for component props | None |

---

## Layout: Master-Detail (D-11)

```
┌──────────────────────────────────────────────────────────┐
│ ForgePage (flex row, h-full overflow-hidden)             │
│                                                          │
│  ┌─────────────────┐  ┌───────────────────────────────┐ │
│  │ ForgeJobList    │  │ ForgeJobDetail                │ │
│  │ w-[280px]       │  │ flex-1                        │ │
│  │ shrink-0        │  │                               │ │
│  │ border-r        │  │ [no job selected]             │ │
│  │ border-border   │  │  → empty prompt              │ │
│  │                 │  │                               │ │
│  │ ScrollArea      │  │ [job selected]               │ │
│  │  ↓ job cards    │  │  → header (agent+badge+prompt)│ │
│  │    newest first │  │  → ForgeMetadataPanel        │ │
│  │                 │  │                               │ │
│  └─────────────────┘  └───────────────────────────────┘ │
└──────────────────────────────────────────────────────────┘
```

**Selection key:** `(hostId, forgeJobId)` pair. Job cards from the merged multi-host list are not unique on `forgeJobId` alone (D-03, P78 D-04). `selectedKey` state is `{ hostId: string; forgeJobId: string } | null`. The detail view reads from the already-loaded `listJobs` row (no `getJob` round-trip needed for the metadata panel).

**Page header:** Standard CodePulse page header pattern — `text-2xl font-bold` page title "Forge", same as `Dashboard.tsx:53` convention. No subtitle needed.

**SectionErrorBoundary:** Wrap both the list region and the detail region independently. If the list fails, the detail can still show a previously-selected job (or vice versa).

---

## ForgeMetadataPanel — Fields Layout (D-02)

Rendered as a two-column CSS grid (`grid-cols-[auto_1fr]` or `grid-cols-2`) inside a `GlassPanel` below the job header. Fields ordered: identity group first, then execution group, then workspace/model/capabilities.

| Group | Fields (display order) | Format |
|-------|------------------------|--------|
| Identity | agent, mode, status | `agent`: capitalize; `mode`: capitalize; `status`: `ForgeStatusBadge` component |
| Execution | pid, exitCode, startedAt, finishedAt | `pid`/`exitCode`: numeric or "—" if null; `startedAt`/`finishedAt`: ISO → `toLocaleString()` (full date + time, local timezone); null → "—" |
| Resources | workspaceId, artifactCount | `workspaceId`: truncated to 12 chars + "…" if longer; `artifactCount`: numeric |
| Configuration | model, capabilities | `model`: string or "default" if null; `capabilities`: parse JSON string → pretty-print as `key: value` lines (one line per capability key); font-mono for the value block |
| Audit | createdAt, updatedAt | ISO → `toLocaleString()` |

**Field key typography:** `text-[10px] font-mono uppercase tracking-widest text-muted-foreground` (CodePulse operational chrome style per DashboardLayout.tsx:179).
**Field value typography:** `text-xs text-foreground` for scalars; `text-xs font-mono text-muted-foreground` for the capabilities block.
**Row padding:** `py-2` (8px) between rows. Section group dividers: a 1px `border-t border-border` with a `text-[10px] font-mono uppercase tracking-widest text-muted-foreground` group label above each group (matches SectionHeader convention, lighter weight).

---

## ForgeHostBadge — Spec (D-03)

A small chip rendered inside each job card alongside the StatusBadge to identify which machine the job came from.

- **Component:** shadcn `Badge` variant="outline" with `text-[10px] font-mono uppercase tracking-wider px-2 py-0` (8px horizontal / 0 vertical — grid-aligned; smaller footprint than the status badge since host is secondary information).
- **Values:** Display the raw `hostId` value as-is (e.g. "desktop", "laptop"). If hostId is longer than 10 chars, truncate to 8 + "…".
- **Color:** Inherits `variant="outline"` — `border-border text-foreground` (no color semantics; host is not a status).
- **Position:** Inline after StatusBadge, on the same `flex items-center gap-2 flex-wrap` row at the top of the card content column.

---

## Nav Entry (D-05, D-06)

| Property | Value | Source |
|----------|-------|--------|
| Route | `/forge` | D-05 (CONTEXT.md) |
| Nav group | CONSOLE | D-05 (alongside Agent Console, Live Run, Executions, Build) |
| Label | "Forge" | D-06 (CONTEXT.md) |
| Icon string | `"flame"` | Claude's Discretion — resolved: `Flame` (Lucide) confirmed present in `lucide-react ^1.8.0`; does not collide with `"hammer"` (Build) |
| Lucide component | `Flame` | Import from `lucide-react` |

**Registration:** Add `flame: Flame` to `iconComponents` map in `src/layouts/DashboardLayout.tsx` (line ~106). Add nav entry to the CONSOLE group's `items` array:
```ts
{ to: "/forge", label: "Forge", icon: "flame", group: "CONSOLE" }
```
Insert after "Build" (last current CONSOLE item).

---

## Copywriting Contract

All decisions pre-populated from D-04 (CONTEXT.md) and Forge source analysis.

| Element | Copy | Source |
|---------|------|--------|
| Page title | "Forge" | D-06 |
| Empty state heading | "No jobs yet" | D-04 — port from `JobList.tsx:131` |
| Empty state body | "Jobs will appear here once the Forge daemon starts syncing." | D-04 — DROP "Launch your first job…" line; use neutral data-availability message |
| No-job-selected prompt (detail pane) | "Select a job to view details" | Port from `JobDetail.tsx:205` — already neutral |
| Capabilities field null | "—" | Claude's Discretion — consistent null display across all metadata fields |
| Model field null | "default" | Claude's Discretion — null = CLI default, "default" is more legible than "—" |
| Loading state aria-label | "Loading jobs" | Port from `JobList.tsx:116` |
| WorkspaceId truncation | "{first 12 chars}…" | Claude's Discretion — workspaceId is a UUID/opaque string; truncation for display |

**No destructive actions exist in Phase 79** (D-01). No confirmation copy needed.

---

## Interaction Contract

### Job Selection
- Click a job card → set `selectedKey: { hostId, forgeJobId }` → render that job's detail pane.
- Selected card: `bg-accent` background + emerald left border `border-l-2 border-primary` to indicate selection (CodePulse active-state convention).
- Hover (non-selected): `hover:bg-accent/50` (Forge convention, compatible with CodePulse tokens).
- `aria-selected={isSelected}` on the card button element.

### Loading State
- `ForgeJobList` loading: render 3 skeleton rows (matching Forge's pattern) using CodePulse `Skeleton` component (`animate-pulse rounded-md bg-accent`). Three rows, each with:
  - Row 1: `Skeleton h-4 w-2/3` (badge + agent name area)
  - Row 2: `Skeleton h-3 w-1/2` (prompt preview)
  - Row 3: `Skeleton h-3 w-1/3` (timestamp)
  - Padding: `p-3 rounded-md flex flex-col gap-2` (12px row padding, 8px gap — both grid-aligned)
- `ForgeMetadataPanel` loading: not applicable — detail renders from the already-loaded `listJobs` row; no second round-trip, no second loading state.

### Relative Timestamps
Use **`relativeTime()` from `src/lib/formatters.ts`** (Claude's Discretion resolved). The CodePulse formatter takes epoch seconds; the Convex `createdAt`/`updatedAt` fields are Convex `_creationTime` (milliseconds) or ISO strings — the adapter converts to epoch seconds before calling. Forge's own `relativeTime()` takes ISO strings and has slightly different formatting ("s ago" vs "just now" for <60s). Use CodePulse's version for consistency; accept the minor formatting difference ("just now" instead of "5s ago").

Full datetime display (startedAt/finishedAt/createdAt/updatedAt in the metadata panel): `new Date(isoString).toLocaleString()` — full local date + time, no relative conversion, since exact timestamps matter for execution audit.

### Transitions
- Card hover/select: `transition-colors` (inherits CodePulse standard, no duration override).
- No entry animations on job cards (list is a data list, not a feature showcase). `GlassPanel` motion for the detail panel: standard `opacity 0→1, y 8→0, duration 0.2 easeOut` via `GlassPanel`'s existing `animate` prop.

---

## Adapter Contract (Type Bridge)

Convex `forgeJobs` row → Forge `Job` shape. Required fields only (stripped tabs removed `logFile`/workspace `rootPath`).

| Convex field | Forge `Job` field | Notes |
|---|---|---|
| `doc.forgeJobId` | `id` | string |
| `doc.agent` | `agent` | string |
| `doc.mode` | `mode` | `'chat' \| 'goal'` |
| `doc.prompt` | `prompt` | `string \| null` |
| `doc.workspaceId` | `workspaceId` | string |
| `doc.status` | `status` | `JobStatus` |
| `doc.pid` | `pid` | `number \| null` |
| `doc.exitCode` | `exitCode` | `number \| null` |
| `doc.startedAt` | `startedAt` | ISO string or null |
| `doc.finishedAt` | `finishedAt` | ISO string or null |
| `doc.artifactCount` | `artifactCount` | number |
| `doc.capabilities` | `capabilities` | JSON string |
| `doc.model` | `model` | `string \| null` |
| `doc.createdAt` | `createdAt` | ISO string |
| `doc.hostId` | (extra) | Carry through for host badge + selection key — NOT on Forge `Job` type; add to an extended `ForgeJobRow` type |
| `doc.updatedAt` | (extra) | Used in metadata panel only; carry through on `ForgeJobRow` |
| n/a | `logFile` | DO NOT map — stripped with tabs |

The adapter lives in `src/hooks/useForge.ts` or a small `src/lib/forgeAdapter.ts`. The extended type `ForgeJobRow = Job & { hostId: string; updatedAt: string }` is defined in the CodePulse codebase (not imported from Forge).

---

## Files to Create / Modify

| File | Action | Notes |
|------|--------|-------|
| `src/pages/ForgePage.tsx` | CREATE | Top-level page; master-detail layout |
| `src/components/forge/ForgeStatusBadge.tsx` | CREATE | Port of `forge/StatusBadge.tsx` with CodePulse token re-skin |
| `src/components/forge/ForgeJobList.tsx` | CREATE | Port of `forge/JobList.tsx` stripped of all action controls |
| `src/components/forge/ForgeJobDetail.tsx` | CREATE | Port of `forge/JobDetail.tsx` header only + ForgeMetadataPanel |
| `src/components/forge/ForgeMetadataPanel.tsx` | CREATE | New component — 12-field metadata grid |
| `src/hooks/useForge.ts` | CREATE | `useQuery` wrappers + type adapter |
| `src/App.tsx` | MODIFY | Add `<Route path="/forge" element={<ForgePage />} />` (lazy-load per convention) |
| `src/layouts/DashboardLayout.tsx` | MODIFY | Add `flame: Flame` to `iconComponents`; add Forge entry to CONSOLE `navGroups` |

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | Badge, ScrollArea, Skeleton (already installed) | not required — primitives already in `src/components/ui/` |

No third-party registries. No new shadcn components are installed in this phase — all required primitives already exist in the codebase.

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending

---

## Source Traceability

| Decision | Source |
|----------|--------|
| D-01 — no action controls | 79-CONTEXT.md |
| D-02 — metadata-only detail pane, 13 fields | 79-CONTEXT.md |
| D-03 — merged list + host badge | 79-CONTEXT.md |
| D-04 — empty state copy (drop launch line) | 79-CONTEXT.md |
| D-05 — `/forge` route, CONSOLE group | 79-CONTEXT.md |
| D-06 — label "Forge", icon not-hammer | 79-CONTEXT.md |
| D-09 — status re-skin to CodePulse tokens | 79-CONTEXT.md |
| D-10 — CodePulse DS is authoritative | 79-CONTEXT.md |
| D-11 — master-detail layout ~280px | 79-CONTEXT.md |
| `flame` icon chosen (not `hammer`) | Claude's Discretion — confirmed present in `lucide-react ^1.8.0` |
| Field ordering (identity → execution → resources → config → audit) | Claude's Discretion |
| capabilities: parse JSON → key:value lines, font-mono | Claude's Discretion |
| timestamps in metadata: `toLocaleString()` (full), not relative | Claude's Discretion |
| Loading skeletons: reuse CodePulse `Skeleton` component pattern | Claude's Discretion |
| relativeTime: use `src/lib/formatters.ts` version | Claude's Discretion |
| Host badge: `Badge variant="outline"` 10px mono uppercase, `px-2 py-0` | Claude's Discretion |
| Token scale, surface colors, glow scale | Phase 71 UI-SPEC (authoritative per D-10) |
| Status token values | `src/index.css:160–163` (.dark `--status-*`) |
