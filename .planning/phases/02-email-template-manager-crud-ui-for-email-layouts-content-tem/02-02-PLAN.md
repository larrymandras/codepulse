---
phase: 02-email-template-manager
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/EmailTemplates.tsx
  - src/App.tsx
  - src/layouts/DashboardLayout.tsx
autonomous: true
requirements:
  - D-01

must_haves:
  truths:
    - "Navigating to /email-templates renders the EmailTemplatesPage component"
    - "Sidebar shows 'Email Templates' nav item with Mail icon"
    - "Page displays 4 tabs: Layouts, Templates, Agent Defaults, Assets"
    - "Each tab content is wrapped in SectionErrorBoundary"
  artifacts:
    - path: "src/pages/EmailTemplates.tsx"
      provides: "Page shell with 4-tab layout"
      contains: "TabsTrigger"
    - path: "src/App.tsx"
      provides: "Route registration for /email-templates"
      contains: "email-templates"
    - path: "src/layouts/DashboardLayout.tsx"
      provides: "Nav entry + icon registration"
      contains: "Email Templates"
  key_links:
    - from: "src/App.tsx"
      to: "src/pages/EmailTemplates.tsx"
      via: "lazy(() => import('./pages/EmailTemplates'))"
      pattern: "lazy.*import.*EmailTemplates"
    - from: "src/layouts/DashboardLayout.tsx"
      to: "/email-templates"
      via: "overviewNavItems entry"
      pattern: 'to: "/email-templates"'
---

<objective>
Create the EmailTemplatesPage shell with 4 tabs and wire it into the app via lazy-loaded route and sidebar navigation.

Purpose: Establishes the page structure that all subsequent plans will populate with content. Without this, there is no route, no page, and no tab structure.

Output: 3 modified files — page component, route registration, nav entry.
</objective>

<execution_context>
@C:\Users\mandr\codepulse\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\mandr\codepulse\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-email-template-manager-crud-ui-for-email-layouts-content-tem/02-CONTEXT.md
@.planning/phases/02-email-template-manager-crud-ui-for-email-layouts-content-tem/02-UI-SPEC.md
@.planning/phases/02-email-template-manager-crud-ui-for-email-layouts-content-tem/02-PATTERNS.md

<interfaces>
<!-- Existing route registration pattern from App.tsx -->
From src/App.tsx (lines 54-56, 115-116):
```typescript
const DesignStudio = lazy(() => import("./pages/DesignStudio"));

<Route path="/design-studio" element={<Suspense fallback={<div className="text-muted-foreground text-sm p-8 text-center">Loading Design Studio...</div>}><DesignStudio /></Suspense>} />
```

<!-- Nav registration pattern from DashboardLayout.tsx -->
From src/layouts/DashboardLayout.tsx (lines 65-98 — iconComponents, lines 118-142 — overviewNavItems):
```typescript
const iconComponents: Record<string, React.ElementType> = {
  // ...existing entries...
  "palette": Palette,
};

const overviewNavItems = [
  // ...existing entries...
  { to: "/design-studio", label: "Design Studio", icon: "palette", group: "OVERVIEW" },
  { to: "/executions", label: "Executions", icon: "list", group: "OVERVIEW" },
  // ...
];
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create EmailTemplatesPage shell with 4 tabs</name>
  <files>src/pages/EmailTemplates.tsx</files>
  <read_first>
    - src/pages/DesignStudio.tsx (page shell pattern — imports, Tabs usage, SectionErrorBoundary wrapping)
    - .planning/phases/02-email-template-manager-crud-ui-for-email-layouts-content-tem/02-UI-SPEC.md (page structure contract, empty states, copywriting)
    - .planning/phases/02-email-template-manager-crud-ui-for-email-layouts-content-tem/02-PATTERNS.md (EmailTemplates.tsx page shell pattern)
  </read_first>
  <action>
Create `src/pages/EmailTemplates.tsx` as a default-exported page component.

**Structure:**
```typescript
import { useState } from "react";
import SectionErrorBoundary from "@/components/SectionErrorBoundary";
import { Tabs, TabsContent, TabsList, TabsTrigger } from "@/components/ui/tabs";
import { Button } from "@/components/ui/button";
import { LayoutTemplate, FileText, UserCircle, ImageOff } from "lucide-react";

export default function EmailTemplates() {
  const [activeTab, setActiveTab] = useState("layouts");

  return (
    <div className="space-y-6">
      <div className="flex items-center justify-between">
        <h1 className="text-2xl font-semibold font-[Cinzel]">Email Templates</h1>
        {/* Primary CTA changes per tab — render conditionally */}
        {activeTab === "layouts" && (
          <Button>New Layout</Button>
        )}
        {activeTab === "templates" && (
          <Button>New Template</Button>
        )}
        {activeTab === "assets" && (
          <Button>Upload Image</Button>
        )}
      </div>

      <Tabs value={activeTab} onValueChange={setActiveTab}>
        <TabsList>
          <TabsTrigger value="layouts">Layouts</TabsTrigger>
          <TabsTrigger value="templates">Templates</TabsTrigger>
          <TabsTrigger value="agent-defaults">Agent Defaults</TabsTrigger>
          <TabsTrigger value="assets">Assets</TabsTrigger>
        </TabsList>

        <TabsContent value="layouts">
          <SectionErrorBoundary name="Layouts">
            {/* Empty state per UI-SPEC */}
            <div className="flex flex-col items-center gap-4 py-16 text-center">
              <LayoutTemplate className="h-12 w-12 text-muted-foreground" />
              <h2 className="text-base font-semibold">No layouts yet</h2>
              <p className="text-sm text-muted-foreground max-w-xs">
                Create a layout to define the outer structure of your emails — header, footer, and CSS.
              </p>
              <Button>New Layout</Button>
            </div>
          </SectionErrorBoundary>
        </TabsContent>

        <TabsContent value="templates">
          <SectionErrorBoundary name="Templates">
            <div className="flex flex-col items-center gap-4 py-16 text-center">
              <FileText className="h-12 w-12 text-muted-foreground" />
              <h2 className="text-base font-semibold">No templates yet</h2>
              <p className="text-sm text-muted-foreground max-w-xs">
                Templates define the content body of an email. Start by creating a layout first.
              </p>
              <Button>New Template</Button>
            </div>
          </SectionErrorBoundary>
        </TabsContent>

        <TabsContent value="agent-defaults">
          <SectionErrorBoundary name="Agent Defaults">
            <div className="flex flex-col items-center gap-4 py-16 text-center">
              <UserCircle className="h-12 w-12 text-muted-foreground" />
              <h2 className="text-base font-semibold">No agent defaults configured</h2>
              <p className="text-sm text-muted-foreground max-w-xs">
                Agent defaults let each AI persona have its own signature and email style.
              </p>
            </div>
          </SectionErrorBoundary>
        </TabsContent>

        <TabsContent value="assets">
          <SectionErrorBoundary name="Assets">
            <div className="flex flex-col items-center gap-4 py-16 text-center">
              <ImageOff className="h-12 w-12 text-muted-foreground" />
              <h2 className="text-base font-semibold">No assets uploaded</h2>
              <p className="text-sm text-muted-foreground max-w-xs">
                Upload logos and avatar images to use across your email layouts.
              </p>
              <Button>Upload Image</Button>
            </div>
          </SectionErrorBoundary>
        </TabsContent>
      </Tabs>
    </div>
  );
}
```

Key details:
- Heading: `text-2xl font-semibold font-[Cinzel]` (matches Operations.tsx / DesignStudio.tsx pattern)
- Empty states use exact copy from UI-SPEC Copywriting Contract
- Empty state icons: LayoutTemplate, FileText, UserCircle, ImageOff (per UI-SPEC)
- No CTA on Agent Defaults empty state per UI-SPEC: "defaults created from agent config, not here"
- SectionErrorBoundary wraps each TabsContent for error isolation
- Primary CTA button text changes per active tab
  </action>
  <verify>
    <automated>npx tsc --noEmit src/pages/EmailTemplates.tsx 2>&1 | head -10</automated>
  </verify>
  <acceptance_criteria>
    - src/pages/EmailTemplates.tsx contains `export default function EmailTemplates()`
    - src/pages/EmailTemplates.tsx contains `<TabsTrigger value="layouts">Layouts</TabsTrigger>`
    - src/pages/EmailTemplates.tsx contains `<TabsTrigger value="templates">Templates</TabsTrigger>`
    - src/pages/EmailTemplates.tsx contains `<TabsTrigger value="agent-defaults">Agent Defaults</TabsTrigger>`
    - src/pages/EmailTemplates.tsx contains `<TabsTrigger value="assets">Assets</TabsTrigger>`
    - src/pages/EmailTemplates.tsx contains `<SectionErrorBoundary name="Layouts">`
    - src/pages/EmailTemplates.tsx contains `No layouts yet`
    - src/pages/EmailTemplates.tsx contains `No templates yet`
    - src/pages/EmailTemplates.tsx contains `No agent defaults configured`
    - src/pages/EmailTemplates.tsx contains `No assets uploaded`
    - src/pages/EmailTemplates.tsx contains `font-[Cinzel]`
    - `npx tsc --noEmit` reports no errors
  </acceptance_criteria>
  <done>EmailTemplatesPage shell renders with 4 tabs, each showing empty state with correct copy, icons, and CTAs per UI-SPEC.</done>
</task>

<task type="auto">
  <name>Task 2: Route registration and nav entry</name>
  <files>src/App.tsx, src/layouts/DashboardLayout.tsx</files>
  <read_first>
    - src/App.tsx (lines 1-60 for lazy imports, lines 113-120 for route entries near Design Studio)
    - src/layouts/DashboardLayout.tsx (lines 19-57 for icon imports, lines 65-98 for iconComponents map, lines 118-142 for overviewNavItems)
  </read_first>
  <action>
**Step 1 — `src/App.tsx`:**

Add lazy import after the Design Studio lazy import (after line 55):
```typescript
// Phase 02: Email Template Manager
const EmailTemplates = lazy(() => import("./pages/EmailTemplates"));
```

Add route after the Design Studio route (after line 116):
```typescript
{/* Phase 02: Email Template Manager */}
<Route path="/email-templates" element={<Suspense fallback={<div className="text-muted-foreground text-sm p-8 text-center">Loading Email Templates...</div>}><EmailTemplates /></Suspense>} />
```

**Step 2 — `src/layouts/DashboardLayout.tsx`:**

Add `Mail` to the lucide-react import block (after `Palette` at line 56):
```typescript
Mail,
```

Add to `iconComponents` map (after `"palette": Palette,` at line 98):
```typescript
"mail": Mail,
```

Add to `overviewNavItems` array (after the Design Studio entry at line 133, before Executions):
```typescript
{ to: "/email-templates", label: "Email Templates", icon: "mail", group: "OVERVIEW" },
```
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - src/App.tsx contains `const EmailTemplates = lazy(() => import("./pages/EmailTemplates"))`
    - src/App.tsx contains `path="/email-templates"`
    - src/App.tsx contains `Loading Email Templates...`
    - src/layouts/DashboardLayout.tsx contains `Mail,` in the lucide-react import block
    - src/layouts/DashboardLayout.tsx contains `"mail": Mail`
    - src/layouts/DashboardLayout.tsx contains `to: "/email-templates", label: "Email Templates", icon: "mail"`
    - `npx tsc --noEmit` reports no errors
  </acceptance_criteria>
  <done>Route /email-templates is lazy-loaded, sidebar shows "Email Templates" with Mail icon, clicking navigates to the page shell.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None | This plan only creates page shell, route, and nav entry — no data flow or API calls |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04 | Denial of Service | EmailTemplates lazy import | accept | Lazy loading is standard pattern; large bundle is Monaco (Plan 03/04), not this page shell. Fallback div shown during load. |
</threat_model>

<verification>
1. `npx tsc --noEmit` — no type errors
2. `grep -c 'email-templates' src/App.tsx` returns 2 (import comment + route path)
3. `grep -c 'Email Templates' src/layouts/DashboardLayout.tsx` returns 1
4. `grep -c 'TabsTrigger' src/pages/EmailTemplates.tsx` returns 4
</verification>

<success_criteria>
- /email-templates route registered and lazy-loaded
- Sidebar shows "Email Templates" nav item with Mail icon in OVERVIEW group
- Page renders 4 tabs with correct labels
- Each tab shows empty state with correct copy from UI-SPEC
- No TypeScript compilation errors
</success_criteria>

<output>
After completion, create `.planning/phases/02-email-template-manager-crud-ui-for-email-layouts-content-tem/02-02-SUMMARY.md`
</output>
