---
phase: 88-analytics-rollup
plan: 03
type: execute
wave: 2
depends_on: ["88-02"]
files_modified:
  - convex/dataRetention.ts
autonomous: false
requirements: [AR-02]

must_haves:
  truths:
    - "The schema + ingest-time-increment deploy (Plan 02) has landed and the by_idempotencyKey index is backfilled before backfillHistorical runs"
    - "backfillHistorical populates aggregates buckets for all pre-rollup historical events without tripping the 16 MiB read limit"
    - "After backfill, each aggregates bucket value equals the count of matching events (rollup count == raw count)"
    - "purgeOldTelemetryEvents deletes only from the events table and never reads, deletes, or patches aggregates rows (verified by source + test)"
  artifacts:
    - path: "convex/dataRetention.ts"
      provides: "Confirmed-unchanged purge that never touches aggregates (verify-only; doc note added if needed)"
      contains: "purgeOldTelemetryEvents"
  key_links:
    - from: "backfillHistorical action (deployed)"
      to: "aggregates table"
      via: "one-time triggered run via Convex dashboard / CLI"
      pattern: "backfillHistorical"
---

<objective>
Run the one-time historical backfill (after the Plan 02 schema+increment deploy lands) and lock in the archival-consistency guarantee: prove dataRetention never touches aggregates.

Purpose: AR-02 requires complete rollup fidelity from day one, including pre-rollup events. The backfill is a paginated action (already coded in Plan 02) that must run AFTER the schema deploy completes its automatic by_idempotencyKey index backfill (Pitfall 6 — Convex guarantees index readiness before new functions register, so triggering the action after deploy is safe by construction). D-12 archival consistency is satisfied by construction; this plan adds the regression test that keeps it true.
Output: aggregates populated with full history; a passing test that purge leaves aggregates untouched.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/88-analytics-rollup-table-durable-fix-for-convex-16-mib-read-li/88-CONTEXT.md
@.planning/phases/88-analytics-rollup-table-durable-fix-for-convex-16-mib-read-li/88-RESEARCH.md
@.planning/phases/88-analytics-rollup-table-durable-fix-for-convex-16-mib-read-li/88-VALIDATION.md
@convex/analyticsRollup.ts

<interfaces>
backfillHistorical (convex/analyticsRollup.ts, from Plan 02): action({}) → { processed }. Triggered once via `npx convex run analyticsRollup:backfillHistorical` or the Convex dashboard Functions runner. Idempotent on re-run for keyed events; for un-keyed events a second run would re-increment (D-05) — so run exactly once.

purgeOldTelemetryEvents (convex/dataRetention.ts:6-21): internalMutation; queries "events" by_timestamp, deletes by _id, BATCH_SIZE 500. Never references "aggregates".
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Verify + regression-test that dataRetention never mutates aggregates</name>
  <files>convex/dataRetention.ts, convex/aggregates.test.ts</files>
  <read_first>
    - convex/dataRetention.ts (full file — confirm only events/heartbeatAlerts/episodicEvents are queried/deleted; aggregates never referenced)
    - convex/aggregates.test.ts (the "Phase 88" describe block from Plan 01 — the retention test lives here)
    - 88-RESEARCH.md "Verified: dataRetention.ts does NOT touch aggregates" + D-12
  </read_first>
  <behavior>
    - Reading dataRetention.ts confirms zero references to the "aggregates" table in any query/insert/patch/delete call
    - The regression test asserts that the set of tables purgeOldTelemetryEvents deletes from contains no "aggregates" entry
  </behavior>
  <action>
    Read convex/dataRetention.ts end to end and confirm no `ctx.db.query("aggregates")`, `ctx.db.insert("aggregates")`, `ctx.db.patch` of an aggregates row, or `ctx.db.delete` of an aggregates row exists anywhere in the file. This is verify-only — make NO functional change. If the file lacks a clarifying comment, add a one-line comment near purgeOldTelemetryEvents stating that aggregates are immutable historical buckets and intentionally untouched (D-12) — comment only, no logic change. Ensure the dataRetention regression test in the "Phase 88" describe block of convex/aggregates.test.ts (scaffolded in Plan 01) asserts the deleted-table set has zero "aggregates" entries; if the assertion is a stub, finalize it now so it passes against the real purge behavior. No fenced code — follow read_first analogs.
  </action>
  <verify>
    <automated>npx vitest run convex/aggregates.test.ts -t "aggregates" 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - convex/dataRetention.ts contains no reference to the "aggregates" table (grep `aggregates` in dataRetention.ts returns 0)
    - convex/aggregates.test.ts "Phase 88" block has a passing test asserting purge deletes no aggregates rows (the dataRetention invariant is GREEN)
    - No functional change to purgeOldTelemetryEvents (only an optional clarifying comment)
  </acceptance_criteria>
  <done>dataRetention confirmed aggregates-safe by source inspection + passing regression test (D-12 locked).</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Trigger + verify the one-time historical backfill against the live deployment</name>
  <action>
    Operator-gated one-time data operation (no source change). After the Plan 02 schema+increment deploy is live (Convex auto-backfills the by_idempotencyKey index before registering new functions — Pitfall 6), trigger the backfill EXACTLY ONCE via the Convex CLI command `npx convex run analyticsRollup:backfillHistorical` (or the Convex dashboard Functions runner). Do NOT re-run — keyed events are idempotent on re-run but un-keyed legacy events (D-05) would re-increment. Then spot-check fidelity per the verification steps below and confirm no 16 MiB read error.
  </action>
  <what-built>
    The backfillHistorical action (convex/analyticsRollup.ts) is deployed (Plan 02 wave). The schema change (idempotencyKey + by_idempotencyKey index) deployed in the same wave; Convex backfilled the index before registering new functions (Pitfall 6 — guaranteed), so the action is safe to run now.
  </what-built>
  <how-to-verify>
    This is a one-time production data operation that runs against the live Convex deployment over ALL historical events — it cannot be reproduced at scale in the unit environment, so it is operator-triggered after deploy.

    1. Confirm Plan 02 is deployed: `npx convex deploy --yes` has run for this phase (or the execute-phase deploy step completed). The Convex dashboard should show `analyticsRollup:backfillHistorical` in the Functions list.
    2. Trigger the backfill ONCE: `npx convex run analyticsRollup:backfillHistorical` (or use the dashboard Functions runner). Run it EXACTLY ONCE — for un-keyed legacy events a second run would re-increment (D-05).
    3. Confirm it completes without a "read limit exceeded" / 16 MiB error and returns `{ processed: <N> }` where N is roughly the historical event count.
    4. Spot-check fidelity: pick 1-2 recent hours and compare an `aggregates` "events" bucket value against the raw event count for that hour+eventType (Convex dashboard data browser, or `eventCountsByPeriod` vs a manual count). They should match.
    5. Open the Analytics page (Heatmap, Sankey, Error-trend) and confirm no Convex read-limit error appears in the dashboard logs at current volume.
  </how-to-verify>
  <acceptance_criteria>
    - backfillHistorical returns `{ processed: N }` with N > 0 (or N == 0 if no historical events) and NO 16 MiB read error
    - Spot-checked aggregates "events" bucket value(s) match the raw event count for the sampled hour(s)
    - Analytics views load with no Convex read-limit error in logs
  </acceptance_criteria>
  <resume-signal>Type "approved" once backfill completed once, returned a processed count with no read-limit error, and spot-check matched; or describe the mismatch/error.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator → backfillHistorical action | A one-time privileged data operation triggered from the Convex dashboard/CLI (operator-authenticated) |
| Retention sweep → events table only | purgeOldTelemetryEvents deletes raw events; the aggregates boundary must remain uncrossed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-88-06 | Integrity | Backfill run more than once over-counts un-keyed events | mitigate | The checkpoint instructs the operator to run backfillHistorical EXACTLY ONCE; keyed events are idempotent on re-run, but un-keyed legacy events (D-05) would re-increment. Operator-gated, single-run, with a spot-check fidelity step before approval. |
| T-88-07 | Integrity | Retention sweep silently corrupts rollup counts | mitigate | dataRetention is confirmed (source + regression test) to delete only from the events table; aggregates are immutable historical buckets (D-12). Rollups stay correct after events are purged. |
| T-88-08 | DoS | Backfill trips the 16 MiB read limit and fails mid-run | mitigate | backfillHistorical is a cursor-paginated action (numItems 200, Plan 02) over events; each page is well under the limit. Verified by the no-read-limit-error acceptance criterion. |
| T-88-SC | Tampering | npm/pip/cargo installs | accept | No package installs. No legitimacy checkpoint required. |
</threat_model>

<verification>
- `npx vitest run convex/` green (dataRetention invariant passes)
- Operator checkpoint: backfill ran once, returned a processed count, no read-limit error, spot-check matched
- D-12 archival consistency locked by source + test
</verification>

<success_criteria>
- AR-02: one-time historical backfill populated rollups for pre-rollup events; rollup count == raw count (spot-checked); archival/retention never inflates or corrupts rollups (source + test)
</success_criteria>

<output>
Create `.planning/phases/88-analytics-rollup-table-durable-fix-for-convex-16-mib-read-li/88-03-SUMMARY.md` when done
</output>
