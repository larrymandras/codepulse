---
phase: 88-analytics-rollup
plan: 04
type: execute
wave: 3
depends_on: ["88-03"]
files_modified:
  - convex/analytics.ts
  - convex/analytics.test.ts
autonomous: false
requirements: [AR-01, AR-03]

must_haves:
  truths:
    - "activityHeatmap, toolFlowSankey, errorRateTrend, tokenSunburst read pre-aggregated aggregates buckets via the by_type_period_bucket index — no raw events/llmMetrics scan"
    - "All .take() count caps on the fat-table scans are removed from these four queries (heatmap ≤1000, sankey ≤1000, errorRateTrend ≤300×3, tokenSunburst ≤30000)"
    - "Every analytics query reads well under 16 MiB at any event volume (aggregates rows are slim, index-bounded)"
    - "errorRateTrend returns all 24 hour slots; an hour with no error buckets renders errors: 0 (never absent)"
    - "activityHeatmap day-of-week × hour mapping is UTC-based and semantically equivalent to the pre-rollup behavior (no timezone change)"
    - "tokenWaterfall stays a raw 30-min llmMetrics time-series with its .take(30000) cap intact (not a rollup candidate)"
  artifacts:
    - path: "convex/analytics.ts"
      provides: "Four analytics queries reading aggregates buckets; tokenWaterfall unchanged"
      contains: "by_type_period_bucket"
    - path: "convex/analytics.test.ts"
      provides: "Heatmap mapping + missing-hour=0 + sankey reconstruction + cap-removal assertions (green)"
  key_links:
    - from: "convex/analytics.ts queries"
      to: "aggregates table (metric_type events / sankey_edge / cost)"
      via: "withIndex by_type_period_bucket"
      pattern: "by_type_period_bucket"
    - from: "convex/analytics.ts toolFlowSankey"
      to: "convex/lib/sankeyClassify.ts"
      via: "categoryOf/outcomeOf for read-time node reconstruction consistency"
      pattern: "sankeyClassify"
---

<objective>
Rewrite the four heavy analytics queries to read pre-aggregated aggregates buckets and remove every .take() count cap, making reads O(buckets) and permanently under the 16 MiB limit. Leave tokenWaterfall raw-but-bounded.

Purpose: This is the payoff of the phase. Buckets are now authoritative (Plan 02) and backfilled (Plan 03), so the read path can switch off raw-event scans and drop the fragile quick-unblock caps that bound data fidelity. Cap removal happens HERE — after buckets are authoritative — never before (constraint 8).
Output: analytics.ts queries read aggregates; full data fidelity restored; 16 MiB risk eliminated.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/88-analytics-rollup-table-durable-fix-for-convex-16-mib-read-li/88-CONTEXT.md
@.planning/phases/88-analytics-rollup-table-durable-fix-for-convex-16-mib-read-li/88-RESEARCH.md
@.planning/phases/88-analytics-rollup-table-durable-fix-for-convex-16-mib-read-li/88-PATTERNS.md
@.planning/phases/88-analytics-rollup-table-durable-fix-for-convex-16-mib-read-li/88-VALIDATION.md
@convex/lib/sankeyClassify.ts

<interfaces>
<!-- aggregates read contract — executor reads buckets, does not scan raw events. -->
aggregates rows by metric_type:
  "events": dimensions { event_type } — written at ingest time (Plan 02). period "hourly".
  "sankey_edge": dimensions { source, target } — two edges per event (Plan 02). period "hourly".
  "cost": dimensions { provider, model, billingType, goalId } — written by computeHourly/rollupDaily cron (existing). period "hourly"|"daily".

Index read pattern (convex/aggregates.ts:201-234 costByPeriod is the production-proven analog):
  ctx.db.query("aggregates").withIndex("by_type_period_bucket", q => q.eq("metric_type", M).eq("period", "hourly").gte("bucket_start", cutoff)).collect()
  // safe: aggregates rows ~200-400 bytes, no payload: v.any()

Dimension extraction (convex/aggregates.ts:370-379 eventCountsByPeriod analog): read (r.dimensions as {...})?.field, group/sum r.value in JS.

Query return shapes MUST be unchanged (consumed by existing UI):
  activityHeatmap → [{ day, hour, count }] (or current shape)
  toolFlowSankey → { nodes, links }
  errorRateTrend → [{ hour, label, errors }] (24 slots)
  tokenSunburst → { tree, totalCost, totalTokens }
  tokenWaterfall → unchanged (raw)

From convex/lib/sankeyClassify.ts: categoryOf, outcomeOf (read-time reconstruction must use the SAME classifier the write path used — already imported in analytics.ts since Plan 01).

Current .take() caps to REMOVE (convex/analytics.ts): line 15 (heatmap ≤1000), line 48 (sankey ≤1000), lines 159/166/173 (errorRateTrend ≤300×3), line 106 (tokenSunburst ≤30000). KEEP line 239 (tokenWaterfall ≤30000). Line 202 (sessionDurations ≤200, slim sessions table) is OUT OF SCOPE — leave it.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rewrite activityHeatmap + errorRateTrend to read "events" aggregates buckets; remove their caps</name>
  <files>convex/analytics.ts, convex/analytics.test.ts</files>
  <read_first>
    - convex/analytics.ts:3-37 (activityHeatmap — current raw scan + .take(1000) + new Date mapping)
    - convex/analytics.ts:146-193 (errorRateTrend — current 3× .take(300) + 24-slot init pattern)
    - convex/aggregates.ts:201-234 (costByPeriod — the index-read analog)
    - convex/aggregates.ts:370-379 (eventCountsByPeriod — dimension extraction analog)
    - convex/analytics.test.ts (heatmap-mapping + missing-hour=0 tests scaffolded in Plan 01)
    - 88-RESEARCH.md Pitfall 4 (UTC mapping — preserve) + Pitfall 7 (missing-hour=0) + D-07/D-08
  </read_first>
  <behavior>
    - activityHeatmap: reads aggregates metric_type "events" period "hourly" bucket_start >= cutoff; sums value across all event_type dimensions per absolute hour; maps each bucket_start via new Date(bucket_start*1000).getDay()/.getHours() into the {day, hour} cell (UTC, unchanged semantics); no raw events scan, no .take cap
    - errorRateTrend: reads the same "events" buckets, filters in JS to error event_types (Error, ToolError, PostToolUseFailure), initializes all 24 hour slots to 0 then fills, returns 24 slots with errors: 0 for empty hours; no raw scan, no .take caps
  </behavior>
  <action>
    Rewrite convex/analytics.ts activityHeatmap to query aggregates with `.withIndex("by_type_period_bucket", q => q.eq("metric_type","events").eq("period","hourly").gte("bucket_start", cutoff)).collect()` (no .take), sum r.value across event_type dimensions per bucket_start, and map bucket_start→{day,hour} with `new Date(bucket_start*1000).getDay()`/`.getHours()` — preserve UTC behavior, do NOT add timezone logic (Pitfall 4). Rewrite errorRateTrend to read the same "events" buckets, filter in JS to the error eventTypes Error/ToolError/PostToolUseFailure (D-08), initialize all 24 hour slots to 0 before filling (Pitfall 7), and return the existing [{hour, label, errors}] shape. DELETE the .take(1000) at line 15 and the three .take(300) at lines 159/166/173. Finalize the heatmap-mapping and missing-hour=0 assertions in convex/analytics.test.ts (scaffolded RED in Plan 01) so they pass against the new logic. Keep return shapes identical to current. No fenced code — follow read_first analogs.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20; npx vitest run convex/analytics.test.ts -t "heatmap" 2>&1 | tail -12; npx vitest run convex/analytics.test.ts -t "missing-hour" 2>&1 | tail -12</automated>
  </verify>
  <acceptance_criteria>
    - activityHeatmap and errorRateTrend both query aggregates via `by_type_period_bucket` with `metric_type: "events"` (grep within each function ≥1)
    - The .take(1000) at heatmap and all three .take(300) in errorRateTrend are gone (grep `.take(1000)` and `.take(300)` in analytics.ts return 0)
    - analytics.test.ts heatmap-mapping and missing-hour=0 tests pass GREEN
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Heatmap + error-trend read events buckets; their caps removed; mapping + missing-hour tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rewrite toolFlowSankey (sankey_edge buckets) + tokenSunburst (cost buckets); remove their caps; confirm tokenWaterfall cap</name>
  <files>convex/analytics.ts, convex/analytics.test.ts</files>
  <read_first>
    - convex/analytics.ts:39-96 (toolFlowSankey — current raw scan + .take(1000) + categoryOf/outcomeOf node/link build)
    - convex/analytics.ts:98-143 (tokenSunburst — current llmMetrics .take(30000) + provider/model tree)
    - convex/analytics.ts:229-249 (tokenWaterfall — KEEP; confirm .take(30000) safe)
    - convex/aggregates.ts:237-276 (costByPeriodByProvider — provider/model grouping from cost dimensions analog)
    - convex/lib/sankeyClassify.ts (categoryOf/outcomeOf — read-time reconstruction must match write-time)
    - convex/analytics.test.ts (sankey reconstruction + cap-removal tests)
    - 88-RESEARCH.md Pitfall 2 (classifier consistency) + Pitfall 5 (waterfall cap) + D-09/D-10 + OQ-1 (cost key reconstruction)
  </read_first>
  <behavior>
    - toolFlowSankey: reads aggregates metric_type "sankey_edge" period "hourly"; reconstructs {nodes, links} by accumulating value per {source, target} edge from bucket dimensions; node set derived from the edge endpoints (same classifier the write path used); returns the existing {nodes, links} shape; no raw scan, no .take cap
    - tokenSunburst: reads aggregates metric_type "cost" (hourly/daily) for the 30-day window; groups by dimensions.provider + dimensions.model (parsing the existing provider::model::billingType::goalId key); returns the existing {tree, totalCost, totalTokens} shape (totalTokens not in cost aggregates — set 0 or omit consistently with current consumers); no llmMetrics scan, no .take cap
    - tokenWaterfall: unchanged; .take(30000) retained on the 30-min llmMetrics window (confirmed safe — Pitfall 5)
  </behavior>
  <action>
    Rewrite convex/analytics.ts toolFlowSankey to query aggregates `metric_type: "sankey_edge"`, `period: "hourly"` via by_type_period_bucket, accumulate `value` per {source, target} edge (dimensions), and reconstruct the {nodes, links} shape — node list is the unique set of edge endpoints. DELETE the .take(1000) at line 48. Rewrite tokenSunburst to read aggregates `metric_type: "cost"` for the 30-day window and group by `dimensions.provider` + `dimensions.model` exactly as costByPeriodByProvider does (OQ-1; parse the 4-segment provider::model::billingType::goalId key if dimensions are stored as a key string), returning the existing {tree, totalCost, totalTokens} shape (set totalTokens to 0 if cost rows lack token data, matching what the current consumer tolerates). DELETE the .take(30000) at line 106. Leave tokenWaterfall (line 229-249) and its line-239 .take(30000) UNCHANGED — add a brief comment confirming the 30-min window keeps it safe (Pitfall 5). Do NOT touch sessionDurations (line 202 .take(200) is out of scope — slim sessions table). Finalize the sankey-reconstruction + cap-removal assertions in convex/analytics.test.ts. No fenced code — follow read_first analogs.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20; npx vitest run convex/analytics.test.ts 2>&1 | tail -20; npx vitest run convex/ 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - toolFlowSankey queries aggregates `metric_type: "sankey_edge"` and tokenSunburst queries `metric_type: "cost"` (grep each ≥1)
    - Cap removal complete: in convex/analytics.ts the ONLY remaining `.take(` calls are line ~239 (tokenWaterfall, 30000) and the sessionDurations one (200). Specifically: `.take(1000)` returns 0, `.take(300)` returns 0, and there is exactly one `.take(30000)` (tokenWaterfall, not tokenSunburst)
    - analytics.test.ts sankey-reconstruction + cap-removal tests pass GREEN
    - Full `npx vitest run convex/` is green
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Sankey + sunburst read aggregates; their caps removed; tokenWaterfall confirmed safe; all convex tests green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify Analytics views render full-fidelity data with no read-limit error</name>
  <action>
    Operator-gated verification (no source change — implementation is complete in Tasks 1-2). After the read-path deploy is live, run `npm run dev` against the deployment, open the Analytics page, and confirm all four rewritten queries (heatmap, sankey, error-trend, sunburst) render full-fidelity data with no Convex 16 MiB read-limit error in the dashboard logs, per the verification steps below. The sankey connected-nodes check confirms write/read classifier consistency (Pitfall 2).
  </action>
  <what-built>
    All four heavy analytics queries (activityHeatmap, toolFlowSankey, errorRateTrend, tokenSunburst) now read pre-aggregated aggregates buckets instead of scanning raw events/llmMetrics. The .take() count caps are removed. Buckets are authoritative (Plan 02) and backfilled (Plan 03). tokenWaterfall stays raw-but-bounded.
  </what-built>
  <how-to-verify>
    The 16 MiB headroom and full-fidelity rendering are only observable against the real dataset in the live deployment.

    1. Ensure the read-path deploy is live (execute-phase deploy step, or `npx convex deploy --yes`).
    2. `npm run dev` (frontend) pointed at the deployment; open the Analytics page.
    3. Activity Heatmap: confirm it renders and shows more density than the old ≤1000-event cap allowed (full fidelity restored, AR-03).
    4. Tool Flow Sankey: confirm nodes and links render connected (no isolated nodes / zero-value links — Pitfall 2 classifier-consistency check) and reflect more than the old ≤1000 cap.
    5. Error-rate trend: confirm 24 hourly slots render, with empty hours showing 0 (not gaps).
    6. Token sunburst: confirm provider/model breakdown renders for the 30-day window.
    7. Open Convex dashboard logs while loading Analytics — confirm NO "read limit exceeded" / 16 MiB error on any of the four queries at current event volume.
  </how-to-verify>
  <acceptance_criteria>
    - All four Analytics widgets render real data with no Convex read-limit error in dashboard logs
    - Sankey renders connected nodes/links (no isolated nodes — confirms write/read classifier consistency)
    - Error-trend shows all 24 slots with 0 for empty hours
    - Visible fidelity exceeds the old capped limits (heatmap/sankey no longer truncated)
  </acceptance_criteria>
  <resume-signal>Type "approved" once all four Analytics views render full-fidelity with no read-limit error; or describe what is missing/erroring.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Analytics query → aggregates table (read-only) | Queries read slim, index-bounded buckets; no untrusted input is written here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-88-09 | Tampering/Integrity | Read-time sankey classifier drifts from write-time | mitigate | toolFlowSankey reconstructs nodes using categoryOf/outcomeOf from the SAME convex/lib/sankeyClassify.ts the ingest-time increment used (Plan 01 extraction). No copy-paste divergence possible; verified by the connected-nodes checkpoint. |
| T-88-10 | DoS | An analytics query trips the 16 MiB limit and fails the dashboard | mitigate | All four queries now read index-bounded `aggregates` (slim rows, no payload: v.any()); caps removed only because buckets are authoritative. tokenWaterfall retains its 30000 cap on a 30-min slim-row window (Pitfall 5). Verified by the no-read-limit-error checkpoint. |
| T-88-SC | Tampering | npm/pip/cargo installs | accept | No package installs. No legitimacy checkpoint required. |
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0
- `npx vitest run convex/` fully green — heatmap mapping, missing-hour=0, sankey reconstruction, cap-removal all pass
- Grep gates: no `.take(1000)` / `.take(300)` in analytics.ts; exactly one `.take(30000)` (tokenWaterfall); four queries read `by_type_period_bucket`
- Operator checkpoint: all four Analytics views render full-fidelity, no read-limit error
</verification>

<success_criteria>
- AR-03: all .take() count caps removed from the four heavy queries; full fidelity restored; every analytics query reads well under 16 MiB at any event volume
- AR-01: analytics.ts queries read O(buckets) from aggregates, not O(events) from raw scans
- tokenWaterfall remains raw-but-bounded (not forced into rollups)
</success_criteria>

<output>
Create `.planning/phases/88-analytics-rollup-table-durable-fix-for-convex-16-mib-read-li/88-04-SUMMARY.md` when done
</output>
