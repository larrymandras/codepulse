---
phase: 83-graph-snapshot-receiver
plan: 03
type: execute
wave: 3
depends_on: ["83-02"]
files_modified:
  - convex/graphSnapshots.fixture.ts
autonomous: false
requirements: [GH-01]
nyquist_compliant: true

must_haves:
  truths:
    - "A faithfully-shaped graph_snapshot POSTed to /runtime-ingest with a valid bearer key returns 200"
    - "After the POST, getProjectGraph returns the active version's {nodes, links} with storedNodeCount matching the stored node count"
    - "Re-POSTing the same snapshotId leaves exactly one active version (no duplicate active rows)"
  artifacts:
    - path: "convex/graphSnapshots.fixture.ts"
      provides: "Faithfully-shaped producer-envelope fixture for the verification POST (includes a vault node with community:null, a truncated source, and one dangling link)"
      min_lines: 25
  key_links:
    - from: "fixture POST → /runtime-ingest"
      to: "getProjectGraph active version"
      via: "case graph_snapshot → upsertGraphSnapshot → graphSnapshotNodes/Links"
      pattern: "getProjectGraph"
---

<objective>
Prove the receiver works end-to-end against a running Convex backend, because the producer's nightly cron (`15 0 * * *`) won't fire on demand and "the mutation returned ok" is not the verification bar (CONTEXT.md Specifics + global rules). Build a faithfully-shaped fixture matching the exact producer envelope (RESEARCH.md Code Examples), POST it to `/runtime-ingest` with a valid bearer key, then confirm via `getProjectGraph` that rows landed, the dangling link was dropped, `community: null` was accepted, and a re-POST does not duplicate the active version.

Purpose: This closes GH-01's real-data verification — the schema + module + dispatch land an actual round-trip, not just unit-green logic.
Output: A reusable fixture module + an operator-confirmed live round-trip.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/83-graph-snapshot-receiver/83-CONTEXT.md
@.planning/phases/83-graph-snapshot-receiver/83-RESEARCH.md
@.planning/phases/83-graph-snapshot-receiver/83-02-SUMMARY.md

<interfaces>
<!-- Producer envelope shape is LOCKED in 83-RESEARCH.md Code Examples (lines 401-451). -->
<!-- Bearer key + verification procedure: 83-RESEARCH.md Environment Availability (lines 587-594). -->

POST target: /runtime-ingest on the Convex deployment (VITE_CONVEX_URL host).
Auth: Authorization: Bearer <CODEPULSE_INGEST_KEY / same key used for Forge ingest> — from environment, never hardcoded.
Body envelope: { events: [ { event_type: "graph_snapshot", data: {…fixture}, timestamp: <epoch>, critical: false } ] }
Read-back: api.graphSnapshots.getProjectGraph (Convex dashboard or a useQuery), expect activeVersion=1 after first POST, storedNodeCount === fixture node count, storedLinkCount === fixture links minus the intentional dangling one.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the faithfully-shaped graph_snapshot fixture</name>
  <files>convex/graphSnapshots.fixture.ts</files>
  <read_first>
    - .planning/phases/83-graph-snapshot-receiver/83-RESEARCH.md — Code Examples: Producer Payload Shape (lines 401-431) and Telemetry Envelope (lines 433-451); Environment Availability fixture-POST recommendation (lines 587-594)
    - convex/graphSnapshots.ts — the upsertGraphSnapshot arg shape the fixture must satisfy (created in plan 02)
    - C:\Users\mandr\astridr-repo\astridr\automation\graph_snapshot.py — build_graph_snapshot() return value, to keep the fixture faithful to the real emit (namespaced ids, community:null for vault, sources[] fields, generatedAt float)
  </read_first>
  <action>
    Create `convex/graphSnapshots.fixture.ts` exporting a `graphSnapshotFixture` object matching the exact producer envelope. Include: `snapshotId: "astridr-project-graph"`; a small `nodes[]` mixing a graphify node (e.g. id `"graphify:codepulse:foo"`, type `"code"`, `community: 1`, source `"codepulse"`) and a vault node (id `"vault:SomeNote"`, type `"note"`, `community: null`, source `"vault"`); a `links[]` with at least one valid link between two present nodes AND one intentional DANGLING link (target id not present, e.g. `"graphify:codepulse:missing"`) so the receiver's drop is observable; a `sources[]` array with all seven fields per source including one with `truncated: true` and emitted < full counts; top-level `nodeCount` / `linkCount` as emitted/merged numbers; `generatedAt` as a float epoch (e.g. `1750312345.678901`). Keep node ids pre-namespaced — do NOT re-namespace. Do NOT hardcode any bearer key in this file. Add a JSDoc noting this is a verification fixture mirroring graph_snapshot.py and that the dangling link is intentional (asserts the D-05 guard).
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `convex/graphSnapshots.fixture.ts` exports `graphSnapshotFixture` with `snapshotId`, `nodes`, `links`, `sources`, `nodeCount`, `linkCount`, `generatedAt`
    - The fixture contains at least one node with `community: null` and one with a numeric `community`
    - The fixture `links` contains exactly one link whose target/source id is absent from `nodes` (the intentional dangling link)
    - The fixture `sources` contains an entry with `truncated: true` and `emittedNodeCount < nodeCount`
    - No bearer key / secret literal appears in the file
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>A faithful, type-clean fixture exists with a vault community:null node, a numeric-community node, a truncated source, and one intentional dangling link.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Live fixture-POST round-trip verification</name>
  <action>HUMAN CHECKPOINT — verify the live round-trip per <how-to-verify> below; the executor automates the POST/read where possible and pauses for operator confirmation. No source change in this task.</action>
  <what-built>
    The full GH-01 receiver: schema tables (plan 01), the `case "graph_snapshot"` dispatch + `upsertGraphSnapshot` versioned-swap + `getProjectGraph`/`listSnapshots` + retention sweep (plan 02), and a faithful producer-envelope fixture (Task 1). Everything is type-clean and the pure-logic suite is green; what remains is the one verification a unit test cannot do — a live POST round-trip against a running Convex backend, since the producer's nightly cron won't fire on demand.
  </what-built>
  <how-to-verify>
    1. Start the Convex dev backend: `npm run dev:backend` (`npx convex dev`) — wait for "Convex functions ready". The schema + new module must deploy without error (this itself confirms the schema validates against the module).
    2. Identify the deployment HTTP host (the `*.convex.site` URL printed by `convex dev`, or `VITE_CONVEX_URL` with the `.cloud`→`.site` host used for httpActions) and the ingest bearer key from the environment (the same key used for Forge ingest — `CODEPULSE_INGEST_KEY` / `VITE_ASTRIDR_API_KEY`). Do NOT paste the key into any committed file.
    3. POST the fixture to `/runtime-ingest`. Example (fill in HOST and read $INGEST_KEY from the env — do not echo it):
       `curl -sS -X POST "$HOST/runtime-ingest" -H "Authorization: Bearer $INGEST_KEY" -H "Content-Type: application/json" -d '{"events":[{"event_type":"graph_snapshot","data": <paste graphSnapshotFixture JSON>,"timestamp":1750312345.678901,"critical":false}]}'`
       Expect HTTP 200.
    4. In the Convex dashboard Functions tab (or via a scratch `useQuery`), run `api.graphSnapshots.getProjectGraph` with no args. Confirm: it returns a non-null object; `nodes` and `links` arrays are populated; `storedNodeCount` equals the fixture node count; `storedLinkCount` equals the fixture link count MINUS the one intentional dangling link (the dangling link was dropped); the `community: null` vault node is present.
    5. Confirm `activeVersion` on the `graphSnapshots` row is `1` (Data tab).
    6. Re-POST the SAME fixture (repeat step 3). Re-run `getProjectGraph`: confirm it still returns exactly one coherent graph (no doubled nodes), and the `graphSnapshots` row's `activeVersion` is now `2` (idempotent full-replacement — one active version, not two).
    7. Negative auth check: repeat the POST WITHOUT the `Authorization` header → expect HTTP 401 (confirms the bearer gate still guards the new path).
    8. Run `api.graphSnapshots.listSnapshots` → confirm one row with `{snapshotId, nodeCount, linkCount, generatedAt, updatedAt}`.
  </how-to-verify>
  <resume-signal>Type "approved" if the round-trip returns the expected populated graph, the dangling link was dropped, re-POST yields activeVersion=2 with one active graph, and unauthenticated POST returns 401. Otherwise describe what diverged (e.g. wrong storedLinkCount, schema deploy error, 401 on the authed POST).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator curl → /runtime-ingest | The verification POST crosses the same bearer-authed boundary the real producer uses |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-83-08 | Information Disclosure | Bearer key in verification command | mitigate | Key is read from the environment at POST time and never written to the fixture file or any committed artifact; the checkpoint instructs the operator not to echo or paste it |
| T-83-01 | Spoofing | /runtime-ingest auth on the new path | mitigate | Step 7 of the checkpoint actively verifies an unauthenticated POST returns 401 — confirms the new `graph_snapshot` dispatch did not bypass `validateIngestAuth` |
| T-83-03 | Tampering | Dangling-link corruption | mitigate | The fixture carries an intentional dangling link; step 4 confirms `storedLinkCount` is one less than emitted, proving the D-05 guard runs on the real path |
| T-83-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan |
</threat_model>

<verification>
- Live: fixture POST → 200; `getProjectGraph` returns a populated active graph with the dangling link dropped and the `community:null` node present; re-POST → `activeVersion=2`, one active graph; unauthenticated POST → 401.
- `npx tsc --noEmit` clean (fixture compiles against the upsert arg shape).
</verification>

<success_criteria>
- A real round-trip stores rows and is observed via `getProjectGraph` (not "the mutation returned ok").
- Idempotent full-replacement confirmed (activeVersion increments; one active graph).
- Bearer auth confirmed still enforced on the new path.
</success_criteria>

<output>
Create `.planning/phases/83-graph-snapshot-receiver/83-03-SUMMARY.md` when done. Record the operator's confirmation (storedNodeCount / storedLinkCount observed, activeVersion progression, 401 on unauth) in the SUMMARY § CHECKPOINT.
</output>
