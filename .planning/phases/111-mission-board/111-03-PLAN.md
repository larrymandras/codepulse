---
phase: 111-mission-board
plan: 03
type: execute
wave: 2
depends_on: [111-01, 111-02]
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/seeds/SEED-007-mission-emitter-revival.md
autonomous: false
requirements: [MISSION-03]

must_haves:
  truths:
    - "D-02: the emitter-revival work — real submittedAt, non-terminal queued/running states, and a mission-to-tool correlation key — is logged as a seed in .planning/seeds/ and is provably not attempted in this phase"
    - "D-03: REQUIREMENTS.md marks MISSION-02 blocked-on-astridr with the probe evidence inline and reassigns it away from Phase 111, so the traceability table stops asserting a requirement Phase 111 provably cannot deliver"
    - "D-04: REQUIREMENTS.md records MISSION-01 as partially delivered — its duration and orphan-recovery halves deferred with the emitter phase, its status and history halves shipped here"
    - "D-06: the honesty fix reached every consumer of subagentJobs, not JobsPanel alone — a whole-tree grep for ActiveAgentsPanel returns zero and JobsPanel carries no live chrome"
    - "D-11: convex/retention.ts's RETENTION_DAYS still has no subagentJobs key — the deliberate non-goal held, and the deferral is recorded in the seed"
    - "D-12: convex/subagentJobs.ts listRecent still uses its unbounded .collect() unchanged — the deliberate non-goal held, and the deferral is recorded in the seed"
    - "Operator can open the two changed surfaces and confirm no panel claims a mission state the data cannot back"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "Traceability that matches what Phase 111 actually shipped"
      contains: "blocked-on-astridr"
    - path: ".planning/seeds/SEED-007-mission-emitter-revival.md"
      provides: "The backlog entry owning the live board, the deferred requirement halves, and both Phase 111 non-goals"
      contains: "SEED-007"
  key_links:
    - from: ".planning/REQUIREMENTS.md"
      to: ".planning/seeds/SEED-007-mission-emitter-revival.md"
      via: "MISSION-01/MISSION-02 deferral notes name the seed that now owns them"
      pattern: "SEED-007"
    - from: ".planning/seeds/SEED-007-mission-emitter-revival.md"
      to: ".planning/seeds/SEED-002-mission-control-jobs-board.md"
      via: "cross-link — SEED-007 is the prerequisite SEED-002's live board waits on"
      pattern: "SEED-002"
---

<objective>
Close the phase by making the planning record match what shipped, and by proving the two
deliberate non-goals actually held.

Purpose: D-03 and D-04 exist because leaving MISSION-01 and MISSION-02 marked `Pending` against a
phase that provably cannot deliver them would make the traceability table lie — the same defect
class the phase's UI work removes. D-02 requires the deferred work be logged rather than dropped.
D-11 and D-12 are non-goals whose value is entirely in NOT having happened, so this plan measures
them rather than assuming them.

Output: corrected `REQUIREMENTS.md`, a new seed owning the deferred work, a measured non-goal
audit, and one operator checkpoint against the running app.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/111-mission-board/111-CONTEXT.md
@.planning/phases/111-mission-board/111-UI-SPEC.md
@.planning/phases/111-mission-board/111-01-SUMMARY.md
@.planning/phases/111-mission-board/111-02-SUMMARY.md
@.planning/seeds/SEED-002-mission-control-jobs-board.md

<interfaces>
<!-- Exact edit targets and the seed frontmatter schema. Extracted from the repo. -->

.planning/REQUIREMENTS.md — the two edit regions:
  lines 48-50  : the MISSION-01 / MISSION-02 / MISSION-03 bullets under "## Mission Board (MISSION)"
  line  46     : the section preamble, which still reads "Frontend-only, on data that streams
                 today." — falsified by the probe; nothing has streamed since 2026-07-07
  lines 93-95  : the traceability rows
                   | MISSION-01 | Phase 111 | Pending |
                   | MISSION-02 | Phase 111 | Pending |
                   | MISSION-03 | Phase 111 | Pending |

.planning/seeds/ — existing entries are SEED-001 … SEED-006, so the next free id is SEED-007.
Frontmatter schema in use (verified against SEED-002):
  id, status, planted, planted_during, trigger_when, scope, origin, paired_seed

The probe evidence to cite inline (from 111-CONTEXT.md `<constraints>`, probe
`npx convex run subagentJobs:listRecent --url http://127.0.0.1:3210`, 2026-08-10T20:49Z):
  7 rows ever | {failed:2, cancelled:3, completed:2} | zero queued, zero running |
  submittedAt === finishedAt in 7 of 7 | newest row 2026-07-07 | 0 rows with sessionId/traceId |
  subagentJobs absent from retention.ts (control: toolExecutions present)
Corroborating code: convex/runtimeIngest.ts:594-596, convex/subagentJobs.ts:13-16,
convex/schema.ts:562-586 and :1028-1041, convex/retention.ts:34.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Correct REQUIREMENTS.md so the traceability table matches what shipped (D-03, D-04)</name>

  <files>.planning/REQUIREMENTS.md</files>

  <read_first>
    - `.planning/REQUIREMENTS.md` lines 44-51 (the MISSION section) and lines 83-102 (the
      traceability table). **Read the file as it actually stands when you open it, not as this
      plan quotes it.** Two of the three MISSION entries will ALREADY have been mutated before
      this task runs — see the mechanism note immediately below — so the "before" text quoted in
      the action section describes the file at plan-authoring time, not at execution time.
    - **MECHANISM — why MISSION-01 and MISSION-03 are already flipped when you arrive.**
      `~/.claude/get-shit-done/workflows/execute-plan.md:450-458` (step `update_requirements`)
      runs unconditionally at the close of EVERY completed plan:
      `gsd-sdk query requirements.mark-complete ${REQ_IDS}`, taking `${REQ_IDS}` from that plan's
      own frontmatter. `111-01-PLAN.md:12` declares `requirements: [MISSION-01, MISSION-03]`, and
      this plan is `wave: 2, depends_on: [111-01, 111-02]`, so 111-01's close-out fires first.
      `~/.claude/get-shit-done/bin/lib/milestone.cjs:48,56` (`cmdRequirementsMarkComplete`) then
      applies two blind rewrites per ID:
        checkbox — `/(-\s*\[)[ ](\]\s*\*\*MISSION-01\*\*)/gi`, turning `- [ ] **MISSION-01**` into `- [x] …`
        table    — `/(\|\s*MISSION-01\s*\|[^|]+\|)\s*Pending\s*(\|)/gi`, turning the row into `… | Complete |`
      Both patterns match the live file exactly (verified 2026-08-10: `.planning/REQUIREMENTS.md:48`
      is literally `- [ ] **MISSION-01** — Operator can see background missions…` and line 93 is
      literally `| MISSION-01 | Phase 111 | Pending |`), so both WILL fire. What that means per ID:
        - **MISSION-01** — arrives here as `[x]` / `Complete`. That flatly contradicts D-04, which
          is the entire reason this task exists. Your job is to **revert** it, not to preserve it.
        - **MISSION-03** — arrives as `[x]` / `Complete`. Here the flip is correct and desired;
          confirm it rather than re-ticking or fighting it.
        - **MISSION-02** — appears in NO plan's `requirements:` frontmatter, so the tool never
          touches it. It arrives untouched as `- [ ]` / `Pending`, exactly as quoted below.
      `execute-plan.md:454` is the ONLY caller of `requirements.mark-complete` in the toolchain
      (verified by grep across `workflows/`, `commands/`, `bin/` and `agents/`), and the only
      invocation left after this task is THIS plan's own close-out with `MISSION-03` alone — which
      cannot re-flip MISSION-01. A revert made here is therefore durable; do not add a defensive
      re-check task or a post-hoc repair step.
    - `.planning/phases/111-mission-board/111-CONTEXT.md` `<constraints>` — the evidence table to
      quote, and the D-03/D-04 wording to honor
    - `.planning/ROADMAP.md` lines 804-819 — Phase 111's entry, **already corrected in place on
      2026-08-10**. Verify that correction is present; do NOT re-correct or re-strike it. If the
      strikethrough goal and the narrowed-scope paragraph are missing, stop and report rather than
      rewriting it.
  </read_first>

  <action>
Three edits to `.planning/REQUIREMENTS.md`. Every claim added must carry its evidence inline in
the same sentence — a bare status flip with the reasoning in another file is the ordering defect
this project's verification rule forbids.

**(1) The section preamble, line 46.** It reads "Frontend-only, on data that streams today. The
parts of SEED-002 needing astridr SEED-023's runtime stay planted." The first clause is falsified.
Strike it with `~~…~~` (do not delete — this file's house style preserves the original text under
strikethrough, as ROADMAP.md's Phase 111 entry already demonstrates) and append a dated correction
noting the probe date and the single decisive fact: nothing has been emitted since 2026-07-07, so
the phase shipped a post-hoc history board rather than a live one.

**(2) The three MISSION bullets, lines 48-50.**
  - **MISSION-01** — **revert the checkbox from `[x]` back to `[ ]`**, then append a
    `**Partial (D-04)**` note. It will read `- [x] **MISSION-01**` when you open the file, because
    111-01's close-out already ran `requirements.mark-complete MISSION-01` off its own frontmatter
    (full mechanism in `<read_first>`). Setting it back to `[ ]` is a deliberate correction of a
    tool that has no way to express "partial" — it is not a state you are preserving. Say exactly
    that in the appended note, so a later session does not read the unchecked box as unstarted
    work and re-flip it. The note itself: the honest per-mission status and history-rendering half
    **shipped in Phase 111**; the *duration* and *orphan-recovery* halves are deferred to SEED-007
    because `submittedAt` is a synthetic copy of `finishedAt` in 7 of 7 rows (so no duration is
    derivable) and no `running` row can arrive (`runtimeIngest.ts:594-596` routes terminal-state
    events only), which makes the orphan clause vacuous rather than merely unbuilt.
  - **MISSION-02** — append `**Blocked on astridr (D-03) — reassigned to SEED-007.**` with the
    evidence inline: `toolExecutions` carries no `jobId` (`schema.ts:562-586`), `subagentJobs`
    carries no `sessionId`/`traceId` (`schema.ts:1028-1041`), 0 of 7 rows carry either, and
    `toolExecutions` is on 14-day retention (`retention.ts:34`) so even a retroactive join key
    could not recover tool activity for the July missions. State explicitly that this requirement
    is NOT claimed by Phase 111.
  - **MISSION-03** — the checkbox will ALREADY read `[x]` when you open the file (same mechanism;
    for this ID the flip is correct and desired, so confirm it rather than re-ticking or
    reverting it). Append the delivery note: Phase 111 removed
    every affordance asserting an unbacked state across **both** consumers of `subagentJobs`
    (`JobsPanel`'s queued/running icons, duration figure and live chrome; `ActiveAgentsPanel`
    deleted outright), citing plans 111-01 and 111-02.

**(3) The traceability rows, lines 93-95.** Only MISSION-02's row will still read `Pending` when
you arrive; MISSION-01's and MISSION-03's will already read `Complete` (mechanism in
`<read_first>`). Write all three to these final values regardless of what each currently holds:
  - `MISSION-01 | Phase 111 | Partial — status/history shipped; duration + orphan recovery
    deferred to SEED-007 (D-04)` — a **revert** of the tool's premature `Complete`
  - `MISSION-02 | SEED-007 (was Phase 111) | Blocked on astridr — no join key exists (D-03)` —
    a rewrite of a still-`Pending` row
  - `MISSION-03 | Phase 111 | Complete` — already correct; confirm, do not rewrite
Keep the table's column count and pipe formatting exactly as-is so the file stays machine-readable.

**Prohibitions:** do not modify any non-MISSION requirement, row, or section — DUR-*, TELE-*,
ENGINE-* and DEBT-* belong to other phases and at least one (DUR-*) is being actively edited by
Phase 110. Do not edit `.planning/ROADMAP.md` (already corrected) or `.planning/STATE.md` (owned by
the phase-close step, and a known clobber surface). Do not touch any file under `src/` or `convex/`.
  </action>

  <verify>
    <automated>grep -c "blocked-on-astridr\|Blocked on astridr" .planning/REQUIREMENTS.md</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "MISSION-02 | SEED-007" .planning/REQUIREMENTS.md` returns 1
    - **MISSION-01 positive checks — these are the load-bearing ones for D-04.** All three must
      hold: `grep -c "^- \[ \] \*\*MISSION-01\*\*" .planning/REQUIREMENTS.md` returns 1 (the
      tool's premature `[x]` was reverted), AND
      `grep -c "^| MISSION-01 | Phase 111 | Partial" .planning/REQUIREMENTS.md` returns 1 (the row
      carries the D-04 disposition rather than the tool's premature `Complete`), AND
      `grep -c "Partial (D-04)" .planning/REQUIREMENTS.md` returns at least 1 (the bullet note landed)
    - `grep -c "^| MISSION-01 | Phase 111 | Complete" .planning/REQUIREMENTS.md` returns 0 — the
      exact wrong state this task exists to undo, checked by name rather than inferred from an
      absence
    - `grep -cE "^\| MISSION-0[12] \| Phase 111 \| Pending \|$" .planning/REQUIREMENTS.md` returns 0.
      **Supplement only — never cite this alone as proof for MISSION-01.** It passes VACUOUSLY when
      the row is stuck at the tool's premature `Complete`, because `Complete` is not `Pending`
      either; the positive checks above are what actually prove the D-04 disposition landed.
    - `grep -c "MISSION-03 | Phase 111 | Complete" .planning/REQUIREMENTS.md` returns 1
    - `grep -c "SEED-007" .planning/REQUIREMENTS.md` returns at least 3 (both deferral notes plus
      the traceability row)
    - `grep -c "2026-07-07" .planning/REQUIREMENTS.md` returns at least 1 (the decisive probe fact
      is inline, not merely referenced)
    - `grep -c "schema.ts:562-586" .planning/REQUIREMENTS.md` returns at least 1 (MISSION-02's
      no-join-key evidence is inline)
    - `grep -c "^- \[x\] \*\*MISSION-03\*\*" .planning/REQUIREMENTS.md` returns 1
    - `git diff --stat .planning/REQUIREMENTS.md` shows changes confined to the MISSION section and
      the three MISSION traceability rows — read the full diff and confirm no DUR/TELE/ENGINE/DEBT
      line was touched. Any deletion you cannot individually account for means a concurrent session
      clobbered the file: restore with `git checkout HEAD -- .planning/REQUIREMENTS.md` and redo,
      never hand-merge.
    - `git status --porcelain` lists no file under `src/` or `convex/` and does not list
      `.planning/ROADMAP.md` or `.planning/STATE.md`
  </acceptance_criteria>

  <done>
    The MISSION section and its three traceability rows state exactly what shipped, with the probe
    evidence and `file:line` citations inline; no other requirement family was touched.
  </done>
</task>

<task type="auto">
  <name>Task 2: Plant SEED-007 for the emitter revival and audit the two non-goals (D-02, D-06, D-11, D-12)</name>

  <files>.planning/seeds/SEED-007-mission-emitter-revival.md</files>

  <read_first>
    - `.planning/seeds/SEED-002-mission-control-jobs-board.md` — the frontmatter schema and prose
      register to match; SEED-007 is the prerequisite SEED-002's live board has been waiting on
    - `.planning/phases/111-mission-board/111-CONTEXT.md` — D-02, D-11, D-12 and the evidence table
    - `.planning/phases/111-mission-board/111-01-SUMMARY.md` and `111-02-SUMMARY.md` — the shipped
      state this audit measures against
  </read_first>

  <action>
**(a) Create `.planning/seeds/SEED-007-mission-emitter-revival.md`**, matching SEED-002's
frontmatter schema exactly: `id: SEED-007`, `status: dormant`, `planted: 2026-08-10`,
`planted_during: v14.0 / Phase 111 (Mission Board scope narrowing)`, a `trigger_when` naming the
condition rather than a date (the astridr emitter resumes writing `subagent_job` events, or the
live-mission board is scheduled), `scope: Medium`, an `origin` citing 111-CONTEXT.md's probe and
`paired_seed: .planning/seeds/SEED-002-mission-control-jobs-board.md`.

Body content — this seed is the single owner of everything Phase 111 deliberately did not do:

  1. **The emitter repair (D-02), cross-repo, astridr-side.** Real `submittedAt` distinct from
     `finishedAt` (today `convex/subagentJobs.ts:13-16` documents the fallback and 7 of 7 rows are
     synthetic copies); non-terminal `queued`/`running` states, which `runtimeIngest.ts:594-596`
     records as living only in Supabase today; and a mission-to-tool correlation key, since
     `toolExecutions` has no `jobId` and `subagentJobs` no `sessionId`/`traceId`.
  2. **The deferred requirement halves.** MISSION-01's duration and orphan-recovery clauses (D-04)
     and MISSION-02 in full (D-03), each with the reason it could not ship in Phase 111.
  3. **D-11 — bound `subagentJobs` in `convex/retention.ts`'s `RETENTION_DAYS`.** Record why it was
     correct to skip it in Phase 111 (7 lifetime rows, nothing since 2026-07-07, and `retention.ts`
     was Phase 110's active file) and why it becomes necessary the moment the emitter revives: a
     revived firehose against an unbounded table is exactly the growth the house precedent at
     `retention.ts:39-48, 79-91` pre-empts for live tables.
  4. **D-12 — bound `listRecent`'s unbounded `.collect()` (`convex/subagentJobs.ts:88`).** It
     collects the whole table then slices 50. Harmless at 7 rows; a real hazard the moment rows
     arrive at volume.
  5. **A carried-forward robustness note from 111-01's threat model.** `JobsPanel.tsx`'s
     `stateIcon[job.status]` is an unguarded prototype-chain lookup: an emitter-supplied status of
     `toString` or `constructor` resolves to an inherited `Object.prototype` member instead of
     `undefined`, so the `??` fallback does not fire. Impact today is a missing icon plus a React
     child warning — no XSS, no disclosure — and UI-SPEC locked that expression as preserve-as-is,
     so it was correctly left alone. The phase that reopens this file should guard it with
     `Object.hasOwn`.
  6. **A typing note.** `src/hooks/useSubagentJobs.ts`'s `SubagentJobStatus` union is narrower than
     the column it models: `convex/subagentJobs.ts:28` declares `status: v.string()` and
     `convex/runtimeIngest.ts:615` writes `d.status ?? "unknown"`. Phase 111 left the union alone
     (no decision authorized widening it) and absorbed the gap with one cast in
     `JobsPanel.test.tsx`. Widening belongs with the emitter phase, which will settle the real
     state set.

Finally, add a one-line cross-reference to SEED-007 inside
`.planning/seeds/SEED-002-mission-control-jobs-board.md` — a single line naming SEED-007 as the
prerequisite its live board waits on. Change nothing else in SEED-002, including its frontmatter.

**(b) Run the non-goal and class-coverage audit and record it in the SUMMARY.** These are claims
about absence, so each one pairs with a control that proves the probe can see a positive — an
absence result from a probe that could not have shown presence is worthless evidence.

**First, set the diff anchor.** Every `git diff` below is anchored to a real pre-phase commit, NOT
to a hardcoded ancestor count:

    PHASE_BASE=b89cd07f    # docs(111): plan Mission Board as 3 plans in 2 waves

That is the last commit before any Phase 111 code landed. Do NOT substitute `HEAD~N`: this phase
commits per task in one sequential checkout (`.planning/config.json` has `"use_worktrees": false`,
and `execute-plan.md:466` states task code is already committed per-task), so by the time this task
runs the phase has produced roughly 7+ commits — 111-01's three tasks plus its metadata commit,
111-02's two tasks plus its metadata commit, and this plan's Task 1 — and any small fixed `HEAD~N`
silently reaches back only into Wave 2, missing every Wave-1 change it claims to be auditing. A
concurrent session committing to this shared branch makes the count less predictable still. If
`git cat-file -e b89cd07f` fails, derive the anchor instead with
`git log --grep="docs(111): plan Mission Board" -1 --format=%H` and record which form you used.

  - **D-11:** `grep -n "subagentJobs" convex/retention.ts` must return nothing inside the
    `RETENTION_DAYS` object. Control: `grep -n "toolExecutions" convex/retention.ts` must return a
    hit inside that object, proving the search reaches the right region of the right file.
  - **D-12:** `grep -n "collect()" convex/subagentJobs.ts` must still show the call at line ~88,
    and `git diff $PHASE_BASE --stat -- convex/` must show `convex/` untouched across this phase's
    commits. Control — this one must prove **range coverage**, not merely non-emptiness:
    `git diff $PHASE_BASE --stat -- src/components/JobsPanel.tsx` must be NON-EMPTY. That file is a
    known Wave-1 edit (111-01 Task 1 rewrites it in full), so it MUST appear if the range spans
    Wave 1. If it is empty, the range is too shallow or anchored wrong — STOP and fix the anchor
    before recording ANY `convex/` result, because a too-shallow range reports "convex untouched"
    for a reason that has nothing to do with whether D-12 held. A merely non-empty `-- src/` diff
    does not establish this: it is satisfied by Wave-2 commits alone.
  - **D-06 (both consumers fixed):** `grep -rn "ActiveAgentsPanel" src/` returns nothing, AND
    `grep -c "animate-pulse" src/components/JobsPanel.tsx` returns 0, AND
    `grep -c "MISSION HISTORY" src/components/JobsPanel.tsx` returns **at least 1** (not an
    exact count — 111-01's own gate was relaxed for the same reason: the rewritten header
    docstring may legitimately name the surface a second time). Control: run the
    `ActiveAgentsPanel` grep against `.planning/` and confirm it DOES return hits — that proves the
    pattern and the tool work and the `src/` zero is a real absence, not a broken search.
  - **D-01 (frontend-only):** `git diff $PHASE_BASE --name-only` lists no path under `convex/` and
    no path outside this repo. Run this only after the `JobsPanel.tsx` range-coverage control
    above has fired — an empty-of-`convex/` result from an unproven range is not evidence.

Paste the actual command output for each into the SUMMARY. Do not paraphrase a result as "verified".
  </action>

  <verify>
    <automated>test -f .planning/seeds/SEED-007-mission-emitter-revival.md &amp;&amp; grep -c "SEED-007" .planning/seeds/SEED-007-mission-emitter-revival.md</automated>
  </verify>

  <acceptance_criteria>
    - `.planning/seeds/SEED-007-mission-emitter-revival.md` exists with frontmatter keys `id`,
      `status`, `planted`, `planted_during`, `trigger_when`, `scope`, `origin`, `paired_seed`
    - `grep -c "D-11\|RETENTION_DAYS" .planning/seeds/SEED-007-mission-emitter-revival.md` returns
      at least 2
    - `grep -c "D-12\|collect()" .planning/seeds/SEED-007-mission-emitter-revival.md` returns at
      least 2
    - `grep -c "SEED-007" .planning/seeds/SEED-002-mission-control-jobs-board.md` returns 1
    - `git diff --stat .planning/seeds/SEED-002-mission-control-jobs-board.md` shows exactly one
      added line and zero deletions
    - `grep -n "subagentJobs" convex/retention.ts` returns no match (D-11 held) while
      `grep -n "toolExecutions" convex/retention.ts` DOES return a match (the control fired)
    - `grep -rn "ActiveAgentsPanel" src/` returns no match while
      `grep -rn "ActiveAgentsPanel" .planning/` DOES return matches (the control fired)
    - `git diff $PHASE_BASE --name-only` (with `PHASE_BASE=b89cd07f`) lists no path under `convex/`,
      AND its range-coverage control fired:
      `git diff $PHASE_BASE --stat -- src/components/JobsPanel.tsx` is non-empty. A `convex/`-clean
      result recorded without that control having fired is not evidence and must not be recorded
      as a pass
    - Every audit bullet above appears in the SUMMARY with its verbatim command output, including
      the control results
  </acceptance_criteria>

  <done>
    SEED-007 exists and owns all six deferred items; SEED-002 links to it; and each non-goal is
    proven held by a probe whose control was observed to fire, with the raw output recorded.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Operator confirms both changed surfaces against the running app</name>

  <read_first>
    - `.planning/phases/111-mission-board/111-01-SUMMARY.md` and `111-02-SUMMARY.md` — including
      any trailing-cluster layout note 111-01 carried forward
    - `.planning/phases/111-mission-board/111-UI-SPEC.md` Copywriting Contract — the exact strings
      the operator is checking against
  </read_first>

  <action>
    Do not implement anything in this task. Confirm first that `npx vitest run` and
    `npx tsc --noEmit` both exit 0, then present the numbered steps below to the operator verbatim
    and stop. Automated coverage already exists for every string being checked; the purpose of this
    gate is the class of defect a jsdom suite structurally cannot see — layout, overflow, a visible
    gap where a panel was removed, and console errors at real mount. Do not fix anything the
    operator reports during this task: record it, and let the phase-close step decide whether it is
    in scope. Silently restyling the trailing cluster would exceed the phase's locked UI contract.
  </action>

  <what-built>
    Plan 111-01 rewrote `JobsPanel` as a mission history surface (MISSION HISTORY header, no
    pulsing dot, `{n} missions` badge, `finished X ago` timestamps with a day tier, three terminal
    status icons). Plan 111-02 deleted `ActiveAgentsPanel` and its mount from the Chat page's
    command-center left rail. Both are covered by passing automated tests, but this project's
    history is explicit that a green suite is not evidence about rendered UI.
  </what-built>

  <how-to-verify>
    1. Confirm the dev server is up. Probe BOTH `http://localhost:5173` and `http://[::1]:5173` —
       Vite frequently binds `::1` only, so an IPv4-only probe reports a healthy server as down.
       If it is not running, start it with `npm run dev` in its own terminal.
    2. Open `http://localhost:5173` and navigate to the **Live Run** page. Find the panel above the
       tab content.
       - It must read **MISSION HISTORY**, not BACKGROUND JOBS.
       - There must be **no pulsing dot** to the left of that label, and no glyph in its place.
       - The count badge must read **"{n} missions"**, not "{n} jobs".
       - With the live data (7 lifetime rows, newest 2026-07-07), each row's right-hand timestamp
         should read something like **"finished 34d ago"** — a `d` unit, not a three-digit `h`
         value, and not a bare `816h`.
       - Confirm no row shows a duration, elapsed time, cost figure, confirm card, or squad
         grouping.
    3. Judge the trailing cluster's layout: `finished 34d ago` is materially longer than the `5s`
       it replaced. Report whether the status badge and timestamp wrap, overflow, or crowd the
       panel at your window width. **Do not ask for a fix in this session** — record it, because
       no decision authorized restyling this cluster and a silent style change would exceed the
       phase's locked scope.
    4. Navigate to the **Chat** page and turn **command center mode ON** (the GRID toggle).
       - The left rail must show **Intelligence Feed only**.
       - There must be **no "ACTIVE AGENTS" panel**, no empty bordered card where it was, no
         "coming soon" tile, and no visible gap that reads as a missing panel.
       - Confirm **Mission Timeline** is still present and rendering — it is a different panel that
         merely shares a word, and it must not have been caught by the deletion.
    5. Open the browser devtools console on both pages and report any error or warning naming
       `JobsPanel`, `Chat`, `SectionErrorBoundary`, or "Functions are not valid as a React child".
       Per this project's error-triage rule, do not dismiss anything you see as pre-existing —
       report it verbatim.
  </how-to-verify>

  <acceptance_criteria>
    - `npx vitest run` exits 0 and `npx tsc --noEmit` exits 0 before the operator is prompted
    - The operator returned an explicit verdict; silence, a timeout, or an idle verifier is NOT a
      pass and must not be recorded as one
    - The SUMMARY records, verbatim: the operator's verdict, the step-3 trailing-cluster layout
      observation, and any console error or warning reported in step 5
    - `git status --porcelain` shows no file modified by this task — a checkpoint changes nothing
  </acceptance_criteria>

  <resume-signal>
    Type "approved" to close the phase, or describe what you saw. Include the trailing-cluster
    layout observation from step 3 either way — it is expected output, not a defect report.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Planning artifact → future session | `REQUIREMENTS.md` and the seeds are read as ground truth by later planning sessions; a false status here propagates as a wrong premise, which is this project's most-repeated failure class |
| Shared git checkout → this plan's commits | A concurrent session can stage or commit into the same branch mid-edit |

This plan writes only Markdown under `.planning/`. It ships no code, no dependency, no network
call, and no data path. The register reflects that honestly rather than being padded.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-111-07 | Repudiation | `REQUIREMENTS.md` recording a requirement status that does not match shipped code | mitigate | Every corrected bullet and row carries its probe date and `file:line` evidence inline in the same sentence, and Task 2's audit re-derives each non-goal claim by grep with a control that must fire — no status is written from expectation. |
| T-111-08 | Tampering | A concurrent session clobbering `.planning/REQUIREMENTS.md` between read and write in this shared checkout | mitigate | Task 1 requires reading the full `git diff` and individually accounting for every deletion; an unaccountable deletion mandates `git checkout HEAD -- .planning/REQUIREMENTS.md` and a redo rather than a hand-merge. `STATE.md` is deliberately excluded from `files_modified` as a known clobber surface. |
| T-111-09 | Information disclosure | Evidence quoted inline from the live self-hosted Convex probe | accept | The quoted facts are row counts, status distributions and timestamps — no credential, no connection string, no token, and no user content. The probe command recorded in `111-CONTEXT.md` carries a localhost URL and no key. |
| T-111-SC | Tampering | npm/pip/cargo installs | accept — not applicable | This plan installs no packages; `files_modified` contains only Markdown. `git status --porcelain` must not list `package.json`. |
</threat_model>

<verification>
- `grep -cE "^\| MISSION-0[12] \| Phase 111 \| Pending \|$" .planning/REQUIREMENTS.md` returns 0
  (supplement — vacuous for MISSION-01 on its own, since the tool leaves that row at `Complete`)
- MISSION-01 carries its D-04 disposition, proven POSITIVELY:
  `grep -c "^- \[ \] \*\*MISSION-01\*\*" .planning/REQUIREMENTS.md` returns 1 and
  `grep -c "^| MISSION-01 | Phase 111 | Partial" .planning/REQUIREMENTS.md` returns 1
- `.planning/seeds/SEED-007-mission-emitter-revival.md` exists and SEED-002 links to it
- The D-11 probe returns empty and its `toolExecutions` control returns a hit
- The `ActiveAgentsPanel` probe returns empty against `src/` and its `.planning/` control returns hits
- `git diff b89cd07f --name-only` lists no path under `convex/`, with the
  `git diff b89cd07f --stat -- src/components/JobsPanel.tsx` range-coverage control observed
  non-empty
- `npx vitest run` exits 0 and `npx tsc --noEmit` exits 0 (regression check after the phase's code
  plans landed)
- Task 3's checkpoint returned an operator verdict, with the step-3 layout observation recorded
</verification>

<success_criteria>
- No planning artifact asserts a requirement status Phase 111 did not deliver
- Every item Phase 111 deliberately deferred has exactly one owner: SEED-007
- Both non-goals (D-11, D-12) are proven untouched by a probe whose control fired
- The operator has confirmed, against the running app, that neither changed surface claims a
  mission state the data cannot back
</success_criteria>

<output>
Create `.planning/phases/111-mission-board/111-03-SUMMARY.md` when done. It must contain the raw
output of every audit probe AND its control, the operator's verbatim checkpoint verdict, and the
trailing-cluster layout observation from checkpoint step 3 — carried forward as an open note rather
than silently fixed.
</output>
