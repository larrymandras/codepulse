---
phase: 111-mission-board
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/JobsPanel.tsx
  - src/components/JobsPanel.test.tsx
  - src/pages/LiveRun.tsx
autonomous: true
requirements: [MISSION-01, MISSION-03]

must_haves:
  truths:
    - "D-05: MISSION-03 governs this surface — no per-mission cost, confirm-card, or squad-grouping affordance is added, and no duration figure is rendered anywhere"
    - "D-08: JobsPanel's stateIcon map has no queued key and no running key, and no unknown key was added in their place"
    - "D-09: no duration is rendered; each row's trailing timestamp carries exactly one meaning — time since the mission finished"
    - "D-10: the panel header reads MISSION HISTORY, the pulsing dot is gone, and no replacement glyph took its place"
    - "D-13: the rendered label is MISSION HISTORY and does not collide with MissionTimelinePanel's MISSION TIMELINE label; Chat.tsx:1124 is not touched by this plan"
    - "Operator sees a 34-day-old mission render as 'finished 34d ago', never as 'finished 816h ago'"
    - "An unmapped status (including the 'unknown' that runtimeIngest.ts:615 can write) renders as the muted Clock icon and an UNKNOWN badge in the idle style — no colored or plausible-looking substitute"
    - "The seconds-epoch normalization guard survives the rewrite, so no row renders a 1970-derived age"
  artifacts:
    - path: "src/components/JobsPanel.tsx"
      provides: "Post-hoc mission history rendering with single-meaning finished-ago timestamps"
      contains: "MISSION HISTORY"
    - path: "src/components/JobsPanel.test.tsx"
      provides: "Regression coverage for header copy, the four age tiers, the epoch-seconds guard, and the unmapped-status fallback"
      min_lines: 80
  key_links:
    - from: "src/components/JobsPanel.tsx"
      to: "src/hooks/useSubagentJobs.ts"
      via: "useSubagentJobs() live query"
      pattern: "useSubagentJobs\\(\\)"
    - from: "src/components/JobsPanel.tsx"
      to: "src/components/StatusBadge.tsx"
      via: "StatusBadge status={job.status} — unmapped statuses fall through to the idle style"
      pattern: "StatusBadge status=\\{job\\.status\\}"
---

<objective>
Turn `JobsPanel` from a live-looking background-jobs board into a truthful post-hoc mission
history surface: prune the icon map of states the emitter never sends (D-08), replace the
dual-meaning elapsed formatter with an explicit single-meaning "finished X ago" including a day
tier (D-09), and strip the live chrome — pulsing dot and "BACKGROUND JOBS" label (D-10).

Purpose: `subagentJobs` holds 7 lifetime rows, `{failed:2, cancelled:3, completed:2}`, newest
2026-07-07. Zero `queued`, zero `running`, and `submittedAt === finishedAt` in 7 of 7. Every
affordance implying streaming, queueing, running, or duration is a claim this data cannot back.

Output: a rewritten `JobsPanel.tsx`, a new `JobsPanel.test.tsx` (none exists today), and a
corrected mount-site comment in `LiveRun.tsx`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/111-mission-board/111-CONTEXT.md
@.planning/phases/111-mission-board/111-UI-SPEC.md
@.planning/phases/111-mission-board/111-PATTERNS.md

@src/components/JobsPanel.tsx
@src/components/BlackboardPanel.test.tsx
@src/components/control-center/MissionTimelinePanel.tsx

<interfaces>
<!-- Contracts the executor needs. Extracted from the codebase — do not go looking for them. -->

From src/hooks/useSubagentJobs.ts (READ-ONLY this phase — do not modify):
  export type SubagentJobStatus = "queued" | "running" | "completed" | "failed" | "cancelled";
  export interface SubagentJobRow {
    jobId: string; agentTypeId: string; status: SubagentJobStatus; taskSnippet: string;
    resultSnippet?: string; error?: string; channelId?: string; chatId?: string;
    submittedAt: number; finishedAt?: number; updatedAt?: number;
  }
  export function useSubagentJobs(): SubagentJobRow[];   // returns [] while loading, never undefined

  NOTE — the TS union above is NARROWER than the runtime value set. `convex/subagentJobs.ts:28`
  declares `status: v.string()` and `convex/runtimeIngest.ts:615` writes `d.status ?? "unknown"`,
  so `"unknown"` (and in principle any emitter string) genuinely reaches this column. Do NOT widen
  the union in this phase — no decision authorizes it. Task 2 handles this with a single explicit
  cast in the test fixture.

From src/components/StatusBadge.tsx (READ-ONLY this phase — do not modify):
  export function StatusBadge({ status, label }: { status: string; label?: string }): JSX.Element;
  Resolution chain (StatusBadge.tsx:48-52): legacyMap[status] -> legacy?.semantic ?? status ->
  semanticStyles[resolvedSemantic] ?? semanticStyles.idle ("bg-muted text-muted-foreground"),
  label = legacy?.label ?? status.toUpperCase(). For "unknown" this already yields the muted idle
  style with the label UNKNOWN. That is the correct honest behavior — verify it, do not "improve" it.

From src/components/EntityRow.tsx (READ-ONLY — shape unchanged this phase):
  props: wrapPrimary?, onClick?, icon, primary, secondary, trailing
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite JobsPanel as a mission history surface (D-08, D-09, D-10)</name>

  <files>src/components/JobsPanel.tsx</files>

  <read_first>
    - `src/components/JobsPanel.tsx` (all 110 lines — this is the diff base)
    - `.planning/phases/111-mission-board/111-UI-SPEC.md` — the Copywriting Contract section is
      BINDING; do not paraphrase any string in it
    - `.planning/phases/111-mission-board/111-PATTERNS.md` — sections "1." through "4." under
      `src/components/JobsPanel.tsx` carry the current-code excerpts and the day-tier precedent
    - `src/components/control-center/MissionTimelinePanel.tsx` lines 55-84 — `normalizeMs` /
      `relativeTime`, the live precedent for the s/m/h/d tier ladder
  </read_first>

  <action>
Five edits to `src/components/JobsPanel.tsx`, all within the file. Do not touch any other file.

**(a) Prune the `stateIcon` map (D-08).** At lines 26-32, delete the `queued:` entry and the
`running:` entry outright. Keep `completed`, `failed`, `cancelled` exactly as-is apart from (b).
Do NOT add an `unknown` key or any other new key — UI-SPEC states this as a prohibition. The call
site at line 88, `stateIcon[job.status] ?? <Clock className="h-3.5 w-3.5 text-muted-foreground/50" />`,
must be left byte-identical: it is the fallback that renders an unmapped status honestly.
Because `running` was the only consumer of `Zap`, remove `Zap` from the lucide-react import block
at lines 12-19. `Clock` stays — it is still the line-88 fallback.
Replace the now-false section comment at line 25 ("covers all five subagentJobs statuses") with an
accurate one naming the three terminal statuses and citing `convex/runtimeIngest.ts:594-596` for
why non-terminal states are absent.

**(b) Hex to token (UI-SPEC Color section).** In the same map, `failed`'s
`text-[#ef4444]` becomes `text-(--status-error)`. Use the Tailwind 4 arbitrary-property form
`text-(--status-error)`, NOT `text-[var(--status-error)]` — confirmed house syntax at
`src/components/blocks/ErrorBlock.tsx:8` and `src/components/brains/SwapHistoryList.tsx:81`.
Do NOT touch the same hardcoded-hex pattern in `BlackboardPanel.tsx` or `SwarmTaskNode.tsx` —
those are out of scope for this phase.

**(c) Rewrite `formatElapsed` (D-09).** Same signature, same name, same call site. Exact behavior:
`const ref = job.finishedAt ?? job.submittedAt;` — falsy returns `""`.
Then **preserve line 40 verbatim**: `const refMs = ref < 1e12 ? ref * 1000 : ref;` — this is the
seconds-epoch guard (`subagentJobs` timestamps are ~1.78e9 Unix seconds). Removing or altering it
yields 1970 dates and makes every threshold comparison pass vacuously. Then
`const s = Math.floor((Date.now() - refMs) / 1000);`, and keep the existing `if (s < 0) return "";`
so a future-dated row renders nothing rather than a negative age. The four tiers, exact strings,
no others:
  s < 60 → `finished moments ago`
  m = Math.floor(s / 60), m < 60 → `finished {m}m ago`
  h = Math.floor(m / 60), h < 24 → `finished {h}h ago`
  otherwise → `finished {Math.floor(h / 24)}d ago`
The day tier does not exist today and MUST be added — without it the one real 34-day-old row
renders `finished 816h ago`. Put **no upper cap** on the day count: `finished 34d ago` is correct
behavior, not a state to round away. Also replace the stale comment at lines 34-36 ("no ms
conversion needed") — it contradicts the guard on the very next line; state instead that the
timestamps are seconds-epoch and the ternary tolerates an already-ms value.

**(d) Header copy and dot removal (D-10).** In the header block at lines 61-69: delete the line
`<span className="w-2 h-2 rounded-full bg-primary animate-pulse" />` entirely — no replacement
glyph, no static dot, no recolor. Change the `<h2>` text `BACKGROUND JOBS` to `MISSION HISTORY`.
Change the badge body `{jobCount} jobs` to `{jobCount} missions`. Leave every className on the
`<h2>`, the `<Badge variant="outline">`, and the wrapping `<div className="flex items-center
justify-between mb-3">` unchanged — UI-SPEC declares all spacing and typography on this surface
inherited and unmodified.

**(e) Empty state copy.** In the block at lines 71-78, keep the `ListTodo` icon and the exact
`flex flex-col items-center justify-center py-6 gap-2 text-center` wrapper unchanged. Change the
heading text to `No mission history` and the body text to
`Background missions run via delegate_task(background=True) will appear here once they finish.`

Finally update the file's header docstring (lines 1-10). It currently describes a "live background
subagent job list" that "flips to completed/failed/cancelled … the next time the live Convex query
updates". That is the same claim this plan removes from the UI, so it must not survive in the
comment (project rule: comments are claims). Rewrite it to describe a post-hoc history board over
terminal-state rows, citing Phase 111 / D-08 / D-09 / D-10 and `runtimeIngest.ts:594-596`.

**Prohibitions for this task:** do not modify `src/hooks/useSubagentJobs.ts`,
`src/components/StatusBadge.tsx`, `src/components/EntityRow.tsx`, or anything under `convex/`.
Do not add a route or a `src/lib/navRegistry.ts` entry. Do not add truncation, `whitespace-nowrap`,
or any width constraint to the trailing cluster — the longer strings are unmodeled and any overflow
is for the 111-03 checkpoint to observe and record, not for this task to pre-emptively style around.
Do not apply the `sketch-findings-codepulse` "Borealis Console" treatment — it is held for v15.0.
  </action>

  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "MISSION HISTORY" src/components/JobsPanel.tsx` returns 1
    - `grep -c "BACKGROUND JOBS" src/components/JobsPanel.tsx` returns 0
    - `grep -c "animate-pulse" src/components/JobsPanel.tsx` returns 0
    - `grep -c 'text-\[#' src/components/JobsPanel.tsx` returns 0
    - `grep -c "text-(--status-error)" src/components/JobsPanel.tsx` returns 1
    - `grep -cE "^\s+(queued|running):" src/components/JobsPanel.tsx` returns 0
    - `grep -c "unknown" src/components/JobsPanel.tsx` returns 0 (no `unknown` key was added)
    - `grep -c "Zap" src/components/JobsPanel.tsx` returns 0
    - `grep -c "ref < 1e12 ? ref \* 1000 : ref" src/components/JobsPanel.tsx` returns 1 (guard preserved)
    - `grep -c "finished moments ago" src/components/JobsPanel.tsx` returns 1
    - `grep -cE 'finished \$\{[a-zA-Z]+\}d ago' src/components/JobsPanel.tsx` returns 1 (day tier exists)
    - `grep -c "{jobCount} missions" src/components/JobsPanel.tsx` returns 1
    - `grep -c "No mission history" src/components/JobsPanel.tsx` returns 1
    - `grep -c 'stateIcon\[job.status\] ?? <Clock' src/components/JobsPanel.tsx` returns 1 (fallback untouched)
    - `git diff --name-only` lists only `src/components/JobsPanel.tsx`
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>

  <done>
    `JobsPanel.tsx` renders a MISSION HISTORY header with no pulsing dot, a `{n} missions` badge,
    three terminal-status icons with the failed tint on a CSS token, and a `formatElapsed` that
    returns exactly one of the four `finished …` strings with the epoch-seconds guard intact.
    No other file changed.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add JobsPanel.test.tsx covering the copy, the four tiers, the epoch guard, and the unmapped-status fallback</name>

  <files>src/components/JobsPanel.test.tsx</files>

  <read_first>
    - `src/components/JobsPanel.tsx` (as rewritten by Task 1)
    - `src/components/BlackboardPanel.test.tsx` (all 174 lines) — the nearest analog: same
      hook-mock + header-label + empty-state + epoch-normalization test shape. Lines 128-173 are
      the directly portable epoch-seconds pair (renders a correct `5m`; asserts NO `\d{4,}h`).
    - `.planning/phases/111-mission-board/111-UI-SPEC.md` — the exact strings to assert
    - `src/test/setup.ts` — confirms no extra global setup is needed for this surface
  </read_first>

  <behavior>
    - Header: renders the text `MISSION HISTORY`; renders no element carrying `animate-pulse`
    - Count badge: with 2 rows, renders `2 missions`
    - Empty state: with 0 rows, renders `No mission history` AND
      `Background missions run via delegate_task(background=True) will appear here once they finish.`
    - Day tier: a row whose `finishedAt` is a seconds-epoch value 34 days in the past renders
      `finished 34d ago`, and the container text matches no `/finished \d{3,}h ago/`
    - Minute tier + epoch guard: a row 300 seconds in the past renders `finished 5m ago`
      (this is the guard's control — without `ref < 1e12 ? ref * 1000 : ref` it would render a
      ~56-year age instead)
    - Sub-minute tier: a row 10 seconds in the past renders `finished moments ago`
    - Unmapped status: a row with `status: "unknown"` renders a `StatusBadge` reading `UNKNOWN`
      carrying the muted idle class `bg-muted`, and renders no `animate-pulse` element
    - Removed affordance: a row with `status: "running"` renders no `animate-pulse` element and
      no green/`--status-ok` icon — it falls through to the same muted `Clock` default as any
      other unmapped status
  </behavior>

  <action>
Create `src/components/JobsPanel.test.tsx`. Follow `BlackboardPanel.test.tsx`'s conventions:
`@testing-library/react` `render`/`screen`, `describe`/`it`/`expect` from `vitest`.

Mock the domain hook only — not `convex/react`. `JobsPanel.tsx` imports from the relative
specifier `"../hooks/useSubagentJobs"`, so mock that exact specifier:
`vi.mock("../hooks/useSubagentJobs", () => ({ useSubagentJobs: vi.fn(() => []) }))`, then drive
each test by setting the mock's return value. `JobsPanel` needs no provider wrapper once the hook
is mocked — it composes only `EntityRow`, `StatusBadge`, and `Badge`, all provider-free.

Build fixture rows as `SubagentJobRow`. Derive every timestamp relative to now, in SECONDS, so the
tests are time-independent: e.g. `Math.floor(Date.now() / 1000) - 34 * 86400` for the day-tier row,
`- 300` for the minute row, `- 10` for the sub-minute row. Never hardcode an absolute epoch.

For the `status: "unknown"` fixture only, use one explicit cast —
`as unknown as SubagentJobRow` — and attach an inline comment recording WHY the cast is honest:
`convex/subagentJobs.ts:28` declares `status: v.string()` and `convex/runtimeIngest.ts:615` writes
`d.status ?? "unknown"`, so the runtime value set is genuinely wider than the `SubagentJobStatus`
union. Do not widen the union in `src/hooks/useSubagentJobs.ts` to avoid the cast — that file is
read-only this phase.

Assert the absence of live chrome structurally, not by text: use the `container` returned by
`render` and assert `container.querySelectorAll(".animate-pulse").length` is `0`. Assert the
no-bogus-hours property with a negative regex over `container.textContent`, mirroring
`BlackboardPanel.test.tsx:128-173`: it must not match `/finished \d{3,}h ago/`.

For the `UNKNOWN` badge, assert both the label (`screen.getByText("UNKNOWN")`) and that its
element's `className` includes `bg-muted` — the label alone would pass even if someone later
mapped `unknown` to a colored semantic, which is exactly the fabrication this phase forbids.
  </action>

  <verify>
    <automated>npx vitest run src/components/JobsPanel.test.tsx</automated>
  </verify>

  <acceptance_criteria>
    - `npx vitest run src/components/JobsPanel.test.tsx` exits 0 with at least 6 passing tests
    - `grep -c "animate-pulse" src/components/JobsPanel.test.tsx` returns at least 1 (the absence
      assertion is present)
    - `grep -c "finished 34d ago" src/components/JobsPanel.test.tsx` returns at least 1
    - `grep -c "finished 5m ago" src/components/JobsPanel.test.tsx` returns at least 1
    - `grep -c "finished moments ago" src/components/JobsPanel.test.tsx` returns at least 1
    - `grep -c "bg-muted" src/components/JobsPanel.test.tsx` returns at least 1
    - Mutation control (run and record the result, then revert): temporarily change the day tier's
      divisor in `JobsPanel.tsx` from `24` to `240` and confirm
      `npx vitest run src/components/JobsPanel.test.tsx` FAILS. A green run after that mutation
      means the day-tier test asserts nothing — fix the test, do not proceed.
    - Mutation control 2 (run, record, revert): temporarily delete the
      `ref < 1e12 ? ref * 1000 : ref` guard (use `const refMs = ref;`) and confirm the suite FAILS.
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>

  <done>
    `src/components/JobsPanel.test.tsx` exists, passes, and both mutation controls were run and
    observed RED before being reverted — recorded in the plan SUMMARY with the actual failure
    output, not asserted from expectation.
  </done>
</task>

<task type="auto">
  <name>Task 3: Correct the mount-site comment in LiveRun.tsx (Stale Docs rule)</name>

  <files>src/pages/LiveRun.tsx</files>

  <read_first>
    - `src/pages/LiveRun.tsx` lines 245-256 — the `<JobsPanel />` mount at line 251 and the
      comment block at 247-249 immediately above it
    - `.planning/phases/111-mission-board/111-UI-SPEC.md` "Mount/route" section — the mount itself
      is locked and must NOT move
  </read_first>

  <action>
`src/pages/LiveRun.tsx:247-249` carries a comment reading "Background subagent jobs (Phase 168
SC-2/SC-3): live-query-driven, surfaces job completion without manual polling regardless of which
tab or WS run is active." That comment makes the same live-streaming claim this plan just removed
from the rendered UI, and the project's Stale Docs rule requires it be corrected in the same
change rather than left to contradict the code.

Replace only that comment's text with an accurate description: the panel is a post-hoc mission
history over terminal-state `subagentJobs` rows, per Phase 111 / D-10, with a pointer to
`.planning/phases/111-mission-board/111-CONTEXT.md` for the probe evidence. Retain the Phase 168
provenance reference so the history is not lost.

**Change nothing else in this file.** Do not move, rename, re-wrap, or restyle the
`<div className="px-4 py-3 border-b border-(--border) shrink-0">` wrapper or the `<JobsPanel />`
mount at line 251 — UI-SPEC locks this mount as the board's home and forbids a new route or a
`navRegistry` entry. The diff for this task must be comment lines only.
  </action>

  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "live-query-driven" src/pages/LiveRun.tsx` returns 0
    - `grep -c "<JobsPanel />" src/pages/LiveRun.tsx` returns 1 (mount preserved)
    - `git diff -U0 src/pages/LiveRun.tsx` shows changed lines that are comment lines only — no
      added or removed JSX, import, or statement line
    - `npx tsc --noEmit` exits 0
    - `npx vitest run src/components/JobsPanel.test.tsx` exits 0
  </acceptance_criteria>

  <done>
    The comment above the `JobsPanel` mount describes a history board; the mount itself is
    byte-identical to before.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| astridr emitter → `POST /runtime-ingest` | Untrusted-by-default input crosses here; gated by `validateIngestAuth` Bearer token (`convex/runtimeIngest.ts`, the `subagent_job` case at 593-625 inherits the gate) |
| `subagentJobs` row → `JobsPanel` DOM | `status`, `agentTypeId`, `taskSnippet`, `error` are all `v.string()` (`convex/subagentJobs.ts:24-36`) — emitter-controlled content rendered into the page |

This plan is a subtraction on a read-only surface. It adds no input handling, no network call, no
auth surface, no persistence, and no new user-supplied data path. The register below is
deliberately short; padding it would be the same fabrication this phase exists to remove.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-111-01 | Tampering (XSS) | `JobsPanel` rendering `job.taskSnippet` / `job.agentTypeId` / `job.error` as React text children | accept | React escapes text children by default; this surface uses no `dangerouslySetInnerHTML` and this plan adds none. Exposure is unchanged from today. Verified by `grep -c dangerouslySetInnerHTML src/components/JobsPanel.tsx` returning 0. |
| T-111-02 | Tampering | `stateIcon[job.status]` at `JobsPanel.tsx:88` — an emitter-supplied status of `toString` / `constructor` resolves to an inherited `Object.prototype` member rather than `undefined`, so the `??` fallback does not fire and React receives a non-element child | accept | Impact is a missing icon plus a React child warning — no script execution, no data disclosure, no persistence. The ingest path is Bearer-gated. UI-SPEC locks this exact fallback expression as preserve-as-is, so hardening it with `Object.hasOwn` is out of scope here; it is recorded in the D-02 emitter seed by plan 111-03, which reopens this file. |
| T-111-03 | Information disclosure | `job.error` rendered into the row `secondary` line | accept | Pre-existing behavior, unchanged by this plan; the error text originates from the operator's own agent runtime and the dashboard is not publicly exposed. No new disclosure is introduced. |
| T-111-SC | Tampering | npm/pip/cargo installs | accept — not applicable | This plan installs no packages and adds no dependency. The Package Legitimacy Gate is not triggered; `package.json` is not in `files_modified` and `git diff --name-only` must not list it. |
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0
- `npx vitest run src/components/JobsPanel.test.tsx` exits 0
- `npx vitest run` exits 0 (no other suite regressed by the copy changes)
- `git diff --name-only` lists exactly: `src/components/JobsPanel.tsx`, `src/pages/LiveRun.tsx`
  (plus the new untracked `src/components/JobsPanel.test.tsx`) — nothing under `convex/`,
  `src/hooks/`, or `src/components/StatusBadge.tsx`
- Both mutation controls from Task 2 were executed and observed RED, with the captured failure
  output pasted into the SUMMARY
</verification>

<success_criteria>
- `JobsPanel` renders `MISSION HISTORY`, `{n} missions`, and no `animate-pulse` element
- No row renders a bare duration; every timestamp reads `finished …` with a single meaning
- A 34-day-old row renders `finished 34d ago`, not `finished 816h ago`
- A status not in the three-key `stateIcon` map renders the muted `Clock` and a muted `UNKNOWN`
  badge — absent, not fabricated
- No file under `convex/`, `src/hooks/`, or `src/components/StatusBadge.tsx` was modified
</success_criteria>

<output>
Create `.planning/phases/111-mission-board/111-01-SUMMARY.md` when done. Record the two mutation-
control runs with their actual output, and note any visual overflow observed in the trailing
timestamp cluster (the `finished moments ago` string is materially longer than the `5s` it
replaces) so the 111-03 checkpoint can evaluate it rather than discovering it live.
</output>
