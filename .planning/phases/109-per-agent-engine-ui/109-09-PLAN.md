---
phase: 109-per-agent-engine-ui
plan: 09
type: execute
wave: 7
depends_on: ["109-01", "109-02", "109-03", "109-04", "109-05", "109-06", "109-07", "109-08"]
files_modified:
  - .planning/phases/109-per-agent-engine-ui/109-LIVE-EVIDENCE.md
autonomous: false
requirements: [ENGINE-03, ENGINE-04, TELE-02]

must_haves:
  truths:
    - "D-03 — default_profile_id arrives on the live swap.catalogue ack as a real profile id, read back from the running stack rather than inferred from code"
    - "D-05 — a live scoped swap.set puts the profile in swap.state's override map with the correct model, and a live scoped restore removes it, proven against a control profile that stays present"
    - "D-06 — with a profile pinned AND a different global override active simultaneously, every brain surface renders the PINNED model"
    - "D-11 — listGlobal returns a real unscoped swap row from the live self-hosted instance, proven against a known-present scoped control so an empty result cannot read as correct"
    - "ENGINE-03, ENGINE-04 and TELE-02 are marked satisfied only after this gate, never from a green unit suite alone"
  artifacts:
    - path: ".planning/phases/109-per-agent-engine-ui/109-LIVE-EVIDENCE.md"
      provides: "pasted raw evidence for every probe, plus the operator sign-off"
      contains: "Operator sign-off"
  key_links:
    - from: "the running astridr-agent container"
      to: "the rebuilt image containing this phase's D-03/D-05 changes"
      via: "in-container symbol probe, not an image timestamp"
      pattern: "get_all_profile_overrides"
    - from: "live Convex reads"
      to: "the self-hosted instance"
      via: "explicit --url targeting, never npx convex's cloud default"
      pattern: "127.0.0.1:3210"
---

<objective>
Prove, against the real running stack, the three claims a green unit suite structurally cannot
prove — and refuse to mark ENGINE-03 / ENGINE-04 / TELE-02 satisfied until it has.

Purpose: ENGINE-04's central claim is "server-confirmed rather than optimistic" — a claim about
cross-process, cross-language, real-time behavior. CodePulse-side unit mocks are already in the
shape the TypeScript expects, so they cannot discover a Python-side payload divergence;
astridr-side tests never pass their payload through Convex's real validator. Phase 108's live gate
caught three defects of exactly this class that unit tests, a three-agent adversarial gate, a code
review and the phase verifier had all passed over. Phase 109 introduces the same boundary risk in
three new places: D-03's new ack field, D-05's new override map, and D-11's query on a field whose
absent-vs-null semantics are already proven non-obvious in this stack.
Output: one evidence file with pasted raw output for every probe, and an operator sign-off.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/109-per-agent-engine-ui/109-VALIDATION.md
@.planning/phases/109-per-agent-engine-ui/109-CONTEXT.md
@.planning/phases/108-per-profile-engine-telemetry-astridr-backend/108-07-SUMMARY.md
@CLAUDE.md

**Precision note, so it is not repeated from CONTEXT.md's own framing:** Phase 108's LIVE GATE
(108-07) found THREE defects — the `session_id`-null drop, the `providerAffinity` array-vs-string
mismatch, and the stale `pinned` row surviving a restore. The milestone-wide total of five includes
two found earlier, in 108-03, by that plan's own unit/adversarial work. Do not cite "five" as the
live gate's own catch count.

**Absence proofs need controls.** Two of this gate's probes assert an ABSENCE (D-05's restore
leaving the profile absent from the override map; D-11's query matching rows whose `scope` field is
missing). An empty result is otherwise indistinguishable from a broken probe. Every absence
assertion below is paired with a control that MUST show the opposite in the same measurement.
</context>

<interfaces>
**Operational rules this gate must obey** (project `CLAUDE.md`):

- The production Convex backend is SELF-HOSTED at `C:\Users\mandr\convex-selfhost\`. `npx convex`
  targets the CLOUD deployment by default. Every read in this gate must explicitly target the local
  instance: `npx convex run <fn> --url http://127.0.0.1:3210 --admin-key "$(docker exec convex-backend ./generate_admin_key.sh)"`.
  A `[]` from a wrong-backend call is a false negative, not a finding.
- NEVER run `npx convex import --replace-all`, and never bulk-delete or bulk-patch a large table on
  the live instance. This gate performs reads plus a handful of real swaps — nothing bulk.
- The astridr rebuild command is
  `COMPOSE_PROFILES=prod,war-room docker compose up --build -d` — never `docker compose restart`,
  which re-runs the stale baked image. The `war-room` profile matters because those five containers
  build their OWN images from the same context; omitting the profile leaves them on the old image.
- Do NOT verify container freshness by comparing image SHAs across services — the war-room agents
  legitimately have different SHAs from `astridr-agent` even on a correct rebuild. Probe for a
  symbol only the new commit contains, inside the running container.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Establish and PROVE the gate's preconditions</name>
  <files>.planning/phases/109-per-agent-engine-ui/109-LIVE-EVIDENCE.md</files>
  <read_first>
    - `CLAUDE.md` sections "Self-Hosted Convex — Operational Rules" and
      "Ástríðr API Integration".
    - `.planning/phases/108-per-profile-engine-telemetry-astridr-backend/108-07-SUMMARY.md` — the
      precedent for how that gate proved freshness and pasted evidence as rows.
    - `.planning/phases/109-per-agent-engine-ui/109-01-SUMMARY.md` — the exact astridr symbol names
      landed (`get_all_profile_overrides`, `profile_overrides`, `default_profile_id`) so the
      freshness probe greps for something that provably only exists post-change.
  </read_first>
  <action>
    Create `.planning/phases/109-per-agent-engine-ui/109-LIVE-EVIDENCE.md` and record, with every
    command's RAW output pasted verbatim beneath it:

    1. Deploy the Convex backend changes to the SELF-HOSTED instance (not the cloud default) and
       paste the deploy output.
    2. Rebuild astridr on `feature/brain-swap`:
       `COMPOSE_PROFILES=prod,war-room docker compose up --build -d`. Paste the output.
    3. **Freshness proof, in-container, by symbol — not by timestamp and not by image SHA.** A
       container BUILT after the commits looks identical to a live-mounted one, so run something of
       the form
       `docker exec astridr-agent python -c "import astridr.providers.router as m, inspect; print('get_all_profile_overrides' in inspect.getsource(m))"`
       and an equivalent probe for `profile_overrides` in `astridr.engine.control_verbs.dispatch`.
       Both must print `True`. Paste both outputs.
    4. Reachability: `curl -s -o /dev/null -w "%{http_code}" http://127.0.0.1:8181/health` and
       `curl -s -o /dev/null -w "%{http_code}" http://127.0.0.1:3210/version` — paste both status
       codes. A non-200 stops the gate; per CLAUDE.md's escalation order try
       `docker restart <container>` FIRST and only then `up -d --force-recreate`.
    5. `docker ps` output showing `astridr-agent` and `convex-backend` healthy.
    6. Record the profile ids available (`known_profile_ids()` equivalent, or the ids visible in
       Settings) — you need at least TWO real profiles, because every absence probe below requires
       a control profile.

    If any precondition fails, STOP and record the failure — do not proceed to the probes with an
    unproven environment. An unproven environment turns every subsequent negative into an
    uninterpretable result.
  </action>
  <verify>
    <automated>bash -c 'curl -s -o /dev/null -w "astridr=%{http_code} " http://127.0.0.1:8181/health; curl -s -o /dev/null -w "convex=%{http_code}\n" http://127.0.0.1:3210/version'</automated>
  </verify>
  <acceptance_criteria>
    - `109-LIVE-EVIDENCE.md` exists and contains six labelled sections, each with pasted raw output.
    - Both in-container symbol probes printed `True` — the running container provably contains this
      phase's astridr changes.
    - Both curl probes returned 200.
    - At least two real profile ids are recorded by name, for use as subject and control.
    - No image-SHA comparison is used anywhere as a freshness argument.
  </acceptance_criteria>
  <done>The stack is provably running this phase's code on both sides, and the evidence file records how that was established.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Live backend probes — D-03's ack field and D-05's override map</name>
  <files>.planning/phases/109-per-agent-engine-ui/109-LIVE-EVIDENCE.md</files>
  <read_first>
    - `.planning/phases/109-per-agent-engine-ui/109-VALIDATION.md` — the "Manual-Only Verifications"
      table's D-03 and D-05 rows.
    - `.planning/phases/109-per-agent-engine-ui/109-01-SUMMARY.md` — the exact field names and shapes
      landed on the astridr side.
  </read_first>
  <what-built>
    Ástríðr now reports `default_profile_id` on the `swap.catalogue` ack and a per-profile
    `profile_overrides` map on `swap.state` / the `swap.get_state` ack. CodePulse confirms a
    per-profile swap by reading that map back, and confirms a scoped restore by the profile's
    ABSENCE from it.
  </what-built>
  <action>
    Present probes A through D below to the operator, run each command yourself where it is
    runnable from this session, and append every RAW response verbatim to `109-LIVE-EVIDENCE.md`
    under a labelled heading per probe.

    Structure each entry as: the exact command or dispatched payload, then the raw response, then
    the verdict on its own line. Never write a verdict without the output that produced it directly
    above it, and never paraphrase a payload — paste it.

    For Probe C specifically, capture SUBJECT-absent and CONTROL-present from ONE pasted
    `profile_overrides` payload, not from two separate runs — the control only proves the probe is
    working if it is read from the same measurement.

    If a probe fails, record it as FAILED with its raw output and STOP; do not continue to Task 3
    with an unresolved failure. Then wait for the operator's explicit approval before proceeding.
  </action>
  <how-to-verify>
    Run these against the RUNNING stack, in a browser devtools console on the CodePulse tab (which
    already holds a live socket) or a raw WS client. Paste every raw response into
    `109-LIVE-EVIDENCE.md`.

    **Probe A — D-03.** Dispatch `{type:"swap.catalogue", target:"brain"}` and read the ack.
    - EXPECT: `default_profile_id` is present and is a real profile id string.
    - FAIL if it is `undefined`, `null`, an empty string, or absent. Do NOT infer it from source —
      read the value off the wire.
    - Paste the ack's `default_profile_id` value and the first two `entries`.

    **Probe B — D-05, the set leg.** Pick two real profiles: SUBJECT and CONTROL. Pin BOTH via
    scoped swaps, to different models:
    `{type:"swap.set", target:"brain", value:"<modelA>", restore:false, profile_id:"<SUBJECT>"}`
    then the same for CONTROL with `<modelB>`.
    - Dispatch `{type:"swap.get_state"}` and paste the whole `profile_overrides` object.
    - EXPECT: both profiles present, each with its own correct model.

    **Probe C — D-05, the restore leg. This is the absence proof and it needs its control.**
    Restore ONLY the SUBJECT (`restore:true` with `profile_id:"<SUBJECT>"`), then dispatch
    `{type:"swap.get_state"}` again and paste the whole `profile_overrides` object.
    - EXPECT: SUBJECT is ABSENT from the map — not present with a `null`, not present with an empty
      string.
    - EXPECT, IN THE SAME OUTPUT: CONTROL is still present with `<modelB>`. This is what makes the
      absence meaningful; an empty map would mean the probe is broken, not that the restore worked.
    - FAIL if the map is empty, or if SUBJECT is present in any form.

    **Probe D — ENGINE-04's central claim, observed in the UI.** With a fresh swap on SUBJECT,
    watch the surface (badge / picker trigger / Settings row) through the swap:
    - EXPECT the "· switching to …" suffix, then the suffix clearing and the base label updating,
      then the success toast.
    - EXPECT that the label does NOT update before the confirmation — an optimistic flip is the
      exact failure this requirement exists to exclude.
    - As a negative control, dispatch a scoped swap for a profile id that does NOT exist. EXPECT an
      immediate error toast naming the server's own reason, with NO pending suffix left behind and
      NO success. Paste the error text.
  </how-to-verify>
  <verify>
    <human-check>Every probe's raw output is pasted into 109-LIVE-EVIDENCE.md, Probe C shows SUBJECT absent AND CONTROL present in the same payload, and Probe D's negative control produced an honest error rather than a success.</human-check>
  </verify>
  <acceptance_criteria>
    - Probe A's pasted ack contains a non-empty `default_profile_id`.
    - Probe B's pasted map contains BOTH profiles with the correct, different models.
    - Probe C's pasted map shows SUBJECT absent and CONTROL still present — both facts read from
      ONE pasted payload, not two separate runs.
    - Probe D records the observed suffix -> cleared -> toast sequence, and its negative control's
      error text.
    - No conclusion in the evidence file is stated without the output that produced it directly
      beneath it.
  </acceptance_criteria>
  <resume-signal>Type "approved" when every probe above is pasted and passing, or describe which probe failed and paste its output.</resume-signal>
  <done>D-03 and D-05 are proven on the wire, with the restore's absence backed by a same-payload control.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Live precedence and history probes, then sign off the phase's three requirements</name>
  <files>.planning/phases/109-per-agent-engine-ui/109-LIVE-EVIDENCE.md</files>
  <read_first>
    - `.planning/phases/109-per-agent-engine-ui/109-VALIDATION.md` — the D-06 and D-11 rows and the
      "Gate ownership" note.
    - `.planning/phases/109-per-agent-engine-ui/109-02-SUMMARY.md` — the `listGlobal` shape and the
      `undefined`-vs-`null` finding it encodes.
    - `CLAUDE.md` — the self-hosted Convex targeting rule, so the reads below do not silently hit
      the cloud deployment.
  </read_first>
  <what-built>
    `resolveActiveBrain` now ranks the live per-profile override above the global override; the
    combined swap history is hosted on Settings' per-profile rows and reads a new `listGlobal`
    query that matches rows whose `scope` field is absent.
  </what-built>
  <action>
    Present probes E through H below to the operator, run each command yourself where it is
    runnable from this session, and append every RAW output (or screenshot reference) verbatim to
    `109-LIVE-EVIDENCE.md` under a labelled heading per probe.

    Every probe here has a mandatory control. Record the subject reading and its control reading
    ADJACENT in the file, so a reader can see the two disagree — a subject-only record proves
    nothing about whether the mechanism works or the probe is broken.

    Then append a closing section with a per-requirement verdict for ENGINE-03, ENGINE-04 and
    TELE-02, each citing which probes support it, followed by a line of the form
    `Operator sign-off: <name>, <date>`. Do not write that line yourself — the operator does, or
    explicitly authorizes it. Record any failed probe as FAILED with its raw output so a
    gap-closure plan can be written; do not omit it and do not round it up to a pass.
  </action>
  <how-to-verify>
    **Probe E — D-06, the precedence inversion. This needs TWO overrides active at once, which no
    mocked test naturally constructs.**
    1. Pin SUBJECT to `<modelA>` via a scoped `swap.set`.
    2. Set a GLOBAL override to a DIFFERENT model `<modelC>` (unscoped `swap.set`).
    3. With both active, check all four surfaces for SUBJECT: the header badge, the picker's
       "This profile" scope (base label AND which row is highlighted as current), the pre-swap
       confirm modal's current-engine column, and the Settings row's engine label.
    - EXPECT: all four show `<modelA>` (the pin), NOT `<modelC>` (the global).
    - CONTROL, in the same state: check a DIFFERENT, UNPINNED profile's surfaces. EXPECT they show
      `<modelC>` (the global). Without this control, "everything shows modelA" could equally mean
      the global rung is broken.
    - Screenshot or paste the four readings for both profiles.

    **Probe F — the honest absent state.** Find or create a profile with no telemetry and no
    override. EXPECT every surface reads exactly "Not reported" — never "Auto", never
    "No brain reported", never another profile's model. Paste the four readings.

    **Probe G — D-11, the highest-risk item in the phase.** Dispatch a real UNSCOPED (global)
    swap so a row with an absent `scope` is written. Then read the LOCAL self-hosted instance
    explicitly:
    `npx convex run controlVerbSwaps:listGlobal --url http://127.0.0.1:3210 --admin-key "$(docker exec convex-backend ./generate_admin_key.sh)"`
    - EXPECT: at least one row, and it is the swap you just dispatched.
    - CONTROL, run in the same session:
      `npx convex run controlVerbSwaps:listByScope '{"profileId":"<SUBJECT>"}' --url http://127.0.0.1:3210 --admin-key "..."`
      EXPECT it returns the scoped rows from Probe B/C. This control is mandatory: a wrong
      `null`/`undefined` choice, a wrong backend, or a bad admin key all return `[]`, and without a
      known-present control an empty `listGlobal` reads identically to "no global swaps yet."
    - Paste both raw outputs.

    **Probe H — D-10/D-12 on screen.** Open Settings and expand SUBJECT's "Swap history".
    - EXPECT the combined list: SUBJECT's own scoped rows unmarked, and the global row from Probe G
      present and marked `GLOBAL`.
    - EXPECT the count badge shows the true count and, if truncated, the caption states it.
    - With SUBJECT pinned, EXPECT the pinned note. Clear the pin and EXPECT the note to DISAPPEAR
      while the history rows remain — that contrast is what proves the note is live-derived rather
      than reconstructed from the rows.
    - Paste or screenshot both states.
  </how-to-verify>
  <verify>
    <human-check>Probes E through H are pasted with their controls, every control showed the OPPOSITE of its subject where specified, and the operator has recorded a dated sign-off line in 109-LIVE-EVIDENCE.md.</human-check>
  </verify>
  <acceptance_criteria>
    - Probe E records four readings for the pinned SUBJECT (all `<modelA>`) AND four for the
      unpinned control (all `<modelC>`), captured in the same two-override state.
    - Probe F records exactly "Not reported" on every surface for a no-telemetry profile.
    - Probe G pastes both the `listGlobal` output (non-empty, containing the just-dispatched swap)
      and the `listByScope` control output, both explicitly targeted at `127.0.0.1:3210`.
    - Probe H records the combined list with the `GLOBAL` marker, the true count badge, and the
      pinned note present-then-absent contrast.
    - `109-LIVE-EVIDENCE.md` ends with a line of the form
      `Operator sign-off: <name>, <date>` plus a per-requirement verdict for ENGINE-03, ENGINE-04
      and TELE-02.
    - Any probe that failed is recorded as FAILED with its raw output — not omitted, not rounded up
      to a pass.
  </acceptance_criteria>
  <resume-signal>Type "approved" to mark ENGINE-03 / ENGINE-04 / TELE-02 satisfied, or describe which probe failed so a gap-closure plan can be written.</resume-signal>
  <done>All four decision probes plus the absent-state and history probes are proven live with controls, and the three requirements carry an operator sign-off rather than a test-suite inference.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| this gate's commands → the LIVE self-hosted Convex instance | a wrong command here can damage production data |
| this gate's commands → the running astridr process | real swaps mutate real routing state |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-109-22 | Denial of service / data loss | a bulk mutation against the self-hosted single-node Convex instance | mitigate | This gate performs READS plus a handful of individual real swaps. `npx convex import --replace-all` is forbidden outright (it deletes every row first — millions of tombstones took the dashboard down for days in 2026-07). No bulk delete, no bulk patch, no mass archival sweep. Stated as an explicit prohibition in the plan's `<interfaces>`. |
| T-109-23 | Information integrity | a probe silently reading the retired CLOUD deployment instead of the local instance and returning a false negative | mitigate | Every Convex read in Probe G carries an explicit `--url http://127.0.0.1:3210` and an admin key generated from the running container, and is paired with a known-present control so an empty result cannot be misread as correct. |
| T-109-24 | Information integrity | concluding "it works" from an absence that a broken probe would produce identically | mitigate | Every absence assertion (Probe C's restore, Probe G's `listGlobal`, Probe H's disappearing pinned note) is paired with a control that MUST show the opposite in the same measurement. |
| T-109-25 | Repudiation | marking requirements satisfied from a green unit suite | mitigate | The plan is `autonomous: false` with two blocking human-verify checkpoints, and the acceptance criteria require a dated operator sign-off with a per-requirement verdict. 103-VALIDATION.md:143 records `VITE_BRAINS_STUB` masking exactly this failure class from the whole suite once already. |
| T-109-SC | Tampering | npm/pip/cargo installs | mitigate | This plan installs no packages. If one becomes necessary, stop and route through the Package Legitimacy Gate first. |
</threat_model>

<verification>
- `109-LIVE-EVIDENCE.md` exists with raw pasted output for every probe A through H.
- Both blocking checkpoints received an explicit operator "approved".
- The full Vitest suite is green and `npx tsc --noEmit` exits 0 at the time of the gate.
- `docker ps` shows `astridr-agent` and `convex-backend` healthy at gate start and gate end.
</verification>

<success_criteria>
- D-03, D-05, D-06 and D-11 are each proven against the running stack, each absence proof carrying
  a same-measurement control.
- ENGINE-03, ENGINE-04 and TELE-02 carry a dated operator sign-off, not a test-suite inference.
- Any failure is recorded as a failure with its raw output, feeding a gap-closure plan rather than
  being rounded up.
</success_criteria>

<output>
Create `.planning/phases/109-per-agent-engine-ui/109-09-SUMMARY.md` when done, and leave
`109-LIVE-EVIDENCE.md` in the phase directory as the durable record.
</output>
