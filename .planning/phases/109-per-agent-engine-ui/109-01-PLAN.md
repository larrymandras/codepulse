---
phase: 109-per-agent-engine-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - C:/Users/mandr/astridr-repo/astridr/providers/router.py
  - C:/Users/mandr/astridr-repo/astridr/engine/control_verbs/dispatch.py
  - C:/Users/mandr/astridr-repo/astridr/api/ws_commands.py
  - C:/Users/mandr/astridr-repo/tests/unit/providers/test_router.py
  - C:/Users/mandr/astridr-repo/tests/unit/engine/test_swap_model.py
  - C:/Users/mandr/astridr-repo/tests/unit/api/test_ws_commands_frame.py
autonomous: true
requirements: [ENGINE-03, ENGINE-04]

must_haves:
  truths:
    - "D-05 — `swap.state` (push) and the `swap.get_state` ack both carry a per-profile override map keyed by profile id"
    - "D-05 — a profile that holds no override is ABSENT from that map, never present with a null value"
    - "D-03 — the `swap.catalogue` ack carries `default_profile_id`, sourced from astridr's own resolved config, not from Convex ordering"
    - "A restore removes the profile's entry from the override map, so CodePulse can confirm a scoped restore against absence"
  artifacts:
    - path: "C:/Users/mandr/astridr-repo/astridr/providers/router.py"
      provides: "get_all_profile_overrides / get_all_profile_override_sources enumerators"
      contains: "def get_all_profile_overrides"
    - path: "C:/Users/mandr/astridr-repo/astridr/engine/control_verbs/dispatch.py"
      provides: "profile_overrides key on build_swap_state_payload"
      contains: "profile_overrides"
    - path: "C:/Users/mandr/astridr-repo/astridr/api/ws_commands.py"
      provides: "default_profile_id on the swap.catalogue ack"
      contains: "default_profile_id"
  key_links:
    - from: "astridr/engine/control_verbs/dispatch.py::build_swap_state_payload"
      to: "astridr/providers/router.py::get_all_profile_overrides"
      via: "router enumerator call"
      pattern: "get_all_profile_overrides"
    - from: "astridr/api/ws_commands.py::_handle_swap_set"
      to: "swap.state push"
      via: "post-dispatch broadcast already present at ws_commands.py:1156-1159"
      pattern: "build_swap_state_payload"
---

<objective>
Land the two — and only two — Ástríðr changes this phase is allowed: D-05's per-profile
override map on `swap.state`, and D-03's `default_profile_id` on the `swap.catalogue` ack.
Both on branch `feature/brain-swap`.

Purpose: CodePulse cannot confirm a per-profile swap or address the default profile without
these. Every downstream CodePulse plan reads one of these two fields.
Output: a new `ModelRouter` enumerator pair, an extended `build_swap_state_payload`, one new
ack field, and pytest coverage for all three.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/109-per-agent-engine-ui/109-CONTEXT.md
@.planning/phases/109-per-agent-engine-ui/109-RESEARCH.md

**Repo for this plan:** `C:/Users/mandr/astridr-repo`, branch `feature/brain-swap`. Verify
`git branch --show-current` reads `feature/brain-swap` before editing anything. Do NOT merge,
rebase, or switch branches.

**Branch divergence (REQUIREMENTS.md carried-forward item 7):** research measured this branch on
2026-08-08 as 432 commits ahead of the shared base and **10 commits behind `main`** — the
"322 behind" figure in REQUIREMENTS.md is stale and must not be relayed. The one concrete
unmerged fix that matters is `main`'s `5e4e257d fix(cors): drop the decommissioned-host default
for CODEPULSE_ORIGIN`; on this branch `astridr/channels/web.py:973` still defaults
`CODEPULSE_ORIGIN` to the retired cloud host `https://tidy-whale-981.convex.site`. **Do not fix
it — it is outside this phase's two-change scope.** Report it in this plan's SUMMARY as an
operator flag.

**Treat in-repo comments and docstrings as claims, not evidence** — several in this area cite a
nonexistent "Ástríðr Phase 184.1". Verify against code before relying on any comment.
</context>

<interfaces>
Verified live 2026-08-08 (`109-RESEARCH.md` §A.2). Current accessor signatures in
`astridr/providers/router.py`:

- `def get_global_override(self) -> str | None:`  (~:774)
- `def get_global_override_source(self) -> str | None:`  (~:778)
- `def get_profile_override(self, profile_id: str) -> str | None:`  (~:805)
- `def get_profile_override_source(self, profile_id: str) -> str | None:`  (~:843)

Backing state: `self._profile_model_overrides` and `self._profile_override_sources` — today only
reachable through per-id `.get()`/`.pop()`. **There is no public enumerator.** That is the gap.

Current `build_swap_state_payload` return keys (`astridr/engine/control_verbs/dispatch.py:77-101`):
`model_override`, `model_source`, `voice_override_id`, `voice_override_name`. Its docstring at
`:78-79` states the payload is in-memory only, never `config/*.yaml` — this is what keeps D-05
inside the Phase-103 D-14 boundary.

`_handle_swap_catalogue` (`astridr/api/ws_commands.py:1167-1240`) currently returns
`{"target": ..., "entries": [...]}`. `_handle_readiness_get` (`:1079-1087`) returns only
`{"ready": ...}` and its only caller stops polling permanently once ready — which is why D-03
lands on `swap.catalogue`, not `readiness.get`.

The authoritative `default_profile_id` computation, per `astridr/engine/bootstrap/core.py:1298`:
`config.profiles[0].id if config.profiles else "personal"`.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add ModelRouter override enumerators and put the per-profile map on swap.state</name>
  <files>
    C:/Users/mandr/astridr-repo/astridr/providers/router.py,
    C:/Users/mandr/astridr-repo/astridr/engine/control_verbs/dispatch.py,
    C:/Users/mandr/astridr-repo/tests/unit/providers/test_router.py
  </files>
  <read_first>
    - `astridr/providers/router.py` — read the region defining `_profile_model_overrides` /
      `_profile_override_sources`, `set_profile_override`, `clear_profile_override` (or whatever
      the restore path actually calls), `get_profile_override` (~:805),
      `get_profile_override_source` (~:843), `get_global_override` (~:774), and `_resolve_model`'s
      precedence chain (~:592-602). You need the exact attribute names — do not assume them from
      this plan's prose.
    - `astridr/engine/control_verbs/dispatch.py` — the whole `build_swap_state_payload` function
      including its `:78-79` in-memory-only docstring.
    - `tests/unit/providers/test_router.py` — the existing override tests, for fixture/style
      conventions.
  </read_first>
  <behavior>
    - `get_all_profile_overrides()` on a router with no overrides returns an empty dict.
    - After `set_profile_override("business", "claude-opus-4-8", source=...)`, the dict contains
      exactly `{"business": "claude-opus-4-8"}` and the returned object is a COPY — mutating it
      does not mutate router state.
    - After clearing/restoring `"business"`, `"business"` is ABSENT from the dict (not present
      with a `None` value).
    - `build_swap_state_payload(router)` returns a `profile_overrides` mapping whose entries carry
      both the model and its source; with two profiles overridden, both appear.
    - `build_swap_state_payload(None)` returns `profile_overrides` as an empty mapping and does
      not raise.
    - The four pre-existing keys (`model_override`, `model_source`, `voice_override_id`,
      `voice_override_name`) are unchanged in name and value.
  </behavior>
  <action>
    Add two public enumerator methods to `ModelRouter`, beside the existing per-id accessors:
    `get_all_profile_overrides(self) -> dict[str, str]` and
    `get_all_profile_override_sources(self) -> dict[str, str | None]`. Each returns a shallow COPY
    of the corresponding backing dict (`dict(self._profile_model_overrides)` /
    `dict(self._profile_override_sources)` — use the real attribute names read from the file).
    Docstring each as "snapshot of every profile currently holding a model override" and state
    that a profile with no override is absent, never present with `None` — CodePulse confirms a
    scoped restore against that absence.

    Extend `build_swap_state_payload` with one new key, `profile_overrides`, whose value is a
    mapping of `profile_id -> {"model": <str>, "source": <str | None>}`, built from
    `router.get_all_profile_overrides()` with the source looked up per id. When `router` is
    `None`, emit `{}` for that key — never omit the key and never emit `None`, so the client can
    distinguish "no overrides" from "field missing" without a shape guess. Do not change any
    existing key. Extend the function's docstring to state that `profile_overrides` is the same
    in-memory-only class of value as `model_override` and is never read from `config/*.yaml`
    (this is the sentence D-05's D-14-safety argument rests on).

    Do NOT change `_resolve_model`, the setters, the restore path, or the emit asymmetry between
    set and restore (`_emit_restore_routing`) — all out of scope.
  </action>
  <verify>
    <automated>cd C:/Users/mandr/astridr-repo &amp;&amp; python -m pytest tests/unit/providers/test_router.py tests/unit/engine/test_swap_model.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "def get_all_profile_overrides" astridr/providers/router.py` returns exactly one hit.
    - `grep -n "def get_all_profile_override_sources" astridr/providers/router.py` returns exactly one hit.
    - `grep -n "profile_overrides" astridr/engine/control_verbs/dispatch.py` returns at least one
      hit inside `build_swap_state_payload`'s return dict.
    - A new pytest asserts the ABSENCE case (profile cleared → key not in dict) AND pairs it with
      a control in the same test that MUST be present (a second, still-overridden profile appears)
      — an empty dict must not be able to pass as correct.
    - A new pytest asserts `build_swap_state_payload(None)` returns `profile_overrides == {}` and
      does not raise.
    - A new pytest asserts the returned dict is a copy: mutating it leaves
      `get_profile_override(...)` unchanged.
    - The pytest command above exits 0.
  </acceptance_criteria>
  <done>`swap.state` and the `swap.get_state` ack both carry `profile_overrides`; an unoverridden profile is absent from it; existing keys byte-unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Report default_profile_id on the swap.catalogue ack</name>
  <files>
    C:/Users/mandr/astridr-repo/astridr/api/ws_commands.py,
    C:/Users/mandr/astridr-repo/tests/unit/api/test_ws_commands_frame.py
  </files>
  <read_first>
    - `astridr/api/ws_commands.py` — `_handle_swap_catalogue` in full (~:1167-1240), AND the
      `__init__`/constructor of the class that owns it, to determine what is already injected.
      **Research explicitly did not check whether `config` is reachable from this handler
      (`109-RESEARCH.md` Open Question #4) — read the dependency injection, do not assume
      `self._config` exists.** If it does not, thread the value in at construction the same way
      the class's other injected dependencies are, mirroring `_handle_swap_set`'s own dependency
      list — do not import config at module scope inside the handler.
    - `astridr/engine/bootstrap/core.py` around `:1298` — the authoritative computation
      `config.profiles[0].id if config.profiles else "personal"`.
    - `astridr/engine/bootstrap/wiring.py:261, :344-350` — note the signature's hardcoded
      `"personal"` default is a fallback literal, not the value's source; the call site above is.
    - `tests/unit/api/test_ws_commands_frame.py` — existing ack-shape test conventions.
  </read_first>
  <behavior>
    - A `swap.catalogue` command with `target: "brain"` returns an ack containing a
      `default_profile_id` key whose value is a non-empty string.
    - With a config carrying profiles `["personal", "business"]`, the value is `"personal"`.
    - With an empty profile list, the value is `"personal"` (the documented fallback) — never
      `None`, never an empty string, never omitted.
    - The existing `target` and `entries` keys are unchanged.
  </behavior>
  <action>
    Add `"default_profile_id"` to `_handle_swap_catalogue`'s returned ack dict, computed as
    `config.profiles[0].id if config.profiles else "personal"` using whatever config handle the
    class actually has (established in `<read_first>`). Do NOT add the field to
    `readiness.get` — D-03's choice of ack is `swap.catalogue`, because `readiness.get`'s only
    caller (`ControlCenterPanel.tsx`'s warm-up poll, `:108-134`) stops polling permanently once
    `ready === true`, so in steady state nothing would ever fetch it, while both CodePulse
    consumers of `default_profile_id` already fetch the catalogue.

    Add a short comment above the new key naming 103-CONTRACT.md §3 and stating this is Ástríðr's
    own resolved chat-channel default profile — what an unattributed `chat.send` actually routes
    to — so a future reader does not "simplify" it back to a Convex-ordering derivation, which is
    the option D-03 explicitly rejected.
  </action>
  <verify>
    <automated>cd C:/Users/mandr/astridr-repo &amp;&amp; python -m pytest tests/unit/api/test_ws_commands_frame.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "default_profile_id" astridr/api/ws_commands.py` returns at least one hit inside
      `_handle_swap_catalogue`'s return construction.
    - `grep -rn "default_profile_id" astridr/api/ws_commands.py | grep -i readiness` returns zero
      hits (the field did NOT land on `readiness.get`).
    - A new pytest asserts the ack contains `default_profile_id` equal to the first configured
      profile's id, with a second case asserting the empty-profiles fallback returns `"personal"`.
    - Both assertions fail if the key is removed (run the test once against a stubbed-out key to
      confirm, or assert the exact expected value rather than mere presence).
    - The pytest command above exits 0.
  </acceptance_criteria>
  <done>`swap.catalogue`'s ack carries a real `default_profile_id`; `readiness.get` is untouched.</done>
</task>

<task type="auto">
  <name>Task 3: Full astridr suite regression + scope/divergence reporting</name>
  <files>
    C:/Users/mandr/astridr-repo/tests/unit/engine/test_swap_model.py
  </files>
  <read_first>
    - `tests/unit/engine/test_swap_model.py` — the existing `build_swap_state_payload` assertions,
      to confirm none of them assert an exact-dict equality that the new key would break.
    - `.planning/REQUIREMENTS.md` (codepulse) carried-forward item 7 — the branch-divergence note
      whose stale figure this plan corrects.
  </read_first>
  <action>
    Run the two touched suites plus the broader swap/router surface and fix any assertion that
    broke purely because `build_swap_state_payload` gained a key (e.g. an `assert payload == {...}`
    exact-equality check should become a per-key assertion, or add the new key — do NOT delete the
    assertion).

    Then record, for this plan's SUMMARY (do not fix either):
    1. The measured branch divergence at execution time — run
       `git log --oneline main..feature/brain-swap | wc -l` and
       `git log --oneline feature/brain-swap..main | wc -l` in `C:/Users/mandr/astridr-repo` and
       paste both raw outputs. REQUIREMENTS.md's "322 behind" figure is stale; report the measured
       numbers, not that figure.
    2. The stale CORS default: paste the live output of
       `grep -n "CODEPULSE_ORIGIN" astridr/channels/web.py` — on `feature/brain-swap` line ~973
       still defaults to the retired cloud host `https://tidy-whale-981.convex.site`. Flag it to
       the operator as an unmerged `main` fix outside this phase's two-change scope. Do not change it.

    Commit both code changes on `feature/brain-swap` as a fresh commit. **Do not use
    `git commit --amend`** and do not use `--no-verify`; before committing, assert
    `git branch --show-current` is `feature/brain-swap` and re-check `git diff --cached --name-only`
    contains only this plan's files, then verify with `git show --stat HEAD` afterwards.
  </action>
  <verify>
    <automated>cd C:/Users/mandr/astridr-repo &amp;&amp; python -m pytest tests/unit/providers tests/unit/engine tests/unit/api -q</automated>
  </verify>
  <acceptance_criteria>
    - The pytest command above exits 0 with zero failures and zero errors.
    - `git log -1 --format=%s` in astridr-repo names phase 109 and the two changes.
    - `git show --stat HEAD` lists only the files in this plan's `files_modified` — no sweep-in
      from a concurrent session.
    - The SUMMARY contains the raw `git log --oneline` counts (both directions) and the raw
      `grep -n "CODEPULSE_ORIGIN" astridr/channels/web.py` output, each pasted verbatim.
  </acceptance_criteria>
  <done>Astridr suites green; both cross-repo flags recorded with pasted evidence, neither silently fixed nor silently dropped.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → astridr WS (`/ws/telemetry`) | untrusted client input crosses here into `_handle_swap_set` / `_handle_swap_catalogue` |
| astridr in-memory router state → broadcast `swap.state` | server state leaves the process to every connected client |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-109-01 | Information disclosure | `build_swap_state_payload`'s new `profile_overrides` broadcast to all connected clients | accept | The map contains only profile ids and model ids — both already broadcast today via `model_override`/`model_routing` telemetry. No secret, credential, or user content is added. Explicitly do NOT add `source` values that could carry a session id or user identifier; `source` is the existing enum-shaped provenance string only. |
| T-109-02 | Elevation of privilege | scoped `swap.set` `profile_id` validation | accept (unchanged) | V4/V5 access control stays exactly where it is — `_handle_swap_set`'s fail-closed `profile_id` check against `known_profile_ids()` (`ws_commands.py:1129-1138`), including its reject-rather-than-degrade branch and its `target='voice'` rejection. This plan adds no new command, no new argument, and no new write path. |
| T-109-03 | Tampering | `default_profile_id` sourced from config | mitigate | Value is read from astridr's own resolved config on the server, never accepted from the client and never derived from client-supplied ordering. The rejected alternative (deriving it from Convex `profileConfigs` ordering) is what would have let a badge dispatch a swap at a profile the operator never named. |
| T-109-SC | Tampering | package installs | mitigate | This plan installs no packages in either repo — no npm/pip/cargo install task exists. If one becomes necessary, stop and route through the Package Legitimacy Gate first. |
</threat_model>

<verification>
- `python -m pytest tests/unit/providers tests/unit/engine tests/unit/api -q` exits 0 in astridr-repo.
- `git branch --show-current` in astridr-repo still reads `feature/brain-swap`.
- `git status --porcelain` in astridr-repo is clean after commit.
- No file outside `files_modified` was changed (`git show --stat HEAD`).
</verification>

<success_criteria>
- `profile_overrides` present on `build_swap_state_payload`'s output, absent-not-null for
  unoverridden profiles, empty-dict when router is None.
- `default_profile_id` present on the `swap.catalogue` ack with the config-sourced value.
- Zero other astridr behavior changed.
- Branch divergence and the `web.py` CORS default reported with pasted raw evidence.
</success_criteria>

<output>
Create `.planning/phases/109-per-agent-engine-ui/109-01-SUMMARY.md` when done (in the **codepulse**
repo — SUMMARYs live with the phase, even for cross-repo work).
</output>
