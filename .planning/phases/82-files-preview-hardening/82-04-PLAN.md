---
phase: 82-files-preview-hardening
plan: 04
type: execute
wave: 3
depends_on: ["82-01", "82-03"]
files_modified:
  - "FORGE-REPO: src/emit/codepulse-emitter.ts"
  - "FORGE-REPO: src/emit/config.ts"
  - "FORGE-REPO: src/workspace/enumerate.ts"
  - "FORGE-REPO: src/http/routes/jobs.ts"
  - "FORGE-REPO: src/emit/codepulse-emitter.test.ts"
autonomous: false
requirements: [FI-13]
must_haves:
  truths:
    - "On a job reaching a terminal state with FORGE_FILE_INGEST_URL set, the daemon enumerates the workspace, classifies + caps files, and POSTs the listing + ≤1 MB previewable bytes to /forge-file-ingest"
    - "With FORGE_FILE_INGEST_URL unset, the daemon is a no-op (no crash, no calls) — independent of log/job emission"
    - "Path-traversal and symlink-escape entries are rejected by the enumeration guard before any bytes are read"
    - "A completed job in the forge repo surfaces its files + capped artifacts in CodePulse's /forge Files tab (live round-trip)"
  artifacts:
    - path: "FORGE-REPO: src/workspace/enumerate.ts"
      provides: "Recursive walk + ignore rules + file-count cap + guardPath/realpath symlink guard + kindTag classification + ≤1 MB byte read"
      contains: "guardPath"
    - path: "FORGE-REPO: src/emit/codepulse-emitter.ts"
      provides: "emitFiles + buildFilesPayload (gated on fileIngestUrl, fire-and-forget, base64 image transport)"
      contains: "emitFiles"
    - path: "FORGE-REPO: src/emit/config.ts"
      provides: "resolveEmitCfg reads FORGE_FILE_INGEST_URL into fileIngestUrl"
      contains: "FORGE_FILE_INGEST_URL"
  key_links:
    - from: "FORGE-REPO: src/http/routes/jobs.ts terminal-state block"
      to: "emitFiles"
      via: "void emitFiles(emitCfg, job, files, artifacts) after status write + workspace promotion"
      pattern: "emitFiles"
    - from: "FORGE-REPO: src/workspace/enumerate.ts"
      to: "src/workspace/manager.ts + src/workspace/kinds.ts"
      via: "guardPath + PathTraversalError + kindTag reused verbatim"
      pattern: "guardPath|kindTag"
---

<objective>
Cross-repo (forge daemon, `C:\Users\mandr\forge` — NOT CodePulse): on a job reaching a terminal state, enumerate the workspace recursively, classify + cap files, apply path-traversal/symlink-escape guards, and POST the listing + ≤1 MB previewable bytes to CodePulse's `/forge-file-ingest` endpoint. Gated on a new `FORGE_FILE_INGEST_URL` env var (no-op when unset, best-effort like the existing emitter). Reuses `FORGE_INGEST_API_KEY` (D-3). This mirrors how Phase 81 split its cross-repo `makeLogSink` work into its own plan.

Purpose: Closes FI-13 / SPEC Req 12 — the producer side of the file/artifact bridge. The CodePulse receiver (82-01) and UI (82-03) are independently testable; this plan lights up the live round-trip.
Output: `src/workspace/enumerate.ts`, `emitFiles`/`buildFilesPayload` in `codepulse-emitter.ts`, the `fileIngestUrl` gate in `config.ts`, the terminal-state trigger in `jobs.ts`, and the path-traversal rejection test.

**EXECUTED IN THE `forge` REPO at C:\Users\mandr\forge — set CWD there before any task. This plan touches NO CodePulse files.**
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/82-files-preview-hardening/82-SPEC.md
@.planning/phases/82-files-preview-hardening/82-RESEARCH.md
@.planning/phases/82-files-preview-hardening/82-PATTERNS.md

<interfaces>
<!-- forge-repo analogs (verified live). All paths below are under C:\Users\mandr\forge. -->

src/emit/codepulse-emitter.ts (verified live):
- L30: export interface EmitCfg { ingestUrl: string | undefined; apiKey; hostId; fetchImpl? }
- L113: export const MAX_ATTEMPTS = 3;  L123: const loggedAuthStatuses = new Set<number>();
- L138-196: export async function emitJob(cfg, job) — gate `if (!ingestUrl || !apiKey) return;`, url = `${ingestUrl}/forge-ingest`, retry loop with exp backoff, 401/403 logged once via loggedAuthStatuses, never throws, never logs apiKey
- L208-221: ForgeIngestWorkspace + ForgeIngestWorkspacesPayload (the payload-interface convention to mirror)

src/emit/log-forwarder.ts (verified live — separate-gate convention):
- L216: ingestUrl: process.env['FORGE_LOG_INGEST_URL']  (separate var, NOT CONVEX_FORGE_INGEST_URL)
- L227: const LOG_INGEST_PATH = '/forge-log-ingest'
- L241-275: makeLogSink — fetch(`${cfg.ingestUrl}${LOG_INGEST_PATH}`, ...)
- For FORGE_FILE_INGEST_URL store the FULL endpoint URL incl. /forge-file-ingest (Pitfall 5) — do NOT double-append a path.

src/emit/config.ts L25: export function resolveEmitCfg(): EmitCfg — reads env vars at startup; add fileIngestUrl from process.env['FORGE_FILE_INGEST_URL'].

src/workspace/manager.ts (verified live — reuse verbatim, do NOT reimplement):
- L20: export class PathTraversalError extends Error
- L34: export function guardPath(root: string, ...parts: string[]): string  (lexical traversal guard, Windows-correct)

src/workspace/kinds.ts (verified live):
- L28: export type FileKind = 'text'|'image'|'video'|'audio'|'pdf'|'binary'|'directory'
- L103: export function kindTag(filename: string): FileKind
- contentTypeFor(filename) also exported (for image Blob content-type if needed)

src/http/routes/artifact.ts L116-148 (verified live — symlink-escape guard to reuse):
  absPath = guardPath(workspace.rootPath, ...parts); catch PathTraversalError → reject;
  realPath = fs.realpathSync.native(absPath); realRoot = fs.realpathSync.native(workspace.rootPath);
  containment check: realPath startsWith realRoot + path.sep (case-insensitive on Windows)

src/http/routes/jobs.ts L264-275 (verified live — terminal-state emit point):
  status = classifyExit(...); updateJob(db, job.id, {status, exitCode, finishedAt});
  const ceTerminal = getJob(db, job.id); if (emitCfg && ceTerminal) void emitJob(emitCfg, ceTerminal);
  // THEN workspace promotion (staged temp dir → workspace.rootPath) on clean completion.
  IMPORTANT: output files land in workspace.rootPath only AFTER promotion completes. Place the
  void emitFiles(...) call AFTER the promotion step (so enumeration sees the promoted files), not at the
  same point as emitJob. Also check src/process/manager.ts:266 — if that terminal path produces a
  promoted workspace, wire emitFiles there too; verify which terminal path carries the promoted workspace.

CodePulse receiver contract (82-01, the POST target):
  POST {fileIngestUrl}  Authorization: Bearer ${FORGE_INGEST_API_KEY}
  body { type: "files", hostId, forgeJobId, files: [{path, kind, sizeBytes}], artifacts: [{path, kind, sizeBytes, textContent?, imageBase64?}] }
  → 200 {ok:true} | 401 bad bearer | 400 bad body
</interfaces>

Caps (Claude's-Discretion, locked at plan time):
- PER_FILE_BYTE_CAP = 1_000_000 (1 MB per text/image artifact — daemon enforces BEFORE POST to avoid oversized Convex doc)
- ARTIFACT_BYTE_CAP_PER_JOB = 10_000_000 (10 MB total artifact bytes/job)
- FILE_COUNT_CAP = 500 (per-job file-count cap)
- IGNORE_DIRS = ['.git', 'node_modules', 'dist', 'build', '.next', 'out', 'coverage', '__pycache__', '.venv']

Emitter discipline (mirror emitJob verbatim): no-op when fileIngestUrl/apiKey unset; MAX_ATTEMPTS retry with exp backoff; 401/403 logged once via loggedAuthStatuses; 4xx drop; 5xx retry then drop; NEVER throws on a job-critical path; NEVER logs apiKey.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Workspace enumeration + guards + kind classification (with path-traversal test)</name>
  <files>FORGE-REPO: src/workspace/enumerate.ts, FORGE-REPO: src/emit/codepulse-emitter.test.ts</files>
  <read_first>
    - src/workspace/manager.ts L20-60 (PathTraversalError + guardPath — reuse verbatim)
    - src/workspace/kinds.ts L25-130 (FileKind, kindTag, contentTypeFor)
    - src/http/routes/artifact.ts L110-148 (guardPath + realpathSync.native containment symlink guard — the exact pattern to reuse on enumeration)
    - .planning/phases/82-files-preview-hardening/82-RESEARCH.md §"Recursive walk + ignore rules" (L468-477), §"Path-traversal guard on enumeration" (L479-502)
    - .planning/phases/82-files-preview-hardening/82-PATTERNS.md §"codepulse-emitter.ts" path-traversal guard block (L771-789)
    - src/emit/codepulse-emitter.test.ts (existing test file — extend with the enumeration guard test group)
  </read_first>
  <behavior>
    - enumerateWorkspace(rootPath) returns { files: ForgeFileEntry[], artifacts: ForgeArtifactEntry[] } where files lists EVERY non-ignored file (path relative to rootPath, kind via kindTag, sizeBytes) up to FILE_COUNT_CAP.
    - Directories in IGNORE_DIRS are skipped entirely (not descended).
    - A path-traversal entry (a relative part resolving outside rootPath) is rejected via PathTraversalError and skipped — never read.
    - A symlink/junction whose realpath escapes rootPath is skipped (realpathSync.native containment check), before any byte read.
    - artifacts contains bytes ONLY for kind text/image files with sizeBytes ≤ PER_FILE_BYTE_CAP, and only until the running total reaches ARTIFACT_BYTE_CAP_PER_JOB; text → textContent string (UTF-8, Buffer.byteLength ≤ 1_000_000 enforced), image → imageBase64 (Buffer base64). Files over cap or of non-previewable kinds appear in files with NO artifact entry.
  </behavior>
  <action>
    Create src/workspace/enumerate.ts. Export ForgeFileEntry {path; kind; sizeBytes} and ForgeArtifactEntry {path; kind; sizeBytes; textContent?; imageBase64?} (these match the CodePulse receiver contract). Export enumerateWorkspace(rootPath: string): { files: ForgeFileEntry[]; artifacts: ForgeArtifactEntry[] }.

    Recursive walk from rootPath: skip any directory whose basename is in IGNORE_DIRS; stop adding files once files.length reaches FILE_COUNT_CAP. For each file: compute the relative path; apply guardPath(rootPath, ...relativeParts) inside try/catch — on PathTraversalError, continue (skip). Then symlink guard: realPath = fs.realpathSync.native(absPath); realRoot = fs.realpathSync.native(rootPath); if !realPath.toLowerCase().startsWith(realRoot.toLowerCase() + path.sep) then continue (skip escape). Classify with kindTag(filename). Push { path: relPath, kind, sizeBytes } to files.

    Byte read (capped): maintain a running artifactTotal. For kind === 'text' and sizeBytes ≤ PER_FILE_BYTE_CAP and artifactTotal + sizeBytes ≤ ARTIFACT_BYTE_CAP_PER_JOB: read UTF-8; if Buffer.byteLength(content,'utf8') ≤ 1_000_000 push {path, kind:'text', sizeBytes, textContent: content} and add to artifactTotal. For kind === 'image' and sizeBytes ≤ PER_FILE_BYTE_CAP and within the job total: read Buffer, push {path, kind:'image', sizeBytes, imageBase64: buf.toString('base64')}. All other files: metadata-only (no artifact entry). Define the caps + IGNORE_DIRS as module consts.

    Extend src/emit/codepulse-emitter.test.ts with a describe("workspace enumeration guards") group: a file inside an IGNORE_DIR is excluded; a path-traversal relative part is rejected (PathTraversalError → skipped, asserted via a fixture or a guardPath spy); a >1 MB text file appears in files with NO artifact entry; a ≤1 MB text file appears in artifacts with textContent. Symlink-escape may be asserted by mocking fs.realpathSync.native to return an out-of-root path and confirming the entry is skipped.
  </action>
  <verify>
    <automated>cd /c/Users/mandr/forge && npx vitest run src/emit/codepulse-emitter.test.ts 2>&1 | tail -6</automated>
  </verify>
  <acceptance_criteria>
    - `src/workspace/enumerate.ts` exports `enumerateWorkspace`, imports `guardPath` + `PathTraversalError` from `../workspace/manager.js` and `kindTag` from `../workspace/kinds.js` (reused, not reimplemented).
    - Enumeration calls `fs.realpathSync.native` on both the candidate path and rootPath and skips entries failing the containment check.
    - A `>1000000`-byte text file yields a files row but no artifacts entry; a `<=1000000`-byte text file yields an artifacts entry with `textContent`.
    - `codepulse-emitter.test.ts` contains a path-traversal/ignore-dir rejection test and `npx vitest run src/emit/codepulse-emitter.test.ts` exits 0.
  </acceptance_criteria>
  <done>enumerateWorkspace built on reused guards + kindTag; caps enforced; path-traversal/symlink/ignore-dir cases tested green.</done>
</task>

<task type="auto">
  <name>Task 2: emitFiles + buildFilesPayload + fileIngestUrl gate</name>
  <files>FORGE-REPO: src/emit/codepulse-emitter.ts, FORGE-REPO: src/emit/config.ts</files>
  <read_first>
    - src/emit/codepulse-emitter.ts L30-39 (EmitCfg), L113-127 (MAX_ATTEMPTS, loggedAuthStatuses), L138-196 (emitJob retry loop — clone verbatim), L208-221 (payload-interface convention)
    - src/emit/log-forwarder.ts L208-227 (FORGE_LOG_INGEST_URL separate-gate convention + LOG_INGEST_PATH)
    - src/emit/config.ts L25-end (resolveEmitCfg)
    - .planning/phases/82-files-preview-hardening/82-PATTERNS.md §"codepulse-emitter.ts" (L701-770)
    - .planning/phases/82-files-preview-hardening/82-RESEARCH.md §"emitFiles extension" (L421-466), §"Transport encoding for image bytes" (L453-466), Pitfall 5 (L642-645)
  </read_first>
  <action>
    In src/emit/codepulse-emitter.ts add the payload interfaces (after the workspaces payload at L221): ForgeFileEntry, ForgeArtifactEntry (import these from enumerate.ts to keep one source of truth), and ForgeFilesPayload { type: 'files'; hostId; forgeJobId; files; artifacts }. Add buildFilesPayload(hostId, job, files, artifacts): ForgeFilesPayload (forgeJobId from job.id, mirroring how emitJob derives ids).

    Add `export async function emitFiles(cfg: EmitCfg & { fileIngestUrl?: string }, job: Job, files: ForgeFileEntry[], artifacts: ForgeArtifactEntry[]): Promise<void>` cloning emitJob's body. Gate: destructure { fileIngestUrl, apiKey, hostId, fetchImpl = fetch }; if (!fileIngestUrl || !apiKey) return (separate gate — NOT ingestUrl). Set url = fileIngestUrl (the FULL endpoint URL — do NOT append a path; Pitfall 5). Same retry loop, same exp backoff, same loggedAuthStatuses 401/403-once handling, same never-throw/never-log-apiKey discipline. POST JSON body buildFilesPayload(...) with the Authorization Bearer header from apiKey.

    In src/emit/config.ts extend resolveEmitCfg to also return fileIngestUrl = process.env['FORGE_FILE_INGEST_URL'] (full endpoint URL). Widen the returned cfg type (or the call sites cfg object) to carry fileIngestUrl. Add a comment that FORGE_FILE_INGEST_URL is the separate gate for file/artifact forwarding (parallel to FORGE_LOG_INGEST_URL), independent of CONVEX_FORGE_INGEST_URL and FORGE_LOG_INGEST_URL.
  </action>
  <verify>
    <automated>cd /c/Users/mandr/forge && npx tsc --noEmit 2>&1 | grep -E "codepulse-emitter|config" || echo "tsc emitter+config clean"</automated>
  </verify>
  <acceptance_criteria>
    - `src/emit/codepulse-emitter.ts` exports `emitFiles` which returns early when `fileIngestUrl` or `apiKey` is falsy and POSTs to `fileIngestUrl` directly (no path appended).
    - `emitFiles` reuses the emitJob retry/backoff/loggedAuthStatuses discipline and never throws / never logs apiKey.
    - `src/emit/config.ts` `resolveEmitCfg` reads `process.env['FORGE_FILE_INGEST_URL']` into `fileIngestUrl`.
    - `npx tsc --noEmit` reports no errors in codepulse-emitter.ts or config.ts.
  </acceptance_criteria>
  <done>emitFiles + buildFilesPayload added with fire-and-forget discipline; fileIngestUrl gate wired into resolveEmitCfg; tsc clean.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Terminal-state trigger + live round-trip verification</name>
  <files>FORGE-REPO: src/http/routes/jobs.ts</files>
  <read_first>
    - src/http/routes/jobs.ts L260-290 (terminal-state block: classifyExit, updateJob, emitJob, then workspace promotion — wire emitFiles AFTER promotion)
    - src/process/manager.ts L255-270 (the manager terminal paths — confirm whether a promoted workspace exists there)
    - .planning/phases/82-files-preview-hardening/82-VALIDATION.md §"Manual-Only Verifications" (live cross-repo round-trip row) and §"Per-Task Verification Map" REQ-12
    - .planning/phases/82-files-preview-hardening/82-SPEC.md Req 12 acceptance (L84-87)
    - docs/forge-deploy-checklist.md (CodePulse repo — the env vars to set for the round-trip; produced by 82-02)
  </read_first>
  <what-built>
    Wired enumerateWorkspace + emitFiles into the daemon terminal-state path in src/http/routes/jobs.ts: after the job status is written AND the workspace is promoted (output files now in workspace.rootPath), call `const { files, artifacts } = enumerateWorkspace(workspace.rootPath); void emitFiles({ ...emitCfg, fileIngestUrl: resolveEmitCfg().fileIngestUrl }, job, files, artifacts);` — fire-and-forget, gated, never on a job-critical await. If src/process/manager.ts also reaches a terminal state with a promoted workspace, wire the same call there. Caps + ignore rules already enforced inside enumerateWorkspace (Task 1).

    Before requesting verification, the executor runs the full forge unit suite (`npx vitest run`) and confirms green, and confirms `npx tsc --noEmit` is clean across the repo.
  </what-built>
  <action>Wire `const { files, artifacts } = enumerateWorkspace(workspace.rootPath); void emitFiles({ ...emitCfg, fileIngestUrl: resolveEmitCfg().fileIngestUrl }, job, files, artifacts);` into the terminal-state block of src/http/routes/jobs.ts AFTER workspace promotion (and the manager.ts terminal path if it carries a promoted workspace) — fire-and-forget, gated, never on a job-critical await. Then run the live round-trip verification in `<how-to-verify>`. The `<what-built>` / `<how-to-verify>` / `<resume-signal>` fields are the authoritative checkpoint detail.</action>
  <how-to-verify>
    1. In the CodePulse Convex deployment, confirm FORGE_INGEST_API_KEY is set (same key used for log ingest) and CODEPULSE_ALLOWED_ORIGIN is set (per docs/forge-deploy-checklist.md).
    2. In the forge daemon environment, set FORGE_FILE_INGEST_URL to the full /forge-file-ingest endpoint URL (e.g. https://<deployment>.convex.site/forge-file-ingest) and FORGE_INGEST_API_KEY to the same bearer.
    3. Start the forge daemon and run a job that produces output files including at least one ≤1 MB text/HTML file, one ≤1 MB image, and one >1 MB or video/pdf/binary file. Let it reach a terminal state.
    4. Open CodePulse /forge, select the completed job, open the Files tab. Confirm: the file listing appears kind-grouped; the text/HTML file previews in the sandboxed iframe and toggles to escaped Source; the image renders inline; the >1 MB/non-previewable file shows "Not previewable in cloud" + local path + VS Code link.
    5. Unset FORGE_FILE_INGEST_URL, restart the daemon, run another job to terminal state. Confirm the daemon makes NO file-ingest calls and does not crash (logs/job emission still work).
  </how-to-verify>
  <verify>Executor runs `npx vitest run` + `npx tsc --noEmit` (forge repo) green, then performs the human round-trip in `<how-to-verify>`: gate SET → files appear in CodePulse /forge Files tab; gate UNSET → daemon no-op, no crash.</verify>
  <acceptance_criteria>
    - `src/http/routes/jobs.ts` calls `void emitFiles(...)` after the workspace-promotion step in the terminal-state block (not before promotion).
    - The full forge unit suite (`npx vitest run`) is green and `npx tsc --noEmit` is clean before verification is requested.
    - Live round-trip (gate SET): the completed job's files + capped artifacts appear in CodePulse's /forge Files tab with correct preview behavior per kind/size.
    - No-op (gate UNSET): the daemon completes a job with no file-ingest calls and no crash.
  </acceptance_criteria>
  <done>emitFiles wired after promotion; forge suite + tsc green; live round-trip (gate set) and no-op (gate unset) both confirmed by the operator.</done>
  <resume-signal>Type "approved" once the live round-trip (gate set) and the no-op (gate unset) are both confirmed, or describe what failed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| workspace filesystem → enumeration | Untrusted output files (possibly symlinks/traversal) read on the daemon host |
| daemon → /forge-file-ingest | Outbound POST carrying file bytes; bearer-authed, fire-and-forget |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-82-14 | Spoofing / EoP | path traversal in enumeration | mitigate | `guardPath` + `PathTraversalError` reused from manager.ts; traversal entries skipped before any FS read; tested |
| T-82-15 | EoP | symlink/junction escape from rootPath | mitigate | `fs.realpathSync.native` containment check on both candidate + rootPath (artifact.ts pattern); escaping entries skipped before byte read |
| T-82-03 | DoS | oversized Convex doc write | mitigate | daemon enforces PER_FILE_BYTE_CAP (≤1,000,000 bytes, Buffer.byteLength) before adding textContent; >cap files ship metadata-only |
| T-82-16 | DoS | unbounded enumeration payload | mitigate | FILE_COUNT_CAP (500) + ARTIFACT_BYTE_CAP_PER_JOB (10 MB) cap the listing + total bytes per job |
| T-82-13 | Info Disclosure | bearer key leak | mitigate | apiKey from FORGE_INGEST_API_KEY in daemon process env only; emitFiles never logs apiKey (loggedAuthStatuses logs status code only); key never enters CodePulse src/ |
| T-82-17 | Availability | emit failure crashes job path | mitigate | `void emitFiles(...)` fire-and-forget; emitFiles never throws (clone of emitJob discipline); job-critical path unaffected |
| T-82-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages (enumeration uses Node fs/path built-ins; emitter reuses existing fetch) |
</threat_model>

<verification>
- `npx vitest run src/emit/codepulse-emitter.test.ts` (forge repo) — enumeration guard + path-traversal + cap tests green.
- `npx vitest run` (forge repo, full suite) — green before round-trip.
- `npx tsc --noEmit` (forge repo) — clean.
- Live round-trip (manual, blocking checkpoint): gate set → files in CodePulse Files tab; gate unset → daemon no-op.
</verification>

<success_criteria>
- enumerateWorkspace produces a kind-classified, capped listing + ≤1 MB previewable bytes, rejecting traversal/symlink escapes.
- emitFiles is gated on FORGE_FILE_INGEST_URL, reuses FORGE_INGEST_API_KEY, fire-and-forget, never throws/logs the key.
- Terminal-state trigger wired after workspace promotion; no-op when the gate is unset.
- Live round-trip confirmed: completed forge job surfaces files + capped artifacts in CodePulse's /forge Files tab; gate unset → no calls, no crash.
</success_criteria>

<output>
Create `.planning/phases/82-files-preview-hardening/82-04-SUMMARY.md` when done
</output>
