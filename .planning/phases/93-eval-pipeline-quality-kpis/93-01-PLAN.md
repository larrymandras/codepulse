---
phase: 93-eval-pipeline-quality-kpis
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - convex/schema.ts
  - convex/evalScores.ts
  - convex/runtimeIngest.ts
  - convex/profiles.ts
  - convex/evalScores.test.ts
  - convex/runtimeIngest.test.ts
autonomous: true
requirements: [EVAL-01]
must_haves:
  truths:
    - "A task_quality event POSTed to /runtime-ingest with a valid Bearer token stores exactly one evalScores row"
    - "The same task_quality event redelivered (same idempotencyKey) inserts no second row"
    - "Changing a persona's modelPreferences via profiles.upsertConfig writes a configChanges audit row"
  artifacts:
    - path: "convex/schema.ts"
      provides: "evalScores table with by_idempotencyKey / by_profileId / by_scoreName indexes"
      contains: "evalScores: defineTable"
    - path: "convex/evalScores.ts"
      provides: "ingestTaskQuality idempotent mutation + processTaskQualityEvent pure helper"
      exports: ["ingestTaskQuality", "processTaskQualityEvent"]
    - path: "convex/runtimeIngest.ts"
      provides: "case task_quality in the eventType dispatch"
      contains: "case \"task_quality\""
    - path: "convex/profiles.ts"
      provides: "configChanges audit insert inside upsertConfig"
      contains: "profile."
  key_links:
    - from: "convex/runtimeIngest.ts"
      to: "api.evalScores.ingestTaskQuality"
      via: "ctx.runMutation in the task_quality case"
      pattern: "evalScores.ingestTaskQuality"
    - from: "convex/evalScores.ts"
      to: "evalScores by_idempotencyKey index"
      via: "early-return dedup before insert"
      pattern: "by_idempotencyKey"
---

<objective>
Close EVAL-01's persistence half: a new `evalScores` Convex table, an idempotent ingest mutation fed by a new `task_quality` case on the existing `/runtime-ingest` dispatch, and the `configChanges` audit-trail gap closed in `profiles.upsertConfig` (a hard prerequisite for EVAL-03 regression detection, per RESEARCH Pitfall 2 / D-11).

Purpose: Ástríðr's emitted `task_quality` scores must land in Convex exactly once even under at-least-once retry — they are currently written to Langfuse only and dropped on the floor.

Output: `evalScores` table (full schema including judge-side fields so downstream plans never re-touch `schema.ts`), `convex/evalScores.ts` domain module with the ingest path, the dispatch case, the audit-insert fix, and Wave-0 test scaffolds.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/93-eval-pipeline-quality-kpis/93-CONTEXT.md
@.planning/phases/93-eval-pipeline-quality-kpis/93-PATTERNS.md
@.planning/phases/93-eval-pipeline-quality-kpis/93-RESEARCH.md

<interfaces>
<!-- Extracted from the live repo. Use these directly — no exploration needed. -->

Idempotency precedent (convex/events.ts:20-30): inside the SAME mutation as the insert,
query the table `.withIndex("by_idempotencyKey", q => q.eq("idempotencyKey", args.idempotencyKey!)).first()`,
`if (existing) return;` before `ctx.db.insert(...)`.

runtimeIngest dispatch (convex/runtimeIngest.ts): `validateIngestAuth`/CORS gate at lines 15-23,
`switch (evt.eventType)` at line 56, existing `case "llm_call"` at 57-76 is the structural analog
(snake_case/camelCase coalesce `d.field ?? d.field_name`, then `ctx.runMutation`). A `timestamp`
local is already in scope inside the loop. Outer try/catch already wraps the loop — do not add a nested one.

configChanges audit precedent (convex/profiles.ts:144, inside updateEmail):
`ctx.db.insert("configChanges", { configKey: \`profile.${id}.emailAddress\`, oldValue, newValue, changedBy: "dashboard", changedAt: now })`.
`upsertConfig` is at convex/profiles.ts:81-114 and currently patches modelPreferences/channels/budget with NO audit insert.

Test convention (convex/runtimeIngest.test.ts:1-54): convex-test is NOT installed. Export the pure
logic as a function mirroring the case block and unit-test with plain vitest. NEVER import convex-test/convexTest.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: evalScores table + idempotent ingest mutation + dispatch case</name>
  <files>convex/schema.ts, convex/evalScores.ts, convex/runtimeIngest.ts, convex/evalScores.test.ts, convex/runtimeIngest.test.ts</files>
  <read_first>
    - convex/schema.ts (read the `events` table block ~L24-46 for the idempotency-index shape, and `llmMetrics` ~L297-320 for the multi-index style)
    - convex/events.ts (L8-48 — the idempotent mutation precedent to copy verbatim)
    - convex/runtimeIngest.ts (L1-80 — imports, auth gate, switch, the llm_call case)
    - convex/runtimeIngest.test.ts (L1-54 — the extracted-pure-function test pattern; convex-test absent)
  </read_first>
  <behavior>
    - Redelivered event: `processTaskQualityEvent` + the mutation given the same idempotencyKey yields exactly one row (second call is a no-op).
    - Field coalescing: `processTaskQualityEvent({ score: 0.8, profile_id: "business", session_id: "s1", event_id: "e1" }, 123)` returns args with `overall: 0.8`, `profileId: "business"`, `sessionId: "s1"`, `idempotencyKey: "e1"`, `scoreName: "task_quality"`.
    - Missing optional fields default: absent profileId → `"unknown"`; absent score/overall → row rejected or defaulted per v. validator (do NOT persist an out-of-range or NaN score).
  </behavior>
  <action>
    In `convex/schema.ts`, add an `evalScores` table with fields: `scoreName: v.string()` (values `"task_quality"` | `"llm_judge"`), `profileId: v.string()`, `sessionId: v.string()`, `overall: v.float64()`, `dimensions: v.optional(v.any())` (per-dimension `{score, rationale}` map — populated only by the judge in Plan 02, defined here so schema is never re-touched), `rubricVersion: v.optional(v.string())`, `judgeModel: v.optional(v.string())` (E5 version stamping, judge-only), `idempotencyKey: v.optional(v.string())`, `timestamp: v.float64()`. Add three indexes: `by_idempotencyKey` on `["idempotencyKey"]`, `by_profileId` on `["profileId", "timestamp"]`, `by_scoreName` on `["scoreName", "timestamp"]`.

    Create `convex/evalScores.ts`. Export `processTaskQualityEvent(data, timestamp)` — a pure function mirroring the dispatch case exactly (coalesce `profileId ?? profile_id ?? "unknown"`, `sessionId ?? session_id ?? "unknown"`, `overall`/`score`, `idempotencyKey ?? event_id`, `scoreName: "task_quality"`). Export `ingestTaskQuality` as a `mutation` with `v.` validators (`overall: v.float64()`, clamp/reject outside `[0,1]`), applying the `by_idempotencyKey` early-return dedup from `convex/events.ts:20-30` before `ctx.db.insert("evalScores", ...)`.

    In `convex/runtimeIngest.ts`, add `case "task_quality":` inside the existing `switch (evt.eventType)` block (after the `llm_call` case), calling `ctx.runMutation(api.evalScores.ingestTaskQuality, {...})` with the coalesced fields plus the in-scope `timestamp`. Do NOT add a new HTTP route or a second auth check — the case inherits `validateIngestAuth`/CORS.

    Create `convex/evalScores.test.ts` and extend `convex/runtimeIngest.test.ts` with a `describe("runtimeIngest — task_quality case")` block, using plain vitest + fixtures per the existing convention. Do NOT import convex-test.
  </action>
  <verify>
    <automated>npx vitest run convex/evalScores.test.ts convex/runtimeIngest.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `convex/schema.ts` contains `evalScores: defineTable` with `.index("by_idempotencyKey"` and `.index("by_profileId"` and `.index("by_scoreName"`.
    - `convex/evalScores.ts` exports `ingestTaskQuality` and `processTaskQualityEvent`.
    - `convex/runtimeIngest.ts` contains `case "task_quality"` and `api.evalScores.ingestTaskQuality`.
    - `npx vitest run convex/evalScores.test.ts convex/runtimeIngest.test.ts` exits 0, with a passing idempotency (single-row) test and a field-coalescing test.
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>A valid task_quality POST stores one row; a redelivered POST (same key) stores no second row; snake/camel fields coalesce; schema carries the full judge-ready field set.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Close the configChanges audit-trail gap in profiles.upsertConfig</name>
  <files>convex/profiles.ts, convex/evalScores.test.ts</files>
  <read_first>
    - convex/profiles.ts (L81-114 upsertConfig — currently no audit insert; L116-152 updateEmail — the audit-insert shape to copy at L144-150)
  </read_first>
  <behavior>
    - A pure helper `personaConfigChangeKey(profileId)` returns `` `profile.${profileId}.modelPreferences` `` (matches the `profile.<id>.<field>` naming of the existing updateEmail precedent).
    - When upsertConfig changes modelPreferences, exactly one configChanges row is written with `configKey` = that key, `oldValue` = prior modelPreferences, `newValue` = new modelPreferences, `changedBy: "dashboard"`.
  </behavior>
  <action>
    In `convex/profiles.ts`, inside `upsertConfig` (L81-114), after reading the existing `profileConfigs` row and before/at the patch, insert a `configChanges` row mirroring `updateEmail`'s shape (L144-150): `configKey: \`profile.${args.profileId}.modelPreferences\``, `oldValue` = the prior `modelPreferences`, `newValue` = the incoming `modelPreferences`, `changedBy: "dashboard"`, `changedAt: now`. Only write the audit row when `modelPreferences` is actually part of the update and differs from the stored value — do not emit a no-op audit row on an unrelated channels/budget-only patch. Export a tiny pure `personaConfigChangeKey(profileId)` helper (used by the audit insert and asserted in tests). This closes RESEARCH Pitfall 2 so EVAL-03's D-11 join has real rows to read. Scope exclusion (RESEARCH Pitfall 2 also named `agentProfiles.update`): it is deliberately NOT instrumented here — per RESEARCH Assumption A1, `agentProfiles` has zero rows and is not the mechanism for the three real operational personas (`personal`/`business`/`consulting`); `profileConfigs.modelPreferences` via `upsertConfig` is the only real persona-model change path, so it is the only audit write-site D-11 needs.
  </action>
  <verify>
    <automated>npx vitest run convex/evalScores.test.ts -t "configChange"</automated>
  </verify>
  <acceptance_criteria>
    - `convex/profiles.ts` `upsertConfig` contains `ctx.db.insert("configChanges"` with a `configKey` of the form `profile.${...}.modelPreferences`.
    - A test asserts `personaConfigChangeKey("business")` equals `"profile.business.modelPreferences"`.
    - `npx vitest run convex/evalScores.test.ts -t "configChange"` exits 0.
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>Editing a persona's model preferences through the dashboard now leaves an auditable configChanges row keyed `profile.<id>.modelPreferences`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Ástríðr → /runtime-ingest | Untrusted HTTP POST crosses into Convex; Bearer-gated |
| dashboard client → profiles.upsertConfig | Clerk-gated persona config edit |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-93-01 | Tampering | replayed/duplicated task_quality ingest | mitigate | `by_idempotencyKey` early-return dedup inside `ingestTaskQuality` (Task 1) |
| T-93-02 | Spoofing | unauthenticated ingest POST | mitigate | inherits `validateIngestAuth` Bearer gate on `/runtime-ingest` (no new route) |
| T-93-03 | Tampering | out-of-range / NaN score persisted | mitigate | `v.float64()` validator + `[0,1]` clamp/reject in `ingestTaskQuality` (Task 1) |
| T-93-SC | Tampering | npm installs | accept | no new packages — `zod` already `^4.4.3`; no install task in this plan |
</threat_model>

<verification>
- `npm test` full suite green after both tasks.
- `npx tsc --noEmit` clean.
</verification>

<success_criteria>
- `evalScores` table exists with the full field set and three indexes.
- Idempotent ingest proven by test (one row on retry).
- `task_quality` dispatch case wired and auth-inherited.
- `configChanges` audit row written on persona model changes (D-11 unblocked).
</success_criteria>

<output>
Create `.planning/phases/93-eval-pipeline-quality-kpis/93-01-SUMMARY.md` when done.
</output>
