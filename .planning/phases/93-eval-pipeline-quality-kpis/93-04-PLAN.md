---
phase: 93-eval-pipeline-quality-kpis
plan: 04
type: execute
wave: 3
depends_on: ["93-02"]
files_modified:
  - convex/evalScores.ts
  - convex/evalScores.test.ts
autonomous: true
requirements: [EVAL-03]
must_haves:
  truths:
    - "A regression alert fires only when both the before and after windows have >=5 judged sessions AND the mean overall drop clears the code-defined threshold"
    - "A 2-vs-2, 4-vs-6, sub-threshold, or single-outlier comparison never fires"
    - "A fired regression alert is created via the createIfNew delivery shape (webhookStatus pending + scheduled sendAlertWebhook), not the bare public alerts.create"
    - "KPI queries return per-persona current score + sparkline history + change-event markers, reading evalScores joined to profileConfigs/profileSwitches/configChanges"
  artifacts:
    - path: "convex/evalScores.ts"
      provides: "detectRegressions internalAction + listPersonaKpis / getPersonaDetail / listJudgedSessions queries"
      exports: ["detectRegressions", "listPersonaKpis", "getPersonaDetail", "listJudgedSessions"]
  key_links:
    - from: "convex/evalScores.ts detectRegressions"
      to: "internal.webhookDelivery.sendAlertWebhook"
      via: "ctx.scheduler.runAfter after an alerts insert with webhookStatus pending"
      pattern: "sendAlertWebhook"
    - from: "convex/evalScores.ts judgeSessionsAction"
      to: "detectRegressions"
      via: "tail call after the allSettled batch"
      pattern: "detectRegressions"
---

<objective>
Build EVAL-03's backend: a before/after window-mean regression detector that raises properly-delivered alerts per D-13, plus the read queries the Quality UI consumes.

Purpose: A persona model/instruction change that quietly hurts quality must be flagged, not silently absorbed into the trend line — and the operator must be able to see per-persona KPIs and drill in.

Output: `detectRegressions` internalAction (called at the tail of the nightly judge action), the KPI read queries, and boundary-tuned tests enforcing D-14's zero-false-positive bar.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/93-eval-pipeline-quality-kpis/93-CONTEXT.md
@.planning/phases/93-eval-pipeline-quality-kpis/93-RESEARCH.md
@.planning/phases/93-eval-pipeline-quality-kpis/93-AI-SPEC.md

<interfaces>
<!-- Extracted from the live repo. -->

Alert delivery precedent (convex/alerts.ts:716-736, createIfNew inside evaluateInternal):
inserts into `alerts` with `{ severity, source, message, acknowledged:false, status:"active", createdAt:now, webhookStatus:"pending" }`
THEN `ctx.scheduler.runAfter(0, internal.webhookDelivery.sendAlertWebhook, { alertId, attempt: 1 })`.
Dedup is by the `source` field. The public `alerts.create` (L22-40) does NOT set webhookStatus or schedule delivery — do NOT use it.
The `alerts` table `details` field is `v.optional(v.any())` — the regression insert should pass structured details directly
(createIfNew's helper does not accept details, so replicate its field set + add details rather than calling it).

Change boundaries (D-11): profileSwitches rows (by_timestamp, fromProfile/toProfile) AND persona-scoped
configChanges rows (by_key, configKey `profile.<id>.modelPreferences` written by Plan 01). Either starts a window.

Detection (D-12/D-14): compare mean `overall` in a ~7-day window before vs after a change event; require >=5 judged
sessions per side; flag when the drop exceeds a conservative code constant. evalScores read via by_profileId
(["profileId","timestamp"]). Persona = profileConfigs (profiles.listConfigs).

judgeSessionsAction (Plan 02) ends with the allSettled batch — append the detectRegressions tail call there.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: KPI read queries</name>
  <files>convex/evalScores.ts, convex/evalScores.test.ts</files>
  <read_first>
    - convex/evalScores.ts (the table shape + indexes from Plan 01; existing internalQueries from Plan 02)
    - convex/profiles.ts (L154-165 listConfigs, L181-191 recentSwitches — persona list + change events)
    - convex/schema.ts (configChanges L259-267, profileSwitches L528-533 — change-event sources)
  </read_first>
  <behavior>
    - `listPersonaKpis` returns one entry per active persona (profileConfigs) with: current mean overall (last N days), a sparkline series, a delta vs the previous period, and an activeRegression flag.
    - `getPersonaDetail(profileId, rangeDays)` returns the multi-dimension time series + change-event markers (profileSwitches + persona-scoped configChanges) within range.
    - `listJudgedSessions(profileId, rangeDays)` returns judged sessions with overall, per-dimension, rationale, sessionId — bounded/paginated.
    - Pure helpers `meanOverall(rows)` and `periodDelta(current, previous)` are unit-testable without ctx.
  </behavior>
  <action>
    In `convex/evalScores.ts`, add public `query` functions: `listPersonaKpis` (iterate `profiles.listConfigs` personas, for each read `evalScores` via `by_profileId` within the default range, compute current mean overall + sparkline + previous-period delta via exported pure helpers `meanOverall`/`periodDelta`, and attach an `activeRegression` boolean derived from an open `eval-regression:${profileId}` alert), `getPersonaDetail` (args `profileId`, optional `rangeDays` default 30 — return per-dimension + overall series plus change-event markers from `profileSwitches` (by_timestamp) and persona-scoped `configChanges` (by_key prefix `profile.${profileId}.`)), and `listJudgedSessions` (args `profileId`, `rangeDays` — return judged `llm_judge` rows with dimensions/rationale/sessionId, bounded). Keep all reads index-first (never `.collect()` unbounded — evalScores volume is tiny but stay index-bound per project convention). Never return an apiKey from any query.
  </action>
  <verify>
    <automated>npx vitest run convex/evalScores.test.ts -t "kpi|meanOverall|delta"</automated>
  </verify>
  <acceptance_criteria>
    - `convex/evalScores.ts` exports `listPersonaKpis`, `getPersonaDetail`, `listJudgedSessions`, `meanOverall`, `periodDelta`.
    - Tests prove `meanOverall`/`periodDelta` math on fixtures and that `getPersonaDetail` surfaces both profileSwitch and persona-scoped configChange markers.
    - Queries read via `by_profileId`/`by_scoreName`/`by_timestamp`/`by_key` indexes (no unbounded `.collect()` on evalScores).
    - `npx vitest run convex/evalScores.test.ts -t "kpi|meanOverall|delta"` exits 0; `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>The UI can read per-persona KPIs, a persona's dimension trend with change markers, and its judged-session list.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Regression detector + delivered alert</name>
  <files>convex/evalScores.ts, convex/evalScores.test.ts</files>
  <read_first>
    - convex/alerts.ts (L716-736 createIfNew delivery shape; L22-40 the non-delivering public create to avoid)
    - convex/webhookDelivery.ts (the sendAlertWebhook internal referenced by the scheduler call)
    - .planning/phases/93-eval-pipeline-quality-kpis/93-RESEARCH.md (Pattern 4; Open Question 3 — run at judge tail)
  </read_first>
  <behavior>
    - Pure `evaluateRegression(before[], after[], thresholds)` returns fire=true ONLY when `before.length>=5 && after.length>=5 && (meanBefore - meanAfter) >= dropThreshold`; returns fire=false for 2-vs-2, 4-vs-6, sub-threshold drop, and a single-outlier swing that does not move the mean past threshold.
    - On fire, the alert insert sets `webhookStatus: "pending"` and schedules `sendAlertWebhook`; `source` = `eval-regression:${profileId}` so an already-open alert for that persona does not re-fire.
    - The alert `details` carries `{ before, after, changeDate, changeType }`; message matches the UI-SPEC copy contract.
  </behavior>
  <action>
    In `convex/evalScores.ts`, add exported pure `evaluateRegression(beforeScores, afterScores, { minPerSide: 5, dropThreshold })` returning `{ fire, meanBefore, meanAfter, drop }`. Add module-level conservative constants: `MIN_SESSIONS_PER_SIDE = 5` and a `REGRESSION_DROP_THRESHOLD` chosen conservatively (D-14 — a meaningful drop on the 0-1 scale, tuned by commit, no settings UI). Add `detectRegressions` as an `internalAction` that: for each active persona, reads its change events (profileSwitches touching the persona + persona-scoped configChanges by `profile.${profileId}.` key prefix), and for each recent change with a ~7-day window each side, reads that persona's `overall` scores via `by_profileId` for both windows, calls `evaluateRegression`, and on fire inserts an `alerts` row replicating the `createIfNew` field set (`severity`, `source: \`eval-regression:${profileId}\``, `message` per the UI-SPEC "{persona} quality dropped {N} pts after {change type} on {date} ({before} → {after})" copy, `acknowledged:false`, `status:"active"`, `createdAt`, `webhookStatus:"pending"`, `details: {before, after, changeDate, changeType}`) THEN `ctx.scheduler.runAfter(0, internal.webhookDelivery.sendAlertWebhook, { alertId, attempt: 1 })`. Guard re-firing by checking for an existing active `eval-regression:${profileId}` alert first (dedup-by-source). Do NOT call the public `alerts.create`.

    Append a `detectRegressions` tail call to `judgeSessionsAction` (Plan 02), after the `Promise.allSettled` batch resolves, so the night's own scores are counted before comparison (RESEARCH Open Question 3 — no extra cron slot).
  </action>
  <verify>
    <automated>npx vitest run convex/evalScores.test.ts -t "regression|alert delivery"</automated>
  </verify>
  <acceptance_criteria>
    - `convex/evalScores.ts` exports `evaluateRegression` and defines `detectRegressions` (internalAction); `judgeSessionsAction` calls `detectRegressions`.
    - Tests prove: fires at >=5/side + over-threshold; does NOT fire at 2-vs-2, 4-vs-6, sub-threshold, or single-outlier.
    - A test asserts the fire path sets `webhookStatus: "pending"` and calls `scheduler.runAfter(..., sendAlertWebhook, ...)` with `source` = `eval-regression:${profileId}`, and that `alerts.create` (public) is NOT used.
    - `npx vitest run convex/evalScores.test.ts -t "regression|alert delivery"` exits 0; `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>A real quality regression after a persona change fires exactly one delivered alert; noise-level comparisons stay silent.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| detectRegressions → alert engine | Internal producer into the shared alert/webhook delivery path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-93-10 | Repudiation / operational trust | false-positive regression alert (crying wolf) | mitigate | >=5 judged sessions/side + conservative code-defined drop threshold; boundary tests forbid firing at 2-vs-2/4-vs-6/sub-threshold/outlier (D-14) |
| T-93-11 | Denial of Service (signal loss) | alert created but never delivered | mitigate | createIfNew delivery shape (webhookStatus pending + scheduled sendAlertWebhook), asserted in test; public alerts.create forbidden |
| T-93-12 | Information Disclosure | apiKey exposed via a KPI query | mitigate | KPI queries read only evalScores/profile tables; no config-slot read returns apiKey |
| T-93-SC | Tampering | npm installs | accept | no new packages |
</threat_model>

<verification>
- `npm test` full suite green.
- `npx tsc --noEmit` clean.
</verification>

<success_criteria>
- Regression detector enforces the D-14 min-sample + threshold gate (proven by boundary tests).
- Alerts are actually delivered (createIfNew shape); per-persona dedup via source.
- KPI queries power the Quality surface, index-bound.
</success_criteria>

<output>
Create `.planning/phases/93-eval-pipeline-quality-kpis/93-04-SUMMARY.md` when done.
</output>
