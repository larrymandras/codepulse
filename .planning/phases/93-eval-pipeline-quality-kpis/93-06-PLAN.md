---
phase: 93-eval-pipeline-quality-kpis
plan: 06
type: execute
wave: 5
depends_on: ["93-03", "93-05"]
files_modified:
  - .planning/phases/93-eval-pipeline-quality-kpis/93-CALIBRATION.md
autonomous: false
requirements: [EVAL-01, EVAL-02, EVAL-03]
user_setup:
  - service: anthropic
    why: "The nightly judge cannot run until the eval LLM slot holds a real API key"
    env_vars:
      - name: intelligence.llm_eval
        source: "Set via the dashboard setLLMConfig mutation (slot='eval') with {provider:'anthropic', model:'claude-haiku-4-5', apiKey:<key>}"
    dashboard_config:
      - task: "Deploy Convex backend to prod (npx convex deploy --yes) so the evalScores table, ingest case, judge action, and cron are live on tidy-whale-981"
        location: "CodePulse repo — npx convex deploy"
      - task: "Ensure astridr's CONVEX_URL + ASTRIDR_INGEST_API_KEY env (Phase 90 compose anchor) point at tidy-whale-981 so spawn_score's mirror actually reaches prod"
        location: "astridr-repo deployment env"
must_haves:
  truths:
    - "A real Ástríðr-emitted task_quality score reaches prod Convex (tidy-whale-981) and appears as an evalScores row"
    - "That score renders on the live Quality page"
    - "The nightly judge action runs (or is manually triggered) against prod and writes at least one llm_judge row"
    - "10-20 real sessions are hand-labeled to seed the E3 calibration gate before the Quality trends are trusted"
  artifacts:
    - path: ".planning/phases/93-eval-pipeline-quality-kpis/93-CALIBRATION.md"
      provides: "Judge calibration reference set: 10-20 labeled sessions + judge-vs-human agreement note"
      min_lines: 20
  key_links:
    - from: "Ástríðr spawn_score (prod)"
      to: "tidy-whale-981 evalScores"
      via: "live mirror POST → /runtime-ingest → ingestTaskQuality"
      pattern: "task_quality"
---

<objective>
Close the phase against D-04's mandatory live-E2E completion bar and seed the E3 judge-calibration gate. Green convex-test/vitest alone does NOT close this phase — a real score must travel the full path and render (the Phase 90 lesson: the cross-repo gate was flagged but only live testing surfaced the real gaps).

Purpose: Prove the loop is actually closed end-to-end in prod, and establish the human-agreement baseline before anyone trusts the Quality trends.

Output: a verified live `task_quality` row + judge row on the Quality page, and `93-CALIBRATION.md` with the labeled reference set.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/93-eval-pipeline-quality-kpis/93-CONTEXT.md
@.planning/phases/93-eval-pipeline-quality-kpis/93-AI-SPEC.md
@.planning/phases/93-eval-pipeline-quality-kpis/93-RESEARCH.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Configure eval LLM key + deploy prod + point astridr env</name>
  <what-built>Plans 01-05 shipped the full pipeline (ingest, judge, regression, UI) but the eval LLM config slot is empty, the backend is not yet deployed to prod, and astridr's mirror target must be confirmed — all operator-only steps Claude cannot do (secrets + prod deploy).</what-built>
  <how-to-verify>
    1. From the CodePulse repo, deploy the backend: `npx convex deploy --yes` (pushes the evalScores table, task_quality ingest case, judge action, and 05:00 cron to tidy-whale-981).
    2. Set the eval config slot via the dashboard Settings LLM config (slot = "eval"): provider `anthropic`, model `claude-haiku-4-5`, apiKey = your Anthropic key. Confirm `getEvalLLMConfigInternal` resolves (judge no longer throws "Eval LLM not configured").
    3. Confirm astridr's `CONVEX_URL` + `ASTRIDR_INGEST_API_KEY` (Phase 90 compose anchor) resolve to tidy-whale-981 and the shared ingest key.
  </how-to-verify>
  <action>Operator performs the deploy + secret + env steps in how-to-verify: deploy the Convex backend to tidy-whale-981, set the intelligence.llm_eval slot via setLLMConfig, and confirm astridr CONVEX_URL/ASTRIDR_INGEST_API_KEY point at prod. Claude cannot perform prod deploy or handle the API key; this checkpoint gates the live-E2E task on those operator-only steps.</action>
  <resume-signal>Type "deployed" once the backend is live, the eval slot holds a real key, and astridr env points at prod.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Live E2E — real score reaches prod and renders (D-04)</name>
  <what-built>The full ingest→store→render path for a real Ástríðr task_quality score, plus the nightly judge and Quality UI.</what-built>
  <how-to-verify>
    1. Trigger a real Ástríðr score emission (run a task that causes `langfuse_eval.py:spawn_score` to fire), OR emit one via the normal Ástríðr flow. Confirm a new `evalScores` row with `scoreName: "task_quality"` appears in the tidy-whale-981 Convex data (dashboard or Convex table view).
    2. Open the live Quality page (`/quality`) and confirm the score contributes to the relevant persona's KPI card (score /100, sparkline updates).
    3. Manually trigger the judge once against prod (run `judgeSessionsAction`, e.g. via the Convex dashboard "Run function" on the internal action, or wait for the 05:00 UTC cron) and confirm at least one `scoreName: "llm_judge"` row lands with 4 dimensions + rationales + rubricVersion + judgeModel, and renders on a persona detail page.
    4. Confirm the judge batch logged its `N sampled / N scored / N failed` summary (E7 liveness).
  </how-to-verify>
  <action>Operator drives a real Ástríðr score emission and verifies the full path per how-to-verify: a task_quality evalScores row lands in prod, it renders on the live Quality page, a manually-triggered judge run writes an llm_judge row that renders on persona detail, and the judge summary count is logged. This is the D-04 completion bar — green tests do not substitute.</action>
  <resume-signal>Type "verified" (or describe the gap) once a real task_quality score AND a judge score are visible on the live Quality page.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Seed the judge-calibration reference set (E3)</name>
  <files>.planning/phases/93-eval-pipeline-quality-kpis/93-CALIBRATION.md</files>
  <read_first>
    - .planning/phases/93-eval-pipeline-quality-kpis/93-AI-SPEC.md (Section 5 Reference Dataset — composition + labeling; E3 ≥0.7 agreement gate)
  </read_first>
  <action>
    Create `93-CALIBRATION.md` capturing the E3 calibration reference set: pull 10-20 real sessions from prod Convex spanning the failure surface (clean completions, error-heavy, tool-churn, expensive/high-cost, plus at least one E4 boundary pair). For each, record the digest the judge would see (from `buildJudgeDigest`) and leave a labeling table for the operator's hand-scored `overall` + 4 per-dimension values, alongside the judge's produced scores. Compute and note the judge-vs-human agreement/rank-correlation, and record the PASS/FAIL against the ≥0.7 E3 gate with a one-line "trust the Quality trends? yes/no until re-calibrated" verdict. This is the frozen basis for future E2/E4/E5 fixtures — note which sessions were promoted into `convex/evalScores.test.ts` fixtures. (The operator is the sole labeler per AI-SPEC Section 1b — surface the labeling table for Larry to fill; do not invent labels.)
  </action>
  <verify>
    <automated>test -f .planning/phases/93-eval-pipeline-quality-kpis/93-CALIBRATION.md && grep -qi "agreement" .planning/phases/93-eval-pipeline-quality-kpis/93-CALIBRATION.md && echo ok</automated>
  </verify>
  <acceptance_criteria>
    - `93-CALIBRATION.md` exists with a 10-20 session table spanning the composition classes and a per-session judge-vs-human labeling structure.
    - It records the E3 ≥0.7 agreement gate result (or an explicit "labels pending — trends not yet trusted" state).
    - It names which sessions became `evalScores.test.ts` fixtures.
  </acceptance_criteria>
  <done>The calibration reference set exists; whether the Quality trends are trusted is an explicit, evidence-backed decision rather than an assumption.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → prod Convex env | Setting the eval apiKey + deploying to tidy-whale-981 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-93-14 | Information Disclosure | eval apiKey stored/handled in prod | mitigate | key set via setLLMConfig into the redacted agentConfigs slot; never returned by public queries (T-07-05); never pasted into a committed file |
| T-93-15 | Repudiation / operational trust | Quality trends trusted before calibration | mitigate | E3 ≥0.7 human-agreement gate recorded in 93-CALIBRATION.md before trends are trusted |
| T-93-SC | Tampering | prod deploy / config | mitigate | operator-gated blocking checkpoints (Tasks 1-2); no auto-advance |
</threat_model>

<verification>
- A real task_quality row + a real llm_judge row visible on the live Quality page (D-04 met).
- 93-CALIBRATION.md seeded with the reference set + agreement gate.
</verification>

<success_criteria>
- The full cross-repo loop is proven live in prod, not just green in tests.
- Judge calibration baseline established before trends are trusted.
</success_criteria>

<output>
Create `.planning/phases/93-eval-pipeline-quality-kpis/93-06-SUMMARY.md` when done.
</output>
