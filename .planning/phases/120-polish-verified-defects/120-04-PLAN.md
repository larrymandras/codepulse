---
phase: 120-polish-verified-defects
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/StatusBadge.tsx
  - src/components/forge/ForgeStatusBadge.tsx
  - src/components/forge/ForgeStatusBadge.test.tsx
  - src/components/StatusBadge.test.tsx
  - .planning/phases/120-polish-verified-defects/120-BADGE-INVENTORY.md
autonomous: true
requirements: [POLISH-05]

must_haves:
  truths:
    - "D-15: the four spine WORDS are applied — running/Running, completed/Succeeded, failed/Failed, stopped/Cancelled — and every domain state with no honest mapping keeps its own distinct label, documented as a deliberate exception with its reason"
    - "D-15: auth_failed keeps a label AND a colour token distinct from failed, preserving the load-bearing SC#4 constraint carried in ForgeStatusBadge's own header comment; queued, pending, stopping_pending and expired likewise keep their own labels because collapsing them would make the badge assert a state that is not true"
    - "D-16: only Failed renders filled — every other status becomes quiet text/outline with no saturated bg-*-900/60 or bg-(--status-*) fill, which is the LARGER half of POLISH-05 since today every status in both maps renders filled"
    - "D-16: the quiet law is applied IN PLACE in the two existing badge modules — no new shared StatusBadge primitive is created, because that is TOKEN-05's job in Phase 122 and building it now would violate D-02's no-refactor rule"
    - "D-17: Executions and Forge are the reach — Executions' status column renders through src/components/StatusBadge.tsx and Forge's through src/components/forge/ForgeStatusBadge.tsx, so those two modules are the whole in-scope surface"
    - "D-17: a file:line inventory of every remaining badge site is produced and handed to Phase 122 as a work-list rather than a discovery task, and it records honestly which sites this plan changed as a side effect of editing a shared module"
    - "Vocabulary and fill changes are asserted against the RENDERED output for each status, with the auth_failed-versus-failed distinctness proven as a paired control rather than as a substring match on the word amber"
  artifacts:
    - path: "src/components/StatusBadge.tsx"
      provides: "Quiet semantic styles with only error filled; completed relabelled to the spine word"
      contains: "SUCCEEDED"
    - path: "src/components/forge/ForgeStatusBadge.tsx"
      provides: "Quiet STATUS_MAP with only failed filled; Succeeded/Cancelled spine words; SC#4 preserved via tokens"
      contains: "Succeeded"
    - path: ".planning/phases/120-polish-verified-defects/120-BADGE-INVENTORY.md"
      provides: "D-17 work-list for Phase 122 plus the documented D-15 label exceptions"
      min_lines: 40
  key_links:
    - from: "src/components/ExecutionTable.tsx"
      to: "src/components/StatusBadge.tsx"
      via: "the status column and the execution-mode column both render through this one component"
      pattern: "StatusBadge"
    - from: "src/components/forge/ForgeJobList.tsx"
      to: "src/components/forge/ForgeStatusBadge.tsx"
      via: "job and command rows render the Forge badge"
      pattern: "ForgeStatusBadge"
---

<objective>
Apply the quiet-badge law and the four-word vocabulary spine to the two badge modules that render
Executions and Forge, and hand Phase 122 a measured inventory of everything else.

Purpose: the milestone's acceptance criterion 5 names Executions specifically. Today *every* status in
both badge maps renders with a saturated fill, so "only Failed is filled" is the larger half of the
work and the label change is the smaller half.

Output: two edited badge modules, their tests updated to assert the new contract with a genuine
control, and `120-BADGE-INVENTORY.md`.

**Read this before you plan your edits — CONTEXT.md's D-17 is partly self-contradictory and this plan
resolves it.** D-17 lists `JobsPanel`, `CronJobList`, `RosterTable`, `RoomListItem`, `IdeationRow`,
`FactsTable`, `hr/AgentCard`, `hr/detail/DetailRuntimeTab` and `BlackboardPanel` as "remaining sites,
inventory for 122" — but all nine of them render through the very same
`src/components/StatusBadge.tsx` that Executions renders through. There is no way to give Executions
the quiet law without those nineteen consumers receiving it too. D-17's list also names
`forge/ForgeJobList`, `forge/ForgeJobDetail` and `forge/ForgeMetadataPanel` as "remaining" when they
are the Forge surface that is explicitly IN scope.

The resolution, and the rule for this plan: **there are exactly two badge implementations, and both are
in scope. The FILL law propagates to all consumers and that is the desired outcome; the VOCABULARY
change is confined to the four spine words so no non-status vocabulary is disturbed.** The inventory
records which sites changed by propagation so 122 inherits a fact, not a surprise. Details in
`<interfaces>`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/120-polish-verified-defects/120-CONTEXT.md
@CLAUDE.md

@src/components/StatusBadge.tsx
@src/components/forge/ForgeStatusBadge.tsx
@src/components/forge/ForgeStatusBadge.test.tsx
@src/components/ExecutionTable.tsx
@src/components/ui/badge.tsx

<interfaces>
<!-- MEASURED 2026-08-17. Two badge implementations; nothing else renders a job status. -->

### A. src/components/StatusBadge.tsx — 61 lines. Executions' status column.
Consumed by NINETEEN files (measured): BlackboardPanel, CronJobList, ExecutionTable, FactsTable,
IdeationRow, JobsPanel, RoomListItem, SwarmTaskDetail, WarRoomTaskCard, hr/AgentCard,
hr/AgentDetailSheet, hr/RosterOrgChart, hr/RosterTable, hr/detail/DetailRuntimeTab, pages/Dreaming,
pages/MeetingBot, pages/Memory, pages/WarRoom, pages/WhatsApp.

Current shape:
  `semanticStyles` — ALL FIVE FILLED:
    ok:    "bg-(--status-ok) text-white"
    error: "bg-(--status-error) text-white"
    warn:  "bg-(--status-warn) text-white"
    info:  "bg-(--status-info) text-white"
    idle:  "bg-muted text-muted-foreground"
  `legacyMap` — 22 entries mapping a domain value to `{ semantic, label }`, labels UPPERCASE.
    Job/execution vocabulary:  queued/QUEUED, running/RUNNING, completed/DONE, failed/FAILED,
                               cancelled/CANCELLED, timed_out/TIMEOUT
    Execution MODES (v6.0):    strict, adaptive, standard, filler, stalled
    Voice call (Phase 72):     live, ended, joining
    Roster (Phase 76):         active, pending, idle, deregistered
    Swarm task (Phase 149):    claimed, verifying, done/DONE, verify_rejected/REJECTED
    Quality (Phase 93):        regression
  Fallback label is `status.toUpperCase()`; fallback style is `semanticStyles.idle`.
  Renders `<Badge variant="secondary" className={cn("rounded-sm text-sm", style)}>`.

**CASE IS NOT CHANGED HERE.** D-15 gives the four spine WORDS, not their case. This component's labels
are uppercase and `ForgeStatusBadge`'s are Title Case — a pre-existing difference. Sentence-case type
law is Phase 122/124's (the same reasoning D-03 uses to keep the wordmark's `font-mono`). Title-casing
only four of this file's 22 labels would create a NEW inconsistency inside one component, which is
worse than the status quo. So: `completed`'s label becomes `SUCCEEDED`, and nothing else changes case.

The four spine words in this file, after the edit:
  running   -> "RUNNING"    (already correct, no edit)
  completed -> "SUCCEEDED"  (was "DONE" — THE ONLY LABEL EDIT IN THIS FILE)
  failed    -> "FAILED"     (already correct, no edit)
  cancelled -> "CANCELLED"  (already correct, no edit)
  there is no `stopped` key here; `cancelled` is this vocabulary's word for it.
Every other `legacyMap` entry keeps its label. Do NOT touch swarm `done` — it is a DIFFERENT
vocabulary that also happens to read "DONE", and `BlackboardPanel.test.tsx` asserts on it.

### B. src/components/forge/ForgeStatusBadge.tsx — 123 lines. Forge's job badges.
Rendered at `forge/ForgeJobList.tsx:109,:235`, `forge/ForgeJobDetail.tsx:113,:135`,
`forge/ForgeMetadataPanel.tsx:95`. Its own header comment carries the SC#4 constraint:
  "auth_failed (amber) MUST be visually distinct from failed (red)".

`STATUS_MAP` — nine entries, EVERY ONE FILLED today:
  queued            "Queued"     bg-zinc-800/60  text-zinc-400                Clock
  running           "Running"    bg-blue-900/60  text-[var(--status-info)]     Loader2  (spins)
  completed         "Completed"  bg-green-900/60 text-[var(--status-ok)]       CheckCircle
  failed            "Failed"     bg-red-900/60   text-[var(--status-error)]    XCircle
  stopped           "Stopped"    bg-zinc-800/40  text-zinc-500                Square
  auth_failed       "Auth Failed" bg-amber-900/60 text-[var(--status-warn)]    KeyRound
  pending           "Queued…"    bg-zinc-800/60  text-primary                 Loader2  (spins)
  stopping_pending  "Stopping…"  bg-amber-900/40 text-[var(--status-warn)]     Loader2  (spins)
  expired           "Expired"    bg-zinc-800/30  text-zinc-600                Clock
Plus an unknown-status fallback chip (`bg-zinc-800/60 text-zinc-400`, `Circle`) that must keep
degrading gracefully rather than throwing.

`colorScheme` (the `data-color-scheme` attribute) is derived from `status`, NOT from the class string:
failed/red, auth_failed|stopping_pending/amber, running/blue, completed/green, queued/slate,
pending/emerald, else stone. **Do not change this derivation** — four tests assert on it and it is the
documented test-compatibility contract ported from forge.

### C. Tests that WILL BREAK and must be corrected, with the reason
  `forge/ForgeStatusBadge.test.tsx` ~:74  `{ status: "completed", label: "Completed" }`
        -> becomes "Succeeded" (D-15).
  `forge/ForgeStatusBadge.test.tsx` ~:77  `{ status: "stopped", label: "Stopped" }`
        -> becomes "Cancelled" (D-15).
  `forge/ForgeStatusBadge.test.tsx` ~:29-36  the auth_failed test asserting
        `expect(badgeHtml).toMatch(/amber/i)`. Under D-16 the `bg-amber-900/60` fill is removed, so a
        substring match on the word "amber" no longer holds unless you deliberately keep an
        amber-NAMED utility. Do not contort the implementation to satisfy a weak assertion — replace
        the assertion with the stronger one described in Task 2.
  `forge/ForgeStatusBadge.test.tsx` ~:59-67  the failed test asserting `/red/i` present and `/amber/i`
        absent. `failed` stays FILLED (D-16), so keep `bg-red-900/60` verbatim and this test keeps
        passing. Verify it does rather than assuming.

### D. Tests that DO NOT break — verify, do not "fix"
  `BlackboardPanel.test.tsx` ~:96,:110 expect "RUNNING" and "DONE". Its "DONE" comes from the SWARM
        state `done`, not from `completed`. Both survive.
  `JobsPanel.test.tsx` ~:165 `expect(badge.className).toContain("bg-muted")` for an `unknown` status,
        which resolves to `semanticStyles.idle`. **This is why `idle` keeps `bg-muted
        text-muted-foreground` unchanged** — `bg-muted` is already a quiet neutral chip, not a
        saturated status fill, so D-16 does not apply to it and changing it would break a test for no
        visual gain.
  `JobsPanel.test.tsx` ~:189 `querySelectorAll(".text-\\(--status-ok\\)").length === 0` for a
        `running` row. `running` maps to the `warn` semantic, so a quiet `text-(--status-ok)` on the
        `ok` semantic cannot appear in that render. Confirm by running it.
  `__tests__/IdeationRow.test.tsx` ~:49 `.bg-\\(--status-error\\)`. That class comes from IdeationRow's
        OWN severity mapping — the test mocks `StatusBadge` (`getByTestId("status-badge")`) — so it is
        unaffected either way. Note it and move on.

### E. Out of scope for this plan
  No new `StatusBadge` primitive, no `ui/badge.tsx` cva variant (it is shadcn-generated), no
  consumer-file edits. `files_modified` is the whole edit surface.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Quiet law + spine word in the shared StatusBadge (D-15, D-16)</name>

  <files>src/components/StatusBadge.tsx, src/components/StatusBadge.test.tsx</files>

  <read_first>
    - `src/components/StatusBadge.tsx` — all 61 lines. Note that `legacyMap` mixes FIVE different
      vocabularies (job status, execution mode, voice call, roster, swarm task) behind one lookup, and
      that `semanticStyles` is keyed on the resolved semantic rather than on the domain value.
    - `src/components/ExecutionTable.tsx` ~:170-189 — this component is rendered TWICE per row: once for
      `row.status` and once for `modeData.mode`. Whatever you do to the styles applies to execution
      MODES as well as statuses, which is why only the fill law travels and the vocabulary change does
      not.
    - `src/components/ui/badge.tsx` — the `variant="secondary"` base classes your `className` merges
      over, so you know what a "quiet" badge actually looks like once the fill is removed. Do not edit
      this file.
    - `src/components/JobsPanel.test.tsx` ~:150-195 — the two assertions that pin `bg-muted` and the
      absence of `text-(--status-ok)`. Read them before choosing your quiet classes.
  </read_first>

  <action>
Record the current `npx vitest run` pass/fail counts before you edit anything.

**Fill law (D-16).** In `semanticStyles`, keep `error` EXACTLY as it is —
`"bg-(--status-error) text-white"` — because Failed is the one status that stays filled. Convert `ok`,
`warn` and `info` to quiet: the status colour moves to the text and a low-opacity border of the same
token replaces the fill, e.g. the shape `text-(--status-ok) border border-(--status-ok)/40 bg-transparent`
(the repo already uses both the `text-(--token)` and `border-(--token)` forms — verify against a live
example such as `RunTimeline`'s `border-(--status-warn)` before committing to the syntax). Leave `idle`
as `"bg-muted text-muted-foreground"`: it is already quiet, and `JobsPanel.test.tsx` pins it.

Do not introduce a hex value, an `rgba()`, or a Tailwind palette colour (`amber-400`, `green-500`).
`CLAUDE.md` § Styling requires accents and status colours to read from the CSS vars.

**Vocabulary (D-15).** Change exactly ONE label: `completed`'s `"DONE"` becomes `"SUCCEEDED"`. Leave
all 21 other `legacyMap` labels, the `status.toUpperCase()` fallback, and the case convention alone —
the reasoning is in `<interfaces>` § A and you should restate it in a short comment above `legacyMap`
so the mixed case is legible as a decision rather than an oversight. In that comment, name D-15 and
state that swarm `done` deliberately keeps "DONE" because it is a different vocabulary.

Update the file's header comment (add one if absent) to state the badge law in one sentence — only
Failed renders filled — so the next editor does not reintroduce a fill.

Then create `src/components/StatusBadge.test.tsx`. This component has 19 consumers and no direct test
today, which is why the fill change is riskier than it looks. Assert:
- `completed` renders "SUCCEEDED" and NOT "DONE";
- swarm `done` still renders "DONE" — the CONTROL that proves the change was scoped to the job
  vocabulary and did not sweep a same-word entry in another vocabulary;
- `failed` renders with the `bg-(--status-error)` fill present;
- `completed`, `running` and a `live` (info/ok) status each render with NO `bg-(--status-*)` fill —
  assert the fill class is absent, and in the same test assert the corresponding `text-(--status-*)` IS
  present, so "quiet" is proven to mean recoloured rather than uncoloured;
- an unmapped status still renders its uppercased raw value and the `bg-muted` idle chip rather than
  throwing.
  </action>

  <verify>
    <automated>npx tsc --noEmit && npx vitest run src/components/StatusBadge.test.tsx src/components/JobsPanel.test.tsx src/components/BlackboardPanel.test.tsx src/components/__tests__/IdeationRow.test.tsx</automated>
  </verify>

  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0.
    - `npx vitest run` shows no new failures against the count you recorded first. Record both numbers.
    - `grep -c 'bg-(--status-error) text-white' src/components/StatusBadge.tsx` is 1 — Failed is still
      filled.
    - `grep -cE 'bg-\(--status-(ok|warn|info)\)' src/components/StatusBadge.tsx` is 0 — the other three
      fills are gone.
    - `grep -c 'bg-muted text-muted-foreground' src/components/StatusBadge.tsx` is 1 — `idle` untouched.
    - `grep -c 'SUCCEEDED' src/components/StatusBadge.tsx` is 1 and `grep -c '"DONE"' src/components/StatusBadge.tsx`
      is 1 (the swarm `done` entry, which must survive).
    - `grep -ciE '#[0-9a-f]{3,6}|rgba?\(' src/components/StatusBadge.tsx` is 0 — no hardcoded colour.
    - The new test's quiet assertions check BOTH the absent fill and the present text token. An
      absent-fill-only assertion passes on a badge that lost its colour entirely and does not satisfy
      this criterion.
    - `BlackboardPanel.test.tsx` and `JobsPanel.test.tsx` pass UNCHANGED — do not edit either. If one
      fails, the implementation is wrong, not the test.
  </acceptance_criteria>

  <done>Executions' status column, and every other consumer of the shared badge, renders quiet for all statuses except Failed, and `completed` reads "SUCCEEDED" without disturbing the four other vocabularies behind the same lookup.</done>
</task>

<task type="auto">
  <name>Task 2: Quiet law + spine words in ForgeStatusBadge, with SC#4 proven by a paired control (D-15, D-16)</name>

  <files>src/components/forge/ForgeStatusBadge.tsx, src/components/forge/ForgeStatusBadge.test.tsx</files>

  <read_first>
    - `src/components/forge/ForgeStatusBadge.tsx` — all 123 lines, including the header comment that
      states the SC#4 constraint and the `colorScheme` ternary chain at ~:88-101.
    - `src/components/forge/ForgeStatusBadge.test.tsx` — all 201 lines. Read every `describe` block
      before editing any of them; four of them assert on `data-color-scheme`, which you must not change,
      and two assert on labels you must change.
    - `<interfaces>` § C above — the four tests that break and why, and the two that must not be
      "fixed".
  </read_first>

  <action>
**Fill law (D-16).** In `STATUS_MAP`, keep `failed`'s `className` EXACTLY `"bg-red-900/60
text-[var(--status-error)]"` — Failed is the only filled status, and `ForgeStatusBadge.test.tsx`'s
`/red/i`-present + `/amber/i`-absent assertions depend on that literal. Strip the `bg-*-900/60` /
`bg-zinc-800/*` fill from the other EIGHT entries, replacing each with a quiet treatment that keeps its
existing text token and adds a low-opacity border of the same token, following whatever syntax you
verified in Task 1. Preserve each entry's semantic colour:
  running          keeps `--status-info`
  completed        keeps `--status-ok`
  auth_failed      keeps `--status-warn`
  stopping_pending keeps `--status-warn`
  pending          keeps `text-primary`  (a test asserts `/text-primary/`)
  queued, stopped, expired  keep their zinc text ramp
The unknown-status fallback chip becomes quiet the same way and must still not throw.

**Vocabulary (D-15).** Exactly two label edits: `completed` "Completed" -> **"Succeeded"**, and
`stopped` "Stopped" -> **"Cancelled"**. `running` is already "Running" and `failed` already "Failed".
Title Case is this component's existing convention and is retained (see `<interfaces>` § A on why case
is not unified in this phase).

**The five documented exceptions, which keep their own labels (D-15).** `auth_failed` "Auth Failed",
`queued` "Queued", `pending` "Queued…", `stopping_pending` "Stopping…", `expired` "Expired". Rewrite the
file's header comment to (a) restate the SC#4 constraint in terms of the NEW quiet treatment — naming
the two tokens rather than the two fills, since the fills are gone — and (b) list these five as
deliberate D-15 exceptions with a one-clause reason each: mapping `queued` or `pending` to Running,
`stopping_pending` to Cancelled, or `auth_failed` to Failed would make the badge assert a state that is
not true, which is the same honesty defect POLISH-04 exists to remove.

**Do NOT change** the `colorScheme` derivation, the `data-status` / `aria-label` attributes, the
`animate-spin` condition (`running || pending || stopping_pending`), the icon choices, or the
unknown-status fallback behaviour.

**Then repair the test file — and strengthen the SC#4 assertion rather than propping it up.**
1. Update the two label expectations to "Succeeded" and "Cancelled".
2. Replace the auth_failed `/amber/i` colour test. A substring match on the word "amber" was only ever
   a proxy for "this badge is not the failed badge", and the quiet treatment removes the word. Assert
   the real property instead, as an explicit PAIRED CONTROL in one test: render `auth_failed` and
   render `failed`, read each badge's class string, and assert that the auth_failed string contains
   `--status-warn` and does NOT contain `--status-error`, while the failed string contains
   `--status-error` and does NOT contain `--status-warn`. Name the test so it is obvious it is the SC#4
   guard, and comment that a one-sided assertion cannot distinguish "distinct" from "both the same
   colour" — which is the whole thing SC#4 protects.
3. Add a fill-law test: `failed`'s class string contains `bg-red-900/60`, and each of the other eight
   statuses' class strings contains NO `bg-` fill token. Assert this by iterating the eight statuses so
   a future ninth status added with a fill fails the suite.
4. Leave the `data-color-scheme`, `animate-spin`, accessibility and unknown-status blocks untouched and
   confirm they still pass.
  </action>

  <verify>
    <automated>npx tsc --noEmit && npx vitest run src/components/forge/ForgeStatusBadge.test.tsx</automated>
  </verify>

  <acceptance_criteria>
    - `npx vitest run src/components/forge/ForgeStatusBadge.test.tsx` exits 0 with a test count strictly
      greater than before (record both).
    - `grep -c 'bg-red-900/60' src/components/forge/ForgeStatusBadge.tsx` is 1.
    - `grep -cE 'bg-(green|blue|amber)-900|bg-zinc-800' src/components/forge/ForgeStatusBadge.tsx` is 0
      — every other fill is gone.
    - `grep -c 'Succeeded' src/components/forge/ForgeStatusBadge.tsx` is 1 and
      `grep -c 'Cancelled' src/components/forge/ForgeStatusBadge.tsx` is 1;
      `grep -cE '"Completed"|"Stopped"' src/components/forge/ForgeStatusBadge.tsx` is 0.
    - `grep -c 'data-color-scheme' src/components/forge/ForgeStatusBadge.tsx` is 1 and the `colorScheme`
      ternary chain is byte-identical in the diff.
    - `grep -c 'Auth Failed' src/components/forge/ForgeStatusBadge.tsx` is 1 — SC#4's label half.
    - The SC#4 test asserts FOUR conditions across two renders (warn present + error absent for
      auth_failed; error present + warn absent for failed). A test asserting only the auth_failed side
      does not satisfy this criterion, because it passes when both badges are amber.
    - The fill-law test iterates the eight non-failed statuses rather than spot-checking one or two.
    - `grep -ciE '#[0-9a-f]{3,6}|rgba?\(' src/components/forge/ForgeStatusBadge.tsx` is 0.
  </acceptance_criteria>

  <done>Forge's badges are quiet except Failed, read Succeeded and Cancelled, keep five honest domain labels, and auth_failed's distinctness from failed is proven by a two-sided control instead of a substring.</done>
</task>

<task type="auto">
  <name>Task 3: The D-17 inventory — a work-list for Phase 122, including what propagated</name>

  <files>.planning/phases/120-polish-verified-defects/120-BADGE-INVENTORY.md</files>

  <read_first>
    - The consumer list in `<interfaces>` § A — then RE-DERIVE it yourself rather than trusting this
      plan. Run a fixed-string search for imports of `StatusBadge` and of `ForgeStatusBadge` across
      `src/` and paste the raw output. A count copied from a plan is not evidence.
    - Your own diffs from Tasks 1 and 2.
  </read_first>

  <action>
Create `.planning/phases/120-polish-verified-defects/120-BADGE-INVENTORY.md` with four sections. This
is D-17's deliverable and Phase 122's TOKEN-05 input, so it must be a work-list, not prose.

**§1 — The two badge implementations, and what this phase did to each.** Name both files, the number of
render sites each has, and the exact change (fill law + which labels). State plainly that there is no
third implementation of a job-status badge in the repo.

**§2 — Sites that changed by PROPAGATION, not by direct edit.** A `file:line` table of every consumer
of `src/components/StatusBadge.tsx` (re-derived, with the command shown). For each, one clause naming
which vocabulary it feeds the badge — job status, execution mode, voice call, roster, swarm task, or
quality — because Phase 122's shared `StatusBadge` primitive has to serve all six and this table is the
requirement list for it. Mark the ones D-17 had listed as "remaining for 122" and state that they were
reached in 120 as an unavoidable consequence of the shared component, so 122 inherits an already-quiet
surface for them rather than a discovery task. **Do not present this as if it were the planned scope —
present it as what actually happened and why the D-17 boundary could not hold as written.**

**§3 — Sites with their OWN badge implementation, genuinely remaining for 122.** Search for the other
badge-shaped components (`WebhookStatusBadge`, `skills/IntakeStatusBadge`, and any component rendering
`<Badge>` from `ui/badge.tsx` directly with a status-coloured className) and list them `file:line`. This
is the part of D-17's inventory that is real: components that do NOT go through either module and
therefore still render filled after this plan. Say how many there are.

**§4 — The D-15 exceptions register.** A table of every domain state that keeps its own label instead of
a spine word, across BOTH modules, with the reason. At minimum: `auth_failed`, `queued`, `pending`,
`stopping_pending`, `expired` from Forge; `timed_out`, and the execution-mode / voice / roster / swarm /
quality vocabularies from the shared badge. Each row's reason must say what false claim the spine word
would make. Note explicitly that swarm `done` and job `completed` are different vocabularies that
diverged in this phase (`DONE` vs `SUCCEEDED`) and that this is intentional.

Close with a one-line "what I dropped and why" — any site you considered and excluded from §3, and the
reason. Precision over volume: a site listed that turns out not to render a status is worse than a
short list.
  </action>

  <verify>
    <automated>test -s .planning/phases/120-polish-verified-defects/120-BADGE-INVENTORY.md && grep -c 'file:line\|\.tsx:' .planning/phases/120-polish-verified-defects/120-BADGE-INVENTORY.md && npx tsc --noEmit && npx vitest run</automated>
  </verify>

  <acceptance_criteria>
    - All four sections exist, and §2 and §3 are tables with real `file:line` references, not prose.
    - §2 includes the literal command used to derive the consumer list and its literal output.
    - §2 states the D-17 boundary problem in the open rather than presenting the propagation as planned.
    - §4 covers every non-spine label in both modules with a stated false claim it avoids.
    - The closing "what I dropped and why" line is present.
    - **UNSCOPED regression gate (added by orchestrator after plan-check, 2026-08-17).** `npx vitest run`
      with NO path filter shows no new failures relative to the baseline total you recorded before
      starting Task 1. Record both totals here in the inventory.
      Rationale: this plan edits `src/components/StatusBadge.tsx`, whose `legacyMap` is consumed by
      **30 non-test files** covered by **17 test files** — Tasks 1 and 2 run only 4 of those 17, so a
      fill-law regression in the execution-mode, roster, swarm-task, voice-call or quality vocabularies
      (this plan's own threat T-120-14) would pass every other gate in the plan silently. This was the
      single blocking finding of the plan-check: 120-04 was the only plan in the phase with no unscoped
      run. Do not re-narrow this command back to a path list.
  </acceptance_criteria>

  <done>Phase 122 receives a measured work-list naming every badge site, which vocabulary it carries, which are already quiet, and which still render filled — plus the exception register that a shared primitive has to honour.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Convex `v.string()` status column → badge render | Both modules receive a status that is cast unchecked from a free-form string; the daemon and ingest can emit values outside the union |
| shared component → 19 consumer surfaces | An edit here changes six unrelated vocabularies at once, so a change intended for job status can misrepresent a roster or swarm state |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-120-11 | Spoofing | A badge asserting a state that is not true — `queued` shown as Running, `auth_failed` shown as Failed — after a vocabulary collapse | mitigate | D-15's five exceptions keep their own labels; the exception register in §4 records the false claim each one avoids; the SC#4 paired-control test fails if auth_failed and failed converge |
| T-120-12 | Tampering | An unmapped status from the daemon dereferencing an undefined config and taking down a list region | mitigate | Both modules' existing fallback chips are preserved and asserted; the unknown-status tests in both files must pass unchanged |
| T-120-13 | Repudiation | A one-sided colour assertion reporting SC#4 as preserved when both badges render the same token | mitigate | The SC#4 test asserts presence AND absence on BOTH renders — the only form that can distinguish distinct from identical |
| T-120-14 | Tampering | The fill law silently mangling the execution-MODE column, roster states or swarm states that share the shared badge | mitigate | Only the fill travels; exactly one label changes in the shared module; swarm `done` is the named control test, and §2 enumerates every affected vocabulary |
| T-120-SC | Tampering | npm/pip/cargo installs | mitigate | No packages are added. If an install becomes necessary, stop and run the Package Legitimacy Gate first. |
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0; `npx vitest run` shows no new failures against the pre-plan baseline
  (record both counts).
- Fill law asserted by grep in both modules: exactly one filled entry each, zero hardcoded colours.
- `BlackboardPanel.test.tsx`, `JobsPanel.test.tsx` and `__tests__/IdeationRow.test.tsx` pass UNEDITED.
- The SC#4 test asserts four conditions across two renders; the fill-law test iterates all eight
  non-failed Forge statuses.
- `120-BADGE-INVENTORY.md` exists with all four sections and re-derived command output.
- **Attended check, not automatable here:** with `dev:noauth` running, open `/executions` and the Forge
  page and confirm by eye that only Failed rows read as filled and that an `auth_failed` Forge badge is
  still obviously not a `failed` one. Record both observations in the SUMMARY. A jsdom class-string
  assertion is not a rendered colour.
</verification>

<success_criteria>
- Only Failed renders filled, in both badge modules.
- `Succeeded` and `Cancelled` are live; `Completed` and `Stopped` are gone from Forge; `DONE` survives
  only as the swarm-task label.
- SC#4 holds and is proven by a two-sided control.
- No shared `StatusBadge` primitive was created and no consumer file was edited (D-16, D-02).
- `120-BADGE-INVENTORY.md` gives Phase 122 a work-list including the propagation fact and the D-15
  exception register.
</success_criteria>

<output>
Create `.planning/phases/120-polish-verified-defects/120-04-SUMMARY.md` when done.

**Do not commit.** Shared checkout on `master` with concurrent sessions; the orchestrator owns the
commit. Never `git add -A`, never `git commit --amend`.

**Every class string, label, line number and consumer count in this plan is a DRAFT from a 2026-08-17
reading, not a spec.** Re-derive the consumer list yourself; correct anything that does not match the
live code and say so in the SUMMARY. If a test this plan predicts will break does not break, or one it
predicts is safe does break, the plan was wrong — report which.
</output>
