---
phase: 120-polish-verified-defects
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/index.css
  - src/layouts/DashboardLayout.tsx
  - .planning/phases/120-polish-verified-defects/120-SHELL-EVIDENCE.md
autonomous: true
requirements: [POLISH-01]

must_haves:
  truths:
    - "D-03: the CodePulse wordmark loses glitch-text AND its drop-shadow glow and its now-dead data-text attribute, while font-mono tracking-wider is deliberately KEPT — a named exception to D-01 recorded so a reviewer does not read it as a violation"
    - "D-04: the dead CSS definitions are deleted, not merely orphaned — .glitch-text with both pseudo-elements and both @keyframes glitch-anim-* blocks, .matrix-bg with its ::after, .nav-active-shadow, .nav-hover-shadow, and the Phase-89 readable/aubergine suppression rule for .matrix-bg only"
    - "D-05: the violet search pill is fixed AT THE PILL with a neutral surface, and --accent is NOT redefined in any of the five theme blocks because that is TOKEN-02's job in Phase 122"
    - "D-06: the Cyberpunk Scrollbar block is removed in full — the thumb-hover box-shadow glow, the cyan-tinted track border, and the cyan thumb fill are one decoration and go together"
    - "The per-item nav glow is gone from BOTH nav renderings, desktop and mobile — a fix landing on only one is the half-fix this codebase has produced before"
    - "The CRT toggle SURVIVES intact: its localStorage default stays false, the toggle control still renders in both header variants, the crt-scanline-bar overlay still renders when enabled, and the readable/aubergine suppression rule for crt-scanline-bar is NOT deleted because readable's no-effects guarantee depends on it"
    - "The three unconditional decorative pulse dots in the shell are de-animated (D-09's rule applied to this file's share of the population), and none of them needed reduced-motion gating because a removed animation cannot violate it"
  artifacts:
    - path: "src/index.css"
      provides: "Kill-list CSS definitions deleted; CRT suppression and avatar-glow retained"
      contains: "crt-scanline-bar"
    - path: "src/layouts/DashboardLayout.tsx"
      provides: "Quiet wordmark, quiet nav in both renderings, neutral search pill, CRT toggle intact"
      contains: "codepulse-crt"
  key_links:
    - from: "src/layouts/DashboardLayout.tsx"
      to: "localStorage codepulse-crt"
      via: "default-false read that must remain exactly as it is"
      pattern: "codepulse-crt\\\" \\)? ?\\?\\? \\\"false\\\"|codepulse-crt"
---

<objective>
Strip the kill-list from the two files that carry almost all of it structurally: `src/index.css`
(the definitions) and `src/layouts/DashboardLayout.tsx` (the shell usages).

Purpose: this is the density peak of POLISH-01. Six of the eight enumerated kill-list items live
here — glitch-text, matrix-bg, both nav-glow classes, the cyan scrollbar glow, the violet search
pill — plus three unconditional pulse dots. Deleting the CSS definitions (not just the usages) is
what makes success criterion 1's grep unambiguous.

Output: a quiet shell, an `index.css` with ~85 fewer lines of decoration, a CRT feature that still
works, and a `readable` theme whose no-effects guarantee is intact.

**The single highest risk in this plan is deleting too much from `index.css`.** Two rules that look
like a matched pair are not: the Phase-89 suppression block contains a `.matrix-bg` rule (delete) and
a `.crt-scanline-bar` rule (KEEP — see `<interfaces>`). Read that block before touching it.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/120-polish-verified-defects/120-CONTEXT.md
@CLAUDE.md

@src/index.css
@src/layouts/DashboardLayout.tsx

<interfaces>
<!-- MEASURED 2026-08-17. Line numbers move in a shared checkout — locate by CONTENT, then edit. -->

### src/index.css — DELETE (D-04, D-06). 836 lines total before this plan.
  ~497-513  `/* Cyberpunk Scrollbar */` + `::-webkit-scrollbar`, `::-webkit-scrollbar-track`,
            `::-webkit-scrollbar-thumb`, `::-webkit-scrollbar-thumb:hover`. The named glow is the
            `box-shadow: 0 0 8px oklch(from var(--primary) ...)` on the :hover rule; the cyan track
            border and cyan thumb fill are the same block (D-06 takes all four rules).
  ~515-561  `/* Glitch effect */` + `.glitch-text`, `.glitch-text::before, .glitch-text::after`,
            `.glitch-text:hover::before`, `.glitch-text:hover::after`, `@keyframes glitch-anim-1`,
            `@keyframes glitch-anim-2`.
  ~563-581  `/* Premium Radial Background */` + `.matrix-bg` and `.matrix-bg::after`.
  ~602-614  the `/* Phase 89 — Category E */` comment + `.nav-active-shadow` + `.nav-hover-shadow`.
  ~646-650  `[data-theme="readable"] .matrix-bg, [data-theme="aubergine"] .matrix-bg { display:none }`
            — dead once `.matrix-bg` is gone.

### src/index.css — DO NOT DELETE. Each of these sits inside or adjacent to a deleted block.
  ~652-655  `[data-theme="readable"] .crt-scanline-bar, [data-theme="aubergine"] .crt-scanline-bar
            { display: none; }`
            **CONTEXT.md's D-04 says to delete "the Phase-89 suppression rules ~646-655". That is
            wrong for the second half and correcting it is part of this plan.** `.crt-scanline-bar`
            has NO CSS definition in this file — it is styled inline by Tailwind at
            `DashboardLayout.tsx:514` — so this suppression rule is the ONLY thing that keeps the CRT
            bar out of `readable`, and `readable` keeping its no-effects guarantee is an explicit
            v15.0 requirement (`REQUIREMENTS.md:45`, TOKEN-03). The CRT feature survives this phase,
            therefore its suppression must survive with it. Delete only the `.matrix-bg` pair above.
  ~616-622  `.avatar-glow` / `.avatar-glow:hover` — not on the enumerated kill list (D-01).
  ~624-644  `[data-theme="aubergine"] body::before` / `::after` — not on the kill list.
  ~490-495  `@keyframes ping-pulse` / `.ping-indicator` — not on the kill list.
  ~594-600  the global `@media (prefers-reduced-motion: reduce)` block. Load-bearing.
  ~657-663  the aubergine reduced-motion suppression. Load-bearing.
  the `--accent: #8b5cf6` declaration at ~151 and every other `--accent` in all five theme blocks —
  D-05 forbids touching the token. Note `.matrix-bg` at ~571 is one of its consumers and goes away
  with the rule; that is a consumer disappearing, not a token change.

### src/layouts/DashboardLayout.tsx — the seven edits
  :146/:147  desktop nav — `nav-active-shadow` / `nav-hover-shadow` in the active/inactive branches
  :278/:279  MOBILE nav (the Settings entry) — the SAME two classes. Easy to miss; both must go.
  :243       the wordmark: `glitch-text` + `drop-shadow-[0_0_8px_oklch(from_var(--primary)_l_c_h_/_0.5)]`
             + `data-text="CodePulse"` all go. `font-mono tracking-wider` STAYS (D-03).
             `shadow-primary` on the same element is a Tailwind shadow-COLOR utility with no
             companion shadow-size class, so it renders nothing — see Task 3.
  :511       `<div className="matrix-bg" />` — the only usage of the class
  :600       the search pill's `bg-accent/50 hover:bg-accent` (D-05)
  :89, :246, :572  three unconditional `animate-pulse` dots (D-09 kill)

### src/layouts/DashboardLayout.tsx — MUST SURVIVE, assert each
  :342-372 and :390-410  the CRT toggle control and its state/event plumbing
  :352, :358, :392, :401, :406  `localStorage.setItem("codepulse-crt", ...)`,
             the `codepulse-crt-toggle` custom event, and
             `JSON.parse(localStorage.getItem("codepulse-crt") ?? "false")`
             — POLISH-01's "CRT-by-default" item is ALREADY SATISFIED by that `?? "false"`.
             Verify and record it; changing it is not the task. SHELL-01 (Phase 124) moves this
             toggle into the new header's overflow menu, so removing it now would delete a feature
             a later phase is scheduled to relocate.
  :512-514   the CRT overlay render, including
             `<div className="crt-scanline-bar w-full h-[5px] bg-primary/40 shadow-[var(--glow-md)]" />`
  :152, :283 nav-ICON `drop-shadow-[0_0_8px...]` / `group-hover:drop-shadow-[...]`. NOT enumerated
             by POLISH-01, which names the two nav-shadow CLASSES specifically. D-01 keeps them and
             Task 3 records them for Phase 124. Leaving them is the correct outcome.
  :303, :369 two more `bg-accent` usages (sidebar-collapse button, a toggle). D-05 scopes the fix to
             the search pill only; these are two of 50 repo-wide `bg-accent` sites that belong to
             TOKEN-02. Task 3 records them.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete the dead CSS definitions and the Cyberpunk Scrollbar (D-04, D-06)</name>

  <files>src/index.css</files>

  <read_first>
    - `src/index.css` lines 485-670 in one pass. That range contains every block this task deletes AND
      every block it must keep, interleaved. Read it before your first deletion and satisfy yourself
      you can name which side of the line each rule falls on.
    - `.planning/phases/120-polish-verified-defects/120-CONTEXT.md` § D-04 and § D-06 — then read the
      `src/index.css — DO NOT DELETE` section of `<interfaces>` above, which corrects D-04 on the
      `.crt-scanline-bar` suppression rule.
  </read_first>

  <action>
Delete, by content, the five blocks named under `### src/index.css — DELETE` in `<interfaces>`,
together with their leading section comments (`/* Cyberpunk Scrollbar */`, `/* Glitch effect */`,
`/* Premium Radial Background */`, and the `/* Phase 89 — Category E ... */` two-line comment). A
section comment left standing above nothing is the orphaned-noise defect this phase exists to remove.

Do NOT delete anything named under `### src/index.css — DO NOT DELETE`. In particular:

- Keep `[data-theme="readable"] .crt-scanline-bar, [data-theme="aubergine"] .crt-scanline-bar
  { display: none; }`. Rewrite the comment above the surviving rule so it no longer claims to cover
  `matrix-bg` — after this edit it suppresses the CRT bar only. Leaving the old comment in place would
  be a stale claim in the file that a later editor could act on.
- Keep `.avatar-glow`, the aubergine `body::before/::after` pair, `@keyframes ping-pulse` /
  `.ping-indicator`, the global reduced-motion block, and the aubergine reduced-motion block.

Do not add anything. This task is deletions only, plus the one comment rewrite. No `--surface-*`
token, no `--hairline`, no replacement scrollbar styling — TOKEN-01 rewrites this file in Phase 122
and any addition here becomes a conflict there. Browsers fall back to their native scrollbar, which
is the intended quiet outcome.

Before finishing, verify no rule left in the file references a class you just deleted (a
`[data-theme]` override, a `@media` variant, an `:is()` list) — search the whole file for each of
`glitch-text`, `matrix-bg`, `nav-active-shadow`, `nav-hover-shadow` and `scrollbar`, not just the
range you edited.
  </action>

  <verify>
    <automated>npx tsc --noEmit && npx vitest run</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c 'glitch' src/index.css` is 0; same for `matrix-bg`, `nav-active-shadow`,
      `nav-hover-shadow`, and `scrollbar-thumb`.
    - CONTROLS, which must all still be present and are what prove the deletion was scoped rather than
      a range delete: `grep -c 'crt-scanline-bar' src/index.css` is 2 (the readable + aubergine
      selectors of the surviving rule), `grep -c 'avatar-glow' src/index.css` is 2,
      `grep -c 'ping-indicator' src/index.css` is 1, `grep -c 'prefers-reduced-motion' src/index.css`
      is unchanged from before the edit.
    - `grep -c 'accent: #8b5cf6' src/index.css` is 1 — the violet token is untouched (D-05).
    - No comment in the file describes a rule that no longer exists: reading the diff, every removed
      rule's section comment went with it, and the surviving suppression rule's comment no longer
      mentions `matrix-bg`.
    - The file's line count dropped by roughly 80-90 lines. Record the exact before/after in the
      SUMMARY; a drop far larger than that means a keep-block was caught in the deletion.
  </acceptance_criteria>

  <done>The kill-list classes have no definition left anywhere in `index.css`, the scrollbar is native, and the CRT suppression plus every non-enumerated effect is byte-identical.</done>
</task>

<task type="auto">
  <name>Task 2: Quiet the shell — wordmark, both nav renderings, matrix-bg, search pill, three dots</name>

  <files>src/layouts/DashboardLayout.tsx</files>

  <read_first>
    - `src/layouts/DashboardLayout.tsx` — read the whole file. It is the highest-density target in the
      phase and it renders the nav TWICE (desktop aside and mobile drawer), which is the specific way
      a fix here goes half-applied.
    - Before editing, run a fixed-string search of this file for each of `nav-active-shadow`,
      `nav-hover-shadow`, `glitch-text`, `matrix-bg`, `bg-accent`, `animate-pulse` and `codepulse-crt`,
      and write the hit list down. You will assert against it afterwards.
    - `.planning/phases/120-polish-verified-defects/120-CONTEXT.md` § D-03 and § D-05.
  </read_first>

  <action>
Six edits, all in this one file.

**A. Nav glow, BOTH renderings.** Remove the `nav-active-shadow` and `nav-hover-shadow` class tokens
from the desktop nav (~:146/:147) and from the mobile nav (~:278/:279). Four token removals, two
renderings. Everything else in those class strings — `text-primary`, `bg-primary/10`,
`hover:bg-primary/5`, `is-active` — stays; the active state remains readable from colour and
background. Do NOT add a replacement active rail: the 2px active rail is SHELL-02's (Phase 124).

**B. The wordmark (D-03).** On the `<h1>` at ~:243 remove `glitch-text`, remove the
`drop-shadow-[0_0_8px_oklch(from_var(--primary)_l_c_h_/_0.5)]` token, and remove the
`data-text="CodePulse"` attribute — that attribute exists solely to feed `.glitch-text::before`'s
`content: attr(data-text)` and is dead the moment the class goes. **KEEP `font-mono tracking-wider`.**
Add a one-line comment above the element naming D-03 and stating that the glow removal is a
deliberate named exception to D-01 and that the mono/tracking type treatment is retained for Phase
122/124 to change, so a reviewer does not read either half as a mistake.

**C. `matrix-bg`.** Delete the `<div className="matrix-bg" />` element at ~:511 entirely. It is a
`position:absolute; inset:0; z-index:-1` decorative layer with `pointer-events:none` and no children,
so removing the element (rather than just the class) leaves nothing behind. Do NOT touch the CRT
overlay block immediately below it at ~:512-514.

**D. The search pill (D-05).** At ~:600 replace `bg-accent/50 hover:bg-accent` with a neutral
treatment built from existing tokens — a muted surface plus the border it already has, e.g. a
`bg-muted/40`-class surface with `hover:bg-muted/60`, keeping `border border-border/50`,
`text-muted-foreground`, `hover:text-foreground` and `transition-colors` as they are. Do not
introduce a hex value and do not touch `--accent` in `index.css`. Leave the two other `bg-accent`
usages in this file (~:303 sidebar-collapse, ~:369 a toggle) alone — D-05 is scoped to the pill and
Task 3 records the rest.

**E. The three unconditional pulse dots (D-09).** Remove the `animate-pulse` token — and only that
token — from the three dots at ~:89 (nav-group label), ~:246 (beside the static string
"Operator: Larry Mandras") and ~:572 (beside the static label "Astridr Runtime Telemetry"). All three
render regardless of any state value, which is exactly D-09's decorative test, and two of them sit
next to hardcoded strings that can never change. Per D-09 the decision is **de-animate, not remove**:
leave the `<span>` and its `w-*`/`h-*`/`rounded-full`/`bg-primary` classes in place as static dots.
None of these needs `prefers-reduced-motion` gating (D-11) because a removed animation cannot violate
it — gating is only for pulses that survive, and plan `120-06` owns those.

**F. Verify-and-record the CRT default.** Do not change it. Read the
`JSON.parse(localStorage.getItem("codepulse-crt") ?? "false")` expression, confirm the default is
`false`, and record the file:line and the literal expression in the SUMMARY as POLISH-01's
"CRT-by-default" item being already satisfied. Leave the toggle control, the `setItem`, the
`codepulse-crt-toggle` event dispatch and listener, and the `crt-scanline-bar` overlay render exactly
as they are.
  </action>

  <verify>
    <automated>npx tsc --noEmit && npx vitest run</automated>
  </verify>

  <acceptance_criteria>
    - `git grep -cF 'nav-active-shadow' -- src/layouts/DashboardLayout.tsx` is 0 and the same for
      `nav-hover-shadow`. Because each appeared TWICE, a count of 1 is a half-fix, not progress —
      state the before count (2 each) and the after count (0 each) in the SUMMARY.
    - `git grep -cF 'glitch-text' -- src/layouts/DashboardLayout.tsx` is 0 and
      `git grep -cF 'data-text' -- src/layouts/DashboardLayout.tsx` is 0.
    - `git grep -cF 'font-mono tracking-wider' -- src/layouts/DashboardLayout.tsx` is >= 1 — D-03's
      keep half is asserted, not just its remove half.
    - `git grep -cF 'matrix-bg' -- src/layouts/DashboardLayout.tsx` is 0.
    - `git grep -cF 'bg-accent' -- src/layouts/DashboardLayout.tsx` is exactly 2 (down from 3) — the
      pill is fixed and the two out-of-scope usages are provably untouched.
    - `git grep -cF 'animate-pulse' -- src/layouts/DashboardLayout.tsx` is 0.
    - CRT SURVIVAL CONTROLS, all still present: `git grep -cF 'codepulse-crt' -- src/layouts/DashboardLayout.tsx`
      is unchanged from before the edit, `git grep -cF 'crt-scanline-bar' -- src/layouts/DashboardLayout.tsx`
      is 1, and `git grep -cF '?? "false"' -- src/layouts/DashboardLayout.tsx` is >= 1.
    - `git grep -cF 'drop-shadow-[0_0_8px_oklch(from_var(--primary)_l_c_h_/_0.8)]' -- src/layouts/DashboardLayout.tsx`
      is 2 — the nav-ICON glows at ~:152/:283 are deliberately UNTOUCHED (D-01). If this is 0 you
      removed something this plan does not authorize.
    - `npx tsc --noEmit` exits 0 and `npx vitest run` shows no new failures against the pre-plan count.
  </acceptance_criteria>

  <done>The shell has no glitch, no matrix wash, no per-item nav glow in either rendering, no violet pill and no decorative pulse — and CRT still works, still defaults off, and still hides under `readable`.</done>
</task>

<task type="auto">
  <name>Task 3: Record the judgment calls this plan deliberately did not act on</name>

  <files>.planning/phases/120-polish-verified-defects/120-SHELL-EVIDENCE.md</files>

  <read_first>
    - Nothing to read first for this task beyond your own diffs. **This plan writes its OWN artifact,
      `120-SHELL-EVIDENCE.md`.** Four wave-1 plans run in parallel against a shared checkout, so no two
      of them share an artifact: `120-01` owns `120-SWEEP-EVIDENCE.md`, `120-03` owns
      `120-SANCTIONED-PATTERNS.md`, `120-05` owns `120-FABRICATION-INVENTORY.md`. Do not write to any
      of those. Still read the Write tool's own result wording — "File created successfully" is what you
      expect here, and "has been updated" means the file already existed and a concurrent session may
      have written it, in which case stop and report rather than overwriting.
  </read_first>

  <action>
Create `.planning/phases/120-polish-verified-defects/120-SHELL-EVIDENCE.md` recording, each with
`file:line`, the exact class or rule, and one sentence of reasoning, the things this plan looked at and
deliberately left alone. This is not padding — every
one of them is a thing a later reviewer or sweep will otherwise flag as a miss:

1. `DashboardLayout.tsx` ~:152 and ~:283 — the nav-ICON
   `group-[.is-active]:drop-shadow-[...]` / `group-hover:drop-shadow-[...]` glows. POLISH-01 enumerates
   "per-item nav glow (`nav-active-shadow`/`nav-hover-shadow`)" by class name; these are a third,
   unnamed nav glow on the same items. D-01 keeps them. Hand them to **Phase 124 / SHELL-01**, which
   rebuilds the nav item, and say plainly that a reviewer reading "the nav is quiet now" will still see
   an icon glow.
2. `DashboardLayout.tsx` ~:303 and ~:369 — the two surviving `bg-accent` usages, plus the measured
   repo-wide figure: **50 `bg-accent` sites across `src/`** of which D-05 fixes exactly one. Hand the
   other 49 to **Phase 122 / TOKEN-02**. Include the command you used so 122 can re-run it rather than
   re-derive the list.
3. `DashboardLayout.tsx` ~:243 — `shadow-primary` on the wordmark. It is a Tailwind shadow-COLOR
   utility with no companion `shadow-{size}` class on the element, so it emits a CSS variable that
   nothing consumes and renders nothing. Not enumerated, so not removed; record it as dead-but-inert
   for **Phase 124**.
4. The `index.css` correction: CONTEXT.md's D-04 instructed deleting the Phase-89 suppression rules at
   ~646-655, but ~652-655 suppresses `.crt-scanline-bar`, whose feature survives this phase and whose
   suppression is what keeps `readable` effect-free (`REQUIREMENTS.md:45`). State that D-04 was
   followed for the `.matrix-bg` half and knowingly not followed for the `.crt-scanline-bar` half, and
   why. **This is the one place in the phase where a locked decision was not executed as written** —
   it needs to be visible, not buried in a commit diff.
  </action>

  <verify>
    <automated>test -s .planning/phases/120-polish-verified-defects/120-SHELL-EVIDENCE.md && grep -c 'crt-scanline-bar' .planning/phases/120-polish-verified-defects/120-SHELL-EVIDENCE.md</automated>
  </verify>

  <acceptance_criteria>
    - The file exists and contains all four items with `file:line` references.
    - Item 4 names D-04 explicitly and states which half was not executed. A summary that merely says
      "kept the CRT suppression" without naming the D-04 conflict does not satisfy this.
    - `git status` shows `120-SHELL-EVIDENCE.md` as a NEW file. If it already existed, a concurrent plan wrote to the wrong artifact — report it rather than merging.
  </acceptance_criteria>

  <done>Every decoration this plan saw and left is on the record with an owning phase, and the one deviation from a locked decision is stated in the open.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `localStorage` → shell render | `codepulse-crt` is read with `JSON.parse` on a user-writable store; this plan must not alter that read path |
| stylesheet → every route | `src/index.css` is global, so an over-broad deletion regresses all 5 themes at once and on every page |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-120-03 | Denial of Service | Deleting the `.crt-scanline-bar` suppression rule, re-enabling a CRT scanline overlay inside the `readable` accessibility theme | mitigate | `<interfaces>` names the rule as KEEP and corrects D-04; Task 1's acceptance criteria assert `grep -c 'crt-scanline-bar' src/index.css == 2` as a control |
| T-120-04 | Tampering | A range-based deletion in `index.css` catching `.avatar-glow`, `ping-indicator` or a `prefers-reduced-motion` block | mitigate | Delete by content not by line range; four named control counts must survive unchanged, and the expected line-count delta is stated so an over-delete shows up as a number |
| T-120-05 | Repudiation | Removing the CRT toggle as "kill-list CRT", deleting a feature Phase 124 is scheduled to relocate | mitigate | The toggle, its event plumbing and its default-false read are enumerated as MUST SURVIVE with an unchanged-count assertion; the requirement item is closed by verify-and-record, not by removal |
| T-120-06 | Information Disclosure | `JSON.parse` on an attacker-writable `localStorage` value | accept | Pre-existing behaviour this plan does not touch; a malformed value throws inside the layout's own read, the same blast radius as today, and hardening it is neither enumerated nor in POLISH-01's scope |
| T-120-SC | Tampering | npm/pip/cargo installs | mitigate | No packages are added. If any install becomes necessary, stop and run the Package Legitimacy Gate first. |
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0; `npx vitest run` shows no new failures against the pre-plan baseline.
- `src/index.css`: zero hits for `glitch`, `matrix-bg`, `nav-active-shadow`, `nav-hover-shadow`,
  `scrollbar-thumb`; controls `crt-scanline-bar` (2), `avatar-glow` (2), `ping-indicator` (1),
  `accent: #8b5cf6` (1) all intact.
- `DashboardLayout.tsx`: zero hits for `nav-active-shadow`, `nav-hover-shadow`, `glitch-text`,
  `data-text`, `matrix-bg`, `animate-pulse`; `bg-accent` down from 3 to exactly 2; the nav-icon
  `drop-shadow` count still 2; `font-mono tracking-wider` still present; every `codepulse-crt`
  reference and `crt-scanline-bar` still present.
- The four deliberate non-actions are recorded in `120-SHELL-EVIDENCE.md` with the D-04 deviation
  named.
- **Attended check, not automatable here:** with `dev:noauth` running, toggle CRT on and confirm the
  scanline bar appears, reload and confirm it does not appear by default, then switch to the
  `readable` theme with CRT on and confirm the bar is suppressed. Record the three observations in the
  SUMMARY. Do not report the CRT items as verified from grep alone — a class present in the source is
  not a rendered overlay.
</verification>

<success_criteria>
- No kill-list class has a definition or a usage left in either file.
- The scrollbar is the browser's native one; no replacement styling was added.
- The wordmark is quiet but still `font-mono tracking-wider` (D-03), and the exception is commented.
- Both nav renderings lost the glow classes; the icon glows are untouched and recorded.
- CRT still toggles, still defaults off, still suppressed under `readable` and `aubergine`.
- `--accent` is unchanged in all five theme blocks.
</success_criteria>

<output>
Create `.planning/phases/120-polish-verified-defects/120-02-SUMMARY.md` when done.

**Do not commit.** Shared checkout on `master` with concurrent sessions; the orchestrator owns the
commit. Never `git add -A`, never `git commit --amend`.

**Every line number in this plan is a 2026-08-17 reading, not a spec.** Locate each target by its
content and verify it says what this plan reports before editing. If it does not, the code is right
and the plan is stale — say so in the SUMMARY. The same applies to any class string quoted here:
treat it as a draft to correct, not text to transcribe.
</output>
