---
phase: 99-skill-launch-dispatch
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/skillRun.ts
  - src/lib/profiles.ts
  - src/pages/Reminders.tsx
  - src/hooks/useAstridrChat.ts
  - src/components/forge/ForgeLaunchModal.tsx
autonomous: true
requirements: [LAUNCH-01, LAUNCH-02, LAUNCH-03]
must_haves:
  truths:
    - "The AutoSendHandoff + RunTarget contract exists in one shared module every downstream launch surface imports"
    - "D-08: the persona list lives in ONE source (src/lib/profiles.ts); Reminders.tsx re-exports it — no second drifting copy"
    - "D-01: last-pick persistence helpers (localStorage key codepulse-skills-run-target) exist and validate the stored value"
    - "D-14a: useAstridrChat.sendMessage accepts an optional profile and spreads it onto chat.send only when present"
    - "D-14: the research-directive probes are implemented exactly as resolved — D-14a profile scopes SecurityContext security/DLP policy only (no new astridr endpoint; a true per-turn persona voice is deferred), and D-14b Forge injects /skill verbatim (claude resolves it, codex does not, agy is disabled)"
    - "D-11: ForgeLaunchModal accepts an initialPrompt prop and prefills its prompt textarea with it instead of always resetting to empty"
  artifacts:
    - path: "src/lib/skillRun.ts"
      provides: "RunTarget type, AutoSendHandoff type, last-pick localStorage helpers"
      contains: "AutoSendHandoff"
    - path: "src/lib/profiles.ts"
      provides: "PROFILES array + ProfileId type (hoisted, single source)"
      contains: "PROFILES"
    - path: "src/hooks/useAstridrChat.ts"
      provides: "profile passthrough on sendMessage → chat.send"
      contains: "opts?.profile"
    - path: "src/components/forge/ForgeLaunchModal.tsx"
      provides: "initialPrompt prop prefilling the prompt textarea"
      contains: "initialPrompt"
  key_links:
    - from: "src/hooks/useAstridrChat.ts"
      to: "chat.send WS command"
      via: "sendCommand spread"
      pattern: "opts\\?\\.profile"
    - from: "src/pages/Reminders.tsx"
      to: "src/lib/profiles.ts"
      via: "import + re-export"
      pattern: "from \"@/lib/profiles\""
---

<objective>
Establish every shared contract the three launch paths consume, as small additive changes to existing interfaces. Nothing user-visible ships here; this plan exists so Waves 2–5 build against fixed, defined types instead of exploring the codebase.

Purpose: Interface-first ordering — define the handoff/type/persona contracts once so parallel downstream plans never invent divergent shapes.
Output: `src/lib/skillRun.ts` (new), `src/lib/profiles.ts` (new, hoisted persona source), a `profile` passthrough on `useAstridrChat.sendMessage`, and an `initialPrompt` prop on `ForgeLaunchModal`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/99-skill-launch-dispatch/99-CONTEXT.md
@.planning/phases/99-skill-launch-dispatch/99-RESEARCH.md
@.planning/phases/99-skill-launch-dispatch/99-PATTERNS.md

<interfaces>
<!-- Contracts THIS plan creates. Downstream plans (02–06) import these verbatim; do not redefine. -->

src/lib/skillRun.ts (NEW):
```typescript
export type RunTarget = "chat" | "forge" | "astridr";

// The payload navigate('/chat', { state: { autoSend } }) carries to Chat.tsx.
// Chat.tsx fires the real chat.send AND recordSkillLaunch on confirmed send.
export interface AutoSendHandoff {
  text: string;        // full invocation incl. leading slash + any args, e.g. "/gsd-plan-phase 99"
  skillName: string;   // registry key — Chat.tsx uses it for recordSkillLaunch({ name })
  profile?: ProfileId; // Ástríðr target only; scopes SecurityContext.profile_id (D-14a) — never a persona-voice switch
}

export const RUN_TARGET_STORAGE_KEY = "codepulse-skills-run-target";
export function isValidRunTarget(v: string | null): v is RunTarget;
export function loadStoredRunTarget(): RunTarget;   // "chat" fallback (SSR-safe)
export function storeRunTarget(t: RunTarget): void;
```

src/lib/profiles.ts (NEW — hoisted verbatim from Reminders.tsx:25-36):
```typescript
export type ProfileId = "personal" | "business" | "consulting";
export const PROFILES: { id: ProfileId; label: string; accentVar: string }[] = [
  { id: "personal", label: "Personal", accentVar: "--status-ok" },
  { id: "business", label: "Business", accentVar: "--status-warn" },
  { id: "consulting", label: "Consulting", accentVar: "--status-info" },
];
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create shared contracts — src/lib/skillRun.ts + src/lib/profiles.ts, and rehome PROFILES</name>
  <files>src/lib/skillRun.ts, src/lib/profiles.ts, src/pages/Reminders.tsx</files>
  <read_first>
    - src/pages/Reminders.tsx (lines 25-48, 103-106) — the exact `PROFILES`/`ProfileId` array to move and the `codepulse-reminders-profile` localStorage read-on-mount/write-on-pick shape to mirror for `skillRun.ts`
    - .planning/phases/99-skill-launch-dispatch/99-PATTERNS.md § "localStorage read-on-mount / write-on-pick" and § "PROFILES persona list — reuse verbatim, never redefine (D-08)"
  </read_first>
  <action>
    Create `src/lib/profiles.ts` exporting `ProfileId` and `PROFILES` moved BYTE-IDENTICAL from `Reminders.tsx:25-36` (same 3 ids, labels, and `accentVar` CSS-var tokens `--status-ok`/`--status-warn`/`--status-info`). Then edit `Reminders.tsx` to `import { PROFILES, type ProfileId } from "@/lib/profiles"` and add re-exports (`export { PROFILES }; export type { ProfileId };`) so any existing importer of `PROFILES` from `Reminders` keeps working — grep `from "@/pages/Reminders"` and `from "../pages/Reminders"` for `PROFILES`/`ProfileId` consumers before finalizing and confirm they resolve. This satisfies D-08 (single source, no second copy).
    Create `src/lib/skillRun.ts` with the exact interface in the `<interfaces>` block: `RunTarget`, `AutoSendHandoff`, `RUN_TARGET_STORAGE_KEY = "codepulse-skills-run-target"`, and `isValidRunTarget`/`loadStoredRunTarget` (SSR-guarded `typeof window === "undefined"` → `"chat"`; validate stored value, fallback `"chat"`)/`storeRunTarget` (writes localStorage). Import `ProfileId` from `./profiles` for the `AutoSendHandoff.profile` field.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run src/pages/Reminders.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Source: `src/lib/profiles.ts` exports `PROFILES` with exactly 3 entries whose `accentVar` values are `--status-ok`, `--status-warn`, `--status-info` (grep the file).
    - Source: `Reminders.tsx` no longer defines `PROFILES` inline — it imports from `@/lib/profiles` (grep shows `from "@/lib/profiles"` and no `const PROFILES = [` remains in Reminders.tsx).
    - Source: `src/lib/skillRun.ts` exports `AutoSendHandoff`, `RunTarget`, `RUN_TARGET_STORAGE_KEY`, `isValidRunTarget`, `loadStoredRunTarget`, `storeRunTarget`; `RUN_TARGET_STORAGE_KEY === "codepulse-skills-run-target"`.
    - Behavior: `loadStoredRunTarget()` returns `"chat"` when localStorage is empty or holds an invalid value.
    - `npx tsc --noEmit` is clean and `Reminders.test.tsx` stays green (zero regressions from the rehome).
  </acceptance_criteria>
  <done>Both lib modules exist with the exact contract; Reminders.tsx sources PROFILES from the new lib and re-exports it; tsc + Reminders tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add profile passthrough to useAstridrChat.sendMessage</name>
  <files>src/hooks/useAstridrChat.ts, src/hooks/useAstridrChat.test.ts</files>
  <read_first>
    - src/hooks/useAstridrChat.ts (lines 80-119) — the `sendMessage` opts object (lines 84-96) and the `sendCommand({ type: "chat.send", ... })` optional-spread block (lines 112-119)
    - src/hooks/useAstridrChat.test.ts (existing) — the mock `sendCommand` assertion style to extend
    - .planning/phases/99-skill-launch-dispatch/99-RESEARCH.md § "Ástríðr Persona Routing (D-14a)" — profile scopes SecurityContext.profile_id only; astridr's ChatSendCommand already declares `profile: str | None = None`, so NO astridr-side change is needed
  </read_first>
  <behavior>
    - Test 1: `sendMessage("hi", { profile: "business" })` calls `sendCommand` with an object where `profile === "business"`.
    - Test 2: `sendMessage("hi")` (no opts) calls `sendCommand` with NO `profile` key present (optional-spread, not `profile: undefined`).
  </behavior>
  <action>
    Add `profile?: string;` to `sendMessage`'s `opts` type (after `swapHandled`, with a one-line comment: "D-07/D-14a: scopes SecurityContext.profile_id server-side only — NOT a persona-voice switch"). Add the matching optional spread `...(opts?.profile ? { profile: opts.profile } : {})` to the `sendCommand({ type: "chat.send", ... })` call, following the exact style of the adjacent `swapHandled` spread. No other change to the send/guard/stream logic. Extend `useAstridrChat.test.ts` with the two `<behavior>` cases against the existing mocked `sendCommand`.
  </action>
  <verify>
    <automated>npx vitest run src/hooks/useAstridrChat.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Source: `useAstridrChat.ts` contains `...(opts?.profile ? { profile: opts.profile } : {})` inside the `chat.send` `sendCommand` call.
    - Behavior (test): with `{ profile: "business" }` the sent command object has `profile: "business"`; without opts the sent object has no `profile` key.
    - The pre-existing `useAstridrChat.test.ts` cases stay green (the guard `if (!text.trim() || isStreamingRef.current || status !== "connected") return;` is unchanged).
  </acceptance_criteria>
  <done>sendMessage forwards an optional profile onto chat.send; two new tests pass; existing hook tests unregressed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add initialPrompt prop to ForgeLaunchModal</name>
  <files>src/components/forge/ForgeLaunchModal.tsx, src/components/forge/ForgeLaunchModal.test.tsx</files>
  <read_first>
    - src/components/forge/ForgeLaunchModal.tsx (lines 70-125) — the `ForgeLaunchModalProps` interface, the `useState("")` for `prompt` (line 93), and the reset-on-open `useEffect` (lines 113-125) whose `setPrompt("")` must become `setPrompt(initialPrompt ?? "")`
    - src/components/forge/ForgeLaunchModal.test.tsx (existing) — established render/prop test harness for this modal
    - .planning/phases/99-skill-launch-dispatch/99-PATTERNS.md § "src/components/forge/ForgeLaunchModal.tsx (MODIFY — add initialPrompt prop)"
  </read_first>
  <behavior>
    - Test 1: rendering `<ForgeLaunchModal open initialPrompt="/gsd-plan-phase 99" .../>` shows a prompt field whose value is `"/gsd-plan-phase 99"`.
    - Test 2: rendering with `open` and NO `initialPrompt` shows an empty prompt field (unchanged default).
    - Test 3: toggling `open` false→true re-applies `initialPrompt` (reset effect keyed on `[open, initialPrompt]`).
  </behavior>
  <action>
    Add `initialPrompt?: string;` to `ForgeLaunchModalProps`, destructure it in the component signature, change the reset effect's `setPrompt("")` to `setPrompt(initialPrompt ?? "")`, and add `initialPrompt` to that effect's dependency array (`[open, initialPrompt]`). Do NOT touch any other modal behavior — agent/workspace/mode/host pickers, the `enqueueLaunch` call, Clerk fail-closed auth, and the non-optimistic pending-row pattern are reused wholesale per D-11. Add the three `<behavior>` test cases.
  </action>
  <verify>
    <automated>npx vitest run src/components/forge/ForgeLaunchModal.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Source: `ForgeLaunchModalProps` includes `initialPrompt?: string`; the reset effect calls `setPrompt(initialPrompt ?? "")` and its dep array is `[open, initialPrompt]`.
    - Behavior (test): prompt field renders `initialPrompt` when provided, empty when omitted, and re-applies on reopen.
    - Source: the `enqueueLaunch` mutation call and Clerk-gated auth path are byte-unchanged (grep confirms no edits to the submit handler).
    - All pre-existing ForgeLaunchModal tests stay green.
  </acceptance_criteria>
  <done>ForgeLaunchModal prefills its prompt from initialPrompt without altering any other behavior; new + existing tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CodePulse client → astridr WS (`chat.send`) | The new `profile` field crosses into astridr's SecurityContext |
| CodePulse client → Convex (`enqueueLaunch`) | Forge launch write (unchanged this plan — only prop shape added) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-99-01 | Elevation of Privilege | `useAstridrChat` profile passthrough | mitigate | `profile` is a policy-scope selector, not a capability grant; astridr falls back to `default_profile_id` for unknown values (RESEARCH V5). Callers (Plan 03) constrain it to the `ProfileId` union before send — this plan only adds the passthrough, sends nothing itself. |
| T-99-02 | Tampering | `ForgeLaunchModal` initialPrompt | accept | `initialPrompt` only prefills an editable textarea the user reviews before submit; `enqueueLaunch`'s Clerk fail-closed gate is untouched. No new trust extended. |
| T-99-SC | Tampering | npm installs | accept | Zero package installs this phase (RESEARCH § Package Legitimacy Audit: N/A, `components.json` registries `{}`). No supply-chain surface. |
</threat_model>

<verification>
- `npx tsc --noEmit` clean across the repo.
- `npx vitest run src/lib src/hooks/useAstridrChat.test.ts src/components/forge/ForgeLaunchModal.test.tsx src/pages/Reminders.test.tsx` green.
</verification>

<success_criteria>
- The three shared contracts (`skillRun.ts`, `profiles.ts`, `sendMessage.profile`, `ForgeLaunchModal.initialPrompt`) exist exactly as specified in `<interfaces>`.
- No user-visible behavior changed; no astridr-side change required (ChatSendCommand.profile already exists).
- Zero test regressions.
</success_criteria>

<output>
Create `.planning/phases/99-skill-launch-dispatch/99-01-SUMMARY.md` when done.
</output>
