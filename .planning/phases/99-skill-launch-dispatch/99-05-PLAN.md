---
phase: 99-skill-launch-dispatch
plan: 05
type: execute
wave: 4
depends_on: [99-04]
files_modified:
  - src/components/skills/SkillLifecycleMenu.tsx
  - src/components/skills/SkillLifecycleMenu.test.tsx
  - src/components/skills/QuickDeck.tsx
  - src/components/skills/QuickDeck.test.tsx
autonomous: true
requirements: [LAUNCH-04]
must_haves:
  truths:
    - "D-02: Run appears on the SkillLifecycleMenu (⋯) AND on QuickDeck tiles — NOT on the SkillCommandPalette"
    - "The ⋯ menu's Run item renders in EVERY scope state (dormant/active/shadowed/multi-scope) — Run never disables"
    - "D-03: the QuickDeck primary tile click is Run; clipboard copy survives as a secondary hover icon"
    - "D-13: QuickDeck copy no longer records a launch (onUse is gone) — copy just copies"
    - "The retired passive open-in-chat hover affordance is gone from QuickDeck (superseded by the Chat target in the chooser)"
  artifacts:
    - path: "src/components/skills/SkillLifecycleMenu.tsx"
      provides: "Run submenu (RunTargetItems) as the first menu item + hosted popovers"
      contains: "useRunLaunch"
    - path: "src/components/skills/QuickDeck.tsx"
      provides: "primary click → RunTargetChooser; copy demoted to secondary icon; no usage recording on copy"
      contains: "RunTargetChooser"
  key_links:
    - from: "src/components/skills/SkillLifecycleMenu.tsx"
      to: "RunTargetItems + useRunLaunch (Plan 04)"
      via: "DropdownMenuSub as first item"
      pattern: "RunTargetItems"
    - from: "src/components/skills/QuickDeck.tsx"
      to: "RunTargetChooser (Plan 04)"
      via: "chip is the chooser trigger"
      pattern: "RunTargetChooser"
---

<objective>
Wire the two Run entry points (LAUNCH-04, D-02/D-03): add a Run affordance to the `SkillLifecycleMenu` (⋯) as a submenu of the target chooser, and make the `QuickDeck` primary tile click open the chooser — demoting clipboard-copy to a secondary hover icon that no longer records a launch (D-13). Both consume the Plan 04 chooser/hook; no launch logic is reimplemented here.

Purpose: put Run exactly where D-02 says it lives (⋯ menu + QuickDeck), and enforce D-03's copy-vs-run split + D-13's stop-recording-on-copy at the QuickDeck surface.
Output: modified `SkillLifecycleMenu.tsx`, `QuickDeck.tsx` + updated tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/99-skill-launch-dispatch/99-CONTEXT.md
@.planning/phases/99-skill-launch-dispatch/99-UI-SPEC.md
@.planning/phases/99-skill-launch-dispatch/99-PATTERNS.md

<interfaces>
<!-- Consumed from Plan 99-04 -->
From src/components/skills/RunTargetChooser.tsx: `useRunLaunch(skill)`, `RunTargetItems`, `RunTargetChooser`.
Runtime requirement: both surfaces render inside a `<SkillLaunchProvider>` (mounted by Plan 06 in Skills.tsx). Tests wrap the component under test in `<SkillLaunchProvider>` + `<MemoryRouter>`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Run submenu to SkillLifecycleMenu (D-02)</name>
  <files>src/components/skills/SkillLifecycleMenu.tsx, src/components/skills/SkillLifecycleMenu.test.tsx</files>
  <read_first>
    - src/components/skills/SkillLifecycleMenu.tsx (206-302) — the DropdownMenuContent with the existing `dormant ? … : multiScope ? … : …` branch; the `onCloseAutoFocus={(e)=>e.preventDefault()}` on the content; how `skill`/`displayName` are already in scope
    - src/components/ui/dropdown-menu.tsx — confirm `DropdownMenuSub`, `DropdownMenuSubTrigger`, `DropdownMenuSubContent`, `DropdownMenuSeparator` are exported
    - src/components/skills/RunTargetChooser.tsx (Plan 99-04) — `useRunLaunch`, `RunTargetItems`, and the RunChatPopover/RunAstridrPopover render props to host
    - .planning/phases/99-skill-launch-dispatch/99-UI-SPEC.md § Interaction Contract #1 (Run is the FIRST item, `Play` icon, separated by a separator, never disables)
    - .planning/phases/99-skill-launch-dispatch/99-PATTERNS.md § SkillLifecycleMenu (MODIFY — add Run item)
  </read_first>
  <action>
    Call `const run = useRunLaunch(skill)` near the top of the component. In the `DropdownMenuContent`, add as the FIRST child (before the existing `dormant ? …` branch) a `DropdownMenuSub`: `DropdownMenuSubTrigger` with a Lucide `Play` icon + "Run", and `DropdownMenuSubContent` containing `<RunTargetItems lastTarget={run.lastTarget} onPick={run.pick} />`. Follow with a `DropdownMenuSeparator`, then the existing item branch UNCHANGED. Render the two controlled popovers `<RunChatPopover open={run.chatOpen} onOpenChange={run.setChatOpen} displayName={skill.displayName} invocation={run.invocation} onSubmit={run.submitChat} />` and `<RunAstridrPopover ... onSubmit={run.submitAstridr} />` as siblings of the DropdownMenu (so they survive the menu closing; the `onCloseAutoFocus` guard already on the content prevents the focus fight). Run must render in EVERY branch (dormant/active/shadowed/multi-scope) — it is above the branch, so it is unconditional; do not gate it. Update SkillLifecycleMenu.test.tsx: add a case asserting the "Run" subtrigger renders in both a dormant and an active skill fixture, and that opening it shows the three target labels.
  </action>
  <verify>
    <automated>npx vitest run src/components/skills/SkillLifecycleMenu.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Source: a `DropdownMenuSub` with a `Play` icon + "Run" is the first item in `DropdownMenuContent`, followed by `DropdownMenuSeparator`, then the pre-existing branch (grep order).
    - Behavior (test): "Run" subtrigger is present for both a dormant fixture and an active fixture (Run never disables); expanding it shows "Send to Chat"/"Launch as Forge Agent"/"Dispatch to Ástríðr".
    - Source: the existing archive/restore/move/delete branch is byte-unchanged aside from being preceded by the new sub + separator.
    - Existing SkillLifecycleMenu tests stay green.
  </acceptance_criteria>
  <done>The ⋯ menu exposes Run (target submenu) as its first, always-available item in every scope state; tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: QuickDeck — primary click → Run, copy secondary, stop recording (D-03/D-13)</name>
  <files>src/components/skills/QuickDeck.tsx, src/components/skills/QuickDeck.test.tsx</files>
  <read_first>
    - src/components/skills/QuickDeck.tsx (whole file) — `QuickDeckProps` (onUse line 14, onOpenInChat line 15), `handleCopy` (32-45) which currently calls `onUse(skill.name)` at line 44, the primary chip `onClick={() => handleCopy(skill)}` (72), and the hover-reveal `MessageSquare` open-in-chat button (88-95)
    - src/components/skills/RunTargetChooser.tsx (Plan 99-04) — `RunTargetChooser` (chip is `asChild` trigger)
    - .planning/phases/99-skill-launch-dispatch/99-UI-SPEC.md § Interaction Contract #1 (QuickDeck: primary click → Run; hover icons `[Copy, Star]`; open-in-chat retired) + § Copywriting Contract (copy-icon tooltip "{invocation} — click to copy")
    - .planning/phases/99-skill-launch-dispatch/99-PATTERNS.md § QuickDeck (MODIFY)
    - src/components/skills/QuickDeck.test.tsx (whole) — existing assertions that copy calls `onUse` and that the hover button calls `onOpenInChat`; these must be inverted/removed
  </read_first>
  <action>
    Remove `onUse` and `onOpenInChat` from `QuickDeckProps` and the component signature (D-13: copy no longer records; open-in-chat retired). In `handleCopy`, DELETE the `onUse(skill.name)` call (line 44) — copy now only writes the clipboard and shows the transient "copied"/"copy failed" state (keep that honest-state feedback). Wrap the primary chip button in `<RunTargetChooser skill={skill}>…chip…</RunTargetChooser>` so the primary click opens the target chooser (Run becomes the default gesture, D-03) — the chip stays visually identical but its click now opens the chooser instead of copying. Move clipboard-copy to the hover-reveal group: replace the `MessageSquare` (open-in-chat) button with a Lucide `Copy` button that calls `handleCopy(skill)` (tooltip/aria "Copy invocation for {name}" / title "{invocation} — click to copy"); keep the `Star` favorite button unchanged. Update QuickDeck.test.tsx: remove/invert the `onUse`-on-copy and `onOpenInChat` assertions; add (1) clicking a chip opens the chooser (asserts a chooser item like "Send to Chat" appears), (2) clicking the secondary Copy icon writes the clipboard (mock `navigator.clipboard.writeText`) and does NOT record any launch (no `recordSkillLaunch`/`onUse` in scope), (3) the favorite toggle still fires. Wrap the render in `<SkillLaunchProvider><MemoryRouter>`.
  </action>
  <verify>
    <automated>npx vitest run src/components/skills/QuickDeck.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Source: `QuickDeckProps` no longer declares `onUse` or `onOpenInChat`; `handleCopy` no longer calls `onUse` (grep confirms neither identifier remains).
    - Source: the primary chip is wrapped by `RunTargetChooser`; the hover group renders a `Copy` button (calling `handleCopy`) and the `Star` button — no `MessageSquare`.
    - Behavior (test): clicking the chip surfaces the chooser (a target label appears); clicking Copy writes the clipboard and records nothing; favorite toggle still works.
    - The transient "copied"/"copy failed" honest feedback is preserved.
  </acceptance_criteria>
  <done>QuickDeck's primary gesture is Run via the chooser; copy is a secondary non-recording icon; open-in-chat is retired; tests updated and green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| QuickDeck / ⋯ menu → chooser | User initiates a launch; no new server boundary crossed here (delegates to Plan 04 paths) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-99-13 | Repudiation (integrity of usage metric) | QuickDeck copy | mitigate | Removing `onUse`-on-copy makes `useCount` reflect real runs only (D-13/LAUNCH-04) — a copy can no longer inflate the metric. |
| T-99-14 | Tampering | Run affordance availability | accept | Run rendering unconditionally in the ⋯ menu is intentional (launching is non-destructive, does not touch the filesystem like lifecycle actions); no new write boundary. |
</threat_model>

<verification>
- `npx vitest run src/components/skills/SkillLifecycleMenu.test.tsx src/components/skills/QuickDeck.test.tsx` green.
- `npx tsc --noEmit` clean (note: Skills.tsx will still pass the now-removed QuickDeck props until Plan 06 lands — this is expected; Plan 06 is the same-milestone follow-on in Wave 5. If tsc is run repo-wide before Plan 06, the only errors are the soon-removed Skills.tsx→QuickDeck prop passes).
</verification>

<success_criteria>
- Run is present on the ⋯ menu (always) and as the QuickDeck primary click; copy is a secondary non-recording icon; open-in-chat is gone from QuickDeck.
</success_criteria>

<output>
Create `.planning/phases/99-skill-launch-dispatch/99-05-SUMMARY.md` when done.
</output>
