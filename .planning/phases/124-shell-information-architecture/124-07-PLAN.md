---
phase: 124-shell-information-architecture
plan: 07
type: execute
wave: 5
depends_on: ["124-06"]
files_modified:
  - src/layouts/DashboardLayout.tsx
  - src/layouts/__tests__/DashboardLayout.test.tsx
autonomous: true
requirements: [SHELL-01]

must_haves:
  truths:
    - "D-07: the header right zone keeps six items and only the settings-shaped controls move into the overflow menu"
    - "D-07 (amended 2026-08-21): the overflow menu holds EXACTLY four items — theme, privacy, CRT, ambient audio. Help is DEFERRED and must not be built"
    - "ThemeSwitcher keeps its lazy() boundary and Suspense fallback — only the mount location moves, and this is verified manually because no bundle-size CI gate exists in this repo"
    - "The overflow trigger is icon-only with aria-label='More options' and a 32x32px hit area; Radix supplies aria-expanded, aria-haspopup and keyboard navigation"
  artifacts:
    - path: "src/layouts/DashboardLayout.tsx"
      provides: "Header overflow DropdownMenu holding four relocated controls"
      contains: "More options"
  key_links:
    - from: "src/layouts/DashboardLayout.tsx"
      to: "src/components/ui/dropdown-menu.tsx"
      via: "DropdownMenu / DropdownMenuTrigger / DropdownMenuContent / DropdownMenuItem"
      pattern: "DropdownMenuTrigger"
---

<objective>
Consolidate the header's right zone: build the `⋯` overflow menu and move the four settings-shaped
controls into it, without losing `ThemeSwitcher`'s lazy chunk boundary.

Purpose: D-07's consolidation is the mechanism D-06's header-height gate is betting on — today's
right zone holds ten elements (eight controls plus two dividers) and POLISH-06 measured 981px of
min-content against 660px available at a 900px viewport. This plan removes four of them from the
row.
Output: a four-item `⋯` menu, the right zone down to six visible items, and a recorded manual
verification of the lazy boundary.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/124-shell-information-architecture/124-CONTEXT.md
@.planning/phases/124-shell-information-architecture/124-UI-SPEC.md
@.planning/phases/124-shell-information-architecture/124-VALIDATION.md
@src/layouts/DashboardLayout.tsx
@src/components/ui/dropdown-menu.tsx

**The planning documents are CLAIMS. The live code is EVIDENCE.** If the repository contradicts an
instruction below, the repository wins — correct the plan in your SUMMARY.

<interfaces>
Live line references read 2026-08-21; plans 124-05 and 124-06 have since edited this file, so
re-locate rather than trusting the numbers.

Right zone today (`DashboardLayout.tsx:601-619`), in DOM order:
`EStopButton` → divider → `SectionErrorBoundary name="Active Brain"`/`BrainHeaderBadge` →
`NotificationBell` → `PrivacyShield` → `Suspense`/`ThemeSwitcher` → `CrtToggle` →
`AmbientAudioPlayer` → divider → `UserMenu`.

**Stays visible (six, D-07):** brain badge, system chip (built in plan 124-08), notification bell,
E-STOP, `⋯`, user menu.
**Moves into `⋯` (four, D-07 as amended):** theme switcher, privacy shield, ambient audio, CRT
toggle.
**"Help" is DEFERRED — do not build it.** No Help control exists anywhere in the app
(`grep -niE "help|circlehelp" src/layouts/DashboardLayout.tsx` returns 0; control: the same search
for `ThemeSwitcher` returns 4, so the probe discriminates). `REQUIREMENTS.md` SHELL-01 was
corrected for this on 2026-08-21 (commit `286c2d51`). Sketch §7 and D-07's original prose both
name it; both are superseded.

**Right-zone left-to-right order is the plan's call** (UI-SPEC Open Question #3 — D-07 fixes the
SET of six, not their order). Default: keep `EStopButton` first, minimising JSX churn.

`ThemeSwitcher`'s lazy boundary — the thing that must not break:
- declaration at `:15-17`: `const ThemeSwitcher = lazy(() => import("../components/ThemeSwitcher").then((m) => ({ default: m.ThemeSwitcher })))`, with a DEBT-03 comment above it recording that it is the entry chunk's only consumer of `@radix-ui/react-select` (42,988 bytes).
- mount at `:611-613`: `<Suspense fallback={<div className="w-9 h-9" aria-hidden="true" />}><ThemeSwitcher /></Suspense>`.
Only the MOUNT relocates. The `lazy()` declaration and the import statement must not change at all.

`CrtToggle` is a local component in this same file (`:314-347`) taking `{ crtEnabled, setCrtEnabled }`
from state declared at `:362`. `PrivacyShield` and `AmbientAudioPlayer` are plain default imports
with no props.

`DropdownMenu` primitives live at `src/components/ui/dropdown-menu.tsx` (255 lines, Radix-backed,
already vendored). They are NOT currently imported by `DashboardLayout.tsx` — this phase is their
first use here.

**No bundle-size CI gate exists in this repository.** `grep -ciE "size-limit|bundlesize|bundle-size"`
returns 0 against `package.json` and 0 against `.github/workflows/ci.yml` (control: `grep -ci npm`
on the same workflow returns 2, so the probe discriminates). Nothing will automatically catch an
entry-chunk regression, which is why Task 2 exists and is explicitly manual.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the overflow menu and relocate the four controls</name>
  <read_first>
    - `src/layouts/DashboardLayout.tsx` — the right-zone block (currently `:601-619`), the
      `ThemeSwitcher` lazy declaration (`:15-17`), and the local `CrtToggle` (`:314-347`)
    - `src/components/ui/dropdown-menu.tsx` (full, 255 lines — the exported primitive names and
      their prop surfaces)
    - `.planning/phases/124-shell-information-architecture/124-UI-SPEC.md` §"Zone 3 — Right"
    - `src/components/skills/SkillLifecycleMenu.tsx` or any existing `DropdownMenu` call site in
      `src/` (find one with `grep -rl "DropdownMenuTrigger" src --include=*.tsx`) for this repo's
      composition idiom
  </read_first>
  <files>src/layouts/DashboardLayout.tsx</files>
  <action>
    Add a `DropdownMenu` to the header's right zone. Trigger: an icon-only button using Lucide
    `Ellipsis` (or `MoreHorizontal`), `aria-label="More options"`, sized to a **32×32px hit area**
    — the UI-SPEC's Spacing correction establishes that this is a check to perform, not a property
    to inherit: the two existing icon-only controls in this file (hamburger `:554-560`, mobile
    close-X `:516-522`) measure 24×24 (`p-1` + `h-4 w-4`), which clears WCAG 2.2's floor with zero
    margin and does NOT meet 32×32. State the exact utility classes you used and the arithmetic
    that reaches 32px in your SUMMARY.

    Do NOT hand-write `aria-expanded` or `aria-haspopup`, and do NOT implement keyboard navigation
    — Radix's `DropdownMenu` provides arrow keys, Home/End, typeahead and Esc for free. Verify
    that it does rather than reimplementing it.

    Move these four existing controls out of the right-zone flex row and into
    `DropdownMenuContent`, in this order: theme switcher, privacy shield, CRT toggle, ambient
    audio. Each is COMPOSED, not reimplemented — they keep their own internal state and
    `localStorage` persistence untouched; only their mount location moves. `CrtToggle` keeps
    receiving `crtEnabled` / `setCrtEnabled` from the same state.

    Wrap each control in a `DropdownMenuItem` with `onSelect={(e) => e.preventDefault()}` so
    operating the control does not close the menu. **`ThemeSwitcher` is itself a Radix `Select`,
    and a Select nested inside a DropdownMenu is a known-awkward composition (nested portals,
    focus scope conflicts).** Verify interactively that the theme can actually be changed from
    inside the menu. If it cannot, fall back to rendering the four controls in a plain `<div>`
    inside `DropdownMenuContent` without `DropdownMenuItem` wrappers — keyboard access then comes
    from each control's own focus handling. Say in your SUMMARY which shape you shipped and what
    you observed that decided it. Do not assume either works.

    Remove the two `w-px h-4 bg-primary/20` divider spans (`:604`, `:616`) — with four controls
    gone they separate nothing, and they are two of the ten elements D-06's re-measurement counts.

    Do NOT add a Help item. Do NOT move `EStopButton`, `BrainHeaderBadge`, `NotificationBell` or
    `UserMenu` out of the visible row. Do NOT touch the header's height classes
    (`min-h-14 flex-wrap gap-y-1`) or the long POLISH-06 comment above the `<header>` — both belong
    to plan 124-10's measurement gate, and changing them here would pre-empt a decision that is
    supposed to be made on a measurement.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run src/layouts/__tests__/DashboardLayout.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "More options" src/layouts/DashboardLayout.tsx` returns 1.
    - `grep -c "DropdownMenuContent" src/layouts/DashboardLayout.tsx` returns 1.
    - `grep -c "DropdownMenuItem" src/layouts/DashboardLayout.tsx` returns 4, OR 0 with the SUMMARY
      recording the plain-`<div>` fallback and the observation that forced it.
    - `grep -ciE "help|circlehelp" src/layouts/DashboardLayout.tsx` returns 0.
    - `grep -cF "w-px h-4 bg-primary/20" src/layouts/DashboardLayout.tsx` returns 0.
    - `grep -cE "aria-expanded|aria-haspopup" src/layouts/DashboardLayout.tsx` returns 0.
    - `git diff -- src/layouts/DashboardLayout.tsx | grep -c "^[-+].*min-h-14"` returns 0 — the
      header height was not touched by this plan.
    - `npx tsc --noEmit` exits 0.
    - SUMMARY states the `⋯` trigger's classes and the arithmetic reaching 32×32px.
  </acceptance_criteria>
  <done>The right zone holds six items and the four settings-shaped controls live in a working `⋯` menu.</done>
</task>

<task type="auto">
  <name>Task 2: MANUAL-ONLY — verify the ThemeSwitcher lazy boundary survived the move</name>
  <read_first>
    - `src/layouts/DashboardLayout.tsx:15-17` (the `lazy()` declaration and its DEBT-03 comment)
    - The relocated `<Suspense><ThemeSwitcher/></Suspense>` mount site from Task 1
    - `.planning/phases/124-shell-information-architecture/124-VALIDATION.md`
      §"Manual-Only Verifications" (row 1)
  </read_first>
  <files>src/layouts/DashboardLayout.tsx</files>
  <action>
    **This verification is MANUAL. There is no automated gate for it in this repository** — the
    absence of a bundle-size check was confirmed with a discriminating control (see this plan's
    `<interfaces>` block). If the `lazy()` boundary is collapsed, `@radix-ui/react-select` lands in
    the entry chunk and nothing in CI will say so.

    Run `git diff -- src/layouts/DashboardLayout.tsx` and inspect it for these three facts,
    reporting each with the diff evidence:
    1. The `import` region around the `lazy()` declaration is UNCHANGED — no `+`/`-` line touches
       `const ThemeSwitcher = lazy(` or its `import("../components/ThemeSwitcher")` call. A `git
       diff` hunk touching those lines is the warning sign Pitfall 4 names.
    2. The `<Suspense fallback={...}>` wrapper still surrounds `<ThemeSwitcher />` at its new
       location, and the fallback is still a fixed-size placeholder so the header does not reflow
       when the chunk arrives.
    3. No new STATIC `import { ThemeSwitcher }` was added anywhere in the file.

    Then take the corroborating build measurement, which is cheap even without a CI gate: run
    `npm run build` and record the entry-chunk filename and byte size from Vite's output, plus
    whether a separate `ThemeSwitcher`-named chunk is emitted. Record the numbers verbatim. If you
    can also obtain the same numbers from the pre-change tree (e.g. by stashing), a before/after
    pair is far stronger than a single figure — but do not fabricate a baseline you did not
    measure; if you only have the after figure, say so.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - SUMMARY states plainly that this verification is manual and that no bundle-size CI gate
      exists, citing the two `grep -ci` results and their control.
    - `git diff -- src/layouts/DashboardLayout.tsx | grep -c "^[-+].*lazy(" ` returns 0.
    - `grep -cE "^import \{ ?ThemeSwitcher" src/layouts/DashboardLayout.tsx` returns 0.
    - `grep -c "Suspense" src/layouts/DashboardLayout.tsx` is unchanged from its pre-plan value
      (quote both).
    - `npm run build` exits 0, and the SUMMARY records the entry-chunk name and byte size, marked
      as either a before/after pair or an after-only figure.
  </acceptance_criteria>
  <done>The lazy boundary is provably intact, verified by diff inspection and a recorded build, with the absence of an automated gate stated plainly.</done>
</task>

<task type="auto">
  <name>Task 3: Assert the menu's exact contents and the visible right-zone set</name>
  <read_first>
    - `src/layouts/__tests__/DashboardLayout.test.tsx` (full, as left by plan 124-06 — note the
      mocks at `:54-56` stub `PrivacyShield`, `ThemeSwitcher` and `AmbientAudioPlayer` as
      `() => null`, so an assertion based on their rendered output will measure nothing; give each
      stub identifiable content before asserting on it)
    - `src/components/skills/RunTargetChooser.test.tsx:15-30` (the per-file `ResizeObserver`
      polyfill — Radix `DropdownMenu` needs it in jsdom; plan 124-05 may have already added it,
      check before duplicating)
    - `src/layouts/DashboardLayout.tsx` as modified in Task 1
  </read_first>
  <files>src/layouts/__tests__/DashboardLayout.test.tsx</files>
  <behavior>
    - The `⋯` trigger renders with the accessible name "More options" and is present on every
      render of the layout.
    - Before the menu is opened, none of the four relocated controls is in the document.
    - After activating the trigger, exactly the four relocated controls are present — and NO Help
      item is present (assert the absence explicitly; the deferral is a decision worth guarding,
      since sketch §7 still names it and a future reader may "restore" it).
    - The six visible right-zone items are still in the document without opening the menu.
  </behavior>
  <action>
    Change the `vi.mock` stubs for `PrivacyShield`, `ThemeSwitcher` and `AmbientAudioPlayer` from
    `() => null` to a stub rendering an identifiable `data-testid` — a `() => null` stub cannot be
    distinguished from "the control is not mounted", so asserting against it would be a probe that
    returns the same result whether the feature works or not. Say in the SUMMARY that you changed
    the stubs and why.

    Write the four behaviour cases. Open the menu with a click or keyboard activation on the
    trigger via Testing Library, then assert the four testids are present. Assert Help's absence by
    a case-insensitive text query returning null.

    Prove the "before opening" case discriminates: it must genuinely fail if the controls were left
    in the visible row. Verify by temporarily moving ONE control back out of `DropdownMenuContent`
    into the right-zone row, re-running, confirming that case FAILS, then restoring. Record both
    runs in the SUMMARY — without this, a passing "not in the document" assertion is
    indistinguishable from a broken stub.
  </action>
  <verify>
    <automated>npx vitest run src/layouts/__tests__/DashboardLayout.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/layouts/__tests__/DashboardLayout.test.tsx` exits 0 with all four new
      cases and every pre-existing test passing.
    - `grep -cF "More options" src/layouts/__tests__/DashboardLayout.test.tsx` returns ≥ 1.
    - `grep -ciE "\bhelp\b" src/layouts/__tests__/DashboardLayout.test.tsx` returns ≥ 1 (the
      absence assertion exists).
    - `grep -cF "() => null" src/layouts/__tests__/DashboardLayout.test.tsx` decreased by 3
      (quote before/after).
    - SUMMARY records the control-moved-back run showing the "before opening" case FAILING, and
      the restored run showing green.
  </acceptance_criteria>
  <done>The menu's exact four-item contents, Help's absence, and the six visible items are asserted by tests shown to discriminate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none newly crossed | This plan relocates four existing client-side controls into a menu. No new data source, no new persistence key, no new network call. Each control keeps its own existing `localStorage` interaction unchanged. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-124-07-01 | Denial of service | `@radix-ui/react-select` returning to the entry chunk if `ThemeSwitcher`'s `lazy()` boundary collapses during the move | mitigate | Task 2's manual diff review with three explicit diff assertions plus a recorded `npm run build`. Flagged as manual because `grep -ciE "size-limit\|bundlesize"` returns 0 in both `package.json` and `.github/workflows/ci.yml` against a discriminating control — nothing in CI will catch it. |
| T-124-07-02 | Elevation of privilege | Burying a control behind a menu that changes who can reach it | accept | All four relocated controls are per-browser presentation preferences (theme, PII masking, CRT effect, ambient audio). None gates data access or a privileged action; moving them changes discoverability only. `PrivacyShield` masks PII in the local render and remains one click away. |
| T-124-07-03 | Repudiation | Building a Help entry nobody scoped | mitigate | Task 1 forbids it, an acceptance criterion greps for zero `help` matches in the component, and Task 3 asserts its absence in a test so a future "restoration" fails loudly. |
| T-124-07-04 | Denial of service | Losing keyboard access to four controls | mitigate | Radix `DropdownMenu` supplies arrow/Home/End/typeahead/Esc; the plan forbids reimplementing it and requires interactive verification of the nested-Select case, with a named fallback shape if it fails. |
| T-124-07-SC | Tampering | npm/pip/cargo installs | accept | Zero installs; `dropdown-menu.tsx` is already vendored; `components.json` → `"registries": {}`. |
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0.
- `npx vitest run src/layouts/__tests__/DashboardLayout.test.tsx` green.
- `npm run build` exits 0 with the entry-chunk figure recorded.
- `npm test` green at plan end.
- SUMMARY records: the nested-Select interactive observation, the `⋯` hit-area arithmetic, the
  lazy-boundary diff evidence, and the control-moved-back mutation run.
</verification>

<success_criteria>
The header's right zone holds six visible items; theme, privacy, CRT and ambient audio live in a
keyboard-navigable `⋯` menu with no Help entry; and `ThemeSwitcher` still loads from its own chunk.
</success_criteria>

<output>
Create `.planning/phases/124-shell-information-architecture/124-07-SUMMARY.md` when done.
</output>
