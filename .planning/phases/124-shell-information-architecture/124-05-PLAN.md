---
phase: 124-shell-information-architecture
plan: 05
type: execute
wave: 3
depends_on: ["124-04"]
files_modified:
  - src/layouts/DashboardLayout.tsx
  - src/layouts/__tests__/DashboardLayout.test.tsx
autonomous: true
requirements: [SHELL-02]

must_haves:
  truths:
    - "D-04: System is health-plus-inventory and Settings does NOT move into the nav — it stays footer-pinned inside the flex column"
    - "D-14: per-domain collapse exists AND the whole-sidebar 48px rail collapse survives and overrides it — restoring the rail restores the domain state"
    - "D-15: domain state persists to localStorage['codepulse-nav-domains'] as four booleans, all open by default, using the existing try/catch idiom"
    - "D-17 (first half): the sidebar is 232px expanded, asserted by a real test replacing DashboardLayout.test.tsx's abandoned test.todo"
    - "The active nav item keeps aria-current=page — after the weight collapse to 400/600 it is the only NON-VISUAL active signal"
    - "Nav labels are sentence-case Geist 13px weight 400; domain headers are the 11px mono uppercase eyebrow in --muted-foreground with no glow"
  artifacts:
    - path: "src/layouts/DashboardLayout.tsx"
      provides: "4 collapsible domains, 232px sidebar, 2px --primary active rail + 6% tint"
      contains: "codepulse-nav-domains"
    - path: "src/layouts/__tests__/DashboardLayout.test.tsx"
      provides: "232px width assertion, 4-domain assertion, aria-current assertion, persistence assertion"
  key_links:
    - from: "src/layouts/DashboardLayout.tsx"
      to: "src/lib/navRegistry.ts"
      via: "navGroups.map into per-domain Collapsible sections"
      pattern: "navGroups"
    - from: "src/layouts/DashboardLayout.tsx"
      to: "src/components/ui/collapsible.tsx"
      via: "Collapsible / CollapsibleTrigger / CollapsibleContent"
      pattern: "CollapsibleTrigger"
---

<objective>
Rebuild the sidebar's structure and typography: 232px wide, four collapsible domains, the eyebrow
domain header, sentence-case nav labels, and the 2px `--primary` active rail with a 6% tint.

Purpose: this is SHELL-02's visible half. The count badges are deliberately NOT in this plan —
they land in 124-06 so this one stays inside its context budget.
Output: a four-domain sidebar with persisted per-domain state, and the first real assertions in a
test block that has held `test.todo` stubs since Phase 96.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/124-shell-information-architecture/124-CONTEXT.md
@.planning/phases/124-shell-information-architecture/124-UI-SPEC.md
@.planning/phases/124-shell-information-architecture/124-PATTERNS.md
@src/layouts/DashboardLayout.tsx
@src/components/ui/collapsible.tsx
@src/lib/navRegistry.ts

**The planning documents are CLAIMS. The live code is EVIDENCE.** Where they disagree, the code
wins — correct the plan in your SUMMARY rather than transcribing it.

<interfaces>
Live line references, read 2026-08-21 — re-check them, plan 124-04 has already edited
`navRegistry.ts` and line numbers in `DashboardLayout.tsx` may have shifted.

- `NavGroup` component: `DashboardLayout.tsx:75-141`. Domain header block `:87-95`; nav item
  `NavLink` `:101-124`; collapsed-rail Tooltip branch `:125-136`.
- `SidebarContent`'s `<nav>`: `:228-236` — maps `navGroups`, inserting a `<Separator>` between
  groups.
- Footer-pinned Settings: `:238-269` — **do not move it** (D-04).
- Rail collapse state + `localStorage` read idiom: `:353-359`; write idiom `:492-496`.
- Desktop `<aside>` width: `:489` — `${sidebarCollapsed ? "w-[48px]" : "w-60"}`.
- Mobile drawer `<aside>` width: `:501` — `w-60`.
- `Collapsible` primitive: `src/components/ui/collapsible.tsx` (31 lines) — Radix-backed, provides
  `aria-expanded` on the trigger natively. Already vendored; do not install anything.

**Design contract values** (from `124-UI-SPEC.md`, which is locked):
- Sidebar 232px expanded, 48px collapsed rail.
- Domain header (eyebrow): 11px, weight **600**, JetBrains Mono, uppercase, `letter-spacing:
  0.08em`, colour `--muted-foreground`, **glow removed**.
- Nav item label: 13px, weight **400**, Geist, **sentence case**, line-height 1.4.
- Weight budget for new chrome is exactly two weights: 400 and 600. No 500, no 700.
- Active item: 2px `--primary` left rail + 6% tint
  `color-mix(in oklab, var(--primary) 6%, transparent)`. **No weight step** — the rail and tint are
  the entire visual distinction, so do not reach for `font-medium` on the active item.
- Hover stays exactly as today (`hover:text-primary hover:bg-primary/5`) — explicitly carried
  forward by the UI-SPEC's accent list, item 2.

**Kill-list items in the current sidebar that this plan removes** (POLISH-01, restated by the
UI-SPEC's Typography section): the domain header's `drop-shadow(...)` text glow and its
`text-primary` colour (`:91`), the small `bg-primary/50` dot beside it (`:90`), the per-nav-item
icon and label `drop-shadow` glows (`:118`, `:121`), and the `font-mono tracking-wider` uppercase
treatment of nav labels (`:109`). The collapsed-rail Tooltip's own `font-mono uppercase
tracking-widest text-primary` styling (`:131`) is a TOOLTIP, not a nav label — leave it alone and
say so in the SUMMARY, so a reviewer knows it was considered rather than missed.

`color-mix(in oklab, var(--primary) N%, transparent)` is already an established idiom in this repo
(`src/index.css:843`, `:861-862`). In a Tailwind arbitrary value, spaces become underscores.

There is currently **no `::before` pseudo-element usage anywhere in `src/**/*.tsx`** (grepped,
zero hits) and no `.is-active` rule in `src/index.css` (grepped, zero hits — the class exists only
as a marker for `group-[.is-active]:` variants). So the rail is new geometry either way.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Per-domain Collapsible with persisted state and rail override</name>
  <read_first>
    - `src/layouts/DashboardLayout.tsx:75-141` (`NavGroup`) and `:228-236` (the `<nav>` map)
    - `src/layouts/DashboardLayout.tsx:353-359` and `:492-496` (the localStorage read/write idiom
      to copy verbatim — the `sidebarCollapsed` one, NOT the `codepulse-crt` one at `:362-383`,
      which adds cross-tab sync this key does not need)
    - `src/components/ui/collapsible.tsx` (full, 31 lines)
    - `src/lib/navRegistry.ts` (post-124-04 — the four sentence-case domains)
  </read_first>
  <files>src/layouts/DashboardLayout.tsx</files>
  <action>
    Add state for per-domain open/closed in the component that owns the sidebar. Key:
    `localStorage["codepulse-nav-domains"]`, value a JSON object of four booleans keyed by the
    lowercase domain name (`command`, `observe`, `agents`, `system`), default all `true`. Copy the
    `try { JSON.parse(localStorage.getItem(KEY) ?? DEFAULT_JSON) } catch { DEFAULT }` initializer
    shape from `:353-359` and the `setState(next); localStorage.setItem(KEY, JSON.stringify(next))`
    write shape from `:492-496`. Do not add a `storage` event listener — no other surface reads
    this key.

    Both `<aside>` elements render `SidebarContent`, and both are mounted simultaneously (desktop
    `hidden md:flex`, mobile drawer). Lift the domain state to whichever scope keeps the two
    instances in agreement, or read the same `localStorage` key in each — state which you chose and
    why in the SUMMARY, and assert in a test that toggling in one does not leave the other stale if
    you chose shared state.

    Rewrite `NavGroup` so its body is a `Collapsible` whose `open` is the domain's boolean and
    whose `onOpenChange` writes the persisted object. The domain header becomes the
    `CollapsibleTrigger` wrapping the eyebrow label; the item list becomes `CollapsibleContent`.
    Do NOT add `aria-expanded` by hand — Radix's `Collapsible` provides it, and a hand-rolled one
    would fight the primitive. Add a chevron affordance (Lucide `ChevronDown`, rotated when
    closed) inside the trigger; `ChevronsLeft`/`ChevronsRight` are already imported for the rail
    toggle, so import `ChevronDown` alongside them.

    Preserve D-14's override: when the whole-sidebar rail is collapsed (`collapsed === true`), the
    domain header is NOT rendered as a trigger — keep today's `collapsed && <div className="pt-3" />`
    icon-divider shape (`:96`) — and every item renders regardless of its domain's boolean. The
    per-domain state is held in memory and in `localStorage` but not applied at 48px, so expanding
    the rail restores whatever domain state was already there. Nothing is reset by collapsing or
    expanding the rail.

    Set the desktop `<aside>` expanded width at `:489` from `w-60` to `w-[232px]`, and the mobile
    drawer `<aside>` at `:501` from `w-60` to `w-[232px]`. Leave the collapsed `w-[48px]`, the
    `transition-[width] duration-normal ease-house`, and every other class on both elements alone.

    Leave the footer-pinned Settings block (`:238-269`), the rail toggle button (`:271-279`), the
    connection dot (`:280-292`) and the avatar dialog untouched (D-04).
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run src/layouts/__tests__/DashboardLayout.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "codepulse-nav-domains" src/layouts/DashboardLayout.tsx` returns ≥ 2 (read + write).
    - `grep -c "CollapsibleTrigger" src/layouts/DashboardLayout.tsx` returns ≥ 1 and
      `grep -c "CollapsibleContent" src/layouts/DashboardLayout.tsx` returns ≥ 1.
    - `grep -cE "aria-expanded" src/layouts/DashboardLayout.tsx` returns 0 — the attribute comes
      from Radix, not from hand-written markup.
    - `grep -cF "w-60" src/layouts/DashboardLayout.tsx` returns 0.
    - `grep -cF "w-[232px]" src/layouts/DashboardLayout.tsx` returns 2.
    - `grep -c "to=\"/settings\"" src/layouts/DashboardLayout.tsx` returns 1 and it is still inside
      the footer block below the `<nav>` (verify by reading; quote the line number in the SUMMARY).
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>Four domains collapse independently, persist to one localStorage key, and are overridden by the 48px rail without losing their state.</done>
</task>

<task type="auto">
  <name>Task 2: Eyebrow header, sentence-case labels, 2px active rail + 6% tint</name>
  <read_first>
    - `src/layouts/DashboardLayout.tsx:87-136` as modified in Task 1 (the header block, the
      `NavLink` className function, the icon and label spans, the collapsed Tooltip branch)
    - `.planning/phases/124-shell-information-architecture/124-UI-SPEC.md` — the Typography table,
      the Color section's "Accent reserved for" list, and the Sidebar Contract's
      "Active item styling"
    - `src/index.css` around `:838-865` (the established `color-mix(in oklab, var(--primary) N%,
      transparent)` idiom)
  </read_first>
  <files>src/layouts/DashboardLayout.tsx</files>
  <action>
    Domain header (eyebrow): delete the `w-1 h-1 rounded-full bg-primary/50` dot span, and restyle
    the label from `text-xs uppercase tracking-widest text-primary font-mono font-bold
    drop-shadow-[...]` to 11px / `font-mono` / `uppercase` / `tracking-[0.08em]` /
    `font-semibold` / `text-muted-foreground`, with the `drop-shadow-[...]` glow REMOVED entirely.
    The registry now stores sentence-case domain names, so the uppercase comes from CSS.

    Nav item label: remove `font-mono` and `tracking-wider` from the `NavLink` className at `:109`,
    set the size to 13px with `leading-[1.4]` and weight 400. Remove the `drop-shadow` glow from
    the icon (`:118`) and from the label span (`:121`) — both are kill-list "per-nav-item glow"
    shapes. Keep the icon's `h-4 w-4 shrink-0` and its `transition-all duration-slow ease-house`.

    Active/inactive/hover treatment:
    - inactive: `text-muted-foreground` (unchanged) with `hover:text-primary hover:bg-primary/5`
      (unchanged — the UI-SPEC's accent list carries this forward explicitly).
    - active: `text-foreground`, background
      `bg-[color-mix(in_oklab,var(--primary)_6%,transparent)]`, plus a 2px `--primary` left rail.
      Do NOT add `font-medium` or any weight step — the UI-SPEC's weight collapse to 400/600 was a
      deliberate ruling and the rail plus tint is the whole visual distinction.
    - Implement the rail as a `::before` pseudo-element on the active `NavLink` via Tailwind
      arbitrary variants (`before:content-[''] before:absolute before:left-0 before:top-0
      before:bottom-0 before:w-[2px]` with the background reading `var(--primary)`), matching the
      sketch's "Active nav rail" CSS pattern. The `NavLink` already carries `relative
      overflow-hidden`, so the pseudo-element positions correctly. **If Tailwind's arbitrary
      `before:` variant does not produce the rule** (there is currently zero `before:` usage in
      `src/**/*.tsx`, so this is unproven in this codebase), fall back to an absolutely-positioned
      `<span aria-hidden="true">` rendered only when active — and say in the SUMMARY which one you
      shipped and what evidence decided it. Do not guess: check the built CSS or the rendered
      element.
    - Keep the `is-active` marker class on the active link. Other `group-[.is-active]:` variants in
      this file may depend on it, and removing it is a wider change than this plan authorises.

    **`aria-current="page"` must survive.** `NavLink` sets it by default on the active link and the
    live code adds no override (`grep -c "aria-current" src/layouts/DashboardLayout.tsx` returns 0,
    control: 5 `NavLink` hits in the same file). Do not swap `NavLink` for a plain `Link`, and do
    not stub the attribute. After the weight collapse it is the only NON-VISUAL signal of which
    item is active.

    Colour: every value above must read a CSS variable. No literal hex anywhere in this change.
    Do not introduce `--astridr` / violet — the colour law reserves it for Ástríðr the entity and
    nothing in the sidebar is her voice.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run src/layouts/__tests__/DashboardLayout.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Within `NavGroup`, `grep -c "drop-shadow" ` over the domain-header and nav-item blocks
      returns 0 (quote the pre-edit and post-edit counts for the whole file in the SUMMARY, and
      account for any remaining hits elsewhere in the file — the collapsed-rail Tooltip and the
      footer Settings link are outside this task).
    - `grep -c "tracking-\[0.08em\]" src/layouts/DashboardLayout.tsx` returns ≥ 1.
    - `grep -c "text-muted-foreground" ` on the domain-header line returns 1 and
      `grep -cE "text-primary font-mono font-bold" src/layouts/DashboardLayout.tsx` returns 0.
    - `grep -c "color-mix(in_oklab,var(--primary)_6%,transparent)" src/layouts/DashboardLayout.tsx`
      returns ≥ 1.
    - `grep -ciE "#[0-9a-f]{3,8}\b" src/layouts/DashboardLayout.tsx` shows no NEW literal hex
      introduced by this change (quote before/after counts).
    - `grep -cE "font-medium|font-bold|font-\[500\]|font-\[700\]" ` over the nav-item and
      domain-header blocks returns 0.
    - `npx tsc --noEmit` exits 0.
    - SUMMARY states whether the rail shipped as a `::before` utility or a `<span>`, and the
      evidence that decided it.
  </acceptance_criteria>
  <done>The sidebar reads as the Borealis eyebrow + sentence-case items with an accent rail, and carries no glow, no mono nav labels and no literal colour.</done>
</task>

<task type="auto">
  <name>Task 3: Replace the abandoned test.todo stubs with real sidebar assertions</name>
  <read_first>
    - `src/layouts/__tests__/DashboardLayout.test.tsx` (full, 195 lines — especially the module
      mock block `:22-70`, the `renderLayout()` helper `:75-81`, and the
      `describe("DashboardLayout Sidebar (UI-04)")` block of six `test.todo`s at `:188-195`)
    - `src/components/skills/RunTargetChooser.test.tsx:15-30` (the per-file `ResizeObserver`
      polyfill idiom this repo uses for Radix primitives — copy it here; do NOT edit
      `src/test/setup.ts`)
    - `src/layouts/DashboardLayout.tsx` as modified in Tasks 1 and 2
  </read_first>
  <files>src/layouts/__tests__/DashboardLayout.test.tsx</files>
  <behavior>
    - The expanded desktop sidebar carries the 232px width class (D-17's first half — this
      replaces the `test.todo("sidebar width is 240px (w-60) when expanded")` at `:194`).
    - Exactly four domain group headers render, reading Command, Observe, Agents, System.
    - The active nav item carries `aria-current="page"` — assert against a real route
      (`MemoryRouter initialEntries={["/alerts"]}`) so the assertion measures the live NavLink
      behaviour rather than a static class.
    - Collapsing a domain hides that domain's items and writes `codepulse-nav-domains` to
      `localStorage`; a subsequent render with that stored value renders the domain closed.
    - All four domains are open on first render when no stored value exists (D-15's default).
  </behavior>
  <action>
    Add the per-file `ResizeObserver` polyfill in a `beforeAll`, following
    `RunTargetChooser.test.tsx:20-24` verbatim — Radix `Collapsible` measures internally and jsdom
    has no `ResizeObserver`, which surfaces as a runtime error rather than a clean failure.

    Extend `renderLayout()` (or add a sibling helper) to accept `initialEntries` so a specific
    route can be mounted. Clear `localStorage` in `beforeEach` so the default-open case is not
    contaminated by a prior test's write.

    Replace the `test.todo` at `:194` with a real assertion at **232px**. Also replace the
    `test.todo` at `:189` ("renders 5 section groups: OVERVIEW, OPERATIONS, SYSTEM, INSIGHTS,
    ADMIN") with a real four-domain assertion — that stub's group names never matched any shipped
    registry and are now doubly wrong; note in your SUMMARY that the stub text was already stale
    before this phase. **Leave the other four `test.todo` lines (`:190`, `:191`, `:192`, `:193`)
    exactly as they are** — icons, count badges, collapsed tooltips and collapsed `aria-label` are
    other plans' or other phases' surface, and silently implementing them here would widen this
    plan's scope.

    Write the five behaviour cases above. For the width assertion, read the desktop `<aside>` and
    assert its `className` contains `w-[232px]` and does NOT contain `w-60` — jsdom does not do
    layout, so this is a source-level assertion and you should say so in the test's own comment
    rather than implying a measured width. The RENDERED 232px is measured in plan 124-09's
    Playwright block; these two are complementary, not duplicates.

    Prove the width test discriminates: temporarily set the aside back to `w-60`, re-run, confirm
    the new test FAILS, restore, confirm green. Record both runs in the SUMMARY.
  </action>
  <verify>
    <automated>npx vitest run src/layouts/__tests__/DashboardLayout.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/layouts/__tests__/DashboardLayout.test.tsx` exits 0 with the three
      pre-existing header-telemetry tests and the three keyboard-shortcut tests still passing.
    - `grep -c "test.todo" src/layouts/__tests__/DashboardLayout.test.tsx` returns exactly 4.
    - `grep -c "232" src/layouts/__tests__/DashboardLayout.test.tsx` returns ≥ 1.
    - `grep -c "aria-current" src/layouts/__tests__/DashboardLayout.test.tsx` returns ≥ 1.
    - `grep -c "codepulse-nav-domains" src/layouts/__tests__/DashboardLayout.test.tsx` returns ≥ 1.
    - `grep -cF "ResizeObserver" src/layouts/__tests__/DashboardLayout.test.tsx` returns ≥ 1.
    - `git diff --exit-code -- src/test/setup.ts` exits 0.
    - SUMMARY records the `w-60` mutation run showing the width test FAILING and the restored run
      showing green.
  </acceptance_criteria>
  <done>The sidebar's structure, width, active state and persistence are asserted, and the width assertion was shown to fail without the change.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `localStorage` → client state | `codepulse-nav-domains` is attacker-writable by anyone with script access to the origin, and is read at every mount. Same trust level as the existing `codepulse-sidebar-collapsed` and `codepulse-crt` keys. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-124-05-01 | Denial of service | Malformed `codepulse-nav-domains` crashing the shell at mount | mitigate | The initializer copies `DashboardLayout.tsx:353-359`'s `try { JSON.parse(...) } catch { <all-true default> }` shape verbatim, so a corrupt or hand-edited value degrades to all-domains-open rather than throwing inside a `useState` initializer — which in the shell would blank every route, not one page. |
| T-124-05-02 | Tampering | Persisted domain booleans | accept | The key holds four presentation booleans. It grants no capability, gates no route, and is not read by any authorization decision — a forged value can only open or close a sidebar section. |
| T-124-05-03 | Elevation of privilege | Sidebar rendering an entry the operator should not reach | accept | Items come from `navRegistry.ts`, whose route set is frozen by plan 124-01's guard, and `src/App.tsx`'s route table is untouched. Reachability is unchanged. |
| T-124-05-04 | Repudiation | Losing `aria-current="page"` while rewriting the render path | mitigate | Task 2 forbids swapping `NavLink` for `Link` or stubbing the attribute, and Task 3 asserts it against a real mounted route. After the 400/600 weight collapse it is the only non-visual active signal. |
| T-124-05-SC | Tampering | npm/pip/cargo installs | accept | Zero installs. `Collapsible` is already vendored at `src/components/ui/collapsible.tsx`; `components.json` → `"registries": {}`. |
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0.
- `npx vitest run src/layouts/__tests__/DashboardLayout.test.tsx` green.
- `npm test` green at plan end.
- `git diff --exit-code -- src/test/setup.ts` and `git diff --exit-code -- src/components/PageHeader.tsx`
  both exit 0 — neither file is in scope.
- SUMMARY records: the `w-60` mutation run, the rail implementation chosen with its evidence, and
  the drop-shadow before/after counts with every remaining hit accounted for.
</verification>

<success_criteria>
The sidebar is 232px, shows four independently collapsible domains whose state persists and is
overridden by the 48px rail, renders the eyebrow header and sentence-case labels with no glow, and
marks the active item with a 2px `--primary` rail, a 6% tint and `aria-current="page"`.
</success_criteria>

<output>
Create `.planning/phases/124-shell-information-architecture/124-05-SUMMARY.md` when done.
</output>
