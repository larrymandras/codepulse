---
phase: 124-shell-information-architecture
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/hooks/useAlerts.ts
  - src/components/AlertBanner.tsx
  - src/pages/Alerts.tsx
  - src/components/__tests__/AlertBanner.test.tsx
autonomous: true
requirements: [SHELL-01, SHELL-02]

must_haves:
  truths:
    - "D-12: useAlertCounts() returns the raw query result and never fabricates a zeroed shape while loading — the defect class is removed at the hook, not worked around at a third call site"
    - "AlertBanner renders nothing while alert counts are loading, rather than computing critical+error on a fabricated zero"
    - "Alerts.tsx's duplicate useQuery workaround is collapsed — one subscription feeds both the tile values and the loading state"
    - "The AlertBanner test mock is updated to the new return type and a loading case is asserted"
  artifacts:
    - path: "src/hooks/useAlerts.ts"
      provides: "Honest useAlertCounts() that preserves undefined"
      contains: "useAlertCounts"
    - path: "src/components/AlertBanner.tsx"
      provides: "Loading guard before any arithmetic on counts"
      contains: "AlertBanner"
    - path: "src/components/__tests__/AlertBanner.test.tsx"
      provides: "Loading-state assertion + updated mock"
  key_links:
    - from: "src/components/AlertBanner.tsx"
      to: "src/hooks/useAlerts.ts"
      via: "useAlertCounts()"
      pattern: "useAlertCounts"
    - from: "src/pages/Alerts.tsx"
      to: "src/hooks/useAlerts.ts"
      via: "useAlertCounts() feeding both MetricCard values and useMetricState"
      pattern: "useAlertCounts"
---

<objective>
Fix `useAlertCounts()` so it stops collapsing "still loading" into a fabricated all-zero shape,
and update its two real callers plus its test mock.

Purpose: D-12's amendment (2026-08-21) rules this a FIX, not a workaround. `Alerts.tsx:137-140`
already works around the hook at the call site by issuing a raw duplicate `useQuery` "to recover
the loading signal"; the new shell badges (plan 124-06) would have been the THIRD workaround.
This plan removes the defect class instead of adding another instance of it.
Output: an honest hook, a guarded banner, one collapsed workaround, and a loading-state test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/124-shell-information-architecture/124-CONTEXT.md
@src/hooks/useAlerts.ts
@src/components/AlertBanner.tsx
@src/pages/Alerts.tsx
@src/components/__tests__/AlertBanner.test.tsx

**The planning documents are CLAIMS. The live code is EVIDENCE.** If the code contradicts an
instruction below, correct the plan in your SUMMARY rather than transcribing it.

<interfaces>
Live state read 2026-08-21.

`src/hooks/useAlerts.ts:16-18` — the defect:
the hook returns `useQuery(api.alerts.countBySeverity) ?? { info: 0, warning: 0, error: 0, critical: 0 }`.

Consumers — a full-repo grep for `useAlertCounts|countBySeverity` over `src/`, `convex/`, `e2e/`
returned exactly these (no other consumer exists):
- `src/components/AlertBanner.tsx:2,5` — `counts.critical + counts.error` at `:8`, no guard.
- `src/pages/Alerts.tsx:5,136` — plus its duplicate-query workaround at `:137-141`.
- `src/components/__tests__/AlertBanner.test.tsx:6,9,12` — the `vi.mock` + `vi.mocked` wrapper.
- `src/hooks/useAlerts.ts:16` — the definition.

`src/pages/Alerts.tsx:136-141` today:
`counts` (zeroed) feeds `MetricCard value=`/`numericValue=` at `:164-167`; `countsRaw` (a raw
duplicate `useQuery`) feeds `useMetricState(countsRaw, undefined, {}).state` at `:141`.

`MetricCard`'s `value` prop is REQUIRED and typed `string | number` (read live at
`src/components/MetricCard.tsx`, `interface MetricCardProps`) — it is NOT optional, so
`value={counts?.critical}` is a type error. Keep an explicit local zeroed default at THAT call
site; the honesty is carried by the `state` prop, which is the six-state contract TOKEN-04
established.

`useMetricState` classifies an object by own-key count (`isEmptyValue`, `src/hooks/useMetricState.ts`)
and returns `loading` for `undefined` — so passing the hook's raw result into it is exactly the
signal `Alerts.tsx` was duplicating a query to recover.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Make useAlertCounts() honest and guard AlertBanner</name>
  <read_first>
    - `src/hooks/useAlerts.ts` (full, 27 lines — note the four sibling hooks that also use
      `?? []`; do NOT change them, D-12's ruling names `useAlertCounts` only)
    - `src/components/AlertBanner.tsx` (full, 40 lines)
    - `.planning/phases/124-shell-information-architecture/124-CONTEXT.md` (D-12 and its
      2026-08-21 amendment)
  </read_first>
  <files>src/hooks/useAlerts.ts, src/components/AlertBanner.tsx</files>
  <action>
    In `src/hooks/useAlerts.ts`, change `useAlertCounts()` to return `useQuery(api.alerts.countBySeverity)`
    with the `??` default REMOVED, so its return type is the query's result or `undefined`. Add a
    short docstring above it naming D-12 and stating why the `undefined` must survive the hook
    boundary: a `0` that actually means "not loaded yet" is the fabricated-confidence defect
    POLISH-04 exists to prevent, and TOKEN-04's six-state law forbids it elsewhere. Also note that
    every caller must guard. Leave `useActiveAlerts`, `useGroupedAlerts`, `useAllAlerts` and
    `useAllAlertsPaginated` untouched — this ruling is scoped to `useAlertCounts`.

    In `src/components/AlertBanner.tsx`, add an early `return null` when the hook's result is
    falsy/undefined, placed BEFORE the `urgentCount` computation at the current `:8` — not after
    it — so no arithmetic ever runs on a fabricated shape. Keep the two existing early returns
    (`urgentCount === 0`, and the `/alerts` pathname suppression) exactly as they are, in their
    current order. Change nothing else in that component: its markup, classes and copy are outside
    this phase's scope.

    Run `npx tsc --noEmit` and fix any type error the change surfaces at a call site, but do NOT
    silence one with a non-null assertion or an `any` — a type error here is the compiler finding
    an unguarded consumer, which is the point.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run src/components/__tests__/AlertBanner.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE "\?\? \{ info: 0" src/hooks/useAlerts.ts` returns 0.
    - `grep -c "useQuery(api.alerts.countBySeverity)" src/hooks/useAlerts.ts` returns 1.
    - `grep -c "D-12" src/hooks/useAlerts.ts` returns ≥ 1.
    - `src/components/AlertBanner.tsx` contains a `return null` guard on the counts result that
      appears at a LOWER line number than the line computing `counts.critical + counts.error`
      (verify by reading, and quote both line numbers in the SUMMARY).
    - `npx tsc --noEmit` exits 0.
    - `grep -cE "as any|!\." src/components/AlertBanner.tsx` shows no new non-null assertion or
      `any` introduced by this change (quote the before/after count in the SUMMARY).
  </acceptance_criteria>
  <done>The hook returns `undefined` while loading and AlertBanner returns null before touching the counts.</done>
</task>

<task type="auto">
  <name>Task 2: Collapse the Alerts.tsx duplicate-query workaround</name>
  <read_first>
    - `src/pages/Alerts.tsx` lines 1-20 and 125-175 (imports, the `Alerts` component body,
      the four `MetricCard` render sites)
    - `src/hooks/useMetricState.ts` (its `isEmptyValue` object handling and the `loading` rule)
    - `src/components/MetricCard.tsx` — `interface MetricCardProps` only (confirm `value` is
      required and typed `string | number`)
  </read_first>
  <files>src/pages/Alerts.tsx</files>
  <action>
    Replace the two-subscription workaround at `src/pages/Alerts.tsx:136-141` with a single read:
    call `useAlertCounts()` once into a variable holding the possibly-`undefined` result, derive
    `countsState` from THAT variable via `useMetricState(<raw>, undefined, {}).state`, and derive a
    separate local display object that applies the explicit zeroed default
    `{ info: 0, warning: 0, error: 0, critical: 0 }` for the four `MetricCard` `value` /
    `numericValue` props at `:164-167`.

    Delete the now-redundant `const countsRaw = useQuery(api.alerts.countBySeverity)` line and its
    three-line comment. Remove the `useQuery` import and the `api` import ONLY if nothing else in
    the file still uses them — grep the file first and say in the SUMMARY which imports you
    removed and which you kept, with the count that justified each.

    Replace the deleted comment with a one-line note recording that the duplicate query existed to
    recover the loading signal `useAlertCounts()` used to swallow, and that Phase 124's D-12 fix
    removed the need for it. Do not delete the history silently — a future reader needs to know
    the duplicate was deliberate, not accidental.

    Keep the four `MetricCard` calls' `state={countsState}` props exactly as they are; the loading
    truth reaches the tile through `state`, which is why the explicit zero at the `value` prop is
    honest rather than fabricated.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run src/pages src/components/__tests__/AlertBanner.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "countsRaw" src/pages/Alerts.tsx` returns 0.
    - `grep -c "useAlertCounts" src/pages/Alerts.tsx` returns 2 (the import and the single call).
    - `grep -c "api.alerts.countBySeverity" src/pages/Alerts.tsx` returns 0.
    - `grep -c "useMetricState" src/pages/Alerts.tsx` returns ≥ 2 (import + call), and its first
      argument is the possibly-`undefined` hook result, not a defaulted object (verify by reading;
      quote the line in the SUMMARY).
    - `npx tsc --noEmit` exits 0.
    - SUMMARY names each import removed with the grep count that justified removing it.
  </acceptance_criteria>
  <done>Alerts.tsx issues one alert-count subscription; the loading signal and the tile values both come from it.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Update the AlertBanner mock and assert the loading case</name>
  <read_first>
    - `src/components/__tests__/AlertBanner.test.tsx` (full — the `vi.mock` at `:5-7`, the
      `vi.mocked` wrapper at `:12`, and every existing `mockReturnValue` call site)
    - `src/components/AlertBanner.tsx` (as modified in Task 1)
  </read_first>
  <files>src/components/__tests__/AlertBanner.test.tsx</files>
  <behavior>
    - Loading (`mockReturnValue(undefined)`): AlertBanner renders nothing — no "active alert"
      text, no button. This case FAILS against the pre-Task-1 component (it would have thrown on
      `counts.critical`), so it is the regression guard for the fix.
    - Resolved with critical > 0: unchanged — banner renders (existing test must stay green).
    - Resolved all-zero: unchanged — banner renders nothing (existing behavior, and it must be
      DISTINGUISHABLE in intent from the loading case; state that distinction in a comment so a
      future reader does not "simplify" the two into one test).
  </behavior>
  <action>
    Add a test asserting the loading case: set `mockUseAlertCounts.mockReturnValue(undefined)`,
    render, and assert `screen.queryByRole("button")` is null AND
    `screen.queryByText(/active alert/)` is null. Adjust the `vi.mocked` typing at `:12` only if
    TypeScript requires it for the `undefined` return — do not widen it to `any`.

    Every existing `mockReturnValue({ ... })` call in the file keeps its object literal unchanged;
    they now represent the resolved case explicitly rather than by accident.

    Add a comment above the new test naming D-12 and stating that an all-zero resolved result and
    an unresolved query are DIFFERENT facts that happen to render identically here, so the two
    tests must not be merged.

    Prove the test discriminates: temporarily revert AlertBanner's guard (delete the early return
    only), re-run, confirm the new test FAILS, then restore the guard and confirm green. Record
    both runs in the SUMMARY. Restore by re-adding the guard, not by `git checkout` — other work
    from Task 1 lives in that file.
  </action>
  <verify>
    <automated>npx vitest run src/components/__tests__/AlertBanner.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/components/__tests__/AlertBanner.test.tsx` exits 0 with the new loading
      test among the passes.
    - `grep -c "mockReturnValue(undefined)" src/components/__tests__/AlertBanner.test.tsx` returns ≥ 1.
    - `grep -c "D-12" src/components/__tests__/AlertBanner.test.tsx` returns ≥ 1.
    - SUMMARY records the guard-removed run showing the new test FAILING, and the restored run
      showing it passing — a guard test that was never shown to fail proves nothing.
    - `git diff -- src/components/AlertBanner.tsx` after restoration still shows the Task 1 guard
      present.
  </acceptance_criteria>
  <done>The loading case is asserted and was shown to fail without the guard.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Convex query → client render | `alerts.countBySeverity` result crosses into UI arithmetic. Untrusted only in the sense of being ABSENT (loading) or THROWN (error) — not attacker-controlled; the query is already public and already read by `/alerts` today. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-124-02-01 | Repudiation | `AlertBanner` / `Alerts` tiles fabricating a zero | mitigate | The hook stops defaulting; AlertBanner returns null before any arithmetic; Alerts.tsx carries the loading truth in `state` while its `value` default is explicit and local. Asserted by Task 3's loading test, shown failing without the guard. |
| T-124-02-02 | Denial of service | An unguarded consumer throwing on `undefined` and unmounting the React tree | mitigate | `npx tsc --noEmit` is an acceptance gate on Tasks 1 and 2, so the compiler enumerates every consumer of the widened type rather than leaving one to fail at runtime. Full-repo grep in `<interfaces>` establishes the consumer set is exactly three files. |
| T-124-02-03 | Information disclosure | Alert counts rendered in shared chrome | accept | Counts are already rendered on `/alerts` and in the existing `AlertBanner` on every route; this plan changes no visibility. Per `CLAUDE.md` SEED-008 the tailnet is this deployment's auth boundary — unchanged by this plan. |
| T-124-02-SC | Tampering | npm/pip/cargo installs | accept | Zero installs in this plan and in the phase (`components.json` → `"registries": {}`); no legitimacy gate applies. |
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0.
- `npx vitest run src/components/__tests__/AlertBanner.test.tsx` green, including the loading case.
- `npm test` green (run once at the end of the plan — this change widens a shared hook's return
  type and any other suite touching it must be shown unaffected).
- SUMMARY records the mutation run for Task 3 and the import-removal greps for Task 2.
</verification>

<success_criteria>
`useAlertCounts()` never fabricates a zero; both real callers are updated; the test mock reflects
the new contract; the loading behavior is asserted and was proven to fail without the fix.
</success_criteria>

<output>
Create `.planning/phases/124-shell-information-architecture/124-02-SUMMARY.md` when done.
</output>
