---
phase: 94-trace-waterfall
plan: 04
type: execute
wave: 3
depends_on: [94-03]
files_modified:
  - src/pages/SessionDetail.tsx
  - src/pages/Analytics.tsx
  - src/components/LangfuseTraceLink.tsx
autonomous: true
requirements: [TRACE-02]
must_haves:
  truths:
    - "SessionDetail has a 'Trace' tab that renders the TraceWaterfall for the current session"
    - "The Trace tab is reachable via the ?tab=trace URL param (deep-link)"
    - "Analytics recent-calls rows link to their session's trace via 'View Trace'"
    - "LangfuseTraceLink.tsx is deleted and has zero remaining references; build stays green"
  artifacts:
    - path: "src/pages/SessionDetail.tsx"
      provides: "Trace tab entry + useSearchParams deep-link wiring + TraceWaterfall render"
      contains: "trace"
    - path: "src/pages/Analytics.tsx"
      provides: "LangfuseTraceLink removed; 'View Trace' cross-link added"
      contains: "View Trace"
  key_links:
    - from: "src/pages/SessionDetail.tsx"
      to: "src/components/TraceWaterfall.tsx"
      via: "render <TraceWaterfall sessionId={id}> in the trace panel"
      pattern: "TraceWaterfall"
    - from: "src/pages/Analytics.tsx recent-calls rows"
      to: "/sessions/:id?tab=trace"
      via: "View Trace Link (encodeURIComponent, sessionId guard)"
      pattern: "\\?tab=trace"
---

<objective>
Wire the `TraceWaterfall` into the product (TRACE-02, D-06/D-07/D-08): add a "Trace" tab to `SessionDetail` with the first `useSearchParams` deep-link wiring (`?tab=trace`), render the waterfall there inside a `SectionErrorBoundary`, delete the dead `LangfuseTraceLink.tsx` and its two Analytics usage sites, and add a "View Trace" cross-link on Analytics' recent-calls rows.

Purpose: The entry points — how an operator gets to the waterfall, and the removal of the dead Langfuse link it replaces.
Output: Modified `SessionDetail.tsx` and `Analytics.tsx`; deleted `LangfuseTraceLink.tsx`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/94-trace-waterfall/94-CONTEXT.md
@.planning/phases/94-trace-waterfall/94-PATTERNS.md
@.planning/phases/94-trace-waterfall/94-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the Trace tab + ?tab=trace deep-link to SessionDetail</name>
  <read_first>
    - src/pages/SessionDetail.tsx (Tab union + TABS array at lines 16-24; activeTab useState at ~26; the generic tab-button map at 59-73; existing per-tab useQuery/panel rendering)
    - src/pages/HivePage.tsx (lines 11, 24-25, 32-34 — useSearchParams deep-link pattern: read param, inbound-takes-precedence)
    - src/hooks/useFocusParam.ts (lines 56-68 — silent no-op / default-on-no-match posture to mirror for an invalid ?tab value)
    - src/components/TraceWaterfall.tsx (the { sessionId } prop contract from Plan 03)
    - src/components/SectionErrorBoundary.tsx (wrapper convention for tab content)
  </read_first>
  <action>
    Extend the `Tab` union to `"overview" | "timeline" | "files" | "bash" | "errors" | "trace"` and append `{ key: "trace", label: "Trace" }` to the `TABS` array (the tab-button map at 59-73 needs no change — it iterates TABS generically). This is the FIRST useSearchParams wiring on this page (confirmed — no existing tab reads the URL), so add `const [searchParams] = useSearchParams()` and initialize `activeTab` from `searchParams.get("tab")`: validate it against the Tab union and fall back to `"overview"` on absent/invalid (silent-fallback posture, per useFocusParam) — initialize the useState from the param rather than a post-mount useEffect override, since the tab is user-togglable client state. Import `useSearchParams` from `react-router-dom`.

    In the trace panel branch (rendered when activeTab === "trace"), render `<TraceWaterfall sessionId={id} />` wrapped in `<SectionErrorBoundary name="Trace">` so a waterfall failure shows the house error-boundary copy ("Trace data failed to load...") without taking down the page. `id` is the existing `useParams` session id. Do not add any new route or nav entry (D-06).
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `rg "key: .trace., label: .Trace." src/pages/SessionDetail.tsx` matches (tab entry added)
    - `rg "useSearchParams" src/pages/SessionDetail.tsx` matches (deep-link wiring present)
    - `rg "<TraceWaterfall" src/pages/SessionDetail.tsx` matches and is wrapped by `rg "SectionErrorBoundary" src/pages/SessionDetail.tsx`
    - The Tab union contains "trace": `rg "\"trace\"" src/pages/SessionDetail.tsx` matches
    - `npx tsc --noEmit` exits 0 and `npm run build` succeeds
  </acceptance_criteria>
  <done>SessionDetail shows a Trace tab that renders the waterfall for the current session and is reachable via ?tab=trace; build green.</done>
</task>

<task type="auto">
  <name>Task 2: Delete LangfuseTraceLink and add the 'View Trace' cross-link on Analytics</name>
  <read_first>
    - src/pages/Analytics.tsx (import at line 30; the header usage at lines 79-85 — <LangfuseTraceLink /> at ~L83, keep TokenSavingsIndicator and the wrapping div; the recent-calls table rendered from the useLlmMetrics() hook imported at line 4)
    - src/components/LangfuseTraceLink.tsx (full 27-line file — deletion target; confirm no other exports)
    - src/components/kg/KGDetailsPanel.tsx (lines 29-40 — provenanceHref helper + ProvenanceLink pattern: encodeURIComponent + optional-null guard + react-router <Link>, the exact shape to mirror for traceHref)
  </read_first>
  <action>
    Remove the `import { LangfuseTraceLink } from "../components/LangfuseTraceLink";` line (L30) and the single `<LangfuseTraceLink />` JSX usage (L83) from Analytics.tsx — keep the wrapping `<div className="flex items-center gap-3">` and the `<TokenSavingsIndicator ... />`. Then delete the file src/components/LangfuseTraceLink.tsx entirely. No replacement affordance goes in the Analytics header (D-07).

    Add a "View Trace" cross-link to each recent-calls table row. Define a small `traceHref(sessionId?: string | null): string | null` helper returning `` `/sessions/${encodeURIComponent(sessionId)}?tab=trace` `` when sessionId is present else null (mirroring KGDetailsPanel's provenanceHref null-guard + encodeURIComponent convention). In the recent-calls `<TableRow>` mapping (rows from useLlmMetrics()), render a cell with a react-router `<Link>` labeled exactly "View Trace" (UI-SPEC Copywriting Contract) targeting `traceHref(call.sessionId)` — render the link ONLY when call.sessionId is present (guard on the null return), otherwise render nothing/dash in that cell. Use `var(--primary)`-token styling for the link (the reserved accent role for cross-nav affordances per UI-SPEC), no hardcoded hex.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `rg "LangfuseTraceLink" src/` returns ZERO matches (component + both usage sites gone — Pitfall 5)
    - The file src/components/LangfuseTraceLink.tsx no longer exists
    - `rg "TokenSavingsIndicator" src/pages/Analytics.tsx` still matches (only the Langfuse link was removed, not its neighbor)
    - `rg "View Trace" src/pages/Analytics.tsx` matches and `rg "\?tab=trace" src/pages/Analytics.tsx` matches
    - `rg "encodeURIComponent" src/pages/Analytics.tsx` matches (href built safely with sessionId guard)
    - `npx tsc --noEmit` exits 0 and `npm run build` succeeds
  </acceptance_criteria>
  <done>Dead LangfuseTraceLink removed with zero dangling references; Analytics recent-calls rows deep-link to the session trace; build green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL `?tab=trace` param → component state | Untrusted URL param selects the active tab |
| `call.sessionId` → deep-link href | Untrusted sessionId string interpolated into a route path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-94-07 | Tampering | Malicious/invalid `?tab=` value driving unexpected state | mitigate | Validate the param against the fixed Tab union; fall back to "overview" on any non-member value (silent no-op posture) — no arbitrary value reaches rendering logic. |
| T-94-08 | Tampering | Injection via sessionId in the View-Trace href | mitigate | `encodeURIComponent(sessionId)` + null guard (mirrors KGDetailsPanel provenanceHref); href is a same-origin route, not raw HTML. |
| T-94-SC | Tampering | npm installs | accept | Zero new packages — only edits/deletion of existing files. |
</threat_model>

<verification>
- `rg "LangfuseTraceLink" src/` returns nothing (deletion complete, Pitfall 5)
- `npx tsc --noEmit && npm run build` green
- Trace tab renders the waterfall; ?tab=trace selects it; Analytics rows link to it
</verification>

<success_criteria>
- SessionDetail has a Trace tab rendering TraceWaterfall, deep-linkable via ?tab=trace
- Analytics recent-calls rows expose a "View Trace" deep-link (guarded on sessionId)
- LangfuseTraceLink.tsx and both usage sites are gone; tsc + build clean
</success_criteria>

<output>
Create `.planning/phases/94-trace-waterfall/94-04-SUMMARY.md` when done
</output>
