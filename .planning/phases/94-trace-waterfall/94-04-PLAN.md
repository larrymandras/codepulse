---
phase: 94-trace-waterfall
plan: 04
type: execute
wave: 3
depends_on: [94-03]
files_modified:
  - src/pages/SessionDetail.tsx
  - src/pages/Analytics.tsx
  - src/components/LangfuseTraceLink.tsx
autonomous: true
requirements: [TRACE-02]
must_haves:
  truths:
    - "SessionDetail has a 'Trace' tab that renders the TraceWaterfall for the current session"
    - "The Trace tab is reachable via the ?tab=trace URL param (deep-link)"
    - "Analytics renders a bounded 'Recent LLM Calls' table sourced from the useLlmMetrics() paginated hook"
    - "Each Recent LLM Calls row with a sessionId exposes a 'View Trace' deep-link to its session's trace"
    - "LangfuseTraceLink.tsx is deleted and has zero remaining references; build stays green"
  artifacts:
    - path: "src/pages/SessionDetail.tsx"
      provides: "Trace tab entry + useSearchParams deep-link wiring + TraceWaterfall render"
      contains: "trace"
    - path: "src/pages/Analytics.tsx"
      provides: "LangfuseTraceLink removed; 'Recent LLM Calls' table with per-row 'View Trace' cross-link"
      contains: "View Trace"
  key_links:
    - from: "src/pages/SessionDetail.tsx"
      to: "src/components/TraceWaterfall.tsx"
      via: "render <TraceWaterfall sessionId={id}> in the trace panel"
      pattern: "TraceWaterfall"
    - from: "src/pages/Analytics.tsx Recent LLM Calls rows"
      to: "/sessions/:id?tab=trace"
      via: "View Trace Link (encodeURIComponent, sessionId guard)"
      pattern: "\\?tab=trace"
---

<objective>
Wire the `TraceWaterfall` into the product (TRACE-02, D-06/D-07/D-08): add a "Trace" tab to `SessionDetail` with the first `useSearchParams` deep-link wiring (`?tab=trace`), render the waterfall there inside a `SectionErrorBoundary`, delete the dead `LangfuseTraceLink.tsx` and its Analytics usage site, and add a compact "Recent LLM Calls" table on Analytics whose rows deep-link into the session trace via a "View Trace" link.

Purpose: The entry points — how an operator gets to the waterfall, the recent-calls table that carries the cross-link, and the removal of the dead Langfuse link it replaces.
Output: Modified `SessionDetail.tsx` and `Analytics.tsx`; deleted `LangfuseTraceLink.tsx`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/94-trace-waterfall/94-CONTEXT.md
@.planning/phases/94-trace-waterfall/94-PATTERNS.md
@.planning/phases/94-trace-waterfall/94-UI-SPEC.md

<interfaces>
<!-- Contracts the executor needs. Extracted from codebase — no exploration required. -->

useLlmMetrics (src/hooks/useLlmMetrics.ts) — wraps usePaginatedQuery(api.llm.recentCallsPaginated):
  useLlmMetrics(initialNumItems = 25) => { calls, status, loadMore }
    - calls: LlmMetricRow[]        (?? [] — safe during load)
    - status: "LoadingFirstPage" | "CanLoadMore" | "LoadingMore" | "Exhausted"
    - loadMore: (numItems: number) => void

LlmMetricRow fields (convex/schema.ts llmMetrics, lines 297-314) — all row values:
    provider: string; model: string; totalTokens: number; latencyMs: number;
    timestamp: number (epoch seconds);
    cost?: number; sessionId?: string;
    cacheReadInputTokens?: number; cacheCreationInputTokens?: number;

Formatters (src/lib/formatters.ts):
    formatCost(dollars: number): string          // "$0.0000"
    formatDurationMs(ms: number): string         // "812ms" / "1m 3s"
    formatTimestamp(ts: number): string          // toLocaleTimeString from epoch seconds

Table primitives already imported in Analytics.tsx (lines 35-42):
    Table, TableHeader, TableBody, TableRow, TableHead, TableCell  (src/components/ui/table.tsx)
Badge primitive: src/components/ui/badge.tsx
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the Trace tab + ?tab=trace deep-link to SessionDetail</name>
  <read_first>
    - src/pages/SessionDetail.tsx (Tab union + TABS array at lines 16-24; activeTab useState at ~26; the generic tab-button map at 59-73; existing per-tab useQuery/panel rendering)
    - src/pages/HivePage.tsx (lines 11, 24-25, 32-34 — useSearchParams deep-link pattern: read param, inbound-takes-precedence)
    - src/hooks/useFocusParam.ts (lines 56-68 — silent no-op / default-on-no-match posture to mirror for an invalid ?tab value)
    - src/components/TraceWaterfall.tsx (the { sessionId } prop contract from Plan 03)
    - src/components/SectionErrorBoundary.tsx (wrapper convention for tab content)
  </read_first>
  <action>
    Extend the `Tab` union to `"overview" | "timeline" | "files" | "bash" | "errors" | "trace"` and append `{ key: "trace", label: "Trace" }` to the `TABS` array (the tab-button map at 59-73 needs no change — it iterates TABS generically). This is the FIRST useSearchParams wiring on this page (confirmed — no existing tab reads the URL), so add `const [searchParams] = useSearchParams()` and initialize `activeTab` from `searchParams.get("tab")`: validate it against the Tab union and fall back to `"overview"` on absent/invalid (silent-fallback posture, per useFocusParam) — initialize the useState from the param rather than a post-mount useEffect override, since the tab is user-togglable client state. Import `useSearchParams` from `react-router-dom`.

    In the trace panel branch (rendered when activeTab === "trace"), render `<TraceWaterfall sessionId={id} />` wrapped in `<SectionErrorBoundary name="Trace">` so a waterfall failure shows the house error-boundary copy ("Trace data failed to load...") without taking down the page. `id` is the existing `useParams` session id. Do not add any new route or nav entry (D-06).
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `rg "key: .trace., label: .Trace." src/pages/SessionDetail.tsx` matches (tab entry added)
    - `rg "useSearchParams" src/pages/SessionDetail.tsx` matches (deep-link wiring present)
    - `rg "<TraceWaterfall" src/pages/SessionDetail.tsx` matches and is wrapped by `rg "SectionErrorBoundary" src/pages/SessionDetail.tsx`
    - The Tab union contains "trace": `rg "\"trace\"" src/pages/SessionDetail.tsx` matches
    - `npx tsc --noEmit` exits 0 and `npm run build` succeeds
  </acceptance_criteria>
  <done>SessionDetail shows a Trace tab that renders the waterfall for the current session and is reachable via ?tab=trace; build green.</done>
</task>

<task type="auto">
  <name>Task 2: Delete LangfuseTraceLink and its Analytics usage site</name>
  <read_first>
    - src/pages/Analytics.tsx (import at line 30; the header usage at lines 79-85 — <LangfuseTraceLink /> at ~L83, keep TokenSavingsIndicator and the wrapping div)
    - src/components/LangfuseTraceLink.tsx (full ~27-line file — deletion target; confirm no other exports)
  </read_first>
  <action>
    Remove the `import { LangfuseTraceLink } from "../components/LangfuseTraceLink";` line (L30) and the single `<LangfuseTraceLink />` JSX usage (L83) from Analytics.tsx — keep the wrapping `<div className="flex items-center gap-3">` and the `<TokenSavingsIndicator ... />`. Then delete the file src/components/LangfuseTraceLink.tsx entirely. No replacement affordance goes in the Analytics header (D-07) — the replacement cross-nav lives in the Recent LLM Calls table (Task 3), not the header.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `rg "LangfuseTraceLink" src/` returns ZERO matches (component + usage site gone — Pitfall 5)
    - The file src/components/LangfuseTraceLink.tsx no longer exists
    - `rg "TokenSavingsIndicator" src/pages/Analytics.tsx` still matches (only the Langfuse link was removed, not its neighbor)
    - `npx tsc --noEmit` exits 0 and `npm run build` succeeds
  </acceptance_criteria>
  <done>Dead LangfuseTraceLink removed with zero dangling references; TokenSavingsIndicator preserved; build green.</done>
</task>

<task type="auto">
  <name>Task 3: Add the 'Recent LLM Calls' table with per-row 'View Trace' deep-link</name>
  <read_first>
    - src/pages/Analytics.tsx (the useLlmMetrics() call at L46 — currently destructures only `calls`; the ui/table imports at 35-42; formatCost already imported at L5; the advisorRecent cost-comparison table at lines 378-397 — the exact shadcn Table composition + `<SectionErrorBoundary>`+`<GlassPanel>` section pattern to mirror)
    - src/components/kg/KGDetailsPanel.tsx (lines 29-44 — provenanceHref helper + ProvenanceLink: `sourceEventId ? \`/route?x=${encodeURIComponent(sourceEventId)}\` : null` + null-guard render, the exact shape to mirror for traceHref)
    - src/lib/formatters.ts (formatCost, formatDurationMs, formatTimestamp signatures — see <interfaces>)
    - src/components/ui/badge.tsx (Badge primitive + its variant prop, for the cache indicator)
    - .planning/phases/94-trace-waterfall/94-UI-SPEC.md (Accent-role table at line 74/89: `var(--primary)` is RESERVED for the "View Trace" cross-nav affordance; Copywriting Contract line 97: primary CTA label is exactly "View Trace")
  </read_first>
  <action>
    Widen the existing hook destructure at L46 to `const { calls: llmCalls, status: llmStatus, loadMore: loadMoreLlm } = useLlmMetrics();` (the aggregate `totalTokens` reduce over `llmCalls` at L72 is unaffected). Add the missing formatter imports (`formatDurationMs`, `formatTimestamp`) to the existing formatters import line at L5 (`formatCost` is already imported); import `Link` from `react-router-dom`; import `Badge` from `../components/ui/badge`.

    Define a small helper `traceHref(sessionId?: string | null): string | null` returning `` `/sessions/${encodeURIComponent(sessionId)}?tab=trace` `` when sessionId is present else null (mirroring KGDetailsPanel's provenanceHref: null-guard + encodeURIComponent). Do NOT interpolate a raw sessionId into the path.

    Add a new dashboard section — a `<SectionErrorBoundary name="Recent LLM Calls">` wrapping a `<GlassPanel>` (mirror the advisorRecent section at 357-401; place it in the LLM/telemetry region of the grid, e.g. after the "Prompt Cache by Model" panel) containing a `<SectionHeader title="Recent LLM Calls" />` and a shadcn `<Table>` composed from the already-imported table primitives. Columns, in order: Time (`formatTimestamp(call.timestamp)`), Model (`call.model` rendered as plain JSX text — auto-escaped), Cost (`call.cost != null ? formatCost(call.cost) : "—"`, tabular-nums), Latency (`formatDurationMs(call.latencyMs)`, tabular-nums), Cache (a `<Badge>` shown only when `(call.cacheReadInputTokens ?? 0) > 0`, else "—" — three-state: undefined/0 → dash, >0 → "cache" badge), Trace. In the Trace cell render a react-router `<Link>` labeled exactly "View Trace" (UI-SPEC Copywriting Contract) to `traceHref(call.sessionId)`, rendered ONLY when the helper returns non-null (guard on the null return; otherwise render "—"). Style the link with the reserved accent role via `style={{ color: "var(--primary)" }}` (UI-SPEC lines 74/89) — no hardcoded hex, tokens only, consistent with the advisorRecent table's `var(--status-ok)` usage at L393.

    Bound the rows via the hook's existing pagination — do NOT render unbounded: iterate the current `llmCalls` page (first page = 25) and, when `llmStatus === "CanLoadMore"`, render a token-styled "Load more" button that calls `loadMoreLlm(25)`. Show an empty-state line (mirror "No execution depth data yet.") when `llmCalls.length === 0`.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `rg "Recent LLM Calls" src/pages/Analytics.tsx` matches (section header present)
    - `rg "View Trace" src/pages/Analytics.tsx` matches and `rg "\?tab=trace" src/pages/Analytics.tsx` matches
    - `rg "encodeURIComponent" src/pages/Analytics.tsx` matches (href built safely with sessionId guard)
    - `rg "loadMore" src/pages/Analytics.tsx` matches (bounded pagination wired, not unbounded render)
    - `rg "#[0-9a-fA-F]{6}" src/pages/Analytics.tsx` shows no NEW hardcoded hex in the added section (tokens only)
    - `npx tsc --noEmit` exits 0 and `npm run build` succeeds
  </acceptance_criteria>
  <done>Analytics renders a bounded Recent LLM Calls table (Time/Model/Cost/Latency/Cache/Trace) sourced from useLlmMetrics(); rows with a sessionId deep-link to the session trace via a token-styled "View Trace" link; build green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL `?tab=trace` param → component state | Untrusted URL param selects the active tab |
| `call.sessionId` → deep-link href | Untrusted sessionId string interpolated into a route path |
| `call.model` / `call.timestamp` → table cells | Untrusted row strings rendered into the Recent LLM Calls table |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-94-07 | Tampering | Malicious/invalid `?tab=` value driving unexpected state | mitigate | Validate the param against the fixed Tab union; fall back to "overview" on any non-member value (silent no-op posture) — no arbitrary value reaches rendering logic. |
| T-94-08 | Tampering | Injection via sessionId in the View-Trace href | mitigate | `encodeURIComponent(sessionId)` + null guard in `traceHref` (mirrors KGDetailsPanel provenanceHref); href is a same-origin route, not raw HTML. |
| T-94-10 | Tampering | XSS via untrusted `model`/`timestamp`/badge text in Recent LLM Calls rows | mitigate | Values rendered as plain JSX children (React auto-escapes) — no `dangerouslySetInnerHTML`, no interpolation into `href`/`style` beyond the token literal and the encoded route path. |
| T-94-SC | Tampering | npm installs | accept | Zero new packages — only edits/deletion of existing files and existing ui primitives (Badge, Table). |
</threat_model>

<verification>
- `rg "LangfuseTraceLink" src/` returns nothing (deletion complete, Pitfall 5)
- `npx tsc --noEmit && npm run build` green
- Trace tab renders the waterfall; ?tab=trace selects it; Recent LLM Calls rows link to it
</verification>

<success_criteria>
- SessionDetail has a Trace tab rendering TraceWaterfall, deep-linkable via ?tab=trace
- Analytics renders a bounded Recent LLM Calls table sourced from useLlmMetrics()
- Table rows expose a "View Trace" deep-link (guarded on sessionId, encodeURIComponent, `var(--primary)` token)
- LangfuseTraceLink.tsx and its usage site are gone; tsc + build clean
</success_criteria>

<output>
Create `.planning/phases/94-trace-waterfall/94-04-SUMMARY.md` when done
</output>
