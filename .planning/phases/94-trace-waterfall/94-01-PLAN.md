---
phase: 94-trace-waterfall
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - convex/schema.ts
  - convex/llm.ts
  - convex/llm.test.ts
  - convex/runtimeIngest.ts
  - convex/runtimeIngest.test.ts
  - convex/_generated/api.d.ts
autonomous: true
requirements: [TRACE-01]
must_haves:
  truths:
    - "A recorded llm_call carrying a traceId persists that traceId on its llmMetrics row"
    - "A recorded llm_call with no traceId persists an undefined traceId (existing rows unaffected)"
    - "The ingest handler accepts both traceId (camelCase) and trace_id (snake_case) payload keys"
    - "A session-scoped query returns all of a session's llmMetrics rows in chronological order for client grouping"
  artifacts:
    - path: "convex/schema.ts"
      provides: "traceId: v.optional(v.string()) on llmMetrics table"
      contains: "traceId: v.optional(v.string())"
    - path: "convex/llm.ts"
      provides: "recordCall traceId arg+insert, and new sessionCalls query"
      contains: "sessionCalls"
    - path: "convex/runtimeIngest.ts"
      provides: "llm_call traceId alias pass-through"
      contains: "d.traceId ?? d.trace_id"
    - path: "convex/_generated/api.d.ts"
      provides: "regenerated codegen including traceId + sessionCalls"
  key_links:
    - from: "convex/runtimeIngest.ts"
      to: "convex/llm.ts recordCall"
      via: "ctx.runMutation(api.llm.recordCall) with traceId arg"
      pattern: "recordCall"
    - from: "convex/llm.ts sessionCalls"
      to: "llmMetrics by_session index"
      via: "withIndex query"
      pattern: "by_session"
---

<objective>
Extend the CodePulse backend so `llmMetrics` rows carry an optional `traceId` grouping key end-to-end: schema field, `recordCall` mutation arg, `/runtime-ingest` alias pass-through, plus a new session-scoped query that returns raw rows for the UI to group client-side. This is the TRACE-01 storage + read contract that Plan 03's `TraceWaterfall` consumes.

Purpose: Nothing groups LLM calls by turn today. This adds the grouping key and the query the waterfall reads, backward-compatible with every existing row (which has no `traceId`).
Output: Modified `schema.ts`, `llm.ts` (+ new `sessionCalls` query), `runtimeIngest.ts`, extended tests, regenerated `_generated/api.d.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/94-trace-waterfall/94-CONTEXT.md
@.planning/phases/94-trace-waterfall/94-RESEARCH.md
@.planning/phases/94-trace-waterfall/94-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Thread traceId through schema, recordCall, and the ingest alias (TRACE-01 core)</name>
  <read_first>
    - convex/schema.ts (llmMetrics table, lines 297-320 — see the goalId/cache-field precedent that this exactly mirrors)
    - convex/llm.ts (recordCall mutation, lines 6-43 — goalId arg at L19, insert field at L38)
    - convex/runtimeIngest.ts (llm_call case, lines 58-77 — goalId alias at L72, cache aliases at L73-74)
    - convex/llm.test.ts (recordCallLogic + makeLlmStore + "goalId persistence" describe block, lines 1-67 — the hand-mirrored no-convex-test pattern)
    - convex/runtimeIngest.test.ts (extractLlmCallGoalId function at L60-63 + its tests at L232-246)
  </read_first>
  <behavior>
    - recordCall persists traceId onto the inserted llmMetrics row when the arg is present
    - recordCall inserts traceId as undefined when the arg is omitted (backward compat — existing rows unaffected)
    - ingest extraction returns the camelCase traceId when present
    - ingest extraction falls back to trace_id (snake_case) when traceId is absent
    - ingest extraction returns undefined when neither key is present (legacy untraced call)
  </behavior>
  <action>
    Add `traceId: v.optional(v.string())` to the `llmMetrics` `defineTable` in schema.ts, positioned immediately after the existing `goalId: v.optional(v.string())` line, with a comment `// Phase 94 TRACE-01 — per-turn trace grouping`. Do NOT add any new index — session-scale client-side grouping is the resolved discretion decision (RESEARCH.md Alternatives Considered), so `by_session` is sufficient.

    In llm.ts `recordCall`, add `traceId: v.optional(v.string())` to `args` (immediately after the `goalId` arg) and add `traceId: args.traceId,` to the `ctx.db.insert("llmMetrics", {...})` object (immediately after the `goalId: args.goalId,` line). Match the exact position and style of `goalId`.

    In runtimeIngest.ts `case "llm_call"`, add `traceId: d.traceId ?? d.trace_id,` to the `ctx.runMutation(api.llm.recordCall, {...})` argument object, immediately after the `goalId: d.goalId ?? d.goal_id,` line, following the identical snake/camel alias convention already used for every field in that case.

    Extend the two Convex test files using the existing hand-mirrored pure-function convention (this repo has NO convex-test library — do not import Convex runtime objects). In convex/llm.test.ts: add `traceId: args.traceId` to the mirrored `recordCallLogic()` insert (same field position as goalId), then add a `describe("recordCall — traceId persistence (Phase 94 TRACE-01)")` block mirroring the goalId block's two-test shape — one asserting the row's traceId equals the passed value, one asserting `toBeUndefined()` when the arg is omitted. In convex/runtimeIngest.test.ts: add an `extractLlmCallTraceId(data)` pure function returning `data.traceId ?? data.trace_id`, then add a `describe("runtimeIngest — llm_call traceId extraction")` block with three tests (camelCase hit, snake_case fallback, undefined when neither present) mirroring the extractLlmCallGoalId tests.

    After the source edits, run `npx convex codegen` (offline — regenerates convex/_generated/api.d.ts only; this is NOT a deploy per the Phase 88 Plan 02 precedent). Do NOT run `npx convex deploy` — prod deploy is operator-gated and lands in Plan 05.
  </action>
  <verify>
    <automated>npx vitest run convex/llm.test.ts convex/runtimeIngest.test.ts</automated>
    <automated>npx convex codegen && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `rg "traceId: v.optional\(v.string\(\)\)" convex/schema.ts` matches exactly one line inside the llmMetrics table
    - `rg "traceId: args.traceId" convex/llm.ts` matches (insert threads the arg)
    - `rg "d.traceId \?\? d.trace_id" convex/runtimeIngest.ts` matches
    - `rg -c "traceId" convex/schema.ts` shows no new `.index(` line was added (grep the index block: `rg "by_trace" convex/schema.ts` returns nothing)
    - `npx vitest run convex/llm.test.ts convex/runtimeIngest.test.ts` passes including the new traceId persistence + extraction describe blocks
    - `npx tsc --noEmit` exits 0 after `npx convex codegen`
    - `rg "traceId" convex/_generated/api.d.ts` matches (codegen picked up the new arg)
  </acceptance_criteria>
  <done>traceId is stored, ingested via both aliases, and codegen+tsc are green; new tests cover present/absent/alias cases.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add the sessionCalls query the waterfall reads (TRACE-02 read path)</name>
  <read_first>
    - convex/llm.ts (cacheStats by_session conditional branch, lines 76-83; costOverTime by_timestamp+order("asc") shape, lines 202-219; recentCalls, lines 117-127)
    - convex/llm.test.ts (mirrored-logic + makeLlmStore pattern established in Task 1)
    - convex/schema.ts (by_session index definition: `["sessionId", "timestamp"]`)
  </read_first>
  <behavior>
    - sessionCalls returns every non-archived llmMetrics row for the given sessionId
    - rows are returned in ascending timestamp order (chronological, for left-to-right bar rendering)
    - no rolling-window cutoff is applied (the Trace tab shows the whole session, unlike cacheStats)
    - each returned row includes the traceId field (undefined for legacy rows)
  </behavior>
  <action>
    Add a new `sessionCalls` query to convex/llm.ts. Args: `sessionId: v.string()`. Handler: query `llmMetrics` `.withIndex("by_session", (q) => q.eq("sessionId", args.sessionId))`, `.filter((q) => q.neq(q.field("archived"), true))`, `.order("asc")`, `.collect()`. Crucially, drop the `cutoff`/`.gte("timestamp", ...)` clause that cacheStats uses — the Trace tab wants the full session, not a rolling window (per PATTERNS.md §llm.ts). Return the raw rows array so the client can group by `traceId`. Do NOT add server-side grouping, cost estimation, or cache derivation — the component owns presentation (D-14 no-estimation, D-13 three-state cache live in the UI, not the query).

    Extend convex/llm.test.ts with a mirrored `sessionCallsLogic(ctx, args)` pure function (same no-convex-test convention) and a `describe("sessionCalls (Phase 94 TRACE-02)")` block asserting: (a) only rows matching sessionId are returned, (b) archived rows are excluded, (c) rows come back in ascending timestamp order, (d) a legacy row with no traceId is still returned with traceId undefined. Seed the mock store with a mix of traced/untraced/archived rows across two sessions.
  </action>
  <verify>
    <automated>npx vitest run convex/llm.test.ts</automated>
    <automated>npx convex codegen && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `rg "export const sessionCalls" convex/llm.ts` matches
    - `rg "by_session" convex/llm.ts` shows sessionCalls uses the by_session index
    - sessionCalls handler contains no `.gte(` timestamp cutoff and no `.take(` cap
    - `npx vitest run convex/llm.test.ts` passes the new sessionCalls describe block (session filter, archived exclusion, asc order, legacy-row traceId undefined)
    - `rg "sessionCalls" convex/_generated/api.d.ts` matches after codegen
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>api.llm.sessionCalls returns full-session, chronological, non-archived rows with traceId; tests green; codegen exposes it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Ástríðr emitter → `/runtime-ingest` | Untrusted `traceId` string crosses the network boundary as part of the `llm_call` payload |
| Convex mutation boundary | `recordCall` args validated by `v.` validators before any DB write |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-94-01 | Spoofing/Tampering | `/runtime-ingest` accepting a forged `traceId` to pollute another session's waterfall | mitigate | Inherited — the whole `/runtime-ingest` route is already Bearer-gated by `validateIngestAuth`; `traceId` rides the same authenticated `llm_call` payload as every other field. No new gate needed; do not add an unauthenticated increment path. |
| T-94-02 | Tampering | Non-string `traceId` reaching the DB | mitigate | `v.optional(v.string())` rejects non-string values at the mutation boundary; `traceId` is an opaque grouping key, never interpolated into a query. |
| T-94-SC | Tampering | npm/pip/cargo installs | accept | This plan installs ZERO new packages (RESEARCH.md Package Legitimacy Audit — N/A). No supply-chain surface introduced. |
</threat_model>

<verification>
- `npx vitest run convex/llm.test.ts convex/runtimeIngest.test.ts` green
- `npx convex codegen && npx tsc --noEmit` exit 0
- No new Convex index added (grep `by_trace` returns nothing)
- No prod deploy performed (that is Plan 05, operator-gated)
</verification>

<success_criteria>
- `llmMetrics` carries `traceId: v.optional(v.string())`; existing rows read back with `traceId === undefined`
- `/runtime-ingest` maps `traceId`/`trace_id` into `recordCall`
- `api.llm.sessionCalls` returns full-session chronological rows for the UI
- All new + existing Convex tests green; codegen + tsc clean
</success_criteria>

<output>
Create `.planning/phases/94-trace-waterfall/94-01-SUMMARY.md` when done
</output>
