---
phase: 94-trace-waterfall
plan: 05
type: execute
wave: 4
depends_on: [94-01, 94-02, 94-03, 94-04]
files_modified: []
autonomous: false
requirements: [TRACE-01, TRACE-02]
must_haves:
  truths:
    - "The traceId schema/ingest/query changes are deployed to prod Convex (tidy-whale-981)"
    - "The astridr stack running the emitter change is rebuilt and live"
    - "A real Ástríðr-emitted llm_call carrying a traceId lands in prod Convex and renders as a grouped turn in the waterfall"
    - "A pre-Phase-94 (legacy, no-traceId) session renders in the flat 'Untraced calls' bucket with no console errors"
  artifacts: []
  key_links:
    - from: "astridr emitter (Plan 02)"
      to: "prod Convex tidy-whale-981 /runtime-ingest"
      via: "live HTTP llm_call carrying traceId"
      pattern: "traceId"
    - from: "prod llmMetrics rows"
      to: "SessionDetail Trace tab waterfall"
      via: "grouped render (D-05 live gate)"
      pattern: "TraceWaterfall"
---

<objective>
Close the phase against the standing live-integration bar (D-05, carried from the Phase 90/93 lesson): convex-test/vitest green is NOT sufficient. This plan is the operator-gated deploy + live verification that a real Ástríðr-emitted `traceId` flows end-to-end and renders grouped in the waterfall, and that legacy untraced rows still render gracefully.

Purpose: Prove the two-repo feature actually works against the live stack, not just in mocks.
Output: Operator sign-off recorded in the SUMMARY; no code changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/94-trace-waterfall/94-CONTEXT.md
@.planning/phases/94-trace-waterfall/94-VALIDATION.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Operator deploy — prod Convex + astridr rebuild</name>
  <what-built>
    Plans 01-04 shipped and are green in vitest/tsc/build: llmMetrics.traceId schema + ingest alias + sessionCalls query (Plan 01), the astridr per-turn traceId contextvar + provider attach (Plan 02), the TraceWaterfall component (Plan 03), and the Trace tab + Analytics cleanup (Plan 04). None of it is live yet — prod Convex still runs the pre-traceId schema and the deployed astridr has no emitter.
  </what-built>
  <how-to-verify>
    Deploy both sides (both are operator-gated — do NOT let the agent auto-deploy prod):
    1. From C:/Users/mandr/codepulse, deploy Convex to prod: `npx convex deploy --yes` (targets tidy-whale-981). Confirm the deploy output lists the updated llm/runtimeIngest/schema modules and that codegen already ran in Plan 01.
    2. From C:/Users/mandr/astridr-repo, rebuild the astridr container so the emitter change (telemetry contextvar + provider attach) goes live: `docker compose up --build -d astridr` (per global CLAUDE.md deployment rule — rebuild, do not restart). If the war-room/other profiles are needed for your test session, bring them up per the operational note in STATE.md.
    3. Confirm astridr is healthy and the deployed CodePulse points at tidy-whale-981.
  </how-to-verify>
  <resume-signal>Type "deployed" once both prod Convex and the rebuilt astridr are live, or describe any deploy failure.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Live E2E verification — grouped trace + legacy fallback (D-05)</name>
  <what-built>
    With both sides live, a real chat turn should now generate LLM calls that carry a shared traceId, land in prod llmMetrics, and render as one grouped turn in the session's Trace tab. Legacy sessions (recorded before this phase) have no traceId and must render in the flat "Untraced calls" bucket without error.
  </what-built>
  <how-to-verify>
    1. Trigger a real Ástríðr chat turn (one user message → one loop iteration). If you want to also exercise Pitfall 2, trigger an automation/queen-driven run and confirm it is traced too (not stuck untraced).
    2. In CodePulse, open that session's detail page and select the Trace tab (or navigate directly to /sessions/{id}?tab=trace). Confirm: the turn's LLM calls appear grouped under ONE collapsible trace group; bars are in chronological order; each bar shows model + a real cost (or an honest "n/a" if cost was absent) + a cache badge (HIT "X% cached" / MISS / no badge); the summary strip shows totals.
    3. Cross-link check (D-08): from Analytics, click "View Trace" on a recent-calls row and confirm it deep-links to that session's Trace tab.
    4. Backward-compat check (Pitfall 4): open a PRE-Phase-94 session (one whose rows predate this deploy). Confirm its calls render in the flat "Untraced calls" group, in chronological order, with NO console errors and no crash.
    5. Open the browser console during steps 2-4 and confirm zero errors.
  </how-to-verify>
  <resume-signal>Type "verified" if grouped-trace render + legacy fallback + cross-link all pass with a clean console; otherwise describe what broke (with the session id and any console output) so gaps can be planned.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Live astridr emitter → prod `/runtime-ingest` | The only new runtime data path exercised end-to-end; auth is the existing Bearer gate |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-94-09 | Tampering | Deploying an untested schema/emitter combination to prod | mitigate | Deploy only after Plans 01-04 are green (vitest + tsc + build); this plan's checkpoint gates the deploy and the live verification, so a broken combination is caught before sign-off rather than silently accepted (the Phase 90 lesson). |
| T-94-SC | Tampering | package installs at deploy time | accept | No installs — `npx convex deploy` and `docker compose up --build` use already-pinned dependencies; zero new packages this phase. |
</threat_model>

<verification>
- Prod Convex (tidy-whale-981) runs the traceId schema/ingest/query; astridr rebuilt with the emitter
- A real traceId-carrying llm_call renders grouped in the waterfall (D-05 met)
- A legacy no-traceId session renders in the Untraced bucket with a clean console
- "View Trace" cross-link resolves to the session Trace tab
</verification>

<success_criteria>
- Both sides deployed (operator-gated, not auto-deployed)
- Live grouped-trace render confirmed against prod
- Backward-compatible legacy fallback confirmed with zero console errors
- Operator sign-off recorded — this is the bar that closes the phase per D-05
</success_criteria>

<output>
Create `.planning/phases/94-trace-waterfall/94-05-SUMMARY.md` when done, recording the operator sign-off, the verified session id(s), and any gaps found.
</output>
