---
phase: 116-galdr-prompt-library
plan: 05
type: execute
wave: 4
depends_on: ["116-04"]
files_modified: []
autonomous: false
requirements: []

user_setup:
  - service: convex-selfhosted
    why: "The galdr HTTP routes are bearer-gated; the backend needs the key before any skill call can succeed, and validateGaldrAuth fails closed without it (D-01)"
    env_vars:
      - name: GALDR_API_KEY
        source: "Generated by Larry (any high-entropy string). Set on the Convex backend with `npx convex env set GALDR_API_KEY <value>` run in his own elevated-free PowerShell window — NOT through the harness `!` prefix, which echoes the full command line into the transcript."
      - name: GALDR_API_KEY
        source: "The same value must also be reachable by the skill script (plan 116-07) as a user-level environment variable or in `~/.claude/skills/galdr/.env`."
    dashboard_config:
      - task: "Confirm the value landed by running `npx convex env list` and checking GALDR_API_KEY is present — the command prints names, and the value must not be pasted into the transcript"
        location: "Convex CLI against the self-hosted instance"

must_haves:
  truths:
    - "prompts and promptVersions exist on the LIVE self-hosted Convex instance, proven by deploy output naming 127.0.0.1:3210 and never .convex.cloud"
    - "convex/_generated/api.d.ts includes the galdr module, so the React plans can type api.galdr.list without exploring"
    - "GALDR_API_KEY is set on the backend, so D-01's fail-closed validator answers 200 rather than 401 for an authorized call"
    - "no npx convex import --replace-all is run against the live instance at any point"
  artifacts:
    - path: "convex/_generated/api.d.ts"
      provides: "typed api.galdr.* surface for the UI plans"
      contains: "galdr"
  key_links:
    - from: "npx convex deploy CLI output"
      to: "the self-hosted instance at 127.0.0.1:3210"
      via: "asserted substring in the printed deployment target"
      pattern: "127\\.0\\.0\\.1:3210"
---

<objective>
Push the Galdr schema and functions to the live self-hosted Convex backend, and prove from
the CLI's own output that they landed there and not on the retired cloud deployment.

Purpose: `npm run build` and `npx tsc --noEmit` both pass against generated types whose
tables were never created. Every downstream plan in this phase — the UI subscriptions, the
skill's HTTP calls — would then fail at runtime while the whole test suite stayed green. This
is the one irreversible action in the phase, so it runs inline in the main session with
Larry present, matching the Phase 107-05 precedent recorded in `116-RESEARCH.md` Pitfall 3.
Output: no repo file changes; a live schema, regenerated types, and recorded CLI evidence.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/116-galdr-prompt-library/116-RESEARCH.md
@CLAUDE.md

`CLAUDE.md` § "Self-Hosted Convex — Operational Rules" is binding for this plan. Read it
before running anything.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-flight — confirm the deploy target before touching it</name>
  <files></files>
  <read_first>
    - CLAUDE.md § "Self-Hosted Convex — Operational Rules" (the 2026-07-22 incident and the hard rules)
    - .planning/phases/116-galdr-prompt-library/116-RESEARCH.md § Pitfall 3 (the Phase 107-05 verification method)
    - convex/schema.ts (confirm the two new tables from plan 116-01 are present in the working tree)
  </read_first>
  <action>
Establish, and print, what the deploy is about to target — BEFORE running it. Do not infer
the target from configuration alone; a `convex.json` / env-file reading is a claim about
intent, and the CLI's own resolved output is the fact.

1. Confirm the backend container is up and answering: `docker ps --filter name=convex-backend`
   and a `curl` against the health path of the self-hosted instance. If it is not answering,
   STOP — do not restart or recreate anything; report the state and hand back. A wedged
   Convex gateway has its own escalation order in `CLAUDE.md` and is not this plan's job.
2. Print the resolved deployment URL the CLI will use, without pushing: run
   `npx convex env list` (it fails loudly against an unreachable or wrong target and prints
   the deployment it reached). Capture the printed target string.
3. Assert the captured target contains `127.0.0.1:3210` and does NOT contain `.convex.cloud`.
   If it names a cloud deployment, STOP and report — `116-RESEARCH.md` records that
   `npx convex` has historically resolved to the cloud deployment `tidy-whale-981` in some
   contexts, and those are two separate databases.
4. Confirm `GALDR_API_KEY` appears in the `npx convex env list` output. Report only whether
   the NAME is present — never print, echo, or copy the value.
5. Confirm `git status` is clean for `convex/` apart from this phase's own committed work, so
   the deploy pushes a known tree.

If step 4 shows `GALDR_API_KEY` absent, that is not a failure of this task — record it and
let the checkpoint in Task 2 handle it, since only Larry can set it.
  </action>
  <verify>
    <automated>docker ps --filter name=convex-backend --format "{{.Names}} {{.Status}}"</automated>
  </verify>
  <acceptance_criteria>
    - The `docker ps` output names a running `convex-backend` container with an `Up` status
    - The captured `npx convex env list` output is quoted in the task record and contains the literal `127.0.0.1:3210`
    - The same captured output does NOT contain `.convex.cloud`
    - Whether `GALDR_API_KEY` is present in the env list is recorded as an explicit yes or no
    - No value of any environment variable appears anywhere in the output
  </acceptance_criteria>
  <done>The target is proven from CLI output, not assumed from config, and recorded before anything is pushed.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: [BLOCKING] Deploy the schema to the live self-hosted instance</name>
  <files></files>
  <read_first>
    - The Task 1 pre-flight record (the captured target string)
    - CLAUDE.md § "Self-Hosted Convex — Operational Rules"
    - .planning/phases/116-galdr-prompt-library/116-RESEARCH.md § Pitfall 3
  </read_first>
  <what-built>
Plans 116-01 through 116-04 added two tables (`prompts`, `promptVersions`), one auth
validator, one domain module, four httpAction handlers, and four routes. All of it is
type-clean and unit-green against generated types — and none of it exists on the live
backend yet.
  </what-built>
  <action>
Run `npx convex deploy` from `C:/Users/mandr/codepulse` INLINE in the main session — not
through a delegated subagent. This is the one irreversible action in the phase, and the Phase
107-05 precedent recorded in `116-RESEARCH.md` Pitfall 3 ran it the same way for the same
reason: the authorization is native to the interactive session and the CLI's own output is the
only acceptable evidence of where it landed.

Never pass `--replace-all`. Never run `npx convex import` in any form. That flag deletes every
existing row first, which is what poisoned every index and took the dashboard down for days on
2026-07-21/22 (repo `CLAUDE.md`, Self-Hosted Convex Operational Rules).

Before the deploy, if Task 1 recorded `GALDR_API_KEY` as absent, Larry sets it himself with
`npx convex env set GALDR_API_KEY <value>` in his own PowerShell window — not through the
harness `!` prefix, which echoes the full command line, secret included, into the transcript.

After the deploy, prove four things from the CLI's own printed output and from live reads
rather than from configuration: the target contains `127.0.0.1:3210`; the output contains no
`.convex.cloud`; `npx convex run galdr:list` returns an empty array rather than an error; and a
paired unauthenticated / bearer-authenticated request against the HTTP-actions surface returns
401 and 200 respectively. The 401 on its own proves nothing — a missing route answers the same
way — so the 200 is the control that makes the 401 mean "the gate decided", and both must be
recorded. Read the HTTP-actions port from the running `convex-selfhost` compose config; it is
not 3210 and this plan deliberately does not assert which port it is.

Follow the steps in the how-to-verify block below in order and quote each result.
  </action>
  <how-to-verify>
1. If Task 1 reported `GALDR_API_KEY` absent, set it FIRST, in your own PowerShell window
   (not through the harness `!` prefix, which prints the full command line into the
   transcript):
   `npx convex env set GALDR_API_KEY <your-value>`
   Any high-entropy string works. Keep the same value for plan 116-07's skill script.
2. Run the deploy from `C:\Users\mandr\codepulse`:
   `npx convex deploy`
   Do NOT add `--replace-all`. Do NOT run `npx convex import` in any form. `--replace-all`
   deletes every existing row first and is what took the dashboard down for days on
   2026-07-21/22.
3. Read the CLI's own output and confirm all four of these, quoting them back:
   - the printed deployment target contains `127.0.0.1:3210`
   - the output contains NO occurrence of `.convex.cloud`
   - the output reports the schema push succeeded (e.g. a "Schema validation complete" or
     equivalent success line — quote whatever the installed CLI version actually prints
     rather than asserting a phrase from an older version)
   - the output does NOT report deleting any index
   If the target names a cloud deployment, stop immediately — nothing downstream in this
   phase is safe to run.
4. Confirm the tables are live by reading, not by inference:
   `npx convex run galdr:list` should return `[]` (an empty array, not an error). An empty
   array proves the table exists and is readable; an error proves it does not. Seeding is
   deliberately empty this phase, so `[]` is the expected healthy answer.
5. Confirm the HTTP surface answers with a real request against the self-hosted site URL
   (the HTTP-actions port, which is NOT 3210 — read it from the running container's compose
   config rather than assuming). Two calls, and compare them:
   - with no `Authorization` header → expect HTTP 401
   - with `Authorization: Bearer <the GALDR_API_KEY value>` → expect HTTP 200
   The 401 alone proves nothing; the 200 is the control that proves the 401 was a real
   authorization decision and not a missing route. Pipe the key in from a variable in your
   own shell — do not paste it into a command line that gets echoed.
6. Confirm `convex/_generated/api.d.ts` now mentions `galdr`, so the UI plans can type
   `api.galdr.list`.
  </how-to-verify>
  <verify>
    <automated>npx convex run galdr:list</automated>
  </verify>
  <acceptance_criteria>
    - `npx convex deploy` output is quoted in the summary and contains `127.0.0.1:3210`
    - That same quoted output contains zero occurrences of `.convex.cloud`
    - `npx convex run galdr:list` returns `[]` and exits 0
    - The unauthenticated GET returns 401 AND the bearer-authenticated GET returns 200 — both recorded, the 200 present as the control for the 401
    - `convex/_generated/api.d.ts` contains the string `galdr`
    - No `npx convex import` and no `--replace-all` appears anywhere in the session record
  </acceptance_criteria>
  <resume-signal>Type "deployed" once the deploy output has been quoted and all six checks pass, or describe what the output actually said.</resume-signal>
  <done>The two tables and four routes are live on the self-hosted instance, proven by a read that returns data and a paired 401/200 that proves the gate works.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local CLI → live production database | An irreversible schema and function push crosses here |
| operator shell → transcript | A bearer secret can leak into the session record here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-116-08 | Tampering / Denial of Service | `npx convex deploy` landing on the cloud deployment | mitigate | Target asserted twice — once pre-flight from `npx convex env list`, once from the deploy's own output — on the literal `127.0.0.1:3210` with an explicit `.convex.cloud` negative |
| T-116-18 | Denial of Service | a mass-delete import poisoning the MVCC tombstone GC | mitigate | `--replace-all` and `npx convex import` are forbidden by name in the checkpoint instructions; the acceptance criteria assert neither appears in the session record |
| T-116-19 | Information Disclosure | `GALDR_API_KEY` echoed into the transcript | mitigate | The key is set by Larry in his own shell, never through the harness `!` prefix; `npx convex env list` is used for NAME presence only; the curl control pipes the value from a shell variable |
| T-116-01 | Spoofing | routes live but unkeyed | mitigate | The paired 401/200 check proves the validator is both wired and satisfiable; a 401-only result would be indistinguishable from a missing route |
| T-116-SC | Tampering | npm/pip/cargo installs | mitigate | Zero new packages; `npx convex` invokes the already-installed `convex@^1.42.0` from `package.json` |
</threat_model>

<verification>
- Deploy output quoted, containing `127.0.0.1:3210` and no `.convex.cloud`
- `npx convex run galdr:list` returns `[]`
- Paired 401 (no header) / 200 (bearer) against the live HTTP surface, both recorded
</verification>

<success_criteria>
- The two tables exist on the live self-hosted instance and are readable
- The four routes answer, fail-closed without a key and 200 with one
- Generated types include `galdr`, unblocking plans 116-06 and 116-08
- No bulk import, no `--replace-all`, no secret in the transcript
</success_criteria>

<output>
Create `.planning/phases/116-galdr-prompt-library/116-05-SUMMARY.md` when done
</output>
