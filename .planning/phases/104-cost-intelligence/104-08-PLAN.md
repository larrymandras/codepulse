---
phase: 104-cost-intelligence
plan: 08
type: execute
wave: 4
depends_on: ["104-04", "104-05", "104-07"]
files_modified:
  - src/components/SDKSpendGuard.tsx
  - src/components/SDKSpendGuard.test.tsx
  - src/components/SDKSpendCapGauge.tsx
  - src/components/SDKSpendCapGauge.test.tsx
  - src/components/CostForecastPanel.tsx
  - src/components/CostForecastPanel.test.tsx
  - convex/forecasts.ts
  - convex/forecasts.test.ts
  - convex/seedGateway.ts
  - src/pages/Settings.tsx
autonomous: true
requirements: [COST-02]

must_haves:
  truths:
    - "D-12: SDKSpendGuard reads its limit and warn fraction from the global daily costBudgets row, and the hardcoded DAILY_CAP and ALERT_THRESHOLD constants no longer exist on any render path"
    - "D-12: no second spend gauge was added beside SDKSpendGuard — it was rewired in place, not duplicated"
    - "D-19: CostForecastPanel reads its monthly limit from the global monthly costBudgets row, and forecasts.ts no longer reads agentConfigs intelligence.budget_cap on any render path"
    - "D-19: the legacy budget-cap form in Settings is retired in favour of the single Cost and Budgets tab, so four cap-editing surfaces collapse to one"
    - "D-11: both panels classify against the budget row's own warnFraction rather than a hardcoded 0.8"
    - "D-16: neither panel gains any control that stops, throttles or swaps anything — the rewire changes the data source, not the behavior"
  artifacts:
    - path: "src/components/SDKSpendGuard.tsx"
      provides: "daily spend gauge sourced from costBudgets, sparkline colored from theme tokens"
      contains: "useCostBudget"
    - path: "convex/forecasts.ts"
      provides: "costForecast reading its monthly cap from costBudgets"
      contains: "costBudgets"
    - path: "src/components/CostForecastPanel.tsx"
      provides: "same visual output, budget row as the data source"
  key_links:
    - from: "src/components/SDKSpendGuard.tsx"
      to: "api.costBudgets.getByScope"
      via: "useCostBudget('global', '', 'daily')"
      pattern: "getByScope|useCostBudget"
    - from: "convex/forecasts.ts"
      to: "costBudgets by_scope_key_period index"
      via: "global monthly budget row lookup"
      pattern: "by_scope_key_period"
---

<objective>
Collapse every remaining spend-cap source onto the `costBudgets` table: `SDKSpendGuard`'s
hardcoded constants (D-12), `CostForecastPanel`/`forecasts.ts`'s
`agentConfigs["intelligence.budget_cap"]` (D-19), and the legacy budget-cap form still sitting in
`Settings.tsx:135-153` that neither CONTEXT.md nor RESEARCH.md named.

Purpose: two caps that can disagree is the stale-second-source pattern D-12 exists to prevent. This
repo currently has FOUR editing or reading surfaces for a spend cap. After this plan there is one.
Output: both panels keep their exact visual output and change only where their numbers come from.

⚠ ORDERING: `npx convex run costBudgets:seedFromLegacyCaps '{}'` (plan 104-04) MUST have been run
against the target deployment before these panels are useful. If it has not, both panels correctly
render their "no budget configured" state — verify that state renders honestly rather than as $0.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/104-cost-intelligence/104-CONTEXT.md
@.planning/phases/104-cost-intelligence/104-UI-SPEC.md
@.planning/phases/104-cost-intelligence/104-PATTERNS.md
@.planning/phases/104-cost-intelligence/104-04-SUMMARY.md
@.planning/phases/104-cost-intelligence/104-07-SUMMARY.md
@CLAUDE.md

<interfaces>
<!-- Contracts consumed -->
api.costBudgets.getByScope({ scope, scopeKey, period }) -> BudgetRow | null   (plan 104-04)
src/hooks/useCostBudgets.ts: useCostBudget(scope, scopeKey, period) -> BudgetRow | null | undefined  (plan 104-07)
src/hooks/useThemeColors.ts:
  export function resolveThemeColors(): ThemeColors;   // statusOk / statusWarn / statusError / statusInfo
  export function useThemeColors(): ThemeColors;       // re-resolves on data-theme change

<!-- Existing exports being retired or changed -->
src/components/SDKSpendGuard.tsx:  DAILY_CAP, ALERT_THRESHOLD  (retire)
                                   classifyCapStatus, projectDayEndSpend  (keep, already parameterized)
src/components/SDKSpendCapGauge.tsx:  re-exports the two constants (must be updated in lockstep)
convex/forecasts.ts:  getBudgetConfig (query), setBudgetCap (mutation)  (retire from render paths)
convex/seedGateway.ts:  local DAILY_CAP/ALERT_THRESHOLD literals feeding an alertRuleCustom seed
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewire SDKSpendGuard onto the global daily budget row and move its sparkline onto theme tokens</name>
  <files>src/components/SDKSpendGuard.tsx, src/components/SDKSpendGuard.test.tsx, src/components/SDKSpendCapGauge.tsx, src/components/SDKSpendCapGauge.test.tsx</files>
  <read_first>
    - src/components/SDKSpendGuard.tsx (the full file — the `DAILY_CAP`/`ALERT_THRESHOLD` constants at :8-9, their five usages at :31, :34, :82, :83, :109, :146, the hardcoded sparkline hex ternary at :90-93, and the existing `bg-[--status-*]` bar classes at :85-88 which are already correct)
    - src/components/SDKSpendCapGauge.tsx (the two-line re-export file that must move in lockstep)
    - src/components/SDKSpendGuard.test.tsx and src/components/SDKSpendCapGauge.test.tsx (both assert `DAILY_CAP === 5.00` and `ALERT_THRESHOLD === 0.8` today — those assertions are what this task replaces)
    - src/hooks/useThemeColors.ts (lines 41-106 — `resolveThemeColors` and the `useThemeColors` hook with its `MutationObserver` on `data-theme`; this already exists specifically because `Sparkline`'s SVG stroke cannot read CSS vars)
    - src/hooks/useCostBudgets.ts (plan 104-07's `useCostBudget`, including its loading-vs-null distinction)
    - .planning/phases/104-cost-intelligence/104-UI-SPEC.md (the Color section's mandatory remediation bullet for this file, and the instruction NOT to restyle its heading)
  </read_first>
  <action>
In `src/components/SDKSpendGuard.tsx`:

1. DELETE `export const DAILY_CAP` and `export const ALERT_THRESHOLD`. They survive only as the
   seed values already written into `costBudgets` by plan 104-04's `seedFromLegacyCaps`.
2. `projectDayEndSpend` currently closes over the module constant. Change its signature to
   `projectDayEndSpend(todaySpend: number, elapsedHours: number, cap: number)` and use the
   parameter in both the `willExceedCap` comparison and the `projectedHitTime` formula. Keep it
   pure and exported — it is D-13's algorithm and `convex/costBudgetEval.ts` mirrors it.
   `classifyCapStatus` already takes `cap` and `alertThreshold` as parameters; leave it unchanged.
3. In the component, read `const budget = useCostBudget("global", "", "daily")`.
   Three distinct states, all rendered distinctly — this is the D-03 honesty rule applied to a cap:
   - `budget === undefined` → the existing skeleton/pulse loading block, unchanged.
   - `budget === null` → a NO-BUDGET state: render today's spend figure and the sparkline, but NO
     gauge bar, NO percentage, NO projection, and the copy
     `"No daily budget set. Set one in Settings → Cost & Budgets."` with a link to
     `/settings` . Do NOT fall back to 5.00 and do NOT render a 0%/empty bar as if it were a real
     zero — a gauge against a cap nobody set is a fabricated threshold.
   - otherwise → today's behavior, with `budget.limit` in place of `DAILY_CAP` and
     `budget.warnFraction` in place of `ALERT_THRESHOLD` at every one of the former usage sites,
     including the `of {formatCost(...)} today` label and the projection sentence.
4. Replace the hardcoded `sparklineColor` hex ternary with `useThemeColors()`:
   `const theme = useThemeColors();` then
   `status === "exceeded" ? theme.statusError : status === "warning" ? theme.statusWarn : theme.statusOk`.
   Do not hand-roll a `getComputedStyle` helper — this hook already exists for exactly this reason.
5. Do NOT restyle the heading. UI-SPEC is explicit that components touched only for a data-source
   rewire keep their current heading style so the diff stays scoped to the behavior change.
6. Do NOT add a second gauge, a second cap display, or any new panel. D-12 forbids it.

In `src/components/SDKSpendCapGauge.tsx`: update the re-export line to drop the two deleted
constants, keeping `classifyCapStatus`. Update `src/components/SDKSpendCapGauge.test.tsx` to remove
its two constant assertions rather than leaving a test asserting a value the product no longer has.

Rewrite `src/components/SDKSpendGuard.test.tsx`:
- delete the `DAILY_CAP === 5.00` / `ALERT_THRESHOLD === 0.8` assertions;
- `projectDayEndSpend(spend, hours, cap)` flags `willExceedCap` against the passed cap, asserted
  with two different cap values so a reintroduced module constant would fail;
- with `useCostBudget` mocked to return `{ limit: 12, warnFraction: 0.5, ... }`, the rendered
  percentage and status classify against 12 and 0.5, not 5 and 0.8;
- with `useCostBudget` mocked to `null`, the component renders the no-budget copy and does NOT
  render a gauge bar or a percentage — assert the absence explicitly, and assert the string `$5`
  appears nowhere in the output;
- with `useCostBudget` mocked to `undefined`, the loading skeleton renders;
- the sparkline `color` prop equals the mocked `useThemeColors().statusOk` value for an ok status.
  </action>
  <verify>
    <automated>npx vitest run src/components/SDKSpendGuard.test.tsx src/components/SDKSpendCapGauge.test.tsx</automated>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>
`SDKSpendGuard` classifies against a `costBudgets` row, renders an honest no-budget state, colors
its sparkline from theme tokens, and no longer exports or reads a hardcoded cap.
  </done>
  <acceptance_criteria>
    - `grep -c 'DAILY_CAP\|ALERT_THRESHOLD' src/components/SDKSpendGuard.tsx` returns 0
    - `grep -c 'DAILY_CAP\|ALERT_THRESHOLD' src/components/SDKSpendCapGauge.tsx` returns 0
    - `grep -cE '#[0-9a-fA-F]{6}' src/components/SDKSpendGuard.tsx` returns 0
    - `grep -c 'useThemeColors' src/components/SDKSpendGuard.tsx` returns at least 1
    - `grep -c 'useCostBudget' src/components/SDKSpendGuard.tsx` returns at least 1
    - `npx vitest run src/components/SDKSpendGuard.test.tsx src/components/SDKSpendCapGauge.test.tsx` exits 0
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Rewire costForecast and CostForecastPanel onto the global monthly budget row (D-19)</name>
  <files>convex/forecasts.ts, convex/forecasts.test.ts, src/components/CostForecastPanel.tsx, src/components/CostForecastPanel.test.tsx</files>
  <read_first>
    - convex/forecasts.ts (the full file — `classifyBudgetStatus` at :38-47, the `agentConfigs` budget-cap read at :87-92, the `getBudgetConfig` query at :126-137, and the `setBudgetCap` mutation at :140-172 with its identity gate and range check)
    - src/components/CostForecastPanel.tsx (the full file — the three stat boxes, progress bar and sparkline whose visual output must not change)
    - convex/costBudgets.ts (plan 104-04 — `getByScope` and the `by_scope_key_period` index)
    - convex/forecasts.test.ts (existing coverage of `classifyBudgetStatus` and `costForecast`)
    - .planning/phases/104-cost-intelligence/104-RESEARCH.md (the "⚠ Gap in CONTEXT.md coverage" section)
    - .planning/phases/104-cost-intelligence/104-UI-SPEC.md (Component Inventory row for `CostForecastPanel`: same visual output, new data source only; do not restyle)
  </read_first>
  <action>
In `convex/forecasts.ts`:
- In `costForecast`'s handler, replace the `agentConfigs` `by_key` read of
  `"intelligence.budget_cap"` with a `costBudgets` read on the `by_scope_key_period` index for
  `("global", "", "monthly")`. `budgetCap` becomes `row?.limit ?? null`.
- Generalize `classifyBudgetStatus(projectedMonthly, budgetCap)` to
  `classifyBudgetStatus(projectedMonthly, budgetCap, warnFraction = 0.8)` and use `warnFraction`
  in place of the hardcoded `0.8` at the warning tier, passing `row?.warnFraction ?? 0.8` from the
  handler. The default keeps every existing call site and test behaving identically (D-11).
- Return the `warnFraction` alongside `budgetCap` in the query result so the panel can render the
  warn tier honestly instead of assuming 80%.
- Mark `getBudgetConfig` and `setBudgetCap` as DEPRECATED with a comment naming D-19 and pointing
  at `api.costBudgets`. Do NOT delete them in this task — Task 3 removes their last caller, and
  deleting an exported Convex function while a deployed client may still reference it is a
  deploy-order hazard. Do NOT change their behavior; they keep writing `agentConfigs`, which is now
  read by nothing.
- Do NOT delete the `agentConfigs["intelligence.budget_cap"]` row itself.

In `src/components/CostForecastPanel.tsx`:
- No layout, heading, spacing or color change. UI-SPEC is explicit: same visual output, new data
  source only.
- Where the panel currently renders a budget progress bar and its "On track / Near limit / Over
  budget" labels, drive the warn threshold from the query's new `warnFraction` field.
- Add the same three-state honesty handling as Task 1: while the query is `undefined`, the existing
  loading state; when `budgetCap` is `null`, render the projection figures but NOT the progress bar
  or a status label, with the copy
  `"No monthly budget set. Set one in Settings → Cost & Budgets."` A progress bar against a null
  cap must not render as 0% or 100%.

Extend `convex/forecasts.test.ts`:
- `classifyBudgetStatus` with an explicit `warnFraction` of `0.5` returns `"warning"` at 50%;
- `classifyBudgetStatus` called with two arguments still warns at 80% (the back-compat guard);
- `costForecast` reads its cap from a `costBudgets` fixture and returns `null` when no global
  monthly row exists;
- `costForecast` does NOT read `agentConfigs` — assert on the fake ctx's recorded index queries
  that no `by_key` lookup for `"intelligence.budget_cap"` occurred.

Create `src/components/CostForecastPanel.test.tsx`:
- with a mocked `costForecast` returning `budgetCap: 120, warnFraction: 0.8`, the progress bar and
  a status label render;
- with `budgetCap: null`, no progress bar and no status label render, the no-budget copy does, and
  no `$0.00` cap figure appears anywhere in the output;
- with the query `undefined`, the existing loading text renders.
  </action>
  <verify>
    <automated>npx vitest run convex/forecasts.test.ts src/components/CostForecastPanel.test.tsx</automated>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>
`costForecast` sources its monthly cap and warn fraction from `costBudgets`, the panel renders the
same visuals, and a missing budget renders as a stated absence rather than a zero.
  </done>
  <acceptance_criteria>
    - `grep -c 'intelligence.budget_cap' convex/forecasts.ts` returns 3 — only inside `getBudgetConfig`, `setBudgetCap` and their deprecation comment, never inside `costForecast`
    - `grep -c 'costBudgets' convex/forecasts.ts` returns at least 1
    - `grep -c 'by_scope_key_period' convex/forecasts.ts` returns at least 1
    - `grep -c 'DEPRECATED' convex/forecasts.ts` returns at least 2
    - `git diff src/components/CostForecastPanel.tsx | grep -cE '^[-+].*(text-2xl|space-y-|grid-cols|font-semibold)'` returns 0 — no layout or typography change
    - `npx vitest run convex/forecasts.test.ts src/components/CostForecastPanel.test.tsx` exits 0
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Retire the last legacy cap-editing surfaces and prove exactly one cap source remains</name>
  <files>src/pages/Settings.tsx, convex/seedGateway.ts</files>
  <read_first>
    - src/pages/Settings.tsx (lines 125-175 — the pre-existing budget-cap form using `useQuery(api.forecasts.getBudgetConfig)` at :135 and `useMutation(api.forecasts.setBudgetCap)` at :136-153; and your plan 104-07 `cost-budgets` tab)
    - convex/seedGateway.ts (lines 1-40 — the local `DAILY_CAP` / `ALERT_THRESHOLD` literals at :5-6 feeding an `alertRuleCustom` threshold seed at :33)
    - src/components/CostBudgetsAdmin.tsx (plan 104-07 — the replacement surface)
    - convex/alerts.test.ts (lines 1-20 — a local re-declaration of the two constants inside a test; decide and record whether it stays)
    - .planning/phases/104-cost-intelligence/104-CONTEXT.md (D-12 and D-19)
  </read_first>
  <action>
RESEARCH.md found a third cap source. This task closes a FOURTH that neither document named:
`Settings.tsx` already renders its own budget-cap input wired to `api.forecasts.getBudgetConfig` /
`setBudgetCap`. Shipping the new Cost & Budgets tab while that form still writes a value nothing
reads leaves an operator editing a number with no effect — a worse failure than the disagreement
D-19 was written about.

1. `src/pages/Settings.tsx` — remove the legacy budget-cap form: its `useQuery(api.forecasts.getBudgetConfig)`,
   its `useMutation(api.forecasts.setBudgetCap)`, its local state, its save handler, and its JSX.
   In its place put a single line of text where the form was:
   `"Budget thresholds moved to the Cost & Budgets tab."` with a control that switches the `Tabs`
   value to `cost-budgets`. Do not leave a disabled input.
2. `convex/seedGateway.ts` — the two local literals feed an `alertRuleCustom` `cost_per_hour`
   threshold seed, which is a DIFFERENT concept from a daily budget and is evaluated by the
   disabled `evaluateInternal` cron. Do not rewire it onto `costBudgets`. Instead rename the local
   constants to `LEGACY_SEED_DAILY_CAP` / `LEGACY_SEED_ALERT_THRESHOLD` and add a comment stating
   that they are a one-time seed for an `alertRuleCustom` rule, that they are NOT the product's
   spend cap (which lives in `costBudgets`), and that the rule they seed only fires while the
   Alerts page is open because its evaluator cron is disabled. Renaming is what makes the
   "exactly one cap source" grep gate below meaningful.
3. `convex/alerts.test.ts`'s local `DAILY_CAP`/`ALERT_THRESHOLD` declarations are test-local
   arithmetic fixtures, not a product cap. Leave them, and record that decision in the summary so
   the grep gate's exemption is deliberate rather than an oversight.

THE ONE-CAP-SOURCE GATE (104-VALIDATION.md's D-12/D-19 row). Run and record the output of:
`grep -rn "DAILY_CAP\|ALERT_THRESHOLD\|intelligence.budget_cap" src/ convex/ --include=*.ts --include=*.tsx | grep -v node_modules`
Every surviving hit must fall into exactly one of three categories, and the summary must classify
each one: (a) inside `convex/forecasts.ts`'s two DEPRECATED functions, (b) the renamed
`LEGACY_SEED_*` constants in `convex/seedGateway.ts`, (c) test-local fixtures in
`convex/alerts.test.ts`. A hit on ANY render path — a component, a hook, or a non-deprecated Convex
query — is a failure of this task.
  </action>
  <verify>
    <automated>npx vitest run src/components/SDKSpendGuard.test.tsx src/components/CostForecastPanel.test.tsx src/components/CostBudgetsAdmin.test.tsx convex/forecasts.test.ts</automated>
    <automated>npm run build</automated>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>
No component, hook or live Convex query reads a hardcoded cap or the legacy `agentConfigs` cap; the
only cap-editing surface is the Cost &amp; Budgets tab.
  </done>
  <acceptance_criteria>
    - `grep -rn 'DAILY_CAP\|ALERT_THRESHOLD\|intelligence.budget_cap' src/ --include=*.ts --include=*.tsx | grep -v node_modules | wc -l` returns 0
    - `grep -c 'getBudgetConfig\|setBudgetCap' src/pages/Settings.tsx` returns 0
    - `grep -c 'LEGACY_SEED_DAILY_CAP' convex/seedGateway.ts` returns at least 1
    - `grep -c 'Cost & Budgets' src/pages/Settings.tsx` returns at least 2
    - `npm run build` exits 0
    - `npx tsc --noEmit` exits 0
    - `npm test` exits 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Convex `costBudgets` row → rendered gauge and forecast | A budget row is the sole input deciding whether an operator sees "on track" or "over budget"; a missing row must not silently become a default. |
| deprecated `forecasts.setBudgetCap` mutation | Still deployed and still authenticated, but now writes a value nothing reads. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-104-34 | Repudiation | `SDKSpendGuard` / `CostForecastPanel` rendering a cap nobody configured | mitigate | Both panels distinguish loading (`undefined`), no-budget (`null`) and configured states, and render no gauge, no percentage and no projection in the no-budget case. Unit tests assert the absence of a gauge and that no `$5` or `$0.00` cap figure appears. Falling back to the old hardcoded 5.00 is explicitly forbidden. |
| T-104-35 | Tampering | a stale second cap source reappearing | mitigate | The one-cap-source grep gate in Task 3 fails on any `DAILY_CAP`, `ALERT_THRESHOLD` or `intelligence.budget_cap` hit under `src/`, and every surviving `convex/` hit must be classified into one of three named exemptions in the summary. |
| T-104-36 | Tampering | deprecated `setBudgetCap` still writable by an authenticated caller | accept | It retains its existing `ctx.auth.getUserIdentity()` gate and its range check, and nothing reads what it writes, so a write is inert. Deleting an exported Convex function while a deployed client bundle may still call it is a worse deploy-order hazard than leaving it inert for one phase; plan 104-11 records it as a Phase 106 cleanup item. |
| T-104-37 | Denial of Service | new reads added by the rewire | mitigate | Each panel adds exactly one single-row indexed `costBudgets` read via `by_scope_key_period`; `costForecast`'s existing 30-day index-bounded `aggregates` read is unchanged. No unbounded read is introduced. |
</threat_model>

<verification>
- `npm test` exits 0 (full suite — this plan changes exported symbols other tests import).
- `npm run build` and `npx tsc --noEmit` exit 0.
- The one-cap-source grep returns zero hits under `src/`, and every `convex/` hit is classified in
  the summary.
- Manual code review: `CostForecastPanel.tsx`'s diff contains no layout, spacing or typography
  change (UI-SPEC's explicit constraint on rewire-only edits).
- Live confirmation that both panels render real numbers after `seedFromLegacyCaps` is DEFERRED to
  plan 104-11.
</verification>

<success_criteria>
- Exactly one cap source exists on every render path: the `costBudgets` table.
- Both panels keep their current visual output and gain an honest no-budget state.
- `SDKSpendGuard`'s sparkline repaints correctly across all six themes because it reads tokens.
- The legacy Settings cap form is gone, not disabled.
</success_criteria>

<output>
Create `.planning/phases/104-cost-intelligence/104-08-SUMMARY.md` when done. Include the full
classified output of the one-cap-source grep gate.
</output>
