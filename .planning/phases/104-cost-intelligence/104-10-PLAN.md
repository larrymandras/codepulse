---
phase: 104-cost-intelligence
plan: 10
type: execute
wave: 3
depends_on: ["104-05"]
files_modified:
  - src/components/CostTrendChart.tsx
  - src/components/CostTrendChart.test.tsx
  - src/components/CostBreakdown.tsx
  - src/components/CostBreakdown.test.tsx
autonomous: true
requirements: [COST-01]

must_haves:
  truths:
    - "D-08: one over-time chart carries a Billed / Billed + Covered toggle, defaults to Billed, and reuses FlexBarChart's stacked-segment shape rather than adding a second chart"
    - "D-08: the default Billed view never displays imputed money"
    - "D-05: the covered portion renders as a visually distinct segment and is never merged into the billed segment for a provider"
    - "D-01: the chart's dollar figures come from the costDerived recomputation rather than the pre-baked cost buckets"
    - "D-03: a bucket whose tokens could not be priced is surfaced in the chart's tooltip or caption rather than silently vanishing from the series"
  artifacts:
    - path: "src/components/CostTrendChart.tsx"
      provides: "billed/covered toggle over the derived cost series, on design tokens"
      contains: "Billed + Covered"
    - path: "src/components/CostTrendChart.test.tsx"
      provides: "toggle behavior and never-merged-headline coverage"
    - path: "src/components/CostBreakdown.tsx"
      provides: "goal-scoped breakdown rendered entirely from design tokens"
  key_links:
    - from: "src/components/CostTrendChart.tsx"
      to: "api.costDerived.costOverTime"
      via: "useQuery replacing the costByPeriodByProvider read"
      pattern: "costOverTime"
---

<objective>
Give the cost-over-time chart its D-08 `Billed` / `Billed + Covered` toggle on top of the derived
series, and complete the mandatory hex-to-token remediation on the two cost components CLAUDE.md
and UI-SPEC both flag as current violations.

Purpose: Phase 103 made a swap to a subscription engine a one-click action, so billed spend can now
collapse for a reason that has nothing to do with usage. The covered segment is what makes that
drop explain itself — without ever merging into the billed number an operator reads as money owed.
Output: `CostTrendChart` toggles between two series; both components render entirely from tokens.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/104-cost-intelligence/104-CONTEXT.md
@.planning/phases/104-cost-intelligence/104-UI-SPEC.md
@.planning/phases/104-cost-intelligence/104-PATTERNS.md
@.planning/phases/104-cost-intelligence/104-05-SUMMARY.md
@CLAUDE.md

<interfaces>
<!-- Backend contract consumed (plan 104-05) -->
api.costDerived.costOverTime({ period, lookbackHours? })
  -> Array<{ bucket_start: number;
             billedByProvider: Record<string, number>;
             coveredByProvider: Record<string, number>;
             unpricedTokensByProvider: Record<string, number> }>

<!-- Existing shapes reused -->
src/components/FlexBarChart.tsx:
  export interface StackedSegment { value: number; color: string; label: string }
  interface FlexBarSegment { label: string; value?: number; max?: number; segments?: StackedSegment[] }

src/lib/providers.ts:  PROVIDER_COLORS, PROVIDER_DISPLAY_NAMES
  // PROVIDER_COLORS is the ONE hex palette exempt from the no-hex rule (per-provider identity,
  // not theme chrome) — UI-SPEC Color section. It stays hardcoded and unchanged.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the Billed / Billed + Covered toggle and move CostTrendChart onto the derived series and design tokens</name>
  <files>src/components/CostTrendChart.tsx, src/components/CostTrendChart.test.tsx</files>
  <read_first>
    - src/components/CostTrendChart.tsx (the full file — the `costByPeriodByProvider` read at :14-17, the `data` mapping into `StackedSegment[]` at :18-24 with its `PROVIDER_COLORS[provider] ?? "#6b7280"` fallback, and the hardcoded `bg-gray-800/50 border-gray-700/50` wrapper at :28 and :39)
    - src/components/FlexBarChart.tsx (lines 1-40 — the `StackedSegment` and `FlexBarSegment` contracts, and how a stacked bar is rendered)
    - src/lib/providers.ts (`PROVIDER_COLORS`, `PROVIDER_DISPLAY_NAMES`)
    - src/pages/Analytics.tsx (lines 285-292 — confirm this component is already wrapped in a `GlassPanel`, which is why its own surface wrapper is redundant)
    - src/components/ui/tabs.tsx and src/components/ui/button.tsx (the two already-installed primitives UI-SPEC names as the acceptable toggle compositions)
    - .planning/phases/104-cost-intelligence/104-UI-SPEC.md (Color section's mandatory remediation bullets for this file, the Copywriting Contract toggle labels, and the covered-segment rendering rule: same provider hex at reduced opacity with a dashed top border)
  </read_first>
  <action>
In `src/components/CostTrendChart.tsx`:

1. DATA SOURCE — replace the `api.aggregates.costByPeriodByProvider` read with
   `useQuery(api.costDerived.costOverTime, { period: "hourly", lookbackHours: 24 })`. The old query
   returns pre-baked ingested dollars; D-01 makes the derived series the truth the UI renders. Do
   NOT delete `costByPeriodByProvider` from `convex/aggregates.ts` — other consumers exist and
   plan 104-05 left it in place deliberately.

2. TOGGLE (D-08) — a two-state segmented control with the exact labels `Billed` and
   `Billed + Covered`, placed in the existing header row next to the `InfoTooltip`. Default state
   is `Billed`. Compose it from an already-installed primitive — a two-item shadcn `Tabs` row or a
   `Button` pair with `variant="default"` / `variant="outline"`; UI-SPEC leaves the choice open and
   both primitives are already present, so do not install anything. The active pill uses
   `--primary` (one of the four roles UI-SPEC reserves the accent for). Give the control an
   accessible group label (`aria-label="Cost series"`) and make each option a real button or tab,
   keyboard reachable.

3. SERIES CONSTRUCTION —
   - `Billed` (default): one `StackedSegment` per provider from `billedByProvider`, coloured
     `PROVIDER_COLORS[provider] ?? "#6b7280"` (keep the existing fallback; `PROVIDER_COLORS` is the
     documented hex exemption), labelled `PROVIDER_DISPLAY_NAMES[provider] ?? provider`. This view
     must contain NO covered value at all — D-08's default view never displays imputed money.
   - `Billed + Covered`: the same billed segments PLUS, per provider, a second segment from
     `coveredByProvider`, rendered in the SAME `PROVIDER_COLORS[provider]` hex at reduced opacity
     via `color-mix(in srgb, ${color} 35%, transparent)`, labelled
     `` `${displayName} (covered)` ``. Do not invent a second colour scale. The two segments stay
     separate entries in the `segments` array — never summed into one provider value (D-05).
   - Sort segments so all billed segments precede all covered segments within a bar, so the covered
     portion always reads as an appended explanation rather than as part of the billed stack.

4. UNPRICED VISIBILITY (D-03) — when any bucket in the window has a non-zero
   `unpricedTokensByProvider` entry, render a caption under the chart:
   `"{n} tokens in this window aren't priced and aren't in this chart."` Do not drop unpriced
   traffic silently and do not add it as a zero-valued segment.

5. HEX REMEDIATION (mandatory, CLAUDE.md §Styling + UI-SPEC) — replace the wrapper
   `bg-gray-800/50 border border-gray-700/50` with `bg-card border-border`. UI-SPEC additionally
   notes this component is already wrapped in a `GlassPanel` by `Analytics.tsx`, so prefer removing
   the redundant surface wrapper entirely and keeping only the layout classes (`rounded-xl p-4`
   included in the parent). If removing it changes the rendered spacing, keep `bg-card border-border`
   rather than reintroducing a grey literal. Every other colour in the file must be a token; the
   only permitted hex are the `PROVIDER_COLORS` values and the existing `"#6b7280"` fallback, both
   of which come from `src/lib/providers.ts`'s exempt palette.

6. Keep the existing empty-state copy `"No cost data yet."` unchanged.
7. This component keeps its CURRENT mono eyebrow heading style — UI-SPEC only pins the new
   panel-title style to components this phase ADDS.

Create `src/components/CostTrendChart.test.tsx` asserting:
- default render shows the `Billed` option active and passes `FlexBarChart` segments whose count
  equals the number of providers with billed spend — with zero covered segments present;
- clicking `Billed + Covered` adds one segment per provider with covered spend, and the billed
  segment values are UNCHANGED (the never-merged guard: assert the billed segment's `value` before
  and after the toggle are equal);
- no `FlexBarSegment` in either mode has a `value` equal to `billed + covered` for a provider (D-05);
- a covered segment's `color` is derived from the same `PROVIDER_COLORS` entry as its billed
  sibling (assert the provider hex appears inside the covered segment's colour string);
- a window with unpriced tokens renders the caption;
- an empty result renders `No cost data yet.`;
- the toggle group has an accessible name and both options are reachable as buttons/tabs;
- the source file contains no hex outside the `PROVIDER_COLORS` import path and the `#6b7280`
  fallback — assert with a source-level regex allowing exactly that one literal.
  </action>
  <verify>
    <automated>npx vitest run src/components/CostTrendChart.test.tsx</automated>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>
The chart defaults to billed-only, adds a visually distinct covered segment on toggle without ever
changing the billed values, states excluded unpriced tokens, and carries no theme hex.
  </done>
  <acceptance_criteria>
    - `grep -c 'Billed + Covered' src/components/CostTrendChart.tsx` returns at least 1
    - `grep -c 'costOverTime' src/components/CostTrendChart.tsx` returns at least 1
    - `grep -c 'bg-gray-800\|border-gray-700' src/components/CostTrendChart.tsx` returns 0
    - `grep -oE '#[0-9a-fA-F]{6}' src/components/CostTrendChart.tsx | sort -u` outputs at most the single line `#6b7280`
    - `grep -c 'color-mix' src/components/CostTrendChart.tsx` returns at least 1
    - `grep -c 'aria-label' src/components/CostTrendChart.tsx` returns at least 1
    - `npx vitest run src/components/CostTrendChart.test.tsx` exits 0
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Complete the hex-to-token remediation on CostBreakdown</name>
  <files>src/components/CostBreakdown.tsx, src/components/CostBreakdown.test.tsx</files>
  <read_first>
    - src/components/CostBreakdown.tsx (the full file — including plan 104-05's data-source rewire; the violations are at :52-64 (`bg-[#10b981]`, `text-[#10b981]`), :88-115 (`bg-amber-500/10`, `text-amber-300`) and :180-198)
    - src/components/MetricCard.tsx (lines 60-140 — the `severityConfig` + `color-mix` pattern that IS the standard to copy)
    - src/components/AlertRulesEngine.tsx (lines 100-260 — the older hardcoded `red-400`/`orange-400`/`yellow-400` Tailwind-scale pattern; UI-SPEC explicitly names this as legacy and forbids propagating it)
    - .planning/phases/104-cost-intelligence/104-UI-SPEC.md (Color section's mandatory remediation bullet for this file, with the exact token mapping)
    - src/components/CostBreakdown.test.tsx (as updated by plan 104-05)
  </read_first>
  <action>
Apply UI-SPEC's exact token mapping to `src/components/CostBreakdown.tsx`. This is a
colour-only change — do NOT alter the data flow plan 104-05 established, do not rename fields, and
do not restructure the table.

Mapping, per the UI-SPEC Color section:
- `#10b981` (both the `bg-[...]` dot and the `text-[...]` label) → `var(--status-ok)`
- `#ef4444` → `var(--status-error)`
- `#eab308` → `var(--status-warn)`
- `bg-amber-500/10` → a `color-mix` tint of the warn token, e.g.
  `bg-[color-mix(in_oklab,var(--status-warn)_10%,transparent)]`
- `text-amber-300` → `text-[var(--status-warn)]`

Use the `MetricCard.tsx` `severityConfig` + `color-mix` shape: a single config object mapping the
state to one token, with derived tints and glows computed by `color-mix` from that one token, so
there are no hardcoded rgba values. Do NOT copy `AlertRulesEngine.tsx`'s Tailwind colour-scale
pattern — UI-SPEC names it legacy.

The component must render correctly, unchanged, across all six themes with no theme-specific
branch. If the `isOpus` highlight currently relies on an amber that carries meaning ("expensive
model"), keep that meaning by mapping it to `--status-warn`; do not silently drop the highlight.

Extend `src/components/CostBreakdown.test.tsx` with a source-level assertion that the component
file contains no hex literal and no `amber-`/`red-`/`green-`/`yellow-` Tailwind colour class, plus
a render assertion that the previously-highlighted row still receives a distinguishing class so the
remediation did not quietly remove a signal.
  </action>
  <verify>
    <automated>npx vitest run src/components/CostBreakdown.test.tsx</automated>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>
`CostBreakdown` renders entirely from design tokens with its existing signals preserved.
  </done>
  <acceptance_criteria>
    - `grep -cE '#[0-9a-fA-F]{6}|#[0-9a-fA-F]{3}\b' src/components/CostBreakdown.tsx` returns 0
    - `grep -cE 'amber-|text-red-|bg-red-|green-[0-9]|yellow-[0-9]' src/components/CostBreakdown.tsx` returns 0
    - `grep -c 'color-mix' src/components/CostBreakdown.tsx` returns at least 1
    - `grep -c 'var(--status-' src/components/CostBreakdown.tsx` returns at least 2
    - `npx vitest run src/components/CostBreakdown.test.tsx` exits 0
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Convex derived series → rendered chart segments | Provider keys originate from ingested telemetry and index into a colour map and the DOM. |
| toggle state → which dollars are displayed | The control decides whether imputed money is on screen; the default must be the conservative one. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-104-43 | Repudiation | imputed subscription money shown as if it were billed | mitigate | The `Billed` view is the default and constructs its segments exclusively from `billedByProvider`; the covered series is only reachable by an explicit operator toggle, renders in a visually distinct reduced-opacity treatment of the same provider colour, and is never summed into the billed segment. Unit tests assert the billed segment values are byte-identical across the toggle. |
| T-104-44 | Repudiation | unpriced traffic silently disappearing from the chart | mitigate | A caption states the excluded token count whenever any bucket in the window carries unpriced tokens (D-03 applied to the chart), rather than dropping those tokens with no trace or adding them as a misleading zero segment. |
| T-104-45 | Information Disclosure | provider keys rendered into segment labels | accept | React escapes text content; the same keys are already rendered by this component's existing legend and by `CostBreakdown`. |
| T-104-46 | Denial of Service | rendering an unbounded number of segments per bar | accept | Segment count is bounded by the distinct provider count in the window (six live providers plus four gateway ids), and the underlying query is index-range-bounded by plan 104-05 (T-104-18). No client-side cap is added. |
</threat_model>

<verification>
- `npx vitest run src/components/CostTrendChart.test.tsx src/components/CostBreakdown.test.tsx` exits 0.
- `npx tsc --noEmit` exits 0 and `npm run build` exits 0.
- Manual code review: the only hex literals remaining in either file come from `src/lib/providers.ts`'s
  exempt `PROVIDER_COLORS` palette plus the single `#6b7280` fallback.
- Manual code review: the `Billed` code path references no covered value at all.
- Visual confirmation across all six themes is DEFERRED to plan 104-11.
</verification>

<success_criteria>
- One chart, two views, billed by default, covered visually distinct and never merged.
- Excluded unpriced tokens are stated rather than silently dropped.
- Both cost components render from design tokens and repaint correctly on a theme change.
</success_criteria>

<output>
Create `.planning/phases/104-cost-intelligence/104-10-SUMMARY.md` when done. Record which toggle
primitive was chosen and whether the redundant surface wrapper was removed or retokenized.
</output>
