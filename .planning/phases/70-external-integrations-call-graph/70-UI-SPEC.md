---
phase: 70
slug: external-integrations-call-graph
status: draft
shadcn_initialized: true
preset: new-york / neutral / css-variables / radius-0
created: 2026-05-24
---

# Phase 70 — UI Design Contract

> Visual and interaction contract for Phase 70: External Integrations & Call Graph.
> Generated by gsd-ui-researcher, verified by gsd-ui-checker.

---

## Design System

| Property | Value | Source |
|----------|-------|--------|
| Tool | shadcn/ui | components.json |
| Style | new-york | components.json |
| Base color | neutral | components.json |
| Preset | css-variables, radius-0 | index.css (`--radius: 0rem`) |
| Component library | Radix UI (via shadcn) | components.json |
| Icon library | Lucide React | components.json `iconLibrary: lucide` |
| Font (body/UI) | Geist Variable | index.css `@fontsource-variable/geist` |
| Font (mono) | JetBrains Mono | index.css `--font-mono` |

Dark mode is the primary runtime theme. All color values below are dark-mode values unless noted.

---

## Spacing Scale

Declared values (multiples of 4 only):

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Icon gaps, inline badge padding, status dot |
| sm | 8px | Compact element spacing, button icon gap |
| md | 16px | Default element spacing, form field gap, panel internal padding |
| lg | 24px | Section padding, inter-section gap |
| xl | 32px | Layout column gap |
| 2xl | 48px | Major section breaks |
| 3xl | 64px | Page-level spacing (rare) |

Exceptions:
- Call graph SVG node sizes: Agent node 48x48px, Tool node 32x32px (both multiples of 8, acceptable)
- Touch targets for icon-only buttons: 44px min (accessibility requirement)
- Sheet panel width: 480px (matches existing AlertRuleForm — do not change)

---

## Typography

Matches established project patterns from AlertRuleForm, SectionHeader, NotificationChannels:

| Role | Size | Weight | Line Height | Usage |
|------|------|--------|-------------|-------|
| Body | 14px (text-sm) | 400 (font-normal) | 1.5 | Form field labels in collapsed state, help text, table cell content |
| Label | 14px (text-sm) | 500 (font-medium) | 1.4 | Form field labels (active/expanded), collapsible section headers |
| Heading | 16px (text-base) | 600 (font-semibold) | 1.3 | Sheet title, panel section headings |
| Section label | 12px (text-xs) | 600 (font-semibold) | 1.2 | SectionHeader uppercase tracking-wide pattern, delivery log timestamps |

All section labels use `uppercase tracking-wide text-muted-foreground` per established `SectionHeader` component pattern.

Mono font (JetBrains Mono) used for: routing key input, email address display when masked.

---

## Color

Dark mode values (primary runtime). Project uses oklch + named CSS variables exclusively.

| Role | CSS Token | Dark Value | Usage |
|------|-----------|------------|-------|
| Dominant (60%) | `--background` | `#09090b` | Page background |
| Secondary (30%) | `--card` / `--secondary` | `#141416` / `#27272a` | GlassPanel cards, Sheet content, collapsible section backgrounds |
| Accent (10%) | `--primary` | `#10b981` (Matrix Emerald) | Reserved list below |
| Status OK | `--status-ok` | `#22c55e` | Healthy node fill, delivery success indicator, enabled toggle |
| Status Error | `--status-error` | `#ef4444` | Errored node fill, error propagation edges, delivery failure indicator |
| Status Warn | `--status-warn` | `#eab308` | Pending/degraded node fill |
| Destructive | `--destructive` | `oklch(0.704 0.191 22.216)` | Delete rule button, remove webhook inline confirm |
| Muted | `--muted-foreground` | `#a1a1aa` | Disabled text, collapsed section header text, empty states |

**Accent (`#10b981`) reserved for:**
- Toggle thumb (enabled state) — matches existing `bg-indigo-600` pattern but use `--primary` per current dark theme
- "Save Rule" primary button (existing pattern)
- Active sidebar nav item indicator
- Delivery log success status dot
- Call graph: no accent — use status-ok green for healthy nodes instead

**Node color contract (call graph SVG):**
- Healthy agent node: `--card` fill (`#141416`), `--border` stroke (`#27272a`), foreground label
- Healthy tool node: `--muted` fill (`#27272a`), `--border` stroke, muted-foreground label
- Errored node (any type): `--status-error` fill (`#ef4444`), white label
- Pending node: `--muted` fill, `--status-warn` stroke dashed
- Error propagation edge: `--status-error` stroke, 2px width
- Normal edge: `--border` stroke (`#27272a`), 1px width
- Arrow heads: same color as their edge stroke

**Toggle (enabled/disabled):**
- Enabled: `bg-indigo-600` track, white thumb — matches existing Settings Toggle component exactly
- Disabled: `bg-gray-700` track, white thumb

---

## Copywriting Contract

### Alert Rule Form — New Sections (D-13)

| Element | Copy |
|---------|------|
| PagerDuty section header | `PagerDuty` |
| PagerDuty toggle label | `Send PagerDuty incident` |
| Routing key field label | `Routing Key` |
| Routing key placeholder | `xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx` |
| Routing key help text | `Events API v2 routing key for this service. Stored per rule.` |
| Severity override label | `Severity override` |
| Severity override help | `Auto-mapped from rule severity. Override only if needed.` |
| Email Digest section header | `Email Digest` |
| Email Digest toggle label | `Include in daily digest` |
| Email section collapsed state | `Off` / `On — daily` / `On — weekly` / `On — daily + weekly` |
| PagerDuty section collapsed state | `Off` / `On — {routing key last 6 chars}` |

### Settings Page — Email Digest Section (D-14)

| Element | Copy |
|---------|------|
| Section header | `EMAIL DIGEST` (uppercase, tracking-wide, muted — matches SectionHeader pattern) |
| Recipient field label | `Recipient email` |
| Recipient field placeholder | `your@email.com` |
| Recipient help text | `Uses profile email address. Edit in Agent Profiles if different.` |
| Schedule field label | `Schedule` |
| Schedule options | `Daily`, `Weekly`, `Daily + Weekly` |
| Enabled toggle label | `Send email digest` |
| Save button | `Save Digest Settings` |
| Success toast | `Digest settings saved.` |
| Error toast | `Could not save digest settings. Check the recipient email and try again.` |

### Settings Page — Delivery History Tab (D-14)

| Element | Copy |
|---------|------|
| Tab label | `Delivery History` |
| Empty state heading | `No deliveries yet` |
| Empty state body | `Delivery history appears here after the first scheduled digest or PagerDuty trigger.` |
| Email log row — success | `Email digest delivered` |
| Email log row — failure | `Email digest failed — {error reason}` |
| PagerDuty log row — trigger | `Incident triggered — {alertRuleId}` |
| PagerDuty log row — resolve | `Incident resolved — {alertRuleId}` |
| PagerDuty log row — failure | `PagerDuty delivery failed — {error reason}` |
| Error detail expand label | `Details` |

### Call Graph — Infrastructure Page (D-10, D-12)

| Element | Copy |
|---------|------|
| Section header | `AGENT CALL GRAPH` (uppercase, tracking-wide, SectionHeader pattern) |
| Empty state heading | `No call graph data` |
| Empty state body | `Call graph appears here after Ástríðr agents run with call graph telemetry enabled.` |
| Node tooltip — agent | `{agentId} · {callCount} calls` |
| Node tooltip — tool | `{toolName} · {callCount} calls · {errorCount} errors` |
| Node tooltip — errored | `{name} · ERRORED` |
| Legend label — healthy | `Healthy` |
| Legend label — errored | `Errored` |
| Legend label — pending | `Pending` |
| Loading state | `Loading call graph…` (skeleton, no spinner text visible) |

### Destructive Actions

| Action | Trigger | Confirmation Pattern |
|--------|---------|----------------------|
| Delete alert rule | "Delete Rule" button in AlertRuleForm footer | Dialog: "Delete rule?" / "This will permanently remove this rule and all its delivery config." / "Delete" (destructive) + "Cancel" (ghost) — matches existing pattern |
| Remove PagerDuty config | "Remove" inline link when routing key saved | Inline confirm: "Remove PagerDuty config?" / "Remove" (destructive text) + "Cancel" (muted text) — matches NotificationChannels webhook remove pattern |

No new destructive actions beyond existing rule deletion. PagerDuty routing key removal is inline (no dialog) per webhook pattern precedent.

---

## Component Inventory

All components listed here already exist in the codebase or are available via shadcn. No new third-party installs required.

### Existing — Reuse As-Is

| Component | Path | Used For |
|-----------|------|---------|
| `GlassPanel` | `src/components/GlassPanel.tsx` | Call graph section wrapper on Infrastructure page |
| `SectionHeader` | `src/components/SectionHeader.tsx` | "AGENT CALL GRAPH" header, "EMAIL DIGEST" header |
| `SectionErrorBoundary` | `src/components/SectionErrorBoundary.tsx` | Wrap CallGraphPanel on Infrastructure page |
| `NotificationChannels` | `src/components/NotificationChannels.tsx` | Extended with Email Digest section |
| `AlertRuleForm` | `src/components/AlertRuleForm.tsx` | Extended with PagerDuty + Email collapsible sections |
| `Collapsible` | `src/components/ui/collapsible.tsx` | PagerDuty and Email sections inside AlertRuleForm |
| `Switch` | `src/components/ui/switch.tsx` | Per-channel enabled toggles inside form sections |
| `Input` | `src/components/ui/input.tsx` | Routing key field, email recipient field |
| `Select` | `src/components/ui/select.tsx` | Schedule picker (Daily / Weekly / Daily + Weekly), severity override |
| `Button` | `src/components/ui/button.tsx` | Save Digest Settings, collapsible chevron trigger |
| `Tabs` | `src/components/ui/tabs.tsx` | Delivery History tab on Settings page |
| `Table` | `src/components/ui/table.tsx` | Delivery log rows |
| `Tooltip` | `src/components/ui/tooltip.tsx` | Call graph node hover tooltips |
| `Skeleton` | `src/components/ui/skeleton.tsx` | Call graph loading state |
| `Badge` | `src/components/ui/badge.tsx` | Delivery status (success/failed) in log rows |
| `Dialog` | `src/components/ui/dialog.tsx` | Delete rule confirmation (already exists) |

### New — Build This Phase

| Component | Path | Description |
|-----------|------|-------------|
| `CallGraphPanel` | `src/components/CallGraphPanel.tsx` | Infrastructure page section: GlassPanel wrapper + SVG call graph + legend + empty state |
| `CallGraphSVG` | `src/components/CallGraphSVG.tsx` | Pure SVG renderer: dagre layout → SVG nodes/edges. No canvas, no React Flow. |
| `DeliveryHistory` | `src/components/DeliveryHistory.tsx` | Settings tab: email + PagerDuty delivery log table with status badges |
| `EmailDigestConfig` | `src/components/EmailDigestConfig.tsx` | Settings section: recipient, schedule, enabled toggle, Save button |

---

## Layout Contracts

### Infrastructure Page — Call Graph Section

```
<SectionErrorBoundary name="Agent Call Graph">
  <CallGraphPanel>
    <GlassPanel>
      <SectionHeader title="AGENT CALL GRAPH" />
      <CallGraphSVG />          ← SVG, min-height 320px, scrollable if > 600px
      <CallGraphLegend />       ← inline row: ● Healthy  ● Errored  ● Pending
    </GlassPanel>
  </CallGraphPanel>
</SectionErrorBoundary>
```

Position: After `ProviderHealthPanel`, before any future panels. Added to `space-y-6` container.

SVG layout direction: top-down (dagre `rankdir: "TB"`). Agent nodes rank above tool nodes.

### AlertRuleForm — PagerDuty Section

```
<Collapsible>
  <CollapsibleTrigger>
    <ChevronRight icon />  PagerDuty  <status-summary text />
  </CollapsibleTrigger>
  <CollapsibleContent>
    <Switch label="Send PagerDuty incident" />
    [when enabled:]
    <Input label="Routing Key" type="password" font-mono />
    <Select label="Severity override" options=[auto, critical, warning, info] />
  </CollapsibleContent>
</Collapsible>
```

Placed after existing Conditions section, before Save/Discard footer.

### AlertRuleForm — Email Digest Section

```
<Collapsible>
  <CollapsibleTrigger>
    <ChevronRight icon />  Email Digest  <status-summary text />
  </CollapsibleTrigger>
  <CollapsibleContent>
    <Switch label="Include in daily digest" />
  </CollapsibleContent>
</Collapsible>
```

Placed after PagerDuty collapsible.

### Settings Page — Email Digest + Delivery History

Email Digest config added inside the existing `NotificationChannels` section, below Discord/Slack rows, separated by a `<Separator />`.

Delivery History: new `<Tabs>` added to Settings page wrapping the "Notification Channels + Preferences" area. Tabs: `Configuration` (existing content) | `Delivery History` (new `DeliveryHistory` component).

---

## Call Graph SVG Specification

### Node Sizing

| Type | Width | Height | Border Radius | Font |
|------|-------|--------|---------------|------|
| Agent node | 120px | 48px | 0px (radius-0) | 12px Geist, weight 500 |
| Tool node | 96px | 32px | 0px (radius-0) | 11px Geist, weight 400 |

### Node States

| State | Fill | Stroke | Stroke Width | Label color |
|-------|------|--------|--------------|-------------|
| Healthy (agent) | `#141416` (--card) | `#27272a` (--border) | 1px | `#ffffff` (foreground) |
| Healthy (tool) | `#27272a` (--muted) | `#27272a` (--border) | 1px | `#a1a1aa` (muted-foreground) |
| Errored (any) | `#ef4444` (--status-error) | `#ef4444` | 2px | `#ffffff` |
| Pending (any) | `#27272a` (--muted) | `#eab308` (--status-warn) | 1.5px dashed | `#a1a1aa` |

### Edge States

| State | Stroke | Width | Arrow |
|-------|--------|-------|-------|
| Normal | `#27272a` (--border) | 1px | filled triangle, same color |
| Error path | `#ef4444` (--status-error) | 2px | filled triangle, same color |

### Dagre Config

```typescript
const g = new dagre.graphlib.Graph();
g.setGraph({ rankdir: "TB", nodesep: 40, ranksep: 48, marginx: 16, marginy: 16 });
```

### Legend

Inline row, bottom of panel, `gap-4 text-xs text-muted-foreground`:
```
● Healthy   ● Errored   ● Pending
```
Dots use 8px circles with status fill colors.

---

## Animation

Follows established `GlassPanel` pattern (motion/react):

| Element | Animation | Duration | Easing |
|---------|-----------|----------|--------|
| GlassPanel entry | `opacity 0→1, y 8→0` | 200ms | easeOut |
| Collapsible expand/collapse | `height auto` (Radix default) | 150ms | easeOut |
| Call graph initial render | Fade in SVG `opacity 0→1` | 250ms | easeOut |
| Node hover | No animation — CSS `opacity: 0.8` on hover | — | — |
| New edge on reactive update | No animation — re-render SVG in place | — | — |
| Delivery log new row | No slide-in (table, not feed) | — | — |

Respect `useReducedMotion` — skip all animations when set (matches GlassPanel pattern).

---

## Interaction States

### Routing Key Input

- Displayed as `type="password"` when not focused (mask characters)
- On focus: reveal plaintext
- On blur with new value: save immediately (same pattern as webhook URL)
- Masking display after save: show only last 6 characters prefixed with `••••` (not last 8 — routing keys are shorter tokens)

### Collapsible Sections (AlertRuleForm)

- Default: collapsed
- Chevron rotates 90deg when expanded (CSS `transition-transform duration-150`)
- Status summary text in trigger: computed from current saved state, not live form state
- Only one section may be open at a time — no constraint enforced (standard UX, both can be open)

### Call Graph Interactions

- Node hover: show Tooltip with node detail (agent id / tool name, call count, error count)
- Node click: no action (phase 70 scope — detail drill-down is future)
- Scroll: SVG contained in scrollable div if height exceeds 600px
- Empty state: shown when `callGraphEdges` returns empty array

### Delivery History Table

- Load: `useQuery` reactive — no manual refresh button
- Rows: newest first
- Pagination: show last 50 entries (no UI pagination control — operator dashboard, single user)
- Status badge: `variant="outline"` green border for success, red for failure

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | Collapsible, Switch (already installed — see ui/ inventory) | not required |

No third-party registries. All components are either already installed shadcn/ui components or custom-built SVG/React components.

dagre (v0.8.5) is already installed as a production dependency — not a registry block. No new npm packages required for this phase.

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
