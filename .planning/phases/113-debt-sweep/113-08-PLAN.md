---
phase: 113-debt-sweep
plan: 08
type: execute
wave: 2
depends_on: [113-07]
files_modified:
  - C:/Users/mandr/convex-selfhost/README.md
  - C:/Users/mandr/convex-selfhost/preflight.ps1
autonomous: false
requirements: [DEBT-07]

must_haves:
  truths:
    - "D-12: the commit lands in its OWN PRIVATE repository initialised in place at C:/Users/mandr/convex-selfhost — never in codepulse, which is confirmed PUBLIC and holds an admin key one careless git add away"
    - "D-13: committed scope is compose plus all TEN *.ps1 scripts (count re-derived from disk 2026-08-11 -- restrict-convex-lan.ps1 was added that day and is NOT optional: it carries the Windows Firewall rules that block LAN access to the unauthenticated Convex mutation surface) plus run-restart-hidden.vbs plus a bootstrap README; the dated diagnosis-*.md and health-report.md operational notes stay out, and the README is what makes reproducible-from-a-fresh-checkout true rather than asserted"
    - "D-13: the README distinguishes scheduled always-on scripts from manual operator scripts, and documents the external prerequisite C:/Users/mandr/scripts/run-hidden.vbs that six of the seven scheduled tasks invoke and that cloning this repo does not provide"
    - "D-16: reproducibility is proven by cloning to a temp path and running a checked-in preflight script that asserts every required file, env key and external prerequisite — WITHOUT starting a second Convex backend"
    - "D-16 control: the preflight is deliberately broken in the temp clone and shown to report that specific failure with a non-zero exit; a preflight never shown to fail is not a check"
    - "The preflight reports per-check PASS/FAIL/SKIP with a counted summary, and a SKIP is stated explicitly with its reason — a gate that can skip itself must prove it evaluated something"
    - "The staged-content secret review is an explicit operator checkpoint before the first commit, and its detector is control-fired against the pre-113 backup to prove it can match"
  artifacts:
    - path: "C:/Users/mandr/convex-selfhost/README.md"
      provides: "Bootstrap instructions, volumes, scheduled-task inventory, secret-file contract, and the logging-block rationale"
      min_lines: 70
    - path: "C:/Users/mandr/convex-selfhost/preflight.ps1"
      provides: "Per-check PASS/FAIL/SKIP fresh-checkout validator that starts no container"
      min_lines: 60
  key_links:
    - from: "C:/Users/mandr/convex-selfhost/preflight.ps1"
      to: "C:/Users/mandr/convex-selfhost/docker-compose.yml"
      via: "docker compose config --quiet structural validation with no output and no container start"
      pattern: "config --quiet"
    - from: "C:/Users/mandr/convex-selfhost/preflight.ps1"
      to: "C:/Users/mandr/convex-selfhost/selfhosted.envfile.example"
      via: "key-NAME-only parity comparison against the operator-supplied env file"
      pattern: "selfhosted.envfile.example"
---

<objective>
Turn the prepared `convex-selfhost` directory into a real, private, reproducible repository: write the
bootstrap README and a checked-in preflight validator, make the first commit under operator review,
push it to a private remote, and prove the result by cloning it to a temp path and running the
preflight — including a deliberate break proving the preflight can fail.

Purpose: the phase's success criterion says the compose `logging:` block and the restart scripts are
"under version control and reproducible from a fresh checkout." A commit alone satisfies the first
half and only asserts the second. D-16 rejected a README checklist verified by reading — the weakest
class of check under this project's own rules — and rejected a full standby bring-up as too risky
next to a memory-pressured backend. What is left is a script that proves the checkout is complete
without starting anything, and that has itself been shown to fail.

Output: `README.md`, `preflight.ps1`, one reviewed initial commit, a private remote, and a temp-clone
proof with its control.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/113-debt-sweep/113-CONTEXT.md
@.planning/phases/113-debt-sweep/113-RESEARCH.md
@.planning/phases/113-debt-sweep/113-PATTERNS.md
@.planning/phases/113-debt-sweep/113-VALIDATION.md
@.planning/phases/113-debt-sweep/113-07-SUMMARY.md
@CLAUDE.md
@README.md
@scripts/verify-skills-page.mjs

<interfaces>
<!-- Facts the executor needs. Verified live 2026-08-11. -->

PROHIBITIONS CARRIED FORWARD FROM PLAN 113-07 — still in force:
- Never Read, cat, or dump `selfhosted.envfile` or `admin-key.txt`.
- Never run `docker compose config` without a filter; `--quiet` produces no output and is safe.
- Never use grep's `-A`/`-B`/`-C` context flags on `docker-compose.yml`.
- `docker-compose.yml.pre113.bak` still holds the pre-parameterization live secret; it is covered by
  the `*.bak` ignore rule and must never be staged.
- Rotation of `INSTANCE_SECRET` is NOT in this phase (D-18).

COMMIT SCOPE (D-13) — stage these by explicit name, never `git add -A`, never `git add .`:
  .gitignore
  README.md
  preflight.ps1
  selfhosted.envfile.example
  docker-compose.yml
  docker-compose.standby.yml
  run-restart-hidden.vbs
  backup-convex.ps1  deploy-retention-to-local.ps1  install-nas-key.ps1  restart-convex.ps1
  restore-cap-48g.ps1  retention-health-check.ps1  retention-root-cause.ps1  soak-watch.ps1
  verify-retention-fix.ps1

DELIBERATELY EXCLUDED (D-13's rejected alternative, already covered by the ignore set): the seven
`diagnosis-2026-08-*.md` files and `health-report.md` — genuine investigation history, held back
because they carry host paths and container internals and would make a later "make it public"
decision costly. Deferred, not discarded.

LIVE SCHEDULED TASKS (verified via Get-ScheduledTask 2026-08-11) — the README's inventory:
  ConvexBackup                  -> backup-convex.ps1                                   daily
  ConvexBackupFull  (Disabled)  -> backup-convex.ps1 -Full                             daily
  ConvexNightlyRestart          -> run-restart-hidden.vbs -> restart-convex.ps1         daily
  ConvexRestoreCap48 (Disabled) -> restore-cap-48g.ps1                                 daily
  ConvexRetentionHealthCheck    -> retention-health-check.ps1                          daily
  ConvexRetentionRootCause      -> retention-root-cause.ps1                            daily
  ConvexSoakWatch               -> soak-watch.ps1                                      time-triggered
  NOT wired to any task (manual operator scripts): deploy-retention-to-local.ps1,
  verify-retention-fix.ps1, install-nas-key.ps1
  EXTERNAL PREREQUISITE: six of the seven tasks launch their script through
  `C:\Users\mandr\scripts\run-hidden.vbs`, which lives OUTSIDE this repo and is not part of its
  scope. ConvexNightlyRestart is the exception — it uses the in-repo `run-restart-hidden.vbs`.

DOCUMENTED CONVEX SELF-HOSTED VARIABLE NAMES (public CLI contract):
  CONVEX_SELF_HOSTED_URL, CONVEX_SELF_HOSTED_ADMIN_KEY   (plus INSTANCE_SECRET, moved in 113-07)

COMPOSE FACTS FOR THE README (no values):
  logging block at :69-73 — json-file driver, max-size 10m, max-file 3, with a rationale comment at
  :63-68 citing 107-OCC-EVIDENCE.md. This block is one of the two artifacts the phase's success
  criterion names by name.
  docker-compose.standby.yml's own INSTANCE_SECRET line reads `__COPY_FROM_PRIMARY_COMPOSE__` — a
  deliberate manual-copy placeholder. Plan 113-07 did NOT change it; the README documents it.
  Named volume `data:` and network `astridr-network:` are declared at :116-121.

RESTART MECHANISM (CLAUDE.md, binding): `ConvexNightlyRestart` is deliberate and health-gated —
`restart-convex.ps1` asserts `:3210/version` returns 200 within 150s and logs INVESTIGATE rather than
claiming success otherwise. It uses `docker restart`, not `compose up --force-recreate`, and the
escalation to force-recreate is documented at `restart-convex.ps1:63`. DEBT-07 must not change any of
this behaviour — only bring it under version control as-is.

PER-CHECK OUTPUT SHAPE TO MIRROR (scripts/verify-skills-page.mjs:26-42): an `ok(cond, label, extra)`
helper logging PASS/FAIL per named check and accumulating failures, rather than one aggregate boolean.

POWERSHELL CONSTRAINTS (this repo has been bitten by all three):
- ASCII only. A UTF-8 em-dash parses as a curly quote under PS 5.1 and yields "missing terminator".
- Never assign to automatic variables ($HOME, $input, $PID, $error) — the assignment errors silently
  and every downstream check then runs against an empty value, fabricating PASS/FAIL evidence.
- Do not redirect native stderr under `$ErrorActionPreference='Stop'`; wrap native calls as
  `cmd /c "... 2>&1"` and check `$LASTEXITCODE`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bootstrap README and a per-check preflight that starts no container (D-13, D-16)</name>

  <files>C:/Users/mandr/convex-selfhost/README.md, C:/Users/mandr/convex-selfhost/preflight.ps1</files>

  <read_first>
    - `README.md` at the codepulse repo root, lines 1-20 — the "clone, copy the template, edit, run"
      Quick Start shape to mirror.
    - `scripts/verify-skills-page.mjs` lines 26-42 — the `ok()` per-check idiom.
    - `CLAUDE.md` § "Self-Hosted Convex — Operational Rules" — the restart rationale the README must
      restate rather than contradict.
    - `.planning/phases/113-debt-sweep/113-07-SUMMARY.md` — the exact state plan 113-07 left the
      directory in, including the `env_file` directive and the template's key names.
    - `C:/Users/mandr/convex-selfhost/restart-convex.ps1` — safe to read (it is a script, not an env
      file); confirm the health gate and the `docker restart` call for yourself before describing
      them in the README.
  </read_first>

  <action>
Write `README.md` covering, in this order:
1. What this directory is and what runs from it: the self-hosted single-node SQLite Convex backend
   and dashboard that CodePulse depends on.
2. Prerequisites: Docker Desktop / `docker compose`, and the external `C:\Users\mandr\scripts\run-hidden.vbs`
   launcher that six of the seven scheduled tasks invoke and that this repo does NOT provide.
3. Bootstrap from a fresh checkout: clone, `cp selfhosted.envfile.example selfhosted.envfile`, fill
   in the three keys, obtain/place `admin-key.txt`, run `preflight.ps1`, then `docker compose up -d`.
4. Secret files and what each must contain — key NAMES only, never values, never an example value:
   `selfhosted.envfile` (`CONVEX_SELF_HOSTED_URL`, `CONVEX_SELF_HOSTED_ADMIN_KEY`, `INSTANCE_SECRET`)
   and `admin-key.txt`. State that `INSTANCE_SECRET` moved out of `docker-compose.yml` in Phase 113
   (D-18) and that the backend now sources it via the `env_file` directive, so a missing key means a
   recreated container comes up without its identity secret.
5. Volumes and network: the named `data:` volume and `astridr-network:`, and an explicit warning that
   the data volume is the database — restoring means swapping volumes, never
   `npx convex import --replace-all` against the live instance.
6. Scheduled tasks: a table of all seven with their script, trigger, enabled/disabled state, and
   whether they launch via the external `run-hidden.vbs` or the in-repo `run-restart-hidden.vbs`.
   Then a separate short list of the three manual operator scripts that are wired to no task, so a
   fresh-checkout reader knows which need re-registering and which are run ad hoc.
7. The `logging:` block: what it sets (json-file, 10m, 3 files) and why, pointing at the rationale
   comment already in the compose file and at `107-OCC-EVIDENCE.md`.
8. `ConvexNightlyRestart` is deliberate and health-gated — restate CLAUDE.md's position, including
   that it uses `docker restart` and that force-recreate is the escalation, not the norm.
9. A note that `docker-compose.standby.yml` still carries a `__COPY_FROM_PRIMARY_COMPOSE__`
   placeholder for its own `INSTANCE_SECRET` and expects a manual copy — documented deliberately
   rather than changed, because it is a disaster-recovery artifact.
10. A note that the dated `diagnosis-*.md` and `health-report.md` files exist on disk but are
    deliberately not committed (D-13), and why.

Write `preflight.ps1` as an ASCII-only PowerShell script with an `ok`-style helper that emits one
`PASS` / `FAIL` / `SKIP` line per NAMED check and accumulates results. It must start no container.
Checks, in this order:

A. Required committed files present — one check per file, using the exact commit-scope list in
   `<interfaces>`. Absent = FAIL.
B. Operator-supplied files: `selfhosted.envfile` and `admin-key.txt`. Present = PASS. Absent = SKIP
   with the reason "expected on a fresh checkout; copy from selfhosted.envfile.example" — NOT a FAIL,
   because a fresh clone legitimately lacks them.
C. Key-name parity: read `selfhosted.envfile.example` and `selfhosted.envfile`, ignore blank and
   comment lines, split each line on the FIRST `=`, and compare the key-NAME sets. Every name in the
   example must exist in the real file. Print only NAMES, never values, never a value length here.
   When the real file is absent, emit SKIP with that reason.
D. Compose validity: `docker compose -f docker-compose.yml config --quiet`, PASS on exit 0. When
   `selfhosted.envfile` is absent, emit SKIP with the reason that the `env_file` target does not
   exist yet — do not let a fresh clone report a spurious FAIL here, and do not silently pass either.
E. External prerequisites: `docker` resolvable, `docker compose` resolvable, and
   `C:\Users\mandr\scripts\run-hidden.vbs` present. The first two are FAIL if missing; the third is a
   documented machine-level dependency, so report it as a named check that FAILS with an actionable
   message rather than being quietly omitted.
F. Scheduled-task inventory: report which of the seven named tasks exist on this machine, as
   informational PASS/SKIP lines. On a fresh machine none exist and that is expected.

End with a summary line printing real counts derived from the accumulated results —
`checks_run=<n> passed=<n> failed=<n> skipped=<n>` — followed by the name of every failed check and
every skipped check with its reason. Exit 1 when `failed > 0`, else 0. A skipped check must be
visible in the summary; a validator that can silently skip itself is the failure mode this project
has been bitten by, and the counts are what make it impossible.
  </action>

  <verify>
    <automated>powershell -NoProfile -ExecutionPolicy Bypass -File C:/Users/mandr/convex-selfhost/preflight.ps1</automated>
  </verify>

  <acceptance_criteria>
    - Run in the REAL directory (where `selfhosted.envfile` exists), `preflight.ps1` exits 0 and its
      summary reports `skipped=0` for checks B, C and D — proving those env-dependent checks actually
      EXECUTED rather than self-skipping. Paste the full output.
    - The key-parity check's output contains only key NAMES; `grep -cE '=[^ ]' ` over the captured
      output finds no `NAME=value` pair.
    - No `docker compose config` invocation in the script lacks `--quiet`:
      `grep -c 'config --quiet' preflight.ps1` is >= 1 and
      `grep -c 'compose config' preflight.ps1` equals that same number.
    - The script contains no non-ASCII byte: verify with a byte scan and paste the result.
    - The script never starts a container: `grep -cE 'compose up|docker run|docker start' preflight.ps1`
      is 0.
    - `README.md` contains a scheduled-task table with all seven task names, names all three manual
      scripts, and names `C:\Users\mandr\scripts\run-hidden.vbs` as an external prerequisite.
    - `README.md` contains no secret value and no example value for any key:
      `grep -cE '(INSTANCE_SECRET|ADMIN_KEY)=[^ ]' README.md` is 0.
  </acceptance_criteria>

  <done>A README that makes a fresh checkout actionable and a preflight that reports every check by name, runs all its env-dependent checks in the real directory, and starts nothing.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Staged-content secret review, first commit, private remote (D-12, D-14)</name>

  <files>C:/Users/mandr/convex-selfhost/README.md, C:/Users/mandr/convex-selfhost/preflight.ps1</files>

  <read_first>
    - `.planning/phases/113-debt-sweep/113-VALIDATION.md` § "Secret-exposure checkpoint" — this review
      is explicitly an operator checkpoint, not an automatable step.
    - `.planning/phases/113-debt-sweep/113-07-SUMMARY.md` — the detector counts recorded there, which
      this task re-fires against staged content.
  </read_first>

  <what-built>
    `convex-selfhost` has a proven `.gitignore`, a parameterized `docker-compose.yml` with no inline
    secret, a key-name-only `selfhosted.envfile.example`, a bootstrap `README.md`, and a
    `preflight.ps1` that exits 0 with zero skipped checks in the real directory. Nothing has been
    staged, committed, or pushed.
  </what-built>

  <how-to-verify>
    Run each step from `C:\Users\mandr\convex-selfhost` and paste its output. Stop and report on any
    disagreement with the stated expectation.

    1. **Confirm you are in the right repository.** `git rev-parse --show-toplevel` must print
       `C:/Users/mandr/convex-selfhost` (or its Windows form). If it prints a codepulse path you are
       in the wrong directory — STOP. codepulse is PUBLIC.

    2. **Stage by explicit name only.** `git add` the exact file list in this plan's `<interfaces>`
       COMMIT SCOPE block. Never `git add -A`, never `git add .`, never a glob.

    3. **Prove nothing else came along.** `git diff --cached --name-only` must equal that list
       exactly. It must NOT contain `selfhosted.envfile`, `admin-key.txt`, any `*.bak`, any `*.log`,
       any `diagnosis-*.md`, or `health-report.md`.

    4. **Prove nothing is left unignored.** `git status --porcelain | grep -c '^??'` must be 0 after
       staging — every remaining file in that 24 GB directory is covered by the ignore set.

    5. **Fire the secret detector on staged content.**
       `git diff --cached | grep -ciE 'INSTANCE_SECRET[=:][[:space:]]*[0-9a-f]{16,}'` must print `0`.

    6. **Fire the SAME detector against the pre-113 backup as a control.**
       `grep -ciE 'INSTANCE_SECRET[=:][[:space:]]*[0-9a-f]{16,}' docker-compose.yml.pre113.bak` must
       print `1`. Without this, a zero in step 5 proves only that the pattern never matches anything.

    7. **Human review — this is the checkpoint.** Larry: read `git diff --cached` yourself. You are
       looking for anything secret-shaped that a regex would not catch: a hostname you would not
       publish, a NAS path, an admin key fragment, a token in a script's comment. Note that the nine
       PowerShell scripts have never been reviewed for embedded credentials — `install-nas-key.ps1`
       in particular is worth a careful read.

    8. **Commit.** `git commit -m "chore: bring convex-selfhost under version control (CodePulse Phase 113 DEBT-07)"`.
       Do not use `--no-verify`.

    9. **Create the private remote and push.**
       `gh repo create convex-selfhost --private --source . --remote origin --push`.
       Then confirm the visibility you actually got, not the one you asked for:
       `gh repo view --json isPrivate,visibility,nameWithOwner` must report `"isPrivate":true`.

    10. **Confirm what landed.** `git show --stat HEAD` — the file list must match step 3's list.
  </how-to-verify>

  <acceptance_criteria>
    - `git rev-parse --show-toplevel` output pasted and pointing at `convex-selfhost`, not codepulse.
    - `git diff --cached --name-only` output pasted and containing exactly the commit-scope list.
    - `git status --porcelain | grep -c '^??'` printed 0.
    - The staged-content detector printed 0 AND the same detector against the pre-113 backup printed
      1. Both pasted — the control is what makes the 0 meaningful.
    - Larry has read `git diff --cached` and explicitly approved, including a statement about the
      nine PowerShell scripts.
    - `gh repo view --json isPrivate,visibility,nameWithOwner` reports `"isPrivate":true`. A public
      result is a stop-and-delete-the-repo condition.
    - `git show --stat HEAD` file list matches the staged list.
    - No `--no-verify` was used.
  </acceptance_criteria>

  <action>
Execute steps 1 through 6 of `<how-to-verify>` and paste every command's real output, then STOP at
step 7 and hand the staged diff to Larry. Steps 8 through 10 run only after he replies. Stage by
explicit filename from the COMMIT SCOPE list only — never `git add -A`, never `git add .`, never a
glob. Never use `--no-verify`. Never `git commit --amend`. If step 1 shows a codepulse path, abort
the whole task and report: codepulse is PUBLIC and this directory holds an admin key.
  </action>

  <verify>
    <automated>git -C /c/Users/mandr/convex-selfhost show --stat HEAD</automated>
  </verify>

  <resume-signal>Type "approved" after reading the staged diff, or name what you saw that should stop the commit.</resume-signal>

  <done>One reviewed commit exists in `convex-selfhost` containing exactly the D-13 scope, the remote is confirmed private by a read-back, and the secret detector returned 0 on staged content while returning 1 on the pre-113 backup control.</done>
</task>

<task type="auto">
  <name>Task 3: Prove reproducibility by temp clone, then prove the preflight can fail (D-16)</name>

  <files>C:/Users/mandr/convex-selfhost/preflight.ps1</files>

  <read_first>
    - `.planning/phases/113-debt-sweep/113-VALIDATION.md` § "DEBT-07 — reproducible checkout" — the
      observable and its mandated control.
    - `C:/Users/mandr/convex-selfhost/preflight.ps1` — the check names you are about to assert on by
      name.
  </read_first>

  <action>
Clone the pushed private repo into the scratch directory
(`C:\Users\mandr\AppData\Local\Temp\claude\C--Users-mandr-codepulse\<session>\scratchpad\cs-clone`),
not into any project or home path. Use `gh repo clone` or `git clone` against the remote — clone the
REMOTE, not the local directory, so the proof covers what was actually pushed.

Run `preflight.ps1` in the clone. Capture the full output. Expect exit 0 with:
- every check-A file PASS (this is the real reproducibility assertion: the checkout is complete);
- checks B, C and D reporting SKIP with their stated reasons, because a fresh clone legitimately has
  no `selfhosted.envfile`;
- a summary line whose `skipped` count is non-zero and whose skipped-check names are listed.
Record the summary counts verbatim. Note explicitly in the SUMMARY that the fresh clone SKIPS the
env-dependent checks and that Task 1 already proved those same checks execute and pass in the real
directory — the two runs together are the complete proof, and neither alone is.

Then run the D-16 control. In the CLONE only, delete `docker-compose.yml` and re-run the preflight.
It must exit 1, its output must name `docker-compose.yml` in a FAIL line, and its summary `failed`
count must be exactly 1 with every other check unchanged. A preflight never shown to fail is not a
check; this is what converts its green into evidence.

Run a second control for the key-parity check, since it is the one most likely to self-skip in a way
that matters: in the CLONE, create a throwaway `selfhosted.envfile` containing only
`CONVEX_SELF_HOSTED_URL=x` (two of the three documented keys deliberately missing — a placeholder
value, never a real one), re-run, and confirm check C now RUNS and FAILS, naming the missing key
names. Confirm the output prints only names and no values.

Delete the entire temp clone afterwards and confirm it is gone. Confirm the real directory was not
touched: `git -C C:/Users/mandr/convex-selfhost status --porcelain` shows no new changes and
`git -C C:/Users/mandr/convex-selfhost log --oneline` still shows exactly one commit.
  </action>

  <verify>
    <automated>git -C C:/Users/mandr/convex-selfhost log --oneline | wc -l</automated>
  </verify>

  <acceptance_criteria>
    - The clone was made from the REMOTE, and the clone command is pasted.
    - The clean-clone preflight exited 0; its full output and summary counts are pasted, and the
      skipped check names appear in that summary.
    - The deleted-compose control exited 1, its FAIL line naming `docker-compose.yml` is quoted, and
      its summary reported `failed=1`.
    - The partial-envfile control caused check C to RUN and FAIL, naming exactly the two missing key
      names, and its output contains no values.
    - The temp clone no longer exists on disk.
    - `git -C C:/Users/mandr/convex-selfhost log --oneline` shows exactly 1 commit and
      `git status --porcelain` there shows no new untracked or modified files.
    - The SUMMARY states in one sentence why the clean-clone run alone would not be sufficient proof.
  </acceptance_criteria>

  <done>A fresh clone of the pushed repo passes the preflight, and the preflight has been shown to fail specifically and correctly for both a missing required file and a missing env key.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local secret-bearing directory → GitHub remote | the first push is the irreversible edge; git history cannot be un-published by a later commit |
| `convex-selfhost` working directory → the PUBLIC codepulse repo | a `git add` run from the wrong directory is the realistic exposure path |
| pushed repository → temp clone | the clone is the artifact under test and must be disposable |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-113-35 | Information Disclosure | committing a secret-bearing file into git history | mitigate | Explicit-name staging with a diff of `--cached --name-only` against a fixed list, a regex detector fired on staged content AND control-fired against the pre-113 backup, plus a blocking human read of `git diff --cached` |
| T-113-36 | Information Disclosure | pushing to a PUBLIC remote by mistake | mitigate | `gh repo create --private` followed by a read-back of `gh repo view --json isPrivate`, treated as a stop-and-delete condition if it reports public — the visibility you got, not the one you asked for |
| T-113-37 | Information Disclosure | committing from the wrong working directory into codepulse | mitigate | `git rev-parse --show-toplevel` asserted before any staging |
| T-113-38 | Information Disclosure | an embedded credential inside one of the nine never-reviewed PowerShell scripts | mitigate | The human checkpoint calls out the scripts specifically, naming `install-nas-key.ps1`; a regex alone would not catch a credential in an unfamiliar shape |
| T-113-39 | Repudiation | a preflight that reports green regardless of checkout state | mitigate | Two independent failure controls (missing required file, missing env key) plus a counted summary that makes a self-skipping check visible |
| T-113-40 | Information Disclosure | the preflight printing env VALUES while checking key parity | mitigate | Key-name-only comparison, asserted by an acceptance criterion that scans the captured output for any `NAME=value` pair |
| T-113-41 | Denial of Service | starting a second Convex backend beside a memory-pressured one | mitigate | D-16 forbids it; the preflight uses `docker compose config --quiet` only, and an acceptance criterion asserts zero `compose up`/`docker run`/`docker start` occurrences in the script |
| T-113-SC | Tampering | npm/pip/cargo installs | mitigate | No packages are added by this plan; if any install becomes necessary, stop and run the Package Legitimacy Gate before proceeding |
</threat_model>

<verification>
- `preflight.ps1` exits 0 with `skipped=0` in the real directory and exits 0 with named skips in a
  fresh clone.
- `preflight.ps1` exits 1 and names the specific missing artifact under both deliberate breaks.
- `gh repo view --json isPrivate` reports `true` for the new repository.
- `git show --stat HEAD` in `convex-selfhost` lists exactly the D-13 commit scope and nothing else.
- The staged-content secret detector printed 0 while the same detector against the pre-113 backup
  printed 1.
- `git status --porcelain` in `convex-selfhost` shows no untracked files after staging.
</verification>

<success_criteria>
- `convex-selfhost` is a private repository containing the compose files, all ten *.ps1 scripts (see the note above -- the count was nine when this plan was written; restrict-convex-lan.ps1 landed 2026-08-11),
  `run-restart-hidden.vbs`, a bootstrap README, a preflight script, the ignore set and the
  key-name-only template — and nothing else.
- A fresh clone of that repository passes the checked-in preflight, and the preflight has been proven
  able to fail for two distinct, specific reasons.
- No secret value is present in any commit, in any transcript line, or in any preflight output.
</success_criteria>

<output>
Create `.planning/phases/113-debt-sweep/113-08-SUMMARY.md` in the codepulse repo when done. It must
contain no secret values — only counts, exit codes, check names and file lists.

Commit ONLY the SUMMARY to codepulse, with a pathspec-limited command. The `convex-selfhost` files
belong to their OWN repository and are never added to codepulse. Re-check `git status` in codepulse
immediately before committing, then:
`git commit -- .planning/phases/113-debt-sweep/113-08-SUMMARY.md`
Verify with `git show --stat HEAD` afterward. Never `git commit --amend` here.
</output>
