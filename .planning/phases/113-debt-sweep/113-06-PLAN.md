---
phase: 113-debt-sweep
plan: 06
type: execute
wave: 3
depends_on: [113-01, 113-02, 113-03, 113-04, 113-05]
files_modified:
  - .planning/phases/113-debt-sweep/113-FLAKE-EVIDENCE.md
  - .planning/REQUIREMENTS.md
  - src/pages/Chat.test.tsx
autonomous: false
requirements: [DEBT-06]

must_haves:
  truths:
    - "D-09: the soak is the FULL SUITE repeated, never the file in isolation — the file already passes alone, so an isolation soak would be a false all-clear and both surviving hypotheses are cross-test or cross-file"
    - "D-09 budget as decided by Larry: tiered — 30 full-suite iterations, then 50 more ONLY if tier 1 is clean; stop early and immediately on any reproduction"
    - "Every iteration's pass/fail is recorded individually in an append-only log; a failure at iteration 23 of 30 is never overwritten by iteration 30's success"
    - "D-10: if it does not reproduce within budget, ship the instrumentation and close as GUARDED — and amend DEBT-06's wording in .planning/REQUIREMENTS.md so a guarded close is not recorded as root-caused"
    - "D-11 still binds on the reproduction branch: a captured failure is fixed at its cause, never by widening a waitFor and never by reshaping the assertion to read source data instead of rendered text"
    - "The soak runs against a stable tree — every other plan in this phase has landed first, so a mid-soak edit cannot be mistaken for a flake"
  artifacts:
    - path: ".planning/phases/113-debt-sweep/113-FLAKE-EVIDENCE.md"
      provides: "The soak record: per-iteration outcomes, the disposition taken, and either the captured failure or the recorded refutation"
      min_lines: 40
    - path: ".planning/REQUIREMENTS.md"
      provides: "DEBT-06 wording matching what was actually delivered"
      contains: "DEBT-06"
  key_links:
    - from: "scripts/soak-vitest.mjs"
      to: ".planning/phases/113-debt-sweep/113-soak-tier1.log"
      via: "append-only per-iteration record"
      pattern: "iteration="
    - from: ".planning/phases/113-debt-sweep/113-FLAKE-EVIDENCE.md"
      to: ".planning/REQUIREMENTS.md"
      via: "the disposition drives whether DEBT-06's wording is amended"
      pattern: "DEBT-06"
---

<objective>
Run the tiered full-suite soak that plan 113-05's runner exists for, and record an honest
disposition: either a captured reproduction that is root-caused and fixed, or a GUARDED close with
`REQUIREMENTS.md` amended to say so.

Purpose: the `Chat.test.tsx` brain-pill failure was seen once in roughly a dozen recorded runs.
Research measured one full suite at 37.98s, so 30 iterations is about 19 minutes and gives roughly a
93% chance of a reproduction if that base rate holds; the tier-2 extension takes it to about 99%. The
budget is not discretionary — Larry set it. What IS at stake is honesty about the outcome: a phase
that closes without a reproduction must not have "root-caused" written into its requirement.

Output: two soak logs, an evidence document, and either a fix or an amended requirement.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/113-debt-sweep/113-CONTEXT.md
@.planning/phases/113-debt-sweep/113-RESEARCH.md
@.planning/phases/113-debt-sweep/113-VALIDATION.md
@.planning/phases/113-debt-sweep/113-HANDOFF.md
@.planning/phases/113-debt-sweep/113-05-SUMMARY.md
@.planning/phases/113-debt-sweep/113-FLAKE-INSTRUMENTATION.md
@.planning/milestones/v13.0-phases/106-consolidation-hardening/deferred-items.md

<interfaces>
<!-- Contracts the executor needs. -->

THE RUNNER (built and control-proven in plan 113-05):
  node scripts/soak-vitest.mjs --iterations <n> --label <text> --log <path> [--command "<cmd>"]
  default command: `npx vitest run` (the FULL suite — this is what D-09 requires)
  appends one line per iteration: `<iso> label=... iteration=<i>/<n> status=PASS|FAIL exit=<c> duration_ms=<ms>`
  on failure: writes `<log>.iteration-<i>.txt` with full output, stops, exits 1

BUDGET (D-09/D-10, decided by Larry 2026-08-11):
  tier 1 = 30 iterations (~19 min at the measured 37.98s/run)
  tier 2 = 50 iterations, run ONLY if tier 1 recorded zero failures (~32 min)
  stop immediately on any reproduction; do not continue to fill the budget

TIMEOUT NOTE: a 30-iteration soak far exceeds the Bash tool's 10-minute ceiling. Launch each tier as
a BACKGROUND process and poll the log file, rather than blocking on the command.

KNOWN-FAILING TESTS THAT ARE NOT THIS PHASE'S — none of them are in the `npx vitest run` unit suite;
they are Playwright e2e specs and must not be conflated with a soak failure:
  e2e/theme-contrast.spec.ts (20 failures, SEED-006)
  e2e/command-center-breakpoints.spec.ts (3 failures, Phase 111's)
  the Seiðr specs galdr/bifrost/loom (flaky under parallel load — same CLASS as this defect, a
  different instance, explicitly out of scope)

D-11, restated so no branch of this plan can forget it: a widened `waitFor` is forbidden, and so is
any reshaping of the assertion that makes it structurally unable to observe the defect.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tier 1 — 30 full-suite iterations with per-iteration recording (D-09)</name>

  <files>.planning/phases/113-debt-sweep/113-FLAKE-EVIDENCE.md</files>

  <read_first>
    - `.planning/phases/113-debt-sweep/113-05-SUMMARY.md` — the runner's proven invocation and its
      two control results.
    - `.planning/phases/113-debt-sweep/113-HANDOFF.md` — the list of failures that are NOT this
      phase's, so a soak failure is classified correctly.
    - `scripts/soak-vitest.mjs` — the argument surface you are about to drive.
  </read_first>

  <action>
First establish the pre-soak baseline on the current tree, so any later failure is unambiguous.
Run `npx vitest run` once and record the file count, test count, failure count, and wall clock.
Record them against research's 2026-08-11 baseline of 298 files / 3958 tests / 0 failures / 37.98s.
If this single run is already red, STOP and report — a soak against a red baseline measures nothing.

Also record `git rev-parse HEAD` and `git status --porcelain` before starting. The soak must run on a
stable tree; if a concurrent session commits mid-soak the measurement is contaminated, and knowing
the starting SHA is how you detect that afterwards.

Then launch tier 1 in the BACKGROUND — it runs about 19 minutes and will exceed any foreground
timeout:
`node scripts/soak-vitest.mjs --iterations 30 --label tier1 --log .planning/phases/113-debt-sweep/113-soak-tier1.log`

Poll the log periodically rather than blocking. When the run ends, derive every number below from the
log's own lines, not from the runner's summary line and not from memory:
- count of lines matching `status=PASS`
- count of lines matching `status=FAIL`
- the highest `iteration=` reached
- min, max, and mean `duration_ms`

Create `.planning/phases/113-debt-sweep/113-FLAKE-EVIDENCE.md` and write the tier-1 section: the
baseline numbers, the starting SHA, the derived counts above, and the verbatim first and last log
lines. If a FAIL was recorded, also inline the full contents of the corresponding
`113-soak-tier1.log.iteration-<i>.txt` — that captured output is the entire deliverable of this
requirement and must not be left in a side file that a later reader will not open.
  </action>

  <verify>
    <automated>grep -c 'iteration=' .planning/phases/113-debt-sweep/113-soak-tier1.log</automated>
  </verify>

  <acceptance_criteria>
    - `.planning/phases/113-debt-sweep/113-soak-tier1.log` contains 30 iteration lines, OR fewer with
      the last one carrying `status=FAIL` (early stop on reproduction). Any other line count is a
      runner malfunction and must be reported, not rounded off.
    - The PASS and FAIL counts written into the evidence file are derived by grepping the log in the
      transcript, and the grep output is shown — not asserted from the runner's own summary.
    - `git rev-parse HEAD` after the soak equals the value recorded before it, or the difference is
      explicitly reported as contamination.
    - The pre-soak single `npx vitest run` reported 0 failures, with its actual file/test counts
      quoted against the 298/3958 baseline.
    - If any iteration failed, the corresponding capture file exists, is non-empty, and its contents
      are inlined into `113-FLAKE-EVIDENCE.md`.
  </acceptance_criteria>

  <done>Thirty full-suite iterations (or fewer, ending on a reproduction) are recorded line by line, and the tier-1 section of the evidence file states the real derived counts.</done>
</task>

<task type="auto">
  <name>Task 2: Tier 2 — 50 further iterations, only if tier 1 was clean (D-09)</name>

  <files>.planning/phases/113-debt-sweep/113-FLAKE-EVIDENCE.md</files>

  <read_first>
    - `.planning/phases/113-debt-sweep/113-FLAKE-EVIDENCE.md` — the tier-1 section you just wrote.
    - `.planning/phases/113-debt-sweep/113-soak-tier1.log` — read the actual lines, not the summary.
  </read_first>

  <action>
This task is CONDITIONAL. If tier 1 recorded any `status=FAIL`, do not run tier 2 at all: write a
one-paragraph tier-2 section in the evidence file stating that it was skipped because a reproduction
was already captured at iteration N, and move directly to Task 3's reproduction branch. Skipping here
is the correct outcome, not an omission — say so explicitly so a later reader does not read the
absent tier as an unrun budget.

Otherwise launch tier 2 in the BACKGROUND against the SAME log path so the record stays continuous
and append-only:
`node scripts/soak-vitest.mjs --iterations 50 --label tier2 --log .planning/phases/113-debt-sweep/113-soak-tier2.log`

Use a separate log file for tier 2 (as shown) so each tier's line numbering stays readable, but
verify explicitly that the tier-1 log still contains its 30 lines afterwards — tier 2 must not have
disturbed it.

Derive the same statistics from the tier-2 log as Task 1 derived from tier 1, and append a tier-2
section to the evidence file with the same fields plus the cumulative iteration total across both
tiers.
  </action>

  <verify>
    <automated>grep -c 'iteration=' .planning/phases/113-debt-sweep/113-soak-tier1.log</automated>
  </verify>

  <acceptance_criteria>
    - Either the evidence file's tier-2 section states the skip and names the tier-1 iteration that
      reproduced, or `113-soak-tier2.log` exists with 50 iteration lines (or fewer ending in FAIL).
    - `113-soak-tier1.log` still contains exactly its original 30 iteration lines after tier 2 — shown
      by a grep count in the transcript, proving append-only behaviour across runs.
    - The cumulative iteration count across both tiers is stated as a number in the evidence file.
    - Any FAIL in tier 2 has its capture file inlined into the evidence file, exactly as in Task 1.
  </acceptance_criteria>

  <done>Tier 2 either ran to 80 cumulative iterations or was deliberately and visibly skipped because tier 1 already reproduced the defect.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 3: Disposition — root-cause the capture, or close GUARDED and amend the requirement (D-10)</name>

  <files>.planning/phases/113-debt-sweep/113-FLAKE-EVIDENCE.md, .planning/REQUIREMENTS.md, src/pages/Chat.test.tsx</files>

  <read_first>
    - `.planning/phases/113-debt-sweep/113-FLAKE-EVIDENCE.md` — both tier sections.
    - `.planning/REQUIREMENTS.md` lines 59-66 — the DEBT-05/06/07 block; DEBT-06's current wording is
      the thing that may need amending.
    - `.planning/phases/113-debt-sweep/113-CONTEXT.md` § D-10 and § D-11.
    - `.planning/milestones/v13.0-phases/106-consolidation-hardening/deferred-items.md` §
      `D-106-04-01` — the three already-refuted causes; do not re-investigate them.
  </read_first>

  <decision>Which disposition does the soak evidence actually support: a captured reproduction to root-cause, or a GUARDED close?</decision>

  <context>
    The soak ran to the decided budget. The two branches below are not equivalent and must not be
    blurred: one delivers a determinstic fix, the other delivers a self-diagnosing next occurrence
    plus a corrected requirement. Writing "root-caused" into a requirement that was closed GUARDED is
    exactly the defect this project's verification rules exist to prevent.

    Present the evidence before the options: cumulative iterations run, PASS/FAIL counts derived from
    the logs, and — if a failure was captured — the match count, each match's `textContent`, and the
    `document.body.innerHTML` length from the instrumentation's capture.
  </context>

  <options>
    <option id="reproduced">
      <name>Reproduced — root-cause and fix from the capture</name>
      <pros>Delivers the requirement as written: deterministic, root-caused from a captured failure.</pros>
      <cons>Scope is unbounded until the cause is understood; the fix may reach beyond the test file.</cons>
    </option>
    <option id="guarded">
      <name>Not reproduced — close GUARDED and amend DEBT-06's wording</name>
      <pros>Honest. Ships the instrumentation so the next occurrence is self-diagnosing, records the refutations, and does not hold the phase hostage to a defect seen once in a dozen runs.</pros>
      <cons>The defect remains latent; the requirement's text must change, which is a deliberate, visible admission rather than a silent one.</cons>
    </option>
  </options>

  <action>
On the `reproduced` branch: work only from the captured evidence. The instrumentation records the
match count, so start by settling whether more than one element carried the testid — that is the one
surviving hypothesis, and research already refuted shared-fixture contamination of the brain mocks,
the async catalogue changing the label mid-assertion, and `useGlobalModelNames`. Do not re-investigate
those three. Fix the cause. D-11 binds absolutely: no widened `waitFor`, no added retry, no
`{ timeout }`, and no rewriting the assertion to read `mockActiveEngineMap` or any other source data
instead of the rendered `textContent`. After the fix, re-run tier 1's 30 iterations against the fixed
tree and record the result as a second tier-1 block in the evidence file — a fix with no post-fix soak
is a hypothesis.

On the `guarded` branch: change nothing in `src/pages/Chat.test.tsx`. Amend the DEBT-06 line in
`.planning/REQUIREMENTS.md` so its text describes what was delivered — instrumentation that captures
the failure at the query site, the recorded refutations, and the soak budget that did not reproduce —
and so it no longer asserts the defect was root-caused. Keep the `waitFor` prohibition in the wording;
that criterion was met. Add a one-line pointer to `113-FLAKE-EVIDENCE.md`.

Either way, finish `113-FLAKE-EVIDENCE.md` with a Disposition section naming the branch taken, the
cumulative iteration count, and — on the guarded branch — an explicit sentence stating that DEBT-06
is closed GUARDED and NOT root-caused.

`.planning/REQUIREMENTS.md` is also edited by the concurrent session in this checkout. Re-read it
immediately before editing, edit only the DEBT-06 line, and never `git checkout --` it.
  </action>

  <acceptance_criteria>
    - The evidence file's Disposition section names exactly one branch and states the cumulative
      iteration count as a number.
    - On the `guarded` branch: `grep -c 'root-caused' .planning/REQUIREMENTS.md` on the DEBT-06 line
      is 0 after the amendment, and the line contains a pointer to `113-FLAKE-EVIDENCE.md`. Quote the
      before and after text of that line in the SUMMARY.
    - On the `guarded` branch: `git diff -- src/pages/Chat.test.tsx` is empty (nothing was "fixed"
      without a captured cause).
    - On the `reproduced` branch: `grep -c 'waitFor' src/pages/Chat.test.tsx` is unchanged from the
      count recorded in plan 113-05, and the file contains no `{ timeout:` added by this plan — D-11
      asserted rather than asserted-about.
    - On the `reproduced` branch: a post-fix 30-iteration soak log exists with 30 `status=PASS` lines.
    - `.planning/REQUIREMENTS.md`'s diff touches the DEBT-06 line and nothing else.
  </acceptance_criteria>

  <verify>
    <automated>npx vitest run src/pages/Chat.test.tsx</automated>
  </verify>

  <resume-signal>Select: reproduced, or guarded.</resume-signal>

  <done>One disposition is recorded, it matches the soak evidence, and on the guarded branch `REQUIREMENTS.md`'s DEBT-06 line no longer claims a root cause.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| captured test output → committed planning artifact | full-suite output is inlined into a tracked evidence file in a PUBLIC repository |
| soak process → shared git checkout | a long-running measurement overlaps another session's commits |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-113-24 | Information Disclosure | inlining full soak output into `113-FLAKE-EVIDENCE.md` in a repo confirmed PUBLIC | mitigate | Before committing, scan the inlined output for absolute home paths, tokens, and any `VITE_`/`ASTRIDR_`/`CONVEX_` value; redact anything that is not test text. The unit suite runs in jsdom with mocked externals, so exposure is low, but the review is mandatory rather than assumed |
| T-113-25 | Repudiation | recording a green soak that ran against a moving tree | mitigate | The starting `git rev-parse HEAD` is recorded and re-checked afterwards; a change is reported as contamination rather than ignored |
| T-113-26 | Tampering | a "fix" that masks the defect instead of removing it | mitigate | D-11 is asserted mechanically in the acceptance criteria (`waitFor` count unchanged, no added `{ timeout:`), not merely stated as an intention |
| T-113-27 | Repudiation | a GUARDED close recorded in `REQUIREMENTS.md` as root-caused | mitigate | The guarded branch requires the DEBT-06 line to be amended and requires an explicit "closed GUARDED and NOT root-caused" sentence in the evidence file |
| T-113-SC | Tampering | npm/pip/cargo installs | mitigate | No packages are added by this plan; if any install becomes necessary, stop and run the Package Legitimacy Gate before proceeding |
</threat_model>

<verification>
- `grep -c 'status=PASS' .planning/phases/113-debt-sweep/113-soak-tier1.log` and the matching
  `status=FAIL` count are shown in the transcript and match the numbers written into the evidence
  file.
- The cumulative iteration count across tiers is stated as a number.
- `npx vitest run` is green at the end of the plan.
- On the guarded branch, `.planning/REQUIREMENTS.md`'s DEBT-06 line no longer claims a root cause.
</verification>

<success_criteria>
- The decided budget was run: 30 iterations, plus 50 more only if the first tier was clean, stopping
  immediately on any reproduction.
- Every iteration's outcome is individually recoverable from an append-only log.
- The disposition recorded matches the evidence, and a GUARDED close is labelled as such in both the
  evidence file and `REQUIREMENTS.md`.
- No widened `waitFor` and no reshaped assertion exist in `src/pages/Chat.test.tsx`.
</success_criteria>

<output>
Create `.planning/phases/113-debt-sweep/113-06-SUMMARY.md` when done.

Commit with a pathspec-limited command — a concurrent session commits to this checkout every few
minutes and also edits `.planning/REQUIREMENTS.md`. Re-check `git status` immediately before
committing, then:
`git commit -- .planning/REQUIREMENTS.md .planning/phases/113-debt-sweep/113-FLAKE-EVIDENCE.md .planning/phases/113-debt-sweep/113-soak-tier1.log .planning/phases/113-debt-sweep/113-soak-tier2.log`
(add `src/pages/Chat.test.tsx` only on the reproduced branch).
Verify with `git show --stat HEAD` afterward. Never `git commit --amend` here.
</output>
