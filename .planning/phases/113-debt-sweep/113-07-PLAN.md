---
phase: 113-debt-sweep
plan: 07
type: execute
wave: 1
depends_on: []
files_modified:
  - C:/Users/mandr/convex-selfhost/.gitignore
  - C:/Users/mandr/convex-selfhost/docker-compose.yml
  - C:/Users/mandr/convex-selfhost/selfhosted.envfile.example
autonomous: false
requirements: [DEBT-07]

must_haves:
  truths:
    - "D-15: the ignore set exists BEFORE any git add and excludes the measured bulk — backups/ 22 GB, migration/ 1.5 GB, rebuild/ 721 MB, forensics/ 868 KB, all *.log, *.bak, soak-watch.state.json and canary-body.json — because a git add -A before it would attempt to stage about 24 GB"
    - "D-14: secrets are gitignored with a committed key-name-only .example template, and that template is built from DOCUMENTED key names — never by reading or dumping the live env file, which the env-file-guard hook correctly blocks"
    - "D-18: INSTANCE_SECRET is PARAMETERIZED out of docker-compose.yml before the first commit; rotation is explicitly NOT in this phase and no rotation task exists here"
    - "D-13's deferred exclusions are enforced by the ignore file: diagnosis-*.md and health-report.md carry host paths and container internals and stay out of the initial commit scope"
    - "D-12: the repository is initialised in place at C:/Users/mandr/convex-selfhost and is separate from codepulse, which is confirmed PUBLIC — nothing here is ever added to the codepulse repo"
    - "The ignore rules are control-proven: git check-ignore shows the matching rule for each bulk path, and shows a non-ignored exit for a file that must be committed"
    - "The compose file is never Read into the transcript and never rendered without a filter, because it holds a live secret until this plan removes it"
  artifacts:
    - path: "C:/Users/mandr/convex-selfhost/.gitignore"
      provides: "Secrets, bulk and deferred-notes exclusion, written before any staging"
      contains: "backups/"
    - path: "C:/Users/mandr/convex-selfhost/selfhosted.envfile.example"
      provides: "Key-name-only template built from documented Convex self-hosted variable names"
      contains: "CONVEX_SELF_HOSTED_ADMIN_KEY="
    - path: "C:/Users/mandr/convex-selfhost/docker-compose.yml"
      provides: "Backend service sourcing INSTANCE_SECRET from the ignored env file instead of an inline literal"
      contains: "env_file"
  key_links:
    - from: "C:/Users/mandr/convex-selfhost/docker-compose.yml"
      to: "C:/Users/mandr/convex-selfhost/selfhosted.envfile"
      via: "env_file directive on the backend service supplying INSTANCE_SECRET"
      pattern: "env_file"
    - from: "C:/Users/mandr/convex-selfhost/.gitignore"
      to: "C:/Users/mandr/convex-selfhost/selfhosted.envfile"
      via: "ignore rule keeping the live secret out of every commit"
      pattern: "selfhosted\\.envfile"
---

<objective>
Make `C:\Users\mandr\convex-selfhost\` safe to put under version control: write the ignore set first,
move the live `INSTANCE_SECRET` out of `docker-compose.yml` and into the already-ignored env file, and
stand up a key-name-only template.

Purpose: two ordering hazards decide whether this item ships safely or catastrophically. `backups/`,
`migration/` and `rebuild/` total about 24 GB, so a `git add -A` before the ignore file exists would
try to stage all of it. And `docker-compose.yml:89` carries a live `INSTANCE_SECRET` inline — that
file sits squarely inside D-13's committed scope while D-14's secret list named only `admin-key.txt`
and `selfhosted.envfile`, so the value is uncovered and would go into git history in the clear, even
in a private repo. Both are fixed here, before plan 113-08 stages anything.

Rotation of that secret is explicitly out of scope. Larry's call, recorded in D-18: parameterize
only. Research separately noted the value is already mirrored to NAS in plaintext by
`backup-convex.ps1`; that exposure is recorded so it is not silently dropped, and it is not actioned
by this phase.

Output: a `.gitignore` proven to cover the bulk, an initialised-but-unstaged repo, a parameterized
compose file, and a `selfhosted.envfile.example`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/113-debt-sweep/113-CONTEXT.md
@.planning/phases/113-debt-sweep/113-RESEARCH.md
@.planning/phases/113-debt-sweep/113-PATTERNS.md
@.planning/phases/113-debt-sweep/113-VALIDATION.md
@CLAUDE.md
@.gitignore
@.env.example

<interfaces>
<!-- Facts the executor needs. Verified live 2026-08-11 — do not re-derive by reading the secret. -->

HARD PROHIBITIONS FOR THIS PLAN — read before touching anything:
- NEVER Read, cat, grep-with-output, `source`, or otherwise dump `selfhosted.envfile` or
  `admin-key.txt`. The env-file-guard hook blocks this and the block is correct.
- NEVER Read `docker-compose.yml` into the transcript. Until Task 3 completes it contains a live
  secret at line 89. Edit it with a non-printing in-place operation and verify with `grep -c`
  (count only). Do NOT use grep's `-A`, `-B`, or `-C` context flags on this file — they would print
  neighbouring lines.
- NEVER run `docker compose config` without a filter that suppresses its output: it RENDERS
  `env_file` contents, including the secret, into its rendered environment block. `--quiet` produces
  no output and is safe; a piped `awk` that prints only a character length is safe; a bare invocation
  is not.
- NEVER run `npx convex env list` bare — it prints `NAME=VALUE` against this self-hosted backend.
- Rotation of `INSTANCE_SECRET` is NOT in this phase (D-18). Do not plan it, do not do it, do not
  suggest doing it inline.

DIRECTORY STATE (verified live 2026-08-11):
  `git status` in that directory -> `fatal: not a git repository` (and no parent repo above it)
  codepulse -> `{"isPrivate":false,"visibility":"PUBLIC"}` — which is why D-12 requires a SEPARATE repo
  Measured sizes: backups/ 22G, migration/ 1.5G, rebuild/ 721M, forensics/ 868K; next largest item
  after those is retention-health.log at 44K, already covered by the *.log rule.

FILES PRESENT (from a live listing, for the ignore-set and later commit-scope decisions):
  9 scripts: backup-convex.ps1, deploy-retention-to-local.ps1, install-nas-key.ps1,
    restart-convex.ps1, restore-cap-48g.ps1, retention-health-check.ps1, retention-root-cause.ps1,
    soak-watch.ps1, verify-retention-fix.ps1
  plus run-restart-hidden.vbs, docker-compose.yml, docker-compose.standby.yml
  secrets: admin-key.txt, selfhosted.envfile
  bulk dirs: backups/, migration/, rebuild/, forensics/
  logs: backup.log, claude-self-diagnose.log, restart-convex.log, restore-cap.log,
    retention-health.log, retention-root-cause.log, soak-watch.log, verify-retention-fix.log
  deferred notes (D-13 rejected them from initial scope): diagnosis-2026-08-01-0530.md through
    diagnosis-2026-08-10-1834.md (7 files), health-report.md
  other: canary-body.json, soak-watch.state.json, retention-health-check.ps1.pre-110.bak

COMPOSE STRUCTURE (from a key-only grep — no values printed):
  line 8   `services:`
  line 9   `  backend:`
  line 89  the inline `- INSTANCE_SECRET=<live value>` entry inside backend.environment
  line 102 `  dashboard:`
  line 116 `networks:`   line 120 `volumes:`
  There is NO `env_file:` directive anywhere in the file today.

WHY env_file AND NOT a `.env` + `${VAR}` INTERPOLATION: `backup-convex.ps1:97` mirrors an explicit
file list to NAS — `docker-compose.yml`, `docker-compose.standby.yml`, `selfhosted.envfile`,
`admin-key.txt`, `backup-convex.ps1`, `soak-watch.ps1`. `selfhosted.envfile` is already in that list,
so putting the secret there keeps disaster recovery working with no change to the backup script. A
new `.env` would not be backed up, and D-18 names "the ignored env file" — this is it.

RESTART SAFETY: `restart-convex.ps1:60` uses `docker restart <container>`, NOT `compose up`. So this
compose edit does not change what the nightly `ConvexNightlyRestart` task does, and the running
container keeps its current environment. The edit takes effect only on the next
`docker compose up`/`--force-recreate`, which is the documented escalation path at
`restart-convex.ps1:63`. That is exactly why the env file must gain the key BEFORE the compose line
is removed.

DOCUMENTED CONVEX SELF-HOSTED VARIABLE NAMES (from the official get-convex/convex-backend
self-hosted README, corroborated by 6+ prior phase artifacts in this repo — public CLI contract, not
a project secret):
  CONVEX_SELF_HOSTED_URL
  CONVEX_SELF_HOSTED_ADMIN_KEY

TEMPLATE FORMAT TO MIRROR: codepulse's own root `.env.example` — a comment above each variable
explaining its purpose, blank or obviously-placeholder values, no live data.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the ignore set FIRST, then initialise the repo, then prove the rules (D-15, D-12)</name>

  <files>C:/Users/mandr/convex-selfhost/.gitignore</files>

  <read_first>
    - `.gitignore` at the codepulse repo root — the "ignore all env files except the committed
      template" idiom (CSO-95-03) to mirror.
    - `.planning/phases/113-debt-sweep/113-CONTEXT.md` § D-14, § D-15 and the D-13 rejected-alternative
      note about `diagnosis-*.md` / `health-report.md`.
    - `.planning/phases/113-debt-sweep/113-PATTERNS.md` § "No Analog Found" — the codepulse
      conventions surfaced for this item.
    - Do NOT read any file inside `C:/Users/mandr/convex-selfhost/` for this task. The directory
      listing you need is reproduced in `<interfaces>` above.
  </read_first>

  <action>
Create `C:/Users/mandr/convex-selfhost/.gitignore` with four commented sections, in this order:

1. Secrets (D-14, D-18): `selfhosted.envfile`, `admin-key.txt`, `.env`, `.env.*`, and an explicit
   un-ignore for `selfhosted.envfile.example` so a future broadening of the secret pattern cannot
   accidentally swallow the template. Comment it with the reason, mirroring codepulse's own
   CSO-95-03 comment style.
2. Bulk and volatile operational data (D-15), with the measured size in a trailing comment on each
   directory so a future reader knows why: `backups/`, `migration/`, `rebuild/`, `forensics/`. Write
   these WITH the trailing slash — a directory-anchored pattern makes git skip recursing into 24 GB
   rather than enumerating it.
3. Volatile files: `*.log`, `*.bak`, `soak-watch.state.json`, `canary-body.json`.
4. Operational notes deliberately outside the initial commit scope (D-13's rejected alternative):
   `diagnosis-*.md`, `health-report.md`. Comment that these are genuine investigation history held
   back because they carry host paths and container internals, and that revisiting them is a
   deliberate later decision, not an oversight.

Only AFTER that file exists, run `git init -b main` in `C:/Users/mandr/convex-selfhost`. Do NOT run
`git add`, `git add -A`, or `git commit` in this plan at all — plan 113-08 owns staging, and the
whole point of this ordering is that nothing can be staged before the ignore set is proven.

Then prove the rules with `git check-ignore`, which reports the matching rule rather than a bare
boolean:
- `git check-ignore -v backups migration rebuild forensics selfhosted.envfile admin-key.txt health-report.md`
  must print a matching `.gitignore:<line>:<pattern>` for each.
- `git check-ignore -v docker-compose.yml restart-convex.ps1 run-restart-hidden.vbs` must exit
  non-zero and print nothing — the control proving the ignore set does not swallow the files that
  MUST be committed. Without this control, "everything is ignored" and "the right things are
  ignored" look identical.
- `git status --porcelain` must list the scripts, the two compose files and `.gitignore` as
  untracked, and must NOT list `backups/`, `migration/`, `rebuild/`, `forensics/`, any `*.log`, any
  `diagnosis-*.md`, `health-report.md`, `selfhosted.envfile`, or `admin-key.txt`.
  </action>

  <verify>
    <automated>cd /c/Users/mandr/convex-selfhost && git check-ignore -v backups migration rebuild forensics selfhosted.envfile admin-key.txt health-report.md && ! git check-ignore -q docker-compose.yml && git status --porcelain | grep -cE '^\?\? (backups|migration|rebuild|forensics|selfhosted\.envfile|admin-key\.txt|diagnosis-|health-report\.md|.*\.log)' | grep -q '^0$'</automated>
  </verify>

  <acceptance_criteria>
    - `.gitignore` exists and `git rev-parse --git-dir` in that directory succeeds (the repo was
      initialised AFTER the ignore file, in that order — state the order you executed them).
    - `git check-ignore -v` printed a matching rule line for every one of: `backups`, `migration`,
      `rebuild`, `forensics`, `selfhosted.envfile`, `admin-key.txt`, `health-report.md`. Paste the
      output.
    - `git check-ignore -q docker-compose.yml` exits NON-ZERO — the control. Paste the exit code.
    - `git status --porcelain` output is pasted in full and contains zero entries matching the bulk,
      log, secret, or deferred-note patterns.
    - `git log` in that directory reports no commits and `git diff --cached --name-only` is empty —
      nothing was staged by this task.
    - No file inside `C:/Users/mandr/convex-selfhost/` was read into the transcript.
  </acceptance_criteria>

  <done>The ignore set exists and is proven load-bearing by `git check-ignore` in both directions; the repo is initialised with nothing staged.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Move the INSTANCE_SECRET value into the ignored env file (D-18) — human only</name>

  <files>C:/Users/mandr/convex-selfhost/selfhosted.envfile</files>

  <read_first>
    - `.planning/phases/113-debt-sweep/113-CONTEXT.md` § D-18 — including its explicit statement that
      rotation is NOT in this phase.
    - The `RESTART SAFETY` note in this plan's `<interfaces>` — why the env file must gain the key
      BEFORE the compose line is removed.
  </read_first>

  <what-built>
    `.gitignore` is in place and proven; the repository is initialised with nothing staged.
    `docker-compose.yml` is untouched and still carries the live `INSTANCE_SECRET` inline at line 89.
  </what-built>

  <how-to-verify>
    This step is yours, Larry — Claude cannot do it. The env-file-guard hook blocks all read and
    write access to `selfhosted.envfile`, and that block is correct: it is what keeps the value out
    of this transcript.

    1. Open `C:\Users\mandr\convex-selfhost\docker-compose.yml` and find line 89, the
       `- INSTANCE_SECRET=<value>` entry inside the backend service's `environment:` block.
    2. Copy that value.
    3. Open `C:\Users\mandr\convex-selfhost\selfhosted.envfile` and append a line
       `INSTANCE_SECRET=<the same value>` — same value, no quotes, no trailing spaces.
    4. Save. Do not remove the compose line yourself; the next task does that, and it takes a backup
       first.
    5. Do not rotate the secret. D-18 keeps rotation out of this phase deliberately — rotating on a
       live self-hosted backend is a production operation on a memory-pressured, volume-sensitive
       stack and is far beyond "put this directory under version control."

    Note for the record, not for action: `backup-convex.ps1:97` already mirrors `selfhosted.envfile`
    to the NAS, so this move keeps disaster recovery intact — and it also means the value has already
    left this machine in plaintext. That exposure is recorded in D-18 so it is not silently dropped.
  </how-to-verify>

  <acceptance_criteria>
    - Larry confirms the key was appended. Claude must NOT attempt to verify by reading the file —
      verification happens structurally in Task 3 via `docker compose config` piped through a
      length-only filter, which never prints the value.
    - `docker-compose.yml` is still unmodified at this point (Task 3 has not run).
  </acceptance_criteria>

  <action>
Claude does nothing in this task except present the five numbered steps above and wait. Do not
attempt to read, write, grep, `source`, or otherwise touch `selfhosted.envfile` — the env-file-guard
hook blocks it and the block is what keeps the value out of this transcript. Do not attempt to verify
the edit by reading the file afterwards; verification happens structurally in Task 3 through a
length-only probe. Do not edit `docker-compose.yml` in this task, and do not suggest rotating the
secret (D-18 keeps rotation out of this phase).
  </action>

  <verify>
    <automated>test -f /c/Users/mandr/convex-selfhost/docker-compose.yml && git -C /c/Users/mandr/convex-selfhost status --porcelain docker-compose.yml | grep -c . | grep -q '^0$'</automated>
  </verify>

  <resume-signal>Type "added" once INSTANCE_SECRET is in selfhosted.envfile, or describe what stopped you.</resume-signal>

  <done>Larry has appended `INSTANCE_SECRET` to `selfhosted.envfile`, and `docker-compose.yml` is still untouched so Task 3 can take its backup from the unmodified original.</done>
</task>

<task type="auto">
  <name>Task 3: Parameterize the compose secret and write the key-name-only template (D-18, D-14)</name>

  <files>C:/Users/mandr/convex-selfhost/docker-compose.yml, C:/Users/mandr/convex-selfhost/selfhosted.envfile.example</files>

  <read_first>
    - This plan's `<interfaces>` block — the compose structure line numbers, the prohibitions, and
      the documented Convex variable names. That block exists precisely so you do not need to read
      the compose file.
    - `.env.example` at the codepulse repo root — the template format to mirror.
    - `.planning/phases/113-debt-sweep/113-CONTEXT.md` § D-14 — the build-from-documented-names rule.
  </read_first>

  <action>
Take a backup FIRST: `cp docker-compose.yml docker-compose.yml.pre113.bak`. That backup still holds
the live value and is covered by the `*.bak` ignore rule from Task 1 — confirm that with
`git check-ignore -q docker-compose.yml.pre113.bak` before continuing. Restoring, if anything goes
wrong, is a `cp` from this backup; never `git checkout`.

Then make two non-printing in-place edits to `docker-compose.yml`. Use `sed -i`, a Node one-liner, or
PowerShell's `[IO.File]::ReadAllText`/`WriteAllText` — anything that does not echo file content. Do
not open the file with the Read tool.

Edit A — delete the inline secret line. Match the whole line on the anchored pattern
`^[[:space:]]*-[[:space:]]*INSTANCE_SECRET=` and remove it. Exactly one line must be removed; verify
with a before/after `wc -l`.

Edit B — add an `env_file` directive to the backend service so the container still receives the
variable. Anchor the insertion on the line `  backend:` (two-space indent, as confirmed at line 9)
and insert immediately after it, at four-space indent:
`env_file:` followed by a list entry `- selfhosted.envfile` at six-space indent. Do not add
`env_file` to the `dashboard:` service and do not touch `docker-compose.standby.yml` — its
`__COPY_FROM_PRIMARY_COMPOSE__` placeholder is a deliberate manual-copy marker for a DR artifact, and
plan 113-08's README documents it rather than changing it.

Then create `selfhosted.envfile.example` containing key NAMES with empty values and a comment above
each, in `.env.example`'s format: `CONVEX_SELF_HOSTED_URL=`, `CONVEX_SELF_HOSTED_ADMIN_KEY=`, and
`INSTANCE_SECRET=`. Build it from the documented names in `<interfaces>` — do NOT derive it by
reading `selfhosted.envfile`. If the env-file-guard hook blocks writing a file whose name contains
`envfile`, stop and hand the exact file contents to Larry to create manually rather than renaming the
file: the name is fixed by D-14.

Now run the verification and its control, in this order:

Verification 1 — the secret is gone from the tracked file:
`grep -v '^[[:space:]]*#' docker-compose.yml | grep -c 'INSTANCE_SECRET'` must print `0`. Use `-c`;
never print the line.

Verification 1 control — the same detector against the backup must print `1`, proving the pattern
actually matches when a secret IS present. A zero-count on an unfiltered file with no control is not
evidence.

Verification 2 — compose still parses and the directive is placed correctly:
`docker compose -f docker-compose.yml config --quiet` must exit 0. `--quiet` renders nothing, which
is what makes it safe here.

Verification 3 — the container will actually receive a non-empty value:
`docker compose -f docker-compose.yml config | awk -F': ' '/INSTANCE_SECRET/{print length($2); exit}'`
must print a single integer greater than 20. This prints only a character count, never the value.

Verification 3 control — copy the compose file to a temp path, point its `env_file` entry at a
filename that does not exist, and run the same pipeline against the copy. It must exit non-zero and
print no integer, proving the check can fail. Delete the temp copy afterwards and confirm
`git status --porcelain` shows no residue.
  </action>

  <verify>
    <automated>cd /c/Users/mandr/convex-selfhost && grep -v '^[[:space:]]*#' docker-compose.yml | grep -c 'INSTANCE_SECRET' | grep -q '^0$' && docker compose -f docker-compose.yml config --quiet && grep -c 'env_file' docker-compose.yml | grep -q '^1$'</automated>
  </verify>

  <acceptance_criteria>
    - `grep -v '^[[:space:]]*#' docker-compose.yml | grep -c 'INSTANCE_SECRET'` printed `0`, and the
      same command against `docker-compose.yml.pre113.bak` printed `1`. Both counts pasted.
    - `wc -l docker-compose.yml` decreased by exactly 1 relative to the backup for Edit A, then
      increased by exactly 2 for Edit B — net +1. State the three numbers.
    - `docker compose -f docker-compose.yml config --quiet` exited 0.
    - The length-only pipeline printed a single integer greater than 20, and the broken-env_file
      control exited non-zero with no integer. Both outputs pasted.
    - `grep -c 'env_file' docker-compose.yml` is exactly 1 and
      `grep -c -- '- selfhosted.envfile' docker-compose.yml` is exactly 1.
    - `git check-ignore -q docker-compose.yml.pre113.bak` exits 0 (the backup holding the live value
      cannot be committed).
    - `selfhosted.envfile.example` exists and contains exactly the three documented key names with
      empty values; `grep -cE '=.+' selfhosted.envfile.example` is 0 — no value on any line.
    - `git diff` on `docker-compose.standby.yml` is empty (untouched).
    - No `docker compose config` was run without a filter, and no file content was printed into the
      transcript at any point.
  </acceptance_criteria>

  <done>`docker-compose.yml` sources `INSTANCE_SECRET` from the ignored env file, the removal is proven by a detector shown to fire on the backup, and a key-name-only template exists.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local disk secrets → git object store | a live `INSTANCE_SECRET` and an admin key sit in a directory about to become a repository; git history is effectively permanent |
| `convex-selfhost/` → the PUBLIC codepulse repo one directory away | a mis-scoped `git add` from the wrong working directory is the realistic path to public exposure |
| compose file → running production backend | the backend container's identity secret is being re-sourced |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-113-28 | Information Disclosure | `INSTANCE_SECRET` committed inline via `docker-compose.yml` | mitigate | Task 3 removes the inline line before any staging exists at all (Task 1 deliberately performs no `git add`), and the removal is proven by a detector control-fired against the pre-edit backup |
| T-113-29 | Information Disclosure | `selfhosted.envfile` / `admin-key.txt` staged by a careless `git add -A` | mitigate | Both are in the ignore set written before `git init`, and `git check-ignore -v` prints the matching rule for each; the inverse control proves the set does not over-match |
| T-113-30 | Information Disclosure | a secret printed into the session transcript by a read or an unfiltered render | mitigate | Explicit prohibitions on Read, on grep context flags, and on bare `docker compose config`; every verification is count-only, exit-code-only, or length-only |
| T-113-31 | Information Disclosure | `docker-compose.yml.pre113.bak` retaining the live value | mitigate | Covered by the `*.bak` ignore rule, and an explicit `git check-ignore -q` on that exact filename is an acceptance criterion |
| T-113-32 | Denial of Service | a next `docker compose up --force-recreate` bringing the backend up with no instance secret | mitigate | Task 2 (human) adds the key to the env file BEFORE Task 3 removes the inline line, and Task 3's length-only check proves the resolved value is non-empty; the nightly restart uses `docker restart` and is unaffected |
| T-113-33 | Tampering | committing 24 GB of backups into a new repo | mitigate | Directory-anchored ignore patterns written before `git init`, with `git status --porcelain` asserted to list none of them |
| T-113-34 | Information Disclosure | the already-existing NAS plaintext mirror of the secret | accept | Recorded in D-18 as a known exposure; rotation is explicitly out of this phase per Larry's decision, and is not silently dropped because it is written into the decision record |
| T-113-SC | Tampering | npm/pip/cargo installs | mitigate | No packages are added by this plan; if any install becomes necessary, stop and run the Package Legitimacy Gate before proceeding |
</threat_model>

<verification>
- `git check-ignore -v` prints a matching rule for every bulk path, secret file, and deferred note;
  and exits non-zero for `docker-compose.yml`.
- `git status --porcelain` in `convex-selfhost` lists only committable files.
- `git log` in `convex-selfhost` reports no commits — nothing was staged or committed by this plan.
- `grep -v '^[[:space:]]*#' docker-compose.yml | grep -c 'INSTANCE_SECRET'` is 0 while the same
  command against the pre-edit backup is 1.
- `docker compose -f docker-compose.yml config --quiet` exits 0 and the length-only probe reports a
  value longer than 20 characters.
- No secret value appears anywhere in the session transcript.
</verification>

<success_criteria>
- The ignore set existed before the repository did, and its rules are proven in both directions.
- The live `INSTANCE_SECRET` is no longer present in `docker-compose.yml`, and the backend still
  resolves a non-empty value for it.
- A key-name-only `selfhosted.envfile.example` exists, built from documented names.
- Nothing was staged, committed, or pushed by this plan; no rotation was attempted.
</success_criteria>

<output>
Create `.planning/phases/113-debt-sweep/113-07-SUMMARY.md` in the codepulse repo when done. The
SUMMARY must contain no secret values — only counts, exit codes and lengths.

Commit ONLY the SUMMARY to codepulse, with a pathspec-limited command; the `convex-selfhost` files
are committed by plan 113-08 into their OWN repository, never into codepulse. Re-check `git status`
in codepulse immediately before committing, then:
`git commit -- .planning/phases/113-debt-sweep/113-07-SUMMARY.md`
Verify with `git show --stat HEAD` afterward. Never `git commit --amend` here.
</output>
