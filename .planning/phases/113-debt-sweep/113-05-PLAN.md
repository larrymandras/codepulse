---
phase: 113-debt-sweep
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/Chat.test.tsx
  - scripts/soak-vitest.mjs
autonomous: true
requirements: [DEBT-06]

must_haves:
  truths:
    - "D-08 as amended: capture happens AT THE QUERY SITE, synchronously, via an inline queryAllByTestId whose output rides on the assertion's own failure message — never via a global onTestFailed hook in src/test/setup.ts, which always fires after Testing Library's auto afterEach(cleanup) has stripped the DOM"
    - "D-08 instrumentation acceptance: the first captured dump comes from a DELIBERATE CONTROL and is NON-EMPTY; an empty dump is a bug in the instrumentation, never evidence the DOM was empty at test time"
    - "D-11: the assertion stays expect(labelBefore).toBe('anthropic-sonnet-5') on rendered textContent — no widened waitFor, no extra retry, and no reshaping to read source data instead of rendered text"
    - "The soak runner records every iteration individually to an append-only log, so a failure at iteration 23 of 30 cannot be overwritten by iteration 30's success"
    - "The soak runner's own failure path is control-proven: it is exercised against a deliberately failing target and shown to record the FAIL, capture the output, and exit non-zero"
    - "src/test/setup.ts is NOT modified — research proved both from @vitest/runner source and empirically that a hook registered there sees an empty body"
  artifacts:
    - path: "src/pages/Chat.test.tsx"
      provides: "Query-site DOM capture on the brain-pill label assertions"
      contains: "queryAllByTestId"
    - path: "scripts/soak-vitest.mjs"
      provides: "Per-iteration append-only soak runner with captured failure output"
      min_lines: 60
  key_links:
    - from: "src/pages/Chat.test.tsx"
      to: "@testing-library/react screen"
      via: "screen.queryAllByTestId('chat-brain-pill-label') evaluated before the toBe assertion"
      pattern: "queryAllByTestId\\(\"chat-brain-pill-label\"\\)"
    - from: "scripts/soak-vitest.mjs"
      to: "npx vitest run"
      via: "child process per iteration, exit code and captured output recorded per line"
      pattern: "vitest run"
---

<objective>
Make the next occurrence of the `Chat.test.tsx` brain-pill flake self-diagnosing instead of lost, and
build the soak harness plan 113-06 will run.

Purpose: the deferred item `D-106-04-01` records three already-refuted causes and one outstanding
lever — capturing the actual `textContent` on failure. Research settled two things about that lever.
First, Vitest 4.1.9 DOES print the received value on a `toBe` mismatch (a control at the exact
assertion site produced `Expected: "DELIBERATE_CONTROL_MISMATCH_9x7q2"` / `Received:
"anthropic-sonnet-5"`), so the original failure's diagnostic value existed and was simply not
retained. Second, and this is why D-08 was amended: the originally-specified `onTestFailed` hook in
`src/test/setup.ts` captures NOTHING. `@vitest/runner`'s finalization order is fixed and sequential
— `afterEach` first, `onTestFailed` last — and `@testing-library/react` auto-registers
`afterEach(cleanup)` because `vitest.config.ts:14` sets `globals: true`, so a global hook always
observes an empty `<body>`. Proven from runner source and empirically.

The remaining live hypothesis turns on whether TWO elements ever match the testid, which a string
diff cannot show and an inline `queryAllByTestId` can.

Output: query-site instrumentation that survives cleanup because it runs before it, a control run
proving the capture is non-empty, and a soak runner with per-iteration logging.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/113-debt-sweep/113-CONTEXT.md
@.planning/phases/113-debt-sweep/113-RESEARCH.md
@.planning/phases/113-debt-sweep/113-PATTERNS.md
@.planning/phases/113-debt-sweep/113-VALIDATION.md
@.planning/phases/113-debt-sweep/113-HANDOFF.md
@.planning/milestones/v13.0-phases/106-consolidation-hardening/deferred-items.md

@src/pages/Chat.test.tsx
@scripts/verify-skills-page.mjs

<interfaces>
<!-- Contracts the executor needs. Extracted from the live codebase — do not go looking for them. -->

THE DEFECT SITE (src/pages/Chat.test.tsx, inside `describe("Chat — composer brain pill ...")` whose
beforeEach at :513-521 resets vi.clearAllMocks/registeredEventHandlers/mockStatus/
mockActiveEngineMap/lastBrainPickerProps/mockCatalogueEntries/mockDefaultProfileId):

  it("keeps the base label byte-identical while pending ...", async () => {           // :581
    mockActiveEngineMap = { "assistant-default": { model: "anthropic-sonnet-5", mode: "inherited" } };
    renderPlainChat();
    const labelBefore = (await screen.findByTestId("chat-brain-pill-label")).textContent;  // :585
    expect(labelBefore).toBe("anthropic-sonnet-5");                                        // :586  <- THE FAILURE
    ...
    expect(screen.getByTestId("chat-brain-pill-label").textContent).toBe(labelBefore);     // :598
    ...
    expect(screen.getByTestId("chat-brain-pill-label").textContent).toBe(labelBefore);     // :608
  });

VERIFIED RUNNER MECHANICS (do not re-derive, do not design around a different order):
  @vitest/runner per-test finalization, chunk-artifact.js:2967-2989 —
    1. afterEach hooks        <- @testing-library/react's auto cleanup() lives here
    2. fixture cleanup
    3. onTestFinished
    4. onTestFailed           <- LAST; body is already empty
  This is fixed control flow, NOT configurable via sequence.hooks.

VERIFIED LIBRARY BEHAVIOUR:
  @testing-library/dom 10.4.1 — getByTestId/findByTestId THROW `TestingLibraryElementError: Found
  multiple elements` on a duplicate match; they do not silently return the first. So a historical
  plain `toBe` mismatch means exactly ONE element existed at query time.
  queryAllByTestId returns an array and never throws — this is the safe probe.

VITEST ASSERTION MESSAGE ARG (this is what carries the capture without reshaping the assertion):
  expect(actual, message?: string).toBe(expected)   // message is included in the AssertionError

MEASURED SOAK FACTS:
  full suite `npx vitest run` = 37.98s (298 files, 3958 tests, 0 failures), measured 2026-08-11
  there is NO `--repeat` CLI flag on this Vitest (proven with a known-invalid-flag control)
  `--sequence.shuffle` defaults to false
  the per-task `repeats` option is the wrong mechanism — it is per-test and reruns only after success

PER-CHECK OUTPUT SHAPE TO MIRROR (scripts/verify-skills-page.mjs:26-42): an `ok(cond, label, extra)`
helper that logs PASS/FAIL per named check and accumulates failures, rather than one aggregate boolean.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Capture DOM state at the query site, not after cleanup (D-08 amended, D-11)</name>

  <files>src/pages/Chat.test.tsx</files>

  <read_first>
    - `src/pages/Chat.test.tsx` — lines 505-615: the whole `describe` block including its `beforeEach`
      at :513-521, the sibling test at :573-579 (the `within(picker)` composition), and the target
      `it` at :581-609.
    - `.planning/phases/113-debt-sweep/113-CONTEXT.md` § D-08 (amended) and § D-11 — D-11 is a
      prohibition, read it before writing a single line.
    - `.planning/phases/113-debt-sweep/113-RESEARCH.md` § "Discovery: onTestFailed fires AFTER
      cleanup" — the reason `src/test/setup.ts` is not the fix site.
    - `.planning/milestones/v13.0-phases/106-consolidation-hardening/deferred-items.md` §
      `D-106-04-01` — trust its reasoning about the three refuted causes; every source line number it
      cites is stale, so re-derive any pointer you need.
  </read_first>

  <action>
Add a module-scope helper in `src/pages/Chat.test.tsx` (near the other test-local helpers, not inside
the `it`) named `captureBrainPillDom(): string`. It must:
- call `screen.queryAllByTestId("chat-brain-pill-label")` — the non-throwing variant, so the capture
  itself can never turn a real failure into a different error;
- return a multi-line string containing: the match count; for each match, its index, its
  `textContent`, and its `outerHTML` truncated to a fixed budget (about 400 characters each, and say
  so in the string when truncation happened); whether a `chat-brain-pill-pending` element is present;
  and the character length of `document.body.innerHTML`.
- The body-length field is deliberate: it is what distinguishes "the DOM was genuinely empty at query
  time" from "the capture ran after cleanup", and a future reader must be able to tell those apart
  from the captured text alone.

Then attach the capture to the three assertions in the target `it` that read the label's
`textContent` — the one at :586 and the two `labelBefore` comparisons at :598 and :608 — by passing
the helper's return value as `expect`'s second (message) argument. Evaluate `captureBrainPillDom()`
into a local const on the line immediately before each assertion so it runs synchronously at query
time, inside the test, long before any `afterEach`.

What must NOT change, per D-11 — verify each of these by diffing your edit:
- `await screen.findByTestId("chat-brain-pill-label")` stays exactly as it is. No `waitFor` wrapper,
  no `{ timeout: ... }`, no added retry, no `await act()` inserted around it.
- The matcher stays `.toBe("anthropic-sonnet-5")` against `.textContent`. Do not switch to
  `toHaveTextContent`, do not assert against `mockActiveEngineMap`, `lastBrainPickerProps`, or any
  other source-data object. Asserting on source rather than rendered text is a mask wearing a
  different hat, and it is named as forbidden in the requirement itself.
- Do not touch `src/test/setup.ts`. Do not add an `onTestFailed` hook anywhere.
- Do not touch the `beforeEach` at :513-521.

Because this checkout is shared and `git diff` against HEAD already reflects another session's
in-progress edits to this file, take a byte-exact backup FIRST:
`cp src/pages/Chat.test.tsx src/pages/Chat.test.tsx.113-05.bak` before editing. That backup — not
`git checkout --` — is how Task 2 restores.
  </action>

  <verify>
    <automated>npx vitest run src/pages/Chat.test.tsx</automated>
  </verify>

  <acceptance_criteria>
    - `npx vitest run src/pages/Chat.test.tsx` exits 0 with the same test count as before the edit.
    - `grep -c 'queryAllByTestId("chat-brain-pill-label")' src/pages/Chat.test.tsx` is exactly 1
      (used once, inside the helper).
    - `grep -c 'captureBrainPillDom' src/pages/Chat.test.tsx` is exactly 4 (one definition, three
      call sites).
    - `git diff -- src/test/setup.ts` is empty and `grep -c 'onTestFailed' src/test/setup.ts` is 0.
    - `grep -c 'waitFor' src/pages/Chat.test.tsx` is unchanged from the pre-edit count (record both
      numbers in the SUMMARY) — D-11's prohibition, asserted rather than asserted-about.
    - The line `expect(labelBefore, ...).toBe("anthropic-sonnet-5")` still compares
      `labelBefore` (a `.textContent` read) to the literal string; quote the final line in the
      SUMMARY.
    - `src/pages/Chat.test.tsx.113-05.bak` exists.
  </acceptance_criteria>

  <done>The three label assertions carry a synchronously-captured DOM description that includes the match count, and nothing about the assertion's shape or timing changed.</done>
</task>

<task type="auto">
  <name>Task 2: Prove the capture is non-empty with a deliberate control, then restore byte-exact</name>

  <files>src/pages/Chat.test.tsx</files>

  <read_first>
    - `src/pages/Chat.test.tsx` — the instrumented `it` from Task 1.
    - `.planning/phases/113-debt-sweep/113-RESEARCH.md` § "Control 1" — the prior control's exact
      command and captured output; you are reproducing its shape with the instrumentation attached.
    - `.planning/phases/113-debt-sweep/113-VALIDATION.md` § "Instrumentation acceptance (D-08 as
      amended)".
  </read_first>

  <action>
Confirm `src/pages/Chat.test.tsx.113-05.bak` exists and is byte-identical to the current file
(`diff` exit 0). If it is not, stop — do not proceed without a restore point, because another session
holds uncommitted work in this file.

Temporarily change ONLY the expected string on the instrumented `:586` assertion to a unique control
value (use a distinctive token so it can never be confused with real output). Run just that test:
`npx vitest run src/pages/Chat.test.tsx -t "keeps the base label byte-identical"`.

Capture the complete failure output verbatim into
`.planning/phases/113-debt-sweep/113-FLAKE-INSTRUMENTATION.md`, including the AssertionError's
`Expected:`/`Received:` lines AND the full capture message.

Then restore from the backup — `cp src/pages/Chat.test.tsx.113-05.bak src/pages/Chat.test.tsx` — and
prove the restore with `diff src/pages/Chat.test.tsx.113-05.bak src/pages/Chat.test.tsx` exiting 0.
Do NOT use `git checkout -- src/pages/Chat.test.tsx`: it would revert to HEAD and destroy a
concurrent session's uncommitted work in this file. Delete the `.bak` only after the diff proves the
restore, and confirm with `git status --porcelain src/pages/Chat.test.tsx.113-05.bak` returning
nothing.

Re-run `npx vitest run src/pages/Chat.test.tsx` and confirm it is green again.
  </action>

  <verify>
    <automated>npx vitest run src/pages/Chat.test.tsx</automated>
  </verify>

  <acceptance_criteria>
    - `.planning/phases/113-debt-sweep/113-FLAKE-INSTRUMENTATION.md` exists and contains the verbatim
      control output.
    - That captured output shows a match count of at least 1 and at least one non-empty `outerHTML` —
      this is D-08's acceptance bar. An empty capture means the instrumentation is broken; fix the
      instrumentation and re-run the control rather than recording the empty result.
    - The captured output shows a `document.body.innerHTML` length greater than 0, proving the
      capture ran before cleanup rather than after it.
    - The captured output shows the `Received:` value as the real rendered label, demonstrating the
      assertion mechanism still reports the actual text.
    - `diff src/pages/Chat.test.tsx.113-05.bak src/pages/Chat.test.tsx` exited 0 BEFORE the backup was
      deleted, and the command used to restore was `cp`, not `git checkout`.
    - `npx vitest run src/pages/Chat.test.tsx` exits 0 after restoration.
  </acceptance_criteria>

  <done>A deliberate control produced a non-empty capture with a real match count and a non-zero body length, and the file was restored byte-exact from its own backup.</done>
</task>

<task type="auto">
  <name>Task 3: Per-iteration soak runner with a control-proven failure path (D-09 harness)</name>

  <files>scripts/soak-vitest.mjs</files>

  <read_first>
    - `scripts/verify-skills-page.mjs` lines 26-42 — the `ok(cond, label, extra)` per-check PASS/FAIL
      logging idiom this repo already uses; mirror its shape rather than inventing one.
    - `.planning/phases/113-debt-sweep/113-VALIDATION.md` § "Soak observable" — the append-per-
      iteration requirement and the tiered budget.
    - `.planning/phases/113-debt-sweep/113-RESEARCH.md` § "Soak mechanics" — the proof that no
      `--repeat` flag exists and that a shell loop is the correct mechanism.
  </read_first>

  <action>
Create `scripts/soak-vitest.mjs`, a plain Node ESM script with no new dependencies.

Arguments: `--iterations <n>` (required), `--log <path>` (required), `--command "<shell command>"`
(optional, defaulting to `npx vitest run`), `--label <text>` (optional, written into each log line).

Behaviour:
- Before iteration 1, append a header line to the log recording the ISO timestamp, the resolved
  command, the iteration budget, and the label. Never truncate an existing log — always append, so a
  tier-2 run does not erase tier 1.
- For each iteration, spawn the command as a child process, capture stdout and stderr, measure wall
  clock, and APPEND exactly one line:
  `<iso> label=<label> iteration=<i>/<n> status=PASS|FAIL exit=<code> duration_ms=<ms>`
  Flush that line to disk before starting the next iteration — a crash mid-soak must not lose the
  record of completed iterations.
- On a non-zero exit, additionally write the full captured stdout+stderr to
  `<log>.iteration-<i>.txt`, print the path, and STOP immediately (do not continue the remaining
  iterations).
- Also mirror each iteration line to the console using the `ok()`-style PASS/FAIL prefix so a human
  watching sees progress.
- Exit 1 if any iteration failed, 0 otherwise. Print a final summary line with
  `iterations_run=<n> passed=<n> failed=<n>` — real counts derived from the recorded lines, not from
  a boolean flag.

Then run BOTH controls and record their output in the SUMMARY:

Control A (per-iteration recording is real): run 3 iterations against a fast, known-passing target
(`--command "npx vitest run src/lib/skills.test.ts"`). Assert the log gained exactly 3 iteration
lines with `iteration=1/3`, `2/3`, `3/3`, all `status=PASS`, and that the exit code was 0. Three
distinct lines is what proves iteration 23 could not be overwritten by iteration 30.

Control B (the failure path is real): create a throwaway test file under `src/lib/` containing a
single deliberately failing assertion, run 3 iterations against it, and assert the log recorded
`iteration=1/3 status=FAIL` with a non-zero `exit=`, that `<log>.iteration-1.txt` exists and is
non-empty, that NO `iteration=2/3` line was written (the runner stopped), and that the script exited
1. Then delete the throwaway test and prove it is gone with `git status --porcelain` showing no
residue.

Without Control B the runner's green is unfalsifiable — a script that always reports PASS looks
identical to a clean soak.
  </action>

  <verify>
    <automated>node scripts/soak-vitest.mjs --iterations 3 --label control-a --log .planning/phases/113-debt-sweep/113-soak-control.log --command "npx vitest run src/lib/skills.test.ts"</automated>
  </verify>

  <acceptance_criteria>
    - Control A: the log contains exactly three iteration lines, numbered 1/3, 2/3, 3/3, all
      `status=PASS`, and the script exited 0. Quote the three lines in the SUMMARY.
    - Control B: the log contains `iteration=1/3 status=FAIL` with a non-zero `exit=` value, contains
      NO `iteration=2/3` line, the per-iteration capture file exists with a byte size greater than 0,
      and the script exited 1. Quote the FAIL line and the capture file's size in the SUMMARY.
    - Running the script a second time against the same log APPENDS rather than truncating — proven
      by the line count growing, quoted in the SUMMARY.
    - The throwaway failing test used for Control B no longer exists: `git status --porcelain src/lib/`
      shows no untracked residue.
    - `scripts/soak-vitest.mjs` adds no dependency: `git diff -- package.json package-lock.json` is
      empty.
    - The script uses `vitest run`, never a bare `vitest` (which would enter watch mode and hang the
      soak): `grep -c 'vitest run' scripts/soak-vitest.mjs` is >= 1 and
      `grep -cE '"npx vitest"|npx vitest(?! run)' scripts/soak-vitest.mjs` is 0.
  </acceptance_criteria>

  <done>The soak runner appends one durable line per iteration, stops and captures on the first failure, and both its PASS-recording and FAIL-recording paths have been demonstrated.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| soak runner → shell child process | `--command` is passed to a spawned process; this script is developer-invoked, never network-reachable |
| captured failure output → committed evidence file | test output can embed environment values from the jsdom render |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-113-20 | Information Disclosure | `<log>.iteration-N.txt` capturing full test output into the repo | mitigate | The evidence file recorded in `113-FLAKE-INSTRUMENTATION.md` must be reviewed before commit; per-iteration capture files go to `.planning/phases/113-debt-sweep/` and are reviewed in plan 113-06's disposition task before any are committed |
| T-113-21 | Tampering | `--command` shell injection in `scripts/soak-vitest.mjs` | accept | Developer-invoked local tooling with no untrusted caller; the argument is supplied by the operator running the soak, and constraining it would block its only intended use |
| T-113-22 | Denial of Service | a soak iteration hanging forever under watch mode | mitigate | The script rejects a bare `vitest` command and the acceptance criteria assert `vitest run`; each iteration's wall clock is recorded so a hang is visible in the log rather than silent |
| T-113-23 | Repudiation | a failure at iteration 23 being lost | mitigate | Append-only per-iteration lines flushed before the next iteration starts, plus a separate capture file per failure; Control A proves the lines are distinct and durable |
| T-113-SC | Tampering | npm/pip/cargo installs | mitigate | No packages are added by this plan — the runner is dependency-free Node ESM, asserted by an empty `package.json`/lockfile diff; if any install becomes necessary, stop and run the Package Legitimacy Gate |
</threat_model>

<verification>
- `npx vitest run src/pages/Chat.test.tsx` exits 0.
- `.planning/phases/113-debt-sweep/113-FLAKE-INSTRUMENTATION.md` contains a verbatim control failure
  with a non-empty capture and a non-zero body length.
- Both soak-runner controls ran, and their quoted log lines appear in the SUMMARY.
- `git diff -- src/test/setup.ts package.json package-lock.json` is empty.
- `npx vitest run` shows no new failures relative to the `113-HANDOFF.md` baseline.
</verification>

<success_criteria>
- The brain-pill label assertions carry a synchronous, pre-cleanup DOM capture including the match
  count, and the assertion itself is unchanged in shape and timing.
- The capture has been proven non-empty by a deliberate control, and the test file was restored
  byte-exact from its own backup.
- The soak runner records every iteration durably and has been shown to record a real failure.
</success_criteria>

<output>
Create `.planning/phases/113-debt-sweep/113-05-SUMMARY.md` when done.

Commit with a pathspec-limited command — a concurrent session commits to this checkout every few
minutes, and this plan touches a file that session has uncommitted work in. Re-check
`git status --porcelain src/pages/Chat.test.tsx` immediately before committing, and if the diff
contains changes you did not make, commit only your own hunks or stop and report. Then:
`git commit -- src/pages/Chat.test.tsx scripts/soak-vitest.mjs .planning/phases/113-debt-sweep/113-FLAKE-INSTRUMENTATION.md`
Verify with `git show --stat HEAD` afterward. Never `git commit --amend` here.
</output>
