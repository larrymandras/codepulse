---
phase: 77-ci-production-hardening
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/gitleaks-scan.yml
  - .gitleaks.toml
autonomous: true
requirements: [OPS-02]
tags: [ci, gitleaks, secrets, security]

must_haves:
  truths:
    - "A Gitleaks secret-scan workflow exists on master, scanning full git history (fetch-depth: 0)"
    - "The workflow distinguishes secret_found (fail, exit 1) from scan_error (neutral/warn, exit 0) from clean (pass)"
    - "A local baseline scan over full history confirms the repo is clean (or known placeholders are allowlisted) before the enforcing run is declared green"
    - "The .env.example pk_test_ Clerk placeholder does not trip the scan"
  artifacts:
    - path: ".github/workflows/gitleaks-scan.yml"
      provides: "Gitleaks CI workflow on master with 3-state classify + CodePulse notify + enforce"
      contains: "gitleaks/gitleaks-action@v3"
    - path: ".gitleaks.toml"
      provides: "CodePulse gitleaks config: useDefault rules + .env.example/test allowlist, empty regexes baseline"
      contains: "useDefault = true"
  key_links:
    - from: ".github/workflows/gitleaks-scan.yml notify step"
      to: "https://tidy-whale-981.convex.site/runtime-ingest"
      via: "curl POST with repo: codepulse"
      pattern: "runtime-ingest"
    - from: ".github/workflows/gitleaks-scan.yml classify step"
      to: "secret_found | scan_error | clean"
      via: "SARIF finding count via jq"
      pattern: "result=secret_found"
---

<objective>
Author a Gitleaks secret-scan CI workflow for CodePulse on `master` (OPS-02, D-02..D-05), mirroring Ástríðr's hardened `gitleaks-scan.yml` with exactly three diffs, plus a CodePulse `.gitleaks.toml`. Run a baseline scan over full git history to confirm clean (allowlisting the `.env.example` placeholders or remediating real hits) BEFORE the enforcing workflow is declared green.

Purpose: CodePulse has no secret scanning today. The Ástríðr workflow's 3-state classify pattern (secret_found -> fail loud, scan_error -> neutral warning, clean -> pass) is the key design: it prevents a scanner infra error from being misread as a leaked secret, which would erode trust in the CI signal. This plan is independent of Plan 01 (separate files, parallel-safe in wave 1).

Output: `.github/workflows/gitleaks-scan.yml` + `.gitleaks.toml`, with a verified-clean baseline over full history.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/77-ci-production-hardening/77-CONTEXT.md
@.planning/phases/77-ci-production-hardening/77-RESEARCH.md
@.planning/phases/77-ci-production-hardening/77-PATTERNS.md
@.planning/phases/77-ci-production-hardening/77-VALIDATION.md

<interfaces>
<!-- Template source-of-truth files (read these verbatim; apply the 3 diffs). Do NOT re-derive the workflow. -->
Template workflow: C:\Users\mandr\astridr-repo\.github\workflows\gitleaks-scan.yml (137 lines — copy verbatim, apply 3 diffs)
Template config:   C:\Users\mandr\astridr-repo\.gitleaks.toml (26 lines — mirror structure, DROP all regexes)

The 3 diffs from the Astridr template:
  Diff 1 (line 5): branches: [main]  ->  branches: [master]
  Diff 2 (notify payload jq, ~line 89): --arg repo "astridr"  ->  --arg repo "codepulse"
  Diff 3 (license): NO GITLEAKS_LICENSE anywhere (personal repo — GITHUB_TOKEN is sufficient). The template already omits it; confirm none is added.

Preserve verbatim: fetch-depth: 0; gitleaks/gitleaks-action@v3 with continue-on-error: true; GITHUB_TOKEN env; GITLEAKS_ENABLE_UPLOAD_ARTIFACT "true"; the full 3-state classify step (SARIF jq finding count); CODEPULSE_INGEST_URL=https://tidy-whale-981.convex.site; permissions {contents: read, pull-requests: read}; actions/checkout@v6 (do NOT downgrade to v4 to match ci.yml — intentional).

.env.example placeholder that WILL trip default rules and must be allowlisted: VITE_CLERK_PUBLISHABLE_KEY=pk_test_your-clerk-key-here (line 8). It is covered by the .env\.example path allowlist below — confirm during baseline.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author gitleaks-scan.yml and .gitleaks.toml from the Astridr template</name>
  <files>.github/workflows/gitleaks-scan.yml, .gitleaks.toml</files>
  <read_first>
    - C:\Users\mandr\astridr-repo\.github\workflows\gitleaks-scan.yml (the exact template — read in full)
    - C:\Users\mandr\astridr-repo\.gitleaks.toml (config structure to mirror)
    - 77-PATTERNS.md "gitleaks-scan.yml" (lines 308-359) and ".gitleaks.toml" (lines 363-390) sections — the 3 diffs and the toml adaptation
    - 77-RESEARCH.md "Pattern 2 / Pattern 3 / Pattern 4" (lines 253-305) — license note, config, baseline procedure
  </read_first>
  <action>
    Create `.github/workflows/gitleaks-scan.yml` as a verbatim copy of the Astridr template with exactly the 3 diffs in the interfaces block: branches [main]->[master]; notify jq --arg repo "astridr"->"codepulse"; and confirm NO GITLEAKS_LICENSE is present (personal repo). Preserve everything else verbatim — especially fetch-depth: 0, gitleaks/gitleaks-action@v3 + continue-on-error: true, the entire 3-state classify step (success->clean; SARIF findings>0->secret_found; else->scan_error), the notify step POSTing to ${CODEPULSE_INGEST_URL}/runtime-ingest, the enforce step (secret_found->exit 1; scan_error->::warning+exit 0; clean->exit 0), permissions {contents: read, pull-requests: read}, and actions/checkout@v6. Create `.gitleaks.toml` per D-03/PATTERNS: title = "codepulse gitleaks config"; [extend] useDefault = true; and an [allowlist] with paths = .env\.example, convex/__tests__/, \.planning/phases/.*/.*-RESEARCH\.md. Start regexes EMPTY — do NOT carry over any of Astridr's 10 test-fixture regexes (they are Astridr-specific). The .env.example path allowlist covers the pk_test_ Clerk placeholder; only add regexes if the Task 2 baseline surfaces a false positive not covered by a path.
  </action>
  <verify>
    <automated>python -c "import yaml; d=yaml.safe_load(open('.github/workflows/gitleaks-scan.yml')); on=d.get(True) or d.get('on'); assert 'master' in str(on)"</automated>
  </verify>
  <acceptance_criteria>
    - `.github/workflows/gitleaks-scan.yml` exists, is valid YAML, and triggers on push/pull_request to `master`
    - The workflow contains `gitleaks/gitleaks-action@v3`, `fetch-depth: 0`, `continue-on-error: true`, the 3-state classify step (`result=secret_found` / `scan_error` / `clean`), and the enforce step with `exit 1` only on `secret_found`
    - The notify payload contains `--arg repo "codepulse"` (not `astridr`) and POSTs to `https://tidy-whale-981.convex.site/runtime-ingest`
    - The workflow contains NO `GITLEAKS_LICENSE` reference
    - `.gitleaks.toml` exists, contains `useDefault = true`, allowlists path `.env\.example`, and has an empty `regexes` list (no Astridr fixture regexes)
  </acceptance_criteria>
  <done>Workflow + config authored with exactly the 3 diffs; classify/enforce logic preserved verbatim; toml allowlist starts empty-regex with the .env.example/test/research paths.</done>
</task>

<task type="auto">
  <name>Task 2: Baseline-scan full git history clean before enforcement</name>
  <files>.gitleaks.toml</files>
  <read_first>
    - .gitleaks.toml (created in Task 1 — the config the baseline scan honors)
    - 77-RESEARCH.md "Pattern 4: Baseline scan procedure" (lines 290-305) and "Code Examples — Gitleaks local baseline scan" (lines 454-468)
    - 77-CONTEXT.md D-05 (baseline-first ordering — this is a [BLOCKING]-style gate before declaring green)
    - 77-VALIDATION.md "Manual-Only Verifications" row "Gitleaks baseline clean over full history"
  </read_first>
  <action>
    Per D-05, run gitleaks over the full git history locally BEFORE the enforcing workflow is declared green. If the `gitleaks` binary is available, run `gitleaks git -v --report-path=gitleaks-baseline.json .` from the repo root (honors `.gitleaks.toml`). If the binary is NOT installed (Windows — may be absent; see RESEARCH Environment Availability), fall back to the report-only CI path: confirm Task 1's workflow is mergeable as the baseline observation (its enforce step already exits 0 on clean) and note that the first `master` run serves as the baseline. Review the report: the `.env.example` `pk_test_your-clerk-key-here` Clerk placeholder is the expected/only likely hit and is already covered by the `.env\.example` path allowlist — confirm it does NOT appear as a finding. For any OTHER finding: if a false positive, add a tightly-scoped entry to `.gitleaks.toml` (prefer a `paths` entry over a broad `regexes` entry); if a REAL secret, STOP — rotate the credential first, then remediate history — do not allowlist a real secret. Delete `gitleaks-baseline.json` (do not commit the report). Only declare the baseline clean once the scan reports zero unallowlisted findings.
  </action>
  <verify>
    <automated>if (Test-Path gitleaks-baseline.json) { exit 1 } else { exit 0 }</automated>
  </verify>
  <acceptance_criteria>
    - A full-history gitleaks baseline was run (local binary) OR the report-only CI fallback path is explicitly documented in the SUMMARY with rationale (binary unavailable)
    - Baseline reports zero unallowlisted findings; the `.env.example` `pk_test_` placeholder is confirmed allowlisted (not a finding)
    - Any real secret found is rotated/remediated (NOT allowlisted); the SUMMARY records the disposition of every finding
    - No `gitleaks-baseline.json` report file is committed to the repo
  </acceptance_criteria>
  <done>Full-history baseline is clean (or all findings dispositioned correctly); the `.env.example` placeholder is confirmed allowlisted; no report artifact committed; the enforcing workflow can be declared green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| git history / source tree → public-ish remote | Committed secrets in any historical commit are an exposure surface; CI scanning is the detective+preventive control |
| CI runner → CodePulse dashboard | Workflow POSTs scan classification to runtime-ingest; the 3-state result must not misrepresent infra errors as leaks |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-77-05 | Information Disclosure | secrets in git history (full tree + history) | mitigate | gitleaks-action@v3 with `fetch-depth: 0` scans full history; `secret_found` -> `exit 1` blocks merge; baseline-first (D-05) confirms current history clean |
| T-77-06 | Spoofing | scan_error misread as a secret leak | mitigate | 3-state classify (SARIF jq finding count) distinguishes infra/permission errors (neutral `::warning`, exit 0) from real findings (fail, exit 1) — preserved verbatim from the proven Astridr template |
| T-77-SC | Tampering | gitleaks-action@v3 supply chain | accept | Official `gitleaks/gitleaks-action` org action (RESEARCH Package Legitimacy Audit: Approved); no npm `gitleaks` slop package installed; GitHub Action pinned to a major tag per Astridr precedent |
| T-77-07 | Information Disclosure | `.gitleaks.toml` over-broad allowlist hiding a real secret | mitigate | Start with EMPTY regexes; allowlist only `.env.example`/test/research paths; baseline (Task 2) forbids allowlisting a real secret — rotate instead |
</threat_model>

<verification>
- `.github/workflows/gitleaks-scan.yml` is valid YAML, triggers on `master`, uses `fetch-depth: 0` + `gitleaks-action@v3`, has the 3-state classify+enforce, notifies with `repo: codepulse`, no `GITLEAKS_LICENSE`
- `.gitleaks.toml` has `useDefault = true`, `.env\.example`/test/research path allowlist, empty regexes
- Full-history baseline scan reports zero unallowlisted findings; no report artifact committed
- (CI-observable, post-merge) the workflow goes green on a clean `master`
</verification>

<success_criteria>
- Gitleaks workflow + config authored as the Astridr mirror with exactly the 3 CodePulse diffs
- 3-state classify/enforce preserved so `scan_error` never reads as a leak
- Baseline confirms history is clean (placeholders allowlisted, real secrets rotated) before enforcement is declared green
</success_criteria>

<output>
Create `.planning/phases/77-ci-production-hardening/77-02-SUMMARY.md` when done
</output>
