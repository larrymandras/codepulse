---
phase: 03-design-token-refresh
plan: 03
type: execute
wave: 3
depends_on:
  - 03-02
files_modified: []
autonomous: false
requirements:
  - DT-05
  - DT-06

must_haves:
  truths:
    - "All existing dashboard pages render without visual regressions"
    - "Dark mode background shows subtle blue tint"
    - "HeroStatsBar tiles show radial gradient accents"
    - "Interactive cards lift on hover"
    - "Reduced-motion disables all new transitions"
  artifacts: []
  key_links: []
---

<objective>
Verify all existing dashboard pages render correctly after the token migration, confirm accent gradients are visible, and validate reduced-motion behavior.

Purpose: DT-05 requires all 15 existing dashboard pages render without visual regressions. This checkpoint ensures no token swap broke any page.
Output: Visual confirmation that the design token refresh is complete and regression-free.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-design-token-refresh/03-CONTEXT.md
@.planning/phases/03-design-token-refresh/03-01-SUMMARY.md
@.planning/phases/03-design-token-refresh/03-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Automated build and type verification</name>
  <files></files>
  <read_first>src/index.css, src/components/MetricCard.tsx, src/components/GlassPanel.tsx, src/components/HeroStatsBar.tsx</read_first>
  <action>
Run the full verification suite to confirm no regressions:

1. `npx tsc --noEmit` — must pass with 0 errors
2. `npm run build` — must succeed (Vite production build)
3. `npm test` — all existing Vitest tests must pass

After all three pass, verify the token and class presence with grep checks:

4. Confirm accent tokens exist in both blocks:
   - `grep -c "accent-cost" src/index.css` should return 2 (one :root, one .dark)
   - `grep -c "accent-health" src/index.css` should return 2
   - `grep -c "accent-activity" src/index.css` should return 2
   - `grep -c "accent-memory" src/index.css` should return 2
   - `grep -c "accent-alerts" src/index.css` should return 2

5. Confirm lift-on-hover exists:
   - `grep -c "lift-on-hover" src/index.css` should return >= 3

6. Confirm no hardcoded hex in HeroStatsBar:
   - `grep -c "#60a5fa" src/components/HeroStatsBar.tsx` should return 0
   - `grep -c "#f87171" src/components/HeroStatsBar.tsx` should return 0

7. Confirm data-accent is used in all three component files:
   - `grep -c "data-accent" src/components/MetricCard.tsx` should return >= 1
   - `grep -c "data-accent" src/components/GlassPanel.tsx` should return >= 1
   - `grep -c "data-accent" src/components/HeroStatsBar.tsx` should return >= 1

8. Confirm dark background is whisper tint:
   - `grep "background.*oklch(0.160 0.012 260)" src/index.css` should match
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | Select-String -Pattern "error TS" | Measure-Object | Select-Object -ExpandProperty Count</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` produces 0 errors
    - `npm run build` exits with code 0
    - `npm test` all tests pass
    - All grep checks above return expected values
  </acceptance_criteria>
  <done>Build, type check, and tests all pass. All token and component changes verified via grep.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Design token refresh: dark theme now uses a subtle blue-tinted OKLCH palette instead of pure monochromatic gray. Five category accent hues (cost/amber, health/green, activity/blue, memory/violet, alerts/red) are defined and applied as radial gradient backgrounds on HeroStatsBar tiles. Interactive cards have a lift-on-hover effect (translateY(-2px) on hover).
  </what-built>
  <how-to-verify>
    1. Run `npm run dev` if not already running
    2. Open http://localhost:5173 in browser (dark mode should be active)

    **Background tint check (DT-01):**
    3. The dashboard background should appear slightly warmer/blue-tinted compared to pure black-gray — subtle, barely perceptible shift

    **Accent gradient check (DT-03):**
    4. Look at the HeroStatsBar KPI tiles at the top of the Dashboard
    5. Each tile should have a very subtle colored glow from the left edge:
       - Sessions tile: blue tint
       - Error Rate / Alerts / Security: red tint
       - Memory Hit Rate / Durable Facts: violet tint
       - Advisor Savings: amber tint
       - Startup Time: green tint

    **Lift-on-hover check (DT-04):**
    6. Hover over any HeroStatsBar KPI tile — it should smoothly lift up ~2px

    **Regression check (DT-05):**
    7. Navigate through these pages and confirm they render correctly (no broken layouts, missing text, or wrong colors):
       - Dashboard (/)
       - Agents (/agents)
       - Analytics (/analytics)
       - Alerts (/alerts)
       - Memory (/memory)
       - Operations (/operations)
       - Security (/security)
       - Infrastructure (/infrastructure)
       - Settings (/settings)
       - Tasks (/tasks)
       - Inbox (/inbox)
       - Briefings (/briefings)
       - Chat (/chat)
       - Sessions (/sessions)
       - Capabilities (/capabilities)

    **Reduced-motion check (DT-06):**
    8. In Chrome DevTools: Rendering tab > check "Emulate CSS media feature prefers-reduced-motion" > set to "reduce"
    9. Hover over a KPI tile — it should NOT lift (no translateY animation)
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or describe any visual issues you see</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | Verification-only plan, no new code |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| N/A | N/A | N/A | N/A | No new attack surface — verification only |
</threat_model>

<verification>
- TypeScript compilation: 0 errors
- Vite build: exits 0
- Vitest: all tests pass
- Human visual verification: all 15 pages render correctly
- Human verification: accent gradients visible on HeroStatsBar tiles
- Human verification: lift-on-hover works
- Human verification: reduced-motion disables new transitions
</verification>

<success_criteria>
All 15 existing dashboard pages render without visual regressions. Accent gradients visible. Lift-on-hover works. Reduced-motion properly disables new effects. Phase 03 Design Token Refresh is complete.
</success_criteria>

<output>
After completion, create `.planning/phases/03-design-token-refresh/03-03-SUMMARY.md`
</output>
