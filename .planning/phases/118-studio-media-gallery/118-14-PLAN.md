---
phase: 118-studio-media-gallery
plan: 14
type: execute
wave: 9
depends_on: ["118-02", "118-13"]
files_modified:
  - hooks/studioThirdLeg.mjs
  - hooks/__tests__/studioThirdLeg.test.mjs
  - .planning/phases/118-studio-media-gallery/118-D09-EVIDENCE.md
autonomous: false
requirements: [D-09, D-11, D-12]

must_haves:
  truths:
    - "D-09 closes: three backends are each proven end-to-end, and the third is a genuinely different code shape from the first two rather than a near-duplicate that satisfies the letter of the decision while defeating its intent."
    - "The third leg's mechanism was selected from plan 118-02's post-authentication measurement before implementation began, never discovered during it."
    - "D-12 holds for the third card: it exists only for the model actually run and references environment-variable NAMES only."
  artifacts:
    - path: ".planning/phases/118-studio-media-gallery/118-D09-EVIDENCE.md"
      provides: "All three per-backend end-to-end proofs with their control pairs"
      contains: "LEG: third"
  key_links:
    - from: ".planning/phases/118-studio-media-gallery/118-OPENART-EVIDENCE.md"
      to: "this plan's implemented branch"
      via: "THIRD_LEG value selects which of three implementation shapes runs"
      pattern: "THIRD_LEG"
---

<objective>
Close D-09 by proving its third backend end-to-end, using whichever mechanism plan `118-02`'s
measurement selected.

Purpose: D-09's intent — three genuinely different code shapes proven end-to-end — is binding and
unchanged. Only the third mechanism was open, and it was resolved in wave 1 precisely so this plan
does not have to discover it.

Output: the third leg implemented and proven, the third recipe card, and D-09 closed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/118-studio-media-gallery/118-OPENART-EVIDENCE.md
@.planning/phases/118-studio-media-gallery/118-CONTEXT.md
@.planning/phases/118-studio-media-gallery/118-RESEARCH.md
@.planning/phases/118-studio-media-gallery/118-13-SUMMARY.md
@docs/studio-sidecar-contract.md

**Treat this plan's prose as a DRAFT, never a spec.** The branch is whatever `118-02` measured.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Read the branch and fix the implementation shape before writing any code</name>
  <files>.planning/phases/118-studio-media-gallery/118-D09-EVIDENCE.md</files>
  <read_first>
    - `.planning/phases/118-studio-media-gallery/118-OPENART-EVIDENCE.md` — the `THIRD_LEG:` line and
      the post-auth tool enumeration behind it.
    - `.planning/phases/118-studio-media-gallery/118-CONTEXT.md` D-09 and both of its AMENDMENT
      blocks — the binding intent (three genuinely different code shapes) and the three authorised
      branches.
    - `.planning/phases/118-studio-media-gallery/118-13-SUMMARY.md` — the shape-difference statement
      for leg 2, so leg 3's difference is measured against BOTH existing legs rather than only
      against the CLI one.
    - `.planning/phases/118-studio-media-gallery/118-RESEARCH.md` § "OpenArt workability concern" —
      the three planning options and the note that an attended-MCP-session-plus-hand-placed-sidecar
      shape is a legitimate recipe-card shape, just a different one than Higgsfield's.
  </read_first>
  <action>
Open `118-D09-EVIDENCE.md` and append a `## LEG: third — selected shape` section recording, BEFORE
any implementation begins:

1. The `THIRD_LEG:` value read verbatim from `118-OPENART-EVIDENCE.md`.
2. Which of the three implementation shapes that selects:
   - `openart-mcp` → an MCP-driven generation path. The differentiating shape is tool invocation
     through an MCP connector rather than a CLI or a raw HTTP queue — arguably the strongest third
     shape for proving the sidecar contract is not CLI-shaped.
   - `openart-mcp-interactive` → an attended MCP session producing the asset, with the file and a
     hand-authored sidecar placed into `media-vault\gen\`. The differentiating shape is that
     **nothing automated writes the sidecar at all** — which is a genuinely distinct and valuable
     test of the contract, since it is the same shape a human or a future Astridr integration would
     use. The recipe card honestly documents this workflow rather than a CLI invocation.
   - `second-direct-api` → the provider named in `118-02`'s amendment. Its differentiating shape
     must be stated concretely against BOTH existing legs.
3. **A concrete shape-difference argument against both existing legs**, written before coding. If
   the selected branch would produce a near-duplicate of the fal.ai client — same auth scheme, same
   queue-and-poll contract, same response handling — **that is a finding to surface now, not a detail
   to discover at review**. Say so plainly and propose the smallest change that restores a genuine
   third shape (a different provider, or a synchronous rather than queued API). Satisfying the letter
   of D-09 with three copies of one client would defeat its stated intent.
4. The environment-variable NAME the leg needs, if any. Never a value.

Do not write implementation code in this task. Its entire purpose is that the branch is a read
decision rather than a discovery.
  </action>
  <verify>
    <automated>node --input-type=module -e "import fs from 'node:fs'; const e=fs.readFileSync('.planning/phases/118-studio-media-gallery/118-D09-EVIDENCE.md','utf8'); const o=fs.readFileSync('.planning/phases/118-studio-media-gallery/118-OPENART-EVIDENCE.md','utf8'); const m=o.match(/THIRD_LEG:\s*(\S+)/); if(!m) throw new Error('no THIRD_LEG in openart evidence'); if(!e.includes('LEG: third')) throw new Error('selected-shape section missing'); if(!e.includes(m[1])) throw new Error('D09 evidence names a different branch than the openart measurement: '+m[1]); console.log('third leg branch carried forward consistently: '+m[1]);"</automated>
  </verify>
  <acceptance_criteria>
    - `118-D09-EVIDENCE.md` contains a `## LEG: third — selected shape` section naming the SAME
      branch string as `118-OPENART-EVIDENCE.md` — the automated check fails if they disagree.
    - The section contains a shape-difference argument measured against BOTH leg 1 and leg 2, not
      just leg 1.
    - If the branch would produce a near-duplicate of leg 2, that is stated explicitly with a
      proposed smallest change — the plan does not proceed to Task 2 pretending otherwise.
    - Any environment variable is named, never valued.
    - No implementation file was created in this task.
  </acceptance_criteria>
  <done>The third leg's mechanism and its shape difference are written down before a line of code, exactly as the D-09 amendment requires.</done>
</task>

<task type="auto">
  <name>Task 2: Implement the selected third leg</name>
  <files>hooks/studioThirdLeg.mjs, hooks/__tests__/studioThirdLeg.test.mjs</files>
  <read_first>
    - `.planning/phases/118-studio-media-gallery/118-D09-EVIDENCE.md` § "LEG: third — selected shape"
      from Task 1 — the branch and its shape argument.
    - `hooks/studioFal.mjs` and `hooks/__tests__/studioFal.test.mjs` from plan `118-13` — the
      conventions to follow, and the client this leg must NOT duplicate.
    - `~/.claude/skills/studio-generate/SKILL.md` — the wrapper both existing legs slot into.
    - `docs/studio-sidecar-contract.md` — the sidecar fields this leg must populate.
  </read_first>
  <action>
Implement per the branch recorded in Task 1. In all branches, extend
`~/.claude/skills/studio-generate/SKILL.md` with the third leg as a sibling and **touch none of the
eight existing media skills**.

**Branch `second-direct-api`:** create `hooks/studioThirdLeg.mjs` following `hooks/studioFal.mjs`'s
house conventions (no shebang, `node:` builtins only, injected `deps`, `AbortController` timeouts,
bounded retries that never cover a 401, no default for the API key, streamed download with a
resolved-path-inside-vault guard) but implementing the DIFFERENT contract the selected provider
actually has. Verify that provider's endpoints, auth header shape and response fields against its own
current documentation — prefer Context7 MCP — and never hand-construct a model identifier; read it
off the provider's own listing. Tests mirror `studioFal.test.mjs`'s control pairs: bounded
polling or its synchronous equivalent, retry policy with a 401 retried zero times, auth header on
every request, unset-key control asserting `fetchImpl` call count 0, secret hygiene against a
captured log buffer, and path-traversal refusal with zero filesystem writes.

**Branch `openart-mcp`:** create `hooks/studioThirdLeg.mjs` as the sidecar-writing and
file-placement half only — the MCP tool invocation itself happens in-session and is not something a
standalone `.mjs` can drive. The module takes the asset URL or bytes plus the provenance the MCP tool
reported, downloads or writes the file into `media-vault\gen\`, and writes the sidecar per the
contract. Tests cover the same path-safety, streaming and sidecar-shape rules, plus one asserting the
sidecar written is byte-identical in structure to what the fal.ai leg produces — that identity is
the actual evidence that the contract is not generator-specific.

**Branch `openart-mcp-interactive`:** no automated generation module is written; the leg is an
attended session plus hand placement. Create `hooks/studioThirdLeg.mjs` anyway as a small helper that
writes a correctly-shaped sidecar for a manually placed file, so the hand-authored path cannot drift
from the contract by hand-editing JSON. Test that it produces the contract-conformant sidecar and
refuses paths outside the vault. **Record honestly in the SUMMARY that this leg is attended rather
than automated** — that is a legitimate and documented shape, but it must not be described as an
automated integration.

In every branch, the recipe card written in Task 3 must honestly describe the workflow that was
actually used.
  </action>
  <verify>
    <automated>npx vitest run hooks/__tests__/studioThirdLeg.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run hooks/__tests__/studioThirdLeg.test.mjs` passes.
    - `hooks/studioThirdLeg.mjs` has no shebang and imports only `node:` specifiers.
    - No `not implemented` throw and no `TODO` in any exported function body, with the same `veo.ts`
      control used in plan `118-13` confirming the pattern finds such markers when they exist.
    - Every filesystem write resolves inside `media-vault\gen\`, with a refusal test asserting zero
      writes.
    - No credential value appears in the module, its tests, or the skill.
    - `git status --porcelain` shows zero modifications under all eight existing media skill
      directories; list them.
    - For the `second-direct-api` branch: the provider's contract was read from its own current
      documentation — quote the source — and the module is NOT a near-duplicate of
      `hooks/studioFal.mjs`. State concretely what differs.
    - For the `openart-mcp-interactive` branch: the SUMMARY states plainly that this leg is attended
      rather than automated.
  </acceptance_criteria>
  <done>The third leg is implemented in the shape the wave-1 measurement selected, with the same safety rules as leg 2 and an explicit statement of how it differs.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Prove the third leg end-to-end, author its card, and close D-09</name>
  <what-built>
    The third generator leg, in the shape selected by plan `118-02`'s post-authentication
    measurement, wired into the same `/studio-generate` wrapper as the Higgsfield and fal.ai legs.
    This checkpoint runs the third and final real generation.

    Depending on the branch it may spend credits on an external account, or may require you to drive
    an MCP session yourself. Claude will say which before doing anything.
  </what-built>
  <how-to-verify>
    1. Claude will state the branch, the model, and what it needs from you — either an approval to
       spend, or a short attended MCP session.
    2. Approve, or reply `skip`.
    3. After generation, Claude runs `/studio-sync` and shows you `/studio`.
    4. Confirm the asset appears with its complete recipe, in the same grid view as the
       sidecar-less control file.

    Replying `skip` leaves D-09 at two of three legs. Claude will record that as an OPEN decision
    rather than closing the phase as if the third leg passed — a skipped leg is a known gap, not a
    pass.
  </how-to-verify>
  <read_first>
    - `.planning/phases/118-studio-media-gallery/118-D09-EVIDENCE.md` — the `## LEG: higgsfield` and
      `## LEG: fal` sections; this task appends `## LEG: third` in the same format.
    - `.planning/phases/118-studio-media-gallery/118-13-SUMMARY.md` — the `npx convex run` invocation
      used for the previous cards and the prohibition on `--push` and `--prod`.
    - `.planning/phases/118-studio-media-gallery/118-VALIDATION.md` § "Control-Pair Requirement".
  </read_first>
  <files>.planning/phases/118-studio-media-gallery/118-D09-EVIDENCE.md</files>
  <action>
State the branch, the model id (read off the provider's own listing, never hand-constructed) and
exactly what is needed from Larry — an approval to spend, or a short attended MCP session — before
asking. On "approved", run the leg per the branch recorded in Task 1, then `/studio-sync`, then
show `/studio`.

On "skip", record D-09 as `OPEN — 2 of 3 legs proven` naming the missing leg, and write no third
recipe card. Do NOT round that up to CLOSED because everything else passed: the phase's verification
reads this section, and a wrong green in a durable artifact is what the next session treats as ground
truth.

Then write the `## LEG: third` and `## D-09 status` sections of `118-D09-EVIDENCE.md` per the
acceptance criteria, and seed the third recipe card with
`npx convex run internal.media.upsertModelCard '<json>' --env-file C:\Users\mandr\convex-selfhost\selfhosted.envfile`,
never with `--push` or `--prod`. On the interactive branch, the card honestly documents an attended
session with a hand-placed file — a legitimate recipe-card shape, not a defect to disguise.
  </action>
  <verify>
    <automated>node --input-type=module -e "import fs from 'node:fs'; const s=fs.readFileSync('.planning/phases/118-studio-media-gallery/118-D09-EVIDENCE.md','utf8'); const m=s.match(/## D-09 status[\s\S]{0,200}/); if(!m) throw new Error('D-09 status section missing'); if(/CLOSED/.test(m[0]) && !/## LEG: third/.test(s)) throw new Error('D-09 marked CLOSED without a third leg proof section'); console.log('D-09 status recorded consistently with the leg sections present');"</automated>
  </verify>
  <acceptance_criteria>
    After the checkpoint returns, Claude must record in `118-D09-EVIDENCE.md` under `## LEG: third`:
    - The branch, the exact invocation or attended procedure, and the model id (read off the
      provider's own listing, never hand-constructed).
    - The generated file's path and its sidecar's contents, with no credential values.
    - **The control pair in one grid view:** the third asset rendering a complete recipe alongside
      the sidecar-less file rendering `No provenance recorded`.
    - The shape-difference statement measured against BOTH previous legs.
    - Elapsed ingest time and whether the 5-minute task or `/studio-sync` produced the row.
    - The thumbnail's byte size, at or under 200 KB.
    - A third `mediaModels` recipe card for the model actually run, seeded with
      `npx convex run internal.media.upsertModelCard '<json>' --env-file C:\Users\mandr\convex-selfhost\selfhosted.envfile`
      and **never** with `--push` or `--prod`. The card honestly describes the workflow that was
      actually used — including, on the interactive branch, that it is an attended session with a
      hand-placed file, which is a legitimate recipe-card shape and not a defect.
    - A closing `## D-09 status` section: `CLOSED` only when all three legs have a proof section with
      a control pair. If any leg was skipped, write `OPEN — <n> of 3 legs proven` and name the
      missing one. **Do not write CLOSED against an unproven leg**; the phase's verification reads
      this section, and a wrong green in a durable artifact is what the next session treats as
      ground truth.
    - The full `mediaModels` row list re-checked: one card per proven leg, zero cards for unrun
      models.
  </acceptance_criteria>
  <done>D-09 is closed with three control-paired end-to-end proofs, or is recorded as OPEN naming exactly which leg is missing — and the mediaModels table holds one card per proven leg and none for any unrun model.</done>
  <resume-signal>Reply "approved" or "skip"</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| third-party generator (MCP connector or a second direct API) → host filesystem | Remote bytes and remote-reported provenance are written into the vault. |
| external credential → process env, logs, evidence artifacts | Branch-dependent, but present in the `second-direct-api` branch. |
| `npx convex run` → live deployment | An attended CLI invocation against production. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-118-04 | Information Disclosure | the leg's API key in logs, the skill, the card or the evidence | mitigate | No default for the key, never printed (asserted against a captured log buffer), card names the variable by NAME only and is quoted and confirmed value-free before close. |
| T-118-06 | Tampering | remote-supplied filename or URL writing outside the vault | mitigate | Every write resolves inside `media-vault\gen\`, with a refusal test asserting zero filesystem writes. |
| T-118-42 | Spoofing | retrying a 401 into a rate-limit ban | mitigate | Retry policy excludes 401 and 4xx validation errors, asserted in one test checking both call counts (`second-direct-api` branch). |
| T-118-43 | Tampering | shipping a stub | mitigate | No `not implemented` throw or `TODO` in any exported body, with the `veo.ts` control confirming the pattern discriminates. |
| T-118-44 | Spoofing | a near-duplicate third client satisfying D-09's letter while defeating its intent | mitigate | Task 1 requires the shape-difference argument to be written BEFORE implementation and measured against both existing legs, with an explicit instruction to surface a near-duplicate as a finding rather than discover it at review. |
| T-118-45 | Spoofing | writing `D-09 CLOSED` against a skipped leg | mitigate | The evidence file's `## D-09 status` section may read `CLOSED` only when all three legs carry a control-paired proof; a skip writes `OPEN — n of 3 legs proven` naming the missing leg. A wrong green in a durable artifact is what the next session reads as ground truth. |
| T-118-10 | Tampering | `npx convex run --push` deploying a shared checkout | mitigate | `--push` and `--prod` forbidden by name with the reason stated. |
| T-118-13 | Spoofing | a hand-constructed model identifier producing a confident false negative | mitigate | Model ids and endpoints are read off the provider's own listing and documentation, never guessed — a well-formed rejection from a real service reads exactly like a policy verdict. |
| T-118-SC | Tampering | npm/pip/cargo installs | accept | No packages installed; `node:` builtins only, enforced by an acceptance criterion. `118-RESEARCH.md`'s Package Legitimacy Audit lists `openart`, `openart-cli` and `@openart/cli` as REMOVED — non-existent on npm — and **no plan may install a package under any of those names.** |
</threat_model>

<verification>
1. Task 1's automated check confirms the branch carried forward matches `118-02`'s measurement.
2. `npx vitest run hooks/__tests__/studioThirdLeg.test.mjs` green.
3. `118-D09-EVIDENCE.md` contains all three `## LEG:` sections and a `## D-09 status` line whose
   value is justified by the sections present.
4. `git status --porcelain` shows zero changes under all eight existing media skill directories.
5. `npm test` full suite at or above baseline.
</verification>

<success_criteria>
- Three backends are each proven end-to-end with a control pair, or the shortfall is recorded as
  OPEN rather than papered over.
- The third leg's shape difference from both other legs is stated concretely and was decided before
  implementation.
- One recipe card per proven leg, no card for any unrun model, no credential values anywhere.
</success_criteria>

<output>
Create `.planning/phases/118-studio-media-gallery/118-14-SUMMARY.md` when done. It MUST quote: the
branch taken, the shape-difference statement against both prior legs, the `## D-09 status` line
verbatim, the full `mediaModels` row list, and before/after `npm test` counts.
</output>
