---
phase: 118-studio-media-gallery
plan: 03
type: execute
wave: 2
depends_on: ["118-01"]
files_modified:
  - convex/schema.ts
  - convex/retention.ts
  - convex/retention.test.ts
autonomous: true
requirements: [D-03, D-05, D-13]

must_haves:
  truths:
    - "The three curated tables `media`, `mediaStyles` and `mediaModels` exist in the deployed schema on the live self-hosted backend, not merely in the working tree."
    - "D-03 holds: none of the three new tables is a key in `RETENTION_DAYS`, the omission carries an explaining comment block in the Phase 116 D-13 style, and a test asserts the absence against a control key that IS present."
    - "D-05's content-hash row identity is expressible: `media` carries a `contentHash` field with a `by_contentHash` index, so dedup is an indexed lookup rather than a table scan."
    - "D-13 holds at the schema level: no migration, seed, or backfill of pre-existing media is introduced."
  artifacts:
    - path: "convex/schema.ts"
      provides: "media, mediaStyles, mediaModels table definitions with indexes"
      contains: "media: defineTable("
    - path: "convex/retention.ts"
      provides: "D-03 exemption comment block; RETENTION_DAYS unchanged"
      contains: "Phase 118 D-03"
    - path: "convex/retention.test.ts"
      provides: "Control-paired assertion that the three tables are absent from RETENTION_DAYS"
      contains: "mediaStyles"
  key_links:
    - from: "convex/schema.ts"
      to: "convex/retention.test.ts"
      via: "source-level schema parse — the test reads schema.ts to prove the tables exist AND are unbounded"
      pattern: "defineTable"
---

<objective>
Add the three Studio tables to `convex/schema.ts`, record D-03's retention exemption as an
explaining comment (adding no `RETENTION_DAYS` key), guard both with a control-paired test, and
deploy the schema to the live self-hosted backend.

Purpose: this is the foundation every later backend plan builds on. Deploying it here — early and
alone — means later plans' failures are about their own code, not about a schema that never landed.

Output: three deployed tables and a test that fails if anyone later "fixes" D-03 by adding a key.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/118-studio-media-gallery/118-CONTEXT.md
@.planning/phases/118-studio-media-gallery/118-PATTERNS.md
@.planning/phases/118-studio-media-gallery/118-D01-EVIDENCE.md
@CLAUDE.md

**Treat this plan's prose as a DRAFT, never a spec.** The `<interfaces>` field lists below are
transcribed from the design doc; if the existing schema's conventions contradict them, follow the
existing schema and note the correction in your SUMMARY.
</context>

<interfaces>
Design-doc §4.3 field lists (`docs/proposals/2026-08-07-seidr-suite-design.md:114-116`) — the
contract the tables must satisfy:

- `media`: `filename`, `absPath`, `mediaType` (image/video/audio), `kind` (gen/ref/style), `model`,
  `provider`, `prompt`, `styleId`, `project`, `params`, `starred`, `tags[]`, `sizeBytes`,
  `width`/`height`/`durationSec`, `thumbStorageId`, `deletedAt`, `createdAt`
- `mediaStyles`: `slug`, `name`, `description`, `prompt`, `previewMediaId`
- `mediaModels`: `slug`, `name`, `type`, `provider`, `recipeMd`, `docsUrl`,
  `aspect`/`resolution`/`duration`, `enabled`

Two fields this phase adds beyond the design doc's list, both load-bearing:
- `media.contentHash` — SHA-256 of the file bytes, D-05's row identity.
- `media.thumbRelPath` — the D-01 fallback branch's populated field (see `118-D01-EVIDENCE.md`).

House table-definition style to copy: `convex/schema.ts:2339-2355` (`pipelineRuns`) — a leading
doc-comment naming the phase and decision IDs, string-literal enums documented in an inline
comment, one index per query access pattern.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Define media, mediaStyles and mediaModels in convex/schema.ts</name>
  <files>convex/schema.ts</files>
  <read_first>
    - `convex/schema.ts:2330-2442` — the `pipelineRuns` and `workspaceSnapshots` definitions and the
      file's closing brace. Read the file's actual tail before appending; do not assume line numbers.
    - `.planning/phases/118-studio-media-gallery/118-PATTERNS.md` § "`convex/schema.ts` — `media`,
      `mediaStyles`, `mediaModels` tables" — the exact donor excerpt and the explicit instruction
      NOT to copy `workspaceSnapshots`'s version-pointer machinery.
    - `.planning/phases/118-studio-media-gallery/118-D01-EVIDENCE.md` § "Resolved transport branch"
      — states which of `thumbStorageId` / `thumbRelPath` is the populated field.
    - `.planning/phases/118-studio-media-gallery/118-UI-SPEC.md` § "Filter Contract" and § "Media
      Card Contract" — the fields the gallery actually filters and renders on (`mediaType`, `kind`,
      `model`, `project`, `starred`, `width`/`height` for the aspect-ratio placeholder).
  </read_first>
  <action>
Append three `defineTable` entries to the schema object in `convex/schema.ts`, immediately after
the `workspaceSnapshots`-family entries and before the object's closing brace. Each gets a leading
doc-comment naming `Phase 118 (Studio)` and the decision IDs it serves, matching the density of the
`pipelineRuns` and `workspaceSnapshots` comment blocks.

**`media`** — fields, with `v.optional(...)` on everything that a provenance-absent row (D-07) or a
non-image row legitimately lacks:
- required: `filename` (string), `absPath` (string), `mediaType` (string, inline comment
  `// "image" | "video" | "audio"`), `kind` (string, inline comment `// "gen" | "ref" | "style"`),
  `contentHash` (string — SHA-256 hex, D-05), `sizeBytes` (number), `starred` (boolean),
  `createdAt` (number)
- optional provenance (D-07 — these are the fields that render "No provenance recorded" when
  absent): `model`, `provider`, `prompt`, `project` (all string), `styleId`
  (`v.optional(v.id("mediaStyles"))`), `params` (`v.optional(v.string())` holding the sidecar's
  `params` object serialised as JSON — a `v.any()` here would defeat validation for no benefit)
- optional media facts: `tags` (`v.optional(v.array(v.string()))`), `width`, `height`,
  `durationSec` (all `v.optional(v.number())`)
- optional thumbnail transport (D-01 — declare BOTH, exactly one is populated per row, per the
  branch recorded in `118-D01-EVIDENCE.md`): `thumbStorageId`
  (`v.optional(v.id("_storage"))`), `thumbRelPath` (`v.optional(v.string())`). Add an inline
  comment naming which branch is live and stating that the other field is declared-but-unpopulated
  so the row shape survives a branch revisit.
- optional lifecycle: `deletedAt` (`v.optional(v.number())` — D-08's soft-delete flag)

Indexes on `media`, one per real access pattern, each with a one-line comment naming the caller:
- `by_contentHash` on `["contentHash"]` — D-05/D-06 dedup lookup from ingest
- `by_createdAt` on `["createdAt"]` — the Gallery grid's newest-first sort
- `by_deletedAt` on `["deletedAt"]` — the Trash tab and the D-08 janitor's 30-day sweep
- `by_kind` on `["kind"]` — the Kind filter Select

**`mediaStyles`**: `slug` (string), `name` (string), `description` (optional string), `prompt`
(optional string), `previewMediaId` (`v.optional(v.id("media"))`). Index `by_slug` on `["slug"]`.

**`mediaModels`**: `slug` (string), `name` (string), `type` (string, inline comment
`// "image" | "video" | "audio"`), `provider` (string), `recipeMd` (string), `docsUrl` (optional
string), `aspect`, `resolution` (optional strings), `duration` (optional number), `enabled`
(boolean). Index `by_slug` on `["slug"]`.

Add an inline comment on `mediaModels.recipeMd` restating D-12: cards exist only for models proven
end-to-end in this phase, and `recipeMd` references environment-variable NAMES only — Convex stores
no keys.

Introduce no migration, no seed data, and no backfill (D-13). The tables start empty.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0.
    - `grep -c "defineTable" convex/schema.ts` is exactly 3 higher than its value before this task
      (record both numbers in the SUMMARY).
    - Each of `media:`, `mediaStyles:`, `mediaModels:` appears exactly once as a
      `<name>: defineTable(` declaration at two-space indentation — the form
      `convex/retention.test.ts`'s existing source-level schema parser matches
      (`/^\s{2}([A-Za-z_][A-Za-z0-9_]*):\s*defineTable\(/gm`).
    - `media` declares all four indexes `by_contentHash`, `by_createdAt`, `by_deletedAt`, `by_kind`.
    - Both `thumbStorageId` and `thumbRelPath` are declared and both are `v.optional`.
    - `contentHash` is a REQUIRED (non-optional) `v.string()` — a row without an identity is not a
      valid row.
    - No `v.any()` appears in any of the three definitions.
    - No seed/migration/backfill code is added anywhere in the diff (D-13).
  </acceptance_criteria>
  <done>The three tables typecheck, follow the house comment/index conventions, and carry both D-01 transport fields plus D-05's content hash.</done>
</task>

<task type="auto">
  <name>Task 2: Record D-03's retention exemption and guard it with a control-paired test</name>
  <files>convex/retention.ts, convex/retention.test.ts</files>
  <read_first>
    - `convex/retention.ts:38-142` — `RETENTION_DAYS` in full and the Phase 116 D-13 exemption
      comment block at `:126-142`, which this comment must match in style verbatim (CONTEXT.md's
      discretion note says so explicitly).
    - `convex/retention.test.ts:1-60` — the file's source-level schema-parsing approach (it reads
      `convex/schema.ts` as text rather than importing the Convex runtime) and the existing
      assertions, so the new test joins the file's conventions instead of inventing a second style.
    - `.planning/phases/118-studio-media-gallery/118-PATTERNS.md` § "`convex/retention.ts` — D-03
      exemption comment" — the literal donor block.
  </read_first>
  <action>
In `convex/retention.ts`, add a comment block immediately AFTER the existing Phase 116 D-13 block,
in the same voice and at the same indentation. It must state:
- that `media`, `mediaStyles` and `mediaModels` are deliberately EXEMPT from `RETENTION_DAYS` and
  must not be added as keys here;
- why: they are curated libraries, the opposite of a firehose, and `pruneBatchV3` deletes by
  whole-table `_creationTime` cutoff, so a calendar rule would delete a starred image for the sole
  offence of being old;
- what bounds `media`'s growth instead: the Phase 118 D-08 janitor (30-day sweep over
  `deletedAt`), which is keyed by the operator's own delete action rather than by calendar age —
  the same "different mechanism, deliberately" structure the Phase 116 block uses for
  `promptVersions`;
- the trap, stated as the 116 block states it: adding these keys would be syntactically valid and
  would pass `retention.test.ts`'s table-existence check while being semantically wrong.

Change **no** entry in `RETENTION_DAYS` and add **no** entry to it.

In `convex/retention.test.ts`, add a `describe` block titled for Phase 118 D-03 containing two
assertions that together form a control pair:
1. **Experimental:** each of `media`, `mediaStyles`, `mediaModels` is present in the set of tables
   parsed out of `convex/schema.ts` AND absent from `Object.keys(RETENTION_DAYS)`. Asserting
   presence-in-schema first is what makes the absence meaningful — an absence assertion alone would
   also pass if the tables were never created.
2. **Control:** a table that IS retention-bounded (use `runtime_events`, read from
   `RETENTION_DAYS` at test time rather than hardcoded, so the control fails loudly if that key is
   ever renamed) is present in BOTH the schema set and `RETENTION_DAYS`. If this control assertion
   fails, the first assertion's method is broken and its pass is meaningless.
3. Additionally assert that `convex/retention.ts`'s source text contains the marker string
   `Phase 118 D-03`, so deleting the explaining comment fails the suite rather than silently
   removing the only record of why the omission is deliberate.
  </action>
  <verify>
    <automated>npx vitest run convex/retention.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run convex/retention.test.ts` passes with a HIGHER test count than before this
      task (record both counts in the SUMMARY).
    - `convex/retention.ts` contains the literal string `Phase 118 D-03`.
    - `git diff convex/retention.ts` shows additions inside the comment region ONLY — zero lines
      changed inside the `RETENTION_DAYS` object literal. Verify by confirming the diff contains no
      added or removed line matching `^[+-]\s+\w+:\s*\d+,` .
    - **Mutation proof (required, not optional):** temporarily add `media: 90,` to `RETENTION_DAYS`,
      re-run `npx vitest run convex/retention.test.ts`, confirm it FAILS, then revert. Quote the
      failing assertion's message in the SUMMARY. A test that stays green under this mutation is not
      guarding anything.
    - The control assertion reads its table name from `RETENTION_DAYS` rather than hardcoding it.
  </acceptance_criteria>
  <done>D-03 is both documented in the place someone would "fix" it and enforced by a test proven to fail when it is violated.</done>
</task>

<task type="auto">
  <name>Task 3: Deploy the schema to the live self-hosted backend and prove it landed</name>
  <files>convex/schema.ts</files>
  <read_first>
    - `CLAUDE.md` § "Commands" (the deploy paragraph) and § "Self-Hosted Convex — Operational
      Rules" — `--env-file` is not optional; a bare `npx convex deploy` can target the retired
      cloud deployment `tidy-whale-981`; `convex deploy` ships the WORKING TREE, not HEAD, and in a
      whole-directory deploy system deploying an earlier commit is a ROLLBACK, not a subset.
    - `.planning/phases/118-studio-media-gallery/118-01-SUMMARY.md` — confirms the backend was
      reachable and healthy during wave 1.
  </read_first>
  <action>
**Deploy is load-bearing and is NOT covered by build or typecheck.** This repo's Convex types come
from generated `api.d.ts`, so `npm run build` and `npx tsc --noEmit` pass happily against a backend
that never received the schema. That is a false-positive verification state; this task exists to
close it.

Order of operations, and the order matters:

1. **Assert the working tree before deploying.** Run `git status --porcelain` and read the output.
   This is a SHARED checkout — a concurrent session's uncommitted files will be shipped by a deploy
   that names no paths, because `convex deploy` ships the working tree. If any file outside this
   plan's `files_modified` list is dirty, STOP and report it rather than deploying. Record the full
   `git status --porcelain` output in the SUMMARY either way.

2. **Deploy with the exact mandated command**, no substitutions:
   `npx convex deploy --env-file C:\Users\mandr\convex-selfhost\selfhosted.envfile`
   If the CLI prompts for confirmation, add `-y` ALONGSIDE `--env-file`, never instead of it. Do not
   use `npm run deploy` (its script body is a bare `npx convex deploy`).

3. **Read the deploy output for a rollback signature.** A `Deleted table indexes:` line means an
   older tree was shipped and live indexes were destroyed. If that line appears, stop and report it
   immediately — do not treat it as noise.

4. **Prove the schema landed against the live backend, control-paired.** A public-function listing
   cannot answer this (it lists public functions only, and this task shipped no functions), so query
   the tables directly over the HTTP API at `127.0.0.1:3210`:
   - Experimental: a read against each of `media`, `mediaStyles`, `mediaModels` must succeed and
     return an empty result — the tables exist and are empty (D-13).
   - Control: the same read shape against a deliberately bogus table name (e.g.
     `mediaDefinitelyNotATable9x7q2`) must FAIL. If both return the same thing, the probe carries no
     information and the deploy is unverified.
   Since no `media.ts` query functions exist yet at this wave, use the Convex CLI's own data
   surface for this (`npx convex data <table> --env-file ...` or the equivalent available in the
   installed CLI version). **Before writing the command into the summary, confirm the subcommand
   exists in the installed CLI version** rather than trial-and-erroring it — if it does not, record
   what you used instead and why.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - The deploy command actually run, quoted verbatim in the SUMMARY, contains the substring
      `--env-file C:\Users\mandr\convex-selfhost\selfhosted.envfile`.
    - `git status --porcelain` output captured BEFORE the deploy is quoted in the SUMMARY, and every
      dirty path in it belongs to this plan.
    - The deploy output contains no `Deleted table indexes:` line. If it does, the plan is BLOCKED
      and reported, not continued.
    - A live read of `media` succeeds and returns zero rows; a live read of a bogus table name
      fails. Both results are quoted in the SUMMARY as the control pair.
    - `npx tsc --noEmit` exits 0 after the deploy regenerates `convex/_generated/`.
    - `npm test` full suite passes with a count no lower than the pre-plan baseline; quote both.
  </acceptance_criteria>
  <done>The three tables exist on the live self-hosted backend, proven by a probe that would have failed had they not.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| working tree → live self-hosted Convex deployment | `convex deploy` ships the whole working tree of a SHARED checkout, so another session's uncommitted code crosses this boundary with yours. |
| schema definition → every future writer | A `v.any()` or an over-permissive optional here removes validation for every mutation written later. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-118-10 | Tampering | live deployment, via a dirty or stale shared checkout | mitigate | Task 3 asserts `git status --porcelain` before deploying and refuses on any dirty path outside this plan; it reads the deploy output for the `Deleted table indexes:` rollback signature; and it uses the exact `--env-file` command so the retired cloud deployment `tidy-whale-981` cannot be targeted by accident. |
| T-118-05 | Tampering (stored XSS) | `mediaModels.recipeMd` | mitigate | Declared `v.string()`, never `v.any()`. The rendering-side control (plain `<pre>`/`font-mono`, never `dangerouslySetInnerHTML`) is enforced in plan `118-10`; this plan's contribution is that the field is a validated string with an inline comment naming the constraint. |
| T-118-04 | Information Disclosure | `mediaModels.recipeMd` holding a credential value | mitigate | Inline schema comment restates D-12: `recipeMd` references environment-variable NAMES only; Convex stores no keys. Enforced at authoring time in plans `118-11` / `118-12` / `118-13`. |
| T-118-08 | Denial of Service | unbounded growth of three retention-exempt tables | accept | Accepted deliberately per D-03: these are curated libraries, and `media`'s growth is bounded by the D-08 janitor (plan `118-05`) keyed on operator deletes rather than calendar age. The exemption is documented in `convex/retention.ts` and guarded by a mutation-proven test so it cannot be silently "fixed" into a data-loss bug. |
| T-118-SC | Tampering | npm/pip/cargo installs | accept | No packages installed. `118-RESEARCH.md` § "Package Legitimacy Audit" records zero new packages for this phase. |
</threat_model>

<verification>
1. `npx tsc --noEmit` clean.
2. `npx vitest run convex/retention.test.ts` green, with a proven mutation failure recorded.
3. The live control pair from Task 3 (real table reads empty; bogus table name errors).
4. `npm test` full suite green at or above the pre-plan baseline count.
</verification>

<success_criteria>
- Three tables exist on the LIVE backend, not just in the working tree.
- D-03's exemption is documented where someone would go to break it, and a mutation-proven test
  makes breaking it fail CI.
- D-05's identity field and both D-01 transport fields exist on `media`.
- Nothing was backfilled (D-13).
</success_criteria>

<output>
Create `.planning/phases/118-studio-media-gallery/118-03-SUMMARY.md` when done. It MUST quote: the
verbatim deploy command, the pre-deploy `git status --porcelain` output, the Task 3 control pair,
the retention-test mutation-failure message, and before/after `npm test` counts.
</output>
