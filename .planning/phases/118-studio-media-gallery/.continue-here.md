# Continue Here — Phase 118 (studio-media-gallery)

**PHASE CLOSED 2026-08-17.** This file previously said "ONE THING REMAINS: the phase verifier"
and instructed the next session to ask Larry for approval before spawning it. That was done —
he approved, the verifier ran, and it passed. Rewritten so nothing here reads as still-pending.

## Status: DONE

- **15 of 15 plans** complete, each with a plan-level `SUMMARY.md`.
- **`118-VERIFICATION.md` — `status: passed`, 16/16 D-01..D-16, no gaps.**
- ROADMAP box ticked, Progress-table row Complete, `completed_phases` **12**, `percent` **100**.
- **v14.0 is 12 of 12 phases complete and all 12 verified.**

**The milestone close is NOT done and is Larry's.** It is performed **BY HAND** — never
`gsd-sdk milestone.complete`. That is the only remaining v14.0 action.

## Deployed 2026-08-17 — nothing from this phase is left undeployed

The `detectCredentialValue` widening (commit `54341f80`) was **deployed** to the self-hosted
backend on Larry's explicit instruction, with the documented invocation:

```
npx convex deploy --env-file C:/Users/mandr/convex-selfhost/selfhosted.envfile -y
```

Preflight and result, measured:

- `git status --porcelain` was **empty**, so the pushed tree was exactly HEAD `2db553ed` —
  nothing uncommitted, and no risk of shipping the concurrent session's work-in-progress.
- Target resolved to **`http://127.0.0.1:3210`**, i.e. the self-hosted backend, NOT the retired
  cloud deployment `tidy-whale-981`. `--env-file` is what makes that true and is not optional.
- **`✔ No indexes are deleted by this push`** — the line that would have signalled a schema
  rollback. Schema validation completed.
- Read-only `media:listModels` afterwards: **3 recipe cards intact**, data undisturbed.
- **Regression check against REAL data, not fixtures:** all 3 live `recipeMd` values were run
  through the new predicate and all 3 are still **ACCEPTED**, including the fal card that
  references `FAL_KEY` by name in prose. A widened security predicate that refuses a legitimate
  existing card would have broken re-upsert of production data.

**One thing was NOT directly proven, stated plainly rather than glossed:** that the new
predicate is *executing* on the backend was not probed end-to-end. The only discriminating live
probe is an `upsertModelCard` call carrying a credential-shaped `recipeMd` — and
`upsertModelCard` is an `internalMutation` with **no delete path for `mediaModels`**, so if it
had failed to refuse it would have left a permanent junk row in a curated singleton table with
no way to remove it. That risk was declined. What is established is that the push shipped a
byte-clean tree containing the fix and completed a full upload/validate/deploy cycle; the final
link is sound inference, not a measurement.

The guard is a **backstop, not a boundary** (its own docstring says so), and no leaked
credential value was found anywhere — this was hardening, not an incident.

## Todos resolved 2026-08-17 (Larry's decisions)

| todo | outcome |
|---|---|
| `118-detectcredentialvalue-misses-fal-key` | **FIXED**, mutation-proven both directions → `todos/completed/` |
| `114-clerk-bounce-tracking-and-dev-keys` | **CLOSED accepted-by-design** — CodePulse stays tailnet-local. Re-measure if a production Clerk instance is ever wanted; the closure does not survive that premise |
| `llm-analytics-rollup-migration-cr01` | **PARKED**, `last_reviewed` set. Neither of its own triggers has fired; it is one-phase-sized, so it is next-milestone roadmap material |

## Carry these forward — measured, do not re-derive

- **`gsd-sdk query phase.complete` cannot be trusted, measured again 2026-08-17.** It returned a
  clean 12-field payload (`state_updated: true`, `warnings: []`) while writing
  `completed_plans` 86 → **88** (the naive `*SUMMARY.md` trap value), leaving `completed_phases`
  at 11 and `percent` at 92 — i.e. failing to do the one thing it was called for — flipping
  `status` to `ready_to_plan` pointing at **Phase 119, which is already COMPLETE**, and wiping
  the whole `stopped_at`/`last_activity` narrative. Its **ROADMAP.md half was correct** (ticked
  the box, fixed the Plans line, ticked six stale plan rows) but it **missed the ROADMAP
  Progress table entirely**, leaving row 118 at `13/15 | In progress`. Judge it by `git diff`,
  never by its payload. Recovery: `git checkout HEAD -- .planning/STATE.md`, then re-apply your
  own edits as asserted literal replaces.
- **`gsd-state-coherence.ps1` is what caught the Progress-table miss.** It derives its Complete
  count from that table, not the checkbox. Run it after every `state.*`/`phase.complete` call.
- **STATE.md stores several facts TWICE** (frontmatter + body prose: `stopped_at`/`Stopped at:`,
  `last_updated`/`Last session:`, `last_activity`/`Last activity:`). Write both from ONE value
  or the next verb re-derives frontmatter from the body and silently reverts you. There are
  **two** `Last activity:` lines — the second is a stale orphan in the archived v12.0 section
  from an older clobber; only the first is live.
- **Counters:** strict `*-*-PLAN.md` → **86**, strict `*-*-SUMMARY.md` → **86**. Naive
  `*SUMMARY.md` → 88 because 117 and 119 carry PHASE-level summaries. **Do not "fix" 86 to 88.**
- **Windows shell traps, both re-hit 2026-08-17.** The Bash tool eats backslashes in
  `-File C:\path\...` (run `.ps1` via the PowerShell tool instead), and PS 5.1 `Get-Content`
  mojibakes BOM-less UTF-8 on a read/write round-trip — use
  `[IO.File]::ReadAllLines/WriteAllLines` with explicit UTF-8 for any STATE.md edit.
- **`workflow.adversarial_verification` is `false`** in `.planning/config.json` (Larry's call;
  `config-set` rejects the key, so it was written directly).
- **Run MCP-dependent work INLINE, never via `gsd-executor`** — that agent type has no MCP access.
- A **concurrent session** has been active in this checkout. Read `git status --porcelain`
  before any deploy, and verify every commit afterwards with `git show --stat HEAD`.

## Housekeeping

**155 commits unpushed, on a PUBLIC repo.** Larry declined to push on 2026-08-16 and has not
revisited it. Raise it before pushing; never push as a side effect. Some belong to the
concurrent session.
