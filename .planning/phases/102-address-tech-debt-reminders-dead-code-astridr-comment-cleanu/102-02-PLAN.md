---
phase: 102-address-tech-debt-reminders-dead-code-astridr-comment-cleanu
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - "astridr-repo: astridr/automation/calendar_cache.py"
  - "astridr-repo: astridr/engine/bootstrap/cron_dispatcher.py"
  - "astridr-repo: astridr/tools/reminders.py"
  - "astridr-repo: tests/unit/engine/bootstrap/test_cron_dispatcher.py"
autonomous: true
requirements: [AUDIT-TD-02]
# AUDIT-TD-02 = v12.0-MILESTONE-AUDIT.md §Tech Debt item 2 (CodePulsePoster dead class +
# stale two-backend narrative). Cross-repo plan: all edits are in astridr-repo
# (C:\Users\mandr\astridr-repo), committed there via `git -C` per the 101/98-05 precedent.
# No formal REQ IDs — scoped by CONTEXT.md decisions D-03, D-04, D-06.

must_haves:
  truths:
    - "D-03: astridr/automation/calendar_cache.py no longer defines CodePulsePoster and cron_dispatcher.py's _run_calendar_cache_refresh passes self._telemetry (the shared ConvexHandler) to refresh(), matching every other cron in that dispatcher"
    - "D-04: a repo-wide sweep of astridr-repo removes the stale two-backend / local-404 / tidy-whale narrative from all four comment sites in one pass (calendar_cache.py, cron_dispatcher.py, tools/reminders.py, test_cron_dispatcher.py); comments state the current truth — one self-hosted backend serving /calendar-ingest"
    - "D-06: static proof bar met on the astridr side — the astridr test suite is green and grep proves ZERO remaining references to CodePulsePoster and ZERO stale two-backend-narrative claims in astridr/ source + tests"
    - "The dead module constants _CP_URL/_LOCAL_URL/_CODEPULSE_URL/_INGEST_KEY orphaned in calendar_cache.py by the class deletion are removed; the SAME-NAMED but LIVE constants in tools/reminders.py and reminder_nudge.py are NOT touched"
    - "refresh() in calendar_cache.py keeps its duck-typed telemetry parameter — no signature change; existing fake-injecting tests keep passing"
  artifacts:
    - path: "astridr-repo: astridr/engine/bootstrap/cron_dispatcher.py"
      provides: "_run_calendar_cache_refresh passing self._telemetry through to refresh()"
      contains: "telemetry=self._telemetry"
    - path: "astridr-repo: tests/unit/engine/bootstrap/test_cron_dispatcher.py"
      provides: "calendar-cache dispatch test asserting shared-telemetry passthrough (no CodePulsePoster import)"
      contains: "kwargs[\"telemetry\"] is telemetry"
  key_links:
    - from: "astridr/engine/bootstrap/cron_dispatcher.py"
      to: "astridr/automation/calendar_cache.py refresh()"
      via: "direct self._telemetry passthrough (no wrapper class)"
      pattern: "_refresh_calendar_cache\\(registry=self._registry, telemetry=self._telemetry\\)"
---

<objective>
In **astridr-repo** (`C:\Users\mandr\astridr-repo`): delete the dead `CodePulsePoster` class from `astridr/automation/calendar_cache.py` (and the module constants it orphans), switch `cron_dispatcher.py`'s `_run_calendar_cache_refresh` to pass the shared `self._telemetry` (`ConvexHandler`) exactly like every other cron in that dispatcher, fix the dispatcher unit test that imports and asserts on `CodePulsePoster`, and sweep the stale "two-backend / local backend 404s on /calendar-ingest / tidy-whale cloud" narrative out of all four comment sites in one pass.

Purpose: Close v12.0 milestone-audit tech-debt item 2. Cloud deployment `tidy-whale-981` is retired (2026-07-15); with `CODEPULSE_CONVEX_URL` unset the poster already resolved to the same URL as `ConvexHandler`, and the local self-hosted backend DOES serve `/calendar-ingest` (`convex/http.ts:96-101`). The comments assert a false topology that two other files cite as license — that is a bug, treated as one.

Output: `calendar_cache.py`, `cron_dispatcher.py`, `tools/reminders.py`, `tests/unit/engine/bootstrap/test_cron_dispatcher.py` edited; astridr suite green; grep proves zero `CodePulsePoster` and zero stale-narrative references. Deleting the class changes which object the LIVE calendar cron calls, so a real cron tick is verified in plan 102-03.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/102-address-tech-debt-reminders-dead-code-astridr-comment-cleanu/102-CONTEXT.md
@.planning/phases/102-address-tech-debt-reminders-dead-code-astridr-comment-cleanu/102-PATTERNS.md

<interfaces>
<!-- Verified current-state anchors in astridr-repo (grep-confirmed 2026-07-22).
     Executor MUST re-read the live files (read_first) before editing. All paths are
     relative to C:\Users\mandr\astridr-repo. -->

astridr/automation/calendar_cache.py:
  - module constants L49-52: _CP_URL, _LOCAL_URL, _CODEPULSE_URL, _INGEST_KEY
      → grep-confirmed: read ONLY inside CodePulsePoster (L153-154). DEAD once the class is gone → DELETE all four.
  - class CodePulsePoster — declared L138, reads _CODEPULSE_URL/_INGEST_KEY → DELETE the whole class (docstring included).
  - refresh() (L177+) — takes telemetry: Any, duck-typed on .send_to → NO CHANGE.
  - module comment L46-48 ("CONVEX TARGET — ... the LOCAL self-hosted backend is the single store") → already correct; leave as-is (delete only if it references the now-deleted constants).

astridr/engine/bootstrap/cron_dispatcher.py — _run_calendar_cache_refresh (~L776-806):
  - import block imports CodePulsePoster (L786) AND refresh — DROP the CodePulsePoster import, keep `refresh as _refresh_calendar_cache`.
  - stale inline comment L794-798 ("NOT self._telemetry ... has no /calendar-ingest route (404) ... CodePulsePoster targets the CLOUD deployment ...") → DELETE outright.
  - call L798 `telemetry=CodePulsePoster()` → change to `telemetry=self._telemetry`.
  - Analog (copy the SHAPE, not the field names): _run_reminder_nudge (same file, single-import + direct self._telemetry passthrough). Same pattern at call sites L393, L588, L727, L906.

astridr/tools/reminders.py:
  - LIVE constants L53-56 (_CP_URL/_LOCAL_URL/_CONVEX_URL/_INGEST_KEY) are read by execute() (L111, L121) → DO NOT DELETE.
  - stale comment block L45-51 ("escape hatch for the transitional case", "History (2026-07-20) ... different databases ... round trip 404'd") → REWRITE to current truth; drop the transitional/escape-hatch/History framing.

tests/unit/engine/bootstrap/test_cron_dispatcher.py (~L190-217):
  - imports CodePulsePoster (L209) and asserts isinstance(kwargs["telemetry"], CodePulsePoster) (L213) → WILL ImportError once the class is deleted.
  - stale inline comment L205-208 (the "404s / never reads" narrative) → DELETE.
  - invert assertion to `assert kwargs["telemetry"] is telemetry` (now IS the shared instance); drop the `is not telemetry` line and the CodePulsePoster import; rename test → test_calendar_cache_refresh_passes_shared_telemetry.

Cross-repo ground truth (codepulse, read-only reference): convex/http.ts:94-101 — one local backend serves /reminders-ingest, /reminders-read, AND /calendar-ingest.

OUT OF SCOPE (do NOT edit — triaged non-narrative / different subsystem):
  - astridr/channels/web.py:839 CODEPULSE_ORIGIN default "https://tidy-whale-981.convex.site" — war-room CORS config, a FUNCTIONAL default not a topology comment; changing it is behavioral and belongs to war-room hardening.
  - unrelated 404 references in ugc/, meeting_bot/, war_room/, api/ — legitimate HTTP handling for other features.
  - tidy-whale-981 as a fake test URL in tests/tools/test_reminders.py, tests/test_langfuse_eval.py, tests/test_web_cors.py — not narrative claims.
  - .planning/ historical docs — out of scope.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete CodePulsePoster + orphaned constants, switch the cron to self._telemetry, fix the dispatcher test</name>
  <files>astridr-repo: astridr/automation/calendar_cache.py, astridr/engine/bootstrap/cron_dispatcher.py, tests/unit/engine/bootstrap/test_cron_dispatcher.py</files>
  <read_first>
    - astridr-repo: astridr/automation/calendar_cache.py (read L1-215: module constants, the CodePulsePoster class, and refresh()'s signature)
    - astridr-repo: astridr/engine/bootstrap/cron_dispatcher.py (read _run_calendar_cache_refresh AND its sibling _run_reminder_nudge for the passthrough shape to copy)
    - astridr-repo: tests/unit/engine/bootstrap/test_cron_dispatcher.py (read test_calendar_cache_refresh_calls_module_refresh_with_deps ~L190-217)
    - .planning/phases/102-address-tech-debt-reminders-dead-code-astridr-comment-cleanu/102-PATTERNS.md §"calendar_cache.py", §"cron_dispatcher.py", §"test_cron_dispatcher.py"
  </read_first>
  <action>
    Work in astridr-repo (`C:\Users\mandr\astridr-repo`). Commit there via `git -C C:/Users/mandr/astridr-repo` per the 101/98-05 cross-repo precedent (worktrees OFF, sequential). Re-check `git -C ... branch --show-current` and staged files immediately before committing (concurrent-session gotcha).

    `astridr/automation/calendar_cache.py`: delete the entire `class CodePulsePoster` (docstring through its `send_to` method). Then delete the four now-orphaned module constants `_CP_URL`, `_LOCAL_URL`, `_CODEPULSE_URL`, `_INGEST_KEY` (grep-confirmed read only inside the deleted class). Leave the module comment L46-48 and `refresh()` untouched — `refresh()` keeps `telemetry: Any` and its duck-typed `.send_to` call; no signature change. If the `os`/`get_pool` imports become unused after the class deletion, remove only the ones with zero remaining reference in the file (grep before removing).

    `astridr/engine/bootstrap/cron_dispatcher.py`: in `_run_calendar_cache_refresh`, drop the `CodePulsePoster` import (keep `from astridr.automation.calendar_cache import refresh as _refresh_calendar_cache`), delete the stale 4-line inline comment (the "NOT self._telemetry ... 404 ... CodePulsePoster targets the CLOUD deployment ... See astridr/tools/reminders.py for the full note" block), and change the refresh call to `result = await _refresh_calendar_cache(registry=self._registry, telemetry=self._telemetry)`. Copy the passthrough SHAPE from the sibling `_run_reminder_nudge` (single `from ... import X as _y`, direct `self._telemetry`, `try/except Exception as exc: # noqa: BLE001` fail-closed). The docstring's `mirroring _run_graph_snapshot` may stay or become `mirroring _run_reminder_nudge` — either is correct.

    `tests/unit/engine/bootstrap/test_cron_dispatcher.py`: in `test_calendar_cache_refresh_calls_module_refresh_with_deps`, delete the `from astridr.automation.calendar_cache import CodePulsePoster` import, delete the stale inline comment (the "404s / never reads" narrative), invert the assertion to `assert kwargs["telemetry"] is telemetry` (drop `assert kwargs["telemetry"] is not telemetry` and the isinstance line), and rename the test to `test_calendar_cache_refresh_passes_shared_telemetry`. Keep the fail-closed / completed-status assertions.
  </action>
  <verify>
    <automated>cd C:/Users/mandr/astridr-repo && python -m pytest tests/unit/engine/bootstrap/test_cron_dispatcher.py tests/automation/ -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "CodePulsePoster" C:/Users/mandr/astridr-repo/astridr C:/Users/mandr/astridr-repo/tests` returns 0 matches
    - `grep -nE "_CODEPULSE_URL|^_CP_URL|^_LOCAL_URL|^_INGEST_KEY" astridr/automation/calendar_cache.py` returns 0 matches (dead constants gone)
    - `astridr/tools/reminders.py` STILL defines `_CP_URL`/`_LOCAL_URL`/`_CONVEX_URL`/`_INGEST_KEY` (live constants untouched) — `grep -c "_CONVEX_URL" astridr/tools/reminders.py` >= 1
    - `cron_dispatcher.py` `_run_calendar_cache_refresh` contains `telemetry=self._telemetry` and NO `CodePulsePoster`
    - `test_cron_dispatcher.py` contains `kwargs["telemetry"] is telemetry` and no `import CodePulsePoster`
    - `python -m pytest tests/unit/engine/bootstrap/test_cron_dispatcher.py tests/automation/ -q` exits 0
  </acceptance_criteria>
  <done>CodePulsePoster and its orphaned constants are gone; the calendar cron passes the shared ConvexHandler like every other cron; the dispatcher test verifies passthrough and imports nothing deleted; scoped pytest green.</done>
</task>

<task type="auto">
  <name>Task 2: Sweep the stale two-backend narrative repo-wide and prove the static bar</name>
  <files>astridr-repo: astridr/tools/reminders.py</files>
  <read_first>
    - astridr-repo: astridr/tools/reminders.py (read the module comment block ~L42-56 — the "full two-backend note" the other files cited)
    - codepulse: convex/http.ts (L94-101 — ground truth: one local backend serves all three routes)
    - .planning/phases/102-address-tech-debt-reminders-dead-code-astridr-comment-cleanu/102-PATTERNS.md §"tools/reminders.py" and §"Stale-topology-comment correction"
    - CLAUDE.md §Bug Fixing (inventory ALL instances before fixing — Larry's standing rule, which D-04 restates)
  </read_first>
  <action>
    First, run the inventory grep across astridr-repo source + tests (NOT .planning) to confirm the complete site list before editing any:
    `grep -rniE "two-backend|tidy-whale|CLOUD deployment|no /calendar-ingest|404s|transitional case|escape hatch|different database" C:/Users/mandr/astridr-repo/astridr C:/Users/mandr/astridr-repo/tests`
    Expected in-scope narrative sites (from the pattern map, minus what Task 1 already removed): `astridr/tools/reminders.py` comment block only. calendar_cache.py's class docstring and cron_dispatcher.py's + test_cron_dispatcher.py's inline comments were deleted in Task 1 with their code. Confirm those three now return 0. If the inventory surfaces any NEW narrative comment beyond `tools/reminders.py`, fix it in this same pass (Claude's Discretion per CONTEXT); do NOT touch the explicitly OUT-OF-SCOPE sites listed in `<interfaces>` (web.py:839, unrelated feature 404s, fake test URLs).

    Rewrite `astridr/tools/reminders.py`'s comment block (~L42-56, the lines ABOVE the live `_CP_URL` constant) to state the current truth plainly: `CONVEX_URL` is the LOCAL self-hosted backend — the single store for both CodePulse and Ástríðr, serving `/calendar-ingest`, `/reminders-ingest`, and `/reminders-read` (see `convex/http.ts`); `CODEPULSE_CONVEX_URL` is a local-dev override, left unset in the normal setup. Remove the "History (2026-07-20) ... different databases ... round trip 404'd" paragraph and the "escape hatch for the transitional case" framing entirely — those two lines are the load-bearing false narrative the other files cited. Do NOT alter the live `_CP_URL`/`_LOCAL_URL`/`_CONVEX_URL`/`_INGEST_KEY` constant definitions below the comment.

    Run the full astridr test suite to prove nothing regressed.
  </action>
  <verify>
    <automated>cd C:/Users/mandr/astridr-repo && python -m pytest tests/tools/test_reminders.py -q && python -m pytest -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rniE "no /calendar-ingest|404s.*calendar|transitional case|escape hatch for the transitional|round trip 404|round-trip 404|different databases" astridr/tools/reminders.py` returns 0 matches
    - `grep -rn "CodePulsePoster" C:/Users/mandr/astridr-repo/astridr C:/Users/mandr/astridr-repo/tests` returns 0 matches (re-confirm after Task 1)
    - `astridr/tools/reminders.py` comment references the single local backend and `convex/http.ts`; the "History (2026-07-20)" paragraph is gone
    - `web.py:839` is UNCHANGED (`grep -c "tidy-whale-981" astridr/channels/web.py` still >= 1 — proves the out-of-scope site was not swept)
    - `python -m pytest tests/tools/test_reminders.py -q` exits 0
    - `python -m pytest -q` exits 0 (full astridr suite green; 2026-07-22 baseline included 49/49 across the reminders/calendar/dispatcher suites)
  </acceptance_criteria>
  <done>Every stale two-backend/local-404 narrative claim in astridr-repo source+tests is corrected to the single-local-backend truth; the out-of-scope war-room default is left alone; full astridr suite green; grep proves zero CodePulsePoster and zero stale-narrative references.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| calendar cron → Convex ingest | After the class deletion, the LIVE cron pushes calendar events via the shared `ConvexHandler` (bearer `ASTRIDR_INGEST_API_KEY`) instead of the bespoke `CodePulsePoster` — the auth header + base URL now come from the shared handler |
| grep sweep → source tree | A repo-wide comment sweep can over-delete live code if scoped too broadly |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-102-04 | Tampering | over-deletion — deleting the LIVE `_CP_URL/_CONVEX_URL/_INGEST_KEY` constants in tools/reminders.py or reminder_nudge.py while removing the dead ones in calendar_cache.py | mitigate | Explicit: delete constants ONLY from calendar_cache.py (grep-confirmed dead); acceptance asserts tools/reminders.py still defines `_CONVEX_URL`; full pytest green |
| T-102-05 | Information disclosure / Spoofing | calendar cron auth after the switch — `self._telemetry` must carry the same bearer + reach the same URL the poster did, or pushes silently fail (or hit the wrong host) | mitigate | 102-03 fires ONE real cron tick and asserts `pushed>0, failed=0` + events render on /reminders; this plan does not print secrets during any grep/test |
| T-102-06 | Tampering | grep sweep touching unrelated 404 handling (ugc/web/war_room/api) or the war-room CORS default | mitigate | Out-of-scope sites enumerated in `<interfaces>`; acceptance asserts web.py:839 tidy-whale default is UNCHANGED |
| T-102-SC | Tampering | supply chain (pip installs) | n/a | No package installs this phase — no legitimacy gate required |
</threat_model>

<verification>
- `grep -rn CodePulsePoster` across astridr/ + tests/ = 0.
- Dead constants gone from calendar_cache.py; live constants intact in tools/reminders.py.
- `_run_calendar_cache_refresh` passes `self._telemetry`; dispatcher test asserts passthrough.
- No stale two-backend/local-404 narrative remains in astridr source+tests; web.py:839 untouched.
- Full astridr pytest suite green.
</verification>

<success_criteria>
CodePulsePoster deleted, the calendar cron aligned to the shared-telemetry convention, the dispatcher test fixed, and the stale two-backend narrative swept from all four comment sites — with no over-deletion of live constants and no touching of out-of-scope sites (D-03, D-04, D-06 static bar). The live cron-tick proof is handed to plan 102-03.
</success_criteria>

<output>
Create `.planning/phases/102-address-tech-debt-reminders-dead-code-astridr-comment-cleanu/102-02-SUMMARY.md` when done.
</output>
