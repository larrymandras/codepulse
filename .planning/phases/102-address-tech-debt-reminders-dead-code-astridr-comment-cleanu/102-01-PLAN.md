---
phase: 102-address-tech-debt-reminders-dead-code-astridr-comment-cleanu
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - convex/reminders.ts
  - convex/reminders.test.ts
  - convex/remindersIngest.ts
  - convex/schema.ts
  - convex/_generated/dataModel.d.ts
  - convex/_generated/api.d.ts
autonomous: true
requirements: [AUDIT-TD-01]
# AUDIT-TD-01 = v12.0-MILESTONE-AUDIT.md §Tech Debt item 1 (orphaned dueSoon/overdue
# dead code + by_dueAt index). No formal REQ IDs — this is a milestone-audit tech-debt
# closure phase scoped by CONTEXT.md decisions D-01, D-02, D-05, D-06.

must_haves:
  truths:
    - "D-01: convex/reminders.ts no longer exports dueSoon, overdue, dueSoonHandler, or overdueHandler; their two unit tests and two imports are removed from convex/reminders.test.ts"
    - "D-02: the reminders table in convex/schema.ts keeps only by_profile and by_status; the by_dueAt index is gone and npx convex codegen has regenerated _generated/*"
    - "D-05: only audit items 1-2 are in scope this phase — this plan touches ONLY the reminders dead-code (item 1); QuickAdd NL parsing (item 3) and the astridr boot-order transient (item 4) are NOT touched"
    - "D-06: static proof bar met on the codepulse side — full Vitest suite green, npx tsc --noEmit clean, and grep proves ZERO remaining references to dueSoon / overdue anywhere in convex/ (including the stale comment in convex/remindersIngest.ts) and zero by_dueAt in convex/"
    - "The live REM-05 path (reminder_nudge.py /reminders-read + listByProfile) is untouched and remains the sole canonical design"
  artifacts:
    - path: "convex/reminders.ts"
      provides: "reminders CRUD queries WITHOUT the dead dueSoon/overdue queries"
      contains: "export const listByProfile"
    - path: "convex/schema.ts"
      provides: "reminders table with by_profile + by_status indexes only"
      contains: "reminders: defineTable"
  key_links:
    - from: "convex/reminders.ts"
      to: "convex/schema.ts"
      via: "surviving live queries (listByProfile) use by_profile only; no query uses by_dueAt after this plan"
      pattern: "withIndex\\(\"by_profile\""
---

<objective>
Delete the orphaned `dueSoon`/`overdue` queries and their `dueSoonHandler`/`overdueHandler` functions from `convex/reminders.ts`, delete their two unit tests plus two imports from `convex/reminders.test.ts`, correct the now-stale `dueSoon/overdue` reference in the `convex/remindersIngest.ts` snooze comment, and drop the now-unused `by_dueAt` index from the `reminders` table in `convex/schema.ts`. These were built for D-11's dedicated-query nudge design but have zero callers — the live REM-05 nudge (`reminder_nudge.py` → `/reminders-read` → `listByProfile` + client-side `_is_due()`) is the canonical design and is untouched.

Purpose: Close v12.0 milestone-audit tech-debt item 1 (reminders dead code). Removing the dead queries and their sole-consumer index eliminates a design-deviation footgun and keeps the schema honest. Correcting the snooze comment satisfies D-06 "zero remaining references to dueSoon/overdue" — after the queries are deleted, that comment would assert a mechanism that no longer exists.

Output: `convex/reminders.ts`, `convex/reminders.test.ts`, `convex/remindersIngest.ts`, `convex/schema.ts` with the dead code and stale reference removed; `_generated/*` regenerated by offline codegen. The live index-drop DEPLOY to the self-hosted backend is deferred to plan 102-03 (operator-gated).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/102-address-tech-debt-reminders-dead-code-astridr-comment-cleanu/102-CONTEXT.md
@.planning/phases/102-address-tech-debt-reminders-dead-code-astridr-comment-cleanu/102-PATTERNS.md

<interfaces>
<!-- Verified current-state anchors (grep-confirmed 2026-07-22). Executor MUST re-read the
     live files (read_first) — line numbers drift, symbol names do not. -->

convex/reminders.ts:
  - dueSoonHandler   — declared L392, uses .withIndex("by_dueAt") L398   → DELETE
  - dueSoon (query)  — declared L405, calls dueSoonHandler L408           → DELETE
  - overdueHandler   — declared L412, uses .withIndex("by_dueAt") L413   → DELETE
  - overdue (query)  — declared L419, calls overdueHandler L421           → DELETE
  - listByProfileHandler / listByProfile (L373-389) — SURVIVING sibling, DO NOT touch. File ends at L422.
  - imports (L20-21): `mutation, query` and `v` — both still used by surviving code; KEEP.

convex/reminders.test.ts:
  - import block L9-21 includes `dueSoonHandler,` (L19) and `overdueHandler,` (L20) → DELETE those 2 lines only
  - it("dueSoon excludes status done ...") uses dueSoonHandler at L477   → DELETE whole it() block
  - it("overdue excludes status done ...") uses overdueHandler at L502   → DELETE whole it() block
  - both it() blocks live INSIDE describe("reminders CRUD handlers", ...) which closes with `});` — leave that closing intact
  - describe("markNotifiedHandler ...") and everything after (L516+) is UNTOUCHED

convex/remindersIngest.ts:
  - snooze branch comment (~L82-84) currently reads:
      // REM-03: a real snooze (status "snoozed" + snoozedUntil), NOT an
      // update with a shifted dueAt — the latter loses the snooze state the
      // dueSoon/overdue queries key off.
  - This is the ONLY surviving `dueSoon/overdue` reference in convex/ after the deletions above
    (grep-confirmed 2026-07-22: the sole non-reminders.ts/test hit). The snooze MUTATION itself is
    live and correct — only the comment's trailing clause is stale. Rewrite it to describe the
    surviving mechanism (snooze state is consumed client-side by reminder_nudge.py's `_is_due()`
    via /reminders-read, NOT by dedicated Convex queries). Do NOT alter the mutation logic.

convex/schema.ts (reminders table, ~L1845-1874):
  - index chain currently: .index("by_profile", ["profileId","status"]).index("by_status", ["status","dueAt"]).index("by_dueAt", ["dueAt"])
  - DELETE the .index("by_dueAt", ["dueAt"]) line (L1874); the chain now ends at by_status. by_status KEEPS its ["status","dueAt"] fields (dueAt is a field IN the by_status index — that is not the by_dueAt index; do NOT alter by_status).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete dead dueSoon/overdue queries, handlers, tests, and fix the stale snooze comment</name>
  <files>convex/reminders.ts, convex/reminders.test.ts, convex/remindersIngest.ts</files>
  <read_first>
    - convex/reminders.ts (read the full file; confirm the tail block dueSoonHandler/dueSoon/overdueHandler/overdue is the entire remainder after listByProfile's closing `});`)
    - convex/reminders.test.ts (read the import block L1-21 and the describe("reminders CRUD handlers", ...) block to locate the two it() blocks exactly)
    - convex/remindersIngest.ts (read the snooze branch ~L81-94 to locate the stale comment and confirm the mutation call itself is unchanged)
    - .planning/phases/102-address-tech-debt-reminders-dead-code-astridr-comment-cleanu/102-PATTERNS.md §"convex/reminders.ts" and §"convex/reminders.test.ts" (verbatim delete targets)
  </read_first>
  <action>
    In `convex/reminders.ts`: delete the four exports `dueSoonHandler`, `dueSoon`, `overdueHandler`, `overdue` — i.e. remove everything from the blank line immediately after `listByProfile`'s closing `});` through end-of-file (this includes the `/** Open|snoozed rows ... */` doc comment preceding `dueSoonHandler`). Do NOT touch `listByProfileHandler`/`listByProfile` (the surviving sibling) or the `import { mutation, query }` / `import { v }` lines (both remain in use). After deletion the file ends cleanly at `listByProfile`'s `});`.

    In `convex/reminders.test.ts`: delete the two import lines `dueSoonHandler,` and `overdueHandler,` from the `from "./reminders"` import block. Then delete the two `it(...)` blocks — `it("dueSoon excludes status done and rows outside the window", ...)` and `it("overdue excludes status done and never returns future rows", ...)` — in their entirety. Both blocks sit inside `describe("reminders CRUD handlers", ...)`; leave that describe's closing `});` and the following `describe("markNotifiedHandler ...")` untouched.

    In `convex/remindersIngest.ts` (per D-06 "zero remaining references to dueSoon/overdue"): rewrite the stale snooze-branch comment (~L82-84) so it no longer references the deleted `dueSoon/overdue` queries. It must describe the SURVIVING mechanism — a real snooze (status "snoozed" + snoozedUntil) preserves state that is consumed client-side by `reminder_nudge.py`'s `_is_due()` via `/reminders-read` + `listByProfile`, NOT by dedicated Convex queries. Keep the REM-03 intent (a real snooze vs. a shifted-dueAt update). Do NOT change the `ctx.runMutation(api.reminders.snooze, ...)` call or any other logic in this file.

    Do NOT change `convex/schema.ts` in this task (Task 2 handles the index). Do NOT touch `reminder_nudge.py` (astridr-repo, out of this repo and out of scope — it is the live design being preserved). Do NOT touch `src/` `overdue` references (e.g. `ReminderList.tsx` — those are unrelated local variables/CSS labels, grep-confirmed 2026-07-22).
  </action>
  <verify>
    <automated>cd C:/Users/mandr/codepulse && npx vitest run convex/reminders.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rniE "dueSoon|overdue" convex/` returns 0 matches (after the deletions AND the remindersIngest.ts comment fix — the only convex/ hits were reminders.ts, reminders.test.ts, and remindersIngest.ts:84, all now gone). This is the D-06 "zero remaining references to dueSoon/overdue" gate, enforced repo-wide across convex/.
    - `convex/reminders.ts` still contains `export const listByProfile` and `export const listByProfileHandler`
    - `convex/remindersIngest.ts` still contains the live `api.reminders.snooze` mutation call (comment fixed, logic unchanged)
    - `npx vitest run convex/reminders.test.ts` exits 0 (surviving reminders tests green, no orphaned-import error)
  </acceptance_criteria>
  <done>The two dead queries, their two handlers, their two unit tests, and their two test imports are gone; the stale dueSoon/overdue reference in remindersIngest.ts's snooze comment is rewritten to the surviving mechanism; reminders.test.ts runs green with no unresolved imports; `grep -rniE "dueSoon|overdue" convex/` returns 0.</done>
</task>

<task type="auto">
  <name>Task 2: Drop the by_dueAt index, regenerate codegen, and prove the static bar</name>
  <files>convex/schema.ts, convex/_generated/dataModel.d.ts, convex/_generated/api.d.ts</files>
  <read_first>
    - convex/schema.ts (read the reminders defineTable block, ~L1845-1874, to see the exact index chain)
    - convex/reminders.ts (confirm no surviving query references by_dueAt after Task 1)
    - CLAUDE.md §"Self-Hosted Convex — Operational Rules" (schema/index changes are fine; this is metadata-only, not a mass data op)
    - .planning/phases/102-address-tech-debt-reminders-dead-code-astridr-comment-cleanu/102-PATTERNS.md §"convex/schema.ts"
  </read_first>
  <action>
    In `convex/schema.ts`, remove the `.index("by_dueAt", ["dueAt"])` call from the `reminders` table's index chain so the chain ends at `.index("by_status", ["status", "dueAt"])`. Keep `by_profile` and `by_status` exactly as-is — `by_status` legitimately indexes `["status", "dueAt"]`; that is a different index from the deleted `by_dueAt` and MUST NOT be altered. Ensure the `defineTable(...)` statement remains syntactically closed (the `by_status` line becomes the final chained call).

    Then run `npx convex codegen` (offline — regenerates `convex/_generated/dataModel.d.ts` and `api.d.ts` for the removed index; it does NOT deploy). Do NOT run `npx convex deploy` in this plan — the live index-DROP against the self-hosted backend is operator-gated in plan 102-03.

    Run `npx tsc --noEmit` and the full `npm test` suite to prove nothing referenced the dropped index or the deleted queries.
  </action>
  <verify>
    <automated>cd C:/Users/mandr/codepulse && npx convex codegen && npx tsc --noEmit && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "by_dueAt" convex/schema.ts` returns 0 matches
    - `grep -rn "by_dueAt" convex/ | grep -v '^Binary'` returns 0 matches across the whole convex/ tree (no query, no generated file references it)
    - `convex/schema.ts` reminders table still contains `.index("by_profile"` and `.index("by_status"`
    - `npx convex codegen` exits 0 and `_generated/` is regenerated
    - `npx tsc --noEmit` exits 0
    - `npm test` exits 0 (full suite green; 2026-07-22 baseline was 204 files / 2331 tests green)
  </acceptance_criteria>
  <done>The reminders table has exactly by_profile + by_status; codegen regenerated; tsc clean; full suite green; grep proves zero by_dueAt / dueSoon / overdue references anywhere in convex/.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| planner/executor → live schema | Schema edits here regenerate types offline only; the live self-hosted backend is NOT deployed in this plan (deferred to 102-03) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-102-01 | Tampering | over-deletion in reminders.ts (removing a LIVE query alongside the dead ones) | mitigate | Delete ONLY the tail block after listByProfile's `});`; acceptance grep asserts `export const listByProfile` survives and `npm test` stays green |
| T-102-02 | Tampering | altering the by_status index while dropping by_dueAt (by_status also lists dueAt as a field) | mitigate | Explicit action note: by_status is a distinct index, KEEP verbatim; acceptance asserts `.index("by_status"` still present |
| T-102-03 | Denial of Service | accidental live deploy from codepulse while removing an index | accept/defer | This plan runs codegen ONLY (offline); the live index DROP is isolated to operator-gated plan 102-03. No `npx convex deploy` in this plan |
| T-102-10 | Tampering | editing the live snooze MUTATION while fixing its stale comment in remindersIngest.ts | mitigate | Action scopes the change to the comment text only; acceptance asserts the `api.reminders.snooze` call still present and `npm test` green |
| T-102-SC | Tampering | supply chain (npm/pip installs) | n/a | No package installs this phase — no legitimacy gate required |
</threat_model>

<verification>
- `convex/reminders.ts` exports no `dueSoon`/`overdue`/`dueSoonHandler`/`overdueHandler` (grep 0).
- `convex/remindersIngest.ts` snooze comment no longer references the deleted queries; the snooze mutation is unchanged.
- `grep -rniE "dueSoon|overdue" convex/` returns 0 across the whole convex/ tree (D-06 gate).
- `convex/schema.ts` reminders table indexes = {by_profile, by_status} only (grep `by_dueAt` = 0).
- `npx convex codegen` regenerated `_generated/*`; `npx tsc --noEmit` clean; `npm test` green.
- The live `reminder_nudge.py` design is untouched (this plan does not enter astridr-repo).
</verification>

<success_criteria>
Dead reminders queries + handlers + tests + the by_dueAt index are removed from codepulse; the stale `dueSoon/overdue` snooze comment in remindersIngest.ts is corrected; codegen regenerated; full suite + tsc green; zero remaining references to dueSoon/overdue anywhere in convex/ and zero by_dueAt (D-01, D-02 code-side, D-06 static bar). The live index DROP deploy is handed to plan 102-03.
</success_criteria>

<output>
Create `.planning/phases/102-address-tech-debt-reminders-dead-code-astridr-comment-cleanu/102-01-SUMMARY.md` when done.
</output>
</content>
</invoke>
