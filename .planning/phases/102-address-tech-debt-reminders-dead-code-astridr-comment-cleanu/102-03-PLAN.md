---
phase: 102-address-tech-debt-reminders-dead-code-astridr-comment-cleanu
plan: 03
type: execute
wave: 2
depends_on: ["102-01", "102-02"]
files_modified: []
autonomous: false
requirements: [AUDIT-TD-01, AUDIT-TD-02]
# Operator-gated live close-out for both audit items. No source files change here — this plan
# performs the live index-DROP deploy (codepulse, self-hosted backend) and the one real
# calendar-cron tick (astridr) that D-02/D-07 require. Scoped by CONTEXT.md decisions D-02, D-07.

must_haves:
  truths:
    - "D-02: the by_dueAt index DROP is deployed to the live self-hosted Convex backend and the index no longer exists on the reminders table there"
    - "D-07: one real calendar_cache refresh tick runs against the live astridr stack after CodePulsePoster's deletion and logs pushed>0, failed=0, with the events rendering on the CodePulse /reminders calendar overlay"
    - "The self-hosted backend is not subjected to any mass data operation — the deploy is a schema/metadata index drop on the small reminders table only, per CLAUDE.md operational rules"
  artifacts: []
  key_links:
    - from: "astridr calendar_cache.refresh() (now via self._telemetry)"
      to: "Convex /calendar-ingest on the self-hosted backend"
      via: "one live cron tick proving the shared ConvexHandler reaches the same URL the deleted poster did"
      pattern: "pushed.*failed"
---

<objective>
Operator-gated live close-out for both audit items, after plans 102-01 (codepulse code) and 102-02 (astridr code) land:

1. Deploy the `by_dueAt` index DROP to the live self-hosted Convex backend (D-02) — the schema change is code-committed in 102-01 but the actual drop on the running backend is a deploy.
2. Trigger ONE real `calendar_cache` refresh tick against the live astridr stack (D-07) — deleting `CodePulsePoster` changed which object the production cron calls (now the shared `ConvexHandler`), so a single real tick proves the "same URL / same auth" assertion end-to-end.

Purpose: Prove the two cleanups are correct against the running system, not just green in unit tests. No full mini-UAT — 101-RETEST-UAT passed 7/7 on 2026-07-21; this is a targeted two-point live check.

Output: A deployed self-hosted backend with the index gone, and a logged calendar tick showing `pushed>0, failed=0` with events visible on `/reminders`. No source files change in this plan.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/102-address-tech-debt-reminders-dead-code-astridr-comment-cleanu/102-CONTEXT.md
@.planning/phases/102-address-tech-debt-reminders-dead-code-astridr-comment-cleanu/102-01-PLAN.md
@.planning/phases/102-address-tech-debt-reminders-dead-code-astridr-comment-cleanu/102-02-PLAN.md

<notes>
- CLAUDE.md §"Self-Hosted Convex — Operational Rules": the production backend is self-hosted single-node SQLite at C:\Users\mandr\convex-selfhost. Schema deploys (including index drops) are FINE; NEVER run `npx convex import --replace-all` or any mass delete/patch. An index drop on the small `reminders` table is metadata-only, not a tombstone-generating mass op.
- Deploy target: codepulse deploy now targets the self-hosted backend (cloud tidy-whale-981 retired 2026-07-15). The exact deploy command + admin-key sourcing are the operator's — the .env / admin key are env-guarded and cannot be read or run by the agent.
- Do NOT print or log the ingest key / admin key during verification.
</notes>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Deploy the by_dueAt index DROP to the live self-hosted backend and confirm it is gone</name>
  <files>none (live deploy of the schema already committed in 102-01; no source files change)</files>
  <action>Operator deploys the codepulse Convex schema (with by_dueAt already removed by 102-01) to the live self-hosted backend via the standard self-hosted deploy — `npx convex deploy --yes` against the self-hosted URL + admin key. NEVER `npx convex import --replace-all` and never any import verb (mass-delete risk per CLAUDE.md). The agent cannot run this: the admin key is env-guarded. This is a metadata-only index drop on the small reminders table.</action>
  <what-built>
    Plan 102-01 removed the `by_dueAt` index from `convex/schema.ts` and regenerated codegen offline. The index still exists on the RUNNING self-hosted backend until a deploy applies the schema. This is a metadata-only index drop on the small `reminders` table — safe per CLAUDE.md operational rules (not a mass data op).
  </what-built>
  <how-to-verify>
    1. From `C:\Users\mandr\codepulse`, deploy the schema to the self-hosted backend using your standard self-hosted deploy (per CLAUDE.md — `npx convex deploy --yes` against the self-hosted URL/admin key, NOT `--replace-all`, NOT any import).
    2. Confirm the deploy succeeds with no error and no unexpected data migration.
    3. Confirm the `by_dueAt` index no longer exists on the `reminders` table on the live backend — e.g. inspect the deployed schema in the self-hosted dashboard's reminders table indexes, or confirm the deploy output shows only `by_profile` + `by_status` for reminders.
    4. Confirm the CodePulse dashboard still loads and `/reminders` still renders reminders (the surviving `by_profile` path is unaffected).
  </how-to-verify>
  <acceptance_criteria>
    - Deploy completes successfully against the self-hosted backend (no error).
    - The reminders table on the live backend has indexes {by_profile, by_status} only — by_dueAt is gone.
    - `/reminders` still loads and lists reminders (no regression from the index drop).
  </acceptance_criteria>
  <verify>
    <human-check>Operator confirms the self-hosted deploy succeeded and the reminders table on the live backend now lists only by_profile + by_status (by_dueAt gone), and /reminders still renders.</human-check>
  </verify>
  <done>by_dueAt index dropped on the live self-hosted backend; /reminders unaffected.</done>
  <resume-signal>Type "approved" once the deploy succeeded and by_dueAt is confirmed gone, or describe any error.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Trigger one real calendar_cache refresh tick and confirm pushed>0, failed=0</name>
  <files>none (live verification only; no source files change)</files>
  <action>Operator triggers ONE real calendar_cache refresh against the running astridr stack (running the 102-02 code) — force the `calendar:cache_refresh` cron or invoke the refresh path against the live registry/telemetry — then reads the astridr log for the `calendar_cache.cron_complete` event and checks the /reminders calendar overlay. Do not print the ingest key at any point.</action>
  <what-built>
    Plan 102-02 deleted `CodePulsePoster`; `_run_calendar_cache_refresh` now passes the shared `ConvexHandler` (`self._telemetry`) to `calendar_cache.refresh()`. This changes the object the LIVE cron calls, so one real tick proves the shared handler reaches the same `/calendar-ingest` URL with the same bearer the poster used.
  </what-built>
  <how-to-verify>
    1. Ensure the astridr stack is running with the code from plan 102-02.
    2. Trigger ONE real `calendar_cache` refresh — either force the cron (`calendar:cache_refresh`) or invoke the refresh path directly against the live registry/telemetry.
    3. In the astridr log, confirm `calendar_cache.cron_complete` reports `pushed>0` and `failed=0` (at least one profile's events pushed, zero failures). Do not print the ingest key.
    4. On the CodePulse dashboard `/reminders` calendar overlay, confirm Google Calendar events render for the pushed profile(s) — proving the events reached the self-hosted backend and the browser reads them.
  </how-to-verify>
  <acceptance_criteria>
    - The astridr log shows `calendar_cache.cron_complete` with `pushed>0` and `failed=0` after the tick.
    - No `calendar_cache.post_skipped` / auth-error / 404 in the log for this tick.
    - `/reminders` calendar overlay shows the refreshed events for at least one profile.
  </acceptance_criteria>
  <verify>
    <human-check>Operator confirms the astridr log shows calendar_cache.cron_complete with pushed>0 and failed=0 (no post_skipped/404/auth error) and the /reminders overlay renders the refreshed events for at least one profile.</human-check>
  </verify>
  <done>One real post-CodePulsePoster calendar tick pushed events successfully (pushed>0, failed=0) and they render on /reminders.</done>
  <resume-signal>Type "approved" once the tick logged pushed>0/failed=0 and events render on /reminders, or describe the failure.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator/deploy → live self-hosted backend | A schema deploy touches the production single-node SQLite backend that serves the live dashboard |
| astridr cron → Convex ingest | The live cron now authenticates + routes via the shared ConvexHandler; a real tick exercises the changed path with real secrets |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-102-07 | Denial of Service | live schema deploy — a wrong deploy verb (`--replace-all` / import) would mass-delete and wedge the backend | mitigate | Explicit: deploy is a plain schema deploy (index drop) on the small reminders table; NEVER `--replace-all`/import; operator confirms deploy output shows no data migration |
| T-102-08 | Information disclosure | secrets (ingest key / admin key) leaking into logs or checkpoint output during live verification | mitigate | Verify by log EVENT fields (pushed/failed) + UI render only; do not print keys; .env stays env-guarded |
| T-102-09 | Spoofing / repudiation | the switched cron silently pushing to the wrong host or with a missing bearer | mitigate | Task 2 asserts pushed>0/failed=0 AND events render on /reminders — end-to-end proof the shared handler reached the right backend with valid auth |
| T-102-SC | Tampering | supply chain (npm/pip installs) | n/a | No package installs this phase |
</threat_model>

<verification>
- Live self-hosted backend reminders table indexes = {by_profile, by_status}; by_dueAt dropped.
- One real calendar_cache tick logged pushed>0/failed=0; events render on /reminders.
- No secrets printed; no mass data operation performed.
</verification>

<success_criteria>
Both cleanups verified against the running system: the by_dueAt index is dropped on the live self-hosted backend (D-02) and the post-CodePulsePoster calendar cron pushes successfully in one real tick with events rendering (D-07). Phase 102 is code-complete AND live-verified.
</success_criteria>

<output>
Create `.planning/phases/102-address-tech-debt-reminders-dead-code-astridr-comment-cleanu/102-03-SUMMARY.md` when done.
</output>
