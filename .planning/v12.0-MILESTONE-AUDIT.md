---
milestone: v12.0
audited: 2026-07-22T14:01:35Z
status: tech_debt
scores:
  requirements: 9/9
  phases: 1/1
  integration: 8/9
  flows: 4/4
gaps:  # Critical blockers
  requirements: []
  integration: []
  flows: []
tech_debt:
  - phase: 101-reminders-calendar-command-center
    items:
      - "Orphaned exports: convex/reminders.ts:405-422 dueSoon/overdue queries (built per D-11) have zero callers — reminder_nudge.py fetches all rows via /reminders-read (listByProfile) and re-filters client-side in _is_due(). Functionally correct today, but the dedicated queries + their by_dueAt/by_status index usage are dead code relative to the documented design (REM-05, design deviation not a defect)."
      - "Contradictory commentary (CAL-01): astridr calendar_cache.py:138-146 + cron_dispatcher.py:792-799 justify CodePulsePoster with 'local backend has no /calendar-ingest (404)' — stale; with CODEPULSE_CONVEX_URL unset (the documented normal state) CodePulsePoster resolves to the same URL as ConvexHandler, and the local backend DOES serve /calendar-ingest (http.ts:96-101). Clean up the comments; keep CodePulsePoster as the escape hatch it now is."
      - "Backlog enhancement (from 101-UAT secondary findings): QuickAdd does not NL-parse dates from the title ('tomorrow at 9am') — the operator's instinct was to type a sentence; explicitly backlog, not a gap."
      - "astridr-repo transient: boot-order persistence.background_error (init race) observed once post-restart, self-heals; deliberately left alone (bootstrap ordering is active WIP on feature/brain-swap) — RETEST-UAT session notes."
nyquist:
  compliant_phases: ["101-reminders-calendar-command-center"]
  partial_phases: []
  missing_phases: []
  overall: compliant
---

# Milestone v12.0 Audit — Personal Productivity: Reminders & Calendar

**Audited:** 2026-07-22 (re-audit, second pass) · **Status:** `tech_debt` (no blockers; 4 deferred items — Nyquist gap CLOSED by `101-VALIDATION.md`, commit `c3cca44`)
**Scope:** Phase 101 (single phase, 7/7 plans incl. 101-07 gap closure), cross-repo (codepulse + astridr-repo).

> **Re-audit delta (14:01Z):** first pass earlier today flagged Nyquist validation as the sole structural gap. `/gsd-validate-phase 101` has since produced `101-VALIDATION.md` (reconstructed, `nyquist_compliant: true`, 0 gaps, 17/17 tasks mapped, 9/9 requirements COVERED, suites verified green: codepulse 81/81 + astridr 49/49). Integration was re-checked against the current working tree — including the uncommitted `events` index rename (`by_timestamp`→`by_timestamp2`) and the new `AppErrorBoundary` in `src/main.tsx` — with **0 new gaps**: all 15 wiring checks PASS, `npx tsc --noEmit` clean, the uncommitted diff never touches `reminders`/`calendarEvents` code paths. Everything below from the first pass remains accurate.

## Requirements Coverage — 3-Source Cross-Reference

All 9 v12.0 requirements cross-checked against (1) `101-VERIFICATION.md` requirements table, (2) SUMMARY frontmatter `requirements-completed`, (3) REQUIREMENTS.md traceability table.

| REQ-ID | Description | VERIFICATION | SUMMARY frontmatter | Traceability | Final |
|---|---|---|---|---|---|
| REM-01 | One Convex store, realtime, origin-tagged | passed (live-verified) | 101-01 | `[x]` Complete | **satisfied** |
| REM-02 | Authed /reminders-ingest + /reminders-read, fail-closed | passed (live 401 probe) | 101-02 | `[x]` Complete | **satisfied** |
| REM-03 | Ástríðr `reminders` tool, same store | passed (live NL round-trip, RETEST test 6) | 101-03 | `[x]` Complete | **satisfied** |
| REM-04 | Recurrence + bounded `until` + one-off never respawns | passed (live spawn + WR-01/02/03 fixes retested) | 101-01, 101-05 | `[x]` Complete | **satisfied** |
| REM-05 | Nudge exactly once, notifiedAt dedupe, correct channel | passed (live cron, 1 Telegram send, dedupe held 2 ticks) | 101-05 | `[x]` Complete | **satisfied** |
| CAL-01 | Per-profile read-only Google cache cron, upsert+prune, isolated failures | passed (live: 3/73/0 events across 3 real accounts) | 101-04 | `[x]` Complete | **satisfied** |
| CAL-02 | One grid, visually distinct, no Google write path | passed (grep-verified no write path; live chip density) | 101-06, 101-07 | `[x]` Complete | **satisfied** |
| UI-01 | Lazy /reminders in navRegistry COMMAND cluster, grouped | passed | 101-06 | `[x]` Complete | **satisfied** |
| UI-02 | Optimistic complete/snooze/quick-add, reduced-motion-safe | passed (WR-06 reconcile fix retested) | 101-06, 101-07 | `[x]` Complete | **satisfied** |

**Score: 9/9 satisfied. Orphans: none** (traceability table lists exactly these 9, all appear in 101-VERIFICATION.md).

## Phase Verification Summary — 1/1 phases verified

| Phase | VERIFICATION | Status | Notes |
|---|---|---|---|
| 101 Reminders & Calendar | exists | **passed** | Initial pass 2026-07-19 (`human_needed`, code-verified) → live-verification addendum 2026-07-20 closed all 5 human items against the running stack (real Ástríðr container, real Google OAuth ×3, real Telegram nudge, real cron ticks) → 101-07 gap-closure re-verified 2026-07-20 (RED `9c246ab` → GREEN `4afabf2`, mutation-verified). 0 blockers, 0 anti-patterns. |

Additional quality gates beyond VERIFICATION:
- **101-REVIEW / 101-REVIEW-FIX:** 8/8 findings fixed (2 Critical + 6 Warning — snooze-kills-nudge, UTC edit shift, double-complete dupe, interval:0 loop, byday interval skip, shared-invite ping-pong, all-day off-by-one, optimistic never-reconciles), each with a regression test.
- **101-UAT:** complete; sole gap (test 8, undated reminders hidden by day filter) closed by 101-07.
- **101-RETEST-UAT (2026-07-21):** 7/7 pass, automated Playwright vs live backend + live astridr cron/chat. Both in-session gaps resolved: stale Convex deployment redeployed + re-probed; NL chat path fixed in astridr-repo (tool_filter cluster + id-in-list + id-error guidance, then tz handling + create_event timeZone), live-verified. Pre-existing astridr log errors (dep_scanner crash-loop, empty heartbeat alerts) also fixed in-session.

## Cross-Phase / Cross-Repo Integration (integration checker, 2026-07-22)

Single-phase milestone — integration = Phase 101 ↔ pre-existing app + codepulse ↔ astridr-repo seams. Fresh static wiring check of the current tree, both repos:

| Check | Result |
|---|---|
| /reminders route + navRegistry + imports (UI-01) | PASS — App.tsx:36,121; navRegistry.ts:124,68; tsc clean |
| http.ts routes all 3 endpoints (REM-02, CAL-01) | PASS — http.ts:28-29,96-101 |
| schema.ts tables + indexes match query usage (REM-01) | PASS — schema.ts:1845-1893; 66/66 tests |
| astridr payloads ↔ Convex handlers, field-by-field (create/snooze/markNotified/calendar_batch) | PASS — all 4 ops match exactly |
| E2E flows: dashboard→astridr read · astridr NL→dashboard · nudge cron→markNotified · calendar cron→overlay | 4/4 WIRED |
| Orphaned exports | 1 — `dueSoon`/`overdue` (see tech debt) |

**Integration score: 8/9 requirement paths fully wired** (REM-05 PARTIAL: works via listByProfile + client-side filter instead of the designed dedicated queries — a design deviation, not a broken flow). **Broken flows: 0.**

## Nyquist Compliance — COMPLIANT

| Phase | VALIDATION.md | Compliant | Action |
|---|---|---|---|
| 101 | exists (`101-VALIDATION.md`, commit `c3cca44`) | **true** (`wave_0_complete: true`) | none |

Closed since the first pass: `101-VALIDATION.md` reconstructed 2026-07-22 from the 7 PLAN + 7 SUMMARY artifacts (State B). 17/17 tasks carry automated verify commands, all 9 requirements roll up COVERED, 0 gaps / 0 escalations, and all mapped suites were re-run green at validation time (codepulse 81/81 across 3 files; astridr-repo 49/49 across 3 files).

## Tech Debt (4 items, all non-blocking)

1. **Orphaned `dueSoon`/`overdue` queries** (`convex/reminders.ts:405-422`) — D-11 design deviation; nudge cron filters client-side instead. Either wire the cron to them or delete them.
2. **Stale/contradictory `CodePulsePoster` rationale** (`calendar_cache.py:138-146`, `cron_dispatcher.py:792-799`) — comments claim the local backend 404s on `/calendar-ingest`; it doesn't, and with `CODEPULSE_CONVEX_URL` unset the poster targets the same URL as `ConvexHandler` anyway. Comment cleanup in astridr-repo.
3. **QuickAdd NL date parsing** — backlog enhancement from UAT secondary findings.
4. **astridr boot-order `persistence.background_error`** — transient, self-heals, deliberately deferred to brain-swap WIP.

## What Was Dropped And Why

The integration checker dropped (a) the CONTEXT.md "consulting enum" risk — no such enum exists in the current implementation (non-finding), and (b) live env-var/OAuth verification — off-limits (.env guard) and already live-verified 2026-07-20/21. This audit reports zero speculative findings; every item above carries file:line or artifact evidence.
