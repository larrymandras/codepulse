# Continue here — session of 2026-08-15/16 (the quality-and-infrastructure thread)

Written 2026-08-16 before an operator reboot.

> **READ THIS FIRST: THIS FILE IS NOT ABOUT PHASE 118's EXECUTION.**
>
> A SECOND SESSION was executing `118-14` in this same checkout while this file was
> written — commits `6e102d38` and `05811a7c` are theirs, and they were still editing
> `hooks/studioThirdLeg.mjs` at 16:03. Phase 118's live state belongs to **`STATE.md`'s
> `stopped_at`** and to that session's plan SUMMARYs, not to this file.
>
> This file covers a *parallel* thread: infrastructure and quality work that ran
> alongside the phase. If the two ever disagree about Phase 118, **`STATE.md` wins.**

## What this thread finished (all committed, working tree clean of my files)

| commit | what |
|---|---|
| `5ea2223a` `de470682` | closed out plan `118-12`, which had been left finished-but-uncommitted |
| `d2179dee` `f1b91803` `5996f7ca` | **plan `118-13`** — D-09's fal.ai leg, proven end to end for $0.003 |
| `d6502fad` | **plan `118-09` closed** — D-14 fired unattended off its real 06:30 trigger |
| `1d230ee7` | **defused a date bomb** in `convex/aggregates.test.ts` |
| `1f93898f` `21e5d844` | **plan `118-15` Tasks 1–2** — `e2e/studio.spec.ts` + `118-GATE-EVIDENCE.md` |
| `06303214` | **made `gsd-state-coherence.ps1` actually check something** |
| `963d3622` | **unblocked the disabled graph-snapshot sweep** |
| several `docs(118)` | STATE.md refreshes, counters re-derived from disk each time |

## Outstanding — needs Larry, in priority order

### 1. The graph-snapshot sweep is fixed but NOT LIVE

`963d3622` is committed and green, but **nothing has been deployed and the cron is still
disabled**. Three steps, in this order, none of them started:

1. `npx convex deploy --env-file C:\Users\mandr\convex-selfhost\selfhosted.envfile`
   — `--env-file` is not optional; a bare deploy can target the retired cloud instance.
   **Check `git status --porcelain` first**: `convex deploy` ships the WORKING TREE, and a
   concurrent session's uncommitted work would go with it.
2. Run the backfill ONCE: `npx convex run internal.graphSnapshots.backfillGraphStoredVersions '{}' --env-file …`
   Read its `windowTruncated` flag. If true, re-run with a larger `window`.
   Until it runs, the sweep **deliberately refuses** and warns — it will not treat an absent
   `storedVersions` as an empty list.
3. Only then re-enable the cron at `convex/crons.ts:145`, and re-read the comment there first.

**This deletes rows on the self-hosted instance.** It is batch-capped at 1,000 per
invocation by design — never raise that to "finish it this time"; that is the mass delete
`CLAUDE.md` forbids, and it is what took the dashboard down for days in 2026-07-21/22.

### 2. Two items Larry approved but that never started

He asked for these in order and only the first of three got done:

- **Chase the intermittent suite failure.** One unidentified failure appeared in a single
  full run on 2026-08-16 and cleared before it could be named; it did not recur across three
  runs. It is **explicitly not claimed as fixed**. `STATE.md` documents parallel-run
  flakiness in the Phase 106 and 115 test files, but "consistent with" is not "identified".
  Standing rule: an intermittent failure is shared-fixture corruption until proven otherwise.
- **Phase 109 follow-up — REPORTED, NOT YET VERIFIED.** `STATE.md` records that Settings'
  `New Profile` writes the `agentProfiles` table while the Agent Profiles card renders
  `profileConfigs`. Verify against the code before acting, and drop it if it does not hold.

### 3. A decision that was never put to him

`e2e/studio.spec.ts` and the graph sweep both went in without a deploy. Nothing in this
thread has been deployed at all. That is deliberate, not an oversight.

## Things that will bite the next session

- **`convex deploy` ships the working tree, not HEAD.** With a second session live in this
  checkout, run `git status --porcelain` immediately before any deploy and say what is dirty.
- **`npx convex run <fn> "$(cat file.json)"` truncates a pretty-printed JSON file to `{`** on
  Windows — the `npx` `.cmd` shim mangles multi-line arguments. Minify to one line.
- **Bash mangles `C:\Users\...` in arguments.** Use forward slashes.
- **`STATE.md` stores several fields TWICE**, in frontmatter and in body prose. Edit BOTH or
  the next `state.*` verb silently reverts your change. Run
  `powershell -File C:\Users\mandr\scripts\gsd-state-coherence.ps1 -Path C:\Users\mandr\codepulse`
  after every edit — as of `06303214` it finally checks the counters instead of self-skipping.
- **The naive `*SUMMARY.md` count now coincidentally equals `total_plans`.** It is still the
  trap; 117 and 119 carry PHASE-level summaries. Use the strict `*-*-SUMMARY.md` pattern.

## What was deliberately NOT done

- **`STATE.md` was not updated at the end of this session.** The concurrent session owns it
  right now — it will be writing plan 14's completion there. Updating it from here would have
  raced them, and the last thing that file needs is two writers. Its `stopped_at` is accurate
  as of `9444f08e` and does not describe the graph-sweep work; fold that in later, calmly.
- **The vault project note was NOT set to `paused`.** CodePulse is not paused — another
  session is executing it. Marking it dormant would have been false on sight.
- **Nothing was deployed and no cron was enabled.** See item 1.
