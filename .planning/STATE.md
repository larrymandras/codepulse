---
gsd_state_version: 1.0
milestone: v14.0
milestone_name: Per-Agent Engine Visibility, Convex Durability & Mission Board
status: executing
stopped_at: Phase 112 plan 112-08 complete (gap-closure: normalized tool_executed.round's explicit-null wire shape, D-14; noFallthroughCasesInSwitch enabled + proven via a discriminating control in both tsconfigs); only 112-07 remains (autonomous:false, operator-gated deploy + live proof) — Phase 112 now 7/8 plans. Phase 115 (workspace-scanner) wave 1 plans 115-01 (POST-helper extraction) and 115-02 (split workspace config + fail-closed loader, D-08/D-17) complete, each in a parallel session; other wave 1-4 plans (115-03/115-04) status not owned by this update; waves 5-6 remain operator-gated.
last_updated: "2026-08-12T16:10:00.000Z"
last_activity: 2026-08-12 -- Plan 115-02 executed (config/workspace.json + gitignored config/workspace.local.json split, D-08/D-17; hooks/workspaceConfig.mjs fail-closed loadWorkspaceConfig/mergeWorkspaceConfig, D-16/D-17; 12 tests, fail-closed contract mutation-proven RED/GREEN twice; full suite 311 files/4133 tests passed, 0 failed; no deploy; commits c4cd72a7/10099e08)
progress:
  total_phases: 12
  completed_phases: 8
  total_plans: 50
  completed_plans: 50
  percent: 67
---

<!-- Counters hand-reconciled 2026-07-24 (gsd-sdk state.*/milestone.complete verbs miscount + clobber — NOT used; Phase 100 close done BY HAND per the established workaround, no gsd-sdk state.*/phase.complete/milestone.complete verbs run).
     ACTIVE MILESTONE = v11.0 (Skills, Phases 97-100): 97 COMPLETE (6 plans), 98 COMPLETE (5 plans), 99 COMPLETE (7 plans = 6 build + 99-07 gap closure), 100 COMPLETE (5 plans: 100-01/02/03/05/04) — completed_plans:23 = 6+5+7+5; total_plans:23; percent by phase = 4/4 = 100.
     v11.0 is now CODE-COMPLETE across all 4 phases. [UPDATE 2026-07-25: v11.0 FORMALLY CLOSED — tagged `v11.0`, `milestones/v11.0-MILESTONE-AUDIT.md` written, archived. The Phase-97 INTAKE-01..04/DAEMON-01/04 traceability rows were reconciled Pending→Complete at that close (see `milestones/v11.0-REQUIREMENTS.md` line ~93) — no code gap, they were delivered by Phase 97 (6/6 plans, live-verified 2026-07-19) and only the checkboxes lagged.] ~~Phase 100's own manual live-Forge-daemon drag verification (archive/move/restore round-trip + honest rollback) remains outstanding, same category as Phase 98/99's own deferred manual checks.~~ **CORRECTED 2026-08-05 (Plan 106-08, per the Stale Docs rule): this sentence was wrong the day after it was written.** `100-HUMAN-UAT.md` Test 1 records the drag round-trip as `PASSED (operator-verified live 2026-07-25)` with concrete detail (`agent-browser` archived onto Cold Storage, the dormant row restored onto Global, and an honest daemon refusal on `test-driven-development` reading "…no longer exists at its source location … Nothing changed on disk" with no false success) — one day AFTER this 2026-07-24 note claimed it outstanding. What was genuinely still open from Phase 100 was its **Test 2**, the CR-02 shadowed-row Cold Storage no-op, marked `UNIT-VERIFIED`/`Accepted` explicitly because "the live catalog contains 0 shadowed-merged skills (every cold skill is dormant-only) — fabricating one was deferred by operator". **Plan 106-08 closed both**: it re-verified the round-trip live on 2026-08-05 (five legs, four `done` command rows plus a genuinely `expired` one, `106-HUMAN-UAT.md` test 8) AND staged the first shadowed-merged row this catalog has ever held to run Test 2's gesture for real (test 9, PASS, with the client-side no-op proven to have fired without the server backstop being reached).
     v12.0 (Reminders & Calendar, Phases 101-102) SHIPPED & ARCHIVED 2026-07-23 (10/10 plans, tagged v12.0) — closed by hand (extract-don't-delete: REQUIREMENTS.md kept live, only the v12.0 section extracted to milestones/v12.0-REQUIREMENTS.md; ROADMAP.md collapsed; audit moved to milestones/). v12.0 scope was Phases 101 (7 plans incl. 101-07 gap closure) + 102 (3 plans, tech-debt close-out).
     NOTE 2026-07-22: this file's WORKING COPY was found reverted to an earlier "executing / Plan 1 of 5" snapshot at the
     start of Plan 98-05's execution (frontmatter status/stopped_at/Current Position all regressed vs. the committed
     HEAD, which correctly showed Phase 98 as milestone_complete/4-4-done) — the same gsd-sdk state.* full-frontmatter-
     resync clobbering bug documented throughout this file's own Decisions history below. Rebuilt from git HEAD
     (`git show HEAD:.planning/STATE.md`) plus this plan's own edits rather than committing the stale regression. -->

# Project State

## Project Reference

See: .planning/PROJECT.md (updated 2026-07-17)

**Core value:** Operators can see the complete operational state of Ástríðr — what's running, what's broken, what it costs — in real time, from a single dashboard, and drive its coding agents from it.
**Current focus:** Phase 115 (workspace scanner) EXECUTING 2026-08-12 — 10 plans in 6 waves; waves 1-4 running, waves 5-6 operator-gated. Phase 112 (telemetry coverage) still in progress — 7/8 plans complete (112-08 done; only 112-07 operator-gated deploy remains); 114, 118 not started

**Phase 109 — per-agent-engine-ui: COMPLETE (10/10, 2026-08-10), VERIFIED `passed` (3/3 must-haves).** All three requirements — ENGINE-03, ENGINE-04, TELE-02 — carry a dated operator sign-off earned against the running stack, never inferred from a green suite. Final live-probe scoreboard A–H: all pass. The operator-attended live gate (109-09) found one real defect that the green suite, a code review and an earlier verification pass had all missed — `useProfileSwap.ts` never reset `unmountedRef` on remount, so React StrictMode latched it and the per-profile swap outcome machine never left `pending` on dev builds (pending suffix never cleared, no toast ever fired; production unaffected). 109-10 fixed it with a regression guard spanning the StrictMode mount→cleanup→remount boundary — proven RED first, and independently mutation-tested by the verifier — then re-verified Probe D live on `:5173` (all four legs, label flip 626 ms AFTER the ack). 109-10 also added `profiles.removeConfig`, closing a real gap: `profileConfigs` rows could be created and never deleted. Evidence: `phases/109-per-agent-engine-ui/109-LIVE-EVIDENCE.md`; verification: `109-VERIFICATION.md`.

**Open follow-ups from Phase 109 (not in its scope, both substantiated):** (1) Settings' `New Profile` writes the `agentProfiles` table while the Agent Profiles card renders `profileConfigs`, so a profile created there succeeds and is then invisible in the section it was created from. (2) `Settings.tsx:239` and `convex/profiles.ts:113` both justify ignoring `agentProfiles` because it has "zero rows in production" — the live self-hosted instance returns **3993**; the rendering decision may still be right but its stated justification is false.

**Next:** v14.0 continues with Phases 110 (Convex Durability), 111 (Mission Board), 112, 113 — each independent of 109.

**LIVE-VERIFY — status 2026-07-27** (a Clerk-signed-in session cleared the auth-gate blocker):

1. **Phase-98 Test 5** — ✅ **VERIFIED LIVE 2026-07-27.** Archive on a skill with a dormant cold copy → toast shows "a dormant copy already exists in cold storage", NOT "Server Error" (CR-03/ConvexError fix `1899d891` confirmed end-to-end; being signed in was load-bearing — `enqueueLifecycle`'s auth guard throws a plain Error that redacts to "Server Error").
2. **Phase-98 Test 4 (shadowed)** — ✅ **VERIFIED LIVE 2026-07-27.** Cold Storage ⋯ menu on a staged shadowed skill (active `claude-code` + dormant `claude-code:available` under one name) showed **Restore disabled** + the shadow tooltip, page did NOT blank (CR-02 holds). Staging cleaned to zero residue.
3. **Chat voice round-trip** — basic STT→native tool→TTS re-verified 2026-07-27 (weather query, clean). The full "Hey Ástríðr" wake → "stop" barge-in → "goodbye" re-arm sequence was NOT re-run this session.

(NOTE 2026-07-27: STATE.md frontmatter was regressed AGAIN by the gsd-sdk resync clobber [v11→v8, counters zeroed to 1/1/13/13]; hand-restored from the committed-HEAD v11.0 values this session per the established workaround.)

## Current Position

Phase: 112 (telemetry-coverage-closure) — IN PROGRESS (7/8 plans; only 112-07 remains, autonomous:false operator-gated deploy). Phase 115 (workspace-scanner) also open — EXECUTING (2/10 plans reported: 115-01 + 115-02 complete, each in its own parallel session; 115-03/115-04 status not owned by this update).
Plan: 115-02 complete this session (split classification config D-08/D-17, fail-closed loader). Phase 112's own position (112-08 complete; only 112-07 remains) is carried forward unchanged from the entry below — this session touched no Phase 112 file.
Status: Plan 115-02 executed 2026-08-12 — see the paragraph below for detail. Phase 112's status as last reported by its own session (unchanged by this plan; this session touched no Phase 112 file): 112-08 complete, only 112-07 (autonomous:false deploy) remains.

Phase 112 status: wave 1 plans 112-01 (cross-repo, astridr-repo) and 112-02 (schema + retention, this repo) executed; wave 2 plan 112-03 (domain modules: governorDecisions.ts + messageRoutes.ts) executed; wave 3 plans 112-04 (ingest routing) and 112-05 (UI surface) executed; wave 4 plans 112-06 (disposition record + drift guard) and 112-08 (gap-closure) executed. 1 plan remains: 112-07 (wave 5, autonomous:false, operator-gated deploy). Phase 113 complete (8/8).
Last activity: 2026-08-12 -- Plan 115-02 executed (config/workspace.json + gitignored config/workspace.local.json split D-08/D-17, 61 local roots all Unclassified per D-16; hooks/workspaceConfig.mjs fail-closed loadWorkspaceConfig/mergeWorkspaceConfig; 12 tests, fail-closed contract mutation-proven RED/GREEN twice — disabling department coercion broke exactly case 6, making the absent-local path return empty roots broke exactly case 2; full suite 311 files/4133 tests passed, 0 failed; no deploy; commits c4cd72a7/10099e08)

Prior activity: 2026-08-12 -- Plan 112-08 executed (convex/runtimeIngest.ts resolveToolExecutionRow's round normalized via normalizeOptional, D-14 — the one CONFIRMED live instance of the defect class 112-04's sweep found, closed; 3 new three-wire-shape tests, mutation-proven RED then GREEN, both outputs quoted in 112-08-SUMMARY.md; noFallthroughCasesInSwitch added to both tsconfig.json and convex/tsconfig.json, proven to fire via a scratchpad flag-off/flag-on control that disagreed (0 errors vs TS7029); full convex/ suite 1509 passed/98 todo/0 failed, full npm test 4121 passed/193 todo/0 failed, no regression; no deploy; commits 13afcadf/05312b01/3846a2d3)

Prior activity: 2026-08-12 -- Plan 115-01 executed (hooks/ingestPost.mjs extracted from hooks/scanner.mjs's inline POST block, D-04; pre-existing wire tests pass unmodified; 6 new direct-contract tests incl. RED/GREEN mutation proof; full suite 310 files/4118 tests passed, 0 failed, no deploy; commits 02175d2a/3ad4a037)

Prior activity: 2026-08-12 -- Plan 112-06 executed (convex/telemetryDispositions.ts GROUP_B_DISPOSITIONS const + convex/telemetryDispositions.test.ts three-layer drift guard, TELE-03/D-10/D-11, mutation-proven against 3 distinct drift classes, full suite 4112 passed/193 todo/0 failed, no deploy, commits 5420f694/f8d27799)

Prior activity: 2026-08-12 -- Phase 115 execution started (waves 1-4 only; blockers manifest deferred waves 5-6 to an attended session)

Prior activity: 2026-08-12 -- Plan 112-04 executed (resolveGovernorDecisionEvent/resolveMessageRoutedEvent + case "governor_decision"/case "message_routed" routed into runtimeIngest.ts dispatch, forwarding to internal.governorDecisions.record/internal.messageRoutes.record, held_reason D-14 null-normalization mutation-proven RED/GREEN, 14 new tests (83->97), no deploy, commits 0e4a3150/60ea4727/ba64bcc3)

**Plan 115-02 complete (2026-08-12).** Built the D-08/D-17 split classification config and its fail-closed loader. `.gitignore` gained a new block (three explicit filenames, not a glob) BEFORE `config/workspace.local.json` existed on disk — `git check-ignore -v` matched all three target filenames pre-creation, with `git check-ignore config/workspace.json` exiting non-zero as the discriminating control. `config/workspace.json` (tracked, public) carries `schemaVersion`/`snapshotId`/`departments`/D-06's exclude sets/a deny-by-default `shareableAllowlist` (D-02, verified programmatically to exclude all 14 measured-leaky extension families)/5 declared roots each with department+evidence (D-14/D-15)/the D-09 `composeFile` path (re-verified live against `astridr-repo/docker-compose.yml:344`). `config/workspace.local.json` (gitignored, never committed) enumerated every top-level directory under `C:/Users/mandr/` per the plan's literal instruction (not narrowed to CONTEXT.md's research-time "~10 ambiguous names" estimate) — **61 local roots, every one `Unclassified`** (D-16), none omitted, none guessed into a real department; `git status --porcelain` on the file produces no output, discriminated against both an untracked probe file in the same directory (which DOES show up) and `git ls-files config/` (lists only `workspace.json`). `hooks/workspaceConfig.mjs` exports `mergeWorkspaceConfig` (pure, no I/O) and `loadWorkspaceConfig` (the only I/O wrapper): local wins on key/root-id collision, an unknown department is coerced to `Unclassified` never a real one, paths normalize to forward slashes, and any local-config failure (absent/malformed/schemaVersion-mismatch) falls closed to tracked-roots-only without ever throwing — a missing/malformed **tracked** config throws by design (the deliberate asymmetry). 12 tests cover all 8 required cases including the passing control (case 1) and the throw-asymmetry (case 8). Per this plan's own verification-discipline mandate, the fail-closed contract was mutation-proven twice, each restored byte-identical (`git diff --stat` empty) before re-observing green: disabling the department-coercion check broke exactly case 6 (11/12 passed); making the absent-local path return empty roots instead of tracked-only broke exactly case 2, D-17's core property (11/12 passed). `npx tsc --noEmit` clean; full suite 311 files passed/17 skipped, 4133 tests passed/193 todo/0 failed (baseline after 115-01 was 310/4118; delta includes this plan's 12 tests plus a concurrent Phase 112-08 session's own additions in the same shared checkout). No deploy, no network call, no scheduled-task change — explicit scope boundary. **Verification-discipline finding, honestly disclosed (not a defect this plan caused):** a literal whole-repo `git grep -F` per-local-root-id check found 23 of 61 ids already present in tracked files, entirely PRE-DATING this plan's two commits (inspected by hand: all 23 are pre-existing, unrelated planning-history or an earlier-session plan-draft mention, none introduced here) — both commits verified via `git show --stat HEAD` to touch only their own intended files, naming zero local roots. The remaining 38 of 61 ids have no pre-existing tracked-file occurrence — the genuinely first-appearance names the `.gitignore` protects going forward. Two directory names (a Windows package-manager root and a hardware-vendor artifact folder) were excluded from the 61 via judgment rather than the plan's literal named exclusion list, on the grounds that both are structurally identical in kind to entries that ARE named (`.npm`/`.cargo`/`.nuget`). Task 2 produced no git commit — its only file is gitignored by design, nothing to stage. Each of the two remaining commits was verified via `git show --stat HEAD` to touch exactly its intended files; a concurrent Phase 112-08 session's own staged files (visible mid-task in the shared index) were never added or committed by this session and were confirmed, after the fact, to have been committed independently by that session (`eb084397`), interleaved cleanly with this plan's own two commits. See `115-02-SUMMARY.md`.

**Plan 112-08 complete (2026-08-12).** Gap-closure plan, added mid-execution after 112-04's adversarial verification confirmed a live silent-drop on `tool_executed.round`. Task 1: confirmed the premise live (bare `round: d.round` at `runtimeIngest.ts:94`, `toolExecutions.round` is `v.optional(v.float64())`), then changed it to `round: normalizeOptional(d.round)`, reusing the existing `isOptionalNumber`/`normalizeOptional` guard pair verbatim — no second guard pair written. `durationMs`/`traceId` were deliberately left untouched (their `??` chains already resolve `null` to `undefined` since astridr sends only the camelCase key), confirmed via `git diff` showing only the `round` line and comments changed. Task 2: added a `112-08` `describe` block to `runtimeIngest.test.ts` (100 tests, up from 97) covering round's three live wire shapes — numeric, explicit-null, absent — mirroring 112-04's `held_reason` three-wire-shape structure including the `:null`-absent serialization assertion. The explicit-null test was mutation-proven: reverting `normalizeOptional(d.round)` to a bare `d.round` made exactly that test fail (99/100 passed), restored byte-identical (empty `git diff --stat` against the committed Task-1 state), re-observed 100/100 green — both runs quoted verbatim in `112-08-SUMMARY.md`. Task 3: wrote a scratchpad control file (a switch with a genuine fallthrough) and compiled it with `./node_modules/.bin/tsc --ignoreConfig` twice — flag-off exited 0 with no output, flag-on produced `TS7029: Fallthrough case in switch` and exited 2 — proving the two runs disagree before trusting either. Added `"noFallthroughCasesInSwitch": true` to both `tsconfig.json` and `convex/tsconfig.json` (single-line additions each); both projects type-checked clean (0 TS7029), matching the plan's measured pre-change baseline. `npx tsc --noEmit` clean throughout; full `convex/` suite 1509 passed/98 todo/0 failed (baseline 1506, +3 matching this plan's new tests, no regression); full `npm test` 4121 passed/193 todo/0 failed. No deploy was run (reserved for 112-07, operator-gated). Each of the three task commits was verified via `git show --stat HEAD` to touch exactly its intended file(s) — no foreign files swept in from the concurrent Phase 115 session (whose own commit `c4cd72a7` landed interleaved in `git log` and was left untouched). `REQUIREMENTS.md`'s TELE-03 checkbox and traceability row were moved to Complete (attributed to both 112-06 and 112-08, since both plans' frontmatter independently list `requirements: [TELE-03]`, and 112-06's own SUMMARY had already claimed `requirements-completed: [TELE-03]` while the checkbox had never actually been updated). See `112-08-SUMMARY.md`.

**Plan 115-01 complete (2026-08-12).** Extracted `hooks/scanner.mjs`'s inline POST-with-bearer block (`:220-241` pre-extraction) into a standalone `hooks/ingestPost.mjs` module exporting `postSnapshot(endpointUrl, ingestKey, body, deps)` — the D-04 extraction, and the highest-risk edit in Phase 115 since it touches the awaited SessionStart path (`hooks/codepulse-hook.mjs:142-145` does `await runScan(...)` inline). The extraction landed on the first attempt — the plan's sanctioned copy-instead-of-extract fallback was not needed. Behavior preservation was proven three ways: (1) the pre-existing regression net, `hooks/__tests__/scanner.test.mjs` (3 tests, real `node:http` server, asserts on actual POST body bytes), passed **unmodified**, confirmed absent from `git diff HEAD --name-only`; (2) 6 new direct-contract tests in `hooks/__tests__/ingestPost.test.mjs` cover bearer-present, bearer-absent (control), non-2xx, unreachable-endpoint, timeout, and caller-owned-path cases; (3) a RED/GREEN mutation proof — `postSnapshot`'s catch block was temporarily changed to `throw err;` instead of returning `{ok:false,status:null}`, observed RED (exactly the two load-bearing non-throwing-contract tests failed, 4/6 still passed), restored byte-identical (`git diff --stat` empty), re-observed GREEN (9/9 combined with the pre-existing suite). `hooks/scanner.mjs`'s diff shows a net line reduction (23 deletions / 2 additions) with nothing else changed, confirmed via the plan's own acceptance-criteria greps (`fetch(` count 0, `AbortController` count 0, one static import line). Full suite green: 310 files passed, 17 skipped, 4118 tests passed, 0 failed (one transient Vitest worker-fork crash on an earlier run, unrelated to this change, did not reproduce on a clean re-run). `npx tsc --noEmit` clean. Manual sanity (`node hooks/scanner.mjs manual-scan --dry-run`) confirmed the untouched `isDirectRun` branch still works (269 skills, 4 covered origins). Threat mitigations T-115-01-01 (log only status/response-text, never the request body) and T-115-01-02 (`ingestKey` appears only in the `Authorization` header value, never logged or returned) verified by direct code inspection. No deploy, no network call to the live backend, no scheduled-task change — this plan's explicit scope boundary. See `115-01-SUMMARY.md`.

**Plan 112-06 complete (2026-08-12).** Built the TELE-03 disposition record (D-10/D-11): `convex/telemetryDispositions.ts` exports `GROUP_B_DISPOSITIONS`, a zero-import plain const (matching `controlVerbSwapsFilters.ts`'s dependency-free precedent) recording all 7 remaining Group B kinds — 3 routed (`governor_decision`→`governorDecisions`, `control_verb_swap`→`controlVerbSwaps`, `message_routed`→`messageRoutes`) and 4 generic-table-by-design (`prompt_assembly`, `structured_output_exhausted`, `vision.capture`, `control_verb_focus`). The file's docstring records D-01's bar (live arrival inside the 14-day window, control-paired), D-02's re-measurement recipe (`events:listByType` bounded, never `events:countByType`, paired against `llm_call` known-present/`definitely_not_a_real_kind_9x7q2` known-absent), D-03's scope caveat (worded without the literal banned phrases so the guard's own negative source-text assertion does not trip on the docstring describing the rule), D-05's resolved low-volume gate, and D-12's deliberate deferral of an astridr-repo-side emitter probe. `convex/telemetryDispositions.test.ts` implements the plan's three-layer drift guard exactly as specified: Layer 1 harness-liveness (both source parsers proven >20 members with known-present/known-absent controls before any downstream assertion), Layer 2 per-kind explicit assertions plus two-directional set equality against an independently-declared canonical list, plus routed↔case and generic↔no-case cross-checks against the live `runtimeIngest.ts` dispatch switch and `schema.ts` table set, and Layer 3 source-text assertions for D-11's date/D-12's deferral and D-03's caveat enforcement — 16/16 tests. All three required mutations were run, one at a time, fully restored between each, with the restore verified byte-identical via `diff` against a pre-mutation scratchpad backup before re-running: **Mutation A** (deleted the `governor_decision` entry) failed exactly the set-equality assertion and the per-kind `governor_decision` assertion (2 failed, 14 passed); **Mutation B** (rewrote `prompt_assembly`'s reason to "MUTATION-PROOF: never emitted...") failed the Layer 3 negative source-text assertion plus the caveat-citation assertion (2 failed, 14 passed); **Mutation C** (flipped `message_routed`'s disposition to generic-table-by-design while leaving its live `case "message_routed":` untouched in `runtimeIngest.ts`) failed the generic-kinds-have-no-case cross-check plus two corroborating assertions (3 failed, 13 passed) — the plan's required signal. Each restore was re-observed at 16/16 green, and `convex/runtimeIngest.ts` was confirmed never actually modified (diff against a pre-mutation backup was empty throughout). Full verbatim run logs in `112-06-SUMMARY.md`. `npx tsc --noEmit` clean throughout; full suite 4112 passed/193 todo/0 failed (baseline 4096, +16 matching this plan's new tests exactly, no regression). No deploy was run (reserved for 112-07, operator-gated). **Shared-checkout note:** `convex/_generated/api.d.ts` regenerated automatically (a live `convex dev` codegen watcher picked up the new source file) and was staged for its own commit, but the concurrent Phase 115 session's commit (`1391104f`) landed in the same instant and swept it in — content correct, commit attribution only. Not corrected via history rewrite per this project's standing prohibition; documented here and in the SUMMARY. See `112-06-SUMMARY.md`.

**Plan 112-04 complete (2026-08-12).** Wired `governor_decision` (D-04) and `message_routed` (D-05/D-13) into the `runtimeIngest` dispatch. `resolveGovernorDecisionEvent` requires non-empty-string `emitter`/`priority` and a real boolean `spoke` (checked via `typeof`, never truthiness), and normalizes `held_reason` through `isOptionalString`/`normalizeOptional` before forwarding to `internal.governorDecisions.record` — the D-14 fix, load-bearing because 424 of 646 live held rows carry `held_reason` as an explicit JSON `null`, which `v.optional(v.string())` rejects outright. `resolveMessageRoutedEvent` requires `channel`/`profile` and applies the same null carve-out to `sender`/`session_id` before forwarding to `internal.messageRoutes.record`; routed because it was measured low-volume (10 rows/14-day window) per D-05's resolved gate, deliberately without a UI surface this phase (D-13) — no `src/` file was touched. Both resolver refusals increment `skippedCount` and log a named warning. `runtimeIngest.test.ts` grew from 83 to 97 tests: three-wire-shape coverage for `held_reason` (explicit string / explicit null-majority / key-absent) plus wrong-typed-optional and required-field controls (including a `spoke: "true"` string control, which a truthiness test would have wrongly accepted), an equivalent smaller block for `message_routed`, and static case-wiring source checks for both. The D-14 null-normalization was mutation-proven: removing `normalizeOptional(` from `held_reason`'s return line made exactly the explicit-null test fail (1 of 97, 96 passed, every other assertion on the same resolver stayed green), restored byte-identical, re-observed 97/97 green. A repo-wide sweep for the same defect class (any `v.optional(v.string())`/`v.optional(v.float64())` field forwarded from `runtimeIngest.ts` without the null carve-out) found 5 further unguarded sites in this same file (`kg_benchmark`'s `workflowRunUrl`, `task_quality`'s `idempotencyKey`, `parseToolPolicyEvent`'s `tool`/`sessionId`/`agentId`/`taskCategory`/`round`/`field`, and both tool-execution resolvers' `durationMs`/`traceId`/`errorMessage`) — none has a confirmed live-null incident behind it, so they were logged to `deferred-items.md`, not fixed (out of this plan's D-04/D-13/D-14 scope). `npx tsc --noEmit` clean; full `convex/` suite (79 files, 1490 tests, 1476 baseline + 14 new) passed with no regression. No deploy was run (reserved for 112-07, operator-gated). Each of the three task commits was verified via `git show --stat HEAD` to touch exactly its intended file — no foreign files swept in from the concurrent Phase 115 session. See `112-04-SUMMARY.md`.

**Plan 112-03 complete (2026-08-12).** Built the Convex-side write/read modules for both Phase 112 tables. `governorDecisions.ts` exports `record` (`internalMutation`, append-only, args match `schema.ts` field-for-field) and `listRecent` (`query`, bounded `.take(GOVERNOR_DECISION_CAP)` over `by_timestamp`), with `GOVERNOR_DECISION_CAP = 50` in a sibling dependency-free `governorDecisionsFilters.ts` (zero imports, mirrors the `controlVerbSwapsFilters.ts` 108-06 client-bundle-defect precedent) so plan 05's component can import the cap without pulling in the Convex server runtime. `messageRoutes.ts` has the same `record`/`listRecent` shape but declares `MESSAGE_ROUTE_CAP = 50` in-file (no filters split — no browser consumer this phase, D-13 deliberately unsurfaced); `listRecent` is kept as plan 07's live post-deploy probe target, not dead code. Both `record` mutations are `internalMutation`, never `mutation` (CR-01), closing the client-callable `api.` namespace per this repo's SEED-008 standing rule. 24 new guard tests (12+12) cover CR-01 (with a raw-file vacuity control), the bounded-read shape, and live-validator args assertions read via `record.exportArgs()`. The CR-01 guard was mutation-proven live: `record` was temporarily downgraded to a public `mutation()`, observed RED (exactly the 2 CR-01 assertions failed, 10/12 passed), restored byte-identical (`git diff --stat` empty), re-observed GREEN (24/24). `convex/_generated/api.d.ts` regenerated via bare `npx convex codegen` (`--env-file` rejected as an unknown option on this subcommand, the plan's documented fallback fired) — +6 lines, alphabetically correct, naming all three new modules; `api.js` needed no edit (uses `anyApi`). `npx tsc --noEmit` clean; full `convex/` suite (79 files, 1476 tests, 1452 baseline + 24 new) passed with no regression. No ingest wiring, no UI — explicit scope boundary per the plan, deferred to 112-04/05. No deploy was run (reserved for 112-07, operator-gated). Each of the four commits was verified via `git show --stat HEAD` to touch exactly its intended file(s) — no foreign files swept in from the concurrent Phase 115 session (6 untracked `115-0N-PLAN.md` files present throughout, correctly left untouched). See `112-03-SUMMARY.md`.

**Plan 112-02 complete (2026-08-12).** Added `governorDecisions` (D-04) and `messageRoutes` (D-13) domain tables to `convex/schema.ts` (both at two-space indentation, matching `retention.test.ts`'s `schemaTables` regex) and bounded both in `RETENTION_DAYS` in the same session (`governorDecisions: 30`, `messageRoutes: 90`, D-06), each with a dated in-line reason recording the measured volume and read-pattern tier. `retention.test.ts` gained a specific-value `toHaveProperty` drift assertion for both new keys and a `schemaTables` presence check, and the new test was mutation-proven: deleting `governorDecisions: 30` was observed to fail exactly that assertion (14/15 passed, the new test the sole failure), the file was restored byte-identical (`git diff --stat` empty) and re-observed green (15/15). `npx tsc --noEmit` clean; full `convex/` suite (77 files, 1452 tests) passed with no regression. No ingest, queries, or UI added — explicit scope boundary per the plan, deferred to 112-03/04/05. No deploy was run (reserved for 112-07, operator-gated). Each of the three task commits was verified via `git show --stat HEAD` to touch exactly its intended file — no foreign files swept in from the concurrent Phase 115 session. See `112-02-SUMMARY.md`.

**Plan 112-01 complete (2026-08-12).** Cross-repo doc-only plan: `astridr-repo/docs/astridr-contract.md` §2.20-§2.24 (`instructions_loaded`, `loop_lifecycle`, `worktree_lifecycle`, `batch_execution`, `auto_memory`) each gained a dated `NOT EMITTED - aspirational` banner in place (D-07/D-09), citing v1.6.0/2026-03-09/`docs/new_claude_capabilities.md`; section numbering §2.19-§2.40 verified unchanged. The three critical-events rows for those same three unfirable kinds (`worktree_lifecycle`, `batch_execution`, `loop_lifecycle`) were removed from the operator-alerting table (D-08), leaving `mcp_connection`/`subagent_job` intact. Task 1's zero-emitter claim was control-paired against `governor_decision` (a real emitter) in the same search run; a bare-substring hit on `worktree_lifecycle` (a pytest function name, not an emitter) was investigated and resolved with a stricter quoted-string-literal search before any prose was written, per this project's verification-discipline rules. Committed once in astridr-repo (`7f61ba1d554568264bdd55797890cd0b9c00a31c`, `feature/brain-swap`, branch not switched), `git show --stat HEAD` confirmed to touch exactly one file. No CodePulse source file was modified. See `112-01-SUMMARY.md`.

**Phase 112 — telemetry-coverage-closure: PLANNED 2026-08-12.** 7 plans across 5 waves, gsd-plan-checker returned **0 blockers, 0 warnings**; requirements TELE-01 + TELE-03 both covered; the blocking decision-coverage gate returns `total: 14, covered: 14, skipped: false` for D-01..D-14 (run with both positional args — the one-arg form self-skips to a false green). Wave shape: 112-01 (astridr-repo contract banners, doc-only, different repo) ‖ 112-02 (schema tables + retention) → 112-03 (domain modules) → 112-04 (ingest routing) ‖ 112-05 (UI surface) → 112-06 (disposition const + drift guard) → 112-07 (**`autonomous: false`** deploy + live proof). **D-05's volume gate was measured and resolved during planning, changing the phase's scope:** `message_routed` returned **10 rows in the 14-day window** (~0.7–1.2/day) against `governor_decision`'s **1,168 rows / 83.3 per day**, both uncapped at `limit: 2000`, control-paired in the same run (`llm_call` present → 1,261; a bogus kind → 0) and unit-checked against wall clock (epoch **seconds**). Measured by the researcher and independently re-run by the orchestrator before being written down. So `message_routed` is low-volume, not a firehose: it is **routed but deliberately NOT surfaced** this phase (new D-13), because the approved `112-UI-SPEC.md` designed only the `governor_decision` surface and the UI-SPEC itself warns its routing-metadata fields are not a reskin of that one. Planning also surfaced a live-data defect CONTEXT.md did not have: `governor_decision.held_reason` arrives as an explicit JSON `null` on **424 of 646** held rows, and `v.optional(v.string())` rejects an explicit `null` outright — **the identical mechanism that made Phase 108's `control_verb_swap` route land zero rows while reporting `dropped: 0`**. New D-14 requires the `isOptionalString`/`normalizeOptional` pair (`runtimeIngest.ts:207-268`) in the same commit that adds the dispatch case, with tests over all three wire shapes. 112-07 accordingly treats an empty domain table as a pre-declared FAILURE, and records `message_routed`'s end-to-end proof as **OPEN** rather than passed, since at ~1 row/day nothing will arrive in the verification window. **Counter note:** `gsd-sdk query state.planned-phase` was run and produced the documented clobber — it regressed `completed_phases` 8→6 and *inflated* `completed_plans` 42→44 although nothing was executed (planning is not completion, which is the tell on its own), while its own JSON claimed it touched only "Status" and "Last Activity". STATE.md was restored from HEAD and only `total_plans` (42→49, +7) plus this paragraph were re-applied by hand, in **both** the frontmatter and body copies. `completed_plans: 42` and `completed_phases: 8` re-derived from disk: 49 `*-PLAN.md`, 42 plan-level `*-SUMMARY.md`, 8 `*-VERIFICATION.md`. Next: `/gsd-execute-phase 112`.

**Phase 115 — workspace-scanner: PLANNED 2026-08-12.** 10 plans across 6 waves; gsd-plan-checker returned **0 blockers, 0 warnings**. Traceability runs through **D-NN decisions, not REQ-IDs** (ROADMAP says `Requirements: TBD`), following Phase 116's precedent: the blocking decision-coverage gate returns `total: 17, covered: 17, skipped: false` — re-run independently by the orchestrator with **both** positional args, and control-paired against Phase 112, which returned its own documented 14/14, so the green is evidence the gate evaluated rather than self-skipped. Wave shape: 115-01 (POST-helper extraction) ‖ 115-02 (split config + fail-closed loader) ‖ 115-03 (D-12 gate + mutation test) ‖ 115-04 (versioned tables + inline prune) → 115-05 (classifier + compose access) ‖ 115-06 (ingest route) → 115-07 (read-incapable walk + dry-run report) → 115-08 (entry point + D-12 case 5) → 115-09 (**`autonomous: false`** deploy + attended review + live proof) → 115-10 (**`autonomous: false`** nightly task). Wave 1's four plans have mechanically-verified zero `files_modified` overlap.

**Three decisions were added during plan-phase and one had its premise corrected.** **D-15/D-16** resolve what CONTEXT.md left open: the vault, `.claude` and `.claude-alt` map to a single root-level **Unclassified** department rather than a guessed one (they demonstrably mix all three contexts), and every ambiguous root ships as a **declared** root mapped to Unclassified — nothing guessed into a real department, nothing invisible. **D-17 amends D-08** on measured evidence: `larrymandras/codepulse` is a **PUBLIC** repo (`gh repo view` → `visibility: PUBLIC`) and 7 of ~10 root names appeared in **zero** tracked files at HEAD, so committing the root list would have been a new, permanent, one-way disclosure. Config is therefore split — tracked rules file plus gitignored `config/workspace.local.json`, loader **fails closed** on a missing/malformed local half, `.gitignore` updated in the same task that creates it. The names were redacted from RESEARCH.md and CONTEXT.md, verified with positive controls. **D-04's premise was wrong in two measurable ways** and both changed implementation without changing its conclusion: `hooks/scanner.mjs` is **not** named in either `settings.json` (control: `hooks` appears 83×/81× in those files) — SessionStart reaches it indirectly via `hooks/codepulse-hook.mjs:142-145`, which **awaits** `runScan` inline, making the constraint *stronger*; and `scanner.mjs` exports exactly one symbol, `runScan` (`:33`), with the POST block inline at `:220-241`, so "import shared helpers rather than copy them" required an extraction refactor first. That extraction is now 115-01, gated on the pre-existing `scanner.test.mjs` staying green unmodified, with copy-instead-of-extract as the sanctioned fallback.

**The planner corrected five defects in its own source artifacts, each independently re-verified against live code by the checker.** `js-yaml` is 5.2.3 with **no default export** (`import yaml from "js-yaml"` throws in an `.mjs`; `safeLoad` is `undefined`) — named `import { load }` required. The compose service keys are `astridr` and `cli-gateway`, **not** `astridr-agent` (that is only a `container_name:`), and `.claude-alt`/`.claude.json` appear only under `cli-gateway` as `~`-relative paths, so without `~` expansion `.claude-alt` wrongly reads `local-only`. RESEARCH.md's `canonicalReportHash` sketch was **defective in a way that would have made D-12's gate pass without evaluating anything** — `JSON.stringify(report, Object.keys(report).sort())` treats the replacer array as a key sorter when it is a key *allowlist applied at every nesting level*; reproduced live, two reports differing in a nested field serialized byte-identically. Replaced with a recursive canonical serializer plus a nested-mutation regression guard. And Assumption A2's timeout risk was **eliminated rather than accepted**: the prune reads `storedVersions` from the meta doc instead of collecting every entity row — that collect is the exact read that disabled `sweepGraphSnapshotVersions` (`crons.ts:145-151`). **Counters deliberately untouched:** `state.planned-phase` was NOT run — Phase 115 sits outside v14.0's phase set (114-119 follow the v14.0 Progress table), and that verb produced the documented clobber for Phase 112 earlier the same day. Next: `/gsd-execute-phase 115`.

**Superseded record — Phase 115 CONTEXT GATHERED 2026-08-12 (`115-CONTEXT.md`, then 14 decisions, now 17).** Picked up as parallel work alongside a concurrent session's Phase 112. Produces the data Phase 114's map renders. **The ROADMAP's "115 depends on 114" was the dependency INVERTED and is corrected in this commit** — the approved design's own graph (`Mandras/02-projects/agentic-os-second-brain.md:48`) reads "C2 → enables C1's workspace lens", so the scanner comes first; the old line was a sequential default from `gsd-phase add`. Three decisions compose into a producer that structurally cannot leak, which matters because it walks the vault, `.claude` and `.claude-alt`: payload is path + metadata and **never contents** (the donor's only content read, `scan.js:149`, is explicitly dropped), secret detection is **deny-by-default per root** rather than a secret-shaped regex, and secret paths are **omitted entirely** rather than flagged. The regex decision is measured, not preferred — the donor's `SECRET_RE` returned **PUBLIC** for `selfhosted.envfile` (holds `INSTANCE_SECRET`), `.claude.json` (inline Bearer token) and `.mcp.json`. Storage changed on evidence found mid-discussion: `graphSnapshots` looked like the obvious reuse, but `crons.ts:145-151` has its retention sweep commented out — `DISABLED 2026-07-14 — times out on self-hosted Convex` — so 115 gets its own tables and bounds growth by an **inline batch-capped prune at ingest**, bounded by construction rather than by a schedule that can be silently disabled. Granularity is grounded in a count: **21,029 files** across `.claude`/`.claude-alt`/vault before any repos, so directories are nodes and files are counts. Also recorded: `schtasks /query` returns zero lines from this environment and is **proven broken by a control** (it cannot find `ConvexNightlyRestart` either), so scheduled-task state is unverified, not absent. Next: `/gsd-plan-phase 115`.

**Phase 113 — debt-sweep: COMPLETE (8/8, 2026-08-12), VERIFIED `passed-with-amended-criterion` (`113-VERIFICATION.md`).** Two of three requirements delivered outright; the third was closed honestly rather than counted as met. **DEBT-05** is a fail-closed guard, not a widened tolerance — `skillSync.ts:142` returns zero prunes unless the producer positively asserts a complete scan (`scannedOriginsComplete === true`, exact-`true` per `:45`, so a malformed snapshot cannot engage pruning), killing the 185 → 131 → 185 oscillation; 34 tests pass. **DEBT-07** verified against the live directory rather than 113-08's own claim: `convex-selfhost/` is a git repo at `880befa`, the **committed** `docker-compose.yml` carries the `logging:` block (json-file, 10m, 3 files), and `restart-convex.ps1` plus 13 sibling scripts are tracked with a clean tree. **DEBT-06 is closed GUARDED and the cause was never identified** — the brain-pill flake did not reproduce across **80 clean full-suite iterations** (tiered 30 + 50, the budget Larry set), each tier preceded by a fresh positive control proving the detector's FAIL path still fired, so no capture ever existed to diagnose from and D-11 forbids fixing without one. The ROADMAP's criterion 2 and `REQUIREMENTS.md`'s DEBT-06 line were both **amended** to say so — a deliberate, visible admission, not a silent green. The `waitFor` half of the bar WAS met and is asserted mechanically: `Chat.test.tsx` untouched, `waitFor` count unchanged at 23, no added `{ timeout:`. The defect remains latent; 113-05's query-site instrumentation makes the next occurrence self-diagnosing. One unattributed finding carried forward: `src/App.test.tsx`'s `/memory` lazy-route timeout, seen once in 86 runs, with the machine-contention hypothesis overturned by tier1b's own slower-but-passing 93s iteration. Full record: `113-FLAKE-EVIDENCE.md`.

**Counter correction, same session (2026-08-12).** `completed_plans` was **36** and `completed_phases` **5**; both were re-derived from disk ground truth to **42** and **8**. Derivation: `.planning/phases/` holds exactly 42 `*-PLAN.md` (matching the already-correct `total_plans: 42`) and 42 plan-level `*-SUMMARY.md` — every plan in the milestone has a summary. `completed_phases: 8` = the 7 phases carrying a `*-VERIFICATION.md` (108, 109, 110, 111, 116, 117, 119) plus 113, verified this session. The two remaining `*-SUMMARY.md` on disk (44 total) are **phase-level** summaries for plan-less phases 117 and 119, not orphans — `gsd-state-coherence.ps1` counts all 44 and compares `total_plans` against the *current phase dir* only, so its findings 2 and 3 are scoping artifacts, not defects. Its finding 1 **was** real and is fixed here: `last_updated` disagreed between frontmatter (`16:52:36`) and body (`16:30:05`), the half-edit that silently reverts on the next `state.*` verb — both copies now carry the same value. No `gsd-sdk state.*` verb was run; every field above was hand-edited in both its frontmatter and body copy.

**Phase 113 — debt-sweep: PLANNED 2026-08-11 (8 plans, 3 waves).** Research ran the controls D-08 mandated and two of them changed the plan. Control 1 confirmed Vitest *does* print the actual `textContent` on the `Chat.test.tsx:585-586` `toBe` failure (`Received: "anthropic-sonnet-5"`), so the deferred item's "unobtainable" framing was wrong. Control 2 **falsified D-08's own mechanism**: `@vitest/runner` always runs `afterEach` before `onFailed`, and `@testing-library/react` auto-registers `afterEach(cleanup)` because `vitest.config.ts:14` sets `globals: true` — so the global `src/test/setup.ts` DOM-dump D-08 specified would have captured an empty `<body>` forever. D-08 was amended on disk; capture moves to the query site. Three decisions were added at Larry's call: **D-17** folds the frontend origin-coupling fix into DEBT-05 (8 sites across 4 files — `Skills.tsx:132,148` filter the Global tab with an exact `.includes("claude-code")`, so D-02 alone would silently drop ~57 plugin skills from the tab; `skillVault.ts:12` is safe by design and must not be touched), **D-18** parameterizes the live `INSTANCE_SECRET` at `docker-compose.yml:89` before DEBT-07's first commit with rotation explicitly out of scope, and the soak budget was set from a measured 37.98s full-suite run to a tiered 30-then-50 iterations. Plan-checker returned VERIFICATION PASSED (1 non-blocking cosmetic warning, since closed). **The decision-coverage gate self-skipped** (`passed: true` but `skipped: true, total: 0` — CONTEXT.md's whole-title-bolded `- **D-01: …**` style parses to zero decisions), so D-01…D-18 coverage was verified by hand instead, with a `D-99` control returning 0 to prove the grep discriminates. Two planner findings no source artifact named: the origin split defeats `hooks/skillScan.mjs:161`'s `${origin}::${name}` dedup key, and `hooks/__tests__/skillScan.test.mjs:139`'s `origin === "claude-code"` filter would have gone **vacuous rather than red** — both independently confirmed and folded into 113-01.

**Phase 111 — mission-board: COMPLETE (3/3, 2026-08-11), VERIFIED `passed` (3/3 must-haves).** The phase was deliberately subtractive: `JobsPanel` became a truthful post-hoc history board and `ActiveAgentsPanel` was deleted outright, so no surface asserts a mission state `subagentJobs` cannot back. `JobsPanel` now renders `MISSION HISTORY` / `{n} missions` / `finished X ago` with the day tier that was missing (the one real 34-day-old row rendered `816h` before), the `stateIcon` map carries only the three terminal statuses, and the seconds-epoch guard survived the rewrite. **The blocking operator checkpoint was actually run** — live against `localhost:5173` on 2026-08-11, verdict `approved`, with screenshot evidence for all nine legs including zero console errors on both pages. Notably absent from those consoles: "Functions are not valid as a React child", the exact symptom T-111-02 predicted for the unguarded `stateIcon[job.status]` prototype-chain lookup. Code review returned 0 critical / 2 warnings, **both fixed in-session** with mutation-proven tests rather than deferred (`111-REVIEW.md` status: resolved) — the day-boundary mutant `h < 24 → h < 240` had been invisible to the pre-existing 34-day test because 816h sits outside the corrupted window. Full suite 3951 passed | 0 failed; `npx tsc --noEmit` exit 0. Two checkpoint observations were routed to backlog rather than patched: 80-char task-snippet truncation with no ellipsis (traced to deliberate upstream astridr policy across four call sites → SEED-007 item 7) and an unopened devtools "1 Issue" badge (→ `todos/pending/111-devtools-issues-panel-entry-unexamined.md`, explicitly NOT classified as benign). Evidence: `111-VERIFICATION.md`, `111-REVIEW.md`, `111-03-SUMMARY.md`.

**Counter reconciliation (2026-08-11, Phase 111 close) — read this before trusting or "fixing" the frontmatter numbers.** `gsd-sdk query phase.complete 111` wrote `completed_plans: 36` against `total_plans: 34` — an impossible counter, and the cheapest possible tell that something is being counted wrong. Cause: it counts SUMMARY.md files (36) against a PLAN.md-derived total (34). Phases 117 and 119 each have a SUMMARY.md but **zero** PLAN.md files (quick-shaped work), so they inflate the numerator only. Re-derived from disk: phases holding PLAN.md files are 108=7, 109=10, 110=6, 111=3, 116=8 → **34 plans, 34 summaries**, so `total_plans: 34` / `completed_plans: 34` is the coherent pair. `completed_phases` was also stale at 3: it matched the ROADMAP checklist, but the checklist itself lagged — **109 and 116 were both complete and `VERIFICATION.md`-passed with full plan/summary parity, yet unticked.** Both were ticked at this close with their evidence inline, giving `completed_phases: 5` (108, 109, 110, 111, 116) and `percent: 42`. **Phases 117 and 119 were deliberately NOT ticked** despite carrying `VERIFICATION.md status=passed`: 119 is actively in flight in a concurrent session — its own commit `73816fb1` records `LOOM_API_KEY` as unset with the paired 200 control still outstanding — and ticking it would be exactly the premature-green this phase existed to remove. Derive these numbers from disk, never from the previous value ± 1.

**Plan 111-02 complete (2026-08-11).** Deleted `ActiveAgentsPanel` outright (component, test, import, mount, `SectionErrorBoundary` wrapper) — it filtered `subagentJobs` on `status === "running"`, a state the table structurally never receives, so it rendered the unfalsifiable "No agents running." permanently (D-07, MISSION-03). `cc-left-rail` now has one child, `IntelligenceFeedPanel`; `SectionErrorBoundary` count in `Chat.tsx` went 17→15 (verified before editing). `Chat.test.tsx`'s seven-panel claims repaired: `SEVEN_PANEL_LABELS` renamed to the count-free `COMMAND_CENTER_PANEL_LABELS` with `ACTIVE AGENTS` dropped, doc comment and three test titles now say "six"; the label array's mutation control was run and captured RED (`ACTIVE AGENTS` re-added → mode-ON test failed with `Unable to find an element with the text: ACTIVE AGENTS`) before being reverted to green. Whole-tree `grep -rn "ActiveAgentsPanel" src/` and `grep -rn "ACTIVE AGENTS" src/` both return no matches. Full suite 297 test files passed | 17 skipped, 3943 tests passed | 193 todo, 0 failed; `npx tsc --noEmit` exit 0. One plan-scope correction (Rule 3): the `cc-left-rail` comment at `Chat.tsx:1028-1033` named the deleted panel in prose and was reworded (no CSS/class change) to satisfy the plan's own whole-file grep acceptance criterion. Executed sequentially on `master`, no worktree — two concurrent sessions (Phase 110-06, Phase 119/Loom) committed unrelated work to this same branch in the same window; neither was touched. See `111-02-SUMMARY.md`.

**Counter note (2026-08-11, this session):** `completed_plans` moved 32→33, crediting only this plan's verified completion — NOT derived from the raw on-disk SUMMARY.md count (34), because one of those two new files (`110-06-SUMMARY.md`, added by a concurrent session in this same window) is an interim checkpoint summary ("Tasks 1-2 summary, Task 3 checkpoint pending" per its own commit message and content), not a completed plan. A future session closing out 110-06 should reconcile this properly once that plan actually finishes.

> **↑ SUPERSEDED at Phase 111 close (2026-08-11).** That reconciliation has now happened — 110-06 finished in the concurrent session, so its summary is no longer interim. See the **Counter reconciliation** block below for the authoritative derivation; do not carry the 32→33 reasoning forward.

**~~Phase 110 is NOT closed — 110-06 (wave 5) is still outstanding.~~ CORRECTED 2026-08-11 at Phase 111 close: Phase 110 IS now closed.** A concurrent session finished 110-06 while Phase 111 was executing. Ground truth: ROADMAP milestone checkbox is `[x]` (completed 2026-08-11), `110-VERIFICATION.md` reads `status: passed`, and the phase has 6 PLAN.md files with 6 SUMMARY.md files — full parity. The struck sentence was true when written earlier in this same session and is preserved rather than deleted so the sequence stays legible. The original text follows for the record: 110-01..110-05 are complete and deployed (2026-08-10 ~17:05 ET, operator-authorized); 110-06 is blocked on observing a real 09:00 UTC `retention-prune` fire against the deployed code (earliest 2026-08-11 09:00 UTC / 05:00 ET; the plan forbids hand-triggering it). Also still open from that phase: a clean zero-`TIMEOUT` health-check re-run, and the 110-05 ROADMAP checkbox. This block was overwritten by `gsd-sdk query state.begin-phase --phase 111` (the documented state.* narrative clobber) and restored by hand. **Counter note, corrected 2026-08-11:** that same call moved `completed_plans` 30→31 and I initially reverted it to 30 as a phantom increment — that revert was wrong. Ground truth is the SUMMARY.md file count on disk (`.planning/phases/*/*-SUMMARY.md`), which was 31 before Phase 111 started: 108=7, 109=10, 110=5, 116=8, 117=1. The committed 30 was stale because a CONCURRENT session completed 117-01 without bumping the counter. Derive this number from disk, never from the previous value ± 1. Read the three numbered notes below before starting 110-06.

**Three things the 110-06 session must know before starting:**

1. **An `aggregates` ALERT after a successful prune is EXPECTED and is NOT prune failure.** The deployed health check measures the oldest doc in a table, not the oldest doc the pruner would delete, so the permanently-protected oldest `period:"daily"` row (165.0h past the 90-day cutoff and growing 24h/day) keeps the ALERT lit forever. DUR-01's real signal is the baseline pair in `110-DUR-EVIDENCE.md`: the oldest `period:"daily"` `_creationTime` must NOT move forward, and the oldest `period:"hourly"` one MUST. A concurrent session is building a predicate-aware `summarizeOverhangProbe` fix; it is NOT part of Phase 110 and was NOT deployed.
2. **Two items are deferred into that session:** a clean zero-`TIMEOUT` health-check re-run (tonight's run hit a live `events` `SystemTimeoutError` — pre-existing and memory-correlated, with prior occurrences 2026-08-04 at 41.7 GiB and 2026-08-05 at 45.95 GiB, not a regression from this phase), and the 110-05 ROADMAP checkbox, deliberately not set tonight because a concurrent session had uncommitted Phase 111 work in ROADMAP.md and updating it would have interleaved the two.
3. **Backend memory is worth a look at start:** 16.02 GiB at 05:30 → 23.57 at 16:44 → 32.51 at 18:00 (flat at 18:05). The day's ~1.04 GiB/h organic rate explains only ~1.3 of the ~8.9 GiB jump; the deploy and a 19-probe health run both sit in that window. Correlation only — no experiment isolated it. `ConvexNightlyRestart` at 02:00 should have cleared it before the cron fires at 05:00.

**Phase 116 (galdr-prompt-library) COMPLETE and VERIFIED `passed` (3/3 goal clauses, 16/16 decisions) on 2026-08-10**, executed in a CONCURRENT session running in this same checkout while Phase 110 executed. All eight SUMMARY.md files are committed (`git ls-files .planning/phases/116-galdr-prompt-library/*SUMMARY.md` → 8), through `2fc72bb8 docs(116-08): summary for the galdr page plan — phase 116 complete (8/8)`. **The "NOT verified yet / no 116-VERIFICATION.md exists" note previously recorded here was correct when written and has since been satisfied:** `116-VERIFICATION.md` now exists and the phase passed goal-backward verification against the LIVE system, not against its own plans' reports — D-15's append-only trail proven by a create→update→restore probe (1→2→3 versions, restore appends rather than rewinds), D-16 proven by archiving that probe (absent from `galdr:list` AND from skill `lookup`, its 3 versions surviving, with `adversarial-plan-review` as the still-visible control), and D-04's no-CORS boundary proven with a control that actually differentiates (`OPTIONS /galdr/list` → 404 vs `OPTIONS /preflight-ingest` → 204; an earlier `/ingest` control was discarded for 404-ing too). Three disclosed gaps, none blocking: the cross-session claim rests on four fresh-context subagents rather than a literal second CLI session; 20 pre-existing `theme-contrast` e2e failures (control-proven unrelated — reverting only the Galdr nav entry reproduces all 20); and D-14's 20-version cap is code-verified but its eviction path has never actually run. The earlier blockers recorded here (116-05's blocking deploy checkpoint, `GALDR_API_KEY` unset) were resolved by that session, not by this one — this note is derived from git, not from its report. (Restored by hand 2026-08-10: `gsd-sdk query state.begin-phase --phase 110` overwrote this block with a bare "Executing Phase 110" — the same documented state.* clobber recorded in the Phase 110 paragraph below, which happened to `state.planned-phase` two days earlier. Only the Current Position block was affected; frontmatter counters were not touched, verified by `git diff`.)

**Phase 110 (Convex Durability) PLANNED (2026-08-10).** 6 plans across 5 waves, verified by gsd-plan-checker with **0 blockers, 2 warnings** (both documentation hygiene, both resolved before commit) — see `110-PLAN-REVIEW.md`. Requirements DUR-01/02/03 all covered; the blocking decision-coverage gate returns `total: 11, covered: 11` for D-01..D-11. Wave shape: 110-01 (cursor/rotation helpers) and 110-02 (DUR-03 root-cause write-up) in wave 1, both independent; 110-03 (predicate + comment sweep) wave 2; then 110-04 deploy → 110-05 health-check → 110-06 live evidence, waves 3-5, all `autonomous: false` because DUR-02's evidence requires a **real 09:00 UTC nightly prune run** against deployed code. Planning surfaced one latent bug not in CONTEXT.md: `retention.ts:159-165` sources `lastCreationTime` only from *deleted* docs, so introducing D-02's skip-predicate would pin the cursor and re-read the same 200 rows until the nightly cap — reviving the 2026-07-30 head-rescan incident. 110-03 Task 1.C ships the fix atomically with the predicate; 110-01 Task 2 adds an all-skipped-batch regression test. DUR-03's framing also corrected: upstream `convex-backend#495` carries an **open, unmerged** PR #522, so the write-up records three states (no knob / known cause / available-but-unmerged fix) and must re-verify issue + PR merge state at write-up time. **Note:** `gsd-sdk query state.planned-phase` was run and clobbered the Phase 116 "Current Position" narrative with the bare string "Ready to execute" — the documented state.* defect (see the 2026-07-31 comment below re: Phase 103). STATE.md was restored from HEAD and only `total_plans` (25→31, +6) plus this paragraph were re-applied by hand; Phase 116's live blocking status was preserved verbatim, and `last_activity` was deliberately left as the 116 session's own more-recent value rather than stomped. Next: `/gsd:execute-phase 110`.

**Plan 109-07 complete (2026-08-09).** Restored real grouping/billing metadata to the picker from CodePulse's own `PROVIDER_BILLING` registry and fixed the confirm-gate collision it exposed. `mapCatalogueVendorToBilling` (new, `src/lib/catalogueBilling.ts`) implements D-13's corrected rule: `"anthropic"` maps to the `anthropic_direct` registry entry; every other non-empty vendor (the OpenRouter routing-slug family) is billed as the OpenRouter catch-all; only an empty/missing vendor reaches `group:"unclassified"`/`costTier:"unknown"` — detection is by emptiness, never by calling the registry's own unmapped-provider fallback, which would make "unclassified" undetectable. `normalizeCatalogueEntry` (`BrainPicker.tsx`) no longer hardcodes `group:"api", billing:"api", costTier:"normal"` for every row. `GROUP_ORDER` gains a fourth, last "Unclassified" entry with a deliberately irregular full-word "UNCLASSIFIED" chip (dashed `--status-warn` border). Because `costTier:"unknown"` now reaches live entries, `needsCostConfirm` can fire for the first time — exposing a mouse/keyboard confirm-gate collision UI-SPEC §F revision 3 already specified the fix for: `BrainPicker.tsx` now computes one hoisted `shouldConfirmCost = scope !== "global" && needsCostConfirm(entry)` and passes it to `BrainPickerRow` as a required `needsConfirm` prop, consumed identically by both the row's own mouse click and the enclosing cmdk keyboard path — the row no longer decides that condition itself. A six-combination (scope × costTier) mouse/keyboard parity test proves the two paths agree in every case, including the specific stacking defect (global scope + unknown cost-tier: modal opens, inline confirm never appears). Rule 1 fix folded in: `BrainPickerRow`'s hardcoded hex vendor-dot fallback (`#6b7280`) replaced with `var(--muted-foreground)`, matching `BrainHeaderBadge`'s existing pattern. `CatalogueEntry["group"]` widened in `src/lib/brainsApi.ts` (named by the plan's own task text but omitted from its declared file list — documented as a deviation). Full suite: 283 passed | 17 skipped (baseline 282/17), 3717 passed | 193 todo (baseline 3699/193) — 18 net new tests, zero regressions. `npx tsc --noEmit` exits 0. No file belonging to plan 109-08's declared scope was touched. Executed sequentially on `master`, no worktree. See `109-07-SUMMARY.md`. Next: Plan 109-08.

**Plan 109-04 complete (2026-08-09).** Fixed the read path so every brain surface answers "what engine is this profile on?" the same way Ástríðr does. `useProfileBrainOverrides()` (new hook, mirrors `useGlobalBrainOverride`'s pull+push shape over `swap.state`'s `profile_overrides` map, delivered by 109-01) feeds a new top rung in `resolveActiveBrain`: a live per-profile override now wins even while a DIFFERENT global override is simultaneously active — the precedence-inversion fix (D-06). The scoped `lastTurn` fallback rung is removed (D-07): a scoped read with no telemetry and no override now returns `source:"none"` instead of rendering a different profile's most-recently-answered model as this profile's own; the fleet-wide (no-`profileId`) `lastTurn` rung is untouched. Three disagreeing absent-state strings ("No brain reported", two independent "Auto" fallbacks) are replaced by the one canonical "Not reported" (italic, muted, no dot) across `BrainHeaderBadge`, Chat's composer pill, `BrainPicker`'s trigger/confirm-modal column, Settings' per-profile row, and `LlmStatusPanel`. `BrainPicker`'s trigger base label, `isCurrent` row highlight, and the pre-swap confirm modal's per-row current-engine column now resolve through the full `resolveActiveBrain` chain instead of raw `activeEngines` telemetry — a profile pinned moments ago shows its pin immediately everywhere, never a stale pre-pin reading. Settings' per-profile engine label reads the same full chain (D-14), so it cannot disagree with the swap-history section plan 109-08 mounts beneath it. `useLastTurnModel`'s docstring, which cited a falsified premise (an emitter bug Phase 108 already fixed, and a nonexistent future astridr phase), is corrected in place. `BrainPicker.tsx:350`'s separate lowercase `"auto"` sentinel (feeds two non-user-visible consumers) is deliberately left unchanged, per the plan's own instruction — an explicit, still-open follow-up. Full suite: 281 passed | 17 skipped (unchanged file count), 3658 passed | 193 todo (baseline 3639) — 19 net new tests, zero regressions. `npx tsc --noEmit` exits 0. Executed sequentially on `master`, no worktree. See `109-04-SUMMARY.md`. Next: Plan 109-05.

**Plan 109-03 complete (2026-08-09).** Deleted the entire per-profile brain-swap stub/live adapter seam (`brainsApi.ts`'s `validateGatewayModelSet`, stub/live adapters, `BRAINS_STUB_ACTIVE`, `VITE_BRAINS_STUB`, `registerBrainsWsSender`, `brainsFixtures.ts`, `BrainsWsRegistrar.tsx`, `e2e/brain-swap.spec.ts`) and replaced it with `src/hooks/useBrainCatalogue.ts` — the one `swap.catalogue` fetcher every brain surface (`BrainPicker`, `BrainHeaderBadge`, Chat's composer pill, Settings' profile rows) now reads. `BrainPicker`'s "This profile" scope dispatches the real, server-registered `swap.set` with `profile_id` (bounded via a `dispatchBounded` wrapper mirroring `GlobalSwapModal`'s own, T-109-08) instead of the never-implemented `gateway.model.set`; both picker scopes read one shared catalogue (D-02), so toggling scope no longer re-fetches. `default_profile_id` (Ástríðr's own resolved value, threaded through by plan 109-01) replaces the `profiles[0]?.profileId` Convex-ordering fallback everywhere it was read (D-03). All five STUB chips/banners are gone with no replacement. Test-count delta fully accounted for (baseline 3649 → 3639, −10, itemized per-file in `109-03-SUMMARY.md` against exactly which now-unreachable branches — D-02's flattening makes only the "API" group/`normal` cost-tier reachable through the picker's live catalogue until plan 109-07 — each dropped test corresponds to). `npx tsc --noEmit` exits 0, `npm run build` succeeds, `npx playwright test --list` lists 38 tests with no reference to the deleted spec. Executed sequentially on `master`, no worktree. See `109-03-SUMMARY.md`. Next: Plan 109-04.

**Plan 109-02 complete (2026-08-09).** Deliberately file-disjoint from every other wave-1/wave-2 plan in this phase, touched zero `src/` files. Built TELE-02's data layer ahead of plan 109-08 (which hosts the combined history on Settings): `listGlobal` (`convex/controlVerbSwaps.ts`) is a new bounded, argument-free query matching absent-scope rows via `q.eq("scope", undefined)` — D-11's own decision text says `scope: null`, which is wrong and would silently return zero rows forever (`runtimeIngest.ts`'s `normalizeOptional()` strips every incoming `null` to `undefined` before storage; Convex's `v.optional(...)` rejects an explicit `null` outright); corrected per `109-RESEARCH.md` D.10 and guarded by a mutation-proven test (flipped to `null`, confirmed RED, reverted, confirmed GREEN). `mergeSwapHistory` (`convex/controlVerbSwapsFilters.ts`, new export) combines `listByScope` + `listGlobal` rows on the WR-02 browser-safe side (no `_generated`/`convex/values` import), tagged `origin: "scoped" | "global"`, capped at `SWAP_HISTORY_CAP` with a true pre-truncation `totalCount`, deterministic descending-timestamp order with an `_id` tie-break. `103-CONTRACT.md` sections 3/8/9 corrected in place (Phase 108 D-08 precedent: dated notes, superseded text kept and labeled) — §3 confirms `default_profile_id` is genuinely live on the `swap.catalogue` ack (verified directly against astridr-repo: `ws_commands.py:1258`, `bootstrap/core.py:1298`, test `test_ws_commands.py:2034` labeled "Phase 109 D-03" — not just transcribed from the plan), §8 confirms why `GlobalSwapModal` cannot host a per-profile readout (D-10 hosts it on Settings instead), §9 records the precedence-inversion fix (D-06's rung insertion) with the explicit-disagreement-rendering idea preserved as deferred, not discarded. One plan-draft acceptance criterion (`grep -c "_generated" == 0`) was already false on the untouched pre-existing file (2 hits, explanatory prose) before this plan touched it — documented as a plan-draft correction in the SUMMARY rather than chased; verified instead via a comment-stripped source-level test matching this repo's established raw/stripped idiom. Full suite: 281 passed | 17 skipped (baseline 280/17), 3649 passed | 193 todo (baseline 3635/193) — 14 net new tests, zero regressions. Executed sequentially on `master`, no worktree. See `109-02-SUMMARY.md`. Next: Plan 109-03.

**Phase 109 UI-SPEC approved 2026-08-08** (`109-UI-SPEC.md`, commit `87ce5c53`; no plans yet — counters unchanged, 109 has no plans). `/gsd-plan-phase 109` stopped at the pre-flight gate because a UI phase had no UI-SPEC; `/gsd-ui-phase 109` was run first at Larry's direction. `gsd-ui-checker` returned 6/6 dimensions after **three** revisions (the workflow's cap is 2; Larry authorized the third), and each revision closed a distinct defect verified against live code rather than a style note — all three were found by the checker and independently re-verified in the main session before being relayed. (1) §G cited the wrong mechanism: the placeholder actually on screen is the `"Auto"` literal in `BrainPicker.tsx:358-360`'s ternary false branch, rendered at `GlobalSwapModal.tsx:624` — `resolveModelDisplayName` is only reached when `engine` is truthy, so a planner following the original text would have patched a path that never runs and left the placeholder visible. (2) D-09's `costTier: "unknown"` makes `needsCostConfirm` (`BrainPickerRow.tsx:83-85`) fire for the first time, stacking the row's inline confirm with `GlobalSwapModal`'s own `needsCostWarning` line — the double-confirm 103-UI-SPEC §3 forbids, and exactly what `BrainPicker.tsx:114-123`'s docstring hardcoded `costTier: "normal"` to prevent. (3) The rev-2 fix guarded `BrainPicker.tsx`'s `handleActivate` only, which a mouse click on a fresh row never reaches — `BrainPickerRow.tsx:146-153` has its own scope-blind local gate that returns at `:149` without calling `onSelect`, and the second click short-circuits at `expandedId === entry.id` before the new condition — so it fixed keyboard, left mouse stacking, and *broke* the mouse/keyboard parity `BrainPicker.tsx:389-396` documents as a guarantee. Final fix hoists the gate: `BrainPicker` computes `shouldConfirmCost = scope !== "global" && needsCostConfirm(entry)` once and passes it to `BrainPickerRow` as a `needsConfirm` prop, making `BrainPickerRow.tsx` a modified file this phase. **Consequence for planning:** the spec now prescribes a two-file confirm-gate refactor in a shipped component — a required consequence of locked D-09, not invented scope — and recommends a mouse/keyboard parity test across every (scope, costTier) pair, since a single-path test passed all three broken revisions. Contract highlights: one canonical `"Not reported"` absent state replacing three disagreeing shipped strings; the 5-state per-profile outcome machine reusing `GlobalSwapModal`'s BSC-04 pattern with the bounded "accepted, not yet confirmed" state specified never to read as success; an explicit "Unclassified" catalogue group that must never silently default to `api` — with a planner note that unmapped-vendor detection must test `PROVIDER_BILLING` membership directly, never `getBillingType()`'s `?? "api"` fallback (`providers.ts:32-35`); and D-10's new collapsible per-profile swap-history section on Settings. **Planned 2026-08-08** — 9 plans, 7 waves (see frontmatter `stopped_at` for the four research corrections and the two plan-time decisions D-13/D-14). Next: `/gsd-execute-phase 109`.

**Milestone v14.0 — Per-Agent Engine Visibility, Convex Durability & Mission Board**, opened 2026-08-06 via `/gsd-new-milestone`. Continues phase numbering from v13.0 (103–107) → **Phases 108+**.

Five scoped areas, each grounded in evidence gathered at scoping rather than inherited from a prior document:

1. **BSC-01 per-agent axis (cross-repo, astridr-first).** v13.0's one PARTIAL requirement. Confirmed 2026-08-06: `SwapSetCommand` (`astridr/api/ws_commands.py:235-247`) has **no** profile/scope field, and `grep -rn "active_engine\|activeEngine"` across astridr-repo returns **zero hits** — ~~the per-profile telemetry emitter does not exist~~ **[CORRECTED 2026-08-07 during Phase 108 discussion: the emitter DOES exist, under the name `model_routing` (`astridr/providers/router.py:399-427`), firing on every resolution from four call sites. The zero-hit grep was true but searched for the wrong name. What is actually missing is a `profileId` in its payload — and it sends `selectedModel` where the consumer reads `model`, so both fields the ingest guard checks are undefined and every event is skipped. See `108-CONTEXT.md` § Phase Boundary.]** Only config exists (`astridr/engine/bootstrap/core.py:1356-1359`), which BSC-01 explicitly forbids as the source. ⚠ **The inherited "astridr Phase 184.1 owns this" attribution is false** — `grep -rn "184\.1"` across astridr's `.planning/` returns nothing; the brain-swap backend shipped as astridr **Phases 185/186**. Nothing was ever scheduled to deliver this. Both halves are owned from this roadmap.
2. **Convex durability.** `aggregates` is defined at `convex/schema.ts:953` but appears **0 times** in `convex/retention.ts` — it is never pruned and grows unbounded. Plus ~~the retention cron's tombstone self-defeat (chain dies at table `[0]`, so the other tables are never reached)~~ **[CORRECTED 2026-08-07: stale — already fixed. `convex/retention.ts:112-127` has carried a cursor-seeked, cutoff-bounded range scan since 2026-07-30. `REQUIREMENTS.md`'s own scoping-evidence table already records this, and no requirement was written to re-fix it; DUR-02 verifies a full pass on the live instance instead.]** and the memory-growth root cause, currently only masked by `ConvexNightlyRestart`.
3. **Mission board (frontend-only).** Upgrade `src/components/JobsPanel.tsx` + `subagentJobs` (`convex/schema.ts:1028`, both astridr-Phase-168 leftovers) on data that streams today. SEED-002's remaining asks stay planted: its astridr half SEED-023 is `dormant`/`scope: Large` and none of its runtime exists (`grep -rln "stream-json\|stream_json" --include=*.py astridr/` returns nothing).
4. **Telemetry coverage.** Per Phase 105's `deferred-items.md` scoping investigation: **Group A (5 kinds)** have zero emitters anywhere in astridr → contract-doc fix, no build. **Group B (7 kinds)** have real emitters → routes justified per-kind, starting with `control_verb_swap` because item 1 needs it.
5. **Small-debt sweep.** Skill-registry prune churn, intermittent `Chat.test.tsx` failure `D-106-04-01`, and versioning `convex-selfhost/`.

**Phase 108 context gathered 2026-08-07** (`108-CONTEXT.md` + `108-DISCUSSION-LOG.md`, commit `adb36cf6`; no plans yet — counters unchanged, 108 has no plans). 4 gray areas discussed, 16 decisions locked (D-01..D-16). **Two claims in item 1 above were corrected during that discussion, from code read this session — see the strikethroughs.** The load-bearing correction: `ModelRouter._emit_model_routing` (`astridr/providers/router.py:399-427`) **does** fire on every resolution from four call sites; what is wrong with it is that its payload carries **no `profileId`** and sends **`selectedModel`**, while CodePulse's `case "model_routing"` (`convex/runtimeIngest.ts:717-748`) reads `d.profileId`/`d.model` — *both* undefined, so `isUnresolvedRouting()` `break`s every event. That is the complete mechanism behind the 93 `{profileId:"unknown"}` rows. The `active_engine` grep returning zero hits was true and is not the same thing as "no emitter" — the emitter is named `model_routing`. Equally: the **whole CodePulse receiving half is already built** (`convex/activeEngine.ts`, `activeEngineSnapshots` at `convex/schema.ts:2062`, the ingest case and its guard) and the Phase 103 `src/components/brains/` UI exists waiting on real rows, so this phase feeds a pipe rather than laying one. Headline decisions: a `contextvars` ContextVar set at the loop boundary carries the profile to both the emitter and a new per-profile override that sits **above** the global one (D-01/D-04, precedent: Phase 94's `_current_trace_id`, Phase 105's D-10); no profile in context → **refuse to emit** (D-02), with a boot seed per profile at the existing `bootstrap/core.py:1343-1359` loop so the table is never empty-by-default (D-03); `swap.set` gains an **optional** scope rather than the contract's separate `gateway.model.set`, keeping the one-dispatch-path promise (D-05), runtime-only with no TTL (D-06), and a **spoken swap stays global** (D-07); **emit-on-change** in astridr rather than per-resolution (D-09) — contract-literal, §4 says "resolution change" — with `activeEngineSnapshots` added to `RETENTION_DAYS` in this phase, since `grep -c` against `convex/retention.ts` returns **0** and this is the phase that starts feeding it (D-10); `control_verb_swap` routes all **six** emit sites (not the two REQUIREMENTS.md cites — `swap_model.py:444,472,483,495` plus `swap_voice.py:211,232`, found by grep this session) into one table discriminated by `verb`, surfaced as a history section in the existing `GlobalSwapModal` (D-13..D-15); ENGINE-05 closes on **backend rows read out of the live instance**, with an unscoped control, never on UI state (D-16). `103-CONTRACT.md` is corrected in place as a phase deliverable (D-08) — its §1 still says the axis is "Not built" and attributes it to the non-existent astridr Phase 184.1, and its §2 specifies the rejected command. Next: `/gsd-plan-phase 108`.

**Counters zeroed and frontmatter switched to v14.0 BY HAND** (no `gsd-sdk state.milestone-switch`), per this project's standing rule — every `state.*` verb here has a documented history of clobbering this file's narrative and miscounting. Frontmatter and body were reset in the same pass, which is the defect Bug #2630's SDK path exists to prevent.

<details>
<summary>v13.0 close (archived narrative) — preserved verbatim, superseded by the position above</summary>

Phase: 107 (aggregates-rollup-sharding) — **COMPLETE, 7 of 7 plans, `OCC-01 VERDICT: PASS`**. Phase 106 COMPLETE (8/8). All 5 phases of v13.0 done.
Plan: all 53 plans executed — milestone v13.0 is code-complete and every requirement is closed except DEBT-03, which is PARTIAL with its blocker named

**Phase 107 Plan 07 COMPLETE (2026-08-06) — OCC-01 SATISFIED, the requirement 107-06 left open.** The read set is now a point lookup and contention went to zero. Full detail in `107-07-SUMMARY.md` and `107-OCC-EVIDENCE.md` §§ H/I; the parts worth carrying forward: **the fix itself was small, the instrument was the hard part.** Three separate measurement traps were found before any number was believed — `docker logs --since` returns 0 lines for every window after container start (NOT clock skew; host/container/WSL clocks agree exactly), a large `--tail` silently serves the ROTATED log file with plausible timestamps and real content (`--tail 40000` current, `80000` stale), and a gap exists between reachable segments. The replacement method windows on in-message RFC3339 timestamps and **was calibrated against a known-good answer before use — it reproduces 107-06's published 368/314 counts and exact table breakdown byte-for-byte**, which simultaneously validates the new method and independently confirms 107-06's raw data. Equally load-bearing: the test fake's `withIndex()` ignored both index name and filter and returned the whole table, so read-set WIDTH was physically untestable and a wrong fix would have stayed green — the same defect class was then found in `events.test.ts` and swept for across the repo. A separate finding worth keeping: hourly OCC counts swing **17x (32-542) on identical code**, so 107-06's `+70.5%` sits inside the noise band and should not be quoted as a precise effect size — its diagnosis was right regardless, and this plan is its direct consequence.

**Phase 107 Plan 07 — IMPLEMENTED AND DEPLOYED 2026-08-05, VERDICT PENDING.** 107-06 diagnosed the unfixed half of OCC-01 correctly and this plan closes it, going further than 107-06's stated lever. 107-06 recommended indexing `shard` alone, which would have taken the read set from ~101 rows to ~13 but still collided on same-shard/different-key writes — a partial lever, and this phase had already spent one full measurement cycle on one of those. Larry chose the full narrowing: `dimensions` is `v.any()` and cannot be indexed, so it is denormalised into an indexed string by the new `convex/lib/aggregateDimensionKey.ts` (the single place a key is ever spelled, used by the live write path AND by the backfill's `insertBucketsBatch` so the two can never disagree), and the new `by_type_period_bucket_key_shard` index pins **every** field with `eq()` — **read set 101 rows → 1**. `by_type_period_bucket` is deliberately untouched; Convex confirmed at deploy time `No indexes are deleted by this push`, so all 10 reader modules still fold whole buckets and no reader file was modified. `AGGREGATE_SHARD_COUNT` stays at 8 so this cycle moves exactly one variable. **The load-bearing work was the test harness, not the fix.** `makeStore()`'s fake `withIndex()` ignored both the index name and the filter callback and returned the entire table, so read-set WIDTH was physically untestable — a point lookup and a whole-bucket scan produced identical results, and a wrong implementation would have stayed green, which is how this phase failed once already. The fake now applies eq/range constraints and records what each query pinned and how many rows it returned; 3 new tests assert the read set directly and fail against the old code (`expected 9 to be less than or equal to 1`), and both required mutation proofs pass. The identical defect in `events.test.ts` (where `first()` returned the table's first row whatever was asked) surfaced as 2 failures and was fixed; a repo sweep found the good pattern already used in forge/swarmTasks/subagentJobs/warRoom/v6Mutations, and `costBudgets.test.ts:315` shares the shape but feeds one row per test and asserts a post-query guard, so it is not a live false-green and was left alone. Suite **3459 pass / 0 fail**, `tsc` and build clean. Live post-deploy rows confirmed carrying `dimension_key` with one shard shared across each ingest's three writes, so 107-03's one-draw-per-ingest contract still holds. Container did NOT restart (`StartedAt` unchanged, 27.36% memory). **Also corrected two now-false comments** (Stale Docs): `schema.ts` claimed a missing `shard` "reads as shard 0", contradicting 107-03's strict equality, and `aggregateShard.ts` called `shard` "unindexed". **Separately, `aggregates` is absent from `retention.ts` entirely — it is never pruned and grows unbounded**; noted as a follow-up candidate, not in scope. Next: the after-window read per `107-OCC-EVIDENCE.md` § H.7, using SEVERAL non-overlapping windows.

**Phase 106 Plan 08 of 8 COMPLETE (2026-08-05) — PHASE 106 CLOSED.** UAT session C ran live 21:10-21:45 UTC against a Forge daemon proven live by an *advancing* heartbeat (14.0 s across a 16 s gap), never merely a listening socket. **Test 8 (drag round-trip, D-08 re-verification) PASS across all five legs**, evidenced from `forgeCommands` rows read out of the live backend rather than from UI state — an optimistic overlay cannot manufacture a claim→execute→complete chain. Legs A-D: archive (5.33 s claim), restore (3.54 s), move-to-project (1.09 s), move-back-to-global (2.16 s), all `done`. Leg C carries two UI-independent corroborations: its `workspaceId` is byte-identical to the `drive-sync-test` id recorded pre-session, and leg D's `sourceOrigin` is a **fifth** project origin absent from the pre-session census, which the registry could only mint if the skill genuinely lived there. **Leg E — the honest-failure leg — passed on a STRONGER observable than predicted**: the row aged out `expired` with `claimedAt`/`executedAt`/`completedAt` all null and the subject skill still on disk, and the surfacing was a **persistent `Expired` row badge** (`RowStatusBadge`, `IntakeStatusBadge.tsx:91-94`), not the transient `Expired — no daemon claimed this command.` toast the plan predicted; the prediction is recorded as corrected rather than quietly reconciled, and a persistent badge is harder to miss than a vanishing toast, so the "must not settle silently into apparent success" assertion is satisfied by a better signal than it asked for. The tray-respawn false-signal was excluded by measurement: Larry quit Forge from the tray (supervisor PID 33516), not just the daemon (PID 33616), and `:57328/health` was confirmed refusing connections before the leg-E drag. **Test 9 PASS — this closes Phase 100's genuinely-open item.** Phase 100 accepted CR-02 as unit-verified for a stated reason ("the live catalog contains 0 shadowed-merged skills … fabricating one was deferred by operator"); that condition was re-measured this session and still held (0 of 488 names), so `uat106-shadow` is **the first shadowed-merged row this catalog has ever held**. An **invalid first trial became the test's control**: it dragged from Overview/All, where `lane` defaults to `"active"` (`SkillRow.tsx:99`, only `ColdStorageView.tsx:59` passes `"cold"`), so with `isDormant()` false for a shadowed row the drop resolved to a genuine archive and the server's LAYER-1 preflight refused it. Trial 2, from inside Cold Storage, produced the quiet `"Uat106 Shadow" is already in Cold Storage — nothing to move.` The two trials differ in exactly one variable and yield different toasts, which resolves what the row count cannot: **both** wrote zero rows, but the collision message can only exist if `enqueueLifecycle` was actually called, whereas the quiet message fires at `Skills.tsx:337-339` on the `noop` branch before `beginPending` — so the client-side CR-02 no-op fired **on its own** and the backstop was never reached. Active copy survived on disk. **A ~56-row registry reconciliation the daemon performed during the session was investigated rather than absorbed**: `claude-code` went 185 → 129 real rows, which is `computeSkillPrunes` removing rows for names with no directory on disk, proven safe by set-diffing registry against filesystem (131 names vs 131 dirs; the only disk-side divergence is `_archived`/`mandras_made_skills`, neither carrying a `SKILL.md`) — no name that exists on disk was pruned. Cleanup verified to zero residue with the rescan's row delta measured as **exactly 3**, so the prune is shown to have taken this plan's fixtures and nothing else; the trigger skill (`enhance-prompt`, chosen only after confirming it had no dormant copy that would trip the collision guard) was verified fully restored, not left half-moved. `git status --porcelain convex src` empty. **Markers**: DEBT-01/02/04 → ✓ SATISFIED (DEBT-02 and DEBT-04 were both still carrying stale verdicts — a superseded `NO-GO` and a "sessions B/C pending" — now rewritten from cited artifacts); **DEBT-03 → ⚠ PARTIAL**, blocker named rather than rounded up: entry chunk 563,616 B against a 512,000-byte threshold, with `chunkSizeWarningLimit` deliberately not raised. See `106-08-SUMMARY.md`. Next: `/gsd-plan-phase 107 --gaps` or an equivalent 107-07 gap-closure plan for OCC-01.

**Phase 106 Plan 05 of 8 CLOSED ON PAPER (2026-08-05) — D-10 SATISFIED, the work itself ran 2026-08-05 and was recorded in `126b6679`; only the SUMMARY, the ROADMAP checkbox and these counters were outstanding.** The laptop is on the tailnet and CodePulse actually works on it: 7/7 checklist steps PASS, with step 5 — the load-bearing one — showing the dashboard rendering **live** data on the laptop whose Sessions figure matched the office PC's at the same moment. That comparison is what makes the verdict SATISFIED rather than "Tailscale says Connected". Every endpoint in `106-TAILSCALE-CHECKLIST.md` was read off the running office PC (verbatim `tailscale status`/`serve status`/`funnel status` plus 21 probes), never assumed from documentation, and CONTEXT.md D-10's `:8443` recollection was confirmed against live output. The checklist's own 2026-08-04 draft had an expectation that was **wrong on its own terms** — it told Larry to compare the Dashboard Sessions tile against `/health`'s `sessions` field, which are different numbers by construction (`/health` returns the `.take(20)`-capped `sessions.listActive`, so it reads `20` at ≥20 active sessions; the tile reads the uncapped `heroStats.summary.activeSessions`, then 279) — and would have produced a **false FAIL** on the single most important step; re-measured and corrected in the open before the run. Blocker A (Vite's DNS-rebinding guard 403ing the MagicDNS hostname while the raw tailnet IP returned 200) was fixed in-plan with `server.allowedHosts: ["lmofficenew.tail5bb6b3.ts.net"]` — one explicit host, deliberately not `true`, which would disable the guard for every host. Step 6 was re-specified live rather than failed: `ConnectionPopover` is dead code and there is no "Disconnected" label in the header at all — the only rendered consequence of the socket status is the `LAT:` chip (`DashboardLayout.tsx:570`, gated at `:445`), so the indicator is an ABSENCE, and the corrected two-machine differential (chip present on the office PC, absent on the laptop, same build, same minute) is a strictly stronger test. **Two defects recorded and deliberately NOT fixed, neither blocking D-10:** (1) Blocker B — `VITE_ASTRIDR_WS_URL` is still `ws://127.0.0.1:8181`, which on the laptop resolves to the laptop and produced all 7 console errors; the fix is a hand edit of `.env.local` to `ws://lmofficenew.tail5bb6b3.ts.net:8181` plus a Vite restart, and it is Larry's because `.env` files are blocked by the env-file-guard hook; (2) Clerk's suffixed-cookie hashing throws `Cannot read properties of undefined (reading 'digest')` on the laptop because `crypto.subtle` is only exposed in a secure context and the laptop reaches Vite over plain HTTP on a non-localhost hostname — auth worked for the session, but the mechanism is silently degraded, and the named fix is publishing 5173 over HTTPS via `tailscale serve`. DEBT-03's marker clause "the laptop Tailscale half … has not started" was corrected in REQUIREMENTS.md per the Stale Docs rule; the rest of the DEBT-01..04 reconciliation is 106-08's and was left alone. DEBT-03 as a whole remains NOT SATISFIED on its bundle half (entry chunk 563,616 B, still above 512,000, honestly documented rather than silenced). See `106-05-SUMMARY.md`. **Counters reconciled this session against git ground truth** (they had drifted to `completed_phases: 3` / `percent: 60` / `completed_plans: 50` while disk showed 4 phases complete and ROADMAP's Phase-106 header still read `3/8` after six plans had run). Next: `106-08` (needs a live Forge daemon + browser), then a `107-07` gap-closure plan for OCC-01's read-set lever.

**Phase 107 Plan 06 of 6 COMPLETE (2026-08-05) — `OCC-01 VERDICT: FAIL`, and the phase goal was NOT achieved.** The after-window measurement ran at 19:26:36Z, ~3 h after `DEPLOY_UTC = 2026-08-05T16:26:33Z`. All three window validity gates PASSED: identical `WINDOW_HOURS: 2` (a longer window was refused despite more time having elapsed), container uptime 7 h with `State.StartedAt` still `2026-08-05T12:45:21.334055274Z` — unchanged to the nanosecond across baseline, deploy and measurement — and a window starting 17:26:36Z, a full hour after the deploy, so every counted line came from the sharded path. **Headline: `BASELINE_RETRIES_PER_1K: 1188.5` (290/244) → `AFTER_RETRIES_PER_1K: 2025.8` (314/155), a +70.5% regression.** Traffic normalization earned its keep in the *opposite* direction from the one it was written to guard against — it exists to stop a quiet period reading as a fix, but here traffic FELL 36.5% (244→155) while OCC ROSE 8.3% (290→314 lines = 145→157 retries), so the raw rate understated the regression. Verdict is FAIL, not PARTIAL (PARTIAL requires the rate to have fallen; it rose 145.0→157.0/hr) and not INCONCLUSIVE (all gates passed, both ingest volumes non-zero so the normalized figure is defined, and Larry characterized traffic as **normal** in § F0). **What DID work, and it is not nothing:** all 8 shards are in use, the one-draw-per-ingest contract holds in live data, and the hot document is gone — top-document share collapsed **66.9% (194/290) → 6.4% (20/314)** across **55 distinct documents**. **Correctness is fully intact:** `FINAL_READ_TOTALS: MATCH` (113 == 113), and the check was proven non-vacuous by a control showing **20 of 23** logical keys in the measured hour are backed by 2–4 shard rows apiece (101 rows in the bucket) — so the readers genuinely had to fold multiple rows and did. This is a clean contention failure, NOT the "lower count paired with a silently broken fold" outcome the method was built to catch. **Why it failed (strong hypothesis, deliberately not stated as fact — it was NOT isolated by a controlled experiment):** the read set was never sharded. All 157 retries are `Udf(events.js:ingest)`, and the contended documents were looked up live and ARE the sharded rows themselves (`metric_type: "events"`/`"sankey_edge"` carrying `shard: 5`, `shard: 3`) — not some unsharded metric type the change missed. Convex OCC conflicts on documents *read from **or** written to*, and both write helpers (`convex/analyticsRollup.ts:43-54` `incrementEventBucket`, `:101-111` `incrementSankeyEdge`) `.collect()` the entire `(metric_type, period, bucket_start)` bucket and match dimension+shard in JS. 107-03 narrowed the WRITE target to one row but left the READ set as the whole bucket — all 8 shards, every dimension key — so any concurrent ingest patching any row in that bucket forces a retry regardless of which shard was written. That also explains the modest *increase*: sharding made each bucket ~8× wider, so every `.collect()` now reads more rows that can invalidate it. **No fix attempted and no rollback** — a rollback would re-create the single hot document behind two multi-day incidents, and the read path is demonstrably correct under sharding. **Next lever, for gap closure: index `shard` and range-bound the bucket lookup to the caller's shard.** Note two corrections recorded against earlier text: (1) raising `AGGREGATE_SHARD_COUNT` beyond 8 was the plan's own leading suggestion and the evidence argues AGAINST it — a higher count widens each `.collect()` further and would likely worsen contention until the read set is narrowed; (2) `convex/lib/aggregateShard.ts`'s header note that widening "needs no schema or index change" is true for raising the COUNT, and is exactly why the recommended lever DOES require an index addition. `git status --porcelain convex src` empty throughout; zero source files modified. See `107-06-SUMMARY.md` and `107-OCC-EVIDENCE.md` §§ F0/F/G. **OCC-01 remains OPEN.**

**Phase 105 (Tool & Trace Observability) — CONTEXT GATHERED 2026-08-03** (`105-CONTEXT.md`, commit `db5c763d`; no plans yet). 4 gray areas discussed, 16 decisions locked (D-01..D-16). Counters unchanged (105 has no plans). Headlines, each grounded on a LIVE query against the running self-hosted Convex rather than assumption: **`toolExecutions` contains ZERO Ástríðr agent tools** — it is Claude Code hook rows (`Bash` 594/16 err, `Edit`, `Read`, `mcp__github__*`) plus gateway pseudo-tools only, while Ástríðr's real tools (`web_search` 125, `telegram_tool` 58/3, `cli_gateway` 103/43, `memory_search`) live in `callGraphEdges` as **cumulative counters with no per-call history**, so OBS-01's "over time" is impossible from either table today; **`tool_policy_event` has no `case` and no `default`** in `convex/runtimeIngest.ts`'s switch, so all four kinds (`tool_call_leaked_as_text`, `execution_denied`, `malformed_policy_boot`, `malformed_policy_reload_rejected`) are **silently discarded at the ingest boundary**; `toolExecutions` carries no `traceId`, so tool spans cannot nest under a turn; and both `TraceWaterfall` feeder queries (`llm.sessionCalls`, `toolExecutions.listBySession`) are unbounded `.collect()`. Decisions: Ástríðr tool calls land as per-call `toolExecutions` rows via the existing `tool_executed` case (D-01), `provider`-tagged with the default view scoped to Ástríðr so a mixed ranking is never shown by accident (D-02); **one astridr-repo commit** adds `durationMs` + `traceId` to `tool_executed` (D-03), widens the leak payload with `tool_was_offered`/`tools_offered_count`/`round`/`agentId` — all already local variables the telemetry send drops (D-08) — and introduces a per-round ContextVar so trace nesting is **reported, not inferred** (D-10, needed because `llm_call` is emitted from inside each provider, which has no access to the loop's `round_num`); a new `toolPolicyEvents` table (D-05) alerting on the **fail-open kinds only** through Phase 104's existing `alerts` + `sendAlertWebhook` path with **no new cron** (D-06), proven by inducing real events live rather than shipping into an unprovable empty view (D-07); hourly aggregate buckets so "over time" survives the 14-day `toolExecutions` prune without raising retention (D-04); tool rows nested under their LLM call with a per-trace cache ratio and **capped reads that state truncation on screen** (D-09/D-11/D-12, the `3b31c9f4` lesson); a new Tools page under `OBSERVE`, stacked sections not tabs, with the three existing tool panels left in place and cross-linked (D-13..D-16). Next: `/gsd-plan-phase 105`.

**Phase 105 Plan 01 of 9 COMPLETE (2026-08-03)** — bounded the four unbounded `toolExecutions`/`llm` reads before D-01's Ástríðr tool volume lands (D-12): `convex/toolExecutions.ts`'s `listBySession`/`successRate`/`avgDuration`/`recentExecutions` and `convex/llm.ts`'s `sessionCalls` all replaced `.collect()` with an explicit exported cap (`SESSION_TOOLS_READ_CAP=1000`, `TOOL_WINDOW_READ_CAP=4000`, `RECENT_SCAN_CAP=500`, `SESSION_CALLS_READ_CAP=1000`) and now report `{ rows|tools, truncated, cap }` instead of a bare array. New `excludeByProvider()` pure helper wired into `ToolExecutionPanel`/`PermissionDecisionsChart` as `excludeProvider: "astridr"` (D-15/D-02 — an EXCLUDE filter, not the CONTEXT.md-suggested include filter on `provider === "claude-cli"`, which was verified live to drop every Claude Code tool-failure row and every gateway row; the exclude-filter direction is proven a zero-regression no-op today via a two-run `toEqual` control test). `TraceWaterfall` renders a token-driven D-12 truncation banner ("Showing the most recent {cap} calls...") only when the cap is hit, no fabricated `{total}` (F2). **Rule 1 fix mid-plan:** the plan's own Task 1 snippet said to keep `.order("asc")` while capping `sessionCalls`/`listBySession` — verified against 105-UI-SPEC.md that this would silently keep the OLDEST rows on a cap hit, not the most recent; both now read `order("desc").take(cap)` then reverse, so a truncated session genuinely shows its most recent calls. `convex/toolExecutions.test.ts` created (Wave 0 gap closed, 11 tests incl. the D-15 control + a negative control proving the forbidden include-filter mechanism). Every new/changed test mutation-verified (4 separate production mutations, all caught). Full suite 3176/3176 passing, `tsc --noEmit` clean, `npm run build` clean. `avgDuration` bounded despite zero live consumers (F4) — flagged as a Phase-106 dead-code candidate, not deleted. `convex/ingest.ts:168`'s missing `provider` on `PostToolUseFailure` (F6) left unfixed by direction — it's the exact baseline the D-15 control depends on. See `105-01-SUMMARY.md`. Next: Plan 02.
**Phase 105 Plan 02 of 9 COMPLETE (2026-08-03)** — the single cross-repo astridr commit set for the phase (`astridr-repo`, branch `feature/brain-swap`, 3 commits `06f01d1a`/`5f90612f`/`c39bb6cc`, **NOT deployed** — plan 105-09 owns deployment). `tool_executed` widened with `durationMs`/`traceId`/`round` (D-03/D-10); the `tool_call_leaked_as_text` policy-event payload widened with `tool_was_offered`/`tools_offered_count`/`round`/`agentId` (D-08), each reusing the identical expression the adjacent `logger.warning` already computes. A new per-round `ContextVar` trio in `astridr/engine/telemetry.py` (D-10) mirrors the existing `traceId` mechanism exactly — set/reset at the turn choke point in `_process_inner` (confirmed live per F1: the round while-loop has no per-iteration `finally` to hook, wrapping it would have been a 620-line reindentation), task-local, independent of trace/goal (asyncio task-locality proven by `asyncio.gather` interleave test). All three provider `llm_call` emits (Anthropic/Ollama/OpenRouter) now attach `round` under an `is not None` guard (round `0` must not be dropped, unlike the sibling truthiness guards on string IDs). `docs/astridr-contract.md` §2.26/§2.34 updated per F3 — `tool_call_leaked_as_text` had never been documented (`grep` returned 0 hits before this plan). Every new/changed test mutation-verified against the production code it guards (6 separate mutations, all caught, all restored byte-identical). Two Rule-2 deviations, both test-only: strengthened the `durationMs` test from a vacuous `>= 0` check to a real-elapsed-time threshold after the plan's own required mutation proof exposed it as non-catching; added 2 tests to `tests/unit/providers/test_ollama.py` (not in the plan's `files_modified`) proving the round ContextVar value actually crosses the `provider.chat()` await boundary, per the verification-discipline's explicit ContextVar+async trap warning. Scoped test runs green throughout (11/11 round+trace context, 709/709 agent, 296/296 providers); a full `tests/unit -m "not live"` run (8212 passed, 0 failed) was gathered as bonus evidence after re-reading the cross-repo scope constraint, not as the primary verification. `astridr/engine/bootstrap/wiring.py` confirmed untouched throughout (empty `git status --short` before/during/after). See `105-02-SUMMARY.md`. Next: Plan 03 (Wave 2, Convex substrate — `toolPolicyEvents` table, ingest `case`, per-call `astridr`-tagged `toolExecutions` rows).
**Phase 105 Plan 03 of 9 COMPLETE (2026-08-03)** — Convex substrate: a new `toolPolicyEvents` table (`by_timestamp` + compound `by_event(["event","timestamp"])`) with an `internalMutation` writer (`record`, reachable only through the Bearer-gated `runtimeIngest` httpAction, WR-06 convention) and 3 honest queries (`recent`, `lastReceivedAt` — D-07: `{timestamp: null}` for an empty table, never a fabricated `0`, `countsByKind` — every one of the 4 kinds zero-filled). `runtimeIngest.ts` gained `case "tool_policy_event"`: `parseToolPolicyEvent` maps all 4 kinds (dual snake/camel coalescing per field — astridr genuinely mixes `tool_was_offered`/`tools_offered_count` snake_case with `sessionId`/`taskCategory` camelCase), truncates `error` at 500 chars with an explicit marker, and REJECTS (`null`) an unrecognised kind rather than storing it under a made-up label — the case's `else` branch `console.warn`s the rejected value instead of silently dropping it, and deliberately NO switch-level `default:` was added (F1: a catch-all would mask a fifth future kind's absence the same way). `case "tool_executed"` extended: after the unchanged `callGraphEdges.upsertEdge` call, an unconditional `api.toolExecutions.insert` now writes a per-call row tagged `provider: "astridr"` (via `ASTRIDR_TOOL_PROVIDER`, assigned by the receiver, never read from the payload — T-105-12). `traceId`/`round` added to `toolExecutions` (+ `by_provider_time` index for 105-06's default Ástríðr-only drill-down); `round` added to `llmMetrics` and threaded through `case "llm_call"`. `toolPolicyEvents: 90` added to `retention.ts`'s build/history tier — bounded from the same commit that creates it (Phase 104 D-20 `gatewayQuotaSnapshots` precedent). F5 confirmed: no backfill exists or was added — nothing to backfill FROM (existing `toolExecutions` rows have no provenance to invent; `toolPolicyEvents` had nothing stored before this plan). **Class-closure audit** (verification-discipline requirement): enumerated all 41 event kinds in `docs/astridr-contract.md` §2.1-2.41 against the switch's ~65 case labels — 11 kinds still have no domain-specific case (`message_routed`, `instructions_loaded`, `loop_lifecycle`, `worktree_lifecycle`, `batch_execution`, `auto_memory`, `prompt_assembly` [contract doc claims a route that doesn't exist in this repo — pre-existing drift], `structured_output_exhausted`, `vision.capture`, `control_verb_swap`, `control_verb_focus`, `governor_decision`), but this is NOT the same defect as pre-105-03 `tool_policy_event`: every event, regardless of switch coverage, is first captured verbatim in the generic `runtime_events` table via the unconditional `api.events.insertEvent` call that precedes the switch (`legacyEventData()` passes all fields through unchanged except for `graph_snapshot`) — logged to `deferred-items.md`, not fixed (out of scope). 4 auto-fixed deviations, all documented in `105-03-SUMMARY.md` (3 Rule-1 comment-trips-own-grep collisions — same class 105-01/105-02 both independently hit — and 1 Rule-2 test-strengthening after the plan's own mutation-verification step exposed a vacuous fixture). Every new/changed test mutation-verified, including the plan's own required proof (`parseToolPolicyEvent` accepting an unknown kind). 44 new test assertions; full suite 3263/3263 passing; `tsc --noEmit` and `npm run build` both clean; `npx convex codegen` regenerated `api.d.ts` (offline, NOT a deploy — confirmed per this project's established precedent). **Nothing deployed** — the live self-hosted instance still runs pre-105-03 code; deployment remains plan 105-09's step. OBS-01/02/03 NOT marked complete (Wave 2 of 5, per the established "green suite != live-verified" convention). See `105-03-SUMMARY.md`. Next: Plan 04 (Wave 3, aggregates — hourly `toolExecutions`/`toolPolicyEvents` rollup buckets, D-04).
**Phase 105 Plan 04 of 9 COMPLETE (2026-08-04)** — Wave 3: four new hourly `tool_calls`/`tool_failures`/`tool_duration_ms`/`tool_duration_samples` aggregate buckets per `{tool, provider}` dimension (D-04), plus a fail-open-only policy alert evaluator (D-06). New `convex/toolPolicyAlertEval.ts`: `ALERTING_POLICY_KINDS` is exactly `malformed_policy_boot`/`malformed_policy_reload_rejected` (never `tool_call_leaked_as_text`/`execution_denied` — a denial is the policy working correctly, proven by a dedicated D-06 isolation negative-control test); severities never collapse (`error` for boot — degrades to a fully permissive policy; `warning` for reload-rejected — fails safe); a completed-hour dedup key (`details.windowStart`) fires at most once per kind per hour; enforcement-free copy proven by an 8-word forbidden-word gate; inserts directly into `alerts` + schedules `internal.webhookDelivery.sendAlertWebhook`, Phase 104's exact path. `convex/aggregates.ts` gained `fetchToolRowsForWindow` (a second bounded `toolExecutions` read, cap 4000, same shape as the existing `llmMetrics` read) and `insertToolUsageBuckets`, each of the four metric types behind its own idempotency guard; `tool_failures` always writes (real `0` for an all-success dimension, never absent); `tool_duration_ms`/`tool_duration_samples` write ONLY for dimensions with at least one numeric `durationMs` (absent, never a fabricated `0ms`). `computeHourly` gained a SECOND, separate `try/catch` tail-append after the existing `evaluateBudgets` block — `convex/crons.ts` and `rollupDaily` both confirmed untouched (empty diff / zero mentions), matching Phase 104's D-14/"do not add a metric-type branch" precedent exactly. Both required mutation proofs passed (adding `execution_denied` to `ALERTING_POLICY_KINDS` breaks the isolation test; changing the tool-usage dimension key separator from `::` to `:` breaks the idempotency test — a re-run doubles every bucket). One Rule-1 deviation (same class 105-01/02/03 each independently hit): the file's own header/guard comments named the literal string `sendAlertWebhook` 3 times, tripping the plan's exactly-1 acceptance grep — reworded. 42 new tests (19 + 23); full suite 3305/3305 passing (up from 3263); `tsc --noEmit` and `npm run build` both clean. **Nothing deployed** — no new public Convex query/mutation was added (all new exports are plain functions called from inside the existing `computeHourly` internalMutation), so no `npx convex codegen`/`deploy` was needed or run; the live instance still runs pre-105-04 code and the fail-open alert path has never fired against real data. OBS-01/02 NOT marked complete (Wave 3 of 5, established convention). See `105-04-SUMMARY.md`. Next: Plan 05.
**Phase 105 Plan 05 of 9 COMPLETE (2026-08-03)** — Wave 3: `TraceWaterfall` now nests tool executions under the LLM round that triggered them (D-09), never guessing an attribution Ástríðr didn't report (D-10), and appends a per-turn cache-ratio to each turn header (D-11). New exported pure helpers: `groupCacheRatio` (mutation-verified identical to `computeSummary`'s session-wide read/(read+creation+prompt) denominator), `groupRoundsForTrace` (partitions a trace group's LLM rows + the session's tool rows into per-round buckets, `typeof === "number"` guarded throughout so round 0 survives), `toolBarMetrics` (seconds-domain bar geometry, honest `hasDuration:false` for unmeasured tool calls). **Finding F1 applied:** the second nesting level is the ROUND, not an individual LLM call — a round can hold more than one `llm_call` row (retry/advisor pass), and attaching tool rows to one specific call inside a multi-call round would be exactly the inference D-10 forbids; single-call rounds render `Round {n} · {model} · {cost}`, multi-call rounds honestly render `Round {n} · {k} calls · {cost}`. Three structurally distinct honest cases (F2): legacy unrounded LLM rows render flat (no synthetic round header); tool rows with a matching `traceId` but no `round` render under "Tool calls with no reported round · {n}"; tool rows matching no rendered trace group at all render under "Untraced tool calls · {n}" at the bottom. New `TraceToolRow` bar (`var(--chart-2)` success / `var(--status-error)` failed, `"duration n/a"` never a fabricated `"0ms"`). The existing D-12 truncation banner now covers both feeder queries (`llm.sessionCalls` + `toolExecutions.listBySession`) in one element, naming which side(s) capped. Test mock scaffold upgraded from a single `mockReturnValue` to a query-identifier dispatcher (F5), migrating all 35 pre-existing assertions with zero tests removed, plus 10 new rendering tests including a scoped `within()` attribution-honesty negative assertion. Both required mutation proofs passed (dropping `cacheCreationSum` from the cache-ratio denominator fails 2 of 3 parity tests; making round attribution fall back to the group's highest round — the exact D-10-forbidden inference — fails the attribution-honesty control). Two deviations: 1 Rule-1 comment-trips-own-grep fix (same class 105-01/02/03/04 each independently hit) and 1 Rule-2 completeness fix (an honest header for the edge case of a round with zero LLM rows, avoiding a crash on `round.llmRows[0]`). Full suite 3328/3328 passing (up from 3305); `tsc --noEmit` and `npm run build` both clean; zero hardcoded hex; `SessionDetail.tsx` untouched (F7). **Nothing deployed** — frontend-only plan, no Convex schema/query change. OBS-03 NOT marked complete (Wave 3 of 5, established convention) — real-trace-size legibility and the truncation notice on a genuinely-capped session are explicitly deferred to plan 105-09. See `105-05-SUMMARY.md`. Next: Plan 06.
**Phase 105 Plan 06 of 9 COMPLETE (2026-08-04)** — Wave 4: OBS-01's read path + panel. New `convex/toolAnalytics.ts`: `classifyToolSource` resolves the D-02 `gateway:claude-cli`/provider-less collision (finding F1) via a tool-name-wins rule over `ASTRIDR_TOOL_PROVIDER`; `usageOverTime` pre-seeds every hour boundary at zero so a gap in tool activity renders as a real zero bar, never a silently-closed point (T-105-30); `usageByTool` returns `avgDurationMs: null` (never `0`/`NaN`) when no duration bucket exists and a `sources[]` list computed independent of the active filter; `recentExecutionsBySource` drills down via the `by_provider_time` index for Ástríðr, a `RAW_SCAN_CAP=500`-bounded JS-classified scan otherwise. Every read capped at `AGG_READ_CAP=5000`, `windowHours` clamped to `MAX_WINDOW_HOURS=720` (T-105-26/T-105-27); zero `.collect()`, zero `?? 0`/`|| 0` fallbacks anywhere in the file. `src/hooks/useToolUsage.ts` ships the three `useQuery(...) ?? DEFAULT` wrapper hooks; `src/components/ToolUsagePanel.tsx` is a self-contained panel (D-02 source filter defaulting to Ástríðr, widest option labelled "All sources" never bare "All"; summary strip; D-12 truncation banner; two `FlexBarChart`s; a per-tool table with honest `n/a` durations/rates; the UI-SPEC empty state verbatim) — zero hex, no `SectionErrorBoundary` (105-08 owns page wrapping), no page/route/nav file touched (finding F5). One Rule-1 bug caught by the plan's own required TDD test before commit (the initial per-row source filter flattened aggregate rows in a way that dropped `provider`, silently misclassifying every astridr row — fixed with a shared `matchesSourceFilter` predicate applied to the already-classified `{tool, source}` pair) plus the now-familiar comment-trips-own-grep fix (two comments independently used the literal "All sources" string). Both required mutation proofs passed (`avgDurationMs`'s zero-guard removed → `NaN` instead of `null`, both at the query layer and independently at the component's render layer). 37 new tests (27 + 10); full suite 3365/3365 passing (up from 3328); `tsc --noEmit` and `npm run build` both clean; `npx convex codegen` regenerated `api.d.ts` (offline only, no deploy). OBS-01 NOT marked complete (Wave 4 of 5, established convention — the panel is not yet mounted on any page and nothing is deployed). See `105-06-SUMMARY.md`. Next: Plan 07 (`ToolPolicyFeed`, OBS-02).
**Phase 105 Plan 07 of 9 COMPLETE (2026-08-03)** — Wave 4: OBS-02's policy/leak feed component. New `src/hooks/useToolPolicyEvents.ts`: `useToolPolicyEvents`/`useToolPolicyCounts` follow the `useQuery(...) ?? DEFAULT` convention; `useToolPolicyLastReceived` deliberately returns the raw query result undefaulted (matching `useCostBudget`'s precedent) so D-07's "still loading" (`undefined`) can never collapse into "never received" (`{ timestamp: null }`). `policyKindPresentation(kind)` implements the UI-SPEC's locked four-kind label/token map and derives its `alerts` flag from `convex/toolPolicyAlertEval.ts`'s own `ALERTING_POLICY_KINDS` constant (imported directly, never re-typed) — proven by a set-equality test so the badge and the evaluator can never disagree; an unrecognised kind renders its raw string verbatim, never hidden. New `src/components/ToolPolicyFeed.tsx`: self-contained, no required props; kind-filter pills (zero-count kinds shown disabled with their zero visible, not hidden); the D-07 last-received line rendered in both the populated and empty states across three distinct on-screen states (loading/never/a real relative time, UNIX-seconds asserted not assumed — the `1a136dc8` seconds-as-ms guard); the D-08 expandable detail block renders only the fields a row actually carries, with `toolWasOffered`/`toolsOfferedCount` each kept to their real three states (undefined/false/true, undefined/0/n); Bell + `InfoTooltip` on the two alerting kinds only; the D-12 truncation banner and the UI-SPEC page-level disclaimer verbatim. **Rule 1 fix mid-plan (caught by the plan's own required TDD test before any commit):** the first draft's empty-state body unconditionally claimed "never received" even while `lastReceivedAt` was still loading — since `useToolPolicyEvents` collapses loading to an empty `rows: []` by design, the empty state fired immediately on mount with a false claim; replaced with a three-state `emptyStateBody()` matching the header line's own honesty. A second Rule-1 fix: the first mutation-regression test (boot vs denied) proved vacuous against the required token-regression mutation (both stayed distinct either way) — replaced with the pair that actually collides (denied vs leaked-as-text), re-verified it fails under the mutation. Plus the now-familiar comment-trips-own-grep class (two instances). Both required mutation proofs passed and reverted byte-identical (toolWasOffered two-state collapse; execution_denied token regression). 18 new tests; full suite 3383/3383 passing (up from 3365); `tsc --noEmit` and `npm run build` both clean. **Nothing deployed** — frontend-only plan, `convex/toolPolicyEvents.ts`/`toolPolicyAlertEval.ts` both unmodified read-only dependencies. OBS-02 NOT marked complete (Wave 4 of 5, established convention — the panel is not yet mounted on any page; live delivery of real induced policy events is plan 105-09's gate). See `105-07-SUMMARY.md`. Next: Plan 08 (Tools page assembly — mounts both `ToolUsagePanel` and `ToolPolicyFeed`).
**Phase 105 Plan 08 of 9 COMPLETE (2026-08-03)** — Wave 5: page assembly. New `src/pages/Tools.tsx` mounts `ToolUsagePanel` (105-06) above `ToolPolicyFeed` (105-07), each in its own named `SectionErrorBoundary` (D-16) — proven independent by a mocked-throw isolation test matching the `3b31c9f4` remediation shape, not just by inspection. `/tools` is now a real lazy-loaded route in `App.tsx` and a real OBSERVE nav entry in `src/lib/navRegistry.ts` with a genuine `Wrench` icon (both the import AND the `iconComponents` map entry — an unmapped key silently falls back to `LayoutDashboard`, T-105-38), placed immediately after `/quality`; `GRAPHS` (Tool Galaxy, MCP Inventory) confirmed untouched. D-15 cross-links (`View in Tools →`, `/tools`) added to all three pre-existing panels (`ToolBreakdown`, `ToolExecutionPanel`, `PermissionDecisionsChart`), additive only — the two panels with an existing flex header row got an inline `ml-auto` link (0 lines deleted each); `PermissionDecisionsChart` had no flex header row on either state, so used the plan's own documented smallest-possible-wrapper fallback (1 line deleted, 5 added, still within budget). `Dashboard.tsx`/`Analytics.tsx` (the three panels' mount sites) confirmed untouched. Six mutations run (boundary merge, section-order swap, and each of the three link removals), all caught, all restored byte-identical. 10 new tests (5 page + 2 ToolBreakdown + 2 ToolExecutionPanel + 1 PermissionDecisionsChart, the latter two panels previously had no test file at all); full suite 3393/3393 passing (up from 3383); `tsc --noEmit` and `npm run build` both clean, `npm run build` producing a dedicated `Tools-*.js` chunk confirming the route resolves. One deviation, not a code defect: `CLAUDE.md`'s "Adding a new page" pattern named the stale pre-Phase-96 `DashboardLayout.tsx` location for nav entries — corrected to `src/lib/navRegistry.ts` per this repo's own Stale Docs rule. `SectionErrorBoundary`'s pre-existing hardcoded-hex debt flagged for Phase 106 (used verbatim, not modified, per the UI-SPEC's own instruction). OBS-01/OBS-02 still NOT marked complete in REQUIREMENTS.md (established "green suite != live-verified" convention — the page is assembled and reachable, but live data/four-theme confirmation is 105-09's gate). See `105-08-SUMMARY.md`. Next: Plan 09 (live validation — the last plan in Phase 105).
**Phase 106 Plan 01 of 8 COMPLETE (2026-08-04)** — two read-only verification sweeps, zero source files modified. **DEBT-01 (typed-api verify-and-close): PASS.** Raw grep found 3 `anyApi` hits in `src`+`convex` (excluding `_generated/`), all classified: every one is a comment in `costBudgetEval.test.ts`/`evalScores.test.ts` explaining Proxy-identity instability, none is an import or real usage; the comment-filtered gate and the import-shaped gate both returned zero lines; `npx tsc --noEmit` exited 0; `src/pages/Ideation.tsx` spot-checked and confirmed routing every Convex call through the typed `api.*` object. Scope stated explicitly as codepulse-only per D-06 (astridr-repo POSTs to HTTP ingest endpoints, never calls the Convex API object). **DEBT-02 (cross-repo cloud-Convex pre-flight sweep): NO-GO — blocks plan 106-03.** Swept both codepulse and astridr-repo for all 5 required patterns (deployment name, cloud hostnames, `VITE_CONVEX_URL`/`CONVEX_DEPLOYMENT`, deploy/CI surfaces, Forge daemon config) with every command and verbatim output recorded. Found a real, currently-active functional writer to the cloud deployment that CONTEXT.md's own D-03 hypothesis ("an initial code grep already came back clean") did not catch: **3 CI workflow files across both repos hardcode `CODEPULSE_INGEST_URL: https://tidy-whale-981.convex.site`** and POST live telemetry there today — codepulse's `gitleaks-scan.yml` (push/PR to master) plus astridr-repo's `gitleaks-scan.yml` (push/PR), `kg-benchmark.yml` (push/PR/dispatch), and `supabase-migration-check.yml` (**daily 7 AM UTC cron**, currently unattended). `kg-benchmark.yml`'s telemetry step is explicitly not `continue-on-error`, so it would hard-fail the job once tidy-whale-981 is cancelled. Everything else swept was correctly classified as non-functional: astridr-repo's `CONVEX_URL`-family reads (`reminders.py`/`focus_digest.py`/`reminder_nudge.py`/`langfuse_eval.py`/`war_room` modules) all default to empty-string or the local self-hosted backend (verified by reading the actual resolution code, not just grep hits); `docker-compose.yml` defaults to `http://convex-backend:3211`; a side-finding of a *different* stale cloud deployment slug (`ideal-sandpiper-297`, referenced as a fallback in astridr-repo's `hooks/` Claude-Code-hook connector scripts) was flagged separately as out of this sweep's named scope. The one load-bearing unresolved check — `VITE_CONVEX_URL`'s actual resolved runtime value, the definitive answer to "does prod CodePulse read cloud Convex" — could not be confirmed (source is a hook-blocked `.env.local`; Vercel dashboard env config is outside this sweep's reach); recorded as a manual check rather than assumed clean, alongside 4 other un-sweepable `.env*` files (codepulse `.env.local`; astridr-repo `.env`, `.env.bak.20260730-173554`, `.env.bak.20260731-081147`), each explicitly labelled `MANUAL — PENDING LARRY`. Quoted and classified both `.planning/AVATAR-HANDOFF.md`'s known-stale 2026-07-08 cloud claim (per D-01) AND a second, previously undocumented stale claim found in astridr-repo's own later `157-RESEARCH.md` (v23.0, out of this plan's repo scope to fix) as doc-only, not functional readers. **Plan 106-03 (cloud export/cancel) must not run until Larry completes the 4 manual `.env*` checks and the 3 CI workflows are repointed to the self-hosted ingest URL or their repointing is folded into 106-03's own scope.** No deviations — the sweep's real-hit finding is the plan's designed failure mode working as intended, not unplanned work. See `106-01-SUMMARY.md` and `106-DEBT-VERIFICATION.md`. Next: Plan 02 (subject to whatever 106-02 covers per the 8-plan roadmap — DEBT-02's cloud export itself is gated NO-GO until the above is resolved).
**Phase 106 Plan 02 of 8 COMPLETE (2026-08-04)** — DEBT-03 measured bundle baseline, zero application source files modified (only `vite.config.ts`, additively). New opt-in `chunk-composition-report` inline Vite plugin (`ANALYZE_BUNDLE=1 npm run build`, matches the existing `drop-unused-ort-wasm` inline-plugin convention) emits `dist/chunk-composition.json` with per-chunk `renderedBytes`/`isEntry`/`isDynamicEntry` and the top 30 modules by `renderedLength` (plus a reconciling tail count/bytes); a default `npm run build` is confirmed byte-for-byte unaffected (no `chunk-composition.json` emitted). Ran the real build and recorded `106-BUNDLE-ANALYSIS.md`: all four of D-09's chunk sizes reproduced exactly — `index` 2,047.37 kB, `react-force-graph-3d` 1,293.89 kB, `useSpeechRecognition` 638.56 kB, `WarRoom` 485.00 kB (confirmed under the 500 kB threshold). Classified every >500 kB chunk with file:line proof: only `index-fgkf2HV8.js` (the entry chunk) is a genuine unconditional-load defect; `react-force-graph-3d` and `useSpeechRecognition` are both reachable only via `lazy()` boundaries (`CodeVaultGraph.tsx:67`, `SkillVaultView.tsx:28`, `App.tsx:36`/`45`), traced from source rather than assumed off the `isDynamicEntry` flag (both report `false` because they're shared dependency chunks split out from two lazy importers each, not dynamic-entry points themselves). New finding beyond D-09: the `useSpeechRecognition` chunk's actual weight is ~94% unused `refractor`/Prism language-grammar data (`react-syntax-highlighter`'s full `Prism` bundle in `CodeBlock.tsx`/`ChatBubble.tsx` registers every language refractor ships, not just the ones this dashboard renders) — the chunk's name is a red herring, not the voice stack D-09's framing implied. Ranked, byte-sourced remediation candidates recorded for plan 106-04: (1) refractor/Prism full-bundle trim (~774,578 bytes), (2) four App.tsx pages still statically imported instead of `lazy()` — `Settings`/`Memory`/`Security`/`Capabilities`, 134,533 measured bytes, plus 9 more named-but-unmeasured candidates beyond the 30-module cap, (3) `Dashboard`→`@xyflow` lazy conversion (234,442 bytes, flagged with its landing-page loading-spinner tradeoff), (4) `@dnd-kit/core` (84,979 bytes, likely a side-effect of fixing #2's `Settings.tsx`). No deviations. See `106-02-SUMMARY.md` and `106-BUNDLE-ANALYSIS.md`. Next: Plan 03 (blocked NO-GO per Plan 01 until Larry resolves the cloud-Convex manual checks, or its repointing is folded into 106-03's scope).
**Phase 106 Plan 06 of 8 COMPLETE (2026-08-04, executed out of wave order — UAT session A, independent of the 106-03/04/05 dependency chain, same precedent as 104-10):** DEBT-04's Session-A scope — closing the three Phase-98 Test-4 menu sub-cases left browser-pending, plus a live re-verification that the D-07 stale-project-origin fix (98-05) still holds. Four disk-staged `uat106-*` fixtures registered live via a real Clerk-authenticated archive/restore round-trip (CLI `--identity` impersonation and a hand-built registry snapshot were both evaluated and rejected — the latter risked pruning the entire 695-row live catalog per `computeSkillPrunes`'s origin-manifest semantics). All 4 live tests PASS: (1) active single-scope — Archive + Move enabled, no tooltip; (2) dormant non-shadowed — Restore enabled, page intact, closes the sub-case Phase 98 left open; (3) multi-scope — Archive/Move correctly disabled, tooltip verbatim "Active in multiple scopes — disambiguation ships in a later release." (traced to `SkillLifecycleMenu.tsx:296-297`/`skills.ts:104-109`), with a documented wording discrepancy against this plan's own acceptance criteria (generic stub, not scope-naming — pre-existing, no fix); (4) stale-project-origin re-verification — moving the workspace's last skill out pruned the now-empty project origin entirely (confirmed via direct `registry:listSkills` query, before/after states both recorded), Archive/Move stayed enabled — the 98-05 fix holds. D-07's code-fix contingency branch is a proven no-op: Test 4 passed, so `convex/skillSync.ts`/`convex/__tests__/skillSync.test.ts`/`src/components/skills/SkillLifecycleMenu.tsx` were all left untouched (`git status --porcelain convex src` empty). All 5 fixture directories removed from disk; a second real lifecycle round-trip triggered the daemon rescan for cleanup verification; zero `uat106-*` residue confirmed by direct registry query, matching Phase 98's own bar. `npx vitest run convex/__tests__/skillSync.test.ts` 20/20 pass; `npx tsc --noEmit` clean. Mid-session, a concurrent-session STATE.md clobber (total_phases 4→8, percent 75→38, stopped_at/last_updated regressed) was caught via the pre-commit `git diff` check and restored from `HEAD` before any commit could sweep it in — not caused by this plan. `106-HUMAN-UAT.md` left `status: in-progress` — plans 106-07/106-08 extend the same artifact. See `106-06-SUMMARY.md`. Next: Plan 03 (still blocked NO-GO per Plan 01, independent of this plan).
**Phase 106 Plan 04 of 8 COMPLETE (2026-08-05)** — DEBT-03 remediation. **Entry chunk 2,042,353 → 563,616 bytes (−1,478,737, −72.4%)**, against a ≥40% requirement. Three deferrals, each measured separately by taking three builds rather than one: (1) all 14 remaining statically imported pages in `src/App.tsx` converted to the file's existing route-level `lazy()` + `Suspense` convention — `Dashboard` included, deliberately, since leaving the landing route eager is what kept `@xyflow` (234,442 B) and its whole panel graph in the entry chunk — taking it to 837,729 B; (2) the Tone.js synthesis library moved behind a memoised dynamic import in `src/lib/audioEngine.ts` (type-only namespace import for the 2 type positions + module-local `let Tone!` assigned by a `tonePromise ??= import("tone")` loader awaited from the already-async `start()`, so all ~90 runtime `Tone.X` call sites are untouched), taking it to 590,788 B and putting the library in its own `isDynamicEntry` chunk (340,276 B); (3) still ≥512,000, so per the plan's own instruction the residual list was *worked* not merely documented — `react-easy-crop` (36,362 B) reached the entry chunk via `DashboardLayout.tsx`'s static `AvatarUploader` import even though the uploader only renders inside a dialog closed on every page load, so it took the `CodeVaultGraph.tsx` sub-component lazy pattern → **563,616 B final**. `@dnd-kit/core` (84,979 B) left as a side effect exactly as 106-02's candidate #4 predicted. **The baseline was re-measured from the pre-plan tree rather than quoted from 106-02's prose** (2,042,353 vs the recorded 2,042,261 — a 92-byte drift from four intervening commits), so before and after share a build; the Task-1-only build additionally *proved* tone was in the baseline entry chunk, which 106-02 could not show at its 30-module cap. **VERDICT: NOT under 512,000** — the 51,616-byte residual is enumerated in full with a `file:line` mount site each (react-dom 452,138 pre-min; Clerk's auth gate wrapping every route 53,145+32,587; the app-wide `sonner` toast host three headless shell listeners publish to 51,597; the Radix select behind the always-visible header ThemeSwitcher 42,988) — going lower means deferring something that must run before first paint, which moves the wait rather than removing it. **`chunkSizeWarningLimit` was NOT raised and no `manualChunks` added** (grep = 0); the build still warns on 3 chunks, which is the intended end state. The analysis states plainly that **bytes were relocated, not deleted** — total emitted JS actually rose 6,647,495 → 6,766,860 across 111 → 188 chunks; what changed is that a visitor to one route no longer downloads the other thirteen. The `useSpeechRecognition` chunk (635,483 B) was *resolved, not assumed*: proved not entry-reachable from the composition JSON (no entry module matches `useSpeechRecognition`/`ChatInput`/`refractor`), both consumers lazy (`App.tsx:44`/`:53`), and 95% of its captured bytes are `refractor` grammars — the Prism-light allowlist trim is left open in `deferred-items.md` with reasons (not voice-gated, two import sites not one, behavioural change, already-lazy chunk). 5 mutation proofs, all restored byte-identical; **one exposed a confound** (swallowing the load failure left the failure test green, because an empty module makes `Tone.start` undefined and `start()` rejects anyway — the test now asserts on `loadTone()` directly), and a near-miss where a multi-line `perl -0pi` silently matched nothing against CRLF was caught by verifying the mutation had landed before believing the run. Deviations: `loadTone` exported (an unexported loader has no observable memoisation under vitest's module cache — the `??=` mutation would have gone undetected); a failed load de-memoised so one transient error can't permanently disable audio; the racy fallback assertion replaced with a deterministic source-shape guard (vitest resolves the dynamic import inside `render()`'s own `act()` flush, so it failed on a different route each run); `convex/react` mock widened with the complete remaining export set; `findBy*` windows widened to 20s for tests that genuinely import whole page trees. `src/contexts/AmbientContext.tsx` needed **no** change (it already awaits `start()` with its own catch) — recorded, not churned. Full suite **3430 passed** (from 3401), 3 consecutive clean runs; `tsc --noEmit` and `npm run build` clean; zero package.json/lock changes. DEBT-03 NOT marked SATISFIED — the sub-500 kB goal is unmet and 106-05's laptop Tailscale half hasn't started. Also logged: a pre-existing intermittent failure at `src/pages/Chat.test.tsx:576`, caught on the *pre-change* baseline run and not caused by this plan. See `106-04-SUMMARY.md` and `106-BUNDLE-ANALYSIS.md` § `## After remediation`. Next: Plan 05, or Plan 03 once its NO-GO clears.
**Phase 105 Plan 09 of 9 COMPLETE (2026-08-04) — PHASE 105 COMPLETE, all 3 requirements SATISFIED.** Live validation against the running self-hosted stack (deployment target proven: tailnet URL, not the retired cloud instance). Schema pushed (`No indexes are deleted by this push`); astridr container rebuilt and proven on 105-02's commit by content grep, not timestamp. D-07: all 4 `tool_policy_event` kinds landed live — `malformed_policy_reload_rejected`/`malformed_policy_boot` via real, deliberate, immediately-reversed policy-file corruption; `execution_denied`/`tool_call_leaked_as_text` via the sanctioned F5 synthetic fallback after real attempts were exhausted (`execution_denied` proved *structurally* unreachable from the commander chat session — `TASK_CATEGORY_TO_CLUSTERS` offers it every cluster; the leak kind survived two real prompts the model correctly refused). D-06 isolation control passed: exactly 2 alerts, correct severities, dedup confirmed, zero enforcement wording. D-01: real Ástríðr `weather` tool calls landed with populated `durationMs`/`traceId`/`round`. OBS-03: trace nesting and cache ratio hand-verified on a real 2-round session (`27980/(27980+27980+946)=49.17%` → displayed `49%`, exact). Four-theme sweep clean; console clean throughout. **Two real defects found live, both fixed and deployed, neither visible to the green unit suite:** (1) `WebhookStatusBadge` mislabeled every digest/dashboard-only/muted alert as "Retrying (0/3)" (pre-existing since Phase 06-05) — two new terminal `webhookStatus` values (`digest`/`skipped`) with accurate copy. (2) D-15 regression — `runtimeIngest.ts`'s pre-existing `command_execution` case (predates Phase 105) inserted a second, untagged `toolExecutions` row for every Ástríðr tool call, leaking Ástríðr activity into `ToolExecutionPanel`/`PermissionDecisionsChart` (confirmed live: one induction bumped the Claude-Code-only `weather` count 2→3) — fixed going forward (`resolveCommandExecutionToolRow`) AND its ~2-week historical backlog (91 untagged rows dating to 2026-07-22) retagged via a new scoped `convex/migrations.ts` migration (`backfillAstridrProviderTag`, dry-run verified then applied), live-confirmed clean on the actual Dashboard. D-12 truncation and the "unattributed tool rows" render path — neither organically reproducible (no live session approaches 1000 calls; no real session combines LLM calls with an untraced tool row) — were live-verified via a second scoped migration (`seedTruncationTestData`/`cleanupTruncationTestData`): exactly 1000 synthetic rows under a dedicated, clearly-fake test sessionId (never a real Ástríðr session) produced the exact expected truncation banner text, a correctly-separated "TOOL CALLS WITH NO REPORTED ROUND" section, and a correctly-separated "UNTRACED TOOL CALLS" section; all seed data deleted immediately after, re-verified empty. `docker stats convex-backend` stable throughout (62.03%→62.66%) across ~2000 total row writes/deletes and 4 deploys. `nyquist_compliant: true` — every Manual-Only row genuinely passed. OBS-01/02/03 all marked SATISFIED in `REQUIREMENTS.md`. See `105-09-SUMMARY.md` and `105-VALIDATION.md`. **Phase 105 (v13.0) now 3 of 4 phases complete. Next: Phase 106 (Consolidation & Hardening) — the last phase in v13.0.**

Plan 2 of 11 COMPLETE (2026-07-31) — ingest prerequisites (cross-repo): `convex/runtimeIngest.ts` gained a `gateway_task_completed` case writing `llmMetrics` rows for CLI-gateway/subscription turns (D-18, `billingType: "subscription"`, model keyed on the opaque provider id per D-06's only viable branch, honesty-tagged `:tokens-unreported` when no tokens are reported); `convex/gatewayQuota.ts`'s `pollAndStore` repointed at a new `CLI_GATEWAY_URL` env var (D-20, no fallback to the dead `ASTRIDR_API_URL` target). Cross-repo astridr-repo change (commit `9adb25b6` on branch `feature/brain-swap`, NOT yet merged to `main` or deployed): `claude_cli.py` now extracts real token counts from the CLI's stream-json `usage` object onto the gateway telemetry payload end-to-end through `web.py`'s forwarding handler; `codex_cli.py`/`antigravity_cli.py` confirmed to have no usage object at all. 33 codepulse + 55 astridr-repo tests passing. See `104-02-SUMMARY.md`.
Plan 3 of 11 COMPLETE (2026-07-31) — rollup widening + backfill: `convex/aggregates.ts` `computeHourly` now writes `tokens_prompt`/`tokens_completion` hourly buckets on the existing `{provider, model, billingType, goalId}` dimension key (D-04), each behind its own idempotency guard, filled from the already-fetched `llmRows` pass (no new scan); `rollupDaily` needed no code change (generic metric_type grouping already covers the new types). New `backfillTokenSplit` internalMutation (manually invoked, `npx convex run aggregates:backfillTokenSplit '{"maxHours": 6}'`, repeated until `done`) resumably fills the same split for history, insert-only end to end (including its own `agentConfigs` cursor, written via insert-not-patch), capped at `maxHours` (default 6/invocation), NOT on a cron. Retention floor resolved from `agentConfigs["retention_days"]` (default 30d) — `convex/retention.ts` itself excludes `llmMetrics` from its pruned tables ("kept forever" there), so that file defines no retention window for this table; the plan's own framing of `retention.ts` as the reach limit didn't hold against the live code, documented as a decision in `104-03-SUMMARY.md`. A full backfill at defaults needs 120 invocations (720h / 6h). 10 new unit tests (34/34 in `aggregates.test.ts`); full repo suite 3002/3002 passing; `tsc --noEmit` clean. See `104-03-SUMMARY.md`.
Plan 4 of 11 COMPLETE (2026-07-31) — cost budget persistence + legacy cap migration: `convex/costBudgets.ts` ships pure UTC-anchored period-boundary helpers (`periodStartFor`/`periodEndFor`/`periodHours` for daily/weekly/monthly — D-10, matching `crons.ts`'s `hourUTC`/`rollupDaily`'s day-floor/`SDKSpendGuard`'s identical floor), Clerk-gated `create`/`update`/`remove` (D-07 derives `unit` server-side from `scope`, never a caller arg; `update` rejects changes to the now-immutable `scope`/`scopeKey`/`period`), unauthenticated `list`/`get`/`getByScope` reads, and an idempotent additive-only `seedFromLegacyCaps` internalMutation migrating `SDKSpendGuard`'s hardcoded `DAILY_CAP=5.00`/`ALERT_THRESHOLD=0.8` (D-12) and `agentConfigs["intelligence.budget_cap"]` (D-19) into seed rows — the monthly seed is skipped honestly (no fictional threshold) when no legacy value exists. Live-checked via `npx convex run forecasts:getBudgetConfig '{}'` → `{budgetCap: null}`: **the live deployment has no `intelligence.budget_cap` row today**, so running the seed live will populate only the daily budget; plan 104-08 should expect the monthly axis to need an operator-set value. 15 new unit tests passing; full repo suite 3017/3017 passing; `tsc --noEmit` clean. See `104-04-SUMMARY.md`. Next: Plan 05.
Plan 5 of 11 COMPLETE (2026-07-31) — cost derivation layer: `convex/costDerived.ts` ships `deriveBucketDollars` (the single tokens-to-dollars function every cost surface routes through — D-03's two honesty guards: zero-token buckets and unresolved rates both return `priced:false` with BOTH dollar fields strictly `null`, never `0`; D-05: subscription turns get a true `$0` `billedUsd` plus a separate `coveredUsd` shadow figure, never merged) plus `costOverTime`/`costBreakdown`/`unpricedModels` queries (all index-bounded on `aggregates.by_type_period_bucket`, zero `llmMetrics` access) and the plain-async `computePeriodSpend` helper (disjoint daily/hourly bounded reads — never 744 hourly buckets for a monthly budget — D-07 api-only scope filtering) for plan 104-06's evaluator. `convex/aggregates.ts`'s `costByGoalPeriod`/`llmByGoal` (HivePage's goal-scoped `CostBreakdown`) now derive dollars through the same function instead of trusting Ástríðr's ingested `cost` field — resolves RESEARCH.md's Open Question 1 (YES, not deferred): the ingested figure survives only as `reportedTotal`/`reportedCost` (evidence, never the rendered truth), so HivePage and Analytics can no longer disagree (D-01/D-12). `CostBreakdown.tsx` renders a token-driven "Unpriced" badge instead of a fabricated `$0.00` for unpriced rows; data-source rewire only, hex remediation deferred to plan 104-10 per the plan's own scoping. 20 new/updated unit tests; full repo suite 3034/3034 passing; `tsc --noEmit` clean. See `104-05-SUMMARY.md`. Next: Plan 06.
Plan 6 of 11 COMPLETE (2026-07-31) — budget alert evaluator (cron tail-append, D-13/D-14/D-15/D-16/D-17): `convex/costBudgetEval.ts` generalizes `SDKSpendGuard`'s burn-rate projection (`projectPeriodEndSpend`) to any budget period, classifies warn/breach plus the D-13 spike case into exactly two firing levels (`classifyBudgetLevel`), and builds alert copy matching the UI-SPEC contract while naming no enforcement action (`buildAlertMessage`, guarded against 6 forbidden words). `evaluateBudgets(ctx, nowSec)` reads `costBudgets` once (whole-table, low double digits by design), resolves spend per budget via plan 104-05's `computePeriodSpend` (usd) or a single bounded `gatewayQuotaSnapshots.by_provider` read (`quota_pct`, D-07 — an absent snapshot is skipped honestly under `skippedNoData`, never evaluated as 0%), dedupes on `(budgetId, level, periodStart)` via `alerts.by_source` with no status filter (D-15), and fires by inserting directly into `alerts` + scheduling `internal.webhookDelivery.sendAlertWebhook` — never the public `alerts.create` (D-17). `convex/aggregates.ts`'s `computeHourly` now calls `evaluateBudgets` at its tail inside a mandatory try/catch (a throw there logs and is swallowed, never fails the rollup) — **zero changes to `convex/crons.ts`** (D-14's hard constraint: `git diff --stat convex/crons.ts` shows nothing; the disabled `evaluate-alert-rules` block stays untouched, confirming this evaluator adds no new cron and no new retry-storm surface after the 2026-07-14 incident that disabled the Phase-6 evaluator). Added read cost stated per-invocation in `104-06-SUMMARY.md` for plan 104-11's live timing check. 28 new unit tests (14 pure math/copy, 11 dedup/fire/isolation against a fake ctx, 3 cron tail-append against the real `computeHourly` handler via a `vi.mock` pass-through spy); full repo suite 3062/3062 passing; `tsc --noEmit` clean. COST-03 NOT marked satisfied in REQUIREMENTS.md this plan — the backend fire-and-deliver path is complete, but per this project's "green suite ≠ live-verified" convention (and matching 104-01/104-04/104-05's own precedent), the full delivered-through-the-existing-routing-layer claim awaits plan 104-11's live confirmation against the running self-hosted instance before being marked. See `104-06-SUMMARY.md`. Next: Plan 07.
Plan 7 of 11 COMPLETE (2026-07-31) — Cost & Budgets admin UI: `src/hooks/useModelPricing.ts`/`useCostBudgets.ts` ship `useQuery(...) ?? DEFAULT` wrapper hooks (`useCostBudget` deliberately returns raw `undefined` while loading, distinct from a genuine `null` "no budget configured", for plan 104-08). `src/components/ModelPricingAdmin.tsx` is a Sheet-based CRUD list for `modelPricing` — displays per-Mtok, stores per-token via one named conversion helper each direction (round-trip unit-tested), an "Unpriced models" section reading the exact same `costDerived.unpricedModels` query the Analytics nudge (104-09) will read, accessible icon-only row actions, zero hardcoded hex. `src/components/CostBudgetsAdmin.tsx` is a Sheet-based CRUD form spanning all 4 `costBudgets` scopes (global/model/provider/quota) from one form (D-09), unit derived from scope not caller-set (D-07), UTC period boundaries stated in copy (D-10), warn-fraction helper recomputed live (D-11), immutable scope/scopeKey/period shown disabled on edit, and a fixed D-16 disclaimer ("Budgets raise alerts. They don't stop work.") with a unit test guarding against enforcement-implying words anywhere else in the render. **Decision:** no fabricated progress bar anywhere in `CostBudgetsAdmin` (including global rows) — `costDerived.costBreakdown` has no exact per-budget-scope/period figure without re-derivation, and `SDKSpendGuard`/`CostForecastPanel` aren't yet rewired onto `costBudgets` (that's plan 104-08), so every row instead states "Threshold set — progress shown on Analytics." `src/pages/Settings.tsx` gained a "Cost & Budgets" tab (after LLM Providers, before Notifications) hosting `CostBudgetsAdmin` above `ModelPricingAdmin`, each independently wrapped in its own `SectionErrorBoundary`; no new route, `App.tsx`/`DashboardLayout.tsx` untouched. Regenerated `convex/_generated/api.d.ts` (Rule 3 blocking fix — it had never picked up `modelPricing`/`costBudgets`/`costDerived`/`costBudgetEval` since plans 104-01/04/05/06). 14 new component tests; full repo suite 3078 tests passing; `tsc --noEmit` and `npm run build` both clean. See `104-07-SUMMARY.md`. Next: Plan 08.
Plan 10 of 11 COMPLETE (2026-07-31) — cost trend toggle + hex remediation (executed out of wave order — this is wave 3; plans 08/09, wave 4, have NOT yet run): `src/components/CostTrendChart.tsx` reads `api.costDerived.costOverTime` instead of the pre-baked `costByPeriodByProvider` (D-01) and gains a D-08 `Billed` / `Billed + Covered` toggle (a `Button`-pair, not shadcn `Tabs`) defaulting to `Billed`; the covered segment renders at `color-mix(in srgb, PROVIDER_COLORS[provider] 35%, transparent)` (same exempt provider hex, reduced opacity), always appended after billed segments and never summed into one value (D-05, unit-tested with a fixture where the same provider appears in both maps). A caption states excluded unpriced tokens (D-03) instead of dropping them. The redundant `bg-gray-800/50 border-gray-700/50` surface wrapper was removed entirely (`Analytics.tsx` already wraps this component in a `GlassPanel`). `src/components/CostBreakdown.tsx`'s hex-to-token remediation (deferred by 104-05) is now complete — tier-flag/runaway-warning/Opus-row colors all read `var(--status-ok/warn/error)` via `color-mix`, matching `MetricCard.tsx`'s `severityConfig` pattern; data flow from 104-05 untouched. Both files verified at zero hex outside the exempt `PROVIDER_COLORS` palette. 9 new tests (`CostTrendChart.test.tsx`) + 2 new tests (`CostBreakdown.test.tsx`); full repo suite 3091/3091 passing; `tsc --noEmit` clean. See `104-10-SUMMARY.md`. Next (at the time): Plan 08/09 (wave 4, not yet executed), then Plan 11 (live verification/UAT).
Plan 8 of 11 COMPLETE (2026-07-31) — cap source consolidation (D-12/D-19): collapsed all four independent spend-cap sources onto `costBudgets`. `SDKSpendGuard.tsx` now reads `useCostBudget("global", "", "daily")` instead of hardcoded `DAILY_CAP=5.00`/`ALERT_THRESHOLD=0.8` (both removed; `projectDayEndSpend`'s cap is now a required parameter); three honest states (loading/no-budget-with-a-link-to-Settings/real-gauge), sparkline color now reads `useThemeColors()` instead of hardcoded hex. `convex/forecasts.ts`'s `costForecast` reads the global/monthly `costBudgets` row via `by_scope_key_period` instead of `agentConfigs["intelligence.budget_cap"]`, returning `warnFraction` alongside `budgetCap`; `classifyBudgetStatus` gained a back-compat `warnFraction=0.8` default; `getBudgetConfig`/`setBudgetCap` marked DEPRECATED (left deployed, deploy-order safety, T-104-36 accepted risk) rather than deleted. `CostForecastPanel.tsx`: zero layout/typography diff, null-budgetCap state now reads "No monthly budget set" with a Settings link, still no fabricated bar/percentage. Task 3 found and closed a FOURTH, undocumented cap source neither CONTEXT.md nor RESEARCH.md named: `src/pages/Settings.tsx`'s `IntelligenceSettings` had its own budget-cap form wired to the now-deprecated `forecasts` functions — removed (not disabled), `Tabs` converted to controlled so a replacement "Go to Cost & Budgets" control can switch tabs. `convex/seedGateway.ts`'s unrelated `DAILY_CAP`/`ALERT_THRESHOLD` (seeding a static `alertRuleCustom` rule, a different concept) renamed to `LEGACY_SEED_*` so the one-cap-source grep gate has exactly one classified exemption there. One-cap-source gate run and fully classified in `104-08-SUMMARY.md`: zero hits on any component/hook/non-deprecated query; every surviving hit is inside a DEPRECATED function, a renamed legacy-seed constant, or a test-local fixture (incl. the pre-existing 104-04 `seedFromLegacyCaps` migration mutation, which by design reads the legacy key once to retire it). Rule 1 fix: `convex/costBudgetEval.test.ts` (not in files_modified) updated for `projectDayEndSpend`'s new cap parameter. Full repo suite 3102/3102 passing (193 todo, 17 pre-existing skipped files); `npm run build` and `tsc --noEmit` both clean. COST-02 requirement checkbox: see `104-08-SUMMARY.md`'s User Setup Required section — live confirmation deferred to plan 104-11 per this project's established convention. See `104-08-SUMMARY.md`. Next: Plan 09 (wave 4, the last unexecuted plan), then Plan 11 (live verification/UAT).
Plan 9 of 11 COMPLETE (2026-07-31) — cost breakdown table + unpriced-models nudge (COST-01 on screen): `src/hooks/useCostDerived.ts` ships `useCostBreakdown`/`useUnpricedModels` wrapper hooks (`useQuery(...) ?? DEFAULT`, matching `useCostByGoal.ts`'s convention) over plan 104-05's `costDerived.costBreakdown`/`unpricedModels` queries. `src/components/CostBreakdownTable.tsx` is a 24h/7d/30d windowed shadcn `Table` (Provider/Model/Billing/Prompt tokens/Completion tokens/Billed/Covered) — an unpriced row renders a single `Unpriced` badge spanning the Billed+Covered columns with its real token counts still shown (D-03, never `$0.00`), a subscription row shows a genuine `$0.0000` billed figure plus its shadow `coveredUsd` with the UI-SPEC tooltip copy verbatim (`InfoTooltip`, no `TooltipProvider` needed), and the footer renders "Billed total"/"Covered total" as two structurally separate rows that are never summed (D-05). `src/components/UnpricedModelsNudge.tsx` renders nothing when the live `unpricedModels` count is 0 or loading, otherwise a status-warn banner with the UI-SPEC copy verbatim and an `Add rates` link to `/settings` — Settings' Cost & Budgets tab is local `useState` component state, not URL-addressable (verified live, not assumed), so the link targets plain `/settings` per the plan's own fallback instruction rather than inventing a query-param scheme. Both mounted in `src/pages/Analytics.tsx`: the nudge as a full-width row above the fold (no `GlassPanel` chrome, per UI-SPEC's "don't compete with the forecast panel" instruction), the table below the `CostTrendChart`/`GatewayQuotaPanel` row — no existing panel's grid weighting, wrapping, or component touched (`git diff` filtered on the four named panels: 0 matches). Rule 1 fix: a doc comment in `UnpricedModelsNudge.tsx` naming the literal substring `localStorage` in prose (explaining what the component deliberately doesn't use) tripped its own acceptance-criteria grep — reworded, same class of fix 104-05's SUMMARY documented for `totalCost`. 15 new component tests; full repo suite 3117/3117 passing; `npm run build` and `tsc --noEmit` both clean. COST-01 NOT yet marked satisfied in REQUIREMENTS.md — per this project's established "green suite ≠ live-verified" convention, live confirmation across all 6 themes and against real `llmMetrics` data is deferred to plan 104-11. See `104-09-SUMMARY.md`. Next: Plan 11 (live verification/UAT) — the only remaining plan in Phase 104.
Plan 11 of 11 **PARTIAL — BLOCKED AT CHECKPOINT** (2026-07-31) — live validation. Task 1 (auto) DONE against the self-hosted deployment at `http://127.0.0.1:3210` (target proven before any write, per LESSONS 2026-07-17): `npx convex deploy` clean with `No indexes are deleted by this push`; `modelPricing:seedDefaults` inserted 23 rows; `costBudgets:seedFromLegacyCaps` seeded the daily budget and correctly REFUSED to invent a monthly one; `aggregates:backfillTokenSplit` reached `done:true` in exactly 120 invocations with convex-backend memory flat ~31.1GiB (ended below its start). Live verdicts in `104-VALIDATION.md`: D-14 PASS (sub-second server-side vs a 15s cap; evaluator confirmed running via its own log line), D-03 PASS (unpriced count of 0 verified ACCURATE against the live model mix), D-04 mechanism PASS (billedUsd == tokens x live table rate to 1e-9 on all 5 rows). Tasks 2-3 are blocking human-verify checkpoints and are NOT done: D-18/A1 need astridr `9adb25b6` merged off `feature/brain-swap` and deployed; D-20 needs `CLI_GATEWAY_URL` set as a Convex env var; D-16 NOT EXERCISED (no alert fired — independently verified as correct arithmetic, not a silent failure). **Phase 104 is NOT complete: no 104-11-SUMMARY.md, no verifier run, no `phase.complete`.**
Plan 11 UPDATE (2026-07-31, Task 2 executed after Larry said proceed): **D-18, D-20 and A1 now PASS live — 6 of 7 Manual-Only rows pass.** Both astridr containers were found running code that PREDATED `9adb25b6` (verified by grepping INSIDE them, not by timestamps); no merge to `main` was needed or done — `feature/brain-swap` is 270 commits ahead and the code was already checked out, so this was a rebuild (`docker compose --profile prod up -d --build astridr cli-gateway`), not a release. One real gateway turn then returned `prompt_tokens: 36137 / completion_tokens: 4`, and Convex went from `{calls:0, tokens:0}` to `{calls:1, tokens:36141}` with the raw `llmMetrics` row carrying `billingType: subscription` and `cost: 0` (a true $0). `CLI_GATEWAY_URL` set to `http://astridr-cli-gateway:8200` (/quota verified HTTP 200 from inside convex-backend); `gatewayQuotaSnapshots` went `[]` -> real rows. REMAINING: 4 Task 3 steps that need a signed-in browser (every costBudgets/modelPricing write is Clerk-gated and the CLI has no identity; the Chrome extension was not connected) — trip a temp budget for D-16/D-15, the live re-price, the honest empty state, and the six-theme pass. Plus D-18's covered-vs-billed render, PENDING the 20:00Z rollup (turn was at 19:13:07Z, inside an incomplete hour; the hourly cron handles it unattended).
Plan 11 UPDATE 2 (2026-08-02, browser pass complete): **PASS** — COST-03 end to end (budget edited to $2.76, evaluator fired, alert rendered on screen), D-16 (message *"Global daily budget at 111% ($3.0629 of $2.7600) — projected to hit $2.7600 by ~08:03 PM."* — concrete figure and time, zero enforcement wording), D-15 dedup (`fired:0, skippedDeduped:1`, still one row), delivery genuinely ran, and the theme sweep across all 4 SELECTABLE themes. D-18's covered-vs-billed render also closed: `billedUsd 0 / coveredUsd 0.180785`, exactly the D-06 shadow rate. **D-03 FLIPPED TO FAILED**: the nudge is faithful (renders 4, query returns 4) but `costDerived.unpricedModels` lists `claude-cli`, which `costBreakdown` simultaneously reports as `priced:true, coveredUsd 0.180785`, and every entry carries ZERO tokens — so the copy implies missing money that doesn't exist; window-unstable too (24h->4, 168h->3, 720h->6). Likely surfaced by this phase's own backfill creating zero-token bucket dims. Step 5 PARTIAL (daily gauge never captured while disabled; the same `budget === null` path IS evidenced on the monthly axis). **The plan's six-theme gate is NOT EXECUTABLE** — `index.css` defines six blocks but `index.html`'s pre-paint script and `ThemeSwitcher` both hard-whitelist the same four; `amber` and light `:root` are unreachable (pre-existing Phase 89). `:root` is a LOAD-BEARING base layer (11 vars only there), not a dead theme. FIXED in-session: `1a136dc8` (WebhookStatusBadge read a seconds timestamp as ms → "Delivered 20645d ago"), `3b31c9f4` (one timing-out query blanked ALL of Analytics because `LlmAnalyticsPanel` lacked a SectionErrorBoundary while 35 siblings had one — 10 panels wrapped, incl. Phase 104's own CostTrendChart; plus a row cap on the unbounded `llm:providerBreakdown`). ROUTED TO GAP PLAN: CR-01 and the `providerBreakdown`→aggregates rewire.
Plan 11 UPDATE 3 (2026-08-02, `aee665c0`): the two SMALL gap items are now FIXED and deployed, so D-03 flips to **PASS**. `priced: false` had been doing double duty — it meant BOTH "no rate resolves" AND "zero tokens reported", so the nudge demanded a rate for `claude-cli`, which was simultaneously `priced: true` with `coveredUsd 0.180785`. Added `unpricedReason: "no-rate" | "no-tokens" | null` and count only `"no-rate"` in both `unpricedModels` and `costBreakdown.unpricedModelCount`; the dollar-field honesty guard is unchanged. Verified against the live data that exposed it: 24h 4→0, 168h 3→0, 720h 6→3, and the three remaining models are each genuinely absent from the 23 seeded rates AND carry real tokens. Also deleted the dead `llm:costOverTime` query + `useCostOverTime` hook (zero consumers in src/, convex/ and astridr-repo). 5 new tests, mutation-verified. **STILL OPEN: CR-01** (`SDKSpendGuard`/`CostForecastPanel` read raw `llmMetrics.cost` against D-01) **and the `providerBreakdown`→aggregates rewire** — both for `/gsd-plan-phase 104 --gaps`.
Plan 11 FINAL (2026-08-03): **104-VALIDATION.md is APPROVED** — `nyquist_compliant: true`, `wave_0_complete: true`, every Manual-Only row executed against the running stack and passing. The last observational row closed on a defect Larry's own testing exposed: toggling the budget off changed nothing because `costBudgets.getByScope` (and `forecasts.costForecast`'s inline read) ignored `enabled`, while `costBudgetEval.ts:209` correctly skipped disabled rows — so the UI painted a gauge, an "On Track" badge and a projection against a cap that could never alert. Both read paths fixed in `b26b22f4`; test fixtures had masked it by omitting the REQUIRED `enabled` field. Re-tested on screen: SDK DAILY CAP now shows `$2.4864 today` + sparkline + "No daily budget set." with no bar, no `$5`, no percentage, no badge. **Live-gate scorecard: 7 defects that no green suite could see, 6 fixed** — convex deploy typecheck (`e9ca3f9a`), multi-paginate in the backfill AND the live `computeHourly` cron plus the over-permissive test mock (`921517db`), the budget-alert dedup read bound (`527adc7f`), ConvexError swallowing + the false delete-rate copy (`d9213c52`), `gatewayQuotaSnapshots` retention (`380f13d9`), the seconds-as-ms "Delivered 20645d ago" badge (`1a136dc8`), the Analytics page-wide blackout from a missing SectionErrorBoundary (`3b31c9f4`), `unpricedModels` accuracy + dead `costOverTime` (`aee665c0`), and this one. **PHASE REMAINS OPEN** — not for validation, but because CR-01 is a live violation of locked decision D-01: `SDKSpendGuard`/`CostForecastPanel` still derive SPEND from raw `llmMetrics.cost` while every other surface derives it from tokens x rates, so two different dollar figures can render on one page. That plus the `providerBreakdown`->aggregates rewire (the `3b31c9f4` row cap is a stopgap) go to `/gsd-plan-phase 104 --gaps`. No `104-11-SUMMARY.md`, no verifier run, no `phase.complete`.

**The UAT confirmed 103-16/17/18 live AND found four further defects, all now fixed and live-re-verified (no PLAN/SUMMARY artifacts — executed inline as gap closure at the operator's direction, so `total_plans` correctly stays 18):**

- 🛑 **BLOCKER** — the brain picker CRASHED whenever catalogue rows rendered from a routed host (Chat composer pill, Settings Swap buttons): `BrainPickerRow`'s Radix `<Tooltip>` had no `TooltipProvider` ancestor outside `DashboardLayout`'s header cluster, so 2 of the phase's 3 swap entry points were unusable on the only axis that can dispatch. Only the header badge worked. Fixed `a9ef7e16` (picker owns its own provider); isolated against a working header-badge control.
- **MAJOR** — the badge rendered the literal `"unknown"` ingest sentinel as a real engine WITH the confirmed-live pulse. Fixed `ddb51d96` (guard at both the ingest and read boundaries).
- **MAJOR** — a never-settling dispatch trapped the operator in a modal showing false in-flight progress, `Done` disabled, no escape but a reload. Fixed `fc9828ff`.
- **MINOR** — the picker opened onto the deferred per-profile axis and blamed transience. Fixed `6afd67dd`.
- Plus, found while inducing a live failure: a FAILED swap's row header still claimed the override applied (`025d7502`), and raw model ids leaked into four operator-facing strings (`35d16d5a`, `025d7502`).

**Convex ingest guard DEPLOYED to the live self-hosted instance 2026-07-30** (`✔ No indexes are deleted by this push`) and proven end-to-end: a real swap now creates ZERO sentinel rows. Cleanup found the problem was never one row — **all 93 rows in `activeEngineSnapshots` were unresolved sentinels**, pruned in 3 batched runs (batch-capped per the self-hosted-Convex rules); the table is now empty and the prune is idempotent. The per-profile telemetry axis has therefore never carried a valid row, which is exactly astridr Phase 184.1's scope.

**BSC-02/04/05 re-marked SATISFIED in REQUIREMENTS.md (2026-07-30); BSC-01 stays PARTIAL solely on the by-design per-profile deferral, not on any defect.**

**ACTIVE: v13.0 (Phases 103-106).** Requirements (BSC-01..05 / COST-01..03 / OBS-01..03 / DEBT-01..04) in REQUIREMENTS.md; roadmap in ROADMAP.md. Phase 103 COMPLETE (18/18 + post-UAT inline gap closure); 105-106 not yet planned. Sequencing: 106 (tech-debt) independent + low-risk; 104/105 additive.

**Phase 104 (Cost Intelligence) — CONTEXT GATHERED 2026-07-30** (`104-CONTEXT.md`, commit `e4675da2`; no plans yet). 4 gray areas discussed, 17 decisions locked. Headlines: CodePulse **recomputes** cost from tokens × rates held in a new Convex `modelPricing` table — `src/lib/modelPricing.ts` has **no entry for `claude-sonnet-5` / `claude-opus-5` / `claude-fable-5`** (the exact models COST-01 names; they fall to the `default` $3/$15 rate, under-pricing Opus-class calls ~5×) and is imported by only three HR surfaces, i.e. it is not on the main cost path at all; unpriced models get an honest "Unpriced" bucket, **never a silent $0**; rollups store **tokens**-by-model so a rate fix re-prices history; subscription/CLI turns keep a true `$0` billed **plus** a separate "covered by subscription" shadow figure (quota burn guarded on its own axis via `gatewayQuotaSnapshots`); one generic `costBudgets` table scoped global/model/provider, per-row daily|weekly|monthly period + warn fraction, **absorbing `SDKSpendGuard`'s hardcoded `DAILY_CAP = 5.00`** rather than running beside it; "spike" = rate-projection-to-period-end, evaluated **at the tail of the existing `computeHourly` cron** — a hard constraint, because `internal.alerts.evaluateInternal` has been **disabled since 2026-07-14** (15s syscall cap → retry storms starving ingest, `convex/crons.ts:42-47`); alert-only, **no enforcement / no brain-swap dispatch**. Known consequence recorded as deferred: the 40+ static alert rules currently fire **only while the Alerts page is open** (`AlertRulesEngine.tsx:274`). Next: `/gsd-plan-phase 104`.

⚠ **Phase 103's original ROADMAP dependency wording ("gates on astridr Phase 184.1 being live end-to-end") was corrected 2026-07-28** — a GLOBAL brain swap already ships live (astridr Phase 185/186: `swap.set`/`swap.catalogue`/`swap.state`, shipped CodePulse UI `BrainControl.tsx`). Only the PER-PROFILE axis is missing (Phase 184.1's actual scope). See `103-CONTEXT.md` `<blocker_reframing>` for full detail.

**Operator decision (2026-07-27, refined 2026-07-28): contract-first, stub-backed for the per-profile axis; the global axis is live and only PARTIALLY verified end-to-end (103-08-T2, closed 2026-07-28 with open gaps).** 103 proceeds against a stub behind one adapter seam for per-profile and ships `103-CONTRACT.md` as a deliverable (shipped in Plan 01). **BSC-05 is reframed, split by axis** — see `103-CONTEXT.md` `<blocker_reframing>`. A green stub is NOT live verification for the per-profile axis; the Phase-90 War Room lesson is consciously deferred there, not satisfied. **The live global-axis checkpoint (103-08-T2, 2026-07-28) found the read path (catalogue, 331 real engines) and the D-15 confirm gate genuinely work, but the write/readback/revert leg does not** — two open defects block it (`GlobalSwapModal.tsx` discards the real `swap.set` result and fans out the deferred per-profile command instead; `BrainHeaderBadge.tsx` never requests `swap.get_state` on load). **Blocking follow-ons:** (1) astridr-repo — register + build Phase 184.1 against `103-CONTRACT.md` for the per-profile axis; (2) codepulse — a gap-closure cycle (`/gsd-plan-phase 103 --gaps`) fixing the two open global-axis defects, then re-running the live dispatch/readback/revert verification to close BSC-05's global half for real.

Next: Phase 103's gap-closure cycle (`103-09..103-16`, 8 plans total — 103-14 and 103-15 added mid-cycle from a live checkpoint finding, 103-16 added afterward from a code review of the whole cycle's diff) is FULLY EXECUTED and its live outcome fully recorded — defects 6a/6b, CR-01 (the `recordRouting` mutation-auth one, 103-10), CR-02, WR-01, WR-03, defect #5, CR-03, WR-02, OBS 7, and a SECOND CR-01 (`GlobalSwapModal`'s stale-reselect reset guard, a code-review finding, 103-16) are all closed. BSC-03's D-15 confirm gate was re-confirmed live. **Two items remain open, neither blocking further Phase 103 work:** (1) OBS 8 (D-11 confirm-modal per-profile accuracy), a new, unfixed defect discovered by 103-13-T1 itself — a follow-up plan (rewire the confirm modal's per-profile "current" read onto `profileConfigs.modelPreferences`, or extend per-profile telemetry) would move BSC-01/BSC-04/BSC-05 from PARTIAL to fully satisfied; (2) 103-16's own fix has NOT yet been live-re-verified against the running stack — its `<verification>` section explicitly defers that to the orchestrator, matching this cycle's established "green suite is not proof of a live fix" pattern. A fresh `/gsd-verify-work` pass, or an operator-attended live checkpoint covering both the reselect fix and OBS 8, would be the natural next step.

---

**Prior milestone —** Milestone: **v11.0 (Skills Command Center — Full Lifecycle & Launch) — ✅ SHIPPED & CLOSED 2026-07-25**, tagged `v11.0`. Phases 97-100 (23 plans), 22/22 requirements. Audit `tech_debt` (0 blockers). Archived to `milestones/v11.0-{ROADMAP,REQUIREMENTS,MILESTONE-AUDIT}.md` + `milestones/v11.0-phases/`.
Phase 100 UAT (operator, 2026-07-25): drag archive→restore round-trip PASSED live; honest-failure path confirmed; CR-02 unit-verified + live dormant→cold no-op observed.
This session also folded a large Skills-page redesign into Phase 100 (3-column command-center layout, right-hand Command Deck, filter chips, bulk-select, bridge-mirror filter, no-op feedback) — all tested, full suite green.
Queued post-v11.0 (operator-confirmed order) — **ALL DONE 2026-07-26** (ad-hoc/quick, not GSD phases; codepulse 565cef36 + astridr 82ead1fe, both pushed):

- **① fix mangled skill display names** ✅ — family-aware `generateDisplayName` (cc_-twin dedup + ≥3-member family + colon-namespace); `migrateDisplayNames` rewrote 198 auto-names on the live self-hosted Convex (0 human-edited touched); 23 unit tests.
- **② Ástríðr bridge coverage** ✅ FULLY DONE — safe-7 (`.claude-alt` plugin re-root + vault `.agents/skills` junction targets) AND the deferred-3 project-repo skills (spike-findings-forge / add-migration / home-assistant-manager, each repo's `.claude/skills` mounted read-only + added to `bridge.yaml`). **All 10 previously-unbridged skills now have cc_ twins — 202 total, live-verified astridr→forge→Convex.** astridr commits 82ead1fe + 46873c84. They load under `skill_auto_approve` (own repos; `blocked_skills` gate applies). Detail: HANDOFF-post-v11.0.md item ②.
- **③ Chat command-center** ✅ — 3-column HUD (chat · real AvatarAura + Control Center · new VitalsRail from existing Convex tables + one lean `getRecentlyUsedSkills` query); mockup→sign-off→build→~6 live iterations. Bonus: Inbox Enter-key AND R-key phantom-expand both fixed (Enter marks-read; R opens the reject input via a new InboxCard `rejectSignal` prop). codepulse commits 565cef36 / 2fac593.

Status: Ready to execute

Phase 103 ✅ **COMPLETE 2026-07-30** — 18/18 plans + a post-UAT inline gap-closure round (6 fix commits, no plan artifacts by direction). Live UAT done (`103-UAT.md`, 16/16 resolved); Convex ingest guard deployed + proven; 93 sentinel rows pruned. BSC-02/04/05 SATISFIED, BSC-01 PARTIAL by design (astridr 184.1 per-profile deferral). Remaining for the milestone: Phases 105-106 (not yet planned).

<!-- The two lines above were RESTORED by hand 2026-07-31: `gsd-sdk query state.planned-phase` overwrote the whole Phase 103 completion narrative with the bare string "Ready to execute" (the documented state.* narrative-clobber defect). Its numeric counters were checked against ground truth and WERE correct this time (total_plans 18+11=29, completed_plans 18, completed_phases 1/4 = 25%) — only the prose was clobbered. -->

**v12.0 (Personal Productivity — Reminders & Calendar) SHIPPED & ARCHIVED 2026-07-23** — Phases 101 (7/7, done 2026-07-20) + 102 (3/3 tech-debt close-out, live-verified 2026-07-23); 9/9 requirements; tagged `v12.0`; milestone audit `tech_debt` (0 blockers, 2 flagged items closed by Phase 102). Archived to `milestones/v12.0-{ROADMAP,REQUIREMENTS,MILESTONE-AUDIT}.md`. Closed by hand (no `gsd-sdk milestone.complete`) — REQUIREMENTS.md kept live with only the v12.0 section extracted.
Last activity: 2026-08-05

**v11.0 resumed** (HISTORICAL mid-execution snapshot — SUPERSEDED by the "v11.0 SHIPPED & CLOSED 2026-07-25" status above; Phases 99/100 were subsequently completed and the milestone shipped): Phase 97 (Real Skill Intake & Daemon Foundation) COMPLETE (6/6 plans, operator-verified live 2026-07-19, commit 495946f). Phase 98 (Lifecycle Mutations) PLANNED 2026-07-21 (4 plans, 3 waves, checker passed); Plan 98-01 EXECUTED 2026-07-21 (Convex substrate — see 98-01-SUMMARY.md); Plan 98-02 EXECUTED 2026-07-21 (Forge daemon executor — see 98-02-SUMMARY.md); Plan 98-03 EXECUTED 2026-07-21 (lifecycle UI building blocks — see 98-03-SUMMARY.md); Plan 98-04 EXECUTED 2026-07-21 (lifecycle menu assembly — see 98-04-SUMMARY.md). Live UAT session (2026-07-21) found 1 real gap (stale-origin prune on move/delete of the last skill in a workspace) alongside 2 passed checks and 2 blocked-on-Clerk-auth checks; gap-closure Plan 98-05 PLANNED + EXECUTED 2026-07-22 (see 98-05-SUMMARY.md) — closes the gap cross-repo (forge `scannedOrigins` manifest + codepulse `computeSkillPrunes` manifest-aware pruning). Daemon code lives in C:\Users\mandr\forge, ROADMAP's "astridr-repo" note is stale. Phase 98 is now 5/5 plans code-complete; the 98-05 fix's own MANUAL verification steps (live G: repro + transient-unmount negative check) remain outstanding. Phases 99 (Launch/Dispatch) + 100 (Control-Surface UX) were subsequently completed — v11.0 shipped & closed 2026-07-25 (tag v11.0).

</details>

## Deferred Items

Items acknowledged and deferred at milestone close on 2026-06-29 (all non-blocking — see v9.0 audit reconciliation):

| Category | Item | Status |
|----------|------|--------|
| uat_gap | Phase 80 80-HUMAN-UAT.md | passed (0 pending — false positive) |
| uat_gap | Phase 84 84-HUMAN-UAT.md | passed (0 pending — false positive) |
| uat_gap | Phase 85 85-HUMAN-UAT.md | passed (0 pending — false positive) |
| verification_gap | Phase 89 89-VERIFICATION.md | human_needed (operator signed off 2026-06-24; flag unflipped) |
| verification_gap | Phase 92 92-VERIFICATION.md | human_needed (live-verified 2026-06-25; flag unflipped) |
| quick_task | 260603-or6-codepulse-register-opus-4-8-in-cost-mode | missing (stale, unrelated to v9.0) |
| context_questions | Phase 078 078-CONTEXT.md | 3 open Qs (answered during v7.0 execution, unmarked) |
| context_questions | Phase 89 89-CONTEXT.md | 3 open Qs (answered during execution, unmarked) |

**Accepted tech debt:** Phases 88 & 90 have no formal `VERIFICATION.md` — 88 covered by Nyquist VALIDATION (47/47 tests), 90 by operator live sign-off (`90-08-SUMMARY`).

**v10.0 close-out cleanup (2026-07-07) — ALL RESOLVED (`audit-open` now reports 0 open items):**

These were completed work flagged only by naming / status-marker mismatches, not real gaps. All quick-task dirs moved to `.planning/quick-archive/` (out of the audit scan); `95-VALIDATION.md` finalized.

| Category | Item | Resolution |
|----------|------|------------|
| quick_task | 260603-or6-codepulse-register-opus-4-8-in-cost-mode | ✅ DONE 2026-06-03 — registered `claude-opus-4-8` @ $5/$25 in `modelPricing.ts` + fixed a latent 3× Opus over-pricing bug; archived |
| quick_task | 260629-close-crossnav | ✅ DONE (commit b0253b3); archived |
| quick_task | 260629-hive-task-agent-link | ✅ DONE (commit b7b8e84); archived |
| quick_task | 260629-mem-event-deeplink | ✅ DONE (commit 58b999f); archived |
| quick_task | 260629-nnf-graphs-hub-tile-index | ✅ DONE (commit 2d9df13); archived |
| quick_task | 260629-oki-reverse-cross-graph-links | ✅ DONE (commit 6cffbae); archived |
| validation_gap | Phase 95 95-VALIDATION.md | ✅ FINALIZED — `status: complete`, `nyquist_compliant: true`; all gates green, both manual checks operator-verified, cross-referenced by 95-VERIFICATION (16/16) |

The `v10.0-MILESTONE-AUDIT.md` (2026-07-06, `gaps_found`) was a stale **mid-flight snapshot** predating Phases 94/95; superseded by the three phase VERIFICATION.md files (all passed) and the archived REQUIREMENTS (9/9 complete).

## v11.0 Roadmap

| Phase | Name | Requirements | Status |
|-------|------|--------------|--------|
| 97 | Real Skill Intake & Daemon Foundation | INTAKE-01..04, DAEMON-01, DAEMON-03, DAEMON-04 | Not started |
| 98 | Skill Lifecycle Mutations | LIFE-01..06, DAEMON-02 | Not started |
| 99 | Skill Launch / Dispatch | LAUNCH-01..04 | ✅ Complete (7/7 plans; verified 10/10 2026-07-23) |
| 100 | Control-Surface UX | UX-01..04 | Not started |

**Execution order:** 97 → 98 (98 reuses the daemon command-execution + registry-rescan plumbing 97 builds). 99 is independent of 97/98 (rides existing chat/Forge/Ástríðr channels, no daemon dependency) and can run in parallel. 100 depends on **both** 98 and 99 (the ⋯ menu and drag lanes wire against both) and is sequenced last.

**Note:** the backlog-promoted "Phase 97: Skill Lifecycle Management" (999.1, promoted 2026-07-17) is folded into **Phase 98** above — same scope (archive/restore/move/delete via Forge daemon, `isShadowing` guard, archive-not-delete default), renumbered once the daemon/intake foundation was sequenced first. Nothing dropped or duplicated.

<details>
<summary>Prior milestone (v10.0) Roadmap — SHIPPED 2026-07-07 (Phase 96 addendum 2026-07-13)</summary>

| Phase | Name | Requirements | Status |
|-------|------|--------------|--------|
| 93 | Eval Pipeline & Quality KPIs | EVAL-01, EVAL-02, EVAL-03 | ✅ Complete (6/6 plans) |
| 94 | Trace Waterfall | TRACE-01, TRACE-02 | ✅ Complete (5/5 plans) |
| 95 | Hardening — Security, Key Rotation, Dependency Majors | HARD-01, HARD-02, HARD-03, HARD-04 | ✅ Complete (4/4 plans) |
| 96 | UI Deep-Dive Cleanup — IA restructure, palette drift, honesty, PageHeader | F1–F10 / D-01–D-11 (cleanup phase, no formal REQ IDs) | ✅ Complete (13/13 plans; re-verified 16/16 after 96-13 gap closure) |

**Execution order:** 93 and 94 are independent (separate schemas, both ride existing ingest paths) — either order or parallel. 95 is independent of both and sequenced last as audit/cleanup work.

<details>
<summary>Prior milestone (v9.0) Roadmap — SHIPPED 2026-06-29</summary>

| Phase | Name | Requirements | Status |
|-------|------|--------------|--------|
| 88 | Analytics Rollup | AR-01, AR-02, AR-03 | ✅ Complete (4/4 plans) |
| 89 | Readable Themes & Editorial Skin Toggle | TH-01..TH-06 | ✅ Complete (7/7 plans) |
| 90 | Agent Room / War Room | ROOM-01..ROOM-04 | ✅ Complete (8/8 plans; live sign-off 2026-06-29) |
| 91 | 3D Memory Galaxy | G3D-01, G3D-02 | ✅ Complete (5/5 plans; FPS≥30 + WebGL-leak operator sign-off 2026-06-29) |
| 92 | Voice-Activated Command Palette (Jarvis Mode) | VOX-01..VOX-04 | ✅ Complete (6/6 plans) |

**Execution order:** 88 → 89 → 92 → 90 → 91 (all done). All five v9.0 phases complete; milestone shipped & archived.

</details>

</details>

## Accumulated Context

### Roadmap Evolution

- Phase 96 added (2026-07-13): UI deep-dive cleanup — IA restructure (dissolve CONSOLE cluster), command palette nav drift, fake header telemetry, hardcoded trust signals, orphaned pages, header/layout consistency. Full audit findings in `.planning/phases/96-ui-deep-dive-cleanup-ia-restructure-command-palette-drift-fa/FINDINGS.md`.
- v11.0 roadmap created (2026-07-17): 4 phases (97-100) derived from the 22 v11.0 requirements. The backlog-promoted "Phase 97: Skill Lifecycle Management" (999.1) is renumbered to Phase 98 — the daemon-executor + real-intake foundation (INTAKE-01..04, DAEMON-01/03/04) was sequenced first as Phase 97 per the dependency analysis (nothing mutates the host without the daemon executor existing). Phase 99 (Launch/Dispatch) is independent of the daemon work (rides existing chat/Forge/Ástríðr channels) and can run in parallel with 97/98. Phase 100 (Control-Surface UX) depends on both 98 and 99 and is sequenced last.
- Phase 102 added: Address tech debt: reminders dead code + astridr comment cleanup
- Phase 107 added (2026-08-05): Aggregates Rollup Sharding — eliminate OCC write contention on the shared `aggregates` counter rows written by `events.ingest` (`convex/analyticsRollup.ts`), root-caused during the 2026-08-05 self-hosted Convex retention-health incident (2nd occurrence of this mechanism, also seen 2026-07-30, previously only mitigated by container recreate, never fixed).
- Phase 114 added (2026-08-08): Workspace Map view — ARMS radial workspace map, two lenses (Larry's workspace ↔ Ástríðr's world). Source: agentic-os-second-brain design (vault note, approved 2026-08-07).
- Phase 115 added (2026-08-08): Workspace scanner — host-side classifier walking workspace roots, versioned `kind:"workspace"` snapshot rows. Source: same design as 114.
- Phase 116 added (2026-08-08): Galdr prompt library — `prompts`/`promptVersions` tables, `/galdr` live-fetch skills, send-to-Chat/clipboard. Source: Seiðr Suite design (SEED-005, `docs/proposals/2026-08-07-seidr-suite-design.md`, approved 2026-08-08).
- Phase 117 added (2026-08-08): Bifrost link hub — `links` table, command-palette jump, liveness dots (SEED-005; /gsd-quick-shaped).
- Phase 118 added (2026-08-08): Studio media gallery — media-vault dir + sidecar provenance, thumbs-only in Convex, watcher ingest, styles/model recipe cards, G-drive backup task (SEED-005).
- Phase 119 added (2026-08-08): Loom curated pipelines — `pipelines`/`pipelineRuns`, React Flow view, `loom-emit.mjs` step events via Convex subscriptions (SEED-005). Note: 114-119 frontmatter counter change (total_phases 6→12, percent 17→8) done in the same pass BY HAND — no gsd-sdk state verbs run, per this file's standing anti-clobber rule.

### Decisions

See PROJECT.md Key Decisions table for full history.

**v14.0 / Phase 109 Plan 02 decisions (2026-08-09, swap-history data layer — D-11, TELE-02):**

- **`listGlobal` matches `q.eq("scope", undefined)`, never `q.eq("scope", null)`** — D-11's own decision text literally says "reading the same `by_scope` index at `scope: null`", which is wrong: `runtimeIngest.ts`'s `normalizeOptional()` strips every incoming `null` to `undefined` before `record` is called, and Convex's `v.optional(...)` rejects an explicit `null` outright, so a stored global-swap row's `scope` key is simply absent, never present-with-null. A literal `null` implementation returns zero rows forever, silently. Corrected per `109-RESEARCH.md` D.10 and guarded by a mutation-proven test (flipped to `null`, confirmed RED, reverted, confirmed GREEN).
- **`mergeSwapHistory` re-caps the combined list and returns a true pre-truncation `totalCount`** — each of `listByScope`/`listGlobal` is independently capped at `SWAP_HISTORY_CAP` (20), so a naive concatenation could render up to 40 rows; the merge slices to 20 after a deterministic descending-timestamp sort (with an `_id` tie-break) and separately reports the real combined count, per this repo's standing bound-and-truncate-on-screen rule.
- **`103-CONTRACT.md`'s literal `grep -c "_generated" == 0` acceptance criterion was already false on the untouched pre-existing file** (2 hits, both explanatory prose in the file's own WR-02 docstring, present before this plan touched it — confirmed via `git show HEAD~1:convex/controlVerbSwapsFilters.ts | grep -c "_generated"`). Treated as a plan-draft inaccuracy rather than chased: the real WR-02 intent (no *import* of `_generated/server`/`convex/values`) is verified instead via a comment-stripped source-level test, mirroring `controlVerbSwaps.test.ts`'s established raw/stripped CR-01 idiom. Zero `import` statements of any kind exist in the file.
- **`default_profile_id` on the `swap.catalogue` ack was verified directly against astridr-repo before writing the 103-CONTRACT.md §3 correction**, not transcribed from the plan's prose: `ws_commands.py:1258` (`_handle_swap_catalogue`), `bootstrap/core.py:1298` (`config.profiles[0].id`), test `test_ws_commands.py:2034-2086` labeled "Phase 109 D-03" — all confirmed present and real.
- Executed sequentially on `master`, no worktree. Zero `src/` files touched (deliberately file-disjoint from every other wave-1/wave-2 plan in this phase). Full suite: 281 passed | 17 skipped (baseline 280/17), 3649 passed | 193 todo (baseline 3635/193) — 14 net new tests, zero regressions. STATE.md updated by hand per this file's established anti-clobber workaround (`gsd-sdk state.*` verbs not run this plan, per the extensive documented history below).

**v13.0 / Phase 105 Plan 02 decisions (2026-08-03, cross-repo astridr telemetry widening — D-03/D-08/D-10, cross-repo astridr-repo commit, NOT deployed):**

- **Round ContextVar set/reset confirmed live at the TURN choke point (`_process_inner`), not a new per-round `try/finally`** — the plan's own F1 finding verified: the round `while True:` loop spans 620+ lines with many `break`/`continue`/`return` exit paths and no existing per-iteration `finally`; the turn-level `finally` (shared with the existing `traceId` reset) already restores the pre-turn value on every exit path.
- **Leak-payload tests assert equality against the captured `structlog` record, not a hand-computed expectation** — required because `run_state.tools_for_turn_names` (`_permitted`)'s exact shape depends on internal tool-filter wiring; with no tools registered it becomes an empty set (not `None`), so `tools_offered_count` reads `0` in that path rather than the `None` case the plan's `<behavior>` block names — the `None` case is only reachable when `run_state` itself is unavailable, never true on the full `process()` path.
- **`is not None` guard (not truthiness) on the provider-side `round` field** — `round == 0` is a real, meaningful value and must not be dropped by the truthiness idiom the sibling `traceId`/`goalId` guards use (those correctly guard against an empty string, which is meaningless).
- **Two Rule-2 test-only deviations, both closing gaps the plan's own verification-discipline instructions warned about:** (1) the `durationMs` test was strengthened from a vacuous `>= 0` check to a real-elapsed-time threshold after the plan's own required mutation proof exposed the original assertion as non-catching; (2) 2 tests added to `tests/unit/providers/test_ollama.py` (not in the plan's `files_modified` list) proving the round ContextVar value actually crosses the `provider.chat()` await boundary — the "ContextVar + async is a known trap" instruction, proven rather than reasoned about.
- Executed sequentially on `master` (codepulse side) / `feature/brain-swap` (astridr-repo side), no worktree. STATE.md/ROADMAP.md updated by hand per this file's established anti-clobber workaround — no `gsd-sdk state.*`/`roadmap.update-plan-progress` verbs run (this file's own Decisions history above documents the resync-clobber bug repeatedly).

**v13.0 / Phase 105 Plan 03 decisions (2026-08-03, Convex substrate — toolPolicyEvents table + tool_executed/tool_policy_event ingest, D-01/D-02/D-05/D-06/D-10, NOT deployed):**

- **F5 confirmed: no backfill exists or was added anywhere in this plan.** Existing `toolExecutions` rows have no `traceId`/`round`/Ástríðr provenance to backfill from, and `toolPolicyEvents` had literally nothing stored before this plan — there is nothing to backfill FROM. D-04's aggregates (plan 105-04) start from now.
- **`parseToolPolicyEvent` rejects (returns `null`), never stores under a made-up label** — an unrecognised `event` value is dropped with a `console.warn` naming the value, and no switch-level `default:` arm was added (F1: a catch-all there would mask a fifth future kind's absence the same way the pre-105-03 missing case masked today's four).
- **`provider` on the new `toolExecutions` row is a receiver-assigned constant (`ASTRIDR_TOOL_PROVIDER`), never read from the payload** (T-105-12) — matches the D-02 shape 105-01 already established with `excludeByProvider`.
- **Class-closure audit (verification-discipline requirement):** enumerated all 41 event kinds in `docs/astridr-contract.md` §2.1-2.41 against `runtimeIngest.ts`'s ~65 case labels. 11 kinds have no domain-specific case, but this is NOT the same defect as pre-105-03 `tool_policy_event` — every event is first captured verbatim in the generic `runtime_events` table via the unconditional `api.events.insertEvent` call that runs before the switch, for every event regardless of switch coverage (`legacyEventData()` passes fields through unchanged except for `graph_snapshot`). Logged to `.planning/phases/105-tool-trace-observability/deferred-items.md`, not fixed — none of the 11 are named in this plan's scope.
- **Two Rule-1 deviations, same class as 105-01/105-02:** this plan's own new doc comments tripped its own acceptance-criteria greps twice (the literal word "round" in a `schema.ts` comment, and the literal text "switch-level `default:`" in a `runtimeIngest.ts` comment explaining why one wasn't added) — both reworded to keep the same meaning without the colliding substring.
- **One Rule-2 deviation:** the first-draft `toolsOfferedCount: 0`-vs-`undefined` test used a fixture (snake_case field = 0, camelCase absent) where `??` and `||` produce identical results, so it stayed green under a `??`→`||` mutation. Added a second test with the camelCase PRIMARY field set to 0 — the only shape where the two operators diverge — which correctly failed under the same mutation.
- Executed sequentially on `master`, no worktree. STATE.md/ROADMAP.md updated by hand per this file's established anti-clobber workaround: `gsd-sdk state.advance-plan` correctly bumped `completed_plans` (31→32) but regressed `stopped_at`/`last_activity` to a stale Plan-01 snapshot (twice — once on `advance-plan` itself, again on a subsequent no-op `state.update-progress` that reported `updated: false` yet still silently rewrote those two narrative fields); both hand-corrected each time. `state.record-metric` reported `{"phase, plan, and duration required"}` despite all args being passed and was abandoned (matches Plan 98-01/02's documented precedent that this repo's STATE.md has no `## Performance Metrics` section to append to). `state.add-decision`/`state.record-session` skipped entirely (same clobbering risk) — this Decisions entry and the Current Position paragraph above were hand-written instead.

**v13.0 / Phase 105 Plan 01 decisions (2026-08-03, bounded reads + D-15 exclude-filter — D-12/D-15):**

- **Read direction inverted from the plan's literal snippet:** the plan's Task 1 action text said to keep `.order("asc")` while adding `.take(cap)` to `sessionCalls`/`listBySession`. Verified live against 105-UI-SPEC.md's "Showing the most recent {cap}" truncation copy that this would be backwards — capping an ascending scan keeps the OLDEST rows, not the newest. Both queries now read `order("desc").take(cap)` then reverse to ascending before returning, so a cap hit genuinely drops the oldest rows.
- **`excludeByProvider` is an EXCLUDE filter (`provider !== "astridr"`), never an INCLUDE filter** — CONTEXT.md's originally-stated mechanism (`provider === "claude-cli"`) was verified live to drop every Claude Code tool-failure row (`convex/ingest.ts:168` never sets `provider`) and every gateway row. The exclude direction is proven a zero-regression no-op via a two-run `toEqual` control test in `convex/toolExecutions.test.ts`.
- **`successRate`/`avgDuration` truncation is computed from the RAW read, before the provider filter** — an all-astridr cap-sized fixture correctly reports `truncated: true` with an empty `tools` array, proving the cap is honestly reported even when the filtered result looks empty.
- **`avgDuration` bounded despite zero live consumers** — left deployed, not deleted (104-08 `getBudgetConfig` deploy-order precedent); flagged as a Phase-106 dead-code candidate.
- **`convex/ingest.ts:168`'s missing `provider` on `PostToolUseFailure` left unfixed** — out of this plan's scope, and it is the exact row shape the D-15 control test's fixture depends on.
- Executed sequentially on `master` (no worktree); STATE.md updated by hand after `gsd-sdk state.advance-plan`/`state.record-metric` each re-clobbered `stopped_at` back to a stale pre-planning value (the documented full-frontmatter-resync bug) — `state.add-decision`/`state.record-session` skipped entirely this plan for the same reason, this Decisions entry and the Session Continuity update below were hand-written instead.

**v13.0 / Phase 104 Plan 08 decisions (2026-07-31, cap source consolidation — D-12/D-19):**

- **`getBudgetConfig`/`setBudgetCap` (`convex/forecasts.ts`) are deprecated in place, not deleted** — deleting an exported Convex function while a deployed client bundle may still reference it is a deploy-order hazard; both stay authenticated and functional (T-104-36: accepted risk, an inert write).
- **`convex/seedGateway.ts`'s `DAILY_CAP`/`ALERT_THRESHOLD` renamed to `LEGACY_SEED_*` rather than rewired onto `costBudgets`** — they seed a structurally different concept (a static `alertRuleCustom` threshold rule evaluated only client-side while the Alerts page is open), not a spend cap.
- **`convex/alerts.test.ts`'s local `DAILY_CAP`/`ALERT_THRESHOLD` constants left unchanged** — test-local arithmetic fixtures, explicitly exempted by the plan's own Task 3 instruction, not a product cap.
- **Settings' `Tabs` converted from uncontrolled (`defaultValue`) to controlled (`value`/`onValueChange`)** so the retired legacy form's replacement control ("Go to Cost & Budgets") can programmatically switch tabs — the only way to satisfy "a control that switches the Tabs value," since the two components live in separate function scopes.
- **Rule 1 fix:** `convex/costBudgetEval.test.ts` (not in the plan's `files_modified`) imported the now-removed `DAILY_CAP` export and called `projectDayEndSpend` with its old 2-arg signature; updated to a local test-only fixture constant and the new 3-arg call, per the same class of fix 104-05's SUMMARY already documented.
- One-cap-source gate fully classified in `104-08-SUMMARY.md`, including several hits beyond the plan's three named categories (test-only comments/fixtures and the pre-existing, out-of-scope 104-04 `seedFromLegacyCaps` migration mutation) — none on a live render path.
- Executed sequentially on `master` (no worktree); STATE.md/ROADMAP.md updated by hand per this file's established anti-clobber workaround — no `gsd-sdk state.*` verbs run.

**v13.0 / Phase 104 Plan 10 decisions (2026-07-31, cost trend toggle + hex remediation — D-01/D-03/D-05/D-08, mandatory CLAUDE.md/UI-SPEC hex remediation):**

- **Toggle primitive: `Button` pair (`variant="default"`/`"outline"`), not shadcn `Tabs`** — `Tabs`' `TabsContent`/view-router semantics (used elsewhere in this codebase as a page-section router, e.g. Settings tabs) didn't fit a same-view, same-data-source display toggle where both "views" render the identical `FlexBarChart` component with different segment data. A plain `role="group" aria-label="Cost series"` button pair is simpler and matches UI-SPEC's explicit "leaves the choice open" note.
- **`CostTrendChart`'s redundant surface wrapper (`bg-gray-800/50 border-gray-700/50 rounded-xl p-4`) was removed entirely, not retokenized** — `Analytics.tsx:287` already wraps this component in a `GlassPanel` with its own `p-4`; retaining a second `bg-card border-border` div would have been non-hex but still redundant chrome.
- **Empty-state copy changed from `"No API cost data yet."` to `"No cost data yet."`** — instructed by the plan's own Task 1 action text and UI-SPEC's Copywriting Contract; the "API" qualifier no longer applies since `costOverTime` spans all billing types (not just `costByPeriodByProvider`'s `billingType:"api"` filter).
- **`CostBreakdown`'s hex remediation uses named module-level class constants** (`RUNAWAY_WRAPPER_CLASS`, `RUNAWAY_BADGE_CLASS`, `OPUS_ROW_CLASS`, etc.), mirroring `MetricCard.tsx`'s `severityConfig` pattern — colour-only change, no data-flow/table restructuring, per the plan's own scoping instruction.
- Both `CostTrendChart.tsx` and `CostBreakdown.tsx` verified at zero hex outside the documented `PROVIDER_COLORS` exemption — the two files CLAUDE.md and UI-SPEC flagged as current violations at phase start are now closed.
- **Executed out of wave order**: this plan (wave 3) ran before plans 08/09 (wave 4) — both were independent of this plan's `costDerived.costOverTime` dependency (104-05, wave 2), so no blocking dependency was violated; plans 08/09 remain outstanding.
- Executed sequentially on `master` (no worktree); STATE.md/ROADMAP.md updated by hand per this file's established anti-clobber workaround — no `gsd-sdk state.*` verbs run.

**v13.0 / Phase 104 Plan 07 decisions (2026-07-31, Cost & Budgets admin UI — D-02/D-03/D-07/D-09/D-10/D-11/D-16):**

- **`CostBudgetsAdmin` renders no fabricated progress bar for any row, including global scope** — every row shows its configured threshold plus "Threshold set — progress shown on Analytics" instead. The plan permitted a bar only where `costDerived.costBreakdown` gives an exact per-scope figure without re-derivation (it doesn't — it returns a lookback-window breakdown, not one scoped to a specific budget row's own period boundary) or by reusing `SDKSpendGuard`/`CostForecastPanel`'s own numbers (both still read their pre-104-08 legacy sources, not `costBudgets`, so borrowing them here risks a transient disagreement with the row being displayed — the exact two-caps-disagree failure D-12 exists to prevent).
- **Query-failure error copy ("Couldn't load pricing rates/budgets. [Retry]") is delegated entirely to the `<SectionErrorBoundary>` wrapping**, not duplicated as a second local error state inside either admin component — `useQuery` either resolves or throws (caught by the boundary); there is no third value to render locally.
- **Regenerated `convex/_generated/api.d.ts`** (Rule 3 blocking fix) — it had never been regenerated since plans 104-01/104-04/104-05/104-06 added `modelPricing.ts`/`costBudgets.ts`/`costDerived.ts`/`costBudgetEval.ts`, so every `api.modelPricing.*`/`api.costBudgets.*`/`api.costDerived.*` reference failed `tsc` until `npx convex codegen` ran (its own internal typecheck step errored on an unrelated pre-existing `@/lib/utils` alias issue in its bundler context, but binding generation completed before that step).
- Executed sequentially on `master` (no worktree); STATE.md/ROADMAP.md updated by hand per this file's established anti-clobber workaround — no `gsd-sdk state.*` verbs run.

**v13.0 / Phase 104 Plan 06 decisions (2026-07-31, budget alert evaluator / cron tail-append — D-13/D-14/D-15/D-16/D-17):**

- **`evaluateBudgets` is invoked as a plain function against `computeHourly`'s own mutation `ctx.db`/`ctx.scheduler`, not through `ctx.runQuery`/`ctx.runMutation` indirection** — that indirection pattern (`evalScores.ts`'s `DetectRegressionsCtx`) exists there because the regression detector is a separately-scheduled action calling into internal functions across a `runAfter` boundary; this evaluator instead runs INSIDE the same `computeHourly` mutation execution and shares its transactional ctx directly, exactly like plan 104-05's `computePeriodSpend` already does for the identical reason.
- **`quota_pct` spend is `100 - remainingPct * 100`** — confirmed `remainingPct` is a 0-1 fraction (not already a percentage) against three independent sources: `gatewayQuota.ts`'s own doc comment, `GatewayQuotaPanel.tsx`'s live `Math.round(remainingPct * 100)` usage, and `68-PATTERNS.md`'s schema comment.
- **A quota budget with no live `gatewayQuotaSnapshots` row for its provider is skipped and counted under a `skippedNoData` return field, never evaluated as 0% used** — the plan's `<interfaces>` block only lists 4 return fields, but Task 2's own action text explicitly requires this 5th counter; the more specific instruction governs, and the field is additive/non-breaking since no downstream plan reads `evaluateBudgets`'s return shape.
- **The `grep -cE 'throttle|swap|disable'` acceptance criterion cannot literally read 0 while `containsForbiddenEnforcementWord`'s guard exists as executable code** (the guard's word list necessarily contains the words it detects) — kept the array rather than obfuscating it; the actual behavioral constraint (no generated alert message ever contains an enforcement word) is enforced in code and unit-tested directly.
- **`internal.webhookDelivery.sendAlertWebhook` (convex's `anyApi`) is a non-`===`-stable Proxy that throws on primitive coercion** — confirmed via a throwaway probe script; scheduler-call assertions in this plan's tests follow `evalScores.test.ts`'s own established convention (assert `delay`/`args` shape, never the function reference's identity) rather than the `toBe(internal.webhookDelivery.sendAlertWebhook)` pattern that failed unpredictably.
- **COST-03 NOT marked satisfied in REQUIREMENTS.md this plan** — the backend fire-and-deliver path is code-complete, but live confirmation that alerts actually deliver through the running self-hosted instance's webhook routing is deferred to plan 104-11 by this plan's own `<verification>` section, matching this project's "green suite is not accepted as proof of a live fix" convention and 104-01/104-04/104-05's own precedent of not marking requirements complete before their full scope lands and is verified.
- Executed sequentially on `master` (no worktree); STATE.md/ROADMAP.md updated by hand per this file's established anti-clobber workaround — no `gsd-sdk state.*` verbs run.

**v13.0 / Phase 104 Plan 05 decisions (2026-07-31, cost derivation layer — D-01/D-03/D-05/D-18, RESEARCH.md Open Question 1):**

- **RESEARCH.md's Open Question 1 answered YES, not deferred:** the goal-scoped `costByGoalPeriod`/`llmByGoal` (HivePage's `CostBreakdown`) now derive dollars through the exact same `deriveBucketDollars` function every other cost surface in the phase uses, instead of summing the ingested `cost` field — closing the two-disagreeing-totals gap D-12 exists to prevent, on the same page family the pattern already burned once.
- **`deriveBucketDollars` has two honesty guards checked in order, both required:** a both-zero-token bucket is unpriced regardless of whether a rate resolves (D-18 — never price imputed/unreported tokens at `$0.0000`), and a `resolveRate` miss is unpriced with BOTH dollar fields strictly `null` (D-03 — never `0` as a stand-in).
- **`billedUsd` and `coveredUsd` are never summed anywhere in this plan's diff** — `costOverTime` keeps three separate provider-keyed maps per bucket (`billedByProvider`/`coveredByProvider`/`unpricedTokensByProvider`), `costBreakdown` keeps `billedTotal`/`coveredTotal` as two independent sums, `computePeriodSpend` only ever touches `billedUsd`.
- **`unpricedModels` is a thin wrapper over `costBreakdown`'s own derivation**, not a second implementation — so the future nudge (104-09) and pricing-admin suggestion list (104-07) can never disagree on the count.
- **`computePeriodSpend`'s daily/hourly read windows are disjoint by construction** and the daily read is skipped entirely for a same-day (`periodStart >= todayStart`) budget — verified by a test that inspects the fake ctx's recorded index-range predicates directly, not just the numeric result.
- **Two doc comments reworded to avoid the literal substring `totalCost`** (in `convex/aggregates.ts` and `src/hooks/useCostByGoal.ts`) — same recurring pattern 104-01/104-02's summaries already documented: this plan's own grep-based acceptance criteria check the whole file, comments included.
- **`src/hooks/useCostByGoal.test.ts` updated though absent from the plan's `files_modified` list** (Rule 1 — broken by this task's own shape change, caught by the full repo suite, not pre-existing/unrelated work).
- Executed sequentially on `master` (no worktree); STATE.md/ROADMAP.md updated by hand per this file's established anti-clobber workaround — no `gsd-sdk state.*` verbs run.

**v13.0 / Phase 104 Plan 04 decisions (2026-07-31, cost budget persistence + legacy cap migration — D-07/D-09/D-10/D-11/D-12/D-19):**

- **`unit` is derived server-side from `scope` inside `create`, never accepted as a caller argument** — D-07's "no fictional dollar enters a quota budget" is enforced structurally, not by validation alone: the `unit` field isn't even in `create`'s args schema being trusted, it's computed from the validated `scope`.
- **`update` explicitly rejects any attempt to change `scope`/`scopeKey`/`period`** rather than silently ignoring those args — protects plan 104-06's future `(budgetId, level, periodStart)` alert dedup key, which implicitly assumes a budget row's identity never moves after creation.
- **`seedFromLegacyCaps` skips the monthly seed honestly when no legacy `agentConfigs["intelligence.budget_cap"]` value exists** (D-19/D-03) — live-checked via `npx convex run forecasts:getBudgetConfig '{}'` → `{budgetCap: null}`: **the live deployment currently has no legacy monthly cap row**, so an operator running the seed today gets only the $5.00/day global budget; the monthly budget needs an explicit operator-set value before plan 104-08's `CostForecastPanel` rewire has anything to show on that axis.
- Weekly UTC boundary uses a documented `345600`s (epoch-Thursday-to-preceding-Monday) offset constant; monthly boundaries use `Date.UTC(year, month[+1], 1)` rather than adding a fixed day count, verified against a 31-day month, a 28-day month, and a December→January rollover.
- Executed sequentially on `master` (no worktree); STATE.md/ROADMAP.md updated by hand per this file's established anti-clobber workaround — no `gsd-sdk state.*` verbs run.

**v13.0 / Phase 104 Plan 03 decisions (2026-07-31, cost intelligence rollup widening — D-04 tokens_prompt/tokens_completion + resumable backfill):**

- **Retention floor sourced from `agentConfigs["retention_days"]`, not `convex/retention.ts`** — the plan's own `read_first` framing named `retention.ts` as "the file that defines how far back raw llmMetrics rows survive," but that module's `PRUNED_TABLES` explicitly excludes `llmMetrics` (its own comment: "kept forever"). The retention window that actually governs `llmMetrics` is the (disabled) `markStaleArchived` cron's config value in `convex/archival.ts`. Used that existing value instead of inventing a second hardcoded number; full detail in `104-03-SUMMARY.md`.
- **`backfillTokenSplit`'s resume cursor in `agentConfigs` is written insert-only (never `ctx.db.patch`)** — the plan's prose scoped "insert-only" to the `aggregates` table, but its own acceptance criteria (`grep -c 'db.delete|db.patch'` → 0) and required unit test check the whole file with no such scoping. Cursor is an append-only log; the newest row for its `configKey` is read as current.
- **Task 1 inlined the two token-split blocks directly in `computeHourly`; Task 2 factored them into a shared `insertTokenSplitBuckets` helper** — matches the plan's own task-boundary wording (Task 2's action text explicitly assigns the extraction to itself), so each commit's diff matches what its own task asked for.
- A full backfill to the default 30-day retention floor needs 120 invocations of `npx convex run aggregates:backfillTokenSplit '{"maxHours": 6}'` — not yet run against the live deployment (operator/orchestrator action, same category as Plan 01's un-run `seedDefaults`).
- Executed sequentially on `master` (no worktree); STATE.md/ROADMAP.md updated by hand per this file's established anti-clobber workaround — no `gsd-sdk state.*` verbs run.

- **STATE.md frontmatter was clobbered a SECOND time (2026-08-02)** by the same gsd-sdk `state.*` defect — `total_phases` 4->8, `percent` 25->13, `last_activity` regressed to "Plan 09 executed". Rebuilt from HEAD again with only this session's edits re-applied. Twice in one phase; treat any STATE.md diff as suspect before committing.

**v13.0 / Phase 104 orchestrator decisions (2026-07-31, execute-phase code review + live gate):**

- **Budget-alert dedup read range-bounded** (`527adc7f`) — `costBudgetEval`'s `alerts.by_source` dedup read had
  no lower bound over a table `retention.ts` keeps forever, so it grew one row per fired period forever INSIDE
  `computeHourly` — the one mutation D-14 exists to protect. Bounded on `createdAt` (composite index makes it
  free; an alert for period P is always written during P). Two structural guards, mutation-tested.

- **CR-01 accepted as a VERIFIED GAP, deliberately not fixed inline** (Larry's call) — `SDKSpendGuard` and
  `CostForecastPanel` still show spend from the raw ingested `llmMetrics.cost` field while the chart/table/
  evaluator derive it from tokens x rates, so two different dollar figures render on one Analytics page. D-01
  says `cost` "stops being the displayed truth"; no plan in the phase ever owned this — 104-08's own threat
  model states `costForecast`'s aggregates read is "unchanged". Route: `/gsd-plan-phase 104 --gaps`.

- **`gatewayQuotaSnapshots` bounded at 30 days** (`380f13d9`, Larry's call) — D-20 revives a dead poller, so a
  permanently-empty table starts accruing ~288 rows/provider/day forever on the instance that has gone down
  twice from read growth. Bounded BEFORE it fills, which avoids ever needing the mass delete that caused this
  module's original tombstone outages. `RETENTION_DAYS` now exported and guarded by a test asserting every key
  is a REAL schema table — the prune casts the table name away, so a typo there is a permanent silent no-op.

- **Two admin-UI honesty fixes** (`d9213c52`) — four handlers discarded `ConvexError.data` behind a generic
  toast (Convex redacts plain-Error messages, so `.data` must be read first); and the delete-rate dialog
  claimed past figures "stay as last computed", the exact opposite of D-04's recompute-on-read.

- **Three defects found ONLY by the live gate** — (1) `npx convex deploy` broke on the `@/` alias because
  104-06 added the repo's only convex-to-src import while `convex/tsconfig.json` had no `paths` map
  (`e9ca3f9a`; fixed properly, NOT with `--typecheck=disable`); (2) `backfillTokenSplit` issued multiple
  paginated queries per invocation and failed on its FIRST live call — grepping the pattern found it again in
  the LIVE `computeHourly` cron (pre-existing, Phase 88, latent only because one 500-row page covered one
  hour), both fixed in `921517db`; (3) the aggregates test mock implemented `paginate()` more permissively
  than Convex, which is why 34 tests stayed green — it now throws Convex's real error and is mutation-tested.

- **STATE.md frontmatter clobber rejected at commit time** — the working copy had been rewritten to
  `total_phases: 8` / `percent: 13` and had lost the Last-session block (v13.0 spans phases 103-106, so 4/25
  are correct). Restored from HEAD and only this session's edits re-applied, per the standing anti-clobber rule.

**v13.0 / Phase 104 Plan 02 decisions (2026-07-31, cost-intelligence ingest prerequisites — cross-repo D-18/D-20):**

- **`resolveGatewayTaskCompleted` assigns (not sums) reported token counts** — an adapter's `usage` object is already the whole run's total, unlike `cost_usd` which several astridr-repo adapters emit incrementally across multiple stream events. Mirrored identically on the astridr-repo side in `task_manager.py`'s `TaskState.prompt_tokens`/`.completion_tokens` accumulation.
- **`astridr/channels/web.py` modified though absent from the plan's `files_modified` list** (Rule 2 — auto-add missing critical functionality): the plan's own action text conditioned this on what the file's forwarding shape turned out to be; reading it showed the `/internal/gateway/task_completed` handler explicitly enumerates every forwarded field rather than passing the body opaquely, so without this fix the two new token fields would have been silently dropped at that hop and the entire cross-repo change would ship no observable effect on the CodePulse side.
- **Two `gatewayQuota.ts` doc comments reworded to avoid the literal substring `ASTRIDR_API_URL`**, same precedent as Plan 01's `resolveRate`/`llmMetrics` wording fix — the plan's acceptance criteria (`grep -c 'ASTRIDR_API_URL' convex/gatewayQuota.ts` returns 0) checks the whole file, not just executable code.
- **The plan's literal `grep -c 'case "gateway.task_completed"'` acceptance check is a regex-dot ambiguity, not evidence of duplication** — `.` matches `_`, so after adding `gateway_task_completed` the regex-mode count is 2; a fixed-string grep (`grep -cF`) confirms the literal dot-cased case still occurs exactly once, unaltered.
- **astridr-repo commit (`9adb25b6`) lands on `feature/brain-swap`, not `main`** — matches this repo's existing v13.0 integration branch; not yet merged or deployed. `CLI_GATEWAY_URL`/`CLI_GATEWAY_API_KEY` also require live operator configuration on the self-hosted Convex deployment before D-20's fix has any observable effect — both are User Setup Required items, not blockers on this plan's own completion.
- Executed sequentially on `master` (codepulse) and `feature/brain-swap` (astridr-repo, pre-existing branch — no branch created or switched by this plan); STATE.md/ROADMAP.md updated by hand per this file's established anti-clobber workaround. `gsd-sdk query state.advance-plan` was invoked once, found to clobber the narrative exactly as this file's LESSONS/Decisions history predicts (`stopped_at`/`Status`/`Last activity` overwritten with unrelated stale boilerplate; numeric counters were correct), reverted via `git checkout -- .planning/STATE.md`, and redone entirely by hand.

**v13.0 / Phase 104 Plan 01 decisions (2026-07-31, cost-intelligence persistence layer — schema + modelPricing CRUD/seed):**

- **`resolveRate` has exactly two hit paths and one miss path, with no default-rate fallback anywhere in executable code (D-03).** Two doc-comment wordings were adjusted (avoiding the literal substrings `llmMetrics` and quoted `"default"` in prose) so the plan's own grep-based acceptance criteria — which check the whole file, not just executable lines — hold against comments too, not just code.
- **`seedDefaults` is idempotent/additive-only and manually operator-triggered** (`npx convex run modelPricing:seedDefaults`), not a cron — consistent with CLAUDE.md's self-hosted Convex rule against unattended bulk mutation. It has not yet been run against the live deployment; `modelPricing.list` returns empty until an operator runs it.
- **`src/lib/modelPricing.ts` kept in place as a seed source only**, not migrated — its 3 HR-surface consumers (`AgentAnalytics.tsx`, `MetricsDashboard.tsx`, `TokenUsageChart.tsx`) are documented as drifting, per 104-CONTEXT.md's explicit deferral to planner's discretion.
- **COST-01/COST-02 NOT marked complete in REQUIREMENTS.md** — this plan ships only the persistence layer (1 of 11 plans in Phase 104); the breakdown chart/table and budget UI that the requirements actually describe land in later plans.
- **`npx convex codegen`'s literal "dataModel.d.ts names both tables" acceptance criterion doesn't hold in this Convex version** (`DataModel` is derived generically via `DataModelFromSchemaDefinition`, not enumerated) — verified via `npx tsc --noEmit` exiting 0 instead, which is the plan's own first-listed verification method for this command.
- Executed sequentially on `master` (no worktree); STATE.md/ROADMAP.md updated by hand per this file's established anti-clobber workaround — no `gsd-sdk state.*`/`roadmap.*` verbs run.

**v13.0 / Phase 103 Plan 13 decisions (2026-07-29, gap-closure live re-verification + requirement restatement — Task 1 operator-delegated live checkpoint, Task 2 record/restate):**

- **Task 1 was driven by the orchestrator via Playwright headless Chromium, not the Claude-in-Chrome extension**, per operator Larry's explicit "I drive in Chrome, you review" delegation — recorded as a run condition/deviation in `103-VALIDATION.md`, not glossed over.
- **OBS 8 (D-11 confirm-modal per-profile accuracy) FAILED live and was left unfixed this cycle**, per Larry's explicit disposition to treat it as a real defect. Every requirement marker Task 2 restated (BSC-01, BSC-04, BSC-05) stays ⚠ PARTIAL rather than satisfied because of this — no marker asserts more than OBS 8 actually showed.
- **OBS 7 recorded as a genuine two-part result:** FAILED on the first live attempt this session (`GlobalSwapModal.runRevert` cleared the override instead of restoring the prior one), fixed live mid-checkpoint by spawning Plan 103-14, then re-verified PASS against the same live stack in the same session. Both halves recorded verbatim in `103-VALIDATION.md`.
- **The original "Live Global-Axis Verification (103-08-T2)" section was left byte-unchanged** — the new "Gap-Closure Live Re-Verification (103-13-T1)" section is purely additive, placed immediately after it. A heading-anchored grep (`grep -n '^## Live Global-Axis Verification'`) confirms exactly 1 occurrence.
- **Discovered and recorded a pre-existing discrepancy in 103-13-PLAN.md's own acceptance criteria**, rather than silently working around it: the plan's stated `grep -c "Live Global-Axis Verification (103-08-T2)"` (non-heading-anchored) check already returned 2 before this plan touched the file (confirmed via `git show HEAD:...` on the prior commit) — the section heading plus a pre-existing cross-reference inside the Validation Sign-Off bullet list. The substantive intent (original section preserved exactly once) holds via the heading-anchored check; the plan's literal grep command was already stale.
- **Did not re-mark BSC-03** — its D-15 confirm-gate re-verification (OBS 10) is noted inline in `REQUIREMENTS.md` without flipping any status, per the plan's explicit instruction.
- **Confirmed the stale "satisfied 2026-07-28" markers were already corrected by commit `0b354131`** before this plan started (`grep -c "satisfied 2026-07-28"` → 0 both before and after) — did not re-apply a landed correction.
- **Full gap-closure cycle (103-09..103-15) is now fully recorded end-to-end.** One new, live-confirmed, unfixed defect remains open (OBS 8) — not one of the original six gap-closure defects, discovered by this plan's own live checkpoint.
- **Executed sequentially on `master`** (worktrees disabled per `config.json`); `STATE.md`/`ROADMAP.md` updated by hand per this file's established anti-clobber workaround — no `gsd-sdk state.*`/`roadmap.*` verbs run.

**v13.0 / Phase 103 Plan 16 decisions (2026-07-29, GlobalSwapModal reset keyed to selection identity — gap-closure wave 4, closes code-review CR-01, BSC-02/BSC-04):**

- **Fix shape matches the plan's own prescribed design, not the reviewer's "reset on `open` false→true" alternative:** `BrainPicker` increments a `globalSelectionNonce` in the exact handler (`handleSelect`'s global branch) that turns an activation into a modal open, passed to `GlobalSwapModal` as a new required `selectionNonce` prop; the reset effect keys off a CHANGE in that value instead of `target.id` equality. `runRevert`'s own `onOpenChange(true)` call never touches `BrainPicker`'s state, so it can never bump the nonce — that asymmetry is what keeps CR-03 (revert-survives-Done) closed while still giving a same-brain reselection the fresh reset the old guard denied it.
- **The interface change (`GlobalSwapModalProps.selectionNonce`, now required) forced a mechanical prop backfill across all 33 existing render calls in `GlobalSwapModal.test.tsx`** — committed in the same Task 1 commit as the source fix (a Rule 3 blocking-issue fix: the existing suite wouldn't compile against the new required prop otherwise), with zero new test cases or behavior changes in that pass. Substantive new coverage landed in the separate Task 2 commit.
- **Task 2's "REAL GlobalSwapModal" requirement was satisfied via a mode-toggling mock, not a second test file:** `BrainPicker.test.tsx`'s existing `vi.mock("@/components/brains/GlobalSwapModal", ...)` gained a module-level `globalSwapModalMode` ("mock" | "real") flag selected via `importOriginal` — a describe block can flip it to "real" and render the ACTUAL component against this file's already-mocked `useAstridrWS`/`useGlobalBrainOverride`/`sonner` seams, with zero new mocking surface needed (both hooks the real component depends on were already mocked at the right level for WR-02/103-12's own tests).
- **The choice between "real" and "mock" is picked via JSX (`{mode === "real" ? <actual.GlobalSwapModal .../> : <MockGlobalSwapModal .../>}`), not a plain conditional function call** — keeps each variant on its own React fiber so hook-rules stay intact even though the two component types are never mounted simultaneously in the same test.
- **The CR-03 regression mutation (guard reverted to `open`) only failed the NEW real-component BrainPicker.test.tsx test, not either of GlobalSwapModal.test.tsx's own pre-existing revert tests** — those tests drive `onOpenChange` as a bare `vi.fn()` spy that never feeds back into the `open` prop actually passed to the component (so `open` never transitions false→true inside that file's own render tree), while the new test exercises BrainPicker's real, closed-loop `globalDialogOpen` state, which genuinely does toggle. This is direct evidence for why the plan required a REAL-component integration test rather than accepting `GlobalSwapModal.test.tsx`'s existing coverage as sufficient.
- **Both required mutation checks performed live**: (1) reverted the reset guard to `target.id` equality — exactly the two new reselect tests (`(a)` completed-swap reselect, `(b)` failed-swap reselect) failed as expected (2 failed/54), restored; (2) reverted the reset guard to `open` — exactly the new CR-03 regression test (`(c)`) failed as expected (1 failed/54), restored. Both restores verified byte-identical against the pre-mutation commit (`git diff` empty).
- **Full suite: 2859/2859 passing (5 new tests over the 2854 baseline — 4 real-mode BrainPicker.test.tsx scenarios + 1 mock-mode selectionNonce-bump check), `tsc --noEmit` clean.**
- **REQUIREMENTS.md not touched this plan** — same established gap-closure-cycle pattern as every prior plan in this cycle except 103-13's own live-checkpoint record/restate: BSC-02/BSC-04 stay ⚠ PARTIAL, the overall requirement re-mark happens after a live re-verification, not per-plan. This plan's own `<verification>` section explicitly defers live re-verification of the fix to the orchestrator and does not claim it.
- **Executed sequentially on `master`** (worktrees disabled per `config.json`); `STATE.md`/`ROADMAP.md` updated by hand per this file's established anti-clobber workaround — no `gsd-sdk state.*`/`roadmap.*` verbs run.

**v13.0 / Phase 103 Plan 15 decisions (2026-07-29, BrainControl restore-to-Auto coverage hardening — gap-closure investigation, no requirement re-mark):**

- **The plan's core premise was investigated and found not to hold for the targeted file.** `103-15-PLAN.md`'s "VERIFIED FACTS" claimed `BrainControl.tsx`'s `dispatchSelection(value, restore=false)` restore branch was "implemented but unreachable from the UI." Reading the live file (`:217-227`) and its git history (commit `6cc040d3`, phase 186-09) showed a "Restore usual brain" button has existed, been reachable, and been tested since that original commit — well before the 103-13/103-14 gap-closure cycle even started. `REQUIREMENTS.md`'s own BSC-01 marker independently corroborates this ("only the pre-existing `BrainControl` was correct", 2026-07-28 investigation).
- **Did not add a duplicate "Reset to Auto" affordance.** The plan's action text asked for a net-new entry; adding a second button doing the same thing the existing one already does would violate 103-CONTRACT.md §8 (single-axis dispatch: one live command, one affordance) and confuse the UI. Treated the plan as "harden + document the existing implementation," not "build new UI" — same class of correction as memory `[[absence-of-spec-symbols-is-not-absence-of-feature]]`.
- **Traced why the live incident still happened despite the working affordance:** `103-13-T1`'s checkpoint script (`103-13-PLAN.md` steps D/E) only exercises `GlobalSwapModal`'s "Revert global swap" toast action, never asks the operator to open Control Center's separate `BrainControl` popover. The operator hit the real 103-14 regression (GlobalSwapModal's revert now restores-to-prior, not clear) in the surface the script tested, and bypassed the UI via raw socket rather than using the untested, independently-working `BrainControl` clear path.
- **Closed the plan's 3 real coverage gaps against the existing implementation:** no-double-fire (exactly one `swap.set` per activation), disabled-while-pending, and a D-14 regression test pinning that BrainControl never self-asserts a "cleared" state from the ack alone — it derives 100% of displayed state from the `override`/`lastTurnModel` props, fed by the parent's `swap.state` readback.
- **Both T1 and T2 landed in one commit** — all three tests were written together as one coherent edit to the same test-file section before either was committed; splitting after the fact would have meant manufacturing an artificial diff boundary. Same "task split follows the actual code seam" precedent as 103-12/103-14.
- **Mutation check performed for real:** hardcoded `restore: false` into `dispatchSelection`'s payload (overriding the `restore` param), ran the suite, confirmed 3 tests fail (1 pre-existing dispatch-shape test + both new tests asserting `restore: true`), reverted. `BrainControl.tsx` has a zero-line net diff — no production code changed this plan.
- **Full suite: 2854/2854 passing (3 new tests over the 2851 baseline), `tsc --noEmit` clean.**
- **`BSC-04`/`BSC-05` not re-marked in `REQUIREMENTS.md` this plan** — same established gap-closure-cycle pattern as Plans 09-12/14: the overall requirement re-mark happens after `103-13`'s live re-verification, not per-plan. That re-verification still needs a step exercising `BrainControl`'s "Restore usual brain" directly, since 103-13-T1's original script never did.
- **Executed sequentially on `master`** (worktrees disabled per `config.json`); `STATE.md`/`ROADMAP.md` updated by hand per this file's established anti-clobber workaround — no `gsd-sdk state.*`/`roadmap.update-plan-progress` verbs run (both returned parse errors against this file's format when attempted).

**v13.0 / Phase 103 Plan 14 decisions (2026-07-29, GlobalSwapModal revert restores prior override — gap closure spawned mid-103-13's live re-verification, closes OBS 7, BSC-04/BSC-05):**

- **Both tasks committed as a single atomic commit, same "task split follows the actual code seam" precedent as Plan 12:** `revertRestoredName` is one state variable threaded through `runRevert` (Task 1's dispatch mechanics) and `describeOutcome` (Task 2's copy selection) — Task 1's own D-14 readback-gate acceptance criterion is only observable through Task 2's restore-aware copy, so splitting the diff into two commits would require reconstructing an intermediate state with no independent test coverage.
- **Prior-override display name resolved from the pre-swap snapshot**, not a catalogue lookup: `GlobalSwapModal` has no catalogue prop, but every profile mirrors the global override's model while one is in force (global override wins outright, `103-CONTRACT.md` §9), so the snapshot row matching the captured model id already carries a catalogue-resolved name. Falls back to the raw model id, never an invented label, per the plan's own instruction.
- **1 Rule-2 auto-fix extended the fix beyond `describeOutcome`** to `handleDismiss`'s summary toast and the result-phase "Profiles ..." label — both independently hardcoded clear-flavored copy for every revert, reproducing the same "surface asserting more than happened" failure class the plan's objective names, using the same `revertRestoredName` value already in scope.
- **Split the plan's illustrative combined confirmed/timeout T2 test into two independent tests:** the existing D-14 readback `useEffect` only listens while `outcome.status === "confirming"` — once the bounded timeout has fallen back to `"accepted"` it does not retroactively resolve to `"confirmed"` on a later matching readback (pre-existing, unrelated behavior). A single continuous flow would have failed for a reason outside this plan's fix.
- **Both required mutation checks performed live**: (1) reverted `runRevert`'s dispatch branch to the old unconditional `restore:true` — exactly 2 new tests failed as expected (2 failed/20, dispatch-shape assertions), restored; (2) reverted `describeOutcome`'s confirmed-restore branch to the old hardcoded clear copy — exactly 1 new test failed as expected (1 failed/20), restored.
- **Full suite: 2851/2851 passing (5 new tests over the 2846 baseline), `tsc --noEmit` clean.**
- **`BSC-04`/`BSC-05` not re-marked in `REQUIREMENTS.md` this plan** — same established gap-closure-cycle pattern as Plans 09-12: the overall requirement re-mark happens after `103-13`'s live re-verification, not per-plan.
- **`total_plans` incremented 13 → 14 mid-cycle** — this plan did not exist when the gap-closure cycle was originally planned (5 plans, 103-09..103-13); it was spawned by a real finding during `103-13`'s own live-checkpoint execution (T1, 2026-07-29), the exact "verification finds a gap, spawn a fix plan" loop this project's process is designed for.
- **Executed sequentially on `master`** (worktrees disabled per `config.json`); `STATE.md`/`ROADMAP.md` updated by hand per this file's established anti-clobber workaround — no `gsd-sdk state.*`/`roadmap.*` verbs run.

**v13.0 / Phase 103 Plan 12 decisions (2026-07-29, GlobalSwapModal honest result reporting + revert-survives-Done — gap-closure wave 2, closes defect #5/CR-03/WR-02, BSC-04/BSC-03/BSC-01):**

- **Task split follows the actual code seam, not a literal per-task file diff:** Task 1's own acceptance criteria grep the WHOLE `GlobalSwapModal.tsx` file for zero `gateway.model.set`/`BRAINS_STUB_ACTIVE` occurrences, which forces BOTH `runSwap` AND `runRevert`'s dispatch mechanics (single awaited command, no per-profile fan-out) into the same Task 1 commit even though the plan's Task 2 action text also describes that conversion. Task 2's commit is therefore the mount-lifecycle-specific diff only: `BrainPicker`'s `globalDialogOpen` decoupling, `GlobalSwapModal`'s reset-effect switching from `[open]` to `[target.id]`, and WR-02.
- **`GlobalOutcome` is a 5-state type** (`pending`/`confirming`/`confirmed`/`accepted`/`error`), richer than the plan's illustrative 3-state (`pending`/`ok`/`error`) sketch — required to satisfy the plan's own explicit D-14/D-15 narrative: an ack alone must never render a success claim, and a `swap.state` readback that never arrives must resolve to an honest "accepted, unconfirmed" state (`GLOBAL_SWAP_CONFIRM_TIMEOUT_MS` = 4000ms) rather than hanging or fabricating success.
- **D-11's confirm-phase copy corrected per its 2026-07-28 amendment**: "will be overwritten" → "will be shadowed while this global override is in force." Pin icons and the computed pinned-default count are byte-unchanged — only the verb.
- **The informational affected-profiles list in the result phase is `lastAction`-aware**: "Profiles now governed by the global override" during a swap vs. "Profiles returning to their own defaults" during a revert — the same list, described accurately for each direction.
- **`BrainPicker.test.tsx` mocks `useGlobalBrainOverride` directly at the module level** rather than driving it through the already-mocked `useAstridrWS().sendCommand` seam — necessary because the hook's mount-time `swap.get_state` hydration call would otherwise consume the existing WR-01 staleness test's `mockSendCommand.mockImplementationOnce()` sequencing (keyed to call order, not argument shape), silently breaking that unrelated test.
- **The `GlobalSwapModal` mock in `BrainPicker.test.tsx` now always renders (mount) with a `data-open` attribute for visibility**, replacing the old `open ? <div>...</div> : null` shape — needed so the new mount-lifecycle tests can assert MOUNT and VISIBILITY as two independently observable facts, matching what the real CR-03 fix actually changed.
- **1 Rule-1 auto-fix:** `GlobalSwapModal.tsx`'s own top docstring quoted the literal strings (`gateway.model.set`, `.catch(() => {})`, `overwritten`) that Task 1's own acceptance-criteria greps check for zero occurrences — same recurring failure class as 103-01/103-03/103-07/103-09/103-11. Reworded without changing meaning; re-verified all 5 greps pass.
- **Both required mutation checks performed live**: (1) reverted `runSwap`'s awaited dispatch to a discarded fire-and-forget with a hardcoded ok ack — the error-reporting test failed as expected (1 failed/14 passed), restored; (2) reinstated the old `if (!next) setGlobalTarget(null)` handler in `BrainPicker.tsx` — exactly the 2 new mount-lifecycle tests failed (2 failed/27 passed), restored.
- **Full suite: 2846/2846 passing (7 new tests over the 2839 baseline), `tsc --noEmit` clean, `npm run build` clean.**
- **`BSC-04`/`BSC-03`/`BSC-01` not re-marked in `REQUIREMENTS.md` this plan** — same established gap-closure-cycle pattern as Plans 09/10/11: the overall requirement re-mark happens after the full cycle and `103-13`'s live re-verification, not per-plan.
- **Executed sequentially on `master`** (worktrees disabled per `config.json`); `STATE.md`/`ROADMAP.md` updated by hand per this file's established anti-clobber workaround — no `gsd-sdk state.*`/`roadmap.update-plan-progress` verbs run.

**v13.0 / Phase 103 Plan 11 decisions (2026-07-29, keyboard-operable brain picker — gap-closure wave 1, closes CR-02/WR-01/WR-03, BSC-02/BSC-03):**

- **`handleActivate` lives in `BrainPicker.tsx` and is the ONLY caller of `handleSelect`** — `BrainPickerRow`'s own local branch logic (`needsConfirm` → expand vs. dispatch) was left byte-identical; only what the row's `onSelect` PROP is bound to changed (`handleSelect` → `handleActivate`), so the row's own click handlers automatically route through the shared activation function with zero row-side logic edits.
- **`event.stopPropagation()` added to all three of `BrainPickerRow`'s button handlers is load-bearing, not cosmetic:** cmdk's `CommandItem` div has its own native `onClick` that calls the same `onSelect` prop once wired — an unguarded mouse click would bubble into it and double-fire `handleActivate`, and an unguarded Cancel click would bubble into a false confirmation (parent still sees `expandedId === entry.id` and treats the bubbled call as confirming).
- **A second activation on the same row (not specifically the "Confirm swap" button) now confirms an expanded row** — a deliberate simplification versus the pre-103-11 row-local code (which only confirmed on the explicit Confirm button click): both mouse and keyboard route through one function, so a second activation on the same entry is unconditionally treated as confirmation once `expandedId === entry.id`, matching the plan's explicit "keyboard parity: two deliberate actions, never one" framing.
- **WR-03's health-dot Tooltip now wraps the row's whole button** (one trigger, one focusable element) instead of a separately-focusable nested span; the health word is folded into the button's own `aria-label` alongside engine name, billing label, and quota state so non-color redundancy survives.
- **WR-01's rapid-scope-toggle unit test needed a real `setTimeout` flush, not a couple of `await Promise.resolve()` ticks**, to honestly prove the guard holds — verified empirically that a bare microtask flush produced a vacuous pass even with the guard mutated away, because the stale response's continuation hadn't actually run by assertion time.
- **3 tasks committed atomically via reconstructed clean intermediate file states** (not raw incremental edits) — `BrainPicker.tsx` and `BrainPickerRow.tsx` each carry content spanning two of the three tasks, so each commit's file content was rebuilt from the original + only that task's diff before staging, keeping every commit independently buildable/testable.
- **3 auto-fixes:** 1 Rule-1 doc-comment/grep-gate literal collision (same recurring class as 103-01/103-03/103-07/103-09), 1 Rule-3 jsdom `scrollIntoView` stub (cmdk calls it on ArrowDown selection changes; not implemented in jsdom), 1 Rule-1 ambiguous keyboard-test search term (cmdk's fuzzy filter re-selected a duplicate-name fixture row on an ambiguous query, fixed by searching a substring unique to the target entry's id).
- **`BSC-02`/`BSC-03` not re-marked in `REQUIREMENTS.md` this plan** — same established gap-closure-cycle pattern as Plans 09/10: the overall requirement re-mark happens after the full cycle and `103-13`'s live re-verification, not per-plan.
- **Full suite: 2839/2839 passing (9 new tests over the 2830 baseline), `tsc --noEmit` clean.**
- **Executed sequentially on `master`** (worktrees disabled per `config.json`); `STATE.md`/`ROADMAP.md` updated by hand per this file's established anti-clobber workaround — `gsd-sdk query state.advance-plan`/`state.update-progress` were attempted first and confirmed to clobber `stopped_at`/`last_activity`/`percent` (narrative reverted to Plan 10's text, `percent` zeroed) despite reporting an error; reverted via `git checkout -- .planning/STATE.md` and redone by hand, per the established lesson.

**v13.0 / Phase 103 Plan 10 decisions (2026-07-29, `recordRouting` → `internalMutation` — gap-closure wave 1, closes CR-01, BSC-01/BSC-04):**

- **Pure authorization-tier fix, zero behavior change:** `recordRouting`'s `args` validators and handler body are byte-unchanged — only the import and the builder (`mutation` → `internalMutation`) changed, matching the plan's explicit instruction and the in-repo `gatewayQuota.ts` `insertSnapshot` precedent exactly.
- **`latestByProfile` deliberately left as a public `query`** — read-only, bounded `.take(200)`, no PII, required by the browser's `useActiveEngine()` hook; this matches the threat register's `accept` disposition (T-103-38) and `gatewayQuota.ts`'s own public `latestByProvider` precedent.
- **Sibling-exposure audit performed and recorded in the summary** (plan requirement): enumerated every non-comment `mutation(`/`internalMutation(`/`httpAction(` export in `convex/activeEngine.ts` and `convex/runtimeIngest.ts`; found only `latestByProfile` (accepted) and `runtimeIngest`'s `httpAction` (the intended public `/runtime-ingest` entry point, named explicitly as reviewed-and-accepted rather than left unmentioned).
- **Mutation check performed for real, not just described:** backed up `convex/activeEngine.ts` to the session scratchpad, reverted `internalMutation` → `mutation` in place, re-ran `convex/activeEngine.test.ts`, confirmed the new CR-01 guard test genuinely fails (1 failed / 7 passed), restored from the backup, and re-verified 8/8 passing plus `tsc --noEmit` clean — proving the guard would actually catch a regression, not just asserting it would.
- **Guard test strips comment lines before matching** so the file's own docstrings (which legitimately use the words "mutation" and "recordRouting" in prose) can't produce a false pass; a companion assertion confirms the raw (unstripped) file still contains the word "mutation", so the stripping logic is proven non-vacuous.
- **Function-only deploy executed live** (`npx convex deploy --yes` against `http://127.0.0.1:3210`) per the plan's explicit instruction and CLAUDE.md's Self-Hosted Convex operational rules — confirmed `✔ No indexes are deleted by this push`, then confirmed `activeEngine:latestByProfile` resolves live and returns `[]` (clean state, no stray rows from prior incidents).
- **0 deviations** — clean namespace/builder swap with no ripple into other call sites; the dual snake/camelCase coalescing in `runtimeIngest.ts`'s `model_routing` case was left untouched as instructed.
- **Full suite: 2830/2830 passing (3 new CR-01 guard tests over the 2827 baseline), `tsc --noEmit` clean.**
- **BSC-01/BSC-04 not re-marked in `REQUIREMENTS.md` this plan** — same established gap-closure-cycle pattern as Plan 09: the overall requirement re-mark happens after the full cycle and `103-13`'s live re-verification, not per-plan.
- **Executed sequentially on `master`** (worktrees disabled per `config.json`); `STATE.md`/`ROADMAP.md` updated by hand per this file's established anti-clobber workaround — no `gsd-sdk state.*`/`roadmap.update-plan-progress` verbs run.

**v13.0 / Phase 103 Plan 09 decisions (2026-07-29, shared global-brain resolution module — gap-closure wave 1, closes defects 6a/6b, BSC-01):**

- **`resolveActiveBrain`'s `mode` field type is derived from `ActiveEngineMap` (`NonNullable<ActiveEngineMap[string]>["mode"]`), not imported directly from `src/lib/brainsApi.ts`** — keeps `useResolvedBrain.ts` at zero dependency on the D-16 per-profile adapter seam, so the plan's anti-stub-masking proof (`grep -c "brainsApi" src/hooks/useResolvedBrain.ts` returns 0) holds structurally, not just by accident.
- **Global-override precedence corrected, not just added:** a global override now wins outright over BOTH a real per-profile reading and a real Mixed-brains disagreement (103-CONTRACT.md §9 — global is rung 2, per-profile is rung 4). This reverses 103-08's own fallback logic, which only used the global reading when no per-profile data existed at all — that inversion WAS the root of the BSC-01 three-way-disagreement bug this plan closes. Required rewriting 2 pre-existing `BrainHeaderBadge.test.tsx` tests whose assertions encoded the old (now-incorrect) precedence.
- **No singleton/shared-network-cache across `useGlobalBrainOverride` instances** — the header badge and the Chat composer pill each own an independent hook instance of the same shared module/algorithm. A connect event therefore fires one `swap.get_state` pull per consumer on the page (not one pull for the whole app) — an intentional consequence of "one shared module," not "one shared React state." Required updating `Chat.test.tsx`'s WR-07 reconnect-re-pull test from a hard-coded count of 1 to a stable multiple of the initial pull count.
- **`isConfirmedLive`'s stub gate applies to profile readings only** — `VITE_BRAINS_STUB` gates the per-profile seam exclusively; the global axis (live regardless of the stub flag) pulses live whenever `resolved.source === "global"`, even with the stub active.
- **3 Rule-1 auto-fixes:** two doc-comment/grep-gate literal collisions in `useResolvedBrain.ts`/`useResolvedBrain.test.tsx` (same failure class as 103-01/103-03/103-07's prior doc-comment-vs-grep-gate deviations), reworded and re-verified 0 hits.
- **Reconnect-rehydration guard mutation-tested per the plan's acceptance criterion**: narrowed the snapshot effect's dependency array to `[]` locally, confirmed the reconnect test fails (`expected 2, got 1`), reverted, re-confirmed all 12 tests green.
- **BSC-01 not re-marked in REQUIREMENTS.md this plan** — it was already (incorrectly) marked satisfied in Plan 07 based on a claim `103-VERIFICATION.md` subsequently found false; this plan is the code fix for that gap, but flipping the checkbox back is this gap-closure cycle's overall job (after `103-13`'s live re-verification), not a single plan's.
- **Full suite: 2827/2827 passing (14 new tests over the 2813 baseline — 12 in the new `useResolvedBrain.test.tsx` + 2 new in `BrainHeaderBadge.test.tsx`), `tsc --noEmit` clean.**
- **Executed sequentially on `master`** (worktrees disabled per `config.json`); `STATE.md`/`ROADMAP.md` updated by hand per this file's established anti-clobber workaround — no `gsd-sdk state.*`/`roadmap.update-plan-progress` verbs run. The `init.execute-phase` query's frontmatter side-effect regression was caught and reverted via `git checkout -- .planning/STATE.md` before any task commit, per the 2026-07-09 lesson.

**v13.0 / Phase 103 Plan 08 decisions (2026-07-28, Playwright stub round trip + live global-axis verification + honest VALIDATION sign-off — Wave 5 of 5, COMPLETES Wave 5 and all 8/8 Phase 103 plans):**

- **`e2e/brain-swap.spec.ts` asserts the accepted/never-optimistic PENDING state (D-15), not the literal per-profile success toast the plan's action text described.** Root cause discovered mid-task: `BrainPicker.tsx`'s `toast.success` only fires when `useActiveEngine()`'s Convex-reactive query reports the swap has genuinely landed (D-14) — the stub adapter's `dispatchSwap` intentionally never writes to `activeEngineSnapshots`, by the same D-14 rule that forbids the UI from asserting its own success. An honest stub round trip cannot manufacture that confirmation without writing directly to the shared production Convex table, which the spec deliberately does not do. Documented in the spec's own header comment.
- **Self-caused-and-disclosed incident:** while diagnosing the above, ran `activeEngine:recordRouting` directly against the live shared self-hosted Convex to test the readback path — the table was genuinely empty before this, so the probe row briefly corrupted the live dashboard's header badge site-wide. A self-cleanup deploy attempt was correctly blocked by the permission classifier; reported as a checkpoint with dashboard-UI-only cleanup steps (no CLI); the user deleted the row before Task 2 began (confirmed empty via `activeEngine:latestByProfile` → `[]`).
- **D-08's scope-reset-on-open is tested by deliberately switching scope to "All profiles", closing without dispatching, reopening, and confirming the reset** — not by a trivial "never touched, still default" open, which would prove nothing about the reset rule itself.
- **`playwright.config.ts`'s `webServer.env`** (not `command`) now sets `VITE_BRAINS_STUB=true` — inert for a locally reused dev server, load-bearing for a fresh CI spin-up; the spec's own skip guard handles the reused-server case honestly.
- **Task 2 was performed live by the coordinator directly, not this executor** — per the checkpoint protocol's prohibition on self-approving or fabricating a human observation. Real results: the badge/catalogue/D-15-confirm-gate genuinely work live (331 real engines via `swap.catalogue`, zero WS frames pre-confirm); the dispatch/readback/revert leg does not. 4 real defects found and FIXED live: `593ce212`+`fa3c974d` (BrainHeaderBadge aria-hidden-focus WCAG violation), `895c2796` (BrainHeaderBadge blind to the global axis, added a labelled `swap.state` fallback), `2e42e5c8` (`registerBrainsWsSender` never wired into the app — every live call silently failed), `220aeb84` (BrainPicker's catalogue fetch scope-blind to the live global axis). 2 more found and deliberately left OPEN as gap-closure candidates: `GlobalSwapModal.tsx:154,159-169` (fans out N deferred `gateway.model.set` calls for global scope, violating `103-CONTRACT.md` §8, and discards the real working `swap.set` result via `.catch(() => {})`); `BrainHeaderBadge.tsx:71-91` (the global fallback only subscribes to `swap.state`, a change event, and never requests `swap.get_state` on mount, so an already-active override never shows on page load).
- **`103-VALIDATION.md` finalized honestly** — every Per-Task Verification Map row re-verified green, all 5 mandatory fixtures ticked with their realizing `brainsFixtures.ts` exports named, a new "Live Global-Axis Verification (103-08-T2)" section records the real per-step results/defects/evidence verbatim (nothing softened), `status: complete`/`nyquist_compliant: true`/`wave_0_complete: true` set. **BSC-05 explicitly NOT marked satisfied** anywhere — not in `103-VALIDATION.md`, not in `REQUIREMENTS.md`, not in `ROADMAP.md`.
- **`REQUIREMENTS.md` untouched this plan** — no BSC-* checkbox flipped; none of BSC-02/BSC-04/BSC-05 reached a new "complete" state (BSC-02 was already marked in Plan 07).
- **Full suite: 2813/2813 passing (0 failures)** — baseline moved from 2794 via the coordinator's four fix rounds' own new tests plus concurrent unrelated Phase-187 activity in the shared checkout; `tsc --noEmit` clean.
- **Recommended next step:** a gap-closure cycle (`/gsd-plan-phase 103 --gaps`) targeting the two open global-axis defects, then re-running the live dispatch/readback/revert leg to close BSC-05's global half for real. The per-profile axis's follow-on (astridr Phase 184.1 registration + implementation against `103-CONTRACT.md`) is unchanged.
- **Executed sequentially on `master`** (worktrees disabled per `config.json`); `STATE.md`/`ROADMAP.md` updated by hand per this file's established anti-clobber workaround — no `gsd-sdk state.*`/`roadmap.update-plan-progress` verbs run.

**v13.0 / Phase 103 Plan 07 decisions (2026-07-28, composer pill + Settings stale-row replacement + CLI fallback notice — Wave 4 of 5, COMPLETES Wave 4):**

- **`AgentProfileRows` sources rows from `useProfileConfigs()` (real, populated), joins `agentProfiles` (confirmed empty in production) only optionally** for `displayName`/`avatarId`/the Edit button's target — the reverse of the pre-existing code's implicit assumption. A `profileConfigs` row with no matching `agentProfiles` row gets a functionally inert Edit click (Sheet gates on `editingProfile !== null`) — a pre-existing `agentProfiles`-population gap, not something this plan's stale-read-removal scope closes.
- **`BrainPicker.tsx` is out of this plan's file scope, so the Settings row's Swap button cannot hard-lock/hide the scope selector** per UI-SPEC §9's literal wording. The picker still defaults to "This profile" on open (D-08's normal behavior), satisfying the practical intent. Documented as the same class of accepted nuance 103-05-SUMMARY.md already recorded for `BrainPickerRow.tsx`'s scope-unaware `needsConfirm` gate under the identical file-scope constraint.
- **Both new surfaces (Settings row, composer pill) fetch `brainsApi.getCatalogue()` independently** for the provider-identity dot color, mirroring `BrainHeaderBadge.tsx`'s (103-06) one-time-fetch-on-mount pattern rather than introducing a new shared catalogue-cache hook outside this plan's file scope.
- **`AgentProfileRows` extracted as its own component** (not inlined in `Settings()`) specifically so `useActiveEngine()`'s call site is a real render descendant of `<SectionErrorBoundary name="Agent Profiles">` — a throwing/undefined query degrades to one boundary fallback instead of blanking the whole Settings page.
- **`BrainFallbackNotice` uses sonner's native `toast.warning(...)`**, not `toast(...)` with a manual class — matches UI-SPEC's "graceful degrade, not a failure" framing in the actual toast API, not just styling.
- **`Settings.test.tsx` tests the exported `AgentProfileRows` component directly**, not the full `Settings` page (which also pulls in Privacy/Ambient contexts, Tabs, avatar upload, LLM provider config, notification channels — all orthogonal). No existing `Settings.test.tsx` precedent existed; this file is new.
- **1 Rule-1 auto-fix:** a `Settings.tsx` doc comment explaining the removed stale read quoted the literal substring `p.model`, which the plan's own zero-hit acceptance grep (`grep -cE 'p\.model' src/pages/Settings.tsx`) would have failed — same failure class as 103-01's/103-03's prior doc-comment-vs-grep-gate deviations. Reworded to paraphrase around the literal string; re-verified 0 hits, `tsc --noEmit` and both affected test suites still green.
- **BSC-01 and BSC-02 marked ✅ satisfied in `REQUIREMENTS.md`** (inline annotation, no traceability-table restructure — v13.0's `REQUIREMENTS.md` is still mid-flight prose, unlike the archived `milestones/v1x.0-REQUIREMENTS.md` files' table format). All three D-05 hosts (header badge — 103-06; composer pill and Settings row — this plan) now mount the same `BrainPicker`; the stale-config-as-live-state read is genuinely gone dashboard-wide.
- **25/25 new tests (11 Settings + 8 Chat + 6 BrainFallbackNotice), `tsc --noEmit` clean, `npm run build` clean, full suite 2794/2794 (0 failures)** — exact match against the 2760 baseline + this plan's 25 new tests + additional tests landed by the concurrently-active unrelated Phase-187 session (verified via `git branch --show-current`/`git diff --cached --name-only`/`git show --stat HEAD` before and after every commit; `KnowledgeGraph.tsx/.test.tsx`, `kg/*`, `useKnowledgeGraph.ts`, `useSavedViews.ts(.test.ts)`, `kgApi.ts(.test.ts)` confirmed untouched by any of this plan's three commits).
- **Executed sequentially on `master`** (worktrees disabled per `config.json`); `STATE.md`/`ROADMAP.md` updated by hand per this file's established anti-clobber workaround — no `gsd-sdk state.*`/`roadmap.update-plan-progress` verbs run.

**v13.0 / Phase 103 Plan 06 decisions (2026-07-28, BrainHeaderBadge — Wave 4 of 5, half-completes Wave 4):**

- **Invisible-trigger relay instead of a second picker.** `BrainPicker` (103-05) owns its own Popover open-state and trigger button internally with no controlled-open/custom-trigger prop, and `BrainPicker.tsx` was out of this plan's file scope. `BrainHeaderBadge` mounts a real `BrainPicker` instance `opacity-0 pointer-events-none` exactly behind its own visible, correctly-labeled (mixed-state-aware) button, and relays clicks via a DOM ref matched against `BrainPicker`'s stable `button[aria-label^="Active brain"]` signature. Because Radix `PopoverContent` renders through a portal, the relayed popover still opens fully visible/interactive at the badge's screen position.
- **`MutationObserver` mirrors `BrainPicker`'s own `data-testid="brain-picker-pending-suffix"` DOM node** into the badge's visible pending state, rather than the badge performing or tracking its own dispatch — `BrainPicker` exposes no pending-state callback, and a second independently-tracked pending state would risk disagreeing with the real in-flight swap (the exact honesty failure D-15 forbids).
- **Provider-identity dot color resolved via a one-time `brainsApi.getCatalogue()` fetch** joining `ActiveEngine.model` (a catalogue entry id) to `CatalogueEntry.vendor` to `PROVIDER_COLORS`, with a `var(--muted-foreground)` token fallback — a model-id prefix heuristic was considered and rejected as an invented reading (works for most stub ids, silently wrong for others, e.g. `anthropic-opus-4-8` vs vendor key `anthropic_direct`).
- **Picker `profileId` sourced from `brainsApi.getDefaultProfileId()`** (falling back to the first known profile) — the badge is not bound to any single profile, so it reuses the same `103-CONTRACT.md` §3 `default_profile_id` resolution 103-07's composer pill will need for the identical "profile-less surface" problem, rather than inventing a second convention.
- **0 deviations — plan executed exactly as written.** The relay/observer designs are exercises of the plan's own "exact component decomposition... Claude's discretion" latitude (`103-CONTEXT.md`), not corrections.
- **BSC-01 intentionally NOT marked complete in REQUIREMENTS.md** — the header badge ships; 103-07 (composer pill, Settings row) hasn't landed. Matches every prior Phase-103 plan's per-plan-vs-full-delivery deferral precedent.
- **14/14 tests, `tsc --noEmit` clean, `npm run build` clean, full suite 2760/2760 (0 failures)** — baseline 2745 + this plan's 14 new tests + 1 additional test landed by the concurrently-active unrelated Phase-187 session (verified via `git branch --show-current`/`git diff --cached --name-only`/`git show --stat HEAD` before and after every commit; that session's own modified files — `useKnowledgeGraph.ts`, `kgApi.ts`, `kgApi.test.ts` — were confirmed untouched by either of this plan's commits).
- **Executed sequentially on `master`** (worktrees disabled per `config.json`); STATE.md/ROADMAP.md updated by hand per this file's established anti-clobber workaround — no `gsd-sdk state.*`/`roadmap.update-plan-progress` verbs run.

**v13.0 / Phase 103 Plan 05 decisions (2026-07-28, BrainPicker — Wave 3 of 5, completes Wave 3):**

- **cmdk `CommandItem` carries `value={entry.id}` + `keywords={[entry.name, entry.vendor]}` but no `onSelect` handler** — `BrainPickerRow`'s own internal button stays the sole click/dispatch entry point. Setting `onSelect` on both the `Item` and a nested button would double-fire a single click (verified by reading `cmdk`'s bundled source directly: the `Item` renders a `data-value` attribute and attaches its own click handling regardless of whether `onSelect` is supplied), which would have broken the "dispatches exactly one `gateway.model.set`" requirement.
- **Row-click dispatch is hardcoded to `mode: "session"`**, never `mode: "default"` (pinned) — the picker has no UI affordance for choosing pin-vs-session at click time; D-02's pin control is Settings row's job (103-07, UI-SPEC §9). Documented so a future plan doesn't rediscover this as a gap.
- **`GlobalSwapProfile[]` for `GlobalSwapModal` is computed internally** (`useProfileConfigs()` × `useActiveEngine()` × the already-fetched catalogue for display-name resolution), not accepted as a prop — keeps `BrainPickerProps` to just `{ profileId, entryScope }`, matching `BrainControl.tsx`'s self-contained-component precedent.
- **`entryScope="global"`'s one-time contextual default (D-08's mixed-badge exception) is tracked via a `useRef` consumed-flag**, not component state — guarantees it fires at most once across the component's mounted lifetime even if a parent re-supplies the prop across multiple opens without unmounting.
- **Known, accepted UX nuance, not fixed this plan:** UI-SPEC §3's "frictions do not stack" rule (an expensive/unknown-tier row under `All profiles` scope should skip the inline confirm and go straight into `GlobalSwapModal`) is not honored — `BrainPickerRow`'s `needsConfirm` gate is scope-unaware by design (103-03, reused verbatim per this plan's explicit non-negotiable constraint), so today that combination gets the inline confirm step AND the modal (double friction, never double dispatch — functionally correct, one extra click). Fixing it needs a scope-aware bypass prop on `BrainPickerRow.tsx`, outside this plan's file scope. Flagged for whichever future plan next touches that file.
- **Rule 3 auto-fix:** the group-order test's `screen.getAllByText(/^(Subscription|API|Local)$/)` also matched `BrainPickerRow`'s "API" billing chips (multiple rows carry that exact text). Re-scoped to `document.querySelectorAll("[cmdk-group-heading]")` — the exact DOM attribute `cmdk`'s own `Group` component renders heading text into, confirmed by reading `cmdk`'s bundled source rather than assumed.
- **BSC-02 / the scope+dispatch half of BSC-03 / the pending half of BSC-04 intentionally NOT marked complete in REQUIREMENTS.md** — the picker ships in isolation; nothing renders/opens it yet (103-06's header badge / composer pill and 103-07's Settings row are the callers). Matches every prior Phase-103 plan's own deferral precedent.
- **14/14 tests, `tsc --noEmit` clean, full suite 2745/2745 (0 failures)** — exact match against the 2731 baseline + 14 new tests, confirming zero regression from the concurrently-active unrelated Phase-187 session (verified via `git branch --show-current`/`git diff --cached --name-only`/`git show --stat HEAD` before and after every commit).
- **Executed sequentially on `master`** (worktrees disabled per `config.json`); STATE.md/ROADMAP.md updated by hand per this file's established anti-clobber workaround — no `gsd-sdk state.*`/`roadmap.update-plan-progress` verbs run.

**v13.0 / Phase 103 Plan 04 decisions (2026-07-28, GlobalSwapModal — Wave 2 of 5, completes Wave 2):**

- **Revert mode-mapping is a three-way branch** (`pinned` -> `mode:"default"` with the snapshot model re-applied, `session` -> `mode:"session"` with the snapshot model re-applied, `inherited` -> `restore:true`/`mode:"default"` with the model omitted, clearing the pinned default the fan-out set rather than re-pinning a model that was never pinned) — kept consistent with `ActiveEngine.mode`'s three-value vocabulary from 103-01/103-03 rather than collapsing session to inherited for convenience.
- **The live `swap.set` axis is dispatched via `useCommandDispatch`; the per-profile axis exclusively via `brainsApi.dispatchSwap`** — two separate call sites by design (not a shared dispatch function) so the D-16 STUB indicator can never leak onto the live global axis's outcome (T-103-16) and command-shape drift stays independently greppable per axis.
- **`dispatch(...)` calls for both `swap.set` invocations are fire-and-forget with `.catch(() => {})`** — found necessary by reading `AstridrWSContext.tsx`'s real `sendCommand`: its `onmessage` handler REJECTS the pending promise (not resolves) when an ack's `status` is `"error"`, making `useCommandDispatch`'s own `if (result.status === "error")` branch unreachable against the real transport (only reachable against a mock that resolves regardless of status, as `BrainControl.test.tsx`'s existing suite does). Pre-existing codebase behavior, not introduced or "fixed" by this plan — noted because it directly shaped the `.catch()` decision.
- **`DialogContent`'s default close (X) button disabled** (`showCloseButton={false}`) so every dismissal routes through the explicit `Cancel`/`Done` handlers — an Escape/overlay-click dismissal would otherwise silently skip the "Revert global swap" toast offer.
- **BSC-03/BSC-04 intentionally NOT marked complete in REQUIREMENTS.md** — the modal ships in isolation; nothing renders/opens it yet (103-05's `BrainPicker` is the caller). Matches the established per-plan-vs-full-delivery precedent (Plans 103-01/02/03's own deferrals).
- **11/11 tests, `tsc --noEmit` clean, full suite 2731/2731 (0 failures)** — exact match against the 2720 baseline + 11 new tests, confirming zero regression from the concurrently-active unrelated Phase-187 session (verified via `git branch --show-current`/`git diff --cached --name-only`/`git show --stat HEAD` before and after every commit, per the session's explicit shared-checkout warning).
- **Executed sequentially on `master`** (worktrees disabled per `config.json`); STATE.md/ROADMAP.md updated by hand per this file's established anti-clobber workaround — no `gsd-sdk state.*`/`roadmap.update-plan-progress` verbs run.

**v13.0 / Phase 103 Plan 03 decisions (2026-07-28, useActiveEngine hook + BrainPickerRow — Wave 2 of 5):**

- **`useActiveEngine` joins `api.activeEngine.latestByProfile` against `useProfileConfigs()`, never `agentProfiles` or `profileConfigs.modelPreferences`** — D-14's "never invent a reading" rule extended to mean zero fallback to any persisted config field, not just zero fallback to the empty legacy table. A profile with no reported telemetry gets an explicit `null` map entry (not a missing key), so a consuming surface can say "not reported" instead of guessing.
- **`resolveHealthStatus` prefers the catalogue entry's own server-reported `health` field over the vendor-keyed `useProviderHealth()` live query**, falling back to the live query only when an entry omits `health` — both sources are server-reported (satisfying D-14) and the row still calls `useProviderHealth()` as the plan's action text specifies, but `103-CONTRACT.md` §3 (published in 103-01) independently added `health` directly to `CatalogueEntry`, making it the more specific per-entry signal when present.
- **`BrainPickerRow`'s expensive/unknown-tier confirm state (`isExpanded`/`onExpandChange`) is a fully controlled prop pair, not local component state** — `expandedRowId` ownership belongs to the 103-05 picker (the only place that can guarantee at most one row expanded across the whole catalogue list), per the plan's explicit "it owns no dispatch logic" instruction and `PATTERNS.md`'s `expandedRowId` recommendation.
- **Rule 1 auto-fix (×2):** doc comments in both new files quoted the exact literal substrings their own plan's zero-hit acceptance greps checked for (`agentProfiles`/`modelPreferences` in `useActiveEngine.ts`; `truncate`/`bg-(green|red|yellow|emerald|gray)-[0-9]` in `BrainPickerRow.tsx`) — same failure class as 103-01's `swap.set`-in-a-comment deviation. Reworded to paraphrase around the literal strings; re-verified 0 hits on all four gates, `tsc --noEmit` and both test suites still green.
- **Rule 3 auto-fix:** jsdom has no `ResizeObserver`, which Radix Tooltip's internal Popper sizing needs to mount — added the identical stub `SkillLifecycleMenu.test.tsx` already uses (`observe`/`unobserve`/`disconnect` no-ops), scoped to `BrainPickerRow.test.tsx` only.
- **BSC-01 intentionally NOT marked complete in REQUIREMENTS.md** — this plan ships two composable primitives; no page mounts `useActiveEngine` or `BrainPickerRow` yet (103-05/103-06/103-07's job). Matches the established per-plan-vs-full-delivery precedent (Plans 103-01/103-02's own deferrals).
- **Full suite verified green modulo 2 pre-existing, out-of-scope failures**: 228/229 test files passed (2716/2718 tests, 193 todo), the 2 failures both isolated to `src/pages/KnowledgeGraph.test.tsx` — `git show --stat` on both this plan's commits confirms neither touched that file or its `.tsx` counterpart; both were already modified/uncommitted (unrelated Phase-187 `zoomToFit` WIP) before this plan started and were left untouched per the scope-boundary rule and the destructive-git-operations prohibition. `tsc --noEmit` clean.
- **Executed sequentially on `master`** (worktrees disabled per `config.json`); STATE.md/ROADMAP.md updated by hand per this file's established anti-clobber workaround — no `gsd-sdk state.*`/`roadmap.update-plan-progress` verbs run.

**v13.0 / Phase 103 Plan 02 decisions (2026-07-28, Convex reactive substrate for per-profile active engine — Wave 1 of 5):**

- **`activeEngineSnapshots` kept structurally separate from `profileConfigs`** — D-03/D-14 boundary: this table holds only the live-resolved engine (telemetry-owned), never the persisted default (Ástríðr-owned, mirrored in `profileConfigs.modelPreferences`). No `configChanges` audit row is written from `activeEngine.ts` — that audit belongs exclusively to the persisted-default write path in `profiles.upsertConfig`; duplicating it here would conflate the two axes. Verified `grep -c 'configChanges' convex/activeEngine.ts` = 0.
- **`recordRouting` built as a public `mutation`, not `internalMutation`** — matches how `runtimeIngest.ts` calls every other sibling case (`api.profiles.upsertConfig`, `api.providerHealth.upsert`, `api.episodic.recordEvent`), not `gatewayQuota.ts`'s `internalMutation`/`internalAction` precedent (a different call pattern, not applicable to the ingest-switch call site).
- **Task 2's [BLOCKING] schema push was handled as a human-action checkpoint, not auto-approved** — per this session's explicit `<checkpoint_notice>` and `CLAUDE.md` §"Self-Hosted Convex — Operational Rules," the agent surfaced the exact `npx convex deploy --yes` / `npx convex run activeEngine:latestByProfile` commands and stopped; the user ran them directly against the live self-hosted instance (`http://127.0.0.1:3210`) and confirmed an additive-only deploy (no index/table deletions) plus `activeEngine:latestByProfile` returning `[]`. A stale `CONVEX_SELF_HOSTED_ADMIN_KEY` in `.env.local` caused an initial 401 (resolved by the user updating the key from `C:\Users\mandr\convex-selfhost\admin-key.txt`) — not an agent-side issue, noted for continuity.
- **BSC-01 intentionally NOT marked complete in REQUIREMENTS.md** — this plan ships only the Convex-side reactive substrate; no UI component consumes `latestByProfile` yet (Plans 103-06/103-07's job). Matches this project's established precedent (Plan 01's own BSC-02/BSC-05 deferral, and Phases 98/99/100/101 generally) of deferring requirement completion to full end-to-end delivery, not per-plan code-completion. No `gsd-sdk requirements.mark-complete` call was made.
- **`convex/_generated/api.d.ts` committed alongside Task 3** (not Task 1) — the regeneration was produced by Task 2's live deploy, which ran between the Task 1 and Task 3 commits.
- **Executed sequentially on `master`** (worktrees disabled per `config.json`); STATE.md/ROADMAP.md updated by hand per this file's established anti-clobber workaround — no `gsd-sdk state.*`/`roadmap.update-plan-progress` verbs run this plan, consistent with Plan 01's precedent and this file's own documented history of those verbs miscounting/clobbering.

**v13.0 / Phase 103 Plan 01 decisions (2026-07-28, contract + adapter seam — Wave 1 of 5):**

- **`gateway.model.set` modeled on the real, shipped `SwapSetCommand`** (`ws_commands.py:224`), never on the dead `gateway.provider.set_enabled` (zero astridr handlers, confirmed during 103-CONTEXT.md's 2026-07-28 correction pass) — `103-CONTRACT.md` cites this explicitly so Phase 184.1 doesn't rediscover it.
- **`model_routing` is not a new telemetry event** — `router.py:426`'s `_emit_model_routing` already calls `self._telemetry.send("model_routing", payload)` on every resolution today; the contract extends its existing payload (profile scoping) rather than inventing a parallel event.
- **Catalogue read is WS (`models.catalog`), not REST** — avoids a second `authHeaders()`-based auth path; mirrors the working `swap.catalogue` precedent (`_handle_swap_catalogue`).
- **No server-side batch swap command** — a global swap dispatches N client-dispatched single-profile `gateway.model.set` commands aggregated with `Promise.allSettled`, falling out of D-12's per-row honesty requirement for free; `103-CONTRACT.md` explicitly tells Phase 184.1 not to design batch semantics in its first cut.
- **Live adapter is a pure factory (`createLiveBrainsAdapter`) taking the WS sender as an argument** — `src/lib/brainsApi.ts` has zero React import, stays fully unit-testable; a module-level `registerBrainsWsSender()` lazily wires the real `useAstridrWS().sendCommand` in at runtime so the exported `brainsApi` singleton remains directly importable everywhere (Claude's discretion, per the plan's "naming of the adapter module" latitude).
- **Rule 1 auto-fix:** a `swap.set` literal string in a `brainsApi.ts` doc comment (explaining the exclusion) failed the plan's own zero-hit acceptance gate (`grep -c 'swap.set'` must return 0). Reworded to avoid the literal substring while keeping the same explanation; re-verified 0 hits, `tsc --noEmit` still clean.
- **BSC-02/BSC-05 intentionally NOT marked complete in REQUIREMENTS.md** — this plan ships only the contract document and the adapter/fixture seam; no UI dispatches a real swap yet (waves 2-5 build that), and BSC-05's live per-profile verification is explicitly deferred to a follow-on gate (Ástríðr Phase 184.1) per `103-CONTEXT.md`'s `<blocker_reframing>`. Matches this codebase's established precedent (Phases 98/99/100/101) of deferring requirement completion to full end-to-end delivery, not per-plan code-completion. No `gsd-sdk requirements.mark-complete` call was made.
- **Executed sequentially on `master`** (worktrees disabled per `config.json`); STATE.md/ROADMAP.md updated by hand per this file's established anti-clobber workaround — `gsd-sdk state.*` calls were avoided for mutations (only the read-only `init.execute-phase` query was used at session start, and its side-effect regression of this file's frontmatter/Current-Position was reverted via `git checkout -- .planning/STATE.md` before any task commit, per the 2026-07-09 lesson).

**v11.0 / Phase 100 Plan 04 decisions (2026-07-24, final control-surface wiring — Skills.tsx + MoveToProjectDialog + integration drop tests, closes out Phase 100):**

- **`dropTargetScope` is fully independent from the category rail's `dropTarget`** — `handleDropOnCategory` and its state are byte-unchanged; `ScopeRail` is mounted as a sibling section immediately below the `CategoryGrid` block (Pitfall 5).
- **`handleDropOnScope` never enqueues on a `dialog` result** — it only calls `setScopeMoveDialog`, opening the page-level `MoveToProjectDialog`; the pending paint (`beginPending`) begins exclusively in the dialog's own `onMoved` callback (fired after a successful `enqueueLifecycle` inside the dialog's `handleConfirm`), never at drop time (D-03, Pitfall 2/4).
- **The enqueue branch calls `beginPending` synchronously before `enqueueLifecycle`**, with a `.catch()` that calls `clearPending` + `toast.error(lifecycleRefusalMessage(err))` — the only rollback path for a LAYER-1 synchronous preflight refusal, since no `forgeCommands` row is ever created for that case and the async reconcile effect (Plan 02) alone could never clear it (Pitfall 3).
- **Host resolved via the existing `resolveHostId(useForgeHostsRaw() ?? [], undefined)` export** from `SkillLifecycleMenu.tsx` — no new host-resolution mechanism invented, matching every other lifecycle call site.
- **Rule 1 auto-fix:** mounting `ScopeRail` made 5 pre-existing `Skills.test.tsx` selectors ambiguous — `ScopeRail`'s own always-visible "Cold Storage" scope entry (Plan 03's fixed 3-entry rail) collided with the pre-existing left-rail Cold-Storage nav-toggle button (both `role="button"`, both containing the text "Cold Storage"). Added `data-testid="cold-storage-nav-toggle"` to the pre-existing button and re-scoped the 5 affected tests to that testid rather than changing either component's visible copy.
- **TDD gate closed per the 100-03 precedent** for a plan where the dedicated test task (Task 3) necessarily follows its own implementation (Tasks 1-2): `handleDropOnScope` was temporarily neutered (early `return`) to confirm a genuine RED (3/3 mutation-affected cases failed with the expected assertion errors: `enqueueLifecycle` called 0 times instead of 1, dialog stub `data-open="false"` instead of `"true"`), then restored byte-identical (diffed against a pre-mutation backup — confirmed identical) and reconfirmed GREEN (27/27).
- **Phase 100 is now 5/5 plans code-complete** (100-01, 100-02, 100-03, 100-05, 100-04) — v11.0's last phase. UX-01..04 marked Complete in REQUIREMENTS.md at this phase-close, matching the LIFE-01..06/LAUNCH-01..04 precedent from Phases 98/99 (requirements marked Complete once all planned code plans land, with the phase's own manual/live-daemon verification tracked as deferred/accepted tech debt, not a blocker to marking). This plan's own `<verification>` section still lists a live-Forge-daemon drag round-trip (archive/move/restore + honest rollback) as outstanding manual verification.
- **Full suite verified green post-wiring:** 215 test files / 2515 tests passed, 0 failures; `tsc --noEmit` clean (excluding pre-existing `TS7006` errors in `src/pages/Inbox.tsx` from a concurrent unrelated session, per this plan's own scope boundary — untouched, not staged).
- **Executed sequentially on `master`** (worktrees disabled per `config.json`); STATE.md/ROADMAP.md/REQUIREMENTS.md updated by hand per this file's established anti-clobber workaround — no `gsd-sdk state.*`/`roadmap.update-plan-progress`/`requirements.mark-complete` verbs run.

**v11.0 / Phase 100 Plan 05 decisions (2026-07-24, SkillRow pending overlay + drag reporting + UX-01/UX-04 audit):**

- **Pending overlay reads `usePendingMove(skill.name)` from context, never a new prop** — every existing `SkillRow` call site (`AllSkillsOverview`/`SkillsInCategory`/`ColdStorageView`) keeps compiling unchanged; matches Plan 02's context-not-prop-drilling design.
- **Pending bar mirrors `CategoryGrid`'s active glow-bar geometry** (`absolute left-0 top-0 bottom-0 w-1`) but in `bg-[var(--status-info)]` with `animate-pulse`, never `--primary` — the D-05 honesty rule that "pending, unconfirmed" can never read as "active/selected." Distinct opacity values (dormant 50%, pending 70%) keep the two states from ever visually overlapping.
- **`onDragStart` calls `setDraggingSkill(skill)`; new `onDragEnd` calls `setDraggingSkill(null)`** — closes the wiring gap `ScopeRail` (100-03) needed to compute per-entry drop validity from `useDraggingSkill()`.
- **Task 2 audit found NO production-code gap** — `SkillRow` has always-rendered `SkillLifecycleMenu` since Phase 98, so the ⋯ menu is identical across all three row surfaces by construction; only test-coverage gaps existed (`AllSkillsOverview` had no menu-render assertion; `SkillsInCategory` had no test file at all — created new). `grep -rn manage-skills src/components/skills/` confirmed the string survives only in `ColdStorageView.test.tsx`'s preserved negative assertion, nowhere in source. QuickDeck's Run-default click gesture (Phase 99 D-02/D-03) accepted as "reconciled" with the row menu's own Run submenu, not a menu-identity requirement.
- **Logged one out-of-scope finding to `deferred-items.md`** (4 pre-existing `TS7006` errors in `src/pages/Inbox.tsx` from a concurrent unrelated session's in-progress `InboxFilterBar.tsx` edit) — left untouched per scope-boundary rule, not staged/committed.
- **Executed sequentially on `master`** (worktrees disabled per `config.json`); STATE.md/ROADMAP.md updated by hand per this file's established anti-clobber workaround — no `gsd-sdk state.*`/`roadmap.update-plan-progress` verbs run.

**v11.0 / Phase 100 Plan 03 decisions (2026-07-24, ScopeRail — 3 always-visible scope drop targets):**

- **Fixed 3-entry `SCOPE_ENTRIES` constant (global/project/cold), never gated on a nonzero count** — a user must be able to drag into an empty Cold Storage (UI-SPEC's explicit "Empty state — Scope rail" note); matches CategoryGrid's markup/hover/active/drop-target pattern verbatim, extended with a destructive invalid-drop branch CategoryGrid lacks.
- **Per-entry validity only computed for the entry currently equal to `dropTargetScope`** (`resolveScopeDrop` is called at most once per render, not once per entry) — reads `useDraggingSkill().draggingSkill` from the 100-02 `SkillControlSurfaceProvider` context; `noop` results fall through to the plain idle branch (no highlight), matching D-02's "honest, no fake feedback" no-op rule.
- **`data-scope` attribute (not a class selector) used for DOM/test addressing** — mirrors the existing `data-testid="category-nav-item"` convention on `CategoryGrid`.
- **TDD gate closed properly**: implementation was written first, then the test file was written and the implementation temporarily renamed aside to confirm a genuine RED failure (`Failed to resolve import "./ScopeRail"`) before restoring it and confirming GREEN (5/5 tests) — avoiding the fail-fast trap of a test suite that could never have failed.
- **UX-02 still NOT marked complete in REQUIREMENTS.md** — this plan ships only the presentational drop-target component; nothing in `Skills.tsx` mounts it or wires a real `enqueueLifecycle`/dialog dispatch yet (Plan 100-04's job). Matches the established per-plan-vs-full-delivery precedent (Phase 98's LIFE-01..06, Phase 99's LAUNCH-01..04, Plans 100-01/02's own UX-01/UX-03 deferrals).
- **Executed sequentially on `master`** (worktrees disabled per `config.json`); STATE.md/ROADMAP.md updated by hand per this file's established anti-clobber workaround — no `gsd-sdk state.*`/`roadmap.update-plan-progress` verbs run.

**v11.0 / Phase 100 Plan 02 decisions (2026-07-24, commandId-correlated pending map + control-surface context):**

- **`usePendingLifecycleMoves()` correlates by `commandId` via `.find()`, never by skillName alone** — ports the `useIntakeFeed.ts:95-100` dedupe precedent, made status-aware: `done` clears silently, `failed`/`expired` clear + `toast.error` (real `lifecycleRefusalMessage`-stripped reason / the reused literal `"Expired — no daemon claimed this command."` copy), `queued`/`executing` left untouched. A row with a different `commandId` for the same skillName never reconciles the entry (Pitfall 1, unit-tested).
- **`clearPending` is the LAYER-1 `.catch()` escape hatch (Pitfall 3)** — a synchronous `enqueueLifecycle` rejection never produces a `forgeCommands` row, so the reconcile effect alone could never clear that entry; Plan 04's caller must call `clearPending` directly from its `.catch()`.
- **Both tasks (pending-map hook + `SkillControlSurfaceProvider`/reader hooks) landed in one GREEN commit** — they share the identical file by design (plan explicitly keeps client-surface state colocated) and the single RED test commit already covered both tasks' acceptance criteria together; splitting would have required an artificial partial-file checkpoint with no independently-verifiable intermediate state.
- **`React.createElement` instead of JSX** in both the implementation and test files, so `usePendingLifecycleMoves.ts`/`.test.ts` could stay plain `.ts` (matching the plan's literal file list) rather than needing a `.tsx` extension solely for the Provider component and one test-harness consumer component.
- **UX-03 NOT marked complete in REQUIREMENTS.md** — this plan ships the client-only optimistic-state machine + context, but nothing enqueues a real `enqueueLifecycle` call yet (Plan 04) and no drop target exists yet (Plan 03); matches the established Phase 98/99 precedent of deferring requirement completion to full end-to-end delivery, not per-plan code-completion.
- **A concurrent session committed unrelated `useAstridrVoice.ts`/`.test.ts` changes to this same `master` branch during this plan's execution** (commits `8788630`/`932b97f`/`798639e`, phase 186-01 voice-debug instrumentation — unrelated to v11.0/Phase 100) — confirmed via `git show --stat` that neither of this plan's own commits (`0506693`, `40768d0`) touched those files; noted per the 2026-07-09 shared-branch-concurrency lesson, no action needed.
- **Executed sequentially on `master`** (worktrees disabled per `config.json`); STATE.md/ROADMAP.md updated by hand per this file's established anti-clobber workaround — no `gsd-sdk state.*`/`roadmap.update-plan-progress` verbs run.

**v11.0 / Phase 100 Plan 01 decisions (2026-07-24, shared scope predicate + drag matrix):**

- **`resolveLifecycleActions` extracted verbatim** from `SkillLifecycleMenu.tsx`'s former inline block (`src/lib/skills.ts`), and `resolveScopeDrop` added alongside it as the complete drag matrix (D-02) — both pure, unit-tested (24 new tests: 6 parity + 18 matrix cells), no React/Convex imports.
- **`resolveScopeDrop` takes an optional `lane` param** (default `"active"`, mirroring `resolveLifecycleActions`) so a future Cold Storage drag caller can pass `lane="cold"` — the only way the "dormant && shadowed" matrix cell is reachable, since `isDormant`/`isShadowing` are mutually exclusive on real origins data (same WR-04 convention `SkillLifecycleMenu.tsx` already uses for its own dormant-branch override).
- **Cold-target drops always noop regardless of shadow status** (already cold, nowhere shorter to go) — the shadow-block only gates the global/project targets.
- **Rule 1 auto-fix:** `SkillLifecycleMenu.test.tsx`'s `isShadowing` mock factory needed updating (not its assertions) after the refactor — ESM same-module calls (`resolveLifecycleActions` → `isShadowing`, both defined in `skills.ts`) are not intercepted by mocking only the module's `isShadowing` export, so the mock factory was extended to also re-derive `resolveLifecycleActions().shadowed` through the same spy. 30/30 existing tests pass with byte-identical assertions.
- Full suite verified green post-refactor: 2470 tests passed, 0 failed; `tsc --noEmit` clean.
- **Executed sequentially on `master`** (worktrees disabled per `config.json`); STATE.md updated by hand per this file's established anti-clobber workaround (see the HTML comment below the frontmatter) — no `gsd-sdk state.*` verbs run.

**v11.0 / Phase 99 (Skill Launch / Dispatch) decisions (2026-07-23, executed 6 build plans + 1 gap closure, wave-based, worktrees disabled → sequential):**

- **Three launch paths behind ONE shared layer (D-01/D-10/D-12).** `SkillLaunchProvider` hosts a single page-level `ForgeLaunchModal` + the Forge-path `recordSkillLaunch`; `useRunLaunch`/`RunTargetChooser`/`RunTargetItems` route Chat & Ástríðr targets by navigating to `/chat` with an `AutoSendHandoff` (router state) and Forge to the provider modal — so no launch logic is duplicated across the many rows/tiles that expose Run. Contracts (`src/lib/skillRun.ts`, `src/lib/profiles.ts`) were built first (Wave 1) so Waves 2–5 built against fixed types.
- **Chat auto-send is a REAL send, not a prefilled composer (LAUNCH-01/03, D-05).** `Chat.tsx` fires `chat.send` on mount from the handoff (StrictMode-guarded single-fire via `firedRef`, honest disconnect toast, router state cleared after firing). The Ástríðr variant forwards the persona `profile` onto `chat.send` (D-07/D-14a) — profile scopes `SecurityContext.profile_id` server-side only, NOT a persona-voice switch; the popover makes no "answered as X" claim (D-09).
- **HONESTY INVARIANT (D-12/D-13) was the phase's headline goal and its main risk.** Code review (99-REVIEW.md) found 2 blockers that both defeated it, both fixed in gap-closure **99-07** with TDD: (CR-01) `useAstridrChat.sendMessage` resolved even on a failed/rejected send, so `Chat.tsx` recorded a phantom launch — fixed by making `sendMessage` return `Promise<boolean>` (true only on `ack.status === "ok"`) and gating `recordSkillLaunch` on `if (sent)`; (CR-02) the Forge path recorded on the OPTIMISTIC `onLaunched` paint before the Convex mutation confirmed — fixed by adding `onLaunchConfirmed?` to `ForgeLaunchModal` (fired only after `await launch(...)` resolves) and moving the provider's record onto it. After the fix `useCount` reflects real runs only. `convex/forge.ts` Clerk fail-closed gate untouched throughout (T-99-10).
- **Dead paths retired (D-13):** copy-to-clipboard no longer records a launch anywhere (SkillRow/QuickDeck/SkillCommandPalette), and the passive `/chat?skill=` open-in-chat affordance (a confirmed no-op) was removed entirely across the shared row/container/palette components; QuickDeck primary click now opens the Run chooser (Run = default gesture, D-02/D-03), copy demoted to a secondary non-recording hover icon.
- **CR-03 deferred (pre-existing, NOT a phase-99 regression):** raw `setIsStreaming(false)` at `useAstridrChat.ts` L189 (introduced 2026-07-20, commit `1900944`) can desync `isStreamingRef` vs the sibling `run.completed` handler and block a subsequent send. Plausible but depends on live backend event ordering — per the 2026-07-20 voice-timing lesson, fixing blind risks a streaming regression; recommended follow-up is to instrument the event lifecycle and fix from a real trace. Candidate future tech-debt/quick task.
- **Executed with worktrees DISABLED** (`workflow.use_worktrees=false`) so all 7 plans ran sequentially on `master`; **STATE.md/ROADMAP.md/REQUIREMENTS.md were updated BY HAND at close** (this entry + the counters + the roadmap/requirements checkboxes), NO `gsd-sdk state.*`/`roadmap.update-plan-progress`/`phase.complete` verbs were run, per this file's established anti-clobber workaround. Verifier independently confirmed 10/10 must-haves against live source (not SUMMARY claims), full suite 2447 tests / tsc clean.

**v11.0 / Phase 98 Plan 05 decisions (2026-07-22, gap closure — stale-origin prune fix, cross-repo):**

- **Producer-declares-coverage manifest pattern** — forge's `buildSkillSnapshot` now returns `scannedOrigins: string[]` alongside `skills`, declaring exactly which origins it covered on this scan (both home roots unconditionally, plus each synced workspace whose root passed an `fs.statSync` reachability check). This lets codepulse's `computeSkillPrunes` distinguish "covered but now empty" (safe to prune) from "unscanned/unreachable" (must stay untouched) — the root cause of the UAT-found gap where the last skill moved out of a project workspace left its `claude-code:project:<key>` row forever, rendering the skill as false multi-scope and disabling Archive/Move.
- **Reachability is keyed on the workspace ROOT, not `.claude/skills`** — an empty-but-reachable workspace still contributes its origin to the manifest (zero skills, origin still declared); an unreachable root (unmounted G: drive) contributes to neither `skills` nor `scannedOrigins` — the T-98-10 transient-unmount safety valve.
- **`computeSkillPrunes`'s legacy no-arg path is byte-for-byte unchanged** — the prunable-origin set defaults to exactly `incomingByOrigin.keys()` when `scannedOrigins` is `undefined`; the manifest, when present, only ever ADDS origins to eligibility (union, never replaces), so all 5 pre-existing tests stayed green with zero test rewrites.
- **`registry.ts`'s prune guard relaxed from a single length check to an OR** (`snap.skills.length > 0 || (Array.isArray(snap.scannedOrigins) && snap.scannedOrigins.length > 0)`) at both `syncInventory` and `syncFullInventory` call sites — a snapshot that emptied an origin now reconciles, while a fully-empty, manifest-less snapshot still cannot wipe the registry (T-98-11).
- **No TDD RED/GREEN split commits** — tests and implementation were authored together per task (one commit per repo) rather than a literal separate failing-test commit; both repos' scoped suites plus `tsc --noEmit` were verified green, and codepulse's full suite (204 test files, 2331 tests) was run as an extra check since this closes out Phase 98's wave — zero regressions.
- **STATE.md working copy was found reverted to a stale "Plan 1 of 5 / executing" snapshot at session start** (frontmatter + Current Position regressed vs. the already-committed `milestone_complete`/4-4-done HEAD) — the same gsd-sdk `state.*` full-frontmatter-resync clobbering bug documented in every prior Phase 98 plan's Decisions entry below. Rebuilt this file from `git show HEAD:.planning/STATE.md` plus this plan's own edits, per the established workaround; no `gsd-sdk state.*` mutating verbs were run this plan.
- **Phase 98's two MANUAL verification steps from the 98-05 plan's own `<verification>` section (live G: repro re-check + transient-unmount negative check) are explicitly deferred** — both require a live Forge daemon and a live Google Drive mount, out of scope for an automated executor pass. LIFE-03/LIFE-04/DAEMON-02/DAEMON-03 (this plan's `requirements` frontmatter) are the specific requirement IDs this gap-closure plan targets; they were already marked Complete in REQUIREMENTS.md at Phase 98's original completion (commit `54b6278`) and remain so — this plan is a bugfix on already-complete requirements, not a new completion event.

**v11.0 / Phase 98 Plan 04 decisions (2026-07-21, lifecycle menu assembly — final integration wave):**

- **`isDormant(skill)`/`isShadowing(skill)` are mutually exclusive against the real registry data model** — `convex/skillSync.ts`'s `groupSkillRowsByName` merges every origin for a skill name into ONE row, so a row can never have ALL origins `=== DORMANT_ORIGIN` (isDormant) AND also a non-dormant origin (isShadowing) simultaneously. The shadow-disabled-Restore branch in `SkillLifecycleMenu.tsx` is implemented exactly as the plan specifies (D-09's client-side half of the two-layer shadow check) and is unit-tested by spying on `isShadowing` directly rather than an impossible origins fixture — it's correct, harmless defensive code, not naturally reachable via today's grouped-row data, and the daemon's LAYER-2 re-check (98-02) remains the real backstop.
- **Restore only ever targets `destination: "global"`** — no destination picker; matches the UI-SPEC's single "Restore" menu item and `validateLifecyclePreflight`'s global-only shadow pre-check (98-01).
- **Host resolved inside `SkillLifecycleMenu` itself** via `useForgeHostsRaw` (IntakeModal's D-08 online-newest convention), with an optional `hostId` override prop — avoids threading a new required prop through every `SkillRow` call site (`AllSkillsOverview`/`SkillsInCategory`/`ColdStorageView`).
- **`gsd-sdk state.advance-plan` and `state.update-progress` both re-clobbered this file's frontmatter** (`status: milestone_complete` → `executing` → `verifying`, `stopped_at` reverted to the Plan-03 text) via the same full-frontmatter-resync side effect documented in Plans 98-01/02/03's own decisions — reverted to `milestone_complete`/Plan-04-complete by hand each time. `state.record-metric`/`state.add-decision`/`state.record-session` were skipped entirely this plan for the same reason (all route through the same disk-rebuild resync); this Decisions entry and the Session Continuity update above were hand-written instead, following the established workaround.
- **LIFE-01..06/DAEMON-02 still NOT marked complete in REQUIREMENTS.md** — this is the final integration plan of Phase 98 (all 4 plans are now code-complete end-to-end: Convex enqueue -> daemon executor -> UI), but the phase's manual UAT (real archive + a real cross-volume move against a live Forge daemon) has not yet run. Matches 98-01/02/03's own precedent of deferring completion to full, human-verified end-to-end delivery — not per-plan code-completion.

**v11.0 / Phase 98 Plan 03 decisions (2026-07-21, lifecycle UI building blocks):**

- **`dropdown-menu` installed cleanly via `npx shadcn add dropdown-menu`** — no hand-authored fallback needed; verified the generated file imports from the already-installed `radix-ui` v1.4.3 meta-package (no new npm dependency, `package.json`/lockfile byte-unchanged).
- **`mapLifecycleStatus` delegates to `mapIntakeStatus`** (imported, not redefined) rather than duplicating the switch statement — lifecycle-domain call sites read cleanly while guaranteeing byte-identical behavior including the "unknown -> queued" defensive fallback.
- **`LifecycleCommandRow` carries `sourceOrigin` and `workspaceId`** beyond the plan's literal field list — both already exist on the Convex doc's `lifecyclePayload` (Plan 98-01 schema) and Plan 98-04's dialogs need `sourceOrigin` to construct their own `enqueueLifecycle` calls from row context; added now to avoid a second adapter pass.
- **LIFE-03/LIFE-04/LIFE-06 still NOT marked complete in REQUIREMENTS.md** — this plan ships the reusable UI building blocks (primitive, hook, 2 dialogs), all unit-tested in isolation, but nothing is wired into `SkillRow`/`ColdStorageView` yet (Plan 98-04's job). Matches 98-01/98-02's own precedent of deferring completion to full end-to-end delivery.
- **`gsd-sdk state.advance-plan` again flipped this file's top-level `status` field** (`milestone_complete` -> `executing`) via the same full-frontmatter-resync side effect documented in Plans 98-01/98-02's decisions — reverted by hand. The generic "Ready to execute" body text was also hand-rewritten to describe what actually shipped, following the same established workaround (`state.record-metric`/`state.add-decision`/`state.record-session` were not run standalone this plan for the same reason; this Decisions entry and the Session Continuity update below were hand-written instead).

**v11.0 / Phase 98 Plan 02 decisions (2026-07-21, Forge daemon lifecycle executor):**

- **`workspaceId` resolves whichever side of the mutation is the project scope** (source for archive/move-from-project, destination for restore/move-to-project) — never a second field. LIFE-03's scope (move only ever crosses global↔project, never project↔project) makes this unambiguous.
- **Delete's cold-only host-truth re-check walks every synced project workspace, not just the global root** — the plan's must_haves truth says "verifies the skill exists ONLY at the cold root", so `runLifecycle` calls `listWorkspaces(db)` and checks each workspace's `.claude/skills` dir before permitting `fs.rmSync`, in addition to the global root.
- **The per-skillName in-flight mutex (Pitfall 4) lives in `CommandPoller`, not `lifecycle-exec.ts`** — the executor stays a pure, stateless function; concurrency control is a dispatch-layer concern, matching where Phase 97's serial intake queue already lives.
- **`rescanCfg.workspaces` became a getter, not a thunk-typed interface change** — zero changes to `skill-rescan.ts`'s `RescanAndSyncDeps`/`BuildSkillSnapshotDeps`; `buildSkillSnapshot`'s existing `const { workspaces } = deps` destructuring transparently invokes the getter, closing the Pitfall-5 startup-snapshot gap entirely at the `index.ts` call site.
- **LIFE-01..06/DAEMON-02 still NOT marked complete in REQUIREMENTS.md** — this plan ships the Forge daemon executor (native fs archive/restore/move/delete, cross-volume-safe, host-truth re-checked, wired into `CommandPoller`), but the UI (Plans 98-03/04) still needs to land, and a manual UAT (real archive + real cross-volume move) is still required before end-to-end delivery is genuinely proven. Matches Plan 98-01's own precedent.
- **`gsd-sdk state.record-metric` re-clobbered the frontmatter `status` field a second time** (flipped `milestone_complete` → `verifying`, and reset `stopped_at` back to "Phase 98 Plan 01") via the same `readModifyWriteStateMd` full-frontmatter-resync side effect documented in Plan 98-01's decisions — reverted to `milestone_complete`/Plan-02-complete by hand. `state.record-metric` itself reported `recorded: false` (this STATE.md has no `## Performance Metrics` section to append to) — no metrics row was added; not worth adding a new section for. `state.add-decision`/`state.record-session` were skipped entirely this plan (same clobbering risk) — this Decisions entry and the Session Continuity update below were hand-written instead, following 98-01's established workaround.

**v11.0 / Phase 98 Plan 01 decisions (2026-07-21, Convex lifecycle substrate):**

- **LAYER-1 pre-flight only enforces global-destination collisions** — Convex has no way to resolve a `workspaceId` to its eventual `claude-code:project:<key>` origin at enqueue time (that mapping only exists after the daemon's registry rescan), so restore/move collisions against a `project` destination are deliberately left to the daemon's LAYER-2 filesystem re-check (D-04: host truth wins over a possibly-stale registry).
- **`kind="collision"` is shared between archive (cold-collision) and move (move-target-collision) refusals** in `lifecycle-refused:<kind>:` — the adapter (`synthesizeLifecycleRefusalReport`) disambiguates house copy by `payload.action`, not a second kind value; delete's cold-only refusal got its own `kind="not-cold"` with a generic UI-SPEC-fallback copy (no specific string was defined for it in the Copywriting Contract).
- **LIFE-01..06 NOT marked complete in REQUIREMENTS.md** — this plan ships only the Convex API/backend substrate (enqueueLifecycle, the pre-flight guards, the ack adapter, the list query); no daemon executes a lifecycle command yet (Plan 98-02) and no UI enqueues one yet (Plans 98-03/04), so the requirements aren't genuinely end-to-end satisfied. Matches the existing precedent that Phase 97's INTAKE-01..04 also remain "Pending" in REQUIREMENTS.md despite Phase 97 shipping live-verified — completion is tracked at full delivery, not per-plan.
- **Corrected two gsd-sdk `state.*` side effects by hand** — `state.advance-plan` set the body "Status:" line to the generic "Ready to execute" (hand-rewritten to describe what actually shipped) and a subsequent `state.update-progress` call's frontmatter resync flipped the top-level `status:` key from `milestone_complete` to `completed` (this file's frontmatter tracks the ALREADY-shipped v12.0 milestone's 7/7 plan counters, not Phase 98's; the resync recomputed status from those counters independent of which phase is actually being worked). Reverted to `milestone_complete`, matching the file's own pre-existing hand-reconciliation convention documented in the HTML comment below the frontmatter. No `state.record-metric`/`state.add-decision` calls were run this plan (both route through the same disk-rebuild resync); this decisions entry and the Current Position update above were hand-written instead.

**v12.0 / Phase 101 Plan 07 decisions (2026-07-20, gap closure):**

- **Exempt `due === undefined` from `dayFiltered` rather than add a new UI grouping** — closed the sole `101-UAT.md` gap (test 8: undated reminders vanish when a calendar day is selected). Undated rows already group under Upcoming (`groups` L493-495), so the fix is a pure filter-predicate change: `due === undefined || startOfDaySeconds(due) === selectedDay`. No Convex change, no new UI, no CSS. RED-first regression test committed before the fix (`9c246ab` test, `4afabf2` fix).
- **Phase 101 is now 7/7 plans complete** (plan 07 added post-UAT as a gap-closure plan). This closes out Phase 101 / v12.0 milestone scope.

**v12.0 / Phase 101 Plan 06 decisions (2026-07-19):**

- **Reminders page profile accents reuse `--status-ok`/`--status-warn`/`--status-info`** (not new `--profile-*` CSS vars, not `--chart-N`) — genuinely 3-hue-distinct across all 5 `data-theme` variants with zero `index.css` changes; `--chart-3/4/5` are grayscale in 3 of the 5 dark themes so couldn't guarantee distinct accents.
- **QuickAdd focuses on "N", not the literal ⌘K/N the UI-SPEC wrote** — `DashboardLayout.tsx` already owns Cmd/Ctrl+K globally for the CommandPalette; duplicating it would double-fire both handlers on the same keystroke.
- **Phase 101 is now 6/6 plans complete.** REM-03/REM-04/REM-05/CAL-01 were already `[x]` in REQUIREMENTS.md's checkbox list from plans 03-05 but the traceability table still said "Pending" for them — hand-synced to "Complete" alongside this plan's UI-01/UI-02/CAL-02 while closing out the phase.

**v11.0 scoping decisions (2026-07-17):**

- **4-phase roadmap, daemon-first** — INTAKE-01..04 + DAEMON-01/03/04 cluster into Phase 97 because they share one delivery boundary: the daemon executes intake and rescans the registry, so intake literally cannot ship live without that daemon plumbing existing first. LIFE-01..06 + DAEMON-02 cluster into Phase 98 since lifecycle mutations reuse the same command-execution + rescan mechanism Phase 97 builds — genuinely a "Phase 97 extension," not a new integration surface. LAUNCH-01..04 form Phase 99, sequenced independently since launch rides existing `chat.send`/`enqueueLaunch`/Ástríðr channels with zero daemon dependency. UX-01..04 form Phase 100, the control-surface layer wiring the ⋯ menu and drag lanes over both the lifecycle mutations (98) and the Run/launch picker (99) — necessarily last.
- **Old backlog Phase 97 renumbered to Phase 98** — its captured context (cross-repo scope, `isShadowing` caution, archive-not-delete default, daemon-offline graceful degradation) is carried forward verbatim into Phase 98's detail block in ROADMAP.md; the phase directory `.planning/phases/97-skill-lifecycle-management/` (currently empty, `.gitkeep` only) will be renamed/renumbered at plan time to match.
- **DAEMON-04 (advertise supported command types) placed in Phase 97, not split** — it's the single capability-negotiation mechanism (`supportedTypes`/`resolveClaimTypes`) that both intake (97) and lifecycle (98) commands ride; built once in 97, lifecycle command types are added to the same array as an implementation detail of Phase 98 (not a new requirement).

**v10.0 scoping decisions (2026-07-04):**

- **3-phase roadmap** — EVAL-01..03 clustered into one phase (Phase 93) since they share the `evalScores` table (ingest, judge action, and KPI/regression UI are a single coherent delivery boundary, not three separable slices). TRACE-01/02 form Phase 94 (schema gates UI, same phase since the waterfall is unusable without `traceId`). HARD-01..04 form Phase 95 (independent audit/rotation/dependency-bump work, no shared schema with 93/94).
- **95 sequenced last** — not a hard dependency, but hardening is audit/cleanup work rather than new-feature delivery; HARD-01 (`/cso`) may surface remediation scope once run, so it isn't gating the eval/trace feature work.
- **No new Ástríðr transport** — both EVAL and TRACE ride existing `/runtime-ingest`-family paths; confirmed no emitter-protocol changes needed cross-repo.

**v9.0 scoping decisions (2026-06-23):**

- **Reverse "3D out of scope"** — opt-in mode on `CodeVaultGraph` only (not a new page); `react-force-graph-3d` not R3F (near-identical prop API, manages own WebGLRenderer).
- **Phase 89 sub-sequence** — token cleanup (77 hex sites) → no-flash script → key consolidation → Aubergine tokens → WCAG-AA axe audit. `class="dark"` stays permanent (all v9.0 themes are dark variants).
- **Phase 90 cross-repo gate** — confirm `POST /api/war-room` ingest path and `warRooms` Convex population before writing any ROOM code. If Join isn't feasible, ship observer mode with honest label.
- **Phase 88 quick-unblock** — `.take()` caps deployed `edb614c` are fragile; this phase replaces them with ingest-time rollups in `convex/analyticsRollup.ts` (new file) + wired from `ingest.ts` / `runtimeIngest.ts`.

**Phase 88 Plan 01 decisions (2026-06-24, Wave 0):**

- **Classifier extracted VERBATIM** into shared `convex/lib/sankeyClassify.ts` (sole source of `categoryOf`/`outcomeOf`, read+write paths; `payload` param dropped per OQ-2). The plan's `<behavior>` examples assumed case-INSENSITIVE `outcomeOf` (`"ToolError"→"Error"`); the real code is case-SENSITIVE (`.includes("error")`), so capitalized forms classify as `"Success"`. Kept the classifier byte-identical (T-88-01) and corrected the TEST expectations instead (`tool_error`/`tool_fail`→`"Error"`).
- **Cross-plan RED-scaffold pattern** — not-yet-built Convex modules are loaded behind a non-literal `@vite-ignore` dynamic import + a loose local module type, so dependent tests RED cleanly without breaking Vite transform or `tsc --noEmit`. Used for the Plan-02 (`incrementEventBucket`/dedup) and Plan-04 (aggregates-backed queries) targets.
- **AR-01/02/03 NOT marked complete** — they are phase-level and only complete at Plan 04; Plan 01 is scaffolding + extraction only.

**Phase 88 Plan 02 decisions (2026-06-24, Wave 1):**

- **Rollup WRITE PATH landed atomically.** `events.ingest` now dedups on `by_idempotencyKey` (early return) and, on a fresh insert, increments the `"events"` + two `"sankey_edge"` buckets — all in ONE OCC mutation (D-01/D-04). Un-keyed events always counted (D-05). The 4 write-path RED tests from Plan 01 (idempotency / no-key-counted / patch-or-insert / backfill-count-equality) are now GREEN.
- **`computeHourly` event-count + error-count branches DELETED (D-02)** in the same wave that adds ingest-time increments — Convex per-deploy atomicity means co-locating them is the only way to avoid a double-count transition tick (Pitfall 1 / T-88-04). Cost read replaced unbounded `.collect()` with a paginated cursor loop (`LLM_PAGE_SIZE 500`, D-03/T-88-05, 16 MiB-safe).
- **`incrementBatch` is `internalMutation`, never public** (T-88-03 — a public increment endpoint = unauthenticated tampering). `backfillHistorical` action exists but is NOT run here (run = Plan 03, operator-gated). `idempotencyKey = body/d.idempotencyKey ?? event_id` wired through both httpActions; neither writes `ctx.db` rollups.
- **Ran `npx convex codegen` (offline, NOT a deploy)** to regenerate `_generated/api.d.ts` for the new `analyticsRollup` module; annotated `backfillHistorical`'s runQuery result as `PaginationResult<Doc<"events">>` to break a tsc TS7022 inference cycle (events↔rollup cross-import).
- **2 remaining RED tests** in `analytics.test.ts` are the Plan-04 read-path targets (`activityHeatmapFromAggregates`/`errorRateTrendFromAggregates`), documented RED-pending Plan 04 — NOT regressions from Plan 02.

**Phase 89 Plan 01 decisions (2026-06-24, Wave 0):**

- **`@axe-core/playwright` operator-approved legitimacy gate** — installed at 4.12.1 after operator confirmed npmjs.com listing (Deque Systems `@axe-core` org, dequelabs/axe-core-npm, millions weekly downloads). Required for TH-06 WCAG-AA contrast audit.
- **`resolveThemeColors` exported module-level** (not inside hook) — shared by the lazy `useState` initializer and the MutationObserver callback; avoids creating a new closure on each render.
- **`waitFor()` required for MutationObserver test** — jsdom fires MutationObserver callbacks asynchronously; `act()` alone does not flush them. Added `await waitFor(() => expect(...))` in the re-resolve test.
- **ThemeSwitcher amber option removed; readable + aubergine added** — per PATTERNS.md §ThemeSwitcher changes; trigger width widened to `w-[160px]`.
- **e2e specs seeded RED-pending** — all 3 e2e specs (contrast, no-fouc, reduced-motion) are scaffolded with `RED-pending: <plan>` comments; they correctly fail until Plans 02-04 ship token blocks, inline script, and CSS suppression rules.
- [Phase ?]: AgentCard amber shadow preserved as status identity
- [Phase ?]: CategoryGrid COLOR_HEX map EXEMPT
- [Phase 89]: Canvas legibility, aubergine grain, vault-node violet, and no-flash classified as manual-only (axe cannot audit canvas/perceptual behaviors); T-89-15 repudiation mitigated — operator sign-off received 2026-06-24 — Per 89-VALIDATION §Manual-Only Verifications; five checks approved by operator; no axe exclusions applied across all 20 WCAG-AA contrast cases
- [Phase ?]: seq optionality for backcompat
- [Phase ?]: livekit-client exact pin + audit clean

**Phase 90 Plan 03 decisions (2026-06-26, Wave 3):**

- **idle treated as closed** — `listRooms` queries both `status="closed"` and `status="idle"` via `by_status`, merges and sorts by `createdAt` desc, bounded to `closedLimit` (default 20, cap 200). Critical Note N6 / Open Question 2 resolved.
- **seq NOT a public arg** — `insertWarRoomEvent` computes `seq` server-side via OCC read-max-then-insert (mirrors `forge.ts:634-641`); clients cannot forge ordering (T-90-INJ accepted disposition confirmed).

**Phase 90 Plan 06 decisions (2026-06-26, Wave 4):**

- **Deep-link auto-select race guard** — `useEffect` fires only when `allRooms.length > 0 && !selectedRoomId`; prevents premature setSelectedRoomId before Convex resolves the room list (Pitfall 6). Dep list uses `allRooms.length` not `allRooms` to avoid re-firing on reference changes.
- **agentsRef stable-closure pattern** — `agentsRef.current = agents` kept in sync each render; WebSocket transcript event callback reads `agentsRef.current` instead of capturing `agents` in closure, avoiding resubscription on every 30 s roster poll cycle.
- **Operator identity constant "operator"** — `pid === "operator"` is the isOperatorSelf check, matching the LiveKit join identity set in Plan 04 token request body.
- **Rule 3: useRosterAgents api-namespace guard** — `(api as any).ns?.list` optional chaining prevents TypeError when partial test mocks omit namespaces (approvalQueue, agentConfigVersions, agentProfiles); production unaffected since all namespaces exist there.
- **listRooms normalization in component** — WarRoom.tsx normalizes the `useQuery` result with `Array.isArray` check to support both the real `{active,closed,hasMore}` Convex shape and the flat-array test mock, without modifying test file or Convex layer.

**Phase 90 Plan 07 decisions (2026-06-26, Wave 5):**

- **Real Join replaces cosmetic state** — `WarRoom.tsx` dropped local `isJoined/isMuted` for `useWarRoomVoice()`; room-change effect calls `voice.leave()` (fixes T-90-LEAK audio leak on room switch).
- **Closed-room read-only (D-06)** — `isRoomEnded` (status≠active OR deep-link to non-existent room) renders the "Room Ended" notice + dimmed grid + disabled Join (Surface D); `TranscriptPanel live={false}`.
- **Live-chunk dedup (D-07)** — live transcript chunks filtered against persisted events by (timestamp, speakerId); persisted events carry `seq` for ordering.

**Phase 90 LIVE INTEGRATION GAP-CLOSURE (2026-06-27..29) — the cross-repo gate that was never closed:**

The 8 build plans were all GREEN in `convex-test`/jsdom, but the feature had **never been run end-to-end against the live stack**. The "Phase 90 cross-repo gate" (confirm `POST /api/war-room` ingest + `warRooms` Convex population) was flagged in scoping but **not actually closed before execution**. Running it live surfaced five layered gaps, each now fixed + committed:

1. **LiveKit + agent workers never started** — `livekit` server and the 5 `war-room-*` workers are behind the `war-room` Docker compose profile; a bare `docker compose up` skips them, so `create_war_room` got `ClientConnectorDNSError: livekit:7880` → HTTP 500 ("launch does nothing"). Fix: `docker compose --profile war-room up -d` (livekit + workers healthy; astridr creds already `devkey`/`secret`).
2. **Phase-90 Convex functions never deployed** — `listRooms` etc. were committed but not pushed; the live deployment ran the OLD array-returning `listRooms` → "Server Error" on the page. Fix: `npx convex dev --once` (deploys to `tidy-whale-981`).
3. **astridr never populated CodePulse's `warRooms`** — `/war-room-ingest` existed on CodePulse but **nothing in astridr ever called it**, so launched rooms never appeared in the list. Fix (commit astridr `97c63643`): `create_war_room`/`close_war_room` fire-and-forget `room.created`/`room.updated` to `${CONVEX_URL}/war-room-ingest` (mirrors the existing Supabase pattern). CodePulse `upsertWarRoom` made to preserve `name`/`createdAt` on update (commit `e09ce37`).
4. **Transcripts never streamed** — agents only wrote to Supabase. Fix (astridr `26874fac`): each Norse agent mirrors its committed response to `${CONVEX_URL}/transcript-ingest` → seq-ordered `warRoomEvents` (ROOM-04). Added `CONVEX_URL`+`ASTRIDR_INGEST_API_KEY` to the `x-war-room-env` compose anchor (workers lacked them).
5. **Two CodePulse bugs found while testing** — (a) launch dialog wiped the form on every parent re-render (effect keyed on the unstable `initialParticipantIds=[]` literal; now `[open]`-only + stable `EMPTY_IDS` ref + regression test) — commit `4c3372d`; (b) no way to delete a room — added `deleteWarRoom` mutation + trash affordance + `closeWarRoom` client; `room.updated` is now patch-only (`insertIfMissing=false`) so a late close can't resurrect a deleted room — commit `1189ff5`.

**Also learned:** rebuilding the `war-room-*` workers **evicts agents from any already-open room** (agents only join at dispatch/creation; a restarted worker does not auto-rejoin) — a live room goes silent after a worker rebuild; launch a fresh room. The astridr `POST /api/war-room/{room}/token` endpoint (commit `4093aec`) is live, Bearer-enforced, malformed-name→400.

**Phase 91 Plan 02 decisions (2026-06-29, Wave 1):**

- **ForceGraph3DLib is the sole react-force-graph-3d import site** — `ForceGraph3D.tsx` is the only file allowed to import `react-force-graph-3d`, `three`, or `3d-force-graph`; the `React.lazy` boundary in `CodeVaultGraph.tsx` keeps Three.js out of the main chunk (SC#2). Verified: `grep -rl "react-force-graph-3d" src` returns only this file.
- **cooldownTicks=150** — the library default is `Infinity`; a finite value is required so `onEngineStop` fires and `zoomToFit(400, 60)` runs on simulation settle (Pitfall 3). Value 150 matches ForceGraphCanvas's 120 ticks with a small buffer for 3D physics.
- **centerNode3DWhenReady appended without touching 2D path** — `centerNodeWhenReady` (L30-61) is byte-unchanged; the 3D function mirrors its RAF/cancel/frames/maxFrames structure exactly. `lookAt` passed as explicit node coords to prevent camera aiming at scene origin for off-center nodes (Pitfall 6).
- [Phase ?]: SC#2 PASS: three.js/react-force-graph-3d confined to ForceGraph3D-[hash].js lazy chunk; zero three.js markers in any of the 3 main index chunks
- [Phase ?]: 93-06: persona identity for Quality-page join = operational profile id stamped on session.active_profile, not shared persona_id
- [Phase ?]: 93-06: SELF-01 was never wired into AgentLoop in prod (dead since Phase 73); bootstrap now wires it, enable flag reverted post-verification (config/self-improvement.yaml)
- [Phase ?]: 93-06: Quality trends NOT trusted until E3 >=0.7 human-agreement gate closes (93-CALIBRATION.md, labels pending)
- [Phase ?]: 93-06 user-directed: astridr web chat cookie-session auth; profile-scoped /{p}/api/* auth bypass closed
- [Phase 95]: HARD-03: TS6 migration resolved via single tsconfig types:["node"] fix (Option A), not per-file globalThis casts; the four D-10 folded majors (diff@8, js-yaml@5, jsdom@29, react-easy-crop@6) were already merged to master and verified green under the new bar
- [Phase 95]: HARD-04: react-day-picker resolved by deleting the dead calendar.tsx primitive (zero consumers), not a 9->10 migration
- [Phase 101-01]: reminders is the Convex source of truth (D-01); complete() spawns the next open occurrence for recurring reminders via computeNextDueAt (D-05); one-offs and past-until recurrences spawn nothing — Locked by 101-CONTEXT.md D-01/D-05/D-09; source field records origin only and never gates writes
- [Phase 101-01]: recurrence.byday uses iCal 2-letter day codes (MO/TU/WE/TH/FR/SA/SU); Convex CRUD handlers extracted as plain *Handler(ctx,...) functions typed 'ctx: {db} | any' for unit-testing without convex-test — byday format was unspecified in the plan interface; handler pattern mirrors the existing evalScores.ts storeEvalScoreHandler precedent to sidestep QueryCtx/MutationCtx structural typing mismatch
- [Phase 101]: Tests invoke real httpAction handlers via Convex's _handler escape hatch (raw handler fn) instead of a hand-duplicated dispatch simulation, closing the test/implementation drift gap the codebase's forgeIngest.test.ts precedent left open
- [Phase 101]: calendarIngest requires calendarAccount (not just profileId+events) as a 400-gated field, since D-10's scoped prune is keyed on (profileId, calendarAccount)
- [Phase 101-03]: RemindersTool (astridr-repo) registers via the manifest TOML system (astridr/tools/manifests/reminders.manifest.toml, tool_id=="reminders"==RemindersTool.name), not config/tools.yaml's builtin/optional lists — confirmed that YAML section has zero consumers in the codebase; snooze is implemented as an op:"update" dueAt shift (not a true status:"snoozed" transition) because /reminders-ingest has no op:"snooze" and drops status/snoozedUntil on op:"update" — follow-up needed in convex/remindersIngest.ts to wire the existing api.reminders.snooze mutation
- [Phase 101-04]: astridr calendar cron registers via the real scheduler (cron_builders.py + cron_dispatcher.py), not jobs.py — jobs.py is JobManager (execution-tracking only), no periodic-job-registration surface exists there; bounded 60-day forward window achieved by post-filtering normalized events since GoogleWorkspaceTool.list_events has no time_min/time_max param — same file-scope correction likely applies to 101-05's coordination note
- [Phase 101-05]: reminder_nudge cron registers via cron_builders.py/cron_dispatcher.py (real scheduler), not jobs.py -- same file-scope correction as 101-04's calendar_cache precedent
- [Phase 102]: Phase 102 Plan 01: self-hosted Convex codegen needed a freshly-generated admin key (docker exec convex-backend ./generate_admin_key.sh) passed as inline shell env vars for a single command, since the stored local admin key was stale and secret config files cannot be read or written per project security policy
- [Phase 102-02]: Deleted dead CodePulsePoster class + orphaned constants from calendar_cache.py, switched cron_dispatcher.py's _run_calendar_cache_refresh to pass self._telemetry (shared ConvexHandler), and swept the stale two-backend/404/transitional narrative out of tools/reminders.py's comment block to state the current single-local-backend truth. Live cron-tick proof deferred to plan 102-03. AUDIT-TD-02.
- [Phase 103]: 103-18: hoisted GlobalSwapModal ownership into a new GlobalSwapContext (GlobalSwapProvider/useGlobalSwap), mounted once in DashboardLayout above the router outlet, closing WR-01 (revert toast firing into an unmounted page-scoped picker).
- [Phase 107]: Wave-0 shard tests (107-01) follow D-01: shard is an explicit test param, no Math.random() dependency in the test file; production draws it once per ingest (107-03)
- [Phase 107]: 107-01 encodes D-04's strict-equality shard match (r.shard === shard, not (r.shard ?? 0) === shard) directly in the legacy-unsharded-row test — a pre-existing row is retired, never patched, by a sharded write
- [Phase 107]: 107-02: left the two pre-existing false-green rollupDaily/eventCountsByPeriod tests untouched, superseded by new _handler-based tests
- [Phase 107]: 107-02: all 7 new multi-shard read-path regression tests went GREEN on first run with zero production changes, confirming CONTEXT.md's no-read-side-change premise before 107-03 lands the sharded write path
- [Phase 107]: Threaded shard through incrementBatch (analyticsRollup.ts), a 4th write call site the plan itself flagged as unnamed in any planning doc — One pickShard() draw per event inside the batch loop, shared across that event's two helper calls, mirroring events.ingest's one-draw-per-call rule
- [Phase 107]: Reworded 3 doc comments in analyticsRollup.ts that tripped their own Task-2 acceptance-criteria greps (literal r.shard === shard / r.shard ?? 0 / pickShard text inside prose comments) — Same comment-trips-own-grep class Phase 105 plans hit 5 separate times; reworded to describe behavior without repeating the literal grepped pattern, re-verified all counts
- [Phase 107]: 107-04: Split the OCC baseline artifact across two task commits (Section A, then Sections B/C) matching the plan's task boundary.
- [Phase 107]: 107-04: events:listRecent {limit:5000} exceeds the self-hosted instance's 16MB single-execution read cap and errors; limit:1000 works and satisfies the coverage guard - plan 107-06 must reuse limit:1000.
- [Phase 108]: 108-01: wrapped every existing chat()-driven telemetry test in a profile_context() helper and rewrote test_model_routing_default into test_model_routing_default_refuses_to_emit — D-02's refuse-to-emit guard makes the plan's literal 4-line-rename instruction incomplete for the default rung, and breaks all pre-existing telemetry tests unless they set a profile in context
- [Phase 108]: 108-02: reworded controlVerbSwaps.ts's listByScope doc-comment (literal ".collect()" text in prose) after it collided with its own Task 3 acceptance-criteria grep — same "comment trips its own grep" defect class Phase 105/107 hit repeatedly; reworded to "unbounded collect" without changing meaning
- [Phase 108]: 108-04: swap_model.py's _execute left unmodified this plan (still unconditionally calls set_global_override/clear_global_override regardless of args["profile_id"]) — wiring it to branch on the scope and call set_profile_override/clear_profile_override instead is explicitly plan 108-05's job (D-07); confirmed by reading 108-05-PLAN.md before editing, which lists swap_model.py in its own files_modified
- [Phase 108]: 108-04: gsd-sdk state.advance-plan and state.add-decision both re-clobbered stopped_at/last_activity back to the stale 108-02 narrative on this plan's own STATE.md update (same documented narrative-clobber defect) — restored via `git show HEAD:.planning/STATE.md` each time and re-applied the counter/narrative edits by hand in the same pass as the decision line, rather than trusting either verb's frontmatter sync
- [Phase 108]: 108-03: model_routing's status:"failed" skip trimmed to a 4-line comment after a first draft's longer comment pushed the stripped-source offset to activeEngine.test.ts's pre-existing isUnresolvedRouting( regex past its fixed 900-char window, which would have broken that unrelated test; verified via a throwaway Node script replicating the test's own strip+slice+regex logic before committing
- [Phase 108]: 108-03: control_verb_swap's own D-13 rationale comment contains the literal substring "isUnresolvedRouting" in prose, so the regression test asserting the case never CALLS it must comment-strip the source first (`stripCommentLinesForIngestTests`, same convention as activeEngine.test.ts/controlVerbSwaps.test.ts) — a raw non-stripped assertion would have failed on the plan's own required comment, not a real guard call
- [Phase 108]: STATE.md hand-edited per this file's established anti-clobber workaround (gsd-sdk state.* verbs not run) — completed_plans 3->4
- [Phase 108]: 108-05: found this file's own frontmatter `stopped_at` still read "Completed 108-03-PLAN.md" despite the Current Position section below it already saying "Plan 108-04 complete" (108-04's own STATE.md update evidently didn't reach the frontmatter, or a concurrent session's gsd-sdk clobber reverted just that field) — corrected both to agree in this plan's edit, per the Stale Docs rule
- [Phase 108]: 108-05: ENGINE-02 left Pending in REQUIREMENTS.md despite its literal text now being true in the code and proven live in-process (scoped swap.set genuinely applies only the profile override; unscoped stays byte-identical) — per this plan's explicit instruction to follow 108-04's precedent of not marking a requirement complete before ENGINE-05's live-stack gate (108-07) runs; REQUIREMENTS.md's ENGINE-02 row gets a prose note recording the code-complete state, not a checkbox flip
- [Phase 108]: STATE.md hand-edited per this file's established anti-clobber workaround (gsd-sdk state.* verbs not run) — completed_plans 4->5
- [Phase 108]: 108-07 Task 4 (2026-08-07, continuation agent): Larry reviewed the full evidence in `108-ENGINE-05-EVIDENCE.md` (three live-proof rounds + the cleanup section) and replied "approved" — ENGINE-05, ENGINE-01, and ENGINE-02 all marked Complete in REQUIREMENTS.md's checklist and traceability table in the same commit. He explicitly approved marking ENGINE-01/ENGINE-02 on the same evidence as ENGINE-05, since both were already mapped to Phase 108 and are now proven live, not merely code-complete. TELE-02 verified unchanged (Phase 109/Pending) after the edit, per its own 2026-08-07 reassignment. `phase.complete` deliberately NOT run — orchestrator-owned per this task's explicit instruction.
- [Phase 108]: STATE.md hand-edited per this file's established anti-clobber workaround (gsd-sdk state.* verbs not run) — completed_plans 6->7
- [Phase 109]: D-05 profile_overrides on swap.state: empty dict (never omitted) when router is None or no overrides; cleared/restored profile absent-not-null
- [Phase 109]: D-03 default_profile_id lands on swap.catalogue brain-target ack only, threaded via a new CommandDispatcher constructor param fed from bootstrap/core.py's existing config.profiles[0].id computation
- [Phase 109]: D-01/D-02 (109-03): the D-16 stub seam is deleted in full (adapter, fixtures, flag, registrar, e2e spec) — one useBrainCatalogue() hook replaces it; both picker scopes share the same catalogue, so scope toggling no longer re-fetches
- [Phase 109]: D-03 (109-03): default_profile_id (via useBrainCatalogue) replaces the profiles[0]?.profileId Convex-ordering fallback on BrainHeaderBadge and Chat's composer pill — an unresolved default addresses no profile, never a guessed one
- [Phase 109]: STATE.md hand-edited per this file's established anti-clobber workaround (gsd-sdk state.advance-plan clobbered stopped_at/Status/Last-activity back to the 109-01 narrative — reverted from git HEAD, completed_plans 9->10 applied by hand)
- [Phase 109]: ENGINE-03/ENGINE-04 left Pending in REQUIREMENTS.md despite this plan's frontmatter listing them — 109-CONTEXT.md's own "Claude's Discretion" section explicitly instructs "should not mark ENGINE-03/ENGINE-04 satisfied from tests alone", requiring an operator-attended live gate (mirroring ENGINE-05's Phase 108 precedent) before either flips; this plan only delivers the dispatch mechanism (Wave 2 of 7), not the full requirement (precedence fix in 109-04, the 5-state confirm machine in 109-06, and the live gate all remain)
- [Phase 109]: D-06/D-07 (109-04): resolveActiveBrain gains a top override rung (a live per-profile pin wins even under a different active global override) and drops the scoped lastTurn fallback (a scoped read with no telemetry/override now returns "none", never another profile's model) — mirrors Ástríðr's own _resolve_model chain, correcting 103-CONTRACT.md §9's originally-inverted description
- [Phase 109]: UI-SPEC §A/§B (109-04): one canonical "Not reported" absent string replaces three disagreeing ones (badge/pill/picker's two independent "Auto" literals); the override rung renders through the exact same JSX branch as a telemetry-sourced pinned reading — invisible to the operator by design
- [Phase 109]: ENGINE-03/ENGINE-04 still left Pending in REQUIREMENTS.md after this plan (109-04) — the precedence fix and honest-absent-state work are code-complete and unit-tested, but the operator-attended live gate (109-CONTEXT.md's Claude's Discretion item, mirroring ENGINE-05) has not run yet; unchanged from 109-03's same note
- [Phase 109]: STATE.md hand-edited per this file's established anti-clobber workaround (gsd-sdk state.* verbs not run) — completed_plans 10->11
- [Phase 109-05]: modelIdsMatch(a,b) = a===b || stripVendorPrefix(a)===stripVendorPrefix(b), exported alongside stripVendorPrefix; used at all 7 model-id equality sites instead of raw ===
- [Phase 109-05]: deriveMixedState folds with modelIdsMatch but keeps the first-seen literal id as the distinctModels representative, never a canonicalized synthetic value
- [Phase 109]: useProfileSwap confirms via profileOverrides[profileId] (swap.state override slot) with a raw === comparison, matching GlobalSwapModal's own override-slot readback precedent -- not a D-08 modelIdsMatch site, since the override slot echoes back exactly what was dispatched.
- [Phase 109]: onPendingChange widened from string|null to {label,kind:inflight|uncertain}|null so the accepted (bounded-timeout) state can render distinctly from in-progress on every mirrored per-profile brain surface, per 109-UI-SPEC.md section C.
- [Phase 109]: D-13 implemented: anthropic maps to anthropic_direct; every other non-empty vendor is the OpenRouter catch-all; unclassified detection is by vendor emptiness, never getBillingType's api fallback
- [Phase 109]: shouldConfirmCost hoisted into one formula in BrainPicker.tsx, consumed identically by the row-render loop and handleActivate; needsCostConfirm stays the pure scope-independent predicate
- [Phase 109]: Filter brain swaps before merging (not after) so totalCount never counts voice rows the list won't render
- [Phase 109]: SwapHistoryList drops the old 'Recent swaps' label since the outer Collapsible trigger now carries the section heading
- [Phase 109]: Badge and disclosure count each call useCombinedSwapHistory independently rather than prop-threading, relying on Convex's client-side query dedup
- [Phase 111]: Preserved the ref < 1e12 epoch-seconds guard verbatim in JobsPanel's formatElapsed; both mutation controls (day-tier divisor, guard deletion) proved it load-bearing
- [Phase 111]: Rewrote JobsPanel's docstring and formatElapsed comment in place (Stale Docs rule) instead of leaving them describe the live-streaming behavior the D-08/D-09/D-10 rewrite removes

### Pending Todos

- **v11.0 next action:** Phase 100 (Control-Surface UX) is now 4/5 plans complete (100-01, 100-02, 100-03, 100-05 done 2026-07-24). Next: execute Plan 100-04 (Skills.tsx integration, Wave 3 — depends on 100-01/02/03; also benefits from 100-05's SkillRow pending overlay + drag reporting). It's the LAST plan of the LAST v11.0 phase.
- **Phase 99 follow-up (non-blocking):** CR-03 pre-existing `isStreamingRef` desync (`useAstridrChat.ts` L189) — needs a live event-lifecycle trace before fixing (do NOT blind-fix; see 2026-07-20 voice-timing lesson). Candidate quick task / tech-debt.
- **Phase 98 follow-up (non-blocking):** its two MANUAL verification steps (live G: workspace repro re-check + transient-unmount negative check) remain outstanding — both require a live Forge daemon + live Google Drive mount.
- **v10.0 (shipped, for reference):** ✅ COMPLETE — all 4 phases (93-96) done, milestone archived 2026-07-13.
- **Operational note:** the `war-room` Docker profile (livekit + 5 workers) must be running for War Room to work — `docker compose --profile war-room up -d`. Rebuilding workers evicts agents from open rooms.
- **Archive-name collision: ✅ RESOLVED/MOOT (2026-06-29)** — verified the feared stale `milestones/v9.0-*.md`/`v10/v11` adversarial-track archives are NOT present in this repo (milestones/ holds only v4/5/7/8; git never tracked milestones/v9.0-*). The only v9.0 file is CodePulse's own `.planning/v9.0-MILESTONE-AUDIT.md`. No rename needed.

### Blockers/Concerns

- None currently blocking v11.0. Phase 98's 98-05 gap closure landed 2026-07-22; its own manual verification steps (live G: repro + transient-unmount negative check) are outstanding but non-blocking for proceeding to Phase 99.
- Cross-repo handoff carried over from v10.0 Phase 96 (out of CodePulse scope, does not block v11.0): astridr-repo `chat.send` security-pipeline bypass + missing approval-block producer, routed to astridr Phase 178.1 (code-complete, pending live UAT as of 2026-07-13).

### Quick Tasks Completed

| # | Description | Date | Commit | Directory |
|---|-------------|------|--------|-----------|
| 260713-q9k | ApprovalBlock update-by-id in Chat — resolution status-update blocks flip the existing card in place (D-05 handoff from astridr 178.1) | 2026-07-13 | 307b90a | [260713-q9k-approvalblock-update-by-id-in-chat-updat](./quick/260713-q9k-approvalblock-update-by-id-in-chat-updat/) |
| 260629-nnf | Complete Graphs Hub tile index — Capabilities, 3D Memory Galaxy, Hive/Swarm tiles | 2026-06-29 | 2d9df13 | [260629-nnf-graphs-hub-tile-index](./quick-archive/260629-nnf-graphs-hub-tile-index/) |
| 260629-oki | Reverse cross-graph deep-links (agent→tools, KG entity→owning agent) — GH-04 round-trip | 2026-06-29 | 6cffbae | [260629-oki-reverse-cross-graph-links](./quick-archive/260629-oki-reverse-cross-graph-links/) |
| 260629-ow5 | Memory ?event= deep-link focus — close the KG-provenance cross-nav target | 2026-06-29 | 58b999f | [260629-mem-event-deeplink](./quick-archive/260629-mem-event-deeplink/) |
| 260629-pcy | Hive swarm-task → agent cross-graph deep-link — Hive joins the cross-nav web | 2026-06-29 | b7b8e84 | [260629-hive-task-agent-link](./quick-archive/260629-hive-task-agent-link/) |
| 260629-qaj | Close out cross-nav — back-chip labels (Hive/Memory) + inbound agent→Hive (?goal=) | 2026-06-29 | b0253b3 | [260629-close-crossnav](./quick-archive/260629-close-crossnav/) |
| Phase 102 P01 | 6min | 2 tasks | 4 files |
| Phase 102 P02 | 25min | 2 tasks | 4 files |
| Phase 103 P18 | 55min | 2 tasks | 6 files |
| Phase 105 P01 | 40min | 3 tasks | 10 files |
| Phase 107 P01 | 12min | 3 tasks | 2 files |
| Phase 107 P02 | 9min | 2 tasks | 2 files |
| Phase 107 P03 | 18min | 3 tasks | 4 files |
| Phase 107 P04 | 20min | 2 tasks | 1 files |
| Phase 108 P01 | 25min | 3 tasks | 5 files |
| Phase 108 P02 | 10min | 3 tasks | 4 files |
| Phase 108 P03 | 35min | 2 tasks | 3 files |
| Phase 109 P01 | 17min | 3 tasks | 6 files |
| Phase 109 P03 | 40min | 3 tasks | 23 files |
| Phase 109 P05 | 15min | 3 tasks | 14 files |
| Phase 109 P06 | 55min | 3 tasks | 10 files |
| Phase 109 P07 | 12min | 3 tasks | 7 files |
| Phase 109 P08 | 10min | 3 tasks | 8 files |
| Phase 111 P01 | 34min | 3 tasks | 3 files |
| Phase 112 P08 | 20min | 3 tasks | 4 files |

## Session Continuity

Last session: 2026-08-12T16:05:00.000Z
Stopped at: Phase 112 plan 112-08 complete (gap-closure: normalized tool_executed.round's explicit-null wire shape, D-14, mutation-proven RED/GREEN; noFallthroughCasesInSwitch enabled + proven via a discriminating scratchpad control in both tsconfigs); only 112-07 remains (autonomous:false, operator-gated deploy + live proof) — Phase 112 now 7/8 plans. Phase 115 (workspace-scanner) EXECUTING in a parallel session as of its last report — waves 1-4 (plans 115-01..115-08) dispatched; waves 5-6 operator-gated, not yet complete.

--- Prior (superseded by the above) --- Phase 112 plan 112-06 complete (GROUP_B_DISPOSITIONS disposition record + three-layer drift guard, TELE-03, D-10/D-11), mutation-proven against 3 distinct drift classes; 1 build plan remains (112-08, gap-closure, wave 4) plus 112-07 (autonomous:false, operator-gated deploy) — Phase 112 now 6/8 plans. Phase 115 (workspace-scanner) EXECUTING in a parallel session as of its last report — waves 1-4 (plans 115-01..115-08) dispatched; waves 5-6 operator-gated, not yet complete.

--- Prior (superseded by the above) --- Phase 115 (workspace-scanner) EXECUTING — waves 1-4 (plans 115-01..115-08) dispatched; waves 5-6 (115-09 deploy+dry-run review, 115-10 elevated scheduled-task install) deliberately deferred, operator-gated. Phase 112 still open at plan 112-05 complete; 2 plans remain (112-06, 112-07)

--- Prior (superseded by the above) --- Completed 112-04-PLAN.md (governor_decision + message_routed routed into runtimeIngest dispatch, D-04/D-05/D-13/D-14, held_reason null-normalization mutation-proven, 14 new tests, TELE-03)

--- Prior (superseded by the above) --- Completed 112-03-PLAN.md (governorDecisions.ts + messageRoutes.ts internalMutation record + capped listRecent, CR-01 mutation-proven, api.d.ts regenerated, TELE-03, commits 4649ec93/dd64983d/947908fa/35e4493c)

--- Prior (superseded by the above) --- Completed 112-02-PLAN.md (governorDecisions + messageRoutes schema tables, RETENTION_DAYS bounds, TELE-03, commits 65a4870e/bb3b4099/4314916a)

--- Prior (superseded by the above) --- Completed 111-01-PLAN.md (JobsPanel mission history rewrite)

--- Prior (superseded by the above) --- Completed 109-01-PLAN.md (Astridr D-05 profile_overrides + D-03 default_profile_id, feature/brain-swap commits 10503e4f/effb7a48/411e0253/8c4842f1)

--- Prior (superseded by the above; re-added by hand 2026-08-07 — `state.record-session` replaced this narrative without demoting it to a Prior entry, per the established anti-clobber convention in this file) --- Completed 108-07-PLAN.md Task 4 (operator sign-off, 2026-08-07). Larry reviewed all three live-proof rounds plus the cleanup section in 108-ENGINE-05-EVIDENCE.md and replied "approved". ENGINE-05, ENGINE-01, ENGINE-02 marked Complete in REQUIREMENTS.md. Phase 108 is 7/7 plans complete. `phase.complete` deliberately NOT run that session — orchestrator-owned. The three carry-forward items it named (TELE-02's surfaced half, the model-id format split, `listByScope`'s signature decision) are now resolved as Phase 109's D-05/D-08/D-11.

--- Prior (superseded) --- `/gsd:plan-phase 107` to break down the sharding fix into tasks. Root cause: convex/analyticsRollup.ts's incrementEventBucket/incrementSankeyEdge do a read-patch-or-insert on ONE shared aggregates row per (metric_type, period, bucket_start, dimensions) tuple, causing sustained OCC retries under concurrent Astridr ingest (1135+ in 24h per 2026-08-05 diagnosis) that built up MVCC memory pressure until events-table index-head queries timed out — 2nd occurrence of this mechanism (also 2026-07-30), each time only cleared by a container recreate (done again 2026-08-05), never root-caused until now. See 107-CONTEXT.md for the full locked decisions (8-way random sharding on events/sankey_edge only, no bulk migration of existing rows, live OCC-log-count verification). **Phase 107 shipped with v13.0 (2026-08-06) — this line was stale and was left in place by `state.record-session`, which updates only `Last session`/`Stopped at`/`Resume file` and never the `Next action` line.**

--- Prior (superseded) --- Completed 108-03-PLAN.md (control_verb_swap ingest case routed to internal.controlVerbSwaps.record, D-13 no-refusal-guard preserved; model_routing status:"failed" skip closing ENGINE-01's fabricated-current-engine gap; 14 new tests, 3 mutation-checked); phases 108-04 through 108-07 (including Task 4 sign-off) have since completed, see the current Stopped-at line above.

--- Prior (superseded by the above) --- Phase 106 Plan 06 complete (skill-lifecycle UAT session A, DEBT-04 Session-A scope closed, 4/4 live tests pass, zero uat106-* residue). Plan 106-03 remains blocked NO-GO (per Plan 01's cloud-Convex sweep, independent of Plan 06) until Larry resolves the manual `.env*` checks or the CI-workflow repointing is folded into 106-03's scope. Plans 106-07 and 106-08 continue extending `106-HUMAN-UAT.md` (status still in-progress). See `106-06-SUMMARY.md` for the full DEBT-04 Session-A closure record.

--- Prior (superseded by the above) --- Execute Plan 105-02 (ingest prerequisites for D-01's Ástríðr tool volume) — depends on this plan's bounded-read guard now being in place. See `105-01-SUMMARY.md` for the full deviation record (read-direction fix, mutation-verification log) and two carried-forward items: `avgDuration`'s zero-consumer status (Phase-106 dead-code candidate) and `convex/ingest.ts:168`'s missing `provider` field on `PostToolUseFailure` (F6, deliberately unfixed — it's the baseline the D-15 control test depends on). [Phase 105 is now fully COMPLETE as of 105-09, 2026-08-04 — this line is retained as historical context only.]

--- Prior (Phase 103, superseded by the above) --- Phase 103's gap-closure cycle is 18/18 plans CODE-complete (103-18 closed WR-01, 2026-07-29). Next step is an operator-attended LIVE re-verification against the running Ástríðr stack covering 103-16 (reselect gets a fresh confirm prompt), 103-17 (pinned-default count reads config), and 103-18 (revert survives navigating away from the Chat composer pill) — a green unit suite is explicitly not accepted as proof per this cycle's established pattern (see 103-VERIFICATION.md). Only after that should BSC-01..05 be re-marked complete in REQUIREMENTS.md and the phase checkbox flipped in ROADMAP.md.

--- Historical (v11.0, closed 2026-07-25) --- Execute Plan 100-04 (Skills.tsx integration — Wave 3, depends on 100-01/02/03). This was the LAST remaining plan in Phase 100 (and v11.0). Plan 100-05 shipped `SkillRow`'s optimistic-pending visual treatment (opacity-70 + pulsing `--status-info` bar, read from the 100-02 `SkillControlSurfaceProvider` context via `usePendingMove`) plus `onDragStart`/`onDragEnd` reporting the dragged skill via `setDraggingSkill` — both pieces 100-04 needs to wire a real drag-drop lifecycle mutation with honest optimistic UI against `ScopeRail` (100-03). Plan 100-05's Task 2 audit also confirmed (not assumed) that the ⋯ `SkillLifecycleMenu` already renders consistently across `AllSkillsOverview`/`SkillsInCategory`/`ColdStorageView` and that no `/manage-skills` string survives anywhere in source — closing UX-01/UX-04's completeness-audit scope with zero production-code changes needed. Plan 100-03 shipped `ScopeRail` (`src/components/skills/ScopeRail.tsx`) — 3 always-visible Global/Project/Cold Storage native HTML5 drop targets mirroring `CategoryGrid`'s markup/highlight pattern verbatim, per-entry valid/invalid/idle states computed from `useDraggingSkill()` (100-02) + `resolveScopeDrop()` (100-01), inline destructive reject hint, presentational + event-forwarding only (props: `dropTargetScope`/`onDragOverScope`/`onDragLeaveScope`/`onDropOnScope`) — Plan 100-04 must import and mount this component directly beneath `CategoryGrid` and wire its callback props to real state + `enqueueLifecycle`/dialog dispatch, no duplication. Locked decisions carried forward from 100-01/02/03: droppable scope rail beneath Categories (D-01); drag matrix mirrors the ⋯ menu exactly via the shared `resolveLifecycleActions`/`resolveScopeDrop` predicate (D-02); drop-on-Project opens MoveToProjectDialog picker (D-03); drag never deletes (D-04); optimistic move + honest rollback reconciled against the lifecycle command-row status (D-05, implemented by 100-02, visualized by 100-05); UX-01/UX-04 treated as completeness+verification, not net-new (D-06, closed by 100-05). **Codepulse-only, frontend-focused — no new daemon/Convex mutation expected** (rides 98's `enqueueLifecycle` + 99's launch). UX-02/UX-01/UX-03/UX-04 requirements NOT yet marked complete in REQUIREMENTS.md — deferred to full end-to-end delivery at Plan 100-04's completion, matching this project's established per-plan-vs-full-delivery precedent (e.g. Phase 98's LIFE-01..06). Non-blocking carryovers: (1) Phase 99 CR-03 `isStreamingRef` desync (`useAstridrChat.ts` L189) — needs a live trace before fixing (99-REVIEW.md); (2) Phase 98's two MANUAL verification steps (live G: repro + transient-unmount — need a live Forge daemon + Google Drive mount); (3) `102-REVIEW.md`'s 4 advisory warnings; (4) a concurrent unrelated session is mid-edit on `src/pages/Inbox.tsx`/`src/components/InboxFilterBar.tsx` (introduces 4 pre-existing TS7006 errors, logged to Phase 100's `deferred-items.md`, not touched by this plan) — noted per the shared-branch-concurrency lesson.
Resume file: .planning/phases/112-telemetry-coverage-closure/112-UI-SPEC.md

## Operator Next Steps

- Review the v11.0 roadmap draft in `.planning/ROADMAP.md` (Phases 97-100) and `.planning/REQUIREMENTS.md` traceability.
- Once approved, proceed with `/gsd-discuss-phase 99` to begin the discuss → ui-spec → plan → execute chain for Phase 99 (Skill Launch / Dispatch) — independent of 97/98, can run any time.
- Optional, whenever a live Forge daemon + Google Drive mount are convenient: run the two manual verification steps from 98-05's plan to fully close out Phase 98's live UAT.
