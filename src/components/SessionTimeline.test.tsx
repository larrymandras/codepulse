import { describe, it } from "vitest";

describe("SessionTimeline", () => {
  describe("provider badges (GW-14)", () => {
    it.todo("renders a provider badge when toolExecution has provider set");
    it.todo("uses PROVIDER_COLORS for badge border and text color");
    it.todo("renders no badge when toolExecutions prop is empty");
    it.todo("falls back gracefully when provider is unknown");
  });
});
