import { describe, it } from "vitest";

describe("LeaderboardTable", () => {
  it.todo("renders ranked rows sorted by composite score descending");
  it.todo("displays rank badge with green tint for rank 1");
  it.todo("shows agent name, score, completion%, avg response time, cost efficiency columns");
  it.todo("calls onRowClick with agentId when row is clicked");
  it.todo("filters to team agents when agentIds provided (ANLY-04)");
  it.todo("renders empty state when no rows");
});
