node_modules/
dist/
# Secrets: ignore all env files except the committed template (CSO-95-03)
.env
.env.*
!.env.example
.worktrees/
graphify-out/

# Claude Code
.claude/
.claire/
.last-update-check

# Tooling artifacts (ephemeral — brainstorm sessions, temp dumps)
.superpowers/
tmp_skills.json

# Test artifacts
playwright-report/
test-results/

# Design artifacts
open-design/

# Raw security-audit output (curated results live in .planning/phases/*/95-SECURITY-AUDIT.md)
.audits/
.vercel

# Phase 115 workspace scanner: the local root list holds Larry's project
# directory names (several read as client engagements) and this repo is PUBLIC —
# committing them is a one-way disclosure (D-17). The dry-run report and its
# approval marker are machine-local derived state (D-12), regenerated per run.
config/workspace.local.json
config/workspace-scan-report.json
config/workspace-scan.approved.sha256

# Nightly /gsd-progress --forensic snapshot (scripts/gsd-progress-snapshot.ps1,
# scheduled task "GSD-Progress-Snapshot", 2026-07-31) — regenerated every run,
# git history of it would just be daily noise commits. Local-read-only by design.
docs/PROGRESS-SNAPSHOT.md
docs/PROGRESS-DIFF.md
docs/gsd-progress-snapshot.log

# GSD overnight execution supervisor (scripts/gsd-overnight-supervisor.ps1,
# 2026-07-31) — loop-state checkpoint, run log, and generated report, all
# regenerated per run.
docs/gsd-overnight-checkpoint-*.json
docs/gsd-overnight-report-*.md
docs/gsd-overnight-supervisor.log
